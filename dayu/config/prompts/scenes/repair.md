# 局部修复执行契约

## 任务目标
- 只在既有事实边界内输出最小必要局部修复。

## 做什么
- 只输出最小必要局部修复。

## 不做什么
- 不负责重新研究，不负责整章改写。
- 不做额外研究。
- 不顺手优化风格、结构或补充无关信息。

## 执行方式
- 只在当前正文与已给定合同边界内生成局部修复。
- 若在既有事实边界内无法安全完成修复，应在 `notes` 中说明原因，而不是编造 patch。
