# PR 190 F13 S1 C2 checkpoint

## Gate metadata

- work unit / slice / checkpoint: F13 / S1 / C2（material → boundary → governance → replacement → durable payload）
- status: `accepted`
- base commit: `62445b59c7a644133b15ca29d34c6e678aa2c047`
- reviewed source/config/test diff identity: `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`
- captured at: `2026-08-06T16:40:43+08:00`
- checkpoint性质: 完整 S1 未提交工作树上的增量审查，不是可部署或可提交中间态
- prerequisite: C1 accepted；C1 覆盖文件自其 acceptance 后未改变

## Incremental review scope

本 checkpoint 只裁决：

1. `compact_material.py` 把 current accepted evidence 与 previous accepted fact 的 claim/逐 fact refs 同源投影到 `PromptLocalProvenanceEntry` 与 immutable boundary；
2. `compaction.py` boundary/atomic expansion/binding validator 与 `context_governance.py` acceptance、combined duplicate/information/caps/audit；
3. `compact_payload.py`、`compact_artifact.py`、`compact_pipeline.py`、`context_events.py` 的 schema-5 strict serialization/reconstruction、proposal/replacement/boundary/coverage/audit/aggregate 全绑定；
4. 对应 provenance、artifact、terminal owner tests。

不在本 checkpoint 单独裁决 rolling/multi-pass/Memory/reconnect/RunInput consumers；这些进入 C3，但 reviewer 可报告 C2 owner contract 对下游造成的 blocker。

## Frozen expectations

- previous fact 与 current evidence boundary refs 均必须非空；普通 context/answer/summary/trace refs 必须为空。
- retained fact 由 Host 原子复制 boundary readable claim + same-entry refs；new fact 只选 current `evidence_material`，refs 是该 fact selected entries 的 boundary-order unique union。
- 无 previous label 支持新 claim 的路径；无 evidence material 的用户/assistant correction 不能形成 accepted EvidenceFact。
- Context Governance 是 proposal → accepted replacement 唯一 owner；combined retained+new 执行 duplicate/information/caps。
- accepted payload/artifact 持久化 `accepted_proposal`（audit-only）与 `accepted_replacement`（consumer truth）；旧 `accepted_candidate` 无 writer/reader/alias。
- strict durable parser 重验 proposal↔boundary↔replacement、coverage、policy audit、per-fact nonempty refs 和 aggregate：aggregate exact 等于逐 fact refs 按 fact/entry 顺序 unique union，同时是 request evidence boundary union 的 unique subset；不得强制跨 fact boundary 全局顺序。
- repair proposal 必须重新走完整 parse/binding；rejected/failed candidate 不得物化 accepted artifact/replacement。
- 不引入 heuristic、兼容 reader、drop ledger、flat aggregate 反向写 fact 或第二 provenance owner。

## Manual retained + two-source-new recomputation

Owner test：`tests/host/test_compaction_contract.py::test_retained_plus_two_source_new_fact_has_exact_per_fact_and_aggregate_refs`。

```text
previous P1 = (evidence:previous-a, evidence:previous-b)
current E1  = (evidence:current-a, evidence:shared)
current E2  = (evidence:shared, evidence:current-b)

retained P1 atom refs =
  (evidence:previous-a, evidence:previous-b)

new fact support (E1, E2) ordered unique union =
  (evidence:current-a, evidence:shared, evidence:current-b)

replacement fact/entry ordered unique union =
  (evidence:previous-a, evidence:previous-b,
   evidence:current-a, evidence:shared, evidence:current-b)
```

该计算不读取任意单 entry、operation flat union、event ordinal 或 claim 文本反推 provenance。

## Validation evidence

实现 Agent与两路 reviewer 均运行该 focused 命令：

```text
source .venv/bin/activate && pytest -q \
  tests/host/test_compact_material.py \
  tests/host/test_compact_artifact_store.py \
  tests/host/test_compact_pipeline.py \
  tests/host/test_compaction_terminal.py \
  tests/host/test_context_compact_events.py
215 passed in 0.85s
```

两路 review 后，Controller 把包含手工复算 owner test 的 `test_compaction_contract.py` 显式并入同一轮，运行上列五个文件加该 contract 文件：`255 passed in 0.87s`。`git diff --check` 通过，source/config/test diff identity 仍为 `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`。

本 checkpoint 的验证是 owner/Host contract 测试与手工 deterministic recomputation，不是 interactive CLI 或真实 provider 行为证据。

## Reviewer decisions

- AgentMiMo: `accepted`；artifact：`docs/reviews/pr-190-f13-s1-c2-review-mimo-20260806.md`。无 blocking/high/medium finding。
- AgentDS: `accepted`；artifact：`docs/reviews/pr-190-f13-s1-c2-review-ds-20260806.md`。无 blocking/high/medium finding。

Controller finding 裁决：

- 接受“可增加非 evidence boundary 非空 refs 定向 negative test”与“可增加 context/omitted 定向 tamper case”为 low residual；production kind/ref invariant、whole-object binding 与现有用户/assistant non-evidence owner test 已覆盖当前强约束，不阻塞。
- 驳回“durable parser 没有显式 tamper test”：`tests/host/test_context_compact_events.py` 已参数化覆盖 replacement claim、selection、refs 与 aggregate 越界/重复/mismatch tamper。
- 接受上游 `RunInputMaterialBlock.accepted_evidence_id` 保留：它只拥有 current evidence admission 的 singular atom，计划明确允许；`PromptLocalProvenanceEntry`、boundary、replacement 与 durable consumer 均只读 `canonical_evidence_refs`，没有第二 provenance owner。

两路均接受且无 unresolved blocking/high/medium finding。本 checkpoint 接受，可进入 C3；若后续修改 C2 覆盖的生产/config/test 文件，则本 acceptance 立即失效并需重跑。
