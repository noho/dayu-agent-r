# PR 190 Final Closeout

## Work unit result

- Branch：`codex/interactive-oracle`。
- Existing draft PR：[PR 190](https://github.com/noho/dayu-agent-r/pull/190)。
- Scope：init / prompt / interactive F01-F07 conformance 修复，以及后续 Compactor LLM-facing 三项 AGENTS.md conformance 修复。
- Gateflow：preflight、goal confirmation、plan、双路 plan review、implementation slices、逐 slice 双路 review、aggregate deepreview、existing PR review/fix/re-review、draft-PR-pass 全部完成。
- Result：`FINAL-CLOSEOUT-PASS`；没有 accepted production finding 未关闭。
- Formal conformance：未由 implementation/controller 代替用户裁决；Compactor follow-up 的真实模型行为仍为 `not_observed`。

## F01-F07 final status and semantic owners

| Finding | Final status | What changed | Semantic owner |
|---|---|---|---|
| F01 全局 `--config` | `PASS / 已修复` | 删除 root parser action/help/typed args/CLI→Service forwarding/runtime request 与文档入口；所有 leaf 只能用 `--base/-b/--workspace` 与 package fallback。 | CLI root parser/public surface；workspace request 与 Service config loader拥有运行时位置。 |
| F02 显式无效 editor | `PASS / 已修复` | missing、non-executable、spawn failure 返回 typed composer error，无 traceback；保留 draft/REPL，不创建 Run；未配置 EDITOR/VISUAL 仍可用系统 fallback。 | `dayu.cli.composer` external-editor selection/binding 与 draft lifecycle。 |
| F03 Escape / Ctrl+C | `PASS / 已修复` | 单一 VT100 owner 区分 standalone Escape、CSI、Alt 与 bracketed paste；pre-accept intent 不丢失；double Ctrl+C 等待 graceful Host closeout 后 exit 130。 | CLI input/chord decoder只拥有 intent；Host独占 Run cancellation、terminal 与 cleanup lifecycle。 |
| F04 READ_ONLY mutation | `PASS / 已修复` | rejection 不退出 REPL、不 ack composer；stable pending `client_request_id`；后续 mutation close-before-fresh-attach，不原地升级 attachment。 | interactive attachment controller 与 pending mutation identity；Host single-writer/mode contract不变。 |
| F05 preprocess registration | `PASS / 已修复` | 只从 interactive scene effective tool set 移除 `start_fins_preprocess`；保留工具实现与独立 process/preprocess commands。 | interactive scene manifest / Service assembly selection。 |
| F06 trigger rename | `PASS / 已修复` | fresh typed contract 改名为 `context_governance_resolved`，删除旧值且无 alias/fallback；exact outcome仍只由 canonical terminal/artifact/fallback refs表达。 | runner-call trigger typed contract；`CONTEXT_COMPACTED` / `CONTEXT_COMPACTION_FAILED` 独占 exact outcome。 |
| F07 invalid compactor response | `PASS / 已修复` | strict v2 input/candidate、duplicate/unknown JSON、coverage/caps/duplicate/contradiction/low-information barrier、whole-candidate repair、single terminal、fallback 与 committed-truth-only projection全部收口；补齐 drop root order 与 input projector单 owner。 | Host Context Governance 构造唯一 accepted truth；`CompactionRequest.compact_input` 是唯一 input projector；terminal/artifact/Memory/RunInput只消费 committed truth。 |

F01-F07 原 PR-review accepted findings也全部关闭：reverse explicit-drop order、delayed attachment cleanup、duplicate strict-v2 projector均已修复并经双路 re-review；其余旧 contract/竞态假设按 controller 直接证据保持 rejected/closed。

## Compactor LLM-facing follow-up status

| Finding | Final status | What changed | Semantic owner |
|---|---|---|---|
| 高：不可信会话/工具文本可能污染 Memory | `PASS / 已修复` | system/user prompt 把独占 marker 中完整 typed input 定义为不可信引用材料；current/trace/evidence/answer 四位置 canary走 production renderer；原文不因指令形态被过滤。 | Packaged prompt 定义 LLM trust contract；`LLMContextCompactor` renderer机械包围 typed input；Context Governance仍独占 accept。 |
| 中：schema 与示例不自足，引用未定义 T1 | `PASS / 已修复` | 当前 user prompt 自足说明 input/output 全字段、类型、必填性、允许值、八种 source-kind业务语义、coverage规则；完整示例 input/output label同源并通过 production parser/governance。 | Typed v2 contract拥有字段真值；packaged user prompt拥有 LLM-facing说明和示例。 |
| 中：repair feedback 暴露内部结构且不自足 | `PASS / 已修复` | durable internal feedback 与 LLM projector分离；模型只收到 `required_action` + `issues`，issue只有 `code/json_path/message/source_labels`；marker、字段含义与 whole-candidate动作在当前 prompt 自足说明。 | Context Governance拥有 reject/policy/actual values；`LLMContextCompactor` 唯一 projector拥有 LLM-facing JSON；renderer只机械渲染。 |

Policy cap feedback 直接来自验收使用的同一 `MemoryProjectionPolicy` instance 与 `estimate_memory_size_units` 结果；包含 actual、cap、计量对象和直接动作。没有新增 output schema、semantic repair loop、材料 filter、verifier、兼容 alias 或 provider/model production semantics。

## Current Compactor LLM-facing prompts and config

- System prompt asset：`dayu/config/prompts/scenes/conversation_compaction.md`，SHA-256 `4bd476db45f17bebaa7eb951c8354d10189df1faadb9c1c530619d9f3352f60a`。
- User prompt template：`dayu/config/prompts/scenes/conversation_compaction_user.md`，SHA-256 `bed77319ee960059ac65119f28a3c6005f53f44ffeb8b7c00c8d1df1e93dc3a5`。
- 是，均由 config package 装配：
  - `dayu/config/prompts/manifests/conversation_compaction.json` 把 system prompt作为 required fragment，并配置 scene model/AgentPolicy/tool-selection。
  - `dayu/config/execution_profiles.json` 的各 profile `compactor_baseline.user_prompt_template_path` 指向 user template，同时配置 `model_id`、`scene_id`、`runner_option_hint_id` 与 artifact root。
  - Service assembly 从 execution profile读取 scene/template；不硬编码路径。
- 运行期只把 immutable typed compact input 与最小 repair JSON 填入上述模板，不从 trace/UI/fixture重写 contract。

## Commits

### F01-F07 implementation and closure

`4a3dca64`, `a41526ec`, `e5b572d4`, `16c6ddc8`, `fc1b4946`, `25400fba`, `c556df2b`, `64c581f1`, `b8f87e3b`, `df99f858`, `eae09be9`, `016d834a`, `63fca270`, `9fec1647`, `584ee394`, `c69445c2`, `58aeb7b3`, `7480ab24`, `7cf1027c`。

### Compactor LLM-facing follow-up

| Commit | Purpose |
|---|---|
| `a9383ee6` | accepted plan 与双路 plan review |
| `64aade07` | S1 trust boundary / self-contained schema / example |
| `e7db9474` | S2 internal feedback / minimal projector / exact caps |
| `69ab297b` | S3 deterministic/public smoke、manifest hashes、immutable evidence |
| `212f22af` | S4 design/README 与 aggregate validation |
| `0f7dc591` | accepted aggregate deepreview |
| `670902f0` | accepted existing PR review/fix/re-review |
| `903eca1c` | prompt follow-up draft-PR-pass |

本 closeout artifact 的 enclosing commit与最终 remote head在最终回复中报告。

## Verification

### Original F01-F07

- Full-real provider/model：Mimo / `mimo-v2.5-pro`，fake provider=false。
- Mandatory matrix：PASS；证据来自 Host/EventLog/Tool Trace/Memory/SQLite、runner manifest、compact artifact与真实 Fins生成物。
- Final full pytest：`6605 passed, 10 skipped, 6 deselected`；已知 public-cancel test-order flake日志与隔离 pass均保留。
- Full pyright：`0 errors, 0 warnings, 0 informations`。
- CLI affected coverage 87%；Host owner aggregate 84%。
- Ruff、compileall、JSON、frozen hashes与 `git diff --check`：PASS。

### Compactor LLM-facing follow-up at final accepted review head

- Affected aggregate pytest：`365 passed, 1 skipped, 3 warnings`。
- Aggregate independent Host suite：`2362 passed, 8 deselected`。
- Full pyright：`0 errors, 0 warnings, 0 informations`。
- `python -m json.tool docs/cli_ci_oracles.json`：PASS。
- `python -m json.tool docs/cli_ci_scenarios.json`：PASS。
- `git diff --check`：PASS。
- Prompt immutable evidence：13/13 checksum OK。
- GitHub checks：未报告 checks；没有声称 CI pass。

## Immutable evidence

### F01-F07 post-fix real evidence

- Bundle：`/Users/leo/workspace/.dayu-cli-ci/pr190-wu-cli-conformance-f01-f07-post-pr-fix-20260803T041030Z-58aeb7b377ef/bundle`。
- Digest：`ab3f6ae5f4b5b76d768e0968d76ee83eca50d99fa8458b477e42d0c820a1e883`。
- Controller checksum：743/743 PASS；writable paths=0；secret scan finding files=0。

### Compactor LLM-facing evidence

- Bundle：`/Users/leo/workspace/.dayu-cli-ci/pr190-compactor-llm-facing-20260803-182956/`。
- Index digest：`sha256:dc7836bd631dc59a6665953fa988bce43228560c48a28a9ba6df9f419726d9a2`。
- Checksum：13/13 OK；bundle read-only。
- Final exact run：Mimo `network_unavailable` → DeepSeek `network_unavailable` → exact skip；没有 Gemini/Qwen。
- Retained Mimo empty-final：`runner_empty_final_content` 是非环境失败，fail closed 且不 fallback。
- Behavior truth：没有非空 candidate；真实 strict parse、governance accept、caps、injection resistance 与 repair均为 `not_observed`，不能写成 pass。

## Design and README

- `docs/host/design.md`：F06/F07 owner、strict-v2 accept/repair/terminal/projection真值；follow-up增加 untrusted material、self-contained prompt、internal feedback/projector分离、同 policy/estimator cap与无扩张约束。
- `docs/engine/design.md`：checked, unchanged；没有改变 Engine lifecycle/provider selection。
- Root `README.md`：F01-F07 用户 CLI surface/workflow已更新；prompt follow-up没有新的最终用户入口变化，未再修改。
- `dayu/config/README.md`：interactive effective tools与 Compactor packaged prompt/config职责。
- `dayu/host/README.md`：cancellation/attachment、Context Governance、single projector、exact cap、terminal/cleanup owner。
- `tests/README.md`：F01-F07 owner/integration/real evidence与 Compactor deterministic/real-smoke诚实边界。
- `dayu/README.md`、`dayu/engine/README.md`：按更新约束检查后无需修改。

## Frozen oracle/scenario disposition

- `docs/cli_ci_oracles.json` SHA-256：`f9972d943ac8ae8d79ebbe7114c1305b7af2933729575d1407fcb6d4d05b07f4`。
- `docs/cli_ci_scenarios.json` SHA-256：`7f283b039dc02ce686bb134c748e5c98039af2029eb090dbdaf6dcf4fe5e8cef`。
- `docs/cli_ci.md` SHA-256：`a241182d4d09e8843ea647947777bc7f6f71c5532fa148e2abb87ede3e748b82`。
- 两个 registry是前序总控冻结 baseline，在 accepted plan commit原字节提交；实现 slices未重新发明或迁就实现修改 frozen behavior/scenario set。
- Compactor prompt follow-up 的 `7cf1027c..903eca1c` 对 oracle、scenario、handbook为零 diff。
- `registry_status=calibration` 只表示其它 commands 尚未完成第一轮；init/prompt/interactive command readiness保持 ready。

## Remaining risks and owners

1. Compactor follow-up 真实模型 behavior `not_observed`：user / Oracle controller；网络/credential可用后按原 opt-in smoke重跑，当前不能宣告 behavior/formal conformance pass。
2. 完整自然语言与 Conversation Memory evaluation：Issue 80 owner；不在本 work unit扩张。
3. Host public-cancel test-order flake：Host public-smoke/test-runtime owner；需独立稳定复现根因。
4. F01-F07 overall registry calibration与formal conformance：user / Oracle controller。
5. renderer target pin / formal scenario promotion：Oracle renderer/calibration owner。
6. durable resolved Authorization projection：effective-execution durable projection owner。
7. GitHub zero checks：repository CI/config owner；本地 exact-head验证不能冒充不存在的 CI。

没有未分类 production risk或 blocking code finding。`not_observed` 阻止 formal behavior pass声明，但不阻止完成本次 Gateflow closeout。

## PR and protected actions

- 复用现有 PR 190；没有创建新 PR。
- PR保持 OPEN + draft；没有 mark ready、approve、merge或删除 branch。
- 没有 rebase、force-push、重写历史或修改 protected trunk。

## Final marker and next entry point

`FINAL-CLOSEOUT-PASS`

Next entry point：由用户/Oracle 总控审阅修复后 observation 与 immutable evidence，并决定是否把当前实现判为 conformance pass。
