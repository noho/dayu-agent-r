# PR 190 F13 S1 C1 checkpoint

## Gate metadata

- work unit / slice / checkpoint: F13 / S1 / C1（v4 contract、structure、prompt）
- status: `accepted`
- base commit: `62445b59c7a644133b15ca29d34c6e678aa2c047`
- reviewed worktree diff identity: `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`
- captured at: `2026-08-06T16:28:58+08:00`
- checkpoint性质: 完整 S1 未提交工作树上的增量审查，不是可部署或可提交中间态

## Incremental review scope

本 checkpoint 只裁决：

1. `dayu/host/compaction.py` 中 fresh v4 input、proposal、accepted fact、replacement、truth DTO 及 frozen/slots/no-default 不变量；
2. `dayu/host/compact_structure.py` 的 descriptor、template、JSON Schema、strict parser exact 同源；
3. `dayu/host/llm_compaction.py` 与 `dayu/config/prompts/scenes/conversation_compaction_user.md` 的 initial/repair schema 与 self-contained LLM-facing 规则；
4. 对应 contract / parser / prompt owner tests 与 fake 仅作为测试驱动器的结构迁移。

不在本 checkpoint 单独裁决 material、durable payload、rolling、Memory 或 Tool Trace；这些分别进入 C2、C3、S2，但 reviewer 可报告 C1 改动造成的跨边界 blocker。

## Frozen expectations

- active v3 symbols、schema、alias、dual parser 均不存在；旧 schema 只允许出现在明确的拒绝/不存在负断言中。
- proposal exact root 七字段，`retained_previous_evidence_fact_labels` 必填；旧 fact 只能 selector retain/omit，新 fact support 只能选择 current `evidence_material`。
- previous fact claim/provenance 不存在供模型重写的输出字段路径。
- accepted fact 每条 refs 非空唯一；accepted replacement 是消费真源，proposal 只保留审计绑定。
- prompt 自足说明字段、类型、必填性、允许 kind、retain/omit、combined caps 和最小 JSON；不暴露 event id、digest、payload ref 或把 provenance 校验交给模型。
- 不引入自然语言 entailment heuristic、compatibility shim、drop ledger 或第二 provenance owner。

## Validation evidence

实现 Agent与两路 reviewer 均独立运行该 focused 命令；两路 review 完成后，Controller 在相同 diff identity 上再次独立重跑：

```text
source .venv/bin/activate && pytest -q \
  tests/host/test_compaction_contract.py \
  tests/host/test_llm_compaction.py
实现 Agent：64 passed in 0.37s
AgentMiMo：64 passed in 0.35s
AgentDS：64 passed in 0.35s
Controller final rerun：64 passed in 0.34s
```

附加结构校验由实现 Agent 运行：template / Draft 2020-12 schema / parser JSON 校验通过。测试结果仅证明 owner contract 测试通过，不等于真实 provider 或 interactive 行为通过。

## Residue evidence

- `dayu/**/*.py`、`tests/**/*.py`、`utils/**/*.py` 中 active `CompactCandidateV3` / `accepted_candidate`：零匹配。
- Service 层保留的 v3 字符串仅为“当前 prompt 不得出现旧 schema”的负断言，不构造或读取 v3。

## Reviewer decisions

- AgentMiMo: `accepted`；artifact：`docs/reviews/pr-190-f13-s1-c1-review-mimo-20260806.md`。无 blocking/high/medium finding；3 个 low finding 均为 accepted-as-is，不改变当前 contract 正确性。
- AgentDS: `accepted`；artifact：`docs/reviews/pr-190-f13-s1-c1-review-ds-20260806.md`。无 blocking/high/medium finding；2 个 low 与 1 个 info 均为维护性/防御性观察。

Controller 裁决：两路都接受，且无 unresolved blocking/high/medium finding；source/config/test diff identity 保持 `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`。`git diff --check` 通过。本 checkpoint 接受，可进入 C2；若后续修改 C1 覆盖的生产/config/test 文件，则本 acceptance 立即失效并需重跑。
