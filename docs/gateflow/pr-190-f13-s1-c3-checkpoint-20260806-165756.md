# PR 190 F13 S1 C3 checkpoint

## Gate metadata

- work unit / slice / checkpoint: F13 / S1 / C3（rolling、multi-pass、Memory、reconnect、RunInput、call sites）
- status: `accepted`
- base commit: `62445b59c7a644133b15ca29d34c6e678aa2c047`
- reviewed source/config/test diff identity: `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`
- captured at: `2026-08-06T16:57:56+08:00`
- checkpoint性质: 完整 S1 未提交工作树上的增量审查，不是可部署或可提交中间态
- prerequisites: C1、C2 accepted；其覆盖的 source/config/test diff 未改变

## Incremental review scope

本 checkpoint 只裁决：

1. `compact_material.py` rolling projection 从 previous strict `accepted_replacement.evidence_facts` 同一 atom 携带 claim + per-fact refs；
2. `compaction_operation.py` reactive multi-pass 聚合 replacement atoms、root retained selector 与 root boundary revalidation；
3. `memory.py`、`run_input.py` 的 Memory/reconnect/ordinary input 逐 fact refs 投影；
4. `compact_pipeline.py`、`dispatch.py`、`engine_ingest.py`、smoke helpers/runtime assembly 及其所有 v4/schema-5 call sites；
5. repair exhaustion/fallback、stale/late single-terminal、reconnect owner tests与全 active Python residue。

S2 才拥有 public Tool Trace 的新 typed fact projection和 README；C3 只允许 `test_tool_trace_queries.py` 的删除-v3/schema-5 fixture 迁移，不提前裁决 S2 新语义。

## Frozen expectations

- rolling previous block 的 `canonical_source_refs` 是 prior compact source/event refs，`canonical_evidence_refs` 是该 accepted fact atom 的证据 refs；两者不得替换、猜测或交叉。
- previous fact 保留/省略由 root selector 表达；多 pass accepted replacements 的 atom 随 claim+refs 一起移动，不能退回 proposal facts或flat aggregate。
- Memory 每条 `EvidenceBackedFactView` 使用自身 atom refs；不得把 event aggregate union 写给所有 facts。
- reconnect 只经 strict canonical terminal parser恢复 replacement；failed/rejected candidate无读入口。
- RunInput 整体 represented refs可以使用 aggregate，但不得用于重建逐 fact refs。
- bounded repair/exhaustion/fallback 不写 accepted Memory/artifact；stale/late不产生第二 terminal。
- fake duplicate repair 必须先经真实 governance typed reject，再按 typed issue/path 处理；不得用 claim/关键词 heuristic 预判。
- active Python v3 symbol/schema、旧 durable key、`PromptLocalProvenanceEntry.accepted_evidence_id` 零残留；仅允许 Service prompt 的两条明确 negative assertion。
- 不引入兼容层、第二 truth、下游 fallback、renderer补偿或越过 UI→Service→Host→Engine 分层。

## Validation evidence

实现 Agent 报告的 focused/integration 命令：

```text
source .venv/bin/activate && pytest -q \
  tests/host/test_memory_projection.py \
  tests/host/test_run_input_builder.py \
  tests/host/test_dispatch_scheduler.py \
  tests/host/test_engine_ingest_mapping.py \
  tests/host/test_compaction_operation.py \
  tests/host/test_proactive_compaction_operation.py \
  tests/host/test_compaction_cancellation_scope.py \
  tests/host/test_accepted_result_projection.py \
  tests/host/test_tool_trace_queries.py \
  tests/host/test_public_compact_smoke.py \
  tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py
595 passed, 1 skipped, 3 warnings in 12.87s
```

两路 review 完成并停止并发测试后，Controller 独占重跑同一完整命令：`595 passed, 1 skipped, 3 warnings in 12.58s`。`git diff --check` 通过，source/config/test diff identity 仍为 `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`。

3 warnings 是第三方 `edgar` deprecation；1 skip 条件为既有条件。本结果属于 owner/integration/Host smoke tests，不是实际 provider 或 interactive CLI scenario。

## Residue evidence

- `rg -n -i 'accepted_candidate|candidate_binding_v4' dayu tests utils --glob '*.py'`：零命中。
- active v3 compact symbol/schema 仅命中 `tests/service/test_host_assembly.py` 两条“不应出现在 prompt”的负断言；无 v3 constructor/reader/writer/fixture。
- upstream accepted evidence atom / `RunInputMaterialBlock.accepted_evidence_id` 是 current evidence admission owner，计划明确允许；它不进入 PromptLocal/downstream read path。

## Reviewer decisions

- AgentMiMo: `accepted`；artifact：`docs/reviews/pr-190-f13-s1-c3-review-mimo-20260806.md`。无 blocking/high/medium finding。
- AgentDS: `accepted`；artifact：`docs/reviews/pr-190-f13-s1-c3-review-ds-20260806.md`。无 blocking/high/medium finding。

Controller 裁决：

- rolling 从同一 accepted atom 复制 claim + per-fact refs，compact event source refs 与 evidence refs 分离；multi-pass只从 accepted replacement 读取贡献项并在 root 再验收；Memory逐 fact refs，无 aggregate反写。
- fake repair 使用 typed issue code + JSON path；没有 claim/关键词 heuristic。
- reviewer 并发期间一条缩减/超时命令的 `304 passed` 不是冻结 C3 集合结果，不能替代实现 Agent与 Controller各自完成的完整 `595 passed, 1 skipped`；最终 checkpoint 采用 Controller 独占重跑结果。
- tests/Host smoke/runtime assembly 明确不是实际 provider 或 interactive CLI evidence；S2仍拥有 public Tool Trace新投影与README。

两路均接受且无 unresolved blocking/high/medium finding。本 checkpoint 接受，可进入 S1 正式两路 full-slice code review；若后续修改 C1-C3 覆盖的 production/config/test 文件，则相关 checkpoint 立即失效并需重跑。
