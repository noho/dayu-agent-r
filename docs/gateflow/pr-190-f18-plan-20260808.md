# PR 190 F18 Revised Implementation Plan

## Gate state 与 binding authority

- Work unit：F18；当前 gate：`final-closeout`，next entry point：F19 新 work unit handoff。
- 原始目标：`docs/gateflow/pr-190-f18-goal-confirmation-20260808.md`。
- Sequencing amendment：`docs/gateflow/pr-190-f18-goal-confirmation-amendment-20260808.md`。
- Controller 裁决：`docs/gateflow/pr-190-f18-fixed-profile-path-adjudication-20260808.md`
  supersede 旧 plan-review adjudication 中“旧 sequence 327 必须阻塞 B2”的结论；最新二路复审裁决为
  `docs/gateflow/pr-190-f18-plan-third-rereview-adjudication-20260808.md`；它 supersede
  `docs/gateflow/pr-190-f18-plan-rereview-adjudication-20260808.md` 中的 ratio、phase 与 budget 设计，但保留旧 review 对
  contract、synthetic replay、工具真源和 bounded execution 的 findings。
- 设计真源：`docs/host/design.md`、`docs/engine/design.md`；Oracle/scenario 真源：`docs/cli_ci.md`、
  `docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`。
- 本 plan-fix 不授权 provider、实现、commit 或 push；两路 re-review 通过后才进入 implementation。

## Goal、success signals 与 non-goals

F18 包含两个裁决边界独立的目标：

1. B1：按现有 accepted record 方式，把
   `interactive.interactive.g06.tool-trace-formal@2` 记录为用户已裁决的 `accepted`。mandatory evidence 只包括 public
   Host Tool Trace resolver/analysis response identity、canonical terminal 六字段 equality 与 secret/path scan。cold analyzer
   `compactor_responses=0` 和 provider-native request id unavailable 保留为 limitation/residual question，不是 mandatory gap。
2. B2：在完全 fresh workspace 中，从第一个 opener 起固定完整 constrained profile，以三条最多各执行一次的真实 MiMo
   chain 聚合观察 replacement+reconnect、same-boundary repair、exhausted fallback；产出逐项 observed-behavior report 交用户
   裁决。自然未触发的分支写 `needs-more-evidence`。无论 evidence 是否完整，用户裁决前 B2 保持 `unadjudicated`，overall
   readiness 保持非 ready。

旧 Trial2 sequence `325 -> 326 -> 327` 的固定 verdict 是：

`unresolved; original owner exception was not durably captured; no causal inference`

它是独立 residual，不再阻塞 fixed-profile B2；新链也不得反向解释、删除、覆盖或重标该失败。

明确 non-goals：不判断跨 opener profile transition supported/rejected；不做 sequence 324/326 synthetic exact replay；不改产品、
schema、公共 failure、测试、README 或 observation tooling；不实现动态 per-Run profile/caps；不处理 Issue 192、Fins tool schema、
B1 cold analyzer enhancement；不使用 DeepSeek、fake/mock provider/tool、output injection、旧 Trial durable state、private SQLite
mutation 或 unit test 冒充正式 observation。不得读取、修改、stage 或提交
`docs/reviews/plan-review-20260808-095346.md`。

## First-principles judgment 与 owner evidence

1. `open_host` construction-time policy 只冻结单个 Host handle。当前 public Session/attach/durable schema 没有 Session-level
   execution-profile manifest，也没有跨 opener mismatch rejection；因此 Trial2 transition contract 是 **unspecified**，不能从
   handle-level design 外推出 illegal 或 supported。若未来要定义该语义，必须另做 goal/design confirmation。
2. Trial2 唯一物理 SQLite 已推进到 sequence 327；`sqlite-before` 不是 transaction-consistent physical snapshot，原
   `candidate_source_failed` logger/traceback 未归档。原库与 synthetic sequence-326 clone 上的 direct candidate experiment 都
   得到 `candidate_prepared`，只反证“现存空 constrained snapshot 必然抛错”，不能恢复原 cause。F18 不再做 counterfactual
   replay，也不据此改产品或新增 typed cause。
3. sequence 325 的 durable 工具真源是 `USER_INPUT_ACCEPTED.effective_tool_set`：exact names、business bundle digest、effective
   schema digest、source refs、display names 与 tool snapshot ref。该 Run 在 Attempt/runner-call manifest 前失败，不存在 target
   runner manifest；报告只能引用 `effective_tool_set`，不得借其它 Run manifest 补真源。
4. B2 registry precondition 是 `relevant-material-over-initial-output-caps`，predicates 29/30 不要求 wide baseline、hot-switch 或旧
   failure closure。Service 可在首 opener 前把一个完整 profile 同源装配为 Host context/compactor/memory typed inputs，故
   fixed-profile fresh path 不需要产品修改。
5. Engine 不拥有 Host context budget、memory、compact artifact 或 Host attempt budget；本轮没有 Engine 修改面。

## Contract 与 change surface

### 不变 contract

- 产品/public API/schema/state machine：零改动。
- `dayu/`、`tests/`、`utils/`、README：零改动。
- observation tooling：复用已冻结的外部 POSIX PTY/public resolver/publication writers 与 tracked final-tree helper，不修改脚本；
  执行前分别记录 digest。publication writers 不得创建 `secret-scan.json`；final-tree helper 不得写其它文件。
- B2 `status=unadjudicated`、`user_adjudication_identity=pending-oracle-controller-adjudication`、
  `applicable_from=after-oracle-controller-adjudication` 与 overall registry readiness：零变化。

### 允许的 tracked 文档/registry surface

- `docs/cli_ci_scenarios.json`
- `docs/cli_ci_oracles.json`
- `docs/cli_ci.md`
- `docs/reviews/pr-190-oracle-adjudication-20260808.md`
- Gateflow implementation/review/final-closeout artifacts

现有B1 bundle `pr190-formal-g1a2xu` 与其 `evidence/public` tree在本work unit中是immutable read-only evidence；Slice 1只在
tracked registry/handbook登记其既有refs/digests，禁止向该bundle写入或重跑finalization。B2执行前必须生成一个全新、与B1及
Trial1/Trial2均不同的run-owned bundle id和独立public/private evidence root；该identity在首次writer前冻结并写入execution index。
不得提交 raw SQLite、raw PTY、日志、credential 或绝对私有路径。

## Slice 1 — B1 exact adjudication projection

### Objective 与 exact field contract

只更新 `tool-trace-formal@2` 及其 Oracle/handbook叙述：

- `status="accepted"`；
- `bundle_ids=["pr190-formal-g1a2xu"]`；
- `report_ids=["evidence/public/observed-behavior-report.md"]`；完整文件 digest 已冻结，B1 section 由同一
  public report 内现存标题 `B1: tool-trace-formal@2` 与 `observation-summary.json#b1` 双重定位，不发明未验证的
  Markdown fragment slug；
- `report_sha256=["de7ee64de11140add816facf9926c2cf17aa13a4176bd873bc0f91ed20b70f79"]`；
- `execution_outcome="error"`，process `exit_codes=[1]` 原样保留，并由 report 说明 cleanup EOT/process exit 不替代 canonical
  `RUN_SUCCEEDED`；
- `evidence_status="sufficient"`、`gap_kind="none"`、
  `conformance_at_observation="matches-accepted-oracle"`；
- `user_adjudication_identity="interactive.interactive.g06.tool-trace-formal@2:user-adjudication:2026-08-08"`；
- `applicable_from="next-interactive-conformance-run-after-F18"`。

只允许以下 B1/global-public relative refs，禁止 B2 refs：

1. `evidence/public/observed-behavior-report.md`，SHA256
   `de7ee64de11140add816facf9926c2cf17aa13a4176bd873bc0f91ed20b70f79`；
2. `evidence/public/observation-summary.json#b1`，SHA256
   `dfe3604bba8c7f8bda6b0d8a80639a87ac77a2d1c03f0e7baa28236e554d7c0a`；
3. `evidence/public/b1-cold-tool-trace/tool-trace-analysis.json`，SHA256
   `f1b162c185c977bdb24882cdc7b1eb8d273defe02cfa1dbf920248d07754ec3a`；
4. `evidence/public/b1-cold-tool-trace/tool-trace-analysis.md`，SHA256
   `2bba07d27ec3307a44cc0d6531c73cb7565bcd72eea7304c7f56d1e0961c41da`；
5. `evidence/public/digest.json`，SHA256
   `567d5539d9e745e78379093117275231c7b96ab5ac03536f13aec9475e476153`；
6. `evidence/public/secret-scan.json`，SHA256
   `6f33e30ced5dbbcc96c6cadcc93bf1c5c2ad3d261c5f3386ccbec74c3ba52ba8`。

`docs/cli_ci_oracles.json` 追加用户裁决 history，并把 summary/notes 精确改成“B1 accepted；cold/provider-id limitation保留；
B2 pending”。`docs/cli_ci.md` 与 adjudication report 同源更新。逐字段断言 B2 的 status/adjudication/applicable identity、
`registry_status` 和 readiness 叙述不变。

### Stop/completion

任一 digest、relative ref、B1 section 或 secret/path scan不匹配即停止，不写 accepted。完成信号是四个 tracked owner 文件的
JSON/diff check 通过，且没有 B2 accepted/ready 投影。

## Slice 2 — B2 fresh fixed-profile formal observation

### Global frozen setup

每条链独立复制 production-init MiMo seed与真实 18-file AAPL corpus，确认 `.dayu`/EventLog/Memory/artifact/SQLite均不存在；
禁止复制 Trial1/Trial2 state。首次 provider call 前，三条 workspace 都经 production ConfigLoader/Service assembly 验证以下同一
profile projection并冻结 config/inventory digest：

- `default_execution_profile_id=standard-256k`；ordinary/compactor model均解析到
  `mimo-v2.5-pro-plan -> mimo/mimo-v2.5-pro`，DeepSeek prohibited；
- context policy：soft ratio `0.018`、hard ratio `0.5`、`max_reactive_compactions_per_run=1`、
  `max_compaction_attempts_per_operation=5`、
  `policy_ref=pr190-constrained-standard-256k`；
- memory policy：turn floor `0`，evidence item/floor `1/1`，evidence char cap `160`，summary cap `160`，anchor/intent/reference
  均 item cap `1`、char cap `160`，selected window item/char cap `8/32768`，policy ref同上；
- execution profile声明 `max_iterations=24`，但 production `interactive` scene 以合法的 `scene_override` 把 effective
  `max_iterations` 冻结为 `20`；`fallback_mode=force_answer`。因此每个 ordinary Run 的 typed hard cap 是20个loop calls加最多
  1个force-answer call，即 `21`。下文的 `25/Run` 只保留为更宽的外部停止 ceiling，不是 typed owner 配置，也不得修改 scene
  去追求24。

首次 provider call 前必须把以下 provider-independent calibration 持久保存到 run-owned private evidence，并发布 path-redacted
typed summary：production ConfigLoader/Service assembly 派生的 effective model/context window、soft/hard threshold、五阶段
`ORDINARY`/`CONTINUATION` decision、memory selection policy（含 floor/caps）、完整 profile/config/inventory digest，以及 Trial2 owner
`predicted_input_tokens` band：initial ordinary `9,048 < 18,874`、同Run continuation `19,174/20,316`、首个post-tool
ordinary `20,363 > 18,874`。任一 typed field或digest无法保存即停止；不得在 provider运行后回填，也不得用
`conservative_input_tokens` 替代 Host pressure 真源。

同一 chain 全部 CLI invocations/reconnect必须保持 config/profile/inventory digest exact equality。只允许 production
`dayu-cli interactive --label memory-cap-constrained-replacement --detail --no-thinking --model mimo-v2.5-pro-plan`、POSIX PTY、
真实工具。复用冻结 harness digest `28e4fd691f9f5198a4cbdc2ef645b797c552af88d5fc6e32df7ed13449dd9424` 与 PTY owner
digest `7e32637439c8625b6ee78ec65484f5b4e54e98cf199f65dd50d965a1739b1223`；不修改 tooling。

### Shared PTY three-Run state machine

三条链使用同一 fixed-profile sequence。Run ordinal只标识依赖顺序，不承诺 stochastic provider 必然触发 compact：

1. **R1 / isolated business-risk acquisition**：只对同一真实 AAPL corpus 的
   `document_id=fil_0000320193-25-000079` 先调用 `get_document_sections` 完成 typed grounding，再只调用
   `read_section(ref=s_0003)` 读取 Part I Item 1A Risk Factors。该 owner material 为 `68,039 chars / 9,782 words`，
   content SHA-256 为 `563b193a9c31061fa48f25b1b27709e94150d5c9cfceace80ce2a961b6a23b36`，且 provider-free exact scan 不含
   `416,161`、`133,050`、`391,035`、`123,216`、`21.7`、`18.2`。R1只形成一个可在160字符cap内表达、带
   canonical SEC provenance的 business-risk EvidenceFact，并必须以 `RUN_SUCCEEDED` 终止；不得调用其它工具。
   R1内任何 reactive operation 都不是 mandatory baseline/target，立即 seal。
2. **R2 / baseline then FY2025 acquisition**：R2 current input 请求真实 FY2025 tool acquisition，同时显式携带无
   provenance 的 `21.7%/18.2%` 并标为待核验用户材料。其 pre-start `ORDINARY` boundary 是唯一合法 baseline：
   compact source 只允许包含 completed R1 business-risk material，R2 current input 仍是独立 current anchor，不得冒充
   completed evidence。baseline 必须 accepted，且 accepted truth 中恰有一个 canonical-provenance previous EvidenceFact；
   否则立即 seal。accepted 后同一 R2 由 `POST_COMPACT/CONTINUATION` 先按同一文档 typed grounding，再只读
   `read_section(ref=s_0013)`（Part II Item 8，`61,252 chars / 9,744 words`）取得 FY2025 total net sales/operating
   income canonical evidence，并必须以
   `RUN_SUCCEEDED` 终止。R2未触发 baseline、baseline未accepted、未取得canonical FY2025 provenance、terminal非succeeded
   或出现任何 reactive operation，都立即 seal；reactive snapshot 不包含同Run `TOOL_RESULT_ACCEPTED`，不得当作target。
3. **R3 / unique target**：唯一 target 是 no-tool Run 的 pre-start `ORDINARY` boundary。immutable initial input 必须直接同时
   包含 previous business-risk atom、completed R2 FY2025 evidence、unsupported `21.7%/18.2%` current/trace material 与真实
   output caps，且 previous+new eligible EvidenceFact 对 item cap=1 构成真实 over-cap 竞争。target未触发、缺任一项、
   出现任何 reactive operation，都立即 seal；不得由 Memory、回答文本、sequence 或 analyzer 重算补足。R3 proactive
   operation的完成条件按chain role区分：replacement/repair必须accepted；exhausted-fallback必须exact观察5次
   rejection、`retry_repair_budget_exhausted`与deterministic fallback。其他未accepted路径才seal `needs-more-evidence`。
4. **R4 / reconnect**：只有 replacement chain 的R3 target accepted 后才正常退出并 fresh reconnect；最多执行一个 no-tool
   follow-up，直接观察 RunInput/Memory 是否继续消费同一 accepted truth。R4内任何 proactive 或 reactive
   operation都是non-covering，立即 seal。

本 profile 的 provider-independent calibration 只使用 Host pressure 真源 `predicted_input_tokens`：有效 MiMo context window
`1,048,576` 与 ratio `0.018` 派生 soft threshold `18,874`；Trial2 typed rows 直接显示 initial ordinary `9,048`、
同Run continuation `19,174/20,316`、首个post-tool ordinary `20,363`。Host 五阶段矩阵对 `CONTINUATION` 的
soft/hard pressure 均为 `ALLOW_DISPATCH`，只有 `ORDINARY` soft pressure 触发 proactive compact。这只证明
`9,048 < 18,874 < 20,363` 的可行带，不保证 fresh MiMo 的精确数值或ordinal；不得调参、追加padding Run或
重跑同链。turn floor固定为`0`；current input始终是独立anchor，不冒充completed recent group。

### Evidence-calibrated execution matrix

预算依据不是成功保证：production typed owner 的 effective ordinary policy 是20个loop calls加最多1个force-answer call，
故每Run typed hard cap为 `21`；正式R1-R3 typed hard cap为3 Runs/`63` calls，replacement reconnect后R1-R4为
4 Runs/`84` calls。原 `25/75/100` 只作为外部更宽停止 ceiling保留，不能反向解释为scene必须达到24。
harness在每个 Run terminal后核对累计ordinary calls与typed hard cap；到达即seal，不启动下一Run。

此前两次 non-covering provider 运行必须原样保留并计入全局预算 ledger：`formal-chain-01/R1` 为5个ordinary calls、
`27.5583s`、0 compactor operation；`formal-chain-02/R1` 为2个ordinary calls、`15.0313s`、0 compactor operation；累计
7个ordinary calls、`42.5896s`。二者分别因 broad financial source contamination 与 typed fiscal filter返回0 facts而不覆盖
scenario，但不得删除、覆盖或重标 PASS。后续最多仍建立3条 fresh、相互独立的**有效候选链**；invalid/non-covering尝试不占
有效链slot，却继续占用统一的 `1620s` provider execution / `1800s` scenario cap与实际call ledger。

expected mandatory compaction 只有R2 baseline与R3 target，即2 operations/10 calls。typed hard上界按per-Run owner计算：R1最多
1 reactive，R2/R3各最多1 proactive+1 reactive，所以正式阶段最多5 operations/25 compactor calls；R4 reconnect再增最多
1 proactive+1 reactive，replacement总上界7/35。execution index必须分别记录proactive/reactive、operation ref与attempt count；任一
reactive operation一经观察即seal，不继续dependent provider。三条候选链各 wall cap `540s`，provider execution总cap
`1620s`，为 scenario `1800s`总预算保留`180s` finalization reserve；到任一cap即停。

wall time使用 monotonic remaining-time allocator：scenario开始冻结 `global_deadline=start+1800s`；若
`global_deadline-now <= 180s`，不得启动任何新provider segment。每条chain冻结
`chain_deadline=min(chain_start+540s, global_deadline-180s)`；每次 CLI Run/reconnect segment 启动前，把
`chain_deadline-monotonic_now` 作为 frozen harness timeout。remaining非正时中止当前segment、保留原始terminal/process结果并seal，
不得启动后续provider；最后180秒只允许cleanup、publication与final scan。

| Chain | 唯一目标与 precondition | 最大 ordinary Runs / provider calls | 最大 compact operations / compactor calls | reconnect | wall cap | 自然 non-trigger disposition |
| --- | --- | --- | --- | --- | --- | --- |
| `replacement-reconnect` | R2 accepted previous business-risk baseline；R2 completed canonical FY2025 evidence；R3 pre-start target直接证明同boundary over-cap并accepted | `4 / 84 typed`（`100` external ceiling；含accepted后R4；R3前后正式阶段`3 / 63 typed`） | expected `2 / 10`；hard `7 / 35`（含R4） | target accepted后最多`1`次 | `540s` | baseline/target未accepted、boundary不完整或FY2025 provenance缺失：seal `needs-more-evidence`；不重跑 |
| `same-boundary-repair` | 仅在前链未观察“同一R3 target source-boundary digest至少一次reject后同operation accepted”时启动；同R1-R3 preconditions | `3 / 63 typed`（`75` external ceiling） | expected `2 / 10`；hard `5 / 25` | `0` | `540s` | target attempt1即accepted或5次耗尽：seal repair `needs-more-evidence`；不启新operation |
| `exhausted-fallback` | 仅在此前未观察5次R3 target rejection + `retry_repair_budget_exhausted` + deterministic fallback时启动；同R1-R3 preconditions | `3 / 63 typed`（`75` external ceiling） | expected `2 / 10`；hard `5 / 25` | `0` | `540s` | target在attempt1-5任一accepted：seal fallback `needs-more-evidence`；不启新operation |

执行顺序最小自适应：第一个fresh workspace slot先追replacement-reconnect；若R3自然 `reject -> accept`，repair同时覆盖；
若自然五次reject后fallback，fallback同时覆盖而replacement仍缺失。每个后续fresh workspace slot在启动前按execution index
选择尚未覆盖的唯一branch role；一条链自然覆盖了其它branch就跳过对应slot，但不得覆盖失败。最多三个
fresh workspace slots，每workspace只执行一条链，总wall cap仍为`1800s`；skip/assignment原因与canonical operation refs必须入index。

任一链发生以下情况立即停止并 seal，且不得继续后续 provider：合法 fixed-profile 下再次
`runner_candidate_invalid`、profile/config digest漂移、非MiMo provider、prerequisite Run非 succeeded、ordinary Run 请求了
非计划工具、任一reactive operation、call/operation/wall cap到达、unexpected setup mutation、immutable capture失败或增量
hygiene precheck失败。特别是再次
`runner_candidate_invalid` 时停止整个 B2
execution，重新开启 owner investigation，不用其它成功链掩盖。

### Mandatory assertions

- initial compactor input中的real caps与Host cap/usage audit同源；
- previous business-risk atom keep/omit明确，FY2025 EvidenceFact只有canonical provenance成立才可 accepted；unsupported百分比不升级；
- represented provenance与Host-owned omitted是同一 immutable boundary的exact complement；
- repair每次使用同一 source-boundary digest和完整replacement，不是patch；fallback只在5次预算耗尽后成立；
- accepted compact artifact/EventLog/Memory/RunInput/public Tool Trace同源；fresh reconnect消费同一 truth；
- screen、argv、keys、exit、files、logs、Tool Trace、SQLite typed before/after与所有失败均原样保存。

## Slice 3 — Publication、registry projection 与 closeout

### Public/private minimum

每条 attempted chain 的 private tree最少保存：profile/config/inventory digest、command/argv/cwd/env-key availability、actions/keys、
raw PTY、screen、process exit、run terminals、filesystem diff、脱敏完整日志、hot Tool Trace/canonical equality、typed artifact/EventLog/
Memory/RunInput/SQLite before-after与failure seal。private数据只供Controller交叉核验，不得补造public缺失字段。

全新B2 bundle的public tree最少保存（不得写入或复制覆盖B1 immutable tree）：

- `observed-behavior-report.md`：只报告B2，逐mandatory item写`observed`/`needs-more-evidence`；旧seq327单列
  `unresolved-residual/not-used-for-B2-mandatory-coverage`；
- `observation-summary.json`：B2-only per-chain budgets、actual counts、terminal refs与verdict；
- 每条执行链的 path-redacted public Host resolver/Tool Trace analysis JSON/Markdown；
- 每个accepted target的B2-only accepted-truth/equality projection；
- `execution-index.json`：workspace/config/harness identity、attempt/skip/seal reason与relative refs；
- `digest.json` 与最后的 `secret-scan.json`。

B2 publication writers先完成所有report/projection、最终`execution-index.json`与覆盖此前全部B2 public files且排除自引用/final scan的
`digest.json`；这些writer全部关闭后，tracked final-tree helper扫描整个evidence tree并独占创建唯一`secret-scan.json`。scan覆盖
digest及此前全部public files，要求exact-secret与absolute-private-path零命中。每条链结束后先对
本链新增 public candidates 做增量 hygiene precheck，通过后才允许下一次 provider；增量 precheck 不是最终 publication scan，
不得替代 final-tree helper。`secret-scan.json` 是public tree最后且唯一的helper write；之后任何写入都使publication失效并必须
从B2最终metadata/digest与final-tree scan重新执行。registry对B1继续引用immutable bundle/旧SHA，对B2只引用新bundle内的
relative path+digest；两者不得共享可写publication root或互相改写digest/scan。

### Registry/Oracle closeout

- 新B2 evidence refs可写入目标scenario的`observed_evidence`，但`status`、pending adjudication、applicable identity与overall
  readiness严格不变；任一自然分支缺失时`evidence_status`必须如实表示`needs-more-evidence`。
- observed behavior report交用户裁决，Agent不得接受B2。
- final closeout分别给出：implementation verdict；旧Trial2 diagnostic verdict（固定unresolved/no inference）；fresh fixed-profile
  real observation verdict；B1 Oracle accepted；B2 Oracle unadjudicated；overall non-ready。

## Validation

Plan/文档实现的确定性验证：

```bash
python -m json.tool docs/cli_ci_scenarios.json >/dev/null
python -m json.tool docs/cli_ci_oracles.json >/dev/null
git diff --check
rg -n 'tool-trace-formal|cap-constrained-memory-replacement|registry_status|readiness' \
  docs/cli_ci.md docs/cli_ci_oracles.json docs/cli_ci_scenarios.json \
  docs/reviews/pr-190-oracle-adjudication-20260808.md
```

Observation validation由execution index逐链断言：actual counts不超过matrix、profile digest全链相等、canonical terminals完整、
nontrigger/失败已seal、public digest与final scan有效。因为产品/schema/test/tooling零改动，本F18不运行pytest/pyright/Ruff/
compileall来冒充observation；若后续出现任何此类改动，视为越过plan并停止。

## README/docs decision、risks 与 completion format

- 不更新README：无已实现产品/public contract/test层变化。CLI CI handbook/adjudication更新属于本work unit状态真源。
- 未过度设计：使用现有profile、CLI、Host owner facts、harness和publication格式；没有新增动态profile、typed cause、analyzer或
  第二业务真源。
- residual：跨opener profile contract unspecified；旧seq327 cause不可恢复；cold analyzer/provider request id limitation；MiMo
  stochastic branch可能不触发。它们分别进入final closeout/residual question，不伪装成已修复。
- completion report固定包含：changed files、验证结果、三链预算/actual/skip/seal表、mandatory evidence逐项状态、public/private
  publication digest/scan、implementation verdict、旧diagnostic verdict、fresh observation verdict、B1/B2 Oracle status、overall
  readiness、residual owner/destination。

计划状态：`executed-needs-more/publication-nonconforming`；F18 已停止 provider observation。冻结 publication 中一个未标注
digest字段与其目标artifact不一致，`observation-summary.json`缺少本plan要求的per-chain budget/count/terminal refs，且缺少
per-attempt public Tool Trace/resolver analysis与`execution-index.json`；public tree不可回写。未覆盖的B2 mandatory evidence与
publication修正一并转交F19新work unit。
