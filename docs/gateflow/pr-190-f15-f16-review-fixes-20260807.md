# PR 190 F15 / F16 Review-Fix Gate

## Gate / scope

- Gate：implementation review `fix`。
- Binding plan：`docs/gateflow/pr-190-f15-f16-plan-acceptance-20260807.md`。
- Controller adjudication：`docs/gateflow/pr-190-f15-f16-implementation-review-adjudication-20260807.md`。
- Review inputs：
  - `docs/reviews/pr-190-f15-f16-implementation-review-mimo-20260807.md`；
  - `docs/reviews/pr-190-f15-f16-implementation-review-ds-20260807.md`。
- 未 commit、未 push、未创建或修改 PR。
- 未修改 oracle、scenario registry/predicate/expected behavior、prompt、Engine、F14 cumulative `compacted_source_refs` frontier 或 `run_transition` writer contract。

## First-principles / owner decision

finding 动机成立。P1 不是“temporary harness 偶尔慢”，而是确定性控制死锁：dependency stop/invalid 只跳当前 action，下一 dependent action仍等待不会出现的 terminal count，cleanup/EOT永远不可达。正确 owner 是 tracked harness-control pure decision与PTY orchestration consumer，而不是加长timeout或伪造terminal。

evidence owner同样明确：Host EventLog/shared lifecycle projector拥有terminal status/public-outbox语义；temporary consumer只汇总canonical facts。valid required non-succeeded是证据不足，不是canonical evidence破损；缺失、duplicate、shape/session错误才invalid。process exit、Run terminal、dependency gate和evidence integrity保持四个独立维度。

## Finding verdict / fix / test

| Finding | Verdict | Fix | Deterministic evidence |
|---|---|---|---|
| MiMo 001：segment evidence invalid后二次崩溃 | `accepted / 已修复` | `_segment_terminal_facts()`改为typed inspection；缺文件、malformed JSON、invalid shape保留原diagnostics与record path/digest，返回`invalid`并阻断依赖链，不静默变成`(0, false)`；segment loop捕获harness异常且继续生成final index。 | `test_required_run_evidence_distinguishes_complete_insufficient_and_invalid`锁定invalid分类；两个ignored harness已`py_compile`/pyright。 |
| MiMo 002：index字段不完整 | `accepted / 已修复` | index逐项/汇总输出`process_outcomes`、七类terminal counts、per-Run record path/digest、`dependency_gates`、strict context compaction count/refs、public evidence，以及分离的secret/path-hygiene scan；raw Host SQLite不进入public index/bundle。 | source/type validation；fresh real evidence实例按accepted plan仍留给clean committed target。 |
| MiMo 003：删除`INDEPENDENT` | `rejected-with-reason / contract已加固` | 保留复用role；新增pure `run_observation_role_for_harness_action()`，required/dependent/independent显式投影，cleanup不允许伪装Run role。 | `test_upstream_to_dependent_ordinal_and_independent_role_are_explicit`。 |
| DS 016：accepted-tool block构造重复 | `accepted-condition / 已修复` | 用`_CanonicalMaterialText`与`_AcceptedToolEvidenceText`两个typed wrapper组成union，进入唯一low-level constructor；exact wrapper在factory验证shared renderer，未使用dict/glue/bool/trusted string，也未把raw exact伪装成normalized canonical。 | `tests/host/test_compact_material.py`全文件通过，既有accepted evidence exact renderer tests保持通过。 |
| DS 017：whitespace boundary test缺口 | `accepted / 已修复` | typed candidate owner与strict persisted parser分别拒绝whitespace-only title；two-anchor projector exact断言`P3/P4`，证明不skip、不default、不renumber。 | `test_whitespace_only_candidate_anchor_is_rejected_at_typed_accept_boundary`、`test_whitespace_only_persisted_replacement_is_rejected_at_read_boundary`、`test_pre_dispatch_answer_anchor_format_matrix_keeps_packed_and_readable_exact`。 |
| DS 018：隐式ordinal `+1` | `accepted / 已修复` | upstream→紧邻dependent accepted ordinal集中为typed pure `dependent_action_accepted_ordinal()`；temporary role builder只消费helper并校验实际action ordinal。 | `test_upstream_to_dependent_ordinal_and_independent_role_are_explicit`。 |
| C01：dependency stop后PTY等1800秒 | `accepted / 已修复` | tracked `classify_remaining_actions_for_safe_stop()`逐项分类；PTY记录全部remaining dependent为`not_run`，不发送依赖输入，立即只发送一次显式EOT，并设置10秒cleanup exit deadline。 | `test_safe_stop_classifies_dependents_and_sends_one_cleanup_eot`；prompt/F14 harness `py_compile`与pyright通过。 |
| C02：valid failure误标complete | `accepted / 已修复` | pure evidence classifier规定：非空required statuses全succeeded才`complete`；valid failed/cancelled/lost为`insufficient`；canonical observation损坏或required acceptance/terminal缺失为`invalid`。后续segment显式`not_run`。 | `test_required_run_evidence_distinguishes_complete_insufficient_and_invalid`覆盖三态及混合terminal。 |
| C03：session与shared projector不同源 | `accepted / 已修复` | 强制`terminal.session_id == accepted.session_id`；terminal class复用`run_status_for_terminal_event()`，public-outbox复用`is_public_outbox_terminal_item_event()`，删除四分支与lost否定映射。 | `test_terminal_session_must_match_accepted_session`；`test_terminal_projection_keeps_each_canonical_terminal_and_reason`精确断言四status与public-outbox布尔序列。 |
| C04：formal status值错误 | `accepted / 已修复` | final index固定`oracle_status: "unadjudicated"`；没有accepted/ready/PASS/scenario success字段。 | source inspection、两份ignored harness pyright。 |
| C05：implementation artifact不实且SHA过期 | `accepted / 已修复` | implementation artifact已改为真实safe-stop/independent-evidence/index行为，并更新三个源码SHA。 | 本artifact下方SHA与`shasum -a 256`一致。 |

## Controller post-fix audit follow-up

| Finding | Verdict | Fix | Deterministic evidence |
|---|---|---|---|
| A：普通本机路径被当作secret exact probe | `accepted / 已修复` | tracked scanner的exact probes只接受实际secret环境值和有意义canary；repo/run/corpus路径不再进入credential probes。secret scan与path hygiene分别输出status/hits/violations。 | `test_public_scan_does_not_treat_ordinary_local_paths_as_secrets`以旧`command.json.cwd`同型repo/corpus绝对路径证明scan complete；`test_public_scan_detects_injected_exact_secret`证明实际secret fixture必命中并invalid。 |
| B：从diagnostic字符串反推missing/duplicate | `accepted / 已修复` | 删除全部`"missing"/"duplicate" in diagnostics` heuristic及duplicate summary字段。invalid observation只确定`invalid=1`，accepted/succeeded/failed/cancelled/lost/missing均为`null`，diagnostics原样保留；aggregate遇未知值也保持`null`。 | source inspection与ignored harness pyright；focused test suite保持invalid三态与diagnostics contract。 |
| C：summary只校验terminal总数 | `accepted / 已修复` | 新增tracked pure `validate_terminal_class_summary()` owner；对accepted数量及succeeded/failed/cancelled/lost四类per-Run分布逐项exact对账。F14 consumer把每条`terminal_class`先转为typed `RunStatus`再调用owner，矛盾即evidence invalid。 | `test_terminal_class_summary_requires_exact_per_run_distribution`覆盖“总数相同但failed/lost分布矛盾”反例及合法四类矩阵。 |
| D：raw DB排除被硬编码为false | `accepted / 已修复` | 删除public collection/final index中的硬编码`raw_host_sqlite*=false`。tracked path hygiene扫描整个拟公开evidence tree，按实际文件拒绝`.sqlite/.sqlite3/.db`、按文本拒绝raw database路径，并拒绝leaf/ancestor symlink；final index只引用scan record path，scan report独占secret/path-hygiene verdict真源。 | `test_public_path_hygiene_detects_raw_database_and_symlink`用真实`.sqlite3`文件、JSON内`host.db`路径和symlink同时证明invalid及三类typed reason。 |

## Final re-review findings follow-up

DeepSeek旧`PASS`未覆盖final publication scan时序，按Controller裁决为该问题上的证据失效；本轮不以其作为pass依据。

| Finding | Verdict | Fix | Deterministic evidence |
|---|---|---|---|
| MiMo 001-P2：prompt harness缺失final scan | `accepted / 已修复` | `run-completion.json`先落盘且只引用scan record path；tracked final-tree helper随后扫描含completion在内的整个evidence tree并独占写`secret-scan.json`。completion不复制尚未形成的status/digest。 | `test_final_publication_scan_covers_final_metadata_and_only_excludes_report`锁定completion/index descriptor coverage与唯一absent-report自排除；ignored harness `py_compile`/pyright通过。 |
| MiMo 002-P3：F14 index未被scan覆盖 | `accepted / 已修复` | 删除pre-scan与index中的scan status/digest；`evidence_status`只表达Run/context/tool collection，final index先写，再执行唯一final-tree scan。 | 同一owner test锁定index descriptor；source inspection锁定index仅保留record path且scan调用位于写index之后。 |
| Controller：report path traversal / resolved containment | `accepted / 已修复` | helper拒绝显式`..` traversal，并对`resolve(strict=False)`结果再次执行root containment；保留ancestor symlink检查和`open("x")`独占创建。 | `test_final_publication_scan_rejects_traversal_and_outside_report`覆盖lexical traversal、outside target及指向root内目录的symlink ancestor；stale report test证明不覆盖。 |
| Controller：final-tree既有fail-closed行为 | `accepted / 已修复` | final orchestration枚举全部regular file与symlink候选，复用唯一secret/path-hygiene scanner。 | `test_final_publication_scan_enumerates_secret_database_and_symlink_candidates`及`test_public_path_hygiene_detects_raw_database_and_symlink`锁定secret、raw DB/path、leaf/ancestor symlink。 |

## Fresh real-evidence review-fix follow-up

fresh run `/Users/leo/workspace/.dayu-cli-ci/f15-f16-postfix-SfyZRB98` 推翻了此前仅由deterministic fixture支持的
publication completion：canonical terminal summary为`accepted=28/succeeded=28`，context compaction observation为
`accepted_count=2`，collection evidence为`complete`；但final scan为`invalid`，secret scan为`complete/hits=0`，
path hygiene有22个`raw_database_path_forbidden`。其中21个来自filesystem before/after/diff及其execution-index嵌入，
另1个来自workspace-mode Tool Trace报告中的`input.hot_db_path`与payload evidence `source_path`。

| Finding | Verdict | Owner fix | Evidence |
|---|---|---|---|
| filesystem snapshot发布raw DB路径 | `accepted / 已修复` | tracked helper新增唯一typed `PublicEvidencePathClassification`与`classify_public_evidence_path()`；`.sqlite/.sqlite3/.db`主库及`-wal/-shm` sidecar共用同一regex/classifier truth。final scanner与snapshot producer复用该函数；snapshot在生成before/after key或symlink target时排除raw DB路径，diff/index只从已过滤snapshot派生。独立只读SQLite projection保持不变。 | owner矩阵覆盖三类主库、全部WAL/SHM、嵌入文本与普通路径反例；final sanitized bundle test得到`status=complete`。 |
| workspace hot Tool Trace泄露DB source path | `accepted / 已修复` | public collector仍调用production `dayu-cli tool_trace analyze`，但输入改为canonical `tool-trace-cold.jsonl`；未修改产品analyzer/schema，未删除报告字段或做字符串替换。2次context compact继续由独立只读EventLog projection提供。 | 对同一fresh workspace手工production分析：cold报告保留28 runs、10 findings、9 tool calls、20 payload rankings与相同signal coverage，`hot=false`且无`.db`路径；resolved candidate direct scan和完整final-tree scan均`complete`、path violations/validation errors均为空。 |
| cold collector首次手工scan仍invalid | `accepted clarification / 已闭环` | 首次direct call传入lexical `/var/...` candidate，而root被canonicalize为`/private/var/...`；scanner精确返回唯一`outside_evidence_root`，`validation_errors=[]`。snapshot断言已在该失败前全部通过。保持scanner containment fail-closed，不放宽规则；改用final orchestrator实际采用的resolved root/candidate复验。 | non-canonical direct scan：`invalid` + exact `outside_evidence_root`；resolved direct scan与full final orchestrator：`complete`、`violations=[]`、`validation_errors=[]`。 |

## Controller minimal P3 adjudication

| Finding | Verdict | Owner fix / reason | Deterministic evidence |
|---|---|---|---|
| DeepSeek P3-02：raw DB regex边界缺少花括号 | `accepted / 已修复` | final scanner扫描任意可解码public evidence文本，包括非JSON stdout/stderr，因此“当前JSON-only”前提不成立。只扩展唯一`_RAW_DATABASE_PATH_PATTERN`的左右boundary以接受`{`/`}`；classifier、scanner与snapshot producer继续复用同一真源，未复制regex或suffix，未放宽fail-closed。 | `test_public_evidence_path_classifier_rejects_brace_wrapped_database_paths`覆盖花括号包围的main、WAL、SHM嵌入文本；既有`test_public_evidence_path_classifier_keeps_ordinary_paths`反例继续为`PUBLISHABLE`。 |
| DeepSeek P3-01：解析symlink chain | `rejected-with-reason / 不修` | snapshot解析chain会把已排除target的语义反向发布到public evidence；final bundle已对任意symlink fail closed，本finding仅涉及审计表现，不成立为本gate correctness修复。 | 既有final-tree symlink owner tests保持通过；未修改ignored harness控制流。 |
| SQLite `-journal`扩展 | `rejected-with-reason / 不扩` | Host durable强制WAL，且本目标显式限定main/WAL/SHM；扩展journal会改变已确认scope，不属于P3-02 owner boundary修复。 | Host/F14 frontier、formal Oracle与scanner其他分类均zero semantic change。 |

## Source digests

- tracked helper `utils/cli_ci_run_observation.py`：`92047be5098f312c962f76f339d41f45d685d7790556d326cdbba0c017176869`；
- ignored prompt harness `workspace/tmp/prompt_observe_calibration.py`：`7e32637439c8625b6ee78ec65484f5b4e54e98cf199f65dd50d965a1739b1223`；
- ignored F14 harness `workspace/tmp/f14_real_cli_observation.py`：`236251ca3a035c435080ccb9f91b3be21faaa2b24e31003b041d207327b4636d`。

## Changed files / docs decision

- Production/helper：`dayu/host/compact_material.py`、`utils/cli_ci_run_observation.py`；fresh follow-up只改后者的唯一typed raw DB path classification/scanner复用。
- Tests：`tests/cli/test_cli_ci_run_observation.py`、`tests/host/test_compact_material.py`、`tests/host/test_context_compact_events.py`，并保留implementation gate已有terminal producer/freeze-dispatch测试改动；fresh follow-up新增main/WAL/SHM、花括号包围的非JSON嵌入文本、普通路径与complete bundle owner矩阵。
- Ignored temporary consumers：`workspace/tmp/prompt_observe_calibration.py`在snapshot producer过滤raw DB path；`workspace/tmp/f14_real_cli_observation.py`以canonical cold JSONL驱动production Tool Trace。
- Docs：`docs/cli_ci.md`同步index三态、safe-stop、session/shared projector、唯一final-tree scan时序、report containment、snapshot/cold Tool Trace owner与`unadjudicated`；`tests/README.md`同步focused owner测试；implementation artifact同步真实控制流与SHA。
- `dayu/host/README.md` / `docs/host/design.md`沿用implementation gate的F15 canonical pair owner说明；本fix未改变该稳定contract。
- 根README、`dayu/README.md`与Engine README不更新：CLI public surface、分层装配与Engine contract未变化。

## Validation

1. Review-fix follow-up tracked helper矩阵：`27 passed`，包含A/C/D反例与actual-secret fixture；相关Host focused aggregate：`122 passed`。
2. F15 compact/context、F16 helper与ordinary freeze/dispatch aggregate：`217 passed`。
3. succeeded/failed/cancelled/lost producer exact reason focused nodes：`6 passed`。
4. `coverage run ...` + `coverage report -m dayu/host/compact_material.py`：`1142 statements / 171 missed / 85%`，达到单文件>=80%。
5. `python -m py_compile workspace/tmp/prompt_observe_calibration.py workspace/tmp/f14_real_cli_observation.py`：通过。
6. `python -m pyright workspace/tmp/prompt_observe_calibration.py workspace/tmp/f14_real_cli_observation.py`：`0 errors, 0 warnings`。
7. `python -m pyright dayu/ tests/ utils/`：`0 errors, 0 warnings`。
8. `git diff --check`：通过。
9. 禁改面审计：`dayu/host/durable/run_transition.py`、oracle/scenario files、prompt、Engine均zero diff；F14 `compacted_source_refs`实现zero diff。
10. final re-review fix tracked helper：`31 passed`，覆盖final completion/index descriptors、唯一absent-report自排除、stale report、traversal/outside containment及final-tree secret/raw DB/symlink枚举。
11. handbook focused aggregate：`126 passed`。
12. 两个ignored harness final `py_compile`通过且pyright为`0 errors, 0 warnings`；全量`python -m pyright dayu/ tests/ utils/`再次为`0 errors, 0 warnings`。
13. final `git diff --check`与禁改面审计通过；三份`shasum -a 256`与本artifact记录一致。
14. fresh real-evidence follow-up owner suite：`55 passed`，tracked helper单文件coverage沿用`82%`；handbook focused aggregate：`150 passed`。
15. cold collector手工闭环：non-canonical direct scan精确为`outside_evidence_root`且无validation error；resolved direct scan与完整final-tree scan均`complete`、无path violation/validation error。
16. follow-up两个ignored harness `py_compile`通过、定向pyright与全量`python -m pyright dayu/ tests/ utils/`均`0 errors, 0 warnings`；changed-code `ruff check`与`git diff --check`通过，未运行全量formatter。
17. Controller minimal P3-02 fix：brace main/WAL/SHM与既有raw/ordinary owner矩阵定向`17 passed`；完整owner suite `55 passed`、focused aggregate `150 passed`；changed Python Ruff与全量pyright通过，final diff-check通过。

## Residual risks / uncovered areas

- `assigned to subsequent accepted clean-target validation gate / Controller`：本gate审计了Controller提供的pre-fix fresh provider/AAPL实例并以其22个path violations定位owner；尚未对当前未提交修复执行新的provider/AAPL rerun。accepted plan仍要求正式复跑只针对clean committed target，因此本gate只提供deterministic final-bundle与同一cold JSONL production analyzer闭环，不把它表述成post-fix real-evidence completion。
- `assigned to independent final re-review gate / Controller`：MiMo final re-review的001/002与Controller traversal补充已修复；本轮fix后仍需独立复验，未通过前不得进入commit或real rerun。
- `covered by current deterministic suite`：F15 strict pair、accepted tool exact path、durable reopen、ordinary candidate freeze/dispatch、terminal producer reason、evidence三态与safe-stop pure control。
- `rejected-with-reason / no scope expansion`：P3-01不解析symlink chain；SQLite `-journal`不进入显式main/WAL/SHM目标。两项均不作为本gate residual correctness risk。
- 没有unclassified residual risk；formal financial/business Oracle保持`unadjudicated`。

## Completion status / next entry point

- Controller minimal P3-02 review-fix gate：complete，唯一accepted finding有owner fix/test证据，P3-01与journal均按Controller边界明确不修。
- Artifact：`docs/gateflow/pr-190-f15-f16-review-fixes-20260807.md`。
- Next Gateflow entry point：等待Controller复验本次最小P3-02 fix；本执行者按用户约束停在fix gate，不commit、不push、不操作PR。

clean committed target通过independent re-review后，post-fix复跑入口为：

```bash
fresh_root="$(mktemp -d /Users/leo/workspace/.dayu-cli-ci/f15-f16-postfix-rerun-XXXXXXXX)"
PYTHONPATH=/Users/leo/workspace/dayu-agent-r /Users/leo/workspace/dayu-agent-r/.venv/bin/python workspace/tmp/f14_real_cli_observation.py --run-root "$fresh_root" --repo-root /Users/leo/workspace/dayu-agent-r --cli /Users/leo/workspace/dayu-agent-r/.venv/bin/dayu-cli
```
