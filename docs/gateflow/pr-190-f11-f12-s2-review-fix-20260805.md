# PR 190 F11/F12 S2 Review Fix

## Gate metadata

- Gate：`code review -> fix`
- Work unit：PR 190 F11/F12 Interactive Memory 收口
- Slice：S2 — Engine generic structured output 与 config capability
- Baseline HEAD：`c8be3e5184b8b797c59458027e991f0284cbb3b5`
- Artifact path：`docs/gateflow/pr-190-f11-f12-s2-review-fix-20260805.md`
- Implementer：AgentCodex（本轮唯一 implementation/fix owner）
- Completion status：**review-fix implementation complete；re-review pending**
- Git state：未 stage、commit、push；未启动 reviewer 或子 Agent
- Next entry point：由总控派发独立 re-review

## Scope and owner judgment

三项问题均由直接代码、冻结 plan/design 与稳定测试事实证明成立，但边界不同：

- `build_request_payload` 是 OpenAI-compatible payload 构造 owner；`None` 是合法值，
  但 default 会让新增 caller 的漏传绕过静态检查。因此移除 default 是 contract 修复，
  不是改变 `None` 业务语义。
- runtime config enum 与 Engine enum 因依赖方向必须各自存在；共同值域的机械映射 owner
  是 Service 装配边界。因此只在 Service owner test 锁定完整集合与逐值构造，不让
  runtime import Engine，也不增加第三份 enum 或 production helper。
- 冻结 plan 与 `docs/host/design.md` 明确 Host Tool Trace 必须复用 Engine canonical
  `SuccessfulRunnerResponseIdentity`。稳定失败的 owner 是 import-boundary test policy；
  不能复制 identity 类型或删除合法 import 来迁就测试。

本 fix gate 不扩张 structured-output schema、Host Tool Trace production contract、provider
行为或 S3/S4 scope。

## Reviewer interference record

原 MiMo review 执行了 `git stash` / `git stash pop`，违反只读 review 约束，并让并行
DeepSeek 在短暂 HEAD-only 工作树中读取到旧 import/hash 状态。按 controller adjudication：

- stash 窗口内的 import、hash 与 baseline test 观察全部作废；
- 当前 S2 工作树已恢复，未触碰唯一不相关 stash；
- 稳定 raw-byte SHA-256 为：
  - `dayu/config/models.json`：
    `dc924f842be81599c00606dae0cbe464d6f766147581d4bbb4167e83044e5f2b`
  - `docs/cli_init_workspace_manifest_v1.json`：
    `0e1ec1047062eecbe6dc8eae89139460058219c881ce4d7960e6c96c7a182469`

本轮没有执行任何 Git state mutation，也没有派发 reviewer。

## Finding closure

| Finding | 状态 | Owner-level closure |
| --- | --- | --- |
| `DS-LOW-01` | 已修复 | `build_request_payload.structured_output` 保持 `StructuredOutputRequest \| None`，移除 default；production 与 direct test callers 全部显式传参；signature owner test 断言 required keyword-only |
| `DS-LOW-02` | 已修复 | Service owner test 比较 runtime/Engine enum 的完整 value 集合，并逐个 runtime value 机械构造 Engine enum；无 production helper、第三 enum 或反向依赖 |
| S1 deterministic import-boundary regression | 已修复 | policy 改用精确 workspace-relative paths；保留全部原允许文件，只精确新增 `dayu/host/durable/tool_trace.py` 与 `dayu/host/tool_trace_analysis_contracts.py` |

## Incremental changed files

- `dayu/engine/runners/openai/payload.py`
- `tests/engine/runners/openai/test_no_extra_payload_bag.py`
- `tests/engine/runners/openai/test_payload_build.py`
- `tests/engine/runners/openai/test_stream_usage_capability_gating.py`
- `tests/engine/runners/openai/test_payload_assistant_reasoning_content_preserved.py`
- `tests/service/test_host_assembly.py`
- `tests/host/test_import_boundary.py`
- `docs/gateflow/pr-190-f11-f12-s2-structured-output-implementation-20260805.md`
- `docs/gateflow/pr-190-f11-f12-s2-review-fix-20260805.md`

没有修改 Host Tool Trace production types/imports，没有新增 runtime/Engine enum，也没有
改变 config publication bytes。

## Validation

### Required Host regression nodes

```bash
pytest -q \
  tests/host/test_active_cancel_dispatch.py::test_cancel_session_replay_after_watchdog_does_not_append_or_propagate \
  tests/host/test_import_boundary.py::test_host_engine_imports_stay_on_allowed_boundary_modules
```

结果：`2 passed`。

### Owner and affected suites

- signature、Service enum boundary 与 payload focused tests：`35 passed, 3 warnings`。
- 局部 payload/runner pyright：`0 errors, 0 warnings, 0 informations`。
- 当前全部 modified test files 的 S2 focused/affected suite：稳定复跑
  `1361 passed, 3 warnings`。
- full `tests/engine`：`602 passed`。
- `pyright dayu tests utils`：PASS，exit 0。

Affected suite 首轮曾在
`test_open_host_active_cancel_watchdog_public_watch_observes_cancelled` 观察到 cancel token
recorder 两次调用。直接 diff 证明该文件的 S2 变化只有
`RunnerSpec.structured_output_capability=NONE` 机械迁移，与失败事实不同源；该 node 隔离
复跑 PASS，随后完整 affected suite 稳定 1361 PASS。因此该非稳定观察不构成本 fix
finding，也没有据此修改 Host cancel owner。

三个 warning 均来自 `edgar` dependency 的既有 deprecation warning。

### Static, publication and integrity checks

- Ruff（全部 modified Python files + 新 structured-output module）：PASS。
- `python -m compileall -q dayu tests utils`：PASS。
- `jq -e` 校验 models catalog 与 workspace manifest：PASS。
- raw-byte SHA-256：与上文稳定值一致。
- `git diff --check`：PASS。

### Branch coverage for affected production files

| File | Branch coverage |
| --- | ---: |
| `dayu/engine/__init__.py` | 100% |
| `dayu/engine/agent.py` | 88% |
| `dayu/engine/contracts/__init__.py` | 100% |
| `dayu/engine/contracts/agent_run.py` | 98% |
| `dayu/engine/contracts/runner.py` | 100% |
| `dayu/engine/contracts/runner_spec.py` | 98% |
| `dayu/engine/contracts/structured_output.py` | 82% |
| `dayu/engine/runners/openai/payload.py` | 83% |
| `dayu/engine/runners/openai/runner.py` | 80% |
| `dayu/host/_execution_config_projection.py` | 91% |
| `dayu/runtime/config_loader.py` | 95% |
| `dayu/service/host_assembly.py` | 93% |

Coverage report total：90%；全部受影响 production files 均 `>=80%`。

## README decision

本 fix 不改变 S2 已写入 `dayu/engine/README.md`、`dayu/config/README.md` 与
`dayu/README.md` 的公开 contract，也不改变用户工作流，故不追加 README。已读取
`tests/README.md`；新增 owner assertions 未改变测试分层、运行入口或 fixture system，
故不更新。

## Residual risks and uncovered areas

- `fixed in current review-fix gate`：`DS-LOW-01`、`DS-LOW-02`、S1 deterministic
  import-boundary regression。
- `covered by later approved S3`：Host compact v3 schema/prompt/parser 与 capability-based
  compactor request selection。
- `covered by later approved S4`：真实 MiMo/DeepSeek endpoint/model/options conformance。
- re-review 尚未执行，派发与裁决 owner 为总控；这不是 implementation agent 可代签的
  acceptance。
- 首轮 watchdog 单次时序观察已被隔离复跑与完整稳定复跑否定为稳定回归，不列为
  residual product risk。
- 没有 unclassified residual risk，也没有需要本 fix gate 新建 issue 的事项。

## Stop state

Fix implementation 与 required validation 已完成。AgentCodex 在此停止 idle；不 stage、
commit、push，不启动 reviewer，等待总控进入 re-review gate。
