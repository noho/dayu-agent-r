# PR 190 F18 Plan-fix Artifact

## Gate、scope 与 reviewed inputs

- Gate：`plan-review -> fix`。
- Work unit：F18。
- Revised target：`docs/gateflow/pr-190-f18-plan-20260808.md`。
- Binding amendment/adjudication：
  `docs/gateflow/pr-190-f18-goal-confirmation-amendment-20260808.md`、
  `docs/gateflow/pr-190-f18-fixed-profile-path-adjudication-20260808.md`、
  `docs/gateflow/pr-190-f18-plan-third-rereview-adjudication-20260808.md`。
- Reviews：`docs/reviews/plan-review-20260808-145152.md`、
  `docs/reviews/plan-review-20260808-145239.md`、
  `docs/reviews/plan-review-20260808-150155.md`、
  `docs/reviews/plan-review-20260808-150209.md`、
  `docs/reviews/plan-review-20260808-155504.md`、
  `docs/reviews/plan-review-20260808-155519.md`。
- Changed files仅为本artifact与revised plan；未修改产品、schema、测试、tooling、registry、handbook或README，未运行provider、
  commit或push，未触碰ownership不明review artifact。

## Finding resolution（第一轮，已被第二轮参数裁决 supersede 的历史）

| Finding | 状态 | Plan fix |
| --- | --- | --- |
| DS-F18-PR01 / MIMO-F18-PR-001 / F18-PA-01 | 已修复 | 删除“跨opener illegal”预判；明确当前contract为`unspecified`，fixed-profile只证明正式scenario，不裁决旧transition；未来支持/拒绝需新goal/design gate。 |
| DS-F18-PR02 / MIMO-F18-PR-002 / F18-PA-02 | 已修复 | 删除sequence324/326 exact restore、synthetic replay与root hard gate；旧cause固定为不可恢复、no inference residual；direct candidate实验只保留反证边界。 |
| DS-F18-PR03 / F18-PA-03 | 已修复 | 工具真源改为sequence325 `effective_tool_set`；明确失败发生在Attempt/runner manifest前，不引用不存在的manifest。 |
| MIMO-F18-PR-003 / F18-PA-03 | 已修复 | 冻结B1 report/summary/digest/secret-scan四个public digest及B1-only refs；给出scenario字段级projection和B2/readiness不变量。 |
| DS-F18-PR04 / MIMO-F18-PR-004 / F18-PA-04 | 已修复 | 旧“blocked observation”被最新amendment supersede；新增可执行三链matrix、PTY Run state machine、direct-evidence预算、dependency/stop/seal与publication contract。 |
| DS-F18-RC01 / MIMO-F18-POINT-001 | 已修复 | 引用binding amendment，旧seq327不再是B2前置；final closeout要求旧diagnostic/fresh observation/B1-B2分别verdict。 |
| DS-F18-RC02 / MIMO-F18-POINT-002 | 已修复 | replacement先在同一constrained policy形成accepted FY2024 previous atom；FY2025 R8后完成R9-R12四个turn，R13 initial input直接证明floor退出与同boundary over-cap。 |
| DS-F18-RC03 / MIMO-F18-POINT-003 | 已修复 | Host每operation 5 attempts + Trial1/Trial2实际call path推导`14/29/2/10/1`和`13/28/2/10/0`硬预算；600秒到cap即停，不承诺自然branch。 |
| MIMO-F18-POINT-004 | 已修复 | 冻结public最小文件、private raw owner、B2-only projection；digest+secret/path scan为最后writer，private SQLite不得补public字段。 |

## Budget derivation and scope judgment（第一轮历史，不再是当前执行预算）

- Trial2直接数据：第1个FY2024 tool Run 10 ordinary calls；第2-6与第7 post-compact各1；第7 Run accepted baseline；第8个
  FY2025 tool Run 7；其后只有第9/10 completed turns。floor=4要求再完成R11/R12，目标只能在R13；replacement reconnect增加R14。
- Trial1直接数据：一个target operation有5个rejected compactor responses后budget-exhausted fallback；Host profile同样规定每
  operation最多5 attempts。因此两operation最多10 compactor calls。
- Trial2 11 Runs端到端约358秒；三条候选链使用registry 1800秒总cap，各600秒。它是hard stop，不是完成保证。
- 最小自适应执行：replacement自然覆盖repair或fallback时跳过对应独立链；只运行缺失branch，总体不超过三链/1800秒。

## Validation、docs decision 与 residual risks

- Validation：对两个plan artifacts执行`git diff --check`及内容检索；未运行与plan-only change无关的产品测试。
- Docs decision：本gate只修Gateflow artifacts，无README触发。
- Residual risks：
  - 旧seq327 owner cause不可恢复：保留F18 closeout residual，不能由新chain关闭；
  - cross-opener profile contract unspecified：未来若需定义，必须新goal/design gate；
  - MiMo自然不触发repair/fallback：对应evidence为`needs-more-evidence`，B2继续unadjudicated；
  - 任一合法fixed-profile chain再次`runner_candidate_invalid`：停止所有后续provider并重开owner investigation；
  - cold analyzer/provider-native request id limitation：只属于B1 residual question。

## Completion status

Plan-fix：`completed`。

Next entry point：AgentMiMo与AgentDS两路独立plan re-review。re-review通过前不得implementation或调用真实provider。

Artifact path：`docs/gateflow/pr-190-f18-plan-fix-20260808.md`。

## 第二轮 re-review fix（2026-08-08）

Controller 根据 `docs/gateflow/pr-190-f18-plan-rereview-adjudication-20260808.md` 对同一 plan 做机械收束：

- memory recent-turn floor 从 `4` 改为合法 typed value `0`，消除 unsupported 百分比与新 FY2025 evidence 被 recent floor
  排除的时序错误；当前输入继续是独立 source anchor，不混同 completed turns；
- soft ratio 从 `0.001` 改为 `0.02`。production effective MiMo context window `1,048,576` 派生 threshold `20,971`；
  owner 数据 `20,316 < 20,971 < 20,998` 与五阶段矩阵中 continuation pressure 始终 allow，直接证明存在 baseline 后再
  proactive compact 的可行带；该校准不保证 stochastic ordinal；
- 删除固定 R1-R14，改成 Phase A（一个 FY2024 tool Run + 最多三个 retention Runs直到首次 accepted baseline）与 Phase B
  （最多一个 FY2025 tool Run + 一个 target Run）的条件式状态机；缺 boundary/自然不触发均原样 seal；
- 每链最多六个 ordinary Runs、两个 compact operations、每 operation 五 attempts；`max_iterations=24` 派生普通调用上界
  `144`，replacement 的一次 reconnect 派生 `168`。三链各 `540s`，总 provider cap `1620s`，为 finalization 预留
  `180s`；历史 29/28 只保留为校准事实，不再冒充 hard cap；
- B1 raw PTY `execution_outcome="error"` 与 `exit_codes=[1]` 原样记录，canonical Run success 与 Oracle conformance 独立；
- publication writers 与 tracked final-tree helper 按现有 contract 分权；逐链使用增量 hygiene precheck，最终唯一
  `secret-scan.json` 由 helper 在所有 public artifact/digest 完成后独占生成；
- B1 report 使用整份相对路径与冻结 digest，section 由现存标题和 `observation-summary.json#b1` 定位，不发明未验证 anchor；
- 合法 fixed-profile 链再次出现 `runner_candidate_invalid` 时停止整个 B2，seal 后重新进入 owner investigation。

第二轮 fix 后状态：`ready-for-plan-re-review`；仍未运行 provider、未修改产品、未 commit/push。

### 第二轮 finding resolution

| Finding | 状态 | 第二轮 plan fix |
| --- | --- | --- |
| `F18-RRA-01` / `MIMO-F18-RR-001` / `MIMO-F18-RR-002` / `F18-DS-RR-001` | 已修复 | floor=0、ratio=0.02、typed feasibility band与conditional Phase A/B替代固定ordinal；首provider前持久保存calibration/profile/selection truth。 |
| `F18-RRA-02` / `MIMO-F18-RR-003` / `F18-DS-RR-002` | 已修复 | expected `21(+1)`只作逐terminal checkpoint；hard `144/168`由typed max_iterations派生；monotonic allocator冻结540秒chain deadline并保留180秒final reserve。 |
| `F18-RRA-03` / `F18-DS-RR-003` | 已修复 | B1 raw `exit=1`投影为`execution_outcome=error`，与canonical Run/Oracle字段分离。 |
| `F18-RRA-04` / `MIMO-F18-RR-004` / `F18-DS-RR-004` | 已修复 | publication writers先完成report/index/digest且不得创建scan；tracked final-tree helper只且独占创建唯一`secret-scan.json`；逐链只做incremental hygiene precheck，final scan后零写入。 |

补充边界：FY2025 tool Run取得真实工具结果后的 reactive target与下一no-tool Run pre-start target都合法，均须在同一initial input
直接证明四项mandatory material；缺任一项即seal。第一轮finding/budget段仅保留审计历史，已由本节与当前revised plan supersede。

## 第三轮 re-review fix（2026-08-08）

本节按 `docs/gateflow/pr-190-f18-plan-third-rereview-adjudication-20260808.md` 取代第二轮的 ratio、phase 与 budget
设计；前两轮只作审计历史。

| Finding | 状态 | 第三轮 plan fix |
| --- | --- | --- |
| `F18-TRRA-01` / `F18-DS-3R-001` / `MIMO-F18-RR3-001` | 已修复 | Host pressure只读`predicted_input_tokens`；ratio=`0.018`、threshold=`18,874`；校准仅声明`9,048 < 18,874 < 20,363`可行带，不使用conservative field或承诺fresh ordinal。 |
| `F18-TRRA-02` / `F18-DS-3R-002` / `MIMO-F18-RR3-004` | 已修复 | R1只取FY2024；R2 pre-start proactive baseline只取completed FY2024，accepted后同Run continuation取FY2025；删除reactive target，R3 pre-start为唯一target。 |
| `F18-TRRA-03` / `F18-DS-3R-003` / `MIMO-F18-RR3-003` | 已修复 | reactive cap冻结为1；expected compaction=`2/10`，正式hard=`5/25`，含R4 reconnect hard=`7/35`；按operation记录proactive/reactive与attempts，任一reactive都non-covering seal。 |
| `F18-TRRA-04` / `F18-DS-3R-004` / `MIMO-F18-RR3-002` | 已修复 | ordinary owner按24 loop calls + 1 force-answer计算；expected=`18(+1)`，正式hard=`75`，含R4 hard=`100`，每terminal继续执行expected checkpoint。 |

附加 role 边界：R3 replacement/repair以accepted为完成；exhausted-fallback以exact 5 rejects、budget exhausted与
deterministic fallback为完成。R4任何proactive/reactive compaction均non-covering seal。最多三个fresh workspace slots可依
execution index追踪仍缺branch，不重跑同workspace。wall/publication/global stop、B1/B2/readiness与旧seq327 residual不变。

Publication identity补充：B1 `pr190-formal-g1a2xu/evidence/public`为immutable read-only tree；B2在首次writer前冻结全新bundle id
与独立public/private root，只在新root生成B2 report/index/digest/scan。两套registry refs/digests分离，禁止B2 finalization使B1
冻结SHA失效。

第三轮 fix 后状态：`ready-for-plan-re-review`；未运行provider，未修改产品，未commit/push。
