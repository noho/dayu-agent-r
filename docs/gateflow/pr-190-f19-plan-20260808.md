# PR 190 F19 execution and publication plan（2026-08-08）

## Authority and scope

Goal owner：`docs/gateflow/pr-190-f19-goal-confirmation-20260808.md`。F19只继续
`interactive.interactive.g06.cap-constrained-memory-replacement@1`真实观察并修复F18 publication contract；不修改产品、
registry/oracle状态或F18 bundle。B2在用户裁决前保持`unadjudicated`，overall readiness不得标ready。

Material setup直接复用F18 MiMo/DS双PASS结论：同一真实AAPL文档先grounding；R1只读`s_0003`（68,039 chars，目标财务值
与21.7/18.2均absent），R2只读`s_0013`（61,252 chars，含416,161/133,050且21.7/18.2 absent）。禁止padding、换文档、
动态profile或非计划工具。

## Pre-provider gate

本work unit只实现run-owned external driver `workspace/tmp/f19_formal_driver.py`与R1-R4 prompt files。driver以prompt-file bytes为
唯一输入owner，执行strict UTF-8、NUL absent、strip后non-empty校验，再把完整prompt作为冻结harness argv中唯一`--prompt`后的
单个元素。`--preflight-only`只输出stdout，不接受文件输出、不创建任何父目录，不要求/创建allocation或deadline，也不spawn
harness/CLI/provider；empty prompt必须在该边界fail closed。

冻结identity：target product commit `ce0c171a022a073c6355ace44e7c5e34a668d4bb`；production CLI SHA
`b44f70ec0117c7aef44ea7ff56d559ed8be9c8fd8b380855f4169a399d7f7cae`；formal harness SHA
`28e4fd691f9f5198a4cbdc2ef645b797c552af88d5fc6e32df7ed13449dd9424`；PTY owner SHA
`7e32637439c8625b6ee78ec65484f5b4e54e98cf199f65dd50d965a1739b1223`；唯一允许model id
`mimo-v2.5-pro-plan`。driver在preflight与每次real spawn前直接验证detached HEAD/clean、CLI file SHA、model、harness/owner SHA；
错误或不存在identity必须在argv spawn前fail closed。

MiMo/DS必须独立review plan、driver、prompt与nonempty/empty self-test。双PASS前不得创建formal bundle、冻结deadline或调用provider。
首轮独立review `plan-review-20260808-180355.md` / `plan-review-20260808-180510.md` 均为FAIL；本修订逐项处理其共同finding：
execution identity、strict deadline/run/chain binding、real projection持久化、三链allocation、segment ordinal/fresh typed state与
preflight零目录创建。首轮FAIL保留，不覆盖；修订后必须重新取得两路独立二值PASS。
RR2 `plan-review-20260808-182447.md` / `plan-review-20260808-182815.md` 仍均为FAIL：前述共同finding已关闭，但分别发现
run-root/evidence相对布局未绑定，以及raw Run rows/R4 accepted compact前置不构成canonical证明。本修订继续只收紧external driver
owner；RR2 FAIL同样保留。RR3 `plan-review-20260808-184346.md` / `plan-review-20260808-184441.md` 仍均为FAIL：两路均确认
typed canonical Run/R4 finding及首轮finding已经关闭，唯一新finding是run root未绑定run tree外的可信parent且ancestor symlink可被
`resolve()`掩盖。Controller接受该finding；RR3 FAIL继续原样保留，本版只修此owner边界并进入RR4双路复审。
RR4 `plan-review-20260808-190541.md` 为PASS，`plan-review-20260808-190759.md` 为FAIL，因此该轮整体未通过：DS直接证明
frozen harness实际导入的PTY owner sibling遗漏于path gate，root外同bytes symlink可通过SHA校验并到达emit/spawn。Controller接受该
同owner finding；MiMo PASS与DS FAIL均原样保留，本版只把该executable sibling纳入既有path gate并进入RR5双路复审。

## Fresh setup and budgets

双PASS后，Controller在run tree外拥有一个machine-local、精确且已存在的canonical runs-parent；其私有绝对identity只冻结在
ignored driver，不接受caller参数、allocation/deadline或run tree内文件自证，public artifact只记录path-redacted owner identity。
在该parent的唯一direct child创建basename以`pr190-f19-b2-`开头且等于`run_id/run_root_id`的新F19 run root；只复制F18 clean seed的immutable
config/corpus文件，不复制`.dayu`、EventLog、Memory、artifact、数据库
或任何attempt state。三个formal workspace从同一seed分别独立创建，首次opener前必须无`.dayu`，config/profile/corpus/input
digests exact equal。production assembly再次保存effective provider/model、context threshold、memory caps、五阶段矩阵与
effective `max_iterations=20` source=`scene_override`；typed hard cap为21 calls/Run，25仅external ceiling。

Controller先独占冻结`pr190.f19.chain_allocation.v1`：run/run-root/target/CLI/model/profile identity与恰好三条
`formal-chain-01..03`，ordinal为1..3；每条entry绑定同名workspace、固定label、R1-R4 sequence及相等的immutable input-tree SHA。
driver每段复算当前workspace排除`.dayu`的input-tree SHA；第四链、错workspace/label、重复/缺失entry或input drift均fail closed。
real mode还强制精确布局：workspace=`<root>/workspaces/<chain>`，attempt=`<root>/evidence/private/<chain>/<segment>`，wrapper
projection为同attempt parent固定sibling，allocation/global/chain deadline、run-owned harness与R1-R4 prompt均位于
`<root>/evidence/private/controller`的固定文件名；harness直接导入的frozen PTY owner也必须是该controller目录中的固定regular-file
sibling。driver通过唯一path helper同时向path gate与execution SHA owner投影该sibling。在读取任何run-owned executable/prompt/JSON及
任何写入/spawn前，从filesystem root到可信parent、
再到root和每个run-owned path逐个对既有lexical component执行`lstat` no-symlink校验；随后要求root是可信parent的direct child、各path
严格等于冻结relative layout且resolved后仍contained by root。root/parent/中间或leaf symlink、`..`、F18/outside parent与同basename错root
全部fail closed；detached repo与production CLI仍只由既有target/clean与file SHA owner校验，不扩成新的绝对location contract。

全部provider-free preflight完成后，Controller独占创建`pr190.f19.deadline.v1`：绑定allocation file SHA、run/target/CLI/model，
包含`start_monotonic`、精确`deadline_monotonic=start+1800s`、精确`provider_start_cutoff_monotonic=deadline-180s`。每条链首次
segment前再独占创建`pr190.f19.chain_deadline.v1`，绑定同一run/chain/workspace/label、global deadline file SHA，且
`global_start <= chain_start <= now < chain_deadline <= global cutoff`、`0 < chain_deadline-chain_start <= 540s`；同链所有R1-R4
segment必须复用该文件。所有JSON与requested timeout拒绝NaN/Infinity。driver real mode强制校验这些identity/窗口，任一gate关闭
即fail closed；segment timeout取requested、per-chain remaining与global cutoff remaining的最小正值。provider execution cap
1620s、finalization reserve 180s、每链wall cap 540s；所有provider calls与failed/not-started segments进入同一execution index。

## Adaptive chain state machine

最多三条fresh有效候选链，每workspace只执行一条：

| Segment | previous canonical Host Runs | accepted ordinal offset | 前置状态 |
| --- | ---: | ---: | --- |
| R1 | 0 | 0 | `.dayu`必须不存在 |
| R2 | 1 | 1 | 恰好1个同label/session的`succeeded` Run |
| R3 | 2 | 2 | 恰好2个同label/session的`succeeded` Run |
| R4 | 3 | 3 | 恰好3个同label/session的`succeeded` Run |

offset由driver按segment固定派生，不接受调用方覆盖；每段spawn前复用tracked `observe_run_terminals`与Host durable typed label-slot
reader，从canonical EventLog严格核验前N个`RUN_ACCEPTED`、唯一terminal、`RunStatus.SUCCEEDED`及同session，不读取或解释raw
`host_runs.status`字符串。R4另外要求第三个Run identity在其accepted/terminal序列区间内恰有一个同session、同run_id的canonical
`CONTEXT_COMPACTED`，并由`resolve_context_compacted_payload`严格解析完整accepted terminal payload；no compact、failed/rejected、
错Run/session、重复或payload drift均不得启动R4。R1-R4共用同一allocation entry、workspace、label与chain deadline；跳段、重复
segment导致count不符时provider不得启动。

1. R1：grounding后只读`s_0003`，形成一个<=160字符、canonical provenance的business-risk fact；terminal必须succeeded，任何
   reactive compaction或工具漂移立即seal。
2. R2：pre-start `ORDINARY`是baseline唯一target，必须accepted且恰有一个previous EvidenceFact；随后continuation grounding并
   只读`s_0013`形成FY2025 evidence；terminal必须succeeded，21.7/18.2不得升级。
3. R3：no-tool pre-start `ORDINARY`是replacement唯一target；source必须同时含previous+new、real caps、Host usage audit与
   unsupported ratios。accepted replacement覆盖keep/omit、canonical provenance与omitted exact complement；同operation自然
   reject后accepted覆盖bounded repair，五次reject+budget exhausted+deterministic fallback覆盖fallback。
4. R4：仅replacement target accepted后fresh reconnect，最多一个no-tool Run，验证artifact/EventLog/Memory/RunInput/public Tool
   Trace与accepted truth同源。

第一链追replacement+reconnect；自然同时覆盖repair/fallback则跳过相应slot，否则第二/三链只追尚缺branch。真实模型未触发即
needs-more；禁止重跑同链、调prompt、追加tool padding或用unit test冒充。

再次出现`runner_candidate_invalid`、digest/profile/provider/tool drift、prerequisite terminal非succeeded、reactive operation、
capture失败或budget gate关闭时立即停止相关链；owner failure需要产品调查时停止全部provider，不在driver/analyzer/CLI补偿。

## Evidence and publication

每个attempt private capture保存profile/config/input identity、wrapper projection、argv/actions/keys/screen/raw PTY、process/typed terminal、
filesystem/log/Tool Trace/durable before-after与failure seal。public publication必须在final scan前完整生成：

real mode的wrapper projection必须在spawn前独占写入已存在attempt parent中的固定sibling文件，记录execution/allocation/chain state、
prompt source/forwarded SHA、唯一argv contract、deadline identity与effective timeout；不得递归建父目录、不得回写该projection，
execution index只引用其file-byte SHA。preflight-only的`not_frozen` stdout不得冒充real projection。

- B2-only human report，逐项标observed/needs-more；
- `observation-summary.json`，逐链包含budget、actual ordinary/compactor calls、terminal refs、seal/verdict；
- 每条attempted chain的path-redacted Host resolver/Tool Trace analysis JSON与Markdown；
- accepted target truth/equality projection（仅有accepted时）；
- `execution-index.json`，包含workspace/config/harness identity、attempt/skip/seal与relative refs；
- material/calibration/wrapper summaries；
- `digest.json`从最终artifact file bytes计算；所有cross-file字段明确命名`*_file_sha256`，禁止未声明canonical digest；
- 最后且唯一writer `secret-scan.json`覆盖digest与此前全部public files。之后零public写入。

F18 immutable failed bundle只作为历史ref列入F19 report，不复制、不改写、不计F19成功证据。

## Validation and closeout

provider-free tooling通过strict compile、Ruff/pyright（若workspace tmp不在项目检查范围则直接以production venv单文件命令验证）、
nonempty/empty preflight、SHA/diff/JSON/path-secret checks。real-path矩阵必须含授权direct-child正例，以及same-basename outside、F18-like
nested、canonical parent ancestor、run root、`workspaces`/`evidence`/`private`、leaf symlink、`..`和wrong-layout反例；每个反例均断言
projection/evidence未创建且subprocess spawn为零。PTY owner regular frozen file正例必须通过；指向root内/外既有target或dangling
target的PTY owner leaf symlink必须在读取owner bytes、projection与spawn前拒绝。无产品代码变化时不运行产品测试冒充observation，
也不触发README。

最终分别输出product/setup implementation verdict、real observation verdict、publication verdict、B2 Oracle status与overall readiness；
逐项report交用户裁决，Agent不得接受B2。计划状态：`RR4-finding-revised-ready-for-independent-RR5`。
