# PR 190 / F17 Plan

- Gate：`plan`
- Work unit：PR 190 / F17，修复冻结 workspace publication manifest 的单一 digest 漂移
- 设计依据：`docs/cli_ci.md`
- Goal confirmation：`docs/gateflow/pr-190-f17-goal-confirmation-20260807.md`
- Artifact path：`docs/gateflow/pr-190-f17-plan-20260807.md`
- 状态：`pass`；按用户要求停在 plan gate，不进入 plan review 或 implementation，不提交、不 push
- 下一入口：`plan review`

## 1. 目标、动机与成功信号

问题成立，且严重性被正确界定为冻结 publication 派生链不闭合，而不是 production publication 行为错误。目标是让当前 package prompt raw bytes、冻结 manifest 中对应 asset entry、manifest raw bytes digest pin 三者重新一致。

成功信号必须同时满足：

- fresh production `InitMode.FIRST` publication 的严格审计 `valid=True`、`issues=()`；
- actual inventory 与 manifest inventory 均为 5 directories / 43 files / 16 model pointers；
- `dayu/config/prompts/scenes/conversation_compaction_user.md` raw SHA-256 与 manifest 对应 entry 相等；
- 保存后的 `docs/cli_init_workspace_manifest_v1.json` raw SHA-256 与测试中的 `FROZEN_MANIFEST_SHA256` 相等；
- `tests/cli/test_smoke_cli_init_provider_matrix.py` 收集并通过全部 71 tests；
- JSON、pyright、Ruff、compileall、`git diff --check` 与 changed-file guard 全部通过。

## 2. 第一性原理判断与直接证据

直接证据形成同源闭环：

- production fixture 通过 `snapshot_managed_roots -> prepare_workspace_transaction -> publish_workspace_transaction`，在 production file lock 下发布 `InitMode.FIRST` tree；因此实际 tree 来自 production publication owner，而非测试 fake。
- `validate_publication_tree(...)` 独立递归枚举实际 `config` tree、读取 raw bytes 计算 SHA-256，并独立发现 `/model/default_model_id` owners；它不按 manifest 文件表反向构造 actual。
- `load_manifest(...)` 对目录、文件、model owner 的数量、排序、唯一性与 schema 做 fail-closed 校验；代码常量固定为 5 / 43 / 16。
- 当前 focused owner test 可重复失败在 `assert report.valid`，报告 issue 为 `file_digest_mismatch:config/prompts/scenes/conversation_compaction_user.md`。
- 当前 prompt raw SHA-256 是 `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`；manifest entry 仍是 `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76`。
- 当前 manifest raw SHA-256 是 `d95de68e69b0aacc712ec6bf468c8604a91460a17f3e2497f397182517a6a9f8`，与当前测试 pin 相等，说明二级 pin 没有自行漂移；错误位于 manifest 对 prompt bytes 的派生 entry。
- `pytest --collect-only -q tests/cli/test_smoke_cli_init_provider_matrix.py` 当前收集 71 tests。
- 仅在内存中把该唯一旧 entry 替换为实际 prompt digest 后，manifest raw SHA-256 推演为 `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22`。该值只作编辑后交叉检查；实现时仍必须从**已保存文件的 raw bytes**重新计算，不能把本计划值当更新真源。

因此 root cause 是 prompt bytes 变更后，其冻结 publication consumer 未同步。倒改 prompt、修改 production transaction、动态生成 expected 或放宽 validator 都会破坏语义 owner，不能实施。

## 3. Owner chain 与修复边界

唯一 owner chain：

1. `dayu/config/prompts/scenes/conversation_compaction_user.md` raw bytes：LLM-facing 内容真源；本 work unit 只读。
2. `dayu.cli.init_workspace` production transaction：publication 行为 owner；原样发布 package asset，行为无误；本 work unit 不改。
3. `docs/cli_init_workspace_manifest_v1.json`：冻结 publication truth consumer，负责承诺精确 tree 与每个 asset digest；这里是唯一 root-cause 修复点。
4. `tests/cli/test_smoke_cli_init_provider_matrix.py::FROZEN_MANIFEST_SHA256`：钉住 manifest 保存后 raw bytes 的二级派生 consumer；只能在第 3 步保存后由实际 raw bytes 推导更新。
5. `utils/smoke_cli_init_provider_matrix.py::{load_manifest,validate_publication_tree}`：严格 schema / audit owner；只用于验证，不改。

允许的产品/测试变更只有两个精确 hunk：

- `docs/cli_init_workspace_manifest_v1.json`：只替换 `config/prompts/scenes/conversation_compaction_user.md` 的 `content_sha256`；不得重排、重格式化或改变任何其它 bytes。
- `tests/cli/test_smoke_cli_init_provider_matrix.py`：只替换 `FROZEN_MANIFEST_SHA256` 的值，值必须来自上一文件保存后的 `Path.read_bytes()` 计算结果。

Gateflow evidence artifact 可位于 `docs/gateflow/`，但不属于产品 contract 变更。禁止修改 prompt、validator、fixture 逻辑、Oracle/scenario/readiness、产品行为、schema、public interface、模型 owner、count 常量或任何兼容逻辑。

## 4. Contract / schema / state machine / public interface 裁决

- Contract：不变；只恢复既有 frozen publication contract 与当前 package asset 的一致性。
- Schema：不变；manifest keys、结构、版本、数量和排序全部保持。
- State machine：不变；FIRST/PRESERVE/OVERWRITE/RESET 及 Gateflow 之外的产品状态均不变。
- Public interface / 用户可见行为：不变。
- LLM-facing 文本：不变；prompt 只读，不能为了让旧 digest 通过而回退内容。
- Oracle / scenario / readiness：不变；三条 replacement scenarios 保持 `unadjudicated`。

## 5. 单一最小 implementation slice

### F17-S1：闭合 frozen manifest digest 派生链

- Objective：同步一个 manifest asset digest，并从保存后的 manifest raw bytes 更新二级 digest pin。
- Expected outcome：strict fresh production audit 无 issue，库存保持 5/43/16，71-test owner suite 全绿。
- Allowed files：
  - `docs/cli_init_workspace_manifest_v1.json`
  - `tests/cli/test_smoke_cli_init_provider_matrix.py`
  - 当前 gate 所需的 `docs/gateflow/pr-190-f17-*` evidence artifacts
- Prerequisites：当前分支非保护分支；implementation 开始前运行并核对 `git status --short`。pre-state dirty path 只允许当前 F17 的 Goal/Plan/Review/Adjudication evidence artifacts：
  - `docs/gateflow/pr-190-f17-goal-confirmation-20260807.md`
  - `docs/gateflow/pr-190-f17-plan-20260807.md`
  - `docs/reviews/plan-review-20260807-143241.md`
  - `docs/reviews/plan-review-20260807-143636.md`
  - `docs/gateflow/pr-190-f17-plan-review-adjudication-20260807.md`
  发现任何其它 tracked 或 untracked dirty path 时，必须在编辑产品/测试文件前立即停止；上述 evidence 只读，不得删除、重置或覆盖。
- Invariants：prompt raw bytes 不变；manifest 目录/文件/model-owner 数量与顺序不变；validator fail-closed 行为不变；不存在 fallback、动态 expected 或第二条 publication 真源。
- Error handling / stop condition：任何第二个 path/digest/model-pointer mismatch、count 漂移、unexpected changed file、计算值不能从保存后 raw bytes复现，或 71-test suite 非全绿时立即停止，不扩 scope 修补。

该 work unit 只有一个相互依赖的两-hunk slice；拆成多个 slice 会制造 manifest 已更新但 digest pin 未同步的中间 contract，合并更多变更则扩大风险。当前方案不新增抽象、helper、schema、测试或兼容分支，因而是满足现有 owner contract 的最小方案，没有过度设计。

## 6. Exact edit sequencing

1. **记录 pre-state 与 mismatch audit**
   - 运行 `git status --short`，逐项核对 pre-existing paths。输出只允许第 5 节 Prerequisites 枚举的 5 个当前 F17 Goal/Plan/Review/Adjudication artifacts；任何其它 tracked 或 untracked dirty path 都是 implementation stop condition，必须立即停止并保留现场，不得先编辑再依赖 post-state guard 补救。
   - 将这 5 个 artifact 作为只读 evidence；不得删除、重置、覆盖或把它们归为本 slice 产品/测试修改。
   - 运行 `shasum -a 256 docs/cli_init_workspace_manifest_v1.json dayu/config/prompts/scenes/conversation_compaction_user.md`；预期分别为当前 manifest pin 与 `22e7bc...c1c88`。
   - 运行 focused owner test：

     ```bash
     source .venv/bin/activate
     pytest tests/cli/test_smoke_cli_init_provider_matrix.py::test_frozen_manifest_matches_fresh_real_publication_tree -q
     ```

     pre-state 预期只因 `conversation_compaction_user.md` 的 digest mismatch 失败；以 goal-confirmation 中已记录的完整 strict report 确认 `issues` 是该 singleton，inventory 实际/manifest 均为 5/43/16。若出现第二项 mismatch，停止并重新 goal confirmation，不修改文件。

2. **只更新 manifest owner entry**
   - 使用 JSON parser 解析 `docs/cli_init_workspace_manifest_v1.json`，在顶层 `files` 数组中按 `path == "config/prompts/scenes/conversation_compaction_user.md"` 精确筛选；必须得到唯一 entry，且其 `content_sha256` 必须完整等于 `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76`。
   - 同一结构化检查必须确认 `files` 共 43 entries，排除目标后的其余 42 entries 各自完整的 `content_sha256` 值均不等于上述旧 digest。`grep`、raw substring、`str.count()` 或前后缀匹配只能辅助观察，不能作为 digest 与 owner path 的绑定证明。
   - 从 prompt `read_bytes()` 计算 digest，并完整断言结果等于 `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`；只把唯一目标 entry 的 `content_sha256` 从上述完整旧值替换为该完整计算值。
   - 保存文件；禁止 JSON formatter、排序或全文件 rewrite。

3. **从已保存 raw bytes 更新二级 pin**
   - 使用 `Path("docs/cli_init_workspace_manifest_v1.json").read_bytes()` 读取已保存 bytes，并以 `hashlib.sha256(raw_bytes).hexdigest()` 计算新 digest。
   - 预期交叉检查值为 `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22`；若不同，先检查是否出现非目标 bytes 变化，不得直接采用计划中的预测值绕过检查。
   - 仅将 `tests/cli/test_smoke_cli_init_provider_matrix.py` 的 `FROZEN_MANIFEST_SHA256` 替换为上述**实际计算值**；不得修改 fixture、assertion 或 validator 调用。

4. **执行 post-state strict audit**
   - 重跑 focused owner test，必须通过其 owner-level assertions：`report.valid`、`report.issues == ()`、5 directories、43 files、`.dayu-init.lock` 存在、16 model owners。
   - 独立断言 prompt raw digest 等于 manifest 对应 entry；manifest raw digest 等于测试常量。
   - 对比 pre/post report，唯一允许的状态转换是 singleton digest mismatch 消失；路径集合、counts 与 owner paths 不得变化。

5. **执行完整验证与 changed-file guard**
   - 按第 7 节顺序运行验证。
   - 最后审查 `git diff --word-diff=plain` 与 `git diff --stat`：产品/测试 diff 必须恰为两个单行 digest 替换；prompt、validator、utils、fixture、Oracle/scenario/readiness、README 和其它产品文件必须零 diff。

## 7. Tests 与验证命令

所有 Python 命令先执行 `source .venv/bin/activate`。

1. Digest / count / strict audit assertions：

   ```bash
   pytest tests/cli/test_smoke_cli_init_provider_matrix.py::test_frozen_manifest_matches_fresh_real_publication_tree \
     tests/cli/test_smoke_cli_init_provider_matrix.py::test_checked_in_manifest_digest_is_stable_across_validation -q
   ```

   预期 2 passed；前者证明 `issues == ()` 与 5/43/16，后者证明 manifest raw digest 与 pin 一致且验证过程不改写 manifest。

2. 完整 owner suite：

   ```bash
   pytest --collect-only -q tests/cli/test_smoke_cli_init_provider_matrix.py
   pytest tests/cli/test_smoke_cli_init_provider_matrix.py -q
   ```

   预期先收集 71 tests，再得到 71 passed；不得通过 deselection、xfail 或修改测试逻辑减少数量。

3. JSON 与 raw digest 独立检查：

   ```bash
   python -m json.tool docs/cli_init_workspace_manifest_v1.json >/dev/null
   shasum -a 256 docs/cli_init_workspace_manifest_v1.json \
     dayu/config/prompts/scenes/conversation_compaction_user.md
   ```

   另用标准库 JSON parser 和只读 Python assertion 检查顶层 `files` 数组：按精确 `path` 必须唯一定位目标 entry，完整断言其 `content_sha256` 等于 prompt `read_bytes()` digest；完整断言其余 42 entries 各自完整的 `content_sha256` 值均不等于旧 digest `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76`；再断言 manifest `read_bytes()` digest 等于测试常量。`grep`、raw substring、`str.count()`、前后缀或字符串包含关系均不能作为 owner 证明。

4. 类型、静态检查与编译：

   ```bash
   python -m pyright dayu/ tests/ utils/
   python -m ruff check tests/cli/test_smoke_cli_init_provider_matrix.py \
     utils/smoke_cli_init_provider_matrix.py
   python -m compileall -q tests/cli/test_smoke_cli_init_provider_matrix.py \
     utils/smoke_cli_init_provider_matrix.py
   git diff --check
   ```

   预期全部 exit 0；pyright 不得新增、扩散或掩盖错误。

5. Changed-file guard：

   ```bash
   git status --short
   git diff --name-only
   git diff --numstat -- docs/cli_init_workspace_manifest_v1.json \
     tests/cli/test_smoke_cli_init_provider_matrix.py
   git diff --word-diff=plain -- docs/cli_init_workspace_manifest_v1.json \
     tests/cli/test_smoke_cli_init_provider_matrix.py
   git diff -- dayu/config/prompts/scenes/conversation_compaction_user.md \
     utils/smoke_cli_init_provider_matrix.py docs/cli_ci.md tests/README.md
   ```

   预期 tracked 产品/测试变更名单只有 manifest 与测试文件，二者各为一个单行 digest replacement；最后一条命令零输出。已存在和后续 Gateflow evidence artifacts 必须单独列明，不能混入产品变更结论。

## 8. README / docs trigger 裁决

- `tests/README.md`：已读取其更新边界。本 slice 不新增测试层级、不改变运行方式或维护规则，71-test suite 也不变，只更新二级 digest pin，因此不更新。
- `dayu/config/README.md`：未修改 `dayu/config/`；prompt 内容与配置行为均不变，不触发。
- 根 `README.md`：安装、初始化入口、参数、输出、日志、workspace 位置和用户工作流均不变，不触发。
- `dayu/README.md`：分层、装配和 UI / Service / Host / Agent 边界均不变，不触发。
- `docs/cli_ci.md`：accepted Oracle、scenario registry、readiness 与 CI 流程均不变；本 work unit 明确禁止机械同步。
- `docs/cli_init_workspace_manifest_v1.json` 必须更新，因为它就是错误语义的 frozen publication consumer，而非说明文档的机械同步。
- `docs/gateflow/` 仅保存本 work unit 的 gate evidence。

## 9. Rollback、风险与未覆盖项

### Rollback

若 post audit 出现第二个 mismatch、count/owner drift、unexpected diff 或验证失败：

1. 立即停止，不改 prompt、validator、fixture 或其它消费者来追绿。
2. 用精确反向 patch 仅恢复本 slice 创建的两个 digest hunk：manifest entry 回到 `59b50e...87b76`，测试 pin 回到 `d95de6...a9f8`；不得用覆盖整个文件的命令伤及并发或用户改动。
3. 重算 raw digests，运行 `git diff --check` 与 changed-file guard，确认回滚没有影响 pre-existing goal-confirmation / Gateflow artifacts。
4. 记录失败证据并回到 goal confirmation；不得在 F17 内扩 scope。

### Residual risks / uncovered areas

- **非目标 bytes 被 formatter 改写**：`fixed in current slice`；由保存后 raw digest、word diff、numstat 和 exact two-hunk guard 阻止。
- **存在第二个 publication mismatch**：`fixed in current slice`；post strict audit 必须 `issues == ()`，否则 slice 不通过并回滚。
- **测试数量被意外减少**：`fixed in current slice`；collect-only 必须仍为 71，再要求 71 passed。
- **真实 provider / Oracle replacement scenarios 未重跑**：`assigned to later work unit`；F17 只修 deterministic frozen publication consumer，不改变 provider、Oracle/scenario/readiness 或产品行为，三条 replacement scenarios 继续由 PR 190 后续既有 owner 处理。
- **其它未来 package asset 再次漂移**：`assigned to later work unit`；现有 strict validator 与 frozen digest test 会 fail closed，本 slice 不引入自动更新 manifest 的新写路径。

无 blocking open question；没有需要新 issue 或用户裁决的未分类 residual risk。

## 10. Implementation completion report 格式

后续 implementation gate 的完成报告必须只陈述可复核事实：

- 改了什么：两个 exact digest hunks及其 owner chain；
- 验证了什么：pre/post strict mismatch、5/43/16、prompt/manifest/pin digest chain、71 passed、JSON/pyright/Ruff/compileall/diff-check；
- docs decision：哪些 README/docs 未触发及原因；
- changed-file guard：产品/测试变更名单与 Gateflow evidence 名单分开；
- 风险：remaining risks、owner/destination 与未覆盖项；
- Gate 状态：implementation artifact path、completion status 和下一 Gateflow entry。

本 artifact 完成后，按用户明确限制停止；不执行 `plan review`、implementation、commit 或 push。
