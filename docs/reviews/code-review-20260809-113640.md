# Code Review

## Scope

- Mode: current changes
- Branch: `codex/interactive-oracle`
- Base: `c7a937e1`
- Output file: `docs/reviews/code-review-20260809-113640.md`
- Included scope:
  - `dayu/host/run_input.py`
  - `tests/host/test_run_input_builder.py`
  - `tests/host/test_dispatch_scheduler.py`
  - `docs/host/design.md`
  - `dayu/host/README.md`
  - S1 implementation artifact
- Excluded scope:
  - unrelated untracked `docs/reviews/plan-review-20260808-095346.md`
  - pre-fix observed report, which is evidence rather than implementation
  - Issue #193 stability work
- Parallel review coverage: 无；single-owner narrow slice was reviewed along both real call paths.

## Findings

未发现实质性问题。

## Evidence-based review

### Intent and semantic ownership

- The trigger is the typed `ActiveRecentWindowFallback` loaded by the Host fallback provider.
- The new text is produced at the Host RunInput projection boundary that owns the degraded input, rather than in UI, Service, Engine, a generic scene prompt or a downstream answer checker.
- `fallback is None` returns no message, so normal ordinary and accepted-compaction inputs preserve the existing message shape.

### Call-path review

1. Attempt-free production dispatch:
   - `prepare_runner_call_candidate_in_transaction`
   - typed fallback load
   - selected material rendering
   - `_fallback_execution_guidance_messages`
   - existing one-system-message normalization
   - final complete-candidate sizing
2. Existing `RunInputBuilder` path:
   - typed provider load
   - selected material rendering
   - same guidance helper
   - existing scene/provider envelope normalization
   - manifest recording from the final messages

Both paths use the same helper. There is no secondary trigger inference, renderer or durable fact.

### Adversarial failure pass

- Missing fallback: no guidance is emitted.
- Fallback with dropped history: guidance is emitted once; dropped material remains absent.
- Current input: remains exactly once and last in the tested ordinary sequence.
- Tools unavailable or user-forbidden: wording does not force a call; it requires both availability and user permission, otherwise an explicit insufficiency response.
- Prompt injection from selected history: guidance is a system message and has higher precedence than selected user/assistant history.
- Near hard threshold: the final complete candidate remains the authoritative budget input. Synthetic tests that assert successful fallback dispatch now reserve capacity for the required guidance; production policy and fail-closed behavior are unchanged.
- Internal leakage: guidance contains no operation, tier, Host, artifact, ref, digest, cursor or policy identifiers.
- State mutation: no EventLog, Memory, artifact, terminal or schema producer changed.

### Semantic ownership drift and overcoupling

- No downstream heuristic, loose parser, compatibility branch, inferred state or duplicated envelope formatter was introduced.
- The guidance reuses the existing private classifier and normalization path; Engine and provider adapters remain unaware of Host fallback semantics.
- Issue #193 remains independent and is not made a prerequisite for this correctness behavior.

### Tests and validation

- Direct builder and production scheduler paths assert the new semantics.
- Existing exact normal system-content tests prove absence on no-fallback paths.
- Full affected files passed: `238 passed`.
- Full Pyright passed with zero errors/warnings/informations.
- Changed Python Ruff, compileall and diff check passed.

## Open Questions

无。

## Residual Risk

- A real provider can still disobey clear instructions. This cannot be solved by weakening the Host boundary or adding a natural-language output heuristic; post-fix real observation is required for Oracle adjudication.
- Long-run adherence measurement remains tracked by Issue #193.

## Conclusion

`pass`
