# WU-TOOLS-01-F01-02-R1 Plan Re-Review Gate

## Metadata

- **Reviewer**: AgentDS (plan re-review gate)
- **Work unit**: WU-TOOLS-01-F01-02-R1
- **Gate**: plan re-review (after plan-fix, before implementation)
- **Plan artifact**: `docs/host/wu-tools-01-f01-02-r1-plan.md`
- **Original reviews**:
  - `docs/reviews/plan-review-20260621-180827.md` (AgentDS)
  - `docs/reviews/plan-review-20260621-181350.md` (AgentMiMo)
- **Controller adjudication**: `docs/reviews/wu-tools-01-f01-02-r1-plan-review-controller-adjudication.md`
- **Plan-fix artifact**: `docs/reviews/wu-tools-01-f01-02-r1-plan-fix-codex.md`
- **Re-review timestamp**: 2026-06-21T18:20:34+08:00

## Scope

仅复核 controller accepted findings（C-F01 到 C-F04）是否由 plan-fix 关闭。不做完整 plan review，除非发现 plan-fix 引入新的 blocking risk。

## Required Check Results

### C-F01: activation 终端状态保证 / 锁顺序 — 已修复

**Controller 要求**：
1. `activate_observation(handle)` 与 `cancel_observation(handle)` 使用同一 observation lock
2. activation 在同一锁下检查 cancellation/terminal/submitted 并标记 submitted
3. 任何 post-accepted activation failure 必须使 observation 成为 terminal FAILED 或 LOST
4. ToolRuntime catch 仅为 safety net
5. 测试覆盖 unexpected activation exception 和 deterministic cancel/activate ordering

**Plan 当前证据**：

- §7 "Fins prepare / activate API"（lines 189-193）：`activate_observation(handle)` and `cancel_observation(handle)` must coordinate through the same observation lock used by current observation mutation, i.e. the implementation must not introduce a separate activation-only lock or rely on an unlocked status check. Activate must, while holding that same observation lock, look up the observation by handle, check cancellation state, terminal status and submitted flag, then atomically mark the record submitted before any executor submit can occur.
- §6 "State-machine changes"（lines 143-146）：After Host wait accept has succeeded, every activation failure path must leave the prepared observation terminal `FAILED` or `LOST` when the observation handle can identify a record; ToolRuntime exception handling is only a safety net for bounded diagnostics.
- §7 "Activation failure收口"（lines 174-178）：Fins activation adapter / runtime must treat terminal observation recording as the primary failure handling path. It must wrap the full activation path that can fail after a prepared observation is identified, including unexpected exceptions before or during submit, and record terminal `FAILED` or `LOST` on that observation before re-raising or returning. ToolRuntime should catch unexpected activation adapter exceptions... This catch is only a safety net; implementation must not rely on ToolRuntime catch as the primary mechanism that makes the Fins observation terminal.
- Slice 2 expected assertions（lines 323-325）：deterministic cancel/activate ordering test proves cancellation and activation share one observation lock；unexpected activation exception after a prepared observation exists is observable as terminal failed/lost and cannot remain indefinitely `PENDING`.

**判定**：已修复。所有 5 个子要求均有直接 plan 文本覆盖，锁语义和终端状态保证均显式写入 plan 第 6、7 节和 Slice 2 验证点。

### C-F02: Slice 1 test fixture 扩展 — 已修复

**Controller 要求**：Slice 1 显式允许 Host test fixture/harness 扩展，注入 `wait_activation_registry` 和 spy/stub activation adapter。

**Plan 当前证据**：

- Slice 1 "Exact allowed changes"（line 246）：Extend Host test fixture / harness construction in `tests/host/test_toolruntime_executor.py` to inject `wait_activation_registry` and spy/stub activation adapters, so tests can assert exact activation call counts without production Fins wiring.
- Slice 1 "Expected assertions"（line 271）：Host tests use a spy/stub activation adapter injected through the extended fixture, not production Fins runtime, to prove activation call count and non-activation paths.

**判定**：已修复。Slice 1 的 allowed changes 和 expected assertions 均显式允许并期望 test fixture 注入 spy/stub adapter。

### C-F03: prepared-but-unaccepted observation 行为 — 已修复

**Controller 要求**：accept rejected / timeout / stale / pre-accept cancellation 后的 prepared-but-unaccepted observation 有显式行为，不引入 durable cleanup ledger 或 public contract 过度设计。

**Plan 当前证据**：

- §7 "Prepared-but-unaccepted observations"（lines 208-213）：For accept rejected, accept timeout, stale execution rejected by Host accept, or cancellation before a wait is accepted, ToolRuntime must not activate the prepared observation. These paths must not introduce a durable cleanup ledger, public cleanup contract, new Host wait state, or new ToolAwaitingOutcome field. A prepared-but-unaccepted observation has no Host wait record, has not submitted executor work, and is process-local to the Fins runtime. It is therefore harmless if it remains as an unreachable process-local orphan until runtime teardown. Implementation may add only narrowly scoped best-effort process-local abandon if the existing prepared handle is already available without changing public contracts. If no safe existing handle path is available, leaving the harmless process-local orphan is the intended behavior for this WU.

**判定**：已修复。显式禁止 activation、禁止 durable cleanup、禁止 public contract 扩展，并将 harmless process-local orphan 定为 intended behavior。best-effort abandon 仅在已有 safe handle path 时可选。

### C-F04: 独立 WaitActivationRegistry 边界选择 — 已修复（rejected-with-reason 已说明）

**Controller 要求**：plan 不切换到 unified registry，并解释 separate WaitActivationRegistry 是有意边界选择。

**Plan 当前证据**：

- §7 "Why this is not overdesigned"（line 223）：The separate `WaitActivationRegistry` is an intentional boundary choice for this WU: executable provider activation remains construction-time Host wiring instead of being added to the public wait binding metadata.

**判定**：已修复。plan 显式声明 separate registry 是有意边界选择，满足 controller 的 rejected-with-reason 要求（无需设计变更，仅需说明）。

## New Blocking Risks

无。Plan-fix 未引入新的 blocking risk。修改范围严格限制在 controller accepted findings 要求的 plan 文本澄清，未扩展 scope 或引入新设计元素。

## Finding Final Status Summary

| Finding | Controller Decision | Final Status |
|---------|-------------------|--------------|
| C-F01 (AgentMiMo F01 + AgentDS F1): activation terminal guarantee / lock-order | accepted | **已修复** |
| C-F02 (AgentDS F3): Slice 1 test fixture extension | accepted | **已修复** |
| C-F03 (AgentMiMo F03 + AgentDS F2): prepared-but-unaccepted behavior | accepted | **已修复** |
| C-F04 (AgentMiMo F02): unified registry alternative | rejected-with-reason | **已修复** |

## Conclusion

**pass** — 全部 4 个 controller accepted findings 已由 plan-fix 关闭，plan 当前文本满足 re-review gate 的每一项 required check。无新增 blocking risk。Plan 可进入 implementation gate。

## Artifact Path

`docs/reviews/plan-review-20260621-182034.md`
