# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `cc07db75`（用户指定；accepted plan commit，Slice 1 已合入）
- Output file: `docs/reviews/code-review-20260816-142141.md`
- Review 时间：2026-08-16 14:21
- Included scope：相对 HEAD 的全部 workspace changes
  - 生产：`dayu/fins/storage/_fs_source_integrity.py`（新，1721 行）、`_fs_source_document_core.py`、`_fs_source_snapshot.py`、`_fs_storage_infra.py`
  - 测试：`tests/fins/test_fins_storage_atomicity.py`（+1138 行）、`test_fins_storage_provider.py`、`test_processor_read_consistency.py`
  - 文档：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice2-implementation-20260816.md`
  - 参照：`docs/gateflow/uf-fix08-existing-source-auto-repair-plan-20260816.md` §6.1/§9 Slice2/§11.1/§14、Slice 1 已合入的 `source_integrity.py`、未修改但行为受影响的 `_fs_filing_upload_state_core.py`、`repository_protocols.py`、`fs_batching_repository.py`
- Excluded scope：无（全部 diff 已逐行阅读；`_fs_source_integrity.py` 全文 1721 行已阅读）
- Parallel review coverage：无；全部由主 reviewer 直接走读
- 独立复现：三份 allowed tests 349 passed（32.50s，3 warnings 均为 edgar deprecated import）；全仓 pyright 0 errors/0 warnings/0 informations

## Findings

### 1-未修复-高-`commit_batch` 公开 Raises 契约与 Slice 2 新行为不同步：`SourceIntegrityPreflightError` 从未声明契约中逃出

- **入口/函数**: `FsBatchingRepository.commit_batch` → `_FsStorageInfra._validate_complete_source_tree` → `_inspect_source_kind_unguarded(whole-kind)`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:299-302`（抛出点）；`dayu/fins/storage/_fs_storage_infra.py:1154-1160`（调用点）；`dayu/fins/storage/repository_protocols.py:421-437` 与 `dayu/fins/storage/fs_batching_repository.py:63-79`（Raises 只声明 `ValueError` / `OSError` / `RuntimeFileLockError`）
- **输入场景**: staged tree 存在无法归属单一 target 的 root structural corruption——staging ticker descriptor 损坏、source root 为 symlink/非目录、source root 含非目录条目、identity descriptor 损坏或重复 document identity、root 条目枚举后被并发删除
- **实际分支**: whole-kind mode 下 `unassignable_root_fact` / `ticker_trusted=False` / root 非目录 → `_close_unassignable_root` → `raise SourceIntegrityPreflightError(UNSAFE_PUBLICATION)`（`RuntimeError` 子类）
- **预期行为**: commit 失败类型按公开契约可预测——caller 按 `ValueError`/`OSError` 捕获并投影为 bounded failure；plan §14 要求 stale/unsafe failure typed/bounded 且 path-free
- **实际行为**: 抛 `SourceIntegrityPreflightError`（typed 但未声明的 `RuntimeError` 子类）；测试 `test_complete_validator_rejects_identity_descriptor_symlink_and_mismatch` 已把断言从 `ValueError, match="identity descriptor"` 改为 `SourceIntegrityPreflightError`，固化新行为。旧代码同一场景抛 `ValueError`
- **直接证据**: 协议 docstring（`repository_protocols.py:430-436`）与 facade docstring（`fs_batching_repository.py:72-79`）均无 `RuntimeError`/`SourceIntegrityPreflightError`；`repository_protocols.py` 在 plan §9 的 Slice 4 allowed files 中、不在 Slice 2，本 slice 无权更新该契约文件；测试断言已证明新异常类型从 `commit_batch` 真实冒出
- **影响**: 所有按声明契约捕获 commit 失败的调用方（`ingestion_runtime.py:5216/5354/5548`、`docling_upload_service.py:1028`、`sec_download_*` 等）会漏捕 `RuntimeError` 子类，异常穿透到 generic failure boundary，可能被投影为 UNEXPECTED_RUNTIME 而非 typed failure；两个 slice 之间公开契约持续为假
- **建议改法和验证点**: controller 裁决二选一：(a) 将 `repository_protocols.py`（仅 Raises 契约行）加入本 slice allowed files 并同步 facade docstring、补 commit 契约测试断言 preflight 类型；(b) 显式接受契约漂移至 Slice 4 并记录为残余风险（plan 目前既要求 slice 独立闭合，又把契约更新排在 Slice 4，二者矛盾）。验证点：commit_batch 对 root corruption 的异常类型、协议 docstring 与实际行为一致
- **修复风险（低/中/高）**: 低（纯契约声明同步或裁决记录）
- **严重程度（低/中/高/严重）**: 高

### 2-未修复-高-`read_filing_upload_state` 行为与 Raises docstring 漂移：Slice 2 通过 seam 间接改变 state core 公共行为，超出 allowed files 且提前实现 Slice 3 目标

- **入口/函数**: `_FilingUploadStateCore.read_filing_upload_state` → document core seam `_classify_source_integrity_unguarded` → `_inspect_source_kind_unguarded(exact-target)`
- **文件(行号)**: `dayu/fins/storage/_fs_filing_upload_state_core.py:138-142`（docstring Raises 声称「published source directory、meta、identity descriptor 结构损坏（含 required meta/descriptor 缺失）时抛 ValueError」）；`_fs_filing_upload_state_core.py:173-200`（实际消费 seam 返回值并已投影 UNSAFE）；`dayu/fins/storage/_fs_source_document_core.py:551-590`（本 slice 改写的 seam，把损坏 target 投影为 UNSAFE classification 而非抛 ValueError）
- **输入场景**: published source 的 `meta.json` 缺失/损坏，或 identity descriptor 缺失
- **实际分支**: seam 机械投影 inspector 的 `UNSAFE/revision=None` classification；state core 的 Slice 1 既有分支 `status in {MISSING, UNSAFE}` → `source_meta=None` → 返回 typed state
- **预期行为**: 按 plan §9 Slice 2 完成条件——「Slice 1 upload-state read 的临时 damaged-target raw fail-closed seam 在本 slice 只获得可消费的 UNSAFE inspection 真源，由 Slice 3 在同一 guard 内机械接入后才成为 public typed state；Slice 2 不得提前修改 state core 或 validator」；Raises 更新排在 Slice 3 step 4
- **实际行为**: public typed state（`UNSAFE/revision=None + source_meta=None`）已在 Slice 2 出现；测试 `test_filing_upload_state_fails_closed_for_missing_source_meta_without_mutation` 已把断言从 `pytest.raises(ValueError, match="缺少 meta.json")` 改为断言 typed state，且 state core 的 Raises docstring 未更新（该文件不在 Slice 2 allowed files）
- **直接证据**: `_fs_filing_upload_state_core.py:138-142` docstring 与 `:183-186` 分支并存于同一函数；git status 确认 `_fs_filing_upload_state_core.py` 无 diff；测试断言新行为。plan 文字「由 Slice 3 机械接入后才成为 public typed state」与「Slice 2 机械投影 classifier（allowed）」存在内在张力，本实现选择了行为提前，且测试固化了提前行为，但契约文档未同步
- **影响**: state core 的公开 Raises 契约撒谎；上游调用方若按声明契约把 `read_filing_upload_state` 的 structural corruption 当作 ValueError 处理，行为已变；plan 的 slice 边界被实质突破（文件未动、行为已变）
- **建议改法和验证点**: controller 裁决：(a) 接受提前行为，授权本 slice 或立即 follow-up 更新 `_fs_filing_upload_state_core.py` 的 docstring/Raises 契约（最小 diff）；(b) seam 在 Slice 3 前保留对 damaged target 抛 path-free ValueError 的旧行为（与「机械投影」要求冲突，需 plan 修订）。验证点：docstring 与行为一致、state core 零 mutation 断言保持
- **修复风险（低/中/高）**: 低（docstring 同步）或中（若退回旧 seam 行为需 plan 修订）
- **严重程度（低/中/高/严重）**: 高

### 3-未修复-中-commit 失败诊断从精确定位退化为通用消息，破坏排障与 LLM-facing 可行动性

- **入口/函数**: `_validate_complete_source_kind_tree`
- **文件(行号)**: `dayu/fins/storage/_fs_storage_infra.py:1161-1180`（两个通用 raise）；测试 `test_fins_storage_provider.py` diff（`primary_document 未精确命中 files` → `filing source publication 不满足 complete canonical manifest contract`；`缺少 manifest` → 同一通用消息）证实
- **输入场景**: primary 不命中 files、manifest 缺失、manifest projection mismatch、source unsafe、source-kind root 结构损坏——五种不同失败
- **实际分支**: 前四类全部落入 `shared_manifest_reasons`/`repair_blocked_reason`/canonical 长度或 per-source COMPLETE 检查，抛出两个通用消息之一
- **预期行为**: 旧 commit validator 给出精确失败定位（`primary_document 未精确命中 files: {id}`、`缺少 manifest`、`manifest 存在 dangling source: ...` 等）；plan §14 要求失败 typed/bounded 且可行动
- **实际行为**: 失败仍 fail-closed，但消息无法区分 failure class；运维与 LLM 消费方（若 commit 失败消息进入 LLM 上下文）失去定位信息
- **直接证据**: diff 中删除的精确消息（`_fs_storage_infra.py` 旧 `_validate_complete_source_files`/`_validate_complete_source_directory`/`_read_complete_source_manifest`）与两个新通用 raise 对照；三处测试断言随实现被削弱为新消息
- **影响**: 排障成本上升；测试断言被实现倒逼泛化（违反「测试断言 owner 级 contract 行为」精神）；不影响 fail-closed 正确性
- **建议改法和验证点**: commit validator 在拒绝时从 inspection payload 投影精确 failure class（manifest missing/projection mismatch/unsafe target/primary mismatch）到 path-free 消息或 typed error；测试断言恢复精确定位
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 4-未修复-中-exact-target inspection 隐含全 kind 扫描成本，single-target 查询退化为 N×M 全量文件哈希

- **入口/函数**: `_inspect_source_kind_unguarded`（exact-target mode）→ `_inspect_source_directory` → `_classify_physical_file_facts`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:264-297`（对每个 child 目录完整 inspect）、`_fs_source_integrity.py:983-989`（每个 declared file 全量 SHA-256）
- **输入场景**: 大 corpus（数百 filing、每 filing 数 MB 文件）下调用 `classify_source_integrity`、`read_source_snapshot`、`read_filing_upload_state`（每 target 一次 exact mode）
- **实际分支**: exact mode 与 whole mode 走同一完整扫描路径，每个 sibling source 的每个文件都被重新哈希
- **预期行为**: 旧实现只解析/哈希单 target 目录的文件（旧 `_classify_source_integrity_unguarded` 只对 target 的 declared files 做 `_hash_regular_file_sha256`）
- **实际行为**: single-target 查询成本从 O(1 source 的文件字节) 放大为 O(全部 source 的文件字节)；且没有共享缓存，N 个 target 依次查询为 N 倍全量哈希
- **直接证据**: `_inspect_source_kind_unguarded:264-297` 的循环无条件调用 `_inspect_source_directory`；`_classify_physical_file_facts` 对 descriptor.sha256 非 None 的每个文件无条件流式哈希。plan §6.1 冻结了「一次 scan 产生完整 inventory」的设计，因此这是 plan 授权的 trade-off，不是实现偏离——但代价在 plan 中没有被量化
- **影响**: 大 corpus 下 classifier/snapshot 延迟显著上升；不阻塞正确性
- **建议改法和验证点**: 在 plan 后续 slice 或独立 perf work unit 量化；可选优化（exact mode 对 sibling 只做结构/身份枚举、跳过 content digest）需要 plan 修订（因 §6.1 冻结了「同一次 scan」契约）
- **修复风险（低/中/高）**: 中（触及冻结契约，需 plan 修订）
- **严重程度（低/中/高/严重）**: 中

### 5-未修复-低-snapshot containment 检查 root 参数退化为恒真，containment 语义成为死代码

- **入口/函数**: `_acquire_snapshot_attempt_unguarded`
- **文件(行号)**: `dayu/fins/storage/_fs_source_snapshot.py`（新代码 `core._require_contained_regular_file(physical_path, physical_path.parent, ...)`）
- **输入场景**: 任意 snapshot 读取
- **实际分支**: `_require_contained_path(path, path.parent)` 中 resolve 后的 path 恒在其父目录内——containment 分支恒真；仅 `is_symlink()/is_file()` 检查仍有 TOCTOU 防御意义
- **预期行为**: 旧代码 root 为 `document_dir`，containment 有真实语义
- **实际行为**: 因 `physical_path = source_directory / normalized_name` 由 inspector 构造（name 已拒绝分隔符），构造层面安全，无实际漏洞；但 root 参数选择使 containment 语义失效，后续维护者可能误读为存在 containment 保证
- **直接证据**: `_fs_storage_infra.py:1266-1268` 的实现（`_require_contained_path` + `is_symlink/is_file`）与 snapshot 调用处 root=`physical_path.parent` 对照
- **影响**: 仅维护性/误读风险；无正确性影响
- **建议改法和验证点**: 若意图只是「open 前复核 regular file」，改为不传虚假 root 的专用复核（或注释说明 containment 已由 inspector 构造保证）；若意图保留 containment，root 应传 document_dir/ticker_dir
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 6-未修复-低-单 source 损坏 + trusted manifest 场景的 reason 语义名不副实

- **入口/函数**: `_apply_manifest_facts`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:1129-1153`
- **输入场景**: 单一 filing target 的 meta/declaration 损坏（content UNSAFE），manifest 文件结构本身可信（ticker 正确、documents 为合法数组、无重复）
- **实际分支**: manifest 逐项 canonical 比较时 `persisted_meta is None` → `expected is None` → `mismatched_ids` 非空且 `_all_content_complete` False → 全部 inventory 叠加 `CROSS_SOURCE_INCONSISTENCY`，shared 槽位标 `SOURCE_MANIFEST_UNTRUSTED`
- **预期行为**: reason 应描述真实事实——manifest 无法与损坏 source 比对（而不是「跨 source 冲突」或「manifest 文件损坏」）
- **实际行为**: 单 source 场景出现「cross-source」reason；manifest 文件未被污染却标「untrusted」。测试断言固化该组合（`(META_UNTRUSTED, CROSS_SOURCE_INCONSISTENCY)`、`(FILE_DECLARATION_UNTRUSTED, CROSS_SOURCE_INCONSISTENCY)`）
- **直接证据**: `_fs_source_integrity.py:1130-1153` 的分支选择；`tests/fins/test_fins_storage_atomicity.py` 中 `test_filing_upload_state_fails_closed_for_missing_source_meta_without_mutation` 与 `test_source_integrity_classifies_published_staged_and_whole_tree` 的 reasons 断言
- **影响**: 决策影响同效（unsafe 一律拒绝 repair，fail-closed）；诊断/LLM 消费方会误判损坏位置。plan §11.1 未对该组合给出 reason 级契约，属于未冻结角落
- **建议改法和验证点**: 为该组合引入或复用精确 reason（或明确 shared 槽位的「不可比对」语义并更新 reason 文档）；测试相应收紧
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 7-未修复-低-whole-kind mode 的 `repair_blocked_reason` 语义失真

- **入口/函数**: `_derive_repair_blocked_reason`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:1282-1288`
- **输入场景**: whole-kind mode（`target=None`）下任一 source content 非 COMPLETE
- **实际分支**: `target_id=None`，所有 source 的 `document_id != None` 恒真 → 任一非 complete content 即返回 `NON_TARGET_SOURCE_INCOMPLETE`
- **预期行为**: whole mode 没有 target，「non-target source」概念不成立
- **实际行为**: 当前唯一消费方（commit validator）只做非 None 检查、Slice 4 用 exact mode，故无决策影响；reason 值为诊断性
- **直接证据**: `_fs_source_integrity.py:1282-1288` 与 commit 消费点 `_fs_storage_infra.py:1163` 对照
- **影响**: 仅诊断语义失真
- **建议改法和验证点**: whole mode 下改为专用 reason 或 None；随 Slice 4 消费面确定
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 8-未修复-低-`_SOURCE_REVISION_META_FIELD` 字面量双定义

- **入口/函数**: 模块级常量
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:60` 与 `dayu/fins/storage/_fs_storage_infra.py`（原 owner）
- **输入场景**: 未来 revision 字段名演进
- **实际分支**: 两处各自定义 `"_published_source_revision"` 字面量
- **预期行为**: storage 持久化 schema 字段名应单一 owner
- **实际行为**: 依赖方向（infra → inspector）使 inspector 不能 import infra 的常量（会成环），复制是当前唯一解；但双定义存在漂移风险，无编译期保障
- **直接证据**: 两文件各自 `Final` 定义相同字面量
- **影响**: 字段名演进时漏改一处即静默破坏分类；当前值一致无实际故障
- **建议改法和验证点**: 将字段名下沉到 inspector 与 infra 共同依赖的更低层模块（如 `_fs_storage_utils`），或 inspector 定义后 infra 引用之
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

1. **exact/whole 相等性契约在 root-unassignable 场景未闭合**：plan §11.1 声称「exact-target mode 与 whole-kind mode 的 inventory/shared reasons/canonical facts 完全相等」；但 target identity descriptor 损坏或 root 存在非目录条目时，exact 返回含 UNSAFE target 的 inventory、whole 抛 `SourceIntegrityPreflightError`（无 inventory 可比较）。§6.1 的 preflight 设计与 §11.1 的相等性断言之间的边界（「两者都返回时」）未写明；同源测试只覆盖 clean fixture。建议 controller 确认该契约的隐含限定。
2. **published snapshot 的 uri↔physical 一致性检查随 `_parse_snapshot_files` 删除**：旧 snapshot 校验 `_local_path_from_uri(portfolio_root, meta.uri) == physical_path`。plan §6.1 有意把 URI 规则留在 commit validator（staging locator 资格），且 `SourceSnapshotFileDescriptor` 不含 uri、快照消费方不依赖 uri——但确认没有其它 published 消费者依赖该一致性检查（review 未发现）。
3. **`_list_document_ids_unguarded`/`_list_external_identities` 与 inspector 的身份枚举双路径并存**：`list_document_ids` public API 仍走旧枚举路径（本 slice 未触及），inspector 自己枚举 identity。语义不同（ID 列表 vs 完整性分类），但物理枚举逻辑重复；plan 未要求合并，是否后续 slice 处理未定。
4. **`_with_repairable_classification` 未断言 source-local status 必须 COMPLETE**：只检查 revision 非 None 且非 UNSAFE。当前调用点（manifest facts 叠加）content 均 COMPLETE，无实际影响；防御性断言缺失。
5. **commit 新通用消息是否进入 LLM 上下文**：若 commit 失败消息会被投影给 LLM（failure evidence），Finding 3 的严重程度应上调；本 review 未追踪到该投影路径（属 Slice 5 接线）。

## Residual Risk

- 独立复现仅覆盖 tests 与 pyright（349 passed / 0 errors）；implementation 文档声称的逐文件 coverage（87%/84%/92%/85%）未独立复现。
- 未运行真实 CLI、UF-PF08、UF-PF12、provider/converter evidence——与 implementation 文档声明一致，符合 plan。
- Finding 4 的 N×M 性能放大未基准测试；大 corpus 延迟影响未知。
- `repair_blocked_reason` 的 Slice 4 消费路径尚未实现，其 whole-mode 语义失真（Finding 7）的实际影响无法验证。
- Finding 1 的上游 failure-boundary 投影（`ingestion_runtime`/workflow 对 RuntimeError 子类的处理）未逐条走读——契约层面已可判定 drift，实际用户可见影响取决于 Slice 5 接线。
- `_fs_filing_upload_state_core.py` 未修改但行为已变（Finding 2），其上游 `FilingUploadPublishedState` 消费方（workflow/service）未纳入本 review 走读范围。

## 结论

- **Blocking findings**: #1（commit_batch Raises 契约与 preflight 行为不同步）、#2（read_filing_upload_state 行为/Raises 漂移 + 越界提前 Slice 3 行为）
- **Nonblocking findings**: #3（commit 诊断消息退化）、#4（exact-mode 全量扫描成本）、#5（snapshot containment 恒真）、#6/#7（reason 语义名不副实）、#8（revision 字段名双定义）
- **验证复现**: 349 passed；pyright 0 errors/0 warnings/0 informations——与 implementation 文档一致
- **总体判定**: **FAIL**（存在 2 个 high-severity contract-drift findings，需 controller 裁决契约同步方案后再进入下一 gate；不修代码、不提交，与 deepreview 边界一致）
