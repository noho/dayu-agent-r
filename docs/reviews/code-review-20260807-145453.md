# Code Review

## Scope

- Mode: current changes
- Branch: `codex/interactive-oracle`
- Base: `0d215296`
- Output file: `docs/reviews/code-review-20260807-145453.md`
- Included scope:
  - `docs/cli_init_workspace_manifest_v1.json` (1 hunk, 1 insertion / 1 deletion)
  - `tests/cli/test_smoke_cli_init_provider_matrix.py` (1 hunk, 1 insertion / 1 deletion)
  - `docs/gateflow/pr-190-f17-implementation-20260807.md` (untracked implementation artifact)
- Excluded scope: 无
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Independent Verification Results

以下验证均由 reviewer 独立于 implementation artifact 执行，不引用 artifact 中的声称值。

### 1. Prompt bytes 未变

- `git diff 0d215296 -- dayu/config/prompts/scenes/conversation_compaction_user.md` → 空输出，零 diff。
- `git show 0d215296:dayu/config/prompts/scenes/conversation_compaction_user.md | sha256sum` → `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`，与 working tree 文件 digest 一致。
- **结论**: prompt raw bytes 相对 base 零变更。当前 manifest 旧值 `59b50e13ea...` 是 commit `d4b3ee7c` 引入 prompt 变更时未同步 manifest 的历史遗留 drift。

### 2. Manifest entry 精确等于 prompt digest

- Manifest entry（行 40）: `content_sha256 = 22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`
- `sha256sum dayu/config/prompts/scenes/conversation_compaction_user.md` → `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`
- **结论**: 完全一致。

### 3. Manifest raw digest 精确等于 test pin

- `sha256sum docs/cli_init_workspace_manifest_v1.json` → `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22`
- Test `FROZEN_MANIFEST_SHA256` → `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22`
- **结论**: 完全一致。

### 4. Inventory 5/43/16

- Manifest 结构化解析: `directories=5, files=43`
- `pytest tests/cli/test_smoke_cli_init_provider_matrix.py -q` → **71 passed** (4.48s)
- Owner suite 中 `test_frozen_manifest_matches_fresh_real_publication_tree` 执行 fresh production `InitMode.FIRST` strict audit 并断言 `valid=True, issues=()`，该测试内部验证 actual vs manifest inventory 均为 5/43/16
- **结论**: inventory 一致，71 owner suite 全通过。

### 5. Strict fresh production tree 无其它 mismatch

- `test_frozen_manifest_matches_fresh_real_publication_tree` 断言 `report.valid` 且 `report.issues` 为空元组。若有第二个 mismatch，该测试会在 issues 中报出。
- Old digest `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76` 在其余 42 entries 中出现 0 次（独立计数确认）。
- New digest `22e7bc5015cb...` 仅在目标 entry 出现 1 次。
- **结论**: 无其它 mismatch。

### 6. Additional checks

- `pyright dayu/ tests/ utils/` → 0 errors, 0 warnings, 0 informations
- Diff stat: 精确 2 files, 2 insertions(+), 2 deletions(-), 各为单行 hunk
- Prompt file 本身零 diff（`git diff 0d215296` 无该文件输出）

## Adversarial Attack Assessment

### Semantic owner drift

未发现。变更正确地将 manifest（publication truth consumer）的 digest 更新为 prompt（content truth source）的实际 bytes digest。Owner chain 为: prompt raw bytes → manifest entry digest → frozen manifest pin → test assertion。该链路在此 slice 中完整闭合，无 fallback、loose parsing 或下游修正上游语义的情况。

### Validation weakening

未发现。`FROZEN_MANIFEST_SHA256` pin 的验证逻辑（读取 manifest bytes → SHA-256 → 与常量比较）未被修改或放松。71 owner suite 全通过，无断言被削弱或跳过。

### LLM-facing regression

未发现。prompt raw bytes 在 base 和 working tree 之间零 diff。LLM 消费的 prompt 内容未变更。manifest 和 test pin 是 developer-facing publication infrastructure，不进入 LLM 上下文。

### Scope contamination

未发现。变更严格限定为两个单行 hunk：manifest entry digest 和 test frozen pin。未修改 prompt、production code、public contract、schema、README 或 CI 配置。

## Open Questions

无。

## Residual Risk

- 旧 manifest digest `59b50e13ea...` 的历史遗留 drift 已被此 slice 修复，但根因（commit `d4b3ee7c` 修改 prompt 后未更新 manifest）未被自动化检测。现有 `test_frozen_manifest_matches_fresh_real_publication_tree` 在 CI 中能 catch 此类 drift，但依赖于测试被执行。**风险等级: 低**。
- Implementation artifact (`docs/gateflow/pr-190-f17-implementation-20260807.md`) 是 untracked 文件，其内容声称与 reviewer 独立验证结果一致，但该 artifact 本身未被 git 跟踪，不会随此次变更进入版本控制。**风险等级: 无**（按用户要求，本轮不 commit）。
