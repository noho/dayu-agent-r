# Code Review — UF-FIX04 S1 re-review

## Scope

- Mode: current changes (diff against base `f609a4d8`)
- Reviewer: AgentMiMo
- Branch: `codex/upload-filing-oracle`
- Base: `f609a4d8238c6b31456c2e1d548079b22b771a68`
- Work unit: `UF-FIX04 shared-calendar-year-validation`
- Slice: `S1-domain-calendar-year-owner`
- Output file: `docs/reviews/code-review-20260814-145012.md`
- Included scope:
  - `dayu/fins/domain/filing_semantics.py`（工作区 diff）
  - `tests/fins/test_fiscal_normalization_contracts.py`（工作区 diff）
  - `tests/fins/test_read_runtime_semantic_ownership_guards.py`（工作区 diff，只读审查）
  - `docs/reviews/code-review-20260814-143751.md`（AgentMiMo 初次 review）
  - `docs/reviews/code-review-20260814-144024.md`（AgentDS 初次 review）
  - `docs/reviews/wu-upload-filing-calendar-year-validation-s1-review-controller-adjudication.md`（controller 裁决）
  - `docs/reviews/wu-upload-filing-calendar-year-validation-s1-review-fix-codex.md`（fix artifact）
  - `dayu/fins/tools/read_runtime.py`（唯一生产 consumer，只读审查）
- Excluded scope: S2/S3 未接线 consumer；UF-PF04；不属于 S1 的文件。
- Parallel review coverage: 无。

## Validation

本 reviewer 在 re-review 中独立复现以下全部验证：

| 命令 | fix artifact 声明 | 复现结果 |
| --- | --- | --- |
| `pytest tests/fins/test_fiscal_normalization_contracts.py tests/fins/test_read_runtime_semantic_ownership_guards.py -q` | exit 0; 98 passed | **exit 0; 98 passed, 3 warnings**（一致） |
| `coverage erase && coverage run -m pytest <三文件集合> -q` + `coverage report --include='dayu/fins/domain/filing_semantics.py' --show-missing --fail-under=80` | exit 0; 211 passed; 141 stmts, 18 missed, 87% | **exit 0; 211 passed; 141 stmts, 18 missed, 87%**（一致） |
| `python -m pyright <三文件>` | 0 errors | **0 errors, 0 warnings, 0 informations**（一致） |
| `git diff --check` | exit 0 | **exit 0，无输出**（一致） |

coverage missing lines: `212, 238, 262, 266, 289, 311, 335-341, 396, 462, 468, 492, 515`

- 396 行（`isoformat` round-trip raise）仍在 missing 中 → DS-1 rejected 状态正确保持。
- 383 行（非 str raise）已从 missing 中消失 → DS-3 修复确认覆盖。
- 非 S1 范围的 missing lines（212/238/262/266/289/311/335-341/462/468/492/515）均来自非本次改动的其它函数。

## Finding Statuses — DS-1 / DS-2 / DS-3 / DS-4 逐项复核

### DS-1 — isoformat round-trip 当前不可达

- **Controller decision**: `rejected-with-reason`
- **理由**: accepted plan 明确要求 `datetime.date` 构造后执行 exact `isoformat()` round-trip；该检查是 canonical output contract 的直接防御，不应删除。
- **Re-review status**: `证据失效`（不构成 finding）
- **验证**: `filing_semantics.py:395-396` 的 `if parsed.isoformat() != value: raise ...` 完整保留。coverage missing 仍包含 396 行，证明未通过测试替身或行为改写伪造覆盖。实现与 rejected 理由一致——round-trip 防御按 accepted plan 原样保留，未增加重复其意图的注释。controller 的 rejected 理由在实现中完全保持。

### DS-2 — year 范围 message 与 bounds 不同源

- **Controller decision**: `accepted`
- **Required fix**: 在 owner 模块内由 `_MIN_CALENDAR_YEAR` / `_MAX_CALENDAR_YEAR` 派生唯一范围文本，两处错误入口共同消费；对外文本不变。
- **Re-review status**: `已修复`
- **验证**:
  - `filing_semantics.py:52` 新增 `_CALENDAR_YEAR_RANGE_TEXT: Final[str] = f"{_MIN_CALENDAR_YEAR}..{_MAX_CALENDAR_YEAR}"`，由 bounds 常量唯一派生。
  - `filing_semantics.py:364`（`parse_calendar_year` raise）和 `filing_semantics.py:421`（`normalize_fiscal_year` raise）均引用 `_CALENDAR_YEAR_RANGE_TEXT`，无硬编码 `1000..9999` 字面量在错误 message 中。
  - docstring 中的 `1000..9999`（行 352/355/412/415）是文档描述，非运行时 message，不影响同源约束。
  - `test_fiscal_normalization_contracts.py:181-201`（`test_calendar_year_entry_points_share_exact_range_message`）直接从两个入口取实际异常，断言均精确等于 `reporting_year 必须是 1000..9999 的整数`，证明两入口 message 同源且对外文本不变。
  - 本 reviewer 独立 `grep` 确认 `_CALENDAR_YEAR_RANGE_TEXT` 仅在 52/364/421 三处出现，错误 message 中无其它 `1000..9999` 字面量。
- **External contract**: 对外 message 文本完全不变。

### DS-3 — full-date 非 str 防御无测试

- **Controller decision**: `accepted`
- **Required fix**: 为 `parse_iso_calendar_date` 增加绕过静态类型的 `None`、整数等 runtime 反例，断言统一 `ValueError` message；只改 owner test，不扩宽生产签名。
- **Re-review status**: `已修复`
- **验证**:
  - 生产签名仍为 `parse_iso_calendar_date(value: str, *, field_name: str = "date") -> datetime.date`，未扩宽。
  - `test_fiscal_normalization_contracts.py:273-288` 新增 `test_parse_iso_calendar_date_rejects_non_string_runtime_values`，参数化 `[None, 2024]`，使用 `cast(str, value)` 模拟绕过静态类型，断言 `ValueError` 且 message 匹配 `date 必须是实际存在的 YYYY-MM-DD 日期`。
  - coverage missing 列表已不包含 383 行（非 str raise 分支），证明该分支已被测试执行覆盖。
  - 与 `parse_calendar_year` 的同类测试（`test_parse_calendar_year_rejects_invalid_runtime_values`，行 160-178）模式对称。
- **Signatures**: 窄签名保持不变，运行时 fail-closed 由测试锁定。

### DS-4 — 模块 docstring 未涵盖 calendar/year owner

- **Controller decision**: `accepted`
- **Required fix**: 更新 `filing_semantics.py` 中文模块概览，明确它同时拥有四位 fiscal/partial year 与 canonical Gregorian full-date 合法性。
- **Re-review status**: `已修复`
- **验证**:
  - `filing_semantics.py:1-7` 模块 docstring 现为："本模块承载 Fins domain 层的窄业务值解析：SEC form、财期、四位 fiscal/partial year、canonical Gregorian full-date、文档质量和财务数据质量。calendar/year 合法性由本模块统一拥有；pipeline、processor、storage decode 与 read runtime 只能消费这里的解析结果，避免各自维护业务规则后产生语义漂移。"
  - 准确声明新增的 calendar year 与 strict ISO date 解析能力。
  - 未写入 upload/download wrapper-owned shape、partial expansion、错误投影或上层调度语义，边界正确。
  - 与 owner API（`parse_calendar_year` / `parse_iso_calendar_date` / `normalize_fiscal_year`）的实际职责一致。

## 修订是否引入新问题

### Correctness / ownership

- 本轮 fix 只新增一个私有派生常量 `_CALENDAR_YEAR_RANGE_TEXT`（行 52）和两个直接 owner contract 测试，不引入新生产逻辑、新 helper、wrapper、协议或新层次。
- `_CALENDAR_YEAR_RANGE_TEXT` 使用 f-string 在模块加载时由 `_MIN_CALENDAR_YEAR` 和 `_MAX_CALENDAR_YEAR` 唯一派生，不存在运行时漂移路径。
- owner 唯一性未被破坏：全仓 `_CALENDAR_YEAR_RANGE_TEXT` 仅在 `filing_semantics.py` 内部出现，无 consumer 引用。

### Type

- pyright 0 errors，无新增或扩散类型问题。
- `cast(str, value)` 和 `cast(int, value)` 在测试中使用是标准的绕过窄签名测试模式，不引入生产类型风险。

### Test

- 从 95 passed 增至 98 passed：净增 3 个测试用例（`test_parse_iso_calendar_date_rejects_non_string_runtime_values` 参数化 2 值 + `test_normalize_fiscal_year_narrows_json_before_delegating_year_owner` 1 个）。
- 旧测试迁移正确：`test_normalize_fiscal_year_rejects_non_positive_or_non_integer_values`（原 match `正整数`）替换为 `test_normalize_fiscal_year_rejects_non_four_digit_or_non_integer_values`（match `1000..9999`），参数从 `[True, False, 0, -1, 2024.0, "2024"]` 更新为 `[True, False, 0, -1, 999, 10000, 2024.0, "2024"]`，覆盖越界年份。
- `test_normalize_fiscal_year_accepts_missing_and_positive_integer` 替换为 `test_normalize_fiscal_year_accepts_missing_and_four_digit_integer`，参数化 `[1000, 2025, 9999]`。
- 未发现为保住旧测试而保留的兼容逻辑。
- coverage 从 86%（139 stmts, 19 missed）提升至 87%（141 stmts, 18 missed），新增 2 statements 且覆盖 1 条 previously missing line（383 行）。

### External message

- 对外错误 message 文本完全不变：`{field_name} 必须是 1000..9999 的整数`（year）和 `{field_name} 必须是实际存在的 YYYY-MM-DD 日期`（date）。
- `_CALENDAR_YEAR_RANGE_TEXT` 的 f-string 展开结果为 `1000..9999`，与修复前硬编码字面量完全一致。

## Open Questions

无。

## Residual Risk

1. S2/S3 生产接线未实现：`covered by later approved slice`。
2. `UF-PF04` 真实 CLI evidence 未运行：`assigned to later work unit`，owner=`UF-PF04`。
3. `filing_semantics.py:396` isoformat round-trip raise 仍为 coverage missing：controller 已 `rejected-with-reason`，不是未分类风险；实现按 accepted plan 原样保留。
4. 18 missed statements 中非 S1 范围的行来自其它函数（`sanitize_fiscal_period_by_sec_form`、`normalize_document_quality`、`normalize_financial_data_quality` 等），不影响 S1 新增代码覆盖。

## Docs Decision

- 本轮 fix 未修改任何 README。
- `dayu/fins/README.md` 与根 `README.md` 仍由 approved S3 在最终 consumer contract 稳定后更新。
- `tests/README.md` 不更新：没有新增测试层级。
- `dayu/README.md`、Host/Engine README 不更新：分层/装配未变化。
- 模块 docstring 已更新（DS-4 修复），属代码内文档，不触发 README 更新。

## Conclusion

**Pass — DS-2/DS-3/DS-4 已修复，DS-1 rejected 状态正确保持，未引入新问题。**

S1 review fix 完全满足 controller adjudication 的要求：

- **DS-2**: `_CALENDAR_YEAR_RANGE_TEXT` 由 bounds 常量唯一派生，两处错误入口共同消费，对外 message 不变，owner-level assertion 锁定同源性。
- **DS-3**: `parse_iso_calendar_date` 非 str 运行时防御分支已由 `cast(str, value)` 参数化测试覆盖，coverage 383 行已从 missing 消失，窄签名未扩宽。
- **DS-4**: 模块 docstring 准确声明 calendar year 与 strict ISO date 职责，未泄漏上层语义。
- **DS-1**: isoformat round-trip 防御完整保留，coverage 396 行仍为 missing，与 controller rejected 理由一致。
- 未引入新 correctness/ownership/type/test 问题；coverage 87% ≥ 80% gate；pyright 0 errors；diff check clean。

## Next Entry Point

`controller S1 acceptance` — controller 对本 re-review artifact 和 fix 状态做最终裁决，确认 S1 是否 accepted 并可进入后续 slice。
