# Code Review — UF-FIX10 S2 re-review

## Scope

- Mode: current changes（只读 re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `7e0941828c09d890ad04e3ff8f2c1cf5e28441ca`
- Output file: `docs/reviews/code-review-20260817-031615.md`
- Included scope: 全部未提交工作树变更（与前两路 review scope 一致）
- Excluded scope: oracle/scenario/frozen evidence、Host/Engine/Service/CLI production、material workflow
- Parallel review coverage: 无（主 reviewer 逐项验证 10 个 accepted finding 的根因关闭证据）

## 结论

**PASS。** 10 个 accepted findings（M-F1..6、D-F1..2、C-F1）全部经直接代码证据确认根因关闭，无新增回归。

## Finding 逐项验证

### M-F1 — rollback helper 捕获 `BaseException` → 已关闭 ✅

- **修复证据**: `filing_upload_publication.py:497` 改为 `except (OSError, RuntimeFileLockError, ValueError) as rollback_error`，不再捕获 `BaseException`。
- **信号传播**: docstring 行 492-493 明确声明 `KeyboardInterrupt`/`SystemExit` 原样传播。
- **测试覆盖**: `test_cancel_rollback_signal_exception_propagates_unchanged`（行 1526-1563）对 `KeyboardInterrupt` 和 `SystemExit` 两种信号均断言 `pytest.raises(signal_type)` 且 rollback 已发生。
- **状态**: 关闭。

### M-F2 — batch acquire / fresh read 的 `RuntimeError` 未映射 → 已关闭 ✅

- **修复证据**:
  - `_read_fresh_state_or_raise`（行 556）: `except (OSError, RuntimeFileLockError, RuntimeError) as error: raise FinsUploadFailureError(fins_upload_prevalidation_io_failure()) from error`
  - `_begin_publication_batch_or_raise`（行 582）: 同一模式。
- **测试覆盖**: `test_batch_acquire_runtime_error_maps_to_typed_storage_io`（行 1566-1597）断言 acquire `RuntimeError` 映射为 `fins_upload_prevalidation_io_failure()` 且无 rollback。
- **状态**: 关闭。

### M-F3 — fresh validator 的 `ValueError` 未封闭 → 已关闭 ✅

- **修复证据**: `filing_upload_publication.py:664-665` 在 fresh validator 调用边界增加 `except ValueError: validation_failure = fins_upload_prevalidation_corruption_failure()`。
- **边界收窄**: 只在 fresh validator 调用处捕获，不扩大为通用 `except Exception`。arbitration/rebase 的 `ValueError` 继续走既有 contract。
- **状态**: 关闭。

### C-F1 — checkpoint2 cancelled outcome 使用 fresh request → 已关闭 ✅

- **修复证据**:
  - 第一取消 checkpoint（行 637-643）: `authoritative_request=request`（initial）。
  - 第二取消 checkpoint（行 685-691）: `authoritative_request=request`（initial）。
  - SKIP outcome（行 710）: `authoritative_request=fresh_request`（仅 success 使用 fresh）。
  - PUBLISH outcome（行 752）: `authoritative_request=fresh_request`。
- **测试覆盖**: `test_second_checkpoint_changed_observation_keeps_initial_request_projection`（行 1478-1523）以 `MISSING→COMPLETE`、`_SecondCheckpointCancellationToken` 断言 `outcome.authoritative_request is initial` 且 `resolved_action == "create"`（非 fresh 的 `update`）。
- **状态**: 关闭。

### M-F4 — conflict grid 缺 `REPAIR_REQUIRED→MISSING/COMPLETE` → 已关闭 ✅

- **修复证据**: `test_arbitration_conflict_grid_is_closed_and_typed` 参数化矩阵（行 1144-1159）新增两行：
  - `(REPAIR_REQUIRED, MISSING, "auto", False, False)` → `SOURCE_REVISION_STALE`
  - `(REPAIR_REQUIRED, COMPLETE, "auto", False, False)` → `SOURCE_REVISION_STALE`
- **状态**: 关闭。

### M-F5/M-F6 — runtime terminal 使用 sleep polling / README 不一致 → 已关闭 ✅

- **修复证据**:
  - 新增 concurrent runtime 测试 `test_durable_runtime_concurrent_identical_upload_persists_ok_and_skip_terminals`（行 8648-8713）和 `test_durable_runtime_concurrent_explicit_create_persists_exact_conflict_terminal`（行 8716-8773）使用 `_HoldingExecutor` + `_run_two_held_operations_concurrently` 收集 held operations，终态通过 `ingestion.read_job()` 一次性读取，不调用 `_wait_terminal`，无 `time.sleep`。
  - `tests/README.md:435` 现写 "Filing shared publication matrix 使用真实 filesystem、线程 barrier、spawn 进程与 held runtime operation 的有界 future 完成通知，不使用 sleep、retry 或状态 polling"。该描述与实现一致。
- **注意**: 既有 `_wait_terminal` 函数（行 11319-11345）仍使用 `time.sleep(0.02)` 轮询，但它只被既有的单任务 runtime 测试使用，不在 filing shared publication matrix 范围内。
- **状态**: 关闭。

### D-F1 — different-ticker test 未证明 batch 段并行 → 已关闭 ✅

- **修复证据**:
  - 新增 `_BatchReadBarrierFilingUploadStateRepository`（行 223-280）在 `read_filing_upload_state_in_batch` 内部设置 `barrier.wait(timeout=10)` + `entered.set()`，barrier 位于 `begin_batch` 返回后的 batch 段内。
  - `test_concurrent_distinct_targets_preserve_exact_union`（行 2390-2464）对 `same_ticker=False` 分支使用 `batch_read_barrier=ThreadBarrier(2)` 和 `batch_read_events={"AAPL": Event(), "MSFT": Event()}`。
  - 行 2464: `assert all(entered.is_set() for entered in batch_read_events.values())` — 证明两个不同 ticker 均已进入 writer-owned fresh read 阶段。
  - 两个 worker 各自取得 per-ticker writer 后在 fresh read barrier 会合，证明无 global lock 串行化。
- **状态**: 关闭。

### D-F2(a) — runtime durable conflict terminal 缺失 → 已关闭 ✅

- **修复证据**: `test_durable_runtime_concurrent_explicit_create_persists_exact_conflict_terminal`（行 8716-8773）：
  - 行 8764: `expected_failure = fins_upload_source_publication_conflict_failure().to_json()`
  - 行 8765: `assert failed.request_summary["action"] == "create"`
  - 行 8766: `assert failed.failure_summary == expected_failure`
  - 行 8767: `assert failed.result_summary["failure"] == expected_failure`
  - 行 8768: `assert failed.result_summary["status"] == "failed"`
  - 行 8769: `assert failed.result_summary["stored_file_count"] == 0`
- **同步**: 使用 `_HoldingExecutor` + `_run_two_held_operations_concurrently`，无 sleep。
- **状态**: 关闭。

### D-F2(b) — explicit create conflict terminal 字段不完整 → 已关闭 ✅

- **修复证据**: SEC workflow 测试 `test_concurrent_explicit_create_obeys_overwrite_rebase_contract`（行 2322-2325）：
  - `assert failed["requested_action"] == "create"`
  - `assert failed["resolved_action"] == "create"`
  - `assert failed["filing_action"] == "create"`
  - `assert failed["stored_file_count"] == 0`
- **状态**: 关闭。

### D-F2(c) — same-ticker union 未证明 aliases union → 已关闭 ✅

- **修复证据**: `test_concurrent_distinct_targets_preserve_exact_union` 的 `same_ticker=True` 分支：
  - 行 2425: 第一请求 `ticker_aliases=("MSFT",)`
  - 行 2435: 第二请求 `ticker_aliases=("GOOG",)`
  - 行 2458-2461: `company_meta = company.get_company_meta("AAPL")`，`assert frozenset(aliases) == frozenset({"MSFT", "GOOG"})`。
- **状态**: 关闭。

## Open Questions

- 无。

## Residual Risk

- 未重跑任何测试/pyright（只读审查）；验证数据引用自 `docs/gateflow/uf-fix10-s2-code-review-fix-20260817.md` 中的 evidence：focused owner 16 passed、SEC 并发 4 passed、runtime durable terminal 2 passed、完整 `pytest tests/fins -q` 1916 passed 1 skipped、pyright 0 errors。
- 既有 `_wait_terminal` sleep polling 仍存在于非 publication-matrix 的单任务 runtime 测试中，不影响本次 finding 范围。
- `test_fins_service_runtime.py` 的 `_UnsafeFilingUploadStateRepository` 动态 monkeypatch fake 仍不在 batch read call path，维持 non-blocking residual。
- same-ticker different-filing union 只经 SEC 一条 workflow 端到端验证；CN/HK 无同 ticker 不同 filing union 的 workflow 测试，维持 plan 冻结的 non-blocking residual。
