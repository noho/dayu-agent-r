# F19 RR5 独立最终 plan + wrapper 复审

- 复审时间：2026-08-08 19:16:42 CST
- 二值结论：**PASS**
- Goal Confirmation SHA-256：`ddebaf1159bf8a19fbc3d171e75b88fdb54df23b60089015c03191cf6a3df004`
- 冻结 plan：`docs/gateflow/pr-190-f19-plan-20260808.md`
  - file SHA-256：`8764962d9b615ee1d383d3e933971e2d62c929e8d53825dcd8d1f45edd5eb8ac`
- 冻结 driver：`workspace/tmp/f19_formal_driver.py`
  - file SHA-256：`f3e151283d21715076d9ef8f41f67ba5170fc5114efce5de3b01c0586ddd141e`
- R1-R4 prompt SHA-256：
  - R1：`681d525c10032a315d9c35d5225e4fe1c991a28caa9899681d887027038e6869`
  - R2：`bbe6610654c9d14252b999a49b283188e5174b048106eb7ee9a1971c8b97728d`
  - R3：`82aaf14ad1b7e3f6034fab465c94070ff4359feecfe08008fb0bab647e54e2c6`
  - R4：`345cac1fcb0641ee8f6bc5fe144fdb87a7fd162a79fad34239d190df7c18386f`

## Reviewed target and scope

本轮只审 RR4 的 PTY-owner path finding 是否关闭，并回归 RR3 real-path matrix、repo/CLI location non-goal、RR2 typed canonical Run/R4 prerequisite，以及首轮 identity/deadline/allocation/offset/projection/preflight gates。没有扩展到产品、analyzer、publication implementation或真实模型行为。

全部动态检查均为 provider-free generic scratch 或纯内存 owner-contract 检查；没有创建正式 F19 root/deadline，没有调用 provider，没有修改冻结目标或 F18 evidence。

## Assumptions tested and direct evidence

| 假设 | 直接证据 | 结论 |
| --- | --- | --- |
| PTY owner path 只有一个派生 owner | source 中 `_pty_owner_path` 仅一个定义、两个消费点：execution SHA 与 real-path gate；没有第二份 sibling 推导 | 通过 |
| PTY owner exact 位于 harness 同 controller sibling | generic direct-child 正例中 helper exact 等于 `<root>/evidence/private/controller/prompt_observe_calibration_frozen.py` | 通过 |
| PTY owner 被纳入全部 real-path contract | `checked_paths`、exact expected layout、lexical component `lstat`、existing regular-file检查与resolved root containment均显式包含同一个helper结果 | 通过 |
| regular frozen PTY owner 可正常通过 | regular harness/owner sibling通过 `_validate_real_paths`；execution identity从同一helper读取owner bytes并得到冻结SHA投影 | 通过 |
| root外既有target的owner symlink fail closed | owner bytes读取前拒绝；recorder为`owner_bytes=0, prompt=0, emit=0, spawn=0`，evidence/projection leaves unchanged | 通过 |
| root内既有target的owner symlink fail closed | 同上，leaf `lstat` 在SHA读取前拒绝，不因resolved target仍在root内而放行 | 通过 |
| dangling owner symlink fail closed | 同上，dangling leaf也由lexical `lstat`识别并拒绝 | 通过 |
| RR3完整path matrix无回归 | direct-child正例通过；same-basename outside、F18-like nested、canonical-parent ancestor symlink、run-root、`workspaces`、`evidence`、`private`、其它leaf symlink、traversal与wrong layout全部`prompt=0, emit=0, spawn=0`拒绝且leaves unchanged | 通过 |
| repo/CLI仍不是新absolute-location contract | 任意测试位置在detached target/clean与CLI SHA owner facts正确时通过；wrong CLI SHA拒绝；driver没有canonical repo/CLI parent检查 | 通过 |
| typed canonical R4无回归 | driver无raw `sqlite3`/`host_runs`查询；no compact、wrong session/run、duplicate与strict payload drift拒绝；唯一同session/run且位于第三Run accepted-terminal区间内的event通过strict resolver | 通过 |
| execution identity无回归 | exact target/model/CLI/harness/PTY owner facts通过；wrong model与wrong CLI SHA拒绝 | 通过 |
| allocation无回归 | exact三链通过；第四链与workspace input digest divergence拒绝；profile/ordinal/label/segment bindings保留 | 通过 |
| deadline无回归 | exact 1800秒window、180秒reserve与chain window通过；NaN/Infinity requested timeout与future start拒绝；effective timeout为最小剩余预算 | 通过 |
| offset/prompt无回归 | R1-R4 offset exact `0/1/2/3`；恰一个`--prompt`且正文为单一argv element；真实R1 prompt strict nonempty通过，empty prompt拒绝 | 通过 |
| projection/preflight无回归 | real projection位于spawn前并exclusive create，重写触发`FileExistsError`；preflight stdout-only不写文件 | 通过 |
| tracked plan不泄露machine-local绝对路径 | 常见machine-local absolute path prefix扫描命中为零；`git check-ignore -v`证明driver与全部prompt由`workspace/` ignore rule覆盖 | 通过 |

静态质量检查：source-only compile、Ruff与单文件pyright全部通过；pyright为0 errors、0 warnings。

## Findings

无 material findings。RR4 的 PTY-owner finding 已由唯一helper把execution SHA与real-path gate绑定到同一exact sibling，并通过lexical no-symlink、regular-file与resolved containment三层检查关闭。

## Review lenses

- **Goal-bound minimal design**：修订只补齐frozen harness直接导入的可执行sibling，没有增加新业务目标或publication验收项。
- **Architecture boundary**：PTY owner与harness共同属于run-owned private controller输入；校验发生在owner bytes读取、projection写入与spawn之前。
- **Best practice / optimal solution**：复用一个path helper消除了SHA owner与path gate的semantic drift，三类symlink反例直接证明fail-closed顺序。
- **Overengineering / overcoupling**：未修改Host/Engine、schema或repo/CLI位置contract；新增边界仅覆盖实际执行依赖文件。

## Open questions

无阻塞 open questions。

## Residual risks and tracking

- 本轮只证明provider-free wrapper与owner gate，不证明真实模型自然触发replacement、bounded repair或budget-exhausted fallback；后续正式observation必须按observed/needs-more记录。
- publication digest、secret/path scan、B2 human report与fresh reconnect仍由正式execution/publication gate验证；本次PASS不构成B2裁决或overall readiness。
- private machine-local runs-parent若变化，必须重新冻结并重新review driver，不得由caller/run tree fallback自证。

## Final plan review conclusion

**pass（binary PASS）**。RR4 PTY-owner finding已关闭，RR3 matrix、typed canonical R4与首轮各gate均无回归。该冻结plan + wrapper可进入另一独立reviewer裁决；只有双路均为PASS后，Controller才可创建正式F19 root/deadline并启动provider。
