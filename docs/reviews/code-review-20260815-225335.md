# Code Review — UF-FIX07 Aggregate Re-Review（LOW Finding 修复确认）

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `64050349`
- HEAD: `6b804001`（实施基线）+ 未提交 aggregate fix diff
- Output file: `docs/reviews/code-review-20260815-225335.md`
- Included scope: 仅复核 `docs/reviews/code-review-20260815-224341.md` LOW Finding 1 的修复
  - `tests/fins/test_docling_upload_service.py`：新增四个 guard owner tests
  - `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md`：更新 Finding 状态与验证证据
- Excluded scope: production/README 与上一轮一致，未修改
- Parallel review coverage: 无。单 reviewer 复核四个 ValueError 分支测试的精确命中与安全消息。

## LOW Finding 1 复核：四个 fail-closed guard 无测试覆盖

### Finding 状态：已修复

**原 Finding**（`code-review-20260815-224341.md`）：
- `_build_upload_source_fingerprint()` 四处 `raise ValueError` fail-closed 分支无测试覆盖
- 行号：1267（空 assets）、1269（缺 primary）、1273（identity 未命中）、1318（material 带 primary）

### 修复验证

**新增四个 guard owner tests**：

1. **`test_filing_fingerprint_rejects_empty_original_assets`**（`test_docling_upload_service.py:3948-3968`）：
   ```python
   with pytest.raises(ValueError, match="^filing fingerprint 必须携带非空 originals$"):
       _build_upload_source_fingerprint([], source_kind=SourceKind.FILING, filing_primary=primary)
   ```
   ✅ 精确命中行 1267，安全消息匹配。

2. **`test_filing_fingerprint_rejects_missing_authoritative_primary`**（`test_docling_upload_service.py:3971-3993`）：
   ```python
   with pytest.raises(ValueError, match="^filing fingerprint 必须携带 authoritative primary$"):
       _build_upload_source_fingerprint([asset], source_kind=SourceKind.FILING, filing_primary=None)
   ```
   ✅ 精确命中行 1269，安全消息匹配。

3. **`test_filing_fingerprint_rejects_primary_without_exact_original_match`**（`test_docling_upload_service.py:3996-4023`）：
   ```python
   with pytest.raises(ValueError, match="^filing primary identity 必须 exact 命中一个 original asset$"):
       _build_upload_source_fingerprint([asset], source_kind=SourceKind.FILING, filing_primary=unmatched_primary)
   ```
   ✅ 精确命中行 1273，安全消息匹配。

4. **`test_material_fingerprint_rejects_filing_primary`**（`test_docling_upload_service.py:4026-4056`）：
   ```python
   with pytest.raises(ValueError, match="^material fingerprint 不得携带 filing primary$"):
       _build_upload_source_fingerprint([asset], source_kind=SourceKind.MATERIAL, filing_primary=illegal_primary)
   ```
   ✅ 精确命中行 1318，安全消息匹配。

### 验证证据

- **Focused tests**：`80 passed in 1.21s`（从 76 增加到 80）✅
- **Affected suite**：`1366 passed, 1 skipped, 3 warnings`（从 1362 增加到 1366）✅
- **pyright**：`0 errors, 0 warnings, 0 informations` ✅
- **docling coverage**：`87%`（从 85% 提升，行 1267/1269/1273/1318 转绿）✅
- **diff check**：clean ✅

### Finding 1 结论：PASS

四个 ValueError 分支均有精确 `pytest.raises(ValueError)` 断言，安全消息完全匹配，无测试弱化或越界。production/README 未修改。

## 原 Aggregate Findings 闭环确认

### Finding 1（role-aware fingerprint）：已修复，仍闭环

- `_UploadSourceFingerprint` typed result 正确编码角色 ✅
- single-file v1 / multi-file v2 公式分离 ✅
- ambiguous descriptor 使用 conservative unsafe contract ✅
- skip/version owner 职责清晰 ✅
- 本轮未修改 production，闭环状态不变 ✅

### Finding 2（README role order）：已修复，仍闭环

- README 已改为 primary-first role order 描述 ✅
- 本轮未修改 README，闭环状态不变 ✅

### Re-review Finding 1（LOW）：已修复

- 四个 guard owner tests 精确覆盖 ✅
- 安全消息匹配 ✅
- 无测试弱化/越界 ✅

## 全范围 Regression 确认

- Semantic ownership ✅
- Atomicity ✅
- 100-file limit ✅
- Help / Tool schema ✅
- Material regression ✅
- No-shim / No-dual-read ✅

## Open Questions

- 无。

## Residual Risk

- R1：primary 与 companion descriptor 完全相同时无法在不引入 path/order identifier 下区分；ambiguous replay 会持续 version churn。
  **分类**：accepted conservative boundary。
- R2：当前已持久化的旧无角色 multi-file digest 首次按 v2 upsert 会 update/version increment。
  **分类**：fixed in current aggregate fix。
- R3：旧 basename-based source schema 的兼容、首次重传或自动修复。
  **分类**：assigned to later work unit `UF-FIX08`。
- R4：同 request/同 document 并发 writer。
  **分类**：assigned to later work unit `UF-FIX10`。
- R5：fresh company meta warning。
  **分类**：assigned to later work unit `UF-FIX11`。
- R6：optional real Docling 与 UF-PF07/UF-PF12 未执行。
  **分类**：assigned to later evidence work。
- R7：registry/oracle/frozen evidence 仍记录修复前观察。
  **分类**：assigned to later evidence work。
- R8：全仓 pytest 的 9 个 failures。
  **分类**：pre-existing unrelated。base `64050349` worktree 同样复现；归 host/service/CLI 对应 owner。

所有 residual risks 已分类；没有 blocking open question。

## 结论

**Decision**：`AGGREGATE RE-REVIEW PASS`

**Finding 状态**：

| Finding | 状态 | 说明 |
| --- | --- | --- |
| Finding 1（role-aware fingerprint） | 已修复，仍闭环 | production 未变，闭环状态不变 |
| Finding 2（README role order） | 已修复，仍闭环 | README 未变，闭环状态不变 |
| Re-review Finding 1（LOW） | 已修复 | 四个 guard owner tests 精确覆盖，安全消息匹配 |

**验证证据**：
- focused tests：`80 passed` ✅
- affected suite：`1366 passed, 1 skipped, 3 warnings` ✅
- pyright：`0 errors` ✅
- docling coverage：`87%`（从 85% 提升）✅
- diff check：clean ✅

**Output file**：`docs/reviews/code-review-20260815-225335.md`
