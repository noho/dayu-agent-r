# Code Review

## Scope

- Mode: current changes (delta re-review)
- Branch: `codex/upload-filing-oracle`
- Base: `0391b589de075f47a2c13f8e173e48e3ae0f1c5e`
- Target: `ef4e432427a8e670fed83904118f9efe39b94473`
- Output file: `docs/reviews/code-review-20260813-125439.md`
- Included scope: 5 files in delta（`dayu/fins/service_runtime.py`、`dayu/cli/commands/fins.py`、`tests/fins/test_fins_service_runtime.py`、`tests/cli/test_fins_commands.py`、`docs/gateflow/uf-fix01-validation-atomic-boundary-implementation-fix-20260813.md`）+ 直接依赖 production symbol（`FsFilingUploadStateRepository`、`_FsStorageInfra.__init__`、`FinsUploadPrevalidationError`、`fins_upload_prevalidation_io_failure`）
- Excluded scope: 其它非 delta 文件、测试运行、subagent
- Parallel review coverage: 无（按约束禁止 subagent）

## Adjudication reference

- 裁决文档：`docs/gateflow/uf-fix01-validation-atomic-boundary-implementation-fix-rereview-adjudication-20260813.md`
- R1 裁决：接受，阻塞。prevalidation repository construction 必须处于 typed operational boundary。
- R2 裁决：接受，阻塞。docstring 必须反映 typed contract。

## Findings

未发现实质性问题。

### R1 核销 — repository construction + read 同 typed try

**verified.** `dayu/fins/service_runtime.py:81-90`：`FsFilingUploadStateRepository(workspace_root, create_directories=False)` 与 `repository.read_filing_upload_state(ticker, document_id)` 现在位于同一个 `try` 块内。

构造链路证据：
1. `FsFilingUploadStateRepository.__init__` → `build_fs_repository_set(workspace_root, create_directories=False)` → `FsStorageCore(workspace_root=workspace_root, ...)`
2. `FsStorageCore` 继承 `_FsStorageInfra`，其 `__init__`（`_fs_storage_infra.py:437`）执行 `self.workspace_root = workspace_root.resolve()`
3. `Path.resolve()` 对无权限目录抛出 `PermissionError`（`OSError` 子类）

I/O mapping 证据：`except (OSError, RuntimeFileLockError) as exc` → `raise FinsUploadPrevalidationError(fins_upload_prevalidation_io_failure()) from exc`。`OSError` 覆盖 `PermissionError`，`RuntimeFileLockError` 覆盖 lock failure。corruption 由 `except ValueError` 覆盖。三个异常类型完整映射到 typed reason。

Cause chain 证据：`from exc` 保留原始 cause。测试断言 `exc_info.value.__cause__` 为 `PermissionError`，其 `__cause__` 为注入的底层 `PermissionError`，构成完整 cause chain。

### R1 核销 — CLI exit 1 exact path-free / operator log / fresh zero mutation

**verified.** `tests/cli/test_fins_commands.py:1184-1267`：
- `assert exit_code == EXIT_FAILURE`（exit 1）
- `assert captured.err == "dayu-cli upload_filing: 上传状态读取失败，请检查工作区存储状态\n"`（exact path-free stderr，不含 `str(tmp_path)`）
- `assert "PermissionError" not in captured.err`（无内部异常泄露）
- `assert "PermissionError" in operator_diagnostic`（operator log 保留底层 cause）
- `assert "解析 storage workspace底层文件系统失败" in operator_diagnostic`（operator log 保留中文 cause message）
- `assert not workspace_root.exists()`（fresh workspace 零 mutation）

### R1 核销 — owner-level typed reason test

**verified.** `tests/fins/test_fins_service_runtime.py:19-80`：
- 注入 `Path.resolve` 使 workspace_root 第二次 resolve 抛出 `PermissionError`
- 断言 `exc_info.value.failure == fins_upload_prevalidation_io_failure()`（typed reason 精确匹配）
- 断言 `isinstance(projected_error, PermissionError)` 与 cause chain 两层深度
- 断言 `not workspace_root.exists()`（零 mutation）

### R2 核销 — docstring typed contract

**verified.** `dayu/cli/commands/fins.py:669-671`：异常说明从 `OSError` / `ValueError` 改为 `FinsUploadUsageError` / `FinsUploadPrevalidationError`，与 `service_runtime.py:75-77` 的 docstring 一致，与实际 `except` 契约一致。diff 仅改 docstring，不改变运行语义。

### 负面检查 — 无 CLI fallback / string classification / non-goal diff

**verified.**
- `dayu/fins/service_runtime.py` delta：只有 `try` 块扩大范围（repository construction 移入），无新增 `hasattr`/`getattr`、`str(exc)` 分类、generic CLI fallback 或 compatibility shim。
- `dayu/cli/commands/fins.py` delta：只有 docstring 异常说明变更，无运行语义变更。
- `docs/gateflow/` delta：只追加 implementation fix 记录段，不改变已有内容。
- 测试 delta：两个新增测试函数，不修改已有测试。

## Open Questions

- 无。

## Residual Risk

- 未运行全仓 pytest（按约束禁止）；delta 只涉及 prevalidation try boundary 移动与 docstring 修正，直接受影响的 79 个测试已由 implementation fix artifact 记录为通过。
- 未运行 pyright（按约束禁止）；delta 不新增类型签名或 import，不存在引入类型错误的路径。
- 无其它未覆盖风险。

## Gate verdict

- **R1**: **CLOSED** — **PASS**
- **R2**: **CLOSED** — **PASS**
