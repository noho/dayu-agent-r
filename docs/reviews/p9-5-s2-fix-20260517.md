# P9.5 S2 Code Review Fix

## Scope

- Gate: P9.5 S2 code review fix follow-up.
- Source adjudication: `docs/reviews/p9-5-s2-code-review-controller-adjudication-20260517.md`.
- Source review: `docs/reviews/p9-5-s2-code-review-ds-20260517.md`.
- Fixed only controller-accepted findings F1/F2/F3.
- No commit, push, PR, review, or re-review action performed.

## Accepted Findings Fixed

- F1: removed dead `_OpenAIUsage` private `TypedDict` and its `__all__` entry from
  `dayu/engine/runners/openai/_types.py`.
- F2: rejected bool-valued tool-call `index` in
  `SSEParser._coerce_tool_call_delta()`, so malformed `true` / `false` indexes
  no longer enter `_OpenAIToolCallDelta` as semantic integers.
- F3: rejected bool-valued tool-call `index` in
  `ToolCallAggregator._resolve_index()`, preserving the aggregator as a second
  defensive boundary when a malformed internal delta is fed directly.

## Changed Files

- `dayu/engine/runners/openai/_types.py`
  - Removed `_OpenAIUsage` and its private export.
- `dayu/engine/runners/openai/sse_parser.py`
  - Reused the parser-internal `_is_tool_call_index()` check before accepting
    raw provider `index`.
- `dayu/engine/runners/openai/tool_call_aggregator.py`
  - Added `_is_tool_call_index()` using `TypeGuard[int]` and rejecting `bool`.
  - Applied it in `_resolve_index()`.
- `tests/engine/runners/openai/test_sse_tool_call_stream.py`
  - Extended id-fallback coverage to prove bool indexes are ignored and calls
    remain separated by id.
  - Added direct aggregator coverage for bool index fallback.

## Validation

- `source .venv/bin/activate && pytest tests/engine/runners/openai tests/engine/test_metadata_boundary.py tests/engine/test_engine_event_contract.py`
  - Result: passed, 229 tests.
- `source .venv/bin/activate && python -m pyright dayu/engine tests/engine`
  - Result: passed, 0 errors, 0 warnings, 0 informations.
- `git diff --check`
  - Result: passed.

## Residual Risks

- No WARN diagnostic was added for bool index rejection. The parser now treats
  bool index as an invalid/missing index and follows the existing id/position
  fallback behavior without logging raw payload.
- No provider public state, public contract, retry behavior, Host governance,
  memory/tool governance, proactive context governance, or P10+ semantics were
  introduced.

## Stop Status

- F1/F2/F3 fix complete.
- Required validation complete.
- No blocking stop condition encountered.
- Awaiting S2 code re-review.
