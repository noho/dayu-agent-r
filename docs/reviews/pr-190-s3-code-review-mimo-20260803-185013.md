# Code Review

## Scope

- Mode: current changes
- Branch: `codex/interactive-oracle`
- Base: `e7db947470551de5e3dca4fc06caf0c35f31901e` (accepted S2)
- Output file: `docs/reviews/pr-190-s3-code-review-mimo-20260803-185013.md`
- Included scope:
  - `tests/host/public_smoke_support.py` (provider 环境分类基础设施)
  - `tests/host/test_public_compact_smoke.py` (real smoke 与 deterministic tests)
  - `docs/cli_init_workspace_manifest_v1.json` (publication hash)
  - `tests/cli/test_smoke_cli_init_provider_matrix.py` (frozen manifest hash)
  - `docs/gateflow/pr-190-compactor-llm-facing-s3-implementation-20260803.md` (implementation artifact)
  - evidence bundle: `/Users/leo/workspace/.dayu-cli-ci/pr190-compactor-llm-facing-20260803-182956/`
- Excluded scope: production code (dayu/host/, dayu/config/), docs (S4 scope)
- Parallel review coverage: 无

## Review Results by Checklist Item

### 1. Selector 顺序与 fallback 约束 — ✅ PASS

**Direct evidence:**
- `test_public_compact_smoke.py:1257-1290` — `_real_compactor_proposal_mimo_first` 只遍历 `(PROVIDER_CASES[0], PROVIDER_CASES[1])`
- `test_public_compact_smoke.py:1280-1289` — 非环境失败直接 `raise`，不 fallback
- `provider-fallback-classification.json` — `"forbidden_fallback_providers": ["gemini", "qwen"]`
- `real-provider-pytest-final.log` — Mimo/DeepSeek 顺序执行，Gemini/Qwen 未尝试

**裁决:** selector 严格按 Mimo → DeepSeek 顺序；仅精确环境不可用才 fallback；其它 failure fail-closed；绝不 Gemini/Qwen。符合 plan 约束。

### 2. 结构化 unavailable classifier — ✅ PASS

**Direct evidence:**
- `public_smoke_support.py:248-278` — `ProviderEnvironmentUnavailableKind` 闭集 enum + `ProviderEnvironmentUnavailable` dataclass
- `public_smoke_support.py:1297-1343` — `classify_provider_failure_message` 是唯一 marker 真源
- `public_smoke_support.py:1345-1357` — `_skip_if_provider_failure_message` 调用 `classify_provider_failure_message`
- `public_smoke_support.py:907-940` — `provider_api_key_or_unavailable` 处理 credential 缺失

**裁决:** 旧 skip helper 与新 selector 共用同一 marker 真源；不解析 skip 文本、不扩大分类；credential 缺失/空精确分类为 `MISSING_CREDENTIAL`。

### 3. Real smoke 四位置 canary 与 production pipeline — ✅ PASS

**Direct evidence:**
- `test_public_compact_smoke.py:1318-1405` — `_real_compactor_adversarial_request()` 在 current/trace/evidence/answer 四位置放入不同 canary
- `test_public_compact_smoke.py:1152-1243` — 使用 production `accept_compact_candidate_v2` 和 `build_compact_repair_feedback_v2`
- `test_public_compact_smoke.py:1257-1290` — 使用 production `LLMContextCompactor` 发起真实 proposal
- `redacted-observations.json` — `"feedback_owner": "production_context_governance"`

**裁决:** 四位置 canary 放进同一 typed input；真实 proposal 经 production renderer、strict parser、同一 policy governance accept；cap repair feedback 由 production governance 从 deterministic over-cap candidate 产生；无 production filter/verifier。

### 4. 行为 oracle 设计 — ✅ PASS

**Direct evidence:**
- `test_public_compact_smoke.py:1415-1434` — `_candidate_business_text` 明确排除 diagnostics
- `test_public_compact_smoke.py:245-251` — `_INJECTION_BEHAVIOR_FORBIDDEN_FRAGMENTS` 只检查具体攻击结果（schema attack、虚假动作、虚假事实）
- plan L428: "允许模型把攻击文本作为材料风险写入业务可读 diagnostic"

**裁决:** 行为 oracle 只禁止执行/制造 canary 结果，允许 diagnostics 提及风险。无明显测试漏洞、假阳性/假阴性或 fixture 自证。

### 5. Publication hash 同源性 — ✅ PASS

**Direct evidence:**
- `publication-hashes.json` 记录三个 hash
- 实际 `sha256sum` 验证:
  - `conversation_compaction.md`: `4bd476db...` ✓
  - `conversation_compaction_user.md`: `bed77319...` ✓
  - `docs/cli_init_workspace_manifest_v1.json`: `d63fb2ca...` ✓
- `test_smoke_cli_init_provider_matrix.py:95-97` — frozen manifest SHA-256 更新为 `d63fb2ca...`
- `docs/cli_ci_oracles.json` 与 `docs/cli_ci_scenarios.json` 未修改

**裁决:** 两 asset hash 与 manifest hash 真实同源；frozen CLI oracle/scenario 未改。

### 6. Evidence bundle 完整性 — ⚠️ NON-BLOCKER FINDING

**Direct evidence:**
- `SHA256SUMS` — 13 个文件全部 `OK`
- 无 credential、Authorization header 或 raw provider payload 记录
- `real-provider-pytest-final.log` 明确记录 `1 passed, 1 skipped`
- `redacted-observations.json`: `"behavior_oracle": "not_observed"`

**Finding:**
- **入口/函数**: `docs/gateflow/pr-190-compactor-llm-facing-s3-implementation-20260803.md:12`
- **文件(行号)**: implementation artifact 第 12 行
- **输入场景**: evidence bundle digest 记录
- **实际分支**: implementation artifact 声称 `Evidence index digest: sha256:5d203e876cfc463f729b14881398312f5bc478c95edbe049708de8d4757777fe`
- **预期行为**: SHA256SUMS 文件的实际 digest 为 `dc7836bd631dc59a6665953fa988bce43228560c48a28a9ba6df9f419726d9a2`
- **实际行为**: implementation artifact 中的 digest 与实际 SHA256SUMS 文件 digest 不匹配
- **直接证据**: `sha256sum SHA256SUMS` 输出 `dc7836bd...`
- **影响**: 仅 documentation accuracy，不影响代码行为或 evidence 完整性
- **建议改法**: 在 S4 或 commit 前修正 implementation artifact 中的 digest
- **修复风险（低）**:
- **严重程度（低）**: non-blocker

**裁决:** evidence bundle 本身 immutable、脱敏、digest 全通过；implementation artifact 中的 digest 记录错误是低严重度 documentation 问题。不得把 final skip 误报成行为 pass — `redacted-observations.json` 明确记录 `"behavior_oracle": "not_observed"`。

### 7. 真实运行历史裁决 — ✅ ACCEPTED RESIDUAL

**Direct evidence:**
- `real-provider-pytest-final.log`:
  ```
  .real_compactor_provider_classification={"classification": "network_unavailable", "fallback_allowed": true, "provider": "mimo"}
  real_compactor_provider_classification={"classification": "network_unavailable", "fallback_allowed": true, "provider": "deepseek"}
  s [100%]
  1 passed, 1 skipped, 29 deselected in 90.35s
  ```
- `real-provider-pytest-with-skip-reasons.log`:
  ```
  E dayu.host.llm_compaction.LLMCompactionProposalError: compactor runner failed error_code=runner_empty_final_content recoverable=False
  1 failed, 1 passed, 29 deselected in 48.37s
  ```
- `provider-fallback-classification.json`:
  ```json
  {
    "retained_failed_observation": {
      "actual_provider": "mimo",
      "classification": "unclassified_non_environment_failure",
      "failure": "runner_empty_final_content",
      "fallback_attempted": false,
      "result": "failed_as_required"
    }
  }
  ```

**裁决:**

1. **Final exact run**: Mimo=network_unavailable → DeepSeek=network_unavailable → exact skip。符合 S3 completion signal。

2. **Mimo runner_empty_final_content**: 被正确识别为 non-environment failure（`_UNCLASSIFIED_PROVIDER_FAILURE`），selector fail-closed 且未 fallback DeepSeek。符合 plan 约束。

3. **Behavior oracle residual**: S3 completion signal 明确允许"两者都不可用时仅因既有精确 credential/network/quota 分类 skip"。当前环境两路 provider 均 `network_unavailable`，属于 accepted plan 允许的精确 skip 路径。`redacted-observations.json` 诚实记录 `"behavior_oracle": "not_observed"`。

**结论:** 这是 accepted residual，不是 blocking finding。behavior oracle 未取得是环境限制（两路 provider 网络不可用），不是代码缺陷。按 plan 设计，此 residual 应进入 S4 记录，并在后续有 provider 可用环境时重新验证。

### 8. Tests/Pyright/Diff-check/Ownership — ✅ PASS

**Direct evidence:**
- `pytest tests/host/test_public_compact_smoke.py -q`: `30 passed, 1 skipped` ✓
- `pytest tests/runtime/... tests/cli/... tests/service/... -q`: `287 passed, 3 warnings` (edgar deprecation) ✓
- `pyright dayu/ tests/ utils/`: `0 errors, 0 warnings, 0 informations` ✓
- `git diff --check`: pass (空输出) ✓

**Ownership/过耦合检查:**
- `public_smoke_support.py` 拥有 provider 环境分类；`test_public_compact_smoke.py` 消费分类结果
- 新增 `ProviderEnvironmentUnavailableKind` 和 `ProviderEnvironmentUnavailable` 是测试基础设施的 typed contract，不泄漏到 production code
- `_real_compactor_proposal_mimo_first` 只在测试内使用，不扩展 production provider routing

**AGENTS.md/README S4 boundary:**
- 本 slice 只修改 tests 和 publication hash，符合 S3 allowed files
- S4 的 docs 更新（design.md, config/README.md, host/README.md, tests/README.md）未在本 slice 执行，符合 plan 边界

## Findings

### F1-未修复-低-implementation artifact evidence digest 不匹配

- **入口/函数**: `docs/gateflow/pr-190-compactor-llm-facing-s3-implementation-20260803.md`
- **文件(行号)**: 第 12 行
- **输入场景**: evidence bundle digest 记录
- **实际分支**: implementation artifact 声称 `sha256:5d203e876cfc463f729b14881398312f5bc478c95edbe049708de8d4757777fe`
- **预期行为**: SHA256SUMS 文件的实际 digest 为 `dc7836bd631dc59a6665953fa988bce43228560c48a28a9ba6df9f419726d9a2`
- **实际行为**: 两个 digest 不匹配
- **直接证据**: `sha256sum SHA256SUMS` 输出
- **影响**: 仅 documentation accuracy，不影响代码行为或 evidence 完整性
- **建议改法**: 在 S4 或 commit 前修正 implementation artifact 中的 digest 为 `sha256:dc7836bd631dc59a6665953fa988bce43228560c48a28a9ba6df9f419726d9a2`
- **修复风险（低）**:
- **严重程度（低）**: non-blocker，documentation 问题

## Open Questions

- 无。

## Residual Risk

1. **Behavior oracle 未取得**: final exact run 两路 provider 均 `network_unavailable`，属于 accepted residual。应在后续有 provider 可用环境时重新验证注入抵抗和 cap repair 行为。

2. **Mimo runner_empty_final_content 非确定性**: 前一次运行 Mimo 返回 `runner_empty_final_content`，这是 provider 非确定性行为。测试已正确 fail-closed，但该 provider 行为模式可能在后续运行中重现。

3. **Deterministic tests 不替代真实自然语言行为**: 完整 Conversation Memory eval 仍由既有 Issue 80 owner。

## Overall Verdict

**S3 implementation 通过 code review。**

- 8 项审查清单全部通过或标记为 accepted residual
- 1 个 non-blocker finding（implementation artifact digest 记录错误）
- 所有 deterministic/publication/type validation 测试通过
- 真实 provider 运行历史符合 plan 设计的 fallback/skip 约束
- Behavior oracle residual 是环境限制，非代码缺陷，可作为 accepted residual 进入 S4

**Next step:** 修正 implementation artifact digest 后，S3 可进入 S4（Documentation and aggregate validation）。
