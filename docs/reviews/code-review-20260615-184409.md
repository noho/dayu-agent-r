# Code Review Re-Review

## Scope

- Mode: current changes (re-review)
- Work unit: `WU-CLI-FINS-OBS-01`
- Gate: re-review
- Slice: `S1-fins-job-event-contract`
- Agent: AgentMiMo
- Branch: `phaseflow/host-issues-implementation`
- Base: `HEAD`（当前未提交 workspace changes）
- Output file: `docs/reviews/code-review-20260615-184409.md`
- Included scope:
  - `dayu/fins/ingestion_runtime.py`（修改）
  - `tests/fins/test_fins_ingestion_runtime.py`（修改）
  - `dayu/fins/README.md`（修改）
  - `tests/README.md`（修改）
- Fix artifact: `docs/reviews/wu-cli-fins-obs-01-s1-fix-codex.md`
- Original reviews:
  - `docs/reviews/code-review-20260615-183010.md`
  - `docs/reviews/code-review-20260615-183203.md`
- Adjudication: `docs/reviews/wu-cli-fins-obs-01-s1-code-review-adjudication-20260615-183453.md`
- Parallel review coverage: 无

## Accepted Findings Status

| Finding | 描述 | 状态 | 证据 |
|---|---|---|---|
| MiMo-001 | non-terminal event append failure WARN test coverage | 已修复 | 新增 `test_non_terminal_event_append_failure_warns_and_job_still_succeeds`（行 1625-1689）：monkeypatch 使 `JOB_RUNNING` append 抛出 `OSError`；断言 job 仍 `SUCCEEDED`；断言 sidecar 只含 `JOB_QUEUED` + `JOB_SUCCEEDED`；断言 caplog 含 `fins.ingestion.job_event_append_failed`、`job_id`、`event_type=job_running`、`error_type=OSError`、`error_summary=event_append_failed`；断言原始错误消息不泄漏。 |
| DS-F002 | removal of event type re-export from `ingestion_runtime.__all__` | 已修复 | `ingestion_runtime.__all__`（行 3979-4004）不含 `FinsIngestionJobEventAppend`、`FinsIngestionJobEventRecord`、`FinsIngestionJobEventType`。测试文件（行 25-29）从 `dayu.fins.ingestion_events` 导入，使用 canonical 路径。 |
| DS-F003 | `dayu/fins/README.md` 和 `tests/README.md` updated under README constraints | 已修复 | `dayu/fins/README.md`：public interface 列表新增 `read_job_events(...)`；public contracts 新增 `dayu.fins.ingestion_events` canonical 入口；storage 说明新增 event sidecar；runtime 说明新增 event sidecar 语义段落。`tests/README.md`：新增 event sidecar 序列、游标读取、bounded payload、并发 sequence、非法 payload 拒绝、non-terminal/terminal WARN 行为等覆盖描述。 |

## Deferred/Rejected Findings Verification

| Finding | 描述 | 预期状态 | 实际状态 |
|---|---|---|---|
| MiMo-002/DS-F001 | `_last_event_sequence_locked` O(n) full read | deferred to S2，不应修复 | 未修复。`_last_event_sequence_locked` 仍遍历全部 event records。 |
| MiMo-003 | empty payload WARN formatting | rejected，不应修复 | 未修复。WARN 日志仍无条件输出 `payload_keys`。 |
| — | terminal duplicate idempotency | not required，不应实现 | 未实现。`_append_terminal_job_event_warn` 无幂等守卫。 |

## Evidence References

- `tests/fins/test_fins_ingestion_runtime.py:1625-1689` — MiMo-001 修复证据
- `dayu/fins/ingestion_runtime.py:3979-4004` — DS-F002 修复证据（`__all__` 不含事件类型）
- `tests/fins/test_fins_ingestion_runtime.py:25-29` — DS-F002 修复证据（canonical import path）
- `dayu/fins/README.md` diff — DS-F003 修复证据
- `tests/README.md` diff — DS-F003 修复证据

## Remaining Blockers

无。

## Residual Risks

- `_last_event_sequence_locked` O(n) 全量读取仍为 deferred S2 事项。
- S2 引入带自定义 payload 的 PROGRESS 事件后，payload 构造 bug 可能被 `_append_job_event_warn` 捕获并 WARN 吞掉，需在 S2 review gate 检查。

## Conclusion

**PASS**。全部 3 个 accepted findings 已修复，3 个 deferred/rejected findings 保持未修复状态，符合 adjudication 要求。
