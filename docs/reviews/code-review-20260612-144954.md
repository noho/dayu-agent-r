# Code Review

## Metadata

- Reviewer: AgentDS
- Mode: current changes scoped review
- Work unit: WU-CM-05 LLM Compaction Proposal Typed Parsing
- Slice: WU-CM-05-S2 invalid proposal diagnostics coverage
- Previous slice commit: 7f2ce2c5
- Plan artifact: docs/host/host-issues/wu-cm-05-llm-compaction-proposal-typed-parsing-plan.md
- Implementation report: docs/reviews/wu-cm-05-s2-implementation-report.md
- Artifact path: docs/reviews/code-review-20260612-144954.md
- Base: 7f2ce2c5 (S1 accepted commit)

## Scope

- Mode: current changes (S2 working tree diff only)
- Branch: work/cm-05-06-08-09
- Included scope:
  - dayu/host/llm_compaction.py S2 diff (one change in `_required_enum`)
  - tests/host/test_llm_compaction.py S2 diff (12 new tests, 2 updated tests, 1 new helper)
  - docs/reviews/wu-cm-05-s2-implementation-report.md
- Excluded from code findings:
  - docs/host/issues-implementation-control.md (Controller bookkeeping dirty file, state-check only)
  - S1 accepted commit 7f2ce2c5 (already reviewed, not re-reviewed unless S2 interaction)
- Parallel review coverage: 无

## Verification

- Tests: 36 passed in 0.26s (py311, source .venv/bin/activate)
- Pyright: 0 errors, 0 warnings, 0 informations (dayu/host/llm_compaction.py, tests/host/test_llm_compaction.py)
- Implementation report claims verified against actual code and test output

## Entry Point Trace

S2 唯一生产变更在 `_required_enum` (`dayu/host/llm_compaction.py:1087-1115`)：

```
LLM raw final answer
  → parse_conversation_compact_output_vnext (line 575, public)
    → _parse_vnext_proposal (line 598)
      → json.loads → _json_object → _required_enum (4 处调用)
        → _required_string → _required_value → source[key]
        → redact_sensitive_diagnostic_values (dayu.runtime.diagnostic_text)
        → truncate_diagnostic_text (dayu.runtime.diagnostic_text)
        → raise ValueError("{field_path} has invalid enum value: {safe_value}")
      → ConversationCompactOutputVNext(...)
    → _validate_vnext_candidate_source_labels
  → LLMCompactionProposalError wrap (line 593-594)
```

`_required_enum` 被 4 个 enum 字段调用：
1. `evidence_backed_facts[i].evidence_kind` (line 672-678, `_FACT_EVIDENCE_KIND_VALUES`)
2. `forward_intents[i].intent_type` (line 763-769, `_FORWARD_INTENT_TYPE_VALUES`)
3. `forward_intents[i].status` (line 772-778, `_FORWARD_INTENT_STATUS_VALUES`)
4. `reference_continuity_items[i].reason` (line 806-812, `_REFERENCE_CONTINUITY_REASON_VALUES`)

S2 变更将 `_required_enum` 的 `ValueError` 消息从：
```
{field_path} has invalid enum value
```
变为：
```
{field_path} has invalid enum value: {safe_value}
```
其中 `safe_value` 经过 `redact_sensitive_diagnostic_values`（Bearer/API key/password/secret/token 模式替换为 `<redacted>`）和 `truncate_diagnostic_text`（240 字符硬上限，超出加 `...` 后缀）。

## Findings

### 1-未修复-低-forward_intents[0].intent_type 枚举值诊断缺少显式测试

- **入口/函数**: `_required_enum` → `_forward_intent_candidates_vnext` (line 763-769)
- **文件(行号)**: dayu/host/llm_compaction.py:763-769, tests/host/test_llm_compaction.py (测试列表)
- **输入场景**: LLM 返回 `forward_intents[0].intent_type` 为非法枚举值（如 `"bad_intent_type"`）时
- **实际分支**: `_required_enum` 在 line 1108 检测到 `value not in _FORWARD_INTENT_TYPE_VALUES`，生成 redacted+truncated safe_value 并抛出 `ValueError`
- **预期行为**: S2 的 `_required_enum` 变更为所有 4 个 enum 字段统一生成 `{field_path} has invalid enum value: {safe_value}` 诊断；`intent_type` 的诊断行为与已测试的 `evidence_kind`、`status`、`reason` 完全相同
- **实际行为**: 3 个 enum 字段（`evidence_kind`、`status`、`reason`）有 `path.*value` 诊断格式断言；`intent_type` 仅有 S1 级别的路径断言（通过旧测试间接覆盖），没有显式的 `forward_intents[0].intent_type.*<bad_value>` 格式断言
- **直接证据**: 
  - S2 新增 `test_parse_conversation_compact_output_vnext_reports_forward_status_enum_value` 覆盖 `status`（line 480-507）
  - S2 新增 `test_parse_conversation_compact_output_vnext_reports_reference_reason_enum_value` 覆盖 `reason`（line 510-536）
  - S2 更新 `test_parse_conversation_compact_output_vnext_reports_fact_evidence_kind_path` 覆盖 `evidence_kind`（line 450-477）
  - 无对应 `intent_type` 测试
- **影响**: 局部测试覆盖缺口。`_required_enum` 是共享 helper，其他 3 个字段的测试已充分验证诊断格式；`intent_type` 的行为由共享代码路径保证，不会被遗漏。但若未来有人对 `intent_type` 做字段级特殊处理（如不同的 error message 模板），该变更不会被现有测试捕获。
- **建议改法和验证点**: 方案 A：在 Slice 3 中添加 `forward_intents[0].intent_type` 枚举值诊断测试，与其他 3 个 enum 字段对齐。方案 B：接受此缺口为 plan scope 限制（plan 明确只要求 3 个 enum 字段），不改动。
- **修复风险（低）**: 纯测试添加，不涉及生产代码变更。
- **严重程度（低）**: 共享 helper 已验证 3 次，代码路径完全一致，实际遗漏风险极低。

## Open Questions

无。

## Residual Risk

1. **DS-F02 延期至 Slice 3**：`_proposal_json` (line 761-775) 和 `_required_mapping` (line 821-831) 仍使用 `cast`。此风险已由 S1 review 记录并在 implementation report 中明确延期至 Slice 3，不阻塞 S2。

2. **`forward_intents[0].intent_type` 枚举值诊断测试缺口**：见 Finding 1。此缺口为 plan scope 限制（plan Slice 2 仅列出 3 个 enum 字段测试），非实现缺陷。建议在 Slice 3 或后续 WU 中补齐。

3. **枚举值脱敏覆盖范围**：`redact_sensitive_diagnostic_values` 仅覆盖 Bearer token、API key、password、secret、token 赋值模式。对于 enum 字段的典型非法值（如 `"bad_evidence_kind"`），这完全足够。若未来 LLM 在 enum 字段中产生非凭证类的敏感内部标识符（如 event_id 格式字符串），当前脱敏不会捕获。但此类值已在 LLM response trace 中存在，且 240 字符截断限制了滥用窗口，风险可接受。

4. **S1-S2 交互**：S2 对 `_required_enum` 的错误消息格式变更（追加 `: {safe_value}`）是纯增量。S1 唯一断言 enum 路径的测试 `test_parse_conversation_compact_output_vnext_reports_fact_evidence_kind_path` 已同步更新 match pattern，旧 pattern 仅匹配路径前缀，不会因为新消息格式而误通过或误失败。无 S1-S2 交互回归。

## Controller Notes

- Implementation report 声称的 36 passed / pyright 0 errors 已独立验证通过。
- DS-F01（S1 finding）已在 `_required_enum` 中以 redacted+truncated actual value 正确处理。
- No commit / push / PR / README change required by this slice.
