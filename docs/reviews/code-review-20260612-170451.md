# WU-CM-08-S1 Code Review

- 日期：2026-06-12
- 审查人：AgentMiMo
- 范围：dirty workspace diff（`tests/host/test_compact_material.py`、`docs/host/issues-implementation-control.md`、`docs/reviews/wu-cm-08-s1-implementation-report.md`）
- Plan：`docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md`

## 结论

**PASS**

## 审查逐项

### 1. S1 是否保持 test-only，没有生产语义/schema/API 变化

✅ 通过。

- `git diff HEAD -- dayu/` 为空，无生产代码变更。
- 变更文件仅限：`tests/host/test_compact_material.py`（测试）、`docs/host/issues-implementation-control.md`（控制文档状态更新）、`docs/reviews/wu-cm-08-s1-implementation-report.md`（实现报告）。
- 控制文档不在 plan 的 "allowed files" 列表中，但 work unit 状态转移更新控制文档是标准治理行为，不构成 scope violation。

### 2. _MaterialPackShape/_VNextInputShape helper 是否符合 plan 的 typed contract

✅ 通过。

- `_MaterialPackShape` 和 `_VNextInputShape` 均为 `NamedTuple` 子类，所有字段具类型标注。
- 无 `Any`、`object`、无类型容器或 untyped 参数。
- `_material_pack_shape()` 和 `_vnext_input_shape()` 函数签名完整类型化，返回值类型由 `NamedTuple` 隐式保证。
- 断言 helper（`_assert_material_pack_shape`、`_assert_vnext_input_shape`）使用 f-string 输出 boundary-specific 失败消息，包含 expected/observed repr。

### 3. vNext top-level key/section count 断言是否正确

✅ 通过。

- `_VNEXT_TOP_LEVEL_KEYS = ("schema_version", "previous_compacted_view", "trace_material", "evidence_material", "answer_material", "current_input_anchor", "instruction")` — 与 `ConversationCompactInputVNext.to_json()` 输出的 7 个 key 完全一致（`compaction.py:1216-1226`）。
- `_CURRENT_VNEXT_MATERIAL_KEYS` 包含 6 个非 `schema_version` 的 key，正向断言循环 `for key in _CURRENT_VNEXT_MATERIAL_KEYS` 覆盖所有当前 section。
- 旧命名排除断言（`stable_input`、`history_input`、`evidence_input`）保留。
- `previous_count` 计算逻辑正确：对 `CompactReadableViewVNext` 的 5 个子组件（`session_summary`、`evidence_backed_facts`、`answer_anchors`、`forward_intents`、`reference_continuity_items`）求和，与生产 `to_json()` 输出结构一致。

### 4. evidence chunk / explicit previous view shape 断言是否提高可读性

✅ 通过。

- `test_single_large_evidence_block_is_chunked_under_same_provenance` 新增 `_assert_material_pack_shape` 断言，明确验证 `("E1.1", "E1.2")` chunk label 属于 evidence section、citable source labels 正确，比原有仅检查 `pack.evidence_labels` 的断言更清晰标识 section boundary。
- `test_build_compact_material_pack_uses_explicit_previous_view_without_snapshot` 新增 material pack shape 和 vNext input shape 双重断言，分别覆盖显式 previous view（`previous_count=1`）和空 previous view（`previous_count=0`）路径。
- 未引入 brittle full JSON snapshot 比较；所有断言均为语义 shape 摘要。

### 5. README 决策和控制文档状态是否正确

✅ 通过。

- `tests/README.md` 不更新：S1 仅在已有 Host 测试文件内添加私有 helper，未引入新测试层、命令或维护规则。决策正确。
- `dayu/host/README.md` 不更新：无生产 Host 代码变更。决策正确。
- `docs/host/issues-implementation-control.md` 状态更新至 `gate: review`、`implementation status: WU-CM-08-S1 implementation completed；awaiting code review`，与 S1 实现完成状态一致。

## 验证确认

| 检查项 | 结果 |
|---|---|
| `pytest tests/host/test_compact_material.py -q` | 35 passed, 0.33s |
| `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings |
| `git diff --check` | clean |

## Findings

无。
