# Code Review

## Scope

- Mode: current changes
- Branch: `codex/download-oracle`
- Base: `0fe85869`（accepted plan HEAD）
- Output file: `docs/reviews/code-review-20260810-171152.md`
- Included scope: `dayu/fins/download_contract.py`、`dayu/cli/arg_parsing.py`、`tests/service/test_fins_direct.py`、`tests/cli/test_arg_parsing.py`、`tests/cli/test_fins_commands.py`（5 个 allowed changed files）+ `docs/gateflow/wu-cli-download-02-slice1-implementation-20260810.md`（implementation artifact）
- Excluded scope: plan 外 production/test 文件、README、Slice 2/3 未实现模块
- Parallel review coverage: 无

## Findings

### 1-未修复-低-AST 检查测试耦合 production 实现结构

- **入口/函数**: `test_download_mutation_mode_conflict_has_one_production_owner`
- **文件(行号)**: `tests/service/test_fins_direct.py`（新增 AST owner test）
- **输入场景**: 任何对 `_validate_download_mutation_mode` 内部实现方式的重构
- **实际分支**: AST walker 遍历所有 `FunctionDef`/`AsyncFunctionDef`，检查 `BoolOp(And)` 中是否同时引用 `overwrite_existing` 与 `rebuild_local_artifacts`
- **预期行为**: owner guard 测试应验证 contract 行为（谁抛出冲突错误、错误消息是否一致），而不是验证 production 代码的 AST 结构
- **实际行为**: 测试断言 `{_validate_download_mutation_mode}` 是唯一包含 `overwrite_existing and rebuild_local_artifacts` conjunction 的函数名。如果 production 将 conjunction 拆为嵌套 `if`、使用中间变量、或将逻辑移入 dataclass `__init_subclass__` 等，测试会误报
- **直接证据**: `ast.parse(module_path.read_text(...))` + `ast.walk(tree)` + `isinstance(node, ast.BoolOp) and isinstance(node.op, ast.And)` + `referenced_names` set comparison → `assert owner_names == {"_validate_download_mutation_mode"}`
- **影响**: 测试与 production 实现结构耦合；重构 validation 内部实现会触发误报，但 contract 行为未变。不会导致错误答案或错误状态，但增加维护成本
- **建议改法和验证点**: 可保留为补充性 guard，但应确保已有 `test_download_mutation_mode_contract_rejects_conflict_with_one_diagnostic` 这类行为测试作为主要 contract 断言。若 production 重构导致 AST 测试失败，应先确认行为测试仍通过再决定是否更新 AST 断言
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-格式化噪声稀释实质变更

- **入口/函数**: `tests/cli/test_arg_parsing.py` 中约 30 处纯机械格式化
- **文件(行号)**: `tests/cli/test_arg_parsing.py`（多处函数签名折叠、行合并）
- **输入场景**: code review 时判断变更意图
- **实际分支**: `ruff format` 对整个文件执行了机械格式化
- **预期行为**: diff 只包含 Slice 1 F12 的实质变更（`--rebuild` help 断言、互斥说明测试）
- **实际行为**: 30+ 行纯格式折叠（函数签名单行化、元组类型声明单行化、Mock 构造单行化）与 2 行实质变更混合在同一 diff 中
- **直接证据**: `git diff 0fe85869 -- tests/cli/test_arg_parsing.py` 显示约 124 行变更，其中 `+65 -89`，但实质新增只有 `--rebuild` help 断言（1 行）和 `test_download_help_explains_mutually_exclusive_mutation_modes` 测试函数（约 25 行）。其余为 `_capture_help`、`test_command_help_contains_core_arguments`、`test_every_parser_scope_omits_removed_config_action` 等函数的签名/体格式折叠
- **影响**: review 时需要逐行区分格式化与逻辑变更，增加 cognitive load。不影响 correctness
- **建议改法和验证点**: 若格式化是 `ruff format` 的结果，应在 implementation artifact 中明确标注哪些文件的哪些区域是纯格式化。或将格式化作为独立 commit 分离
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 无。

## Residual Risk

| 风险 | 分类 | 说明 |
|---|---|---|
| AST owner test 与 production 实现结构耦合 | 低 | 已有行为测试覆盖 contract；AST 测试为补充性 guard |
| 格式化噪声 | 低 | 不影响 correctness，仅影响 review 体验 |
| CLI help 尚未在 README 投影 | covered by later slice | accepted plan Slice 3 稳定后处理 |
| 真实 CLI post-fix evidence 尚未执行 | covered by later slice | accepted plan §9.2；仅在所有 slice/review 稳定后执行 |

## 综合判定

**PASS**。

Slice 1 F12 的实现正确、最小化且语义 ownership 清晰：

1. **唯一 semantic owner**: `_validate_download_mutation_mode` 是 `overwrite/rebuild` 冲突判断的唯一真源。`FinsDownloadRequest.__post_init__` 与 `FinsDownloadEffectiveFilters.__post_init__` 复用同一 helper；CLI 只做 help 投影，不注册 parser-side validator。
2. **pre-workspace side effects**: 冲突在 `FinsDownloadRequest.__post_init__` 中触发，发生在 `build_fins_download_request` → `FinsDownloadRequest(...)` 构造时，早于 `_resolve_workspace_root` 和 `FINS_DIRECT_SERVICE_FACTORY`。CLI 测试精确断言 `factory_calls == []`、`workspace_root` 不存在。
3. **help 自解释**: `--overwrite` 和 `--rebuild` 的 help 文案各自声明不可与对方同时使用。
4. **standalone modes 不受影响**: parametrized 测试覆盖 `--overwrite` only 和 `--rebuild` only 两个合法 sentinel。
5. **test ownership**: 测试断言 contract 行为（合法矩阵接受、冲突拒绝且诊断一致、非 bool TypeError、CLI exit 2 且无副作用）。AST owner test 为补充性 guard。
6. **无 scope drift**: 未修改 Slice 2/3、README、Service、runtime、workflow、provider 或 storage。
7. **无脆弱 AST 测试风险**: AST 测试为低严重度 finding，已有行为测试作为主要 contract 断言。
