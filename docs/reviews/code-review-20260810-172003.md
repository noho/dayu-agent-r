# Code Review (Re-review)

## Scope

- Mode: current changes (re-review of Slice 1 code review fix)
- Branch: `codex/download-oracle`
- Base: `0fe85869` (accepted-plan HEAD)
- Previous review: `docs/reviews/code-review-20260810-171054.md`
- Adjudication: `docs/gateflow/wu-cli-download-02-slice1-code-review-adjudication-20260810.md`
- Fix artifact: `docs/gateflow/wu-cli-download-02-slice1-review-fix-20260810.md`
- Output file: `docs/reviews/code-review-20260810-172003.md`
- Included scope: 验证两个 accepted low findings 的修复状态；核验 production diff 不变性；确认无新增 correctness/scope finding
- Excluded scope: Slice 2/3、README、post-fix CLI evidence、accepted slice commit；本轮不运行测试（验证证据来自 fix artifact）

## Findings

### 先前 Finding 状态

#### Finding 1 (code-review-20260810-171054.md #2): AST 单 owner 测试对 helper 内部重构敏感 → **已修复**

- **裁决**: accepted
- **Fix 描述**: 删除 `test_download_mutation_mode_conflict_has_one_production_owner` 及仅为它引入的 `ast`、`dayu.fins.download_contract as download_contract_module` imports
- **直接证据**:
  - `rg "ast|download_contract_module|test_download_mutation_mode_conflict_has_one_production_owner" tests/service/test_fins_direct.py` → 无匹配（`ast` 和 `download_contract_module` 已删除，函数已删除）；`from typing import cast` 保留（为 `test_download_mutation_mode_contract_rejects_non_boolean_fields` 服务）
  - 行为测试完整保留：`test_download_mutation_mode_contract_accepts_legal_matrix` (00/10/01)、`test_download_mutation_mode_contract_rejects_conflict_with_one_diagnostic` (11)、`test_download_mutation_mode_contract_rejects_non_boolean_fields` (TypeError)、CLI 冲突测试
  - Production 单 owner 守卫保留为 `rg` 静态证据：`rg "overwrite_existing and rebuild_local_artifacts" dayu/ --glob '*.py'` → 仅 `dayu/fins/download_contract.py:79` 一处
- **关闭确认**: ✅ 已关闭

#### Finding 2 (code-review-20260810-171054.md #1): `test_arg_parsing.py` 中 ruff 机械格式化改动超出 F12 变更面 → **已修复**

- **裁决**: accepted
- **Fix 描述**: 逐 hunk 还原 F12 外的函数签名、类型声明、集合推导、调用和 assert 折叠
- **直接证据**:
  - `git diff -- tests/cli/test_arg_parsing.py` 精确仅含两个 hunks:
    1. `COMMAND_HELP_EXPECTATIONS["download"]` 新增 `"--rebuild"` inventory 项（行 68）
    2. 新增 `test_download_help_explains_mutually_exclusive_mutation_modes` 函数（行 376-399）
  - 原 `code-review-20260810-171054.md` 列出的约 20 个与 F12 无关的函数（`_LOG_LEVEL_SELECTOR_CASES`、`_DEBUG_STREAM_LOG_FILE_CASES`、`test_command_help_contains_core_arguments` 等）不再出现在 diff 中
- **Formatter baseline 归因**: `ruff format --check tests/cli/test_arg_parsing.py` 报告 `Would reformat`，但 `ruff format --diff` 确认所有提议 hunks 均为按裁决还原的 F12 外既有 baseline 折叠，无 hunk 触及 F12 新增代码。此偏差已在 fix artifact §5.3 记录，属于 intentional adjudicated deviation，不重新全文件格式化
- **关闭确认**: ✅ 已关闭

### 新增 Finding 检查

沿完整 call chain 重新走读 production diff（`dayu/cli/arg_parsing.py`、`dayu/fins/download_contract.py`），确认：

- `_validate_download_mutation_mode` 仍是唯一 semantic owner
- `FinsDownloadRequest.__post_init__` 与 `FinsDownloadEffectiveFilters.__post_init__` 仍复用同一 helper
- `_prevalidate_download_request` → `build_direct_download_request` → `build_fins_download_request` → `FinsDownloadRequest.__post_init__` 调用链未变，冲突拒绝仍在 `_resolve_workspace_root` 与 `FINS_DIRECT_SERVICE_FACTORY` 前
- 两个 `store_true` flag 的 help 文案互斥说明未变
- 无新增 import、类型、异常类型、状态机或 I/O 路径

**无新增 correctness/scope/ownership finding。**

## Open Questions

无。

## Residual Risk

| 风险 | 分类 | 缓解 |
|---|---|---|
| detached clean worktree / installed CLI 下的真实 argv 与 help 未验证 | covered by later approved slice | accepted plan §9.2 的 CLI evidence gate；用户明确禁止本轮运行 |
| README 未投影 F12 用户可见互斥语义 | covered by later approved slice | accepted plan Slice 3 稳定后的 README decision |
| `test_arg_parsing.py` 的 `ruff format --check` 报告 `Would reformat`（对 intentionally reverted baseline hunks） | intentional deviation，已文档化 | fix artifact §5.3 记录归因；后续 slice 可在独立 format commit 中处理 baseline |
| F13/F14 尚未实现 | covered by later approved slice | Slice 2 / Slice 3 |
