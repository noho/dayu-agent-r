# PR 190 Re-Review Gate 核对报告

## 核对范围

- **Mode**: PR re-review gate
- **PR**: 190 — "fix(cli): close interactive conformance gaps"
- **Author**: noho
- **Head**: codex/interactive-oracle (dcc08399)
- **Base**: main
- **URL**: https://github.com/noho/dayu-agent-r/pull/190
- **核对输入**:
  - `docs/gateflow/pr-190-f17-pr-review-adjudication-20260807.md`（controller 裁决）
  - `docs/reviews/pr-190-review-20260807-152555.md`（AgentDS 修正后 review）
- **核对目标**: 验证 Adjudication 中各项裁决的证据闭合性
- **Output file**: docs/reviews/pr-190-review-20260807-153345.md

## PR 状态确认

| 检查项 | 预期 | 实际 | 状态 |
|--------|------|------|------|
| PR state | OPEN | OPEN | ✅ |
| Draft status | true | true | ✅ |
| Head commit | dcc08399 | dcc08399 | ✅ |
| GitHub reviews | 无 | 无（API 返回 `[]`） | ✅ |
| GitHub checks | no checks | gh pr checks 返回失败（无配置） | ✅ |
| Workspace dirty | 无产品代码改动 | `git status --short` 只显示 docs/reviews artifact | ✅ |

**结论**: PR 状态与 Adjudication 记录一致，未发生意外变化。

## D3 Branch/Baseline/One-Entry+Pin Correction 闭合核对

### Adjudication 裁决

- **裁决**: `accepted`，仅 evidence correction
- **理由**: review 把完整 PR 的 `main..HEAD` 历史差异误归给 F17 work-unit pre-state

### AgentDS 修正内容

AgentDS 在 `pr-190-review-20260807-152555.md` 的 finding 3 中已明确修正：

- **标注**: "证据失效-低-F17 evidence baseline error（reviewer 已撤回）"
- **F17 commit boundary**: base=`e1217811`，target=`305c1012`
- **直接证据**:
  - `git show e1217811:docs/cli_init_workspace_manifest_v1.json | shasum -a 256` 为 `d95de68e...`
  - `git show e1217811:tests/cli/test_smoke_cli_init_provider_matrix.py` 中 `FROZEN_MANIFEST_SHA256` 同为 `d95de68e...`
  - F17 preflight production strict report 只有 `conversation_compaction_user.md` 这一处 singleton mismatch
  - `git diff e1217811..305c1012` 对两个 consumer 各为 1 insertion / 1 deletion，仅更新目标 manifest entry 与 test pin
- **范围结论**: `main..HEAD` 中的其它历史差异不得归为 F17

### 闭合验证

- ✅ AgentDS 已撤回原 finding 的产品 defect 定性
- ✅ 明确标注为 "证据失效"，不构成产品 finding
- ✅ F17 commit boundary 与 Adjudication 记录一致（base=e1217811, target=305c1012）
- ✅ 修正范围仅限于 `pr-190-review-20260807-152555.md` 的证据描述，未修改产品代码或其它 finding

**结论**: D3 evidence correction 已闭合。

## D1 Runtime/Engine 层隔离及 Invariant Test 核对

### Adjudication 裁决

- **裁决**: `rejected-with-reason`
- **理由**:
  1. `dayu.runtime` 按架构硬约束不得 import `dayu.engine`
  2. runtime 配置 schema 与 Engine execution contract 属于不同层
  3. Service 机械投影是正确边界
  4. **关键**: reviewer 建议增加的 invariant 已存在：`test_structured_output_capability_enums_map_mechanically_by_value`

### 证据核对

#### 1. 层隔离正确性

- `dayu/runtime/config_loader.py:108-113`：定义 `StructuredOutputCapabilityConfig`
- `dayu/engine/contracts/structured_output.py:19-31`：定义 `StructuredOutputCapability`
- `dayu/service/host_assembly.py:1807-1808`：通过 `.value` 字符串桥接
- **确认**: runtime 不 import engine，Service 层通过机械投影桥接，符合架构约束

#### 2. Invariant Test 存在性

```bash
$ grep -r "test_structured_output_capability_enums_map_mechanically_by_value" tests/
tests/service/test_host_assembly.py:def test_structured_output_capability_enums_map_mechanically_by_value() -> None:
```

**确认**: 测试存在于 `tests/service/test_host_assembly.py`，验证两侧完整 value set 相等并逐项可构造。

#### 3. AgentDS Finding 1 状态

AgentDS 的 review 中 finding 1 仍保留为 "Dual StructuredOutputCapability 枚举定义双真源"，但：
- 该 finding 的 "直接证据" 部分已正确记录了层隔离机制
- 该 finding 的 "影响" 部分承认 "当前两个枚举值集合恰好一致，但无 invariant test 保证一致性"
- **问题**: AgentDS 未发现已存在的 invariant test，导致 finding 的 "直接证据" 与 Adjudication 裁决冲突

**结论**: D1 裁决理由成立。AgentDS 的 finding 1 应被拒绝，因为 invariant test 已存在，且层隔离是正确的架构设计。

## D2 无现行互斥 Contract/触发证据核对

### Adjudication 裁决

- **裁决**: `rejected-with-reason`
- **理由**:
  1. 当前 Engine design 的合法矩阵只由 request variant 与 Runner capability 决定
  2. 没有 stream 互斥 contract
  3. review 未提供 provider protocol 的直接失败证据，并承认当前无触发路径

### 证据核对

#### 1. AgentDS Finding 2 描述

- **入口**: `build_request_payload` / `AgentRunRequest.__post_init__`
- **场景**: 未来代码路径同时设置 `stream=True` 和 `structured_output` 非 None
- **实际行为**: payload 会同时包含 `"stream": True` 和 `"response_format": {...}`
- **影响**: "当前不可触发（compactor 使用非流式，interactive 不传 structured_output），但为 latent contract violation"

#### 2. 直接证据缺失

- AgentDS 未提供任何 provider protocol 文档或实际 API 调用失败的证据
- AgentDS 明确承认 "当前不可触发"
- **问题**: finding 基于 "未来代码路径" 的假设，而非现行 contract 或实际触发证据

**结论**: D2 裁决理由成立。AgentDS 的 finding 2 应被拒绝，因为没有现行互斥 contract 或直接失败证据。

## D4 README 明确不迁移旧 Slot 核对

### Adjudication 裁决

- **裁决**: `rejected-with-reason`
- **理由**:
  1. 根 README 已明确公开：共享 namespace 是 `cli.agent.<label>`
  2. 旧 `cli.prompt.*` / `cli.interactive.*` slot 不会被自动读取或迁移
  3. tests 也将旧 scope 明确分类为 `OTHER`
  4. 这是 accepted fresh contract，不是遗漏
  5. 新增 fallback/migration 还会违反当前仓库禁止兼容层的约束

### 证据核对

#### 1. README 明确说明

```markdown
不传 `--label` 时，每次启动创建一个新的未绑定 Session；传入 label 时复用
`cli.agent.<label>` 对应 Session。`prompt --label earnings` 与
`interactive --label earnings` 使用同一个 label owner 和同一个 Session；旧
`cli.prompt.*` / `cli.interactive.*` slot 不会被自动读取或迁移。
```

**确认**: README 明确说明旧 slot 不会被自动读取或迁移。

#### 2. CLAUDE.md 约束

```markdown
禁止兼容性代码：
- 兼容性 re-export：仅为保持旧导入路径而转发符号。
- 兼容性 wrapper / facade：方法体仅透传到真源模块，不增加有效语义。
```

**确认**: 仓库约束禁止兼容层，新增 fallback/migration 会违反约束。

#### 3. AgentDS Finding 4 状态

AgentDS 的 review 中 finding 4 仍保留为 "session scope 统一迁移无向后兼容路径"，但：
- 该 finding 的 "建议改法和验证点" 提议 "在 `_build_session_label_key` 中增加 fallback 查找旧 scope"
- **问题**: AgentDS 未检查 README 是否已明确说明，且建议的改法违反仓库约束

**结论**: D4 裁决理由成立。AgentDS 的 finding 4 应被拒绝，因为 README 已明确说明，且新增 fallback 会违反仓库约束。

## MiMo M2/M3 核对

### M2：CONTEXT_COMPACTION_FAILED 没有未来 bounded-storage fallback

- **裁决**: `rejected-with-reason`
- **理由**: review 明确承认当前 failed payload 是固定小型封闭结构且当前行为正确；finding 只假设未来 schema 可能扩张
- **核对**: 符合 "未来假设" 的定义，不应作为当前 defect

### M3：compact_structure.py 没有同名专属测试文件

- **裁决**: `rejected-with-reason`
- **理由**: 仓库要求 owner contract coverage，不要求生产模块与测试文件同名；这是文件组织偏好
- **核对**: 符合 "文件组织偏好" 的定义，不应作为 correctness/stability/maintainability defect

**结论**: M2/M3 裁决理由成立。

## M1 No-Checks CI Gap 核对

### Adjudication 裁决

- **裁决**: `deferred-with-owner`
- **理由**: 事实成立，是 CI coverage gap，不是产品 code defect。F17 已在 clean committed target 运行 71/71 owner suite、full pyright、Ruff、compileall、JSON/digest/count 与 committed-range diff check；但这些本地证据不能伪装成 GitHub CI PASS。

### 证据核对

- `gh pr checks 190` 返回失败（无配置）
- PR 有 745 files changed 但无自动化 CI 验证运行
- F17 本地验证：71/71 tests passed、pyright 0 errors、Ruff clean、compileall success

**结论**: M1 裁决理由成立。这是 CI coverage gap，由 repository CI / merge policy 后续裁决。

## Gate 结论

### 已闭合项

1. **D3 evidence correction** ✅
   - AgentDS 已撤回原 finding 的产品 defect 定性
   - 明确标注 F17 commit boundary (base=e1217811, target=305c1012)
   - 修正范围仅限于 review artifact，未修改产品代码

### 已拒绝项（理由成立）

1. **D1 runtime/Engine 层隔离** ✅
   - 层隔离正确，符合架构硬约束
   - Invariant test 已存在：`test_structured_output_capability_enums_map_mechanically_by_value`

2. **D2 stream+structured_output 互斥** ✅
   - 无现行互斥 contract
   - 无直接失败证据
   - 当前不可触发

3. **D4 session scope 统一** ✅
   - README 已明确说明旧 slot 不迁移
   - 新增 fallback 会违反仓库约束

4. **M2 未来假设** ✅
   - 当前行为正确
   - finding 基于未来 schema 扩张假设

5. **M3 文件组织偏好** ✅
   - 测试覆盖已存在
   - 同名文件不是 correctness/stability/maintainability 要求

### 已延期项

1. **M1 CI gap** ✅
   - 事实成立，是 CI coverage gap
   - 由 repository CI / merge policy 后续裁决

## Remaining Product Finding

**无。**

所有 Adjudication 中的产品 finding 均已被拒绝或证据修正。没有遗留的产品 code defect。

## Risks

### 1. CI Coverage Gap（M1，deferred-with-owner）

- **风险**: PR 有 745 files changed 但无 GitHub CI 验证
- **缓解**: F17 本地验证通过（71/71 tests、pyright、Ruff、compileall）
- **Owner**: repository CI / merge policy 后续裁决
- **建议**: 合并前配置至少包含 pyright + pytest 的 status check

### 2. Dual Enum 维护风险（D1，rejected）

- **风险**: `StructuredOutputCapability` / `StructuredOutputCapabilityConfig` 双定义，新增值需两处同步
- **缓解**: Invariant test 已存在，会在编译/测试期捕获不一致
- **严重程度**: 低，当前值一致且有测试保护

### 3. Session Scope Breaking Change（D4，rejected）

- **风险**: 旧 `cli.prompt.*` / `cli.interactive.*` session 升级后无法通过 label 管理
- **缓解**: README 已明确说明，PR 仍为 DRAFT
- **严重程度**: 低，breaking change 在 draft 阶段可接受

### 4. Stream+Structured Output Latent Risk（D2，rejected）

- **风险**: 未来代码路径可能同时设置 `stream=True` 和 `structured_output`
- **缓解**: 当前不可触发，且 provider 会在 HTTP 层拒绝
- **严重程度**: 低，latent risk

## 总体结论

**PR 190 re-review gate 通过。**

- 所有 Adjudication 裁决的证据核对均闭合
- 没有遗留的产品 code defect
- D3 evidence correction 已完成并验证
- 唯一 remaining risk 是 CI coverage gap（M1），已 deferred-with-owner
- PR 状态仍为 OPEN/DRAFT/CLEAN，head dcc08399，无 GitHub review write

**建议**: 在配置 GitHub CI 后，可进入下一步 gate 决策。
