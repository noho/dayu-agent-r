# PR 190 F11/F12 S5 registry/docs review fix（2026-08-06）

## Gate metadata

- Work unit：PR 190 F11/F12 interactive memory closure。
- Slice：S5 registry/docs lifecycle replacement。
- Gate：双路 code review 后的 fix。
- Review adjudication：`docs/reviews/pr-190-f11-f12-s5-registry-review-adjudication-20260806.md`。
- Fix result：`PASS`。
- Current gate / next entry point：MiMo 与 DeepSeek dual re-review。
- Stop boundary：本 artifact 后不启动 reviewer，不 stage/commit/push，不修改 PR body。
- Artifact path：`docs/reviews/pr-190-f11-f12-s5-registry-fix-20260806.md`。

## Scope、动机与 owner

裁决接受了一个低严重度文档歧义：implementation artifact 原 validation 表把变更前历史引用子集、当前
`command=interactive` inventory、当前完整 registry、owner-defined predicates 与 scenario-referenced predicate ids 压在
两行中，导致 `611`/`612` 与 `29`/`30` 看似冲突。直接 JSON inventory 证明 registry contract 和数据本身正确，问题只属于
implementation artifact 对 validation 结果的投影精度，因此修复 owner 是该 artifact 的 validation 说明，不是 registry、handbook、
readiness、consumer、production code 或测试。

本 fix 只修改：

1. `docs/reviews/pr-190-f11-f12-s5-registry-implementation-20260806.md`；
2. 新增本 fix artifact。

未修改 `docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`、`docs/cli_ci.md`、readiness artifact、accepted contract、
production code、tests、README 或 PR 状态。

## Changed wording

原 validation 表使用以下两行，混合了不同集合和不同计数对象：

- `stable predicate current resolution`：`611 records、768 refs、29 stable predicate ids`；
- `current owner distribution`：`766 refs → interactive owner；2 refs → prompt owner`。

现改为四个互斥、可复核的口径：

1. **historical referenced subset**：基线 `1a79ff1859117027340910152c0ce208a7f37b5d` 中至少引用一个
   `interactive.*` predicate 的 611 records / 768 refs / 29 referenced predicate ids；ref owner 为
   766 `cli.interactive.core-execution@2` + 2 `cli.prompt.core-execution@1`，referenced id owner 为 28 interactive@2 +
   1 prompt@1；
2. **current `command=interactive` inventory**：当前 612 records / 768 refs / 28 referenced predicate ids；全部 refs 与 ids
   均由 interactive owner 解析；
3. **current full registry inventory**：当前 1059 records / 1614 refs / 64 referenced predicate ids；ref owner 为
   770 `cli.interactive.core-execution@2` + 728 `cli.prompt.core-execution@1` +
   116 `cli.init.workspace-initialization@1`，referenced id owner 为 28 interactive@2 + 26 prompt@1 + 10 init@1；
4. **accepted owner schema inventory / stable resolution**：current accepted owners 共定义 66 stable predicates；owner-defined
   分布为 30 `cli.interactive.core-execution@2` + 26 `cli.prompt.core-execution@1` +
   10 `cli.init.workspace-initialization@1`，全部 1614 refs 唯一解析。

该改动只提高 validation 叙述精度，没有改变 registry 字段、lifecycle、predicate contract、current-owner resolution 或任何
accepted contract。

## Validation

所有命令均在 `source .venv/bin/activate` 后以只读方式执行：

| Validation | Result |
| --- | --- |
| 两份 JSON：`python -m json.tool` | PASS；2/2 valid |
| inventory / uniqueness | PASS；4 oracle records、1059 scenario records，0 duplicate keys |
| oracle supersession graph | PASS；0 dangling、0 cycle、0 asymmetric edge |
| scenario supersession graph | PASS；0 dangling、0 cycle、0 asymmetric edge |
| accepted owner schema inventory | PASS；66 owner-defined predicates：30 interactive@2、26 prompt@1、10 init@1；0 duplicate current owner |
| historical referenced subset | PASS；611 records、768 refs、29 referenced ids；refs 766 interactive@2 + 2 prompt@1；ids 28 interactive@2 + 1 prompt@1 |
| current `command=interactive` inventory | PASS；612 records、768 refs、28 referenced ids；全部解析到 interactive@2 owner |
| current full registry inventory / stable resolution | PASS；1059 records、1614 refs、64 referenced ids；0 dangling；refs 770 interactive@2 + 728 prompt@1 + 116 init@1；ids 28 interactive@2 + 26 prompt@1 + 10 init@1 |
| registry SHA-256 | PASS；oracles `3404e241dbd71c6244da24b0dbb080022d4c57b36f040ac3456e7a18dbc97acf`；scenarios `f4363fc5e7026ad075f4b7f855342cae493a4852d21bd72ef6e53b3f2d588e37` |
| replacement evidence refs | PASS；3 replacement records 的 17 个唯一 target 均存在；report 与 manifest digest exact |
| evidence report SHA-256 | PASS；`bbaa52a04100932c09e0a8e20d19c81ed6d865378db502bc6d4f1936c9694411` |
| evidence manifest SHA-256 | PASS；`38f0b01f12c2ab55ce1af3c16080b71013d1a19512d65051f5532b747f71da0d` |
| `git diff --check` | PASS |

未运行 pytest、coverage、pyright 或 frozen formal scenarios：本 fix 只修改 review 文档措辞，且用户明确要求的 gate validation
是 JSON、inventory、supersession graph、stable owner resolution、registry SHA-256、evidence refs 与 diff check。该省略不改变
任何既有代码 gate 结论。

## Findings、residual risks 与 uncovered areas

- accepted documentation-precision finding：**fixed in current slice**；等待双路 re-review 确认最终状态。
- frozen `accepted_oracle_refs`：裁决为 rejected-with-reason，不是本 fix finding；未修改。
- 三条 replacement scenario 的 Formal Oracle pending：**assigned to later work unit**，owner 为 Oracle controller。
- immutable evidence root 的长期保留：**assigned to later work unit**，owner 为 CLI CI evidence-retention owner；当前 refs 与 digest
  完整。
- 未分类 residual risk、blocking open question、needs-more-evidence：无。

## Completion status

S5 review fix：**PASS**。Accepted contract 与 registry digest 不变。Gateflow 当前 gate / next entry point 为双路 `re-review`；
按用户 stop boundary，本轮停在该 gate 入口，不启动 MiMo 或 DeepSeek reviewer。
