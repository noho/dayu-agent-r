# UF-FIX02 action-and-update-identity — Plan Review Delta Re-review（AgentDS 独立）

## 元信息

- Reviewer：AgentDS（Controller 裁决后的 delta re-reviewer；未读取 AgentMiMo 原/新 artifact；未启动子 agent）
- 目标 plan（修订版）：`docs/gateflow/uf-fix02-action-and-update-identity-plan-20260813.md`
- Binding Goal Confirmation：`docs/gateflow/uf-fix02-action-and-update-identity-goal-confirmation-20260813.md`
- Controller 裁决：`docs/gateflow/uf-fix02-action-and-update-identity-plan-review-adjudication-20260813.md`
- 原 review（本轮基线）：`docs/reviews/plan-review-20260813-172351.md`
- Baseline HEAD：`114430ce312ca6d8eb9c9f4cb7bb0a1f0bdba5a0`（复核时 HEAD 一致）
- 结论：**pass**

## Reviewed scope

逐项验证 Controller accepted 的 MIMO-001/002/003 与 DS-01/02/03 在修订 plan 中的精确修复状态，
并重做 goal drift / owner / slice / validation / UF-PF02 / static audit。验证基于 plan 文本与
直接代码事实，不依赖 AgentMiMo artifact。

## 逐 finding 修复状态

### MIMO-001 — 已修复

- **裁决要求**：plan 显式说明 UF-A08 修复后 intentionally stale 与后续 owner；不得修改 scenario status。
- **plan 证据**：§3.1 明确 `UF-A08-update-missing-overwrite-creates` 是 intentionally stale observed evidence，
  本 WU 不修改其 status，状态更新唯一归属后续统一 conformance refresh owner；§4 冻结 registry 段重复该声明；
  §11 static audit 含 "`UF-A08` 只被标注为 intentionally stale，本 WU 无 registry/evidence/status 写入"；
  §12 risk 表将 UF-A08 stale 归类到 "later unified conformance refresh"。
- **registry no-touch**：§3.1 禁止刷新两个 frozen registry；§6.4 no-touch 清单含两个 registry 与全部既有 evidence；
  §11 diff audit 用 `git diff --exit-code -- docs/cli_ci_scenarios.json docs/cli_ci_oracles.json docs/host/design.md
  docs/engine/design.md` 强制零差异。拒绝部分（改 status）未被引入。
- **代码事实复核**：两个 frozen registry SHA-256 实测仍为 plan §4 声称的 `a357e5a1…`/`88b04ca4…`，逐字节一致。

### MIMO-002 — 已修复

- **裁决要求**：publication transition 写出错误/取消分支，绑定现有及新增 rollback assertions。
- **plan 证据**：§5.2 显式写出 `on any store/create failure: discard/rollback whole batch` 与
  `on precommit cancellation: discard/rollback whole batch`，均声明 "published old state remains byte-for-byte
  unchanged"；并写 "禁止提交半成品或另起补偿 batch"。S2 Tests first 1 新增断言："reset 后任一 blob store/final
  create failure，或 final checkpoint/precommit cancellation：whole batch 被 discard/rollback，published old
  source/meta/manifest/company tree SHA 仍等于 old"。§8 matrix "atomic batch" 行绑定该断言与既有
  failure/cancel 套件。

### MIMO-003 / DS-03 — 已修复

- **裁决要求**：reset 不丢失 caller 持有的 `previous_meta`；version 与 `first_ingested_at` 从该真源派生并由测试固定。
- **plan 证据**：§5.2 写出 transition invariant："`previous_meta` 在 reset 前由 caller 持有，是 version 与
  `first_ingested_at` 派生的唯一真源；reset 只清 staging identity tree，不得清空、重读或以 reset 后 missing
  状态替代该引用；changed fingerprint 按既有规则递增 version，不得回退到 v1"。S2 Tests first 1：改名 update 断言
  "version 按 reset 前 `previous_meta` 派生，`first_ingested_at` 等于初次 create 的值且不被本次 `updated_at`
  替代"；delete→auto 断言 "version 不回退，`first_ingested_at` 等于 logical delete 前的初次 create 值"。
  §5.3 可观察状态转移表与 §8 matrix renamed update 行同源固定。
- **代码事实复核**：`_build_upsert_meta` 保留 `first_ingested_at`（`docling_upload_service.py:774-779`）；
  reset 由 `SourceDocumentRepositoryProtocol.reset_source_document` 提供、caller batch 线性化不变，与我原 review
  假设 5/6 一致。

### DS-01 — 已修复

- **裁决要求**：声明 material 同步获得 shared-owner 一致性效果并加最小 parity tests；边界为不新增 material
  typed usage、不改 workflow 生产代码、不扩 focused-real。
- **plan 证据**：§3.1 显式声明 "filing/material 共用的 action、skip 与 publication owner 同步拒绝 update-missing
  upsert、禁止 deleted auto skip，并对 existing full-input update 做完整替换"，且 "material 只接受 shared owner
  的一致性效果与最小 owner parity tests；不新增 material typed usage/public error projection，不修改 CLI/Service/
  SEC/CN/HK material workflow 生产代码，也不把 material 扩入 UF-PF02 focused-real"。§4 owner 表 action resolution
  行声明 "同形 material owner 输入有意得到同形结果，不新增 material 专用规则"。测试：S1 test 2 一个 material parity
  test（update-missing ± overwrite 于 converter 前失败、converter call count 0）；S2 test 1 一个 material
  publication parity test（deleted + equal full input auto 不 skip、active 完整、`first_ingested_at` 保持）。
  §6.3.1 README 只声明共享 owner 一致性效果、不宣称 material typed usage/focused-real。§8 matrix 新增
  "material shared-owner parity" 行。§11 audit 限制 "material 变化仅来自 shared owner，只有 S1/S2 两个最小 parity
  tests"。§12 将 material broader typed error projection 归类 `UF-PF12 或后续明确 work unit`。
- **代码事实复核**：material 路径复用同一 `resolve_upload_action`（`sec_upload_workflow.py:504`）与同一
  `prepare_upload`（`docling_upload_service.py:290/304`）；共享 owner 声明成立。

### DS-02 — 已修复

- **裁决要求**：S2 明确删除 `_resolve_upsert_mode` import 与旧断言，以 owner action/replacement matrix 取代，
  全仓零命中。
- **plan 证据**：S2 "Test migration" 显式点名 "删除 `test_docling_upload_service.py` 中 `_resolve_upsert_mode`
  import 与 `assert _resolve_upsert_mode("update", None, True) == "create"` 错误 upsert pin；不得保留 compat
  shim"，并要求 `rg -n '_resolve_upsert_mode' --glob '*.py' .` 全仓生产/测试 Python 源码零命中；并正确限定
  plan/review 历史 artifact 不属于实现源码审计域（否则本 plan 自身引用即会误伤）。§11 static audit 含同一条 rg。
  §6.1 明确生产侧删除该函数、`_upsert_source_document` 收敛为 create-only 并改名。
- **代码事实复核**：`_resolve_upsert_mode` 生产调用点唯一（`docling_upload_service.py:476`，位于
  `_store_upload_assets`，reset 后即被覆盖为 `"create"`）；测试 import 在 `test_docling_upload_service.py:34`、
  错误 pin 在 `:1334`，与 plan 点名完全一致；`_upsert_source_document` 唯一调用点在 `:531`，收敛后无 dead branch。

## 本轮新增 audit

### Goal drift

§2.1 六条 goal 与 Goal Confirmation 一一对应；§3.2 minimality 与 GC minimality 一致；material parity 与 UF-A08
stale 声明均为 Controller 裁决明确授权的有界补救，非新增目标。无新增验收标准、无 future-slice work 进入实现。

### Owner

§4 owner 表无新增 owner/registry/schema；no-touch 清单覆盖 CLI/Service/storage/SEC/CN/HK workflow/Host/Engine/
runtime/config 与两个 frozen registry。复核确认 action/precondition/skip 三 helper 全部由
`DoclingUploadService`（定义 `docling_upload_service.py:161/979/1227`）与 validator
（`ingestion_runtime.py:962/969` 复用同一 helper）构成唯一决策真源；S1 stop condition 的 "CLI prevalidation 与
workflow fresh state 不同源" 检查项与该结构一致。

### Slice

S1 只改 admission/skip/message，不改 mutation；S2 才改 `_store_upload_assets` reset→create。S1 中间态
（deleted equal input 走非 reset update 路径，`merged["is_deleted"] = False` 显式写回，见
`docling_upload_service.py:784`）自洽，与我原 review 排除项 2 一致。S2 stop condition 覆盖 reset 无法
same-batch old-or-new 的情况。

### Validation

§6.2/§8.2 引用的全部 12 个测试文件与 `upload_filing_test_support.py` 实测存在；§8.3 逐生产文件 coverage ≥80%
（两个文件各自独立判定）；§8.4 完整 pyright。S1/S2 Tests first 红灯来源与生产变更一一对应。

### UF-PF02

- CLI `--overwrite` 参数实测存在（`dayu/cli/commands/fins.py:345` 传入 `overwrite=args.overwrite`）。
- 精确 stderr 声明可成立：CLI 投影为 `dayu-cli upload_filing: {exc.failure.message}` + `render_cli_error`
  单行 print（`dayu/cli/output.py:152`）；当前文案实测为 `update 目标不存在；请改用 create 或允许覆盖`
  （`ingestion_runtime.py:753`），§5.4 改为 `update 目标不存在；请改用 create` 后 §9.2 B 的精确断言与
  §5.4 内部一致。
- 其余 UF-PF02 设计（真实 dayu-cli、八类 observation、secret scan、focused-real verdict）未变，
  我原 review 已用直接证据验证其可执行性。

### Static audit

§11 覆盖 registry no-touch、UF-A08 stale 标注、material 范围、rg 零命中、docstring 与
CLAUDE.md 硬约束检查项；diff audit 命令只允许 §6.1–§6.3 路径。

## Findings

无新增 material finding。所有 Controller accepted findings 均已精确修复，无未修复/部分修复项。

## Open questions

1. S1 test 3 的 "forbidden Service factory" 若 `tests/cli/test_fins_commands.py` 尚无现成模式，
   implementation agent 需在测试文件内自建小 double；plan 已禁止新增 fake helper 文件与修改 support 文件，
   不构成风险。
2. `_upsert_source_document` 收敛后的新名字仍未指定，属 implementation agent 自由裁量，无风险（延续原 review
   open question 2）。

## Residual risks and suggested tracking destination

| Risk | Destination |
| --- | --- |
| UF-A08 frozen observed evidence 修复后 intentionally stale | 后续统一 conformance refresh（plan §12 已归类） |
| material broader typed error projection / full-real | UF-PF12 或后续明确 work unit（plan §12 已归类） |
| 同请求竞争窗口 / corruption repair / multi-file collision / format | UF-FIX10 / UF-FIX08 / UF-FIX07 / UF-FIX06（plan §12 已归类） |
| UF-PF02 精确 stderr 依赖新文案与投影格式逐字节一致 | 实现时按 §5.4/§9.2 双处锁定；偏差必须如实 classified，不得放宽断言 |

## Final conclusion

**pass**

六个 Controller accepted findings（MIMO-001/002/003、DS-01/02/03）全部精确修复：registry no-touch 与后续
refresh owner 边界、failure/cancel rollback 分支与断言、`previous_meta`/version/`first_ingested_at` 真源
不变量、material shared-owner parity 声明与最小测试、旧 upsert pin 迁移与零命中审计，均有 plan 文本与直接
代码事实双重支撑。重做的 goal drift/owner/slice/validation/UF-PF02/static audit 未发现新 material blocker。
