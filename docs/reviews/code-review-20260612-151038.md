# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: `main`
- Output file: `docs/reviews/code-review-20260612-151038.md`
- Included scope:
  - `tests/host/test_llm_compaction.py` (unstaged changes)
  - `docs/host/issues-implementation-control.md` (unstaged changes)
- Excluded scope:
  - `docs/reviews/wu-cm-05-s3-implementation-report.md` (untracked, documentation only)
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Analysis

### DS-F02 修复验证

**原问题**: `tests/host/test_llm_compaction.py` 中 `_proposal_json` 使用 `cast` 进行类型祝福 JSON。

**当前状态**: 已修复。

**证据**:
1. `git diff` 显示 `from typing import cast` 已移除，替换为 `from typing import TypeGuard`
2. `_proposal_json` 函数体中 `cast(dict[str, JsonValue], parsed)` 已替换为 `dict(_required_mapping(parsed, field_name="proposal"))`
3. `_material_json_from_compactor_prompt` 中 `cast(JsonValue, json.loads(...))` 已替换为 `parsed: JsonValue = json.loads(...)`
4. `_required_mapping` 中 `cast(Mapping[str, JsonValue], value)` 已替换为 `_is_json_mapping` + TypeGuard
5. `rg -n "cast\(|from typing import .*cast" tests/host/test_llm_compaction.py` 无匹配

### 弱类型逃逸检查

**检查项**: cast/Any/object/裸弱类型逃逸或通过 TypeGuard 伪装绕过。

**结果**: 无逃逸。

**证据**:
1. `rg -n "Any|: object|-> object" tests/host/test_llm_compaction.py` 无匹配
2. `rg -n "cast\(" dayu/host/llm_compaction.py` 无匹配（生产代码已在 S1/S2 清理）
3. pyright 无错误，类型系统验证通过

**TypeGuard 使用合理性**:
- `_is_json_mapping(value: JsonValue) -> TypeGuard[Mapping[str, JsonValue]]` 使用标准库 `TypeGuard` 进行类型缩窄
- 函数体使用 `isinstance(value, Mapping)` 进行运行时检查
- TypeGuard 是 Python 3.10+ 标准库功能，不是弱类型逃逸
- 相比 `cast`，TypeGuard 提供了运行时安全保障

### Helper 签名和中文 docstring 检查

**检查项**: helper 签名和中文 docstring 是否符合项目约束。

**结果**: 符合。

**证据**:
1. `_compact_input_json`:
   - 签名: `(compact_input: ConversationCompactInputVNext) -> Mapping[str, JsonValue]`
   - 中文 docstring: 完整，包含参数、返回值、异常
2. `_is_json_mapping`:
   - 签名: `(value: JsonValue) -> TypeGuard[Mapping[str, JsonValue]]`
   - 中文 docstring: 完整，包含参数、返回值
3. `_required_mapping`:
   - 签名: `(value: JsonValue, *, field_name: str) -> Mapping[str, JsonValue]`
   - 中文 docstring: 完整，包含参数、返回值、异常
4. `_proposal_json`:
   - 签名: `(compact_input: ConversationCompactInputVNext) -> dict[str, JsonValue]`
   - 中文 docstring: 完整，包含参数、返回值、异常

### 测试覆盖检查

**检查项**: 测试是否仍覆盖 public parser 边界且不依赖私有 parser。

**结果**: 符合。

**证据**:
1. 测试调用的是 `parse_conversation_compact_output_vnext`，这是 `dayu.host.llm_compaction` 的 public API
2. helper 函数 (`_proposal_json`, `_compact_input_json`, `_required_mapping`, `_is_json_mapping`) 仅用于构造测试数据，不涉及私有 parser
3. 测试覆盖了以下 public parser 边界:
   - malformed JSON
   - top-level non-object
   - missing required key
   - field type path
   - nested array type path
   - array item type path
   - top-level array overlimit
   - nested array overlimit
   - old schema rejection

### README 不更新判断

**检查项**: README 不更新的判断是否成立。

**结果**: 成立。

**证据**:
1. `dayu/host/README.md`:
   - 这个 slice 没有改变 Host 公共 API、稳定架构边界、事件流、状态机或面向开发者的 Host 契约
   - 只是清理了测试代码中的 `cast` 使用
   - 按照 `CLAUDE.md` 的 README 更新触发规则，`dayu/host/` 修改需要检查并按需更新 `dayu/host/README.md`，但本次修改不涉及 README 职责范围内的内容
2. `tests/README.md`:
   - 这个 slice 只是在现有的 Host 测试文件中改变了 helper 实现
   - 没有添加新的测试层、命令或维护规则
   - 按照 `CLAUDE.md` 的 README 更新触发规则，`tests/` 修改需要检查并按需更新 `tests/README.md`，但本次修改不涉及 README 职责范围内的内容

### Correctness/Stability/Maintainability 检查

**检查项**: 是否有 correctness/stability/maintainability finding。

**结果**: 无。

**证据**:
1. 测试全部通过: `pytest tests/host/test_llm_compaction.py -q` → 37 passed in 0.27s
2. pyright 无错误: `python -m pyright dayu/ tests/ utils/` → 0 errors, 0 warnings, 0 informations
3. 生产代码未改变: `git diff main...HEAD -- dayu/host/llm_compaction.py` 的变更已在 S1/S2 完成，S3 只改测试
4. 测试覆盖了 public parser 的关键边界条件
5. helper 函数签名清晰，类型注解完整，docstring 符合规范

## Open Questions

无

## Residual Risk

无

## Conclusion

PASS

本次 review 确认 WU-CM-05-S3 已完成 Contract and boundary cleanup 目标:
1. DS-F02 已修复: `_proposal_json` 不再使用 `cast`，改用 `_required_mapping` + TypeGuard 进行显式 narrowing
2. 无弱类型逃逸: cast/Any/object 已全部移除，TypeGuard 使用合理
3. helper 签名和中文 docstring 符合项目约束
4. 测试仍覆盖 public parser 边界且不依赖私有 parser
5. README 不更新的判断成立
6. 无 correctness/stability/maintainability finding
