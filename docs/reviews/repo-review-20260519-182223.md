# Code Review

## Scope

- Mode: All Repository
- Branch: `feat/host-p10-5-public-contract-freeze`
- Base: N/A (全仓 review)
- Output file: `docs/reviews/repo-review-20260519-182223.md`
- Included scope: `dayu/contracts/`, `dayu/runtime/`, `dayu/engine/`, `dayu/host/`（含 `durable/`）, `dayu/config/`, `tests/`
- Excluded scope: `dayu/fins/`（财报领域能力，本次未覆盖）, `dayu/service/`, `dayu/ui/`（未发现对应代码目录）, `utils/`, `workspace/`
- Parallel review coverage: 6 个 subagent 分别覆盖 contracts+runtime、engine、host core、host tools+memory+compaction、config+durable、tests

## 整体评价

**仓库整体质量：高**。约 80K+ 行生产代码，架构设计成熟，分层严格，关键不变量守护到位。

- **架构合规性**：全部通过。`UI -> Service -> Host -> Engine` 依赖方向严格，无反向 import。`dayu.runtime` 不 import 上层业务包。import boundary 测试通过 AST 扫描自动守护。
- **类型严格性**：全部通过。无 `Any`/`object`/无类型参数。pyright 集成到位。
- **docstring 完整性**：全部通过。所有公开类、函数、模块均有中文 docstring。
- **事务性保证**：Host admission 操作在 `run_write` 事务内完成，EventLog append 与 state mutation 同事务提交，terminal closeout 通过 CAS 断言确保原子性。
- **幂等性**：所有 command 操作通过 `IdempotencyStore` 实现 semantic digest 幂等。
- **取消边界**：Engine 协作式取消设计正确，token 检查点覆盖全生命周期。

## Findings

### 01-未修复-中-tool_runtime awaiting retry 缺少 diagnostic ref

- **入口/函数**: `ToolRuntimeExecutor._accept_awaiting_with_retry`
- **文件(行号)**: `dayu/host/tool_runtime.py:2813-2848`
- **输入场景**: 工具等待 accept 超时（HostTransactionRetryExhaustedError）
- **实际分支**: 超时后直接构造 `ToolAwaitingAcceptTimedOut`
- **预期行为**: 与 `_accept_with_retry`（行 2922-2971）一致，超时时发射 diagnostic ref 附加到结果
- **实际行为**: 不发射 diagnostic，下游 trace / 日志缺少诊断引用
- **直接证据**: `_accept_with_retry` 在 timeout 分支调用 `self._diagnostic_emitter.emit`；`_accept_awaiting_with_retry` 跳过此步骤
- **影响**: awaiting accept timeout 时问题排查缺少诊断信号
- **建议改法和验证点**: 在 `_accept_awaiting_with_retry` 的 timeout 分支添加 `self._diagnostic_emitter.emit` 调用；验证 awaiting timeout 日志包含 diagnostic ref
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 02-未修复-中-tool_runtime duplicate governance 冗余逻辑

- **入口/函数**: `ToolRuntimeExecutor._execute_one`
- **文件(行号)**: `dayu/host/tool_runtime.py:2463-2479`
- **输入场景**: 工具调用触发 duplicate governance 判定
- **实际分支**: 行 2470-2474 赋值 `duplicate_governed` boolean，行 2475-2479 重复相同条件检查才覆写 `policy_decision`
- **预期行为**: 单次条件判断后直接覆写
- **实际行为**: 同一条件判断执行两次，第一处仅赋值 boolean 但未使用
- **直接证据**: 两处条件完全相同：`policy_decision.kind is ToolPolicyDecisionKind.ALLOW and duplicate_decision.kind is not DuplicateDecisionKind.ALLOW`
- **影响**: 冗余逻辑不影响运行时正确性，增加维护歧义
- **建议改法和验证点**: 删除冗余的 `duplicate_governed` 赋值，直接在条件分支内覆写 `policy_decision`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 03-未修复-中-waiting resolve path 假设 started_event_id 非 None

- **入口/函数**: `DefaultHostResolveWaitService._resolve_in_transaction`
- **文件(行号)**: `dayu/host/waiting.py:786-809`
- **输入场景**: resume 路径返回 UPDATED 但 run 无 started_event_id（数据异常）
- **实际分支**: `created_event_id = result.run.started_event_id if created_event is not None else result.run.terminal_event_id`
- **预期行为**: 对 started_event_id 为 None 的情况有防御性处理
- **实际行为**: 传入 `IdempotencyResultRef` 的 created_event_id 可能为 None
- **直接证据**: 行 787-798 的条件假设 dispatch_record 非 None 时 started_event_id 一定非 None
- **影响**: 数据异常时幂等记录写入可能包含 None event_id，影响后续幂等重放
- **建议改法和验证点**: 添加 assert 或 fallback 处理 started_event_id 为 None 的情况
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 04-未修复-中-tool_runtime TruncationManager cursor single_use 硬编码

- **入口/函数**: `TruncationManager._store_cursor`
- **文件(行号)**: `dayu/host/tool_runtime.py:1490-1509`
- **输入场景**: 工具结果截断后存储 cursor
- **实际分支**: `single_use=True` 硬编码
- **预期行为**: 文档明确说明所有 cursor 均为单次使用
- **实际行为**: LLM 需在单次 fetch_more 调用中获取全部剩余内容，不支持分页式多次补读，但未在类文档中说明
- **直接证据**: 行 1506: `single_use=True`
- **影响**: 若业务场景需要分页补读，当前设计不支持且约束未文档化
- **建议改法和验证点**: 在 TruncationManager 类 docstring 中明确 single_use 约束
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 05-未修复-中-durable bootstrap DDL 非原子性

- **入口/函数**: `bootstrap_host_durable_store`
- **文件(行号)**: `dayu/host/durable/schema.py:966-989`
- **输入场景**: 首次创建 Host durable 数据库
- **实际分支**: `isolation_level=None` 下每条 DDL 自动提交，PRAGMA user_version 独立提交
- **预期行为**: 全部 DDL + PRAGMA 在同一事务中完成
- **实际行为**: 进程在 bootstrap 期间崩溃时，部分表已创建但 user_version 未设置
- **直接证据**: connection.py:202 使用 `isolation_level=None`；schema.py:985-988 无 BEGIN/COMMIT 包裹
- **影响**: 极端崩溃场景下 bootstrap 非原子。由于 IF NOT EXISTS 保证幂等恢复，实际安全但增加启动复杂度
- **建议改法和验证点**: 使用 `BEGIN IMMEDIATE` 包裹全部 DDL + PRAGMA user_version
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 06-未修复-中-durable after-commit callback 错误吞没

- **入口/函数**: `_run_after_commit`
- **文件(行号)**: `dayu/host/durable/transaction.py:360-382`
- **输入场景**: 多个 after-commit callback 中多个失败
- **实际分支**: for 循环遍历所有 callbacks，只记录 first_error
- **预期行为**: 收集所有 callback 错误或至少记录后续失败
- **实际行为**: 后续 callback 的失败被静默吞没，只抛出第一个错误
- **直接证据**: 行 371-378: `for` 循环只记录 `first_error`；行 378-382: 循环结束后才抛出
- **影响**: 缓存刷新等场景中后续 callback 失败无诊断信息
- **建议改法和验证点**: 收集所有错误到列表，抛出聚合错误或至少记录 warning 日志
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 07-未修复-中-tests 缺少 service/ui 层测试

- **入口/函数**: N/A
- **文件(行号)**: `tests/` 目录结构
- **输入场景**: N/A
- **实际分支**: 测试目录只有 contracts/engine/host/runtime 四层
- **预期行为**: 若 service/ui 层已实现，应有对应测试目录
- **实际行为**: 无 service/ 或 ui/ 测试目录
- **直接证据**: `find tests/ -maxdepth 1 -type d` 只有 contracts、engine、host、runtime
- **影响**: 若这些层已实现但无测试，违反覆盖率目标
- **建议改法和验证点**: 确认 service/ui 层是否已实现；若已实现则补充测试
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中（需确认）

### 08-未修复-中-tests 缺少 Host crash recovery 端到端测试

- **入口/函数**: N/A
- **文件(行号)**: `tests/host/`
- **输入场景**: Host 进程崩溃后重启
- **实际分支**: 只测试了 `host.close()` 后 reopen，未测试进程崩溃场景
- **预期行为**: 有端到端测试验证崩溃后 SQLite WAL 恢复和状态一致性
- **实际行为**: crash recovery 路径依赖 SQLite WAL + schema 约束保障，无显式测试
- **直接证据**: 无对应测试文件
- **影响**: 生产环境崩溃后状态一致性未被测试守护
- **建议改法和验证点**: 补充模拟进程强制终止后重启的测试，验证 EventLog 和状态索引一致性
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 09-未修复-低-engine 异常消息敏感标记精度

- **入口/函数**: Agent 模块敏感标记常量
- **文件(行号)**: `dayu/engine/agent.py:182-191`
- **输入场景**: 异常消息包含 "bearer" 无尾随空格
- **实际分支**: `"bearer "` 含尾部空格，不匹配 "bearer" 单独出现
- **预期行为**: 精确匹配敏感关键词
- **实际行为**: `"header"` 过于宽泛可能误杀无害消息（如 "header row"）
- **直接证据**: `_SENSITIVE_EXCEPTION_MARKERS` 定义
- **影响**: 防御性安全措施的精度问题，误杀后果仅为消息被 redact
- **建议改法和验证点**: 考虑使用 word boundary 匹配或更精确的 marker 列表
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 10-未修复-低-host open_host fallback 8192 魔法数字

- **入口/函数**: Host opener
- **文件(行号)**: `dayu/host/open_host.py:75-76`
- **输入场景**: context_budget_policy=None 时的兜底
- **实际分支**: 使用 8192 token 窗口和 1024 reserved output tokens
- **预期行为**: 有明确的模型能力关联说明
- **实际行为**: 硬编码数字缺少与具体模型能力的关联
- **直接证据**: `_INTERNAL_COMMAND_FALLBACK_CONTEXT_WINDOW_SIZE = 8192`
- **影响**: 注释已说明非生产默认值，但缺少更强的 fail-closed 机制
- **建议改法和验证点**: 在注释中说明该值与 target model 的关系
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 11-未修复-低-host session watch 20ms 轮询间隔

- **入口/函数**: watch_session_events
- **文件(行号)**: `dayu/host/open_host.py:348`
- **输入场景**: 无新事件时的 watch loop
- **实际分支**: 每 20ms 执行一次数据库查询
- **预期行为**: 使用事件驱动或更长的轮询间隔
- **实际行为**: `_SESSION_WATCH_POLL_INTERVAL_SECONDS = 0.02`
- **直接证据**: 常量定义
- **影响**: 高频 watch 场景下可能产生不必要的 SQLite 读压力
- **建议改法和验证点**: 考虑使用 SQLite notification 机制或增大间隔
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 12-未修复-低-tests import boundary 辅助函数重复

- **入口/函数**: N/A
- **文件(行号)**: `tests/contracts/test_import_boundary.py:53-63` 等 4 个文件
- **输入场景**: N/A
- **实际分支**: `_imported_module_names` 等函数在 4 个 import boundary 测试文件中完全重复
- **预期行为**: 抽取为共享测试辅助模块
- **实际行为**: 维护四份相同逻辑
- **直接证据**: 4 个 test_import_boundary.py 文件各自独立定义相同辅助函数
- **影响**: 增加同步维护成本
- **建议改法和验证点**: 抽取到 `tests/_import_boundary_helpers.py`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- **RECOVERING 状态 Run 的 cancel 路径**: `admission.py:765` 注释标记为"后续 phase 负责"。`cancel_session_runs` 是否会因存在 RECOVERING Run 而拒绝整个请求？（host core subagent）
- **proactive compaction orphan artifact**: compaction 在事务外执行，若成功但 Run 已被 cancel，compaction 结果被丢弃——是否产生 orphan compact artifact 文件？（host core subagent）
- **continuation 空 final answer**: `agent.py:999` continuation 路径可能 append 空串到 `continuation_content_parts`，普通 continuation + stop 路径是否应同样拒绝空 final answer？（engine subagent）
- **SSE 混合信号**: SSE 先有增量后有 fatal error 的混合信号，Host 侧处理是否完备？（engine subagent）
- **RunnerSpec NaN/Inf**: `stream_idle_timeout_seconds` 未做 `math.isfinite` 校验，与 `AgentPolicy.tool_execution_timeout_seconds` 不一致。（engine subagent）
- **resolve_wait 幂等安全**: `_resolve_resume` 调用后若 transition.status 不是 UPDATED，idempotency 记录尚未写入——调用方重试是否导致重复 event 写入？（host tools subagent）
- **batch timeout 提前终止**: 批级 timeout 逐个 call 递减，后续 call 可能极短甚至为零——是否需要 batch 级别提前终止策略？（host tools subagent）

## Residual Risk

- **`dayu/fins/` 未覆盖**: 财报领域能力边界、storage 仓储协议与实现未在本次 review 范围内。
- **`dayu/host/durable/` schema migration**: 无迁移路径，schema 变更需创建全新数据库（文档化的有意设计）。
- **memory projection lag**: memory repair / catch_up 长时间阻塞时，RunInputBuilder 可能因 snapshot 缺失或过期而阻塞 dispatch。
- **TruncationManager 内存**: run-scoped cursor dict 无主动内存上限保护，高频工具调用场景需关注。
- **OpenAI runner 测试依赖私有属性**: `runner._http_client._session` 注入方式依赖内部实现细节，重构可能导致测试大面积失效。
- **配置无 schema 验证**: 配置错误只在运行时被发现，不会在加载时被捕获。
