# Plan Review — WU-HOST-SESSION-EVENT-DELIVERY-01 S3 Minimal Amendment

## Reviewed target and scope

- Target：`docs/host/wu-host-session-event-delivery-01-plan.md` S3 slice，基于 `5ac328f0` 的最小 amendment
- Scope：只审查 amendment 变更，不审查 partial S3 实现、不读 AgentDS 本轮 artifact
- Reviewer：AgentMiMo
- Base：`5ac328f0`
- 输入 artifact：
  - Stop scope artifact：`docs/reviews/wu-host-session-event-delivery-01-slice3-stop-scope-codex.md`
  - Controller adjudication：`docs/reviews/wu-host-session-event-delivery-01-slice3-stop-condition-controller-adjudication.md`
  - Plan fix artifact：`docs/reviews/wu-host-session-event-delivery-01-slice3-stop-plan-fix-codex.md`

## Amendment diff summary

Plan diff（`git diff 5ac328f0 -- docs/host/wu-host-session-event-delivery-01-plan.md`）变更共 6 处：

1. S3 Allowed tests 列表新增 `tests/host/test_projection_read_model.py` 与 `tests/host/test_public_host_admin.py`
2. 新增授权段落限定两个文件的修改边界
3. S3 Tests/assertions 新增 standalone fake、两个新增 caller、pyright 零错误、已知 5 个错误归属说明共 4 条
4. S3 focused test 命令加入两个新文件
5. 8.4 pyright 命令从 `python -m pyright dayu/ tests/ utils/` 改为 `pyright`
6. 8.4 source scans 新增 `rg` constructor callsite 扫描命令，人工判定要求加入 callsite scan gate

## Assumptions tested

1. 两个文件确实是 direct composition caller 而非 standalone `create_host_command_handle` 路径
2. 两个文件不是新的 terminal writer/producer，不应进入 §5.3 production manifest
3. "同一个实例"传播语义是否精确
4. 授权边界是否足够窄，不误放 production manifest 修改或 scope 扩张
5. pyright 命令变更是否等价
6. 已知 5 个错误归属是否正确

## Findings

无 material finding。

逐项审查结论：

**A. 新增两个 test caller 是否精确**

- `test_projection_read_model.py:142-145`：直接构造 `HostCommandHandle(durable_store=..., admission_service=create_host_admission_service(...))`，不经过 `create_host_command_handle`。
- `test_public_host_admin.py:211-214`：同样直接构造 `HostCommandHandle(durable_store=..., admission_service=create_host_admission_service(...))`。
- 两者都没有 `terminal_post_commit_port` 参数，完整 pyright 报告各 2 个 required port 缺失错误。
- `rg` 扫描未发现第三个必须新增的 caller。
- 结论：精确，evidence-based。

**B. Composition owner 是否正确**

- 两个文件是 test composition caller，不是新的 terminal writer/producer。
- §5.3 production manifest（terminal writer/producer qualified callsite 表）未被修改——diff 中无该表的变更。
- amendment 正确区分了"composition caller"与"terminal writer"：前者需要 port 传播但不新增 manifest row。
- 结论：正确。

**C. 同一 endpoint 传播**

- 授权段落写明"把同一实例同时传入`create_host_admission_service`与`HostCommandHandle`"。
- 这与 S3 item 9 的"production constructors 显式接收同一个 opener terminal port"一致。
- 结论：语义对齐。

**D. Standalone fake / 完整 pyright / callsite scan 是否足够**

- `test_command_handle.py` standalone exact-notice runtime fake 已在 S3 allowlist，继续作为必过项。
- Amendment 显式添加了"完整 pyright 必须零错误"与"全测试 constructor callsite scan 必须归零 required port 错误，且不得出现第三个漏列 caller"两条 gate。
- 8.4 source scans 新增了 `rg -n 'HostCommandHandle\\(|create_host_admission_service\\(|HostDispatchScheduler\\.open\\(' tests --glob '*.py'` 命令。
- 结论：足够。

**E. 是否误改 production manifest 或扩张 scope**

- §5.3 production manifest 表未变。
- S3 Allowed production modules 未变。
- 授权段落明确禁止"修改测试业务场景或断言语义""修改 production constructor 或 terminal flags/dataflow""引入 optional/default/fallback、临时 port 或 rebind"。
- 冻结边界段落也明确不授权修改 production、tests、control doc、phaseflow umbrella 或 accepted plan 其它 slice。
- 结论：未误改，未扩张。

**F. Pyright 命令变更**

- 从 `python -m pyright dayu/ tests/ utils/` 改为 `pyright`。
- 两者等价：`pyright` CLI 默认读取 `pyrightconfig.json`，覆盖项目全部路径。
- 结论：无风险。

**G. 已知 5 个错误归属**

- `test_admission_multiprocess.py` 4 个旧字段断言错误 + `test_phase7_waiting_integration.py` 1 个 required factory 参数错误，均已在 S3 allowlist 内。
- Amendment 声明"继续在原 S3 allowlist 内按 owner contract 修复，本 amendment 不为其扩大 scope"。
- 结论：归属正确，scope 边界清晰。

## Open questions

无。

## Residual risks

无 material residual risk。Amendment 范围极窄、边界精确，不引入新 coupling 或 scope 模糊。

## Conclusion

**PASS**

Amendment 精确、最小、evidence-based。新增两个 test caller 的授权边界正确，production manifest 未被修改，验证 gate 完整，无 blocking finding。
