# Code Review

## Scope

- Mode: current changes (rereview of wu-cli-download-01 Slice 1 review fix)
- Branch: `codex/download-oracle`
- Base: `85e71774075e07d5b8f24a35930664fe80ffe322`（accepted plan amendment）
- Output file: `docs/reviews/code-review-20260810-013727.md`
- Included scope:
  - 全部 25 个 working tree changed files（+1368/−766）
  - 新增 untracked `dayu/fins/download_contract.py`
  - 全部 review fix changed production files：`download_contract.py`、`ingestion_runtime.py`、`fins_direct.py`、`sec_pipeline.py`、`sec_download_workflow.py`、`cn_pipeline.py`、`cn_download_workflow.py`、`sec_sc13_filtering.py`、`sec_rebuild_workflow.py`、`cn_download_rebuild.py`、`download_tools.py`、`filing_semantics.py`、`cn_form_utils.py`、`cli/commands/fins.py`、`cli/arg_parsing.py`
  - 全部 review fix changed test files（10 tests）
  - 原 review artifacts：`docs/reviews/code-review-20260810-004602.md`（DS）、`docs/reviews/code-review-20260810-004038.md`（MiMo）
  - Fix evidence：`docs/gateflow/wu-cli-download-01-slice1-review-fix-20260810-013105.md`
- Excluded scope:
  - Slice 2/3/4 未实施代码
  - README 文件（计划留待 closeout 统一更新）
  - 已冻结 registry/oracle 文件
  - Host/Engine 模块（不在本次 allowlist）
- Parallel review coverage: 无（单 reviewer 全覆盖）

## Findings

### DL-R01 — resolved — `FinsDownloadDateRange` 成为日期不变量唯一 owner，`start_is_explicit` typed bool 完整穿透 adapter-pipeline-workflow 链

- **入口/函数**: `FinsDownloadDateRange.__post_init__` → `build_fins_download_request` → adapter → pipeline → workflow → SC13 retry / CN business limits
- **文件(行号)**:
  - `dayu/fins/download_contract.py:68-90` — `__post_init__` 强制三个不变量
  - `dayu/fins/download_contract.py:174-179` — builder 构造 `FinsDownloadDateRange` 并传入 `start_is_explicit=start is not None`
  - `dayu/fins/ingestion_runtime.py:3651-3652` — adapter request 使用 `date_range=request.date_range` 完整透传
  - `dayu/fins/pipelines/sec_pipeline.py:1819-1821` — SEC adapter 从 `request.date_range.start_text/end_text/start_is_explicit` 取值
  - `dayu/fins/pipelines/sec_pipeline.py:556,606,652` — `start_is_explicit: bool` 作为 required keyword-only 参数
  - `dayu/fins/pipelines/sec_download_workflow.py:280,510` — workflow 接收并传给 `_retry_sc13_if_empty`
  - `dayu/fins/pipelines/sec_sc13_filtering.py:680-681` — `start_is_explicit` 直接 return 阻止 SC13 回溯
  - `dayu/fins/pipelines/cn_pipeline.py:643,693,1369` — CN adapter 同样透传 typed bool
  - `dayu/fins/pipelines/cn_download_workflow.py:56,255` — `use_default_business_limits=not start_is_explicit`
- **输入场景**: 用户显式给出 `--start 2025-01-01` 时 `start_is_explicit=True`；未给时为 `False`（即使未来 builder 产生默认非空起点，`start_is_explicit` 保持 `False`）
- **实际分支**: typed `bool` 无默认值、无 `start_date is None` 反推、无兼容 wrapper；`rg -n "start_date is None\|start_date is not None"` 在两个 workflow implementation 中返回空
- **直接证据**:
  - `download_contract.py:68-90`：`__post_init__` 拒绝显式无边界（`FinsDownloadUsageError("显式起始日期必须提供 start_bound")`）、倒序范围（`"--start 不能晚于 --end，请检查下载日期范围"`）、接受非显式非空边界供未来默认窗口
  - `download_contract.py:174-179`：builder 无重复倒序判断；倒序在 `__post_init__` 被统一拒绝
  - `sec_download_workflow.py:280`：`start_is_explicit: bool` 无默认值
  - `sec_sc13_filtering.py:680-681`：`if start_is_explicit: return list(filings)`
  - `cn_download_workflow.py:255`：`use_default_business_limits=not start_is_explicit`
  - AST 验证：所有 pipeline public 方法与 workflow implementation 的 `start_is_explicit` 均为无默认值 required keyword-only `bool`
- **影响**: 原 DL-R01 的 typed field 未穿透问题已修复。即使 builder 未来引入默认窗口，下游不会从字符串反推显式性，SC13 retry 与 CN 默认限制行为正确。
- **建议改法和验证点**: 已修复。验证点：`test_sc13_explicit_start_never_expands_lower_bound`（line 4366）、`test_download_non_explicit_nonempty_start_keeps_default_business_limit`（line 488）
- **修复风险（—）**: 已完成
- **严重程度（—）**: 已解决

### DL-R02 — resolved — `_cleanup_stale_filing_dirs` 与 `_CleanupStaleFilingDirs` Protocol 已删除，non-deletion owner test 保留

- **入口/函数**: 删除 `_cleanup_stale_filing_dirs`（定义）与 `_CleanupStaleFilingDirs`（Protocol）
- **文件(行号)**:
  - `dayu/fins/pipelines/sec_pipeline.py`：函数定义与 `sec_download_workflow.py` 中的调用已删除
  - `dayu/fins/pipelines/sec_download_workflow.py`：`_CleanupStaleFilingDirs` Protocol 删除；`run_download_stream_impl` 签名移除 `cleanup_stale_filing_dirs` 参数及调用
  - `tests/fins/test_sec_pipeline_download.py`：新增 `test_sec_ordinary_download_keeps_unselected_historical_document`（line 2866）作为公开 download 行为证明
- **输入场景**: `rg -n "_cleanup_stale_filing_dirs\|CleanupStaleFilingDirs" dayu tests` 返回空
- **实际分支**: 函数定义与 Protocol 不存在
- **直接证据**:
  - `rg -n "_cleanup_stale_filing_dirs\|cleanup_stale_filing_dirs\|CleanupStaleFilingDirs" dayu/ tests/` 返回空
  - `test_sec_ordinary_download_keeps_unselected_historical_document` 验证普通 SEC 下载保留非目标历史文档（`assert historical_meta_path.exists()`）
  - 旧 `FsFilingMaintenanceRepository` import 已从 `test_sec_pipeline_download.py` 移除，但该仓储仍在其他测试文件中正常使用（storage atomicity、download stream、runtime 测试）
- **影响**: 原 DL-R02 的 dead code 已清除；non-deletion 行为由公开接口测试覆盖。
- **建议改法和验证点**: 已修复。
- **修复风险（—）**: 已完成
- **严重程度（—）**: 已解决

### DL-R03 — deferred（未变化）— process CLI `--overwrite → rebuild_processed` 参数名与语义不一致

- 状态与原 review 一致：preprocess/upload 路径保留 `rebuild_processed` 字段，`--overwrite` 参数名在 process 命令中语义不精确。不在本 WU scope。
- 后续由独立 process-command 专项 WU 处理，owner 为 process command 的公开参数到 preprocess request 的映射契约。

### MiMo 001 — rejected（未变化）— `resolve_target_periods` 错误聚合变 fail-fast

- 多个无效 token 从聚合错误变为 fail-fast 只影响未承诺的错误格式。新 domain owner（`parse_fiscal_period_filter_value` in `filing_semantics.py`）仍以中文 actionable 错误 fail closed。
- 未在 `cn_form_utils` 或其它下游增加 catch、错误重建或兼容语义。裁决维持 rejected。

### MiMo 002 — rejected（未变化）— `_parse_form_types` 去重确认

- 原 MiMo finding 记录的去重逻辑预期与实际一致、影响为无，属于覆盖确认而非 material finding。代码未改。

### 新 Finding DS-RR01 — resolved — `_filter_filings_to_windows` 作为统一 inclusive 窗口终检正确加入 SEC workflow

- **入口/函数**: `run_download_stream_impl` → `_filter_filings_to_windows`
- **文件(行号)**: `dayu/fins/pipelines/sec_download_workflow.py:762-799`
- **输入场景**: `_extend_with_browse_edgar_sc13` 或 `_retry_sc13_if_empty` 可能引入窗口外候选
- **实际分支**: `_filter_filings_to_windows` 在 `_extend_with_browse_edgar_sc13` 之后、`_retry_sc13_if_empty` 之前各执行一次；后者使用 retry 在 place 中扩展后的 `form_windows`
- **预期行为**: 所有候选在进入 filing outcome 前必须满足 `form_windows[form] <= filing_date <= end_date`
- **实际行为**: 两次过滤确保 browse-edgar 结果在窗口内，且 SC13 retry 扩展窗口后的结果也被正确筛选
- **直接证据**:
  - `sec_download_workflow.py:496-501`：第一次 `_filter_filings_to_windows`（pre-retry）
  - `sec_download_workflow.py:516-521`：第二次 `_filter_filings_to_windows`（post-retry，使用 retry 在 place 中修改后的 `form_windows`）
  - `sec_download_workflow.py:787-793`：`lower_bound = form_windows.get(filing.form_type)` → `if lower_bound is None: continue` → `if lower_bound <= filing_date <= end_date: selected.append(filing)`
- **影响**: 正确性改进——browse-edgar 返回的窗口外 SC13 候选不会导致 retry 误跳过（pre-retry filter 清除窗口外候选使 retry 正确触发），且 retry 扩展窗口后的候选不会被误过滤（post-retry filter 使用扩展后的 `form_windows`）。
- **建议改法和验证点**: 已正确实现。验证点：`test_sc13_retry_expands_window_and_finds_filing`（line 4354）仍通过，`test_sc13_explicit_start_never_expands_lower_bound`（line 4366）新增。
- **修复风险（—）**: 已正确实现
- **严重程度（—）**: 已确认正确

## 逐项核验清单

### date-range `__post_init__` 不变量 / 错误类型
- ✅ `start_is_explicit=True` + `start_bound=None` → `FinsDownloadUsageError("显式起始日期必须提供 start_bound")`
- ✅ `end_is_explicit=True` + `end_bound=None` → `FinsDownloadUsageError("显式结束日期必须提供 end_bound")`
- ✅ `start_bound > end_bound` → `FinsDownloadUsageError("--start 不能晚于 --end，请检查下载日期范围")`
- ✅ 非空 bound + `explicit=False` 合法（`test_download_date_range_allows_non_explicit_nonempty_bounds`）
- ✅ 错误类型统一为 `FinsDownloadUsageError(ValueError)`

### builder 去重
- ✅ `_parse_form_types`（`download_contract.py:213-255`）使用 `seen: set[str]` 去重，保持首次出现顺序
- ✅ 去重发生在 canonicalization 之后（`canonical not in seen`）——避免大小写/别名变体绕过

### required bool 从 adapter 经 SEC/CN pipeline 到 workflow
- ✅ SEC: `SecDownloadAdapter.download()` → `pipeline.download_stream(start_is_explicit=request.date_range.start_is_explicit)` → `run_download_stream_impl(start_is_explicit)` → `host._retry_sc13_if_empty(start_is_explicit)`
- ✅ CN: `CnDownloadAdapter.download()` → `pipeline.download_stream(start_is_explicit=request.date_range.start_is_explicit)` → `run_cn_download_stream_impl(start_is_explicit)` → `use_default_business_limits=not start_is_explicit`
- ✅ 所有中间签名均为 required keyword-only `bool`，无默认值
- ✅ AST 验证全部 pipeline public 方法与两个 workflow implementation 一致

### 无 `start_date` 反推
- ✅ `rg -n "start_date is None\|start_date is not None"` 在两个 workflow 中返回空
- ✅ `rg -n "start_is_explicit.*=.*start_date\|反推"` 返回空
- ✅ `not start_is_explicit` 仅用于 CN workflow 的 `use_default_business_limits` 赋值

### SC13 与 CN 默认限制反例
- ✅ `start_is_explicit=True` 阻止 SC13 retry：`retry_sc13_if_empty:680-681`
- ✅ `start_is_explicit=False` + 非空 `start_date` 仍触发 SC13 retry（`test_sc13_retry_expands_window_and_finds_filing`）
- ✅ `start_is_explicit=False` + 非空 `start_date` 仍启用 CN FY 五年限制（`test_download_non_explicit_nonempty_start_keeps_default_business_limit`）
- ✅ `start_is_explicit=True` 不触发 SC13 retry（`test_sc13_explicit_start_never_expands_lower_bound`）

### 删除 stale helper 但 non-deletion owner test 保留
- ✅ `_cleanup_stale_filing_dirs` 函数定义与 `_CleanupStaleFilingDirs` Protocol 已删除
- ✅ 直接固化 stale helper 的测试已删除
- ✅ `test_sec_ordinary_download_keeps_unselected_historical_document`（line 2866）保留

### 无兼容代码
- ✅ `rg -n "hasattr\|getattr"` 在全部 changed production files 中返回空
- ✅ `FinsDirectUsageError` 已从 `fins_direct.py` 类定义与 `__all__` 中完全删除；`cli/commands/fins.py` 的 catch 已改为 `FinsDownloadUsageError`
- ✅ `FinsDownloadRequest` 从 `ingestion_runtime.py` 的 `__all__` 移除；import site 全部指向 `dayu.fins.download_contract`
- ✅ `_TOKEN_TO_PERIOD` 从 `cn_form_utils.py` 删除
- ✅ download production 路径无新增 compatibility re-export、shim、wrapper 或别名

### `tests/fins/test_cn_pipeline.py` 逐 hunk 确认
- ✅ diff hunks 止于 `@@ -480,6 +485,63 @@`（新增 `test_download_non_explicit_nonempty_start_keeps_default_business_limit`，line 488-542）
- ✅ 首个 upload test `test_upload_filing_stream_uploads_files_with_docling` 从 line 546 开始，在 diff 范围外
- ✅ `_PipelineDownloadFakeDiscoveryClient` 的 `candidates` 字段与 `default_candidates` 提取仅影响 download fake 行为
- ✅ 所有 download test 新增 `start_is_explicit=True`（已有显式日期测试）或 `start_is_explicit=False`（新测试）
- ✅ 零 upload fixture/helper/测试区域 diff

### 全部 current diff 新增问题检查
- ✅ 未发现新的 correctness 问题
- ✅ 未发现新的 stability 问题（无新增并发、partial failure、rollback、retry 风险）
- ✅ 未发现新的 ownership drift 问题（全部语义有唯一 owner）
- ✅ 未发现新的 overcoupling 问题
- ✅ `_filter_filings_to_windows` 的调用顺序（pre-retry → retry → post-retry）正确处理了 `form_windows` 在 retry 中被 in-place 扩展的语义
- ✅ `build_direct_download_request` 在 `fins_direct.py` 作为 service 层翻译 helper 正确封装 builder 调用
- ✅ `_prevalidate_download_request` 在 CLI 层正确先于 workspace resolution 执行静态校验

## 测试与静态验证

- 完整 Slice 1 test suite: **874 passed**, 3 warnings（edgartools deprecation），12.95s
- pyright: **0 errors, 0 warnings, 0 informations**
- ruff: All checks passed
- compileall: exit 0
- git diff --check: exit 0

## Open Questions

- 无

## Residual Risk

- DL-R03 仍为 deferred，由后续 process-command 专项 WU 接管
- Slice 2/3/4 的 provider policy（UA gate）、public terminal summary、canonical cancellation、Docling process boundary、storage concurrency 与 integrity repair 仍未实施
- README 按 accepted plan 留待 closeout 统一更新
- 未运行真实 CLI；网络 provider 行为不在本 review 验证范围
- `_filter_filings_to_windows` 与 SC13 retry 的 `form_windows` in-place mutation 交互正确，但该 mutation 是预存行为（非本 review fix 引入），若未来 SC13 retry 不再原地修改 `form_windows`，需同步调整 post-retry filter 的语义

## 结论

**PASS** — 无 blocker。

DL-R01 和 DL-R02 的修复均正确实施并验证。DL-R03 维持 deferred。MiMo 001/002 维持 rejected。当前 review fix diff（base `85e71774` → working tree）未引入新的 correctness、stability 或 ownership 问题。

全部 874 tests 通过，pyright 零报错，静态 stop-condition 全部满足，residual risk 限于 accepted plan 的后续 slice 范围。
