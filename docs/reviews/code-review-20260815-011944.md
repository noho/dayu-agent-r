# Code Review

## Scope

- Mode: current changes
- Branch: codex/upload-filing-oracle
- Base: c5446b770d238aafd8c42552dadbe132cba94ad2
- Output file: docs/reviews/code-review-20260815-011944.md
- Included scope: 29 files changed (+1318/-476)
  - `dayu/fins/domain/company_meta_contract.py` (new): CompanyMeta commit intent & authoritative merge contract
  - `dayu/fins/storage/_fs_storage_infra.py`: identity guards, lock ordering, lstat-based I/O typing
  - `dayu/fins/storage/_fs_company_meta_core.py`: resolve_company_ticker single route, inventory scan
  - `dayu/fins/storage/repository_protocols.py`: CompanyTickerAliasConflictError, CompanyTickerIdentityCorruptionError
  - `dayu/fins/storage/fs_company_meta_repository.py`: protocol delegation
  - `dayu/fins/storage/__init__.py`: re-exports
  - `dayu/fins/tools/upload_tools.py`: CompanyTickerIdentityCorruptionError catch ordering
  - `dayu/fins/tools/read_runtime.py`: resolve_company_ticker single route, typed error codes
  - `dayu/fins/tools/error_contract.py`: WORKSPACE_IDENTITY_CORRUPTED, STORAGE_UNAVAILABLE
  - `dayu/fins/upload_failure.py`: TICKER_ALIAS_CONFLICT code, typed failure projection
  - `dayu/fins/pipelines/upload_company_meta.py`: intent-based company meta resolution
  - `dayu/fins/pipelines/sec_company_meta.py`: intent-based SEC company meta
  - `dayu/fins/pipelines/cn_download_company_meta.py`: intent-based CN company meta
  - `README.md`, `dayu/fins/README.md`: ticker alias documentation
  - 16 test files: storage contract, identity contract, company meta contract, ingestion tools, etc.
- Excluded scope: generated/vendor/build/cache
- Parallel review coverage: 无

## Findings

### 1-未修复-低-`_scan_actual_published_company_identities` 对每个 candidate 持 publication guard 后逐个读取 descriptor/meta，锁粒度为单 entry

- **入口/函数**: `_FsStorageInfra._scan_actual_published_company_identities`
- **文件(行号)**: `dayu/fins/storage/_fs_storage_infra.py:891-957`
- **输入场景**: workspace 内有 N 个 published corpus 时，`resolve_company_ticker` 或 `commit_batch` identity guards 路径
- **实际分支**: 逐个 candidate 持 publication guard → lstat → read descriptor → read meta → release
- **预期行为**: 在 workspace identity guard 内扫描全部 published identities，每个 entry 独立持 publication guard 防止并发 commit 交错
- **实际行为**: 实现正确——每个 candidate 独立持 publication guard，按 candidate key 排序确保确定性，identity guard 防止新 corpus 并发插入
- **直接证据**: `_fs_storage_infra.py:931-956`，每个 candidate 独立 `_acquire_publication_guard_for_key` → `_release_lock_after_operation`
- **影响**: N 较大时锁获取/释放次数线性增长；当前生产规模下不构成瓶颈
- **建议改法和验证点**: 若未来 corpus 数量显著增长，可考虑批量 snapshot 策略；当前无需修改
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-`_recover_single_batch_dir` 在 `_PHASE_STARTED` 阶段跳过 identity/publication guards 直接清理 staging

- **入口/函数**: `_FsStorageInfra._recover_single_batch_dir`
- **文件(行号)**: `dayu/fins/storage/_fs_storage_infra.py:2134-2307`
- **输入场景**: 进程在 `begin_batch` 后、任何 publication 前崩溃，留下 `_PHASE_STARTED` orphan
- **实际分支**: `phase == _PHASE_STARTED` 且无 backup → 直接进入 cleanup 分支
- **预期行为**: `_PHASE_STARTED` 的 batch 尚未执行任何 publication，staging 内无已发布状态需要保护，可安全清理
- **实际行为**: 实现正确——`_PHASE_STARTED` 时 staging 尚未 swap 到 target，无需 identity/publication guards
- **直接证据**: `_fs_storage_infra.py:2245-2274`，`_PHASE_STARTED` 走到最后的 `elif` 分支，只在 backup 存在且 target 缺失时恢复 backup
- **影响**: 无——逻辑正确
- **建议改法和验证点**: 无需修改；注释已说明意图
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-未修复-低-`upload_tools.py` 的 `ValueError` catch 范围较宽，可能捕获非参数类 ValueError

- **入口/函数**: `FinsUploadToolCallable.__call__`
- **文件(行号)**: `dayu/fins/tools/upload_tools.py:113-120`
- **输入场景**: `prepare_observed_upload` 内部抛出非参数类 `ValueError`（如 storage 内部校验失败）
- **实际分支**: `except ValueError as exc` → `_failed_outcome(error=_ERROR_INVALID_ARGUMENT)`
- **预期行为**: 参数错误归类为 `invalid_argument`；存储/业务错误应归类为 `fins_upload_start_failed`
- **实际行为**: `CompanyTickerIdentityCorruptionError` 已在前面单独 catch 并投影为 `fins_upload_start_failed`；其余 `ValueError`（如 ticker 非法、alias 冲突的 `ValueError` 子类）归类为 `invalid_argument`。这是正确的——`CompanyTickerAliasConflictError` 继承自 `ValueError` 但语义上确实是用户输入错误
- **直接证据**: `upload_tools.py:103-120`，catch 顺序：`FinsIngestionStartCancelledError` → `CompanyTickerIdentityCorruptionError` → `ValueError` → `OSError` → `Exception`
- **影响**: 当前 catch 顺序正确；`CompanyTickerAliasConflictError` 作为 `ValueError` 子类被 `except ValueError` 捕获，投影为 `invalid_argument`，这是合理的——用户需要修正 alias 声明
- **建议改法和验证点**: 若未来 storage 内部抛出非用户输入类 `ValueError`，需评估是否需要单独 catch；当前无需修改
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 4-未修复-低-`merge_company_meta_for_commit` 的 `preserve_published` 模式在 identity 未变化时保留旧 `updated_at`

- **入口/函数**: `merge_company_meta_for_commit` → `_company_meta_from_published`
- **文件(行号)**: `dayu/fins/domain/company_meta_contract.py:239`
- **输入场景**: `preserve_published` 模式，identity 未变化
- **实际分支**: `final_identity == current_published.ticker_identity` → `updated_at = current_published.updated_at`
- **预期行为**: 没有真实 mutation 时保留 published `updated_at`，避免无意义时间戳刷新
- **实际行为**: 实现正确——只在 identity 真正变化时才更新 `updated_at`
- **直接证据**: `company_meta_contract.py:239`，条件判断 `final_identity == current_published.ticker_identity`
- **影响**: 无——行为正确
- **建议改法和验证点**: `test_preserve_published_without_identity_mutation_keeps_updated_at` 已覆盖此路径
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

无。

## Residual Risk

1. **测试覆盖**: 新增 `test_company_identity_storage_contract.py` 和 `test_company_meta_contract.py` 覆盖了核心 identity 冲突、并发发布、corruption 分类、锁序、meta-less 状态等关键路径。`test_fins_storage_provider.py` 和 `test_fins_ingestion_tools.py` 的增量修改覆盖了 `CompanyTickerIdentityCorruptionError` catch 顺序和 `resolve_company_ticker` 单一路由。单文件覆盖率目标 >= 80% 已满足。

2. **README 触发规则**: `dayu/fins/` 修改已触发 `dayu/fins/README.md` 更新；用户可见 CLI `--ticker` 别名行为已触发根目录 `README.md` 更新。两处 README 变更均在职责范围内。

3. **pyright**: 未在本次 review 中运行 pyright；需在合入前确认无新增类型错误。

4. **旧 route/fallback/helper 零残留**: read runtime 的 `_resolve_canonical_ticker` 现在只通过 `company_repository.resolve_company_ticker` 单一路由，不再有旧的 fallback 路径。storage 的 `resolve_company_ticker` 在 identity guard 内扫描 descriptor + CompanyMeta 构造唯一 index，无 loose fallback。

5. **跨市场/V.BA/语法变体 query**: `test_unique_index_supports_cross_market_and_canonical_equivalent_queries` 覆盖了 `0700-hk`、`tcehy.us`、`600519.sh`、`kweichow` 等语法变体。`test_meta_less_corpus_can_be_supplemented_and_routes_aliases` 覆盖了 `V.BA` / `VISA` 路径。

6. **duplicate canonical / conflict**: `test_conflicting_identity_is_rejected_before_published_side_effect` 覆盖了 incoming alias 冲突。`test_duplicate_published_alias_is_typed_corruption_not_incoming_conflict` 覆盖了 durable duplicate owner corruption。
