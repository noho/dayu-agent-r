# Code Review（Re-Review）

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `a29c8eb5`（UF-FIX08 Slice 2 HEAD）
- Output file: `docs/reviews/code-review-20260816-155308.md`
- Re-review 输入：前 review `docs/reviews/code-review-20260816-153600.md`（4 findings，Finding 1 由 controller
  裁决为 blocker）；
  `docs/gateflow/uf-fix08-existing-source-auto-repair-slice3-code-review-fix-20260816.md`；
  更新后的 `docs/gateflow/uf-fix08-existing-source-auto-repair-slice3-implementation-20260816.md`
- Included scope: 5 个生产文件、7 个测试文件的全部 workspace changes（相对 `a29c8eb5`，无 staged/committed 增量）
- Excluded scope: 无
- Parallel review coverage: 无（单 reviewer 全链路走读）

## 前 review findings 状态

| Finding | 前状态 | 本轮验证 | 状态 |
| --- | --- | --- | --- |
| 1（中）：validated request 重复实现 static selection 派生规则 | 未修复 | 已修复 |
| 2（低）：docstring Raises 遗漏 | 未修复 | 已修复 |
| 3（低）：SEC/CN identity mismatch 测试未命中 workflow guard | 未修复 | 已修复 |
| 4（低）：矛盾 derived projection 无直接回归测试 | 未修复 | 已修复 |

## 修复验证（逐项证据）

### Finding 1：`_project_fins_upload_filing_selection` 唯一 owner

- **入口/函数**: `_project_fins_upload_filing_selection`（`ingestion_runtime.py:914-960`）
- **验证结论**: 通过。该纯函数只接受已规范化保序 `tuple[Path, ...]`，duplicate（941-943）、selector
  cardinality（944-945）、multi-file primary 必填（946-947）、membership（950-951）、implicit primary
  （948）与 companions 保序（952-956）全部且只在这里定义；不读取 filesystem、不产生 usage 文案，失败面为
  封闭 `_FinsUploadFilingSelectionFailure`（836-843），成功面为 frozen `_FinsUploadFilingSelectionProjection`
  （846-875）。
- **两个调用方**: static admission（1287-1294）只把 closed failure 映射到既有 usage code（映射表
  878-894 五元全量、无 KeyError 面）；validated constructor（`_validate_filing_selection_matches_request`
  994-1009）复用同一 `_normalize_fins_upload_paths` → 同一 projection → 同一
  `to_file_selection()` 构造 expected selection。旧 `_select_fins_upload_filing_files` 已删除，全仓无引用
  （grep 零命中）。
- **无第二套 raw 推导**: 构造函数路径不再有自己的一套 selector/primary/companions 分支，只有 projection
  failure → `ValueError`（含 closed enum value）与 selection 不相等 → `ValueError` 两个边界投影。

### Finding 2：Raises 完整

- `read_filing_upload_state()`（`_fs_filing_upload_state_core.py:75-84`）已补
  `RuntimeError: exact inspector 未返回 target，或可信状态缺少 business meta 时抛出`。
- `ValidatedFinsUploadFilingRequest.__post_init__`（793-796）已补 `FinsUploadUsageError`；
  `_validate_filing_selection_matches_request`（979-983）已补 `FinsUploadUsageError`。
- `to_file_selection()` 可抛的 `FinsUploadFormatError` 是 `ValueError` 子类
  （`upload_format_contract.py:85`），已由既有 `ValueError` 行覆盖，非遗漏。

### Finding 3：SEC/CN identity mismatch 测试命中 workflow guard

- `tests/fins/test_sec_pipeline_upload_filing_stream.py:1464-1525` 与
  `tests/fins/test_cn_pipeline.py` 同构测试改为只漂移 `internal_document_id`
  （`replace(validated, internal_document_id=f"{...}-mismatch")`）。`internal_document_id` 不参与
  `__post_init__` 的 published-state 校验，构造合法通过；异常由 workflow
  `_assert_authoritative_filing_identity`（`sec_upload_workflow.py:309-330`）抛固定
  `RuntimeError("filing authoritative identity mismatch")`，测试精确断言该 RuntimeError 且
  `begin_tokens == []`、published tree 零 mutation。生产 workflow 守卫恢复直接覆盖。

### Finding 4：derived=A/basename=B 矛盾场景钉住

- `tests/fins/test_fins_storage_atomicity.py:6493-6586` 三阶段：同 basename COMPLETE →
  `derived_from` 精确指向 A 且 Docling basename 属于 B → `REPAIR_REQUIRED` + 仅
  `DERIVED_PROJECTION_MISMATCH` + trusted revision 保留 → derived 缺失 + basename 双命中 →
  `UNSAFE`/`revision=None`/`FILE_DECLARATION_UNTRUSTED`。设计接受的 repairable 语义与真实歧义的
  UNSAFE 收口都被直接断言。

### 直接构造 fail closed（re-review 附加检查）

- `test_validated_filing_repair_contract_rejects_incomplete_selection_and_target` 新增 duplicate
  forgery 分支（`files=(primary, primary)` → `ValueError` 含 `duplicate_file_path`）；empty delete
  selection（`非空完整`）、截断 selection（`不完整一致`）、expected target 错配（`expected target`）继续
  覆盖。异常面全部 typed/bounded（closed enum value 或固定中文文案），无路径泄漏。

## Findings

未发现实质性问题。四个前 findings 均已修复并验证；未发现新缺陷。

（以下为可被 controller 直接忽略的观察，不构成 finding：`_FILING_SELECTION_FAILURE_USAGE_CODES`
映射为模块级 dict，若未来新增 failure enum 成员而漏配，static admission 会抛裸 KeyError——该映射与 enum
同模块相邻，风险极低，如偏好可加一条与 `upload_failure.py` 相同的模块级完整性断言。）

## 回归核对（原 8 项 Slice 3 adversarial focus）

1. 同 guard 单一 inspection 消费：`_fs_filing_upload_state_core.py` 逻辑未变（仅 docstring），通过。
2. repair contract/validated union 封闭：`upload_repair_contract.py`、`__post_init__` 校验链未变，通过。
3. validator precedence：`validate_fins_upload_filing_request` 逐行核对未变（static → published
   invariant → UNSAFE → REPAIR_REQUIRED exact auto → 原语义 → company decision），通过。
4. 三个 failure factory：`upload_failure.py` 未变，通过。
5. raw runtime start 传播：runtime 方法未变，通过。
6. same-basename owner：`_fs_source_integrity.py` 未变（30 行 diff 与前 review 一致），通过。
7. fixture 迁移意图：SEC/CN/CLI/ingestion fixtures 前 review 已核，本 fix 只在两处 identity 测试改注入
   字段，意图加强而非漂移，通过。
8. scope/typing/README：见下。

## 独立复验证据

```text
pytest -q tests/fins/test_fins_ingestion_runtime.py tests/fins/test_fins_service_runtime.py tests/fins/test_upload_failure.py
  333 passed
pytest -q tests/fins/test_fins_storage_atomicity.py tests/fins/test_fins_storage_provider.py
  298 passed（含扩展后的三阶段 same-basename 回归）
pytest -q <两 UF-FIX07 Docling 节点 + same-basename 回归 + SEC/CN identity mismatch 两节点>
  5 passed
pytest -q tests/fins
  1802 passed, 1 skipped（skip 为仓库既有环境条件 skip）
python -m pyright dayu/ tests/ utils/
  0 errors, 0 warnings, 0 informations
coverage（focused matrix + test_upload_failure，独立重跑）:
  _fs_source_integrity.py 85%、_fs_filing_upload_state_core.py 93%、upload_repair_contract.py 82%、
  ingestion_runtime.py 88%、upload_failure.py 92%（全部 >=80%）
git diff --check：干净
README/oracle/scenario/design/registry diff：无输出
新增 diff 签名扫描：无 object/Any/hasattr/getattr/伪 callable cast
```

## Open Questions

- 无。

## Residual Risk

- 与前 review 相同的 Slice 序列残留：staged reset/repair publication（Slice 4）、stale/blocked 接线与
  SEC/CN/HK async typed failed event（Slice 4/5）、material repair、旧 schema migration、UF-PF08/UF-PF12
  evidence——均已有明确 owner，非本 slice 范围。
- `read_filing_upload_state` 的 MISSING fast-path（ticker 目录不存在时不经 inspector 直接构造
  classification）为基线既有行为，与 inspector rule 1 语义等价。
- 未运行真实 CLI/provider/converter evidence（plan 禁止）。
- 发现并行 review artifact：`docs/reviews/code-review-20260816-155217.md`（同 base 的 re-review，
  本 session 期间由其它 agent 产生）与本 review 结论独立；另前 review
  `docs/reviews/code-review-20260816-153600.md` 与 `docs/reviews/code-review-20260816-153026.md` 仍为
  未追踪文件。controller 合并裁决时需注意避免重复计票。

## Verdict

**PASS**。四个前 findings 全部闭环（含 controller 裁决的 blocker），修复未改变 static validation
precedence、未改变 exact primary/companions 行为、未引入新语义漂移或测试弱化；独立复验
1802 tests、pyright、逐文件 coverage、scope/typing/README/oracle/design guards 全部通过。
