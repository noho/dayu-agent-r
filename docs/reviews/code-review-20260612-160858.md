# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: `main`
- Output file: `docs/reviews/code-review-20260612-160858.md`
- Included scope: dirty diff (unstaged + untracked) covering 5 test files and 1 control doc
- Excluded scope: production code (`dayu/host/`) was not changed in this diff
- Parallel review coverage: 无

## Review Checklist

### 1. read_api EventLogRow → HostEvent projection tests 真实覆盖 FAILED/CANCELLED/LOST final_answer=None

**结论：覆盖正确。**

`test_read_api_terminal_policy.py` 新增三个测试：

- `test_failed_terminal_projection_never_builds_final_answer`（行 28–49）
- `test_cancelled_terminal_projection_never_builds_final_answer`（行 53–75）
- `test_lost_terminal_projection_never_builds_final_answer`（行 78–100）

每个测试通过 `_project_terminal_event` 打开真实 SQLite durable store，在 `run_read` 事务中调用生产函数 `_host_event_from_row(transaction, row)`，传入含误导性 `final_answer` / `content` 字段的 inline payload 的 `EventLogRow`。

直接证据 — 生产代码 `read_api.py`：
- `_failed_host_event`（行 820–844）：`final_answer=None` 硬编码，只读 `error_message`
- `_cancelled_host_event`（行 847–871）：`final_answer=None` 硬编码，只读 `cancel_reason`
- `_lost_host_event`（行 874–898）：`final_answer=None` 硬编码，只读 `error_message`

测试 assert `event.final_answer is None` 且正确读取 `error_message` / `cancel_reason`，沿真实投影路径验证，不是 dataclass 构造测试。

### 2. terminal summary payload tests 准确覆盖 preference、descriptor incomplete/malformed、overlong preserved

**结论：覆盖正确。**

`test_terminal_summary_payload.py` 新增四个测试：

- `test_overlong_allowed_text_is_preserved_by_source_selection`（行 180–200）：用 `"终态回答" * 4096` 验证两个 source-selection helper 保留长文本不做截断。符合 plan 的"caller-side truncation is out of scope"。
- `test_continuity_resolver_prefers_run_final_answer_over_artifact`（行 203–241）：写入 terminal artifact（`content="artifact fallback answer"`），RUN payload 同时有 `final_answer="inline final answer"` 和 descriptor。assert 返回 `"inline final answer"`。
- `test_continuity_resolver_requires_complete_terminal_descriptor`（行 244–283）：分别测试只有 `ref` 没有 `digest`、只有 `digest` 没有 `ref` 两种情况。assert 两者均返回 `(None, None)`，且不读取裸 `content` / `summary_text`。
- `test_continuity_resolver_rejects_malformed_terminal_descriptor`（行 286–316）：`terminal_summary_ref=123`（int 类型非法）。assert `pytest.raises(HostDurableError, match="terminal_summary_ref")`。

直接证据 — 生产代码 `_terminal_answer.py` 行 49–66：`_optional_descriptor_text` 在 `not isinstance(value, str)` 时 raise `HostDurableError`；`terminal_summary_ref is None or terminal_summary_digest is None` 时 early return `None`。

### 3. empty_final_answer governance shape 断言正确

**结论：断言正确。**

`test_engine_ingest_mapping.py` 的 `test_empty_final_answer_closes_failed_without_run_succeeded`（行 428–469）新增六行：

```python
assert "content" not in payload         # ATTEMPT_FAILED payload
assert "final_answer" not in payload
run_payload = _payload(result.events[1])
assert run_payload["error_code"] == "empty_final_answer"
assert "content" not in run_payload     # RUN_FAILED payload
assert "final_answer" not in run_payload
```

验证 Host governance 把 Engine 空 `final_answer` 转换为 `RUN_FAILED` 后，终态 payload 不携带可展示的 `content` 或 `final_answer`。两个终态 event（ATTEMPT_FAILED + RUN_FAILED）均被覆盖。

### 4. memory projection assertions 符合 consumer/adapter hydration 事实

**结论：符合事实，无误写反。**

`test_memory_projection.py` 三处改动：

- `test_pre_compact_projection_only_builds_selected_recent_window`（行 302–303）：新增 `assistant_item = snapshot.trace_memory.selected_recent_window[1]` + `assert assistant_item.included_reason is MemoryIncludedReason.SELECTED_RECENT_WINDOW`。验证 assistant item 以 `SELECTED_RECENT_WINDOW` 原因纳入，不是 evidence-backed fact。
- `test_projection_consumer_hydrates_terminal_content_as_final_answer`（行 459–463）：断言从 `selected[0].text == "artifact final answer"` 改为更结构化的 `assert len(selected) == 1` + `assert selected[0].text` + `assert selected[0].included_reason` + `assert evidence_fact_memory.evidence_backed_facts == ()`。
- `test_memory_direct_consumer_does_not_follow_terminal_descriptor`（新，行 466–498）：传入只有 `terminal_summary_ref` / `terminal_summary_digest`（无 inline `final_answer`）的 `RUN_SUCCEEDED` 事件给 `build_conversation_memory_snapshot_from_events`。assert `selected_recent_window == ()`，证明 direct consumer 不自行跟随 descriptor。descriptor hydration 属于 durable projection / run-input adapter。

直接证据 — 生产行为一致性：
- direct consumer 只读 inline `final_answer`（lenient），不访问 durable payload store
- durable projection consumer 通过 `ProjectionRunner` + `ConversationMemoryProjectionConsumer` 走完整 durable 路径，先 hydrate descriptor-backed terminal content 为 transient `final_answer`，再进入 memory consumer
- 两者最终都只进入 `selected_recent_window`，不进入 `evidence_fact_memory`

### 5. 弱类型、无类型签名、README 漏更新

**结论：无问题。**

- 所有新增测试函数均有完整类型标注（`-> None`、`-> HostEvent`、`-> tuple[str | None, str | None]`）
- 无 `Any`、`object`、无类型参数
- README 更新未触发：本次变更仅涉及 `tests/` 目录下已有 Host 测试文件的补充断言和新测试文件，不新增测试层、命令或维护规则；无 `dayu/host/` 生产代码变更

### 6. pytest/pyright 可信

**结论：可信。**

- `pytest tests/host/test_terminal_summary_payload.py tests/host/test_read_api_terminal_policy.py tests/host/test_engine_ingest_mapping.py tests/host/test_memory_projection.py -q`：**94 passed in 0.83s**
- `python -m pyright dayu/ tests/ utils/`：**0 errors, 0 warnings, 0 informations**

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- 本次 S1 变更仅涉及测试和控制文档，无生产代码改动。WU-CM-06 剩余生产代码清理是 Slice 2（`dayu/host/terminal_summary_payload.py` 和 `dayu/host/_terminal_answer.py` docstring 收敛）。
- `test_read_api_terminal_policy.py` 的 `EventLogRow` 通过 `canonical_json_dumps` 构造 inline `payload_json`，正确模拟了 durable store 中的序列化格式。
