# Code Review

## Scope

- Mode: current changes
- Branch: codex/upload-filing-oracle
- Base: `f6b2d04c`
- Output file: `docs/reviews/code-review-20260818-025138.md`
- Included scope: targeted re-review，只验证 `docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-s2-rereview-adjudication-20260818.md` 两项
- Excluded scope: production code、frozen evidence、oracle、scenario registry、README
- Parallel review coverage: 无

## Targeted Verification

### 验证项 1：activate/run 已在 try/finally 内

**目标**：`activate_observation` 与 `executor.run_next()` 必须位于 `try` 块内，确保激活/执行异常均经 `finally` abandon。

**直接证据**：`tests/fins/test_fins_ingestion_tools.py` 新增函数 `test_upload_tool_raw_period_is_canonical_at_runner_contract_boundary` 第 246-255 行（相对 diff）：

```python
try:
    runtime.activate_observation(record.handle)
    executor.run_next()
    assert len(upload_runner.calls) == 1
    validated_request = upload_runner.calls[0]
    assert isinstance(validated_request, ValidatedFinsUploadFilingRequest)
    assert validated_request.normalized_fiscal_period == expected_period
finally:
    asyncio.run(runtime.abandon_observation(record.handle))
assert runtime._observations == {}
```

**结论**：`activate_observation` 和 `executor.run_next()` 均在 `try` 块内，`abandon_observation` 在 `finally` 块内。**通过**。

---

### 验证项 2：CN case id 为 UF-024，seeded 判定使用该 id

**目标**：恢复 CN case id 为原值 `UF-024`，seeded 判定继续使用该 id。

**直接证据**：

1. `tests/cli/test_fins_commands.py` diff 第 14-17 行：CN case 保持原 id `UF-024`、ticker `600519`，未被重命名：

```python
(
    "UF-024",
    ("--ticker", "600519", "--fiscal-year", "2024", "--fiscal-period", "9M"),
    "--fiscal-period 仅支持 FY、H1、Q1、Q2、Q3、Q4",
),
```

2. `tests/cli/test_fins_commands.py` diff 第 32-35 行：`seed_workspace` 判定包含 `UF-024`：

```python
seed_workspace = case_id in {
    "UF-S2-seeded-invalid-report-date",
    "UF-024",
}
```

**结论**：CN case id 保持 `UF-024` 未被改写，seeded 判定正确引用该 id。**通过**。

---

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- 无。两项验证均基于直接代码证据通过。
