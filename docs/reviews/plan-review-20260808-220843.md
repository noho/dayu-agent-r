# F20 RR6 最终独立 Plan Review（AgentMiMo）

## Reviewed target and scope

- review round：F20 RR6；reviewer：AgentMiMo；二值结论：`PASS`。
- target plan：`docs/gateflow/pr-190-f20-plan-20260808.md`，冻结 SHA-256：
  `66074bc59b468c2614e14b7e6840a39b45d09aac9e4454dbff576550ac8b27f7`。
- plan-review fix：`docs/gateflow/pr-190-f20-plan-review-fix-20260808.md`，冻结 SHA-256：
  `83fe517bd60534429abced41289203ec1bc78bc78db14b84cb57b84946a6f743`。
- binding goal：`docs/gateflow/pr-190-f20-goal-confirmation-20260808.md`，冻结 SHA-256：
  `9e537fb663e647dccb702056316ad7a9a79c15a14149f8f222c76b289a9c67e0`。
- RR5 adjudication：`docs/gateflow/pr-190-f20-rr5-plan-review-adjudication-20260808.md`，冻结 SHA-256：
  `a1926e2c231f8ece5c1a9e8f5aeefb8380f390a7ea12e2bf58293252441e7f0f`。
- 重点范围：RR5 finding closure；attempt-free pre-compact canonical budget trigger；proof-only candidate projection边界；
  post-compact/R4 allow manifest与identity；corruption matrix；双typed port、网络隔离、formal stock CLI、storage、deadline/publication。
- 排除范围：产品与测试修改、provider/network调用、F18/F19、accepted oracle/scenario、
  `docs/reviews/plan-review-20260808-095346.md`及 DS RR6 artifact。上述排除对象均未读取或修改。

## Assumptions tested

1. **Goal-bound minimal design**：binding goal已同步接受RR5裁决，仅把不存在的pre-compact runner manifest从trigger contract删除；
   post-compact/R4 allow、proof双端口、formal stock CLI、storage与publication目标均未扩张或削弱。无产品修复、公共schema或兼容层漂移。
2. **Pre-compact owner fact**：`dayu/host/dispatch.py:1952-2010`先冻结complete candidate并计算ordinary sizing；只有
   `ALLOW_DISPATCH`调用allow commit。compact/hard分支直接调用`append_context_budget_evaluated_in_transaction`，明确传入
   `attempt_id=None`、`execution_id=None`，随后进入compact/hard owner，不调用runner manifest recorder。
3. **Canonical trigger sufficiency**：`dayu/host/context_events.py:104-128`的strict
   `ContextBudgetEvaluatedPayload`直接拥有decision/run、candidate cursor、candidate logical projection ref、input digest、stage、policy、
   estimator、prediction、threshold、pressure与action；其deterministic identity严格绑定run/cursor/digest/stage/policy/estimator。
   plan据此只以唯一attempt-free fact加同Run request/同operation terminal证明R2/R3 trigger，满足
   `soft <= predicted < hard`与`compact_soft_threshold`，没有继续要求manifest kind/ref/SHA/sizing snapshot或Attempt/execution/dispatch。
4. **Proof-only projection不冒充Host事实**：完整pre-compact candidate若需审计，只能使用
   `projection_kind="run_owned_precompact_candidate_proof"` closed variant；cursor、logical ref、input digest必须与canonical budget逐字段相等，
   并显式标记`host_runner_manifest=false`。它只属于provider-free场景存在性proof，不能命名成Host manifest，不能进入formal
   private/publication stage schema，也不能被stock CLI链引用补证。
5. **Allow-only manifest边界**：`dayu/host/dispatch.py:2815-2885`的
   `_commit_dispatch_candidate_in_transaction`才拥有complete sizing snapshot、prepared runner-call manifest与Run/Attempt/execution/dispatch
   原子提交。plan继续要求每次accepted后的唯一`post_compact` allow candidate具备真实manifest及accepted truth、Memory、budget与实际
   pending dispatch identity equality；hard则在Attempt/dispatch前fail closed，不得算作consumption。
6. **R4 reconnect未退化**：R4成功仍要求独立`ordinary` allow candidate、真实Host manifest、第二次accepted truth binding、
   Run/Attempt/execution/dispatch equality及proactive operation count=2。若R4 soft-trigger第三operation，只能bounded收口后seal为
   `needs-more-evidence`/failed，不能借pre-compact proof projection或R3 post-compact ref冒充reconnect。
7. **Corruption coverage**：stage variants为`additionalProperties=false`封闭union；self-test明确拒绝为pre-compact compact trigger
   伪造manifest/sizing snapshot/non-null Attempt/execution，拒绝ordinary budget/logical ref与post-compact真实manifest/candidate ref互换，
   拒绝pre-compact digest冒充accepted consumption，以及R4 compact/第三operation冒充成功reconnect。
8. **双port proof与Host ledger**：`dayu/host/api.py:837`的同一`HostLocalExecutionOptions`确实同时拥有
   `worker_factory`和`context_compactor`，由`dayu/host/dispatch.py:1412`的`HostDispatchScheduler.open`消费；ordinary allow dispatch在
   `:4653`调用worker factory，proactive path在`:2269`调用compactor。plan把worker ledger收窄为allow-only manifests并明确排除
   pre-compact trigger；compactor ledger仍与proposal manifests/attempts/terminals闭合，两ledger disjoint且共享同durable store、
   transaction runner及production admission/service API truth。
9. **OS sandbox与parent audit**：冻结`/usr/bin/sandbox-exec` SHA
   `8290e4be7387a0df83cd1559e86afd880464f269450573d012795761fe298f16`与本机bytes一致。OS deny在任何Dayu import前覆盖process tree；
   OS-only parent/spawned-child numeric TCP/UDP只接受`EPERM`与no-contact。parent audit只记录parent AF_INET/AF_INET6/DNS/sendto attempts，
   每次先ledger后typed exception，三个resolver probe在resolver前fail closed；child无hook、allowlist或target/argv/PID/pipe/pickle推断。
10. **Formal、storage、deadline/publication未退化**：formal仍是production CLI → production assembly/default worker →
    `open_host`构造production compactor，只做segment后canonical evidence acceptance，无wrapper/interception。Fins材料仍只经
    `ProcessedDocumentRepositoryProtocol`/`DocumentBlobRepositoryProtocol`与production Fs实现，并在fresh seed重算；Host tool
    request/result/canonical terminal与storage identity fail closed。99/73/73与245 cap、global/allocation/active deadline typed owners、
    attempted/provider-not-started封闭union、terminal双向穷尽、Tool Trace、digest、secret-scan last-writer及zero-write-after均保持完整。
11. **Architecture/optimality/overengineering/overcoupling**：RR5修订删除了错误的durable事实要求，没有新增替代manifest或产品投影；
    proof-only projection被限制在external proof owner，formal只使用真实Host canonical facts。三片仍按proof、bounded observation、publication
    分割，是binding goal内最小、可审计且不跨层补偿的方案。

## Findings

未发现 material finding。RR5 的错误manifest前提已按真实Host提交边界完整移除，同时allow-only manifest与identity gate未被削弱。

## Open questions

无 blocking open question。

未治理`intent_type`/`reason`缺少universal finite serialized upper-bound owner仍是已明确的residual owner question；当前F20只证明
固定合法有限canonical metadata下的caps场景存在性，不应把该residual扩大为本轮blocker或任意accepted output安全承诺。

## Residual risks and suggested tracking destination

- proof-only projection与canonical budget digest/ref若不能逐字段闭合，或实现误把它写入formal schema：Slice 1直接FAIL、provider start=0；
  归属F20 implementation/root-cause adjudication。
- 真实MiMo不按recipe调用、repair/fallback不自然出现、R4触发第三operation：按sealed observed-behavior report记录
  `needs-more-evidence`/failed，不重跑补证；归属F20 real-observation closeout。
- OS/profile/bootstrap、typed policy、Host tool/storage closure或fresh corpus identity漂移：Slice 1 fail closed；归属F20 implementation gate。
- deadline/publication schema、terminal complement、digest、secret scan或last-writer失败：保持publication FAIL，只能以新publication identity
  重做且不得追加provider；归属F20 publication adjudication。
- universal finite upper-bound owner问题：若未来需要承诺，应进入独立goal confirmation，不得在F20 external tooling补偿。

## Final plan review conclusion

`PASS`。

当前冻结RR6 plan已达到code-generation-ready：attempt-free pre-compact trigger只消费Host真实存在的canonical budget/request/terminal；
proof-only complete candidate projection与Host/formal manifest语义隔离；post-compact和R4 allow仍由真实manifest及dispatch identities证明。
双port proof、OS deny/parent audit、formal stock CLI、storage、deadline/publication均无退化。只有同一revision的MiMo/DS latest review均为
`PASS`后，Controller才可接受plan并进入Slice 1；本review本身不授权实现或provider。
