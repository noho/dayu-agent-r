# Code Review

## Scope

- Mode: current changes
- Branch: `feat/wu-ctx-01`
- Base: `126e67ca` (accepted Slice 2 protected commit)
- Output file: `docs/reviews/code-review-20260724-071249.md`
- Included scope: 全部 tracked 与 untracked Slice 3 文件
  - 新增：`dayu/host/context_anchor.py`、`tests/host/test_context_anchor.py`
  - 修改 production：`_runner_call_manifest.py`、`admission.py`、`context_budget.py`、`context_events.py`、`dispatch.py`、`engine_ingest.py`、`recovery.py`、`run_input.py`、`waiting.py`
  - 修改 tests：`test_context_budget.py`、`test_context_budget_evaluated.py`、`test_dispatch_scheduler.py`、`test_engine_ingest_mapping.py`、`test_public_resolve_wait_resume.py`、`test_public_steer.py`、`test_recovery_scan.py`
  - 修改 docs：`dayu/host/README.md`、`tests/README.md`
- Excluded scope: `docs/host/issues-implementation-control.md`（Controller-owned）
- Parallel review coverage: 无

## Findings

未发现实质性问题。

以下是对各审查维度的逐项 adversarial pass 结论，全部基于直接代码/数据证据。

### 1. CONTEXT_BUDGET_EVALUATED fact 与 adaptive estimator 独立性

**结论：通过。** `ContextAnchorDiagnostic` 从 `context_events.py` 迁移到 `context_budget.py`（`context_budget.py:172`），成为 sizing owner 的 typed 内部类型。`context_events.py` 的 `_anchor_diagnostic_payload` helper（`context_events.py:779`）只做序列化，不拥有 diagnostic 语义。fact builder `build_context_budget_evaluated_payload`（`context_events.py:264-271`）从同一个 `ContextSizingResult` 读取 `fallback_reason` 与 `anchor_diagnostic`，不重新计算。

### 2. Resolver 同一 transaction snapshot、reverse keyset 无任意总 cap、latest accepted compact boundary

**结论：通过。**
- `resolve_context_anchor`（`context_anchor.py:369`）校验 `transaction` 为 `HostTransaction` 实例，不自开 transaction。
- `_read_anchor_rows`（`context_anchor.py:442`）使用固定 `_SCAN_PAGE_SIZE=64` 的 keyset pagination，无任意总条数上限。
- compact boundary 检测：`context_anchor.py:503` — 遇到 `CONTEXT_COMPACTED` 立即 `return (tuple(collected), True)`。
- `resolve_context_anchor` 最终判断（`context_anchor.py:435-439`）：有 boundary 时 `ACCEPTED_COMPACT_INVALIDATED`，无 boundary 时 `USAGE_MISSING`。
- 索引 `INDEX_EVENT_LOG_SESSION_SEQUENCE(session_id, event_sequence)` 覆盖查询条件。

### 3. Manifest + unique accepted link + strict paired usage + unique accepted completion 全 conjunction 与 event ordering

**结论：通过。**
- `_call_item`（`context_anchor.py:641`）按 conjunction 逐项校验：
  - link 数量：0 → `ITERATION_LINK_MISSING`；>1 → `ITERATION_LINK_INVALID`（`context_anchor.py:666-685`）
  - ambiguous link identity：`context_anchor.py:688-699`
  - link-manifest matching：`context_anchor.py:700-711`（`_link_matches_manifest` 校验全部 identity fields）
  - completion 数量：0 → `ITERATION_INCOMPLETE`；>1 → `ITERATION_COMPLETION_AMBIGUOUS`（`context_anchor.py:738-749`）
  - finish reason eligibility：`context_anchor.py:751-757`
  - event ordering `manifest < link < usage < completion`：`context_anchor.py:758-762`（completion sequence > link sequence）；`context_anchor.py:781-788`（usage sequence between link and completion）
  - usage 数量：>1 → `USAGE_AMBIGUOUS`（`context_anchor.py:763-767`）
  - usage strict parse 与 pairing 验证：`_parse_usage_evidence`（`context_anchor.py:954`）校验 identity、policy_ref、estimator_digest、estimated_input_tokens、observation_digest、pairing nested object 全部精确匹配。

### 4. Missing usage 可跨越但 invalid/ambiguous/incomplete/lineage gap 必须 barrier

**结论：通过。**
- `_call_item` 中 `usage=None` 时返回 `_CallEvidence`（`context_anchor.py:789-795`），不产生 barrier。
- `resolve_context_anchor`（`context_anchor.py:410-411`）对 `item.usage is None` 的 call 执行 `continue`，跳过该 call 继续扫描更旧 anchor。
- invalid/ambiguous/incomplete 全部在 `_call_item` 中产生 `_Barrier`，`resolve_context_anchor` 遇到 barrier 立即返回 fallback（`context_anchor.py:405-406`）。
- 测试 `test_newer_lineage_barrier_never_falls_through_to_old_anchor` 覆盖 8 种 mutation。

### 5. Terminal Run 不能补 completion

**结论：通过。** resolver 只接受 `ITERATION_COMPLETED` preview event（`context_anchor.py:560-571`），不接受 `RUN_SUCCEEDED`/`RUN_FAILED`/`RUN_CANCELLED`/`RUN_LOST`。`_parse_completion_evidence`（`context_anchor.py:910`）校验 `event_class=PREVIEW`、`engine_event_type=iteration_completed`、`finish_reason ∈ {stop, length, tool_calls}`。

### 6. 全部 compat 维度

**结论：通过。** `_compatibility_mismatch`（`context_anchor.py:1079`）校验：
- `validation_status=complete`、非 compactor identity、eligible kind（`context_anchor.py:1092-1098`）
- `sizing_snapshot.status=complete`（`context_anchor.py:1099-1100`）
- provider、model、context_window_size、estimator_id/version、request_semantics_digest 全部精确匹配（`context_anchor.py:1101-1114`）
- 测试 `test_resolver_rejects_each_compatibility_dimension` 覆盖 5 维反例。

### 7. Usage pairing 来自 durable strict projection 而非 raw 猜测

**结论：通过。** `_parse_usage_evidence`（`context_anchor.py:954`）从 durable `USAGE_REPORTED` projection signal 严格解析，校验 identity、pairing nested object 的 8 个字段（manifest_event_id、manifest_payload_ref、manifest_digest、iteration_link_event_id、input_snapshot_digest、observation_digest）全部精确匹配，observation_digest 通过 `build_usage_observation_diagnostic` 重算验证（`context_anchor.py:1032-1041`）。

### 8. Signed delta 含负值不 clamp、range invalid 精确 fallback 完整 E_current 且 Run 不失败

**结论：通过。**
- 公式（`context_budget.py:994-998`）：`signed_delta = conservative_input_tokens - anchor.conservative_anchor_tokens`，`anchored_prediction = anchor.usage_anchor_tokens + signed_delta`。无 clamp。
- range check（`context_budget.py:999-1008`）：`abs(signed_delta) > MAX_CONTEXT_TOKEN_COUNT` 或 `anchored_prediction > MAX_CONTEXT_TOKEN_COUNT` → `ARITHMETIC_RANGE_INVALID`；`anchored_prediction <= 0` → `PREDICTION_NON_POSITIVE`。
- fallback 时 `predicted = conservative_input_tokens`（`context_budget.py:971`），即完整 `E_current`。
- `ContextAnchorDiagnostic.__post_init__`（`context_budget.py:228-245`）独立复核公式一致性。

### 9. Provider no-usage 与 supports flag 语义

**结论：通过。**
- resolver 不读取 `supports_stream_usage`；它只检查 durable `USAGE_REPORTED` fact 是否存在且 strict valid。
- `request_semantics_digest` 包含 `supports_stream_usage` 值，作为 compatibility 维度之一（`context_anchor.py:1113-1114`）。
- 测试 `test_actual_usage_presence_does_not_depend_on_supports_flag`（supports=false + actual usage → anchor）和 `test_supports_flag_does_not_invent_missing_usage`（supports=true + no usage → fallback）覆盖两个边界。

### 10. Accepted compact immediate 强制 fallback

**结论：通过。**
- `dispatch.py:_build_candidate_sizing_result`（`dispatch.py:774-790`）：`POST_COMPACT` 和 `REACTIVE_POST_COMPACT` stage 固定使用 `build_conservative_context_sizing_result` + `ACCEPTED_COMPACT_INVALIDATED` fallback reason，不调用 anchor resolver。
- `engine_ingest.py:_StartReactiveRecoveryOperation`（`engine_ingest.py:813-830`）：`REACTIVE_POST_COMPACT` 同样固定 conservative fallback。

### 11. Ordinary/fallback/steer/wait/Engine continuation 同事务接线

**结论：通过。**
- ordinary dispatch：`dispatch.py:1917-1925` — 在 candidate 所属 transaction 中调用 `_build_candidate_sizing_result`。
- steer：`admission.py:3125-3152` — manifest append 后在同一 transaction 解析 anchor。
- wait resume：`waiting.py:1340-1355` — resolution transaction 中解析 anchor。
- Engine continuation：`engine_ingest.py:3813-3847` — manifest 写入后以 `manifest_event_sequence - 1` 为 cursor 解析。
- startup replay：`recovery.py:1093-1120` — 不调用 resolver，直接 `build_frozen_context_sizing_result_from_atoms` 复用 source atoms。

### 12. Startup replay 冻结 source atoms 且新 fact identity

**结论：通过。** `recovery.py:_startup_continuation_budget_result`（`recovery.py:1093`）使用 `build_frozen_context_sizing_result_from_atoms`，保留 source `estimate_method`、`predicted_input_tokens`、`anchor_diagnostic`、`fallback_reason`，只重绑定 stage 为 `CONTINUATION`。`recovery.py` 的 `rebind_frozen_context_sizing_result` helper（`context_budget.py:1244`）从 source sizing 派生新 cursor/identity。

### 13. Reactive/continuation hard 仍 allow

**结论：通过。** `_pressure_and_decision` 函数在 `context_budget.py` 中实现 15-cell action matrix。`REACTIVE_POST_COMPACT` 和 `CONTINUATION` 的 hard pressure 均为 `ALLOW_DISPATCH`，不触发 terminal failure。

### 14. Public 七字段无 anchor 泄露

**结论：通过。** `context_events.py:build_context_budget_evaluated_payload`（`context_events.py:264`）序列化 `fallback_reason`（nullable）和 `anchor_diagnostic`（nullable nested object）到 canonical fact。Host public projector 和 Service DTO 只消费七字段（predicted_input_tokens、context_window_size、utilization_basis_points、pressure_level、budget_decision、policy_ref、policy_snapshot_digest），不读取 anchor refs、usage、delta、stage 或 policy internal。README 更新确认此分离。

### 15. 语义所有权边界

**结论：通过。**
- `context_anchor.py`：Host-private durable anchor 读取与 compatibility 判定唯一 owner。
- `context_budget.py`：公式、range validation、diagnostic typed type、sizing result 唯一 owner。
- `run_input.py:resolve_prepared_runner_call_context_anchor_in_transaction`：从 frozen candidate 构造 query 的唯一 projection helper。
- `context_events.py`：canonical fact schema 与序列化 owner。
- 各 lifecycle producer（dispatch/engine_ingest/admission/waiting/recovery）：各自 transaction-local integration owner。
- `ContextSizingFallbackReason` 定义在 `context_budget.py`，被 `context_anchor.py` 导入使用——这是共享 typed enum，不是语义漂移。

### 16. 严格类型与 docstring

**结论：通过。** 全部新增 dataclass 使用 `frozen=True, slots=True`。所有函数提供完整中文 docstring，包含参数、返回值、异常。pyright `0 errors, 0 warnings, 0 informations`。无 `object`/`Any`/无类型参数。

## Open Questions

无。

## Residual Risk

- `context_anchor.py` branch coverage 为 82%，接近但超过 80% 阈值。部分异常路径（如 `_parse_manifest_evidence` 中 hot descriptor mismatch、EventLog scope mismatch）的测试覆盖依赖集成测试而非直接 unit test。
- `build_frozen_context_sizing_result_from_atoms` 不重新验证 `ContextAnchorDiagnostic` 内部公式一致性（信任 source fact）。这是正确的设计选择（source fact 已被接受时已验证），但若未来 source fact 写入路径有 bug，replay 不会捕获。
- `_read_anchor_rows` 的 SQL 使用 `event_type IN (...)` 过滤，配合 `(session_id, event_sequence)` 索引。在极端长 session（数十万 events）下，每次 anchor scan 最多读 `_SCAN_PAGE_SIZE * N` 行；实际性能取决于 session 历史长度和 compact 频率。当前无性能问题证据。
