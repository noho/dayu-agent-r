# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `d97d233b`
- Output file: `docs/reviews/code-review-20260817-012504.md`
- Included scope: `d97d233b` 后全部 workspace diff，含 staged/unstaged changes 与 branch committed changes；AGENTS.md、accepted plan (`docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md`)、S1 implementation artifact (`docs/gateflow/uf-fix10-s1-implementation-20260816.md`)、`docs/host/design.md`、`docs/engine/design.md`
- Excluded scope: generated/vendor/build/cache；S2 lifecycle wiring；SEC/CN/HK workflow changes
- Parallel review coverage: 无

## Findings

未发现实质性问题。

以下为审查过程中确认的关键验证点与非阻塞观察。

### 已验证的 S1 contract 关键点

1. **S1 behavior-preserving 严格成立**：`filing_upload_publication.py` 只包含无 I/O 的 closed arbitration types/helpers，无 `execute_prepared_filing_publication()`，无 batch lifecycle。`_PreparedFilingAssetMutation` 未接入 `prepare_upload()`。现有 filing early-skip 路径完全不变。SEC/CN/HK workflow 零修改。无 S2 workflow activation。

2. **Publication identity contract 健壮**：`FilingUploadPublicationIdentity`（`repository_protocols.py`）frozen + slots，path-free assets，sorted by name，严格 `__post_init__` 校验 ticker canonical、SHA-256 format、exact asset role constraints（original 不得有 `derived_from`、docling 必须有）、primary 必须命中 docling 且 `derived_from` 指向 primary original、companions 精确分割其余 originals。`is_deleted=True` 拒绝形成 identity。

3. **Staging meta 九字段唯一 owner 确认**：`_build_upsert_meta()` 从实例方法提升为 `docling_upload_service.py` 模块级私有 helper。旧实例方法已删除。`prepare_upload` 调用路径与 `rebase_prepared_filing_create_overwrite()` 均委托该唯一 owner。无复制字段逻辑。

4. **Batch fresh read 单视图**：`read_filing_upload_state_in_batch()` 通过 `_resolve_active_batch()` 验证 token open/同 core/ticker match，只读 `staging_ticker_dir`。共享 `_read_filing_upload_state_from_ticker_dir_unguarded()` 确保 company meta 与 source state 来自同一次 writer-owned 稳定 view。不读 published path。

5. **Protocol fake conformance 完整**：三个 structural fake（`_FixedFilingUploadStateRepository`、runtime `_ForbiddenFilingUploadStateRepository`、tools `_ForbiddenFilingUploadStateRepository`）均实现 `read_filing_upload_state_in_batch()`，独立 `batch_calls` 记录，forbidden fake 正确 record-then-`AssertionError`。无 cast/default/getattr/兼容逃逸。

6. **Typed conflict owner 路径自由**：`SOURCE_PUBLICATION_CONFLICT` factory 产生固定文案、有界（≤240 字符）、无 path/ticker/document/revision/fingerprint/异常文本。与 `SOURCE_REVISION_STALE` 语义分离。

7. **Arbitration 纯函数覆盖封闭表**：`arbitrate_filing_upload_publication()` 覆盖 stable MISSING/COMPLETE/REPAIR_REQUIRED → PUBLISH、MISSING→COMPLETE auto+no-overwrite exact equality → SKIP、MISSING→COMPLETE create+overwrite → REBASE_CREATE_OVERWRITE → PUBLISH、MISSING→COMPLETE other → CONFLICT、REPAIR_REQUIRED changed → SOURCE_REVISION_STALE、其它 changed → SOURCE_PUBLICATION_CONFLICT。UNSAFE 在入口拒绝。

8. **`_build_upsert_meta` 提升无语义变化**：新模块级 helper 签名为 `_build_upsert_meta(*, previous_meta, source_fingerprint, document_version, base_meta) -> JsonObject`，九字段逻辑与原实例方法逐行一致。`first_ingested_at`/`created_at` 从 `previous_meta` 保持，`_published_source_revision` 不进入 staging meta（由 storage source-document owner 在 commit 时覆盖）。

9. **`rebase_prepared_filing_create_overwrite` 职责分离**：只接受 initial MISSING + create + overwrite + NoExistingSourceRepair。以 fresh source meta 为 `previous_meta`，复用 prepared bytes/fingerprint，`_resolve_document_version()` 计算新 version，`_build_upsert_meta()` 重建 meta。`dataclasses.replace()` 只更新 `previous_meta`/`meta`/`document_version`，保持 `pending_assets`/`initial_skip_disposition`/`conversion_events`。

10. **Tests 证明 owner contract**：
    - `test_filing_upload_publication.py`：publication identity exact equality（path-free、sorted、role constraints）、arbitration closed table（stable/convergence/rebase/conflict）、retransmission/repair preservation、input contract rejection。
    - `test_docling_upload_service.py`：prepared filing helpers roles/skip、reject base/delete/material/result、meta owner shared by prepare/rebase、rebase revision ownership。
    - `test_fins_storage_atomicity.py`：batch reader single staging view、durable identity reuse、invalid capability rejection、no mutation。
    - `test_fins_ingestion_runtime.py`/`test_fins_ingestion_tools.py`：protocol fake conformance、`batch_calls == []` 零行为断言。

11. **`_FsCompanyMetaCore` refactoring 正确**：`_read_company_meta_from_ticker_dir_unguarded` 接受 caller-owned ticker root，委托 `_read_published_company_identity` 做 strict descriptor/meta/identity 解析。现有 `read_company_meta()` 改为先定位 published root、再委托该 helper。batch reader 只传 `staging_ticker_dir`。`FileNotFoundError` 边界正确转移为 `CompanyTickerIdentityCorruptionError`（目录存在但缺少 descriptor）或 `None`（ticker root 合法但无 company meta）。

12. **`_project_filing_upload_publication_identity` 防御性投影**：从同一次 trusted inspection 投影，只在 filing + COMPLETE + user-upload + 所有 identity 字段完整时非 None。异常（TypeError/ValueError）静默返回 None。不从 manifest 字符串或目录名补值。

## Open Questions

- 无。

## Residual Risk

1. **未覆盖的 arbitration 状态转换**：`COMPLETE → MISSING`、`COMPLETE → REPAIR_REQUIRED`、`MISSING → REPAIR_REQUIRED` 三个 changed-observation 分支无直接测试。代码通过 catch-all `conflict_decision(fins_upload_source_publication_conflict_failure())` 正确处理，但缺少 dedicated test 证明这些边界。风险低，不影响 correctness。

2. **`test_fins_service_runtime.py` 动态 monkeypatch fake**：按 Controller acceptance 维持未来触发式、非阻塞 residual。本 work unit 无 batch-read 路径经过该 fake。只有未来 work unit 使其 prevalidation 路径消费 batch read 或扩展 required protocol 时，才需同步 conform。

3. **`cast()` 使用**：`_project_filing_upload_publication_identity` 在显式类型检查后使用 `cast(str, ...)` / `cast(int, ...)` / `cast(bool, ...)`。运行时安全，但类型检查器无法追踪。这是 Python 类型系统的已知限制，不影响 correctness。

4. **Coverage 数据**：implementation artifact 报告四模块合计 82%（≥80% 目标）。逐文件：arbitration 86%、Docling 88%、storage batch-state core 95%、typed failure 84%。未独立验证，依赖 implementation artifact 声明。
