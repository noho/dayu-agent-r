# Code Review

## Scope

- Mode: current changes
- Branch or PR: `codex/prompt-oracle-adjudication`
- Base: `3c892188`（WU-CLI-PROMPT-01 S3 accepted commit）
- Output file: `docs/reviews/code-review-20260731-185612.md`
- Decision: `pass`
- Included scope: `dayu/cli/arg_parsing.py`、`dayu/cli/main.py`、`dayu/cli/errors.py`、`tests/cli/test_arg_parsing.py`、`docs/reviews/wu-cli-prompt-01-s4-implementation-controller.md`
- Excluded scope: S1-S3 已接受实现、尚未开始的 S5-S6、Host/Service/Engine 与 frozen oracle/scenario；仅沿 S4 真实入口读取必要调用链，不把其它 slice 纳入裁决。
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- 本 slice 的 owner tests 与真实 raw-bytes subprocess 已证明 exit、可编码 diagnostic、日志文件与 workspace 零副作用；按 accepted plan，预置 workspace 的 Host/runtime SQLite、EventLog、Tool Trace before/after 仍需在 final gate 重放 `prompt.PC-BD-03-invalid-utf8-positional` 与 `prompt.P62-log-parent-missing` 后形成 frozen-scenario evidence。
- 本次执行的是 S4 定向 pyright；完整仓库 pyright 仍由 final gate 负责。该项是 gate sequencing，不是当前代码 finding。

## Evidence

### Contract 与 owner 对齐

- `dayu/cli/arg_parsing.py:228-264` 在 `parse_cli_args` 内先物化有效 argv，并在 `parser.parse_args` 前逐 token 执行 strict UTF-8 encode；异常路径只把静态 `INVALID_UTF8_INVOCATION_DIAGNOSTIC` 交给 `parser.error`，没有 token、raw bytes、`repr`、底层异常文本、replace/ignore 或下游补偿。
- `dayu/cli/main.py:88-121` 的真实入口先配置严格 UTF-8 标准流，再解析 argv；parser 的 `SystemExit(2)` 在日志文件打开、runtime logging 和 command runner 之前收敛。因此 surrogateescape 文本没有进入 Service、Host、Engine 或 provider 的参数/执行路径。
- `dayu/cli/errors.py:11-16` 把 usage 与 resource-preparation 定义成互不继承的 typed error；`dayu/cli/main.py:99-107` 仅在显式日志目的地边界分别映射为 2/1，没有 blanket exception remap。
- `dayu/cli/main.py:168-185` 对空白 path 抛 `CliUsageError`，对 `open(..., mode="a", encoding="utf-8")` 的 `OSError` 抛 `CliResourcePreparationError`。该路径没有 `mkdir`，父目录不存在时不会创建父目录或目标文件。

### Adversarial failure pass

- command、option value、positional 三种 surrogate 位置都在同一 argv validation loop 中命中，不依赖 argparse 分支顺序。
- raw `0xff` subprocess 同时携带可创建的 `--log-file` 目标；测试断言目标文件与 workspace 均不存在，能发现“先准备资源、后拒绝 argv”的错误排序。
- missing-parent 路径使用真实 `open` 失败，并把 runtime logging 配置与 runner 设为 fail-on-call；exit 1、可行动 path/OS reason、父目录/目标不存在及两个 zero-call contract 同时成立。
- blank path 独立保持 exit 2；已有父目录下 create/append 的既有测试继续覆盖成功路径，未把所有日志打开错误粗暴改成同一分支。

### Semantic ownership drift / architecture pass

- UTF-8 校验停留在 CLI invocation parser owner；没有向 Host codec、provider adapter 或文件编码扩散。
- 日志目的地分类停留在 CLI resource owner，runtime logger 只在资源成功打开后接收 stream；没有让 runner、Host 或测试 fixture 推导错误类别。
- 没有 `getattr`/`hasattr`、默认 fallback、兼容 shim、字符串黑名单、SQLite 补写或目录创建补偿。

### Tests 与静态验证

- `pytest -q tests/cli/test_arg_parsing.py -k 'utf8 or invalid_utf8 or log_file_parent or log_file_is_empty'`：`8 passed`。
- `pytest -q tests/cli/test_arg_parsing.py tests/cli/test_prompt_command.py --cov=dayu.cli.arg_parsing --cov=dayu.cli.main --cov=dayu.cli.errors --cov-report=term-missing`：`153 passed`；`arg_parsing.py 100%`、`errors.py 100%`、`main.py 94%`。
- `pyright dayu/cli/arg_parsing.py dayu/cli/main.py dayu/cli/errors.py tests/cli/test_arg_parsing.py`：`0 errors, 0 warnings, 0 informations`。
- `git diff --check`：通过。

## Gate Conclusion

S4 implementation review `pass`。UTF-8 invocation rejection 与日志目的地错误分类均位于接受计划指定的唯一 owner boundary，退出码、错误输出、文件副作用和 primary-operation zero-call contract 有直接代码与测试证据；可进入 accepted slice commit。
