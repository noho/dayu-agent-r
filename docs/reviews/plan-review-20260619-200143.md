# WU-CM-13 Final Focused Plan Re-Review — N1-N4 Closure Verification

## Reviewed Target

- **Plan**: `docs/host/host-issues/wu-cm-13-unified-compact-pipeline-plan.md`（final revision）
- **Previous re-review**: `docs/reviews/plan-review-20260619-195521.md`（pass）
- **Review type**: final focused re-review — 只核对 N1-N4 是否关闭
- **Review date**: 2026-06-19

## N1-N4 Closure Verification

### N1: `compact_pipeline_source_snapshot_from_pre_dispatch_view(...)` 签名与 digest/source refs 计算

**状态：已关闭**

§5.2 行 230-246：

- **签名**（行 230-235）：`def compact_pipeline_source_snapshot_from_pre_dispatch_view(*, trigger_source: ContextCompactionTriggerSource, run: RunRow, material_view: PreDispatchCompactMaterialView) -> CompactPipelineSourceSnapshot`
- **字段来源映射**（行 238-246）：明确列出每个字段来自 `run` 还是 `material_view`
- **digest 计算**（行 243）：`material_view_digest` 使用 `selected_material_view_digest(material_view.material_blocks)` — 代码确认该函数存在于 `compact_material.py:573`，返回 `str`
- **source refs 计算**（行 244）：`material_source_refs` 使用 `selected_material_source_refs(material_blocks=..., selected_block_ids=...)` — 代码确认该函数存在于 `compact_material.py:553`
- **校验**（行 246）：`run.input_event_sequence != material_view.source_boundary.current_input_event_sequence` 时抛 typed validation error

### N2: `selection_policy_digest` 命名与 `MemoryProjectionPolicy` digest 来源一致性

**状态：已关闭**

- **参数重命名**（行 254）：`selection_policy_digest: str`（原 `policy_digest`）
- **文档说明**（行 262）：明确 "必须由同一个 `MemoryProjectionPolicy` 调 `digest_memory_projection_policy(memory_policy)` 得到；它不是 `BudgetEstimate.estimator_digest`，也不是 `source_snapshot.material_view_digest`"
- **Proactive 伪代码**（行 372）：`selection_policy_digest = digest_memory_projection_policy(memory_policy)`
- **Reactive 伪代码**（行 405）：`selection_policy_digest = digest_memory_projection_policy(memory_policy)`
- **代码验证**：`digest_memory_projection_policy` 存在于 `memory.py:1059`，返回 `MemoryPolicyDigest`（`TypeAlias = str`，`memory.py:27`）

两处 pseudocode 使用同一函数、同一 `memory_policy` 来源，一致。

### N3: `CompactPipelineFallbackSelectedMaterialHandoff` production 消费路径

**状态：已关闭**

- **类型定义**（行 189-220）：`CompactPipelineFallbackSelectedMaterialHandoff` 含 `selected_block_ids`、`material_blocks`、`source_refs`、`fallback_input_digest`、`selected_material_view_digest`、`selected_recent_window_turn_floor`、`selected_raw_turn_count`
- **嵌入 fallback decision input**（行 207）：`CompactPipelineFallbackDecisionInput.fallback_handoff: CompactPipelineFallbackSelectedMaterialHandoff | None`
- **语义说明**（行 199）：只服务 production fallback branch 的 selected material 校验/渲染，由 `build_fallback_decision_input(...)` 构造
- **Proactive 伪代码**（行 389-391）：`fallback_handoff = fallback_input.fallback_handoff` → "dispatch.py uses fallback_handoff only to validate/render selected fallback material"
- **Reactive 伪代码**（行 431-432）：同上
- **§6.4 显式声明**（行 470）："`CompactPipelineFallbackSelectedMaterialHandoff` 是 production fallback decision input 的一部分，不是 test-only type"

### N4: `build_tier_recovery_request_plans` 使用 `build_compact_material_pack(...)` 返回完整 `CompactionRequest`

**状态：已关闭**

- **签名**（行 267-272）：`def build_tier_recovery_request_plans(*, source_snapshot, root_request_plan, memory_policy) -> tuple[CompactPipelineRecoveryRequestPlan, ...]`
- **文档说明**（行 275）：明确 "每个 tier 必须调用/复用 `build_compact_material_pack(...)` 构造完整 `CompactionRequest.material_pack`，再返回含完整 `CompactionRequest` 的 `CompactPipelineRequestPlan`"
- **代码验证**：`build_compact_material_pack` 存在于 `compact_material.py:1017`，已在 §5 allowed imports 中（行 98："可以 import compact_material.py"）

## New Blocker Check

无新 blocker。N1-N4 全部关闭，无遗留 open question。

## Final Plan Review Conclusion

**pass**

N1-N4 全部关闭。修订 plan 现在是 code-generation-ready：

| Item | Closure Evidence |
|---|---|
| N1 | §5.2 行 230-246：完整签名 + 字段来源映射 + digest/source refs 计算函数 + validation error |
| N2 | §5.2 行 254/262 + §6.1 行 372 + §6.2 行 405：参数重命名 + 来源说明 + 两处 pseudocode 一致 |
| N3 | §5.1 行 199/207 + §6.1 行 389 + §6.2 行 431 + §6.4 行 470：production consumption path 明确 |
| N4 | §5.2 行 275：明确 "调用/复用 `build_compact_material_pack(...)` 构造完整 `CompactionRequest.material_pack`" |
