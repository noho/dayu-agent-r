# Code Review — 复审

## Scope

- Mode: focused re-review（单 finding 修复验证）
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: HEAD（uncommitted workspace changes only）
- Output file: `docs/reviews/code-review-20260618-185732.md`
- Reviewer identity: AgentDS
- Re-review source:
  - `docs/reviews/code-review-20260618-184822.md` Finding 1
  - `docs/reviews/code-review-20260618-185121.md` Finding 001
- Accepted finding: `test_evidence_chunks_share_same_durable_query_text` 仍断言旧默认 chunk label `E1.1/E1.2/E1.3`
- Fix under review: `docs/reviews/wu-cm-12-fix-r1-s1-fix-codex-20260618.md` 记录的 `tests/host/test_compaction_operation.py` 修改
- Included scope: `tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text`（diff vs HEAD）
- Excluded scope: 其余 uncommitted changes（Slice 1 production code 与 `test_compact_material.py`/`test_llm_compaction.py`，已在首轮 review 通过）

## Findings

### 原 Finding 状态：已修复

原 finding（编号 1 / 001）指向 `test_evidence_chunks_share_same_durable_query_text` 的 label 断言仍为 chunk 语义 `("E1.1", "E1.2", "E1.3")`，与 Slice 1 移除默认 evidence chunking 后的实际行为 `("E1",)` 不一致。

**修复验证 — 全部通过。**

- **入口/函数**: `test_evidence_chunks_share_same_durable_query_text`
- **文件(行号)**: `tests/host/test_compaction_operation.py:1632-1726`
- **直接证据**:
  1. **Docstring 迁移**: `"同一 durable request 被 chunk 后各 evidence chunk 复用同一 query_text。"` → `"长 evidence 默认不 chunk，单个 block 使用同一 durable request 的 query_text。"` — 语义已从 chunk 迁移到 no-default-chunk。
  2. **Label 断言**（line 1722-1724）:
     - `assert labels == ("E1",)` — 单一 `E1` label ✅
     - `assert not {"E1.1", "E1.2", "E1.3"}.intersection(labels)` — 排除旧 chunk 标签 ✅
     - `assert all("." not in label for label in labels)` — 无 chunk-style `.` 分隔符 ✅
  3. **Durable query_text 断言**（line 1725-1726）:
     - `assert len({query_text for _label, query_text in query_texts}) == 1` — 所有 block 共享同一 query_text ✅
     - `assert query_texts[0][1] == '工具参数: {"arguments":{"chapter":"MD&A","ticker":"MSFT"}}'` — query_text 来自 durable tool request arguments ✅
  4. **Session/event ID 重命名**: `session-selected-query-chunk` → `session-selected-query-no-chunk`，`event-tool-result-query-chunk` → `event-tool-result-query-no-chunk`，`selected-evidence-chunk` → `selected-evidence-no-chunk`，`input-query-chunk` → `input-query-no-chunk` — 消除误导性 `chunk` 字样。
  5. **Helper docstring 更新**: `"写入会被 chunk 的 selected evidence"` → `"写入长 selected evidence"`，`"读取 chunk labels 与 query_text"` → `"读取 evidence label 与 query_text"`。

### 验证结果

| 检查项 | 结果 |
|--------|------|
| `pytest tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text -q` | `1 passed in 0.26s` |
| `pytest tests/host/test_compact_material.py tests/host/test_llm_compaction.py tests/host/test_compaction_operation.py -q` | `127 passed in 0.51s` |
| `pyright tests/host/test_compaction_operation.py` | `0 errors, 0 warnings, 0 informations` |
| 生产代码修改 | 无 — `git diff` 确认修改仅限于 `tests/host/test_compaction_operation.py`，`dayu/host/compact_material.py` 和 `dayu/host/compaction.py` 的 uncommitted changes 为 Slice 1 原有变更，非本次 fix 引入 |

### 修复质量评估

- **修复与 finding 对齐**: 完全对齐。finding 要求的三个条件全部满足：docstring 反映 no-default-chunk 语义、label 断言为单一 `E1`、durable query_text 断言保留。
- **修复完整性**: 不仅修改了断言，还同步更新了 session/event id、helper docstring、block_id 等辅助标识，避免了"新旧语义混用"的残留混淆。
- **修复侵入性**: 低。仅修改测试文件，未触及生产代码。

## Open Questions

- 测试函数名 `test_evidence_chunks_share_same_durable_query_text` 仍包含 `chunks` 字样。Fix 文档已说明这是为保留 handoff 指定的 pytest node id 而有意保留。不阻塞 correct behavior，后续可单独重命名。
- `_ACCEPTED_TOOL_EVIDENCE_MATERIAL_LIMIT` 仍在 `dayu/host/run_input.py` 中 — 这属于 Slice 2 范围，不在本 fix scope。

## Residual Risk

- 单条超长 evidence 不再默认拆分后的 compactor input 预算风险仍由 Context Governance selection / fallback / fail-closed 处理，不属于本 code review fix 范围。
- 该测试函数名含 `chunks` 可能让后续读者困惑。建议在 WU-CM-13 或合适 WU 中重命名。

## Conclusion

**fixed**

原 finding（`test_evidence_chunks_share_same_durable_query_text` 断言旧 chunk label）已修复。测试 docstring、断言、辅助标识全部迁移到 Slice 1 的 no-default-chunk 语义：label 为单一 `E1`，无 `E1.1`/`E1.2`/`E1.3` 或 chunk-style 标签，durable query_text 断言完整保留。生产代码未被修改。验证通过（127 passed，0 pyright errors）。
