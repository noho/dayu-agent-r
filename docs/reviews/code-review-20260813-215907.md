# Code Review

## Scope

- Mode: current changes（UF-FIX03-S1 review-fix 后 re-review）
- Branch: `codex/upload-filing-oracle`（HEAD = `662c9ad4`，无未 merge 提交，全部 S1 changes 仍在 workspace 未提交）
- Base: `662c9ad4b234894e54c62b56368c7682a09f596e`（gateflow: accept plan for UF-FIX03）
- Output file: `docs/reviews/code-review-20260813-215907.md`
- 裁决与修复输入:
  - 裁决：`docs/gateflow/uf-fix03-s1-code-review-adjudication-20260813.md`（F1/F2 均 accepted，五条修复 gate）
  - 修复记录：`docs/gateflow/uf-fix03-s1-review-fix-20260813.md`
  - 原 review：`docs/reviews/code-review-20260813-213823.md`（PASS）、`docs/reviews/code-review-20260813-214459.md`（FAIL，F1/F2）
  - implementation artifact（已纠正版）：`docs/gateflow/uf-fix03-s1-implementation-20260813.md`
- Included scope: 全部 11 个未提交变更文件（5 生产 + 6 测试）+ 裁决、修复记录与两份原 review 全文；生产链沿
  `prepare_upload → publish/commit_prepared_upload_batch → SEC/CN filing/material stream → pipeline facade →
  FinsUploadPipelineResult → ProductionFinsUploadRunner → FinsUploadResultSummary → durable job record / direct RESULT`
  全程走读，并按裁决逐项复核对 F1/F2 的修复证据。
- Excluded scope: S2（empty/corrupt/label）、S3（CLI/docs/direct no-artifact）、UF-PF03、`dayu/host|engine|runtime|config|service|cli`、
  README、冻结 JSON 均按 plan 边界确认 no-touch（未修改，diff 无命中）。
- Parallel review coverage: 无（单人完整走读；无 subagent 分片）。
- Review 时间: 2026-08-13 21:59（本机时钟生成）

## 验证复跑记录（可执行证据）

| 检查项 | 命令/来源 | 结果 |
| --- | --- | --- |
| focused tests | `pytest -q tests/fins/test_docling_upload_service.py tests/fins/test_sec_pipeline_upload_filing_stream.py tests/fins/test_sec_pipeline_upload_material_stream.py tests/fins/test_cn_pipeline.py tests/fins/test_fins_ingestion_runtime.py tests/fins/test_fins_service_runtime.py` | `286 passed, 3 warnings in 4.92s`（warning 为 `edgar` 依赖弃用，非本 slice） |
| 九个关键回归点测试 | 逐名运行 service early-cancel、direct runner-unavailable、summary 拒绝矩阵、CN/SEC cancelled、commit failure、original-only store、AST inventory、material 语义回归 | `20 passed, 3 warnings in 1.03s`（含参数化展开） |
| pyright | `python -m pyright dayu/ tests/ utils/` | `0 errors, 0 warnings, 0 informations`（仅提示可升级 pyright 版本） |
| coverage | 修复记录同款 `coverage run`（6 个 S1 focused 文件）+ `coverage report -m` | 与修复记录逐文件一致：ingestion 2167/198/91%、cn_pipeline 413/130/69%、docling 398/51/87%、sec 145/7/95%、service 124/14/89%，合计 3247/400/88% |
| constructor inventory | `rg -n 'UploadOperationResult\(' dayu/fins --glob '*.py'` | 恰 4 点，全部位于 `docling_upload_service.py`（317/398/557/1419），均显式传 `stored_file_count` |
| summary inventory | `rg -n 'FinsUploadResultSummary\(' dayu/fins --glob '*.py'` | 恰 4 点：service_runtime 131/305、ingestion_runtime 3945/4423 |
| workflow 死 catch 残留 | `rg -n 'DoclingConversionCancelledError' dayu/fins/pipelines/sec_upload_workflow.py dayu/fins/pipelines/cn_pipeline.py` | 零命中（exit 1）；import 与 catch 均删除 |
| old-field audit | `rg -n '\buploaded_files\b' dayu tests --glob '*.py'` | 零命中（exit 1） |
| progress key | `rg -n '_PAYLOAD_FILE_COUNT\|"file_count"' dayu/fins/ingestion_runtime.py` | 249/7229/7617 保留；progress 测试断言新 count 键不出现 |
| whitespace | `git diff --check` | 通过 |
| 冻结 JSON | `shasum -a 256 docs/cli_ci_scenarios.json docs/cli_ci_oracles.json` | `a357e5a1…efb` / `88b04ca4…70b8`，与 plan §1 完全一致 |
| no-touch | `git diff 662c9ad4 --name-only` | 仅 5 生产 + 6 测试文件；Host/Engine/runtime/config/storage/Service/CLI/README/冻结 JSON 零改动 |
| 未创建 commit | `git rev-parse HEAD` | `662c9ad4`（= base），gateflow 纪律保持 |

## Findings

未发现实质性问题。

以下逐项说明两个 accepted finding 的闭合证据与裁决五点修复 gate 的逐条核对。

### F1 闭合：三个缺失 owner 行为场景

1. **service early-cancelled** — 新测试 `test_production_upload_runner_early_cancel_uses_request_count`
   （`tests/fins/test_fins_service_runtime.py:170`）通过 `DefaultFinsRuntime.create(...).get_ingestion_runtime()` 装配真实
   `ProductionFinsUploadRunner`，注入 `_AlwaysCancelledChecker`，断言 `status == "cancelled"`、
   `requested_file_count == len(request.files)`、`stored_file_count == 0`。该测试直达生产分支
   `service_runtime.py:130-137`（`if cancellation_checker():` 为 `run_upload` 首条判定），不是 AST 级 keyword audit。
   coverage 复跑确认 `131-137` 已退出 missing 集（原 review 记录的 131/134-135 已闭合）。
2. **direct runner-unavailable** — 新集成测试 `test_direct_upload_without_runner_reports_requested_and_zero_stored_counts`
   （`tests/fins/test_fins_ingestion_runtime.py:4958`）经 `_build_ingestion_runtime(...)` 默认
   `upload_runner=None`（helper 定义 7666-7701 直接透传 `None` 进 `FinsIngestionRuntime.create`）触达生产分支
   `ingestion_runtime.py:3940-3956`，断言 `FinsResultStatus.FAILURE`、`error_message == direct_upload_runtime_unavailable_message()`、
   details `requested files == "2"` / `stored files == "0"`。coverage 复跑确认 `3940-3956` 已退出 missing 集。
3. **skipped-zero-request 拒绝** — `test_upload_summary_count_owner_rejects_invalid_status_matrix` 新增
   `("skipped", 0, 0)` 用例，命中 `FinsUploadResultSummary.__post_init__` 中 skipped 分支的
   `requested_file_count < 1` raise（现 1312 行）；`("skipped", 1, 1)` 同时覆盖 skipped 分支的
   stored 非零 raise。coverage 复跑确认 1312 已退出 missing 集。

修复 gate 第 3 条（implementation artifact 表述纠正）亦已核对：`uf-fix03-s1-implementation-20260813.md:120-125`
明确承认 review 前 coverage 声明失真并列出纠正后的 missing 明细；`:156-158` 把未 commit 原因纠正为 Gateflow
review/fix/re-review 顺序约束，删除了“用户禁止 commit”的失真表述。

### F2 闭合：workflow 死 catch 删除与 cancellation ownership

- SEC/CN 两个 upload workflow 的 `except DoclingConversionCancelledError` 块、对应 import 与 inline
  `UploadOperationResult(...)` 构造均已删除；rg 双文件零命中。
- 不可达性论证经复核对成立：converter 唯一 raise 点 `docling_process_converter.py:401/509`，生产链路中 converter
  仅经 `docling_upload_service.py:738`（`_build_docling_assets`，位于 `_build_pending_assets` 内）调用，该调用整体被
  `docling_upload_service.py:336-342` 的 `except DoclingConversionCancelledError` 吸收并投影为
  `_build_cancelled_result`（1419，`stored_file_count=0`）。workflow try 块后续
  `stage_upload_company_meta_decision` / `commit_prepared_upload_batch` / `_store_upload_assets` / `commit_batch`
  均无该异常 raise 点；`DoclingConversionCancelledError` 继承自 `RuntimeError` 而非 `DoclingConversionError`
  （docling_process_converter.py:242），删除后即使未来拓扑变化落入 generic catch 也只会映射为 runtime 分类，
  不会静默吞掉取消语义。真实 cancellation owner 唯一，位于 `DoclingUploadService` 的 typed cancelled result 与
  precommit `_is_cancelled` 路径。
- 删除后的拓扑有双侧端到端行为测试锁定：SEC `test_upload_filing_observably_classifies_cancelled_docling_storage_and_generic`
  （cancelled 参数组：`status == "cancelled"`、`stored_file_count == 0`、`UPLOAD_COMPLETED`、无 failure、caplog 为空）与
  CN `test_upload_filing_conversion_cancelled_has_zero_stored_count`（cancelled + stored=0 + published tree 为空）。
- 附带核对：`cn_download_filing_workflow.py:314` 的 `except DoclingConversionCancelledError` 属 download 拓扑
  （307 行直接调用 `convert_to_json_bytes`，无 service 级吸收），catch 可达且为正确 owner，未删除是正确边界判断，
  不构成 ownership drift。
- constructor inventory 按裁决从六点收紧为四点：`UploadOperationResult(...)` 生产构造恰 4 点、全部位于
  `docling_upload_service.py` 且显式 `stored_file_count`；`FinsUploadResultSummary(...)` 恰 4 点、
  `FinsUploadPipelineResult` parser 唯一 `cls(...)` 一点。AST audit 测试
  `test_production_upload_count_constructors_are_explicit_and_complete` 同步断言 4+4+1，已通过。

### S1 回归复核（裁决外但属本 slice 契约）

- **计数/状态矩阵**: `FinsUploadPipelineResult.__post_init__`（ok→>=1，非 ok→==0，failed 必带 failure）与
  `FinsUploadResultSummary.__post_init__`（ok→requested>=1 且 stored==requested，skipped→requested>=1 且 stored==0，
  其余→stored==0）constructor 与 parser 双侧 accept/reject/bool/负数参数化测试全部通过；
  `_validate_required_non_negative_count` 与 `_required_upload_result_int` 均显式拒绝 bool。
- **commit 后消费**: `test_upload_filing_commit_failure_never_publishes_staged_count` 注入 OSError/RuntimeError 均得
  `failed/stored=0`，分类保持 `storage/storage_io` 与 `runtime/unexpected_runtime`，published tree SHA 为空，无 caller rollback。
- **material 仅机械迁移**: SEC/CN material 仅补 count 关键字；`test_upload_material_failure_preserves_existing_user_visible_semantics`
  （SEC+CN 双侧）通过并新增 stored=0 断言，generic 文案与 company-first publication 顺序未改。
- **durable/direct 同源**: 同一 `FinsUploadResultSummary` 投影 `to_json_summary()` 与 `_upload_result_details()`
  （6329 起，读 typed requested/stored 字段）；direct 测试断言 details 值为 `"1"/"1"` 且无 `uploaded files`。
- **progress/fingerprint 不变**: `_PAYLOAD_FILE_COUNT` 保留，progress 测试断言新 count 键不出现；frozen SHA 不变。

## Open Questions

无。F1/F2 的闭合证据均可在当前 workspace 直接复现，无阻碍 confident judgment 的问题。

## Residual Risk

1. **`cn_pipeline.py` 整文件 coverage 69%**：remaining missing（907-909、1309-1336、1381-1398、1428-1493、1514-1527、
   1553-1739）为既有、未修改的 conversion-error/download/facade/helper 路径，其中 907-909 为 CN `DoclingConversionError`
   catch（CN 测试注入 OSError/RuntimeError，绕开该 catch）。分类为 **assigned to later work unit**，与裁决
   “大型既有缺口作为明确 residual risk”的边界一致，不阻塞本 slice。
2. **`ingestion_runtime.py:1318/1320`**：`FinsUploadResultSummary` 的 failure_reason 矩阵两个 raise 行
   （failed 必带 / 非 failed 禁止 failure_reason）无 summary 级反向用例；已对照 base
   （`git show 662c9ad4:tests/...`）确认 base 同样无此类测试，属既有缺口且两行为非 S1 修改行（diff 中为 context），
   非本修复引入的回归。pipeline 级等价矩阵有测试覆盖。
3. **`ingestion_runtime.py:3938-3939`**：direct producer 入口取消分支体（`_emit_direct_cancelled_result` + return）
   未被覆盖——既有 direct 取消测试经 `_BarrierUploadRunner` 在 runner 内部屏障处取消，producer 入口 checker 恒为 False。
   两行为非 S1 修改的既有代码，缺口先于本 slice 存在，非回归。
4. 未运行 `tests/fins` 全量、CLI、service 与 UF-PF03 证据回归；本 review 与两份原 review 一致，只复跑 S1 focused 命令。
   S2/S3 范围按 plan 冻结未实现、未伪报。

## Verdict

**PASS** — 裁决的两项 accepted finding 均已在当前 workspace 真正闭合：F1 的三个缺失 owner 行为场景已有直达生产分支的
行为/集成测试且退出 coverage missing 集，implementation artifact 失真表述已纠正；F2 的 SEC/CN workflow 死 catch 已删除，
cancellation ownership 唯一收束于 `DoclingUploadService` 且双侧端到端测试锁定，constructor inventory 静态与 AST 双侧一致
为四点。五点修复 gate 全部通过。未发现新的实质性 finding；remaining missing 均为既有、未修改且已分类的 residual risk。
