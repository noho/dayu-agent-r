# WU-CM-13 Slice 1 — Deep Review

- **Reviewed scope**: Current workspace diff only (Slice 1: thin helper contracts + `compaction_evidence.py` cleanup)
- **Review type**: adversarial code review (deepreview)
- **Design sources**: `docs/host/design.md`, `docs/engine/design.md`
- **Control doc**: `docs/host/issues-implementation-control.md`
- **Accepted plan**: `docs/host/host-issues/wu-cm-13-unified-compact-pipeline-plan.md`
- **Plan adjudication**: `docs/reviews/plan-review-wu-cm-13-adjudication-20260619.md`
- **Timestamp**: 2026-06-19 21:12:29 CST

## 0. Scope Verification

| File | Change Type | Status |
|------|------------|--------|
| `dayu/host/compact_pipeline.py` | New (+1092 lines) | Slice 1 core delivery |
| `tests/host/test_compact_pipeline.py` | New (+581 lines) | Slice 1 focused tests |
| `dayu/host/compaction_evidence.py` | Deleted (-658 lines) | Evidence cleanup per plan §10 |
| `tests/host/test_compaction_operation.py` | Modified (-1564 lines) | Removed all evidence-related tests; removed `compaction_evidence` imports |
| `tests/host/test_compact_material.py` | Modified (+264 lines) | 4 migrated evidence tests + helper signature update |
| `tests/host/test_run_input_builder.py` | Modified (-1/+1) | Test rename: `compaction` → `compact` |
| `tests/README.md` | Modified (-1/+1) | Added `test_compact_pipeline.py` to test coverage listing |

**Scope matches plan Slice 1 exactly.** No dispatch/ingest/RunInput wiring (saved for Slice 2a/2b/2c per plan §12). No changed files outside allowed list (plan §4).

**Pre-review validation (user-reported):** `pytest tests/host/test_compact_pipeline.py tests/host/test_compact_material.py tests/host/test_compaction_operation.py -q` → 91 passed; `python -m pyright dayu/ tests/ utils/` → 0 errors; `git diff --check` clean; `rg compaction_evidence|SelectedEvidenceBlockRef|collect_selected_compaction_request_evidence_inputs dayu/ tests/` → no matches.

## 1. Accepted Plan Constraint Compliance

### 1.1 Thin helper owner (not lifecycle coordinator)

| Constraint | Evidence | Verdict |
|-----------|----------|---------|
| No `dispatch`/`engine_ingest` imports | `rg -n "dispatch\|engine_ingest" dayu/host/compact_pipeline.py` → no matches | ✅ |
| No EventLog writes | No `EventLogAppendRequest`, `event_log_store.append_event`, `HostTransaction` write calls | ✅ |
| No artifact writes | No `LocalArtifactStore`, artifact write calls | ✅ |
| No Run/Attempt state transitions | No `RunStatus`, `attempt_status`, `RUN_RECOVERING`, `RUN_STARTED`, state mutation | ✅ |
| No `compaction_operation` loop control | Does not import `run_compaction_operation`; returns plans only | ✅ |
| Allowed imports only | `compact_material`, `compact_payload`, `compaction`, `context_budget`, `context_fallback`, `context_policy`, `durable.codec`, `durable.errors`, `durable.state`, `memory`, `engine.contracts.messages` — all in plan §5 allowed list | ✅ |
| Forbidden imports absent | No `durable.state.transition`, `durable.event_log.write`, worker, lifecycle, or scheduler imports | ✅ |

### 1.2 No tier 5

| Check | Result |
|-------|--------|
| `rg "tier_5\|tier.5\|current.input.only\|fallback_tier" dayu/host/compact_pipeline.py` | No matches |
| `build_fallback_decision_input` action_hint | Only `FALLBACK_ACTION_DISPATCH` or `FALLBACK_ACTION_FAIL_CLOSED` |
| `CompactPipelineFailedPayloadInput` fields | No `fallback_tier` field |

✅ Plan §8 constraint satisfied.

### 1.3 Caller-owned lifecycle guards

`CompactPipelineSourceSnapshot` carries only neutral fields: `session_id`, `run_id`, `input_event_sequence`, `material_view_digest`, `material_source_refs`, `source_boundary`. No `expected_run_status`, `expected_attempt_status`, dispatch record status, or session active slot. Commit guard remains in `dispatch.py` / `engine_ingest.py`.

✅ Plan §7 constraint satisfied.

### 1.4 WU-CM-14 shared helper

- `select_ordinary_protected_raw_tail(...)` (line 703-746): Shared raw-tail selection eligibility that replaces the old RunInput-only logic.
- Uses `protected_recent_turn_group_ids_for_material_blocks(...)` and `is_turn_group_material_block(...)` from `compact_material.py` — same core selection as WU-CM-14.
- `_raw_tail_block_represented_by_memory(...)` (line 990-1012): Deduplicates against memory's `selected_recent_source_refs` and `selected_recent_content_digests` — replaces the RunInput-only `_DurableProtectedRecentRawTailProvider` dedup logic.
- `CompactPipelineProtectedRawTailProvider` Protocol (line 322-341): Defined for Slice 2c wiring; will replace `_ProtectedRecentRawTailProvider` in `run_input.py`.
- `_message_from_material_block(...)` (line 1015-1041): Renders USER_INPUT → `UserMessage`, ASSISTANT_FINAL_ANSWER → `AssistantMessage`, ACCEPTED_TOOL_EVIDENCE → `SystemMessage` with readable evidence content. Filters internal refs — no `tool_call_id`, event id, payload ref, digest, or cursor in output.

✅ Plan §9 constraint satisfied. Note: Slice 2c wiring (replacing `_DurableProtectedRecentRawTailProvider` in `run_input.py` to call this helper) is not in this diff; that's per plan.

### 1.5 compaction_evidence.py cleanup

| Plan §10 mapping | Implementation | Verdict |
|---|---|---|
| Read only selected refs | Deleted; covered by `test_compact_pipeline.py` request plan selected refs tests | ✅ |
| Descriptor raw payload (not preview) | `test_pre_dispatch_evidence_reads_descriptor_raw_payload` in `test_compact_material.py:1666` | ✅ |
| Semantic query from durable atom | Already in `test_compact_material.py:1658` (pre-existing `test_pre_dispatch_evidence_query_text_is_not_truncated`) | ✅ |
| Missing request atom → limited signal | `test_pre_dispatch_evidence_missing_request_atom_emits_limited_signal` in `test_compact_material.py:1765` | ✅ |
| Raw evidence missing/preview → fail closed | `test_pre_dispatch_evidence_payload_damage_fails_closed` (parametrized, 2 cases) in `test_compact_material.py:1808` | ✅ |
| No envelope → no evidence block | `test_pre_dispatch_tool_result_without_envelope_yields_no_evidence_block` in `test_compact_material.py:1710` | ✅ |
| Malformed envelope/missing raw/PEM → fail closed | Covered by `test_pre_dispatch_evidence_payload_damage_fails_closed` parametrize | ✅ |
| Malformed compacted payload → fail closed | Deleted; covered in `test_compaction_operation.py` operation-level tests (out of Slice 1 scope per plan "不扩大 operation owner") | ⚠️ See Finding 1 |
| Evidence id dedup | `test_compacted_payload_input_derives_semantic_refs` in `test_compact_pipeline.py:218` verifies accepted evidence mapping refs | ✅ |
| RUN_SUCCEEDED final answer → material | Already in `test_compact_material.py` existing pre-dispatch tests | ✅ |
| Stable derived fact refs | Covered by `test_compacted_payload_input_derives_semantic_refs` + `test_compaction_operation.py` operation-level tests | ✅ |

`rg` verification confirmed zero references to `compaction_evidence`, `SelectedEvidenceBlockRef`, `collect_selected_compaction_request_evidence_inputs`, or `CompactionRequestEvidenceInputs` in `dayu/` or `tests/`.

✅ Primary deletion and migration complete. See Finding 1 for one residual note.

## 2. Findings

### 001-未修复-低-compact_pipeline 行数超过计划 <500 行目标

- **位置**: `dayu/host/compact_pipeline.py` (1092 lines) vs plan §4 "目标小于 500 行"
- **问题类型**: 最佳实践偏离
- **当前代码**: 模块共 1092 行。其中约 500 行为 dataclass/Protocol 定义含完整中文 docstring、约 350 行为 public/private 函数含完整中文 docstring、约 60 行为 imports、约 30 行为 `__all__`。
- **反例/失败场景**: 行数超标主要是 plan §5.1/§5.2 要求的 10 个 dataclass + 4 个 Protocol + 17 个函数的完整中文 docstring 所致——属于 plan 自身要求的合规开销，不是过度设计。实际"有效逻辑"（去 docstring/imports/`__all__`）约 600 行。
- **为什么有问题**: 1092 行虽超目标，但模块行为严格保持在 thin helper 边界内（零 lifecycle import、零 state write、零 EventLog write）。这不是过度设计的信号，而是 493 行 docstring 的结果（项目要求完整中文 docstring）。
- **直接证据**: Line count 1092；`rg -c '"""' dayu/host/compact_pipeline.py` → ~200 lines of docstrings；`rg -c '@dataclass\|class.*Protocol' dayu/host/compact_pipeline.py` → 14 type definitions per plan spec。
- **影响**: 无。模块语义仍是 thin helper。
- **建议改法和验证点**: 不需要修改。若后续 review 要求收紧，可将 plan §4 的 "500 行" 改为 "~1100 行（含完整中文 docstring）"以消除偏差。
- **修复风险（低）**: 不修。
- **严重程度（低）**: 不影响正确性、可维护性或 plan 合规。

### 002-未修复-低-malformed compacted payload fact refs 测试迁移未完全闭环

- **位置**: Plan §10 mapping row "malformed compacted payload fact refs fail closed" → `test_compact_pipeline.py` / `test_compaction_operation.py`
- **问题类型**: 测试缺口（微小）
- **当前代码**: Plan §10 该行裁决为 "迁移到 `test_compact_pipeline.py` request plan / accepted fact refs helper tests"。当前 `test_compact_pipeline.py` 中的 `test_compacted_payload_input_derives_semantic_refs` 测试了正常 accepted payload input 的 semantic refs 派生，但没有新增 malformed compacted payload（如缺少 `accepted_candidate` 字段、`evidence_backed_facts` 非 list、fact item 非 object）的 fail-closed 测试。
- **反例/失败场景**: 旧 `test_compaction_operation.py` 的 `test_compaction_request_evidence_inputs_reject_malformed_compacted_payload` 被删除后，如果 `accepted_evidence_mapping_refs_for_candidate(...)` 未来在处理 malformed candidate 时行为改变，可能没有单元测试捕获。
- **为什么有问题**: 旧测试是 integration-level（通过 `collect_selected_compaction_request_evidence_inputs`），迁移到 `test_compact_material.py` 或 `test_compact_pipeline.py` 时应保留 edge case coverage。当前 `build_compacted_payload_input` 和 `accepted_evidence_mapping_refs_for_candidate` 对 malformed input 的行为（raise 还是 return empty？）没有显式单元测试覆盖。
- **直接证据**: `rg "malformed_compacted_payload\|malformed.*compact.*payload\|reject.*compacted" tests/host/test_compact_pipeline.py tests/host/test_compact_material.py` → no matches。旧 test 函数在 `test_compaction_operation.py` 中被删除。
- **影响**: 微小——`accepted_evidence_mapping_refs_for_candidate` 在 `compact_payload.py` 中有自己的类型校验；malformed payload 在 operation loop 的更高层也会被质量检查拦截。
- **建议改法和验证点**: Slice 2 实施前，在 `test_compact_pipeline.py` 中新增 1 个 test：`build_compacted_payload_input` 对 `candidate.evidence_backed_facts[*].evidence_labels` 引用不存在 label 时的行为验证。或确认 `compact_payload.py` 已有对应测试覆盖此 edge case。
- **修复风险（低）**: 加一个 test。
- **严重程度（低）**: 不阻塞 Slice 2。

### 003-未修复-低-`_dedupe_texts` 函数与 dispatch/ingest 中的同名函数重复

- **位置**: `dayu/host/compact_pipeline.py:964-971` vs `dayu/host/dispatch.py` / `dayu/host/engine_ingest.py`
- **问题类型**: 最佳实践偏离
- **当前代码**: `_dedupe_texts` 在 compact_pipeline.py 中定义为模块级私有函数，实现为 `tuple(dict.fromkeys(values))`。dispatch.py 和 engine_ingest.py 中各自有同名或等价的去重逻辑。
- **反例/失败场景**: 如果未来需要改变去重策略（如需要保持最后出现而非首次出现），需要在三处独立修改。
- **为什么有问题**: 轻微重复。但该函数太简单（一行 body），且 Slice 2 计划在 wiring 时将 dispatch/ingest 中的调用替换为 pipeline helper，重复会自然消除。
- **直接证据**: `rg "_dedupe_texts\|_dedupe" dayu/host/dispatch.py dayu/host/engine_ingest.py` 确认两处均有独立定义。
- **影响**: 无运行时影响。Slice 2 完成后 dispatch/ingest 中的版本会被移除。
- **建议改法和验证点**: Slice 2a/2b 时将 dispatch/ingest 中的去重调用替换为 `compact_pipeline._dedupe_texts`（或将其提升为 compact_material.py 的 shared helper）。
- **修复风险（低）**: 等 Slice 2 自然消除。
- **严重程度（低）**: 不阻塞。

## 3. Architecture Boundary Review

### Layering
`compact_pipeline.py` stays strictly within Host layer. Dependencies flow downward: Host helper → `compact_material.py` / `context_fallback.py` / `compaction.py` / `context_budget.py` → `durable.codec` / `durable.errors` / `durable.state`. No cross-layer leakage.

### Ownership
Clear separation maintained:
- **compact_pipeline.py**: Snapshot construction, request plan, recovery plans, pass queue, payload input, fallback decision input, raw-tail selection
- **compact_material.py**: Material source (unchanged)
- **context_fallback.py**: Fallback selection/budget helper (unchanged)
- **dispatch.py / engine_ingest.py**: Lifecycle, EventLog, state transitions (unchanged in this diff)

### Public contracts
No public API, schema, EventLog event type, durable payload, compact artifact schema, Engine provider contract, or EngineEvent contract changes. All types in `compact_pipeline.py` are Host-internal (underscore-prefixed private helpers or `__all__`-exported but Host-internal).

### Dependency direction
`compact_pipeline.py` → `compact_material.py`, `context_fallback.py`, `compaction.py`, `context_budget.py`, `context_policy.py`, `memory.py`, `durable.codec`, `durable.errors`, `durable.state`, `engine.contracts.messages`. All downward. No reverse dependencies. No cycle risk.

## 4. Correctness Review

### 4.1 Source snapshot validation
`compact_pipeline_source_snapshot_from_pre_dispatch_view` (line 344-387) validates:
- `trigger_source` type check → `TypeError`
- `run` type check → `TypeError`
- `material_view` type check → `TypeError`
- Input cursor matches material boundary → `ValueError` (fail closed, caller handles)

These guards prevent silent stale snapshot construction.

### 4.2 Fallback decision error handling
`build_fallback_decision_input` (line 587-700) has dual-path error handling:
- Success path: `build_recent_window_fallback_selection(...)` + `estimate_recent_window_fallback_budget(...)` → constructs full `CompactPipelineFallbackDecisionInput` with `fallback_handoff`
- Exception path: catches any `Exception`, constructs `FALLBACK_POLICY_DECISION_SELECTION_FAILED` payload with `action_hint=FALLBACK_ACTION_FAIL_CLOSED`, `selection=None`, `fallback_handoff=None`

This mirrors existing dispatch.py/engine_ingest.py fail-closed semantics.

### 4.3 Ordinary raw-tail empty floor
`select_ordinary_protected_raw_tail` (line 703-746): When `selected_recent_window_turn_floor == 0`, returns empty handoff (no messages, no blocks, empty digest). This correctly handles the "no floor protection" case.

### 4.4 Reactive pass queue edge case
`build_reactive_pass_queue_plan` (line 505-545): When `len(selected) <= 1`, returns empty `pass_requests=()`. This correctly handles the single-block case where multi-pass is unnecessary.

### 4.5 Tier 2 conditional skip
`build_tier_recovery_request_plans` (line 428-502): Tier 2 only added when `len(degraded) > 0 and degraded != source_snapshot.previous_compacted_view`. This correctly avoids a no-op tier 2 when degrade produces same view as original.

## 5. Stability / Robustness Review

### 5.1 Synthetic material block consistency
`_fallback_material_blocks` (line 873-892) directly constructs a `RunInputMaterialBlock` for the current input anchor. All optional fields default to `None`/`()`/`False` per the dataclass definition (compact_material.py:245-258). The construction is valid and consistent with `run_input_material_block(...)` factory semantics.

### 5.2 Message rendering
`_message_from_material_block` (line 1015-1041) handles 4 block kinds:
- `USER_INPUT` → `UserMessage`
- `ASSISTANT_FINAL_ANSWER` → `AssistantMessage` (no `reasoning_content`, no `tool_calls`)
- `ACCEPTED_TOOL_EVIDENCE` → `SystemMessage` with `_accepted_tool_evidence_content(...)`
- Generic `EVIDENCE_MATERIAL` section → `SystemMessage` with `_RECENT_EVIDENCE_PREFIX`
- Fallback → `SystemMessage(content=block.text)`

`_accepted_tool_evidence_content` (line 1044-1067) raises `HostDurableError` when `readable_tool_name is None` — fail closed on malformed evidence blocks. LLM-facing output is business-readable: `tool_name=...`, `query=...`, `source=...`, `result=...`. No internal refs exposed.

### 5.3 No cancellation observation
All functions are pure/synchronous data transformations. They do not observe or swallow `CancellationToken`. This is correct per plan §13 item 6: "helper functions do not observe or swallow cancellation."

## 6. Test Quality Assessment

### 6.1 Coverage of public API
| Public function | Test function | Coverage |
|---|---|---|
| `compact_pipeline_source_snapshot_from_pre_dispatch_view` | `test_source_snapshot_uses_run_and_material_view_truth` + `test_source_snapshot_rejects_input_boundary_mismatch` | Happy path + boundary validation |
| `build_normal_compact_request_plan` | `test_normal_request_plan_keeps_current_input_out_of_selected_segment` + `test_reactive_request_plan_sets_attempt_identity_without_semantic_drift` | Proactive + reactive + proactive/reactive equivalence |
| `build_tier_recovery_request_plans` | `test_tier_recovery_request_plans_use_fallback_caps_degrade_and_delta_only` | Tier 1/2/3 structure + caps/degrade/delta-only |
| `build_reactive_pass_queue_plan` | `test_reactive_pass_queue_builds_single_block_passes` | Multi-block → one-per-pass |
| `build_compacted_payload_input` | `test_compacted_payload_input_derives_semantic_refs` | Semantic ref derivation |
| `build_fallback_decision_input` | `test_fallback_decision_input_dispatch_and_fail_closed` | Dispatch + fail_closed + no `fallback_tier` |
| `select_ordinary_protected_raw_tail` | `test_ordinary_protected_raw_tail_selects_recent_group_and_memory_dedupes` | Protected group selection + memory dedup |
| `selection_policy_digest` flow | `test_memory_policy_digest_helper_is_selection_policy_source` | End-to-end digest propagation |

### 6.2 Migrated evidence tests
4 new tests in `test_compact_material.py`:
- Descriptor raw payload (not envelope preview) — 1 test
- No envelope → no evidence block — 1 test
- Missing request atom → limited signal — 1 test
- Raw evidence damage (missing raw + legacy preview) → fail closed — 1 parametrized test (2 cases)

All migrated tests use `build_pre_dispatch_compact_material_view(...)` (real production path), not old `collect_selected_compaction_request_evidence_inputs` (shadow path).

### 6.3 Deleted evidence tests
All `collect_selected_compaction_request_evidence_inputs` calls and `SelectedEvidenceBlockRef` usage removed from `test_compaction_operation.py`. No regression in operation loop tests — they still cover compaction operation, artifact, proposal manifest, reactive multipass, attempt budget, and cancellation.

### 6.4 No cheating
- Tests use real `compact_pipeline_source_snapshot_from_pre_dispatch_view(...)` (production constructor), not hand-crafted snapshots that bypass validation.
- `test_fallback_decision_input_dispatch_and_fail_closed` uses real `context_budget_policy_from_threshold_tokens(...)` and exercises both `dispatch` and `fail_closed` paths with different window sizes.
- `test_memory_policy_digest_helper_is_selection_policy_source` verifies the digest propagates from `digest_memory_projection_policy(...)` through to `plan.selected_segment.policy_digest`.
- No mocks, stubs, or test doubles that weaken assertions.

## 7. Open Questions

1. **Malformed compacted payload edge case coverage**: `build_compacted_payload_input` → `accepted_evidence_mapping_refs_for_candidate` behavior with malformed `evidence_backed_facts` (e.g., `evidence_labels` referencing non-existent labels) is not explicitly tested. Low priority — `compact_payload.py` and operation-level tests cover most paths.

2. **`_dedupe_texts` consolidation**: Will be naturally resolved in Slice 2a/2b wiring when dispatch/ingest switch to pipeline helpers. No action needed now.

## 8. Residual Risks

| ID | Risk | Severity | Owner |
|----|------|----------|-------|
| WU-CM-13-S1-R1 | malformed compacted payload fact refs test coverage gap | Low | Slice 2 or post-wiring audit |
| WU-CM-13-S1-R2 | `_dedupe_texts` duplication across 3 modules | Low | Slice 2a/2b wiring |
| WU-CM-13-S1-R3 | `compact_pipeline.py` line count 1092 exceeds plan <500 target; docstring overhead explains ~500 lines of excess | Trivial | Plan doc update or accept |

## 9. Final Deep Review Conclusion

**Verdict: PASS**

The Slice 1 implementation is clean, correct, and fully compliant with the accepted plan. All plan constraints are satisfied:

- **Thin helper**: Module is behaviorally thin — no lifecycle imports, no EventLog writes, no state transitions, no artifact creation, no operation loop control. The 1092-line count is inflated by ~500 lines of required Chinese docstrings for 14 type definitions and 17 functions; effective logic is proportionate.
- **No tier 5**: Confirmed absent from all code, tests, and payload fields.
- **Caller-owned guards**: `CompactPipelineSourceSnapshot` carries only neutral fields; commit guard remains in dispatch/ingest.
- **WU-CM-14 shared helper**: `select_ordinary_protected_raw_tail` and `CompactPipelineProtectedRawTailProvider` Protocol provide shared selection eligibility; `_message_from_material_block` renders LLM-facing messages without internal refs.
- **Evidence cleanup**: `compaction_evidence.py` fully deleted; all references removed (verified by `rg`); 4 new tests migrated to `test_compact_material.py` using real production paths; old `test_compaction_operation.py` evidence tests removed without weakening operation loop coverage.
- **Tests**: 91 passed, pyright 0 errors, no cheating or weakening.

Slice 1 is ready for accepted-slice commit. Slice 2a/2b/2c wiring can proceed.
