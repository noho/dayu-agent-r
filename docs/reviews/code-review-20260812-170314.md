# Code Re-Review S1

## Gate 元数据

- **gate**: `code re-review S1`
- **work unit**: `UF-FIX09 shared-interruptible-docling-converter`
- **slice**: `UF-FIX09-S1`
- **reviewer**: AgentDS（独立第二路 re-reviewer）
- **base**: `9527c6e0fd430082219b13daee31d300a8b44be4`（accepted plan commit）
- **控制文档**: `docs/gateflow/uf-fix09-s1-code-review-adjudication-20260812.md`
- **首轮 AgentDS artifact**: `docs/reviews/code-review-20260812-164911.md`
- **首轮 AgentMiMo artifact**: `docs/reviews/code-review-20260812-165218.md`
- **output file**: `docs/reviews/code-review-20260812-170314.md`
- **re-reviewed at**: `2026-08-12T17:03:14+08:00`

## Digest 校验

| 文件 | 冻结 SHA-256 | 实际 SHA-256 | 状态 |
| --- | --- | --- | --- |
| `dayu/fins/pipelines/docling_process_converter.py` | `dab1d5ef4fe308bd4a73d9d983dde038fc304430b1d56a762c6c5a29e740cc86` | `dab1d5ef4fe308bd4a73d9d983dde038fc304430b1d56a762c6c5a29e740cc86` | ✓ 未变 |
| `tests/fins/test_docling_process_converter.py` | `10028e22186162c8a5aab271a490323a5a2bd4bd451baca07c7269c92130c861` | `10028e22186162c8a5aab271a490323a5a2bd4bd451baca07c7269c92130c861` | ✓ 匹配 |

**说明**: 测试 SHA-256 与裁决文档记录 (`24e29281...`) 不同，符合预期——AgentCodex 已修改测试文件闭环两项 accepted gaps。生产 SHA-256 与原始冻结值完全一致，确认生产代码未受任何修改。

## Scope

- **Included scope**: 上述两个文件
- **Excluded scope**: 旧 `cn_docling_process.py`、旧 runner/tests、caller 迁移、另一 re-reviewer 的 artifact
- **Contract evidence**: 首轮两路 review artifact、裁决文档、根 `CLAUDE.md`、`dayu.runtime.interruptible_process`、`dayu.documents.docling_runtime`
- **README decision**: S1 不变更 README（与裁决文档一致）

## Closure Matrix — Accepted Gaps 验证

### Accepted Gap 1: `mkdtemp`/`write_bytes` 及 `rmtree` 组合的分类/资源/cause

**裁决要求**: 新增 deterministic tests：
- `mkdtemp` 失败 → 不创建 handle，返回 `IPC_PROTOCOL`
- `write_bytes` 失败 → 仍删除已创建 temp
- `rmtree` 同时失败 → `CLEANUP` 优先级覆盖，保留原失败链

**实施验证**:

| 测试用例 | 文件行号 | 覆盖场景 | 验证点 | 结果 |
| --- | --- | --- | --- | --- |
| `test_converter_mkdtemp_failure_is_ipc_without_handle_or_cleanup_leak` | 1178-1227 | `mkdtemp` 抛 `OSError` | kind=`IPC_PROTOCOL`, `__cause__` 为原始异常, 无 handle 实例, 无 cleanup 调用 | ✓ |
| `test_converter_input_write_failure_is_ipc_and_removes_temp_without_handle` | 1230-1265 | `write_bytes` 抛 `OSError` | kind=`IPC_PROTOCOL`, `__cause__` 为写入异常, 无 handle, temp 已删除 | ✓ |
| `test_converter_input_write_and_rmtree_failures_prioritize_cleanup_and_preserve_chain` | 1268-1325 | `write_bytes` 失败 + `rmtree` 失败 | kind=`CLEANUP`, `__cause__` 为 cleanup 异常, 链内保留 IPC→write 链, temp 最终被清理 | ✓ |

**合同性评估**: 三个测试均通过公开 contract（`DoclingConversionError.kind`、`__cause__` 链、handle 无副作用、temp 资源收口）验证行为，未复制私有状态机内部分支或状态。测试使用 `_install_recording_converter_dependencies` 统一 fake 基础设施，与已有测试风格一致。非过拟合。

### Accepted Gap 2: `terminate`/`kill` 普通异常的 FAILED phase、CLEANUP、close/temp

**裁决要求**: 增加 typed fake handle failure injection：
- terminate 抛异常 → 记录 FAILED phase → 映射 `CLEANUP` → 仍调用 close 与 temp cleanup
- terminate 未退出后 kill 抛异常 → 记录 FAILED phase → 映射 `CLEANUP` → 仍调用 close 与 temp cleanup

**实施验证**:

| 测试用例 | 文件行号 | 覆盖场景 | 验证点 | 结果 |
| --- | --- | --- | --- | --- |
| `test_converter_terminate_failure_is_cleanup_with_failed_phase_and_full_cleanup` | 1420-1456 | `terminate` 抛 `RuntimeError` | kind=`CLEANUP`, `__cause__` 为 terminate 异常, FAILED phase="terminate_completed", close 仍调用, kill 未调用, temp 已清理 | ✓ |
| `test_converter_kill_failure_is_cleanup_with_failed_phase_and_full_cleanup` | 1459-1496 | terminate 未退出 → `kill` 抛 `RuntimeError` | kind=`CLEANUP`, `__cause__` 为 kill 异常, FAILED phase="kill_completed", terminate→kill→close 顺序保持, temp 已清理 | ✓ |

**合同性评估**: 两个测试通过 `_RecordingHandle` 的 `terminate_failure`/`kill_failure` ClassVar 注入失败，并验证公开可观察行为（异常 kind、cause 链、handle 调用顺序、temp 清理、结构化日志输出）。日志断言使用 `caplog` 读取 production `_log_cleanup_phase` 产生的记录——这是 production 有意暴露的诊断接口，非私有状态机复制。非过拟合。

## Rejected Findings — 未实施确认

| 被驳回 Finding | 来源 | 裁决 | 当前状态 |
| --- | --- | --- | --- |
| `_close_handle` while-loop 无上界 | AgentMiMo F2, AgentDS F1 | rejected-with-reason | ✓ 未实施——line 641 仍为 `while True:` 无上限 |
| 测试隐式依赖 Docling 安装 | AgentMiMo F1 | rejected-with-reason | ✓ 未实施——line 23 仍为模块级 import |
| spawn probe 依赖 pytest | AgentDS F6 | rejected-with-reason | ✓ 未实施——line 311 仍使用 `pytest.MonkeyPatch()` |
| `wait_result=None` 防御分支 | AgentDS F4 | rejected-with-reason | ✓ 未实施——line 683-687 无变更 |
| closed JSON 私有递归分支 | AgentDS F5 | rejected-with-reason | ✓ 未实施——`_is_closed_json_value` 无变更 |

**确认**: 5 项被驳回 findings 均未被错误实施到生产代码或测试中。生产 SHA-256 未变，所有修改仅限于测试文件且严格限定在两项 accepted gaps 范围内。

## Validation Evidence

### 测试执行

```
39 passed in 0.62s (owner tests)
1196 passed, 1 skipped in 45.58s (fins full regression)
```

### Pyright

```
0 errors, 0 warnings, 0 informations
```

### 覆盖率

```
dayu/fins/pipelines/docling_process_converter.py  331 stmts  18 missed  95%
```

覆盖率从首轮的 93%（24 missed）提升至 95%（18 missed），恰好消除 6 个 previously-missed 行，与两项 accepted gaps 的新增测试对应。剩余 18 个未覆盖行（86, 122, 195, 255, 411-412, 470, 592, 684, 696, 708, 821, 832-833, 877, 930, 932, 935）均为裁决文档已确认接受的防御/死代码路径。

### 新增测试的合同质量

- 全部使用中文 docstring 描述测试意图
- 通过 `_install_recording_converter_dependencies` 复用已有 fake 基础设施
- 断言目标均为公开 contract 面：`DoclingConversionError.kind`、`__cause__` 链、`_RecordingHandle` 调用序列、temp 存在性、结构化日志输出
- 未引入新的 fake/mock 类型或测试 seam
- 未复制私有状态机内部状态或分支逻辑

## New Findings

无新增 finding。两项 accepted gaps 的修复全部精准闭环，无 scope creep，无过度测试。

## Open Questions

无。所有 contract 证据完整，裁决文档与两项 artifact 的 closure 已独立验证。

## Residual Risk

| 风险 | 分类 | owner / 下一步 |
| --- | --- | --- |
| 18 个 defensive/dead-code 路径未覆盖 | 已裁决接受 | 不阻塞 S1；首轮 review 已确认行为正确或不可达 |
| 旧 CN runner 与新版并存 | covered by S2 | S2 migration/delete |
| 真实 Docling 未在 S1 执行 | covered by S2/S3 | S2 integration / S3 |
| 非 POSIX descendant guarantee | assigned to later work unit | runtime platform work unit |
| 新增测试依赖 `_RecordingHandle` 的 ClassVar 注入模式 | 低 | 与已有 34 个测试的 fake 架构统一；若 runtime handle 接口变更，测试需同步，但这属于 contract evolution 的正常范围 |

## Docs Decision

S1 re-review 无新增文件或行为变更，不触发 README 更新。

## Completion

- **AgentCodex 修复验证**: 测试文件修改严格限定在 5 个新增测试用例，闭环两项 accepted gaps；生产文件未变
- **驳回 findings 未实施**: 5/5 confirmed not implemented
- **新增测试合同质量**: 非过拟合，非私有状态机复制
- **Owner tests**: 39/39 passed
- **Fins regression**: 1196 passed, 1 skipped
- **Pyright**: 0 errors, 0 warnings
- **Coverage**: 95%（+2pp vs 首轮）

## 结论

**PASS**

两项 accepted gaps 已正确闭环于测试文件。生产代码未变。被驳回 findings 未被错误实施。新增测试精确验证公开 contract 行为，无 overfitting 或私有 state-machine 复制。无 new findings，无 unresolved open questions。

路径: `docs/reviews/code-review-20260812-170314.md`
