# Plan Review: conversation memory smoke 诊断增强

- **Reviewed target and scope**: `docs/reviews/gateflow-plan-conversation-memory-smoke-diagnostics-20260619-110317.md`
- **Review date**: 2026-06-19
- **Reviewer**: planreview skill

## Assumptions Tested

1. 只增强 smoke 诊断，不改变 Host compact / memory 生产语义。
2. EventLog 中已有 rejected attempt payload，smoke 可以解析为 operator-facing audit。
3. DEBUG 明细能暴露 offending field 的 durable ref 或 diagnostic 文本，而不读取 compact material 正文。
4. stdout 粘连可通过 line buffering / flush 在 smoke 入口修复。
5. README 更新只描述用户可见 smoke 行为，不扩写 Host 设计。

## Findings

无 blocking findings。

### 1-已裁决-低-EventLog 读取边界需要保持为 diagnostic compact suite 例外

- **位置**: S1 compact audit 结构化诊断
- **问题类型**: 架构边界
- **当前写法**: smoke 会读取本次 session 的 compact EventLog rows。
- **反例/失败场景**: 如果实现顺手读取 memory 表、compact material 正文或 Host private implementation，conversation memory smoke 会从 public smoke 漂移成内部状态测试。
- **为什么有问题**: 用户要求范围限定在日志不足与 smoke compact 覆盖加强，并且不改变生产 memory 语义。
- **直接证据**: 计划明确“不读取 memory 表或 compact material 正文；只读取本次 session 的 compact EventLog rows 与 compact artifact 文件路径”。
- **影响**: 若偏离，后续 review 不可验收。
- **建议改法和验证点**: 实现时只解析 EventLog payload 元数据；测试中构造 `EventLogRow`，不接触 Host private memory/material。
- **修复风险（低）**:
- **严重程度（低）**:
- **裁决**: accepted，作为实现约束处理。

## Open Questions

无 blocking open questions。用户已确认 dirty baseline 可继续修改，并要求继续 plan / implement；同时明确不要 commit，因此 gateflow commit/PR gates 本轮不执行。

## Residual Risks and Suggested Tracking Destination

- 真实 compactor prompt 仍可能持续产出 invalid proposal：本轮只增强诊断，后续若要提升 accept 率，应另开 compact prompt / candidate validation work unit。
- issue-80 memory eval 目标未在 smoke 中完整实现：按用户要求不纳入本轮。

## Final Plan Review Conclusion

pass-with-risks

