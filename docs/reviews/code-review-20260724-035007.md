# Code Review

## Scope

- Mode: current changes
- Branch: `feat/wu-ctx-01`
- Base: `ed43bcf2` (gateflow: accept first-call producer plan for WU-CTX-01)
- Output file: `docs/reviews/code-review-20260724-035007.md`
- Included scope: HEAD ed43bcf2 之后全部 workspace changes（staged + unstaged + committed），排除 `docs/host/issues-implementation-control.md`（Controller-owned diff）
- Excluded scope: `docs/host/issues-implementation-control.md`
- Parallel review coverage: 5 个专项 subagent 并行覆盖
  - manifest & candidate (`_runner_call_manifest.py`, `context_budget.py`, `compact_payload.py`, `compaction_operation.py`, `context_fallback.py`)
  - run_input & source replay (`run_input.py`, `accepted_result_projection.py`, `memory.py`)
  - lifecycle producers (`admission.py`, `dispatch.py`, `waiting.py`, `recovery.py`, `engine_ingest.py`)
  - durable deletion & schema (`durable/state.py`, `durable/run_transition.py`, `durable/schema.py`, `command.py`, `open_host.py`)
  - tests & negative guards (全部 `tests/host/` 变化)
- Design truth: `docs/host/design.md` §25
- Plan: `docs/reviews/wu-ctx-01-plan-codex.md`、`docs/reviews/wu-ctx-01-slice-1-first-call-producer-plan-amendment-codex.md`
- Controller adjudication: `docs/reviews/wu-ctx-01-slice-1-first-call-producer-plan-rereview-controller-adjudication.md`

## Findings

### 01-未修复-中-`_create_running_admission_result` 死代码且违反 manifest-before-start contract

- **入口/函数**: `admission.py::_create_running_admission_result`（line 2691）
- **文件(行号)**: `dayu/host/admission.py:2691-2780`
- **输入场景**: 任何调用此函数的路径
- **实际分支**: 函数已定义但全代码库零调用点（`grep -rn` 确认）
- **预期行为**: 若为 dead code 则应删除；若为 first-call producer 则必须在 start transition 前提交 manifest
- **实际行为**: 函数调用 `create_running_run_with_starting_attempt_in_transaction` 直接创建 running Run + starting Attempt + pending dispatch，但未先调用 `record_prepared_runner_call_candidate_in_transaction` 提交 manifest。违反 Slice 1 核心 manifest-before-start contract
- **直接证据**: line 2729 调用 `create_running_run_with_starting_attempt_in_transaction`，无前置 manifest 写入；`grep -rn "_create_running_admission_result" dayu/host/ tests/` 仅返回定义行
- **影响**: dead code 本身无运行时影响；若未来被启用则违反 manifest-before-start contract，导致 worker strict load 找不到 manifest
- **建议改法和验证点**: 删除此函数及关联的 `_CreateAdmissionRequest` 若无其它引用；或若为未来预留，添加 manifest-before-start 并标注 TODO
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 02-未修复-中-`_source_policy_snapshot_in_transaction` 死代码

- **入口/函数**: `engine_ingest.py::_source_policy_snapshot_in_transaction`（line 926）
- **文件(行号)**: `dayu/host/engine_ingest.py:926-972`
- **输入场景**: 任何调用此函数的路径
- **实际分支**: 函数已定义但全代码库零调用点
- **预期行为**: 若为 dead code 则应删除
- **实际行为**: reactive recovery 路径（line 778）已从 `source_candidate.policy_snapshot` 获取 policy，此函数未被使用
- **直接证据**: `grep -rn "_source_policy_snapshot_in_transaction" dayu/host/` 仅返回定义行
- **影响**: dead code 增加维护负担，误导读者认为存在另一条 source policy 加载路径
- **建议改法和验证点**: 删除此函数
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 03-未修复-中-`source_boundary_refs` 解析端缺少位置编码校验

- **入口/函数**: `compact_payload.py::parse_context_compacted_semantic_payload`
- **文件(行号)**: `dayu/host/compact_payload.py:179-180`
- **输入场景**: 从持久化 `CONTEXT_COMPACTED` payload 解析 `source_boundary_refs`
- **实际分支**: 直接取 `source_refs[0]` 作为 `current_input_ref`，`source_refs[1:]` 作为 `compacted_source_refs`
- **预期行为**: 解析端应验证 `source_refs[0]` 确实对应 `current_input_ref` 的语义，或至少做 assert
- **实际行为**: 写入端 `source_boundary_refs(request)` 通过 `dict.fromkeys((request.current_input_ref, *request.material_source_refs, ...))` 保证顺序，但解析端无任何校验。payload corruption 会静默产生错误的 `current_input_ref`
- **直接证据**: line 179 `current_input_ref=source_refs[0]`、line 180 `compacted_source_refs=source_refs[1:]`，无 assert/check
- **影响**: 若持久化数据因 migration、manual edit 或 bug 导致首位不是 `current_input_ref`，compact payload 语义错误，影响后续 dispatch 的 `current_input_ref` 匹配
- **建议改法和验证点**: 在解析端添加 `assert len(source_refs) >= 1` 并在 docstring 中说明位置编码约定；或改为 key-based 解析
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 04-未修复-中-15-cell 测试仅覆盖 12 cells，CONTINUATION stage 缺失

- **入口/函数**: `tests/host/test_context_budget.py::test_context_sizing_stage_matrix_separates_pressure_from_action`
- **文件(行号)**: `tests/host/test_context_budget.py:65-140`
- **输入场景**: `ContextSizingStage.CONTINUATION` × 3 pressure levels
- **实际分支**: parametrize 仅包含 4 个 stage（ORDINARY、POST_COMPACT、REACTIVE_POST_COMPACT、DISPATCH_FALLBACK），共 12 test case
- **预期行为**: 5-stage/15-cell 显式穷举要求测试覆盖全部 5 个 stage
- **实际行为**: `CONTINUATION` stage 的 3 个 cell（NORMAL→ALLOW、SOFT→ALLOW、HARD→ALLOW）未被测试验证。docstring 写 "4x3"
- **直接证据**: line 148 docstring `"4x3 stage-aware action矩阵"`；parametrize 列表无 `ContextSizingStage.CONTINUATION`；`context_budget.py` line 909-920 定义了 CONTINUATION 的三个 case
- **影响**: CONTINUATION 行为与 REACTIVE_POST_COMPACT 相同（全部 ALLOW），当前不会产生回归 bug；但若未来 CONTINUATION 行为独立分化，缺乏测试保护
- **建议改法和验证点**: 在 parametrize 矩阵中补充 `ContextSizingStage.CONTINUATION` 的 3 个 cell，更新 docstring 为 "5x3"
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 05-未修复-低-`_validate_sizing_snapshot` COMPLETE 路径存在不可达的冗余 None 校验

- **入口/函数**: `_runner_call_manifest.py::_validate_sizing_snapshot`
- **文件(行号)**: `dayu/host/_runner_call_manifest.py:1383-1402`
- **输入场景**: `status == COMPLETE` 的 sizing snapshot
- **实际分支**: line 1387 先做 `any(atom is None for atom in value_atoms)` 全量 None guard（包含 `conservative_input_tokens` 和 `context_window_size`），但 line 1393 和 1399 又显式检查这两个字段是否为 None
- **预期行为**: 校验逻辑无死代码
- **实际行为**: line 1393 `if snapshot.conservative_input_tokens is None` 和 line 1399 `if snapshot.context_window_size is None` 是不可达代码
- **直接证据**: line 1370-1382 `value_atoms` 包含这两个字段；line 1387 的 `any(...)` 已拦截所有 None
- **影响**: 代码质量，误导读者认为有特殊风险
- **建议改法和验证点**: 删除 line 1393-1395 和 1399-1401 的冗余 None 分支
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 06-未修复-低-`BudgetEstimate.estimator_digest` docstring 未同步更新

- **入口/函数**: `context_budget.py::BudgetEstimate.estimator_digest`
- **文件(行号)**: `dayu/host/context_budget.py:278-279`
- **输入场景**: docstring 阅读
- **实际分支**: docstring 写 `估算输入、策略与常量的 digest`
- **预期行为**: `_estimator_digest` 已移除 policy 字段，docstring 应反映新策略
- **实际行为**: docstring 仍提及 "策略"，但 digest payload 只包含 `estimator_contract`、`input`、`constants`、`estimate`
- **直接证据**: line 279 `":param estimator_digest: 估算输入、策略与常量的 digest。"`；`_estimator_digest` payload 无 policy 字段
- **影响**: 文档债务，不影响运行时
- **建议改法和验证点**: 更新 docstring 为 `估算输入、估算器契约与常量的 digest`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/低/严重）**: 低

### 07-未修复-低-`_hard_threshold_closeout` docstring 未说明 stage 排除原因

- **入口/函数**: `dispatch.py::_hard_threshold_closeout`
- **文件(行号)**: `dayu/host/dispatch.py:760-787`
- **输入场景**: 函数接收 `ContextSizingStage` 参数
- **实际分支**: 只处理 ORDINARY/POST_COMPACT/DISPATCH_FALLBACK
- **预期行为**: docstring 应说明为何排除 REACTIVE_POST_COMPACT 和 CONTINUATION
- **实际行为**: docstring 写 `stage 不是已冻结的三种治理阶段时抛出` 但未解释原因
- **直接证据**: `_stage_pressure_action` 中 REACTIVE_POST_COMPACT+HARD=ALLOW、CONTINUATION+HARD=ALLOW；调用方只传 POST_COMPACT 或 DISPATCH_FALLBACK
- **影响**: 若未来新增 stage 且 hard=BLOCK，调用方得到 ValueError 而非正确 closeout
- **建议改法和验证点**: docstring 补充说明 REACTIVE_POST_COMPACT 和 CONTINUATION 的 hard action 为 ALLOW，不需 closeout
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 08-未修复-低-`engine_ingest.py` 中 `cast()` 抑制类型缩窄

- **入口/函数**: `engine_ingest.py::_continuation_sizing_snapshot`
- **文件(行号)**: `dayu/host/engine_ingest.py:6477-6493`
- **输入场景**: continuation sizing snapshot 构造
- **实际分支**: `cast(str, ...)` / `cast(int, ...)` 逻辑上安全（因 `unavailable_reason is not None` 检查在前）
- **预期行为**: 使用显式 `assert` 或 `raise HostDurableError` 替代 `cast`
- **实际行为**: 若 `_ContinuationFrozenSources` 不变量被违反，`cast` 会在运行时掩盖 bug
- **直接证据**: line 6477-6493 使用 `cast(str, ...)` 和 `cast(int, ...)`
- **影响**: 运行时 bug 掩盖，与项目 "禁止使用 Any" 精神不一致
- **建议改法和验证点**: 替换为显式类型断言
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 09-未修复-低-recovery CAS_LOST 缺少显式 guard

- **入口/函数**: `recovery.py::_start_recovery_dispatch_or_ready`
- **文件(行号)**: `dayu/host/recovery.py:853-872`
- **输入场景**: recovery start transition 返回 CAS_LOST
- **实际分支**: `NOT_FOUND` 和 `INVALID_STATE` 抛 `_StartupRecoveryStartRollback`，但 `CAS_LOST` fall through 返回 `RECOVERY_DISPATCHED`
- **预期行为**: CAS_LOST 应有显式 guard 或 rollback
- **实际行为**: `CAS_LOST` 可能不可达（因 `start_recovery_run_with_starting_attempt_in_transaction` 对 precondition 不匹配返回 `INVALID_STATE`），但无显式 assert
- **直接证据**: line 853-872 无 CAS_LOST 分支处理
- **影响**: 若 CAS_LOST 可达，返回语义不正确的 `RECOVERY_DISPATCHED`
- **建议改法和验证点**: 添加 `assert result.status is not RunMutationStatus.CAS_LOST` 或显式 rollback
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Slice 1 边界审计

以下为 Slice 1 必须确认的边界约束，基于直接代码证据：

| 约束 | 结果 | 直接证据 |
|------|------|----------|
| CONTEXT_BUDGET_EVALUATED 零引用 | ✅ PASS | `grep -rn "CONTEXT_BUDGET_EVALUATED\|HostContextUsageView\|HostActivityKind.CONTEXT_USAGE" dayu/host/` 零结果 |
| USAGE_ANCHORED 仅保留 frozen enum | ✅ PASS | `grep -rn "USAGE_ANCHORED" dayu/host/` 仅 `context_budget.py:71` 枚举定义 |
| public projection 零引用 | ✅ PASS | 无 `HostContextUsageView`、`EntrypointContextUsage` 定义或使用 |
| usage anchor 算法零引用 | ✅ PASS | 无 `_estimate_usage_observation_input` 定义或使用 |
| direct promotion 零残留 | ✅ PASS | `grep -rn "promote_queued_run\|PromoteQueuedRunInput\|PromotionSkipReason\|promote_next_queued_run" dayu/ tests/` 零结果 |
| 5-stage 闭集 | ✅ PASS | `context_budget.py` line 83-90 定义 5 个 stage；line 909-920 显式穷举 15 cells |
| manifest-before-start | ✅ PASS | 所有 producer（dispatch、waiting、recovery）在 start transition 前调用 `record_prepared_runner_call_candidate_in_transaction` |
| startup exact replay | ✅ PASS | recovery 从 `load_prepared_runner_call_source_in_transaction` 读取 source manifest/candidate，不读 current config |
| steer strict parse + rollback | ✅ PASS | admission `_strict_steer_candidate_inputs` 同一 transaction 内 strict parse；失败整笔 rollback |
| wait completed/cancelled continuity | ✅ PASS | `accepted_result_projection.py` 共用 strict core；planned/committed event id 逐字相等 |
| failed/lost 零新 Attempt | ✅ PASS | `waiting.py` failed→`fail_run_from_waiting_in_transaction`、lost→`mark_run_lost_from_waiting_in_transaction`，无 resume candidate/manifest |
| Engine continuation 仅 CONTINUATION stage | ✅ PASS | `engine_ingest.py` 使用 `ContextSizingStage.CONTINUATION`，不调用 pre-start recorder |
| SessionContinuityView.source_refs 必填 | ✅ PASS | `run_input.py` line 341-349 无默认值，全部 construction site 显式提供 |
| `_create_execution_handle` 旧 helper 零 import | ✅ PASS | 所有使用均为 `execution_handle_support.create_execution_command_handle` 的合规 partial/wrapper |

## Open Questions

- 无。

## Residual Risk

- optional real compactor smoke 未启用（既有显式 opt-in 外部 gate）。
- Gemini real-runner smoke 受外部 quota 限制，未提供成功调用证据。
- 项目默认排除的 stress marker 未在本轮额外启用。
- `_create_running_admission_result`（dead code）若被误启用将违反 manifest-before-start contract。
- recovery `CAS_LOST` 分支缺少显式 guard 和测试覆盖。
- `_source_policy_snapshot_in_transaction`（dead code）增加维护负担。

## Review Method

本 review 使用 5 个并行专项 subagent 覆盖：
1. manifest & candidate production（sizing snapshot、stage/action matrix、identity/digest）
2. run_input & source replay（startup exact replay、source policy strict load、steer rollback）
3. lifecycle producers（admission、dispatch、waiting、recovery、engine_ingest 的 manifest-before-start）
4. durable deletion & schema（direct promotion 删除完整性、schema 变更、composition 装配）
5. tests & negative guards（静态审计、行为 contract 测试、反例覆盖）

主 reviewer 整合、去重、复核证据链并裁决 severity。所有 findings 均基于直接代码证据。
