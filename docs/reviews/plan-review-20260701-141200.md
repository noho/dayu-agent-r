# WU-WAIT-02 Production Poller Plan Re-Review

- **Review target**: `docs/host/wu-wait-02-production-poller-plan.md`
- **Work unit**: WU-WAIT-02 / GitHub Issue #90
- **Gate**: plan re-review
- **Reviewer**: AgentMiMo
- **Design sources**: `docs/host/design.md`, `docs/engine/design.md`
- **Control doc**: `docs/host/issues-implementation-control.md`
- **Plan-fix artifact**: `docs/reviews/wu-wait-02-plan-fix-codex.md`
- **Controller adjudication**: `docs/reviews/wu-wait-02-plan-review-controller-adjudication.md`
- **Original review artifacts**: `docs/reviews/plan-review-20260701-135815.md`, `docs/reviews/plan-review-20260701-140124.md`

## Scope

Re-review the plan after plan-fix gate. Verify all 7 controller-accepted findings are fixed or explicitly deferred with owner/destination. Check whether the plan-fix introduced new material blockers. Do not re-litigate already accepted plan direction.

## Accepted Finding Verification

### Finding 1: Terminal resolve clearing poll claim fields

**Source**: AgentDS F01 (High) — accepted by controller

**Verification**: **FIXED**

- Slice 1 "Exact changes" line 379 now explicitly states: "Update `_mark_wait_record_terminal_row` or the equivalent terminal wait mutation to set `poll_claim_id=NULL`, `poll_claim_owner_id=NULL`, `poll_claimed_at=NULL`, and `poll_claim_expires_at=NULL` on resolved / failed / lost terminal transitions."
- Slice 1 "Data flow / state transitions" lines 399-401: "ready or lost: call resolve_wait(wait_id, poll-idempotency-key) → resolve_wait terminal mutation clears poll claim fields"
- Slice 1 "Tests" lines 436-437: "terminal wait transition clears `poll_claim_id`, `poll_claim_owner_id`, `poll_claimed_at`, and `poll_claim_expires_at`"
- Slice 1 "Invariants" line 424: "resolve_wait terminal mutation clears poll claim fields" confirmed in data flow
- The invariant from line 164-165 now has a corresponding implementation step and test assertion.

### Finding 2: Atomic claim acquisition uses UPDATE ... WHERE; rowcount 0 is claim conflict

**Source**: AgentMiMo F01 + AgentDS F02 (Medium) — accepted by controller

**Verification**: **FIXED**

- "Durable State Helpers" line 168: "Claim acquisition must be a single atomic write operation, or an equivalent single-row write primitive, where eligibility checks and claim-field updates happen in the same statement. A separate read result must never authorize an adapter call."
- Slice 1 "Exact changes" lines 375-376: "set claim fields by an atomic `UPDATE ... WHERE` / equivalent write statement where poll eligibility and claim-field assignment happen in the same statement; treat `rowcount == 0` or no returned row as a claim conflict, skip the adapter call, increment claim-conflict diagnostics, continue to another wait, and do not run release logic for a claim that was never acquired."
- Slice 1 "Data flow / state transitions" lines 391-396: diagram shows "atomic claim write: UPDATE host_wait_records SET poll_claim_id / owner / claimed_at / expires_at WHERE wait is poll-eligible and unclaimed-or-expired → claimed row returned"
- Slice 1 "Data flow" lines 405-406: "claim CAS rowcount 0: skip adapter call and do not release"
- Slice 1 "Error handling" line 415: "Claim acquisition `rowcount == 0` means another poller won the claim or the row became ineligible. Treat it as a claim conflict: skip adapter work, increment diagnostics, and never release a claim id that was not acquired."
- Slice 1 "Tests" lines 438-439: "claim acquisition conflict increments diagnostics and does not call adapter or release an unacquired claim"

### Finding 3: Missing adapter current-WU policy is explicit; residual owner/destination recorded

**Source**: AgentDS F03 (Medium) — accepted by controller

**Verification**: **FIXED**

- "Backoff Policy" lines 310-311: "Missing adapter remains a retryable poller configuration/runtime error in this WU. It uses capped-delay indefinite retry rather than terminalizing the wait, because this work unit cannot prove whether the adapter is permanently invalid or temporarily absent during deployment. The poller must record `missing_adapter` in wait-row last outcome/error fields and increment runtime diagnostics so operators can repair registration or handle the wait through a later owner."
- Slice 1 "Error handling" line 412: "Missing adapter is a retryable poller error with capped-delay indefinite backoff; it must not call `resolve_wait`, must update wait-row last outcome/error metadata, and must increment runtime diagnostics for operator visibility."
- "Residual Risks" line 691: "Missing adapter uses capped-delay indefinite retry and does not terminalize waits in this WU to avoid false terminalization during deployment/configuration gaps. Owner: WU-WAIT-02 Slice 1. Destination: wait-row `poll_last_outcome` / runtime diagnostics for operator visibility; future owner WU-WAIT-03 or provider lifecycle work if terminal provider-failure policy is needed."
- Slice 1 "Tests" line 443: "missing adapter records retryable `missing_adapter` diagnostics and capped backoff without terminalizing the wait"

The policy choice (capped-delay indefinite retry, not terminalization) is explicit. Operator visibility through wait-row metadata and runtime diagnostics is specified. Residual owner (WU-WAIT-03 / provider lifecycle) and destination are recorded.

### Finding 4: Claim batch semantics are not all-or-nothing ambiguous

**Source**: AgentMiMo F02 (Low) — accepted by controller

**Verification**: **FIXED**

- Slice 1 "Exact changes" line 377: "Implement `claim_batch_size` as repeated single-row claim attempts up to `limit`, or as per-row isolated CAS attempts inside one write transaction. Successful row claims are returned independently; the batch is not all-or-nothing."

### Finding 5: close_drain_timeout_seconds behavior and durable-store close safety are explicit

**Source**: AgentMiMo F03 (Low) — accepted by controller

**Verification**: **FIXED**

- "Poll Loop Lifecycle / Close drain" lines 282-285: "If `close_drain_timeout_seconds` is set and the current poll round does not finish in time, treat the timeout as an operator-visibility threshold, not as permission to detach the poller from the Host close sequence. After the timeout fires, `close()` logs a structured error with the current diagnostics snapshot, keeps the close gate closed, continues waiting for the in-flight poll task or worker thread to finish, and does not return while that task/thread can still touch the durable store."
- Lines 285-286: "Because `open_host` closes the durable store only after `poller.close()` returns, this rule prevents durable store close while a background poll path can still run SQL."
- Lines 286-287: "If a synchronous adapter never returns, Host close may remain blocked; this is the explicit safety tradeoff for keeping the existing synchronous adapter contract."
- Lines 288: "Tests must cover normal drain and sleep cancellation."
- Slice 2 "Error handling" lines 533-534: "If `close_drain_timeout_seconds` fires, `close()` records/logs the timeout and continues waiting until the running poll task/thread is stopped; it must not return while the poller can still access the durable store."
- Slice 2 "Tests" line 549: "Close drain timeout records/logs a timeout and still prevents durable store close until the in-flight poll path has actually stopped."

### Finding 6: Shared per-wait backoff takeover drift tradeoff is documented

**Source**: AgentDS F04 (Low) — accepted by controller

**Verification**: **FIXED**

- "Backoff Policy" lines 306-308: "`poll_backoff_attempts` is shared per wait record across poller instances. Repeated process crash / claim-expiry / takeover cycles can make a wait reach `backoff_max_delay_seconds` faster than a per-process counter would, but the delay is bounded by `backoff_max_delay_seconds` and the shared state intentionally prevents process restarts from resetting a failing wait into a tight loop."
- "Residual Risks" line 692: "Shared per-wait backoff can reach maximum delay faster after repeated crash / claim-expiry / takeover cycles. Owner: WU-WAIT-02 Slice 1. Destination: bounded by `backoff_max_delay_seconds` and documented as intentional shared-throttling behavior."

### Finding 7: Abandon CAS rowcount 0 behavior is explicit

**Source**: AgentDS F05 (Low) — accepted by controller

**Verification**: **FIXED**

- Slice 1 "Error handling" lines 416-417: "Abandon CAS `rowcount == 0` means the row changed status, another path already handled it, or a newer claim exists. Treat it as skipped/conflict, do not assume abandon success, do not mark `poll_abandoned_at`, and rely on a later eligible round if the wait is still cancelled."
- Slice 1 "Tests" line 444: "abandon CAS conflict is treated as skipped and leaves later cancelled retry possible"

## New Findings Check

The plan-fix made textual clarifications and explicit specifications within the existing plan structure. No architectural changes were introduced. The following were verified:

1. **Data flow diagram consistency**: The Slice 1 data flow diagram (lines 391-407) was updated to show "atomic claim write" and includes the `rowcount 0` path. Consistent with the error handling text.

2. **Close sequence consistency**: The `open_host` close order (lines 258-263) places supervisor close before scheduler close. The close-drain timeout behavior (lines 282-287) specifies that `close()` does not return while the poll task can still touch the durable store. These are consistent — the durable store close happens after `poller.close()` returns, which only happens after the poll task finishes.

3. **Backoff policy consistency**: The backoff fields, policy, and tradeoff documentation are internally consistent. The shared-per-wait design is explicitly motivated by avoiding a separate claim table.

4. **Missing adapter policy consistency**: The "capped-delay indefinite retry" policy is consistently stated in Backoff Policy (line 310), Slice 1 Error handling (line 412), Slice 1 Tests (line 443), and Residual Risks (line 691).

No new material blockers were found.

## Architecture Boundary Verification

The plan-fix did not change any architecture boundaries:

- **Host vs Engine**: Poller still delegates to `resolve_wait`. No Engine changes.
- **UI/Service vs Host**: Poller remains internal Host runtime. No Service-facing APIs added.
- **Durable truth vs runtime diagnostics**: Claim/backoff fields remain durable. Diagnostics remain runtime-only.
- **Wait-row extension vs separate claim table**: Decision unchanged.

## Conclusion: **pass**

All 7 controller-accepted findings are verified as fixed in the plan. Each finding has:

- An explicit specification in the plan text (exact changes, error handling, data flow, or policy sections).
- A corresponding test assertion in the Slice 1 or Slice 2 test lists.
- Consistent treatment across related plan sections.

The plan-fix did not introduce new material blockers. No architectural changes were made. The plan is code-generation-ready.

| # | Finding | Source | Severity | Status |
|---|---|---|---|---|
| 1 | Terminal resolve clearing poll claim fields | AgentDS F01 | High | **fixed** |
| 2 | Atomic claim acquisition / rowcount 0 semantics | AgentMiMo F01 + AgentDS F02 | Medium | **fixed** |
| 3 | Missing adapter current-WU policy + residual owner | AgentDS F03 | Medium | **fixed** |
| 4 | Claim batch semantics clarity | AgentMiMo F02 | Low | **fixed** |
| 5 | Close drain timeout behavior + durable-store safety | AgentMiMo F03 | Low | **fixed** |
| 6 | Shared per-wait backoff drift tradeoff | AgentDS F04 | Low | **fixed** |
| 7 | Abandon CAS rowcount 0 behavior | AgentDS F05 | Low | **fixed** |

**New findings**: None.

**Blocking open questions**: None.

**Plan review conclusion**: **pass**
