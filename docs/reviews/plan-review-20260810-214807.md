# Plan Review — `WU-CLI-DOWNLOAD-03-DL-F15` Re-review

- **Review target**: `docs/gateflow/wu-cli-download-03-dl-f15-plan-20260810.md`（修订后）
- **Binding goal**: `docs/gateflow/wu-cli-download-03-dl-f15-goal-confirmation-20260810.md`
- **Adjudication**: `docs/gateflow/wu-cli-download-03-dl-f15-plan-review-adjudication-20260810.md`
- **Fix document**: `docs/gateflow/wu-cli-download-03-dl-f15-plan-review-fix-20260810.md`
- **Previous reviews**: DS review `docs/reviews/plan-review-20260810-214223.md`、MiMo review `docs/reviews/plan-review-20260810-214126.md`
- **Baseline**: `54dd750a2e300e943eb25d9e49c09d31145ef1fb`
- **Date**: 2026-08-10
- **Review artifact**: `docs/reviews/plan-review-20260810-214807.md`
- **Review posture**: re-review — 只验证原 findings 与裁决 PR-F1～PR-F4 的修复状态；不扩出新 scope

## Scope of This Re-review

本 re-review 仅做以下验证：
1. 逐项核对总控裁决 PR-F1～PR-F4 是否已按 Required fix 落实到修订后 plan
2. 判断修订后 plan 是否仍存在 blocker 或 goal drift
3. 给出每个原 finding 的最终状态与整体结论

不重新做全量 adversarial review，不扩出新 findings，不修改任何文件。

## PR-F1: 代码行号引用偏差（MiMo finding 3.1）— 已修复

- **裁决**: `accepted`
- **Required fix**: `538-602` → `510-602`
- **修订后 plan §4.2**: `dayu/documents/docling_runtime.py:510-602`：`run_docling_pdf_conversion(...)` ...
- **验证**: 行号已更正。`run_docling_pdf_conversion` 定义从 line 510、结束于 line 602，引用与 baseline 代码一致。
- **最终状态**: **已修复**

## PR-F2: coverage-supporting tests 过度强制 / 80% 可达性未预估（MiMo 3.2 + DS finding 1）— 已修复

- **裁决**: `accepted-in-part`
- **Required fix**: 将 §8.5/§9.2/§10 改成增量策略；候选 cases 从强制项改为有界 inventory，达到 80% 即停，不可达则 stop/回总控；不得扩大产品 diff、放宽阈值或绕过
- **修订后 plan 证据**:

  | 位置 | 修订前 | 修订后 |
  |---|---|---|
  | §8.5 首段 | "为满足已确认的...补齐该 owner 已公开且与 attempt 装配直接相关的稳定分支" | "implementation 必须先只完成 §8.4 两个核心 tests，并立即按 §9.2 测量...coverage baseline 与 missing lines。若 baseline 已达到 80%，本节候选 case 一个也不新增并立即停止；若不足，只能根据报告中的实际 missing lines，从下列有界候选集中选择能覆盖缺口的最小 case，一次补一个最小 case...达到 80% 后立即停止" |
  | §8.5 候选列表 | 无明确"可选"标记 | 明确标注 "有界候选 inventory 如下；它们只是在 baseline 低于 80% 时可选择的 owner cases，不是全部强制项" |
  | §8.5 末段 | "若上述 owner cases 仍不足 80%，只能增加同一文件内对现有分支的测试；禁止为了 coverage 改 production" | "若有界候选全部用尽后仍低于 80%，或剩余 missing lines 只能通过 §6.1 之外 test/product 文件...才能覆盖，立即停止并回总控；不得扩大产品 diff、放宽阈值、加 pragma: no cover、改 omit/source 配置或继续发明候选" |
  | §9.2 | 单次 `erase -> run -> report --fail-under=80` | 分两阶段：先 baseline（仅 §8.4 核心 tests + `--show-missing`），再按 missing lines 迭代补测，最后单独 `--fail-under=80` 门禁 |
  | §10 | 风险表中无 coverage 不可达条目 | 新增 "核心 tests 的 coverage baseline 低于 80% → 按 missing lines 从 §8.5 有界 inventory 逐个补最小 case，达到即停；若边界内不可达则 stop/回总控，不允许改 production、扩模块或绕过门禁" |
  | §11 | stop condition 无 coverage 不可达项 | 新增 "§8.5 有界候选无法达到 coverage 80%" 作为 stop condition |

- **验证**: 增量策略完整：先 baseline → 按 missing lines 选最小候选 → 逐轮重测 → 达到即停 → 不可达则回总控。候选明确降级为 inventory 而非强制清单。stop condition 覆盖了全部异常路径（扩产品、放宽阈值、pragma、omit/source bypass）。无 goal drift — coverage 仍是 required success signal，但实现路径从"强制全做"变为"增量最小补测"。
- **最终状态**: **已修复**

## PR-F3: 真实 download 可能引入非目标分类/文档失败（MiMo 3.3 + DS finding 2 + DS open question 2）— 已修复

- **裁决**: `accepted-in-part`
- **Required fix**: 保留 production `dayu-cli download`；verdict 只绑定目标文档；非目标 failure/分类差异只登记不扩修；阻断目标闭链则标 external evidence gap 并停止
- **修订后 plan 证据**:

  | 位置 | 关键修订 |
  |---|---|
  | §9.4 首段 | "正式真实 run 只能在 **accepted implementation commit** 创建后执行：从该 commit 建立 detached HEAD 的独立 clean validation environment，运行前记录 `git rev-parse HEAD` 精确等于 accepted implementation commit，并确认 `git status --porcelain` 为空。不得把未提交工作树...当作正式 target" |
  | §9.4 裁决规则 | "裁决规则只绑定本次选定的一个目标文档：首选 0700 Q3；仅在其 provider availability gap 时改用 0066 Q2" |
  | §9.4 目标通过条件 | 目标文档完成 conversion/publication，artifacts 同 identity 且完整，process 精确消费该 document ID；形成闭链 |
  | §9.4 非零 exit code | "若非目标 failure 单独导致命令非零，但目标闭链仍完整，不把该非零机械升级为 DL-F15 failure" |
  | §9.4 非目标处理 | "非目标文档 failure、分类差异或新的 provider/storage/runner observation 只登记直接证据与建议 owner，不进入修复、不扩大测试/产品 diff，也不改变目标 verdict。若这些非目标问题阻断目标文档形成上述 evidence 闭链，则本次正式补跑标为 `requiring explicit user decision` evidence gap，并停止扩修" |
  | §10 | 新增 "真实 run 出现不阻断目标闭链的非目标 failure/分类差异 → 只登记直接证据并分配建议 owner；不参与 DL-F15 target verdict，不在本 WU 修复" |
  | §10 | 新增 "provider 不再返回冻结的 0700 Q3，或非目标问题阻断目标闭链 → 先使用既有 0066 Q2...仍无法形成目标 evidence 时标为 `requiring explicit user decision` external evidence gap" |
  | §11 | 新增 "非目标真实 observation 诱发扩修" 与 "非目标问题阻断目标 evidence" 为 stop condition |

- **验证**: 目标 verdict 已从"download 整体成功"收窄为"目标文档闭链形成"。非目标行为的分流规则完整（登记不扩修、阻断标 gap、不改变目标 verdict）。Download 命令保留为 production 全量 pipeline，但证据 verdict 只绑定目标文档。无 goal drift。
- **最终状态**: **已修复**

## PR-F4: deterministic test 使用 fake converter（DS finding 3）— 证据失效

- **裁决**: `rejected-with-reason`
- **理由**: 测试替换 converter factory 但保留真实 attempt planner/runner/callback，精确拥有 stream lifecycle contract；真实 converter 组合链由用户要求的 production CLI run 覆盖。把真实重型 converter 引入 deterministic owner test 会增加外部依赖且不能稳定制造 first-attempt failure。
- **Required fix**: 无；implementation closeout 明确两类证据边界即可
- **修订后 plan §8.4**: 保持不变 — "factory 只替换 `build_docling_pdf_converter`，使真实 `_plan_conversion_attempts -> _build_attempt_converter -> run_docling_pdf_conversion -> convert callback` 保持在测试路径中"
- **验证**: 原 DS finding 3 的担心（fake converter 绕过真实 converter 构造路径）被总控裁决为不成立 — 理由充分：stream lifecycle 才是 contract owner，converter 构造是 coverage-supporting 的范围。修订后 plan 未因此扩 scope。
- **最终状态**: **证据失效**（原 finding 被裁决 rejected-with-reason，不再构成 risk 或需修复项）

## Goal Drift 检查

修订后 plan 与 goal confirmation 的 success signals 对比：

| Success signal | 修订后 plan 落点 | 是否漂移 |
|---|---|---|
| 首 attempt 关 stream 失败后第二 attempt 成功 | §8.4 test 1（不变） | 无 |
| auto 三档 stream identity 独立 + first cause/last exception | §8.4 test 2（不变） | 无 |
| production runner 不变 | §8.6 + §9.3 diff guard（不变） | 无 |
| 单文件 coverage ≥ 80% | §8.5 + §9.2（改为增量策略，阈值不变） | 无（路径优化，目标不变） |
| 真实 CLI evidence run | §9.4（增加 detached HEAD / target-specific verdict / non-target 分流，但仍保留 production download + process 路径） | 无（强化边界，目标不变） |
| 不扩展 DL-F12～F14、分类、Oracle | §4.3、§6.2、§9.4（不变，增强 non-target stop condition） | 无 |

修订全部属于 process clarification 与 risk bounding，未新增 goal、未放松验收标准、未扩大 scope。无 goal drift。

## Blocker 检查

- PR-F1～PR-F4 已全部按裁决落实或确认无修复需要
- 修订后 plan 的 production change (§8.3)、core tests (§8.4)、validation commands (§9.1-9.3)、README decision (§7) 的实质内容与原始 plan 一致
- 所有 stop conditions 完整且与裁决对齐
- `fix complete` 文档与修订后 plan 一致
- 无 blocking open question

## Final Plan Review Conclusion

**`pass`**

原 DS review 的三个 findings (PR-F2/PR-F3/PR-F4) 中：
- PR-F2（coverage 可达性）与 PR-F3（证据 run 非目标 diff）已按裁决完整修复
- PR-F4（fake converter）已被裁决 rejected-with-reason，证据失效
- PR-F1（行号引用）已修复

修订后 plan 无 blocker、无 goal drift、无未裁决 open question。plan 保持 code-generation-ready，可安全交给 implementation agent。
