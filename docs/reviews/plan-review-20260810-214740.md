# Plan Re-Review: WU-CLI-DOWNLOAD-03-DL-F15

## 1. Review Target

- **Plan**: `docs/gateflow/wu-cli-download-03-dl-f15-plan-20260810.md`（已修订）
- **Binding Goal**: `docs/gateflow/wu-cli-download-03-dl-f15-goal-confirmation-20260810.md`
- **Baseline**: `54dd750a2e300e943eb25d9e49c09d31145ef1fb`
- **Adjudication**: `docs/gateflow/wu-cli-download-03-dl-f15-plan-review-adjudication-20260810.md`
- **Fix Record**: `docs/gateflow/wu-cli-download-03-dl-f15-plan-review-fix-20260810.md`
- **Previous Review**: `docs/reviews/plan-review-20260810-214126.md`
- **Re-Review Date**: 2026-08-10
- **Re-Review Scope**: 验证原 review findings 与总控 PR-F1～PR-F4 是否已按裁决修复

## 2. 原 Review Findings 状态

### 2.1 Finding 3.1：代码行号引用偏差

- **原问题**: Plan §4.2 声称 `run_docling_pdf_conversion` 在 `538-602` 行，实际在 `510-602` 行
- **裁决**: PR-F1 `accepted`
- **修订后 plan §4.2**: `dayu/documents/docling_runtime.py:510-602`：`run_docling_pdf_conversion(...)` 为 `auto` 规划三档...
- **验证**: 行号已更正为 `510-602`，与 baseline 代码一致
- **最终状态**: **已修复**

### 2.2 Finding 3.2：Coverage Tests 是否 Goal-Bound

- **原问题**: §8.5 的 6 类 coverage-supporting tests 不直接验证 DL-F15 的核心 goal，可能导致 implementation agent 花费过多时间
- **裁决**: PR-F2 `accepted-in-part`
- **Required fix**: 将 §8.5/§9.2/§10 改成增量策略和 stop condition；保留候选 test inventory 作为有界选择集，不把它们全部设为强制
- **修订后 plan §8.5**:
  - "implementation 必须先只完成 §8.4 两个核心 tests，并立即按 §9.2 测量 `dayu/documents/docling_runtime.py` 的 coverage baseline 与 missing lines"
  - "若 baseline 已达到 80%，本节候选 case 一个也不新增并立即停止 coverage 补测"
  - "若不足，只能根据报告中的实际 missing lines，从下列有界候选集中选择能覆盖缺口的最小 case，一次补一个最小 case 或一个不可再拆的参数化 case，随即重测"
  - "达到 80% 后立即停止，不机械完成候选清单"
  - "若有界候选全部用尽后仍低于 80%，立即停止并回总控；不得扩大产品 diff、放宽阈值、加 `pragma: no cover`、改 omit/source 配置或继续发明候选"
- **修订后 plan §9.2**: 固定 baseline/增量/最终门禁命令，明确增量策略
- **修订后 plan §10**: 增加"核心 tests 的 coverage baseline 低于 80%"风险，处理方式为"按 missing lines 从 §8.5 有界 inventory 逐个补最小 case，达到即停；若边界内不可达则 stop/回总控"
- **验证**: 增量策略已落实，stop condition 已明确，有界候选 inventory 已列出但非强制
- **最终状态**: **已修复**

### 2.3 Finding 3.3：真实 Evidence Run 的分类风险

- **原问题**: §9.4 的真实 CLI run 会 download 和 process 真实文档，可能触发分类逻辑，产生与之前不同的结果
- **裁决**: PR-F3 `accepted-in-part`
- **Required fix**: 在 §9.4 明确 target-specific evidence verdict、non-target observation 处理与 stop condition
- **修订后 plan §9.4**:
  - "裁决规则只绑定本次选定的一个目标文档：首选 0700 Q3；仅在其 provider availability gap 时改用 0066 Q2"
  - "非目标文档 failure、分类差异或新的 provider/storage/runner observation 只登记直接证据与建议 owner，不进入修复、不扩大测试/产品 diff，也不改变目标 verdict"
  - "若这些非目标问题阻断目标文档形成上述 evidence 闭链，则本次正式补跑标为 `requiring explicit user decision` evidence gap，并停止扩修"
  - "目标文档再次出现 closed-stream、后续 attempt 无法读取同一 PDF 或目标 publication/consume 因本 Slice 行为失败，才属于 DL-F15 target failure"
- **验证**: target-specific verdict 已明确，非目标 observation 处理已落实，stop condition 已明确
- **最终状态**: **已修复**

## 3. 总控 PR-F1～PR-F4 状态

### 3.1 PR-F1：代码行号引用偏差

- **裁决**: `accepted`
- **Required fix**: 把 `538-602` 更正为 `510-602`
- **验证**: 修订后 plan §4.2 已更正为 `510-602`
- **最终状态**: **已修复**

### 3.2 PR-F2：Coverage-supporting tests 是否 goal-bound / 80% 可达性未预估

- **裁决**: `accepted-in-part`
- **Required fix**: 将 §8.5/§9.2/§10 改成增量策略和 stop condition；保留候选 test inventory 作为有界选择集，不把它们全部设为强制
- **验证**:
  - §8.5 已改为增量策略，有界候选 inventory 非强制
  - §9.2 已固定 baseline/增量/最终门禁命令
  - §10 已明确 allowed boundary 内不可达即 stop/回总控
- **最终状态**: **已修复**

### 3.3 PR-F3：真实 download 可能引入非目标分类/文档失败

- **裁决**: `accepted-in-part`
- **Required fix**: 在 §9.4 明确 target-specific evidence verdict、non-target observation 处理与 stop condition
- **验证**:
  - §9.4 已明确 target-specific verdict（只绑定 0700 Q3 或替代 0066 Q2）
  - 非目标 failure/分类差异只登记，不扩修、不改变 target verdict
  - 阻断目标闭链则标 external evidence gap 并停止
- **最终状态**: **已修复**

### 3.4 PR-F4：deterministic test 使用 fake converter

- **裁决**: `rejected-with-reason`
- **Required fix**: 无；implementation closeout 明确两类证据边界即可
- **验证**: 修订后 plan §8.4 保留 factory-only fake converter 边界，未引入重型 deterministic conversion
- **最终状态**: **已修复**（无需修改，plan 保持原样）

## 4. Blocker / Goal Drift 检查

### 4.1 Goal 一致性

- **Goal Confirmation**: "让一次 `convert_pdf_bytes_with_docling` 调用中的每个 Docling attempt 都从同一份 immutable `raw_bytes` 新建独立、仍可读的 `DocumentStream`"
- **修订后 plan §3.1**: "唯一目标是：一次 `convert_pdf_bytes_with_docling(...)` 调用中的每个 attempt，都从同一份 immutable `raw_bytes` 与同一个 `stream_name` 新建独立、初始可读的 `DocumentStream`"
- **验证**: Goal 一致，无漂移

### 4.2 Semantic Owner 一致性

- **Goal Confirmation**: `convert_pdf_bytes_with_docling` 的"immutable PDF bytes -> attempt-local DocumentStream"装配边界
- **修订后 plan §4.1**: `convert_pdf_bytes_with_docling` 是唯一装配 owner
- **验证**: Owner 一致，无漂移

### 4.3 Slice 边界一致性

- **修订后 plan**: 只有一个 Slice S1
- **Allowed files**: `dayu/documents/docling_runtime.py`、`tests/documents/test_docling_runtime.py`、`tests/README.md`
- **Production runner**: `cn_docling_process.py` 不修改
- **验证**: Slice 边界清晰，无过度耦合

### 4.4 Open Questions 收敛

- **原 review open questions**:
  1. Coverage requirement 的优先级 → 已由 PR-F2 裁决：增量策略，达到 80% 即停止
  2. 真实 evidence run 的时机 → 已由 PR-F3 裁决：在 accepted implementation commit 的 detached clean environment 运行
- **验证**: Open questions 已收敛

## 5. Final Plan Review Conclusion

**结论**: `pass`

所有原 review findings 已按裁决修复，总控 PR-F1～PR-F4 已全部落实。修订后的 plan：

1. 行号引用已更正（PR-F1）
2. Coverage tests 改为增量策略，有界候选 inventory 非强制，达到 80% 即停止（PR-F2）
3. 真实 evidence run 明确 target-specific verdict，非目标 observation 只登记不扩修（PR-F3）
4. Fake converter 测试边界保持原样（PR-F4 rejected-with-reason）

无 blocker，无 goal drift，plan 可安全交给 implementation agent。

## 6. Artifact Path

`docs/reviews/plan-review-20260810-214740.md`
