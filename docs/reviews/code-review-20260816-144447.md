# Code Review

## Scope

- Mode: current changes（re-review cycle）
- Branch: `codex/upload-filing-oracle`
- Base: `cc07db75`（HEAD, accepted plan commit）
- Output file: `docs/reviews/code-review-20260816-144447.md`
- 前序 reviews：`code-review-20260816-142141.md`（FAIL，8 findings，2 blocking）、`code-review-20260816-143020.md`（PASS，3 non-blocking）
- Included scope：
  - 原 Slice 2 四个 production files：`_fs_source_integrity.py`（新）、`_fs_source_document_core.py`、`_fs_source_snapshot.py`、`_fs_storage_infra.py`
  - Controller 授权的四个 owner-contract files：`repository_protocols.py`、`fs_batching_repository.py`、`_fs_filing_upload_state_core.py`、`fs_filing_upload_state_repository.py`
  - 三份 allowed tests：`test_fins_storage_atomicity.py`、`test_fins_storage_provider.py`、`test_processor_read_consistency.py`
  - Implementation artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice2-implementation-20260816.md`
- Excluded scope：无非预期文件变更
- Parallel review coverage：无；主 reviewer 逐项验证 Controller accepted findings A-E

## Controller Accepted Findings 闭环验证

### A：`commit_batch` Protocol/Facade typed Raises — ✅ 已闭环

- **FAIL review #1 问题**：`commit_batch` 抛 `SourceIntegrityPreflightError`（`RuntimeError` 子类）但 protocol/facade Raises 只声明 `ValueError`/`OSError`
- **root-cause 修复**：`repository_protocols.py:431-432` 新增 `SourceIntegrityPreflightError` 到 `commit_batch` Raises；`fs_batching_repository.py:73-74` 同步
- **直接证据**：git diff 两文件各增加 2 行 Raises 声明；protocol docstring 与 facade docstring 一致
- **验证点**：caller 按声明契约可预测 commit 失败类型，不再漏捕 `RuntimeError` 子类
- **结论**：闭环

### B：upload-state core/protocol/facade UNSAFE typed contract 且无 descriptor raw 旁路 — ✅ 已闭环

- **FAIL review #2 问题**：`_classify_source_integrity_unguarded` seam 通过 `_source_meta_path_for_read` 读取 damaged descriptor → raw `ValueError` 逃逸；state core docstring 仍声称抛 `ValueError`
- **root-cause 修复**：
  1. `_fs_filing_upload_state_core.py`：从 protocol 中移除 `_source_meta_path_for_read`；seam 签名从 `meta_path: Path` 改为 `ticker_dir: Path`
  2. `read_filing_upload_state` 传 `ticker_dir=target_dir` 而非 `meta_path=core._source_meta_path_for_read(...)` — identity descriptor 读取不再发生在 seam 之前
  3. inspector 内部处理 missing identity descriptor → 返回 `UNSAFE` typed classification
  4. core/protocol/facade 三处 docstring 均更新为「可归属 target 的结构损坏返回 `UNSAFE` typed state」
- **直接证据**：git diff `_fs_filing_upload_state_core.py` 移除 `_source_meta_path_for_read` 定义与调用；seam 传 `ticker_dir`；docstring 从「抛 ValueError」改为「返回 UNSAFE typed state」
- **验证点**：`test_filing_upload_state_fails_closed_for_missing_identity_descriptor_without_mutation` 从 `pytest.raises(ValueError)` 改为断言 `UNSAFE` typed state；`test_filing_upload_state_fails_closed_for_missing_source_meta_without_mutation` 同样断言 typed state
- **结论**：闭环。descriptor 缺失的 raw ValueError 旁路已被消除

### C：trusted manifest + local UNSAFE 不虚构 cross/shared reason 但真实 cross 仍拒绝 — ✅ 已闭环

- **FAIL review #6 问题**：单一 source 损坏 + trusted manifest → 旧代码产生 `CROSS_SOURCE_INCONSISTENCY` + `SOURCE_MANIFEST_UNTRUSTED`，reason 名不副实
- **root-cause 修复**：`_apply_manifest_facts` line 1134-1136 增加 `expected is not None` guard — 损坏 source 无法产生 canonical projection 时 `expected=None`，跳过比较，不加入 `mismatched_ids`
- **直接证据**：`_fs_source_integrity.py:1134` `if actual is None: continue` + line 1136 `if expected is not None and dict(actual) != dict(expected)` — damaged source 不参与 mismatch 判定
- **效果**：
  - 单一 source 损坏 + trusted manifest → `mismatched_ids` 为空 → inventory 不变，无 shared reason，仅保留 local UNSAFE
  - 真实 cross-source damage（多 source 损坏或 manifest dangling/conflict）→ 仍产生 `CROSS_SOURCE_INCONSISTENCY`
- **验证点**：test grid 中 `meta_missing`/`meta_malformed` 等 local UNSAFE 不再携带 `CROSS_SOURCE_INCONSISTENCY`
- **结论**：闭环

### D：whole-kind blocked reason 准确 — ✅ 已闭环

- **FAIL review #7 问题**：whole-kind mode 无 target 时，任一非 complete 即返回 `NON_TARGET_SOURCE_INCOMPLETE`，语义失真
- **root-cause 修复**：`_derive_repair_blocked_reason` lines 1284-1287 — whole-kind mode（`target is None`）增加专用分支：`canonical_manifest_items` 为空 → `CANONICAL_MANIFEST_UNAVAILABLE`；否则 `None`
- **直接证据**：`_fs_source_integrity.py:1284-1287` 新增 `if target is None:` 分支，不再落入 `NON_TARGET_SOURCE_INCOMPLETE`（该 reason 只在 exact-target + non-target incomplete 时返回，line 1289-1294）
- **验证点**：implementation doc 声称 direct payload tests 覆盖两分支
- **结论**：闭环

### E：revision field/helper 唯一 owner 且 snapshot lifecycle 不回退 — ✅ 已闭环

- **FAIL review #8 + PASS review #2 问题**：`_source_revision_from_meta`、`_source_meta_without_revision`、`_SOURCE_REVISION_META_FIELD` 在 integrity 和 infra 两个模块重复定义
- **root-cause 修复**：
  1. `_fs_storage_infra.py` 中三个定义已全部删除（grep 确认无结果）
  2. `_fs_source_integrity.py` 成为唯一 owner
  3. `_fs_source_snapshot.py:36` 从 `_fs_source_integrity` 导入 `_source_revision_from_meta`
  4. `_fs_source_document_core.py:39,41` 从 `_fs_source_integrity` 导入 `_SOURCE_REVISION_META_FIELD` 和 `_source_meta_without_revision`
  5. 无兼容 re-export
- **直接证据**：grep 确认 infra 不再定义这三个符号；snapshot 和 document core 的 import source 已改为 integrity module
- **snapshot lifecycle 验证**：`_acquire_snapshot_attempt_unguarded` 仍通过 inspection payload 获取 `complete.revision`；FD/marker/retry/cleanup 逻辑未修改
- **结论**：闭环

## FAIL Review #3-#5 与 PASS Review #1-#3 状态

| finding | 状态 | 说明 |
|---|---|---|
| FAIL #3 commit 诊断消息退化 | `covered by Slice 5` | Controller 裁决由 Slice 5 bounded failure projection 拥有 |
| FAIL #4 exact 扫描成本 | `residual / later work unit` | §6.1 冻结完整 inventory 扫描，性能优化需 plan 修订 |
| FAIL #5 snapshot containment 恒真 | `rejected-with-reason` | physical_path 由 inspector normalized name 构造，parent 即 document directory |
| PASS #1 marker retry 旧读取 | `covered by later slice / pre-existing` | Controller 要求保持 marker retry 与 resource lifecycle |
| PASS #2 revision 双定义 | `已修复` | 见 Finding E 闭环 |
| PASS #3 inventory 排序 | `rejected-with-reason` | 固定 kind tuple + inspector 内 document-ID sort 提供稳定顺序 |

## Findings

### 1-未修复-低-`_read_published_marker` 仍存在旧 meta 解析路径（pre-existing，Controller 已裁决）

- **入口/函数**: `_read_published_marker()` in `_fs_source_snapshot.py:945`
- **文件(行号)**: `_fs_source_snapshot.py:972-995`
- **输入场景**: full snapshot 的 marker retry 路径
- **实际分支**: 通过 `_source_meta_path_for_read` 独立解析 identity descriptor 与 meta path
- **预期行为**: marker retry 独立读取是设计选择；Controller 要求保持
- **实际行为**: identity descriptor 缺失时抛 raw `ValueError`，不经过完整性分类
- **直接证据**: `_fs_source_snapshot.py:972` 调用 `core._source_meta_path_for_read(...)`
- **影响**: marker retry 路径遇到 identity descriptor 缺失时抛 raw ValueError。但主 snapshot 路径已通过 inspector 返回 UNSAFE，且 Controller 已裁决保持
- **建议改法和验证点**: 后续 slice 若迁移 marker retry，需独立验证 retry 语义
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低（Controller accepted residual）

### 2-未修复-信息-`list_source_integrity` 排序依赖隐式循环顺序（Controller rejected-with-reason）

- **入口/函数**: `list_source_integrity()` in `_fs_source_document_core.py:525`
- **文件(行号)**: `_fs_source_document_core.py:530-542`
- **输入场景**: 所有 list_source_integrity 调用
- **实际分支**: `(SourceKind.FILING, SourceKind.MATERIAL)` 循环 + inspector 内部 `sorted(inspections_by_id)`
- **预期行为**: 返回按 `(source_kind, document_id)` 稳定排序
- **实际行为**: 功能正确——FILING 总在 MATERIAL 前，每个 kind 内按 document_id 排序
- **直接证据**: 旧代码显式 `sorted()` 已移除；新代码 `return tuple(inventory)` 依赖循环顺序
- **影响**: 功能无影响，契约不再显式编码
- **建议改法和验证点**: Controller 认为当前顺序已足够稳定
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 信息（Controller rejected）

## Open Questions

无。

## Residual Risk

- 旧 schema corpus 被 fresh inspector 拒绝（accepted plan residual）
- exact 扫描成本未量化（Controller residual，later work unit）
- commit 内部通用 ValueError 消息（Controller 裁决 Slice 5 拥有）
- marker retry 旧读取路径（Controller 要求保持）
- material existing-source repair 未授权（后续 work unit）
- 合法并发更新与 company warning（UF-FIX10/UF-FIX11）

## Verification Results

- **Tests**: `pytest tests/fins/test_fins_storage_atomicity.py tests/fins/test_fins_storage_provider.py tests/fins/test_processor_read_consistency.py -q` → **351 passed, 3 warnings**（up from 349 in first review cycle；3 warnings 均为 edgar deprecated import）
- **Pyright**: `python -m pyright dayu/ tests/ utils/` → **0 errors, 0 warnings, 0 informations**
- **`git diff --check`**: 通过
- **Scope guard**: 8 个 production files（4 original + 4 Controller-authorized）、3 份 tests、1 个 implementation artifact；两份 review artifacts 为 gate 输入
- **Protected files**: `docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`、`docs/host/design.md`、`docs/engine/design.md` 无 diff
- **README**: 无 diff（按 slice scope 不修改）
- **未执行**: UF-PF08、UF-PF12、真实 CLI evidence、registry/oracle 修改
- **未 commit/push/PR**

## Adversarial Attack Surface Summary

| 审查维度 | 结论 |
|---|---|
| commit_batch Raises 契约 | ✅ protocol/facade 均声明 `SourceIntegrityPreflightError` |
| upload-state UNSAFE typed contract | ✅ seam 改传 `ticker_dir` 避免 descriptor raw 旁路；docstring 同步 |
| trusted manifest + local UNSAFE 不虚构 reason | ✅ `expected is not None` guard 阻止 damaged source 参与 mismatch |
| whole-kind blocked reason | ✅ 专用 `CANONICAL_MANIFEST_UNAVAILABLE` 分支 |
| revision field/helper 唯一 owner | ✅ infra 定义已删除，integrity module 唯一拥有 |
| snapshot lifecycle | ✅ FD/marker/retry/cleanup 未回退 |
| `_fs_source_integrity.py` 是否真为唯一 owner | ✅ 37 函数，无 `Any/object/getattr/hasattr` |
| 18 reasons precedence | ✅ enum 声明顺序，REPAIR_REQUIRED/UNSAFE 互斥 |
| exact/whole 同 scan | ✅ 测试证明一致 |
| 测试覆盖 vs 自证 fixture | ✅ 真实 filesystem state，351 passed |
| scope/docstring/strict typing | ✅ 全部函数有完整中文 docstring，严格类型 |

## PASS/FAIL

**PASS** — Controller accepted findings A-E 全部 root-cause 闭环。FAIL review 的 2 个 blocking findings（#1 commit_batch Raises、#2 upload-state behavior/docstring drift）已通过 protocol/facade/core docstring 同步和 seam 重构消除。FAIL review 的 non-blocking findings #3-#5 与 PASS review 的 #1-#3 均已按 Controller 裁决处理（accepted/residual/rejected）。351 tests passed，pyright 0 errors，diff-check 通过。
