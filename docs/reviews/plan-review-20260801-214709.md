# Plan Review: WU-CLI-INTERACTIVE-02 S5/F13 Plan Amendment Proposal

## 1. Review target 与范围

- **Reviewed artifact**: `docs/reviews/wu-cli-interactive-02-s5-f13-plan-amendment-proposal-codex.md`
- **Target plan**: `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md` 的 S5 allowed-file / validation closure
- **Review scope**: amendment proposal 的动机、inventory 声称、变更清单、non-goals、validation 与 residual risks
- **Base HEAD**: `331d38dcaeebe3a929b7fa52d4e161a1c6504c55`（已验证一致）
- **生成时间**: `2026-08-01T21:47:09+08:00`

## 2. Assumptions tested

| # | Assumption | 验证方式 | 结论 |
|---|---|---|---|
| A1 | HEAD 与 proposal 声称的 base 一致 | `git rev-parse HEAD` | ✓ `331d38dcaeebe3a929b7fa52d4e161a1c6504c55` |
| A2 | FinalAnswerData 在 tests 中有 35 个直接构造、分布在 19 个文件 | `rg -l --glob '*.py' '\bFinalAnswerData\s*\(' tests \| wc -l` + `rg -n ... \| wc -l` | ✓ 35 calls / 19 files |
| A3 | EngineRunOutcomeFinalAnswer 在 tests 中有 4 个直接构造、分布在 3 个文件 | `rg -l ...` | ✓ 4 calls / 3 files |
| A4 | ContextCompactor 相关在 tests 中有 7 个文件 | `rg -l ...` | ✓ 7 files |
| A5 | 三类去重 union 为 25 个文件 | `sort -u` on all 3 searches | ✓ 25 unique files |
| A6 | 原 §9.1 owner-level 测试清单覆盖 5 个文件 | 人工比对 plan §9.1 与 proposal §2.2 的"已允许"标记 | ✓ 5 个：test_engine_event_contract、test_compaction_operation、test_dispatch_scheduler、test_engine_ingest_mapping、test_llm_compaction |
| A7 | 遗漏 20 个文件 | 25 - 5 = 20 | ✓ |
| A8 | proposal §2.2 表格中的文件分类（FA/OA/CR）与 HEAD 实际分布一致 | 逐文件 rg 验证 | ✓ 全部匹配 |
| A9 | `ContextCompactor` 是 Protocol、`FakeContextCompactor` 实现该 Protocol、`FakeConversationCompactorVNext` 不实现该 Protocol | 代码直接读取 | ✓ 架构与 proposal §3.2 的 candidate-only owner 描述一致 |
| A10 | 未发现 alias 构造或 mock-configured compactor return | `rg` alias/mock scan | ✓ |

## 3. Findings

无 material findings。

## 4. Open questions

无 blocking open question。

以下为非阻塞澄清点，不影响 proposal 通过：

**OQ-1: `FakeConversationCompactorVNext` 的 identity 来源**

proposal §9.3 声称 `FakeConversationCompactorVNext` 仍只拥有 vNext candidate、不伪造 Engine identity。当前代码确认该类不实现 `ContextCompactor` Protocol，只返回 `ConversationCompactOutputVNext`。F13 后 `FakeContextCompactor.compact()` 需返回 `CompactorProposal`（candidate + identity），identity 由 `FakeContextCompactor` 层构造。这是 §3.2 所述"candidate-only 的 `FakeConversationCompactorVNext` 仍只拥有 vNext candidate"的精确语义，无需修改 proposal。

建议 implementation agent 在 §9.1 mechanical closure 中明确：`FakeContextCompactor` 在调用 `FakeConversationCompactorVNext().compact()` 后、返回 `CompactorProposal` 前，构造安全 test identity 并包装。

## 5. Residual risks

| 风险 | 分类 | 处理 |
|---|---|---|
| amendment 后 HEAD 新增 direct constructor / compactor fake | `requiring explicit controller decision` | proposal §3.1 point 7 已要求 S5 implementation 前重扫；新 hit 即停止并再次 amend |
| test-only safe identity 无法证明真实 provider continuity | `covered by later approved slice` | proposal §4 non-goals 明确保持行为项 29/G06 外部证据边界 |
| broad Host/Engine tests、pyright、coverage 尚未运行 | `covered by later approved slice` | 本 gate 禁止 implementation；由 amended §10.5 在获批 S5/S6 执行 |
| S4/S5 cross-slice 共享 7 个测试文件 | `no conflict` | S4 增加 terminal guard tests，S5 增加 required identity 字段；两者独立、S4 先完成 |
| 25 个 mechanical closure 文件中部分（如 `_PreparedManifestReactiveCompactor`）可能需要非平凡适配 | `low risk` | proposal §9.1 已限制为"补齐 required typed value、解包/保留同一个 proposal identity 和随 contract 迁移类型标注"，禁止改变测试场景和行为断言 |

没有 unclassified residual risk。

## 6. Plan review 结论

**pass**

Proposal 的动机成立、严重性评估准确。inventory 声称经独立 repo-wide rg 验证完全一致（35/19、4/3、7、25/5/20）。变更清单精确、non-goals 完整、validation 策略全面。§3.2 的同源 test identity 规则语义清晰且与实际代码架构一致。§9.1 的机械改动边界约束充分，能防止 implementation agent 借机引入 optional/default/compatibility seam。

Proposal 可以交给 implementation agent。

## 7. Output artifact

- `docs/reviews/plan-review-20260801-214709.md`
