# Code Review — PR 190 F11/F12 S3 DeepSeek Implementation Review

## Scope

- Mode: current changes (S3 implementation artifact)
- Branch: `codex/interactive-oracle`
- Base HEAD: `1943904eea9e30357805c9f1d2b6f6e815b37c86`（feat(engine): add typed structured output contract）
- Output file: `docs/reviews/pr-190-f11-f12-s3-ds-review-20260805.md`
- Included scope: 完整 git diff（47 files, +3033/-3179），覆盖 `dayu/host/` 全部 compact 相关 production 模块、`dayu/cli/init_catalog.py`（S2 cross-slice fix）、两个 compactor prompt asset、design docs、全部受影响的 Host/CLI/Service/Assembly tests 与 `utils/` smoke harness
- Excluded scope: `docs/gateflow/` 下 plan/checkpoint/scope-correction/implementation artifact 为 plan 真源而非实现变更；`.gitignore`、`pyproject.toml` 等未变更文件
- Parallel review coverage: 四个 Explore subagent 分别覆盖 v2 cleanup scan / prompt 审查 / structure descriptor 审查 / accept owner & governance 审查；主 reviewer 已复核全部 subagent 覆盖区域并补充直接走读

## Review Method

本 review 沿以下路径逐行走读：

1. `compact_structure.py`（新文件，699 行）→ 唯一 output structure descriptor owner
2. `compaction.py`（3804 行）→ typed domain contract owner
3. `context_governance.py`（769 行）→ accept owner
4. `llm_compaction.py`（841 行）→ Host compactor / prompt rendering
5. `compact_payload.py` / `compact_artifact.py` / `context_events.py` → canonical event/artifact strict boundary
6. `memory.py` / `run_input.py` / `dispatch.py` / `engine_ingest.py` → downstream consumers
7. 两个 compactor prompt asset（`conversation_compaction.md` 822 bytes, `conversation_compaction_user.md` 3337 bytes）
8. `init_catalog.py` → S2 cross-slice fix
9. 全部受影响的 tests/fakes/fixtures/smoke harness

每个审查维度均从真实入口/调用链追踪数据流，包含 adversarial failure pass 与 semantic ownership drift pass。

---

## Findings

### 001-PASS-v2-完全删除无生产残留

- **入口/函数**: 全局 production code scan
- **文件(行号)**: `dayu/host/` 全部 14 个 production 文件 + `dayu/cli/init_catalog.py`
- **输入场景**: 扫描 v2 schema literal（`dayu.context_compaction.output.v2`、`input.v2`）、旧字段名（`diagnostics`、`explicitly_dropped_sources`、`drop_reason`、`omission_kind`）、v2 typed symbol（`CompactAcceptedTruthV2`、`CompactCandidateV2`、`CompactInputV2` 等）、兼容 alias/re-export/wrapper
- **直接证据**:
  - 全量 `grep` 扫描：production 代码中 v2 schema literal、`diagnostics`、`explicitly_dropped`、`drop_reason`、`omission_kind`、`dropped_sources` 均为 0 matches
  - `compaction_operation.py` diff 明确展示全部 `V2` → `V3` typed import 替换（`CompactAcceptedTruthV2` → `CompactAcceptedTruthV3`、`accept_compact_candidate_v2` → `accept_compact_candidate_v3` 等）
  - `context_governance.py` diff 明确展示删除 `CompactCandidateDiagnosticV2`、`CompactDropReasonV2`、`CompactExplicitlyDroppedCoverageV2` 的 import 与全部类型/逻辑使用
  - 旧 `_represented_sections()` private helper 替换为 `derive_compact_represented_sections_v3()`
  - 旧 `_collect_coverage_issues()` + `drops_by_label` / `explicitly_dropped_sources` 逻辑替换为 omitted complement
  - `compact_structure.py` 是新文件，从头只定义 v3
  - `_OLD_REPAIR_FEEDBACK_MARKER = "PREVIOUS_VALIDATION_REPORT_JSON"` 仅存在于 `tests/host/test_llm_compaction.py:63` 作为负断言，production 无此 marker
- **影响**: 无
- **严重程度**: PASS

### 002-PASS-S2-Custom-model-capability-修复正确

- **入口/函数**: `_build_custom_openai_record()`
- **文件(行号)**: `dayu/cli/init_catalog.py:781`；`tests/cli/test_init_catalog.py:662`
- **输入场景**: 用户通过 `dayu-cli init` 创建 Custom OpenAI-compatible 动态模型记录
- **直接证据**:
  - Production fix: `init_catalog.py:781` 显式写入 `"structured_output_capability": StructuredOutputCapabilityConfig.NONE.value`（即 `"none"`）
  - Owner test: `tests/cli/test_init_catalog.py:662` 同时断言 raw JSON `"none"` 与 resolved typed enum `NONE`
  - ConfigLoader 未增加默认值、兼容 fallback 或 provider 名称推断
  - S3 implementation artifact 记录的最终 full suite `6696 passed` 确认四条原失败测试已定点通过
- **影响**: 无
- **严重程度**: PASS

### 003-PASS-structure-descriptor-同源派生

- **入口/函数**: `compact_output_template_v3()` / `compact_output_json_schema_v3()` / `compact_output_prompt_rules_v3()` / `parse_compact_candidate_v3()`
- **文件(行号)**: `dayu/host/compact_structure.py`
- **输入场景**: 任意调用方请求 template、formal JSON Schema、prompt rules 或 candidate parsing
- **直接证据**:
  - 唯一 immutable descriptor tree：`_ROOT` → `_SUMMARY`, `_FACT`, `_ANCHOR`, `_INTENT`, `_REFERENCE`（均为 module-level frozen dataclass instances，`compact_structure.py:75-155`）
  - `compact_output_template_v3()`（行 158）→ `_template_object(_ROOT)` 机械生成
  - `compact_output_json_schema_v3()`（行 168）→ `_schema_object(_ROOT)` 机械生成，所有 object 设 `additionalProperties=False`、所有字段在 `required` 中
  - `compact_output_prompt_rules_v3()`（行 178）→ `_prompt_rules_object(_ROOT)` 机械生成，不含 `type: object`、`properties`、`required`、`additionalProperties` 等 formal JSON Schema 关键字树
  - `parse_compact_candidate_v3()`（行 200）→ 使用 `object_pairs_hook=_strict_object_pairs` 拒绝 duplicate key，对 root 与所有 nested object 调用 `_exact_object()` 执行 exact-key 校验
  - JSON Schema name 固定为 `dayu_context_compaction_output_v3`（行 30）
  - `compact_output_json_schema_digest_v3()`（行 191）→ 消费同一次 `compact_output_json_schema_v3()` projection
  - 每次 public API 调用返回 fresh value（`_template_object` / `_schema_object` 内用 dict comprehension 构造新 dict），调用方 mutation 不污染 owner-held structure
  - Owner test: `test_compact_structure_projections_are_fresh_and_digest_is_stable`（`test_compaction_contract.py:141`）验证调用方修改返回值后 fresh projection 不受污染
- **影响**: 无
- **严重程度**: PASS

### 004-PASS-strict-parser-完备

- **入口/函数**: `parse_compact_candidate_v3()` / `parse_conversation_compact_output_vnext()`
- **文件(行号)**: `dayu/host/compact_structure.py:200-253`；`dayu/host/llm_compaction.py:775-796`
- **输入场景**: LLM 返回的任意 JSON 文本
- **直接证据**:
  - Duplicate key: `_strict_object_pairs`（行 401-416）使用 `object_pairs_hook` 在 dict 构造前拒绝重复 key（每个 key 重复立即 `ValueError`）
  - Unknown key: `_exact_object`（行 419-444）对每个 object 比较 `expected`（descriptor fields）与 `actual`（JSON keys），unknown key 立即 reject（`unknown_json_key: {path}.{first_unknown}`）
  - Missing key: 同上，missing key 立即 reject（`missing_required_key: {path}.{first_missing}`）
  - Type check: `_required_text`（行 447-468）校验 `isinstance(value, str)` 且非空；`_required_array`（行 470-488）校验 `isinstance(value, list)`；`_required_text_tuple`（行 491-520）对 array 每项校验 string、非空、唯一
  - Enum check: `_parse_intent`（行 625-658）对 `status` 文本调用 `CompactForwardIntentStatusV3(status_text)`，非法值 `ValueError` → `invalid_enum_value`
  - Duplicate source label: `_required_text_tuple`（行 515-516）拒绝同一 array 内重复项
  - Cap check: 不属于 parser 层，由 accept owner 的 `_collect_policy_issues` 执行
  - `session_summary` null: `_parse_summary`（行 541-563）在 value 为 `None` 时返回 `None`（key 存在但值为 null）；key 缺失时 `_exact_object` 直接 reject
  - Owner tests 覆盖全部反例：`test_strict_parser_rejects_duplicate_key_at_every_object_shape`（6 parameterized variants）、`test_strict_parser_rejects_unknown_missing_type_enum_and_blank`（8 parameterized variants）、`test_strict_parser_rejects_invalid_json_and_unsupported_shape`
- **影响**: 无
- **严重程度**: PASS

### 005-PASS-LLM-facing-prompt-自足且无治理术语

- **入口/函数**: `_user_prompt_vnext()` / `conversation_compaction.md` / `conversation_compaction_user.md`
- **文件(行号)**: `dayu/host/llm_compaction.py:678-772`；`dayu/config/prompts/scenes/conversation_compaction.md`（14 行，822 bytes）；`dayu/config/prompts/scenes/conversation_compaction_user.md`（37 行，3337 bytes）
- **输入场景**: compactor LLM 调用的 system/user prompt 渲染
- **直接证据**:
  - System prompt（`conversation_compaction.md`）只有 14 行纯业务指令，不含 Host 治理术语、digest、cursor、EventLog、Attempt、scheduler、compact operation、policy audit、omitted coverage
  - User prompt（`conversation_compaction_user.md`）37 行，自足说明字段名、含义、类型、必填性、允许值、nullable、来源规则；通过 `<<compact_output_rules>>` 占位符注入从 descriptors 机械派生的 concise rules（不含 formal JSON Schema 关键字树），通过 `<<compact_output_template>>` 占位符注入同源 concrete template
  - 占位符机制：`llm_compaction.py:212-222` 强制要求 user_prompt_template 包含恰好一个 `<<compaction_request>>`、一个 `<<compact_output_rules>>`、一个 `<<compact_output_template>>` 占位符
  - `_user_prompt_vnext`：initial 路径不注入 repair protocol（`repair_feedback=None` 时无 `REPAIR_FEEDBACK_JSON_BEGIN`/`END` 块）
  - Repair 路径：`llm_compaction.py:714-729` 追加 repair block，包含 `previous_attempt_number`、`required_action`（完整重产要求）、bounded issues（去除了 `request_digest` 和 `source_boundary_digest` field）
  - `_repair_feedback_prompt_json_vnext`（行 733-756）只投影 `required_action` 和 `issues`，明确排除 `request_digest`、`source_boundary_digest`、`previous_attempt_number`、`additional_issue_count` 等 Host 内部字段
  - Owner test `test_prompt_assets_keep_initial_contract_compact_and_self_contained`（行 524-588）验证：rendered prompt 不含 `additionalProperties`、`$schema`、`type: object`、`request_digest`、`source_boundary_digest`、`Host`、`omitted coverage`、`policy audit`、`策略用量`、旧 repair marker
  - Owner test `test_repair_feedback_is_separate_and_requires_whole_candidate`（行 406-521）验证：initial 无 repair block；repair 含 repair block 且不含 internal digest value/field name；所有 secret/password 均被 redacted；bounded 在 32/240/8192 内
  - Prompt size metrics: system prompt 2510→822 bytes（-67%），user prompt 13919→3337 bytes（-76%），model-owned fields 从 8→6 keys
- **影响**: 无
- **严重程度**: PASS

### 006-PASS-accept-owner-覆盖分区与审计同源-fail-closed

- **入口/函数**: `accept_compact_candidate_v3()` / `_validate_committed_coverage()` / `validate_compact_policy_usage_audit_candidate_binding_v3()` / `validate_compact_represented_coverage_candidate_binding_v3()`
- **文件(行号)**: `dayu/host/context_governance.py:92-149`；`dayu/host/compact_payload.py:410-436`；`dayu/host/compaction.py:1658-1699`、`dayu/host/compaction.py:1756-1779`
- **输入场景**: strict candidate 通过 structure parser 后进入 Host accept barrier
- **直接证据**:
  - **唯一 accept owner**: `accept_compact_candidate_v3`（`context_governance.py:92`）是唯一构造 `CompactAcceptedTruthV3` 的函数（使用私有 `_COMPACT_ACCEPTANCE_PERMIT`）；`CompactAcceptedTruthV3.__post_init__`（`compaction.py:1994-2012`）强制校验 `_permit is _COMPACT_ACCEPTANCE_PERMIT`，外部构造直接 `ValueError`
  - **同一 Memory policy**: `accept_compact_candidate_v3` 先通过 `compact_output_caps_v3_from_memory_policy(memory_policy)` 投影 expected caps（行 112），再 `ValueError` 拒绝 `compact_input.output_caps != expected_caps`（行 113-114）；`_policy_usage_audit`（行 499-538）使用同一 `policy` instance 和同一 `derive_compact_policy_usage_actuals_v3` 派生 actual/cap
  - **Coverage partition**: `derive_compact_represented_sections_v3`（`compaction.py:1702-1753`）从 candidate provenance 单一派生 represented sections；omitted 由 `accept_compact_candidate_v3`（行 136-140）按 root boundary 顺序计算 complement（在 represented 中不存在的 entry）；`CompactAcceptedTruthV3.__post_init__` 强制校验 intersect 为空、union equals boundary、omitted 顺序跟随 boundary
  - **Committed coverage 重验**: `_validate_committed_coverage`（`compact_payload.py:410-436`）从 committed payload 恢复 coverage 后重验 represented/omitted 顺序、disjoint、union equals boundary、`current_input_ref` 不在 `compacted_source_refs` 中，并调用 `validate_compact_represented_coverage_candidate_binding_v3` 验证 committed represented 与 persisted candidate exact 同源
  - **Policy audit 重验**: `CompactArtifactWriteRequest.__post_init__`（`compact_artifact.py:67-71`）调用 `validate_compact_policy_usage_audit_candidate_binding_v3` 和 `validate_compact_represented_coverage_candidate_binding_v3`；`compact_payload.py:109` 的 `parse_context_compacted_semantic_payload` 也调用前者
  - **Memory 同源**: `memory.py:1673` 的 durable estimator 调用 `validate_compact_policy_usage_audit_candidate_binding_v3`
  - **Fail closed**: `validate_compact_policy_usage_audit_candidate_binding_v3`（`compaction.py:1671-1699`）九项 actual 逐一与 `derive_compact_policy_usage_actuals_v3` 比较（任一不等 `ValueError`），再逐项 actual ≤ cap 检查（任一超 cap `ValueError`）；没有静默接受或 fallback
  - **字符计量同源**: `compact_text_size_units_v3`（`compaction.py:1608-1618`）使用 Python `len(text)`；`estimate_memory_size_units`（`memory.py:1035`）复用同一个 `compact_text_size_units_v3`
  - **Downstream single source**: `dispatch.py:180`、`engine_ingest.py:111`、`run_input.py:311` 均 import `CompactAcceptedTruthV3`，不自行重算 coverage/caps/semantic replacement
  - Owner tests: `test_accept_owner_derives_exact_coverage_and_canonical_label_order`、`test_accept_owner_derives_omitted_complement_for_committed_round_trip`、`test_policy_usage_audit_derivation_and_validation_fail_closed`（验证 actual tampered down / cap tampered down 均 `ValueError`）、`test_compact_artifact_write_rejects_candidate_audit_mismatch`、`test_compact_artifact_write_rejects_candidate_coverage_mismatch`
- **影响**: 无
- **严重程度**: PASS

### 007-PASS-successful-response-identity-保留

- **入口/函数**: `run_prepared_compactor_proposal()` / `_validated_prepared_response_identity()`
- **文件(行号)**: `dayu/host/llm_compaction.py:331-390`、`dayu/host/llm_compaction.py:393-430`
- **输入场景**: LLM 返回成功 final answer 后被 parse → reject 或 accept
- **直接证据**:
  - `run_prepared_compactor_proposal`（行 372-386）：parse 产生 `LLMCompactionValidationError` 时通过 `exc.report` 保留原 `response_identity`（行 378-381）；parse 产生 `LLMCompactionProposalError` 时保留（行 383-386）
  - `LLMCompactionProposalError.__init__`（行 105-111）显式携带 `successful_response_identity: SuccessfulRunnerResponseIdentity | None`
  - `LLMCompactionValidationError.__init__`（行 122-143）同样携带
  - `_validated_prepared_response_identity`（行 393-430）验证：Engine run id 匹配 `compactor_engine_run_id`、不携带普通 attempt/execution id、effective provider/model 匹配 RunnerSpec
  - Non-final outcome（行 358-362）与 timeout（行 352-357）均 `successful_response_identity=None`
  - `finish_reason=length`（行 367-371）携带 response_identity
  - Owner test: `test_structured_output_transport_is_selected_only_by_typed_capability` 验证 capability 三态不依赖 provider 名称
- **影响**: 无
- **严重程度**: PASS

### 008-PASS-测试-owner级别覆盖且含反例

- **入口/函数**: 全部 Host/CLI test files
- **文件(行号)**: `tests/host/test_llm_compaction.py`（841 行，16 test functions）、`tests/host/test_compaction_contract.py`（667 行，21 test functions）、`tests/host/test_public_compact_smoke.py`、`tests/host/test_compact_artifact_store.py`、`tests/host/test_compact_pipeline.py`、`tests/host/test_compaction_terminal.py`、`tests/cli/test_init_catalog.py` 等
- **输入场景**: 全量测试 suite
- **直接证据**:
  - **Owner-level assertions**: 测试断言 typed owner 的 contract 行为（typed type rejects、exact schema literal、caps 无 default、template/schema/rules/parser 同源、coverage partition exact、audit derivation fail closed、input binding mismatch fail closed），不测试间接 consumer 的 fallback 行为
  - **Negative test coverage（反例）**:
    - Duplicate key: `test_strict_parser_rejects_duplicate_key_at_every_object_shape`（6 parameterized variants 覆盖 root/summary/fact/anchor/intent/reference 每个 object shape）
    - Unknown/missing/type/enum/blank: `test_strict_parser_rejects_unknown_missing_type_enum_and_blank`（8 parameterized variants）
    - Label existence/kind/duplicate: `test_accept_owner_rejects_deterministic_invalid_matrix`（≥6 parameterized variants）
    - Item cap: `test_each_memory_section_item_cap_accepts_equal_and_rejects_plus_one`（4 parameterized variants）
    - Size cap: `test_each_memory_section_size_cap_accepts_equal_and_rejects_plus_one`（4 parameterized variants）
    - Audit mismatch: `test_policy_usage_audit_derivation_and_validation_fail_closed`（actual↓ / cap↓ 均 `ValueError`）
    - Coverage mismatch: `test_compact_artifact_write_rejects_candidate_coverage_mismatch`
    - Schema/type/binding reject: `test_v3_typed_contract_rejects_invalid_nested_types_and_bindings`（10+ reject cases）
    - Duplicate/contradiction: `test_duplicate_and_schema_provable_contradiction_are_rejected`
    - Forbidden old fields: `test_compacted_semantic_parser_rejects_unknown_old_candidate_field`、`test_compacted_payload_rejects_old_summary_proposed_verified_fact_refs`
    - Terminal invariant: `test_two_competing_terminal_writers_commit_exactly_one_canonical_terminal`、`test_multiple_terminals_fail_closed_without_inventing_third_truth`
    - Prompt injection: `test_adversarial_material_is_preserved_inside_static_untrusted_boundary`（4 parameterized injection locations）
    - Secret safety: `test_secret_bearing_duplicate_key_report_and_repair_feedback_are_safe`
    - S2 capability fix: `tests/cli/test_init_catalog.py` 同时断言 raw JSON `"none"` 与 typed enum `NONE`
  - **Coverage**: 所有 production 文件 branch coverage ≥80%（80.65%–89.66%），full suite `6696 passed, 0 errors`
- **影响**: 无
- **严重程度**: PASS

### 009-[低]-compactor_input_projection 版本号与 v2 语义混淆

- **入口/函数**: `_compactor_input_projection_json()`
- **文件(行号)**: `dayu/host/llm_compaction.py:88`
- **输入场景**: compactor input projection artifact 被持久化时写入 `schema_version` 字段
- **实际分支**: `_COMPACTOR_PROJECTION_SCHEMA_VERSION = "compactor_input_projection.v2"` 是新增常量（本 PR 首次引入），用于标记 projection artifact
- **直接证据**: 行 88 定义了该常量，行 634 写入 projection JSON 的 `schema_version` 字段。该版本号与旧 compact output v2 无关（属于独立版本空间），但在全量 v2 清理的上下文中，任何刚做完 v2→v3 迁移的读者都会将其误读为遗留 v2
- **预期行为**: 若这是全新引入的 projection format，应使用 `.v1`；若确有兼容原因需要用 `.v2`，应加显式注释说明该版本与旧 compact v2 contract 无关
- **影响**: 代码可读性与维护混淆；不影响正确性
- **建议改法和验证点**: 改为 `compactor_input_projection.v1`，或添加注释 `# 独立于旧 compact output v2 的版本空间`。验证：确认 projection artifact reader 同步更新
- **修复风险（低）**: 需确认 artifact reader 是否依赖该 version string
- **严重程度（低）**:

### 010-[低]-LLM-facing-prompt-暴露-Host-治理概念-"覆盖账本"

- **入口/函数**: `_user_prompt_vnext()` 注入的 prompt 文本
- **文件(行号)**: `dayu/config/prompts/scenes/conversation_compaction_user.md:34`
- **输入场景**: compactor LLM 调用时渲染的 user prompt
- **实际分支**: prompt 文本中写有 `不要输出覆盖账本或省略原因；只输出需要保留的五类业务内容及其真实来源引用，未引用材料无需单列。`
- **直接证据**: "覆盖账本" 直接对应 Host governance 概念 "coverage ledger"，属于 design doc §24.2 明确禁止暴露给 LLM 的 Host-owned fact（`represented coverage、omitted coverage 与 policy usage audit 都必须由 Host ... 确定性派生；不得把这些 Host-owned facts 伪装成模型输出要求`）。虽然此处是禁止性指令（告诉模型不要输出），但术语本身仍然命名了 Host 内部抽象
- **预期行为**: 用 LLM-facing 的业务语言替换，例如 `不要输出"已覆盖 X 项"或"省略了 Y 项"等统计说明；只输出需要保留的五类业务内容及其真实来源引用，未引用材料无需单列。`
- **影响**: 模型可能从 prompt 中的概念名推断出 Host 内部存在覆盖追踪机制，但因为是禁止性指令，实际影响不大
- **建议改法和验证点**: 替换 "覆盖账本" 为业务可读的禁令表述；验证 prompt asset 全文不再出现 Host 治理概念
- **修复风险（低）**: 仅改 prompt asset 文本，不涉及代码逻辑
- **严重程度（低）**:

### 011-[低]-repair-body-暴露-attempt-计数术语

- **入口/函数**: `_user_prompt_vnext()`
- **文件(行号)**: `dayu/host/llm_compaction.py:721-722`
- **输入场景**: repair 路径渲染 prompt 时追加 attempt 信息
- **实际分支**: `前次 attempt number：` + `str(repair_feedback.previous_attempt_number)` 直接出现在 LLM-facing prompt 中
- **直接证据**: "attempt" 虽非大写 Host 类型 `Attempt`，但在本项目的语义体系中 `attempt` 是 Host 治理术语（design doc §25 反复使用 `attempt`、`compaction attempt`、`max_compaction_attempts_per_operation` 等）。模型本不需要知道这是第几次尝试，只需要知道"前一次输出未通过校验，需要基于同一输入重新生成"
- **预期行为**: 替换为 `前次输出编号：` 或不暴露序号
- **影响**: 低。模型可能根据 attempt number 推断 Host 内部的 retry 循环，但不影响输出质量
- **建议改法和验证点**: 将 `前次 attempt number` 改为 `前次输出编号`；验证 repair prompt 全文不含 "attempt" 词
- **修复风险（低）**: 仅改字符串拼接，不涉及类型变更
- **严重程度（低）**:

### 012-[低]-parser-错误标签名不准确-CONST_TEXT-使用-invalid_enum_value

- **入口/函数**: `parse_compact_candidate_v3()`
- **文件(行号)**: `dayu/host/compact_structure.py:221`
- **输入场景**: candidate JSON 的 `schema` 字段值不等于 `COMPACT_OUTPUT_SCHEMA_V3`
- **实际分支**: `raise ValueError("invalid_enum_value: $.schema")`
- **直接证据**: `schema` 字段在 `_ROOT` 中的 descriptor 类型是 `_FieldKind.CONST_TEXT`（行 137-140），不是 `_FieldKind.ENUM`。错误标签 `invalid_enum_value` 与 descriptor 类型不一致。功能上不影响（调用方通过 prefix matching 映射 issue code），但 log grep-ability 偏差
- **预期行为**: 使用 `invalid_const_value: $.schema` 等标签
- **影响**: 极低。调用方 `_structure_validation_report`（`llm_compaction.py:808`）按 message prefix 映射 code，`invalid_enum_value` 映射到 `CompactValidationIssueCodeV3.INVALID_ENUM_VALUE`，这在修复上下文中语义仍可接受，但严格来说 schema 是 const 不是 enum
- **建议改法和验证点**: 改为 `invalid_const_value` 并确认 `_structure_validation_report` 的 code_by_prefix 是否需要新增条目
- **修复风险（低）**: 需同步修改 parser 错误文本和 `_structure_validation_report` 的前缀映射
- **严重程度（低）**:

### 013-[低]-validate_compact_represented_coverage_candidate_binding_v3-未列入-__all__

- **入口/函数**: 模块 `__all__` 导出
- **文件(行号)**: `dayu/host/compaction.py`（`__all__` 在 ~3734 行）
- **输入场景**: 模块使用者通过 `__all__` 理解公共 API surface
- **实际分支**: `validate_compact_represented_coverage_candidate_binding_v3` 被 `compact_payload.py:26`、`compact_artifact.py:25` 导入并在 production 中使用，但不在 `compaction.py` 的 `__all__` 中。对比 `validate_compact_policy_usage_audit_candidate_binding_v3` 已在 `__all__` 中
- **直接证据**: `__all__` 只包含 `validate_compact_policy_usage_audit_candidate_binding_v3`（行 ~3801），缺少 `validate_compact_represented_coverage_candidate_binding_v3`
- **影响**: 低。函数可正常 import 使用；但 `__all__` 的不一致会让扫描 API surface 的读者遗漏该函数
- **建议改法和验证点**: 将 `"validate_compact_represented_coverage_candidate_binding_v3"` 加入 `compaction.py` 的 `__all__`
- **修复风险（低）**: 纯 export 声明，不影响任何调用方
- **严重程度（低）**:

### 014-[低]-测试文件残留-v2-文档字符串

- **入口/函数**: 多个测试 fixture/helper 的 docstring
- **文件(行号)**:
  - `tests/host/fake_compaction.py:77` — `:param candidate: strict v2 candidate。`（实参类型为 `CompactAcceptedTruthV3`）
  - `tests/host/fake_compaction.py:111` — `:param candidate: strict v2 candidate。`
  - `tests/host/fake_compaction.py:115` — `:returns: 与 candidate source-kind contract 一致的 v2 input。`
  - `tests/host/fake_compaction.py:751,753` — 两处 "v2"
  - `tests/host/test_context_compact_events.py:547` — `fresh v2 anchor`
  - `tests/host/test_compact_material.py:170,409` — 两处 "v2"
- **输入场景**: 无运行时影响；仅文档字符串中的 stale 文本
- **实际分支**: 代码逻辑已全部使用 v3 类型，但 docstring 中的 "v2" 未被更新
- **直接证据**: 各文件:行号如上
- **影响**: 仅代码可读性；不影响测试断言或生产行为
- **建议改法和验证点**: 批量替换 stale docstring 中的 "v2" → "v3"
- **修复风险（低）**: 纯文档修改
- **严重程度（低）**:

---

## Findings Summary

| # | 维度 | 结论 |
|---|---|------|
| 001 | fresh v3 删除 v2/explicit drop ledger/reason/diagnostics/compatibility | PASS |
| 002 | S2 Custom capability none 修复 | PASS |
| 003 | single structure descriptor 同源派生 | PASS |
| 004 | strict parser（duplicate/unknown/missing/type/enum/label/cap） | PASS |
| 005 | LLM-facing prompt 自足、无治理术语、initial/repair 边界 | PASS |
| 006 | accept owner、coverage partition、policy audit、artifact/event/Memory 同源 fail closed | PASS |
| 007 | successful response identity 在 parse/reject 后保留 | PASS |
| 008 | 测试 owner-level 覆盖且含反例 | PASS |
| 009 | `compactor_input_projection.v2` 版本号混淆 | 低 |
| 010 | "覆盖账本" 暴露 Host 治理概念 | 低 |
| 011 | repair body 暴露 attempt 计数术语 | 低 |
| 012 | parser CONST_TEXT 错误标签使用 invalid_enum_value | 低 |
| 013 | coverage binding validator 未列入 `__all__` | 低 |
| 014 | 测试文件残留 v2 docstring | 低 |

**8 项 PASS，6 项 LOW。未发现中/高/严重缺陷。**

---

## Open Questions

1. `validate_input_binding` 当前只在 `compact_artifact.py:64`（artifact write request 构造）调用。`compact_payload.py` 的 `_validate_committed_coverage`（行 410-436）重验了 coverage partition 等式但没有调用 `validate_input_binding` 检查 current_input_ref/source_boundary/output_caps 的 input 级绑定。当前设计中 `_validate_committed_coverage` 的调用者是 durable payload parser（从已提交的 EventLog 读取），此时 input binding 的校验已在 artifact write 时完成，payload 级校验 coverage 等式即可。这是合理的分层校验，不构成 gap；但若未来有绕过 artifact writer 直接构造 `CONTEXT_COMPACTED` 的路径，则需要在此处补充 input binding 校验。

2. `compact_text_size_units_v3` 使用 Python `len(text)`，`estimate_memory_size_units` 也使用同一函数。当前实现是字符数计量，与 token-based 计量存在语义差异。这是有意的设计选择（见设计 doc 25 节 usage-anchored adaptive sizing），但 S4 真实 provider observation 需要确认该字符计量在 provider token 计量下的偏差是否在 policy ratio 允许范围内。

---

## Residual Risk

1. **Diff surface 大**：S3 是一次原子 breaking migration，触及 47 个文件。风险由 full suite（6696 passed）、逐文件 branch coverage（均 ≥80%）、本 review 与另一路独立 review 共同控制。

2. **未执行 S4 真实 provider evidence**：两个 `utils/` smoke harness 只做获准的 mechanical v3 migration；真实场景语义、evidence contract、compactor prompt 在真实 provider 上的表现未在此 S3 验证。这是 accepted plan 明确 defer 到 S4 的范围。

3. **`compact_text_size_units_v3` 与 token-based sizing 的语义差异**：当前字符计量是设计选择，但 provider token 计量偏差需要在 S4 usage-anchored adaptive sizing 中通过实际 observation 确认。

4. **未覆盖的 conformance 测试**：Conversation Memory 的完整 eval harness deferred 到 GitHub Issue 80；当前 `utils/` smoke 是初步验收标准。

---

## Review Artifact Metadata

- Reviewer: DeepSeek (independent implementation reviewer)
- Review type: adversarial deep review with semantic ownership pass
- Review date: 2026-08-05
- Evidence base: 完整 git diff（11611 lines）、全部 production 文件逐行走读、全部受影响的 test/smoke files、design docs 24-25 节、engine design doc structured-output contract、accepted plan checkpoint + scope correction + S3 implementation artifact
- Tools used: `grep`/`rg` 全量扫描、`Read` 逐文件走读、4 Explore subagent 并行专项审查、diff section 定位
