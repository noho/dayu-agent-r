# Code Review — upload-filing-ticker-alias-contract 聚合深度评审

## Scope

- Mode: current changes（Gateflow aggregate deepreview，非单 slice 复述）
- Branch: `codex/upload-filing-oracle`
- Base: `5508d0445bd1d649fee54f4ec3d65f99e2484493`（accepted plan commit）
- HEAD: `2b11cb21`（accepted S2；含 S1 `c5446b77`）
- Output file: `docs/reviews/code-review-20260815-023958.md`
- Included scope: base..HEAD 全部 72 个文件（生产 26 个 Python 文件 + 测试 27 个 + docs 19 个）。已完整读取：goal confirmation、accepted plan、两轮 plan fix、全部 plan/slice controller adjudications、8 份 slice review artifact。
- Excluded scope: 无生产代码被排除。`docs/reviews/` 下 slice review 原文只读摘要（其结论已由 adjudications 汇总核对）。
- Parallel review coverage:
  - subagent A（CLI/tools/read_runtime 链路）：`dayu/cli/commands/fins.py`、`dayu/cli/arg_parsing.py`、`dayu/fins/tools/{upload_tools,fins_tools,error_contract,read_runtime}.py`、`ingestion_runtime` 静态校验与 prepare 链、`service_runtime` prevalidation、`_fs_filing_upload_state_core`、CLI/tool 测试。
  - subagent B（pipelines/resolver 链路）：`fmp_company_info`、`upload_company_meta`、SEC/CN download/upload workflows、`cn_download_company_meta`、`sec_6k_primary_document_repair`、`ingestion_runtime` 决策链、`service_runtime` material handoff。
  - subagent C（storage 原子性/锁图）：`_fs_storage_infra` 全文、`_fs_company_meta_core` 全文、`fs_company_meta_repository`、`repository_protocols`、`upload_failure`、`company_meta_contract`、`_fs_identity` 全文、`dayu/runtime/filelock` 全文——锁图无环、release 顺序、recovery phase 机、lstat 分型、index 登记顺序均已独立核实；产出 F1–F5 五个低 severity 观察（已整合为 Finding 1/4 与 residual risk）。
  - 主 reviewer 亲自走读：`ticker_normalization.py`、`document_models.CompanyMeta`、`company_meta_contract.py`、`_fs_storage_infra.py` 全部新增 hunk（identity commit/scan/index/recovery/lstat 分型）、`_fs_identity.py` descriptor 双向校验、`upload_failure.py` mapper、`read_runtime` route、CLI diff、README diff、并发/契约测试抽查。
  - 未覆盖区域：`tests/fins/test_company_identity_storage_contract.py`（2113 行）为抽查关键用例而非逐行；`dayu/fins/storage/_fs_storage_utils.py` 删除行的全部历史引用点仅以 residue scan 与回归测试兜底。

## 验证结论（独立复现）

主 reviewer 在本机重新执行（`source .venv/bin/activate`）：

- `coverage run --branch -m pytest -q tests/fins tests/tools/test_combined_tools_acceptance.py` → **1604 passed, 1 skipped**（与 S2 fix addendum 声称一致）。
- `pytest -q tests/fins/test_company_identity_storage_contract.py tests/fins/test_fins_storage_atomicity.py` → **190 passed**。
- 全量 `python -m pyright dayu/ tests/ utils/` → **0 errors**（仅版本升级提示）。
- 逐文件 branch coverage（本次生成数据）：所有修改生产文件 `>=81%`，最低 `sec_pipeline.py=81%`、`_fs_storage_infra.py=84%`；与实现报告一致。
- 计划 residue scan 四个 pattern 全部零命中（`resolve_existing_ticker`、`_canonicalize_ticker_alias`、`_normalize_company_ticker_aliases`、`upsert_company_meta_for_upload`、`strip().upper()` read fallback 等）。
- owner 探针直接复现：`V.BA→V-BA`、`BRK.B→BRK-B`、`AAPL.SW→AAPL-SW`、`AAPL+(AAPL,US.AAPL)→aliases=()`、`DELTA+(MSFT,)→('MSFT',)`、`V+(V.BA,)→('V-BA',)`、`0700+(TCEHY,)→HK('TCEHY',)`、9 字符 `ValueError`。
- 旧行为固化测试已迁移：`test_fins_storage_atomicity.py` 的 late-conflict（"命中多个公司目录"）断言与 `resolve_existing_ticker` 用法已删除，duplicate 场景改由 typed `duplicate_owner` corruption 测试覆盖；无 compat shim 倒逼生产分支。

## Findings

### 1-未修复-中-同一 durable corruption 事实在三个入口产生三种投影，upload tool 的 typed corruption 分支在 prepare 路径不可达（死分支），corrupt meta.json 被投影为 invalid_argument 且内部错误原文进入 LLM 上下文
- **入口/函数**: `FinsUploadToolCallable.__call__`（tool 入口）/ `FinsIngestionRuntime.start_upload`（awaiting 入口）/ `service_runtime.prevalidate_fins_upload_filing_request_for_workspace`（CLI 入口）
- **文件(行号)**: `dayu/fins/storage/_fs_company_meta_core.py:75-77`（`_get_company_meta_unguarded` 抛裸 `ValueError`）；`dayu/fins/storage/_fs_filing_upload_state_core.py:80-96`（`read_filing_upload_state` 只吸收 `FileNotFoundError`）；`dayu/fins/tools/upload_tools.py:105-112`（`except CompanyTickerIdentityCorruptionError`）与 `:113-120`（`except ValueError` → `_ERROR_INVALID_ARGUMENT`，`message=str(exc)`）；`dayu/fins/ingestion_runtime.py:4204-4237`（material 分支完全不触 storage）；`dayu/fins/service_runtime.py:81-90`
- **输入场景**: workspace 中某 published corpus 的 `meta.json` JSON/schema 非法、被替换为 symlink/非 regular file，或 meta canonical 与 descriptor 不一致，或 target ticker 目录被替换为 symlink/regular file（durable corruption 事实）；用户经工具/CLI/service 发起 filing upload。
- **实际分支**: `read_filing_upload_state` → `_get_company_meta_unguarded` → `CompanyMeta.from_dict` 抛裸 `ValueError`（如 `"CompanyMeta.ticker_aliases 必须是字符串数组"`）→ 不被 `FileNotFoundError` 分支吸收 → tool 的 `except ValueError` 命中（typed corruption 分支只在 `prepare_observed_upload` 被 monkeypatch 时可达）→ `error=invalid_argument`、`message=str(exc)` 把内部 schema 术语原文投影给 LLM。同一 corruption 事实还有两条未 typed 路径：`_get_company_meta_unguarded` 对 symlink/非 regular `meta.json` 经 `_read_json` 抛 generic `ValueError`（`_fs_storage_utils.py:595-602`），而 `_read_published_company_identity` 对同一物理事实抛 `invalid_meta` corruption（`_fs_storage_infra.py:1014-1028`）；`begin_batch`/`_commit_batch_with_publication_guard` 对 target 非目录/symlink 抛 plain `ValueError`（`_fs_storage_infra.py:546-547`、`:773-774`），经 mapper 兜底落为 `UNEXPECTED_RUNTIME`（"请检查运行日志"），而扫描路径对同一事实抛 `invalid_descriptor` corruption。
- **预期行为**: 按 plan §5.6/§5.7 与 S1 re-review residual risk，published corruption 应唯一投影为 typed、path-free、可行动的 corruption failure（read 侧 `workspace_identity_corrupted`，upload 侧 `storage/storage_io`），同一事实同一 owner。
- **实际行为**: 同一 corruption 事实三种投影并存：read route 扫描时 → typed `workspace_identity_corrupted` ✓；commit-time 扫描时 → typed `storage/storage_io` ✓；upload 启动时（filing prevalidation 读 meta）→ tool 侧 `invalid_argument` + 内部原文、CLI 侧 `"上传状态已损坏，请检查工作区存储状态"`（第三种文案）、material 侧 job 内 → `unexpected_runtime`（第四种）。LLM 被告知"检查 ticker/action/文件路径"，永远修不好 workspace；operator 收不到 workspace 已损坏的信号。
- **直接证据**: `_fs_company_meta_core.py:76-77` 抛普通 `ValueError`；`_fs_filing_upload_state_core.py:71-73` docstring 只声明 `ValueError`；`upload_tools.py:105` 的 typed 分支与 `:113` 的宽分支顺序正确但前者在真实 prepare 链不可达（`_normalize_upload_request` 对 material 不读 storage，filing 只经 `read_filing_upload_state`）；固化该分支的测试 `test_fins_ingestion_tools.py:1095` 用 monkeypatch 注入异常、未走真实 corruption 路径；`service_runtime.py:87-88` 证明 CLI 把同一 ValueError 投影为第三种文案；storage subagent 独立核实 `_fs_storage_utils.py:595-602` 与 `_fs_storage_infra.py:546-547/773-774` 的未 typed 分支（F2/F3 同根）。
- **影响**: 错误原因语义漂移（semantic ownership drift）：durable corruption 被归责为用户输入错误或"运行期未知错误"；LLM-facing 输出泄露内部类型/字段术语（违反 AGENTS.md LLM-facing 约束）；typed handler 是死代码；失败原因依赖"在哪里检测到"而非业务事实。
- **建议改法和验证点**: 在 storage read/write owner boundary（`get_company_meta`/`read_filing_upload_state`/`begin_batch`/commit backup 的 target 结构判定）复用 `_read_published_company_identity` 的同一 typed 分型（strict parse 失败、symlink/non-regular、identity mismatch → `CompanyTickerIdentityCorruptionError`；begin/commit 的 target 非目录/symlink → `invalid_descriptor`），使 tool/CLI/service 统一投影；新增真实 corrupt meta.json 与 symlink target 的 tool/CLI/awaiting 三个入口 projection 测试，替换 monkeypatch 测试。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 2-未修复-中-material 上传的 ticker/alias 准入边界与 filing 不对称，且 `SecDownloader.normalize_ticker` 的 `strip().upper()` loose fallback 仍是 SEC material producer 的 ticker 归一化路径——本 WU 目标明确要求消除的 pattern 残留
- **入口/函数**: `SecPipeline.upload_material` / `sec_upload_workflow.run_upload_material_stream` → `host._downloader.normalize_ticker(ticker)`；`_normalize_upload_request`（material 无 ticker/alias 语法与数量校验）
- **文件(行号)**: `dayu/fins/downloaders/sec_downloader.py:1125-1150`（`normalize_ticker` 回退 `strip().upper()`）；`dayu/fins/pipelines/sec_upload_workflow.py:446`（消费该 fallback 派生 canonical 并用于 `begin_batch`/`stage_company_meta_for_upload`）；`dayu/fins/ingestion_runtime.py:7178-7197`（material 只校验 action/source_kind）；`dayu/fins/tools/upload_tools.py:331-350`（material request construction 只做 `_required_text` 非空校验）；`dayu/fins/ingestion_runtime.py:885-893`（filing 有 `_MAX_TUPLE_ITEMS=100` 与逐 alias grammar 校验）
- **输入场景**: 工具调 `start_fins_upload`（`upload_kind=material`）传非法 ticker（如 `"Apple Inc."`）或非法 alias（如 `"a apl"`）、超量 alias（>100 项）；CLI `upload_material --ticker "AAPL,<非法别名>"`。
- **实际分支**: material 请求通过 `_normalize_upload_request` 进入 job → producer `normalize_ticker("Apple Inc.")` 走 `try_normalize_ticker` 失败 → 回退 `strip().upper()` 得 `"APPLE INC."` → `begin_batch` 的 canonical 校验 `ValueError` → terminal `fins_upload_failure_from_exception` 兜底 → `UNEXPECTED_RUNTIME`（"上传执行失败，请检查运行日志后重试"）。非法 alias 同理在 job 内由 decision 的 builder `ValueError` → `unexpected_runtime`；alias 数量无任何上限。
- **预期行为**: 与 filing 对称：非法 ticker/alias 应在启动边界投影 usage failure（`INVALID_TICKER`/`INVALID_TICKER_ALIAS`）；pipelines 不保留 loose fallback——goal confirmation §7 明确"CLI、upload tool、resolver、download/upload pipelines 与 read runtime 只复用 contract，不保留重复 grammar、loose fallback 或查询时猜测"；accepted plan §12.4 residue scan 的目标即此类 symbol。
- **实际行为**: 同一业务事实（非法 ticker/alias）filing 侧有界 usage failure、material 侧泛化 runtime 错误；fallback 无数据损坏（`begin_batch` canonical 校验 fail closed，published tree 不变），但错误原因 owner 漂移，且 plan 的 residue scan 符号清单漏掉 `SecDownloader.normalize_ticker`，使该 fallback 逃过零命中门槛。
- **直接证据**: `sec_downloader.py:1142-1149`（fallback 代码原文）；`sec_upload_workflow.py:446`（调用点）；`ingestion_runtime.py:7178-7197` 全文无 ticker/alias 循环；`upload_tools.py:340-350` 构造 material request 不做 grammar 校验；filing 侧 `ingestion_runtime.py:885-893` 对照。controller 对 material 缺 company-name 的 defer 裁决（S2 re-review）只覆盖 company-name 投影，不覆盖 ticker/alias 准入与 fallback。
- **影响**: LLM 对 material 非法输入得不到可行动反馈；错误分类（usage vs runtime）随入口漂移；goal 声明的 loose-fallback 消除目标未完全达成；超量 alias 无界进入 job。
- **建议改法和验证点**: 两个方向二选一或组合：(a) 在 `_normalize_upload_request` 的 material 分支补齐 ticker/alias 语法与 `_MAX_TUPLE_ITEMS` 数量准入，投影 filing 同源 usage code；(b) 删除 `SecDownloader.normalize_ticker` 的 fallback 或在 sec_upload_workflow 改用唯一 `ticker_normalization` owner（fallback 删除需评估其它 downloader 调用点，`grep` 显示该 proxy 还有 download 侧调用方）。补 material 非法 ticker/alias 的 tool/CLI 投影测试。
- **修复风险（低/中/高）**: 中（fallback 删除涉及 download 侧既有调用方，需逐点迁移验证）
- **严重程度（低/中/高/严重）**: 中

### 3-未修复-低-下载 producer fresh 且 identity 未变化时不 stage，caller 仍无条件 commit，产生无意义 full-tree swap
- **入口/函数**: `sec_company_meta.upsert_company_meta`（返回 `None` 分支）/ `cn_download_company_meta.stage_company_meta_for_cn_download`（返回 `None` 分支）→ `sec_download_workflow._publish_sec_post_repair_mutations` / `cn_download_workflow._publish_cn_company_after_repair` 的无条件 `commit_batch(company_batch)`
- **文件(行号)**: `dayu/fins/pipelines/sec_company_meta.py:109-110`；`dayu/fins/pipelines/cn_download_company_meta.py:70-71`；`dayu/fins/pipelines/sec_download_workflow.py:815-827`；`dayu/fins/pipelines/cn_download_workflow.py:428-440`；`dayu/fins/storage/_fs_storage_infra.py:640-644`（document-only 路径）
- **输入场景**: 同一公司重复 download 且无新 alias（幂等重复执行）。
- **实际分支**: 无 intent 且 `publishes_new_corpus=False` → `_commit_batch_with_publication_guard` → 对 `begin_batch` 时 copytree 的完整树执行 backup→swap（`_fs_storage_infra.py:568-569` copytree 完整 staging）。
- **预期行为**: 零 mutation 的 batch 不应产生物理 swap。
- **实际行为**: 每次幂等重复 download 产生一次完整目录 rename + backup 生命周期；正确性无影响（staging 是完整拷贝，published 语义不变），但这是本 WU 引入的 keep-return 优化后的新暴露面（base 上 SEC download 总是 upsert 即总是有 mutation）。
- **直接证据**: `sec_company_meta.py:107-110` 的 `return`；`sec_download_workflow.py:824-827` 仍调用 `commit_batch`；commit 分支条件 `_fs_storage_infra.py:641-644`。
- **影响**: 无意义 I/O 与 backup 目录 churn；非正确性问题。
- **建议改法和验证点**: caller 收到 `None` 时 `rollback_batch` 而非 commit；或 storage 提供零 mutation 检测（需评估是否过度设计，倾向 caller 侧收口）。补重复 download 无 swap 的断言。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 4-未修复-低-`_company_meta_path(ticker, state)` 是被删除 `_upsert_company_meta_impl` 遗留的死代码，保留 staging meta 写路径的第二入口
- **入口/函数**: `_FsStorageInfra._company_meta_path`
- **文件(行号)**: `dayu/fins/storage/_fs_storage_infra.py:3321-3337`
- **输入场景**: 无调用方（全仓库 grep 唯一命中为定义本身）。
- **实际分支**: 永不执行。
- **预期行为**: 被删除的 S1 `_upsert_company_meta_impl` 写路径 locator 应随 owner 迁移一并删除；当前 staging 中 ticker-root `meta.json` 的唯一真实写入点是 `_prepare_company_identity_commit` 内联 `state.staging_ticker_dir / _SOURCE_META_FILENAME`（`:843`）。
- **实际行为**: 同一 durable 事实（staging 公司 meta 写路径）保留一个无人使用的第二入口，后续维护者可能误以为存在第二条写语义路径。
- **直接证据**: storage subagent 全仓 grep（dayu/ + tests/）唯一命中为定义；`_upsert_company_meta_impl` 已删除且无残留引用。
- **影响**: 语义所有权残留（死代码暗示第二写入口）；无运行影响。
- **建议改法和验证点**: 删除该方法，并确认 `_company_meta_path_for_read` 之外无其它冗余投影；删除后跑 storage 回归 + pyright。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## 业务 contract 逐条闭环核对（goal confirmation 十条 + 语义 owner）

1. **CSV 第一项 canonical、后续项用户显式 alias、信任声明不联网核验**：闭环。CLI `_parse_ticker_csv` 只调 `build_company_ticker_identity`；`DELTA,MSFT`→`('MSFT',)`、`AAPL,AAPL,US.AAPL`→`()` 直接复现；direct `upload_filing` 按 plan 特许以 raw CSV 进入 Fins typed validator（usage code owner 不被 CLI 替代）；tool schema 明确"系统信任声明且不联网核验"。
2. **唯一 normalizer 统一 US/CN/HK、长度边界、语法变体、V.BA**：闭环，除 Finding 2 的 `SecDownloader.normalize_ticker` fallback 残留。`V.BA→V-BA`、`BRK.B→BRK-B`、`AAPL.SW→AAPL-SW`、9 字符拒绝均直接复现；FMP resolver 消费同一 grammar（非法外部 symbol 丢弃、canonical 请求 fail closed）。
3. **唯一 identity contract 合并 canonical/显式/跨市场/resolver aliases、稳定去重**：闭环。`build_company_ticker_identity` 以 canonical 字符串稳定去重、首次出现顺序保留；`CompanyTickerIdentity.lookup_tickers()` 是唯一投影（storage index、resolver、CLI argv 再生成均消费它）；跨市场 `0700+(TCEHY,)` 保留 HK market + US alias。
4. **CompanyMeta durable 与 storage index 由同一 contract 投影；同一 normalized alias 只属一个 corpus**：闭环。`CompanyMeta.to_dict/from_dict` 只经 `ticker_identity` 投影，缺字段/wrong type/market mismatch/非法 alias 全 fail closed；`_build_unique_company_identity_index` 先登记 descriptor canonicals 再登记 strict-valid meta aliases，`duplicate_owner` typed corruption；index 不新增 durable 文件（符合 plan §7.6）。
5. **冲突发布前原子拒绝；typed failure 有界可行动**：闭环（conflict 路径）+ Finding 1 缺口（corruption 路径的启动侧投影）。`CompanyTickerAliasConflictError` 构造器强制 normalized 字段；commit 在 target backup/swap 前完成 scan+conflict 判定；`storage/ticker_alias_conflict` mapper + `_STORAGE_FAILURE_CODES` JSON round-trip 一致；conflict-before-swap、双向 canonical conflict、并发首发改 alias 一胜一败测试均为 durable-state 断言。
6. **list_documents canonical/alias/跨市场/resolver alias 同 corpus**：闭环。`resolve_company_ticker` 是唯一 route，`try_normalize_ticker` 归一查询 token 后查唯一 index，无 upper fallback；e2e 测试 `{"ticker":"apple"}` 与 canonical 返回 exact 相同结果；meta-less canonical-only 与健康 corpus 共存双向测试通过。
7. **CLI/tool/resolver/pipelines/read runtime 只复用 contract**：基本闭环，Finding 2 为唯一残留。plan 列出的 8 个重复 helper 全部零命中；`read_runtime` 删除 normalize/upper fallback 且 NOT_FOUND 唯一投影点；九个 read tools 共用 `_ticker_parameter_schema`；CLI/tool 无独立 dedupe。
8. **fresh/stale/new 不丢 alias、非 identity 字段不被改写、并发不丢不覆盖**：闭环。`resolve_upload_company_meta_decision` fresh+新 alias → `preserve_published` intent（绝不 keep 吞掉）；`merge_company_meta_for_commit` 在 commit-time authoritative current 上 stable union（current aliases 在前）；`updated_at` 只在真实 mutation 时更新；spawn 跨进程 lost-update/union/changed-stale-reject/双 material 进程测试全部通过且为 durable 断言。
9. **meta-less corpus canonical-only、可补 meta、不阻断健康 corpus**：闭环。descriptor 是 canonical 的 durable owner；`meta.json` 缺失是合法 canonical-only 状态（不贡献 alias、非 corruption）；补 meta 走 `refresh_if_stale(expected=None)`；双向 canonical/alias 冲突在 swap 前 typed 拒绝。
10. **README 只在职责边界内更新**：闭环。根 README 只更新用户可见 CSV 语义与冲突拒绝行为；`dayu/fins/README.md` 更新 owner 分解、锁序、typed failure 与 FMP 接口；`tests/README.md`、`dayu/README.md`、Host/Engine design 未动，符合触发规则与 S2 fix 的 README decision。

**语义 owner 核对**：`CompanyTickerIdentity`（ticker_normalization）✓；`CompanyMeta`（document_models）✓；commit intent/authoritative merge（company_meta_contract，唯一 pure function，storage 机械调用、pipeline 不 import storage 语义）✓；storage route/unique index/锁序（storage）✓；typed failure（upload_failure + error_contract）除 Finding 1 外 ✓；`CompanyTickerIdentityCorruptionError` closed kinds（invalid_descriptor/invalid_meta/identity_mismatch/duplicate_owner）与 `os.lstat` 显式分型（仅 ENOENT 为 missing，EACCES/EIO 保留 operational 类别，symlink/non-regular fail closed）✓。

**durable atomic uniqueness / recovery / lock graph**：锁图无环（identity-changing commit: writer→recovery→identity→incoming pub(释放)→sorted pubs(逐个释放)→target pub；read: identity→sorted pubs；recovery: recovery→nonblocking writer→identity→pub；document-only: writer→target pub；无 publication→identity/recovery 反向路径）。recovery 对所有 recovered tree 无条件取 identity guard（R1 裁决落地），`_PHASE_STARTED` orphan 只清 staging（尚无 publication，controller 已裁决）。`publishes_new_corpus` 在 same-ticker writer 后由显式 `os.lstat` 冻结；begin/commit backup 判定均为 ENOENT-only。earliest-primary + release-note、COMMITTED 后不回滚规则均有测试固化。

**compat shim / loose fallback / residue**：plan 清单零命中；旧 `resolve_existing_ticker`/list-index/`upsert_company_meta_for_upload` 已删；旧测试（late conflict、fresh 忽略 NEW、旧 Fmp constructor）已迁移，无 compat 分支。唯一残留为 Finding 2 的 `SecDownloader.normalize_ticker` fallback（不在 plan residue 符号清单内）。

**cross-slice 组合检查**：S1 checkpoint 的临时契约（final-meta stage、`resolve_existing_ticker`、list index、read fallback）在 S2 已全部原子替换，无半契约残留；`_fs_storage_utils.py` 删除的重复 grammar helper 无 import 残留；`test_fins_storage_atomicity.py` 的旧固化断言已按 owner 迁移；S1/S2 之间唯一跨 slice 缺口是 Finding 1（S2 在 scan/commit 边界引入 typed corruption 分类，但 prevalidation 读取边界未切换）。

**过度耦合检查**：`company_meta_contract` 只 import domain 契约；storage 依赖 domain contract 但不 import pipeline；pipeline 消费 storage 协议；无反向 import；`FsCompanyMetaRepository` 与 `_FsStorageInfra` 边界清晰（`_ActiveBatchState` 与 intent 记录在 infra，协议层只透传）；`upload_failure` 新增 `dayu.fins.storage` import 为 exception-only，无环。

## Open Questions

- Finding 1 中 tool 的 typed corruption 分支是否应保留（等待 storage read boundary 升级）还是删除并统一 ValueError 投影——需 controller 裁决 owner 收口方向（storage read 分型 vs tool 侧分型），两者不能并存。
- Finding 2 中 `SecDownloader.normalize_ticker` fallback 的 download 侧调用点（`sec_downloader.py:1205` 等）迁移范围未在本次走读——若删除 fallback，需要独立评估 download 侧影响面。
- `upload_filings_from --infer` 的 canonical mismatch `RuntimeError` 分支因 resolver 与 CLI 同源 normalize 恒假（死防御）；保留为未来 resolver 演进护栏还是删除，属风格裁决，不作为 finding。
- NOT_FOUND 与 material 非法 ticker 的错误文本无长度边界、原样回显输入（pre-existing，`_resolve_canonical_ticker` 本次重写未引入新边界）——是否并入后续 LLM-facing 收口 WU。

## Residual Risk

- **已分类到后续 WU（controller 已裁决/plan 已列）**：旧 workspace 歧义 alias/旧 CompanyMeta schema（fresh schema 边界）；UF-PF05 真实 CLI evidence（用户排除）；oracle/scenario registry/冻结 evidence（用户排除）；workspace scan 成本与 recovery 全量 guard 等待（无实测证据不预建 cache）；SEC upload/download/CN download resolver_version 字面量不一致（`market_resolver_v1.0.0` vs `market_resolver_v1` vs `cn_download_v1`——既有行为、本 WU 未恶化，但 S2 intent 的 fresh 判定现在消费这些不一致常量，download/upload 交替互相刷新非身份字段，建议后续 WU 收敛为单一 owner 常量）；material 缺 company-name 的 pipeline-direct 投影（controller defer）。
- **pre-existing、本 WU 未引入**：`tests/cli/test_upload_filings_from_command.py::test_upload_filings_from_usage_empty_and_write_failures` 基线失败（controller 已隔离复现）；空 alias 复用 `EMPTY_TICKER` 文案的误导性；NOT_FOUND 错误文本无界回显；`scan_company_meta_inventory`（operator inventory，非 authoritative 路径）把 lock-only 残余 candidate 投影为 `invalid_meta` status（`_fs_company_meta_core.py:110-134`，与 typed corruption 语义名不副实）；`_recover_orphan_batch_dirs` 对 symlink/非目录 token dir 静默跳过无 action 记录（`:2142-2143`，fail-closed 但审计不完整）。
- **test gaps**：Finding 1 的 monkeypatch 测试未覆盖真实 corrupt meta 路径；material 非法 ticker/alias 无 tool/CLI projection 测试（Finding 2）；ACL/NFS 真实外部文件系统行为未验证（errno 注入已覆盖分型逻辑，S2 fix 已声明）；storage subagent 未逐行阅读 `test_company_identity_storage_contract.py` 全文（主 reviewer 已抽查关键并发/锁序用例并独立复跑 190 passed）。
- **未检查区域**：`tests/fins/test_company_identity_storage_contract.py` 2113 行仅抽查关键用例（并发/锁序/恢复/lstat 分型均已抽查且独立复跑 190 passed）；Host/Engine 全仓非 Fins 测试未运行（本 WU 未修改 Host/Engine）。

## 结论

**PASS（核心 contract）/ FAIL（closeout 条件）**：核心架构——唯一 identity owner、commit-time authoritative merge、workspace 原子唯一性、锁图、recovery、typed conflict、meta-less canonical-only、`list_documents` 同 corpus——全部经直接代码证据与独立复现验证成立；无数据丢失、无并发窗口、无 published 副作用先行缺陷。但两项中 severity finding 均属本 WU 自身目标范围（错误原因唯一 owner、loose fallback 消除），且在 slice-level review 与 adjudication 之外的新证据（跨入口组合后暴露），按 Gateflow 语义阻塞 final closeout：

- **blocker-1（中）**：durable corruption 的启动侧投影未收口（Finding 1）。
- **blocker-2（中）**：material 准入不对称与 `SecDownloader.normalize_ticker` loose fallback 残留（Finding 2）。
- 非 blocker：Finding 3（低，下载幂等 swap）与 Open Questions 项可由 controller 裁决 defer。

两项 blocker 修复后：补真实 corrupt-meta 三入口投影测试、material 非法 ticker/alias 准入测试，重跑 focused/relevant regression、逐文件 coverage、全量 pyright 与 residue scan，即可进入 final closeout。
