# Plan Re-Review — wu-cli-interactive-02 S5/F13 第三次 utils-closure amendment accepted-finding fix

## 0. Review metadata

- Review type: adversarial independent re-review（按 Controller 裁决的 fix 范围复核 accepted finding 闭合）
- Provider: DeepSeek
- Reviewer: AgentDS
- Reviewed target:
  - `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md`（完整 plan，重点 §9.1、§10.5、§13、§15）
  - `docs/reviews/wu-cli-interactive-02-s5-f13-utils-closure-plan-amendment-proposal-codex.md`（amendment proposal）
  - `docs/reviews/gateflow-wu-cli-interactive-02-s5-utils-closure-amendment-review-adjudication-20260802.md`（Controller adjudication）
  - `docs/reviews/plan-review-20260802-000526.md`（MiMo review）
  - `docs/reviews/plan-review-20260802-000107.md`（AgentDS review）
- Base HEAD: `e7f578dc7bdfafb51a859be2db584300e08f81fb`
- Branch: `codex/interactive-oracle`
- Review timestamp: `20260802-002027`
- Artifact path: `docs/reviews/plan-review-20260802-002027.md`
- Working tree: 20-file S5 implementation dirty set（绝对只读，未修改、未 revert、未 stage、未格式化）
- Scope: 独立复核 Controller 裁决的 accepted finding 是否闭合；不依赖任何既有 review pass 自动当结论

## 1. Review method summary

1. 从 accepted HEAD `e7f578dc` 独立运行所有 inventory 命令，不依赖 plan/proposal/review 自述。
2. 独立运行 §10.5 完整五类 pattern 去重验证脚本，逐断言检查。
3. 逐行核对两个 utils smoke 的 exact call site、AgentRunRequest 同源性与 identity 字段映射。
4. 扫描 plan 全文确认零残留 32/27+5 total union 算术错误。
5. 验证 5-file allowed-file delta 与 6-file builder-only set difference 的术语区分。
6. 验证 `test_context_compact_events.py` 是早已授权但 identity closure 未覆盖的额外 builder-only 文件。
7. 同步检查 2-file/4-call、identity source、smoke/non-goals/validation 没有 drift。
8. 应用全部 5 个 review lens：goal-bound minimal design、architecture boundary、best-practice、optimal-solution、overengineering、overcoupling。

## 2. Assumptions tested — 独立复核结果

| # | Assumption | 独立复核方式 | 复核结果 |
|---|-----------|-------------|---------|
| A1 | identity union = 27 files | `{rg -l FA; rg -l OA; rg -l CR} \| sort -u \| wc -l` | ✅ 27 |
| A2 | builder union = 8 files | `{rg -l CB; rg -l RB} \| sort -u \| wc -l` | ✅ 8 |
| A3 | overlap = 2 files | `comm -12` identity vs builder | ✅ `test_compaction_operation.py` + `test_dispatch_scheduler.py` |
| A4 | builder-only set difference = 6 files | `comm -13` identity vs builder | ✅ 6 files（含 `test_context_compact_events.py`） |
| A5 | complete mechanical union = 33 files | `cat identity builder \| sort -u \| wc -l` | ✅ 33 |
| A6 | second amendment allowed-file delta = 5 | 6 builder-only files − 1 早已授权 = 5 | ✅ `test_context_compact_events.py` 是早已授权但不在 identity closure 的 builder-only file |
| A7 | `test_context_compact_events.py` 早已属于 S5 owner tests | plan §9.1 L542 列出，但无 FA/OA/CR hit | ✅ 确认：它在 owner-test 清单且 builder-only |
| A8 | §10.5 五类 pattern 去重可执行且得到 33 | 完整运行 §10.5 脚本 | ✅ 全部 7 个断言通过 |
| A9 | plan 中零残留 "32" total union | `rg '32' plan \| grep -iE 'total\|union\|mechanical'` | ✅ 零命中 |
| A10 | "33" total union 在所有关键位置一致 | `rg '33' plan` 的 7 处命中 | ✅ §9.1(L691)、§10.5(L1081/L1121/L1146)、§13(L1275)、§15(L1392/L1401) |
| A11 | utils 2-file/4-call exact delta | `rg -n FA\|OA` on utils | ✅ 2 files / 4 calls |
| A12 | 每个 call site 持有同一次 AgentRunRequest | 逐行读取 4 个 call chain | ✅ 全部有 typed `request: AgentRunRequest` 或 `self._request: AgentRunRequest` |
| A13 | `AgentRunRequest` 有 `run_id/attempt_id/execution_id/runner_spec` | HEAD 代码 | ✅ 全部存在 |
| A14 | `RunnerSpec` 有 `provider/model` | HEAD 代码 | ✅ 存在 |
| A15 | utils 不在 20-file dirty set | `git diff --stat HEAD \| grep utils` | ✅ exit 1（零命中） |
| A16 | utils 无 CR hit | `rg -l CR_pattern utils` | ✅ 零命中 |
| A17 | two utils 与 25-file tests closure 无重叠 | 文件列表比对 | ✅ 零重叠 |
| A18 | smoke commands 已在 §10.5 冻结 | 逐行核对 §10.5 L1039-L1051 | ✅ 三条命令精确对应 |
| A19 | non-goals 边界无 drift | plan §2 + proposal §7 | ✅ 无越界 |
| A20 | AgentDS A5 arithmetic conclusion 已被正确拒绝 | Controller §2 + §3 + plan §15 | ✅ rejected-set-arithmetic |

**所有 20 项独立复核均通过。**

## 3. 重点维度逐项检查

### 3.1 Identity union 27

独立 `rg` 计数的 FA 37 calls / 21 files、OA 6 calls / 4 files、CR 7 files，三类去重 union = 27 files。结果与 plan §9.1、Controller §1、MiMo review §2 完全一致。

### 3.2 Builder union 8

独立 `rg` 计数的 CB 8 calls / 6 files、RB 7 calls / 4 files，两类去重 union = 8 files。结果与 plan §9.1 表、Controller §1、MiMo review §2 完全一致。

### 3.3 Intersection 精确 2

`comm -12` 确认 27-file identity closure 与 8-file builder closure 的交集精确为：
- `tests/host/test_compaction_operation.py`
- `tests/host/test_dispatch_scheduler.py`

`diff -u` 验证与 plan §9.1 L682-683 声明的 overlap 文件完全一致。

### 3.4 Builder-only set difference = 6

`comm -13` 确认 builder closure 相对 identity closure 的 set difference 精确为 6 files：

1. `tests/host/test_compact_material.py`
2. `tests/host/test_compaction_terminal.py`
3. `tests/host/test_context_compact_events.py`
4. `tests/host/test_memory_projection.py`
5. `tests/host/test_proactive_compaction_operation.py`
6. `tests/host/test_run_input_builder.py`

`diff -u` 验证与 plan §10.5 L1128-1135 的预期 builder-only 列表完全一致。

### 3.5 Complete union = 33

`cat identity builder | sort -u | wc -l` = 33。公式 `27 + 8 - 2 = 33` 成立。

### 3.6 Allowed-file delta = 5（术语边界正确）

6 个 builder-only 文件中，`test_context_compact_events.py` 在第二次 amendment 前已属于 S5 owner-level allowed tests（plan §9.1 L542 列出），但没有 FA/OA/CR hit，因此不在 27-file identity closure 中。其余 5 files 才是第二次 amendment 新增的 allowed-file delta。

Plan §9.1 L687-689 明确表述："其中 `tests/host/test_context_compact_events.py` 在第二次 amendment 前已经属于 S5 allowed owner tests，但没有 FA/OA/CR hit；其余 5 files 才是第二次 amendment 新增的 allowed-file delta"。术语区分精确，没有混淆。

### 3.7 `test_context_compact_events.py` 身份确认

- Plan §9.1 L542：列入 S5 owner-level / behavior tests 允许触及清单 ✅
- 独立 `rg` 确认：CB(3) + RB(4) 命中，但 FA/OA/CR 零命中 ✅
- 结论：早已授权但 identity closure 未覆盖的额外 builder-only 文件 ✅

### 3.8 §10.5 五类 pattern 去重验证

完整运行 plan §10.5 L1083-1136 的去重脚本，所有 7 个 `test` 断言和 2 个 `diff -u` 断言均通过：

```
✅ identity=27
✅ builder=8
✅ overlap=2
✅ builder_only=6
✅ mechanical=33
✅ overlap exact match
✅ builder_only exact match
```

验证脚本直接在 accepted HEAD 上可执行且得到 33，无歧义。

### 3.9 Active 32 / 27+5 零残留

全文扫描 plan，在 total union / mechanical union 上下文中零 "32" 命中。所有 active total-union completion signal 已修正为 33。唯一的 "32" 命中是历史 review artifact 路径中的 timestamp（如 `plan-review-20260801-143257`），不是算术值。

### 3.10 2-file/4-call、identity source、smoke/non-goals/validation 无 drift

#### 2-file/4-call

独立 `rg` 确认两个 utils 共 4 个 direct constructor calls：

| 文件 | 行号 | 构造 |
|------|------|------|
| `utils/smoke_host_public_awaiting_entrypoint.py` | 2010 | `FinalAnswerData(...)` |
| `utils/smoke_host_public_conversation_memory_scenarios.py` | 1843 | `FinalAnswerData(...)` |
| `utils/smoke_host_public_conversation_memory_scenarios.py` | 1748 | `EngineRunOutcomeFinalAnswer(...)` |
| `utils/smoke_host_public_conversation_memory_scenarios.py` | 1794 | `EngineRunOutcomeFinalAnswer(...)` |

与 proposal §3.1 和 plan §9.1 表完全一致。utils 无 CR hit 也已独立确认。

#### Identity source

每个 call site 的 `AgentRunRequest` 同源性已验证：

- `_AnswerHandle._request: AgentRunRequest`（L1841）— 构造时注入
- `_AcceptingSmokeCompactorRunner.__call__(request: AgentRunRequest, ...)`（L1733）— typed 参数
- `_RejectingSmokeCompactorRunner.__call__(request: AgentRunRequest, ...)`（L1779）— typed 参数
- `_DeterministicCompactWorker.accept(snapshot, request: AgentRunRequest)`（L1644）— typed 参数

proposal §4.1 的字段映射表（run_id/attempt_id/execution_id ← request.*, effective_provider/model ← request.runner_spec.*）与实际代码结构一致。禁止项（不从 config/manifest/相邻 event 反推）完整且可执行。

#### Smoke

Plan §10.5 L1038-1051 冻结三条 smoke 命令：public awaiting、memory-reactive-compact、memory-compact-fallback。命令参数、suite、provider 配置与 proposal §6.2 一致。

#### Non-goals

Plan §2（全局 non-goals）、proposal §7（本次 amendment non-goals）无越界：不修改 production/test/utils 代码、不改变 schema、不新增共享 helper、不把 synthetic `UNAVAILABLE` 当真实 provider evidence、不修改 review artifacts。

#### Validation

Plan §10.5 的 validation closure 完整：
- Pre/post inventory（5 条 rg 命令 + 五类 pattern 去重脚本）
- Full pyright `dayu/ tests/ utils/`
- 三条 smoke（覆盖 4 个 call site 的运行时路径）
- Coverage ≥80% per modified production file
- Registry/JSON/proof/ref/secret checks

utils 的测试/coverage 豁免按项目规则（AGENTS.md："`utils/` 下的脚本默认无需测试、无覆盖率要求"）正确适用，未扩散到 production/tests。

## 4. Architecture boundary review

- Engine `SuccessfulRunnerResponseIdentity` 是唯一 business owner ✅
- Host 只机械验证/携带/持久化 ✅
- 两个 utils 是 synthetic smoke fixture consumer，不成为第二套 identity owner ✅
- 没有跨层反向依赖 ✅
- S4 guard 不预埋 optional identity，S5 在 winner writer 上做 required typed extension ✅

## 5. Overcoupling review

- File-local private helper 只封装 typed construction，不跨文件共享 ✅
- 不允许跨文件万能 helper、共享 singleton identity ✅
- Identity 字段从同一个 `AgentRunRequest` 直接取，不存在跨 event/operation/attempt 的隐式依赖 ✅
- 5-file allowed-file delta 只参与的 allowed scope 检查，不替代 builder closure 或 complete union ✅

## 6. Overengineering review

- 只新增 2 个 file-local narrow helper + 模块级常量 + 4 个 required identity 参数构造/透传 ✅
- 不增加 abstraction、framework、shared module、通用 builder、migration 或 compatibility layer ✅

## 7. Findings

**无 material findings。**

本次独立 re-review 对 Controller 裁决的 accepted arithmetic finding 做闭合复核，所有 claims 均通过独立 evidence 验证：

1. Identity union 27、builder union 8、intersection 2、builder-only set difference 6、complete union 33 — 全部通过独立 `rg` + `comm` + `sort -u` 复核。
2. Second amendment 新增 allowed-file delta 仍为 5；术语没有混淆（5-file delta ≠ 6-file builder-only set difference）— 已通过文件级比对确认。
3. `test_context_compact_events.py` 是早已授权但 identity closure 未覆盖的额外 builder-only 文件 — 已在 plan §9.1 owner-test list 和独立 inventory 中双源确认。
4. §10.5 五类 pattern 去重验证可执行并得到 33 — 完整运行脚本，全部 7 个 test 断言 + 2 个 diff -u 断言通过。
5. Active 32 / 27+5 total 公式零残留 — 全文 `rg` 扫描 confirmed。
6. 2-file/4-call exact delta — 独立 `rg` + 逐行代码阅读 confirmed。
7. Identity source — 每个 call site 的 `AgentRunRequest` 同源性已逐行验证。
8. Smoke/non-goals/validation — 对照 plan §2 + §10.5 + §13 + proposal §7，无 drift。

Controller adjudication（`gateflow-wu-cli-interactive-02-s5-utils-closure-amendment-review-adjudication-20260802.md`）中 MiMo finding 001 的 fix 已正确落地：plan 的 total union 修正为 33，术语边界（5-file allowed-file delta vs 6-file builder-only set difference vs 33-file union）在所有相关位置一致。AgentDS 的 arithmetic conclusion（27+5=32）已正确被拒绝且不会复活。

本次 review 完全独立于既有 MiMo/AgentDS review pass——所有计数、断言、代码证据均从 accepted HEAD 直接重新运行，不依赖任何既有 review 的 intermediate result。

## 8. Open questions

无阻塞 open question。

以下决策已在 plan/proposal/adjudication 中固定，implementation 不得自行改向：

- 两个 utils file-local helper 的签名、参数 required 且无 default
- 4 个 call site 的 identity 字段映射（proposal §4.1 表冻结）
- `UNAVAILABLE + None` 策略
- Zero behavior/output/provider/oracle delta
- 5-file allowed-file delta ≠ 6-file builder-only set difference ≠ 33-file complete union

## 9. Residual risks

| 风险 | 分类 | 跟踪 |
|------|------|------|
| accepted HEAD 后出现新的 tests/utils constructor 或 typed-return consumer | 已由 plan §9.1/§10.5 覆盖 | implementation pre-inventory fail closed → Controller |
| `RunnerSpec` 包含 `endpoint`/`api_key_ref`/`headers` 敏感字段 | 已由 plan §9.2/§10.5 覆盖 | identity type 只取 `provider`/`model`；canary 扫描 |
| synthetic smoke identity 被误当真实 provider continuity evidence | 已由 plan §9.7/§12.1 覆盖 | 行为项 29/G06 外部 evidence owner 不变 |
| 3 条 smoke 命令因环境/API key 无法运行 | 已知 gap | plan §10.5 已分类，不用 fake 替代 |
| 两个 utils 未新增测试/coverage | 已由项目规则覆盖 | pyright + inventory + smoke + code review 是 required closure |
| 未知终端 Shift+Enter sequence | 已由 plan §12.1 覆盖 | 只支持证实 sequence |

无 unclassified residual risk。

## 10. Final plan review conclusion

**结论：PASS**

第三次 S5/F13 utils-closure amendment 的 accepted arithmetic finding 已完全闭合。所有独立复核的 claims 均通过：

- Identity union 27、builder union 8、intersection 2、builder-only set difference 6、complete union 33 — 全部独立 verified。
- §10.5 五类 pattern 去重脚本可执行且 yield 33 — 全部 9 个断言 passed。
- Active 32 / 27+5 零残留 — confirmed by full-text scan。
- 5-file allowed-file delta 与 6-file builder-only set difference 的术语边界在所有关键位置一致。
- `test_context_compact_events.py` 的正确身份（早已授权但 identity closure 未覆盖）已双源 confirmed。
- 2-file/4-call、identity source、smoke/non-goals/validation 无 drift。
- Plan 的 §9.1、§10.5、§13、§15 修订与 Controller 裁决完全一致。

Plan 处于 `code-generation-ready / accepted-finding-fix-applied / awaiting-re-review` 状态；本次 re-review 验证了 accepted finding 已闭合。Controller 最终复核后可进入 accepted plan amendment commit → S5 implementation。

---

本 artifact 不修改任何既有文件，不 commit、push 或 PR。

Artifact path: `docs/reviews/plan-review-20260802-002027.md`
