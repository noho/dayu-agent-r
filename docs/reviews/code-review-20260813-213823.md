# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `662c9ad4`
- Output file: `docs/reviews/code-review-20260813-213823.md`
- Included scope: UF-FIX03-S1 未提交 changes，11 files changed (+821, -39)
  - Production: `dayu/fins/ingestion_runtime.py`, `dayu/fins/pipelines/docling_upload_service.py`, `dayu/fins/pipelines/sec_upload_workflow.py`, `dayu/fins/pipelines/cn_pipeline.py`, `dayu/fins/service_runtime.py`
  - Tests: `tests/fins/test_docling_upload_service.py`, `tests/fins/test_sec_pipeline_upload_filing_stream.py`, `tests/fins/test_sec_pipeline_upload_material_stream.py`, `tests/fins/test_cn_pipeline.py`, `tests/fins/test_fins_ingestion_runtime.py`, `tests/fins/test_fins_service_runtime.py`
  - Intent/contract: `docs/gateflow/uf-fix03-summary-bounded-errors-plan-20260813.md` S1, `docs/gateflow/uf-fix03-s1-implementation-20260813.md`
- Excluded scope: S2 (empty/corrupt/label), S3 (CLI/docs/direct no-artifact), `dayu/host/**`, `dayu/engine/**`, `dayu/runtime/**`, `dayu/config/**`, `dayu/cli/**`, `dayu/service/**`, `docs/cli_ci_scenarios.json`, `docs/cli_ci_oracles.json`, README
- Parallel review coverage: 无

## Findings

未发现实质性问题。

以下逐项说明 review 覆盖的审计路径与结论：

### 1. original-only stored count

`docling_upload_service.py:518` — `_store_upload_assets` 逐 asset 落盘后，仅当 `asset.source == _ASSET_SOURCE_ORIGINAL` 时 `stored_original_count += 1`。派生 Docling 资产 (`_ASSET_SOURCE_DOCLING`) 不计数。`_PendingFileAsset.source` 已收紧为 `Literal["original", "docling"]`，运行期 provenance 字符串保持不变。`stored_original_count` 传入 `UploadOperationResult(stored_file_count=stored_original_count)`。fingerprint fixture digest 保持 `099dc9636e306c75f1d5d64dd0210123956ba73888e968088c7279baab1d7fdd`，canonical bytes/schema 未改。

**结论**: ✓ — 只计 original，不计 derived。

### 2. commit后消费

`_upload_summary_from_result` (`service_runtime.py:305`) 从 `result.stored_file_count` 读取 pipeline stored count。pipeline result 来自 workflow 的 `upload_result.stored_file_count`。SEC/CN workflow 中 `upload_result` 是 `DoclingUploadService.publish_prepared_upload(...)` 的返回值，该方法只在 `commit_prepared_upload_batch(...)` 成功返回后才构造 `UploadOperationResult(status="uploaded", stored_file_count=...)`。`commit_batch(...)` 抛错时进入 `except` 分支，走既有 failure terminal builder，显式 `stored_file_count=0`，不消费 staged count。

**结论**: ✓ — stored count 只从 commit 成功后的 result 消费。

### 3. 完整状态矩阵含 cancelled

`FinsUploadPipelineResult.__post_init__()` (`ingestion_runtime.py:1193-1219`):
- ok: `stored_file_count >= 1` ✓
- skipped/deleted/failed/cancelled: `stored_file_count == 0` ✓
- 非 ok + positive stored: rejected ✓

`FinsUploadResultSummary.__post_init__()` (`ingestion_runtime.py:1283-1320`):
- ok: `requested >= 1`, `stored == requested` ✓
- skipped: `requested >= 1`, `stored == 0` ✓
- deleted/cancelled/failed: `requested >= 0`, `stored == 0` ✓

测试覆盖: `test_upload_pipeline_count_owner_accepts_complete_status_matrix` 覆盖 5 种合法组合；`test_upload_pipeline_count_owner_rejects_invalid_status_matrix` 覆盖 5 种非法组合（含 `cancelled+1`）；`test_upload_summary_count_owner_accepts_complete_status_matrix` 覆盖 5 种合法 summary 组合（含 `cancelled+0`）；`test_upload_summary_count_owner_rejects_invalid_status_matrix` 覆盖 7 种非法 summary 组合（含 `cancelled+1`）。constructor 与 parser 两条路径均已覆盖。

**结论**: ✓ — `cancelled+0` 被接受，`cancelled+positive` 被拒绝，constructor 与 parser 均已覆盖。

### 4. requested owner

`requested_file_count` 在四个 production summary constructor 点均显式传入 `len(request.files)` / `len(raw_request.files)`:
1. `service_runtime.py:134` — early-cancelled: `len(raw_request.files)`, stored=0 ✓
2. `service_runtime.py:308` — `_upload_summary_from_result`: `len(request.files)`, stored=result ✓
3. `ingestion_runtime.py:3948` — direct runner-unavailable: `len(_raw_upload_request(request).files)`, stored=0 ✓
4. `ingestion_runtime.py:4426` — job runner-unavailable: `len(_raw_upload_request(request).files)`, stored=0 ✓

AST 审计测试 `test_production_upload_count_constructors_are_explicit_and_complete` 断言恰好 4 个 `FinsUploadResultSummary(...)` 调用，全部包含 `requested_file_count` 和 `stored_file_count`。

**结论**: ✓ — 四点全覆盖，无 default。

### 5. durable/direct 同源

- direct RESULT details: `_upload_result_details(summary)` (`ingestion_runtime.py:6350-6354`) 从 `summary.requested_file_count` / `summary.stored_file_count` 读取。✓
- durable JSON: `FinsUploadResultSummary.to_json_summary()` (`ingestion_runtime.py:1363-1364`) 输出 `requested_file_count` / `stored_file_count`。✓
- 两者消费同一 `FinsUploadResultSummary` 实例，不允许各层重算。

**结论**: ✓ — direct 与 durable 从同一 summary 投影。

### 6. progress / fingerprint 不变

- `_PAYLOAD_FILE_COUNT == "file_count"` 保持不变 (`ingestion_runtime.py:249`)。
- `_upload_context_request_progress_payload` (`ingestion_runtime.py:7617`) 仍只包含 `file_count`。
- `_upload_context_summary_progress_payload` (`ingestion_runtime.py:7640-7652`) 从 request progress payload 继承，只追加 `upload_status` 和 `document_id`，不含 `requested_file_count` / `stored_file_count`。
- 测试 `test_start_upload_with_runner_writes_bounded_result_summary` 断言 progress payload 不含新 count 字段。✓
- fingerprint fixture digest 保持 `099dc9636e306c75f1d5d64dd0210123956ba73888e968088c7279baab1d7fdd`。✓

**结论**: ✓ — progress 继续使用既有 `file_count`，fingerprint 未改。

### 7. material 仅机械迁移

- `sec_upload_workflow.py:599`: material success 路径增加 `stored_file_count=upload_result.stored_file_count`。✓
- `sec_upload_workflow.py:627`: material failure 路径增加 `stored_file_count=0`。✓
- `cn_pipeline.py:1205`: CN material success 路径增加 `stored_file_count=upload_result.stored_file_count`。✓
- `cn_pipeline.py:1233`: CN material failure 路径增加 `stored_file_count=0`。✓
- 测试仅在既有 success/failure/cancel 测试上增加 `stored_file_count` 断言，未修改 material generic failure 文案、company-first publication 顺序或任何 material 行为。✓

**结论**: ✓ — material 仅机械补齐 required count，行为未变。

### 8. 所有 constructor 与 tests

`UploadOperationResult(...)` production 6 点:
1. `docling_upload_service.py:317` — skipped, stored=0 ✓
2. `docling_upload_service.py:398` — deleted, stored=0 ✓
3. `docling_upload_service.py:557` — uploaded, stored=stored_original_count ✓
4. `docling_upload_service.py:1419` — cancelled, stored=0 ✓
5. `sec_upload_workflow.py:288` — conversion-cancelled, stored=0 ✓
6. `cn_pipeline.py:908` — conversion-cancelled, stored=0 ✓

`FinsUploadResultSummary(...)` production 4 点: 见上方 §4。

`FinsUploadPipelineResult.from_pipeline_json` 的 `cls(...)` 调用: `ingestion_runtime.py:1238`，关键字集合与 plan §5.2 一致。✓

AST 审计测试精确断言 4 个 summary + 6 个 operation + 1 个 pipeline parser cls。✓

**结论**: ✓ — constructor inventory 完整，无遗漏无 default。

### 9. `uploaded_files` 零残留

`rg -n '\buploaded_files\b' dayu tests --glob '*.py'`: 零命中。`to_json_summary()` 已用 `requested_file_count` / `stored_file_count` 替换 `uploaded_files`。payload 中的 `uploaded_files` key 已从 `_store_upload_assets` 返回的 payload 中删除。✓

### 10. pyright

`python -m pyright dayu/fins/ingestion_runtime.py dayu/fins/pipelines/docling_upload_service.py dayu/fins/pipelines/sec_upload_workflow.py dayu/fins/pipelines/cn_pipeline.py dayu/fins/service_runtime.py`: `0 errors, 0 warnings, 0 informations`。✓

### 11. Tests

`pytest -q` (6 focused test files): `283 passed, 3 warnings in 5.25s`。3 warnings 均为 edgar deprecated import，非本 slice 新增。✓

### 12. commit failure regression

`test_upload_filing_commit_failure_never_publishes_stored_count` — SEC filing `commit_batch` 分别注入 `OSError` 和 `RuntimeError`：terminal 均为 `failed`、`stored_file_count=0`，既有分类保持 `storage/storage_io` 与 `runtime/unexpected_runtime`，published tree SHA 保持空值，`rollback_tokens == []`（commit ownership transfer 后不发生 caller rollback）。✓

## Open Questions

无。

## Residual Risk

1. **`cn_pipeline.py` 整文件 coverage 为 68%**（目标 ≥80%）。未覆盖行集中在既有 download/facade/helper 分支 (`1349-1376, 1421-1438, 1468-1533, 1554-1567, 1593-1779`) 以及既有 conversion-cancelled catch (`908-940`)。S1 修改的 filing success/failure、material success/failure、count owner/parser 与 CN service-normalized cancelled 路径已有行为测试。分类为 **assigned to later work unit**，owner 为 CN pipeline 既有 download/facade 测试覆盖。本 slice 不存在未分类 correctness finding。

2. **S2/S3/UF-PF03 未实施**。empty/corrupt/label (S2)、CLI bounded stderr/direct no-artifact (S3)、真实 CLI evidence (UF-PF03) 均为 approved later work，本轮未运行、未伪报。

## Verdict

**PASS** — S1 实现与 plan contract 完全一致，无实质性 findings。
