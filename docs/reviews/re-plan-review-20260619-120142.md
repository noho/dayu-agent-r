# Re-Plan Review: Conversation Memory smoke/log diagnostics and smoke coverage boundary

## Reviewed Target

- **Plan artifact**: `docs/reviews/wu-cm-conversation-memory-smoke-log-diagnostics-plan-20260619-115520.md`
- **Adjudication**: `docs/reviews/wu-cm-conversation-memory-smoke-log-diagnostics-plan-review-adjudication-20260619-120027.md`
- **Previous review**: `docs/reviews/plan-review-20260619-115816.md`
- **Re-review timestamp**: 20260619-120142

## Purpose

判断此前 plan review 的 5 个 findings 是否已在 updated plan 和 adjudication 中修复或仍阻塞 implementation。

## Finding Status Summary

| ID | 严重程度 | 简述 | Adjudication Status | Re-review Verdict |
|----|----------|------|---------------------|-------------------|
| 01 | 中 | 缺少 `_durable_options_from_open_host_options()` 实现细节 | rejected-with-reason | ✅ 已解决 |
| 02 | 中 | `_compact_audit_report_from_rows()` 测试复用路径不明确 | accepted | ✅ 已修复 |
| 03 | 低 | `proposal_manifest_ref` 缺失时的 failure_stage 映射过于绝对 | accepted | ✅ 已修复 |
| 04 | 中 | stdout 换行规则缺少具体实现指导 | accepted | ✅ 已修复 |
| 05 | 低 | Slice 2 README 边界说明可能过于详细 | accepted | ✅ 已修复 |

## Detailed Finding Analysis

### Finding 01: `_durable_options_from_open_host_options()` 实现路径不明确

- **Adjudication status**: rejected-with-reason
- **Adjudication reason**: 函数已存在于 `utils/smoke_host_public_conversation_memory_scenarios.py`，构造 `HostDurableStoreOptions` from `OpenHostOptions` using existing imports
- **Re-review verification**: ✅ 已验证函数存在
  - `utils/smoke_host_public_conversation_memory_scenarios.py:3121` 定义 `_durable_options_from_open_host_options(options: OpenHostOptions) -> HostDurableStoreOptions`
  - `utils/smoke_host_public_conversation_memory_scenarios.py:3108` 已有调用点
- **Re-review verdict**: **已解决**。Adjudication 正确识别此 finding 基于错误假设（函数不存在），实则函数已存在且可直接使用。无需 plan 修改。

### Finding 02: `_compact_audit_report_from_rows()` 测试复用路径不明确

- **Adjudication status**: accepted
- **Plan fix applied**: ✅ 是
- **Evidence in updated plan**:
  - Slice 1 Exact allowed changes (line 181): "`_compact_audit_report()` 只能负责 durable store 打开与 row 读取，必须直接返回 `_compact_audit_report_from_rows(rows)`，禁止复制 report 构造逻辑。"
  - Data Flow (line 154): "调用 `_compact_audit_report_from_rows(rows)` 生成完整 report；该纯函数内部复用 `_compact_audit_summary_from_rows(rows)` 和 `_compact_request_trigger_sources(rows)`。"
- **Re-review verdict**: **已修复**。Plan 明确要求 `_compact_audit_report()` 必须调用 `_compact_audit_report_from_rows()`，禁止逻辑重复。测试只覆盖 from_rows 函数。

### Finding 03: `proposal_manifest_ref` 缺失时的 failure_stage 映射过于绝对

- **Adjudication status**: accepted
- **Plan fix applied**: ✅ 是
- **Evidence in updated plan**:
  - Implementation Decisions #4 (line 128): "该 stage 映射依赖当前 Host 记录 proposal manifest 的事实：manifest 在 proposal run input 成功准备后才可能出现。若后续 Host 改变 manifest 写入时机，diagnostic smoke 需要随生产 payload 语义重新校准。"
- **Re-review verdict**: **已修复**。Plan 明确标注映射依赖当前 Host 实现，未来需校准。符合 Finding 03 的建议。

### Finding 04: stdout 换行规则缺少具体实现指导

- **Adjudication status**: accepted
- **Plan fix applied**: ✅ 是
- **Evidence in updated plan**:
  - Implementation Decisions #6 (line 136-139): 明确列出需要 `flush=True` 的具体 print 站点：
    - "现有 `run_smoke()` 中关键 summary 行使用 `print(..., flush=True)`：最终 `SMOKE TOOL_CALLS_BY_KEY`、compact artifact summary、compact audit summary、operation / histogram / detail 行、compact acceptance、`SMOKE PASS`、workspace kept。"
    - "`main()` 的 `SMOKE FAIL` 使用 `print(..., file=sys.stderr, flush=True)`，确保异常路径不会和最后一个 stdout summary 粘连。"
  - Slice 1 Tests (line 216): "使用 `capsys` 或格式化 helper 断言 stdout line 均以独立 `SMOKE` 前缀开始；异常路径至少通过 `main()` 的 `SMOKE FAIL` flush 单元断言覆盖。"
- **Re-review verdict**: **已修复**。Plan 提供了具体的 flush 站点列表和测试断言要求，implementation agent 可直接执行。

### Finding 05: Slice 2 README 边界说明可能过于详细

- **Adjudication status**: accepted
- **Plan fix applied**: ✅ 是
- **Evidence in updated plan**:
  - Slice 2 Exact allowed changes (line 247): "当前 smoke 不覆盖 memory correctness 全量验证；不要在 README 提及内部 issue 编号或把未来 eval 写成已实现能力。"
- **Re-review verdict**: **已修复**。Plan 明确禁止提及内部 issue 编号，符合根 README 的 Agent 更新约束。

## Additional Adjudication Findings (from DS review)

Adjudication 还记录了 AgentDS 的 findings，均已 accepted 并修复：

| DS ID | 简述 | Plan Fix Evidence |
|-------|------|-------------------|
| DS-01 | tests need boundary / error paths | Slice 1 Tests (line 209-215): 增加 empty rows、payload non-object、empty diagnostic refs、helper type mismatch、missing operation id 测试 |
| DS-02 | `_compact_audit_report` signature consistency | Slice 1 (line 179): 保持 keyword-only `*, session_id: str` |
| DS-04 | diagnostic suffix algorithm under-specified | Implementation Decisions #4 (line 120): 明确 split / offset / fallback 算法和两个示例 |
| DS-05 | typed payload helper mismatch behavior undefined | Implementation Decisions #2 (line 109): 明确字段缺失和类型不匹配返回 `None` 或空 tuple |

## Open Questions Status

此前 review 的 4 个 open questions：

1. **`_durable_options_from_open_host_options()` 的实现路径**
   - **Status**: ✅ 已解决。函数已存在，adjudication 正确识别。

2. **现有 `_compact_row_trigger_source()` 和 `_compact_row_operation_id()` 是否需要重构以复用 `_compact_row_payload()`**
   - **Status**: ✅ 已在 plan 中明确。Slice 1 (line 196): "修改 `_compact_row_trigger_source()` 与 `_compact_row_operation_id()` 复用 `_compact_row_payload()`。"

3. **`diagnostic_refs` 的解析规则是否稳定**
   - **Status**: ✅ 已在 plan 中明确。Implementation Decisions #4 (line 120): 详细描述 split / offset / fallback 算法。

4. **tests/README.md 是否有类似根 README 的 Agent 更新约束**
   - **Status**: ⚠️ 未在 plan 或 adjudication 中明确，但不影响 implementation。tests/README.md 的更新范围已由 plan 约束（只补充测试边界说明）。

## Residual Risks Status

此前 review 的 4 个 residual risks：

1. **Production memory compact failure 未修复**
   - **Status**: ✅ 已明确。Plan non-goals (line 63) 和 Risks (line 332) 均声明后续 work unit。

2. **Real LLM long25 验证成本高**
   - **Status**: ✅ 已明确。Slice 3 (line 295): "Long smoke may call a real LLM and be costly / flaky. If not run, closeout must state that unit / assembly tests covered the no-real-LLM diagnostics path."

3. **EventLog payload 字段稳定性**
   - **Status**: ⚠️ 仍存在但已降低风险。Plan 声明依赖当前 payload 语义（line 128），但未提供 payload schema 版本检测机制。风险可接受，因为 smoke 是诊断工具，非 production 关键路径。

4. **`fallback_action=dispatch` 的 hard fail 语义**
   - **Status**: ✅ 已明确。Non-goals (line 62): "不因为 `fallback_action=dispatch` 放宽验收；`memory-compact` 中任何 `CONTEXT_COMPACTION_FAILED` 仍是 hard fail。"

## Conclusion

**pass**

此前 plan review 的 5 个 findings 均已解决或修复：

- Finding 01: rejected-with-reason（函数已存在）
- Finding 02-05: accepted 并在 updated plan 中修复

Adjudication 记录的 AgentDS findings 也均已修复。Updated plan 现在是 code-generation-ready，implementation agent 可直接执行 Slice 1-3。

唯一未完全收敛的 open question（tests/README.md 的 Agent 更新约束）不阻塞 implementation，因为 plan 已约束更新范围。

Residual risks 均已在 plan 中明确声明，属于可接受的 tradeoff。

## Recommendation

Implementation agent 可开始执行。建议按 Slice 1 → Slice 2 → Slice 3 顺序执行，每个 slice 完成后运行 validation commands 确认。
