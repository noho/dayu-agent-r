# Code Review (Re-review)

## Scope

- Mode: current changes (re-review post code-review-fix)
- Branch: `codex/interactive-oracle`
- Base: `21b602c1`
- Output file: `docs/reviews/code-review-20260803-222626.md`
- Review role: 独立 re-review，核验 Controller 对 F01/F02/F03 的裁决及修复完整性
- Previous review: `docs/reviews/code-review-20260803-221641.md`（F01/F02/F03，`pass-with-findings`）
- Fix artifact: `docs/gateflow/pr-190-compactor-output-business-semantics-s1-code-review-fix-20260803.md`
- Included scope: PR 190 S1 五个 dirty files（prompt owner、owner test、public smoke、publication manifest、CLI hash test）+ fix artifact
- Excluded scope: 第一路 review `docs/reviews/code-review-20260803-220950.md`（AgentMiMo，`pass`）；已提交 plan gate 全量历史；所有 frozen production files（`compaction.py`、`context_governance.py`、`memory.py`、`conversation_compaction.md`）；frozen oracle/scenario/`cli_ci.md`；所有 README/design
- Parallel review coverage: 无

## Context read

- 前次 review `docs/reviews/code-review-20260803-221641.md`（F01 中、F02 低、F03 低，`pass-with-findings`）
- Fix artifact `docs/gateflow/pr-190-compactor-output-business-semantics-s1-code-review-fix-20260803.md`（Controller 裁决与修复记录）
- `tests/host/test_llm_compaction.py`（fix 后全文，重点行 329-449）
- `dayu/config/prompts/scenes/conversation_compaction_user.md`（prompt owner，fix 前后未变）
- `tests/host/test_public_compact_smoke.py`（fix 前后未变）
- `docs/cli_init_workspace_manifest_v1.json`（fix 前后未变）
- `tests/cli/test_smoke_cli_init_provider_matrix.py`（fix 前后未变）

## Controller Adjudication Verification

### F01 — `accepted` → 独立核验：已修复

Controller 裁决：在 owner test 中补齐 7 个 frozen semantic fragment 的独立 assertion。

独立核验方法：对 prompt 文本与 test 文本做逐 fragment 交叉检查。

| # | Fragment | 在 prompt 中 | 在 owner test 中 | 状态 |
|---|---|---|---|---|
| 1 | `不得发明新结论` | ✓ (`answer_anchors` detail) | ✓ (line 400) | **已覆盖** |
| 2 | `继续保留旧内容会过时、冲突或误导` | ✓ (`superseded` reason) | ✓ (line 412) | **已覆盖** |
| 3 | `replacement 中保留的是替代后的当前内容` | ✓ (`superseded` reason) | ✓ (line 413) | **已覆盖** |
| 4 | `丢弃它不会损失独立业务信息` | ✓ (`redundant` reason) | ✓ (line 415) | **已覆盖** |
| 5 | `不得加入材料没有的事实、结论或任务` | ✓ (`session_summary.text`) | ✓ (line 377) | **已覆盖** |
| 6 | `不得把 trace_material 或 answer_material 当作事实依据` | ✓ (`evidence_facts.claim`) | ✓ (line 385) | **已覆盖** |
| 7 | `不把工具证据、未来动作或新推断伪装成既有结论` | ✓ (`answer_anchors` section) | ✓ (line 393) | **已覆盖** |

直接证据：
- `test_llm_compaction.py:374-383`（`session_summary_semantics` for loop）：8 条 assert，含 fragment 5
- `test_llm_compaction.py:384-391`（`evidence_fact_semantics` for loop）：6 条 assert，含 fragment 6
- `test_llm_compaction.py:392-403`（`answer_anchor_semantics` for loop）：8 条 assert，含 fragment 1、7
- `test_llm_compaction.py:404-421`（`drop_reason_semantics` for loop）：14 条 assert，含 fragment 2、3、4

每个 fragment 均为独立 `assert fragment in user_prompt`，不再依赖前缀匹配间接覆盖。七个 fragment 在 prompt 中存在且在 test 中被断言，闭环。

**F01 裁决核验结论：`accepted`，修复完整，无遗漏。**

### F02 — `accepted` → 独立核验：已修复，无误禁

Controller 裁决：扩展 static forbidden guard，覆盖当前活跃内部术语。

修复内容（`test_llm_compaction.py:422-436`）：

```python
for forbidden in (
    "schema_version",
    "current_input_anchor",
    "previous_compacted_view",
    "evidence_backed_facts",
    "reference_continuity_items",
    "Compact",                    # ← 新增：前缀覆盖 Compact* 类型名
    "compaction.py",              # ← 新增：内部模块名
    "context_governance",         # ← 新增：内部模块名
    "memory.py",                  # ← 新增：内部模块名
    "MemoryProjectionPolicy",     # ← 新增：Host 治理类型
    "SessionSummaryMemoryView",   # ← 新增：Host 内部类型
    "event_id",                   # ← 新增：内部治理标识
    "payload_ref",                # ← 新增：内部治理标识
):
    assert forbidden not in user_prompt
```

独立核验：

**覆盖检查**：13 个 forbidden term 全部在 test 中存在，全部在 prompt 中 absent。`Compact`（大写 C）大小写敏感，不会误禁 prompt 中的业务必需小写 `compact`（出现于 `dayu.context_compaction.output.v2`、`dayu.context_compaction.input.v2`、`<<compaction_request>>`、`UNTRUSTED_COMPACTION_MATERIAL` 等）。

**无误禁检查**：
- `"Compact" in "dayu.context_compaction.output.v2"` → `False`（小写 `c`）
- `"Compact" in "<<compaction_request>>"` → `False`
- `"compaction.py" in "<<compaction_request>>"` → `False`（不含 `.py`）
- 其余 6 个新增 forbidden term 在 prompt 中均为 absent（独立全文搜索验证）

**F02 裁决核验结论：`accepted`，修复完整，无误禁。**

### F03 — `rejected-with-reason` → 独立核验：驳回成立

Controller 裁决：F03（substring 前缀匹配策略系统性漏检同一 sentence 后半句）与 F01 同根，无独立 root cause。

独立核验：
- F03 描述的现象（前缀匹配漏检后半句）是 F01 中 7 项 missing fragment 的产生机制，不是独立缺陷
- F01 的修复方案（每个独立语义判断有独立 assertion，不再依赖前缀）同时消除了 F03 描述的风险
- F03 不需要独立的代码修改、测试补充或 owner 变更
- 修复后，没有任何 fragment 仅通过前缀匹配间接覆盖——每个语义判断都有 exact substring assertion

**F03 裁决核验结论：`rejected-with-reason`，驳回成立，修复后证据不再成立。**

## Scope Drift Check

### Prompt bytes 未漂移

- `sha256sum dayu/config/prompts/scenes/conversation_compaction_user.md` → `a2f5711c84f6fdd51f921e5d266d05cdb3f6a34a6c8321ffc42f0c5dc75a0dce`
- 与 implementation artifact、前次 review 记录一致

### Publication manifest 未漂移

- `sha256sum docs/cli_init_workspace_manifest_v1.json` → `fb6d0ba8fbf01b093419d178daf09c145bc8643e03b900703a91f2a3ff005f6c`
- 与 `FROZEN_MANIFEST_SHA256` 一致
- 与 implementation artifact、前次 review 记录一致

### Public smoke 未漂移

- `sha256sum tests/host/test_public_compact_smoke.py` → `64ade7605786c2308e16e087e37ee4fa5f519886113cbe33923144a526c33e31`
- 与 fix artifact 记录一致

### CLI hash test 未漂移

- `FROZEN_MANIFEST_SHA256` 值未变：`fb6d0ba8fbf01b093419d178daf09c145bc8643e03b900703a91f2a3ff005f6c`

### Modified files scope

`git diff --name-only` 仍只包含 5 个 allowed implementation files。没有文件新增到 diff、没有文件从 diff 消失。

### Review artifacts preserved

- `docs/reviews/code-review-20260803-220950.md`：`sha256:3d1547d2...`（与 fix artifact 记录一致）
- `docs/reviews/code-review-20260803-221641.md`：`sha256:d62942a5...`（与 fix artifact 记录一致）

### Owner test hash

- `tests/host/test_llm_compaction.py`：`sha256:4d77165a473467c8fd57964e06c3b07cc5679e05917c093d98d637af6974eac0`
- 与 fix artifact 记录一致

**Scope drift 结论：无漂移。prompt、manifest、两级 hash、public smoke、review artifacts 全部 preserved。唯一变化的文件是 owner test（`test_llm_compaction.py`），变化范围严格限于 F01/F02 修复。**

## Validation

| Command | Result |
|---|---|
| `pytest tests/host/test_llm_compaction.py::test_prompt_assets_are_self_contained_for_fresh_v2_contract -q` | `1 passed` |
| `pytest tests/host/test_public_compact_smoke.py::test_default_compactor_prompt_is_llm_facing_and_self_contained -q` | `1 passed` |
| `pytest tests/host/test_llm_compaction.py tests/host/test_public_compact_smoke.py -q` | `54 passed, 1 skipped` |
| `pytest tests/host/test_memory_projection.py -q -k 'accepted_compact_without_summary_clears_prior_session_summary'` | `1 passed` |
| `python -m pyright dayu/ tests/ utils/` | `0 errors, 0 warnings, 0 informations` |
| `git diff --check` | pass，无输出 |

skipped test 是 opt-in real compactor smoke（`DAYU_RUN_REAL_COMPACTOR_SMOKE=1` 未设置），符合预期。

## Findings

未发现实质性问题。

三项前次 finding 的 Controller 裁决均已独立核验通过：F01 七项 fragment 全部有 durable assertion；F02 forbidden guard 覆盖完整且无误禁；F03 驳回成立。Prompt/manifest/hash/scope 无漂移。全部验证通过。

## Open Questions

无。

## Residual Risk

- `assigned to later work unit`：真实 provider 对字段分类、drop reason 与 repair cap 的稳定遵循度；owner 为 real Compactor conformance evidence work unit
- `assigned to later work unit`：frozen oracle/scenario 的 current-head readiness refresh；owner 为独立 readiness refresh work unit
- `assigned to later work unit`：`forward_intents.status` 与 `reference_continuity.reason` 的 LLM-facing 业务语义；owner 为后续独立 LLM-facing schema work unit
- `fixed in current slice`：F01 七项 frozen semantic regression blind spots、F02 活跃内部术语 guard 缺口、F03 重复 finding

所有 residual risk 均已分类并有 owner。无未分类 residual risk。

## Verdict

`pass`

F01/F02 修复完整且经独立逐项核验通过；F03 驳回成立；prompt/manifest/hash/scope 零漂移；全部 focused tests、public smoke、memory regression、pyright 与 diff check 通过。本 slice 已可进入 accepted slice checkpoint。
