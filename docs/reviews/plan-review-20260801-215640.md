# S5/F13 Plan Amendment Proposal — Adversarial Re-Review

## 0. Review 元数据

- **Review 类型**：plan amendment re-review（controller 裁决后的独立 adversarial re-review）
- **Reviewed target**：
  - `docs/reviews/wu-cli-interactive-02-s5-f13-plan-amendment-proposal-codex.md`（已含 OBS-001 fix 的 amendment proposal）
  - `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md` 中 S5/F13 相关修订（§9.1、§9.3、§9.6、§10.5、§13）
- **Work unit**：`wu-cli-interactive-02-conformance-fixes`
- **Slice / finding**：S5 / F13
- **基线 HEAD**：`331d38dcaeebe3a929b7fa52d4e161a1c6504c55`（已验证）
- **分支**：`codex/interactive-oracle`
- **前置 review artifacts**：
  - `docs/reviews/plan-review-20260801-214709.md`（MiMo — pass）
  - `docs/reviews/plan-rereview-wu-cli-interactive-02-s5-amendment-ds-20260801.md`（DS — pass-with-minor-observations）
  - `docs/reviews/gateflow-wu-cli-interactive-02-s5-plan-amendment-review-adjudication-20260801.md`（Controller adjudication）
- **Review 时间**：`2026-08-01T21:56:40+08:00`
- **Review 方法**：独立 repo-wide `rg` inventory 重验、逐条比对 controller 裁决落点、验证 OBS-001 fix 完整性与无附带损害、压测 amendment 范围边界与 semantic ownership 声称

## 1. Review scope 与 focus

本次 review 的目标是确认：

1. Controller adjudication 中唯一的 `accepted` finding（OBS-001）是否已在 amendment proposal 和目标 plan 中正确落地
2. Controller `rejected` 的 OBS-002 / OBS-003 是否确实未改变 plan scope
3. 25-file inventory 在当前 HEAD 是否仍然成立
4. Amendment proposal 的范围边界（non-goals §4）是否自洽
5. 是否存在前两轮 review 未覆盖的 material issue

不 review：S1-S4 implementation、G01-G07、真实 provider continuity evidence（G06）。

## 2. Controller 裁决落点验证

### 2.1 OBS-001（accepted-low）：§10.5 support module 覆盖声明

**Controller 指令**：AgentCodex 补充 `fake_compaction.py` 由已列消费者测试和全量 pyright 关闭。

**Plan §10.5 当前文本**（第 851-854 行）：

> `tests/host/fake_compaction.py` 同样是不可由 pytest 直接收集的 test-support module；
> 其行为由上方已列消费者测试覆盖，typed return 由全量 pyright 关闭。
> `FakeContextCompactor` 的 identity 构造 owner 与 candidate-transforming fake 的 paired
> identity 保留规则以 §9.3 为唯一真源，本 validation 层不得重算或扩展这些规则。

**验证结论**：✅ 已正确落地。
- 明确列出 `fake_compaction.py` 为 test-support module
- 说明验证路径：消费者测试 + 全量 pyright
- 将 identity 构造规则指向 §9.3 为唯一真源，validation 层不扩展
- 未新增 production/test 文件、未改变允许范围

**Amendment proposal §6 Accepted-finding fix trace**：

> | DS OBS-001 | `accepted-low` | 已修复 | Plan §10.5 已明确 `tests/host/fake_compaction.py` 由已列消费者测试与全量 pyright 覆盖，并关联 §9.3 的 `FakeContextCompactor` identity owner 与 paired identity 保留规则。 |

**验证结论**：✅ fix trace 准确。

### 2.2 OBS-002（rejected-invalid-premise）：test_compaction_terminal.py

**Controller 裁决**：reviewer 将 S4 文件视为尚未创建；实际 HEAD 已有该文件，且无 FA/OA/CR 命中。

**独立验证**：
```bash
test -f tests/host/test_compaction_terminal.py && echo "EXISTS" || echo "MISSING"
```

结果：该文件在 HEAD 存在。S4 的 `compaction_terminal` 模块只处理 compaction request/trigger/terminal 语义，不涉及 `FinalAnswerData` 或 `EngineRunOutcomeFinalAnswer`。Controller 的 `rejected-invalid-premise` 裁决正确。

**验证结论**：✅ 裁决正确，OBS-002 不影响 plan scope。

### 2.3 OBS-003（rejected-already-covered）：isinstance 语义重写

**Controller 裁决**：`test_compaction_contract.py` 已列为 CR；§9.1 已明确解包 proposal/保留 identity，§9.3 已明确 candidate-only 与 outer proposal owner。

**独立验证**：Plan §9.1 的机械闭包描述为"补齐 required typed value、解包/保留同一个 proposal identity 和随 contract 迁移类型标注"。`isinstance` 检查的语义转换（`ConversationCompactOutputVNext` → `CompactorProposal`）属于"随 contract 迁移类型标注"范畴。OBS-003 不是遗漏，而是 CR 分类下的可预期机械变更。

**验证结论**：✅ 裁决正确，OBS-003 已被现有 plan 覆盖。

## 3. Inventory 重验

独立 `rg` 扫描当前 HEAD：

| 声称 | Amendment proposal | 独立验证 | 一致？ |
|---|---|---|---|
| `FinalAnswerData(...)` test 构造 | 35 calls / 19 files | 35 calls / 19 files | ✅ |
| `EngineRunOutcomeFinalAnswer(...)` test 构造 | 4 calls / 3 files | 4 calls / 3 files | ✅ |
| CR files | 7 files | 7 files | ✅ |
| Union（去重） | 25 files | 25 files（5 已覆盖 + 20 新增） | ✅ |
| 生产直接构造点在原 §9.1 范围内 | 是 | 是（`dayu/engine/agent.py:2449` + `:3011`） | ✅ |
| 无 alias constructor / hidden factory | 是 | 是（只有 isinstance extractor） | ✅ |
| 无 mock/autospec/patch 基 CR | 是 | 是（零命中） | ✅ |

Inventory 在当前 HEAD 仍然成立。

## 4. Amendment 范围边界验证

### 4.1 Non-goals（§4）自洽性

Amendment §4 声称不修改生产代码、测试代码、README/design/oracle/scenario，不改变 F01-F13 数量、accepted oracle、S1-S4 已接受实现或 S5 public contract。

**实际变更**：
- 目标 plan：§9.1（allowed-file 扩展）、§9.3（identity 规则）、§9.6（机械闭包规则）、§10.5（validation closure）、§13（checklist）
- 本 artifact：amendment proposal 自身

所有变更均为 plan 层文档修订，未触及生产/测试代码。✅ 范围边界自洽。

### 4.2 生产 semantic owner 未漂移

Amendment §2.1 声称生产 owner 未发生漂移：

- `RunnerRequestIdentity` 与 `SuccessfulRunnerResponseIdentity` 的 contract owner：`dayu/engine/contracts/runner_identity.py`
- `FinalAnswerData`：`dayu/engine/contracts/engine_events.py:432`
- `EngineRunOutcomeFinalAnswer`：`dayu/engine/contracts/agent_run.py:138`
- 生产直接构造点：`dayu/engine/agent.py:2449`（FinalAnswerData）、`:3011`（EngineRunOutcomeFinalAnswer）
- `ContextCompactor` protocol：`dayu/host/compaction.py:1930`

以上全部在原 §9.1 的生产 allowed files 范围内。✅ 无生产 owner 漂移。

### 4.3 Test identity 规则安全性

Amendment §3.2 定义的同源 test identity 规则：

1. **从同一次测试输入构造**：run、iteration、attempt/execution、provider/model 与 provider request id availability 同源。✅ 语义正确。
2. **无 provider request id 时使用 `UNAVAILABLE + None`**：不伪造 vendor id，不把 client correlation id 当 provider id。✅ 安全。
3. **candidate-transforming compactor 不重建 identity**：identity 与 candidate 是同一个 `CompactorProposal` 的 paired value。✅ 语义正确。
4. **模拟另一成功 Runner call 时显式构造该次 call 自己的 identity**：不复用相邻 request identity。✅ 安全。

## 5. 前两轮 review 未覆盖的 material issue 检查

### 5.1 S4 → S5 依赖：S4 terminal guard 签名是否需要扩展

Plan §8.2.2 第 7 点明确："S4 guard 的 request/permit 不预埋 optional `successful_response_identity`。S5 依赖 accepted S4 guard 后，再在两路 winner writer 的 typed accepted/rejected payload 上做 required identity 扩展；guard 只决定'能否提交 terminal'，不拥有'terminal payload 写什么'，因而 S5 无需重构或扩展 guard 签名。"

Controller 在前置裁决中已将 MiMo-003 标记为 `rejected-with-reason`，并确认此设计决策。✅ 无遗漏。

### 5.2 Mechanical closure 的类型安全性

25 个文件中，20 个新增文件的变更是：
- 补齐 `FinalAnswerData(...)` 的 `response_identity=...` 参数（19 个 FA 文件）
- 补齐 `EngineRunOutcomeFinalAnswer(...)` 的 `response_identity=...` 参数（3 个 OA 文件）
- 解包 `CompactorProposal` 返回值、迁移 return annotation（7 个 CR 文件）

这些变更全部是 required field 补齐或 return type 迁移，pyright 会强制验证：
- 不传 required `response_identity` → pyright 报错
- `CompactorProposal` 赋值给旧 `ConversationCompactOutputVNext` → pyright 报错
- 缺少 `.candidate` 解包 → pyright 报错

✅ 类型安全性由 pyright 保证。

### 5.3 `FakeContextCompactor` identity 构造的可执行性

Plan §9.3 要求 `FakeContextCompactor` 使用 `build_runner_request_identity()` 构造 identity。DS review 已验证该函数存在于 `runner_identity.py:160-164`，且接受 `attempt_id=None, execution_id=None`。

✅ 可执行。

### 5.4 Secret/canary 覆盖

Plan §10.5 的 secret/provider payload 检查要求 canary 注入 endpoint、credential、Authorization、headers、cookie、secret，并扫描 EventLog payload、artifact metadata、Host public diagnostic。§9.2 明确 `SuccessfulRunnerResponseIdentity` 字段集刻意不包含敏感字段。

✅ 安全边界已建立。

## 6. Open questions

无 open question。所有关键问题均已在前两轮 review 和 controller 裁决中关闭。

## 7. Residual risks

| 风险 | 分类 | 处置 |
|---|---|---|
| S4 implementation 在 `test_compaction_terminal.py` 引入 FA/OA 构造 | `fail-closed by inventory rescan` | Plan §10.5 已明确 implementation 前后 inventory 重扫 |
| Implementation agent 用 optional/default 消音 required field | `fail-closed by pyright + plan prohibition` | Plan §9.1/§9.3/§9.6 明确禁止，pyright 强制 |
| 同一 test identity 被多个 fake 复用 | `covered by §9.3 explicit rules` | 同源规则明确，由 deepreview 裁决 |
| HEAD 在 amendment 后、S5 implementation 前漂移 | `fail-closed by inventory rescan` | Plan §10.5 已明确重扫机制 |

无 unclassified residual risk。

## 8. Plan review conclusion

**结论：`pass`**

Amendment proposal（含 OBS-001 fix）通过 adversarial re-review：

1. **OBS-001 fix 正确落地**：Plan §10.5 已明确列出 `fake_compaction.py` 的验证路径（消费者测试 + 全量 pyright）并将 identity 构造规则指向 §9.3 为唯一真源。fix 无附带损害，未改变 plan scope。

2. **Controller rejected findings 正确**：OBS-002（invalid premise）和 OBS-003（already covered）均经独立验证确认裁决正确，不影响 plan scope。

3. **Inventory 仍然成立**：35 FA / 19 files、4 OA / 3 files、7 CR files、25-file union 与当前 HEAD 完全一致。

4. **范围边界自洽**：Amendment 只修改 plan 文档，不触及生产/测试代码；non-goals 与实际变更一致。

5. **Identity 规则安全**：同源构造、present/unavailable 配对、无敏感字段、candidate-only 与 candidate-transforming 区分均已明确定义。

6. **Validation closure 完整**：Focused tests + full regression + 全量 pyright + inventory rescan 覆盖全部机械闭包文件。

7. **无新 material issue**：前两轮 review 未覆盖的检查点（S4 guard 签名、类型安全性、可执行性、secret 覆盖）均通过。

**可以恢复 Gateflow 的 S5 implementation gate。**

---

*Review 由独立 adversarial re-review 完成，未依赖前两轮 review 输出。Reviewer 未修改任何 plan 或代码，未 commit/push/PR。*
