# Code Review

## Scope

- Mode: PR
- PR: 190
- Title: fix(cli): close interactive conformance gaps
- Author: noho
- Head: codex/interactive-oracle (b819309c)
- Base: main
- URL: https://github.com/noho/dayu-agent-r/pull/190
- Mergeable: CLEAN
- Draft: true
- Checks: no checks reported on the branch
- Output file: docs/reviews/pr-190-review-20260803-225333.md
- Included scope: PR 相对 main 的完整 diff（390 files, +145427/-15591 lines）；关键实现文件、tests、Gateflow artifacts、README、LLM-facing prompts
- Excluded scope: docs/gateflow/ 下的历史 review artifacts（仅做存在性确认）、docs/reviews/ 下的旧 review 文件、cli_ci_scenarios.json（72K 行测试场景数据）
- Parallel review coverage: 4 subagents 覆盖
  - Subagent 1: compaction v2 schema, context governance, compaction terminal, compact pipeline, LLM prompts
  - Subagent 2: CLI session execution, composer, SIGINT chord, attachment controller
  - Subagent 3: runner identity, dispatch scheduler, engine ingest, run_input, memory
  - Subagent 4: test coverage, CLAUDE.md conformance, README updates

## Findings

### 01-未修复-中-9 个 readable-view 类仍使用 VNext 后缀，schema 常量已迁移为 V2

- **入口/函数**: `dayu/host/compaction.py` 中 9 个公开类型
- **文件(行号)**: `dayu/host/compaction.py` lines 82, 664, 701, 732, 774, 820, 917, 956, 1001
- **输入场景**: 任何导入 `compaction.py` 模块公开类型的调用方
- **实际分支**: schema 常量 `COMPACT_INPUT_SCHEMA_V2` / `COMPACT_OUTPUT_SCHEMA_V2` 和 23 个 candidate/validation 类型已全部使用 `V2` 后缀，但以下 9 个 readable-view 类型仍保留 `VNext` 后缀：
  - `TraceReadableKindVNext` (line 82)
  - `ReadableFactItemVNext` (line 664)
  - `ReadableAnswerAnchorItemVNext` (line 701)
  - `ReadableAnswerAnchorVNext` (line 732)
  - `ReadableForwardIntentVNext` (line 774)
  - `ReadableReferenceContinuityItemVNext` (line 820)
  - `TraceReadableItemVNext` (line 917)
  - `EvidenceReadableItemVNext` (line 956)
  - `AnswerReadableItemVNext` (line 1001)
- **预期行为**: 全部公开类型应统一使用 `V2` 后缀，与 schema literal `dayu.context_compaction.output.v2` 一致
- **实际行为**: 同一模块内存在双命名标准：`CompactCandidateV2` 与 `ReadableFactItemVNext` 共存
- **直接证据**: `compaction.py` line 27 `COMPACT_INPUT_SCHEMA_V2 = "dayu.context_compaction.input.v2"` vs line 82 `class TraceReadableKindVNext(StrEnum)`
- **影响**: 开发者搜索 `V2` 会遗漏 9 个同属 v2 contract surface 的公开类型；命名不一致增加认知负担
- **建议改法和验证点**: 将 9 个类名从 `*VNext` 重命名为 `*V2`，更新所有导入和引用；pyright 验证无新增错误
- **修复风险（低/中/高）**: 低（纯重命名，无语义变化）
- **严重程度（低/中/高/严重）**: 中

### 02-未修复-低-compaction.py 中 61 处 docstring 仍使用 vNext 而非 v2

- **入口/函数**: `dayu/host/compaction.py` 全部 docstring
- **文件(行号)**: `dayu/host/compaction.py` 约 61 处 docstring
- **输入场景**: 阅读 compaction.py 文档的开发者
- **实际分支**: `context_governance.py`、`compaction_terminal.py`、`compact_pipeline.py` 和两个 prompt 文件均无 vNext 引用，但 `compaction.py` 的 docstring 全部使用 "vNext"
- **预期行为**: docstring 应使用 "v2" 或 "严格 v2" 与 schema 版本一致
- **实际行为**: 例如 line 35 `"""vNext compact material pack 的 LLM-facing section。"""`
- **直接证据**: `compaction.py` line 35 vs `context_governance.py` line 1 `"""Host Context Governance 的 deterministic v2 accept owner。"""`
- **影响**: 搜索 "v2" contract 文档会遗漏 compaction.py 的 docstring
- **建议改法和验证点**: 批量替换 docstring 中 "vNext" 为 "v2" 或 "严格 v2"
- **修复风险（低/中/高）**: 低（纯文档变更）
- **严重程度（低/中/高/严重）**: 低

### 03-未修复-低-CompactPipelineAttemptDispatchSnapshot 是空 Protocol

- **入口/函数**: `CompactPipelineProtectedRawTailProvider.load_ordinary_raw_tail`
- **文件(行号)**: `dayu/host/compact_pipeline.py` lines 169-170
- **输入场景**: 任何向 `load_ordinary_raw_tail` 传入 snapshot 参数的调用方
- **实际分支**: Protocol 声明无任何成员，任何对象均满足该协议
- **预期行为**: 若为 forward declaration 应有 docstring 说明；若为 contract 应有成员
- **实际行为**: 空 Protocol，无结构约束
- **直接证据**: `compact_pipeline.py` line 169-170 仅有 class 声明和 docstring，无方法或属性
- **影响**: 无运行时或静态类型安全；调用方可传入任意对象
- **建议改法和验证点**: 添加实际成员或 docstring 说明 forward declaration 意图
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 04-未修复-低-InteractiveComposerCompletionResult 类型别名过宽且无外部消费者

- **入口/函数**: `dayu/cli/session_execution.py` 模块级类型别名
- **文件(行号)**: `dayu/cli/session_execution.py` line 2144
- **输入场景**: 读取 session_execution.py 类型注解的开发者
- **实际分支**: 别名声明为 `_InteractiveComposerCompletion | EntrypointRunTerminalResult | str | int`，但仅用于两处 `wait_tasks` 集合注解
- **预期行为**: 每个 task 应有精确的窄类型注解
- **实际行为**: 宽 union 掩盖了各 task 的实际返回类型差异
- **直接证据**: line 2144 定义，line 1623 和 1809 使用
- **影响**: 误导类型检查器和读者；无运行时影响
- **建议改法和验证点**: 移除别名，各 task 使用具体类型注解或 `set[asyncio.Task[object]]`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 05-未修复-低-composer 与 driver 分裂 phase 所有权

- **入口/函数**: `PromptToolkitInteractiveComposer.accept_submit` 和 `_drive_interactive_tty_repl`
- **文件(行号)**: `dayu/cli/composer.py` lines 417-418；`dayu/cli/session_execution.py` lines 1878, 1959, 2397, 2106
- **输入场景**: composer phase 状态转换路径
- **实际分支**: `accept_submit(record_history=True)` 内部设置 `RUNNING`，但 driver 也在外部设置 `SUBMITTING`、`RUNNING`、`CANCELLING`、`IDLE`
- **预期行为**: phase 应有唯一 owner
- **实际行为**: composer 和 driver 都修改 phase，分裂所有权
- **直接证据**: `composer.py` line 435 `self._phase = InteractiveComposerPhase.RUNNING` vs `session_execution.py` line 1959 driver 也设置 phase
- **影响**: 当前正确但维护脆弱；任一方独立修改可能导致 phase 漂移
- **建议改法和验证点**: 将 phase 所有权收束到单一模块（driver 或 composer）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 无。

## Residual Risk

- **CI checks**: PR 无 reported checks（"no checks reported on the codex/interactive-oracle branch"）。merge 前需确认 CI pipeline 是否配置正确。
- **PR size**: 390 files, +145K/-15K lines。虽已通过 Gateflow 分阶段 review，但单 PR 体量仍然很大，增加 reviewer 遗漏风险。
- **VNext -> V2 重命名**: Finding 01/02 的 9 个类名和 61 处 docstring 如不在 merge 前修复，会固化双命名标准。
- **prompt_toolkit 挂起**: Finding 05（subagent 2）指出 Host terminal 无超时——若 engine 卡死，CLI 将永久挂起无用户逃逸路径。这是设计取舍但需记录。

## Positive Findings

以下区域经 subagent 深入走读确认无实质问题：

- **Semantic ownership**: `context_governance.py` 唯一拥有 acceptance decision；`compaction_terminal.py` 唯一拥有 transaction-local commit permit。`_COMPACT_ACCEPTANCE_PERMIT` 私有单例防止外部构造 truth。
- **Compaction terminal invariant**: 单 terminal 不变量在 write transaction 内正确执行，支持 COMPACTED/FAILED/INVALID_MULTIPLE 三种 disposition。
- **Pre-start governance coalescing**: `dispatch.py` 的 level-bit 飞行模式正确合并信号，无 await 间隙丢失信号，`asyncio.shield` 防止 caller 取消传播。
- **Runner response identity**: `SuccessfulRunnerResponseIdentity` 的 PRESENT/UNAVAILABLE 不变量验证完备，无遗漏分支。
- **Memory committed_compact_applied**: compaction 事件后正确跳过 budget limiting，由上游 `_validate_committed_candidate_policy` 保证策略一致性。
- **Reactive pass label rebinding**: `_bind_reactive_pass_to_root_labels` 正确处理 `current_input_anchor` 不在 source_boundary 的边界情况。
- **LLM-facing prompt conformance**: prompt 字段与 v2 schema 逐字段精确匹配，不可信材料边界双向防御正确，repair feedback 协议自足。
- **Test coverage**: CLI 87%、Host 84%，均超 80% 门槛。v2 schema、repair feedback、compaction terminal、SIGINT chord、pre-start coalescing 均有确定性测试。
- **README updates**: 所有触发条件对应的 README 均已更新。
- **命名完整性**: `context_compaction_completed` -> `context_governance_resolved` 重命名无残留引用。
