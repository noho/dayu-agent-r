# Code Review

## Scope

- Mode: all repository
- Branch: refactor/host-layer-followup-wu-layer-01-02
- Review date: 2026-06-02 22:11:58 CST
- Output file: docs/reviews/repo-review-20260602-221158.md
- Included scope:
  - `dayu/contracts/` — 公共契约包 (11 files)
  - `dayu/engine/` — Engine 核心层 (agent.py, contracts, provider_extensions, _default_runner)
  - `dayu/engine/runners/openai/` — OpenAI Runner 实现 (14 files)
  - `dayu/host/` — Host 核心层 (admission, dispatch, engine_ingest, tool_runtime, waiting, open_host, API, etc.)
  - `dayu/host/durable/` — Host 持久化基础设施 (22 files)
  - `dayu/host/` context/memory/compaction 子系统 (16 files)
  - `dayu/runtime/` — 层中立运行期基础设施 (17 files)
  - `dayu/service/` — Service 装配层 (1 file)
- Excluded scope:
  - `dayu/fins/` — 财报领域包（不在本 repo scope）
  - `dayu/ui/` — UI 层（不在本 repo scope）
  - `dayu/render/` — 渲染工具
  - `utils/` — 辅助脚本
  - `.venv/` — 虚拟环境
  - `docs/` 下已有 review artifacts
- Parallel review coverage:
  - Slice 1 (Agent): `dayu/contracts/` 全部文件
  - Slice 2 (Agent): `dayu/engine/` 核心层全部文件
  - Slice 3 (Agent): `dayu/engine/runners/openai/` 全部文件
  - Slice 4 (Agent): `dayu/host/durable/` 全部文件
  - Slice 5 (Agent): `dayu/host/` context/memory/compaction 全部文件
  - Slice 6 (Agent): `dayu/host/` tool_runtime, waiting, admission, dispatch, engine_ingest 全部文件
  - Slice 7 (Agent): `dayu/runtime/` + `dayu/service/` 全部文件
- Not-covered areas: `tests/` 目录下的测试文件未逐文件走读（仅在各 slice 中对照验证）

## Findings

### 1-MEDIUM — `ToolAwaitSpec`/`ToolAwaitSnapshot` datetime 字段缺少时区校验

- **入口/函数**: `ToolAwaitSpec.__post_init__`, `ToolAwaitSnapshot.__post_init__`
- **文件(行号)**: `dayu/contracts/tool_await.py:48-61`, `dayu/contracts/tool_await.py:79-87`
- **输入场景**: 构造 `ToolAwaitSpec(deadline=datetime(2026,1,1), ...)`（naive datetime）
- **实际分支**: `__post_init__` 校验 `await_kind` 类型和 `resume_token` 非空，但从未检查 `deadline`
- **预期行为**: 与 `ToolResultMeta` 一致，datetime 字段应强制时区感知或显式文档化 naive 许可
- **实际行为**: naive datetime 直接通过，下游与 aware datetime 比较时会抛 `TypeError`
- **直接证据**: 对比 `ToolResultMeta.__post_init__`（`tool_result.py:49-54`）有显式 `_datetime_awareness` 检查，`ToolAwaitSpec.__post_init__` 中对 `self.deadline` 零行引用
- **影响**: 生产者创建带 naive deadline 的 `ToolAwaitSpec` 成功，Engine 消费者与 aware datetime 比较时运行时失败
- **建议改法和验证点**: 在 `ToolAwaitSpec.__post_init__` 和 `ToolAwaitSnapshot.__post_init__` 中添加时区感知校验
- **修复风险（低）**:
- **严重程度（中）**:

### 2-MEDIUM — `ToolParametersSchema` 允许空字符串 property key

- **入口/函数**: `ToolParametersSchema.__post_init__`
- **文件(行号)**: `dayu/contracts/tool_schema.py:54-66`
- **输入场景**: `ToolParametersSchema(type="object", properties={"": {"type": "string"}}, ...)`
- **实际分支**: 循环只校验 `required` 中 name 在 `property_names` 中存在，不遍历 `self.properties` 验证 key 非空
- **预期行为**: 空字符串 property key 应被 `ValueError` 拒绝，与 `ToolCallRequest._validate_json_value` 对 argument key 的校验一致
- **实际行为**: 构造成功，产生的 schema 不可用
- **直接证据**: `ToolCallRequest.__post_init__:88-90` 显式检查 `key.strip() == ""`，`ToolParametersSchema.__post_init__:54-66` 无此检查
- **影响**: LLM 可见的工具 schema 含空属性名，导致 provider 序列化时混乱错误
- **建议改法和验证点**: 在 `__post_init__` 中添加 property key 非空校验
- **修复风险（低）**:
- **严重程度（中）**:

### 3-MEDIUM — `GeminiToolCallState` 缺少 `__post_init__` 校验

- **入口/函数**: `GeminiToolCallState` 类定义
- **文件(行号)**: `dayu/contracts/tool_call.py:31-44`
- **输入场景**: `GeminiToolCallState(thought_signature="")` 或 `thought_signature="   "`
- **实际分支**: 无 `__post_init__`，构造直接成功
- **预期行为**: 空白 `thought_signature` 是无效的 provider 认证 token，与同模块 `ToolCallRequest.__post_init__` 的校验标准一致
- **实际行为**: 空字符串被接受，下游序列化为 `{"google": {"thought_signature": ""}}` 会导致 provider API 错误
- **直接证据**: `tool_call.py:31-44` 无 `__post_init__`，同文件 `ToolCallRequest.__post_init__:75-95` 校验所有字符串字段
- **影响**: 空 thought_signature 传播到 provider API 调用时产生难追踪错误
- **建议改法和验证点**: 添加 `__post_init__` 拒绝空白 `thought_signature`
- **修复风险（低）**:
- **严重程度（中）**:

### 4-MEDIUM — SSE Content-Type 启发式判断过于宽松

- **入口/函数**: `_is_sse_response`
- **文件(行号)**: `dayu/engine/runners/openai/runner.py:126-141`
- **输入场景**: provider 返回 `Content-Type: text/plain` 或 `application/octet-stream` 且 `stream=True`
- **实际分支**: 函数对不含 `"json"` 子串的任意 Content-Type 返回 `True`，送入 SSE 解析器
- **预期行为**: 对非 SSE Content-Type 应使用白名单而非排除 `json` 的黑名单
- **实际行为**: 非 SSE 内容被送入 SSE 解析器，产生 `sse_invalid_json` 协议错误并以 `RunnerDoneData(ERROR)` 终止
- **直接证据**: `runner.py:140` — `return _JSON_CONTENT_TYPE_FRAGMENT not in content_type`
- **影响**: 错误配置或恶意 provider 返回非 SSE Content-Type 时产生混淆性错误信息，虽然最终安全降级但诊断不清晰
- **建议改法和验证点**: 使用预期 Content-Type 白名单代替黑名单
- **修复风险（低）**:
- **严重程度（中）**:

### 5-MEDIUM — `terminal_attempt_row` CAS WHERE 子句未包含 terminal refs 守卫

- **入口/函数**: `terminal_attempt_row`
- **文件(行号)**: `dayu/host/durable/state.py:3756-3817`
- **输入场景**: 在已写入 terminal refs 的 attempt 上再次调用 `terminal_attempt_row`
- **实际分支**: WHERE 子句仅检查 `status IN (STARTING, RUNNING)`，不包含 `terminal_event_id IS NULL AND terminal_event_sequence IS NULL AND terminal_at IS NULL`
- **预期行为**: 与 `cancel_running_attempt_row`（`state.py:3876`）一致，应同时守卫 terminal refs 为 NULL
- **实际行为**: 由于 schema CHECK 约束，终态 status 不会是 STARTING/RUNNING，所以双重终端在当前 schema 下不可能发生，但防御纵深不一致
- **直接证据**: `cancel_running_attempt_row:3876` 包含 `_TERMINAL_REFS_UNSET_WHERE_SQL`，`terminal_attempt_row:3790` 不包含
- **影响**: 若 schema CHECK 约束将来被放宽，此函数将成为双重终端写入的薄弱点
- **建议改法和验证点**: 添加 `_TERMINAL_REFS_UNSET_WHERE_SQL` 以保持与同类函数一致
- **修复风险（低）**:
- **严重程度（中）**:

### 6-MEDIUM — Compaction `_merge_tuple_field_patch` CLEAR-then-REPLACE 导致 evidence_refs 污染

- **入口/函数**: `_merge_tuple_field_patch`
- **文件(行号)**: `dayu/host/compaction_operation.py:436-473`
- **输入场景**: pass 1 产生 CLEAR patch（含 pass 1 的 evidence_refs），pass 2 产生 REPLACE patch（含 pass 2 的 evidence_refs）
- **实际分支**: CLEAR 设置 `evidence_refs = list(patch.evidence_refs)`，REPLACE 执行 `evidence_refs.extend(patch.evidence_refs)`，最终结果包含两组合并的 evidence refs
- **预期行为**: REPLACE 的 evidence_refs 应只来自产生 REPLACE 的 pass，不应包含之前 CLEAR pass 的 evidence refs
- **实际行为**: CLEAR 的 evidence refs（来自较早 pass）被携带进最终 REPLACE 结果
- **直接证据**: `compaction_operation.py:443` — `evidence_refs = list(patch.evidence_refs)`（CLEAR 分支），`compaction_operation.py:454` — `evidence_refs.extend(patch.evidence_refs)`（REPLACE 分支，不清空先前累积）
- **影响**: CLEAR evidence 不支持新的 REPLACE value，产生语义错误的数据完整性
- **建议改法和验证点**: REPLACE 操作应重置 evidence_refs 而非 extend
- **修复风险（低）**:
- **严重程度（中）**:

### 7-MEDIUM — ATTACH_ACTIVE 策略对 ACCEPTED 状态 Run 不记录幂等

- **入口/函数**: `_start_run_attach_active` 中 ACCEPTED 分支
- **文件(行号)**: `dayu/host/admission.py:1018-1028`
- **输入场景**: 对 ACCEPTED 状态的 Run 以相同 `client_request_id` 发起 ATTACH_ACTIVE retry
- **实际分支**: `active.status == RunStatus.ACCEPTED` 时直接返回，不调用 `idempotency_store.record_idempotent_result`
- **预期行为**: 与非 ACCEPTED active Run 一致（`admission.py:1029-1049`），应记录幂等结果
- **实际行为**: ACCEPTED Run 后续转为 RUNNING 后，同一 `client_request_id` 的 retry 可能无幂等保护，允许创建重复 queued Run
- **直接证据**: `admission.py:1018-1028` — ACCEPTED 分支无 `record_idempotent_result` 调用，对比 `admission.py:1029-1049` 的非 ACCEPTED 分支有
- **影响**: 幂等保护不对称，retry 可能导致重复 Run 创建
- **建议改法和验证点**: 在 ACCEPTED 分支也调用 `record_idempotent_result`
- **修复风险（低）**:
- **严重程度（中）**:

### 8-MEDIUM — 无并发 compaction 守卫

- **入口/函数**: Context Governance proactive/reactive compaction 路径
- **文件(行号)**: `dayu/host/context_governance.py`, `dayu/host/compaction_operation.py`
- **输入场景**: 两条治理路径同时为同一 session/run 发起 proactive compaction
- **实际分支**: `max_proactive_compactions_per_run` 是策略上限，不是互斥锁；无锁、版本检查或乐观并发控制
- **预期行为**: 应有机制防止同一 run 上同时执行两个 compaction
- **实际行为**: 两个 compaction 可能同时运行，产生重复 `CONTEXT_COMPACTED` 事件和 compact artifact
- **直接证据**: 所有 compaction 文件中无跨路径互斥机制
- **影响**: 重复 compact artifact 消耗存储，可能混淆下游消费者
- **建议改法和验证点**: 引入 run 级 compaction 互斥（durable lock row 或 in-memory guard）
- **修复风险（中）**:
- **严重程度（中）**:

### 9-LOW — Engine `_make_final_after_close` 方法名误导

- **入口/函数**: `_make_final_after_close`
- **文件(行号)**: `dayu/engine/agent.py:2010-2028`
- **输入场景**: 方法被调用时
- **实际分支**: 方法只构造 terminal `EngineEvent`，不调用 `_close_runner_once()`
- **预期行为**: 方法名暗示会关闭 Runner
- **实际行为**: Runner 关闭完全依赖外层 `finally` 块
- **直接证据**: `agent.py:2010-2028` 内无 `_close_runner_once()` 调用，对比 `_make_cancelled_terminal_with_close:2124` 显式关闭
- **影响**: 代码可审计性降低
- **建议改法和验证点**: 重命名为 `_make_final_terminal_event` 或类似名称
- **修复风险（低）**:
- **严重程度（低）**:

### 10-LOW — SSE parser `_cancel_pending_readany` 吞没 task 异常

- **入口/函数**: `_cancel_pending_readany`
- **文件(行号)**: `dayu/engine/runners/openai/runner.py:858-880`
- **输入场景**: readany task 已完成但带异常
- **实际分支**: `pending.done()` 返回 True 时直接 return，不调用 `pending.result()` 或 `pending.exception()`
- **预期行为**: 应消费 task 异常以避免 "Task exception was never retrieved" 警告
- **实际行为**: 未消费的 task 异常可触发 asyncio 警告
- **直接证据**: `runner.py:860-861` — `if pending.done(): return`，对比 `dayu/runtime/cancellation.py:324-336` 的正确处理
- **影响**: asyncio event loop 产生噪音警告
- **建议改法和验证点**: 在 `pending.done()` 分支调用 `pending.exception()` 消费异常
- **修复风险（低）**:
- **严重程度（低）**:

### 11-LOW — `_types.py` 中存在未使用类型声明

- **入口/函数**: 模块级定义
- **文件(行号)**: `dayu/engine/runners/openai/_types.py:143,152,161,195`
- **输入场景**: N/A（死代码）
- **实际分支**: `_OpenAIToolCallFinal`, `_OpenAIChoiceFinalMessage`, `_OpenAIChoiceFinal`, `_ChunkAggregationKind` 全仓库无引用
- **预期行为**: 未使用类型应清理
- **实际行为**: 死代码增加维护负担
- **直接证据**: 全仓库 grep 仅 `_types.py` 和 docs 中有引用
- **影响**: 维护负担
- **建议改法和验证点**: 移除未使用类型
- **修复风险（低）**:
- **严重程度（低）**:

### 12-LOW — `HostRow.get()` 使用 O(n) 线性扫描

- **入口/函数**: `HostRow.get`
- **文件(行号)**: `dayu/host/durable/transaction.py:120-123`
- **输入场景**: 每次列访问
- **实际分支**: 遍历 `self.columns` 列表查找列索引
- **预期行为**: 使用 O(1) 字典查找
- **实际行为**: 对于宽表（event_log 19 列），每次 `_require_text(row.get("event_id"), ...)` 都是线性扫描
- **直接证据**: `transaction.py:120-123` — `for index, name in enumerate(self.columns)`
- **影响**: 批量 fetchall 操作性能
- **建议改法和验证点**: 惰性构建 `dict[str, int]` 索引
- **修复风险（低）**:
- **严重程度（低）**:

### 13-LOW — `truncate_limit_key_for_strategy` 无 enum 穷举测试

- **入口/函数**: `truncate_limit_key_for_strategy`
- **文件(行号)**: `dayu/contracts/tool_schema.py:203-213`
- **输入场景**: 新增 `ToolTruncationStrategy` 成员但未更新 `_TRUNCATE_LIMIT_KEYS_BY_STRATEGY`
- **实际分支**: 新成员命中 `KeyError`
- **预期行为**: 应有 `ValueError` 或编译时/测试时的穷举守卫
- **实际行为**: `KeyError` 作为无类型内部错误传播
- **直接证据**: 无测试验证 `set(ToolTruncationStrategy) == set(_TRUNCATE_LIMIT_KEYS_BY_STRATEGY.keys())`
- **影响**: 未来添加策略时成为潜在故障点
- **建议改法和验证点**: 添加穷举测试 + 生产代码中用 `.get()` 并抛 `ValueError`
- **修复风险（低）**:
- **严重程度（低）**:

### 14-LOW — `@runtime_checkable` Protocols 从不做运行时 isinstance 检查

- **入口/函数**: `CancellationToken`, `ToolCallable`, `ToolExecutor` 协议定义
- **文件(行号)**: `dayu/contracts/cancellation.py:20`, `dayu/contracts/tool_declaration.py:37`, `dayu/contracts/tool_executor.py:22`
- **输入场景**: N/A（装饰器效果未被使用）
- **实际分支**: `@runtime_checkable` 启用运行时 `isinstance` 检查，但无任何 `__post_init__` 方法使用
- **预期行为**: 要么使用运行时检查，要么移除装饰器
- **实际行为**: 装饰器增加元类复杂度但无运行时安全收益
- **直接证据**: 全 contracts 包 `__post_init__` 方法中零 `isinstance(x, CancellationToken)` 调用
- **影响**: 轻微运行时性能损失
- **建议改法和验证点**: 移除 `@runtime_checkable` 或在消费边界添加运行时检查
- **修复风险（低）**:
- **严重程度（低）**:

### 15-LOW — Evidence chunking 可能跨越 Unicode surrogate boundary

- **入口/函数**: `_evidence_chunks`
- **文件(行号)**: `dayu/host/compact_material.py:1623`
- **输入场景**: evidence text 在 chunk 边界处包含 surrogate pair 或多字节 grapheme cluster
- **实际分支**: `text[start : start + EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS]` 按字符切片，可能在中文字符的 surrogate pair 中间切割
- **预期行为**: 应在 grapheme cluster 边界上安全切片
- **实际行为**: 可能产生无效 Unicode
- **直接证据**: `compact_material.py:1623` 的字符级切片
- **影响**: 在含罕见 Unicode 字符的证据文本中产生无效输出
- **建议改法和验证点**: 在切片边界上添加 Unicode 安全校验
- **修复风险（低）**:
- **严重程度（低）**:

## 已验证正确的关键领域

以下领域经逐路径走读确认**未发现实质性问题**：

- **Engine 状态机**: CREATED → ITERATING → EXECUTING_TOOL → FORCE_ANSWER → terminal 迁移正确，取消边界（commit boundary）逻辑一致
- **Engine 工具执行**: 双射校验、重复 tool_call_id 检测、超时/取消/异常路径均正确处理
- **Engine adversarial 覆盖**: 空 messages、max_iterations=0、force-answer 空内容、Runner 无 done event、未知 finish_reason 等均 fail-closed
- **Host durable 层**: EventLog append-only 语义、CAS 状态转换、终态不可逆、事务边界、幂等实现、payload 冷热分离均正确
- **Host ToolRuntime accept barrier**: session/run/attempt/execution 四维 identity 校验完整，stale execution 正确拒绝
- **Host 工具重复调用治理**: owner/waiter 串行化正确，幂等复用语义正确
- **Host wait/resolve 状态机**: 多守卫入口检查完善，late result 正确处理，幂等保护完整
- **Host EngineEvent ingest**: identity 校验完整，事件路由正确，stale event 静默拒绝
- **Host dispatch**: lane acquire、worker accept、cancel 传播逻辑正确
- **Host admission**: submit_followup queue/steer 状态校验完整，幂等保护总体正确（除 Finding 7 外）
- **dayu.runtime**: import 边界全部干净，取消/race helper、lane、filelock、tools_discovery、config_loader、scene_prepare、assembly、tool_truncation、diagnostic_text 均正确实现
- **dayu.service**: host_assembly 编排正确，路径包含校验到位
- **Import 边界**: 所有层均遵守规定的依赖方向，无反向 import、无跨层穿透

## Open Questions

- Host memory projection 中 `_replace_item_by_id` 的实现是否使用 O(1) 字典查找？在执行 slice 中未完全展开阅读
- Compaction `_merge_tuple_field_patch` CLEAR+REPLACE 合并逻辑（Finding 6）在当前 pass 串行执行的正确路径中是否真的可达？需要确认是否所有 pass 共享同一个 merged patch 还是各自独立

## Residual Risk

- 未覆盖 `tests/` 目录逐文件走读；测试质量仅通过各 slice 中对照代码路径推断
- `dayu/fins/` 包不在本仓库 scope，其与 Host/ToolRuntime 的集成边界未审
- Host memory/compaction 子系统的 Unicode 边界处理（Finding 15）仅静态分析，未用实际多语言文本验证
- 并发 compaction 守卫缺失（Finding 8）在生产环境中尚未触发过的可能性较高（当前单 worker 部署），但架构上存在
