# Code Review（Slice 5 re-review）

## Scope

- Mode: current changes（re-review：UF-FIX08 Slice 5 code-review-fix 后的未提交 diff）
- Branch: `codex/upload-filing-oracle`
- Base: `main`
- Output file: `docs/reviews/code-review-20260816-181411.md`
- 前置 review artifacts：`docs/reviews/code-review-20260816-175716.md`（本 reviewer 产出）、`docs/reviews/code-review-20260816-174830.md`（并行 reviewer，本次已读用于闭环）
- 裁决与修复 artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice5-code-review-fix-20260816.md`、更新后的 `docs/gateflow/uf-fix08-existing-source-auto-repair-slice5-implementation-20260816.md`
- Included scope:
  - `dayu/fins/pipelines/_filing_upload_fresh_validation.py`（新增，Controller scope amendment）
  - `dayu/fins/pipelines/sec_upload_workflow.py`、`dayu/fins/pipelines/cn_pipeline.py`（当前 diff）
  - `tests/fins/test_sec_pipeline_upload_filing_stream.py`、`tests/fins/test_cn_pipeline.py`（当前 diff）
  - 回归核验（未在 fix 中改动但属于 Slice 5）：`tests/fins/test_processor_read_consistency.py`、`tests/fins/test_fins_ingestion_runtime.py`
  - 只读走读：`service_runtime.py:60-94`（preflight 边界一致性对照）、`upload_failure.py`、`ingestion_runtime.py`、`_fs_filing_upload_state_core.py`、`dayu/runtime/filelock.py`
- Excluded scope: Slice 1–4 已提交文件、download workflow、README、registry/oracle/evidence、Host/Engine 实现
- Parallel review coverage: 无 subagent

## 总体结论

**PASS**。两个 accepted blockers（A：`RuntimeFileLockError` 漏捕；B：SEC/CN 双 owner helper）均按 owner 语义正确修复；四项 rejected findings 的裁决与 accepted plan §5.5/§11.4、用户 scope 及语义所有权纪律一致；原 Slice 5 全部 repair/stale/snapshot/process/durable 测试无回退；scope/pyright/coverage/frozen guards 实测全部通过。**无新增 findings**。

## Accepted blockers 核验

### Blocker 1：`_filing_upload_fresh_validation.py` 唯一 owner — PASS

- **唯一 owner 成立**：`resolve_fresh_filing_request()` 仅在该模块定义一次（`:30-69`），全仓 grep 确认消费方只有 `sec_upload_workflow.py:163` 与 `cn_pipeline.py:794` 两处机械调用；SEC/CN 不再各自持有异常映射副本（本地 helper 与 `validate_fins_upload_filing_request`/`FinsUploadUsageError`/prevalidation factory imports 已全部从 workflow 移除，grep 无残留）。
- **无反向依赖**：模块只依赖 `ingestion_runtime`（validator）、`storage`（protocol）、`upload_failure`（reason/factory owner）、`dayu.runtime.filelock`（层中立）；无任何模块 import 回该模块之外的反向引用，无 import cycle。
- **机械消费**：workflow 仅做 `isinstance(fresh_resolution, FinsUploadFailureReason)` 判别 → 各自 `_build_*_filing_failure_event` 或继续执行；不重判 status/revision/reasons/action，identity guard 未扩大。
- **无重复判断**：`fins_upload_prevalidation_io_failure`/`corruption_failure` 的生产调用点只有两处——preflight owner（`service_runtime.py:88-90`，Slice 3 已提交）与本 resolver；映射策略不再三份。
- 朴素 required-keyword 接口（`repository` + `request`）符合架构硬约束；模块 docstring 明确所有权声明。

### Blocker 2：`RuntimeFileLockError`/`OSError` 统一映射 prevalidation IO + 真实测试 — PASS

- 映射：`except (OSError, RuntimeFileLockError): return fins_upload_prevalidation_io_failure()`（`_filing_upload_fresh_validation.py:64-66`）。`RuntimeFileLockTimeoutError` 是 `RuntimeFileLockError` 子类，自然覆盖。
- 一致性对照：preflight 边界（`service_runtime.py:86-88`）本就以完全相同的 `(OSError, RuntimeFileLockError) -> io / ValueError -> corruption` 映射收敛；本次修复使 fresh 边界与 preflight owner 模式精确对齐，而非发明新语义——修复方向正确。
- 真实测试：`test_upload_filing_fresh_read_failures_use_prevalidation_failure_owner` 参数化扩为三例，含 `RuntimeFileLockError("private lock detail")`；断言事件序列精确等于 `[UPLOAD_FAILED]`（即无 `UPLOAD_STARTED`）、`kind=storage/code=storage_io`、固定 path-free message、`str(read_error) not in str(result)`（异常文本不泄漏）、`converter_calls == []`（fix 新增的更强断言）、batch/company/source token 全空、published tree SHA 为空。

### Blocker 3：catch precedence 与 fail-loud — PASS

- 顺序实测正确：① `FinsUploadPrevalidationError`（RuntimeError）→ `exc.failure`；② `FinsUploadUsageError`（ValueError 子类）在 `except ValueError` 之前原样 `raise`，不会落入 corruption 分支；③ `(OSError, RuntimeFileLockError)` → io failure；④ structural `ValueError` → corruption failure。
- 其它 `RuntimeError`（storage 的 "exact inspector 未返回 target / 可信状态缺 business meta"、producer invariant）不被捕获，fail-loud；identity invariant（`_assert_authoritative_filing_identity`）位于 resolver 之外，RuntimeError 原样传播——既有 identity-mismatch 测试已迁移 monkeypatch 到 `fresh_validation_module` 并继续通过（`pytest.raises(RuntimeError)` + 零 mutation 断言）。
- docstring Raises 完整声明 `FinsUploadUsageError` 与 `RuntimeError`。

### Blocker 4：rejected findings 裁决符合 plan §5.5/§11.4 与 scope — PASS

| 被拒 finding | 裁决核验 |
| --- | --- |
| fresh usage 扩大 durable typed schema | 拒绝正确。plan §5.5/§11.4 的 fresh/durable 契约只冻结 prevalidation failure；usage 属 validator 的调用方可修正契约，closed `FinsUploadFailureCode` 无等价 usage code，映射为 storage/runtime 是伪映射。当前 diff 无任何 usage schema 变更（`upload_failure.py`/`ingestion_runtime.py` production 零 diff） |
| CN/HK 复制 SEC corruption grid | 拒绝正确。plan §11.4 明文：CN 一个 repair success + Phase B conflict + unsafe + atomicity；HK 一个 repair success + unsafe/stale。当前测试精确满足，五类 grid 已由 shared service/storage owner 与 SEC market 层覆盖；未做机械复制 |
| 测试 JSON helper 抽共享 support | 拒绝正确。`tests/fins/upload_filing_test_support.py` 零 diff；两个 helper 仅服务各自 market fixture 注入，不承诺生产语义，不构成跨 scope test-support owner |
| internal `RuntimeError` 一并收敛 | 拒绝正确。producer invariant 收敛为 storage failure 会掩盖生产 bug；fail-loud 符合语义所有权纪律与 review 要求 3 的 "identity invariant fail-loud" |

### Blocker 5：原 Slice 5 测试无回退 + 工程 guards — PASS

- 原测试全部保留并通过：SEC 五类 corruption repair、CN/HK 各自 repair success、CN/HK fresh unsafe、CN Phase B stale rollback、snapshot/process_filing 只消费新 primary、durable typed failure 逐字段同源。
- 实测复验（本 reviewer 独立执行）：四个 affected 文件 `430 passed`；full Fins suite `1842 passed, 1 skipped`；全仓 pyright `0 errors, 0 warnings, 0 informations`；branch coverage `_filing_upload_fresh_validation.py 100%`、`sec_upload_workflow.py 92%`、`cn_pipeline.py 92%`（与 fix artifact 声明一致，均 ≥80%；上一轮 finding 003 指出的 CN 副本 173-178 未覆盖问题随 helper 删除而消解，共享模块 100% 覆盖）。
- `git diff --check` 通过；frozen files（`docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`、`docs/host/design.md`、`docs/engine/design.md`、四个 README、`upload_failure.py`、`ingestion_runtime.py`、`tests/fins/upload_filing_test_support.py`）零 diff；scope 为三份 production（两份 allowed + 一份 Controller 批准的 scope amendment）+ 四份 allowed tests + 三份 artifacts。
- 新模块无 `Any`/`object`/反射/compat shim/目录扫描/raw meta/异常字符串判定；monkeypatch 测试迁移到 shared owner 模块，workflow 未保留 compatibility re-export。

## Findings

未发现实质性问题。

## Open Questions

- 无。两轮 review 的已修与已拒 findings 均有明确 owner/scope 裁决，不阻塞 re-review acceptance。

## Residual Risk

- fresh `FinsUploadUsageError` race（preflight 后 target 状态漂移）在 async job 路径仍落 generic `_save_failed_from_exception(str(exc))`：Controller 已裁决 rejected-with-reason（不扩 schema、不伪映射），行为 fail-closed 且 usage 文案 path-free；如后续需要 typed 化，归对应 usage schema work unit。
- CN/HK market 层 repair corruption 网格仅 Docling-missing（plan §11.4 内）：全网格由 shared service/storage owner 测试与 SEC market 层覆盖；market-specific 组合回归面略窄，如后续 regression 需要可再评估。
- internal `RuntimeError`/identity guard fail-loud 在 async job 路径表现为 generic job failure：producer-bug-only 路径，刻意不掩盖，属既有 runtime boundary。
- download unsafe 回归与 README 归 Slice 6；一般并发收敛归 UF-FIX10；fresh company warning 归 UF-FIX11；material repair、旧 schema migration、真实 evidence（UF-PF08/UF-PF12）归各自独立 work unit。
- 未运行真实 CLI/evidence 命令（plan 明确禁止，符合预期）。
