# WU-CM-06 Plan Re-Review — Accepted Findings Verification

- **Reviewed target**: `docs/host/host-issues/wu-cm-06-terminal-summary-text-policy-plan.md`
- **Scope**: re-review of revised plan — verify all accepted findings from previous reviews have been fixed
- **Previous reviews**: `docs/reviews/plan-review-20260612-154220.md` (AgentMiMo), `docs/reviews/plan-review-20260612-154418.md` (AgentDS)
- **Reviewer**: AgentMiMo
- **Date**: 2026-06-12

## Accepted Findings Verification

### Finding AgentDS-001 (中): read_api projection 路径未经测试覆盖 policy matrix

**Status: ✅ FIXED**

Plan Slice 1 now explicitly requires:

> "Add direct read API projection tests for `RUN_FAILED`, `RUN_CANCELLED`, and `RUN_LOST` EventLog rows proving their projected `HostEvent.final_answer` is `None`. Dataclass construction tests do not satisfy this requirement because they do not exercise `_host_event_from_row(...)` / read API projection logic."

Allowed files now include `tests/host/test_read_api_terminal_policy.py, or the nearest existing read API projection test file, for focused EventLog row -> HostEvent projection assertions`.

**Evidence**: Existing `test_context_compact_events.py:test_run_lost_projects_to_lost_host_event` only checks `kind` and `terminal_status`, not `final_answer`. No FAILED or CANCELLED projection tests exist. The plan correctly identifies this gap and makes the requirement unconditional.

---

### Finding AgentDS-003 (低): empty_final_answer 转为 RUN_FAILED 的 governance 决策未在 policy matrix 中显式建模

**Status: ✅ FIXED**

Plan Slice 1 now explicitly requires:

> "Add or strengthen an `empty_final_answer` ingest test proving Host governance converts empty Engine `final_answer` to `RUN_FAILED` and the terminal summary payload does not contain displayable final-answer `content`."

Allowed files now include `tests/host/test_engine_ingest_mapping.py, only for a focused empty_final_answer terminal summary shape assertion`.

**Evidence**: Existing `test_engine_ingest_mapping.py:test_empty_final_answer_closes_failed_without_run_succeeded` checks `error_code` and `recoverable` but does not assert absence of displayable `content` field. The plan correctly identifies this as a gap to strengthen.

---

### Finding AgentMiMo-002 (低) + AgentDS-002 (低): memory inline-only consumer difference

**Status: ✅ FIXED**

Policy table now has two separate rows:

| Terminal / continuity path | Allowed source | Forbidden sources |
|---|---|---|
| Compaction material / run input continuity | strict continuity resolver; may fall back to artifact `content` | bare `content`, `summary_text`, nested `summary` |
| Conversation Memory selected recent window | inline `RUN_SUCCEEDED.final_answer` only, leniently; does NOT follow artifact descriptor indirection | must not create evidence-backed facts from terminal summary or assistant final answer |

**Evidence**: `memory.py:1625` uses `assistant_final_answer_text_from_run_payload` (inline only, LENIENT); `compact_material.py:1995` uses `assistant_final_answer_continuity_text` (artifact fallback, STRICT). The plan now explicitly distinguishes these consumers.

---

### Finding AgentDS-005 (低): memory projection evidence-backed fact 分离测试可能被 skip

**Status: ✅ FIXED**

Plan Slice 1 now explicitly requires:

> "Add a focused memory projection assertion proving assistant terminal text enters only `selected_recent_window` with `MemoryIncludedReason.SELECTED_RECENT_WINDOW`, not evidence-backed fact memory; also prove descriptor-only `RUN_SUCCEEDED` does not materialize an assistant item in memory projection."

**Evidence**: `test_memory_projection.py` has no assertions checking `MemoryIncludedReason.SELECTED_RECENT_WINDOW` for assistant final answer items. The plan now makes this requirement unconditional (removed "only if current coverage is missing" conditional language).

---

### Finding AgentDS-004 (低): overlong text policy 归属合理但缺少 caller-side 验证点

**Status: ✅ FIXED**

Plan now explicitly states:

> "Overlong allowed text is preserved by this policy helper; truncation belongs to display, storage, or budget-specific callers, not to terminal text source selection. Caller-side truncation verification is out of scope for WU-CM-06 and remains owned by each caller's budget/display tests."

**Evidence**: `terminal_summary_payload.py:_text_field()` preserves text as-is with no truncation. The plan now clearly scopes overlong handling to callers and documents the out-of-scope boundary.

---

### Finding AgentMiMo-001 (低): Slice 1 测试增量可能极小

**Status: ✅ FIXED**

Plan has been revised to replace conditional language ("only add assertions if current tests do not already cover") with explicit, unconditional test requirements. Each test item now states what to prove rather than what to check first. The allowed files section is specific about which test files to create or modify.

---

## Residual Verification

| Check | Result |
|---|---|
| `tests/host/test_read_api_terminal_policy.py` exists? | No — plan correctly treats it as new file to create |
| `tests/host/test_engine_ingest_mapping.py` exists? | Yes — plan correctly scopes to focused assertion addition |
| `tests/host/test_memory_projection.py` exists? | Yes — plan correctly scopes to focused assertion addition |
| No existing read_api projection test file? | Confirmed — no `tests/host/test_read_api*.py` exists; `test_context_compact_events.py` has one RUN_LOST projection test but doesn't check `final_answer` |
| No `MemoryIncludedReason` assertions in existing tests? | Confirmed — `test_memory_projection.py` has no `SELECTED_RECENT_WINDOW` or `MemoryIncludedReason` references |
| No overlong text tests? | Confirmed — `test_terminal_summary_payload.py` has no overlong/long text tests |

## Open Questions

None. All accepted findings have been verified as fixed.

## Residual Risks

| ID | Risk | Suggested tracking |
|---|---|---|
| RR1 | `tests/host/test_read_api_terminal_policy.py` is a new file — implementation agent must create it with proper fixtures for mock EventLogRow construction | Track in Slice 1 implementation report |
| RR2 | Existing `test_empty_final_answer_closes_failed_without_run_succeeded` may need strengthening rather than a new test — implementation agent should verify the existing test's assertions before adding new ones | Track in Slice 1 implementation report |

## Conclusion

**PASS**

All 6 accepted findings (5 from AgentDS, 1 from AgentMiMo, plus the overlapping consumer-difference finding) have been verified as fixed in the revised plan. The plan is now specific, unconditional, and ready for implementation agent handoff.

Key improvements:
1. read_api projection tests now explicitly required (new test file)
2. empty_final_answer governance shape test now explicitly required
3. Policy table now distinguishes compact material vs memory projection consumers
4. Evidence-backed fact separation test now unconditional
5. Overlong caller-side truncation explicitly scoped out of WU-CM-06
6. Conditional language removed from all test requirements
