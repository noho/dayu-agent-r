# Code Review（UF-FIX07 Slice 3 Re-review）

## Scope

- Mode: current changes（re-review，同一 review session，未 clear）
- Branch: `codex/upload-filing-oracle`
- Base: `3911c790f0147876148f1ee4d038eb14fe7becd7`（与 HEAD 一致；review 对象为未提交 workspace changes）
- 前序 review: `docs/reviews/code-review-20260815-205240.md`（两项 LOW + 一项 residual portability）
- Review fix artifact: `docs/gateflow/uf-fix07-slice3-review-fix-20260815.md`（`REVIEW FIX COMPLETE / RE-REVIEW PENDING`）
- Output file: `docs/reviews/code-review-20260815-210310.md`
- Accepted plan: 用户指定的文件名 `docs/gateflow/uf-fix07-accepted-plan-20260815.md` 在 `docs/gateflow/` 下不存在；实际 accepted plan 为 `docs/gateflow/uf-fix07-multi-file-primary-collision-plan-20260815.md`（经 `uf-fix07-multi-file-primary-collision-plan-re-review-adjudication-20260815.md` 裁决 `PLAN ACCEPTED`，§6.6/§6.7/Slice 3/stop conditions 均已在本 session 前序 review 中读取核对）。本 re-review 以实际存在的 accepted plan 为 contract 真源。
- Included scope（未提交改动，相对前序 review 的增量已用 `diff` 逐行比对）:
  - `dayu/fins/pipelines/docling_upload_service.py`（增量仅两处 docstring Raises 修正，无任何行为代码变化）
  - `tests/fins/test_docling_upload_service.py`（增量：新增 `_FailingNthUploadBlobRepository` 与 `test_filing_fresh_blob_store_failure_rolls_back_once_with_zero_publication`；多处测试路径显式 `resolve(strict=False)`）
  - 其余 Slice 3 allowed tests 与前序 review 状态一致
  - `docs/gateflow/uf-fix07-slice3-review-fix-20260815.md`（本轮新增 artifact）
- Excluded scope: 已 commit 的 Slice 1/2 代码；README（Slice 4）；UF-FIX08/10/11；registry/oracle/frozen evidence；UF-PF07/UF-PF12
- Parallel review coverage: 无（主 reviewer 单路复核）
- 验证复现: affected 8 文件 `pytest` → `630 passed, 1 skipped, 3 warnings`（exit 0，与 review-fix artifact 声明一致）；affected 9 文件 `pyright` → exit 0

## 前序 findings 复核（逐项闭环判定）

### 复核 1-已修复-低-两个生产 helper 的 docstring Raises 声明

- 前序 finding 1（`_build_upload_source_fingerprint`、`_build_stored_file_entry` 声称「Raises: 无」）已修复。
- 直接证据: `dayu/fins/pipelines/docling_upload_service.py:1237-1239` 现声明「ValueError: filing asset 缺少 ``original_filename``，或 source kind 不受支持时抛出」（同时覆盖了 else 分支的 `ValueError(f"不支持的 source_kind: ...")`）；`docling_upload_service.py:1612-1613` 现声明「ValueError: filing asset 缺少 ``original_filename`` 时抛出」。两处声明与真实抛出路径（`_require_filing_original_filename` 行 1166-1167、else 分支行 1269-1270）同源一致。
- 生产 diff 增量经 `diff /tmp/uf07_slice3_full.diff /tmp/uf07_slice3_review_fix.diff` 逐行比对，仅此两处 docstring 变化，无行为代码改动。闭环。

### 复核 2-已修复-低-fresh target blob store failure 零发布测试

- 前序 finding 2（plan 要求「第 N 次 blob store failure：fresh target 零发布」缺直接断言）已修复。
- 直接证据: `tests/fins/test_docling_upload_service.py:438-506` 新增 `_FailingNthUploadBlobRepository`（正向计数注入，`fail_at <= 0` 抛 ValueError，未命中时转发真实 staging 写入）；`test_docling_upload_service.py:2091-2148` 新增 `@pytest.mark.parametrize("fail_at", (1, 2, 3))` 的 `test_filing_fresh_blob_store_failure_rolls_back_once_with_zero_publication`，断言 `begin_calls == 1`、`commit_calls == 0`、`rollback_calls == 1`、`published_tree_sha256(tmp_path, "AAPL") == {}`、`get_source_meta` 抛 `FileNotFoundError`。
- 覆盖位置核实: `files=[companion, primary]` + `filing_primary=primary` → `ordered_files=(primary, companion)` → staging 写入次序为 primary original(1)、companion original(2)、derived(3)；fail_at 三档分别命中三个不同写入位置，均走真实 `commit_prepared_upload_batch` 的 finally rollback（`_publish_prepared_upload` 测试 helper 直接调生产 top-level owner，`test_docling_upload_service.py:718-742`）。闭环。

### 复核 3-已修复-低-测试路径显式 resolve 且生产校验不放宽

- 前序 residual（新增测试隐式依赖 pytest `tmp_path` 已 realpath）已修复。
- 直接证据: 增量 diff 显示以下位置均改为显式 `resolve(strict=False)`：`test_filing_converts_only_primary_and_publishes_all_companions` 的 files、`test_filing_same_basename_assets_are_collision_free_and_path_private` 的 first_dir/second_dir、`test_filing_preparation_exactly_associates_derived_with_primary_original`、`test_filing_generated_identity_collision_fails_before_converter_and_batch`、`test_filing_final_source_failure_rolls_back_once_with_zero_publication`、`test_filing_commit_failure_leaves_fresh_target_unpublished`、新 blob failure 测试、`test_execute_upload_existing_full_input_replaces_exact_complete_set` 的 old_dir/new_dir、`test_filing_fingerprint_excludes_path_identity_but_tracks_filename_and_content` 的 first_dir/moved_dir、`test_filing_one_hundred_originals_publish_with_one_conversion` 的 100 个文件；剩余 `_build_filing_original_asset_identity` 调用点的输入变量均溯源至上述已 resolve 路径（`test_filing_identity_is_stable_and_request_order_independent` 原本已 resolve）。pipeline/runtime 测试的 expected identity 此前已用 `resolve(strict=False)`（前序 diff 行 364-366 等）。
- 生产校验未放宽: `_build_filing_original_asset_identity` 的 absolute+resolved 不变量断言（`docling_upload_service.py:1106`）原样保留，生产 diff 零行为变化。闭环。

三项全部闭环，无 rejected、partially fixed 或 needs-more-evidence。

## Findings

未发现实质性问题。

（前序 review 中已完整验证的 Slice 3 核心事实——identity 公式与 plan §6.6 逐字节一致、request-local collision 在 converter/batch 前 fail-closed、authoritative 非首 primary 只转换一次、companions 无 derived/无 conversion_started、stored entry/URI/`primary_document`/`original_filename`/`derived_from`/事件 exact table、filing fingerprint 移目录 skip/改名 update、N+1 与 100 边界、conversion/final/commit 原子零发布、snapshot/process/read 只消费 exact derived primary、material 全链回归——因本轮生产代码零行为变化、测试变化仅为路径 resolve 与新测试，全部结论保持成立；新增 blob failure 测试不改变任何既有断言语义。）

## Open Questions

- 无。

## Residual Risk

- 旧 basename-based source schema 的兼容、首次重传或自动修复：`UF-FIX08`（assigned to later work unit）。
- 同 request/document 并发 writer：`UF-FIX10`。
- fresh company meta warning：`UF-FIX11`。
- frozen registry/oracle/evidence 与真实 UF-PF07/UF-PF12：未修改、未执行，等待另行授权。
- README 同步：Slice 4 职责，本 slice 不更新不是 defect。
- `_FailingNthUploadBlobRepository` 的注入异常文案沿用旧注入器的 `"forced replacement blob failure"`，用于 fresh-target 测试时文案含「replacement」一词略有误导，但不影响测试正确性与可执行性，不构成 finding。
- 未复跑逐文件 coverage gate（Slice 4 的 80% gate）；review-fix artifact 声明 `docling_upload_service.py` 86%，与本次实测 630 passed 一致。

## Review conclusion

**PASS。**

三项前序 findings/residual 全部闭环且证据逐行核实；生产代码相对前序 review 零行为变化（仅 docstring 修正）；fresh-target blob store failure 的 1/2/3 次写入失败三档测试覆盖真实 rollback 路径；测试路径显式 resolve 后不再依赖 pytest realpath 行为，生产 normalized-path 校验未放宽；完整 correctness/ownership/atomicity/identity/only-primary/downstream 主线无新发现。`630 passed, 1 skipped` 与 pyright exit 0 实测复现。无新增 blocker 或 finding。
