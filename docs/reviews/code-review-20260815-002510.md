# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `5508d0445bd1d649fee54f4ec3d65f99e2484493`
- Output file: `docs/reviews/code-review-20260815-002510.md`
- Included scope: 当前 workspace 和 branch 相对 base 的所有 staged/unstaged/uncommitted changes
- Excluded scope: 无
- Parallel review coverage: 无

## Review Context

Gateflow S1 re-review。只审当前未提交 S1 改动及已接受的 F1/F2 fix。必须基于直接代码和数据流证据复审。重点验证：

- F1: 6-K 单文件 branch coverage >=80% 且测试有效
- F2: `UploadCompanyNameRequiredError` 只表达缺公司名，runtime 不再吞 identity/builder `ValueError`
- F3: 机械 read consumer 迁移维持 accepted-with-note
- F4: help 不改的裁决被遵守
- 无 S2 提前实现、无 UF-PF05/oracle/scenario/frozen evidence、无其他 finding

## Findings

未发现实质性问题。

### F1 验证：6-K branch coverage 与测试有效性

**入口**: `sec_6k_primary_document_repair.py` 全模块

**验证内容**:

1. **新增测试有效性**: 以下 4 个测试均为有效 owner/public-path 行为测试：
   - `test_standalone_6k_reconcile_publishes_source_and_processed_together` (L4218): 通过 monkeypatch `_assess_active_6k_candidate` 稳定处理器评估，从 durable storage 断言 `published_meta["primary_document"]` 与 `processed_meta["reprocess_required"]`。不以 event payload 代替持久化证据，符合 owner 级 contract 测试要求。
   - `test_prepared_6k_reconcile_handles_non_candidates_and_invalid_files` (L4301): 覆盖非 6-K form 早退、无 HTML 候选早退、缺失 payload 早退、非法 files contract 的 `ValueError` fail-closed。
   - `test_standalone_6k_reconcile_normalizes_public_filters` (L4358): 覆盖 public path ticker 去重与清洗后空过滤器的 `ValueError`。
   - `test_standalone_6k_reconcile_rolls_back_failed_document` (L4391): 覆盖单文档失败时 batch rollback 行为。

2. **Coverage 数据**: 独立执行 `coverage run --branch -m pytest -q tests/fins/test_sec_pipeline_download.py` 后，`coverage report --include=dayu/fins/pipelines/sec_6k_primary_document_repair.py --fail-under=80` 通过，branch coverage 为 91%。**pass**。

3. **测试有效性判定**: 测试断言 durable storage state（source meta primary_document、processed reprocess_required），不是 event payload 或内存状态。monkeypatch 仅替换处理器评估函数（非 I/O owner），保留了真实 batch/publication/storage 路径。

### F2 验证：UploadCompanyNameRequiredError 语义收窄

**入口**: `upload_company_meta.py:_require_upload_company_name` (L201) 与 `ingestion_runtime.py` (L1052-1061)

**验证内容**:

1. **异常语义**: `UploadCompanyNameRequiredError` (L25) 继承 `ValueError`，仅在 `_require_upload_company_name` (L214-216) 的空名称分支抛出。语义唯一：缺公司名。

2. **Runtime catch 收窄**: `ingestion_runtime.py` L1060 的 catch 精确为 `except UploadCompanyNameRequiredError`，不再 blanket catch `ValueError`。这意味着：
   - `build_company_ticker_identity` 在 ticker/alias 非法时抛出的 `ValueError` 会穿透到上层，不会被误分类为 `COMPANY_NAME_REQUIRED`。
   - `resolve_upload_company_meta_decision` 在 identity mismatch 时抛出的 `ValueError` (L78) 同样不会被误分类。

3. **投影映射**: `FinsUploadUsageCode.COMPANY_NAME_REQUIRED` (L673) 的 usage message 为 "当前公司缺少有效元数据；create/update 必须提供 --company-name" (L778)，语义与异常一致。

**结论**: F2 已正确关闭。`UploadCompanyNameRequiredError` 只表达缺公司名，identity/builder `ValueError` 不会投影为 `COMPANY_NAME_REQUIRED`。**pass**。

### F3 验证：机械 read consumer 迁移

**入口**: `read_runtime.py:2753`

**验证内容**:

唯一的机械迁移行：

```python
# L2753: return company_meta.company_name, company_meta.ticker_identity.market
```

原来从 `company_meta.market` 读取 market，现在从 `company_meta.ticker_identity.market` 读取。这是 strict CompanyMeta field migration（`CompanyMeta` 删除了顶层 `market` field）所必需的最小改动。read route、fallback schema、错误投影均未改变。Controller F3 已接受该最小必要 consumer 迁移。**pass**。

### F4 验证：help 不改的裁决被遵守

**入口**: `arg_parsing.py:_add_upload_ticker_argument` (L1080)

**验证内容**:

F4 controller 裁决为 `rejected-with-reason`：现有 upload help 已自足且不暴露内部 producer 术语。

当前 help 文本 (L1096-1098)：
```
"公司 ticker CSV：第一项是该公司财报归档的 canonical ticker，后续项是用户明确声明的"
"同公司查询别名；系统信任声明且不联网核验，成功保存公司元数据后均查询同一归档。"
```

该文本自足说明 CSV 格式、canonical 语义、别名语义和信任边界，不暴露内部 producer 术语（如 `build_company_ticker_identity`、`CompanyTickerIdentity`）。本次 S1 未修改 help 文本，遵守了 F4 裁决。**pass**。

### S2 提前实现检查

**验证内容**:

- `unique route`: grep `unique.*route` 零命中。
- `typed conflict`: grep `typed.*conflict` 零命中。
- `workspace lock`: grep `workspace.*lock` 零命中。
- `commit-time merge`: grep `commit.*time.*merge` 零命中。
- `UF-PF05`: grep `UF-PF05` 零命中。
- `oracle/scenario/frozen`: grep `oracle\|scenario.*frozen` 零命中。

**结论**: 无 S2 提前实现混入当前 diff。**pass**。

### Semantic Ownership Drift 检查

**验证内容**:

1. `CompanyTickerIdentity` 唯一 owner：`build_company_ticker_identity` 拥有 canonical ticker、market、exchange、accepted aliases 的构建与去重。FMP/SEC/CN/CLI 只提供声明输入，不再各自实现 normalizer/dedupe。直接证据：`fmp_company_info.py`、`sec_company_meta.py`、`cn_download_company_meta.py` 均调用 `build_company_ticker_identity`。

2. `CompanyMeta.ticker_identity` 是 durable company meta 中 ticker identity 的结构化真源。storage read/write/index 均消费 `ticker_identity` / `lookup_tickers()`。直接证据：`_fs_company_meta_core.py` L79/L160/L206/L226/L371。

3. `UploadCompanyNameRequiredError` 是"缺公司名"的唯一异常语义。`ingestion_runtime` 只捕获该类型。直接证据：L1060 `except UploadCompanyNameRequiredError`。

4. `resolve_upload_company_meta_decision` 是 filing/material 单进程 S1 merge decision owner。直接证据：`upload_company_meta.py` L42 的函数签名与 docstring。

**结论**: 无 semantic ownership drift。**pass**。

### 严格类型检查

**验证内容**: `python -m pyright dayu/ tests/ utils/` 返回 `0 errors, 0 warnings, 0 informations`。**pass**。

## Open Questions

无。

## Residual Risk

1. **S1 route 半契约（covered by approved S2）**: `resolve_existing_ticker`、alias index 仍为 `dict[str, list[str]]`、read-time late multiple-owner `ValueError` 仍存在。这是 accepted plan 明确允许的 S1 residue。

2. **Snapshot/lost-update window（covered by approved S2）**: `UploadCompanyMetaDecision.company_meta` 与 `upsert_company_meta` 暂存 final snapshot；尚无 commit-time authoritative merge。

3. **Regression baseline failures（assigned to later work unit）**: S1 实现报告 §5.1 的 6 项回归失败均不在本次 changed path，与本次 diff 无逻辑/数据同源关系。

## Conclusion

**PASS**。

所有 4 个重点验证项（F1/F2/F3/F4）均通过。无 S2 提前实现，无 semantic ownership drift，无严格类型错误，无其他实质性问题。S1 implementation 已达到 re-review 标准。
