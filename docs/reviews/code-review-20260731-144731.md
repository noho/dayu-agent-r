# Aggregate Deep Re-review：AAPL 下载与 canonical ticker 布局

## Review scope

- Accepted plan: `b3f90c5d`
- Accepted slices: `7cd1f3eb`、`e5771f38`
- Aggregate finding fix：SEC primaryDocument 逐 segment drive 校验。

## Verdict

PASS。前次 P1 已解决；无新增 correctness、stability 或 maintainability finding。

## Findings

无。

## P1 resolution

- `C:primary_doc.xml`、`C:/primary_doc.xml` 和
  `xsl/C:/primary_doc.xml` 均在 SEC filing collection owner 处拒绝。
- 真实故障值 `xslSCHEDULE_13G_X02/primary_doc.xml` 仍投影为
  `primary_doc.xml`。
- 普通 `aapl-20240928.htm` 保持不变。

## Aggregate checks

- SEC producer projection 与 storage filename boundary 同源且无下游 fallback。
- ticker normalization、canonical physical locator、company alias resolution、
  document private mapping 与 read projection 各有唯一 owner。
- journal、backup、lock、inventory 与 published lookup 复用同一 ticker locator
  helper；非法或旧布局 fail closed。
- `list_documents` 及另外八个 read tools 不拼接目录、不反推 ticker/document
  identity，并继续通过仓储协议读取。
- README 与测试手册描述当前已实现契约，没有记录未来计划或兼容承诺。

## Validation

- SEC pipeline download tests：101 passed。
- storage provider + atomicity tests：249 passed。
- full Fins tests：1015 passed，1 skipped（新增两个 list_documents 用例另行通过）。
- full pyright：0 errors。
- affected production coverage：97% / 91% / 82%。

## Residual risks

- 未执行 live SEC 网络下载。
- fresh schema 不自动迁移旧 `portfolio/id-*`；该边界由任务和项目 schema 规则明确
  接受。
