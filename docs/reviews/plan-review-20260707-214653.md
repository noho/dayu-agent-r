# Plan Review — current_time / get_current_time 语义边界修正

## Reviewed Target And Scope

- Target: [docs/reviews/wu-cli-smoke-01-current-time-tool-boundary-fix-codex.md](/Users/leo/workspace/dayu-agent-r/docs/reviews/wu-cli-smoke-01-current-time-tool-boundary-fix-codex.md) 的 `Gate: plan`
- Scope: `prompt` / `interactive` / `wechat` 的时间工具选择边界、`current_time` LLM-facing 文本、`get_current_time` LLM-facing description、测试与 README 同步。
- Out of scope: 时区扩展、工具执行语义、compact prompt、财报工具 schema。

## Assumptions Tested

- `prompt` 只需移除 `"utils"` tag，不需要改变 tools provider 或 tool discovery。
- `interactive` 与 `wechat` 继续保留 `"utils"` tag 即可保留 `get_current_time`。
- `base/tools.md` 的 `<when_tool get_current_time>` 条件块在 `prompt` 不选工具后会被过滤，不会把工具指引残留给模型。
- `current_time()` 是所有入口静态当前时间文本的单一生成点。
- 现有 `tests/runtime/test_scene_assets_migration.py` 通过真实 `prepare_scene` 装配可覆盖 manifest 选择和条件块过滤。

## Findings

未发现阻塞 plan 进入 implementation 的实质性问题。

## Architecture Boundary Review

计划只修改配置资产、LLM-facing 文本生成、工具 schema 文案和对应测试，没有把 Host、Engine、Service 内部治理状态投影给模型，也没有新增跨层依赖。`dayu.service.scene_context` 继续作为入口 LLM-facing slot 文本生成点，`dayu.tools.utils.provider` 继续作为工具定义真源，边界合理。

## Best-Practice Review

通过真实 `prepare_scene` 装配验证工具选择和条件块过滤，优于只断言 JSON 字段。测试计划同时覆盖文案禁用内部术语和工具 description 关键规则，能证明用户可见语义边界。

## Optimal-Solution Review

从 `prompt.json` 删除 `"utils"` 是当前最小且直接的修正。替代方案如新增 `"time"` tag 策略、动态工具描述或入口运行时过滤都更重，且会扩大当前 work unit 的耦合面。

## Overengineering Review

计划没有新增策略对象、配置 schema、provider 包装层或兼容逻辑，符合最小满足需求。

## Overcoupling Review

计划没有把 `prompt`、`interactive`、`wechat` 的行为绑成同一硬编码运行时分支；差异仍由 manifest 表达，测试通过 scene id 遍历验证，耦合风险低。

## Open Questions

无。

## Residual Risks And Suggested Tracking Destination

- 历史 `docs/reviews/` artifact 中存在旧行为描述，不作为当前用户文档真源；无需本 work unit 回写。分类：assigned to historical artifact archive。

## Conclusion

pass
