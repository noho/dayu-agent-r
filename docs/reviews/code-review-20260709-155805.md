# Deep Re-Review: WU-SEMANTIC-OWNERSHIP-01 P0-B Fix

## Metadata

- Work unit: `WU-SEMANTIC-OWNERSHIP-01`
- Sub WU: `P0-B` — Fins preprocess/upload typed result contracts
- Role: AgentDS (Claude Code Agent), re-review only
- Base commit: `6731b451` (P0-A accepted)
- Scope: workspace diff after P0-B fix (P0B-F01, P0B-F02, P0B-F03 closure)
- Date: 2026-07-09
- Prior artifacts:
  - Controller adjudication: `docs/reviews/wu-semantic-ownership-01-p0-b-code-review-controller-adjudication.md`
  - Initial reviews: `docs/reviews/code-review-20260709-154254.md` (AgentDS), `docs/reviews/code-review-20260709-154555.md` (AgentMiMo)
  - Fix report: `docs/reviews/wu-semantic-ownership-01-p0-b-fix-codex.md`

## Re-Review Constraints

- 只 re-review P0B-F01, P0B-F02, P0B-F03 是否闭合。
- 不修改代码，不 commit，不 push。
- 确认 deferred/info items 没有被误当作已修或被越界实现。
- 确认新增 focused test 覆盖 accepted findings，且没有引入新 P0-B blocker。

## Finding Closure Table

| Finding | Source | Required Fix | Implementation | Test Coverage | Verdict |
|---|---|---|---|---|---|
| P0B-F01 | AgentDS 001 | 恢复 `company_meta_repository` Raises 为 `无。`；更新 `_build_upload_result` Raises | `cn_pipeline.py:426-427` Raises `无。` ✅；`cn_pipeline.py:1244-1245` Raises `KeyError: payload 缺少显式 status 时抛出。` ✅ | N/A（docstring 变更，非运行时路径） | **Closed** |
| P0B-F02 | AgentMiMo F-01 / AgentDS OQ-1 | `_bounded_preprocess_summary` 拒绝分类总数 > `selected_count`；保留 cancellation-safe partial | `ingestion_runtime.py:5800-5807` categorized_count > selected_count → ValueError；不使用 `==` 而用 `>`，保留 `selected_count > sum` 场景 ✅ | `test_preprocess_result_status_rejects_over_classified_counts`: over_classified → ValueError ✅；cancellation_partial (selected=2, skipped=1) → SUCCEEDED ✅ | **Closed** |
| P0B-F03 | AgentMiMo F-02 | `result_status()` 显式表达 skipped-only success 分支 | `ingestion_runtime.py:637-638` `if self.skipped_count > 0: return SUCCEEDED` ✅；processed success / unsupported-or-failed-or-no-selected failure 语义未变 ✅ | `test_preprocess_result_status_separates_skipped_and_not_supported`: skipped_only → SUCCEEDED ✅；unsupported_only → FAILED ✅ | **Closed** |

## P0B-F01 Detail: Docstring Placement

**`company_meta_repository` (line 417-428)**:
```python
@property
def company_meta_repository(self) -> CompanyMetaRepositoryProtocol:
    """返回公司元数据仓储。
    ...
    Raises:
        无。
    """
```
- Raises 已从错误的 `KeyError: payload 缺少显式 status 时抛出。` 恢复为 `无。` ✅
- 该 property 是简单 getter（`return self._company_repository`），不涉及 payload/status，文档现在与实现一致。

**`_build_upload_result` (line 1234-1253)**:
```python
def _build_upload_result(self, action: str, **payload: JsonValue) -> dict[str, JsonValue]:
    """构建统一上传结果。
    ...
    Raises:
        KeyError: payload 缺少显式 status 时抛出。
    """
    return {
        ...
        "status": payload.pop("status"),  # 无默认值 → KeyError on missing
        ...
    }
```
- Raises 从 `无。` 改为 `KeyError: payload 缺少显式 status 时抛出。` ✅
- 代码 `payload.pop("status")` 无默认值，在 status 缺失时确实抛出 `KeyError`，文档与行为一致。
- 所有调用点（line 889, 915, 1150, 1195 等）均显式传递 `status=` 参数 ✅

## P0B-F02 Detail: Cross-Field Invariant

**`_bounded_preprocess_summary` (line 5790-5807)**:
```python
categorized_count = (
    bounded.processed_count
    + bounded.skipped_count
    + bounded.failed_count
    + bounded.not_supported_count
)
if categorized_count > bounded.selected_count:
    raise ValueError("预处理分类计数总和不得超过 selected_count")
```
- 使用 `>` 而非 `==`：仅拒绝分类总数**超过**选择数量的不可能状态 ✅
- 允许 `selected_count > categorized_count`：cancellation closeout 前 partial summary（部分文档已被 selected 但尚未完成分类遍历）可合法通过 ✅
- 校验位于 owner boundary（summary 构造后的首个共享校验入口），不在下游消费者 ✅

**Focused test `test_preprocess_result_status_rejects_over_classified_counts`**:
- `over_classified`: selected=1, processed=1, skipped=1 → `pytest.raises(ValueError, match="selected_count")` ✅
- `cancellation_partial`: selected=2, skipped=1 → `result_status() is SUCCEEDED` ✅

## P0B-F03 Detail: Explicit Skipped-Only Success

**`result_status()` (line 619-639)**:
```python
_bounded_preprocess_summary(self)
if self.processed_count > 0:
    return FinsPreprocessResultStatus.SUCCEEDED
if self.selected_count == 0 or self.failed_count > 0 or self.not_supported_count > 0:
    return FinsPreprocessResultStatus.FAILED
if self.skipped_count > 0:
    return FinsPreprocessResultStatus.SUCCEEDED  # ← 显式分支
return FinsPreprocessResultStatus.SUCCEEDED
```
- 第 637-638 行新增显式 `if self.skipped_count > 0: return SUCCEEDED` ✅
- 产品语义完整保留：
  - `processed_count > 0` → SUCCEEDED（第 634 行）✅
  - `selected_count == 0` / `failed_count > 0` / `not_supported_count > 0` → FAILED（第 636 行）✅
  - `skipped_count > 0` → SUCCEEDED（第 637-638 行，显式）✅
  - 最终 fallthrough → SUCCEEDED（空摘要边缘情况）✅
- 下游消费者（`_emit_direct_result` line 2852, `_run_preprocess_job` line 3274）统一调用 `summary.result_status() is FAILED` ✅

**Focused test `test_preprocess_result_status_separates_skipped_and_not_supported`**:
- `skipped_only` (selected=1, skipped=1) → `SUCCEEDED` ✅
- `unsupported_only` (selected=1, not_supported=1) → `FAILED` ✅
- 同时验证 JSON summary 中 `skipped_count` 与 `not_supported_count` 独立正确 ✅

## Deferred / Info Items Integrity Check

| Item | Original Decision | Current State | Verdict |
|---|---|---|---|
| `FinsUploadPipelineResult.status` 为 `str` 而非 enum | deferred-with-owner | 仍为 `str`，未做 enum 化 ✅ | 未越界 |
| 测试中 `ingest_method` 使用原始字符串 | rejected-with-reason | 测试仍断言 storage JSON 字符串值 ✅ | 未误改 |
| `docs/host/issues-implementation-control.md` 在 diff 中 | rejected-with-reason | 仅更新 gate → `re-review`，治理文档 ✅ | 正常 |
| `read_runtime.py` `meta is None` fallback to `UPLOAD` | deferred-with-owner | fallback 行为保留 (`else FinsIngestMethod.UPLOAD`) ✅ | 未越界 |
| `SecPipeline._build_result` 在 P0-B scope 外 | deferred-with-owner | `sec_pipeline.py` 零 diff ✅ | 未触达 |

## Propagation Audit (Post-Fix)

沿 fix-codex 中的 propagation audit 路径逐项确认：

### Preprocess summary counts
- Producer (`_execute_preprocess_request` line 3592-3605): `skipped_count=len(skipped_ids)`, `not_supported_count=len(not_supported_ids)` — 独立字段 ✅
- Validation (`_bounded_preprocess_summary`): 非负校验、count-to-ID 一致性、cross-field `<= selected_count` — 完整 ✅
- Status (`result_status()`): 消费 validated summary，显式 skipped-only success ✅
- JSON (`to_json_summary()`): 消费 validated `bounded.*` 字段 ✅
- Details (`_preprocess_result_details`): 消费 `json_summary["not_supported_count"]` ✅
- Progress payload (`_preprocess_summary_progress_payload`): 使用 `_non_negative_count` 校验 `not_supported_count` ✅
- Job record (`_run_preprocess_job`): `summary.to_json_summary()` ✅

### CN upload result
- Producer (`_build_upload_result`): `payload.pop("status")` fail-fast, docstring 正确 ✅
- Consumer (`FinsUploadPipelineResult.from_pipeline_json()`): `_required_upload_result_text(result, "status")` ✅
- Docstring: owner boundary 正确，`company_meta_repository` 不再声称 upload 异常 ✅

## Test Suite Verification

Fix-codex 报告的测试结果：
- `pytest tests/fins/test_fins_ingestion_runtime.py -k "preprocess_result_status or upload_pipeline_result_requires_status or unsupported_document_records_not_supported"`: 4 passed
- `pytest tests/fins tests/service/test_fins_direct.py tests/cli/test_fins_commands.py`: 399 passed, 1 skipped
- `pytest tests/tools/test_doc_tools_provider.py`: 47 passed
- `pyright`: 0 errors
- `git diff --check`: pass

新增 focused test 覆盖：
- `test_preprocess_result_status_separates_skipped_and_not_supported` — P0B-F03 跳过/不支持分离 ✅
- `test_preprocess_result_status_rejects_over_classified_counts` — P0B-F02 跨字段不变量 ✅
- `test_upload_pipeline_result_requires_status` — upload typed result fail-fast ✅
- `test_preprocess_result_status_separates_skipped_and_not_supported` 和 `test_start_preprocess_unsupported_document_records_not_supported_summary` 中的 progress payload / JSON summary 断言更新 ✅

未引入新 P0-B blocker。

## New Findings

无。re-review 范围内（P0B-F01/F02/F03 闭合、deferred/info 完整性、新增测试覆盖）未发现新问题。

## Residual Risk

- 与 fix-codex 报告一致：P0-B deferred items（upload status enum hardening、SEC download result status defaults）仍为 deferred，需在后续 sub WU 中处理。
- `result_status()` 最终 fallthrough `return SUCCEEDED` 覆盖全零摘要（selected=0 且 all-zero counts），语义合理（空操作 = 成功），不需要额外处理。

## Conclusion

**pass**

P0B-F01, P0B-F02, P0B-F03 三点全部闭合。Docstring 归属正确、cross-field invariant 到位且保留 cancellation-safe partial、skipped-only success 已显式化。Deferred/info items 未被误改或越界实现。新增 focused test 覆盖 accepted findings 且无新 blocker。P0-B 可以 proceed to accepted commit。
