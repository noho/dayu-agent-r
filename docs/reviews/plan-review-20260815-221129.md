# Plan Re-Review — UF-FIX07 aggregate review plan amendment（fix 后复核）

- Re-reviewed target：`docs/gateflow/uf-fix07-aggregate-review-plan-amendment-20260815.md`（修订版，538 行）
- Fix artifact：`docs/gateflow/uf-fix07-aggregate-review-plan-amendment-fix-20260815.md`（127 行）
- Prior reviews（只读）：`docs/reviews/plan-review-20260815-215532.md`（2 findings）、
  `docs/reviews/plan-review-20260815-220208.md`（2 LOW + 2 open questions）
- 基线 HEAD：`1e04273f0688806bcc0746a0f2178a01d4bc092b`
- Review mode：同 plan session re-review，未 clear；逐项核对 fix 裁决落点、修订后 amendment 全文与生产代码事实
- 本次审查只新增本 artifact；未修改任何实现、测试、README、gateflow、review 或 goal confirmation 文件；
  未运行 UF-PF07/PF12；未 commit/push/PR。

## 逐项状态

### 215532-F1（中）：`_resolve_document_version` 未在签名处绑定 unsafe 行为 — 已修复

- 修订位置：amendment §4.2 新增三个 helper 的 exact branch pseudocode；§4.5 data flow 绑定同一 typed result。
- 独立核对：
  - `_can_skip_upload` pseudocode 与当前生产代码 `docling_upload_service.py:1215-1220` 逐行对应，新增 unsafe gate
    （`if not source_fingerprint.identical_skip_safe: return False`），且放在 deleted 检查之后、digest 比较之前，
    与 §4.4 的既有条件顺序要求一致。
  - `_resolve_document_version` pseudocode 与当前生产代码 `1275-1295` 对应，unsafe 分支在 safe digest-change 分支之前
    无条件 `_increment_document_version(previous_version)`；`previous_meta is None` 恒 `v1`；safe 分支保持既有
    “非空 previous digest 且不等才增长”规则。§4.2 关键不变量段落明确 unsafe 增量不得受 digest 相等、previous
    fingerprint 缺失、overwrite 或 deleted 状态影响——消除了“只改参数类型不改逻辑”的逃逸路径。
  - §4.2 末段明确 `prepare_upload()` 传递同一 `_UploadSourceFingerprint` 实例、两个 helper 不得分别重建安全性、
    持久化只消费 `.value`；与 §4.5 data flow 四处 version 分支（no previous → v1；existing+unsafe → +1；
    existing+safe+changed → +1；existing+safe+same/missing → keep）一致。
- **状态：`已修复`（已纳入计划）。**

### 215532-F2（低）：ambiguous replay 未冻结精确 version 值 — 已修复

- 修订位置：§5.1/§5.2 全部改为精确断言。
- 独立核对：
  - §5.1：flip 场景精确断言 create `document_version="v1"`、flip `"v1" -> "v2"`、同选择 replay `status="skipped"` 且
    version 仍 `"v2"`、converter call count 不增长。
  - §5.2：create(primary=P1) `"v1"`、same-primary replay `"v2"`、flip to P2 `"v3"`，且明确要求“直接读取 repository
    meta/typed prepared mutation 的 canonical version，不能只断言定性‘version increment’”——堵死了 Finding 1+2 组合的
    验证逃逸。
  - §5.2 新增 recovery 闭环：`v3` 后改变一个 descriptor 使 primary 重新唯一 → digest 改变 uploaded `"v4"` → 原样
    replay `skipped` 且保持 `"v4"`。该链与 §4.2 pseudocode 逐分支吻合（safe + changed digest → +1；safe + same → keep）。
- **状态：`已修复`（已纳入计划）。**

### 220208-F1（低）：multi-file safe whole-set move test 缺失 — 已修复

- 修订位置：§5.3 新增“multi-file safe whole-set move owner test”。
- 独立核对：
  - 场景完整：目录 A create（两个可区分 descriptor 的 originals）→ 整组移动到目录 B、primary/basename/bytes/role 不变
    → auto update；断言两个 path-derived storage identities 均改变、新旧 typed fingerprint `.value` 相同且
    `identical_skip_safe=True`、`status="skipped"`、version 仍 `"v1"`、converter call count 不增长。
  - 关键点已覆盖：用新目录 authoritative primary 调用 builder 必须 exact 命中新 original identity（role-to-asset 关联
    不依赖旧 path——prepare 流程中 `_build_upload_source_fingerprint` 先于 `_can_skip_upload` 执行，builder 必然以新
    identity 被行使）；skip 后 repository 保留旧已发布 tree，不错误声称发布新 identity。
  - 与 goal 3.1.2/3.1.4、原 plan §12 move 语义一致。
- **状态：`已修复`（已纳入计划）。**

### 220208-F2（低）：old v1 multi-file transition 缺独立 fixture — 已修复

- 修订位置：新增 §5.4。
- 独立核对：
  - fixture 必须在测试内按 amendment 前公式独立计算旧无角色 digest（descriptors 排序 + 固定 JSON 参数 + SHA-256），
    显式禁止调用新 production builder、禁止 production legacy helper/dual-read/兼容 branch——与 §3.2 non-goal、stop
    condition 6 一致。
  - 断言链完整：seed `previous_meta`（含 old digest）→ 断言 v2 `.value != old_digest`、`identical_skip_safe=True`
    （该断言隐含 fixture 必须使用可区分 descriptors，从而专门证明 digest-mismatch 路径而非 unsafe 路径）→
    `_can_skip_upload(...) is False` → `prepare_upload` 返回 update mutation（非 skipped）→ canonical
    `document_version="v2"` → 再用 v2 digest 重放断言 identical-skip 且保持 `"v2"`。
  - 同 fixture 固定单文件 amendment 前 digest 与当前单文件 `.value` 相等、单文件仍 identical-skip，防止把 multi-file
    version transition 误扩到 single-file——与 §4.3 的单文件公式不升级条款互证。
  - 轻微注记（非 finding）：seed `previous_meta={..., ...}` 的 `...` 隐含必须携带 `is_deleted: False`（否则
    `_can_skip_upload` 沿既有 contract 抛 KeyError），实现时按现有测试 harness 惯例补全即可。
- **状态：`已修复`（已纳入计划）。**

### 220208-OQ1：goal confirmation 与 amendment 关系 — 已收敛

- 修订位置：新增 §1.1。
- 独立核对：§1.1 明确 amendment 不 supersede、不修改、不重开 goal confirmation，仍为 binding scope contract；
  Finding 1 修复被直接映射到已确认目标 #3（“CLI、LLM-facing tool、Service、fresh validation、manifest/storage
  publication 与 downstream process 必须使用同一个 authoritative primary fact”）。该映射与上一轮 review 的独立推演
  一致：skip 决策属于 Service 行为，丢弃 primary 事实即违反目标 #3，role-aware fingerprint 是落实既有目标而非新增
  目标，不构成 goal drift。§8 静态核验清单也已把 goal confirmation 加入 no-diff 列表。
- **状态：`已收敛`（已纳入计划）。**

### 220208-OQ2：validation 收窄为 focused file — 已收敛

- 修订位置：§8 恢复原 accepted plan Slice 4 的 aggregate affected gate。
- 独立核对：
  - 13 个 affected test files 与原 plan Slice 4 逐项一致（upload_format_contract、ingestion_runtime、service_runtime、
    arg_parsing、fins_commands、ingestion_tools、docling_upload_service、docling_upload_service_integration、
    sec_pipeline_upload_filing_stream、sec_pipeline_upload_material_stream、cn_pipeline、fins_storage_atomicity、
    processor_read_consistency）。
  - full pyright `dayu/ tests/ utils/`、六个生产文件逐文件 branch coverage `>=80%` 与 214245 记录的六个修改生产文件
    完全对应；本 aggregate fix 唯一生产文件 `docling_upload_service.py` 单独列出并明确 `>=80%`；其余五个报告仅作
    non-regression acceptance，且明确“不授权修改它们来追 coverage”，无 scope 扩大。
  - 静态核验清单新增两份 plan-review artifacts、原 plan 与 goal confirmation 的 no-diff 检查。
- **状态：`已收敛`（已纳入计划）。**

## 修订后 amendment 的对抗性复检（新问题排查）

- **§4.2 pseudocode 与 §4.4/§4.5/§5 交叉一致性**：unsafe 无条件 +1 的语义在四处（§4.2 pseudocode、§4.2 关键不变量、
  §4.4、§4.5）表述一致，无冲突；deleted/overwrite 检查顺序保持既有 contract。
- **恢复语义（新增）**：§4.4 新增“descriptor 重新唯一即恢复 safe、该次 update 后重放可 skip、churn 非永久状态”；
  与 digest-change 自动触发 update 的机制一致，§5.2 recovery 测试固定了该主张，非空洞声明。
- **old v1 → v2 交互**：v2 dict envelope 与旧 list payload 序列化形态不同，digest 必不等（非 SHA-256 碰撞），
  fail-safe update 成立；单文件公式保留，普通单文件 skip 不被打断。
- **反例回归**：相同 basename+相同 bytes 双路径 flip、普通 descriptor flip、请求顺序变化、重复重传、整组跨目录移动、
  overwrite/delete/material 七类反例在修订版下的行为与上轮 review 结论一致；错误 skip 的完备性论证不受修订影响。
- **边界与所有权**：无新增 allowed files、无新 public type/storage schema/migration、无路径 identity 泄漏、无 input
  order 依赖；skip/version owner 分离比原版更精确。
- **非 material 注记**：§5.4 fixture 的“固定 JSON 参数”未逐字复述
  `ensure_ascii=False, sort_keys=True, separators=(",", ":")`，但 §4.3 已将其定为统一序列化参数且 fixture 指向
  “amendment 前公式”（即当前生产代码 1241-1272 的同一参数），不构成实施歧义。

## Findings

无新 findings。上轮 4 项 findings 与 2 项 open questions 已全部在修订版 amendment 中闭合，且各修订条款与当前生产代码
事实、原 accepted plan Slice 4、goal confirmation 目标 #3 逐项一致；未发现修订引入的新材料问题。

## Open Questions

无。

## Residual risks

修订版 §10 与原 fix artifact 的 residual 表一致，全部已分类：

- ambiguous replay 持续 churn：accepted conservative boundary，绑定当前 descriptor state，非永久标记（§5.2 已固定
  恢复路径）；
- old v1 multi-file 首次 v2 upsert update/version increment：fixed in current aggregate fix（§5.4 fixture 固定）；
- UF-FIX08/UF-FIX10/UF-FIX11 与 evidence 类风险：均 assigned to later work unit，边界未扩大。

## Final plan re-review conclusion

**PASS**

逐项状态：215532-F1 `已修复`、215532-F2 `已修复`、220208-F1 `已修复`、220208-F2 `已修复`、
220208-OQ1 `已收敛`、220208-OQ2 `已收敛`；无新 findings、无 blocking open question。修订版 amendment 已
code-generation-ready，可进入 implementation gate。
