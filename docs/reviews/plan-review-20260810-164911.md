# Plan Re-Review: `WU-CLI-DOWNLOAD-02-DL-F12-F14`（修订稿）

## Reviewed Target

- **Artifact**: `docs/gateflow/wu-cli-download-02-plan-20260810.md`（经 plan review fix 修订）
- **Fix artifact**: `docs/gateflow/wu-cli-download-02-plan-fix-20260810.md`
- **Adjudication**: `docs/gateflow/wu-cli-download-02-plan-review-adjudication-20260810.md`
- **Previous review**: `docs/reviews/plan-review-20260810-161417.md`（6 findings, conclusion=fail）
- **Work unit**: `WU-CLI-DOWNLOAD-02-DL-F12-F14`
- **Branch**: `codex/download-oracle`
- **HEAD**: `3811f95c82fbf0daf15740a5d217eed4d8b49df5`

## Re-review Scope

独立核验每个原 finding 是否真正 closed，并 adversarial challenge：
- category-first matrix
- `CN_FISCAL_PERIOD_ORDER` ownership
- fresh-schema strict parse
- all constructors/consumers
- slice allowed files（特别是 Slice 2/3 共有文件边界）
- coverage/README/production evidence scope

---

## 原 Finding 逐项核验

### Finding 1 (原 HIGH): `sec_pipeline.py` 不在 Slice 3 allowed files

- **状态**: **已关闭** ✓
- **修订证据**:
  - Slice 3 allowed production files 新增 `dayu/fins/pipelines/sec_pipeline.py`（plan line 369）
  - Slice 3 allowed test files 新增 `tests/fins/test_sec_pipeline_download.py`（plan line 382）
  - §5.3 line 247: "SEC 的 4 个 `sec_pipeline.py` 构造点与 `ingestion_runtime.py` 的 2 个 non-persisted generic adapter 构造点都显式传 `()`"
  - Slice 3 item 10（line 405）: 明确 `sec_pipeline.py` 4 个构造点显式传空 tuple
  - Slice 3 item 11（line 406）: "SEC owner test 断言 public JSON 明确包含 `"covered_fiscal_periods": []`"
  - §4.3 构造点表行 4 列出了 SEC 的 4 处构造
  - §7.1 test union 已包含 `tests/fins/test_sec_pipeline_download.py`

### Finding 2 (原 MEDIUM): `to_json_value()` 未列入更新范围

- **状态**: **已关闭** ✓
- **修订证据**:
  - §5.3 line 248: 完整 JSON 链：`FinsDownloadPublicDocument.to_json_value()` 显式写入 `"covered_fiscal_periods": list(self.covered_fiscal_periods)`
  - `FinsDownloadPublicSummary.to_json_value()`（`direct_events.py` line 465）逐行调用 `row.to_json_value()`，新增字段通过委托自动进入 `documents[]`
  - §4.3 表行 5 列出了 serializer 与全链 consumers
  - Slice 3 item 11（line 406）: "显式 `to_json_value()` -> `documents[]` -> wait adapter/CLI row 全链断言 identity + coverage 同源"
  - §7.3 JSON validation（line 470）: `json.dumps()` + `json.loads()` strict round-trip，逐行断言 `documents[].covered_fiscal_periods` 始终存在

### Finding 3 (原 MEDIUM): `FinsDownloadEffectiveFilters` 双 true 防御缺口

- **状态**: **已关闭** ✓
- **修订证据**:
  - §5.1 line 115: 新增唯一私有 `_validate_download_mutation_mode(...)` helper
  - "`FinsDownloadRequest.__post_init__` 调用该 helper...`FinsDownloadEffectiveFilters.__post_init__` 作为同一事实的公共投影，也调用同一个 helper"（line 116）
  - §5.1 固定不变量（line 125）: "`FinsDownloadEffectiveFilters(True, True)` 与 request 一样失败"
  - Slice 1 item 1（line 284）: "新增 §5.1 唯一私有 mode helper；request 与 effective-filter 复用"
  - Slice 1 item 5（line 288）: "两种类型的 `00/10/01` 均成功，`11` 均由同一 helper 抛精确 `FinsDownloadUsageError`；静态 guard 证明模块内只有 helper 含双 true 判断"
  - §10 风险表行 1: "`download_contract.py` 唯一私有 helper；request/filter 复用，parser只投影help"
  - 符合裁决要求："不得形成双 owner" — helper 是唯一实现真源，两个类型只复用

### Finding 4 (原 MEDIUM): rebuild missing 回归断言不足

- **状态**: **已关闭** ✓
- **修订证据**:
  - Slice 2 item 9（line 333）: "CN bare 与 HK bare rebuild 都显式断言 workflow `missing_periods == []`、typed/public 投影 `missing_periods == ()`，provider/HTTP 为 0，不覆盖 source，也不触发 process/processed/reprocess"
  - Slice 3 item 8（line 403）: "CN/HK bare rebuild 继续显式断言空 missing 与 local-only/no-process 边界"

### Finding 5 (原 LOW): results/report 分类优先级不足

- **状态**: **已关闭** ✓
- **修订证据**:
  - §5.3 新增完整的 category-first 分类矩阵（lines 201-223）:
    - Material family 判定表：results/report/ambiguous 三元判定，只用 `category_text`
    - Family 内分类矩阵：7 行，含 identity、covered、必须排除的误判
    - 正负例覆盖：`中期業績`+half-year→Q2/H1、`中期報告`→H1 singleton、`末期業績`+full-year→Q4/FY、`年度報告`→FY singleton、category 缺失/冲突/ambiguous→None
    - 明确禁止："禁止先把 title/category 拼成字符串后靠 tuple 遍历顺序选第一个命中"（line 203）
  - Slice 3 item 3（line 398）: "完整实现并参数化测试 §5.3 category-first 矩阵及正负例；共享 `HALF YEAR`/`半年`、`FULL YEAR`/`全年` token 的结果只能由 material family 消歧，不得靠遍历优先级"

### Finding 6 (原 LOW): candidate 字段迁移未穷举

- **状态**: **已关闭** ✓
- **修订证据**:
  - §4.3 完整 `rg` 构造/消费点穷举表（lines 96-108）: 6 行覆盖全部 typed contract
  - 明确列出 `candidate.fiscal_period` 的 4 个 production consumers 和 `cn_download_filing_workflow.py`、`cn_download_source_upsert.py`
  - §6 共有文件边界表（lines 350-357）: 精确拆分 Slice 2/3 在 4 个共有文件上的修改范围
  - §6 前言（line 263）: "implementation 开始时必须重跑同一组 `rg` 防止基线漂移"

### 原 Open Questions — 全部已关闭

| Open Question | 裁决/修复 | 状态 |
|---|---|---|
| `covered_fiscal_periods` 默认值 | 无默认值；SEC 和 non-persisted 构造点显式传 `()`（adjudication line 29, plan §5.3 line 247） | ✓ |
| `commit_cn_filing_source_document` 的 `form_type` 派生 | `form_type`/`fiscal_period`/`report_kind` 均从 `identity_period` 派生（plan §5.3 line 244） | ✓ |
| CLI 行 coverage 展示格式 | `form_or_period=<identity>` + `covered_fiscal_periods=[...]`，不新增内部 label（plan §5.3 line 249） | ✓ |

---

## 新 Findings

### N1-未修复-中-`cn_report_selection.py` 消费 `query.target_periods` 但不在 Slice 2 allowed files，字段重命名会导致 Slice 2 pyright 失败

- **位置**: §6 Slice 2 allowed production files（lines 306-313）；§6 共有文件边界表（lines 350-357）；`dayu/fins/pipelines/cn_report_selection.py` lines 144, 198
- **问题类型**: 切片过粗 / 不可直接实施
- **当前写法**: Slice 2 exact change 2（line 326）要求 "`CnReportQuery.target_periods -> discovery_periods` 全量机械迁移 production/tests"。但 Slice 2 的 7 个 allowed production files 不包含 `cn_report_selection.py`。共有文件边界表只覆盖 4 个文件，`cn_report_selection.py` 不在其中，它只在 Slice 3 allowed files（line 363）出现。
- **反例/失败场景**: `cn_report_selection.py` 当前在 line 144 (`for period in query.target_periods`) 和 line 198 (`if inferred_period not in query.target_periods`) 直接访问该字段。Slice 2 在 `cn_download_models.py` 中重命名该字段后（不保留 alias），`cn_report_selection.py` 在 pyright 下必定失败。implementation agent 要么被阻塞无法继续 Slice 2，要么被迫越界修改未授权文件。
- **为什么有问题**: Fix 文档 §3 的 `rg` 输出已经证明了 `cn_report_selection.py` 是 `target_periods` 的消费者（"`cn_report_selection.py`" 列在 `rg -n --glob '*.py' 'TargetPeriodResolution|resolve_target_periods|\.target_periods'` 结果中），但修订后的 plan 仍未将其纳入 Slice 2。这不是未知的 discovery risk，而是已知 call site 被遗漏。
- **直接证据**:
  - `dayu/fins/pipelines/cn_report_selection.py` line 144: `for period in query.target_periods:`
  - `dayu/fins/pipelines/cn_report_selection.py` line 198: `if inferred_period not in query.target_periods:`
  - Plan fix §3 `rg` 枚举: 确认 `cn_report_selection.py` 是 period policy/query rename 的受影响文件
  - Plan §6 Slice 2 allowed production files: 不含 `cn_report_selection.py`
  - Plan §6 共有文件边界表: 不含 `cn_report_selection.py`
- **影响**: 实施 Agent 在 Slice 2 被 pyright 阻塞 / 被迫越界修改 / 需停止并交 gate owner 更新 plan
- **建议改法和验证点**:
  1. 将 `cn_report_selection.py` 加入 Slice 2 allowed production files（仅限 query rename，不涉及 classification 变更）
  2. 或将 `cn_report_selection.py` 加入 §6 共有文件边界表，Slime 2 只允许 query rename 部分，Slice 3 允许 classification 重写
  3. 在 §6 前言 "implementation 开始时必须重跑 `rg`" 之后增加：已确认 `cn_report_selection.py` 的两处 `target_periods` 访问必须在 Slice 2 迁移
- **修复风险**: 低（增加一个 allowed file 或一行共享表项）
- **严重程度**: 中

---

### N2-未修复-低-`CN_FISCAL_PERIOD_ORDER` 的 slice 归属未在共有文件边界表中明确

- **位置**: §5.2 line 138；§6 共有文件边界表（lines 350-357）
- **问题类型**: 切片过粗
- **当前写法**: §5.2 将 `CN_FISCAL_PERIOD_ORDER` 定义在 `cn_download_models.py`，由 form policy canonicalization、`CnReportPeriodProjection` 与 selection sort 共同消费。但 §6 共有文件边界表中 `cn_download_models.py` 的 Slice 2 列只写了 "`CnReportQuery.target_periods -> discovery_periods` 与 query docstring"，未提及该常量。
- **反例/失败场景**: 若 implementation agent 严格按边界表执行，Slice 2 不会添加 `CN_FISCAL_PERIOD_ORDER`。但 `resolve_download_period_policy.__post_init__`（Slice 2）需要 canonical order 校验；`CnReportPeriodProjection.__post_init__`（Slice 3）也需要。两处需要同一常量。
- **为什么有问题**: 常量应该在首次需要时定义（Slice 2 的 policy `__post_init__`），但表未写明。implementation agent 可能在 Slice 2 内联 canonical order（与 Slice 3 需保持一致），也可能在 Slice 2 就定义常量但不确定是否越界。风险较低，因为常量值简单且两 slice 都修改同一文件，但属于可避免的 ambiguity。
- **直接证据**:
  - Plan §5.2 line 138: "CN/HK 的 canonical period order 由 `cn_download_models.py` 新增唯一常量 `CN_FISCAL_PERIOD_ORDER`"
  - Plan §6 共有文件边界表 `cn_download_models.py` 行：Slice 2 列未提及该常量
  - Plan §5.3 line 193: `CnReportPeriodProjection` 使用 `CN_FISCAL_PERIOD_ORDER` canonical order — 这是 Slice 3 的消费者
- **影响**: implementation agent 需自行判断常量归属 / 轻微 ambiguity
- **建议改法和验证点**: 在 §6 共有文件边界表 `cn_download_models.py` 的 Slice 2 列增加 "`CN_FISCAL_PERIOD_ORDER` 常量定义"；Slice 3 列保持 "period projection 消费该常量做 canonical validation"
- **修复风险**: 低
- **严重程度**: 低

---

## Adversarial Challenge Results

### Category-first matrix

**结论: 通过**。Material family 判定三元逻辑（results/report/ambiguous）覆盖了所有 category_text 组合。Family 内 7 行矩阵覆盖了已知四材料（Q2 result、H1 report、Q4 result、FY report）和所有 ambiguous 路径。正负例要求（line 224）覆盖了 `中期業績` vs `中期報告`、`末期業績` vs `年度報告`、category 缺失、冲突、report+quarter title 等关键反例。

一个边界 note: "末期"（final）token 在 category_text 中触发 `results` family 判定，但 family 内矩阵的 Q4 行写的是 "Final Results, or full-year/annual/twelve-month result"。当前已知 evidence（`12056833` 的 title 含 "全年"）会命中 "full-year" token。若未来存在 `category_text="末期業績"` 但 title 不含 "全年"/"年度"/"twelve-month" 的罕见发行人，分类会返回 `None`（因为 results family 内无匹配行），这会按既有 selection policy 排除该 candidate。这是 fail-safe 行为，不是错误。不构成 finding。

### `CN_FISCAL_PERIOD_ORDER` ownership

**结论: 通过，含一个低优 ambiguity（见 N2）**。Owner 明确（`cn_download_models.py`），与 domain `FISCAL_PERIODS`（`dayu/fins/domain/filing_semantics.py` line 79）的职责分离清晰：domain `FISCAL_PERIODS` 负责成员校验（membership），CN download `CN_FISCAL_PERIOD_ORDER` 负责 canonical sort order。各模块不重复硬编码排序 tuple。`FinsDownloadDocumentResult` 和 `FinsDownloadPublicDocument` 只用 `FISCAL_PERIODS` 做成员校验，不要求 CN-specific 排序——canonical order 由 `CnReportPeriodProjection` 唯一拥有并投影。

### Fresh-schema strict parse

**结论: 通过**。Plan §5.3 lines 246-247 明确 rebuild 对 `covered_fiscal_periods` 使用 strict required-field parser：缺字段、非 list、非 canonical period、重复、乱序或不含 identity 一律 fail closed，不用 `.get(..., default)` 或旧 schema fallback。Slice 3 item 8（line 403）对所有四类 workflow result（ordinary/skip/failed/rebuild）要求 typed coverage 且 fresh-schema meta 缺失/畸形 fail closed。§10 风险表行 9 明确 "schema任务按 fresh schema起库；不做旧库fallback"。

### All constructors/consumers

**结论: 通过**。§4.3 表覆盖了 6 类 typed contract 的全部 production 构造/消费点。代码验证：
- `FinsDownloadDocumentResult`: 9 处构造（CN 3 + SEC 4 + runtime 2），均已列入 §4.3 表行 4
- `FinsDownloadPublicDocument`: 1 处构造（runtime），列入表行 5
- `FinsDownloadPublicSummary.to_json_value()`（`direct_events.py` line 465）: 委托 `row.to_json_value()` 生成 `documents[]`，新增字段自动进入 JSON — plan §5.3 line 248 正确描述了此委托链
- `dayu/service/fins_wait_adapter.py`: plan 明确为 "机械 JSON consumer"，不修改生产代码，仅测试断言新字段（test 在 Slice 3）
- `build_cn_filing_ids(...)`: download 路径只有 `cn_download_workflow.py` 与 `cn_download_filing_workflow.py` 两处调用；upload 路径不改写（fix §3 明确记录）

### Slice allowed files — 共有文件边界

**结论: 发现一个 gap（见 N1）**。已有文件边界表对其他 4 个共有文件的 Slice 2/3 拆分清晰且可执行。但 `cn_report_selection.py` 的 `query.target_periods` 访问未被共享表覆盖，且不在 Slice 2 allowed files 中。

其他文件的覆盖完整性核验：
- `cn_download_filing_workflow.py` 和 `cn_download_source_upsert.py` 是 `candidate.fiscal_period` 的消费者，在 fix `rg` 中定位，已正确归入 Slice 3 allowed files
- `sec_pipeline.py` 已在修订中补充
- `cninfo_downloader.py` 仅需 query rename，正确归入 Slice 2
- `cn_pipeline.py` 的 `FinsDownloadDocumentResult` 构造点需要 coverage 字段（只在 Slice 3 引入），正确归入 Slice 3

### Coverage/README/production evidence

**结论: 通过**。
- Coverage: §7.2 整文件 line coverage >=80%，不豁免大文件，逐文件报告；§7.1 test union 覆盖 §4.3 全部 test files
- README: §8 明确三份 README 需更新，各有职责理由；`tests/README.md` 从"不更新"改为"需要更新"是因为本 WU 实质扩展了 download owner matrix 和整文件 coverage 口径，属于其现有职责（fix §7）
- Production evidence: §9.1-9.6 定义完整的 post-fix evidence 计划，含 F12 real contract、CN bare-default 回归、HK baseline issuer 选择、0700 四材料对账（含 PDF 内容证据）、evidence closeout；证据采集后冻结，不自动更新 Oracle

---

## Assumptions Tested

| 假设 | 验证结果 |
|---|---|
| 修订 plan 已按裁决关闭全部 6 个原 finding | **成立**。逐项核验均有直接 plan 文本证据 |
| category-first matrix 能消除 report/results 的共享 token 误判 | **成立**。material family 三元判定 + family 内 7 行矩阵覆盖全部已知材料和无匹配路径 |
| `CN_FISCAL_PERIOD_ORDER` 单一定义、多处复用 | **基本成立**。owner 在 `cn_download_models.py`，与 domain `FISCAL_PERIODS` 职责分离。slice 归属有轻微 ambiguity（N2） |
| fresh-schema strict parse 对新旧 workspace 行为正确 | **成立**。fail-closed on missing/malformed coverage，不做旧 schema fallback |
| §4.3 `rg` 穷举覆盖了全部 constructors/consumers | **成立**。6 类 typed contract 全覆盖，production/test 文件均已列入 §6/§7.1 |
| Slice 2 能独立完成 `target_periods → discovery_periods` 全量重命名 | **不成立**。`cn_report_selection.py` 不在 Slice 2，见 N1 |
| `FinsDownloadPublicSummary.to_json_value()` 的委托链正确传播 coverage | **成立**。`documents` 数组通过 `row.to_json_value()` 逐个生成，新增字段自动进入 |

---

## Open Questions

1. `cn_report_selection.py` 的 Slice 2 归属修复后，是否需要更新 §7.1 test union？当前 union 已包含 `test_cn_report_selection.py`，该文件在 Slice 2 和 Slice 3 同时允许时，测试覆盖应无歧义。
2. `dayu/service/fins_wait_adapter.py` 作为"确认无需修改的机械 JSON consumer"是否需要在 §4.3 表中标注"production file not modified"以避免 implementation agent 困惑？当前表中未区分"需要改"和"只需测试改"。

---

## Residual Risks

| 风险 | 跟踪 |
|---|---|
| `末期` token 作为独立 Q4 result marker 的泛化能力 | 当前 design 要求在 `results` family 内匹配 "full-year/annual/twelve-month" token；`12056833` evidence 包含这些 token。若未来发行人只以 "末期" 标记、不含年度/全年 token，会返回 `None`（fail-safe，不误判为 FY report） |
| `ingestion_runtime.py` 整文件 80% line coverage 的测试成本 | Plan 拒绝豁免，要求真实测量。若无法达到需从 diff 删除非必要修改。见 §7.2 |
| Slice 2 全量 rename 范围可能还有未被 `rg` 发现的 call site | §6 前言要求 implementation 开始时重跑 `rg` 防漂移，已作为 stop condition |

---

## Plan Review Conclusion: **fail**

**理由**: N1（`cn_report_selection.py` 不在 Slice 2 allowed files 但必须参与 `target_periods → discovery_periods` 全量重命名）是 blocking 级别的实施缺口。与第一轮 Finding 1 同模式：已知 call site（由 fix 文档 §3 `rg` 确认）未被纳入正确 slice 的 allowed files，implementation agent 会在 Slice 2 遇到 pyright 失败。

修复方向明确（增加一个 allowed file 或一行共享表项），修复风险低。N2 为低优，可在同一修订中处理。6 个原 finding 全部已关闭，category-first matrix、fresh-schema strict parse、constructors/consumers 穷举、coverage/README/evidence scope 均通过 adversarial challenge。

---

## Review Metadata

- **Review 时间**: 2026-08-10T16:49:11+08:00
- **Review 类型**: adversarial plan re-review
- **Base commit**: `3811f95c82fbf0daf15740a5d217eed4d8b49df5`
- **Reviewer**: Claude (planreview skill, 独立于 MiMo/DS)
- **Previous review**: `docs/reviews/plan-review-20260810-161417.md`
- **Output**: `docs/reviews/plan-review-20260810-164911.md`
