# Code Review — PR 190 S3 Compactor LLM-facing conformance

## Scope

- Mode: current changes (S3 slice only)
- Branch: `codex/interactive-oracle`
- Base: `e7db947470551de5e3dca4fc06caf0c35f31901e` (accepted S2)
- Plan: `docs/gateflow/pr-190-compactor-llm-facing-f01-f03-plan-20260803.md` (Slice S3)
- Implementation artifact: `docs/gateflow/pr-190-compactor-llm-facing-s3-implementation-20260803.md`
- Evidence bundle: `/Users/leo/workspace/.dayu-cli-ci/pr190-compactor-llm-facing-20260803-182956/`
- Review artifact: `docs/reviews/pr-190-s3-code-review-ds-20260803-184939.md`
- Included scope:
  - `tests/host/public_smoke_support.py` — 新增 structured environment unavailable classifier
  - `tests/host/test_public_compact_smoke.py` — Mimo-first real smoke、四位置 canary、cap repair、behavior oracle
  - `docs/cli_init_workspace_manifest_v1.json` — 两 prompt asset SHA-256
  - `tests/cli/test_smoke_cli_init_provider_matrix.py` — frozen manifest SHA-256
- Excluded scope: production code（S3 不修改）、S1/S2 已接受改动、`docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`
- Parallel review coverage: 无（单 reviewer 全量走读）

## Findings

### 1-未修复-无缺陷-residual—真实行为 oracle 未取得（两路环境不可用）

- **入口/函数**: `test_real_compactor_resists_injection_and_repairs_policy_caps` → `_real_compactor_proposal_mimo_first`
- **文件(行号)**: `tests/host/test_public_compact_smoke.py:1154-1315`
- **输入场景**: `DAYU_RUN_REAL_COMPACTOR_SMOKE=1`，Mimo 与 DeepSeek 均返回 `network_unavailable`
- **实际分支**: Mimo credential 可用但调用时网络不可用 → `classify_provider_failure_message` 返回 `NETWORK_UNAVAILABLE` → fallback DeepSeek → DeepSeek 同样 `network_unavailable` → 两路环境不可用后精确 `pytest.skip`
- **预期行为**: 按 plan S3 completion signal——"两者都不可用时仅因既有精确 credential/network/quota 分类 skip，并在实现 artifact 中记录实际 provider/两路不可用原因"
- **实际行为**: 精确 skip，未取得真实 proposal；injection 抵抗与 cap repair 行为 oracle 未观测
- **直接证据**:
  - `real-provider-pytest-final.log`: Mimo `network_unavailable` → DeepSeek `network_unavailable` → skip（行 1-4）
  - `provider-fallback-classification.json`: `final_exact_run.result = "skip_after_both_environment_unavailable"`（行 13）
  - `redacted-observations.json`: `behavior_oracle = "not_observed"`（行 29）
  - plan S3 completion signal 原文："两者都不可用时仅因既有精确 credential/network/quota 分类 skip"（plan 行 437）
  - plan risk table 原文："真实 provider 输出存在随机性或 credential/network/quota 不可用 | requiring explicit environment evidence at implementation"（plan 行 524）
- **影响**: 真实自然语言行为证据缺失；deterministic tests 验证了 setup/contract，但不替代真实 LLM 行为观察
- **建议改法和验证点**: 不修改代码或 oracle。在有 credential/network 可用的环境中重新运行 `DAYU_RUN_REAL_COMPACTOR_SMOKE=1 pytest tests/host/test_public_compact_smoke.py -q -k 'real_compactor'`，取得真实 proposal 后验证 injection 抵抗与 cap repair 行为。此 residual 已由实现 artifact 明确记录，owner 为 S3 real-provider smoke 环境
- **修复风险（低）**: 无需修复；仅需环境就绪后重跑
- **严重程度（低）**: 非代码缺陷；plan 显式允许此路径；deterministic contract tests 全绿；residual 已文档化

### 2-未修复-无缺陷-非阻塞—Mimo `runner_empty_final_content` 正确 fail-closed

- **入口/函数**: `_real_compactor_proposal_mimo_first` → `compactor.compact` → `run_prepared_compactor_proposal`
- **文件(行号)**: `tests/host/test_public_compact_smoke.py:1292-1302`；`dayu/host/llm_compaction.py:379`
- **输入场景**: 较早运行中 Mimo 返回空 final content（`runner_empty_final_content`）
- **实际分支**: `LLMCompactionProposalError` 被 `except Exception` 捕获 → `classify_provider_failure_message(case, str(exc))` 对 `runner_empty_final_content` 返回 `None`（不匹配任何环境 marker）→ `_record_unclassified_real_provider_failure` 记录 `unclassified_non_environment_failure` → `raise`（原异常透传，不 fallback DeepSeek）
- **预期行为**: plan S3 明确要求" Mimo 的其它失败必须 fail，不得 fallback"（plan 行 422）
- **实际行为**: 测试 FAIL，不 fallback。与 plan 一致
- **直接证据**:
  - `real-provider-pytest-with-skip-reasons.log`: 完整 traceback 显示 `LLMCompactionProposalError` → test failed（行 99-101）
  - `provider-fallback-classification.json`: `retained_failed_observation.result = "failed_as_required"`（行 20）
  - `redacted-observations.json`: `retained_non_environment_failure_run.fallback = "forbidden_and_not_attempted"`（行 37）
  - `classify_provider_failure_message` 对 `runner_empty_final_content` 返回 `None` 的直接代码路径：`tests/host/public_smoke_support.py:1297-1310`——四个 marker 列表均不匹配此消息
- **影响**: 无负面影响；正确证明了 selector 的 fail-closed 行为；空 final 发生在 strict parser 之前，不表明 prompt contract 不自足
- **建议改法和验证点**: 保留此证据不覆盖。此行为是正确的非环境 fail-closed。若后续 Mimo 稳定后此错误不再出现，证据仍有效
- **修复风险（低）**: 无需修复
- **严重程度（低）**: 正确的 fail-closed 行为，非缺陷

## 逐项裁决

### 1. Selector 严格性 ✅ PASS

- **直接证据**: `tests/host/test_public_compact_smoke.py:1268`——`provider_cases = (PROVIDER_CASES[0], PROVIDER_CASES[1])`，只迭代 Mimo 和 DeepSeek
- `grep -n "PROVIDER_CASES\[" tests/host/test_public_compact_smoke.py` 确认：`PROVIDER_CASES[2]` 和 `PROVIDER_CASES[3]`（Gemini/Qwen）从未在 real smoke 路径中被访问
- Mimo 非环境失败 → `classify_provider_failure_message` 返回 `None` → `raise`，不 fallback（行 1299-1302）
- **裁决**: 无偏离

### 2. 结构化 unavailable classifier ✅ PASS

- **直接证据**:
  - `ProviderEnvironmentUnavailableKind` enum（`public_smoke_support.py:251-256`）定义 5 种精确分类
  - `classify_provider_failure_message`（行 1300-1347）复用既有 `_NETWORK_FAILURE_MARKERS`、`_TEMPORARY_PROVIDER_UNAVAILABLE_MARKERS`、`_EXPLICIT_UNAVAILABLE_MARKERS`、`_TEMPORARY_PROVIDER_RATE_LIMIT_MARKERS`——与旧 `_skip_if_provider_failure_message` 共用同一 marker 真源
  - 旧 `_skip_if_provider_failure_message`（行 1363-1375）改为调用 `classify_provider_failure_message` 后 `pytest.skip(unavailable.reason)`
  - `provider_api_key_or_unavailable`（行 915-936）精确检查 `None` 或 `strip() == ""`，返回 `MISSING_CREDENTIAL`
  - 无 skip 文本解析；selector 不解析 `unavailable.reason` 字符串
- **裁决**: 无偏离

### 3. Real smoke 构造 ✅ PASS

- **直接证据**:
  - `_real_compactor_adversarial_request()`（行 782-847）：单一 `CompactionRequest`，在 `current_input_text`、`trace_material`、`evidence_material`、`answer_material` 四处放置不同 canary
  - 使用 production `LLMContextCompactor`（行 1285-1291）发起真实 proposal
  - 使用 production `parse_conversation_compact_output_vnext`（在 `LLMContextCompactor.compact` 内部）解析
  - 使用 production `accept_compact_candidate_v2`（行 1214-1218）验收
  - `_deterministic_over_cap_candidate()`（行 875-920）是 typed `CompactCandidateV2`，不经过任何 production filter/verifier
  - repair feedback 由 `build_compact_repair_feedback_v2`（行 1190）从同一 `policy` instance 的 governance reject 产生
  - `grep -n "filter\|verifier\|sanitize\|clean\|strip_injection\|blacklist\|blocklist"` 在测试文件中无命中
- **裁决**: 无偏离

### 4. 行为 oracle ✅ PASS

- **直接证据**:
  - `_INJECTION_BEHAVIOR_FORBIDDEN_FRAGMENTS`（行 309-314）：只禁止 schema attack（`output.attack-v9`）、虚假动作（`北辰零息债券`）、虚假事实（`999亿元`、`不存在任何经营风险`）出现在业务区
  - `_candidate_business_text()`（行 926-945）：有意排除 `diagnostics` 和 `explicitly_dropped_sources`；docstring 明确说明"diagnostics 可用业务可读方式提及材料风险，因此有意不纳入禁止命中范围"（行 928-929）
  - oracle 不使用全字符串禁见规则；不禁止模型在 diagnostics 中提及攻击文本
  - 测试 `test_provider_environment_failure_classification_rejects_unknown_failure`（行 432-445）验证非环境失败不被误分类——覆盖假阳性路径
  - `test_provider_credential_lookup_returns_structured_missing_reason`（行 412-429）用 monkeypatch 精确控制 credential 为空——覆盖 credential 边界
  - 无 fixture 自证：`_deterministic_over_cap_candidate` 是 typed 构造，不依赖前次测试输出
- **裁决**: 无偏离

### 5. Publication hash 一致性 ✅ PASS

- **直接证据**:
  - `sha256sum` 命令验证：
    - `conversation_compaction.md`: `4bd476db45f17bebaa7eb951c8354d10189df1faadb9c1c530619d9f3352f60a` = manifest 中值 = implementation artifact 中值 = `publication-hashes.json` 中值
    - `conversation_compaction_user.md`: `bed77319ee960059ac65119f28a3c6005f53f44ffeb8b7c00c8d1df1e93dc3a5` = manifest 中值 = implementation artifact 中值 = `publication-hashes.json` 中值
    - `docs/cli_init_workspace_manifest_v1.json`: `d63fb2ca415e914c9aaa3959b0b88be2072d1997b70799c9a2ac7de91fce2408` = 测试常量中值 = `publication-hashes.json` 中值
  - `git diff e7db9474 -- docs/cli_ci_oracles.json docs/cli_ci_scenarios.json` 无输出——frozen CLI oracle/scenario 未改
  - `publication-validation.log`: 287 passed——frozen manifest 与 package config tree 同源
- **裁决**: 无偏离

### 6. Evidence bundle ✅ PASS

- **直接证据**:
  - 路径: `/Users/leo/workspace/.dayu-cli-ci/pr190-compactor-llm-facing-20260803-182956/`——新路径，与之前 evidence 不重叠
  - 文件权限: `r--r--r--`（只读）
  - `redacted-observations.json`: `"credentials": "not_recorded"`, `"provider_payload": "not_recorded"`——脱敏确认
  - `SHA256SUMS`: 12 个文件全部 `OK`
  - `real-provider-pytest-final.log`: 明确显示 Mimo → DeepSeek 分类与精确 skip；未将 skip 误报为 behavior pass
  - `validation-summary.md`: 正确记录"strict parsing, governance acceptance, cap compliance, and injection behavior could not be observed"
  - `redacted-observations.json`: `behavior_oracle = "not_observed"`, `result = "precise_skip_after_mimo_and_deepseek_network_unavailable"`——不声称 pass
- **裁决**: 无偏离

### 7. 真实运行历史裁决 ✅ accepted residual → S4

- **final exact run**: Mimo `network_unavailable` → DeepSeek `network_unavailable` → exact skip
  - plan S3 completion signal 显式允许此路径："两者都不可用时仅因既有精确 credential/network/quota 分类 skip"
  - plan risk table 将此场景分类为 `requiring explicit environment evidence at implementation`，非 blocking
  - 实现正确处理：精确 skip，不伪造 pass，记录两路原因
- **retained `runner_empty_final_content`**: 正确 fail-closed，不 fallback
  - plan S3 显式要求："Mimo 的其它失败必须 fail，不得 fallback"
  - `classify_provider_failure_message` 正确返回 `None`（空 final 不在四个环境 marker 列表中）
  - 测试正确 raise 原异常
- **behavior oracle 未取得**:
  - 非代码或测试设计缺陷——环境不可用导致
  - plan 和实现 artifact 均已分类此场景
  - deterministic contract tests（30 passed）验证了 owner-level setup 正确性
- **裁决**: **accepted residual**，可进入 S4。不构成 blocking finding。依据 frozen plan 的直接证据（completion signal + risk table），此结果在 plan 允许路径内。Residual 已在实现 artifact 和 evidence 中正确记录，owner 为 S3 环境
- **S4 注意事项**: S4 documentation 应记录此 residual；不将"behavior oracle 未取得"写成"behavior 已验证"

### 8. 验证通过 ✅ PASS

- **Deterministic tests**: `tests/host/test_public_compact_smoke.py -q` → 30 passed, 1 skipped（`deterministic-public-smoke-final.log`）
- **Publication/assembly tests**: 287 passed, 3 warnings（均为既有 `edgar` deprecation）（`publication-validation.log`）
- **Pyright**: 0 errors, 0 warnings, 0 informations（`pyright-final.log`）
- **Diff check**: pass（`diff-check.log` 为空）
- **Evidence SHA256SUMS**: 全部 OK
- **Ownership**: 未发现 semantic ownership drift。Classifier 在 `public_smoke_support.py`（测试基础设施 owner），selector 在 `test_public_compact_smoke.py`（real smoke owner），不穿透 production boundary
- **过耦合**: 未发现。测试通过 public API（`LLMContextCompactor`、`accept_compact_candidate_v2`、`build_compact_repair_feedback_v2`）与 production 交互，不依赖内部实现细节
- **AGENTS.md / CLAUDE.md**: 未违反
- **README S4 boundary**: S4 文档更新不在本 slice scope；S3 未修改任何 README

## Open Questions

无。

## Residual Risk

| Risk | Owner | Classification |
|---|---|---|
| 真实 provider injection/cap behavior oracle 未取得 | S3 real-provider smoke 环境 | accepted residual per plan；需在 credential/network 可用环境中重跑 |
| deterministic tests 不能替代真实自然语言行为证据 | 既有 Issue 80（Conversation Memory eval） | 已知限制，不在 S3 scope |
| `runner_empty_final_content` 的外部 provider 非确定性 | Mimo provider 端 | 非代码缺陷；selector 正确 fail-closed |
| 单次 real provider 调用只覆盖一个 provider 的行为 | S3 设计 | 按 Mimo-first 设计；非缺陷 |
