# Code Review — WU-CTX-01 Slice 2 Implementation Re-Review

## Scope

- Mode: current changes
- Branch: `feat/wu-ctx-01`
- Base: `b6f297b4`
- Output file: `docs/reviews/code-review-20260724-060844.md`
- Included scope: Slice 2 production/tests/README diff 相对 base `b6f297b4`，以及 CTRL-S2-IMPL-01 fix（仅 `tests/host/test_durable_schema.py`）
- Excluded scope: Controller-owned `docs/host/issues-implementation-control.md`、implementation artifact `docs/reviews/wu-ctx-01-slice-2-implementation-codex.md`、fix artifact `docs/reviews/wu-ctx-01-slice-2-implementation-review-fix-codex.md`、adjudication artifact `docs/reviews/wu-ctx-01-slice-2-implementation-review-controller-adjudication.md`、初审 artifacts `docs/reviews/code-review-20260724-055928.md` 与 `docs/reviews/code-review-20260724-055648.md`

## Reference Documents Read

- 根 `AGENTS.md`
- `docs/reviews/wu-ctx-01-plan-codex.md` §8.3
- 初审 artifacts（`code-review-20260724-055928.md`、`code-review-20260724-055648.md`）
- Controller adjudication（`wu-ctx-01-slice-2-implementation-review-controller-adjudication.md`）
- Fix artifact（`wu-ctx-01-slice-2-implementation-review-fix-codex.md`）

---

## Findings

未发现实质性问题。

### Detailed Evidence

#### 1. CTRL-S2-IMPL-01 fix 验证：测试名/docstring/assertion 与 HOST_SCHEMA_VERSION=24 同源

**fix diff**（`tests/host/test_durable_schema.py`）：

| 项目 | fix 前（base） | fix 后（当前） |
| --- | --- | --- |
| 函数名 | `test_host_schema_version_is_queue_policy_check_version` | `test_host_schema_version_is_context_budget_canonical_fact_version` |
| docstring | "queue policy CHECK schema 23" | "context budget canonical fact schema 24" |
| assertion | `HOST_SCHEMA_VERSION == 23` | `HOST_SCHEMA_VERSION == 24` |

**production 同源验证**：`dayu/host/durable/schema.py` diff 将 `HOST_SCHEMA_VERSION` 从 23 改为 24，owner 是新增 `CONTEXT_BUDGET_EVALUATED` canonical fact contract（Slice 2 新增 event type）。测试的函数名、docstring 和 assertion 现在准确描述了同一 owner。

**注意**：Controller adjudication 仅要求更新函数名和 docstring（因 Controller 假定 assertion 已是 24），但实际 fix 也把 assertion 从 23 改为 24。这是正确且必要的——assertion 在 base 中是 23，production schema 已改为 24，不更新 assertion 会导致测试失败。此项修改未超出"只修改 `tests/host/test_durable_schema.py`"的 scope 约束。

**运行验证**：`pytest tests/host/test_durable_schema.py::test_host_schema_version_is_context_budget_canonical_fact_version` → `1 passed in 0.30s`。

**结论：CTRL-S2-IMPL-01 已正确修复，无 scope drift。**

#### 2. fix 无 production/public/schema 行为 scope drift

fix artifact 声明仅修改 `tests/host/test_durable_schema.py`。实际 `git diff HEAD -- tests/host/test_durable_schema.py` 确认 unstaged 变更仅限该文件，且仅涉及函数名、docstring 和 assertion 值。没有 production 文件、public contract、schema、Controller control doc 或其他 test 被修改。

**结论：零 scope drift。**

#### 3. DS-F1 未被提前实现，仍属于 Slice 3

全量 diff 搜索确认：
- 无 `ContextAnchorDiagnostic` 构造逻辑
- 无 `resolve_context_anchor` 或 `CompatibleContextAnchor` 调用
- 无 `anchor_diagnostic` 非 `None` 赋值
- `ContextEstimateMethod` 仍仅两个成员：`USAGE_ANCHORED` 和 `CONSERVATIVE_FALLBACK`
- 所有 producer 固定使用 `build_conservative_context_sizing_result` 或 `build_conservative_context_sizing_result_from_atoms`，强制 `estimate_method=CONSERVATIVE_FALLBACK`
- `build_context_budget_evaluated_payload` 中 `"anchor_diagnostic": None` 硬编码

**结论：DS-F1 的 anchored builder roundtrip 继续由 accepted Slice 3 覆盖，未被提前实现。**

#### 4. DS-F2/F3 保持 Controller 驳回边界

- DS-F2（`AssertionError` → `ValueError`）：`parse_context_budget_evaluated_payload` 中 `else: raise AssertionError(...)` 未被修改，保持 Controller 裁决的 rejected 状态。
- DS-F3（`_context_usage_activity` 前置 `Mapping` 检查）：`read_api.py` 未新增 `isinstance(raw, Mapping)` pre-check，保持 Controller 裁决的 rejected 状态。

**结论：DS-F2/F3 驳回边界未被突破。**

#### 5. 此前结论仍成立

以下结论在初审中已验证，本次 re-review 确认 fix 未影响任何 production/test 语义，结论不变：

| 维度 | 初审结论 | re-review 确认 |
| --- | --- | --- |
| canonical fact schema/strict parser/deterministic identity/idempotent append/conflict fail-closed | ✅ 成立 | ✅ 不变 |
| 15 类 producer manifest→fact→transition 同事务顺序 | ✅ 成立 | ✅ 不变 |
| CAS/precondition rollback 零孤立写入 | ✅ 成立 | ✅ 不变 |
| startup strict-load matching source manifest + source budget fact | ✅ 成立 | ✅ 不变 |
| Host 七字段 public view → Service typed pass-through | ✅ 成立 | ✅ 不变 |
| policy missing / failed-lost wait / unavailable continuation 零伪造 fact | ✅ 成立 | ✅ 不变 |
| Slice 2 仅 conservative，不含 anchor/USAGE_ANCHORED 越界 | ✅ 成立 | ✅ 不变 |
| AGENTS 类型/docstring/README/coverage 约束 | ✅ 满足 | ✅ 不变 |

---

## Open Questions

无。

## Residual Risk

与初审 residual risks 一致，fix 未新增任何 residual risk：

1. `context_events.py` 局部 durable import（已知架构约束，长期需提取 shared contract 层）。
2. `build_context_budget_evaluated_payload` 前向兼容缺口（Slice 3 必做，已由 Controller 裁决纳入 Slice 3 计划）。
3. `HOST_SCHEMA_VERSION` 23→24 无迁移脚本（本项目 schema 变更按 fresh schema 起库，不要求旧库迁移）。
4. `ContextSizingResult` 与 canonical payload 的 anchored 类型扩展属于已冻结的 Slice 3 工作。

---

## Accepted Finding Closure

| Finding | Controller 裁决 | Fix 状态 | re-review 确认 |
| --- | --- | --- | --- |
| CTRL-S2-IMPL-01 | accepted，必须修复 | 已修复 | ✅ 函数名/docstring/assertion 均与 HOST_SCHEMA_VERSION=24/context budget canonical fact owner 同源 |
| DS-F1 | 当前 Slice 2 驳回；Slice 3 计划内 | 不在 fix scope | ✅ 未被提前实现 |
| DS-F2 | rejected | 不在 fix scope | ✅ 驳回边界未突破 |
| DS-F3 | rejected | 不在 fix scope | ✅ 驳回边界未突破 |

---

## Verdict

**pass**

CTRL-S2-IMPL-01 已正确修复。fix 仅修改 `tests/host/test_durable_schema.py` 的函数名、docstring 和 assertion，与 production `HOST_SCHEMA_VERSION=24` 和 context budget canonical fact owner 完全同源。无 production/public/schema scope drift。DS-F1 未被提前实现。DS-F2/F3 驳回边界保持不变。此前所有 canonical fact、producer ordering/rollback、startup strict replay、public secrecy 结论均仍成立。
