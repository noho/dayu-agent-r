# F19 RR4 独立最终 plan + wrapper 复审

- 复审时间：2026-08-08 19:05:41 CST
- 二值结论：**PASS**
- Goal Confirmation：`docs/gateflow/pr-190-f19-goal-confirmation-20260808.md`
  - file SHA-256：`ddebaf1159bf8a19fbc3d171e75b88fdb54df23b60089015c03191cf6a3df004`
- 冻结 plan：`docs/gateflow/pr-190-f19-plan-20260808.md`
  - file SHA-256：`9ad1cee81e0e469715e248f11318900fe7449b5a0d47a4eea443c9dc457ea207`
- 冻结 driver：`workspace/tmp/f19_formal_driver.py`
  - file SHA-256：`c8fa0d4ba70a347cb0b91a9660aa6366c1765dafd8ae593c65434f7d3f2540aa`
- R1-R4 prompt file SHA-256：
  - R1：`681d525c10032a315d9c35d5225e4fe1c991a28caa9899681d887027038e6869`
  - R2：`bbe6610654c9d14252b999a49b283188e5174b048106eb7ee9a1971c8b97728d`
  - R3：`82aaf14ad1b7e3f6034fab465c94070ff4359feecfe08008fb0bab647e54e2c6`
  - R4：`345cac1fcb0641ee8f6bc5fe144fdb87a7fd162a79fad34239d190df7c18386f`

## Reviewed target and scope

本轮只复审 RR3 的唯一 finding：real run-root 的 machine-local owner、direct-child/relative-layout/containment contract，以及从 filesystem root 到可信 parent 和 run-owned lexical components 的 no-symlink gate。随后回归 RR2 typed canonical Run/R4 prerequisite 与首轮 identity、deadline、allocation、offset、projection、preflight findings。

所有动态检查均为 provider-free generic scratch 或纯内存 owner contract 检查；没有创建正式 F19 root/deadline，没有调用 provider，没有修改冻结 plan、driver、prompt 或 F18 evidence。

## Assumptions tested and direct evidence

| 假设 | 直接证据 | 结论 |
| --- | --- | --- |
| canonical runs-parent 是 run tree/caller/allocation 外的唯一 owner | driver 使用 private machine-local constant；real CLI 不接受 parent 参数；allocation/deadline schema 不产生该路径；plan 只发布 path-redacted owner identity | 通过 |
| run root 只能是 canonical parent 的 direct child，且 basename 等于 run id | 授权 direct-child 正例通过；同 basename outside 与 F18-like nested root 均在 prompt 读取前抛出 `ValueError` | 通过 |
| canonical parent 自身的 ancestor symlink 不可被 `resolve()` 掩盖 | generic scratch 令 canonical parent 的 ancestor 为 symlink；逐组件 `lstat` gate 在 prompt/emit/spawn 前拒绝 | 通过 |
| run-owned 任一既有 lexical component symlink 均拒绝 | run-root、`workspaces`、`evidence`、`private`、prompt leaf 与 fresh output leaf symlink 六类反例全部拒绝 | 通过 |
| traversal、wrong layout 与 resolved escape fail closed | 字面 `..` 与 wrong wrapper layout 反例均在读取 run-owned prompt/JSON 前拒绝；实现还对 required directory/file 与 fresh-leaf parent做 resolved containment | 通过 |
| 每个 path 反例在首次写入/进程启动前终止 | 全部反例 recorder 均为 `prompt=0, emit=0, spawn=0`；projection/evidence leaf 状态保持 unchanged | 通过 |
| repo/CLI 没有被升级成新的绝对位置 contract | `_execution_identity` 仍只验证 detached target/clean 与 CLI file SHA；任意位置的测试 path 在 owner facts正确时通过，wrong CLI SHA 拒绝 | 通过 |
| private absolute owner 不泄漏到 tracked plan/public contract | 对 plan 扫描 machine-local absolute path prefixes，命中为零；`git check-ignore -v` 证明 driver 与 prompts 由 `workspace/` ignore rule覆盖 | 通过 |
| R2-R4 继续使用 canonical typed Run owner | driver 无 `sqlite3`/`host_runs` 查询；复用 `observe_run_terminals` 与 typed label-slot reader | 通过 |
| R4 只在第三 Run accepted-terminal 区间内唯一 accepted compact 后启动 | no compact、wrong session、wrong run、区间外、duplicate 与 strict payload drift 均拒绝；唯一同 session/run 的区间内事件通过并实际调用 `resolve_context_compacted_payload` | 通过 |
| exact execution identity 无回归 | exact target/model/CLI/harness owner facts通过；wrong model 与 wrong CLI SHA 拒绝；detached target HEAD 直接核验为冻结 commit且 clean | 通过 |
| allocation 为 immutable exact 三链 | valid 3-chain通过；第四链与 workspace input digest divergence 拒绝；profile/input/ordinal/label/segment binding保留 | 通过 |
| deadline/run/chain binding 与预算无回归 | exact 1800 秒 window、180 秒 reserve 与有效 chain window通过；NaN/Infinity requested timeout、future start拒绝；effective timeout取最小剩余预算 | 通过 |
| R1-R4 offset/prompt contract 无回归 | 四段 offset exact `0/1/2/3`；每段恰一个 `--prompt` 且正文为单一 argv element；真实 R1 prompt strict UTF-8/nonempty通过，empty prompt拒绝 | 通过 |
| projection/preflight 无回归 | real projection在 spawn 前以 exclusive create落盘，第二次写触发 `FileExistsError`；stdout-only projection不创建目录 | 通过 |

静态质量检查：source-only compile、Ruff、单文件 pyright全部通过；pyright为0 errors、0 warnings。

## Findings

无 material findings。RR3 的 write-root identity 与 ancestor symlink finding 已由外部 owner、direct-child contract、逐组件 lexical `lstat` 和 resolved containment共同关闭；未发现将 repo/CLI location错误纳入该 contract 的 scope drift。

## Review lenses

- **Goal-bound minimal design**：修改只收紧已确认的 external observation owner boundary，不改变产品、Host/Engine、analyzer或B2业务语义。
- **Architecture boundary**：machine-local runs-parent由 ignored private driver唯一拥有，不接受run tree内文件或caller自证；public projection只承诺脱敏 identity。
- **Best practice / optimal solution**：先 lexical `lstat`、再 exact layout、最后 resolved containment形成互补防线，直接覆盖整树搬移与symlink canonicalization反例。
- **Overengineering / overcoupling**：没有新增产品层 abstraction、schema或repo/CLI位置耦合；修复范围保持在run-owned external driver。

## Open questions

无阻塞 open questions。

## Residual risks and tracking

- 本轮只证明 provider-free wrapper gate，不证明真实模型会自然触发 replacement、bounded repair或budget-exhausted fallback；这些必须在正式 observation 中按 observed/needs-more 原样记录。
- publication digest、path/secret scan、human-readable B2 report 与fresh reconnect仍需由后续正式 execution/publication gate验证；本次 PASS 不构成B2裁决或overall readiness。
- machine-local parent是本次私有 observation tooling 的冻结环境事实；若执行机器或runs-parent变化，必须重新生成并重新review driver，而不是从caller/run tree引入fallback。

## Final plan review conclusion

**pass（binary PASS）**。RR4 指定的唯一 RR3 finding 已关闭，RR2 typed canonical Run/R4 gate及首轮identity/deadline/allocation/offset/projection/preflight contract均无回归。该 plan + wrapper 可以进入另一独立 reviewer裁决；只有双路均为PASS后，Controller才可创建正式F19 root/deadline并启动provider。
