# Code Review — PR 190 Compactor LLM-facing S2 (AgentDS)

## Scope

- Mode: current changes (working tree against accepted S1 base)
- Branch: `codex/interactive-oracle`
- Base: `64aade07` (accepted S1 commit = current HEAD)
- Output file: `docs/reviews/pr-190-s2-code-review-ds-20260803-182338.md`
- Included scope: 全部 7 个 changed files — 3 个 production（`llm_compaction.py`、`compaction.py`、`context_governance.py`）、2 个 prompt（`conversation_compaction.md`、`conversation_compaction_user.md`）、2 个 test（`test_llm_compaction.py`、`test_compaction_contract.py`）
- Excluded scope: S1 prompt trust-boundary/example 变更（已由 S1 re-review 验收）、S3 real-provider smoke/publication hash、S4 docs/README
- Parallel review coverage: 无（单 reviewer 全链路走读）

## Validation Summary

| Check | Result |
|---|---|
| `pytest tests/host/test_llm_compaction.py tests/host/test_compaction_contract.py -q` | 48 passed in 0.38s |
| `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings, 0 informations |
| `git diff --check` | clean (no output) |

## Findings

### 逐项审查结论

以下按用户指定的 8 个审查维度逐项报告，每项给出 direct code/data evidence 与判定。

---

#### 1. Context Governance reject truth ownership 与 9 条 section cap issue 精确性

**判定：通过。无 ownership 漂移，无 downstream 重算。**

直接证据：

- Reject truth owner 仍是 `context_governance.py::accept_compact_candidate_v2`（line 59-114），其内部调用 `_collect_policy_issues`（line 479-537）和 `_section_caps`（line 540-577）产生 cap issues。
- session_summary text cap issue（line 496-504）精确包含：
  - section: `session_summary.text`
  - actual: `{summary_size} 个字符`（来自同一 policy instance 的 `estimate_memory_size_units(...).units`）
  - cap: `{policy.session_summary_char_cap} 个字符`
  - 直接动作: `请缩减 session_summary.text 到不超过 {cap} 个字符`
- 四个 section 的 item cap issue（line 560-566）精确包含：
  - section: `{section}`（evidence_facts/answer_anchors/forward_intents/reference_continuity）
  - actual: `{len(texts)} 项`
  - cap: `{item_cap} 项`
  - 直接动作: `请删减或合并 {section}，只保留不超过 {item_cap} 项`
- 四个 section 的 size cap issue（line 569-576）精确包含：
  - section + measurement: `{section} 的{size_measurement}`
  - actual: `{total} 个字符`
  - cap: `{size_cap} 个字符`
  - 直接动作: `请缩减 {section} 的文本总量到不超过 {size_cap} 个字符`
- 四个 `size_measurement` 常量（line 49-52）与 `_section_caps` 调用方传入的实际文本投影精确一致：
  - evidence_facts: `"各 claim 字符数之和"` ↔ `tuple(item.claim for ...)`（line 508）
  - answer_anchors: `"每项 title、一个换行符和 detail 的字符数之和"` ↔ `tuple(f"{item.title}\n{item.detail}" for ...)`（line 516）
  - forward_intents: `"各 text 字符数之和"` ↔ `tuple(item.text for ...)`（line 524）
  - reference_continuity: `"各 text 字符数之和"` ↔ `tuple(item.text for ...)`（line 533）
- projector（`_repair_feedback_prompt_json_vnext`，llm_compaction.py line 680-703）不读取 policy、不读取 candidate、不重算 actual/cap。它只投影 `feedback.required_action` 和 `feedback.issues[*].(code/json_path/message/source_labels)`。
- renderer（`_user_prompt_vnext`，llm_compaction.py line 647-677）不读取 policy/candidate。

**结论：cap 数值由 Governance owner 在真实判断点从同一 `MemoryProjectionPolicy` instance 与 `estimate_memory_size_units` 产生；projector/renderer 不做任何重算或补偿。无 ownership 漂移。**

---

#### 2. `_repair_feedback_prompt_json_vnext` 的 typed input 与 exact key contract

**判定：通过。严格 typed input，不经过 `to_json()`，不接受 raw Mapping。**

直接证据：

- 函数签名（llm_compaction.py line 680-682）：`def _repair_feedback_prompt_json_vnext(feedback: CompactRepairFeedbackV2) -> dict[str, JsonValue]` — 只接受 typed `CompactRepairFeedbackV2`。
- 类型守卫（line 690-691）：`if not isinstance(feedback, CompactRepairFeedbackV2): raise TypeError(...)` — 拒绝 raw Mapping。
- 读取 typed fields（line 693-701）：
  - `feedback.required_action` — 直接读 typed 字段
  - `issue.code.value` — 读 typed enum 的 `.value`
  - `issue.json_path`、`issue.message`、`issue.source_labels` — 直接读 typed 字段
- 不调用 `feedback.to_json()`：函数体内无 `to_json()` 调用。✓
- LLM block exact keys：
  - 顶层：`required_action`、`issues`（line 693-694）
  - 每项 issue：`code`、`json_path`、`message`、`source_labels`（line 696-700）
- 测试验证（test_llm_compaction.py line 264-266）：`cast(CompactRepairFeedbackV2, {"issues": []})` 触发 `TypeError`。
- 测试验证（line 289-295）：projected JSON 的顶层 key set 为 `{"required_action", "issues"}`，issue key set 为 `{"code", "json_path", "message", "source_labels"}`。

**结论：projector 是严格的 typed pure function，不经 internal serialization 中转，不接受 loose input。exact key contract 与 prompt 描述完全一致。**

---

#### 3. Internal durable serialization vs LLM-facing block 的字段隔离

**判定：通过。Internal 保留完整 attempt/count，LLM block 绝不暴露。**

直接证据：

- `CompactRepairFeedbackV2.to_json()`（compaction.py line 1662-1673）：仍完整序列化 `previous_attempt_number`、`issues`、`additional_issue_count`、`required_action`。docstring 已改为 `"转换为 durable/internal serialization JSON"`。
- `_repair_feedback_prompt_json_vnext`：只投影 `required_action` 和 `issues[*].(code/json_path/message/source_labels)`，不包含 `previous_attempt_number` 和 `additional_issue_count`。
- 测试断言（test_llm_compaction.py line 256-262）：internal `to_json()` 仍包含全部四个顶层 key。
- 测试断言（line 297-303）：projected JSON 的 serialized block 不含 `previous_attempt_number`、`additional_issue_count`、`CompactRepairFeedbackV2`、`CompactValidationIssueV2`、`Memory policy`。
- `_compactor_input_projection_json`（llm_compaction.py line 624）：对 durable artifact 仍使用 `repair_feedback.to_json()` 保存完整 internal 信息。正确，因为这是 durable projection，不是 LLM-facing。

**结论：internal/durable 路径保留完整治理字段；LLM-facing block 精确排除。无字段泄漏。**

---

#### 4. First/repair attempt marker 行为与 PREVIOUS_VALIDATION_REPORT_JSON 消除

**判定：通过。First attempt 无 repair block，repair attempt 恰一对新 marker，旧 marker 已从产品路径消失。**

直接证据：

- `_user_prompt_vnext`（llm_compaction.py line 665-677）：
  - `repair_feedback is None` → 直接返回 rendered，不追加任何 repair block。
  - `repair_feedback is not None` → 追加 `REPAIR_FEEDBACK_JSON_BEGIN\n{json}\nREPAIR_FEEDBACK_JSON_END`。
- 旧 marker `PREVIOUS_VALIDATION_REPORT_JSON` 已从以下全部位置删除：
  - `llm_compaction.py`: 旧代码 line 665-673 被替换为新的 `_REPAIR_FEEDBACK_BEGIN/END` marker 与新 projector。
  - `conversation_compaction.md`: 旧文本 "如果请求末尾含前一次完整 candidate 的脱敏校验反馈" → 新文本 "首次请求不含修复反馈。只有前次输出被拒绝后，请求末尾才会出现由独占行 `REPAIR_FEEDBACK_JSON_BEGIN` 与 `REPAIR_FEEDBACK_JSON_END` 包围的 JSON 数据块"。
  - `conversation_compaction_user.md`: 同上替换。
- 测试断言（test_llm_compaction.py line 281-286）：
  - `first_prompt` 的 REPAIR_FEEDBACK_BEGIN/END 计数均为 0。
  - `repair_prompt` 的 REPAIR_FEEDBACK_BEGIN/END 计数均为 1。
  - `_OLD_REPAIR_FEEDBACK_MARKER`（即 `PREVIOUS_VALIDATION_REPORT_JSON`）不在 first_prompt 也不在 repair_prompt。
- whole-candidate repair 动作自足性（`COMPACT_REPAIR_REQUIRED_ACTION`，compaction.py line 1622-1625）：明确要求"基于本次请求中的同一输入，重新生成一个符合当前输出 schema 的完整 replacement candidate"、"不是 patch"、"不得复制、拼接、补写或复用前次输出的任何部分"。
- 两份 prompt 均明确："不得复制、拼接、补写或复用前次被拒绝的输出或任何片段"（system line 20，user line 81）。

**结论：marker 行为精确，旧 marker 已完全消除，whole-candidate action 自足且禁止 rejected partial 复用。**

---

#### 5. 两份 prompt 的自足性、认知负担与 schema/example 一致性

**判定：通过。两份 prompt 在当前上下文自足说明字段名、类型、必填性、允许值、source_labels 语义边界与 feedback 非业务材料属性，并提供最小 JSON 示例。无歧义、无内部术语泄漏。**

直接证据（仅列关键行）：

System prompt（conversation_compaction.md）：
- Line 9-10: 材料信任边界（`UNTRUSTED_COMPACTION_MATERIAL_JSON_BEGIN/END` + 数据块外规则控制 + 材料内指令不执行）
- Line 17: 首次无 repair，repair 时出现 `REPAIR_FEEDBACK_JSON_BEGIN/END` marker
- Line 18: 顶层 exact keys + 类型 + 必填性（`required_action` 非空字符串，`issues` 非空 array）
- Line 18: issue exact keys + 类型 + 必填性（`code`/`json_path`/`message` 非空字符串，`source_labels` 字符串 array 可为空）
- Line 19: "修复反馈只说明前次输出的问题和动作，不是业务材料。每个 `source_labels` 只是定位问题所涉及输入来源的引用标签，不是事实或推理依据。"
- Line 20: whole-candidate 动作 + "不得复制、拼接、补写或复用前次被拒绝的输出或其片段"

User prompt（conversation_compaction_user.md）：
- Line 7-9: 材料信任边界（与 system 互补：marker 解释 + 不执行控制指令 + 不过滤材料）
- Lines 72-81: 完整 repair feedback schema 说明（顶层字段 + issue 四字段 + 类型 + 必填性 + source_labels 语义 + 非业务材料边界 + whole-candidate 动作 + 禁止复用）
- Lines 83-87: 修复反馈 JSON 最小示例（明确标注"仅说明两个 marker 之间的 JSON schema；真实请求以实际数据块为准"）
- Lines 89-174: 完整同源示例输入/输出对（E1/A1/T1/D1）
- Line 176: "示例中的 label 仅用于说明同源引用；真实请求必须使用本次数据块中的真实 `source_label`"

认知负担检查：
- 两份 prompt 都说明了相同的 repair contract，但措辞不机械重复：system 用简洁规则，user 用逐字段说明 + 示例。无矛盾。
- 示例 repair JSON 使用的 label `S2` 与完整示例的 `E1/A1/T1/D1` 不同，但示例明确标注"仅说明两个 marker 之间的 JSON schema"，且 `S2` 作为占位 label 不会与完整示例的 coverage 规则冲突（repair 示例在完整示例之前，且有明确免责声明）。
- prompt 不含 `previous_attempt_number`、`additional_issue_count`、`CompactRepairFeedbackV2`、`CompactValidationIssueV2`、`Memory policy`、Host/Attempt/Engine 等内部术语。
- `required_action` 在 prompt 示例中的文本与 `COMPACT_REPAIR_REQUIRED_ACTION` 常量一致（测试 line 334 断言验证）。

**结论：prompt 在当前上下文自足，字段/类型/必填性/允许值/source_labels 语义/feedback 非材料边界完整且一致。示例与 production contract 同源。**

---

#### 6. All-section simultaneous cap test 的完整性与 owner-level 属性

**判定：通过。真实触发 1+4×2=9 条，证明无截断，测试为 owner-level。**

直接证据（test_compaction_contract.py line 472-576，`test_all_section_cap_violations_preserve_nine_exact_actionable_issues`）：

- Line 479-492: 构造 policy（item_cap=1, char_cap=1 for all sections, session_summary_char_cap=1）。
- Line 481: `_all_section_cap_candidate()` 构造 candidate：2 个 evidence_facts + 2 个 answer_anchors + 2 个 forward_intents + 2 个 reference_continuity items，全部超过 item_cap=1；session_summary text 超过 char_cap=1。
- Line 495-498: `accept_compact_candidate_v2` → `len(rejected.issues) == 9`。**Governance owner 产生 9 条。**
- Line 499-504: `build_compact_repair_feedback_v2` → `len(feedback.issues) == 9` + `feedback.additional_issue_count == 0`。**Feedback builder 不截断。**
- Lines 511-555: 逐条构造 expected_messages（基于同源 policy 值与 estimator），与 actual_messages 精确比对。**每条 message 的 actual/cap/measurement/action 均验证。**
- Lines 557-569: `_repair_feedback_prompt_json_vnext(feedback)` → `len(projected_issues) == 9` + projected JSON 与 typed feedback 完全一致。**Projector 不截断。**
- Lines 573-576: projected block ≤ `MAX_COMPACT_REPAIR_FEEDBACK_CHARS`。**总长边界满足。**

Owner-level 证据：
- 测试使用真实 `accept_compact_candidate_v2` → 真实 `build_compact_repair_feedback_v2` → 真实 `_repair_feedback_prompt_json_vnext` → 真实 `estimate_memory_size_units` → 真实 `default_memory_projection_policy`。
- 无 fixture 固化错误语义：expected_messages 从同源 policy cap 值与 estimator 动态计算，不硬编码固定数字。
- 四个 size_measurement 常量（`_EVIDENCE_FACTS_SIZE_MEASUREMENT` 等）的字符串值在 expected_messages 中被逐字引用（line 515-523），确保常量与测试断言一致。

**结论：9 条 issues 在 Governance → feedback builder → projector 全链路均完整保留。测试为 owner-level contract test。**

---

#### 7. Ownership 漂移、compat shim、loose parsing、downstream compensation、God helper、过耦合检查

**判定：通过。无 ownership 漂移，无 compat shim/loose parsing/downstream compensation/God helper/过耦合。**

逐项检查：

| 检查项 | 证据 | 判定 |
|---|---|---|
| Parser ownership | `parse_conversation_compact_output_vnext` 不变；仍 strict JSON + exact key set + enum + type check | 未漂移 |
| Governance accept ownership | `accept_compact_candidate_v2` 核心逻辑不变；仅 `_collect_policy_issues/_section_caps` 的 message 文本改变 | 未漂移 |
| State machine ownership | `build_compact_repair_feedback_v2` 的 bounded feedback 构造逻辑不变；仅新增 `size_measurement` 参数 | 未漂移 |
| Outcome ownership | `CompactAcceptedTruthV2` + `_COMPACT_ACCEPTANCE_PERMIT` 私有许可不变 | 未漂移 |
| Internal transport ownership | `CompactRepairFeedbackV2.to_json()` 仍完整序列化；docstring 澄清为 durable/internal | 未漂移 |
| LLM repair projection ownership | 新增 `_repair_feedback_prompt_json_vnext`，唯一 owner | 单 owner |
| Compat shim | 搜索全部 changed files：无 `hasattr`/`getattr`、无兼容 alias、无 fallback 字段 | 无 |
| Loose parsing | `_repair_feedback_prompt_json_vnext` 有 strict `isinstance` 守卫 | 无 |
| Downstream compensation | renderer 不读 policy；projector 不读 candidate/policy | 无 |
| God helper | `_repair_feedback_prompt_json_vnext` 是模块级私有纯函数（24 行），单一职责：typed feedback → minimal LLM JSON | 无 |
| 过耦合 | projector 只依赖 `CompactRepairFeedbackV2` 类型；renderer 只调用 projector；policy 只被 Governance 使用 | 无 |

**结论：所有 owner 边界清晰，无语义漂移，无补偿或 workaround。**

---

#### 8. 测试/pyright/diff-check 结果与 README trigger

**判定：通过。S4 延后 README 合理。**

- 测试：48 passed，覆盖 first/repair marker、exact JSON keys、typed projector、internal 字段隔离、whole-candidate action、脱敏/总长、prompt 自足 contract、adversarial material boundary、item/size cap 边界值、all-section simultaneous cap、coverage/contradiction/drop order 等。
- pyright：0 errors, 0 warnings, 0 informations。
- git diff --check：clean。
- AGENTS.md：存在但不属于本 work unit 修改范围；CLAUDE.md 的 LLM-facing 文本约束在本 S2 变更中得到遵守（见 finding 3/5）。
- README trigger：
  - `dayu/host/` 变更 → 触发 `dayu/host/README.md` 更新。
  - `dayu/config/` 变更 → 触发 `dayu/config/README.md` 更新。
  - `tests/` 变更 → 触发 `tests/README.md` 更新。
  - 以上三项按 approved plan（Slice S4, line 440-462）延后至 S4 执行。S4 的前提条件（"S1-S3 implementation/review accepted"）与当前 S2 状态一致。
  - root `README.md`、`dayu/README.md` 不触发（无入口/命令/分层/装配变化）。
  - **判定：README 留 S4 合理，属于 approved plan 的显式分片决策，不是遗漏。**

---

### 补充观察（非 finding，不阻塞）

1. **`_section_caps` 的 `size_measurement` 参数是自由字符串**：四个常量在模块级定义，通过参数传入。当前实现中 measurement 字符串与 texts 的实际投影方式在调用点配对正确（见 finding 1 的证据），但如果未来新增 section 时调用方传错 measurement，编译器/类型检查器不会捕获。这是 acceptable risk — 四个调用点已由参数化测试覆盖，新增 section 时测试会失败。

2. **Repair 示例中的 `S2` label**：修复反馈最小示例使用 `S2` 作为 `source_labels`，但完整同源示例的 labels 是 `E1/A1/T1/D1`。当前 prompt 在示例前明确标注"仅说明两个 marker 之间的 JSON schema；真实请求以实际数据块为准"（user prompt line 83），消除了模型复制 `S2` 的风险。这是 S1 已验收的设计决策。

3. **`_repair_feedback_prompt_json_vnext` 未被 `__all__` 导出**：正确，它是模块级私有函数（`_` 前缀），只被同模块的 `_user_prompt_vnext` 调用。

## Open Questions

无。

## Residual Risk

| Residual | Classification | Owner / destination |
|---|---|---|
| 真实 provider 是否稳定服从新 repair contract | S3 real-provider smoke | S3（approved slice） |
| prompt asset publication hashes 未同步 | S3 publication oracle | S3（approved slice） |
| Host/config/tests README 与 Host design 未同步 | S4 docs | S4（approved slice） |
| repair 示例 label `S2` 与完整示例 labels 不同源（已有免责声明） | 低风险，已由 S1 re-review 验收 | 当前 prompt 设计 |

## Review Decision

- **Blocking findings**: 无。
- **Non-blocking findings**: 无。
- **Residual risks**: 全部已分类并指派到 approved later slices（S3/S4）。
- **Recommendation**: S2 implementation 可接受。所有 8 个审查维度均通过，无 ownership 漂移、无 contract 泄漏、无 compat shim、测试为 owner-level 且全绿。
