# Code Review

## Scope

- Mode: current changes
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: HEAD (uncommitted workspace changes only)
- Output file: `docs/reviews/code-review-20260618-184822.md`
- Included scope: `dayu/host/compaction.py`, `dayu/host/compact_material.py`, `tests/host/test_compact_material.py`, `tests/host/test_llm_compaction.py` (workspace diff vs HEAD)
- Excluded scope: committed branch history; `dayu/host/run_input.py` (Slice 2); `docs/host/design.md` / `docs/engine/design.md` (design truth, read-only); `docs/host/issues-implementation-control.md` (control doc, read-only); `docs/reviews/wu-cm-12-fix-r1-s1-implementation-codex-20260618.md` (implementation report, read-only reference)
- Parallel review coverage: 无

## Findings

### 1-未修复-中-slide-1-移除默认-chunk-导致未纳入-scope-的测试回归失败
- **入口/函数**: `tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text`
- **文件(行号)**: `tests/host/test_compaction_operation.py:1721`
- **输入场景**: 写入 9000 字符的 `TOOL_RESULT_ACCEPTED` evidence，通过 `collect_selected_compaction_request_evidence_inputs` 读取后经 `build_initial_material_pack` 构造 pack，断言 evidence label 为 chunk 形式 `("E1.1", "E1.2", "E1.3")`
- **实际分支**: `_evidence_blocks`（`dayu/host/compact_material.py:1310`）不再调用已删除的 `_evidence_chunks`，每个 `InitialEvidenceMaterial` 只产生一个 `CompactEvidenceBlock`，label 为 `E1`
- **预期行为**: 测试已迁移到新的 no-chunk 语义，断言 label 为 `("E1",)`，或测试被标记为 Slice scope 内需更新项
- **实际行为**: `assert ('E1',) == ('E1.1', 'E1.2', 'E1.3')` — 测试失败，1 failed
- **直接证据**:
  - `dayu/host/compact_material.py:1310-1332` 中 `_evidence_blocks` 对每个 `InitialEvidenceMaterial` 生成一个 `CompactEvidenceBlock`，单一 label `E1`
  - `tests/host/test_compaction_operation.py:1721` 断言期望 `("E1.1", "E1.2", "E1.3")`
  - 实际运行时证据：
    ```
    assert ('E1',) == ('E1.1', 'E1.2', 'E1.3')
    At index 0 diff: 'E1' != 'E1.1'
    ```
  - `pytest tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text -q` → 1 failed
  - 验证命令：`pytest tests/host/test_compact_material.py tests/host/test_llm_compaction.py tests/host/test_compaction_operation.py tests/host/test_run_input_builder.py -q` → 199 passed, 1 failed
- **影响**: 未纳入 Slice 1 allowed files 的既有测试回归失败；若该测试在 CI 中运行，会阻塞后续 gate；语义上测试仍验证"同一 durable request 的 evidence 共享 query_text"这一正确行为，仅 label 断言需迁移
- **建议改法和验证点**:
  1. 将 `tests/host/test_compaction_operation.py` 纳入本 Slice 更新范围，或将测试迁移到 Slice 3（focused regression）时处理
  2. 将 `test_evidence_chunks_share_same_durable_query_text` 的 label 断言从 `("E1.1", "E1.2", "E1.3")` 改为 `("E1",)`，保留 query_text 唯一性断言
  3. 如果延迟到 Slice 3，在当前 implementation report 中记录此 residual regression
- **修复风险（低）**: 仅改测试断言，不涉及生产代码；test 的 query_text 去重逻辑（`len({query_text for _label, query_text in query_texts}) == 1`）无需改动
- **严重程度（中）**: 生产代码行为正确，但测试回归未被发现会导致后续 gate 阻塞；不阻塞 Slice 1 核心目标但必须在 closeout 前修复

## Open Questions

- `_provenance_from_evidence_blocks`（`dayu/host/compact_material.py:2862`）保留了 `evidence_blocks` 参数但立即 `del`，是否为有意保留签名兼容性？当前无 caller 依赖该参数语义（唯一的 caller 在 `build_compact_material_pack:1036`，其构造的 `evidence_blocks` 仅用于 provenance 调用和 pack 构造，无外部依赖）。非阻塞问题，但后续 WU 清理时可考虑移除该无用参数。

## Residual Risk

- 单条 accepted evidence 不再默认拆分后，极长 evidence（如 >100KB）可能让 compactor input 超过上下文硬预算。这由 Context Governance selection / fallback / fail-closed 处理，不在 Slice 1 DTO 字段 cap 中处理。已由 plan 记录为 known risk，deferred to WU-CM-13。
- `PromptLocalProvenanceEntry.chunk_parent_label` 和 `chunk_ordinal` 字段保留默认 `None` 但不再有生产代码设置非 None 值。当前不影响正确性，但属于 dead data field。不在 Slice 1 scope。
- Slice 2 的 `_ACCEPTED_TOOL_EVIDENCE_MATERIAL_LIMIT` 仍未处理；`dayu/host/run_input.py` 中该常量仍为 8。

## Conclusion

**pass-with-findings**

Slice 1 核心改动正确：DTO 私有字段长度 guard 已移除（`CurrentInputAnchorVNext.text` 和 `EvidenceReadableItemVNext.response_text` 改为 non-empty 校验），默认 evidence chunking 已删除（`_evidence_chunks`、`_EvidenceChunk`、`evidence_chunk_label`、`EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` 全部从 production code 移除，`CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS` 和 `EVIDENCE_BLOCK_CHUNK_VNEXT_TEXT_MAX_CHARS` 全部删除），`MAX_VNEXT_*` output parser safety guards 保留，provenance/content digest 语义完整。测试证明长 current input 和长 evidence 保持完整进入 vNext DTO。

唯一 finding 是 `tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text` 因 chunk label 断言仍为旧语义而失败（1 failed / 199 passed in broader host test sweep）。该测试不在 Slice 1 allowed files 中，需要在后续 slice 或 closeout 前迁移。

验证结果：
- `pytest tests/host/test_compact_material.py tests/host/test_llm_compaction.py -q` → 79 passed
- `pyright dayu/host/compaction.py dayu/host/compact_material.py tests/host/test_compact_material.py tests/host/test_llm_compaction.py` → 0 errors
- `git diff --check` → clean
- 全量搜索 `_evidence_chunks|_EvidenceChunk|evidence_chunk_label|EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS|CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS|EVIDENCE_BLOCK_CHUNK_VNEXT_TEXT_MAX_CHARS` 在 `dayu/` 和 `tests/`（排除 docs/）下 → `dayu/` 零结果，`tests/` 仅 `test_compaction_operation.py:1632` 的测试函数名（非符号引用）
