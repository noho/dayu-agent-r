# Code Review

## Scope

- Mode: current changes（只读 re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `7e0941828c09d890ad04e3ff8f2c1cf5e28441ca`（与 HEAD 相同；审查范围 = 全部未提交工作树变更）
- Output file: `docs/reviews/code-review-20260817-032141.md`
- Included scope: `dayu/fins/pipelines/filing_upload_publication.py`、`docling_upload_service.py`、`sec_upload_workflow.py`、`cn_pipeline.py`、`dayu/fins/README.md`、`tests/README.md`、`tests/fins/test_filing_upload_publication.py`、`test_sec_pipeline_upload_filing_stream.py`、`test_cn_pipeline.py`、`test_docling_upload_service.py`、`test_fins_ingestion_runtime.py`、accepted plan gate sync
- Excluded scope: oracle/scenario/registry/frozen evidence、Host/Engine/Service/CLI production、material workflow、`test_fins_ingestion_tools.py`/`test_fins_service_runtime.py`（本轮无 diff）
- Parallel review coverage: 无（主 reviewer 逐文件走读；本轮为第二次独立 review 的 re-review 核实）
- 约束遵守：未修改任何生产/测试/计划；未运行 UF-PF10/UF-PF12；未改 oracle/scenario/evidence；仅运行确定性 focused pytest 独立复核

## 结论

两路首轮 review 的全部 accepted findings（M-F1..6、D-F1..2、C-F1）均已按 Controller adjudication 在正确 owner boundary 修复，逐项直接证据如下；新 adversarial pass 的八个指定方向全部有 owner 级或 workflow 级确定性证据，未发现新的实质性问题。

## Accepted findings 逐项验证

### M-F1（accepted-low）— 根因关闭 ✅

- 生产证据：`filing_upload_publication.py:497` 现在只捕获 `(OSError, RuntimeFileLockError, ValueError)`，与 `BatchingRepositoryProtocol.rollback_batch` 声明的异常集（`repository_protocols.py:726-730`：`ValueError`/`OSError`/`RuntimeFileLockError`）精确一致；`KeyboardInterrupt`/`SystemExit` 不再被重写为 `STORAGE_IO`，docstring（:491-492）显式承诺原样传播。
- 测试证据：`test_cancel_rollback_signal_exception_propagates_unchanged`（test_filing_upload_publication.py:1527）参数化两类信号，断言原样抛出且 `rollback_tokens == begin_tokens`（恰好一次 rollback）。

### M-F2（accepted-blocker）— 根因关闭 ✅

- 生产证据：`_read_fresh_state_or_raise`（:556）与 `_begin_publication_batch_or_raise`（:582）两个窄边界均增加 `RuntimeError` → `fins_upload_prevalidation_io_failure()`，与协议声明（`repository_protocols.py:496` batch read、`:692` begin_batch 均可抛 `RuntimeError`）对齐；未扩大为通用 `except Exception`。
- 测试证据：`test_batch_acquire_runtime_error_maps_to_typed_storage_io`（:1566）断言 exact io reason 且 acquire 失败零 token 零 rollback；`test_batch_fresh_read_runtime_error_maps_to_typed_storage_io`（:1600）断言 exact io reason、`batch_reads == [(token, doc_id)]`、rollback == begin。

### M-F3（accepted-blocker）— 根因关闭 ✅

- 生产证据：只在 fresh validator 调用边界（`filing_upload_publication.py:664-666`）把 `ValueError` 映射为 `fins_upload_prevalidation_corruption_failure()`；arbitration（:270/:285/:345 等）与 rebase（:726）的 programming invariant `ValueError` 继续按 §6.4 走既有 unexpected 契约，未做全函数兜底。
- 测试证据：`test_fresh_validator_value_error_maps_to_typed_corruption`（:1654）用 corrupt ticker state 触发 validator `ValueError`，断言 exact corruption reason 且 rollback == begin。

### C-F1（accepted-blocker，Controller 补充）— 根因关闭 ✅

- 生产证据：两个取消 checkpoint 均返回 `authoritative_request=request`（initial，`filing_upload_publication.py:637-642` 与 `:685-690`）；SKIP（:709）与 PUBLISH（:751）outcome 携带 fresh request；docstring（:607-609）显式记录该选择。
- 测试证据：`test_second_checkpoint_changed_observation_keeps_initial_request_projection`（:1478）构造 `MISSING -> COMPLETE`（initial `create` / fresh `update`），精确断言 cancelled outcome `authoritative_request is initial` 且 `resolved_action == "create"`；`test_second_checkpoint_cancels_closed_candidate_before_any_publication_mutation`（:1380）三分支断言 `observations == 2`（恰好两个 checkpoint）、`authoritative_request is initial`、commit 为空、rollback == begin、零 company/source stage。

### M-F4（accepted-low）— 根因关闭 ✅

- 测试证据：conflict grid（test_filing_upload_publication.py:1090-1140）现为 7 行并新增 `expected_failure_code` 参数列；`REPAIR_REQUIRED -> MISSING` 与 `REPAIR_REQUIRED -> COMPLETE` 两行显式存在，均断言 `SOURCE_REVISION_STALE`。

### M-F5 / M-F6（accepted-blocker）— 根因关闭 ✅

- 测试证据：新 runtime 竞争用例不再调用 `_wait_terminal` 或任何 sleep/polling；`_run_two_held_operations_concurrently`（test_fins_ingestion_runtime.py:3762-3788）收集恰好两条 held operation，在 2 个真实线程执行并以 `future.result(timeout=20)` 有界等待完成，job record 在完成通知后各读取一次（真实 happens-before）。`_wait_terminal` 仅剩 baseline 既有用例使用（HEAD 中 16 处，非本轮新增），不在 adjudication 修复边界内。
- README 证据：tests/README.md:435 新段落把"无 sleep/polling"精确限定为 Filing shared publication matrix 的"held runtime operation 有界 future 完成通知"，声明与修复后的实现事实一致，未降级文档容纳违规测试。
- 独立复核：`test_durable_runtime_concurrent_identical_upload_persists_ok_and_skip_terminals`、`test_durable_runtime_concurrent_explicit_create_persists_exact_conflict_terminal` 均 PASSED。

### D-F1（accepted-blocker）— 根因关闭 ✅

- 测试证据：`_BatchReadBarrierFilingUploadStateRepository`（test_sec_pipeline_upload_filing_stream.py:223-280）继承真实 `FsFilingUploadStateRepository`，barrier 位于 `read_filing_upload_state_in_batch` 内——即 begin_batch 成功取得各自 writer 之后、batch 段之内；`test_concurrent_distinct_targets_preserve_exact_union` 的 different-ticker 分支（:2407-2412、:2462-2464）为 AAPL/MSFT 各配 `Event`，断言两事件均 set 且两者最终均 `ok`（2 commits）。若存在全局锁导致串行，第二方无法到达 barrier → `BrokenBarrierError`（RuntimeError 子类）→ 被 `_read_fresh_state_or_raise` 投影为 typed failure → status `failed` → `["ok", "ok"]` 断言响亮失败。无生产 hook、无 sleep、无时间窗口断言。
- 独立复核：该测试两分支均 PASSED。

### D-F2(a)（accepted）— 根因关闭 ✅

- 测试证据：`test_durable_runtime_concurrent_explicit_create_persists_exact_conflict_terminal`（test_fins_ingestion_runtime.py:8652-8723）断言一条 `SUCCEEDED` 一条 `FAILED`；loser 的 `failure_summary == fins_upload_source_publication_conflict_failure().to_json()`（exact code/message）、`result_summary["failure"]` 同 JSON、`request_summary["action"] == "create"`、`stored_file_count == 0`；winner `ok`、stored 1。独立复核 PASSED。

### D-F2(b)（accepted-low）— 根因关闭 ✅

- 测试证据：`test_concurrent_explicit_create_obeys_overwrite_rebase_contract` overwrite=False 分支（test_sec_pipeline_upload_filing_stream.py:2317-2327）精确断言 failure == canonical factory JSON、`requested_action/resolved_action/filing_action == "create"`、stored 0、`status != "skipped"`、`failure["code"] != "unexpected_runtime"`。

### D-F2(c)（accepted）— 根因关闭 ✅

- 测试证据：`test_concurrent_distinct_targets_preserve_exact_union` same_ticker 分支（:2425/:2435、:2457-2461）两请求分别携带 `("MSFT",)` 与 `("GOOG",)`，最终从 canonical company meta 单一真源断言 `accepted_aliases` 精确等于 `{MSFT, GOOG}`（len 2），无 lost update。

## 新 adversarial pass

### 1. typed failure 不落 generic — ✅

workflow 捕获顺序为 `FinsUploadFailureError` → `OSError` → `Exception`（sec_upload_workflow.py:306-331、cn_pipeline.py:938-960）；owner 的全部存储/冲突/unsafe/corruption 失败均为 `FinsUploadFailureError`，先于 generic handler 被 typed handler 消费并按 initial request 投影 failed terminal。非 state-dependent usage（§6.4 设计允许）与 arbitration/rebase invariant 经 `finally` 单次 rollback 后落入 generic `UNEXPECTED_RUNTIME`，与冻结契约一致，无 drift。

### 2. rollback 信号 — ✅

M-F1 修复后 catch 集与协议声明精确一致；`KeyboardInterrupt`/`SystemExit` owner 直测覆盖传播与单次 rollback。

### 3. cancel action 时序 — ✅

checkpoint1 在 `begin_batch` 后、fresh read 前（`filing_upload_publication.py:631`），owner 测试证明命中时 `batch_reads == []`；checkpoint2 在 closed arbitration / validation 后、任何 mutation 或 SKIP/CONFLICT terminal 前（:679），三分支测试证明零 commit、零 stage、单次 rollback；`observations == 2` 证明恰好两个 checkpoint。取消 outcome 一律携带 initial request，changed-observation 用例证明 cancelled action 不再随竞争时机漂移。

### 4. runtime held executor 真实完成通知 — ✅

两条 runtime operation 在真实线程中执行，`future.result(timeout=20)` 建立真实 happens-before，job record 在完成通知后读取一次；无 sleep/retry/polling。

### 5. different-ticker batch 内并行证据 — ✅

见 D-F1：barrier 位于真实仓储的 writer-owned fresh read 内（begin_batch 之后），Event 证明双方同时进入 batch 段，串行化假设会使测试确定性失败。

### 6. same-ticker alias union — ✅

见 D-F2(c)：并发 alias 并集从 canonical company meta 单一真源精确断言，无 consumer 从 raw fields 重算。

### 7. durable conflict terminal — ✅

见 D-F2(a)：durable failure summary 与 result failure 与 canonical factory JSON 精确相等，action/stored 完整断言，单 failed 单 succeeded。

### 8. 单一终态 — ✅

生产：`batch_terminal_started` 标志在每条 terminal 路径（cancel/skip/conflict/commit 转交）前置位，`finally`（`filing_upload_publication.py:755-761`）仅在其为 False 时以 `sys.exception()` 为 primary 执行唯一一次 rollback；所有正常返回路径均已置位，不存在双重 rollback 或"返回成功但状态半提交"。workflow 每条 stream 只 yield 一个 `UPLOAD_COMPLETED` 或一个 failure event。测试：cancel/skip/rollback-failure 用例均断言 `rollback_tokens == begin_tokens` 且 commit 为空；runtime 用例每 job 恰好一条终态 record。

## Findings

未发现实质性问题。

## Open Questions

- 无。首轮 DS open question（cancelled outcome 的 authoritative_request 真源）已由 Controller 升格为 C-F1 并关闭；DS Q2（batch read `ValueError` 归属）已被 Controller 明确 rejected-with-reason，本轮未重新计为 finding。

## Residual Risk

- 独立复核只运行了确定性 focused pytest（publication owner 27 passed；SEC 并发/取消 13 passed；CN/HK 并发 2 passed；runtime durable 并发 2 passed），未独立重跑完整 `pytest tests/fins -q` 与全仓 pyright；fix artifact 声称的 `1916 passed, 1 skipped`、coverage 与 pyright 0 error 引用自其证据，未在本 review 中独立复跑。
- SEC 级 `test_shared_publication_cancellation_checkpoints_rollback_without_mutation` 的 `cancel_at_observation=[5,6]` 与 prepare 路径取消观察次数耦合（首轮 DS review 已分类的 residual）；owner 级 `observations == 2` 与 checkpoint 语义精确断言已固定权威语义，该耦合影响有限。
- 第二 checkpoint 在 validation-failure 路径上也先观察 cancel（取消优先于 typed failure）。§7.1 step 7 文字只写"closed arbitration 后"，实现选择与 §3.1/§6.4 的"等待 writer 期间取消不得投影业务结局"意图一致，且无 mutation/durable 副作用；建议 controller 在 acceptance 时确认该读取为冻结意图。
- `_begin_publication_batch_or_raise` 未捕获协议声明的 `ValueError`（ticker 单路径组件契约）：输入为 validated `normalized_ticker.canonical`，该分支不可达，且与既有 workflow 直接调用 `begin_batch` 的同类暴露一致；与 DS Q2 的 rejected-with-reason 裁决同源，未计为 finding。
- `_wait_terminal` 仍被 baseline 既有非并发 durable terminal 测试使用（HEAD 已有 16 处），与 tests/README 的"并发断言不使用 sleep/polling"表述不冲突；若未来 work unit 要求全仓消除 polling，属该 fixture owner 的独立范围。

## Review Methodology

本 re-review 从 Controller adjudication 的九项 accepted findings 出发：先读两路首轮 review、adjudication 与 fix artifact，再逐行走读当前工作树的四个生产文件 diff 与五个测试文件 diff，对每项 finding 在 production + owner test 两层核对直接证据链，最后按八个指定方向做新 adversarial pass。关键证据全部引用当前文件行号；focused 测试结果为本 reviewer 独立运行，非引用 fix artifact。
