# PR Review

## Scope

- Mode: PR
- Repository: `noho/dayu-agent-r`
- PR: 150
- URL: https://github.com/noho/dayu-agent-r/pull/150
- Title: WU-CM-12 Conversation Memory drift repair
- State: OPEN
- Base: `main`
- Head: `wu-cm-12-conversation-memory-drift`
- Review focus: current work unit additions — Conversation Memory smoke/log diagnostics
- Work unit commits: `60d895ee`, `69d47ecd`, `5714358f`, `9413ca1c`
- Key files reviewed:
  - `utils/smoke_host_public_conversation_memory_scenarios.py`
  - `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`
  - `README.md`
  - `tests/README.md`
  - `docs/host/conversation-memory-smoke-compact-followup.md`
  - `docs/reviews/pr-150-review-20260619-122248.md` (prior gate review)
- Excluded scope: full historical WU-CM-12 PR contents before this work unit
- Parallel review coverage: 无

## Constraint Verification

### 1. CONTEXT_COMPACTION_FAILED 仍 hard fail

**确认：是。**

`_assert_compact_acceptance()` (line 3243-3244) 中：

```python
if failed_total > 0:
    raise RuntimeError("memory-compact observed CONTEXT_COMPACTION_FAILED")
```

任何 `CONTEXT_COMPACTION_FAILED`（proactive 或 reactive）都会导致 `RuntimeError`，即使 fallback dispatch 让当前 Run 继续执行。测试 `test_compact_acceptance_requires_event_log_audit_summary` (line 441) 验证了此行为。

### 2. README 不声称完整 memory correctness eval 已实现

**确认：不声称。**

`README.md` 5.3 节明确写道：

> 当前 smoke 不覆盖 conversation memory correctness 的全量验证。

`tests/README.md` assembly helpers 描述准确列举了新覆盖的诊断点（compact operation timeline / rejected histogram / manifest missing stage / CONTEXT_COMPACTION_FAILED hard fail），没有声称覆盖 memory correctness eval。

### 3. PR gate artifact 足够性

**确认：足够。**

- 16 tests passed, 0 pyright errors
- 先前已有独立 code review、re-review 和 aggregate deepreview（`docs/reviews/code-review-20260619-121033.md`、`code-review-20260619-121538.md`、`wu-cm-conversation-memory-smoke-log-diagnostics-aggregate-deepreview-20260619-121709.md`）
- `docs/host/conversation-memory-smoke-compact-followup.md` 已更新实施状态节，记录本 work unit 的边界和未修复项
- GitHub checks 未报告（branch 无 CI），已在前次 review artifact 中记录为 residual risk

## Findings

### 1-未修复-低-`_compact_pressure_reserve_tokens` 条件分支无实际效果

- **入口/函数**: `_compact_pressure_reserve_tokens`
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py` (2863-2865)
- **输入场景**: 任何调用 `_compact_pressure_padding` 的路径
- **实际分支**: `context_window_size >= _COMPACT_PRESSURE_LARGE_WINDOW_TOKENS` 为 `True` 或 `False`
- **预期行为**: 不同 context window size 应返回不同的 reserve tokens
- **实际行为**: `if` 分支和 `else` 分支均返回相同的 `_COMPACT_PRESSURE_RESERVE_TOKENS`（160,000），条件判断无实际效果
- **直接证据**: line 2863-2865:

  ```python
  if context_window_size >= _COMPACT_PRESSURE_LARGE_WINDOW_TOKENS:
      return _COMPACT_PRESSURE_RESERVE_TOKENS
  return _COMPACT_PRESSURE_RESERVE_TOKENS
  ```

- **不影响 correctness**: 当前所有 context window 均返回相同值，行为一致
- **影响**: 死代码；后续维护者可能误以为两个分支有不同语义
- **建议改法和验证点**: 若当前确实只支持一种 reserve 策略，删除 `if` 分支，直接返回常量；若后续需要区分大小窗口，补上不同的返回值。运行 `pytest tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py` 验证。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

**注意**: 此问题在本 work unit 之前已存在（非本次引入），但作为本次 review scope 内代码的可维护性问题报告。

## Open Questions

无。

## Residual Risk

- GitHub checks 未在 `wu-cm-12-conversation-memory-drift` branch 上报告，CI 状态不可用作额外证据。
- PR 150 是大型 existing WU-CM-12 PR，本 review artifact 仅重新验证当前 smoke/log diagnostics work unit。
- Production memory compact failure（`docs/host/conversation-memory-smoke-compact-followup.md` 第 3 个问题）仍不属于本 work unit 范围。
- `_compact_pressure_reserve_tokens` 的死代码表明 smoke 压力注入对小 context window 的适配可能需要后续确认。

## Conclusion

PR review gate status: **pass** for the current work unit additions on PR 150.

三个用户指定约束全部确认通过：CONTEXT_COMPACTION_FAILED 仍 hard fail；README 不声称完整 memory correctness eval；PR gate artifact 足够。唯一 finding 是 `_compact_pressure_reserve_tokens` 的死代码（低严重程度，pre-existing，不影响 correctness）。
