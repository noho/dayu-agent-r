# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `69bc9d2af91788303c839d01ad937cf9b802eb1d`（UF-FIX01 plan factual-correction checkpoint）
- Implementation commit: `3caca6fa0091a738c5c78cc5165a49fed82c6458`
- Fix commit（本路 re-review target HEAD）: `5a6d80c2b0e87c5810a3bbedb1ab74524a205adc`
- Fix baseline（adjudication checkpoint）: `54c867f8224290659e01ac454caf463349b20c67`
- Output file: `docs/reviews/code-review-20260813-124100.md`
- Included scope: fix commit 19 个文件（`dayu/cli/commands/fins.py`、`dayu/fins/service_runtime.py`、`dayu/fins/upload_failure.py`、`dayu/fins/pipelines/{sec_pipeline,sec_upload_workflow,cn_pipeline,docling_upload_service,upload_company_meta}.py`、`dayu/fins/storage/*`、对应 tests、README/artifact 更新）；另对未变动的关键链路（`dayu/service/fins_direct.py`、`dayu/fins/ingestion_runtime.py` 的 validated 透传与 direct producer、`docling_process_converter.py` 异常层级）做真源走读
- Excluded scope: 未修改的 Host/Engine、frozen evidence、oracle/registry；`docling_process_converter.py`（确认 fix commit 零 diff，仅读异常类层级）
- Parallel review coverage: 无（本路禁止子 agent，全部结论为主 reviewer 独立逐行走读）
- 独立裁决声明：本路独立核销 A1–A5/C1–C3，未参考任何 re-review 结论。
- Verification: 本路未运行测试与 pyright（会话中被指示停止执行命令）；测试执行证据依据 fix artifact 记录（`591 passed, 1 skipped`；pyright `0 errors`）与已直接阅读的测试断言主体。

## Findings

### 1-未修复-低-`_prevalidate_upload_filing_request` docstring 声明的异常契约已过时

- **入口/函数**: `_prevalidate_upload_filing_request`（CLI 层）
- **文件(行号)**: `dayu/cli/commands/fins.py:677-680`（`:raises OSError: published state 读取失败时抛出。ValueError: published storage state 损坏时抛出。`）
- **输入场景**: 静态阅读即可见。
- **实际分支**: fix 后 `prevalidate_fins_upload_filing_request_for_workspace`（`service_runtime.py:85-90`）把 storage 读的 `OSError/RuntimeFileLockError` 与 `ValueError` 全部包装为 `FinsUploadPrevalidationError`；CLI 不再向上层透传裸 `OSError/ValueError`。
- **预期行为**: docstring 与实际异常契约一致（CLAUDE.md 编码硬约束）。
- **实际行为**: 声明仍写 OSError/ValueError 透传，与实现漂移。
- **直接证据**: `service_runtime.py:87-90` 两个 `except ... raise FinsUploadPrevalidationError(...) from exc`；`fins.py:678-680` 旧 raises 未同步。
- **影响**: 仅维护性；与 A5 同类别（A5 已修复 `_upload_filing_stream`，本函数漏改）。
- **建议改法和验证点**: 将 raises 改为 `FinsUploadUsageError` / `FinsUploadPrevalidationError` 两行，与 `service_runtime.py:75-77` 的契约一致。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-prevalidation 仓储构造期 OSError 落在 typed wrap 之外，走 CLI generic 分支

- **入口/函数**: `prevalidate_fins_upload_filing_request_for_workspace` → `FsFilingUploadStateRepository.__init__` → `build_fs_repository_set` → `_FsStorageInfra.__init__`
- **文件(行号)**: `dayu/fins/service_runtime.py:81-84`（构造在 try 外）；`dayu/fins/storage/_fs_storage_infra.py:436-446`；`dayu/cli/commands/fins.py:217-219`（generic 分支）
- **输入场景**: `workspace_root.resolve()` 失败（symlink loop、根路径不可解析等极窄场景）。
- **实际分支**: 构造期（`create_directories=False`，无 mkdir、无 identity 读、无 batch recovery）唯一 eager 失败点是 `resolve()`；其 OSError 经 `_project_filesystem_error`（`_fs_storage_utils.py:474-496`）投影为 path-free OSError，但**不是** `FinsUploadPrevalidationError`，落入 CLI generic `except Exception` → `str(exc)`。
- **预期行为**: A4 要求 service factory 前的 storage OSError 均经 typed bounded path-free owner 投影并 exit 1。
- **实际行为**: exit 1 正确、文案 path-free（storage owner 已投影），但未走 `FinsUploadPrevalidationError` 固定文案，且 generic 分支不做 operator log（`fins.py:217-219` 无 `_LOGGER.exception`）。
- **直接证据**: `service_runtime.py:85` try 只包住 `read_filing_upload_state`；`_fs_storage_infra.py:437-439` resolve 的 except 在构造期即可触发。
- **影响**: 公开契约的“typed bounded”在该角落为“path-free but untyped”；不泄漏路径、不影响 exit code。
- **建议改法和验证点**: 把 `FsFilingUploadStateRepository(...)` 构造移入 try 并按 io/corruption 分类，或由 storage owner 在构造期直接抛 path-free typed 错误；补一个 resolve 失败的 CLI 级断言。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## 裁决项核销（A1–A5 / C1–C3）

| 项 | 状态 | 独立核销证据 |
| --- | --- | --- |
| A1 authoritative handoff | **CLOSED** | runner 不再还原散参：`service_runtime.py:177-190` 直接把 `ValidatedFinsUploadFilingRequest` 交给 `sec_pipeline.upload_filing(request)` / `cn_pipeline.upload_filing(request)`；facade 签名改收 typed request（`sec_pipeline.py:742-763`、`cn_pipeline.py:737-757`），旧标量参数（含 company_id/company_name 兼容字段）已删除；workflow 在 prepare 前用注入的同一 repository 读 fresh snapshot（`sec_upload_workflow.py:167-170`、`cn_pipeline.py:786-789`），对 `preflight.request` 调同一 `validate_fins_upload_filing_request`（同 validator，`ingestion_runtime.py:942`），`_assert_authoritative_filing_identity` 比对 canonical ticker/document_id/internal_document_id 不一致即 `RuntimeError` fail closed（`sec_upload_workflow.py:381-405`、`cn_pipeline.py:1796-1820`）；action/source_meta/company_meta_decision/document ids 只从 `authoritative_request` 取（`sec_upload_workflow.py:180-190`、`cn_pipeline.py:797-806`）；`prepare_upload(previous_meta=...)` 不再自行读 state（`docling_upload_service.py:226-287`，`_safe_get_document_meta` 调用已删除）；filing 链用 `stage_upload_company_meta_decision`（`sec_upload_workflow.py:235`、`cn_pipeline.py:855`），`upsert_company_meta_for_upload` 生产调用方仅剩 material 路径（`sec_upload_workflow.py:530`、`cn_pipeline.py:1137`），无兼容 wrapper。同一 repository 实例由 `DefaultFinsRuntime` 注入两个 pipeline（`service_runtime.py:552/563`）。 |
| A2 conflict 拒绝与旧派生值丢弃 | **CLOSED** | validator 以 closed usage code 拒绝前置冲突（`ingestion_runtime.py:969-987`：`CREATE_TARGET_EXISTS`/`UPDATE_TARGET_MISSING`/`COMPANY_NAME_REQUIRED`），无字符串分类；workflow fresh recheck 只消费 fresh state 派生的 `resolved_action`/`company_meta_decision`，preflight 旧值不进入任何 prepare/stage 路径（`sec_upload_workflow.py:180-190`、`cn_pipeline.py:797-806`）；仍可能发生的 publication race 落入 `except OSError` → typed `STORAGE_IO` 投影（batch/storage owner fail closed，符合裁决）；`stage_upload_company_meta_decision` 对非 stage disposition 不写（`upload_company_meta.py:108-109`）。 |
| A3 S3 原子性 owner tests | **CLOSED** | SEC 新增 `test_upload_filing_stage_failure_rolls_back_one_batch_and_preserves_published_tree`、`test_upload_filing_fresh_recheck_discards_stale_action_and_company_decision`、`test_upload_filing_rollback_failure_logs_primary_and_recovery_evidence`、`test_upload_filing_authoritative_identity_mismatch_fails_closed`；CN 新增同名 stage-failure/fresh-recheck/identity 与 HK 用例 `test_hk_upload_filing_facade_consumes_typed_request_and_fresh_snapshot`；共享支持 `tests/fins/upload_filing_test_support.py`（268 行，batch 计数/树/SHA 夹具）。生产侧 rollback 语义有直接证据：`commit_prepared_upload_batch` finally 恰好一次回滚（`docling_upload_service.py:890-909`），`rollback_prepared_upload_batch` 失败时 `operation_error.add_note(...)` + `raise operation_error from rollback_error` 保留 primary/recovery（`docling_upload_service.py:926-943`），无补偿删除、无二次 batch。注：batch 计数/SHA 断言的逐行测试体因会话指令停止而未全文读完，执行证据依据 fix artifact。 |
| A4 prevalidation exit 1 path-free | **CLOSED（附 finding 2 角落）** | `service_runtime.py:85-90` 把 read 期 `OSError/RuntimeFileLockError`→io reason、`ValueError`→corruption reason 包装为 `FinsUploadPrevalidationError`（`from exc` 保留 operator cause）；CLI 新分支（`fins.py:208-211`）先 `_LOGGER.exception` 再渲染固定文案、exit 1，顺序在 `FinsDirectStreamProtocolError`/generic 之前且与 `FinsUploadUsageError`（exit 2）为兄弟类无遮蔽；CLI 级测试 `test_upload_filing_prevalidation_io_failure_is_typed_bounded_and_path_free`（注入含绝对路径的 `PermissionError`，断言 exit 1、exact 单行 stderr、无 tmp_path/Traceback/PermissionError）与 `test_upload_filing_prevalidation_descriptor_corruption_is_typed_and_path_free`（断言 exit 1、corruption 文案、path-free）已直接阅读。构造期 `resolve()` 角落见 finding 2。 |
| A5 docstring | **CLOSED** | `_upload_filing_stream` docstring 已改为“Service/runtime stream 打开失败时原样透传”（`fins.py:646`）。但 `_prevalidate_upload_filing_request` 的 raises 未同步（finding 1）。 |
| C1 CLI frozen matrix | **CLOSED** | `test_upload_filing_usage_matrix_precedes_service_factory_and_workspace_mutation` 参数化穿过真实 `cli_main.main`：逐 case 断言 `exit_code == EXIT_USAGE_ERROR`、`captured.out == ""`、`captured.err == "dayu-cli upload_filing: {expected_reason}\n"` exact 单行、`factory_calls == []`、`service.upload_filing_requests == []`、`service.stream_calls == []`、`not workspace_root.exists()`（fresh tree zero）；factory 仅被 monkeypatch 替换常量，production validator/storage owner 未被 fake 复制（`tests/cli/test_fins_commands.py:1050-1106`）。已直接读到 suffix 组（UF-028/030-038 共 11 case）的 exact reason；parametrize 头部其余 case 群与全部 frozen case 枚举因会话指令停止未再读，执行证据依据 fix artifact。 |
| C2 typed catch 顺序/operator cause | **CLOSED** | 层级事实已核实：`DoclingConversionError(RuntimeError)` 与 `DoclingConversionCancelledError(RuntimeError)` 为兄弟类、均非 OSError 子类（`docling_process_converter.py:176/202`）→ 顺序 cancelled → Docling → OSError → generic 无遮蔽、分类可观察；SEC/CN workflow 均按该顺序显式 catch（`sec_upload_workflow.py:286-327`、`cn_pipeline.py:906-963`），每支 `_LOGGER.exception(...)` 保留 operator cause，public 只投影 `fins_upload_failure_from_exception` 的 closed reason，禁止 `str(exc)` 参与 public 分类；marker tests 已直接读到名字（SEC `test_upload_filing_observably_classifies_cancelled_docling_storage_and_generic`、CN `test_upload_filing_storage_and_generic_failures_use_distinct_typed_catches`）。 |
| C3 material non-goal | **CLOSED** | material 保留既有 state owner 读（`sec_upload_workflow.py:498-501` `_safe_get_document_meta` → `previous_meta=` 传给 prepare）、保留 `upsert_company_meta_for_upload` 与独立 company batch（`sec_upload_workflow.py:526-544`、`cn_pipeline.py:1137`）；material 失败投影已回退为既有 `str(exc)` 用户语义（`sec_upload_workflow.py:625-633`、`cn_pipeline.py:1233-1240`）；SEC/CN 均新增 `test_upload_material_failure_preserves_existing_user_visible_semantics`。UF-FIX09：fix commit diff 不含 `docling_process_converter.py`；`tests/fins/test_fins_ingestion_runtime.py` 在 fix 中有 47 行改动，本路未读完其内容（见 Residual Risk）。 |

## Open Questions

- 无阻碍裁决的问题。以下两点留待 Controller 备忘：
- direct 路径下 workflow fresh recheck 抛出的 `FinsUploadUsageError`（preflight 后状态漂移冲突）经 `_run_direct_stream_producer` 捕获（`ingestion_runtime.py:3672-3709`），`_classify_direct_error` 归 `USER_INPUT`（`ingestion_runtime.py:6396-6397`，ValueError 子类），exit 1 且沿用同一 closed actionable 文案；preflight 同码冲突为 exit 2。裁决 A2 只要求“closed actionable reason 拒绝”，未钉死该场景 exit code；job 路径同异常投影本路未走读。
- identity mismatch 的 `RuntimeError("filing authoritative identity mismatch")` 会经 CLI generic 分支上 public stderr（exit 1、bounded、path-free，但为英文内部文案且 generic 分支无 operator log）。

## Residual Risk

- 本路未执行测试与 pyright（会话指令停止执行命令）；套件通过证据依赖 fix artifact 记录（`591 passed, 1 skipped`；pyright `0 errors, 0 warnings`）与已阅读的测试断言主体，未在本会话复核。
- C1 parametrize 头部其余 frozen case 群未逐项再读（已直接核销 suffix 组与断言主体）。
- A3 的 batch 计数/SHA 断言测试体与 `test_upload_filing_rollback_failure_logs_primary_and_recovery_evidence` 测试体未逐行读完。
- `tests/fins/test_fins_ingestion_runtime.py` 在 fix commit 的 47 行改动（UF-FIX09 composition 相关）未读。
- UF-PF01 focused-real bundle 按裁决延后，未运行。

## 结论

**PASS。** A1–A5、C1–C3 全部 CLOSED：validated object 从 CLI prevalidation 经 Service/runtime/runner 到 SEC/CN/HK facade 原样透传不拆散；workflow 以注入的同一 repository 读 fresh snapshot、调用同一 validator、identity 不一致 fail closed，且只消费 authoritative `resolved_action`/`source_meta`/`company_meta_decision`；filing 链路已删除第二 company 决策与 `prepare_upload` 第三读路径；SEC/CN/HK 共用同一 BatchToken 且 rollback 保留 primary cause；CLI frozen matrix 逐 case 断言 exit 2/factory+service zero/fresh tree zero；prevalidation operational failure 经 typed bounded path-free owner 投影 exit 1；typed catch 顺序（cancelled → Docling → OSError → generic）层级正确、可观察、operator cause 入日志；material/UF-FIX09/non-goals 未见回归。新发现 2 项均为低严重度（docstring 漂移一处、prevalidation 构造期 resolve 失败走 generic 的 path-free-but-untyped 角落），不构成本 gate 阻塞，建议并入后续任意 fix 提交。
