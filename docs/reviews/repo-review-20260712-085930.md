# Code Review

## Scope

- Mode: All Repository
- Branch: phaseflow/host-issues-control
- Base: main
- Output file: docs/reviews/repo-review-20260712-085930.md
- Included scope: `dayu/` 全部生产代码、`tests/` 关键测试、项目指令文件
- Excluded scope: `workspace/`（运行时数据）、`utils/`（辅助脚本）、`docs/`（设计文档）、`.venv/`、`__pycache__/`
- Parallel review coverage: 5 个子代理分别覆盖 Host 层、Engine 层、Fins 层、Runtime+Contracts 层、CLI+Service+Tools 层

## Findings

### 1-未修复-高-tool_runtime.py 7597 行 God File

- **入口/函数**: `dayu/host/tool_runtime.py` 全文件
- **文件(行号)**: `dayu/host/tool_runtime.py` (7597 行, 212 个 class/def)
- **输入场景**: 任何需要修改 tool policy、tool fact accept、duplicate governance、tool truncation、fetch_more、execution capsule 或 tool trace 的场景
- **实际分支**: 单文件承载 6 项截然不同的职责
- **预期行为**: 按职责拆分为独立模块（tool_policy、tool_fact_accept、tool_execution_capsule、tool_truncation 等）
- **实际行为**: 7597 行单文件，模块 docstring 承认同时承载 5 项职责
- **直接证据**: `wc -l` 返回 7597；`grep -c "^def \\|^async def \\|^class "` 返回 212
- **影响**: 违反"禁止 God object/function"编码硬约束。单文件过大使代码审查、测试隔离和并发修改极为困难
- **建议改法和验证点**: 按职责拆分为 `tool_policy.py`、`tool_fact_accept.py`、`tool_execution_capsule.py`、`tool_truncation.py`、`tool_duplicate_governance.py`（已有独立文件）等；拆分后确保 import 边界不引入循环依赖
- **修复风险（低/中/高）**: 高（涉及大量内部符号重导出和测试更新）
- **严重程度（低/中/高/严重）**: 高

### 2-未修复-中-ingestion_runtime.py 6824 行 God Module

- **入口/函数**: `dayu/fins/ingestion_runtime.py` 全文件
- **文件(行号)**: `dayu/fins/ingestion_runtime.py` (6824 行)
- **输入场景**: 修改下载/预处理/上传管线的运行时逻辑
- **实际分支**: `FinsIngestionRuntime` 类同时承载三条业务线的完整运行时
- **预期行为**: 拆分为 job_store、direct_stream、observation_registry 等子模块
- **实际行为**: 6824 行单文件，16+ 个 dataclass、FinsIngestionJobStore 协议及实现、~30 个方法
- **直接证据**: 文件行数 6824，包含 download/preprocess/upload 三套 producer、三套 observed stream
- **影响**: 违反"禁止 God object/function"约束。三条管线的错误处理和状态转换逻辑交织
- **建议改法和验证点**: 拆分为 `job_store.py`、`direct_stream.py`、`observation_registry.py` 等
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 中

### 3-未修复-中-durable/state.py 6594 行职责集中

- **入口/函数**: `dayu/host/durable/state.py` 全文件
- **文件(行号)**: `dayu/host/durable/state.py` (6594 行)
- **输入场景**: 修改 Session/Run/Attempt/DispatchRecord/WaitRecord 的 CRUD 或 CAS 逻辑
- **实际分支**: 5 种实体的 row dataclass、序列化、所有 CRUD 查询、所有 CAS mutation 集中在单文件
- **预期行为**: 按实体类型拆分子模块
- **实际行为**: 80+ 个公开函数和 20+ 个 dataclass 集中在单文件
- **直接证据**: 文件行数 6594
- **影响**: 任何对 WaitRecord 的修改都必须在包含 Session/Run/Attempt 逻辑的同一文件中进行
- **建议改法和验证点**: 按实体拆分为 `session_state.py`、`run_state.py`、`attempt_state.py`、`dispatch_state.py`、`wait_state.py`
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 4-未修复-中-run_transition.py 6489 行 transition helper 过度集中

- **入口/函数**: `dayu/host/durable/run_transition.py` 全文件
- **文件(行号)**: `dayu/host/durable/run_transition.py` (6489 行)
- **输入场景**: 修改 Run 创建、启动、promotion、recovery、cancel、terminal closeout、wait resolution 等 transition
- **实际分支**: 30+ 个 dataclass 输入类型和 20+ 个 transition 函数
- **预期行为**: 按 Run 生命周期阶段拆分
- **实际行为**: 全部 transition 路径集中在单文件
- **直接证据**: 文件行数 6489
- **影响**: 与 Finding 3 同类问题，增加冲突风险
- **建议改法和验证点**: 按阶段拆分为 `run_create.py`、`run_start.py`、`run_cancel.py`、`run_terminal.py`、`run_recovery.py`、`run_wait.py`
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 5-未修复-中-context_fallback.py 用 None 默认值绕过 MemoryProjectionPolicy

- **入口/函数**: `_fallback_caps_allow_append`
- **文件(行号)**: `dayu/host/context_fallback.py` (行 765-787)
- **输入场景**: 上游未注入 `MemoryProjectionPolicy` 时调用 fallback selection
- **实际分支**: `if memory_policy is None: return True`
- **预期行为**: 上游必须提供 policy，缺失时应报错或使用明确的降级策略
- **实际行为**: None 时直接返回 True，绕过 item cap 和 char cap 限制
- **直接证据**: 行 779-780: `if memory_policy is None: return True`
- **影响**: 当上游未注入 policy 时，fallback selection 的限制被完全绕过，可能导致 context window 超出 hard budget
- **建议改法和验证点**: 要求上游必须提供 policy（改为非 Optional），或在 None 时使用保守默认值并记录 warning
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 6-未修复-中-durable state.py 用 assert 做类型收窄

- **入口/函数**: `_slot_row_from_session_list_host_row`
- **文件(行号)**: `dayu/host/durable/state.py` (行 1211-1217)
- **输入场景**: `python -O` 模式运行时
- **实际分支**: 7 个 `assert xxx is not None` 做类型收窄
- **预期行为**: 使用显式 `if` + `raise HostDurableError`
- **实际行为**: `assert` 在 `-O` 模式下被跳过，后续代码可能接收到 None
- **直接证据**: 行 1211-1217 的 7 条 assert 语句
- **影响**: `python -O` 模式下类型收窄失效，可能导致 AttributeError
- **建议改法和验证点**: 替换为 `if xxx is None: raise HostDurableError(...)`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 7-未修复-中-recovery scan 单事务遍历全部非终态 Run

- **入口/函数**: `StartupRecoveryScanner.scan`
- **文件(行号)**: `dayu/host/recovery.py` (行 193-219)
- **输入场景**: 长时间故障后重启，积累大量非终态 Run
- **实际分支**: 单事务内 `read_non_terminal_runs` 读取全部非终态 Run
- **预期行为**: 分批执行，避免单事务过大
- **实际行为**: 单事务内遍历全部，持锁时间线性增长
- **直接证据**: 行 204: `for run in read_non_terminal_runs(transaction):`
- **影响**: 极端场景下（数百个非终态 Run）SQLite 写入超时或阻塞
- **建议改法和验证点**: 分批执行（每批 50 个 Run），或使用 read-only transaction 扫描后逐个 write transaction 处理
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 8-未修复-中-SEC 限流状态文件非原子写入

- **入口/函数**: `_save_sec_throttle_state`
- **文件(行号)**: `dayu/fins/downloaders/sec_downloader.py` (行 754-775)
- **输入场景**: 进程在写入 state.json 时崩溃（OOM kill、SIGKILL）
- **实际分支**: 普通 `open("w")` 写入后直接关闭
- **预期行为**: 使用 atomic write（write-to-tmp + `os.replace` + `os.fsync`）
- **实际行为**: 无 fsync、无 atomic rename，文件可能被截断
- **直接证据**: 行 768-774 使用普通 `open("w")`，对比 `ingestion_runtime.py:2026-2033` 正确使用 tmp + `os.replace` + `os.fsync`
- **影响**: 崩溃后文件截断导致限流状态丢失，恢复后可能立即触发 SEC 封禁
- **建议改法和验证点**: 对齐 `FsFinsIngestionJobStore._write_record_locked` 的 atomic write 模式
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 9-未修复-中-search_document 静默吞掉非取消异常

- **入口/函数**: `FinsReadRuntime.search_document`
- **文件(行号)**: `dayu/fins/tools/read_runtime.py` (行 1023-1026)
- **输入场景**: BM25F 索引构建或语义 profile 增强过程中发生非预期异常
- **实际分支**: `except Exception: pass` 静默吞掉所有非取消异常
- **预期行为**: 缩小为 `except (OSError, ValueError)` 并记录 warning 日志
- **实际行为**: 搜索继续执行但使用空索引，结果质量严重下降但不报错
- **直接证据**: 行 1023-1026 的 `except FinsReadCancelledError: raise` + `except Exception: pass`
- **影响**: LLM 收到质量极差的搜索结果但无法感知，基于不完整信息生成错误分析
- **建议改法和验证点**: 缩小异常范围并记录 warning；或在索引为空时标记降级标志
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 10-未修复-中-CN/HK 下载器无协作式取消

- **入口/函数**: `CninfoDiscoveryClient` / `HkexnewsDiscoveryClient` 的 HTTP 方法
- **文件(行号)**: `dayu/fins/downloaders/cninfo_downloader.py`、`dayu/fins/downloaders/hkexnews_downloader.py`
- **输入场景**: 用户在 CN/HK 下载过程中请求取消
- **实际分支**: HTTP 方法不接受 `cancellation_checker` 参数，`time.sleep` 阻塞
- **预期行为**: 对齐 SEC 下载器的协作式取消
- **实际行为**: 只能等当前请求超时或重试耗尽
- **直接证据**: `cninfo_downloader.py` 的 `_http_get_json`（行 557）无 `cancellation_checker` 参数
- **影响**: 长超时场景下用户无法中断 CN/HK 下载
- **建议改法和验证点**: 为 CN/HK 下载器的 HTTP 方法添加 `cancellation_checker` 参数
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 11-未修复-中-CN/HK 下载器临时文件无自动清理

- **入口/函数**: `CninfoDiscoveryClient.download_report` / `HkexnewsDiscoveryClient.download_report`
- **文件(行号)**: `dayu/fins/downloaders/cninfo_downloader.py` (行 337-353)、`dayu/fins/downloaders/hkexnews_downloader.py` (行 334-350)
- **输入场景**: 下载流程在写入后、调用方 unlink 前崩溃
- **实际分支**: PDF 写入 `tempfile.gettempdir()` 子目录，`delete=False`，无清理机制
- **预期行为**: 使用 context manager 或 pipeline 完成后统一清理
- **实际行为**: 临时文件残留在磁盘
- **直接证据**: `cninfo_downloader.py:337-353` 使用 `tempfile.NamedTemporaryFile(..., delete=False)`
- **影响**: 长期运行后 `/tmp` 目录积累大量未清理的 PDF 文件
- **建议改法和验证点**: 引入 context manager 或在 pipeline 完成后统一清理
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 12-未修复-中-Host 对 dayu.engine.contracts 的大量导入

- **入口/函数**: Host 层多个模块
- **文件(行号)**: `dayu/host/dispatch.py`、`dayu/host/run_input.py`、`dayu/host/api.py`、`dayu/host/engine_ingest.py`、`dayu/host/llm_compaction.py` 等
- **输入场景**: `dayu.engine.contracts` 演变为包含行为逻辑时
- **实际分支**: Host 7+ 个模块直接导入 `dayu.engine.contracts` 下的类型
- **预期行为**: contracts 应属于公共契约层而非 Engine 业务层
- **实际行为**: Host 对 Engine 命名空间的直接依赖使架构边界模糊
- **直接证据**: `grep -rn "from dayu\\.engine\\." dayu/host/` 返回 30+ 行结果
- **影响**: 当前不构成实际违反（contracts 是纯类型），但若 contracts 包演变为包含行为逻辑，Host 会隐式获得对 Engine 的行为依赖
- **建议改法和验证点**: 明确文档化 `dayu.engine.contracts` 的层级定位；若不属于 Engine 业务层，考虑移入 `dayu.contracts`
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 13-未修复-中-DocumentMeta 使用 dict[str, Any]

- **入口/函数**: `DocumentMeta` 类型定义
- **文件(行号)**: `dayu/fins/domain/document_models.py` (行 33)
- **输入场景**: 仓储层所有 meta 读写操作
- **实际分支**: `DocumentMeta = dict[str, Any]`，被 `repository_protocols.py` 多个方法消费
- **预期行为**: 升级为 TypedDict（`_SourceDocumentMeta` 已在 read_runtime.py 中存在）
- **实际行为**: 宽泛类型导致下游无编译期检查
- **直接证据**: `document_models.py:33` 定义为 `dict[str, Any]`，被 `get_source_meta`（行 176）等方法直接返回
- **影响**: 仓储协议层暴露无类型字典，违反"禁止 Any"约束
- **建议改法和验证点**: 将 `read_runtime.py` 中的 `_SourceDocumentMeta` TypedDict 提升到 domain 层
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 14-未修复-中-config overlay 对非 map 字段全量替换

- **入口/函数**: `_overlay_roots`
- **文件(行号)**: `dayu/runtime/config_loader.py` (行 ~460-485)
- **输入场景**: 用户编写部分覆盖的 workspace 配置
- **实际分支**: 非 map 字段使用 `merged[field_name] = workspace_value` 直接覆盖
- **预期行为**: 增加缺失字段的 warning 日志，或改为 require-complete 语义
- **实际行为**: package 配置中的字段被静默丢失
- **直接证据**: `merged[field_name] = workspace_value` 无任何警告
- **影响**: silent data loss，用户无意间丢失 package 默认值中的关键字段
- **建议改法和验证点**: 增加字段完整性校验或 warning 日志
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 15-未修复-中-_FinsSigintMonitor 与 CliSigintMonitor 重复实现

- **入口/函数**: `_FinsSigintMonitor` / `CliSigintMonitor`
- **文件(行号)**: `dayu/cli/commands/fins.py` (行 182-256)、`dayu/cli/agent_entrypoint.py` (行 31-113)
- **输入场景**: 修改 SIGINT handler 语义
- **实际分支**: 两个类字段、方法签名和逻辑完全一致
- **预期行为**: 复用 `CliSigintMonitor`
- **实际行为**: 完整复制品
- **直接证据**: 两者实现几乎逐行一致
- **影响**: 违反"重复逻辑必须抽取"约束
- **建议改法和验证点**: 删除 `_FinsSigintMonitor`，复用 `CliSigintMonitor`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 16-未修复-中-web_tools.py God Module

- **入口/函数**: `dayu/tools/web/web_tools.py` 全文件
- **文件(行号)**: `dayu/tools/web/web_tools.py` (~2950 行)
- **输入场景**: 修改 web fetch pipeline、challenge 处理或 Playwright fallback
- **实际分支**: `_fetch_web_page_business` 单函数约 470 行，10 层异常处理
- **预期行为**: 拆分 fetch pipeline、challenge 处理、Playwright fallback 为独立模块
- **实际行为**: 文件自述"约 2000 行"但实际接近 3000 行
- **直接证据**: `_fetch_web_page_business` 行 1821-2294，约 470 行
- **影响**: 违反"禁止 God function"约束，错误路径难以追踪
- **建议改法和验证点**: 拆分 `_fetch_web_page_business` 为 fetch pipeline、challenge 处理、Playwright fallback 子函数
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

## Open Questions

1. **`dayu.engine.contracts` 的层级定位**: 该包是 Engine 业务层的一部分，还是独立于分层架构的公共契约包？如果是后者，应移入 `dayu.contracts` 以消除 Host -> Engine 的名义依赖。

2. **三条下载管线的取消能力不对称**: SEC 管线有完善的协作式取消，CN/HK 管线完全没有。是否需要对齐？

3. **config overlay 语义**: 当前 workspace overlay 对非 map 字段采用全量替换且无警告。是否需要增加字段完整性校验？

4. **`DocumentMeta` 类型升级路径**: 是否应将 `read_runtime.py` 中的 `_SourceDocumentMeta` TypedDict 提升到 domain 层作为仓储协议的正式类型？

## Residual Risk

1. **Host 层未完整走读区域**: `tool_runtime.py`（7597 行）只读取了前 200 行和函数列表；`admission.py`、`dispatch.py`、`waiting.py`、`memory.py`、`llm_compaction.py` 的完整调用路径未逐一验证。

2. **durable 子目录**: `transaction.py`、`connection.py`、`event_log.py` 的 SQLite 事务管理、连接池、EventLog append 语义和并发安全未逐一验证。

3. **Engine 层 Provider 协议碎片化**: choice policy 严格依赖 OpenAI 协议语义，接入非标准 provider 时需逐一验证。

4. **Fins 层 SEC 限流状态文件损坏**: 非原子写入在进程崩溃时可能导致状态文件截断。

5. **Web tools 全局 session 生命周期**: 进程内 session 不会被显式关闭，依赖进程退出回收。

6. **测试覆盖**: 未系统审查测试文件对上述 findings 的覆盖情况。
