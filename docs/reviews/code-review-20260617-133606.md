# WU-CLI-ACTIVITY-01 Slice A 代码重审（AgentDS 认定 Findings 修复验证）

## Scope

- Mode: current changes (re-review)
- Work unit: `WU-CLI-ACTIVITY-01`
- Gate: code re-review for Slice A only
- Reviewed artifact: adjudication `docs/reviews/code-review-wu-cli-activity-01-slice-a-adjudication-20260617-132855.md`
- Fix artifact: `docs/reviews/wu-cli-activity-01-slice-a-fix-codex.md`
- Output file: `docs/reviews/code-review-20260617-133606.md`
- Included scope: DS F-1 / F-2 / F-3 only; no Slice B-F expansion
- Excluded scope: AgentMiMo findings (none accepted), any new issues beyond the three accepted findings

## Findings

### DS F-1 — 已修复 — activity projection allowlist 测试覆盖补全

- **入口/函数**: `tests/host/test_host_activity_event_projection.py` 中新增测试函数
- **文件(行号)**: `tests/host/test_host_activity_event_projection.py:353-710`
- **输入场景**: 各测试分别覆盖 TOOL_RESULT_ACCEPTED completed/cancelled、TOOL_AWAITING、RUN_WAITING、四类 context compaction event、RUN_ACCEPTED/RUN_STARTED lifecycle、display fallback 链、descriptor 降级、bounded summary 边界
- **实际分支**: 所有新增测试均直接调用 `_host_event_from_row` 走 allowlist 投影路径
- **预期行为**: allowlist 各分支的 activity status、severity、title、summary、display_name 等均应有直接回归断言
- **实际行为**: 82 passed，pyright 0 errors
- **直接证据**:
  - `test_tool_result_completed_and_cancelled_outcomes` (line 353): 断言 completed→COMPLETED/INFO、cancelled→CANCELLED/WARNING，含 summary 文本
  - `test_tool_awaiting_and_run_waiting_activity_projection` (line 399): 断言 TOOL_AWAITING/RUN_WAITING 均投影 WAITING status，含 tool_name/display_name fallback
  - `test_context_compaction_activity_projection` (line 446): 参数化覆盖四类 compaction event，断言 kind/status/severity/title/summary 完整映射
  - `test_non_terminal_run_lifecycle_activity_projection` (line 505): 断言 RUN_ACCEPTED→STARTED、RUN_STARTED→IN_PROGRESS
  - `test_tool_display_fallback_chain_for_missing_snapshot_parts` (line 633): 覆盖 run 缺失、tool_set 非 Mapping、display_names 非 Mapping、空 display_name 四类 fallback
  - `test_tool_display_fallback_when_input_event_missing` (line 687): monkeypatch `_EVENT_LOG_STORE` 模拟 input event 缺失→fallback
  - `test_activity_descriptor_read_degrades_to_no_activity` (line 607): 断言 payload descriptor 缺失时 identity 保留且 activity=None
  - `test_bounded_summary_boundaries` (line 576): 参数化空白字符串、空白符、精确 180 字符、181 字符截断
  - `test_provider_protocol_error_activity_is_bounded` (line 547): 断言 raw payload ref 不泄露、summary 不超过 180 字符
- **影响**: 已消除——allowlist 各分支、fallback 链、降级路径、边界条件均有直接回归断言
- **建议改法和验证点**: 无需额外修改
- **修复风险（低）**:
- **严重程度（中）**: **已修复**

### DS F-2 — 已修复 — `_tool_display_name` 不再局部构造 `EventLogStore()`

- **入口/函数**: `_tool_display_name` (read_api.py:1404)
- **文件(行号)**: `dayu/host/read_api.py:136-137, 1421`
- **输入场景**: 任意需要通过 `effective_tool_display_names` 查找工具展示名的 activity 投影
- **实际分支**: `_tool_display_name` → `_EVENT_LOG_STORE.read_event_by_id(...)` 替代原先的 `EventLogStore().read_event_by_id(...)`
- **预期行为**: 依赖形状明确，不自建 EventLogStore 实例
- **实际行为**: 模块私有 `_EVENT_LOG_STORE` 在模块加载时构造一次，所有 activity 投影复用同一实例
- **直接证据**:
  - Line 136-137: `_EVENT_LOG_STORE` 定义为模块级私有实例，注释说明"EventLogStore 是无状态 durable primitive 方法容器"
  - Line 1421: `input_event = _EVENT_LOG_STORE.read_event_by_id(transaction, run.input_event_id)` 替代原先局部构造
  - 搜索 `read_api.py` 全文，`EventLogStore()` 仅出现在 line 137，无其他局部构造点
  - 测试 line 704 通过 `monkeypatch.setattr(read_api, "_EVENT_LOG_STORE", _MissingEventLogStore())` 验证 fallback 行为
- **影响**: 已消除——依赖归属清晰，无局部构造
- **建议改法和验证点**: 无。残留风险（若 EventLogStore 后续引入构造函数状态）已在 fix artifact 中记录
- **修复风险（低）**:
- **严重程度（低）**: **已修复**

### DS F-3 — 已修复 — `_activity_from_row` 不再重复校验 `event_class`

- **入口/函数**: `_activity_from_row` (read_api.py:1050)
- **文件(行号)**: `dayu/host/read_api.py:1050-1088`
- **输入场景**: 任意非终态 event 的 activity 投影
- **实际分支**: `_activity_from_row` 仅做 event_type 匹配，不再调用 `_public_event_class_from_durable`
- **预期行为**: event_class 验证只在 `_host_event_from_row`（及各终态 handler）中执行一次
- **实际行为**: `_host_event_from_row` (line 882)、`_succeeded_host_event` (line 948)、`_failed_host_event` (line 974)、`_cancelled_host_event` (line 1004)、`_lost_host_event` (line 1034) 各自调用 `_public_event_class_from_durable`，这些调用点在 `_activity_from_row` 之前完成；`_activity_from_row` 本身不含 event_class 校验
- **直接证据**:
  - `_activity_from_row` (line 1050-1088): 全文搜索无 `_public_event_class_from_durable` 调用
  - 所有 event_class 校验均在 `_host_event_from_row` 及其调用的终态 handler 中完成，形成单一验证点
- **影响**: 已消除——验证点清晰，无重复调用
- **建议改法和验证点**: 无需额外修改
- **修复风险（低）**:
- **严重程度（低）**: **已修复**

## Open Questions

无。

## Residual Risk

- Slice B-F 未验证：Service / CLI activity 消费、渲染、去重行为仍属后续 Slice 工作，不在本次重审范围
- `_EVENT_LOG_STORE` 模块级实例假设 EventLogStore 保持无状态：已在 fix artifact 中记录为当前 Slice 接受风险；若后续引入构造函数状态，需切换为显式注入

## Conclusion

**pass** — Slice A 三项已接受 finding (DS F-1/F-2/F-3) 均已修复。82 项 host 测试全部通过，pyright 零报错。无未修复或部分修复项。
