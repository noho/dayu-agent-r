# AgentMiMo Plan Re-Review: WU-CLI-ACTIVITY-01 Activity Composer Plan

**Reviewer**: AgentMiMo
**Re-Review Date**: 2026-06-17 13:04:17
**Plan Artifact**: `docs/host/host-issues/wu-cli-activity-01-activity-composer-plan.md`
**Work Unit**: WU-CLI-ACTIVITY-01
**GitHub Issue**: #144
**Current Gate**: Plan Re-Review
**Original Review Artifact**: `docs/reviews/plan-review-20260617-124817.md`
**Adjudication Truth**: `docs/reviews/plan-review-wu-cli-activity-01-adjudication-20260617-125229.md`

## 1. Re-Review Scope

本 re-review 严格按 adjudication 裁决复查：只验证 adjudication 中 `accepted` / `accepted-with-modification` 的 MiMo 原始 findings 是否已在 fixed plan 中修复，不重新扩大 scope，除非发现新的 blocker。

Adjudication 要求针对 MiMo findings 的 plan fixes：

1. Clarify `event_type` copies EventLog row `event_type`（F002）
2. Specify Host-owned tool display snapshot path（F003）
3. Add terminal identity tests（F004）
4. Clarify Service DTO field source（F005）
5. Add external editor edge behavior（F006）
6. Define deterministic `REASONING_DELTA` / `CONTENT_DELTA` behavior（F007）
7. Add prompt_toolkit early compatibility validation（F008）
8. Service README check（F009，deferred-with-owner）

## 2. Finding Status Verification

### F001: HostEvent.event_class / event_type 枚举未定义

- **原始严重程度**: 高
- **Adjudication 决定**: rejected-with-reason（code fact: `dayu/host/api.py` 已有 `HostEventClass`）
- **状态**: 证据失效
- **验证**: Adjudication 确认 `dayu/host/api.py` 已定义 public `HostEventClass`，包含 `CANONICAL_FACT`、`PREVIEW`、`DIAGNOSTIC`、`PROJECTION_SIGNAL`。Finding 前提不成立。Fixed plan 第 170-173 行明确复用已有值，不得新增同名 enum。
- **结论**: 证据失效，无需 plan fix。

### F002: HostEvent.event_type 语义不明确

- **原始严重程度**: 中
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 174-175 行明确："`HostEvent.event_type` 必须直接复制 EventLog row 的 `event_type`。它是 public event identity label，不是 UI 文案、业务事实、财报事实或 activity 分类依据。"
- **结论**: 已修复。

### F003: Host-owned tool metadata lookup 实现路径未指定

- **原始严重程度**: 高
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 226-239 行新增 "Tool display lookup" 专节：
  - Host admission 在构造 `USER_INPUT_ACCEPTED.effective_tool_set` 时，从 `ToolBundle.definitions` 提取 `{tool_name, display_name}`，冻结为 `effective_tool_display_names` snapshot（第 226-229 行）。
  - `display_name` 来源只能是 `ToolDefinition.display.name`（第 230-232 行）。
  - `read_api` 通过 run/input payload 查该 Host-owned snapshot，按 `tool_name` lookup，缺失 fallback stable `tool_name`（第 233-235 行）。
  - 是 payload shape 扩展，不是 durable schema migration（第 236-237 行）。
  - 只能由 Host public projection 使用，不得让 Engine/Service/CLI import `ToolBundle`（第 238-239 行）。
- **结论**: 已修复。实现路径具体、边界清晰、fallback 明确。

### F004: terminal 事件需要 event_class / event_type 测试

- **原始严重程度**: 中
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 497-498 行 Slice A Tests 新增："terminal HostEvent `SUCCEEDED` / `FAILED` / `CANCELLED` / `LOST` 都必须携带正确 `event_class` 和 `event_type`。" 第 780-781 行 Expected assertions 也有对应断言。
- **结论**: 已修复。

### F005: Service DTO 字段与 HostActivityView 不完全一致

- **原始严重程度**: 中
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 348-350 行明确："`run_id`、`event_sequence`、`dedupe_key` 只能来自 public `HostEvent` 字段；display semantics 只能来自 `HostEvent.activity`。Service 不解析 private payload，也不得用 `HostEvent.event_class` / `event_type` 决定 UI kind、title、severity。"
- **结论**: 已修复。字段来源和使用限制均明确。

### F006: 外部编辑器边界场景不足

- **原始严重程度**: 低
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 386-389 行明确："编辑器返回空白时不 submit；用户取消编辑器时不 submit；编辑器启动失败时保留当前 draft，并在 stderr 显示 bounded diagnostic message（包含 stderr / 错误消息的有界摘要，不进入 stdout）。" 第 642-643 行 Slice D Tests 要求覆盖这些场景。
- **结论**: 已修复。

### F007: REASONING_DELTA / CONTENT_DELTA 的 activity 投影规则不够具体

- **原始严重程度**: 中
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 220-222 行明确选择确定性规则："`REASONING_DELTA` 和 `CONTENT_DELTA` 都必须保留 `event_class` / `event_type` identity，但 `activity=None`。raw reasoning delta、raw content delta 永不投影为 activity。" 不再有 "或" 的不确定表述。第 474-475 行 Slice A 和第 494 行 Tests 均有覆盖。
- **结论**: 已修复。

### F008: prompt_toolkit async 兼容性风险未充分评估

- **原始严重程度**: 中
- **Adjudication 决定**: accepted
- **状态**: 已修复
- **验证**: Fixed plan 第 618-620 行 Slice D 新增 early validation step："在替换 REPL wiring 前，先做 prompt_toolkit early compatibility validation：用最小 wrapper / 单元测试验证当前依赖版本可表达 Ctrl+J、Ctrl+R、Ctrl+X Ctrl+E、输入态 Ctrl+C 以及 async / key binding 基本组合；失败则先停止，不做大范围 REPL 改造。" 第 645 行 Tests 也要求 "prompt_toolkit minimal compatibility test 先于 broad REPL wiring 落地"。
- **结论**: 已修复。Early validation 前置于 broad REPL wiring，风险可控。

### F009: README 触发检查未覆盖 dayu/service/ 目录

- **原始严重程度**: 低
- **Adjudication 决定**: deferred-with-owner
- **状态**: 已修复
- **验证**: Fixed plan 第 306-308 行明确："如果 `dayu/service/README.md` 存在，implementation agent 必须只读检查 `dayu/service/entrypoint_runtime.py` 的 entrypoint behavior 变化是否影响该 README 职责范围，并在 completion report 记录更新 / 不更新结论。" 第 712-713 行 Slice F 也有对应说明。
- **结论**: 已修复。Implementation agent 有明确的只读检查义务。

## 3. New Blocker Check

复查 fixed plan 后，未发现新的 blocker：

- `HostEvent` 扩展的 validation 和 public export 在 Slice A stop condition 中有约束，不会引入 breaking change。
- `USER_INPUT_ACCEPTED.effective_tool_set` payload shape 扩展明确不是 durable schema migration。
- prompt_toolkit early validation 前置于 broad REPL wiring，风险可控。
- 所有 findings 的修复均在原 plan 结构内完成，未引入新的架构风险或过度耦合。

## 4. Remaining Unfixed Findings

无。所有 adjudication 中 accepted / accepted-with-modification 的 MiMo findings 均已修复。

## 5. Conclusion

**Plan Re-Review Conclusion: pass**

所有 adjudication 中 accepted / accepted-with-modification 的 MiMo 原始 findings 均已在 fixed plan 中修复：

| 原始 Finding | 状态 |
|---|---|
| F001: HostEventClass 枚举未定义 | 证据失效（adjudication 确认已有 code fact） |
| F002: event_type 语义不明确 | 已修复 |
| F003: Host-owned tool metadata lookup 未指定 | 已修复 |
| F004: terminal 事件 identity 测试缺失 | 已修复 |
| F005: Service DTO 字段来源不清 | 已修复 |
| F006: 外部编辑器边界场景不足 | 已修复 |
| F007: REASONING_DELTA/CONTENT_DELTA 投影不确定 | 已修复 |
| F008: prompt_toolkit async 兼容性风险 | 已修复 |
| F009: Service README 检查缺失 | 已修复 |

未发现新的 blocker。Residual risks 均有缓解策略。Plan 已 code-generation-ready，可进入 implementation gate。
