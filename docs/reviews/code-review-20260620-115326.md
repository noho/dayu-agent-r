# Code Review — WU-CM-15 Focused Re-Review

## Scope

- Mode: current changes
- Branch: `phase/wu-cm-15`
- Base: `97518e93` (accepted plan commit)
- Output file: `docs/reviews/code-review-20260620-115326.md`
- Included scope:
  - `utils/smoke_host_public_conversation_memory_scenarios.py` (unstaged, +1721/-77)
  - `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py` (unstaged, +327/-77)
  - `tests/README.md` (unstaged, +1/-1)
  - `docs/host/issues-implementation-control.md` (unstaged, +10/-10)
- Excluded scope: `docs/reviews/` (review artifacts, not production code)
- Parallel review coverage: 无

## Findings

未发现实质性问题。

### 逐项裁决

#### 1. `_SMOKE_REACTIVE_OLD_MARKER` 是否真实进入 reactive-r1-old-seed prompt — **PASS**

- **入口**: `_reactive_compact_round_specs()` → RoundSpec label `reactive-r1-old-seed`
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:2620`
- **直接证据**: `prompt=f"记录旧上下文种子 {_SMOKE_REACTIVE_OLD_MARKER}，后续只作为 compact 可丢弃历史。"` — marker 硬编码进 prompt 模板字符串。
- **测试覆盖**: `test_pure_spec_selection_tool_fact_requirements_and_long20_final_label` 行 318: `assert _SMOKE_REACTIVE_OLD_MARKER in reactive_spec_by_label["reactive-r1-old-seed"].prompt`
- **oracle 是否空转**: 否。r1 prompt 含 marker → r1 成为 Host 历史 → r6 触发 reactive overflow → Host compactor 接受 → Host recovery dispatch 使用 compacted view → acceptance 断言 recovery 不含 old marker。marker 从 prompt 到 compacted view 到 recovery dispatch 形成完整数据链。

#### 2. memory-reactive-compact suite-local selected recent cap — **PASS**

- **入口**: `_smoke_options_for_suite()` → `_reactive_compact_smoke_memory_policy()`
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:3231-3275`
- **只影响该 suite**: 行 3231 `if suite is not SuiteMode.MEMORY_REACTIVE_COMPACT: return options` — 非 reactive suite 直接返回原始 options。
- **selected recent cap 计算**: `max(1, turn_floor * 2)`。默认 `selected_recent_window_turn_floor` 为 4 → cap = 8 items。
- **r1 old seed 被排除的推理**: 6 轮布局中，r6 是 overflow 目标轮。selected recent 仅保留最近 `turn_floor` 轮 = 4 轮（r3-r6），r1 和 r2 被推出 selected recent window。r1 只通过 compacted semantic view 表示。
- **r5 protected recent 和 current input 保留**: r5 在 selected recent window 内（属于 r3-r6 范围），r6 current input 始终保留。
- **测试覆盖**: `test_reactive_runtime_assembly_bounds_selected_recent_window` 行 168-187 验证 cap 计算公式和 fallback cap 约束。

#### 3. `_assert_reactive_compact_acceptance` / `_deterministic_dropped_old_marker` 覆盖 — **PASS**

- **missing dropped marker**: 行 578-589，构造 `dropped_old_marker=None` 的 observation，断言 `RuntimeError("missing dropped old marker expectation")`。
- **old marker leak**: 行 591-614，构造 recovery dispatch text 含 `_SMOKE_REACTIVE_OLD_MARKER`，断言 `RuntimeError("reactive recovery dropped old unexpectedly contains marker")`。
- **happy path**: 行 545-572，recovery dispatch 只含 current + recent marker（不含 old marker），`_assert_reactive_compact_acceptance` 通过。
- **`_deterministic_dropped_old_marker`**: 行 544 断言 `== _SMOKE_REACTIVE_OLD_MARKER`，确认 helper 返回正确值。

#### 4. `_patched_compactor_runner` identity sanity check + finally restore — **PASS**

- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:1851-1868`
- **finally restore**: 行 1856 `finally: llm_compaction._run_agent_request = original_runner` — 无论 try 内是否异常，均恢复原值。
- **identity check**: 行 1854 `if llm_compaction._run_agent_request is not runner: raise RuntimeError(...)` — patch 后立即验证赋值生效。对于常规模块属性赋值，`is not` 检查有效。
- **`yield` 位置**: 在 try 块内，finally 始终执行。

#### 5. `_compact_pressure_reserve_tokens` 是否已删除且未留下死代码 — **PASS**

- **函数定义**: `grep` 确认 `utils/smoke_host_public_conversation_memory_scenarios.py` 中无 `_compact_pressure_reserve_tokens` 定义或调用。
- **`_COMPACT_PRESSURE_LARGE_WINDOW_TOKENS`**: 同样已从该文件删除。
- **替代实现**: `_COMPACT_PRESSURE_RESERVE_TOKENS` (575,000) 和 `_COMPACT_FALLBACK_PRESSURE_RESERVE_TOKENS` (160,000) 作为 `Final[int]` 常量，通过 `_compact_pressure_padding_with_reserve()` 使用。
- **pyright**: 0 errors, 0 warnings。

#### 6. fallback deterministic smoke acceptance 覆盖 — **PASS**

- **proactive request**: `_assert_fallback_dispatch_acceptance` 行 3683 `if summary.requested_proactive < 1: raise RuntimeError("...proactive CONTEXT_COMPACTION_REQUESTED")`。
- **selected/dropped/current refs**: 行 3700-3705 验证 `selected_block_ids`、`dropped_block_ids`、`current_input_ref` 均非空。
- **pressure bounds**: `_assert_fallback_pressure_bounds` 行 3706-3721 验证 pressure 在 soft 和 hard threshold 之间。
- **dispatch markers**: 行 3713-3721 验证 final dispatch 含 current input 和 selected recent marker，不含 dropped old marker。
- **测试覆盖**: `test_fallback_acceptance_helper_requires_proactive_request_and_window` 行 648-692 覆盖 happy path、missing request 和 below soft threshold 三种场景。

#### 7. tests/README.md 最小更新 — **PASS**

- **变更**: 行 129 从 `memory-core` / `memory-compact` 扩展为 `memory-core` / `memory-compact` / `memory-reactive-compact` / `memory-compact-fallback`，并补充 `reactive deterministic recovery dispatch oracle` 和 `compact failure fallback selected window oracle` 描述。
- **触发规则**: `tests/` 修改 → 检查 `tests/README.md`。变更仅新增 suite 名称和 oracle 描述，未扩展 README 职责范围。
- **符合触发规则**: 是。最小更新，只反映新增测试覆盖。

## Open Questions

无。

## Residual Risk

1. **`_patched_compactor_runner` monkey-patch 与并发安全**: 当前 smoke 是单进程顺序执行，不存在并发风险。若未来 smoke 支持并行 suite，需评估 `llm_compaction._run_agent_request` 的全局状态竞争。当前不在本 WU scope 内。
2. **reactive suite selected recent cap 依赖默认 `selected_recent_window_turn_floor`**: `_reactive_compact_smoke_memory_policy` 假设 `policy.selected_recent_window_turn_floor <= 4`（即 `_SMOKE_REACTIVE_HISTORY_GAP_ROUNDS + 1`）。若 Service 装配产出更大的 floor，会抛出 `ValueError`。当前 smoke 配置不受影响，但有明确 fail-fast。
3. **`_fake_compaction_proposal_from_material_json` 不回写 marker**: `_sanitize_compactor_material_text` 移除所有 smoke marker，避免 compacted view 回写测试探针。测试 `test_fake_compactor_proposal_does_not_echo_material_markers` 覆盖了此行为。

## Verification

- `pyright utils/smoke_host_public_conversation_memory_scenarios.py tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`: 0 errors, 0 warnings
- `pytest tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`: 20 passed
- `git diff` 4 files, all unstaged
