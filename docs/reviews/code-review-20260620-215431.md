# Code Review

## Scope

- Mode: current changes (aggregate deepreview gate)
- Branch: `phase/wu-eng-02-r1`
- Base: `main`
- Output file: `docs/reviews/code-review-20260620-215431.md`
- Included scope: full branch diff relative to `main`, covering implementation, tests, README/docs, control doc/artifacts.
- Excluded scope: `docs/reviews/` review artifacts (read for context but not production code), `docs/host/issues-implementation-control-archive.md` (archived history).
- Parallel review coverage: 无。

## Accepted Plan Compliance

逐项核对 accepted plan `docs/host/host-issues/wu-eng-02-r1-provider-debugging-correlation-plan.md` 的决策：

| Plan Decision | 代码证据 | 结论 |
|---|---|---|
| Decision 1: Service default policy enablement | `host_assembly.py:1107-1109` 改为 `OPENAI_X_CLIENT_REQUEST_ID` | PASS |
| Decision 2: No provider string branches | 无 `model.provider ==` 分支 | PASS |
| Decision 3: No end-user governance field | 无 `safety_identifier` / `user_id` 添加 | PASS |
| Decision 4: Existing Runner log site adds client correlation | `runner.py:617-624` 同一行、同一 DEBUG 级别添加 `client_correlation_id=%s` | PASS |
| Decision 5: Provider request id stays x-request-id only | `runner.py:94` 定义 `_PROVIDER_REQUEST_ID_HEADER_NAMES = ("x-request-id",)`；`_extract_provider_request_id` 仅遍历该 allowlist | PASS |
| Decision 6: Tool Trace preserves client fallback | `tool_trace.py:911-916` skip 条件增加 `and client_correlation_id is None` | PASS |
| Decision 7: Terminal diagnostic reuses existing boundary | `read_api.py` 和 `outbox.py` 调用同一 `_append_terminal_diagnostic_suffix` helper，不改写 durable payload | PASS |

## Accepted/Rejected Findings Handling

| Finding | Controller Decision | 实现处理 | 结论 |
|---|---|---|---|
| `_terminal_diagnostics` 独立测试 | accepted | `test_terminal_diagnostics.py` 8 个测试覆盖全部分支 | PASS |
| 双 id suffix 格式锁定 | accepted | `test_terminal_diagnostic_suffix_includes_both_ids_in_stable_order` 断言 provider 在前、client 在后 | PASS |
| Tool Trace `raw_payload_ref` 存在时保留 `diagnostic_ref` | accepted | `test_diagnostic_trace_uses_raw_payload_ref_without_provider_id` 断言 `diagnostic_ref == "raw-ref-fallback"` | PASS |
| `message=""` 边界处理 | accepted | `_append_terminal_diagnostic_suffix` line 35 处理 `message == ""`；测试 `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_empty` 锁定 | PASS |
| `_lost_host_event` 不追加 suffix | rejected-with-reason | 未修改 `_lost_host_event`；re-review 确认 `_lost_lifecycle_plan` 设 `provider_request_id=None, client_correlation_id=None` | RESPECTED |

## Findings

未发现实质性问题。

## Verification Walkthrough

沿真实代码路径逐行走读完整数据链：

1. **Service assembly default**: `host_assembly.py:_runner_spec_from_model` → `RunnerSpec(client_correlation_policy=OPENAI_X_CLIENT_REQUEST_ID)` → ordinary baseline 与 compactor baseline 均默认启用。
2. **Engine runner header mapping**: `runner.py:_client_correlation_id_from_state` → `request_identity.client_correlation_id` → `_do_attempt(client_correlation_id=...)` → `X-Client-Request-Id` header 发送。
3. **Runner response log**: `runner.py:617-624` → 同一 DEBUG 行 `runner.http.response ... provider_request_id=%s client_correlation_id=%s`。
4. **Provider request id extraction**: `runner.py:_extract_provider_request_id` → `_PROVIDER_REQUEST_ID_HEADER_NAMES = ("x-request-id",)` → 仅提取 `x-request-id`。
5. **Host terminal payload**: `engine_ingest.py` 已有 `_run_failed_plan` 写入 `provider_request_id` / `client_correlation_id` 到 EventLog payload。
6. **Tool Trace diagnostic projection**: `tool_trace.py:911-916` → skip 条件 `provider_request_id is None and client_correlation_id is None` → 仅 client correlation 存在时保留行。
7. **Terminal diagnostic suffix**: `_terminal_diagnostics.py:_append_terminal_diagnostic_suffix` → 格式化 `provider_request_id=...` / `client_correlation_id=...` 后缀 → `read_api._failed_host_event` 和 `outbox._error_message` 调用同一 helper。
8. **Durable payload integrity**: 测试 `test_failed_terminal_projection_appends_correlation_suffix` 断言 `row.payload_json == original_payload_json` 和 `payload["message"] == "provider failed"` → durable payload 未被修改。

确认：

- 无新增日志点、无日志等级变更
- 无 durable terminal payload message / digest 修改
- 无 infrastructure header 映射为 `provider_request_id`
- Tool Trace 不伪造 `provider_request_id` 或 `diagnostic_ref`
- 架构分层无违规：Service → Engine（typed RunnerSpec）、Host → Engine（EventLog payload read）、Host 内部 projection（同一层）
- live watcher 与 outbox fallback 共享同一 suffix formatting helper，输出一致
- README 更新按触发规则执行（Service/Engine/Host/tests README 已更新，root README 未错误更新）

## Open Questions

无。

## Residual Risk

1. **`_lost_host_event` 未来同步风险**（继承自 code review Finding 3）：如果后续 `_lost_lifecycle_plan` 开始携带 `provider_request_id` 或 `client_correlation_id`，`_lost_host_event` 不会自动展示这些 ID。当前 `_lost_lifecycle_plan` 显式设二者为 `None`，因此无实际影响。该风险已由 controller 裁决为 rejected-with-reason，deferred 到对应 WU 的 plan reviewer 检查。

2. **`_cancelled_host_event` 不追加 suffix**：`_cancelled_host_event` 使用 `cancel_reason` 而非 `error_message`，且 plan scope 仅限 failed terminal。当前 `_cancel_lifecycle_plan` 不携带 correlation ids。此为 plan scope 内的有意设计，非遗漏。

## Verification Notes

- `pytest tests/service/test_host_assembly.py tests/runtime/test_smoke_host_public_multiturn_assembly.py tests/engine/runners/openai/test_request_identity.py tests/engine/runners/openai/test_streaming_capability_and_content_type.py tests/engine/runners/openai/test_http_error_event.py tests/engine/runners/openai/test_runner_diagnostics.py tests/host/test_terminal_diagnostics.py tests/host/test_read_api_terminal_policy.py tests/host/test_tool_trace_projection.py tests/host/test_tool_trace_queries.py tests/service/test_entrypoint_runtime.py tests/service/test_entrypoint_runtime_prompt_path.py tests/cli/test_prompt_command.py -q`: `211 passed, 3 warnings`
- `pyright`: `0 errors, 0 warnings, 0 informations`
- `git diff --check`: passed
