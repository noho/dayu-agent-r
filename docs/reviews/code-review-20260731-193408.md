# Code Review

## Scope

- Mode: current changes（WU-CLI-PROMPT-01 S1–S6 aggregate implementation review）
- Branch or PR: `codex/prompt-oracle-adjudication`
- Base: `a394f831`
- Reviewed head: `7b2c2c35`
- Output file: `docs/reviews/code-review-20260731-193408.md`
- Included scope: S1–S6 的生产代码、测试、根 README、tests README、implementation/review artifacts，以及冻结 `cli.prompt.core-execution@1` 与全部 400 条 accepted prompt scenarios；重点核对 cancel UI projection、accepted Run repeated-SIGINT closeout、startup bootstrap、UTF-8 invocation boundary、log-file resource classification、shared logging selector/runtime contract。
- Excluded scope: init、process、interactive、context compaction 的既有业务语义；Engine force-answer 与 Host Tool Trace owner；新增 rollback/race/transaction 需求；最终 gate 才执行的全部 frozen real-provider/PTY scenario 重放及新 evidence/digest 绑定。
- Parallel review coverage: `review_cancel_ui` 专项走读 S1–S3 的 UI、真实 SIGINT handler、prompt/Service/Host cancel 与 cleanup 调用链，并执行早期 launcher 和 post-terminal cleanup 对抗探针；`review_logging_boundary` 专项走读 S4–S6 的 argv、parser、resource error、runtime logging、README 与测试矩阵。主 reviewer 独立复核全部 owner 边界、冻结 contract/digest、关键测试与两个专项结论，并执行聚合测试、完整 pyright 和 diff check。

## Findings

未发现实质性问题。Gate 结论：`pass`；仍须通过 final gate 的 frozen exact scenario 重放。

## Evidence

- S1 的公共取消 UI 只按 typed `HostTerminalStatus.CANCELLED` 投影固定 `Cancelled.`，不读取或匹配 `cancel_reason`；已知 watchdog、未知未来 reason 与 `None` 均由 owner-level 测试证明不泄漏，durable typed reason owner 未被改动。
- S2 删除 prompt 第二次 SIGINT 与 Host cancel waiter 的竞争分支；accepted Run 只发一次 typed graceful cancel，并通过 `cancel_entrypoint_run_and_wait` 等待 canonical terminal 后才进入 prompt-local lifecycle cleanup。受控双 SIGINT 测试证明第二次信号不能越过 terminal barrier，cursor 只在 terminal 后推进；interactive 的既有第二次 SIGINT 语义保持不变。
- S3 把 console script 与 module invocation 都收敛到 `dayu.cli.__main__:run_module`，并把 application import 与完整 `main()` 调用置于同一 `KeyboardInterrupt -> 130` 边界；startup 注入测试覆盖 runtime prepare、Host open、Session ensure 与 monitor install，断言无 traceback、已建立资源关闭且无未 accepted Run cancel。
- S4 在 argparse 前物化 argv 并逐项 strict UTF-8 编码验证，固定诊断不回显原始 token；invalid argv 在默认/显式日志打开、runtime configure 与 runner 前退出 2。显式 log-file 空白值与 `OSError` 分属 typed usage/resource error，missing parent 不 mkdir、不创建目标并在 primary operation 前退出 1。
- S5 的 8 个公开 spelling 与 8 个快捷 selector 从单一 spec 派生；root/command/action 使用独立 occurrence owner，parse 后统一拒绝任意两个 selector。`debug_stream` 位于互斥组外，只与 quiet 冲突；`log_file` 独立。runtime 的 logger/handler gate 与精确 admission filter 保持 ordinary threshold 和 stream-debug 正交，quiet fail closed。
- 测试覆盖 16×16 有序 selector 冲突、跨 parser scope、fresh namespace、14 个合法 selector+debug-stream、4 个 quiet 冲突、15 个 debug-stream+log-file 组合、七个 runtime threshold、完整 ordinary/stream admission，以及真实 raw POSIX invalid byte、missing-parent 与重复 SIGINT owner side effects。
- README 已同步用户可见日志、资源错误和 prompt graceful cancellation 语义；残留“第二次 Ctrl+C 本地退出”只明确描述 interactive 既有行为，没有把旧 prompt 行为写回文档。
- 聚合独立验证：`pytest -q tests/cli/test_output.py tests/cli/test_prompt_command.py tests/cli/test_arg_parsing.py tests/cli/test_public_package_entrypoints.py tests/runtime/test_log.py tests/runtime/test_log_levels.py tests/cli/test_interactive_command.py` 为 `653 passed`；完整 `pyright` 为 `0 errors, 0 warnings, 0 informations`；`git diff --check a394f831..7b2c2c35` 通过。
- 冻结 registry 相对 base 未修改：`docs/cli_ci_oracles.json` blob 均为 `9a74b2feb7ca7a71302e76aa31e9c63796e10908`，`docs/cli_ci_scenarios.json` blob 均为 `f59e1daccd1c39b68a81a8ab05e01f617a80b6cb`。

## Open Questions

- 无。

## Residual Risk

- Startup 的 frozen exact timing 尚须 final replay。当前机器上的 installed-console 非冻结探针中，`dayu-cli --help` 在 0.05 秒发送 SIGINT 为 10/10 exit 130；本 reviewer 没有把它冒充 exact `PC-CN-01` prompt scenario。更早的非冻结对抗样本仍显示解释器前置窗口：同一 installed `dayu-cli prompt --base ... probe` 在 0.005 秒为 5/5 returncode `-2`，0.010 秒为 5/5 exit `1` 且出现 Python `init_import_site` traceback；这些信号发生在 Python site/console launcher 导入、即任何项目 bytecode 与 `run_module()` 捕获边界之前。accepted plan 已明确不新增 native launcher 或 interpreter-level owner，因此本 aggregate 不把该非冻结 pre-bytecode 窗口升级为当前 implementation finding；若后续要求把 oracle“从进程启动开始”解释为覆盖 OS 创建进程后的每个瞬间，则需单独裁决可拥有该阶段信号的分发/launcher owner，不能宣称当前纯 Python bootstrap 已覆盖。
- Repeated-SIGINT 的 frozen exact timing尚须 final replay。专项对抗探针使用生产 `execute_prompt_on_session`、真实 `CliSigintMonitor` 与 Host Protocol barrier double，精确在 canonical CANCELLED 已释放、monitor 已关闭、`HostSessionAttachment.aclose()` 已开始但未结束时发送第二个真实 SIGINT；结果 exit 130，但 attachment 只到 cancelled marker、没有 finished marker。该时序晚于冻结 P46/PC-BD-02 中紧邻第一次取消、发生于 Host terminal 前的第二次按键，且未使用真实 Host SQLite，因此不能据此声称 frozen scenario 失败、Run/attempt 仍 running或真实 DB/log 已泄漏。它保留为 post-terminal local cleanup adversarial risk；final evidence 必须至少证明冻结时序下 watcher/runtime/attachment/Host context/log 均完成清理。
- 本 review 不以 fake/owner tests 替代真实 evidence。final gate 仍需按 registry 精确重放 P44、P45、P46、P47、P62、PC-BD-02、PC-BD-03、PC-CN-01..07、PC-LS 32、PC-DS 15、PC-DQ 4、PC-LC 256，并核对 argv/按键时间线、screen、exit、filesystem、Host/runtime SQLite、EventLog/Tool Trace、Run/attempt terminal state；任何 exact frozen failure 都应重新打开 implementation fix loop。
