# Plan Review Re-check: PR 190 / F17

- **Reviewed target**: `docs/gateflow/pr-190-f17-plan-20260807.md`（修订后）
- **Review time**: 2026-08-07 14:42:53 +0800
- **Review mode**: 限定复审 —— 只核对 adjudication 中 accepted F17-P1/P2 闭合、F17-P3 未误落实、owner/scope 未漂移
- **Adjudication reference**: `docs/gateflow/pr-190-f17-plan-review-adjudication-20260807.md`
- **Goal confirmation**: `docs/gateflow/pr-190-f17-goal-confirmation-20260807.md`

## 复审范围

本复审不重新做完整 adversarial plan review，只验证 adjudication 的三个裁决是否被正确落实：

1. **F17-P1 (accepted)**：pre-state dirty check 是否加入了显式 stop condition
2. **F17-P2 (accepted)**：旧 digest 绑定校验方法是否明确为 JSON parser + 精确 path 定位
3. **F17-P3 (rejected-with-reason)**：是否被误落实（不应出现）
4. **Owner/scope drift**：修订后 plan 是否保持了 Goal Confirmation 的 owner chain 和 scope boundary

## F17-P1 闭合验证：pre-state dirty check 显式 stop condition

### Adjudication 要求

> 修订时必须允许本 work unit 已知的 Goal/Plan/Review/Adjudication artifacts，同时规定任何其它 dirty path 都立即停止；不得要求删除或覆盖既有 evidence。

### Plan 当前写法（第 5 节 Prerequisites，第 75-81 行）

```
- Prerequisites：当前分支非保护分支；implementation 开始前运行并核对 `git status --short`。pre-state dirty path 只允许当前 F17 的 Goal/Plan/Review/Adjudication evidence artifacts：
  - `docs/gateflow/pr-190-f17-goal-confirmation-20260807.md`
  - `docs/gateflow/pr-190-f17-plan-20260807.md`
  - `docs/reviews/plan-review-20260807-143241.md`
  - `docs/reviews/plan-review-20260807-143636.md`
  - `docs/gateflow/pr-190-f17-plan-review-adjudication-20260807.md`
  发现任何其它 tracked 或 untracked dirty path 时，必须在编辑产品/测试文件前立即停止；上述 evidence 只读，不得删除、重置或覆盖。
```

### Plan 当前写法（第 6 节步骤 1，第 90-92 行）

```
- 运行 `git status --short`，逐项核对 pre-existing paths。输出只允许第 5 节 Prerequisites 枚举的 5 个当前 F17 Goal/Plan/Review/Adjudication artifacts；任何其它 tracked 或 untracked dirty path 都是 implementation stop condition，必须立即停止并保留现场，不得先编辑再依赖 post-state guard 补救。
```

### 验证结论：✅ 闭合

- 5 个允许的 evidence artifact 已显式枚举，与当前 `git status --short` 输出一致（5 个 untracked Gateflow artifacts，零 tracked 修改）。
- 显式 stop condition："发现任何其它 tracked 或 untracked dirty path 时，必须在编辑产品/测试文件前立即停止"。
- Evidence 保护："上述 evidence 只读，不得删除、重置或覆盖"。
- 步骤 1 与 Prerequisites 的 stop condition 表述一致且互为补充，无矛盾。

## F17-P2 闭合验证：旧 digest 绑定校验方法明确化

### Adjudication 要求

> 应按精确 `path` 定位唯一 entry，再校验该 entry 的完整 `content_sha256`，并确认其余 42 entries 不使用旧值。raw substring/grep 不能作为 owner 绑定证明。

### Plan 当前写法（第 6 节步骤 2，第 103-106 行）

```
- 使用 JSON parser 解析 `docs/cli_init_workspace_manifest_v1.json`，在顶层 `files` 数组中按 `path == "config/prompts/scenes/conversation_compaction_user.md"` 精确筛选；必须得到唯一 entry，且其 `content_sha256` 必须完整等于 `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76`。
- 同一结构化检查必须确认 `files` 共 43 entries，排除目标后的其余 42 entries 各自完整的 `content_sha256` 值均不等于上述旧 digest。`grep`、raw substring、`str.count()` 或前后缀匹配只能辅助观察，不能作为 digest 与 owner path 的绑定证明。
```

### 验证结论：✅ 闭合

- 明确要求使用 JSON parser（非 raw text 工具）。
- 按精确 `path` 做相等匹配定位唯一 entry。
- 要求完整 `content_sha256` 值相等断言（非 substring/prefix 匹配）。
- 要求其余 42 entries 各自完整的 `content_sha256` 均不等于旧值。
- 显式声明 grep/raw substring/str.count()/前后缀匹配只能辅助观察，不能作为绑定证明。
- 第 7 节步骤 3（第 152 行）的独立 JSON 检查与此一致，形成双重约束。

## F17-P3 未误落实验证

### Adjudication 裁决

> `rejected-with-reason`：原 plan rollback 已明确"重算 raw digests"，且 `git diff` 会显示任何相对 HEAD 的额外 bytes 变化；finding 所称额外未提交 bytes 可能不出现在 diff 的前提不成立。现有精确反向 patch、raw digest 重算和 changed-file guard 已充分，不新增重复真源。

### F17-P3 finding 原文建议

> rollback 验证增加独立 raw digest check：manifest `read_bytes()` SHA-256 必须重新等于 `d95de6...a9f8`，测试文件中 `FROZEN_MANIFEST_SHA256` 必须重新等于 `d95de6...a9f8`。

### Plan 当前写法（第 9 节 Rollback，第 199-201 行）

```
2. 用精确反向 patch 仅恢复本 slice 创建的两个 digest hunk：manifest entry 回到 `59b50e...87b76`，测试 pin 回到 `d95de6...a9f8`；不得用覆盖整个文件的命令伤及并发或用户改动。
3. 重算 raw digests，运行 `git diff --check` 与 changed-file guard，确认回滚没有影响 pre-existing goal-confirmation / Gateflow artifacts。
```

### 验证结论：✅ 未误落实

- Rollback 步骤 3 的"重算 raw digests"是 plan 原文已有表述，不是 F17-P3 建议的新增独立 raw digest assert。
- Plan 未增加 F17-P3 建议的"manifest `read_bytes()` SHA-256 必须重新等于 `d95de6...a9f8`"等独立 digest 断言。
- 未引入"新增重复真源"——这与 adjudication 的 rejected-with-reason 理由一致。

## Owner/Scope Drift 验证

### Goal Confirmation owner chain 对照

| Goal Confirmation owner | Plan 第 3 节 | 漂移？ |
|---|---|---|
| 1. prompt raw bytes: 只读 | "本 work unit 只读" | ✅ 一致 |
| 2. production transaction: 不改 | "行为无误；本 work unit 不改" | ✅ 一致 |
| 3. manifest JSON: 唯一修复点 | "唯一 root-cause 修复点" | ✅ 一致 |
| 4. FROZEN_MANIFEST_SHA256: 二级派生 | "只能在第 3 步保存后由实际 raw bytes 推导更新" | ✅ 一致 |
| 5. validator: 只用于验证，不改 | "只用于验证，不改" | ✅ 一致 |

### Goal Confirmation scope boundary 对照

| Goal Confirmation 非目标 | Plan 对应约束 | 漂移？ |
|---|---|---|
| 不修改 prompt 内容、产品行为或 validation 逻辑 | 第 3 节 + 第 4 节明确禁止 | ✅ 一致 |
| 不修改目录、文件名、model projection owner、Host/Engine schema 或 public contract | 第 4 节："Schema：不变；manifest keys、结构、版本、数量和排序全部保持" | ✅ 一致 |
| 不修改 accepted Oracle、scenario registry 或 readiness | 第 4 节："Oracle / scenario / readiness：不变；三条 replacement scenarios 保持 `unadjudicated`" | ✅ 一致 |
| 不创建新 PR，不 merge/mark ready/approve/rebase/force-push | 第 1 节："停在 plan gate，不进入 plan review 或 implementation，不提交、不 push" | ✅ 一致 |
| changed product consumers 仅为 manifest 一个 entry 与测试 digest pin | 第 3 节："允许的产品/测试变更只有两个精确 hunk" | ✅ 一致 |

### Goal Confirmation 目标覆盖

| Goal Confirmation Goal | Plan 成功信号 / 验证 | 漂移？ |
|---|---|---|
| prompt raw digest、manifest entry、manifest raw digest 与测试 pin 严格闭合 | 第 1 节成功信号 (a)(c)(d) | ✅ 一致 |
| inventory 精确为 5/43/16 | 第 1 节成功信号 (b) | ✅ 一致 |
| 71 tests 全通过 | 第 1 节成功信号 (e) | ✅ 一致 |
| 证明不存在第二个 mismatch | 第 1 节成功信号 (a) `valid=True`、`issues=()` | ✅ 一致 |

### 新增内容审查

修订后的 plan 在第 5 节 Prerequisites 和第 6 节步骤 1/2 增加了 F17-P1/P2 的 fix 内容。这些新增内容：

- **F17-P1 fix**：显式枚举 5 个允许的 dirty path + 显式 stop condition。这是 implementation guard 的澄清，不扩张 scope，不改变 owner chain。
- **F17-P2 fix**：明确 JSON parser + 精确 path 匹配 + 完整 digest 断言 + 其余 42 entries 独立校验 + grep 降级为辅助。这是实施方法的澄清，不扩张 scope，不改变 owner chain。

两者均不构成 goal drift 或 scope creep。

## Open Questions

无。adjudication 的三个裁决均已正确落实，无遗留 ambiguity。

## Residual Risks

复审范围内的 residual risks 已在原 plan 第 9 节覆盖，本次修订未引入新风险：

- F17-P1 fix（显式 pre-state dirty stop）降低了"在脏工作树上编辑"的风险，正向改善。
- F17-P2 fix（JSON parser 精确校验）降低了"substring 匹配误判"的风险，正向改善。
- F17-P3 未误落实，避免了"新增重复真源"的风险。

## Plan Review Conclusion

**`pass`**

- F17-P1 ✅ 闭合：pre-state dirty check 已加入显式 stop condition，5 个允许 artifact 已枚举，stop 语义明确。
- F17-P2 ✅ 闭合：旧 digest 校验方法已明确为 JSON parser + 精确 path 定位 + 完整值断言 + 其余 42 entries 独立校验；grep/substring 降级为辅助。
- F17-P3 ✅ 未误落实：rollback 保持原 plan 的"重算 raw digests"表述，未新增 F17-P3 建议的独立 digest assert。
- Owner chain ✅ 未漂移：5 层 owner 与 Goal Confirmation 完全一致。
- Scope boundary ✅ 未漂移：所有 Goal Confirmation 非目标均被 plan 显式禁止，无新增目标、验收标准或 future-slice work。
