# Code Review（Re-review）

## Scope

- Mode: current changes（re-review，接续 `code-review-20260816-131113.md`，未 /clear）
- Branch: `codex/upload-filing-oracle`
- Base: `c8e75629`（UF-FIX08 accepted plan commit）
- Output file: `docs/reviews/code-review-20260816-132720.md`
- Review 时间：2026-08-16（本机系统时钟生成 timestamp）
- 复审对象：当前未提交 diff 相对上次 review（131113）的增量 + 既有 findings 闭环
- Review inputs:
  - `docs/reviews/code-review-20260816-130711.md`（CR-130711-001/002）
  - `docs/reviews/code-review-20260816-131113.md`（CR-131113-01 ~ 05）
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-slice1-code-review-adjudication-20260816.md`（Controller 裁决：全部 accepted）
- Included scope:
  - 增量生产代码：`repository_protocols.py`（read_filing_upload_state Raises 更新）、`source_integrity.py`（UNSAFE 显式 gate）、`_fs_filing_upload_state_core.py`（Raises 更新）
  - 增量测试：`tests/fins/test_fins_storage_atomicity.py`（2 个 damaged-state 零 mutation owner tests、FUTURE-member 四态 gate test、snapshot/publish helpers）、`tests/service/test_fins_direct.py`（fixture 改走 request identity 真源）
  - plan amendments：`docs/gateflow/uf-fix08-existing-source-auto-repair-plan-20260816.md`（42 行增量，Slice 1/2/3/4/5 + §8.1 affected-files）
  - implementation artifact 回写：`slice1-implementation-*.md` Validation/Code-review-fix 段
  - 证据复核：`_fs_identity.py:230-267`（descriptor 缺失的异常类别）、`_fs_storage_utils.py:425-470`（path-free 投影）
- Excluded scope: 上次 review 已覆盖且未变化的文件内容（`upload_repair_contract.py`、`tests/cli`、`tests/fins/test_fins_ingestion_runtime.py`、`__init__.py` 等增量未变）。
- Parallel review coverage: 无（单 reviewer）。
- 验证执行：focused 四文件 `637 passed, 3 warnings in 27.20s`（与 adjudication/artifact 数字一致）；全仓 pyright `0 errors, 0 warnings`；`git diff --check` PASS；`_fs_source_document_core.py` 仍严格 4 行（=2 处机械替换）。

## 证据复核前置说明：131113 的 CR-131113-02 证据失效

上一轮 review 用 `workspace/tmp/review-seam-probe/` 下的隔离 probe 得出"字面 raise NotImplementedError 不触发 reportAbstractUsage"的结论。本轮复核发现该 probe **从未被 pyright 分析**：`pyrightconfig.json` 的 `exclude` 含 `"workspace"`，被 exclude 的文件静默跳过，输出 `0 errors` 是"未分析"而非"无错误"。将同一 probe 移至排除目录之外（`/tmp/review-probe-outside/`）重跑：

- 字面 `raise NotImplementedError` 变体：`1 error - Cannot instantiate abstract class "ConcreteA" — "PA.reset" is not implemented (reportAbstractUsage)`；
- `NoReturn` helper 变体：`0 errors, 0 warnings, 0 informations`。

即 adjudication 的 A/B 实验机制被独立复现：**protocol 方法体字面 raise 会使 pyright 将其视为 abstract，全仓 130 个 concrete repository/fake 实例化点全部报 `reportAbstractUsage`；helper 是保持 runtime 确定性拒绝同时允许 concrete 类可实例化的必要设计，不是骗过 pyright 的胶水 seam**。CR-131113-02 的结论被本轮证据推翻，详见下文闭环裁决。

## Findings

### 既有 finding 闭环裁决

| finding | 裁决 | 直接证据（本轮复核） |
| --- | --- | --- |
| CR-130711-001（rejection helper 非典型 Protocol 设计） | **已修复/已裁决闭环** | adjudication 按 Controller 要求完成全仓 A/B：字面 raise → `130 errors` 全部 `reportAbstractUsage`（首 `cn_pipeline.py:392:56`，末 `tests/tools/test_combined_tools_acceptance.py:890:25`）；恢复 helper 后全仓 pyright 0 errors。本轮在排除目录外独立复现同一机制，证据成立。Slice 4 删除 helper/body 已写入 plan（见 plan amendments 核对） |
| CR-130711-002（Service/CLI fixture 跨层依赖） | **已修复** | `tests/service/test_fins_direct.py` 当前 diff：新增 `import dayu.fins.ingestion_runtime as ingestion_runtime`，fixture 改 `canonical_ticker, filing_document_id = ingestion_runtime._filing_upload_request_identity(raw_filing_request)`，`build_sec_filing_ids`/docling pipeline import 已删除（diff 中无该 import）；CLI 的 docling import 为 base 已有依赖，本 slice 未新增 |
| CR-131113-01（Slice 4 缺 production facade 接管路径） | **已修复（plan 层面）** | plan diff 三处：§8.1 production affected-files 表新增 `fs_source_document_repository.py` 行（"Slice 4删除protocol transitional rejection后，由production facade显式delegate真实core repair owner"）；Slice 4 allowed files 新增该文件；Slice 4 新增 step 4（删除 transitional body 与模块级 helper、protocol 恢复纯 method contract、facade 显式 override 只 delegate 共享 core 不复制判断）、step 8（真实 facade owner test + 移除/迁移 Slice 1 rejection test）、完成条件追加"transitional rejection/helper 已删除，production facade调用精确进入共享core repair owner"。实现属 Slice 4，本 slice 无越界实现 |
| CR-131113-02（129 错误叙事缺可复现证据——本 reviewer 上轮 finding） | **证据失效** | 上轮隔离 probe 被 `pyrightconfig.json` 的 `workspace` exclude 静默跳过（0 errors 系"未分析"）。排除目录外重跑：字面 raise → `reportAbstractUsage` 精确复现；helper → 0 errors。adjudication 的全仓 130 条 A/B（含首尾错误定位与 129→130 差异来源：新增 rejection test 实例化点）已在 implementation artifact "Validation" 段回写精确命令与证据。该 finding 的上轮结论（"论证不可复现、helper 必要性存疑"）被推翻，撤回 |
| CR-131113-03（damaged upload-state read 异常面与 owner tests 未闭合） | **已修复** | (1) protocol 与 mixin 的 `read_filing_upload_state` Raises 已更新：`ValueError: ... 或 published source directory、meta、identity descriptor 结构损坏（含 required meta/descriptor 缺失）`；`OSError: identity descriptor、meta 或其它 published state operational 读取失败时抛出 path-free 文件系统异常`。(2) 两个真实 filesystem owner tests：`test_filing_upload_state_fails_closed_for_missing_source_meta_without_mutation`（删除 meta.json → `ValueError("缺少 meta.json")`，断言 `str(workspace_root)`/`str(source_directory)` 不在异常、tree 快照 bytes 完全一致）与 `test_filing_upload_state_fails_closed_for_missing_identity_descriptor_without_mutation`（删除 descriptor → `ValueError("identity descriptor")`，同零 mutation 断言）。(3) 行为链复核：`_read_identity_descriptor`（`_fs_identity.py:230-267`）descriptor 缺失走 `descriptor_path.is_file()` False → `ValueError("identity descriptor 必须为 non-symlink regular file")`，operational 读取失败经 `_raise_path_free_error` 抛 path-free OSError——与更新后的 docstring 边界一致。(4) plan Slice 2/3/5 已补 typed 收敛与 failure boundary（见下） |
| CR-131113-04（classification 末分支缺显式 UNSAFE gate） | **已修复** | `source_integrity.py` 当前 `__post_init__`：REPAIR_REQUIRED 分支 return 后新增 `if self.status is not SourceIntegrityStatus.UNSAFE: raise ValueError("status 必须是封闭四态")`，再执行 UNSAFE 不变量；`test_source_integrity_classification_rejects_future_status_member` monkeypatch 等形含 `FUTURE` member 的 enum，断言 FUTURE 命中显式四态 gate（match "封闭四态"）而非静默继承 UNSAFE 规则 |
| CR-131113-05（Service fixture 绕过 request identity owner） | **已修复** | 与 CR-130711-002 同源同证据：fixture 从 raw request 调 `ingestion_runtime._filing_upload_request_identity` 取 canonical ticker/document ID，删除跨 pipeline 推导；classification 的 ticker/document_id 与 request identity 精确同源 |

### accepted plan amendments 核对

| amendment | plan 位置（当前工作区） | 核对结论 |
| --- | --- | --- |
| Slice 1 step 3：damaged required meta/descriptor 缺失必须 path-free fail closed，禁止静默视为 MISSING；owner tests 断言零 mutation | Slice 1 步骤 3 增补 | 已落地：docstring 承诺 + 2 个零 mutation tests + 行为链核对一致 |
| Slice 2：damaged state typed 收敛 | Slice 2 step 1 与完成条件增补 | 正确：既有 target 的 required meta/identity descriptor 缺失或损坏归属 `UNSAFE/revision=None` typed inspection，不再作为 `MISSING`、不得抛 raw structural 异常旁路 classification；且明确 Slice 2 不得提前修改 state core/validator（与 Slice 1 step 3 的"Slice 3 机械替换"表述一致，sequencing 自洽） |
| Slice 3：state core 机械消费 inspector payload，damaged → `UNSAFE + source_meta=None` typed state | Slice 3 step 1 增补 | 正确：临时 classifier seam 的移除点、typed state 形状（`source_integrity=UNSAFE/revision=None + source_meta=None`）与 `FilingUploadPublishedState.__post_init__` 已冻结的 UNSAFE→meta=None 不变量完全对齐 |
| Slice 4：protocol helper 删除 + production facade 接管 | §8.1 affected-files 表、Slice 4 allowed files/step 4/8/完成条件 | 正确且闭合：删除 transitional body/helper、protocol 恢复纯 contract、facade 显式 override 机械 delegate 共享 core（禁止复制 status/revision/reason 判断）、真实 facade owner test、移除/迁移 Slice 1 rejection test。无胶水 seam 永久化路径 |
| Slice 5：fresh re-read failure boundary | Slice 5 step 5 增补 | 正确：fresh `read_filing_upload_state()` 与 fresh validator 放入同一显式 failure boundary，path-free `ValueError/FileNotFoundError` 与 `FinsUploadPrevalidationError` 一并经 upload-failure owner 收敛为唯一 typed failed event，不得从 async stream 冒 raw exception、不得在 workflow 重新推断 integrity |

### 本轮新增 finding

**未发现新的 blocking correctness/ownership/scope finding。**

本轮对增量代码（Raises 更新、UNSAFE gate、3 个新测试、fixture 迁移、plan amendments）做完 adversarial pass 后的结论：

- 四态/reason 封闭性、UNSAFE fail-closed、state/meta 同源、guard 内读取、rejection 语义均保持上轮已核实的正确状态，本轮未引入回归；
- 新测试均为真实 filesystem fixture（`_publish_filing_upload_state_source` 走真实 batch commit），断言 owner 级行为而非适配实现的弱断言；
- scope 无越界：`_fs_source_document_core.py` 仍严格 2 处机械替换（4 行 diff）；修改文件集与 Slice 1 allowed files 一致；新增 docs 均为 gateflow/review 流程产物；
- fixture 迁移消除跨层 pipeline 依赖后，Service/CLI 测试与 validator 行为（本 slice 尚不消费 `source_integrity`）之间不存在新增耦合。

两个不构成 finding 的 residual nits（记录不裁决）：

- `_FilingUploadStateCoreProtocol._source_meta_path_for_read`（私有 typing-only protocol，`_fs_filing_upload_state_core.py:68-89`）的 Raises 仍写 "ValueError: identity 或 source kind 非法"+"OSError: identity descriptor 读取失败"，未列 descriptor 结构缺失的 ValueError 面；该语义承诺以公开的 `FilingUploadStateRepositoryProtocol` 与 concrete 方法 docstring（均已更新）为准，私有 protocol 属投影滞后，建议 Slice 3 机械替换时顺带对齐。
- 两个 damaged-state 测试的 path-free 断言检查 `str(exc)` 而非 `repr`/`__cause__`/`__notes__`；当前生产投影机制（`_raise_path_free_error` 清理 context）与 classifier 的 path-free message 使断言成立，但该断言不能防御未来异常链中混入 locator 的回归。属测试强度边际项。

## Open Questions

- 无（上轮 3 个 open questions 已被 adjudication 与 plan amendments 全部闭合）。

## Residual Risk

- 与 adjudication residual 一致：Slice 1 damaged-state read 仍是 path-free structural exception fail-closed，`UNSAFE` typed state 属 Slice 2/3；transitional helper 与 facade 接管属 Slice 4；SEC/CN/HK typed event 收敛属 Slice 5；README 属 Slice 6。以上均有 plan amendment + owner 记录，无未分类 residual。
- 未运行 UF-PF08/UF-PF12、真实 CLI/provider/converter evidence（与本 slice 声明一致）。
- `/tmp/review-probe-outside/` 探针为本次复核的临时证据文件，位于仓库外，不进入 diff。

## 结论

- **Pass**。既有 6 项 findings 全部闭环：5 项已修复、1 项（CR-131113-02，本 reviewer 上轮 finding）证据失效并撤回——其机制被本轮排除目录外的复现实验与 adjudication 的全仓 A/B 双重证实，`_reject_unimplemented_source_repair()` 是保持 concrete 类可实例化的必要 transitional 设计，且 Slice 4 已计划删除，无胶水 seam 永久化。
- accepted plan amendments（Slice 2 damaged typed 收敛、Slice 4 helper 删除 + facade 接管、Slice 5 fresh re-read failure boundary）均已在 plan 文本落地并与 Slice 1 已冻结 contract 自洽。
- 当前 slice 无新 blocking correctness/ownership/scope finding。
- 验证复现：focused 四文件 637 passed；全仓 pyright 0 errors；`git diff --check` PASS。
- 未修改任何代码、未提交。Slice 1 可进入下一 gate（Slice 2）。
