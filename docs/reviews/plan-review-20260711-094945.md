# WU-SEMANTIC-OWNERSHIP-01 P3-J Fixed Plan Re-Review — AgentMiMo

## Review Target

- Artifact: `docs/host/wu-semantic-ownership-01-p3-j-durable-schema-weak-contract-plan.md` (fixed version)
- Work unit: `WU-SEMANTIC-OWNERSHIP-01 / P3-J - Host durable schema and weak-contract hardening backlog`
- Reviewer: AgentMiMo
- Review date: 2026-07-11
- Review type: Re-review of controller-accepted plan fixes (PF-01..PF-06)
- Plan fix artifact: `docs/reviews/wu-semantic-ownership-01-p3-j-plan-fix-codex.md`
- Controller adjudication: `docs/reviews/wu-semantic-ownership-01-p3-j-plan-review-controller-adjudication.md`

## Review Scope

This re-review verifies that all 6 controller-accepted plan fixes (P3-J-PF-01 through PF-06) are properly closed in the fixed plan, and reports any new material plan findings.

**Not in scope**: Re-litigating source finding dispositions (already validated by both original reviewers), re-reviewing non-accepted observations, or proposing new plan directions.

## PF-01 Verification — Reduce event_type blast radius and split S1

**Controller requirement**: Split S1 into smaller semantic closures. EventLog event type append/row-decoder/fresh-schema closure as its own slice. Queue policy + RunResult terminal-status as separate slice. Consumer-wide redirection must not be required in the first event-type slice unless needed for invalid write rejection.

**Fixed plan evidence**:

- §5: 4 implementation slices: S1 EventLog Event Type, S2 Queue Policy + RunResult, S3 Idempotency + Descriptor Kind, S4 Legacy Config
- §5 S1 Purpose: "Create a code-generation-ready Host EventLog event-type legal set while preserving lifecycle categories."
- §5 S1 Non-goals: "No queue policy change, no RunResult terminal-status change, no idempotency change, no descriptor-kind change. No consumer-wide redirection of every local event-type comparison. Redirect only code that must compile or pass typed append / row-decoder validation."
- §5 S2 Purpose: "Make `queue_policy` a single Host-owned typed contract with fresh-schema DDL rejection. Delete `AdmissionPolicy` rather than aliasing it. Make `RunResultRow.terminal_status` typed at the durable read-model row boundary."
- §5: "This intentionally exceeds the small-cleanup default upper bound of 3 because controller-accepted finding P3-J-PF-01 proves the former S1 coupled three different owner closures"

**Verdict: FIXED** ✓

S1 is correctly narrowed to EventLog event type append/decoder/fresh-schema closure. Consumer-wide redirection is explicitly non-goal. Queue policy and RunResult moved to S2. The 4-slice justification is documented.

## PF-02 Verification — Make event type owner and legal set code-generation-ready

**Controller requirement**: Define the chosen event type owner structure at plan level. Pre-enumerate current production event type baseline. Add test-fixture migration strategy. Specify implementation ordering.

**Fixed plan evidence**:

- §3.1: "keep `HostRunEventType` and `HostAttemptEventType` as the run / attempt lifecycle categories; add owner-owned category enums for admission / command request events, tool / wait events, context governance events, runner-input events, engine diagnostic events, and preview events"
- §3.1: "add aggregate helpers such as `parse_host_event_type(value: str) -> HostEventType`, `serialize_host_event_type(value: HostEventType) -> str`, `all_host_event_type_values() -> tuple[str, ...]`"
- §3.8.1: Complete production baseline table with 8 categories and 35+ values, grouped by lifecycle category
- §3.8.1: 3 exact source-scan `rg` commands with instructions to "complete the exact source-scan table before editing code"
- §3.8.1: Test fixture migration order: 4 steps — (1) replace arbitrary fixtures before DDL, (2) keep only invalid-rejection test values renamed to `INVALID_TEST_EVENT_TYPE`, (3) add owner parser/serializer + validation + DDL CHECK, (4) add invalid append and mutated-row decoder tests
- §3.8.1: Known fixture hotspots table (TYPE_A, host.test, TEST_EVENT, lowercase dotted) with migration direction for each

**Verdict: FIXED** ✓

Owner structure is category-preserving (not flat enum). Production baseline is pre-enumerated. Fixture migration strategy and ordering are explicit and code-generation-ready.

## PF-03 Verification — Remove queue_policy alias ambiguity and define the single owner

**Controller requirement**: Delete the alias option. State the single queue policy owner explicitly. Require deleting `AdmissionPolicy` if no external production consumers. Add source scan validation.

**Fixed plan evidence**:

- §3.2: "new `dayu/host/queue_policy.py` Host contract owner with `RunQueuePolicy` and parse / serialize helpers. `AdmissionPolicy` is not the owner and must be deleted if source scan confirms it has no external production consumers."
- §3.8.2: "Delete `AdmissionPolicy`; do not alias, re-export, or keep a facade."
- §3.8.2: Residual scans:
  ```
  rg -n 'AdmissionPolicy' dayu tests
  rg -n 'RunQueuePolicy\s*=|AdmissionPolicy\s*=|from dayu\.host\.admission import AdmissionPolicy' dayu tests
  ```
  "Both scans must prove there is no production `AdmissionPolicy` reference and no compatibility re-export."

**Code verification**: `grep -rn 'AdmissionPolicy' dayu/host/ --include="*.py" | grep -v test` confirms `AdmissionPolicy` is only used in `admission.py` (definition + 9 references). No external consumers in `durable/` or `api.py`. Deletion is safe.

**Verdict: FIXED** ✓

Alias option is completely removed. Single owner is `RunQueuePolicy` in new `queue_policy.py`. Deletion of `AdmissionPolicy` is required with source scan proof.

## PF-04 Verification — Resolve descriptor kind diagnostic ownership and consumer fail-closed boundary

**Controller requirement**: List current descriptor kind baseline including diagnostic kinds. Assign each kind to an owner. Clarify producer-side vs consumer expected-kind validation. Remove broad "payload_resolution fails closed on unknown" if it makes consumer a second global owner.

**Fixed plan evidence**:

- §3.8.4: Complete descriptor kind baseline table with 7 values, each with owner/producer:
  - `tool_call_arguments_json` → ToolRuntime
  - `tool_call_semantic_query_text` → ToolRuntime
  - `runner_call_input_manifest` → RunInputBuilder / Engine ingest / compaction
  - `runner_call_input_projection` → RunInputBuilder / Engine ingest
  - `selected_tool_schema_snapshot` → RunInputBuilder
  - `compactor_input_projection` → Compaction operation
  - `compaction_rejected_attempt_diagnostic` → Compaction operation
- §3.5: "`compaction_rejected_attempt_diagnostic` is included in the descriptor kind legal set as a compaction diagnostic descriptor owned by `dayu/host/compaction_operation.py` as producer and the descriptor-kind owner as value contract. It is not excluded as ad hoc metadata."
- §3.5: "`payload_resolution` validates the caller's expected descriptor kind by parsing that expected kind and then checking missing / mismatched descriptor metadata; it must not become a second global unknown-kind owner for unrelated descriptors."
- §3.8.4: "Payload metadata producer helpers must write descriptor kind through the owner and reject unknown descriptor kinds before write." (producer-side)
- §3.8.4: "`payload_resolution` parses the expected descriptor kind and fails closed on missing or mismatched descriptor metadata. Unknown descriptor rejection for all descriptors belongs to producer-side validation, not this consumer." (consumer-side)

**Code verification**: `grep -rn 'compaction_rejected_attempt_diagnostic' dayu/host/ --include="*.py"` confirms it is used in `compaction_operation.py` (definition + usage), `dispatch.py`, and `engine_ingest.py`. It is a real production descriptor kind with a clear producer.

**Verdict: FIXED** ✓

Diagnostic descriptor kind is assigned to compaction operation as producer. Baseline is complete. Producer-side unknown rejection is separated from consumer expected-kind validation. `payload_resolution` is no longer a second global owner.

## PF-05 Verification — Decide idempotency DDL CHECK strategy before implementation

**Controller requirement**: State whether S2 adds Python-level typed validation only or also adds DDL checks. If DDL checks omitted, explicitly record that decision and reason. Adjust tests accordingly.

**Fixed plan evidence**:

- §3.4: "P3-J deliberately does not add fresh-schema DDL `CHECK` constraints for `idempotency_records.scope_kind` or `idempotency_records.result_kind`. Current direct evidence shows the values are Host operation-driven and extend when new Host commands or wait/tool operations are added; a DDL closed set would turn ordinary command extension into schema migration work without proving a stronger durable invariant."
- §3.4 Propagation requirements: "no P3-J DDL `CHECK`; Python-level typed validation and row decoder fail-closed behavior are the owner boundary."
- §3.8.3: "P3-J chooses Python-level typed validation only and intentionally omits DDL `CHECK` for idempotency scope / result kind."
- §3.8.3: "Reason: current values are Host operation-driven and extend with new Host commands, wait operations, or tool operations; making them DDL-closed would turn ordinary Host command extension into schema migration work without proving a stronger durable invariant."
- §3.8.3 Tests must assert: "fresh schema still has no `scope_kind IN (...)` or `result_kind IN (...)` DDL `CHECK`" and "adding a new Host command kind requires updating the Python owner and its tests, not schema DDL."

**Code verification**: `grep -rn 'scope_kind\|result_kind' dayu/host/durable/schema.py` confirms `scope_kind TEXT NOT NULL` and `result_kind TEXT NOT NULL` with no CHECK constraints. The plan's claim is consistent with current code.

**Verdict: FIXED** ✓

DDL CHECK strategy is explicitly decided: Python-level typed validation only, no DDL CHECK. Reason is recorded (operation-driven extensibility). Tests must assert intentional DDL omission.

## PF-06 Verification — Clarify admission.py sequencing across S1/S2

**Controller requirement**: Assign `admission.py` changes by semantic owner per slice. State that one slice must not preemptively refactor constants owned by a later slice.

**Fixed plan evidence**:

- S1 allowed files: `admission.py` listed as "Production append-path modules only when needed to pass typed values into `EventLogAppendRequest`"
- S1 Non-goals: "No idempotency change, no descriptor-kind change."
- S2 Non-goals: "No idempotency `_OPERATION_*` or result-kind refactor in `admission.py`; those belong to S3."
- S3 Non-goals: "No S2 queue-policy or terminal-status refactor."
- S3 Purpose: "Keep `admission.py` sequencing clean: only idempotency constants move here, never S2 queue-policy owner work."

**Verdict: FIXED** ✓

admission.py changes are cleanly assigned per slice by semantic owner. Cross-slice preemption is explicitly forbidden in each slice's non-goals.

## New Material Finding Check

After verifying all 6 PF fixes, I checked for new material findings by stress-testing the fixed plan against the review lenses:

### Architecture Boundary Review

- S1 event type owner stays within Host layer (`lifecycle_events.py`). ✓
- S2 queue policy owner is in new `queue_policy.py` within Host. ✓
- S3 idempotency/descriptor changes stay within Host durable modules. ✓
- S4 legacy config removal stays within runtime/CLI boundary. ✓
- No reverse dependencies introduced. ✓

### Best-Practice Review

- DDL CHECK is used as defense-in-depth (S1 event type, S2 queue policy), not as sole validation. ✓
- Python-level typed validation is chosen for extensible sets (idempotency). ✓
- Row decoder fail-closed behavior preserved. ✓
- Stop conditions are slice-local and do not defer plan decisions to implementation. ✓

### Optimal-Solution Review

- 4 slices are justified by separate owner boundaries and rollback surfaces. ✓
- S1 narrowed to append/decoder/schema (not consumer-wide redirect). ✓
- `AdmissionPolicy` deletion (not alias). ✓
- `execution_target` correctly deferred (not a P3-J closed set). ✓

### Overengineering Review

- No broad whole-store migration. ✓
- No generic ORM or HostRow replacement. ✓
- No typed wrapper for every payload metadata key. ✓
- No idempotency DDL CHECK (correctly identified as overengineering for extensible set). ✓

### Overcoupling Review

- S1 touches `admission.py` only for event-type append validation (narrow). ✓
- S2 touches `admission.py` for queue policy (not idempotency). ✓
- S3 touches `admission.py` for idempotency (not queue policy). ✓
- No cross-slice coupling detected. ✓

**No new material findings identified.**

## Residual Risks

1. **S1 fixture migration volume**: The known fixture hotspots table lists 4 categories of arbitrary test event types across 10+ test files. Implementation agent must do careful per-file migration. Risk: low (well-documented migration directions).

2. **S2 AdmissionPolicy deletion verification**: Source scan must confirm no external consumers. Current code evidence shows `AdmissionPolicy` is admission.py-internal only. Risk: low (code already verified).

3. **S3 descriptor kind completeness**: If implementation scan finds a descriptor kind outside the 7-value baseline, stop condition applies. Risk: low (baseline appears complete from current scan).

4. **4 slices vs control-doc budget**: Plan explicitly justifies exceeding the 1-3 slice default. Risk: none (justification is sound).

## Blocking Open Questions

None. All PF fixes are closed. No new blocking questions identified.

## Conclusion

**pass**

All 6 controller-accepted plan fixes (P3-J-PF-01 through PF-06) are properly closed in the fixed plan:

| PF | Status | Key Evidence |
|---|---|---|
| PF-01 | ✓ Fixed | S1 narrowed to EventLog append/decoder/schema; S2 handles queue policy + terminal status; consumer-wide redirect is non-goal |
| PF-02 | ✓ Fixed | Category-preserving owner structure; production baseline pre-enumerated; fixture migration order explicit; scan rules specified |
| PF-03 | ✓ Fixed | `AdmissionPolicy` deletion required; no alias/re-export; residual scans specified |
| PF-04 | ✓ Fixed | 7-value baseline complete with owners; diagnostic kind assigned; producer-side unknown rejection separated from consumer expected-kind validation |
| PF-05 | ✓ Fixed | Python-level typed validation only; no DDL CHECK; reason recorded; tests assert intentional omission |
| PF-06 | ✓ Fixed | admission.py changes assigned per slice by semantic owner; cross-slice preemption forbidden |

New material findings: 0
Blocking open questions: 0

The plan is code-generation-ready and safe to enter implementation gate.
