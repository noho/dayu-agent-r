# Code Review

## Scope

- Mode: current changes
- Branch: wu-cm-12-conversation-memory-drift
- Base: HEAD (uncommitted workspace diff only)
- Output file: docs/reviews/code-review-20260618-201451.md
- Review type: Focused re-review of PR review fix for accepted controller findings
- Controller accepted findings under review:
  - A: `_load_context_fallback_tx` 中 `current_input_ref` 缺失应先于 mismatch 诊断
  - B: `build_recent_window_fallback_selection` 需 item cap、char cap、`memory_policy=None` 三个边界测试
- Included scope:
  - `dayu/host/context_fallback.py` (uncommitted diff: diagnostic reorder)
  - `tests/host/test_run_input_builder.py` (uncommitted diff: 3 boundary tests + fixture extension)
  - `docs/reviews/wu-cm-12-pr-review-fix-codex-20260618.md` (Codex fix gate document as truth reference)
- Background artifacts: `docs/reviews/pr-150-review-20260618-195915.md`, `docs/reviews/pr-150-review-20260618-200404.md`
- Design truth: `docs/host/design.md`, `docs/engine/design.md`
- Control doc: `docs/host/issues-implementation-control.md`
- Excluded scope: All committed PR #150 changes, deferred/rejected controller findings, CI configuration
- Parallel review coverage: 无

## Goal

判断两个 controller accepted findings 是否完全关闭，以及本轮 fix 是否引入新 correctness、typing、测试、AGENTS.md 或过度设计问题。

## First-Principles Verification

### 动机判断

Controller accepted findings 动机成立。Finding A（诊断顺序）是日志/排障可观测性缺陷，当前 fail closed 不变仅改善错误消息准确性。Finding B（边界测试缺失）是回归保护缺口，`_fallback_caps_allow_append` 作为 fallback selected window caps 唯一执行点缺少独立回归测试。两项均有明确证据支撑，严重性评估合理。

### 路径判断

Codex fix gate 选择的两项修复路径均为最小化修改：A 仅重排两行检查顺序，B 仅通过既有 public helper `build_recent_window_fallback_selection` 增加三个 focused tests。未引入新抽象、新契约或新模块。路径合理，无过度设计。

## Findings

### 逐个 Finding 关闭验证

#### Accepted Finding A: fallback current_input_ref 诊断分支应先于 mismatch

- **入口/函数**: `_load_context_fallback_tx`
- **文件(行号)**: `dayu/host/context_fallback.py:373-378`
- **修复检查**:
  1. 新代码第 374-375 行：`if window_current_ref is None: raise HostDurableError("fallback current_input_ref is missing")` — 先检查缺失 ✅
  2. 新代码第 376-377 行：`if window_current_ref != current_input_ref: raise HostDurableError("fallback current_input_ref mismatch")` — 后检查 mismatch ✅
  3. 第 378 行 `actual_current_input_ref = window_current_ref` 在两次检查通过后赋值，语义正确 ✅
  4. 旧不可达代码（原第 376-378 行）已完全移除，无可达性问题 ✅
- **测试覆盖验证**:
  1. `test_eventlog_context_fallback_provider_fail_closes_on_payload_drift` 新增 `("missing_current_ref", "fallback current_input_ref is missing")` 参数化 case ✅
  2. `_context_fallback_failed_payload("missing_current_ref")` 在第 3285-3286 行 `del window["current_input_ref"]`，随后第 3304 行 `fallback_window_digest(window)` 对**删除字段后的 window** 重新计算 digest，确保 digest 校验通过后命中 missing 分支 ✅
  3. `current_ref_mismatch` case 仍存在且不受影响 ✅
- **结论**: Finding A **完全关闭**。

#### Accepted Finding B: fallback selected window caps 需要 item cap、char cap、memory_policy=None 三个明确边界测试

- **入口/函数**: `build_recent_window_fallback_selection` → `_fallback_caps_allow_append`
- **文件(行号)**: `tests/host/test_run_input_builder.py:1261-1453`
- **修复检查**:

  **测试 1: `test_recent_window_fallback_item_cap_rejects_append` (line 1261)**
  - 通过 `_memory_policy(fallback_selected_recent_window_item_cap=2)` 设置 item cap=2，char cap=4096（非限制因素）
  - 4 blocks：old:user(seq=1), mid:user(seq=2), new:user(seq=3), current:event-current(seq=4)
  - 预期：current 必须保留（required），new:user 被 item cap 允许（1+1=2 <= 2），mid:user 被 item cap 拒绝（2+1=3 > 2）→ selected=("new:user", "current:event-current"), blocked="mid:user"
  - 断言验证通过 ✅

  **测试 2: `test_recent_window_fallback_char_cap_rejects_append` (line 1326)**
  - 通过 `_memory_policy(fallback_selected_recent_window_item_cap=8, fallback_selected_recent_window_char_cap=10)` 设置 item cap 不限制（8），char cap=10
  - 各块 size_units：current("current")=7, new("new")=3, mid("mid")=3
  - 预期：current(7) + new(3)=10 <= 10 → new 被允许；current(7)+new(3)+mid(3)=13 > 10 → mid 被拒绝
  - 断言验证通过 ✅

  **测试 3: `test_recent_window_fallback_without_memory_policy_allows_uncapped_append` (line 1391)**
  - 传入 `memory_policy=None`，模拟旧调用点无 caps 场景
  - 预期：所有 4 个 blocks 均被选择，`blocked_next_block_id is None`
  - 断言验证通过 ✅

- **辅助函数变更**: `_memory_policy` (line 3034) 新增 `fallback_selected_recent_window_item_cap: int = 4` 参数，原硬编码值 4 改为参数化。已有调用点不传该参数时默认 4（原行为不变），新测试可显式控制 item cap。变更最小化，无回归风险 ✅

- **结论**: Finding B **完全关闭**。

### 新引入问题检查

#### Correctness

- 诊断顺序变更不改变 fail closed 语义，只改变错误消息文本；两种错误都是 `HostDurableError`，调用方已有的 `pytest.raises(HostDurableError, match=...)` 模式兼容 ✅
- 新测试的 fixture 在删除 `current_input_ref` 后重新计算 `fallback_window_digest(window)`（第 3304 行），确保 digest 校验通过后正确命中 missing 分支而非 digest mismatch。链路完整 ✅
- 新边界测试通过 public API `build_recent_window_fallback_selection` 而非内部 `_fallback_caps_allow_append` 测试，覆盖完整调用链（caps → hard budget check → selection assembly），测试不绑定实现细节 ✅
- 无新增 correctness 问题。

#### Typing

- `_memory_policy` 新增参数 `fallback_selected_recent_window_item_cap: int = 4`，类型注解完整 ✅
- 三个新测试函数均无类型注解问题（测试函数在 pytest 中惯例不注解返回值），函数体无类型敏感操作 ✅
- `pyright dayu/host/context_fallback.py tests/host/test_run_input_builder.py` → `0 errors, 0 warnings, 0 informations` ✅
- 无新增 typing 问题。

#### 测试

- 新增 3 个边界测试 + 1 个参数化 fixture case，均通过公共 API 测试
- `pytest tests/host/test_run_input_builder.py -q` → `80 passed in 0.69s`，无 regression ✅
- 新测试各自验证一个独立边界（item cap 拒绝、char cap 拒绝、无 caps 全部通过），断言覆盖 `selected_block_ids`、`blocked_next_block_id`、`dropped_block_ids` ✅
- 无新增测试问题。

#### AGENTS.md / CLAUDE.md 合规

- 诊断重排不涉及 LLM-facing 文本变更，不触发 Agent 语义约束 ✅
- 新测试函数使用中文 docstring，符合编码规范 ✅
- 不影响分层架构、不引入新模块、不新增公共接口 ✅
- 无违反 AGENTS.md / CLAUDE.md 项。

#### 过度设计

- 修复范围严格限定在两个 accepted findings，未扩散到 deferred / rejected findings ✅
- 未引入新抽象、新模块、新配置项或新契约 ✅
- 测试 helper `_memory_policy` 的参数化是现有测试基础设施的自然扩展，不构成新设计 ✅
- 无过度设计问题。

## Conclusion

**PASS**

两个 controller accepted findings 均已完全关闭。本轮 fix 未引入任何新 correctness、typing、测试、AGENTS.md 或过度设计问题。诊断重排和边界测试均为最小化、证据充分的修改。

## Open Questions

无。

## Residual Risk

- 本轮未处理的 deferred / rejected findings（`compaction_evidence.py` dead code cleanup、`_vnext_compact_candidate_semantic_lines` 防御深度不对称、recovery tier rejected attempts 诊断 gap）仍记录在 `docs/reviews/wu-cm-12-pr-review-fix-codex-20260618.md` residual risks 中，待后续 gate/phase 处理。当前不阻塞 merge。
