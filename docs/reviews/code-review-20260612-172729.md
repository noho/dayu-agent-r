# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: `main`
- Output file: `docs/reviews/code-review-20260612-172729.md`
- Included scope:
  - WU-CM-08 plan commit `fce2fca0` (plan artifact)
  - WU-CM-08-S1 accepted slice commit `bd3515d1` (`tests/host/test_compact_material.py`)
  - WU-CM-08-S2 accepted slice commit `5cb68505` (`tests/host/test_public_compact_smoke.py`)
  - Uncommitted control-doc gate update (`docs/host/issues-implementation-control.md`)
  - Cross-reference: `docs/host/design.md`, `docs/engine/design.md`, `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md`
- Excluded scope:
  - WU-CM-05 / WU-CM-06 production changes (`dayu/host/_terminal_answer.py`, `dayu/host/llm_compaction.py`, `dayu/host/terminal_summary_payload.py`) — these belong to earlier work units already deepreviewed and accepted; the `main...HEAD` diff includes them only because the branch carries multiple work units. Only test changes in `bd3515d1` and `5cb68505` are in WU-CM-08 scope.
  - Earlier deepreview artifacts in `docs/reviews/` — already reviewed and accepted.
- Parallel review coverage: 无

## Verification

| Check | Result |
|---|---|
| `pytest tests/host/test_compact_material.py tests/host/test_public_compact_smoke.py tests/host/test_llm_compaction.py tests/host/test_terminal_summary_payload.py tests/host/test_read_api_terminal_policy.py tests/host/test_memory_projection.py tests/host/test_engine_ingest_mapping.py -q` | 178 passed, 1 skipped |
| `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings |
| `git diff --check` | clean |
| `git log --oneline fce2fca0..HEAD -- dayu/host/` | 无输出 — WU-CM-08 未修改任何生产文件 |

## Findings

### F01-未修复-低-`_assert_label_only_fake_proposal` 硬编码单 fact 假设

- **入口/函数**: `_assert_label_only_fake_proposal` (test_public_compact_smoke.py:963-994)
- **文件(行号)**: `tests/host/test_public_compact_smoke.py:974-982`
- **输入场景**: 未来 smoke test 需要构造包含 0 个或多个 evidence_backed_facts 的 fake proposal 时
- **实际分支**: `assert len(fact_candidates) == 1` 硬编码为 1；`assert fact["evidence_labels"] == ["E1"]` 硬编码为 E1
- **预期行为**: helper 应在文档中明确声明其单 fact/单 label 假设，或提供参数化支持
- **实际行为**: 当前硬编码假设未在 docstring 中显式声明，新 contributor 可能误用
- **直接证据**: 第 976-980 行的 `len(fact_candidates) == 1` 和 `["E1"]` 字面量
- **影响**: 低。当前所有调用方都满足该假设（smoke test 只构造单 evidence item），不影响正确性。但未来新增 multi-evidence smoke 时，该 helper 不可直接复用，需要额外封装或替换
- **建议改法和验证点**: 在 docstring 中显式声明"本 helper 假设 proposal 恰好包含 1 个 evidence_backed_fact，label 固定为 E1"；或者将该假设参数化（`expected_label: str = "E1"`），让调用方传入期望 label。验证点：已有 negative test `test_label_only_fake_proposal_rejects_canonical_ref_leakage` 不受影响
- **修复风险（低）**: docstring 修改无风险；参数化需要更新现有调用方
- **严重程度（低）**: 不阻塞 merge，属于 helper 可复用性限制

### F02-未修复-低-`_assert_compactor_material_section_shape` 对 `previous_compacted_view` 仅检查 key 存在性

- **入口/函数**: `_assert_compactor_material_section_shape` (test_public_compact_smoke.py:893-920)
- **文件(行号)**: `tests/host/test_public_compact_smoke.py:903-905`
- **输入场景**: compactor material JSON 的 `previous_compacted_view` 为非 null 非 object 的意外值时（如裸字符串 `"unexpected"`）
- **实际分支**: `for key in _COMPACTOR_MATERIAL_TOP_LEVEL_KEYS: assert key in material_json` — 只检查 key 存在，不检查 `previous_compacted_view` 值类型是否为 `None` 或 `Mapping`
- **预期行为**: 根据 vNext schema，`previous_compacted_view` 应为 `null`（首次 compact）或 `object`（后续 compact）。若生产代码因 bug 产生非预期类型（如字符串），smoke helper 应捕获
- **实际行为**: 当前 helper 未对 `previous_compacted_view` 做类型校验，而 `trace_material`/`evidence_material`/`answer_material` 通过 `_COMPACTOR_MATERIAL_LIST_SECTION_KEYS` 做了 `isinstance(section_value, list)` 检查。这种不一致可能让生产 bug 漏过 smoke
- **直接证据**: 第 903-905 行的 `key in material_json` 循环；第 911-915 行的 list section type check 是局部的（只覆盖三个 list section）；第 916 行只检查 `current_input_anchor` 是 Mapping，第 919 行只检查 `instruction` 是 Mapping，但 `previous_compacted_view` 和 `schema_version` 未做类型断言
- **影响**: 低。生产代码中的 `ConversationCompactInputVNext.to_json()` 通过 typed dataclass 保证正确类型，实际不会产生此问题。但作为 defensive smoke check，类型覆盖不完整
- **建议改法和验证点**: 为 `previous_compacted_view` 添加 `assert material_json["previous_compacted_view"] is None or isinstance(material_json["previous_compacted_view"], Mapping)`；或显式记录本 helper 不做深层类型校验，深层校验由 typed production path 保证。验证点：添加 negative test `test_compactor_material_section_shape_rejects_non_null_non_object_previous_view`
- **修复风险（低）**: 仅增加断言，不影响现有 positive tests
- **严重程度（低）**: 不阻塞 merge

### F03-未修复-低-control-doc 状态更新缺少 WU-CM-08-S2 与 aggregate deepreview 之间的 gate artifact 引用

- **入口/函数**: `docs/host/issues-implementation-control.md` 的 WU-CM-08 状态章节（第 917-919 行）以及 "当前状态" 表（第 142-151 行）
- **文件(行号)**: `docs/host/issues-implementation-control.md:146`（当前状态表 `gate` 行）、`docs/host/issues-implementation-control.md:919`（WU-CM-08 状态描述结尾）
- **输入场景**: 后续读者（人或 agent）需要理解 WU-CM-08 从 S2 accepted commit 到 aggregate deepreview gate 的完整过渡
- **实际分支**: 未提交的 diff 已正确将 `gate` 从 `accepted slice commit` 更新为 `review`，将 `implementation status` 更新为包含 `5cb68505` 和 aggregate deepreview in progress。但 S2 变为 accepted 后的中间状态裁决（即 S2 code review → accepted commit）在 WU-CM-08 条目中缺少显式 S2 re-review / controller decision 引用
- **预期行为**: WU-CM-08 条目中应完整列出 S2 的 code review artifacts、controller decision（如有）和 accepted commit，与 S1 的格式对称
- **实际行为**: WU-CM-08 条目中 S2 信息为：`S2 code review artifacts：docs/reviews/code-review-20260612-171737.md、docs/reviews/code-review-20260612-200000.md；AgentMiMo / AgentDS review 均为 PASS。WU-CM-08-S2 accepted slice commit 5cb68505。当前处于 WU-CM-08 aggregate deepreview gate。` — S2 re-review 和 controller decision 引用缺失（但 S1 同样缺失，属于一致的不完整）
- **直接证据**: 对比 WU-CM-05（第 869 行）有详细的 per-slice review/re-review/controller decision 引用链，而 WU-CM-08（第 917-919 行）的引用链更简略
- **影响**: 低。不影响当前 gate 推进的正确性（gate、status、next entry point 均已正确更新）。主要影响后续人工追溯 S2 review 裁决链
- **建议改法和验证点**: 如果 S2 review 确实直接 PASS 无额外 fix/re-review，应在条目中显式说明"直接 PASS，无 re-review"；如果存在 re-review step，应补充引用。验证点：与 `docs/reviews/` 下实际存在的 review artifact 交叉核对
- **修复风险（低）**: 仅文档补充
- **严重程度（低）**: 不阻塞 merge

## Open Questions

- 无。

## Residual Risk

- **RR1**: WU-CM-05 遗留的 `tests/host/fake_compaction.py` 中 `cast(...)` 残余（非阻塞，已在 WU-CM-05 aggregate deepreview 中记录为 deferred）。WU-CM-08 S2 plan 已明确触及时只记录不修复。
- **RR2**: Slice 1/Slice 2 仅对现有测试做窄范围 assertion refactoring。未覆盖所有 `build_compact_material_pack` / `conversation_compact_input_vnext_from_material_pack` 调用点的 shape assertion retrofit — 这是 plan 明确的设计选择（"Refactor only the narrow assertions"），不是遗漏。未来若 compact material section 命名变更，部分旧测试仍依赖 inline assertion 而非专用 helper，可能需要在当时一并 retrofit。
- **RR3**: `test_compact_material.py` 和 `test_public_compact_smoke.py` 的测试 helper 被限定为模块级私有函数（`_` 前缀），不导出到 `tests/` 公共测试工具层。若其他测试模块需要复用类似 shape assertion，可能导致重复实现 — 但当前无此需求，plan 也明确 non-goal 包含"不把测试 helper 移到生产代码"。

## Conclusion

**PASS** — 未发现阻断性问题。

WU-CM-08 S1（`bd3515d1`）和 S2（`5cb68505`）严格按照 accepted plan 实施，范围限制在测试文件内，无生产代码修改，无 schema/API 变更，无 memory semantic 变更：

- S1 新增 `_MaterialPackShape`/`_VNextInputShape` typed test helpers 及对应 assertion 函数，并 retrofit 到关键 assertion 点，使 section name 和 label boundary 的测试失败可精确定位
- S2 新增 `_assert_compactor_material_section_shape`、`_assert_no_forbidden_compactor_material_terms`、`_assert_material_evidence_contains_marker`、`_assert_label_only_fake_proposal` 等 public smoke helper，并为每个 non-trivial helper 提供 positive/negative 自测
- 所有 helper 使用 typed NamedTuple 和不暴露内部类型名的 boundary-specific 错误消息
- 主动检查 stale legacy section key（`stable_input`/`history_input`/`evidence_input`）不会重新泄漏
- LLM-facing material 的 forbidden terms 检查覆盖 compactor prompt 和 runtime material 两层
- pyright 0 errors，178 tests passed（1 skipped 为可选的 real compactor smoke）
- Control doc gate 更新正确反映当前 aggregate deepreview 状态
- README 触发判断正确：无生产代码修改 → `dayu/host/README.md` 不触发；未新增测试层/命令/维护规则 → `tests/README.md` 不触发

3 个低严重度 findings（helper 可复用性限制、defensive type check 覆盖不完整、control doc 引用完整性）均不阻塞 merge。
