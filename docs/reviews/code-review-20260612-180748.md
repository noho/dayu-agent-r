# Code Review — WU-CM-09 S1 Focused Re-review

## Scope

- Mode: focused re-review of low-risk fixes from prior S1 reviews
- Branch: `work/cm-05-06-08-09`
- Base: prior reviews `code-review-20260612-180244.md`, `code-review-20260612-180250.md`
- Output file: `docs/reviews/code-review-20260612-180748.md`
- Re-review scope: 4 个低风险修复项是否关闭

## 逐项验证

| # | 修复项 | 结论 | 证据 |
|---|---|---|---|
| 1 | `_SNAPSHOT_INTEGRITY_SCAN_ID` unused constant removed | ✅ 已关闭 | `grep -rn '_SNAPSHOT_INTEGRITY_SCAN_ID' dayu/ tests/` 返回空；memory.py 中无该常量定义 |
| 2 | row snapshot_digest column mismatch test added | ✅ 已关闭 | `test_memory_snapshot_integrity_classifies_row_digest_column_mismatch` (line 959) 使用 `_replace_snapshot_digest` helper 仅篡改 `snapshot_digest` 列，断言 `DIGEST_MISMATCH`；coverage missing 不再包含 1302 |
| 3 | unknown item kind test added | ✅ 已关闭 | `test_memory_snapshot_integrity_classifies_unknown_item_kind` (line 1005) 使用 `_insert_unsupported_memory_item_kind(store, snapshot, "mystery_memory_kind")` 插入完全未知 kind，断言 `UNSUPPORTED_ITEM_KIND`；coverage missing 不再包含 1372 |
| 4 | validation: 26 passed / coverage 80 / pyright 0 / diff check clean | ✅ 全部通过 | 见下方验证表 |

## 验证结果

| Check | Result |
|---|---|
| `pytest tests/host/test_memory_projection.py -q` | **26 passed** in 0.35s |
| `pytest --cov=dayu.host.durable.memory --cov-report=term-missing` | 26 passed, `memory.py` **80%** (332 stmts, 66 missed) |
| `python -m pyright dayu/host/durable/memory.py tests/host/test_memory_projection.py` | **0 errors**, 0 warnings |
| `git diff --check` | **clean** |

## 残余项

前次 review 的 Finding 02（`_memory_snapshot_integrity_issues_for_row` identity 读取失败分支 line 1219-1220 未覆盖）仍为 open，不在本次 focused re-review 范围内。该路径需要将 `snapshot_json` 或 `snapshot_digest` 列设为 NULL 才能触发，属于 SQLite 文件级损坏的极端场景，严重程度低，可在后续 S2/S3 补齐。

## Conclusion

**PASS** — 4 个低风险修复项全部关闭。26 passed / coverage 80% / pyright 0 / diff check clean。无 blocker。
