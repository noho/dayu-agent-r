# WU-CM-08 Aggregate Deepreview

## Review Scope

- Design source: `docs/host/design.md`, `docs/engine/design.md`
- Control doc: `docs/host/issues-implementation-control.md`
- Plan artifact: `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md`
- Commits: `fce2fca0` (plan), `bd3515d1` (S1), `5cb68505` (S2)
- Uncommitted: control-doc gate update (`docs/host/issues-implementation-control.md`)
- Implementation files:
  - `tests/host/test_compact_material.py` (S1)
  - `tests/host/test_public_compact_smoke.py` (S2)
- Production reference: `dayu/host/compact_material.py`, `tests/host/fake_compaction.py`

## Validation

| Check | Result |
|---|---|
| `pytest tests/host/test_compact_material.py tests/host/test_public_compact_smoke.py -q` | 46 passed, 1 skipped |
| `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings |
| `git diff --name-only fce2fca0^..HEAD` | 14 files (plan, reviews, control doc, 2 test files) |
| `git diff --name-only` (uncommitted) | `docs/host/issues-implementation-control.md` only |
| `compact_material.py` coverage | 87% (target ≥ 80%) |

## Dimension Reviews

### 1. Compaction Material Readability Smoke Tests — Self-Contained

**Verdict: PASS**

Slice 1 (`test_compact_material.py`) introduced `_MaterialPackShape` and `_VNextInputShape` as typed `NamedTuple` test helpers with concrete field contracts. The assertion helpers `_assert_material_pack_shape` and `_assert_vnext_input_shape` emit boundary-specific failure messages referencing section names (`previous_compacted_view`, `trace_material`, etc.). Tests are self-contained: they construct material packs via production `build_compact_material_pack` / `build_initial_material_pack`, then assert shape via helpers, without depending on external golden files.

Slice 2 (`test_public_compact_smoke.py`) extracted focused assertion helpers (`_assert_compactor_material_instruction_contract`, `_assert_compactor_material_section_shape`, `_assert_no_forbidden_compactor_material_terms`, `_assert_material_evidence_contains_marker`, `_assert_label_only_fake_proposal`) with positive and negative test coverage. Each helper identifies the failing boundary in its assertion message.

### 2. Old Naming Leakage (stable_input / history_input / evidence_input)

**Verdict: PASS — No leakage**

- Production code (`dayu/host/compact_material.py`, `dayu/config/prompts/`): zero occurrences of `stable_input`, `history_input`, or `evidence_input` as LLM-facing section names.
- Design doc (`docs/host/design.md`): zero occurrences. Uses `previous_compacted_view`, `trace_material`, `evidence_material`, `answer_material`, `current_input_anchor` throughout.
- Test code: old names appear ONLY in negative assertion constants (`_FORBIDDEN_COMPACTOR_PROMPT_TERMS`, `_FORBIDDEN_COMPACTOR_MATERIAL_TERMS`, `_STALE_COMPACTOR_MATERIAL_SECTION_KEYS`) that explicitly reject these names from LLM-facing material. This is correct defensive coverage.
- Production function `collect_selected_compaction_request_evidence_inputs` in `dayu/host/compaction_evidence.py` uses `evidence_inputs` as an internal function name, not as LLM-facing content. Not a violation.

### 3. LLM-Facing Semantic Constraints

**Verdict: PASS**

- `test_default_compactor_prompt_is_llm_facing_and_self_contained` validates the compactor system prompt and user prompt template against `_FORBIDDEN_COMPACTOR_PROMPT_TERMS` (21 terms including internal type names, migration terms, and old section names).
- `_assert_no_forbidden_compactor_material_terms` validates material JSON against `_FORBIDDEN_COMPACTOR_MATERIAL_TERMS` (10 terms).
- `_assert_compactor_material_instruction_contract` validates `output_schema_name` equals the LLM-facing identifier `conversation_compact_output_v1`, not the internal Python type name.
- Prompt contains self-describing fields: "输入 JSON 说明", "输出 JSON 字段", "输出 JSON 最小示例", "label 是本次请求内的引用标签".
- `FakeCompactorRunAgent.__call__` validates each compactor request material via `_assert_compactor_material_instruction_contract`.

### 4. Test Helper Coupling

**VerPASS — Acceptable coupling**

- S1 helpers (`_MaterialPackShape`, `_VNextInputShape`, `_material_pack_shape`, `_vnext_input_shape`) are module-private in `test_compact_material.py`. They use `NamedTuple` with concrete typed fields; no `Any`, `object`, or untyped containers.
- S2 helpers are module-private in `test_public_compact_smoke.py`. `FakeCompactorRunAgent` is a `@dataclass(slots=True)` with typed fields.
- `tests/host/public_smoke_support.py` was not modified; S2 did not need to extract shared helpers there.
- `tests/host/fake_compaction.py` retains the pre-existing `cast(Mapping[str, JsonValue], value)` at line 465 from WU-CM-05. This was explicitly documented in the plan as out-of-scope ("document it in the implementation report and do not fix it in WU-CM-08 unless it causes a direct test or pyright failure"). It does not cause pyright or test failure.

### 5. README Trigger Judgment

**Verdict: PASS — No README update needed**

Per AGENTS.md trigger rules:
- `tests/` modification → check `tests/README.md`. WU-CM-08 only added focused assertion helpers and small tests inside existing `tests/host/` files. No new test layer, command, or maintenance rule was introduced. No update needed.
- `dayu/host/` modification → not triggered (no production code changed).
- No new shared helper module introduced.

### 6. Pyright / Test Coverage

**Verdict: PASS**

- `pyright dayu/ tests/ utils/`: 0 errors, 0 warnings.
- `compact_material.py` line coverage: 87% (target ≥ 80%). Uncovered lines are primarily defensive type/error checks (`__post_init__` validation paths) and some pre-dispatch builder edge cases already covered by integration smoke tests.
- `test_compact_material.py`: 35 passed.
- `test_public_compact_smoke.py`: 11 passed, 1 skipped (real compactor smoke gated by `DAYU_RUN_REAL_COMPACTOR_SMOKE=1`).

### 7. Phaseflow Gate Record

**Verdict: PASS**

- Control doc "当前状态" table correctly updated: gate → `review`, implementation status → `WU-CM-08-S2 accepted slice commit 5cb68505; WU-CM-08 aggregate deepreview in progress`, next entry point → `WU-CM-08 aggregate deepreview gate`.
- WU-CM-08 section status text correctly updated with S1/S2 accepted slice commits, review artifacts, and aggregate deepreview gate reference.
- All review artifacts exist and are cross-referenced: plan reviews (4), S1 implementation report, S1 code reviews (2), S2 implementation report, S2 code reviews (2).

## Residual Risks

| ID | Status | Description | Owner |
|---|---|---|---|
| WU-CM-08-R1 | closed | Old naming in test negative assertions — intentional defensive coverage, not leakage | N/A |
| WU-CM-08-R2 | deferred-with-owner | `tests/host/fake_compaction.py` line 465 `cast(...)` residual from WU-CM-05 — explicitly out of WU-CM-08 scope per plan; does not block | WU-CM-05 residual, non-blocking |
| WU-CM-08-R3 | closed | `collect_selected_compaction_request_evidence_inputs` internal function name — not LLM-facing, no violation | N/A |

## Conclusion

**PASS**

WU-CM-08 aggregate deepreview passes. All validation checks pass (46 tests passed, pyright 0 errors, coverage 87%). No old naming leakage in LLM-facing content. Test helpers are self-contained, typed, and boundary-specific. No README update triggered. Control doc gate record is accurate. No blocking residual risks.

## Artifacts

- Plan: `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md`
- Plan reviews: `docs/reviews/plan-review-20260612-164857.md`, `docs/reviews/plan-review-20260612-165055.md`, `docs/reviews/plan-review-20260612-165449.md`, `docs/reviews/plan-review-20260612-191500.md`
- S1 implementation report: `docs/reviews/wu-cm-08-s1-implementation-report.md`
- S1 code reviews: `docs/reviews/code-review-20260612-170451.md`, `docs/reviews/code-review-20260612-090406.md`
- S2 implementation report: `docs/reviews/wu-cm-08-s2-implementation-report.md`
- S2 code reviews: `docs/reviews/code-review-20260612-171737.md`, `docs/reviews/code-review-20260612-200000.md`
- Aggregate deepreview: this artifact
