# Code Review — WU-CLI-SMOKE-01-R1 Slice 1 Narrow Re-review (DS-F03)

## Scope

- Mode: current changes（narrow re-review）
- Branch: `phaseflow/wu-cli-smoke-01-r1`
- Base: `929691ea`
- Output file: `docs/reviews/code-review-20260721-010824.md`
- Review type: Narrow gate re-review，仅接受 DS-F03 closure 验证
- Included scope:
  - `tests/cli/test_thinking_renderer.py`（当前 diff vs base `929691ea`）
  - `dayu/cli/thinking.py`（生产代码 `record()` / `close()` owner contract）
  - `dayu/service/entrypoint_runtime.py`（`EntrypointThinking` DTO 当前定义）
- Excluded scope（按 Controller adjudication，不重开）:
  - DS-F01（terminal fence / drain TOCTOU）— rejected-with-reason
  - DS-F02（Service prompt / interactive 集成测试未注入 transient）— deferred-with-owner to Slice 2
  - Slice 2 所有内容
- Parallel review coverage: 无（narrow gate，单 reviewer 逐行走读）

## Gate Context

本轮 narrow gate 仅验证三件事：

1. DS-F03（`CliThinkingRenderer.close()` 后 `record()` 抑制缺少直接单元测试）是否被新测试真实关闭；
2. test-only fix 是否越界或引入回归；
3. focused tests、pyright、`git diff --check` 证据是否可信。

不重开 DS-F01 / DS-F02，不提前扩入 Slice 2，不修改任何生产代码、测试、design、plan、control、umbrella、fix 或已有 review artifacts。

## Method

1. 阅读 `dayu/cli/thinking.py` `record()` 方法的完整分支顺序：`_closed` / `_enabled` → dedupe → runtime_id reset → `runtime_sequence <=` 逆序 → 写入。
2. 逐行走读新测试 `test_thinking_renderer_suppresses_records_after_close`，反向验证每个早期 return 分支是否会掩盖 close 语义。
3. 独立运行 `pytest`、`pyright`、`git diff --check` 验证 fix-codex 与 fix-controller-validation 中声明的证据。
4. 检查 test-only fix 是否修改了生产代码、公共 contract 或 design/plan/control artifact。
5. 检查 `_thinking()` helper 的参数 rename（`event_sequence` → `runtime_sequence` + `runtime_id`）是否与 DTO 当前字段签名一致。

## Findings

### DS-F03-确认已修复-[无新缺陷]

- **入口/函数**: `CliThinkingRenderer.record()` → `test_thinking_renderer_suppresses_records_after_close`
- **文件(行号)**: `tests/cli/test_thinking_renderer.py:121-142`（新测试）；`dayu/cli/thinking.py:95`（`_closed` gate）
- **输入场景**: renderer enabled → 首次 `record()` 正常输出 → `close()` → 以全新 `dedupe_key`、更大 `runtime_sequence`、不同 `text_delta` 再次 `record()`
- **实际分支**: 第二次 `record()` 的唯一拦截分支是 `dayu/cli/thinking.py:95` — `if self._closed or not self._enabled: return`
- **预期行为**: close 后 `record()` 不向 stderr 追加任何输出
- **实际行为**: stderr 内容与 close 前完全相同（精确字符串相等断言）
- **直接证据**:
  - `dayu/cli/thinking.py:95`: `self._closed` 是 `record()` 方法第一个检查条件，优先级高于所有 dedupe / 逆序 / disabled 分支
  - 新测试第 129 行：首次 `record()` 后 `assert stderr.getvalue() == "Thinking: 正在分析收入变化"` — 证明 renderer 处于可输出状态
  - 新测试第 136 行：使用 `dedupe_key="thinking-2"` — 不在 `_seen_dedupe_keys` 中，排除 dedupe 掩盖（`dayu/cli/thinking.py:97`）
  - 新测试第 137 行：使用 `runtime_sequence=2` > `_last_runtime_sequence=1` — 排除逆序掩盖（`dayu/cli/thinking.py:102-103`）
  - 新测试第 135 行：`runtime_id` 默认 `"runtime-1"` 与首次相同 — 不触发 runtime_id reset 分支（`dayu/cli/thinking.py:99-101`）
  - 新测试第 138 行：`text_delta="关闭后不应输出"` 与首次不同 — 若泄漏，精确字符串断言必然失败
  - 新测试第 142 行：`assert stderr.getvalue() == output_before_close` — 精确字符串匹配，非 count/substring 弱断言
- **影响**: DS-F03 已由直接 owner-level 测试真实关闭；无新增 material defect
- **建议改法和验证点**: 无需进一步修改。当前测试已满足 owner contract 的直接回归保护要求
- **修复风险（低）**: test-only，不修改生产代码
- **严重程度（低）**: 无新缺陷

### 补充验证：test-only fix 边界检查

- **入口/函数**: `tests/cli/test_thinking_renderer.py` 完整 diff vs base `929691ea`
- **文件(行号)**: `tests/cli/test_thinking_renderer.py`（全文）
- **检查项**:
  1. **生产代码未修改**: `dayu/cli/thinking.py` 的 diff 仅来自 Slice 1 原始实现（`event_sequence` → `runtime_sequence` 重命名 + `runtime_id` 新增 + `<=` 语义修正），不由 DS-F03 fix 引入。DS-F03 fix 仅在测试文件中增加一个测试函数。
  2. **公共 contract 未修改**: `EntrypointThinking` DTO 字段（`run_id`, `runtime_id`, `runtime_sequence`, `dedupe_key`, `text_delta`）在 Slice 1 中变更，DS-F03 fix 只消费已有 public DTO，不修改其定义。
  3. **Design / plan / control / umbrella / fix artifact 未修改**: 仅新增本 review artifact。
  4. **`_thinking()` helper 参数与 DTO 一致**: 当前 helper 签名 `runtime_sequence: int, runtime_id: str = "runtime-1"` 与 `EntrypointThinking` 的 `runtime_sequence: int`, `runtime_id: str` 字段完全匹配；`_require_positive_int()` 校验通过。
  5. **helper rename 上下文**: base `929691ea` 的 `EntrypointThinking` 使用 `event_sequence` 字段；Slice 1 将其 rename 为 `runtime_sequence` 并增加 `runtime_id`。测试 helper 的 rename 是跟随 DTO 变更，不属于 fix 越界。
- **实际行为**: test-only fix 未越界、未引入回归
- **直接证据**: `git diff 929691ea -- tests/cli/test_thinking_renderer.py` 仅包含测试文件变更；`git diff 929691ea -- dayu/cli/thinking.py` 的所有生产代码变更是 Slice 1 原始实现的一部分（含 `_last_runtime_id`、`_last_runtime_sequence`、`<=` 语义等），不由 fix 触发
- **影响**: 无
- **建议改法和验证点**: 无需修改
- **修复风险（低）**: 不适用
- **严重程度**: 无缺陷

### 补充验证：证据可信性独立复核

- **入口/函数**: 独立运行 pytest / pyright / git diff --check
- **文件(行号)**: N/A（命令行验证）
- **检查项**:
  1. `pytest tests/cli/test_thinking_renderer.py tests/cli/test_prompt_command.py tests/cli/test_interactive_command.py -q` → **99 passed, 3 warnings**（warnings 均为 `.venv` 内 `edgar` 依赖既有 `DeprecationWarning`，不由本次 fix 引入）
  2. `pyright` → **0 errors, 0 warnings, 0 informations**（版本提示可升级至 v1.1.411，不影响结果）
  3. `git diff --check` → **exit 0，无输出**（无空白错误）
- **实际行为**: 三项证据均可信，与 fix-codex.md §Validation 和 fix-controller-validation.md §Direct Validation 一致
- **直接证据**: 上述命令的 stdout/stderr 输出
- **影响**: 无
- **建议改法和验证点**: 无需修改
- **修复风险（低）**: 不适用
- **严重程度**: 无缺陷

## Open Questions

无。

## Residual Risk

- 当前 Slice 1 test-only fix scope 内无未覆盖 residual risk。
- DS-F02（Service prompt / interactive 集成测试未注入 transient）仍按 Controller adjudication 归属 Slice 2；本 narrow gate 不进入、不重分类、不提前扩入。
- `dayu/cli/thinking.py` 的生产代码变更（`runtime_id` reset、`<=` 语义）是 Slice 1 原始实现的一部分，其正确性由已有测试套件覆盖，不在本 narrow gate 范围内。

## Conclusion

- **DS-F03: FIXED**。新测试 `test_thinking_renderer_suppresses_records_after_close` 提供了 `CliThinkingRenderer.close()` owner contract 的直接、无歧义回归保护：dedupe、逆序、disabled 三个早期 return 分支均不能掩盖 close 语义。
- **无新增 material defect**。
- **test-only fix 未越界**：仅新增一个测试函数，未修改生产代码、公共 contract 或 design/plan/control artifact。
- **验证证据可信**：独立运行的 pytest（99 passed）、pyright（0 errors）、git diff --check（pass）与 fix artifact 声明一致。
