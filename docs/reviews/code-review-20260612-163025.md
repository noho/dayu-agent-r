# WU-CM-06-S2 Code Review — AgentDS

## Review Scope

- **Work unit**: WU-CM-06 Terminal Summary Text Policy Convergence
- **Slice**: S2 Naming / Docstring Convergence
- **Files**: `dayu/host/terminal_summary_payload.py`, `dayu/host/_terminal_answer.py`, `docs/host/issues-implementation-control.md`
- **Design sources**: `docs/host/design.md`, `docs/engine/design.md`
- **Control**: `docs/host/issues-implementation-control.md`
- **Plan**: `docs/host/host-issues/wu-cm-06-terminal-summary-text-policy-plan.md`

## Validation Commands Executed

| Command | Result |
|---|---|
| `pytest tests/host/test_terminal_summary_payload.py -q` | 14 passed |
| `pytest tests/host/test_read_api_terminal_policy.py tests/host/test_public_host_event.py -q` | 7 passed |
| `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings, 0 informations |
| `git diff --check` | passed |

## Review Dimensions

### 1. S2 是否只做 docstring/naming convergence，无行为/接口/schema 变化

**Verdict: PASS**

Diff 证据：
- 3 文件变更，51 行增加，12 行删除。
- `terminal_summary_payload.py`：仅 module docstring、`PayloadTextReadPolicy` enum docstring、`assistant_final_answer_text_from_run_payload`、`terminal_summary_content_text_from_payload`、`_text_field` 的 docstring 变更。无 import 变更、无函数签名变更、无返回值变更、无逻辑变更。`__all__` 不变。
- `_terminal_answer.py`：仅 module docstring、`assistant_final_answer_continuity_text`、`_optional_descriptor_text` 的 docstring 变更。无 import 变更、无函数签名变更、无返回值变更、无逻辑变更。`__all__` 不变。
- `docs/host/issues-implementation-control.md`：仅状态表三项（gate, implementation status, next entry point）更新及 WU-CM-06 状态段落追加 S1 accepted commit 和 S2 implementation report 引用。

代码逻辑零变更，验证通过：已存在的 14 个 payload 测试全部通过，7 个 broader policy 测试全部通过。

### 2. Docstring 是否准确表达 assistant final-answer continuity text source selection

**Verdict: PASS**

`_terminal_answer.py` module docstring 准确表述了解析顺序：
- 先读 inline `final_answer`
- 缺失或空白时再通过 `terminal_summary_ref` / `terminal_summary_digest` descriptor 读取并校验 artifact `content`

代码实现核对（`_terminal_answer.py:60-83`）：
```python
final_answer = assistant_final_answer_text_from_run_payload(...)  # line 60: inline first
if final_answer is not None:
    return final_answer                                           # line 64-65: inline wins
# lines 66-83: descriptor fallback path
```

`terminal_summary_payload.py` 准确说明了两个 helper 的职责：
- `assistant_final_answer_text_from_run_payload`：仅读 `final_answer` 字段（通过 `_PAYLOAD_FIELD_FINAL_ANSWER = "final_answer"`）
- `terminal_summary_content_text_from_payload`：仅读 artifact payload 的 `content` 字段（通过 `_PAYLOAD_FIELD_CONTENT = "content"`）

两个 helper 均不读取裸 `content`、`summary_text` 或 nested `summary`——与 docstring 声明一致。

### 3. 是否正确说明 terminal summary artifact content 仅为 RUN_SUCCEEDED continuity fallback

**Verdict: PASS**

四处明确声明，语义一致无矛盾：

| 位置 | 声明 |
|---|---|
| `_terminal_answer.py` module | "artifact ``content`` 只是成功终态 continuity 的 fallback，不是通用 terminal summary、失败诊断、取消原因、lost 诊断、episode summary 或 evidence-backed fact 来源" |
| `assistant_final_answer_continuity_text` docstring | "terminal summary artifact ``content`` 只在 ``RUN_SUCCEEDED`` continuity 路径中作为 fallback；失败、取消和 lost 终态的 diagnostic 文本不能通过本 resolver 变成 assistant final answer" |
| `terminal_summary_payload.py` module | "terminal summary artifact 的 ``content`` 只是在 ``RUN_SUCCEEDED`` continuity 路径中，由上层 descriptor 校验后使用的 fallback 文本源；它不是通用 terminal summary、episode summary 或 evidence-backed fact 来源" |
| `terminal_summary_content_text_from_payload` docstring | "该 ``content`` 只供 ``RUN_SUCCEEDED`` 的 assistant final-answer continuity fallback 使用... 它不是失败、取消或 lost 终态的 assistant final answer，也不会被本 helper 升级为 evidence-backed fact" |

所有声明均精确表达了"仅在 `RUN_SUCCEEDED` continuity 路径中作为 fallback"的边界，不做超出其职责的语义承诺。

### 4. Consumer 边界是否和 S1 证据一致

**Verdict: PASS**

Docstring 声明的 consumer 边界（`_terminal_answer.py` module docstring）：

> compaction material 使用本 strict continuity resolver 并允许 digest-checked artifact fallback；Conversation Memory selected recent window 直接消费 inline ``final_answer`` 且保持 lenient；durable projection / run-input adapter 可以先把 descriptor-backed terminal artifact ``content`` hydrate 成 transient ``final_answer``，再交给 memory consumer。

代码证据逐一核对：

| Consumer | 代码位置 | Helper | Policy | 与 docstring 一致 |
|---|---|---|---|---|
| compaction material | `compact_material.py:2012-2015` | `assistant_final_answer_continuity_text` | `STRICT_NON_EMPTY` | ✅ strict + artifact fallback |
| compaction evidence | `compaction_evidence.py:413-416` | `assistant_final_answer_continuity_text` | `STRICT_NON_EMPTY` | ✅ strict + artifact fallback |
| Conversation Memory selected recent window | `memory.py:1625-1628` | `assistant_final_answer_text_from_run_payload` | `LENIENT_NON_EMPTY` | ✅ inline only, lenient |
| durable projection hydration | `durable/memory.py:215-243` | 先 inline `STRICT`，再 `assistant_final_answer_continuity_text` `STRICT`，合并进 transient `final_answer` | `STRICT_NON_EMPTY` | ✅ hydrate descriptor-backed → transient `final_answer` |
| run-input adapter hydration | `run_input.py:3005-3036` | 同 durable projection 模式 | `STRICT_NON_EMPTY` | ✅ hydrate descriptor-backed → transient `final_answer` |

关键差异验证：
- **Memory consumer** 使用 `assistant_final_answer_text_from_run_payload`（inline-only），**不是** `assistant_final_answer_continuity_text`——docstring 称"直接消费 inline `final_answer`"，与代码一致。
- **Memory consumer** 使用 `LENIENT_NON_EMPTY`——docstring 称"保持 lenient"，与代码一致。
- **Durable projection / run-input adapter** 先尝试 inline，fallback 到 `assistant_final_answer_continuity_text`，然后合并进 transient payload 的 `final_answer` 字段——docstring 称"hydrate 成 transient `final_answer`"，与代码一致。
- Memory consumer 随后的 `_selected_assistant_item` 从 payload 中读 `final_answer`，由于 durable projection 已将 artifact `content` hydrate 进该字段，memory consumer 透明获得 continuity 文本，但自身不执行 descriptor 解析——docstring 边界表述准确。

### 5. 是否违反 LLM-facing 文本语义约束或 README 触发判断

**Verdict: PASS**

**LLM-facing 文本语义约束**：
- 变更文件为 Host 内部 Python docstring（`terminal_summary_payload.py` 和 `_terminal_answer.py`），面向的是开发者而非 LLM。
- 这些模块不是 tool schema（无 `@tool` 装饰器）、不是 prompt fragment（不在 `dayu/config/prompts/` 下）、不是 EventLog payload 定义、不会被投影进 LLM 上下文。
- 即使从保守角度审视，docstring 中使用的是业务可读描述（"assistant final-answer continuity"、"terminal summary artifact"、"RUN_SUCCEEDED continuity fallback"），没有暴露内部治理标识为业务事实、没有使用代码类型名或内部模块名要求模型理解。
- 不违反 Agent 语义约束。

**README 触发判断**：
- `dayu/host/README.md`：S2 只改 docstring，不改行为、公共契约或 package-level API。按 plan README Decision 不触发更新。✅
- `tests/README.md`：S2 不改测试文件。不触发更新。✅
- 其他 README：diff 不涉及对应目录。不触发更新。✅

## Adversarial Failure Pass

对每条 docstring 声明做了反例检查：

1. **"先读取 inline `final_answer`"** → 若 `final_answer` 字段存在但为空白字符串，`_text_field` 返回 `None`（line 99: `value.strip() == ""`），正确进入 descriptor fallback。✅
2. **"terminal summary artifact `content` 只在 `RUN_SUCCEEDED` continuity 路径中作为 fallback"** → 该函数被调用时接收 `transaction` 和 `run_payload` 参数，调用方必须已经确认在 `RUN_SUCCEEDED` 上下文中。函数本身不做 event type 判断，但这是设计边界（调用方职责）而非 docstring 虚假陈述。✅
3. **"失败、取消和 lost 终态的 diagnostic 文本不能通过本 resolver 变成 assistant final answer"** → `read_api.py` 只在 `RUN_SUCCEEDED` 时调用 `HostFinalAnswerView` 投影，`RUN_FAILED`/`RUN_CANCELLED`/`RUN_LOST` 的 `final_answer=None`。该声明是对调用方边界的准确描述。✅
4. **"本 resolver 不截断过长文本"** → 代码中无任何截断逻辑（无 `text[:N]`、`len()` 检查、truncation 参数）。✅
5. **"descriptor 只是 artifact 引用标签和 digest 校验材料，不是业务事实"** → `_optional_descriptor_text` 只返回字符串用于传给 `sqlite_payload_object` 做 artifact fetch，不解析内容、不投影给 LLM。✅

无发现反例或不一致。

## Conclusion: PASS

WU-CM-06-S2 Naming / Docstring Convergence 严格审查通过。零 findings。

S2 完全遵守 plan 约束：仅做 docstring 收敛，无行为变更、无接口变更、无 schema 变更、无 import 变更。Docstring 准确表达了 assistant final-answer continuity text source selection 语义、terminal summary artifact `content` 的 fallback-only 角色，以及四类 consumer 的策略边界。Consumer 边界声明与 S1 代码证据完全一致。未违反 LLM-facing 文本语义约束，README 更新按触发规则正确判定为不触发。

### 验证摘要

| 项 | 结果 |
|---|---|
| `pytest tests/host/test_terminal_summary_payload.py -q` | 14 passed |
| `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings, 0 informations |
| `git diff --check` | passed |
| 行为/接口/schema 变更 | 0 |
| Docstring 准确性 | 正确 |
| Consumer 边界与代码证据一致性 | 一致 |
| LLM-facing 语义约束 | 不违反 |
| README 触发判断 | 正确不触发 |
| Findings | 0 |
