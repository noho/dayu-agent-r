# Code Review

## Scope

- Mode: current changes (WU-CM-05 aggregate deepreview)
- Branch: `work/cm-05-06-08-09`
- Base: `main`
- Output file: `docs/reviews/code-review-20260612-153234.md`
- Included scope:
  - Commit `153c43e3`: plan gate acceptance
  - Commit `7f2ce2c5`: S1 — direct typed candidate parser
  - Commit `da8cda65`: S2 — complete invalid proposal diagnostics coverage
  - Commit `f3a3c0e3`: S3 — contract and boundary cleanup
  - Current uncommitted change to `docs/host/issues-implementation-control.md` (aggregate deepreview gate update)
- Excluded scope:
  - `dayu.engine`, `dayu.service`, `dayu.fins`, `dayu.config`
  - Durable schema / migration
  - GitHub issue / PR metadata
- Parallel review coverage: 无
- Validation performed:
  - `pytest tests/host/test_llm_compaction.py -q`: **37 passed in 0.27s**
  - `python -m pyright dayu/ tests/ utils/`: **0 errors, 0 warnings, 0 informations**

## Findings

### 1-unresolved-低-fake_compaction.py 残留 cast 不阻塞但应记录

- **入口/函数**: `tests/host/fake_compaction.py:_json_object`
- **文件(行号)**: `tests/host/fake_compaction.py:465`
- **输入场景**: 所有使用 `fake_compaction_proposal_from_material_json(...)` 的测试路径
- **实际分支**: `_json_object` 内 `return cast(Mapping[str, JsonValue], value)`
- **预期行为**: WU-CM-05 计划目标是消除 production cast 和宽 Mapping 祝福；测试代码残留 cast 不违反计划边界，但应作为 residual risk 记录
- **实际行为**: `cast(Mapping[str, JsonValue], value)` 在 `_isinstance(value, Mapping)` 检查后仍然存在，绕过 pyright 类型收窄
- **直接证据**: `tests/host/fake_compaction.py:11` 导入 `cast`，`tests/host/fake_compaction.py:465` 使用 `cast`
- **影响**: 仅测试代码；不影响 production parsing 路径或 LLM-facing 错误消息。pyright 对测试代码的类型安全降低，但不引入 production 弱类型绕过
- **建议改法和验证点**: 可在后续 cleanup 中用 `TypeGuard` + `assert` 模式替换（参考 `tests/host/test_llm_compaction.py:_is_json_mapping` 的做法），或保持现状因为测试 helper 的 `isinstance` 检查已提供运行时安全
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## 重点检查结果

### 1. vNext proposal typed parsing 是否真正移除 production cast 和宽 Mapping 祝福

**结论: PASS。**

- `from typing import cast` 已从 `dayu/host/llm_compaction.py` 移除（diff 第 9 行）
- `_parse_vnext_proposal` 返回类型从 `Mapping[str, JsonValue]` 改为 `ConversationCompactOutputVNext`（`llm_compaction.py:598`）
- `cast(Mapping[str, JsonValue], parsed)` 已移除，替换为 `_json_object(parsed, "proposal")`（`llm_compaction.py:613`）
- `parse_conversation_compact_output_vnext` 不再把宽 Mapping 传给各 section parser（`llm_compaction.py:591`）
- 生产代码中 `rg -n "cast\\("` 无命中（`llm_compaction.py` 内）
- 宽 `Mapping[str, JsonValue]` 仅存在于局部 JSON helper 边界（`_json_object` 返回值），不作为跨函数 proposal contract

### 2. invalid proposal diagnostics 是否覆盖计划要求并保持脱敏/截断

**结论: PASS。**

计划要求的每个 invalid proposal class 均有直接测试：

| 测试类 | 测试函数 | 匹配模式 |
|--------|---------|----------|
| malformed JSON | `test_..._rejects_malformed_json` | `not valid JSON` |
| top-level 非 object | `test_..._rejects_top_level_non_object` | `proposal must be object` |
| 缺必填 key | `test_..._reports_missing_required_key_path` | `missing required key: diagnostics` |
| 字段类型错误 | `test_..._reports_field_type_path` | `session_summary.summary_text` |
| 嵌套数组类型 | `test_..._reports_nested_array_type_path` | `answer_anchors[0].anchor_items` |
| 数组元素类型 | `test_..._reports_array_item_type_path` | `diagnostics[0].source_labels[0]` |
| 顶层数组超限 | `test_..._reports_top_level_array_overlimit` | `evidence_backed_facts` |
| 嵌套数组超限 | `test_..._reports_nested_array_overlimit` | `answer_anchors[0].anchor_items` |
| 非法 enum (evidence_kind) | `test_..._reports_fact_evidence_kind_path` | `evidence_backed_facts[0].evidence_kind.*bad_evidence_kind` |
| 非法 enum (intent_type) | `test_..._reports_forward_intent_type_enum_value` | `forward_intents[0].intent_type.*bad_intent_type` |
| 非法 enum (status) | `test_..._reports_forward_status_enum_value` | `forward_intents[0].status.*bad_status` |
| 非法 enum (reason) | `test_..._reports_reference_reason_enum_value` | `reference_continuity_items[0].reason.*bad_reason` |
| unknown label | `test_..._rejects_unknown_label` | `unknown source label` |
| stale label | `test_..._rejects_stale_label` | `stale source label` |
| cross-section label | `test_..._rejects_cross_section_label` | `cross-section label` |
| ordinal 路径 | `test_..._reports_anchor_ordinal_path` | `answer_anchors[0].anchor_items[1].ordinal` |
| candidate safety-net | `test_..._wraps_candidate_safety_net` | `compactor vNext proposal schema invalid` |
| old schema fail-closed | `test_..._fails_closed_for_old_schema` | 已有测试保留 |

脱敏/截断验证：

- `_required_enum` 对非法 enum 值调用 `redact_sensitive_diagnostic_values(...)` 和 `truncate_diagnostic_text(...)`（`llm_compaction.py:1109-1113`）
- 实际运行验证：非法 enum 值 `bad_evidence_kind` 经脱敏/截断后仍出现在 exposed message 中：`compactor vNext proposal schema invalid: evidence_backed_facts[0].evidence_kind has invalid enum value: bad_evidence_kind`
- 脱敏/截断函数本身有独立单元测试（由 S2 覆盖），本 aggregate review 不重复验证

### 3. LLM-facing 错误/字段路径是否自解释且不泄漏内部治理事实

**结论: PASS。**

实际运行验证 exposed error messages：

- 缺必填 key: `compactor vNext proposal schema invalid: 'missing required key: diagnostics'`
- 字段类型: `compactor vNext proposal schema invalid: session_summary.summary_text must be string`
- 嵌套数组: `compactor vNext proposal schema invalid: answer_anchors[0].anchor_items must be array`
- 非法 enum: `compactor vNext proposal schema invalid: evidence_backed_facts[0].evidence_kind has invalid enum value: bad_evidence_kind`

所有错误消息：
- 使用完整 JSON field path（如 `evidence_backed_facts[0].evidence_kind`），自解释
- 不包含 event_id、payload_ref、digest、cursor、tool_call_id 等内部治理标识
- 不包含 Runner 内部状态、Host 调度状态或 Engine 实现细节
- 非法 enum 值经过脱敏和截断处理

### 4. 测试 helper cleanup 是否关闭 DS-F02 且未引入弱类型绕过

**结论: PASS。**

- `tests/host/test_llm_compaction.py` 移除了 `from typing import cast`（diff 第 682 行），替换为 `from typing import TypeGuard`
- `_required_mapping` 从 `assert isinstance(...); return cast(...)` 改为 `assert _is_json_mapping(...); return value`（`test_llm_compaction.py:872-873`）
- `_is_json_mapping` 使用 `TypeGuard[Mapping[str, JsonValue]]` 实现类型安全的类型收窄（`test_llm_compaction.py:876-883`）
- `_proposal_json` 和 `_compact_input_json` 不再使用 `cast`（`test_llm_compaction.py:800-817`）
- `_material_json_from_compactor_prompt` 不再使用 `cast`（`test_llm_compaction.py:843`）
- DS-F02（production cast 消除）已在 S1 关闭；测试代码 cleanup 在 S3 完成

### 5. Host/Engine 分层边界和 runtime helper 使用是否符合 AGENTS.md

**结论: PASS。**

- 变更仅涉及 `dayu/host/llm_compaction.py`（Host 层）和 `tests/host/test_llm_compaction.py`（Host 测试）
- 不涉及 `dayu.engine`、`dayu.service`、`dayu.fins`、`dayu.config`
- 不修改 Engine contract、Engine state machine、Runner overflow behavior 或 provider retry
- `dayu.runtime` 未被引入或修改
- Host 内部 helper（`_field_path`、`_item_path`、`_json_object`、`_required_enum` 等）均为模块级私有函数，不向 Engine 或 Service 暴露
- `LLMCompactionProposalError` 保持为唯一 public proposal failure type
- `ConversationCompactOutputVNext.__post_init__` 保持不变作为 Host contract safety net

### 6. README 不更新判断是否成立

**结论: 成立。**

- `dayu/host/README.md` 记录 stable developer-facing Host compact behavior、public contracts、context governance semantics 和 LLM-facing schema。本次变更仅收紧内部 parser diagnostics，不改变 stable behavior、public contracts 或 LLM-facing schema
- `tests/README.md` 记录 test layering、commands 和 maintenance rules。本次变更仅在现有 `test_llm_compaction.py` 中增加 test cases，不引入新 test layer、新 command category 或 changed maintenance convention
- `dayu/config/` 未修改，不需要检查 config README

### 7. residual risk tests/host/fake_compaction.py existing cast 是否应阻塞 WU-CM-05

**结论: 不阻塞。**

- `tests/host/fake_compaction.py` 在 WU-CM-05 范围内**无任何 commit**（`git log --oneline main...f3a3c0e3 -- tests/host/fake_compaction.py` 无输出）
- 该文件的 `cast` 用法（`_json_object` 第 465 行）是 pre-existing 测试 helper 代码，不在 S3 approved scope 内
- `cast` 仅用于测试代码内部的 JSON object 校验，不影响 production parsing 路径
- 该 `cast` 前有 `isinstance(value, Mapping)` 运行时检查，提供运行时安全保障
- 控制文档已记录为 residual risk（`issues-implementation-control.md:869`）

## Open Questions

- 无。

## Residual Risk

1. **`tests/host/fake_compaction.py:465` 残留 `cast(Mapping[str, JsonValue], value)`**: pre-existing 测试 helper 代码，不在 WU-CM-05 scope 内。可在后续 cleanup 中用 `TypeGuard` 模式替换。不阻塞 WU-CM-05。

2. **`_required_enum` 脱敏/截断行为未在 enum 测试中显式断言**: 当前 enum 测试验证异常类型和 exposed message 中包含 field path + invalid value，但未单独断言 `redact_sensitive_diagnostic_values` 和 `truncate_diagnostic_text` 的生效。脱敏/截断函数本身有独立单元测试覆盖（S2），此处为测试关注点分离的 residual，非功能缺陷。

3. **`_json_object` 新增 non-string key 检查（`llm_compaction.py:980-982`）无独立测试**: 该检查是 defense-in-depth，标准 `json.loads` 不会产生 non-string key。可接受为无测试覆盖的低风险增强。

## Conclusion

**PASS。**

WU-CM-05 实现了计划目标：将 LLM compaction proposal parsing 收敛为显式 typed validation boundary，消除了 production `cast` 和宽 `Mapping[str, JsonValue]` 跨函数传递，为所有 invalid proposal class 提供了完整 field-path diagnostics 测试覆盖。Host/Engine 分层边界未被违反，LLM-facing 错误消息自解释且不泄漏内部治理事实。测试 helper cleanup 关闭了 DS-F02。pytest 37 passed，pyright 0 errors。
