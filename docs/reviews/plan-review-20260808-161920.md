# PR 190 F18 最终独立 plan re-review（AgentDS）

- 复审时间：2026-08-08 16:19:20 Asia/Shanghai
- 复审目标：`docs/gateflow/pr-190-f18-plan-20260808.md`
- 同步输入：`docs/gateflow/pr-190-f18-plan-fix-20260808.md`、`docs/gateflow/pr-190-f18-plan-third-rereview-adjudication-20260808.md`、`docs/reviews/plan-review-20260808-155504.md`
- 最新 plan SHA256：`c67d85af69c34e9db77bbfed0f508df95f4e21a29d9004a9c4229df14c42ec0f`
- 审查边界：ratio/pressure owner、R1-R4 material/state reachability、reactive exclusion、ordinary/compactor budgets、branch role、publication ownership、global stop；未调用 provider，未修改 plan/产品，未读取指定 ownership 不明 artifact。

## 二值 Gate verdict

**PASS**。未发现仍会阻塞 implementation/formal observation 的 material finding。上一轮四项 finding 均已在 owner boundary 收敛；最新 publication root 补丁也消除了 B1 immutable bundle 与 B2 新 evidence writer 的冲突。真实 MiMo 自然不触发 repair/fallback或某个预期 boundary 仍是已声明 residual，必须写 `needs-more-evidence`，但不是 plan defect。

## Owner-level direct verification

### 1. predicted-only ratio `0.018` 与 stage matrix成立

- Host pressure/action只消费 `predicted_input_tokens`（`dayu/host/context_budget.py:1309-1332`），ratio owner按 `floor(context_window*ratio)` 派生 threshold（同文件 `:1561-1571`）。MiMo effective window `1,048,576` 因而得到 `18,874`。
- 现有 typed rows给出 initial ordinary `9,048 < 18,874`、同一 R1 continuation `19,174/20,316`、首个 post-R1 ordinary `20,363 > 18,874`。plan不再读取 conservative `20,998`。
- 五阶段矩阵中 `CONTINUATION` 对 normal/soft/hard都 `ALLOW_DISPATCH`，`ORDINARY` soft才 proactive compact（`dayu/host/context_budget.py:1430-1511`）。因此 R1初始可 dispatch，同Run工具循环不会被soft pressure提前compact，R2 pre-start具备合法 baseline trigger band。
- plan把 fresh数值/ordinal保留为运行时观察，不承诺 stochastic success；R2未自然触发即seal，不调参或重跑，边界诚实。

### 2. R2 baseline source、post-compact FY2025 acquisition 与 R3 target可达

- pre-dispatch delta查询严格使用 `event_sequence < current_input_event_sequence`，只读取 `USER_INPUT_ACCEPTED`、`TOOL_RESULT_ACCEPTED`、`RUN_SUCCEEDED`（`dayu/host/compact_material.py:3000-3039`）。所以 fresh R2 pre-start只有completed R1 group可进入delta；R2 current input不可能冒充completed evidence。
- current input由独立 anchor构造并被selection保护（`dayu/host/compact_material.py:3337-3352`）；floor `0`不额外保护completed group。R2 root segment因此可选择完整R1 atomic group，baseline accepted truth只能从canonical FY2024 evidence建立唯一 EvidenceFact。
- accepted baseline只消费其selected R1 refs；R2 current anchor仍进入 accepted后的 immediate `POST_COMPACT` candidate。`POST_COMPACT` soft pressure允许dispatch，后续真实FY2025 tool results在同一R2的 `CONTINUATION` candidates中消费；这些stage不要求第二次 proactive operation。
- R3 pre-start时，accepted coverage frontier跳过已完整消费的R1 prefix，从completed R2 group开始（`dayu/host/compact_material.py:3070-3101,3134-3184`）。该group机械包含R2 user trace中的unsupported `21.7%/18.2%`、FY2025 `TOOL_RESULT_ACCEPTED` material及answer；previous compacted view同时保留FY2024 atom。R3 current input继续独立，因此R3是唯一能同boundary满足previous/new/unsupported/caps的target。
- 旧typed topology在FY2025完成后的下一 ordinary candidate observed predicted `26,227`，高于 `18,874`；它支持R3 trigger reachability，但plan仍正确地把fresh未触发处理为`needs-more-evidence`。

### 3. reactive全部non-covering成立

- reactive ingress冻结的是当前Run pre-dispatch material view，再追加current anchor（`dayu/host/engine_ingest.py:2451-2467,5226-5248`）；同Run稍后产生的tool result不能逆向进入snapshot。
- plan已删除reactive target，并对R1-R4任一reactive operation统一seal；因此不会用错误snapshot补R2/R3 mandatory coverage，也不会由成功链覆盖失败。

### 4. ordinary与compactor hard budgets成立

- Engine主loop最多24次Runner call；tool-loop exhaustion且profile `fallback_mode=force_answer` 时还能额外调用一次 `_run_force_answer`（`dayu/engine/agent.py:881-887,1094-1101,2316-2395`）。故每Run hard为25，R1-R3为`75`，含R4为`100`。历史`18(+1)`仅是逐terminal checkpoint，不冒充hard cap。
- `max_reactive_compactions_per_run=1`是合法positive typed policy。冻结calibration下R1 initial ordinary低于soft，故R1最多1个reactive operation；R2/R3各最多1 proactive+1 reactive。正式hard为`1+2+2=5 operations / 25 calls`。R4再允许1 proactive+1 reactive，replacement hard为`7/35`。
- 每operation最多5个全局semantic attempts。proactive schedule和reactive multi-pass都复用同一个全局attempt counter，不会按pass额外突破5（`dayu/host/compaction_operation.py:751-834,1086-1100,1532-1589`）。mandatory baseline+target expected upper因此为`2/10`。
- 任一reactive一经观察即停止dependent provider；hard matrix诚实计入同一active Run在harness重新取得控制前仍可能发生的non-covering operation。

### 5. branch role 与 fallback owner成立

- replacement/repair只有R3 target accepted才完成；repair coverage还要求同一root source-boundary digest先reject后accepted。proactive attempt schedule的前两次是root initial/root repair，满足whole replacement与same-boundary约束。
- exhausted-fallback只在R3 exact五次 rejection、attempt count达到5、canonical `retry_repair_budget_exhausted=true`并进入Host tier 4/5 deterministic dispatch fallback时完成；中途accepted不得重标fallback。
- 最多三个fresh workspace slots按尚缺role自适应分配，自然缺支路保持`needs-more-evidence`；没有output injection、同workspace重跑或无界补跑。

### 6. publication、B1/B2 ownership 与 global stop成立

- B1 `pr190-formal-g1a2xu/evidence/public`在F18为immutable read-only；Slice 1只把既有relative refs/digests登记到registry/handbook，不重写旧report/digest/scan。
- B2在首次writer前冻结全新bundle id和独立public/private roots；B2-only report/index/digest/scan不写入、复制或覆盖B1 tree。B1/B2 registry refs分别绑定各自bundle与digest。
- B2 writers先完成report/projection/final index/digest，tracked helper最后独占创建唯一`secret-scan.json`，之后零写入；符合handbook与helper fail-closed contract（`docs/cli_ci.md:1394-1405`，`utils/cli_ci_run_observation.py:849-879`）。
- fixed-profile下再次`runner_candidate_invalid`会停止整个B2并重新进入owner investigation；B2仍`unadjudicated`、overall non-ready，Agent不替用户接受B2。

## Assumptions tested

1. current R2/R3 input是否被错误算作completed group：否，SQL排他终点与独立anchor直接证明。
2. baseline是否会消费R2 FY2025 intent：否，selected source refs只来自completed R1；current anchor受保护。
3. R3是否靠Memory/analyzer重算FY2025：否，completed R2 EventLog group是直接material owner。
4. reactive能否补入同Run新tool result：否，snapshot时序直接排除。
5. `24`是否等于每Run全部calls：否；plan已按force-answer修成25。
6. reactive cap是否等于per-chain cap：否；plan已分别计算per-Run proactive/reactive hard上界。
7. B1/B2能否共用可写publication root：否；最新plan已物理/身份分离。

## Material findings

**无。**

## Open questions / residual risks

- fresh MiMo可能让R2/R3自然不触发、target首attempt即accepted、或无法自然形成五次reject；按plan写`needs-more-evidence`，不阻塞本gate。
- 首次provider前必须实际保存并核对fixed profile、candidate calibration和harness/config/inventory identities；任一缺失按plan停止，不能事后回填。
- cold analyzer `compactor_responses=0` 与provider-native request id unavailable继续作为B1 residual question；不扩入本work unit。
- 旧seq327 cause继续为`unresolved/no causal inference`；fresh成功不得反向解释它。

## Final conclusion

- plan review：**PASS**
- provider gate：可由Controller在另一独立review也PASS且Gateflow条件满足后开启
- B1 Oracle planned status：`accepted`
- B2 Oracle：`unadjudicated`
- overall readiness：`non-ready`
