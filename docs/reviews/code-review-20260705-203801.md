# Code Review

## Scope

- Mode: current changes
- Branch: phaseflow/host-issues-control
- Base: 35d947ea (WU-WAIT-04 accepted plan commit) + current workspace changes
- Output file: docs/reviews/code-review-20260705-203801.md
- Included scope: S1 changed files only — `dayu/service/host_assembly.py`, `dayu/fins/ingestion/wait_adapter.py`, `tests/service/test_host_assembly.py`, `dayu/fins/README.md`, `tests/README.md`
- Excluded scope: `docs/host/issues-implementation-control.md` (controller dirty file, not S1 implementation logic); `docs/reviews/wu-wait-04-s1-implementation-codex.md` and `docs/reviews/wu-wait-04-s1-controller-validation.md` (read for context only)
- Parallel review coverage: 无

## Findings

未发现实质性问题。

### 详细逐目标核验

**1. `ServiceAssemblyOverrides.wait_poller_policy` typed opt-in 与传播链**

- `ServiceAssemblyOverrides.wait_poller_policy: WaitPollerRuntimePolicy | None = None` (`host_assembly.py:176`) — 类型正确，默认 `None` 保持向后兼容。
- `_compose_options` 线 839: `wait_poller_policy=request.overrides.wait_poller_policy` — 直传，无转换、无静默丢失。
- Host 消费端 `open_host.py:1058-1064`: `None` 检查 → `enabled` 检查 → 正确短路。
- 传播链完整：`ServiceAssemblyOverrides` → `compose_open_host_options` → `_compose_options` → `OpenHostOptions` → Host `open_host`。

**2. `build_fins_wait_poll_adapter_registry` 校验与构造**

- `wait_adapter.py:272-295`: 复用 `_deterministic_tool_names(tool_names)` 校验非空、受支持工具名、重复检测。
- 使用稳定 `FINS_INGESTION_WAIT_ADAPTER_KEY` (`wait_adapter.py:71-73`)，与 wait adapter binding 和 activation adapter 保持一致。
- 单个 `WaitPollAdapterRegistration` 注册项 — 正确，因为 poll adapter 按 `adapter_key` 分发（非按 tool_name），而 wait adapter binding 已在 tool_name → adapter_key 层完成路由。
- `tool_names` 仅用于装配期校验（注释明确标注），不新增 poller 逻辑，语义正确。

**3. `_tooling_options_from_discovery` 与 `_fins_wait_poll_adapter_registry_from_provider_configs` 数据流**

- `host_assembly.py:1723-1725`: `available_tool_names` 提取为 `frozenset`，供三个 registry builder 复用。DRY 改进，无副作用。
- `host_assembly.py:1837-1868`: `_fins_wait_poll_adapter_registry_from_provider_configs` 与 `_fins_wait_activation_registry_from_provider_configs` 结构对称：
  - 同源调用 `_fins_awaiting_registry_inputs_from_provider_configs` → `None` guard 正确。
  - `fins_awaiting_runtime is None` fail-fast → 正确，防御性检查。
  - `isinstance(fins_awaiting_runtime, FinsIngestionRuntime)` fail-fast → 正确，运行时类型守卫。
  - 使用同一个 `fins_awaiting_runtime` 构造 poll adapter → 正确，与 tool callable、activation adapter 共享运行时。
- Host 消费端 `open_host.py:1066-1080`: `wait_poll_adapter_registry` 缺失时 `HostApiError(INVALID_STATE)` fail-fast → 在 `open_host` 装配期即暴露配置错误，不会到运行时才发现。

**4. 测试覆盖**

已验证覆盖点（均通过）：

| 测试 | 覆盖场景 |
|---|---|
| `test_compose_open_host_options_uses_runtime_tuning_from_config` (line 221) | 默认 `wait_poller_policy is None` |
| `test_compose_open_host_options_passes_explicit_wait_poller_policy` (line 326) | 显式 `WaitPollerRuntimePolicy` override 等值到达 `OpenHostOptions` |
| `test_tooling_options_without_fins_awaiting_providers_has_no_wait_adapter_registry` (line 799) | 无 Fins awaiting provider → 三个 registry 均为 `None` |
| `test_tooling_options_binds_fins_wait_adapter_registry_for_enabled_awaiting_providers` (lines 847, 871-875) | 启用 awaiting providers → poll registry 存在，adapter 类型正确，runtime 共享（`is` 检查） |
| `test_service_fins_awaiting_wiring_uses_shared_runtime_for_activation` (lines 906, 917-922) | discovery → tooling → activation → poll adapter 全链路共享同一个 runtime（`is` 检查） |
| `test_tooling_options_skips_wait_adapter_for_missing_awaiting_tool_definition` (line 977) | provider tool 不在 ToolBundle → 三个 registry 均为 `None` |
| `test_tooling_options_skips_wait_poll_adapter_for_disabled_awaiting_provider` (line 1011) | 禁用 provider → 三个 registry 均为 `None` |
| `test_fins_awaiting_provider_workspace_root_mismatch_fails_before_open_host` | workspace 不一致 → 在 wait adapter registry builder 阶段 fail-fast（间接覆盖 poll registry 路径的同源校验） |

未覆盖边界：`fins_awaiting_runtime is None` 但 `registry_inputs` 非 `None` 的 fail-fast 分支在 poll adapter builder 中无独立测试。该分支与 activation adapter builder 结构完全相同，且生产路径中 runtime 创建与 registry 构造由同一调用链保证一致性，风险极低。

**5. Public-contract-only 边界**

- 无 UI-facing wait record query — 确认。
- 无 durable wait row 从 Service/UI 路径读取 — 确认。
- 无 manual resolve 作为生产级验证 — 确认。
- 无 Host/Engine contract drift — 确认。`OpenHostOptions.wait_poller_policy`、`HostToolingOptions.wait_poll_adapter_registry`、`WaitPollerRuntimePolicy`、`WaitPollAdapterRegistry` 均为 Host 已有 public types。Service 层仅装配 Host public typed inputs。

**6. README 更新**

- `dayu/fins/README.md`: 新增 `build_fins_wait_poll_adapter_registry(...)` 到 Fins ingestion awaiting provider 列表，更新 "Wait adapter 与 Host resume" 段落提及 poll adapter registry。内容均为当前已实现能力，在 README 的 Agent 更新约束范围内（"当前代码已实现"、"架构边界"）。
- `tests/README.md`: 更新 Service composition、Fins awaiting assembly、tests/fins 三个段落的覆盖描述。内容均为已存在的测试事实，符合测试手册职责。

**7. AGENTS 约束**

- 所有新增/修改函数和类均有完整中文 docstring（含参数、返回值、异常说明） — 确认。
- 无 `Any`/`object`/无类型签名 — 确认（pyright 0 errors 佐证）。
- 无 magic strings 超出 schema-style 例外 — 确认（错误消息使用字符串字面量，属 schema exception 允许范畴）。
- 无兼容性 facade/re-export/wrapper — 确认。

## Open Questions

无。

## Residual Risk

- S1 未覆盖真实 background poller E2E resolution（属于 S2 public-only awaiting E2E smoke）。
- `fins_awaiting_runtime is None` + `registry_inputs` 非 `None` 的 fail-fast 分支在 poll adapter builder 中无独立测试（结构上与 activation adapter builder 完全对称，且生产路径保证一致性，风险极低）。
- 未新增 runtime config schema for poller enablement；当前仅支持显式 typed `WaitPollerRuntimePolicy` override（S1 设计范围内）。
