# Code Review（re-review）

## Scope

- Mode: current changes（Gateflow UF-FIX07 Slice 2 code review re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `9d0a2f8a`
- Output file: `docs/reviews/code-review-20260815-202058.md`
- 上一轮 review artifact：`docs/reviews/code-review-20260815-201037.md`（DS：1 条低危 Finding 1）
- Fix artifact：`docs/gateflow/uf-fix07-slice2-review-fix-20260815.md`（`REVIEW FIX COMPLETE / RE-REVIEW PENDING`）
- Included scope: 同一 Slice 2 文件集合，重点复核 Finding 1 修复所触及的 `dayu/fins/upload_format_contract.py`、`dayu/fins/tools/upload_tools.py`、`tests/fins/test_upload_format_contract.py`、`tests/fins/test_fins_ingestion_tools.py`，并复核上一轮全部结论。
- Excluded scope: Slice 3/4、Host/Engine/runtime/storage、registry/oracle/evidence。主控已明确：既有 delete+files adapter 英文消息与 schema `maxItems`/ingestion 上限 owner design 仅记录、本轮不扩修。
- Parallel review coverage: 无。单一 reviewer 独立复核。

## Finding 最终状态（Gateflow 闭集）

| Finding | 主控裁决 | 复核结论 | Gateflow 最终状态 |
| --- | --- | --- | --- |
| DS Finding 1：material-primary failure 为英文且 ValueError hint 未含 primary | `accepted` | 修复完整成立，详见下方复核证据 | `已修复` |

MiMo review（`docs/reviews/code-review-20260815-200314.md`）无 finding，无需状态流转。

## Finding 1 修复复核（逐项证据）

### 1.1 failure 文本归属 typed projection owner，无文案双真源

- 唯一真源：`dayu/fins/upload_format_contract.py:614-616` 在 `project_fins_upload_format_text()` 内产生
  `upload_tool_material_primary_failure = "upload_kind=material 不得提供 primary；请省略 primary 字段"`，
  写入 `FinsUploadFormatTextProjection.upload_tool_material_primary_failure`。
- schema 消费：`upload_format_contract.py:633-637` 的 `upload_tool_primary` 描述逐字内嵌
  `f"{upload_tool_material_primary_failure}。"`，schema 与 failure 字段在构造期同源。
- adapter 消费：`upload_tools.py:389` `raise ValueError(FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure)`，
  机械消费同一 frozen 字段，无第二份文本。
- grep 复核：`must be omitted for material` 已无任何残留；`请省略 primary` 仅存在于
  `upload_format_contract.py:615` 一处生产定义。
- 结论：无文案双真源。中文、可行动、自解释。

### 1.2 hint 收敛为唯一 Final 且含 primary

- `upload_tools.py:58-60` 新增 `_INVALID_ARGUMENT_HINT: Final[str]`，文案
  `请检查 ticker、upload_kind、action、files、primary、会计期间和材料字段后重试。`
- 唯一消费点：`upload_tools.py:123` ValueError 分支。OSError/Exception/Corruption 分支各自保留既有专用 hint，
  均不适用本 finding，无漂移面。
- 该常量写法与 `dayu/fins/tools/fins_tools.py:82`、`dayu/tools/doc_tools.py:77` 既有工具模块惯例一致。
- 结论：无 hint 漂移；owner 级 filing primary 错误（如 `--primary 只能指定一次`）经同一分支投影时也携带含 primary 的 hint。

### 1.3 schema 与 tool outcome 精确一致

- `tests/fins/test_fins_ingestion_tools.py`（`test_upload_tool_material_primary_failure_is_owned_and_has_zero_side_effects`）：
  - `outcome.result.message == FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure`（精确相等）；
  - `outcome.result.hint == "请检查 ticker、upload_kind、action、files、primary、会计期间和材料字段后重试。"`（精确相等）；
  - `outcome.result.error == "invalid_argument"`；
  - `state_repository.calls == []`、`executor.submitted_job_ids == ()`、`runtime._observations == {}`、
    job store 无文件、且新增了上一轮 residual risk 指出的 workspace tree 快照前后对比
    （`_snapshot_tool_workspace_tree(workspace_root) == before_tree`）。
- `test_upload_tool_adapter_projects_zero_or_one_filing_primary_selector` 中
  `str(material_error.value) == FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure`（精确相等，替代原英文 match）。
- `test_upload_tool_calendar_year_schema_and_usage_messages_are_business_neutral` 中
  `FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure in str(primary_schema["description"])`
  （schema 内嵌逐字断言）。
- `tests/fins/test_upload_format_contract.py` 中
  `assert f"{tool_material_primary_failure}。" in tool_primary_text` 与精确全文断言。
- 结论：schema 描述、adapter ValueError、ToolFailedOutcome.message 三者逐字同源，无 schema/tool outcome 不一致。

### 1.4 无 adapter fallback / 无新增内部术语 / 无越界

- adapter 为直接 raise，无 fallback、无 `hasattr/getattr`、无第二套分发。
- 新 failure 文本与 hint 均无 Path/tuple/Host/Engine/asset identity 等内部术语；既有负向断言
  （`test_upload_tool_calendar_year_schema_and_usage_messages_are_business_neutral` forbidden_fragment 循环）继续通过。
- fix 未触碰 delete+files adapter 英文消息（`upload_tools.py:476` 保持原样）与 `files.maxItems=100` owner design，
  符合主控边界，不扩修。
- 修改文件仍全部位于 Slice 2 白名单；未修改 CLI 行为、Slice 1 owner、Host/Engine/runtime/storage/Docling/README/
  registry/oracle/evidence 与任一 review artifact（git status 复核）。

## 上一轮全部结论复核

- 逐项清单（append 不吞重复 occurrence、path projection 无业务判断、raw selectors 生效链、schema shape、
  filing 0/1 selector 与 material fail closed、invalid combos 零副作用、唯一文本 owner、无首项/顺序推断、
  无 Host/Engine drift、严格类型/docstring/tests）全部维持通过；fix 未改变任何生产行为路径，仅收敛文案与 hint。
- 独立复验：
  - affected tests：`721 passed, 3 warnings in 16.48s`（与 fix artifact 的 721 一致；+1 为新失败投影测试）；
  - targeted pyright：`0 errors, 0 warnings, 0 informations`；
  - `git diff --check`：exit 0；
  - 单生产文件 branch coverage：`arg_parsing.py 99%` / `commands/fins.py 81%` / `upload_tools.py 92%` /
    `upload_format_contract.py 89%`，全部 ≥80%，与 fix artifact 一致。
- 上一轮 Open Questions（无）维持。

## Findings

### 未发现实质性问题

修复后的 Slice 2 未发现新的实质性问题。上一轮 Finding 1 已 `已修复`；无新引入的文案双真源、adapter fallback、
hint 漂移、schema/tool outcome 不一致或新 blocker。

## Open Questions

- 无。

## Residual Risk

维持上一轮记录并按主控边界收紧：

- 既有 delete+files adapter 英文消息（`upload_tools.py:476`）：`assigned to later work unit`，主控明确本轮不处理，
  owner/destination 为后续 LLM-facing 文案收敛或 registry/evidence work unit。
- schema `files.maxItems=100` 与 `ingestion_runtime._MAX_TUPLE_ITEMS` 两份独立字面量：`assigned to later work unit`，
  accepted plan 明确边界，本 fix 不改。
- CLI usage matrix 未覆盖 delete+primary/delete+files 的 CLI 级 case（owner 级与 tool 级已覆盖），风险很低。
- `test_upload_tool_invalid_primary_fails_before_state_or_observation_registration` 未做 workspace tree 快照对比
  （forbidden state repo + observation/job 零副作用断言等价；新增的 material-primary 测试已含 tree 快照）。
- Slice 3 asset/storage/process、Slice 4 README/closeout、UF-FIX08/10/11、registry/evidence 与 UF-PF07/UF-PF12：
  均按 accepted plan 归属后续 slice/work unit，不视为本 slice defect。

## Conclusion

- Re-review 结论：`pass`。
- Finding 1 最终状态：`已修复`；无新增 finding；无 blocking open question；无未分类 residual risk。
- 本 gate 仅 review，未修改代码/测试/artifact，未 stage/commit/PR/真实 evidence。
- Next entry point：由 AgentController 裁决进入 slice acceptance 或下一 gate。
