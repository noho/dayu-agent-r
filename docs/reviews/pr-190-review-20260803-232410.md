# Code Review — PR 190 独立 Re-review（第二路）

## Scope

- **Mode**: PR Re-review（同一 PR-review gate 的第二路独立复核）
- **PR**: 190 — fix(cli): close interactive conformance gaps
- **Author**: noho
- **Head**: codex/interactive-oracle (b819309c)
- **Base**: main (113ea34d)
- **Mergeable**: CLEAN
- **Draft**: true
- **Checks**: no reported checks on the branch
- **Output file**: docs/reviews/pr-190-review-20260803-232410.md
- **Preservation set（原样保留，供后续 rereview 读取）**:
  1. `docs/reviews/pr-190-review-20260803-225333.md`（第一路初审）
  2. `docs/reviews/pr-190-review-20260803-225520.md`（第二路初审）
  3. `docs/gateflow/pr-190-pr-review-adjudication-no-code-resolution-20260803-231451.md`（控制器裁决）
- **Review independence**: 本 re-review 完整读取并保留上述三份 artifact，独立回到代码、设计真源与测试逐项复核，不因两路初审一致而投票

---

## Re-review Method

逐项复核控制器对 F-01 至 F-07 的 disposition，每项以直接代码/设计/测试证据独立判断：

1. 先读取控制器声称的直接证据行号，在文件中确认其存在且含义与控制器描述一致
2. 再主动搜索可能反驳 disposition 的相反证据（旧引用残留、未文档化的消费者、隐藏分支）
3. 对 deferred 项，确认债务是否非阻塞（不改变运行时语义、不影响 correctness）
4. 对 rejected 项，确认 finding 的前提是否确实不成立
5. 最后做一轮独立的 adversarial pass，搜索两份初审和控制器均未覆盖的 new blocker

---

## Controller Disposition 逐项复核

### F-01：九个 readable-view 类型仍使用 `VNext` 后缀

- **控制器 disposition**: `deferred-with-owner`
- **控制器证据**: merge base 已存在 9 个名称；追溯至 2026-06-05 commit `473dd5eb`；类型名不参与 schema literal、候选验收、持久化或运行时分支
- **独立复核结论**: **disposition 成立，证据充分**

**直接证据（独立收集）**:

1. `git merge-base main codex/interactive-oracle` → `113ea34d47b95812d79aa31705949bbb46bc6061`
2. `git show 113ea34d:dayu/host/compaction.py | grep 'class.*VNext'` → merge base 中存在 27 个 `VNext`-suffixed 类（ReadableFactItemVNext 等 9 个 + ConversationCompactLabelSectionVNext 等 18 个已在本 PR 中被重命名为 V2）。PR 已清理 18/27，剩余 9 个 readable-view 类型是未完成部分
3. `git log --all --oneline -S 'TraceReadableKindVNext' -- dayu/host/compaction.py` → 最早出现在 `473dd5eb` (2026-06-05, WU-CM-01 #116)
4. 当前 head 中 9 个名称的消费者：`dayu/host/compact_material.py`（material pack builder）、多个 Host tests。全部消费者按类型注解引用，不依赖名称后缀做分支
5. 类型名称不进入 `COMPACT_INPUT_SCHEMA_V2` / `COMPACT_OUTPUT_SCHEMA_V2` 这两个 schema literal，也不进入 durable payload 或 LLM-facing prompt

**阻塞性判断**: 非阻塞。债务在 PR merge base 已存在；PR 已将其从 27 个减少到 9 个；部分重命名会制造同一 owner 的两次不完整迁移

---

### F-02：`compaction.py` 剩余 `vNext` docstring

- **控制器 disposition**: `deferred-with-owner`（与 F-01 同一 destination）
- **控制器证据**: merge base 95 行 → 当前 61 行（PR 已减少 34 行）；docstring 不进入 LLM-facing prompt、不改变 schema/durable state
- **独立复核结论**: **disposition 成立，证据充分**

**直接证据（独立收集）**:

1. `git show 113ea34d:dayu/host/compaction.py | grep -c 'vNext'` → 95
2. `grep -c 'vNext' dayu/host/compaction.py` → 61
3. 当前 diff 仅新增一行含 `vNext` 的文案，是把 base 中的旧术语延续到变更行（"返回与实际成功 Runner call 身份配对的 vNext proposal"）
4. `grep -rn 'vNext' dayu/host/context_governance.py dayu/host/compaction_terminal.py dayu/host/compact_pipeline.py dayu/config/prompts/scenes/` → 这些文件已清零 vNext 术语

**阻塞性判断**: 非阻塞。docstring 不影响 correctness；分拆修复会导致同一 owner 的两次不完整迁移

---

### F-03：`CompactPipelineAttemptDispatchSnapshot` 是空 Protocol

- **控制器 disposition**: `rejected-with-reason`
- **控制器证据**: 是 opaque lower-layer seam；两个生产 provider 均 `del snapshot`；全仓无 lower seam 消费其成员
- **独立复核结论**: **disposition 成立，reject 理由正确**

**直接证据（独立收集）**:

1. `dayu/host/compact_pipeline.py:169-170`：docstring 明确 "pipeline-owned second-read provider hook 的 attempt snapshot 协议"
2. `dayu/host/compact_pipeline.py:1-6`：模块限定为薄 helper contract，不读取 EventLog、不创建 Attempt、不推进 lifecycle
3. `dayu/host/api.py:715-740` 的 `AttemptDispatchSnapshot` 是 concrete value 的真源
4. 两个 provider implementation（`_NoopProtectedRecentRawTailProvider`、`_DurableProtectedRecentRawTailProvider`）均显式 `del snapshot`，全仓无 lower seam 对 Protocol 成员的消费
5. raw-tail 所需真实数据由另外三个窄协议参数（`current_facts`、`memory`、`compact`）独立提供

**原 finding 为何不成立**: finding 将"Protocol 无成员"视为缺陷，但该 Protocol 的语义正是"不允许下层依赖其 shape"。添加 `AttemptDispatchSnapshot` 的具体字段会泄漏上层 concrete shape 到不消费它的下层 helper。用 `object` 替代违反项目 `禁止使用 object` 约束。

---

### F-04：`InteractiveComposerCompletionResult` union 过宽

- **控制器 disposition**: `rejected-with-reason`
- **控制器证据**: union 精确描述 `asyncio.wait` 的异构 task result 上界；任务身份在 await 前已知
- **独立复核结论**: **disposition 成立，reject 理由正确**

**直接证据（独立收集）**:

1. `dayu/cli/session_execution.py:2144`：union = `_InteractiveComposerCompletion | EntrypointRunTerminalResult | str | int`，精确对应 `asyncio.wait` 集合中四类 task 的返回类型
2. 各 task 的窄类型注解：
   - `_InteractiveActiveTurn.submit_task`: `asyncio.Task[EntrypointRunTerminalResult]`
   - `_ActiveTurnCloseout.cancel_task`: `asyncio.Task[EntrypointRunTerminalResult]`
   - acceptance tasks: `asyncio.Task[str]`
   - SIGINT monitor task: `asyncio.Task[int]`
3. Driver 先按 task identity 判断（`composer_task in done`, `current_acceptance_task in done`, `queued_acceptance_task in done`, `sigint_task in done`, `active.submit_task in done`），再 await 已识别的窄变量；不从 union result 值猜测语义类型
4. 改为 `set[asyncio.Task[object]]` 直接违反项目编码硬约束 "禁止使用 object"

**原 finding 为何不成立**: union 不是对业务结果做模糊分支，而是在类型层面为 `asyncio.wait(FIRST_COMPLETED)` 的异构容器提供精确上界

---

### F-05：composer 与 driver 分裂 phase ownership

- **控制器 disposition**: `rejected-with-reason`
- **控制器证据**: phase storage 唯一 owner 是 composer 的 `_phase` 私有字段；driver 通过 public Protocol 调用 `set_phase`
- **独立复核结论**: **disposition 成立，reject 理由正确**

**直接证据（独立收集）**:

1. Phase storage owner: `dayu/cli/composer.py:323` `_phase: InteractiveComposerPhase` — 私有字段，仅在 `PromptToolkitInteractiveComposer` 类内读写
2. Public protocol: `dayu/cli/composer.py:216` `InteractiveComposer.set_phase(self, phase: InteractiveComposerPhase) -> None` — 公开方法声明
3. Driver 调用: `dayu/cli/session_execution.py` 中 7 处 `composer.set_phase(...)` 调用（lines 1797, 1878, 1959, 2047, 2104, 2106, 2397）——全部通过 public Protocol，无直接字段访问
4. Composer 内部同步 transition:
   - Enter chord 解码时立即进入 `SUBMITTING`（line 574）——必须在 event-loop 调度前完成
   - `accept_submit` 原子清理 pending 后进入 `RUNNING`（line 418）
   - `reject_submit_delivery` 恢复文档后进入 `IDLE`（line 445）
5. Driver 驱动的 lifecycle transition（SUBMITTING/RUNNING/CANCELLING/IDLE）与 composer 内部 transition 是互补关系：driver 负责何时发生，composer 负责保存并投影为按键语义

**原 finding 为何不成立**: finding 将"transition 调用者"误判为"状态 owner"。phase 的存储、读取、按键解释唯一属于 composer；driver 只是通过公开协议协调 Host lifecycle transition

---

### F-06：`intent_type` / `reason` 从 enum 变为 free-text

- **控制器 disposition**: `rejected-with-reason`
- **控制器证据**: prompt 自足声明开放语义；所有生产消费者不按旧 enum 分支；parser 与 prompt 同源一致
- **独立复核结论**: **disposition 成立，reject 理由正确**

**直接证据（独立收集）**:

1. Prompt owner（`conversation_compaction_user.md`）:
   - `intent_type`: "非空字符串，表示业务可读的后续动作类别，例如 `next_analysis_step`；不得写系统调度状态、程序类型或内部错误码"
   - `reason`: "非空字符串，说明后续对话为什么仍需保留该指代、术语或对象关系"
   - 同一 schema 中 `status` 和 drop `reason` 明确列出闭集值，形成清晰对照
2. Parser（`llm_compaction.py`）: 两字段使用 `_required_string`；typed contract `CompactForwardIntentV2.intent_type: str`、`CompactReferenceContinuityV2.reason: str`
3. **全量生产消费者审计**（独立 grep 验证）:
   - `grep -rn 'ForwardIntentTypeVNext\|ReferenceContinuityReasonVNext\|open_question\|pending_clarification\|pending_user_visible_task\|next_step_note\|local_reference\|ordinal_reference\|ellipsis_recovery\|recent_state' dayu/host/ --include='*.py' | grep -v test` → **零命中**
   - 无一生产代码按旧 enum token 执行分支
4. 消费者行为:
   - Context Governance: 仅将 canonicalized `intent_type+text` 用作 duplicate identity
   - compact material / Memory: 只复制、序列化/反序列化
   - RunInput: 只渲染为业务可读上下文
5. `tests/host/test_context_compact_events.py` 以 `custom_follow_up` 和 `retain_for_next_comparison` 证明 parser 接受任意非空业务文本

**原 finding 为何不成立**: finding 的假设前提"下游消费者需要防护"不成立。经全量审计，零生产消费者依赖旧枚举值做分支。新增 allowlist 会改变已冻结的 prompt/schema 语义，并把已移除的旧 enum 重新引入 accept owner

---

### F-07：`source_boundary_refs[0]` 是未文档化隐式契约

- **控制器 disposition**: `rejected-with-reason`
- **控制器证据**: design doc 明确文档化位置 contract；parser owner 一次性解码并投影为 typed fields；消费者不索引 raw list
- **独立复核结论**: **disposition 成立，reject 理由正确**

**直接证据（独立收集）**:

1. `docs/host/design.md:3429`（独立 grep 验证存在且内容一致）:
   > 持久化顺序是 contract：第一项必须是 `current_input_ref`，其余去重 refs 是本次 accepted compact 实际覆盖的 `compacted_source_refs`
2. Design doc 同段明确禁止消费者索引 raw list、按 ref 前缀猜角色、或从 event sequence/timestamp 推断覆盖范围
3. Parser owner（`compact_payload.py:175-211`）:
   - `source_refs[0]` → `current_input_ref`（line 193）
   - 完整 tuple 校验: `source_refs == (current_input_ref, *compacted_source_refs)`（line 209-210）
   - 后续 typed fields 投影: `source_boundary`、`represented_coverage`、`explicitly_dropped_coverage`
4. 两个 writer 同源遵守: `compact_payload.py:780-784`、`compact_pipeline.py:288-299`
5. Memory consumer（`memory.py:1229-1240`）只消费 parser 已投影的 `compacted_semantics.current_input_ref` 与 `.compacted_source_refs`，不索引 raw list

**原 finding 为何不成立**: finding 的事实前提"未文档化"不成立——design doc 明确文档化了位置 contract。raw index 仅在指定 parser owner 内用于解码，随后立即投影成 typed fields

---

## 独立 Adversarial Pass

在复核控制器 disposition 之外，执行一轮独立的 adversarial pass，搜索两份初审和控制器均未覆盖的 new correctness 或 conformance blocker。

### 检查项 1：旧类型/旧函数/旧常量残留

- `grep -rn 'CompactQualityCheckResultVNext\|check_conversation_compact_output_vnext\|ConversationCompactOutputVNext\|CONVERSATION_COMPACT_OUTPUT_SCHEMA_VERSION_VNEXT' dayu/host/ --include='*.py' | grep -v test` → **零命中**
- `grep -rn 'conversation_compact_input_vnext_from_material_pack\|conversation_compact_label_looks_stale_vnext' dayu/ --include='*.py' | grep -v test` → **零命中**
- **结论**: 无残留

### 检查项 2：`_COMPACT_ACCEPTANCE_PERMIT` 排他性

- `grep -rn '_COMPACT_ACCEPTANCE_PERMIT' dayu/host/ --include='*.py' | grep -v test`:
  - `compaction.py:1682`: 定义
  - `compaction.py:1711`: `CompactAcceptedTruthV2.__post_init__` 校验
  - `context_governance.py:39`: 导入
  - `context_governance.py:113`: 使用
- **结论**: 仅有 governance owner 导入并使用。排他性成立

### 检查项 3：`_validate_committed_coverage` 完整性

- `dayu/host/compact_payload.py:350-378`: 覆盖 7 项不变量校验:
  1. represented 遵循 root boundary 顺序
  2. dropped 遵循 root boundary 顺序
  3. represented ∩ dropped = ∅
  4. represented ∪ dropped = boundary（exact partition）
  5. represented coverage == candidate-derived sections
  6. dropped coverage == candidate drops
  7. current_input_ref 不在 compacted_source_refs 中
- **结论**: 校验完整

### 检查项 4：CONTEXT_COMPACTED payload schema 完整性

- `_COMPACTED_REQUIRED_FIELDS` 包含:
  - `source_boundary`（新增）
  - `represented_coverage`（新增）
  - `explicitly_dropped_coverage`（新增）
  - `source_boundary_refs`（保留，与 typed boundary/coverage 交叉校验）
  - `successful_response_identity`（新增）
  - `accepted_proposal_manifest_ref` / `accepted_proposal_manifest_digest`（新增）
- Builder（`build_context_compacted_payload`）写入全部字段
- Validator（`validate_context_compacted_payload`）通过 `_require_exact_fields` 强制全部必填字段存在
- **结论**: payload schema 完整

### 检查项 5：Engine 契约 public export

- `dayu/engine/__init__.py`: 正确导出 `SuccessfulRunnerResponseIdentity`、`ProviderRequestIdAvailability`
- `dayu/engine/contracts/__init__.py`: 正确 re-export
- `dayu/engine/agent.py:606-614`: 正确构造 `SuccessfulRunnerResponseIdentity`
- **结论**: Engine 契约集成完整

### 检查项 6：无跨层反向依赖

- Engine 层对 Host 层的 import: **零**
- `dayu/runtime` 对上层 import: **零**
- Host 层对 CLI/Service 层的 import: **零**
- **结论**: 分层依赖正确

---

## Findings

### 本 re-review 新增 finding: 无

经独立复核控制器对 F-01 至 F-07 的全部 7 项 disposition，每项均以直接代码/设计/测试证据独立验证，未发现控制器的裁决错误或遗漏。

经独立 adversarial pass 搜索旧类型残留、permit 排他性、coverage 校验完整性、payload schema 完整性、Engine 契约集成与分层依赖，未发现新的 critical、high 或 medium correctness/conformance blocker。

两份初审中的其他 Open Questions/Residual Risk 条目（multi-pass 测试覆盖、real compactor 条件跳过、v1→v2 兼容迁移、recovery tier provenance）未以独立、证据完整的 finding 形式在本 re-review 中重新提出；这些条目属于测试覆盖增强和后续 work unit 的范畴，不构成当前 PR correctness blocker。

---

## Verdict

**本 re-review 确认控制器全部 7 项 disposition 正确。当前 head (b819309c) 不存在新的 critical、high 或 medium correctness 或 conformance blocker。Deferred naming debt（F-01、F-02）非阻塞；五项 rejected finding（F-03 至 F-07）确实不成立。**

PR 190 的 compactor v1→v2 schema 迁移在 semantic ownership（`_permit` 哨兵）、coverage enforcement（exact partition）、terminal state management（事务内线性化）和 repair feedback bounding（硬上限）方面是正确的架构改进。F01–F07 交互一致性要求全部通过独立验证。

---

## Residual Risk

- PR 无 reported CI checks；merge 前需确认 CI pipeline 配置
- Real compactor smoke test 条件跳过（需 `DAYU_RUN_REAL_COMPACTOR_SMOKE=1`）；CI 中不会执行真实模型路径
- F-01/F-02 deferred naming debt 需后续专门 work unit 处理；本 PR 不做部分重命名
