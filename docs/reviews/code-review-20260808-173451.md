# F18 final tracked closeout 独立审查（DS）

## 审查结论

**FAIL**

当前 tracked closeout 对 B2 的状态裁决、尝试分类、F19 handoff 和顶层文件校验基本一致，但冻结的 public evidence bundle 不满足已接受 plan 的最小 publication contract，且 public summary 内部存在错误的 material artifact identity。因此不能把当前 publication 作为完成 final closeout 的自洽证据。

## 审查范围

- 当前分支：`codex/interactive-oracle`
- 审查起点：`HEAD=1c92a5bebdf6dd428eaf7577c5d97f5105079d6e`
- tracked diff：`docs/cli_ci.md`、`docs/gateflow/pr-190-f18-plan-20260808.md`、`docs/reviews/pr-190-oracle-adjudication-20260808.md`
- 新增 closeout material：`docs/gateflow/pr-190-f18-b2-material-amendment-20260808.md`、`docs/gateflow/pr-190-f18-final-closeout-20260808.md`
- 独立 plan review：`docs/reviews/plan-review-20260808-172014.md`、`docs/reviews/plan-review-20260808-172234.md`
- 外部 evidence：只读核验 bundle `pr190-f18-b2-fixed-XUmH8YBg` 的 public tree，并按需读取三次 attempt 的既有 private typed capture；未写 external bundle、未调用 provider。
- 按任务约束，未读取、修改或暂存 ownership 不明的 `docs/reviews/plan-review-20260808-095346.md`。

## Findings

### [Major] Public bundle 缺失 plan 强制要求的 execution index 与逐执行链 Host analysis artifacts

直接证据：

1. 已接受 plan 在 `docs/gateflow/pr-190-f18-plan-20260808.md:255-266` 明确规定 public minimum：
   - `observation-summary.json` 必须包含 per-chain budgets、actual counts、terminal refs 与 verdict；
   - 每条执行链必须有 path-redacted public Host resolver/Tool Trace analysis JSON/Markdown；
   - 必须有 `execution-index.json`，记录 workspace/config/harness identity、attempt/skip/seal reason 与 relative refs；
   - digest 与 final scan 必须在这些 publication writers 完成后生成并覆盖此前全部 public files。
2. 冻结 public tree 实际只有六个文件：`observed-behavior-report.md`、`observation-summary.json`、`material-audit-summary.json`、`pre-provider-calibration-summary.json`、`digest.json`、`secret-scan.json`。不存在 `execution-index.json`，也不存在两条已执行 provider chain 的 Host resolver/Tool Trace analysis JSON/Markdown。
3. 当前 `observation-summary.json:2-17` 的 attempts 只有 `outcome`、`reason` 和指向 private tree 的 `ref`；`provider_ledger` 仅在 `:46-51` 给出聚合总数，没有 plan 要求的 per-chain budget、actual count 和 terminal ref。
4. `secret-scan.json:2-27` 明确列出的最终被扫描对象只有五个现存文件，`scanned_file_count=5`、`scanned_byte_count=27450`。这证明 final scan 与当前不完整 tree 一致，不能证明 plan 要求的 publication 已完成。

影响：

- human closeout 无法仅凭 public evidence 逐链核验 attempt identity、terminal、预算、seal reason 及 public Host analysis；private capture 不能补造或替代 plan 明确要求的 public 字段。
- `docs/gateflow/pr-190-f18-final-closeout-20260808.md:17-28` 将该 bundle 声明为 frozen publication，但 publication owner 的最低契约尚未满足，plan/material/closeout 与实际 evidence tree 不同源。

修复边界：

- 在 publication owner 按既有格式补齐每条已执行链的 path-redacted Host resolver/Tool Trace JSON/Markdown、完整 `execution-index.json`，并让 summary 持有 plan 要求的逐链结构化字段；不得由 tracked closeout 文本、analyzer fallback 或 private-only refs 代偿。
- 完成后按 plan 顺序重建 digest、最终 secret/path scan，并刷新所有引用这些文件的 SHA。不得覆盖、隐藏或重标两条 non-covering 与一条 provider-not-started 结果。

### [Major] `observation-summary.json` 持有错误的 material audit SHA

直接证据：

1. public `observation-summary.json:33` 声明 `material_audit_sha256=37eee3c9665c21874b8caf545acd0a49af0e2d5faa69a52a257ff4702acf10e4`。
2. 对同目录 `material-audit-summary.json` 直接执行 SHA-256，结果为 `6399d1280a7c567ed96e563739f0338b32371f01ea3c578357582ff38c15af8f`；compact/canonical JSON hash也不等于 `37eee3...`。
3. public `digest.json:5-7`、`secret-scan.json:9-11` 与 tracked closeout `docs/gateflow/pr-190-f18-final-closeout-20260808.md:23` 都记录实际值 `6399d128...`。六个顶层文件自身的实际 SHA 与 closeout 列表完全相等，因此冲突位于 summary 的结构化 cross-artifact identity，而不是复算方法。

影响：

- public summary 对其 material owner artifact 作出不可验证的错误 identity 承诺；依赖 summary 的消费者无法证明 observation 使用的 material audit 与 bundle 中 artifact 相同。
- 外层 digest 虽能验证错误 summary 文件未被篡改，却不能把错误的内部业务引用变成正确事实。

修复边界：

- 由 summary/publication owner 从最终 material audit artifact 的实际 digest生成该字段，禁止在 closeout 展示层择一或解释性兜底。
- 因 summary 内容变化会改变自身 SHA，必须依次重建 summary、digest、final scan，并同步 tracked SHA refs。

## 已通过的独立核验

- 两条真实 provider segment 明确保留为 `RUN_SUCCEEDED` 但 non-covering：分别为 5 calls / 27.5583s 与 2 calls / 15.0313s；总计 7 ordinary calls / 42.5896s，compactor calls 为 0。没有隐藏、覆盖或重标 PASS。
- 第三条 private typed capture 直接显示 `formal segment requires a non-empty prompt`、`exit_code=null`、event sequence `0 -> 0`、`pty_actions=[]`、`run-terminals.evidence_status=not_observed`，与“CLI spawn 前拒绝、provider not started”一致。
- `docs/cli_ci_scenarios.json` 与 `docs/cli_ci_oracles.json` 相对 HEAD 无 diff；B2 仍为 `status=unadjudicated`、pending adjudication、after-adjudication applicable identity，registry 保持 `calibration`，overall readiness 未标 ready。
- F19 handoff 在 plan、handbook、adjudication 与 final closeout 中均要求新 work unit、新 deadline、新 bundle、fresh fixed-profile workspace，明确禁止复制 F18 durable state或覆盖既有 non-covering 结果。
- closeout 列出的六个 public 文件 SHA 均与实际文件完全相等；final scan 的五文件、27450 bytes、secret/path/errors `0/0/0` 与当前 public tree完全相等。该检查只证明当前不完整 tree 的完整性，不消除上述两个 findings。
- tracked diff `git diff --check` 通过；两个 registry JSON 可由 `jq` 解析。新增/修改行与本次新增 review/closeout artifacts 未检出绝对私有路径、credential pattern 或 trailing whitespace；public final scan同样为零命中。
- 本次无产品代码、schema、runtime、harness 或测试变更；未用 pytest、pyright、Ruff、compileall 冒充 evidence validation。

## Residual risk

- F18 真实模型没有触发 compactor；replacement、same-boundary repair、budget-exhausted fallback、accepted durable truth 与 reconnect 仍是 `needs-more-evidence`，这与当前 B2 `unadjudicated`/overall not-ready 一致。
- 旧 Trial2 sequence 327 owner exception不可恢复、cross-opener profile contract unspecified、cold analyzer `compactor_responses=0` 与 provider-native request id unavailable 仍按现有 closeout 保留；本审查没有把它们升级为 B2 mandatory gap，也没有把 B2 视为 accepted。

## Final verdict

**FAIL**。先修复 public publication contract 与 summary material identity，再重新生成 digest/final scan及 tracked SHA refs，才能重新进行 final closeout review。
