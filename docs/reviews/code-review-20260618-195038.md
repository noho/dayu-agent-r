# Code Review

## Scope

- Mode: current changes (focused re-review of aggregate fix)
- Branch: wu-cm-12-conversation-memory-drift
- Base: HEAD uncommitted changes only
- Output file: docs/reviews/code-review-20260618-195038.md
- Included scope:
  - `dayu/host/compact_material.py`: `_provenance_from_evidence_blocks` signature change (remove unused `evidence_blocks` param) and call site update
  - `tests/host/test_compaction_operation.py`: test rename from `test_evidence_chunks_share_same_durable_query_text` to `test_evidence_block_shares_durable_query_text_without_chunking`
  - `docs/reviews/wu-cm-12-fix-r1-aggregate-fix-codex-20260618.md`: previous Codex review artifact as background
- Excluded scope: all other files, committed changes, design docs beyond verification
- Parallel review coverage: 无

## Review Purpose

本 re-review 聚焦四个判断：

1. 已接受的两个 low findings 是否完全关闭
2. compact material 语义是否未改变
3. 是否引入新的 correctness、typing、测试或 AGENTS.md 违规
4. artifact 是否足以作为 fix gate 证据

## Pre-review Verification

- `pytest tests/host/test_compact_material.py tests/host/test_compaction_operation.py -q`: 90 passed in 0.49s
- `pyright dayu/host/compact_material.py tests/host/test_compaction_operation.py`: 0 errors, 0 warnings, 0 informations
- `git diff --check`: clean（无 whitespace 告警）

## Findings

### 判断 1：已接受 findings 是否完全关闭 — 已关闭

**Finding 1（移除 `_provenance_from_evidence_blocks` 未使用 `evidence_blocks` 参数）：**

- 入口/函数：`_provenance_from_evidence_blocks`（compact_material.py:2862）
- 直接证据：
  - 旧签名 `def _provenance_from_evidence_blocks(evidence_blocks, selected_blocks)` 中 `evidence_blocks` 从未被使用，仅通过 `del evidence_blocks`（原 line 2871）静默消除
  - 函数体实际逻辑始终通过 `selected_blocks` 过滤 `CompactMaterialSection.EVIDENCE_MATERIAL`（line 2871）
  - 新签名 `def _provenance_from_evidence_blocks(selected_blocks)` 移除了死参数和 `del evidence_blocks`
  - 唯一调用点 line 1036 已同步更新为 `*_provenance_from_evidence_blocks(selected_blocks)`
  - `evidence_blocks` 局部变量在调用点仍被 `_pack_evidence_blocks(selected_blocks)` 创建（line 1031），并在 `CompactMaterialPack` 构造中使用（line 1044）——未被误删
- 状态：**完全关闭** ✅

**Finding 2（测试名从 chunking 语义改为 no-default-chunk 表达）：**

- 入口/函数：`test_evidence_block_shares_durable_query_text_without_chunking`（test_compaction_operation.py:1632）
- 直接证据：
  - 旧名 `test_evidence_chunks_share_same_durable_query_text` 暗示 chunking 行为，但测试实际验证的是"默认不 chunk，单个 block 共享同一 durable query_text"
  - 新名 `test_evidence_block_shares_durable_query_text_without_chunking` 准确表达 no-default-chunk 语义
  - 测试 body（lines 1637-1726）与断言完全未变：验证单一 `E1` label、无子 label（`E1.1` 等）、单一 query_text、query_text 内容正确
- 状态：**完全关闭** ✅

### 判断 2：compact material 语义是否未改变 — 未改变

- 入口/函数：`build_compact_material_pack` → `_provenance_from_evidence_blocks` 调用链
- 直接证据：
  - `_provenance_from_evidence_blocks` 在修改前后均通过 `selected_blocks` 过滤 EVIDENCE_MATERIAL section 的 block，按 `_FIRST_ORDINAL` 枚举生成 `PromptLocalProvenanceEntry`
  - 旧参数 `evidence_blocks` 从未参与任何分支判断、数据变换或返回值构造——它在函数体内唯一引用是 `del evidence_blocks`
  - 调用点 `evidence_blocks = _pack_evidence_blocks(selected_blocks)` 仍然存在，仍用于构造 `CompactMaterialPack.evidence_material`（line 1044）——与 provenance 路径无关
  - provenance entry 生成的所有前置校验（`accepted_evidence_id`、`tool_result_event_ref`、`tool_call_event_ref`、非空 text）保持不变
  - 测试断言无任何修改，90 个测试全部通过
- 结论：compact material provenance 语义零变化

### 判断 3：是否引入新的 correctness、typing、测试或 AGENTS.md 违规 — 无

- **Correctness**：函数行为等价变换（移除死参数），无新分支、新副作用、新错误路径
- **Typing**：pyright 0 errors, 0 warnings；新签名 `tuple[RunInputMaterialBlock, ...] -> tuple[PromptLocalProvenanceEntry, ...]` 类型完整
- **测试**：测试 body 未修改，断言强度未削弱；无新增需覆盖的行为路径；现有 90 个测试全部通过
- **AGENTS.md / CLAUDE.md**：
  - 未修改架构边界、公共契约、状态机、durable schema 或 EventLog 语义
  - 未引入反向依赖、跨层泄漏、God object/function
  - 未违反 LLM-facing 语义约束（`_provenance_from_evidence_blocks` 是 Host 内部 helper，不进入 LLM 上下文）
  - 未触发 README 更新条件（详见下方 README 检查）

### 判断 4：artifact 是否足以作为 fix gate 证据 — 足以

- Codex review artifact（`docs/reviews/wu-cm-12-fix-r1-aggregate-fix-codex-20260618.md`）记录了：
  - 两个 fix 的具体改动
  - 验证结果（pytest 90 passed, pyright 0 errors, git diff --check clean）
  - README 检查结论
  - 残余风险评估
- 本 re-review artifact 补充了独立的逐项验证和语义不变性证明

## Open Questions

无

## Residual Risk

- 残余风险低。本次仅删除死参数和改测试名，不改变任何生产行为。
- `_provenance_from_evidence_blocks` 内部通过 `selected_blocks` 按 section 过滤，与 `_pack_evidence_blocks` 对同一批 `selected_blocks` 做同样过滤——两条路径独立过滤同一数据源。这是既有设计（非本次引入），不在本 re-review 范围。

## Conclusion

**PASS** — 两个已接受 findings 完全关闭，compact material 语义未改变，未引入新的 correctness、typing、测试或 AGENTS.md 违规，artifact 足以作为 fix gate 证据。无阻断 finding。
