# Code Review

## Scope

- Mode: current changes re-review（用户显式限定 `docs/cli_ci.md` 及本轮新增 registries）
- Branch or PR: `phaseflow/host-issues-control`
- Base: `main`
- Output file: `docs/reviews/code-review-20260710-141049.md`
- Included scope: `docs/cli_ci.md`、`docs/cli_ci_scenarios.json`、`docs/cli_ci_oracles.json`；复核 `code-review-20260710-135625.md` 的 001-007
- Excluded scope: 当前 branch 其它工作、尚未实现的 runner/ANSI replay/extractor/financial oracle 代码
- Parallel review coverage: 无；单一控制文档与两个最小 registry

## Findings

未发现实质性问题。

上一轮 findings 最终状态：

- 001 已修复：assessment preflight 在任何写入/dispatch 前执行；不再创建 per-run Phaseflow control_doc。
- 002 已修复：CLI CI 明确为只读 assessment；每个 candidate 使用独立稳定 WU identity，用户选择后重新启动正式 Phaseflow goal confirmation。
- 003 已修复：primary verdict 增加固定聚合优先级；full/focused pass 排除 unresolved oracle/blocked/limited；current-design mismatch 的 conformance 与 design-review 方向分离。
- 004 已修复：固定 `docs/cli_ci_scenarios.json` / `docs/cli_ci_oracles.json` 路径和最小 schema；初始状态显式为 `calibration`，不再默认伪装 full-real。
- 005 已修复：定义默认安全授权边界、可选 operator auth profile 字段和无 profile 时的一次主路径/无自动重试约束。
- 006 已修复：明确 SUT Fins path 与独立 financial oracle producer 边界、producer provenance 和非同源证明。
- 007 已修复：定义 CI-owned marker、bounded retention、pass/failure cleanup、latest unresolved workspace 和磁盘 fail-closed 边界。

## Open Questions

- 无。

## Residual Risk

- 当前 registries 处于 `calibration` 且为空，因此文档可执行的默认结果是 calibration，不是 full-real；完整 scenario/oracle 内容需要后续真实运行、用户裁决和正式 WU 逐步填充。
- runner、VT replay、evidence extractor 与 independent financial oracle producer 尚未实现；本文只冻结其控制契约，不能把文档 re-review 当作这些运行能力已经落地。
