# Plan Review — Re-review after plan-review-fix

## Reviewed Target and Scope

- Target: `docs/gateflow/pr-190-compactor-output-business-semantics-plan-20260803.md`（plan-review-fix 后版本）
- Scope: 仅核验三项内容：(1) controller 裁决记录是否正确；(2) Gateflow 自动推进/accepted checkpoint 权限是否正确；(3) 冻结业务语义与 scope 未被改坏。不重新 review 前次已通过的 owner boundary、reason 自足性、hash 路径或 README 触发判断。
- Evidence: 当前 plan 文件、前次三份 review artifact（`plan-review-20260803-212134.md`、`plan-review-20260803-214309.md`、`plan-review-20260803-214733.md`）、`dayu/host/compaction.py:98-101`（`COMPACT_FACT_SOURCE_KINDS_V2`）、`dayu/host/memory.py:1731-1733`（null → empty summary）。
- Durable input digests verified:
  - `pr-190-review-20260803-203709.md`: `e7add55e6c95c783ca8d92c8f8d15b223836851e70cfd73a902f15207d0d9841` ✓
  - `plan-review-20260803-212134.md`: `1d592ae41f6ed42b8b0c2e30fe37ebfa96751347859d2b0bf8ddb07aad46ae02` ✓

## Assumptions Tested

1. Controller 裁决记录是否与前次 review findings 一致。
2. Gateflow 元数据变更（plan status、gate、auto-advance、checkpoint permissions）是否合理。
3. 冻结业务语义（Exact LLM-facing wording semantics）是否未被修改。
4. Non-goals 是否正确纳入 controller 裁决的 deferred/rejected items。
5. Scope boundary 是否未被扩大或缩小。

## Findings

无 material findings。

### 验证详情

#### 1. Controller 裁决记录核验

**AgentMiMo：无 finding，plan review pass。**
- 与 `plan-review-20260803-214309.md` 结论一致（该 review 结论为 `pass`，无 material findings）。✓

**AgentDS F01（`forward_intents.status` / `reference_continuity.reason` 语义缺口）：`deferred-with-owner`。**
- 与 `plan-review-20260803-214733.md` 的 F01 一致（该 review 将其标记为低严重度、deferred scope gap）。✓
- owner 正确指定为后续独立 LLM-facing schema work unit。✓
- 当前 work unit 不扩 scope 的裁决合理——该缺口与 Goal Confirmation 已确认的字段范围无关。✓

**AgentDS F02（hash encoding 一致性）：`rejected-with-reason`。**
- 与 `plan-review-20260803-214733.md` 的 F02 一致（该 review 将其标记为低严重度，publication test 提供安全网）。✓
- rejection 理由正确：`sha256sum` 和 publication test 都读取最终 raw bytes，bytes 漂移会被 fail closed。✓

**Controller conclusion**："本次 follow-up 只纠正 Gateflow 推进与 checkpoint 权限元数据，不改变已冻结的字段语义、实现 slice 或验证范围。" — 与本 re-review 的 scope 一致。✓

#### 2. Gateflow 自动推进与 accepted checkpoint 权限核验

**Plan status 变更**：`plan-complete` → `plan-review-fix-complete`。合理——反映 plan 已完成 review fix 阶段。✓

**Gate 变更**：`plan` → `re-review`。合理——当前处于 plan review fix 后的 re-review 阶段。✓

**Next entry point 变更**：原来为 "plan review；用户明确要求本轮仅执行 plan gate，因此本轮不自动推进" → 现在为 "re-review；通过后按 Gateflow 自动进入 accepted plan commit、implementation 及后续 gates，在没有 stop condition 时持续推进到 `final closeout pass`"。

分析：
- 前次 review 时用户明确限制 "本轮仅执行 plan gate"，因此 plan 正确声明不自动推进。
- 当前 re-review 阶段，plan-review-fix 已完成，controller 裁决无阻塞 finding，自动推进到 accepted plan commit 是合理的 Gateflow 行为。
- "在没有 stop condition 时持续推进" 保留了 stop condition 机制，不是无条件推进。✓
- 自动推进范围限于 plan gate 的 intended artifacts（plan + review artifacts），不越权处理 implementation artifacts。✓

**Non-goals 中的 checkpoint 权限细化**（line 74）：
- 原来："不 stage、commit、push、修改 PR、mark ready、请求 reviewer 或发布外部 comment。"
- 现在："不在 gate 接受前 stage/commit/push 未接受的实现，也不把 unrelated dirty files 纳入 checkpoint；accepted plan gate 可 stage/commit/push 其 intended plan/review artifacts，后续 accepted checkpoint 同样只处理本 gate 的 intended artifacts。" + 新增 line 75："不 merge、approve、mark ready、请求 reviewer、删除分支或发布未获授权的外部 comment。"

分析：
- 细化了 stage/commit/push 权限：gate 接受前禁止，accepted 后只允许处理该 gate 的 intended artifacts。✓
- 明确 "unrelated dirty files" 不纳入 checkpoint。✓
- 将 "mark ready、请求 reviewer、merge、approve、删除分支" 保持为禁止。✓
- 权限边界清晰：accepted plan gate 可 stage/commit/push plan/review artifacts；后续 accepted checkpoint 只处理各 gate 的 intended artifacts。✓

**Completion report format 更新**（line 317）：
- 原来："Gate state：implementation 完成后进入 `code review`，不得跳过 gate、stage/commit/push 或宣称 work unit 完成。"
- 现在："Gate state：implementation 完成后进入 `code review`；implementation slice 在 code review 接受前不得 stage/commit/push，接受后进入 accepted slice checkpoint，并继续按 Gateflow 自动推进，不能跳过 gate 或提前宣称 work unit 完成。"

分析：
- 明确了 implementation slice 的 stage/commit/push 时机：code review 接受后才允许。✓
- "继续按 Gateflow 自动推进" 与 next entry point 的 auto-advance 一致。✓
- "不能跳过 gate 或提前宣称 work unit 完成" 保留了 Gateflow 约束。✓

**新增 line 319**："本 plan 的 re-review 通过后，自动进入 accepted plan commit；该 checkpoint 可 stage/commit/push 当前 plan gate 的 intended artifacts。此后在没有 Gateflow stop condition 时持续推进，直至 `final closeout pass`。"
- 明确了 accepted plan commit 的权限范围。✓
- "intended artifacts" 的界定与 non-goals 一致。✓

#### 3. 冻结业务语义核验

逐行比对 Exact LLM-facing wording semantics 章节（lines 103-137），与前次 review 通过的版本完全一致：

- `session_summary` 定义（lines 108-112）：包括 "candidate 被接受后，当前会话摘要变为空，包括清除先前已接受的摘要"。✓ 未变。
- `evidence_facts` 定义（lines 114-119）：包括 "必须由 `support_labels` 对应的 accepted `evidence_material` 或 `previous_evidence_fact` 直接支持"。✓ 未变。
- `answer_anchors` 定义（lines 121-126）：包括 "只能使用既有 schema 允许的 `answer_material` 或 `previous_answer_anchor`"。✓ 未变。
- 四种 reason 定义（lines 130-137）：`superseded`、`redundant`、`out_of_scope`、`policy_limit` 各有独立定义和禁止条件。✓ 未变。
- `policy_limit` 的冻结纠正："首次请求、没有 repair feedback、或当前 feedback 没有明示具体 cap 时禁止猜测或使用"。✓ 未变。

代码证据确认：
- `COMPACT_FACT_SOURCE_KINDS_V2` 仍为 `(PREVIOUS_EVIDENCE_FACT, EVIDENCE_MATERIAL)`。✓
- `_session_summary_from_accepted_event` 对 `summary is None` 仍返回 `_empty_session_summary_memory()`。✓

#### 4. Non-goals 纳入 controller 裁决核验

新增两项 non-goal：
- line 72："不补充 `forward_intents.status` 或 `reference_continuity.reason` 的业务语义；该缺口按 controller 裁决交由后续独立 LLM-facing schema work unit。" — 对应 AgentDS F01 `deferred-with-owner`。✓
- line 300-301（Risks 章节）："`forward_intents.status` 与 `reference_continuity.reason` 的 LLM-facing 业务语义。Controller 已将 AgentDS F01 裁决为 `deferred-with-owner`；owner 是后续独立 LLM-facing schema work unit，不阻塞本 slice。" + "AgentDS F02 的 encoding concern 已由 Controller `rejected-with-reason`。" — 对应 controller 裁决记录。✓

Non-goals 未被不当扩大或缩小。原有的12项保持不变，新增的2项均来自 controller 裁决。✓

#### 5. Scope boundary 核验

- Goal、motivation、success signal：未变。✓
- Direct code and test evidence：未变。✓
- Owner and contract decision：未变。✓
- Affected file boundary：未变。✓
- Single implementation slice：未变（除 step 7 的 checkpoint 权限细化）。✓
- Owner-level test matrix：未变。✓
- Manifest and hash update procedure：未变。✓
- Validation commands：未变。✓

## Open Questions

无。

## Residual Risks

无新增 residual risk。controller 裁决的两项 finding（AgentDS F01 deferred、AgentDS F02 rejected）已在 non-goals 和 risks 章节正确记录。

## Conclusion

`pass`

plan-review-fix 正确完成了三项工作：
1. Controller 裁决记录与前次 review findings 一致，deferred/rejected 裁决合理。
2. Gateflow 元数据变更（auto-advance、checkpoint permissions）边界清晰：accepted 前禁止 stage/commit/push，accepted 后只处理该 gate 的 intended artifacts，保留 stop condition 机制。
3. 冻结业务语义（session_summary:null 清除语义、claim 双来源、policy_limit 可见条件、四种 reason 定义）与 scope 完全未变。

plan 可进入 accepted plan commit checkpoint。

## Output file

- `docs/reviews/plan-review-20260803-215317.md`
