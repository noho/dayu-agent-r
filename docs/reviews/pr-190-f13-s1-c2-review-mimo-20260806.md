# PR-190 F13 S1 C2 Review — AgentMiMo

## Review metadata

- checkpoint: `docs/gateflow/pr-190-f13-s1-c2-checkpoint-20260806-164043.md`
- reviewer: AgentMiMo
- reviewed at: 2026-08-06
- scope: material → boundary → governance → replacement → durable payload (schema-5)
- verdict: **accepted** (无 blocking/high/medium finding)

## Review method

1. 完整阅读 `compact_material.py`、`compaction.py`、`context_governance.py`、`compact_payload.py`、`compact_artifact.py`、`context_events.py`、`compact_structure.py`、`compact_pipeline.py`。
2. 完整阅读对应 owner tests：`test_compact_material.py`、`test_compact_artifact_store.py`、`test_compact_pipeline.py`、`test_compaction_terminal.py`、`test_context_compact_events.py`、`test_compaction_contract.py`。
3. 运行 C2 focused tests：215 passed in 0.87s。
4. 手工复算 checkpoint 的 retained+E1+E2 例子，逐步验证代码路径。
5. 按 checkpoint frozen expectations 逐项裁决。

## 手工复算验证

Checkpoint 例子：

```text
previous P1 = (evidence:previous-a, evidence:previous-b)
current E1  = (evidence:current-a, evidence:shared)
current E2  = (evidence:shared, evidence:current-b)

retained P1 atom refs = (evidence:previous-a, evidence:previous-b)
new fact support (E1, E2) ordered unique union = (evidence:current-a, evidence:shared, evidence:current-b)
replacement fact/entry ordered unique union = (evidence:previous-a, evidence:previous-b, evidence:current-a, evidence:shared, evidence:current-b)
```

**代码路径手工复算：**

1. `derive_compact_accepted_replacement_v4` (compaction.py:1634-1711)
   - retained facts (line 1670-1690): 遍历 boundary，对 `retained_previous_evidence_fact_labels` 中的 label `P1`，从 boundary entry 复制 `canonical_evidence_refs = ("evidence:previous-a", "evidence:previous-b")`。✅
   - new facts (line 1691-1704): 遍历 boundary，对 `support_labels=("E1","E2")` 匹配的 entry 按 boundary 顺序 extend refs → `["evidence:current-a", "evidence:shared", "evidence:shared", "evidence:current-b"]` → `dict.fromkeys()` 去重 → `("evidence:current-a", "evidence:shared", "evidence:current-b")`。✅

2. `CompactAcceptedReplacementV4.canonical_evidence_refs` property (compaction.py:1592-1606)
   - flattened fact-ordered unique union: retained refs → new fact refs → `("evidence:previous-a", "evidence:previous-b", "evidence:current-a", "evidence:shared", "evidence:current-b")`。✅

3. test `test_retained_plus_two_source_new_fact_has_exact_per_fact_and_aggregate_refs` (test_compaction_contract.py:547-589)
   - 断言 retained refs、new fact refs、aggregate refs 与手工复算完全一致。✅

## Frozen expectations 逐项裁决

### 1. previous/current evidence boundary refs 非空；普通 context refs 为空

**accepted。**

- `CompactSourceBoundaryEntryV4.__post_init__` (compaction.py:693-706)：
  - `PREVIOUS_EVIDENCE_FACT` 和 `EVIDENCE_MATERIAL`：调用 `_require_non_empty_unique_string_tuple(canonical_evidence_refs)`，空 refs 时抛 `"canonical_evidence_refs must be non-empty"`。✅
  - 其余 source kind：`canonical_evidence_refs` 非空时抛 `"canonical_evidence_refs must be empty for non-evidence source kind"`。✅
- `CompactMaterialBlock.__post_init__` (compaction.py:415-424)：`EVIDENCE_BACKED_FACT` 必须非空，其余 kind 必须为空。✅
- `PromptLocalProvenanceEntry.__post_init__` (compaction.py:323-334)：`ACCEPTED_TOOL_EVIDENCE` 和 `EVIDENCE_BACKED_FACT` 必须非空，其余 kind 必须为空。✅
- test `test_evidence_boundary_entry_rejects_empty_canonical_refs` (test_compaction_contract.py:738-757)：parametrized 覆盖 `PREVIOUS_EVIDENCE_FACT` 和 `EVIDENCE_MATERIAL`。✅
- **observation**：无显式测试断言非 evidence kind 的 boundary entry 带非空 refs 时被拒绝。该约束由 `CompactSourceBoundaryEntryV4.__post_init__` 的 elif 分支（compaction.py:702-706）强制执行，但仅由 `_evidence_provenance_input()` 等 helper 的构造成功隐式覆盖。不构成 blocker，但建议补充 parametrized negative test。

### 2. retained fact 原子复制 boundary claim + refs；new fact 只选 current evidence，refs 是 boundary-order unique union

**accepted。**

- `derive_compact_accepted_replacement_v4` (compaction.py:1634-1711)：
  - retained (line 1670-1690)：`claim=entry.readable_text`、`canonical_evidence_refs=entry.canonical_evidence_refs`，逐 boundary entry 原子复制。✅
  - new (line 1691-1704)：只允许 `COMPACT_FACT_SOURCE_KINDS_V4 = (EVIDENCE_MATERIAL,)`（compaction.py:116），refs 按 boundary 顺序 extend + `dict.fromkeys` 去重。✅
  - 无 previous label 支持新 claim 的路径（retained 只允许 `PREVIOUS_EVIDENCE_FACT`，new 只允许 `EVIDENCE_MATERIAL`，kind guard 分别在 line 1677 和 line 1762-1770）。✅

### 3. Context Governance 是 proposal → accepted replacement 唯一 owner

**accepted。**

- `accept_compact_candidate_v4` (context_governance.py:87-166) 是唯一入口。✅
- `_CompactAcceptancePermit` (compaction.py:2481-2487) 是 opaque token，只有 `context_governance.py` import 并使用（line 41）。✅
- `CompactAcceptedTruthV4.__post_init__` (compaction.py:2520-2521) 验证 `_permit is _COMPACT_ACCEPTANCE_PERMIT`。✅
- combined retained+new 的 duplicate/information/caps/audit 全部在 governance 中执行（line 133-142）。✅

### 4. accepted payload 持久化 accepted_proposal（audit-only）与 accepted_replacement（consumer truth）；旧 accepted_candidate 无 writer/reader/alias

**accepted。**

- `compact_artifact_json_vnext` (compact_payload.py:789-832)：`accepted_proposal`（line 812）与 `accepted_replacement`（line 813）均独立持久化。✅
- `build_context_compacted_payload` (context_events.py:1197)：同样持久化两者。✅
- `rg -n -i 'accepted_candidate|candidate_binding_v4' dayu tests utils --glob '*.py'` 零命中（implementation artifact 报告）。✅
- consumer truth 是 replacement：memory.py 读取 `accepted_replacement` atom 自己的 refs（implementation artifact 报告）。✅

### 5. strict durable parser 重验 proposal↔boundary↔replacement、coverage、audit、per-fact nonempty refs 和 aggregate

**accepted。**

- `ContextCompactedSemanticPayload.__post_init__` (compact_payload.py:85-160)：
  - proposal-boundary binding (line 114-121)：调用 `compact_proposal_boundary_binding_issues_v4`。✅
  - proposal-replacement binding (line 122-126)：调用 `validate_compact_proposal_replacement_binding_v4`，re-derives replacement from scratch。✅
  - aggregate boundary subset (line 127)：`_validate_aggregate_boundary_unique_membership`。✅
  - aggregate equality (line 128-133)：`accepted_evidence_mapping_refs == replacement.canonical_evidence_refs`。✅
  - audit replacement binding (line 156-159)：`validate_compact_policy_usage_audit_replacement_binding_v4`。✅
  - coverage partition + replacement binding (line 160)：`_validate_committed_coverage`。✅

- aggregate 不强制跨 fact boundary 全局顺序：`_validate_aggregate_boundary_unique_membership` (compact_payload.py:501-526) 只检查 uniqueness 和 boundary subset，不检查 ordinal 单调性。✅
- `_validate_committed_coverage` (compact_payload.py:472-498)：represented/omitted 与 boundary 精确 partition，replacement 派生值一致。✅

### 6. repair proposal 必须重新走完整 parse/binding

**accepted。**

- repair loop（compaction_operation.py:1023-1064）：repair proposal 走同一 `accept_compact_candidate_v4` 调用（line 1023），无 shortcut 或 relaxed validation。✅
- repair feedback（context_governance.py:169-228）：`build_compact_repair_feedback_v4` 从 `CompactValidationReportV4` 构建 bounded feedback。✅
- `required_action` 固定为 `COMPACT_REPAIR_REQUIRED_ACTION`（compaction.py:2413-2416），要求 whole-candidate regeneration。✅

### 7. rejected/failed 不得物化 accepted artifact/replacement

**accepted。**

- `accept_compact_candidate_v4` (context_governance.py:87-166) 只有两个返回路径：`CompactValidationReportV4`（line 123/142）或 `CompactAcceptedTruthV4`（line 157-166）。✅
- `CompactValidationReportV4.__post_init__` (compaction.py:2386-2387) 验证 issues 非空。✅
- caller（compaction_operation.py:1028）通过 `isinstance` 分流，rejected path 不产生 accepted truth。✅
- EventLog：`CONTEXT_COMPACTED`、`CONTEXT_COMPACTION_FAILED`、`CONTEXT_COMPACTION_ATTEMPT_REJECTED` 使用互不相交的 required-field tuple（context_events.py:1022-1084）。✅

### 8. 不引入 heuristic、兼容 reader、flat aggregate 反向写 fact 或第二 provenance owner

**accepted。**

- 无 heuristic/scoring/weighting：全部逻辑是 deterministic validation 和 mechanical projection。✅
- 无兼容 reader/迁移：只操作 v4 typed contract。✅
- 无 flat aggregate 反向写 fact：`canonical_evidence_refs` 在 `derive_compact_accepted_replacement_v4` 中构造一次（line 1682-1688 retained, 1698-1704 new），后续无 mutation。✅
- 无第二 provenance owner：`_CompactAcceptancePermit` 单一 token，只有 governance 持有。✅

### 9. aggregate exact union + boundary subset（不误加跨 fact 全局顺序）

**accepted。**

- `CompactAcceptedReplacementV4.canonical_evidence_refs` (compaction.py:1592-1606)：逐 fact 按 tuple 顺序 flatten，`dict.fromkeys` 保留首次出现顺序去重。✅
- aggregate equality check (compact_payload.py:128-133)：persisted refs 必须等于 property 派生值。✅
- boundary subset check (compact_payload.py:501-526)：只检查 uniqueness 和 `ref in available`，不检查 ordinal 单调性。✅
- 手工复算确认：retained fact refs `(previous-a, previous-b)` + new fact refs `(current-a, shared, current-b)` → aggregate `(previous-a, previous-b, current-a, shared, current-b)`，与 test 断言一致。✅

## Findings

### F-01 (low/informational)：缺少非 evidence kind boundary entry 带非空 refs 的显式 negative test

- severity: low
- evidence: `compaction.py:702-706` 的 elif 分支强制非 evidence kind 必须空 refs，但 `test_compaction_contract.py` 无 parametrized test 直接断言该路径
- 影响：约束由 production code 强制，仅由 helper 构造成功隐式覆盖；若未来重构移除 elif 分支，不会立即有 test 失败
- 建议：补充 parametrized negative test 覆盖 `TRACE_MATERIAL`、`ANSWER_MATERIAL`、`PREVIOUS_SESSION_SUMMARY`、`PREVIOUS_ANSWER_ANCHOR`、`PREVIOUS_FORWARD_INTENT`、`PREVIOUS_REFERENCE_CONTINUITY` 六种 kind
- verdict: **accepted**（不阻塞，可作为后续补充）

### F-02 (low/informational)：durable parser tamper 检测无显式 round-trip test

- severity: low
- evidence: `ContextCompactedSemanticPayload.__post_init__` (compact_payload.py:85-160) 完整重验所有 binding，但 C2 focused tests 中未见专门构造 tampered payload 然后断言 parse 失败的测试
- 影响：parser 正确性由 binding validator 保证；tamper 检测是 validator 的自然推论，但无显式回归保护
- 建议：在 `test_compact_pipeline.py` 或 `test_compaction_terminal.py` 中补充：构建合法 payload → 修改单个字段（如 aggregate ref、replacement fact claim、audit actual）→ 断言 parse 抛出 ValueError
- verdict: **accepted**（不阻塞，可作为后续补充）

## 总结

C2 checkpoint 覆盖的 material → boundary → governance → replacement → durable payload 全链路通过完整代码审查和手工复算验证。所有 frozen expectations 均满足：

- retained/new fact refs 同源、边界有序、去重正确
- empty refs fail closed（evidence 必须非空，非 evidence 必须为空）
- Context Governance 是唯一 acceptance owner
- strict parser 完整重验所有 binding
- aggregate 等于逐 fact ordered unique union 且是 boundary subset，不强制跨 fact 全局顺序
- repair 走完整 re-validation
- rejected/failed 不物化 accepted artifact
- 无 heuristic、兼容 reader、flat union 反写、第二 owner

两个 low/informational finding 不阻塞 acceptance，建议作为后续补充。
