# Code Review — WU-CM-14 Aggregate Deepreview

## Scope

- Mode: current changes
- Branch: wu-cm-14-final-answer-preservation
- Base: d4b271cb (accepted plan commit)
- Output file: docs/reviews/code-review-20260619-193018.md
- Included scope:
  - `dayu/host/run_input.py` — protected recent raw tail provider/view、dedupe、LLM-facing evidence source filter、MemorySnapshotView 扩展
  - `dayu/host/dispatch.py` — proactive compact selection floor 传递、RunInputBuilder 构造注入
  - `dayu/host/engine_ingest.py` — reactive frozen material assembly 修复、reactive compact selection floor 传递
  - `tests/host/test_run_input_builder.py` — post-compaction raw tail、dedupe、skip 条件、LLM-facing filter
  - `tests/host/test_dispatch_scheduler.py` — proactive/reactive compact selection floor、frozen material stop-condition
- Excluded scope: `dayu/host/compact_material.py` 未修改（复用已有 helper）
- Parallel review coverage: 无（单一 reviewer 沿主链路走读）
- Prior review artifacts:
  - `docs/reviews/code-review-20260619-190815.md`
  - `docs/reviews/code-review-20260619-191152.md`
  - `docs/reviews/code-review-20260619-192312.md`
  - `docs/reviews/code-review-20260619-192408.md`
  - `docs/reviews/code-review-wu-cm-14-adjudication-20260619.md`

## 独立验证

- `pytest tests/host/test_run_input_builder.py tests/host/test_compact_material.py tests/host/test_dispatch_scheduler.py -q` → 220 passed
- `python -m pyright dayu/host/run_input.py dayu/host/dispatch.py dayu/host/engine_ingest.py` → 0 errors
- `git diff --check d4b271cb..HEAD` → 仅 review artifact 中一处 trailing whitespace，生产代码 clean

## Accepted Plan Constraints 核对

| 约束 | 状态 | 证据 |
|---|---|---|
| 不新增 public API/schema/EventLog event type/memory kind | ✅ | 所有新增符号均为 `_` 前缀私有；`MemorySnapshotView` 新增字段有默认值 `=()`；`create_*_run_input_builder` 新参数默认 `None`；`__all__` 无新增 |
| 不新增 WU-CM-14 专属 floor/ordinal parser | ✅ | 复用 `MemoryProjectionPolicy.selected_recent_window_turn_floor` |
| WU-CM-13 residual 保持为 residual | ✅ | 未重构 unified compact pipeline；`_frozen_reactive_material_blocks` 只修复 protected floor 语义，不包含 memory/compact/continuity material |
| LLM-facing text 只含业务可读语义 | ✅ | `_llm_facing_evidence_source_text` 过滤 7 类内部前缀；`_fallback_message_from_material_block` 不渲染 `block_id`/`canonical_source_refs`/`payload_refs` |

## 主链路走读摘要

按真实代码路径逐行走读：

1. **ordinary post-compaction RunInput raw tail 注入**：`RunInputBuilder.build()` (run_input.py:1988-2009) → `compact.compact_artifact_ref is not None` 激活 → `_DurableProtectedRecentRawTailProvider.load_protected_recent_raw_tail()` (run_input.py:1423-1454) → `_load_protected_recent_raw_tail_tx()` (run_input.py:1456-1504) → `build_pre_dispatch_compact_material_view()` → `_protected_recent_raw_tail_blocks()` → `_fallback_message_from_material_block()` → 注入 `bounded_context_messages` 位于 `memory.messages` / `compact.messages` 之后、`continuity.messages` 之前

2. **proactive compact selection floor**：`_governance_phase()` → `select_compact_segment(selected_recent_window_turn_floor=...)` (dispatch.py:1853-1861)

3. **reactive compact selection floor**：`_reactive_compaction_request()` → `select_compact_segment(selected_recent_window_turn_floor=pending.selected_recent_window_turn_floor)` (engine_ingest.py:3790-3796)

4. **reactive frozen material assembly**：`_start_reactive_context_recovery()` → `build_pre_dispatch_compact_material_view()` → `_frozen_reactive_material_blocks(material_view=material_view)` (engine_ingest.py:1333-1344) → 返回 `(*material_view.material_blocks, current_input_anchor)`

5. **raw tail dedupe 链路**：`_memory_selected_recent_source_refs()` / `_memory_selected_recent_content_digests()` → `MemorySnapshotView.selected_recent_source_refs` / `selected_recent_content_digests` → `_raw_tail_block_represented_by_memory()` (run_input.py:3561-3583)

6. **LLM-facing evidence source 过滤**：`_accepted_tool_evidence_content()` → `_llm_facing_evidence_source_text()` → `_is_internal_evidence_source_part()` (run_input.py:3156-3215)

## Findings

### F1-未修复-低-Reactive compact-success recovery dispatch 路径未被 scheduler E2E 测试直接覆盖

- **入口/函数**: `HostDispatchScheduler` reactive compact → recovery dispatch → `RunInputBuilder.build()` no-fallback branch
- **文件(行号)**: `tests/host/test_dispatch_scheduler.py`
- **输入场景**: reactive overflow → compact accepted → recovery dispatch → ordinary RunInput with `fallback is None` and `compact.compact_artifact_ref is not None`
- **实际分支**: `test_reactive_root_compact_selection_passes_protected_recent_floor` 直接调用 `_frozen_reactive_material_blocks` 验证 frozen blocks，然后通过 scheduler 验证 compact selection `excluded_reason_codes`。但未验证 recovery dispatch 后 `RunInputBuilder.build()` 输出的 Engine messages 是否包含 protected raw tail
- **预期行为**: plan §5.2 要求 "Assert the same protected recent raw tail appears in Engine messages"，或 "the focused test must explicitly construct the recovery Attempt state and call the same public/internal builder path used by dispatch"
- **实际行为**: 现有测试验证了 frozen material blocks 的 block kind 组成和 compact selection 的 floor 排除，但 recovery dispatch 的 Engine messages 内容未被直接断言
- **直接证据**: `test_dispatch_scheduler.py:5682-5818` — frozen blocks 断言和 selection 断言存在，但 scheduler drain 后未检查 dispatched request 的 messages
- **影响**: 低 — `_DurableProtectedRecentRawTailProvider` 与 proactive post-compaction 路径共享同一实现，proactive 路径的 `test_post_compaction_ordinary_build_preserves_protected_recent_raw_tail` 已覆盖该 provider 的核心行为；reactive recovery dispatch 走的是同一个 `RunInputBuilder.build()` no-fallback 分支
- **建议改法和验证点**: 可选：在 reactive scheduler E2E 测试中，drain 后捕获 worker received request 并断言 messages 包含 protected raw tail。但 proactive focused test 已覆盖同一 provider 逻辑
- **修复风险（低）**: 仅新增测试断言
- **严重程度（低）**:

## 逐项 Review 重点回应

### ordinary post-compaction RunInput 注入条件
**确认正确。** `RunInputBuilder.build()` (run_input.py:1988-2009) 在 `fallback is None` 且 `compact.compact_artifact_ref is not None` 时调用 raw tail provider。fallback 分支 (run_input.py:2010-2034) 不走 raw tail provider。provider 自身也有 `compact.compact_artifact_ref is None` 的防御性检查 (run_input.py:1441-1446)。激活条件精确。

### raw tail 只来自 EventLog-backed post_compact_delta_material
**确认正确。** `_load_protected_recent_raw_tail_tx()` (run_input.py:1487-1492) 通过 `build_pre_dispatch_compact_material_view()` 构造 material view。`material_view.material_blocks` 只包含 `USER_INPUT_ACCEPTED`、`RUN_SUCCEEDED`、`TOOL_RESULT_ACCEPTED` 三种 event type 的 delta blocks。`_protected_recent_raw_tail_blocks` 进一步按 `selected_recent_window_turn_floor` + `is_turn_group_material_block` 过滤。

### selected_recent_window_turn_floor 在所有 compact selection 路径一致
**确认正确。**
- Proactive normal: `dispatch.py:1859-1861` ✅
- Proactive recovery tier 1: `dispatch.py:1521-1522` ✅ (pre-existing)
- Reactive root: `engine_ingest.py:3796` ✅
- Proactive fallback: `dispatch.py:2223-2224` ✅ (pre-existing)
- Reactive fallback: `engine_ingest.py:3902` ✅ (pre-existing)

### reactive frozen material assembly 恢复 protected recent turn group material
**确认正确。** `_frozen_reactive_material_blocks` (engine_ingest.py:4005-4027) 改为接收 `material_view`，返回 `(*material_view.material_blocks, current_input_anchor)`。`material_view.material_blocks` 包含 EventLog-backed post-compact delta material。此修复范围精确覆盖 WU-CM-14 需求，未越界实现 WU-CM-13。

### dedupe 逻辑
**确认基本可靠。** 三层检查：
1. `content_digest` 匹配（快速路径，存在规范化差异 — 前次 review F3 已识别）
2. `canonical_source_refs` 匹配（user input/assistant answer 的 event id 匹配）
3. `evidence_refs` 匹配（`accepted_evidence_id`/`tool_result_event_ref`/`tool_call_event_ref`）

source_ref 检查是去重正确性的主要保障。content_digest 差异不影响功能正确性。

### LLM-facing text 清洁性
**确认正确。** `_llm_facing_evidence_source_text` 过滤 7 类内部前缀（`tool_call_event:`、`tool_result_event:`、`event:`、`eventlog:`、`payload:`、`artifact:`、`digest:`）。`_fallback_message_from_material_block` 不渲染内部标识。测试验证 `tool_call_id` 和 `event-prior-tool` 不出现在最终 messages 中。

### 文件边界、类型签名、中文 docstring
**确认符合 AGENTS.md。**
- 类型签名：所有新增函数和类均有完整类型标注，无 `Any`、无 `object`
- 中文 docstring：所有新增 public/private 函数均有完整中文 docstring
- 文件边界：新增类型/函数均在 plan 允许范围内
- pyright 0 errors，220 tests passed

### backward compatibility
**确认无破坏。** `MemorySnapshotView` 新增字段 `selected_recent_source_refs` 和 `selected_recent_content_digests` 均有默认值 `= ()`，所有现有构造调用使用 keyword arguments，不受影响。`create_no_tool_run_input_builder` 和 `create_tool_enabled_run_input_builder` 新增 `memory_projection_policy` 参数默认 `None`，现有调用不受影响。4 个现有 compact 测试增加了 `memory_projection_policy=_compact_no_floor_memory_policy()` 以避免 floor 生效干扰旧测试语义。

## Open Questions

无。

## Residual Risk

| ID | Risk | Severity | Notes |
|---|---|---|---|
| WU-CM-14-RR-1 | EventLog 双读（governance + raw tail provider） | 低 | Plan 已标记为 WU-CM-13 范围（deferred） |
| WU-CM-14-RR-2 | content_digest 规范化不一致降低 dedupe 快速路径命中率 | 低 | source-ref 检查可靠兜底 |
| WU-CM-14-RR-3 | `_protected_recent_raw_tail_blocks` 静默空返回无 diagnostic | 低 | 功能正确，排障辅助缺失 |
| WU-CM-14-RR-4 | Reactive compact-success recovery dispatch Engine messages 未被 scheduler E2E 直接断言 | 低 | proactive focused test 已覆盖同一 provider 逻辑 |

## Verdict

**PASS.** WU-CM-14 实现在 accepted plan 约束内完成，所有 acceptance signals 由生产路径和测试共同满足。未发现实质性 correctness、architecture 或 test blocker。220 tests passed，pyright 0 errors，git diff --check clean（生产代码）。
