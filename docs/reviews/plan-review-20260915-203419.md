# Plan review：midea-q1-full-report

- gate：plan review；reviewer：controller，按 planreview skill 执行。
- target：`docs/plans/midea-q1-full-report.md`；scope：CN 同日形态选择、旧缓存公开覆盖与验证；未实施生产修改。
- timestamp：来自本机 `date +%Y%m%d-%H%M%S`。

## 已挑战假设及直接证据

1. 是否仅凭标题推测漏选：Raw 两次窗口均三条且无后页，中文正文先于中文全文；baseline CLI 真实落盘正文。选择缺陷成立；全文表格仍必须在真实验收单独检查。
2. 全文优先会不会覆盖真实修订：key 把 amended 和日期放在形态前，保持现有版本选择行为。同日更正正文优先于未更正全文；同日更正全文优先于更正正文。跨日期版本并无足够证据可推翻，因此不扩展。
3. 只有正文会不会消失：保留 filter，仅排序，不拒绝唯一正文。普通季度报告不必含“全文”标签。
4. ID 能不能作为新旧依据：仅末端字符串平手，不宣称时间顺序。此决定必须在 helper docstring 和 README 明示。
5. 旧缓存能否更新：`cn_download_identity.py` 的 CN 分支直接返回财期 ID；`cn_download_filing_workflow.py:183-220` 在 COMPLETE/non-overwrite 时 skip；overwrite 禁止 skip/reuse。计划没有错误承诺普通增量自动替换，使用公开 overwrite。
6. 审核是否把不同层混为一谈：storage COMPLETE 只是物理完整；PDF 内容表格需独立验收。Raw、meta、manifest、内容 hash、remote fingerprint 分别核查。

## 特别审查视角

- Architecture/ownership：改动在 pipeline selection owner；不让 downloader、storage 或消费者自行识别正文。
- Best practice：public selection contract 回归加现有真实仓储 workflow 验证。baseline focused tests 186 passed。
- Optimal solution：现有 overwrite 足以修复旧 cache；无需新 API、强制网络 revalidation 或新 identity。
- Overengineering：单私有 key + token 常量，无新 abstraction、schema 或历史迁移。
- Overcoupling：生产仅一个文件；workflow 测试只验公开既有合同，不联动改动 storage。

## Findings

无 blocking finding。不以风格或无证据推测制造 blocker。

## Open questions / residual risks

- 全文 PDF 的财表内容与真实覆盖后的幂等性尚未验收：covered by later approved validation，本轮 controller 负责；未通过不能交付成功。
- overwrite 当前 `_resolve_previous_completed_meta` 会舍弃 previous meta，版本可能重新 v1，不能承诺历史版本递增或自动 supersede：现有行为，scope 外；本轮用逐轮 Raw 快照留痕，不新增版本承诺。
- 不同日期的最新正文可能仍优先旧全文：有意保留当前版本优先级，不把单一真实同日问题扩成跨版本选择重构；后续若有直接反例再单独 work unit。
- 未新增通用 PDF 内容完整性识别：投资 workflow 主 Agent 负责业务原表审核，本轮只核实目标报告。
- commit/push/PR 受用户明确约束，不构成需要再次询问的隐式授权；各 commit gate 应记 no-commit，不能宣称 PR gate pass。

## Decision / validation / completion

- conclusion：pass-with-risks；controller accepted。
- fix / re-review：无 accepted finding，无需修复；上述限制写入 implementation/closeout。
- docs decision：按计划更新三个 README 职责内内容。
- artifact：`docs/reviews/plan-review-20260915-203419.md`。
- next：accepted plan commit（用户约束 no-commit），然后 implementation S1。
