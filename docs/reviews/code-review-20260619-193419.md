# Code Review — WU-CM-14 Focused Re-review (Dead Code Deletion + Controller Adjudication)

## Scope

- Mode: current changes (focused re-review)
- Branch: wu-cm-14-final-answer-preservation
- Base: d4b271cb (accepted plan commit)
- Output file: docs/reviews/code-review-20260619-193419.md
- Included scope: `dayu/host/engine_ingest.py` workspace deletion of `_current_only_material_blocks`
- Excluded scope: all other production/test files unchanged from previous aggregate review
- Previous aggregate artifacts:
  - `docs/reviews/code-review-20260619-192740.md`
  - `docs/reviews/code-review-20260619-193018.md`

## Controller Adjudication Verification

### ACCEPT: 删除 `_current_only_material_blocks` 死代码 → **确认正确关闭**

**直接证据：**

1. `rg '_current_only_material_blocks' dayu/ tests/` → 无匹配（exit code 1）
2. `_current_input_anchor_material_block`（被 `_current_only_material_blocks` 包装的底层函数）仍被 `_frozen_reactive_material_blocks` (engine_ingest.py:4020-4027) 直接调用，保留正确
3. 在 committed diff 中，`_current_only_material_blocks` 的唯一 caller 是旧版 `_frozen_reactive_material_blocks`（已重构为 `material_view.material_blocks` + `_current_input_anchor_material_block`），重构后该函数变为零 caller

**workspace diff 核实：** 删除范围精确，仅移除 `_current_only_material_blocks` 函数定义（23 行），不触碰 `_current_input_anchor_material_block` 或任何其他代码。

**验证结果：**
- `pytest ... -q` → 220 passed
- `pyright dayu/host/engine_ingest.py` → 0 errors
- `git diff --check` → clean

**结论：** 死代码删除正确，无残留 caller，无 regression。**Finding 已关闭。**

---

### REJECT as nonblocking: reactive compact-success recovery dispatch scheduler E2E 直断言缺失 → **同意裁决，不挑战**

**裁决理由复核：** Controller 认为 "现有 focused recovery Attempt + same RunInputBuilder.build path 满足 accepted plan 的替代覆盖"。

**直接证据：**

1. `test_post_compaction_ordinary_build_preserves_protected_recent_raw_tail` (test_run_input_builder.py:2715) 通过 `create_no_tool_run_input_builder(..., memory_projection_policy=policy, compact_artifact_provider=DurableCompactArtifactProvider(...))` 构造 builder，然后调用 `builder.build()` — 这与 dispatch scheduler 在 no-fallback ordinary path 使用的完全相同的 builder 构造和 build 路径
2. proactive 和 reactive compact-success recovery dispatch 最终都走到同一个 `RunInputBuilder.build()` no-fallback 分支（run_input.py:1988-2009），由同一个 `_DurableProtectedRecentRawTailProvider` 提供 raw tail
3. focused test 直接覆盖了 provider 的核心逻辑：compact artifact 存在时加载 protected raw tail、floor 过滤、dedupe、LLM-facing 清洁性
4. Plan §5.2 允许 "If a full scheduler-level reactive E2E is not used, the focused test must explicitly construct the recovery Attempt state and call the same public/internal builder path used by dispatch" — focused test 满足此替代条件

**结论：** 裁决合理，reactive scheduler E2E 缺失的 Engine messages 直断言不构成 blocker。**不挑战。**

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

| ID | Risk | Severity | Notes |
|---|---|---|---|
| WU-CM-14-RR-1 | EventLog 双读（governance + raw tail provider） | 低 | deferred to WU-CM-13 |
| WU-CM-14-RR-2 | content_digest 规范化不一致 | 低 | source-ref 检查兜底 |
| WU-CM-14-RR-3 | `_protected_recent_raw_tail_blocks` 静默空返回 | 低 | 功能正确，排障辅助缺失 |

## Verdict

**PASS.** Accepted dead-code finding 已正确关闭。Rejected finding 的裁决有直接证据支撑，不挑战。无新 blocker。220 tests passed, pyright 0 errors, git diff --check clean。
