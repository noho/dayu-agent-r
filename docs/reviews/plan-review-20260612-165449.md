# WU-CM-08 Plan Fix Focused Re-Review — AgentMiMo

## 审查对象

- Plan artifact: `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md` (fix 版本)
- 前序 review: `docs/reviews/plan-review-20260612-164857.md` (AgentDS, PASS-WITH-FINDINGS, 6 findings)
- 前序 review: `docs/reviews/plan-review-20260612-165055.md` (AgentMiMo, PASS, 4 suggestions)

## 审查范围

仅核对 plan fix 是否已解决上述两份 review 的全部 findings / suggestions，不重新评估 plan 动机、scope 或 slicing 质量。

## 审查时间

2026-06-12T16:54:49+08:00

## 结论

**PASS**

全部 findings 已解决。无剩余 blocking 或 non-blocking findings。

---

## 逐项核对

### AgentDS Review Findings (plan-review-20260612-164857.md)

#### Finding 1 — Stale name exclusion test 已存在；需明确 strengthen 理由

| 状态 | **已解决** |
|---|---|
| 原始问题 | Plan 未承认 `test_conversation_compact_input_vnext_maps_material_without_citable_current_anchor` (L379-382) 已有 stale-name 排除断言，未说明 strengthen 的具体增量。 |
| Plan fix 响应 | Slice 1 "Exact changes" 第 2 条明确写道："Strengthen the existing stale-name coverage in `test_conversation_compact_input_vnext_maps_material_without_citable_current_anchor`: it already excludes `stable_input`, `history_input`, and `evidence_input`; add positive assertions that all six current top-level JSON keys are present (`previous_compacted_view`, `trace_material`, `evidence_material`, `answer_material`, `current_input_anchor`, `instruction`)." |
| 判定 | 精确承认了现有测试及其行号范围，明确了增量（positive assertion of all 6 keys），且补充了 post-compaction pack-shape 覆盖（第 3 条）。完全满足原始建议。 |

#### Finding 2 — "Summarize material pack shape" 过于模糊

| 状态 | **已解决** |
|---|---|
| 原始问题 | Plan 未指定 helper 的输入、输出类型、failure message 格式，实现 agent 无法确定性生成代码。 |
| Plan fix 响应 | Slice 1 "Exact changes" 第 1 条定义了两个具体 typed contract：`_MaterialPackShape`（NamedTuple 或 frozen dataclass，6 个字段）和 `_VNextInputShape`（NamedTuple 或 frozen dataclass，6 个字段），并要求 assertion helpers "emit boundary-specific failure messages naming `previous_compacted_view`, `trace_material`, `evidence_material`, `answer_material`, `current_input_anchor`, `instruction`, `prompt-local label`, or `citable_source_labels` as appropriate"。 |
| 判定 | Contract 具体到字段名、字段类型、failure message 内容，code-generation-ready。 |

#### Finding 3 — Allowed files 未覆盖条件性 production fix 路径

| 状态 | **已解决** |
|---|---|
| 原始问题 | S1 允许修改 production 文件的条件存在但 allowed files 未包含 `compact_material.py` / `compaction.py`，实现 agent 边界不清。 |
| Plan fix 响应 | Slice 1 "Exact changes" 最后一条明确写道："Do not modify production files in this slice. If the strengthened tests expose a real production defect in `dayu/host/compact_material.py` or `dayu/host/compaction.py`, stop the slice, record the defect in the implementation report and control doc, and request a separate production-fix scope instead of silently broadening WU-CM-08." |
| 判定 | 采用了 AgentDS 建议的第二个选项（defer to new issue），边界明确，不存在歧义。 |

#### Finding 4 — `public_smoke_support.py` 未在 S2 allowed files 中

| 状态 | **已解决** |
|---|---|
| 原始问题 | S2 的 helper 提取可能需要修改 `public_smoke_support.py`，但该文件不在 allowed files 中。 |
| Plan fix 响应 | S2 Allowed files 第 2 条明确包含 `tests/host/public_smoke_support.py`，约束为："only if avoiding duplication requires extracting a genuinely general public-smoke assertion helper; do not move compact-specific helpers there and do not change existing helper signatures or behavior." |
| 判定 | 文件已加入，约束精确（仅限提取通用 helper、不改现有签名/行为），完全满足原始建议。 |

#### Finding 5 — 提取的 helper 缺少自身正确性验证

| 状态 | **已解决** |
|---|---|
| 原始问题 | S2 未要求 helper 本身有 positive + negative test case，可能产生 false negative / false positive。 |
| Plan fix 响应 | S2 "Exact changes" 第 3 条明确写道："Add positive and negative tests for each newly extracted non-trivial helper. Negative cases must assert the boundary-specific failure message, for example material section shape, stale legacy section, forbidden internal term, missing evidence marker, or canonical ref leakage in a fake proposal." |
| 判定 | 要求每个 non-trivial helper 有正例和反例，反例必须验证 boundary-specific failure message，完全满足原始建议。 |

#### Finding 6 — Residual risk tracking 和 control doc 更新未说明

| 状态 | **已解决** |
|---|---|
| 原始问题 | Plan 未说明 WU-CM-08 完成后如何更新 control doc、产出哪些 artifacts。 |
| Plan fix 响应 | 新增 "Artifacts And State Transitions" section，明确：(1) plan acceptance 后 control doc 更新为 `implementation` / `WU-CM-08-S1 implementation gate`；(2) S1 写 `docs/reviews/wu-cm-08-s1-implementation-report.md`；(3) S2 写 `docs/reviews/wu-cm-08-s2-implementation-report.md`；(4) aggregate deepreview 覆盖两 slices、baseline drift、README decisions、residual risks；(5) residual risks 记录在 control doc 中并附 owner 或 non-actionable rationale。 |
| 判定 | 完整覆盖了 control doc 状态转换、artifacts 产出、residual risk 记录，完全满足原始建议。 |

---

### AgentMiMo Review Suggestions (plan-review-20260612-165055.md)

#### Suggestion 1 — S1 应明确是 strengthen existing 还是 add new standalone

| 状态 | **已解决** |
|---|---|
| 原始建议 | S1 "Add or strengthen" 措辞模糊，当前 test 已有 stale-name 断言。 |
| Plan fix 响应 | S1 第 2 条明确为 "Strengthen the existing stale-name coverage in `test_conversation_compact_input_vnext_maps_material_without_citable_current_anchor`"，并列出具体增量。 |
| 判定 | 措辞从 "Add or strengthen" 改为 "Strengthen the existing"，消除了歧义。 |

#### Suggestion 2 — S2 应明确是重构现有 helper 还是新增并行 helper

| 状态 | **已解决** |
|---|---|
| 原始建议 | `_assert_compactor_material_instruction_contract` 已存在，S2 应说明如何处理重叠。 |
| Plan fix 响应 | S2 "Exact changes" 第 1 条明确写道："by extending or splitting the existing `_assert_compactor_material_instruction_contract(...)` rather than adding a parallel overlapping helper"。 |
| 判定 | 明确选择了 extend/split 而非并行新增，消除了歧义。 |

#### Suggestion 3 — S1/S2 validation 应显式要求先验证 baseline 再开始修改

| 状态 | **已解决** |
|---|---|
| 原始建议 | Goal Confirmation 末尾隐含了 baseline 验证，但 S1/S2 validation section 未显式要求。 |
| Plan fix 响应 | S1 Validation 第 1 条和 S2 Validation 第 1 条均明确写道："Before edits: `pytest tests/host/test_compact_material.py tests/host/test_public_compact_smoke.py -q` to confirm the 41 passed, 1 skipped baseline has not drifted." |
| 判定 | 两个 slice 的 validation section 均在修改前显式要求 baseline 验证，完全满足建议。 |

#### Suggestion 4 (Info) — 实现完成后建议 comment Issue #95

| 状态 | 未在 plan fix 中体现，但此为 Info 级别建议，不阻塞 plan review。实现 agent 可在 aggregate deepreview 阶段自行决定是否执行。 |
|---|---|

---

## 额外观察

Plan fix 相比原版还增加了以下改进，非 findings 要求但提升了 code-generation-ready 程度：

1. **S2 `fake_compaction.py` 处理**：新增说明 "If new assertions interact with `tests/host/fake_compaction.py` and encounter the pre-existing `cast(...)` residual from WU-CM-05, document it in the implementation report and do not fix it in WU-CM-08 unless it causes a direct test or pyright failure inside the approved slice." 这回应了 AgentDS review §7.2 的非 finding 观察。

2. **S2 helper 提取约束**：明确了 "extend or split the existing helper rather than adding a parallel overlapping helper"，降低了实现时的决策歧义。

3. **README Decision section 细化**：增加了条件性 `tests/README.md` 更新说明（"If a new shared helper module is introduced, or if `tests/host/public_smoke_support.py` gains a new reusable helper that future tests are expected to use, update `tests/README.md` to document the maintenance pattern"）。

---

## 总结

| 前序 review | findings 数量 | 已解决 | 未解决 |
|---|---|---|---|
| AgentDS (164857) | 6 | 6 | 0 |
| AgentMiMo (165055) | 4 suggestions | 3 | 1 (Info, non-blocking) |

Plan fix 已解决全部 blocking 和 non-blocking findings。结论: **PASS**。
