# Code Review — UF-FIX07 Aggregate Re-Review（状态机终态核对）

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `64050349`
- HEAD: `6b804001`（未移动）+ 未提交 aggregate fix diff
- 复核目标：`docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md` 状态机终态
- Output file: `docs/reviews/code-review-20260815-225952.md`
- Included scope: 仅核对 Decision、Next entry point、Completion findings 列表
- Excluded scope: 不复读代码、不改已有文件、不跑测试

## 状态机核对

### 1. Gate 元数据（第 14-15 行）

```
- Decision：`AGGREGATE FIX COMPLETE / RE-REVIEW PASS`
- 下一入口：controller aggregate acceptance
```

**检查结果**：
- ✅ Decision 已从 `RE-REVIEW PENDING` 更新为 `RE-REVIEW PASS`
- ✅ 下一入口已从"不执行 review"更新为 `controller aggregate acceptance`

### 2. Completion status（第 156-157 行）

```
- Decision：`AGGREGATE FIX COMPLETE / RE-REVIEW PASS`。
- Next entry point：controller aggregate acceptance。
```

**检查结果**：
- ✅ 与 Gate 元数据一致

### 3. Completion Aggregate findings（第 154 行）

```
- Aggregate findings：原 Finding 1、Finding 2、guard owner tests LOW、全仓失败证据精度 LOW 与证据回引 LOW 均 `已修复`。
```

**检查结果**：
- ✅ 列出五项 findings
- ⚠️ Finding 状态表只有三行（Finding 1、Finding 2、Re-review Finding 1），缺少两行

### 4. Finding 状态表（第 22-27 行）

```
| Finding | 裁决 | Fix 状态 | 直接证据 |
| --- | --- | --- | --- |
| Finding 1：filing fingerprint 未编码 authoritative primary... | `accepted` | `已修复` | ... |
| Finding 2：README 把 role order 错写为 input order | `accepted` | `已修复` | ... |
| Re-review Finding 1（LOW）：四个新增 fail-closed guard 缺少 direct owner coverage | `accepted` | `已修复` | ... |
```

**检查结果**：
- ⚠️ 只有三行，缺少"全仓失败证据精度 LOW"与"证据回引 LOW"两行

## Findings

### 1-未修复-低-Finding 状态表与 Completion findings 列表不一致

- **入口/函数**: `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md` Finding 状态表 vs Completion status
- **文件(行号)**: 第 22-27 行（Finding 状态表，3 行）vs 第 154 行（Completion，5 项）
- **输入场景**: 读取 fix artifact 的 Finding 状态表和 Completion status
- **实际分支**: Finding 状态表只有 Finding 1、Finding 2、Re-review Finding 1（LOW）三行
- **预期行为**: Completion 提到五项 findings（原 Finding 1、Finding 2、guard owner tests LOW、全仓失败证据精度 LOW、证据回引 LOW），Finding 状态表应有五行
- **实际行为**: Finding 状态表缺少"全仓失败证据精度 LOW"与"证据回引 LOW"两行；第 28 行总结"两个原 aggregate findings 与一项 re-review LOW"也只提到三项
- **直接证据**: 第 154 行"原 Finding 1、Finding 2、guard owner tests LOW、全仓失败证据精度 LOW 与证据回引 LOW 均 `已修复`"；第 22-27 行只有三行；第 28 行"两个原 aggregate findings 与一项 re-review LOW"
- **影响**: 状态机不完整——读者无法从 Finding 状态表追溯全部五项 findings 的裁决与证据链
- **建议改法和验证点**: 在 Finding 状态表补充两行：
  - `| Re-review Finding 2（LOW）：fix artifact 对 pre-existing 全仓 9 失败的记录措辞不精确（1/9 未在 base 复现） | accepted | 已修复 | 8/1 定向隔离证据见 code-review-20260815-225405.md |`
  - `| Re-review Finding 3（LOW）：fix artifact 来源链完整性不足 | accepted | 已修复 | Aggregate re-review inputs 同时列出 224341 与 225405，Validation 显式回引 225405 |`
  同步更新第 28 行总结
- **修复风险（低）**: 纯文档修订
- **严重程度（低）**:

## Open Questions

- 无。

## Residual Risk

- 本复核未重跑测试、未运行 UF-PF07/PF12、未修改任何实现/测试/artifact、未提交/PR。

## 结论

**Decision**：状态机终态大部分正确（Decision ✅、Next entry point ✅），但 Finding 状态表与 Completion findings 列表不一致（3 行 vs 5 项），需补充两行 LOW findings。

**Output file**：`docs/reviews/code-review-20260815-225952.md`
