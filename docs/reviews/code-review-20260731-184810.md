# Code Review

## Scope

- Mode: current changes（Gateflow S3 implementation review）
- Branch: `codex/prompt-oracle-adjudication`
- Base: `ccf836f0`（S2 accepted commit）
- Output file: `docs/reviews/code-review-20260731-184810.md`
- Included scope: `dayu/cli/__main__.py`、`pyproject.toml`、`tests/cli/test_arg_parsing.py`、`tests/cli/test_prompt_command.py`、`tests/cli/test_public_package_entrypoints.py`、`docs/reviews/wu-cli-prompt-01-s3-implementation-controller.md`
- Excluded scope: S1/S2 已接受实现、后续 S4-S6、interactive/process/context compaction、Host/Engine 状态机与冻结 oracle/scenario 语义
- Parallel review coverage: 无；本 review 沿 console/module bootstrap、`main()` startup、prompt Host/Session/monitor takeover 与 cleanup 主链路独立走读。

## Findings

未发现实质性问题。Gate 结论：`pass`。

## Evidence

- 公共 startup owner 已收敛到 `dayu.cli.__main__.run_module`：`KeyboardInterrupt` 捕获范围同时包含 `from dayu.cli.main import main` 与随后的完整 `main()` 调用；`dayu.cli.__main__` 顶层只加载层中立退出码常量，没有再次引入 Service/Host/Engine/Fins 重型依赖。
- `pyproject.toml` 的唯一真实 console script 已指向 `dayu.cli.__main__:run_module`；`python -m dayu.cli` 仍在同一模块调用同一函数，没有兼容 wrapper 或第二套 startup 语义。
- `main()` 的标准流配置发生在其既有内部 `KeyboardInterrupt` 捕获之前；新增 standard-stream 注入测试因此确实证明外层 bootstrap 覆盖完整 application 调用，而不只是复用 `main()` 自身的 catch。lazy-import 属性注入则独立证明重型 application import gap 被外层边界覆盖。
- prompt startup 注入沿真实调用链覆盖 runtime prepare、Host opener、Session ensure commit 前和 monitor install：Host context、Session attachment 均由既有 owner cleanup；monitor install 中断发生在 submit task 创建前，测试断言没有 submit/cancel Run。该阶段已原子创建的 Session 是完整 Host fact，不是半初始化 Run/attempt，也没有引入 rollback 要求。
- 定向测试复核：`pytest -q tests/cli/test_arg_parsing.py -k 'run_module or module_run_function' tests/cli/test_prompt_command.py -k 'startup_interrupt'` 为 `4 passed`（组合 `-k` 只选择本次命令匹配项）。
- 独立真实进程 probe：安装后的 `.venv/bin/dayu-cli --help` 在启动后 0.05 秒注入 SIGINT 连续 20 次均为 exit 130，且无 Traceback/KeyboardInterrupt；与冻结场景使用的 `dayu-cli` 入口一致。
- 静态与差异检查：指定 S3 Python 文件 pyright 为 `0 errors`；`git diff --check` 通过。

## Open Questions

无。

## Residual Risk

- 最终仍需由 controller 按 P47、PC-CN-01..06 的冻结 exact argv、PTY 时序、已初始化 workspace 与 filesystem/SQLite before-after 重放；本 review 的 `--help` startup probe 只验证 launcher/bootstrap 信号边界，不替代 frozen scenario evidence。
- 独立诊断确认 `python -m dayu.cli` 在 0.05 秒注入时仍可能在解释器尚未执行任何项目 bytecode 前返回 `-2`；这不构成本 slice blocker：冻结启动场景精确入口均为 `dayu-cli`，且项目代码尚未执行时不存在可由 Python CLI bootstrap 接管的 owner。进入 `dayu.cli.__main__.run_module` 后的 import/application gap 已由代码与测试覆盖；若未来要把解释器 pre-bytecode 窗口纳入公开 contract，需要独立的非 Python launcher 设计与新的 scope/owner 决策，不能在本 slice 增加 package import signal side effect。
