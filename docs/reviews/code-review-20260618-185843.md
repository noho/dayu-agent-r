# Code Review Re-Review

## Scope

- Mode: focused re-review (WU-CM-12-FIX-R1 Slice 1 accepted finding)
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: HEAD (uncommitted workspace changes)
- Reviewer: AgentMiMo
- Output file: `docs/reviews/code-review-20260618-185843.md`
- Included scope: `tests/host/test_compaction_operation.py` (uncommitted diff vs HEAD), `docs/reviews/wu-cm-12-fix-r1-s1-fix-codex-20260618.md`
- Excluded scope: production code files (`dayu/host/compact_material.py`, `dayu/host/compaction.py`) — only verified no additional changes were introduced by the fix
- Finding source:
  - `docs/reviews/code-review-20260618-184822.md` Finding 1
  - `docs/reviews/code-review-20260618-185121.md` Finding 001

## Accepted Finding

`test_evidence_chunks_share_same_durable_query_text`（`tests/host/test_compaction_operation.py:1632`）仍断言旧默认 chunk label `E1.1/E1.2/E1.3`，但 Slice 1 production code 已移除默认 evidence chunking。Expected fixed state：

1. 测试 docstring / assertions 反映 no-default-chunk 语义
2. labels 断言单个 `E1`，无 `E1.1` / `E1.2` / `E1.3` 或 chunk-style labels
3. durable query_text 断言保留
4. 验证通过
5. 确认无 production code 在本次 fix 中被修改

## Finding Status: FIXED

### Evidence

**1. 测试 docstring 已迁移为 no-chunk 语义**

- 旧：`"同一 durable request 被 chunk 后各 evidence chunk 复用同一 query_text。"`
- 新：`"长 evidence 默认不 chunk，单个 block 使用同一 durable request 的 query_text。"`
- 位置：`tests/host/test_compaction_operation.py:1635`

**2. 内部标识已从 chunk 语义迁移**

- `session_id` / `event_id` / `tool_call_event_id`：从 `*-chunk` 改为 `*-no-chunk`
- `block_id`：从 `"selected-evidence-chunk"` 改为 `"selected-evidence-no-chunk"`
- `current_input_ref`：从 `"input-query-chunk"` 改为 `"input-query-no-chunk"`
- 辅助 docstring：从 `"写入会被 chunk 的 selected evidence"` 改为 `"写入长 selected evidence"`；从 `"读取 chunk labels 与 query_text"` 改为 `"读取 evidence label 与 query_text"`

**3. label 断言已迁移为单个 E1**

- 旧断言（已删除）：
  ```python
  assert tuple(label for label, _query_text in query_texts) == (
      "E1.1",
      "E1.2",
      "E1.3",
  )
  ```
- 新断言（`test_compaction_operation.py:1721-1724`）：
  ```python
  labels = tuple(label for label, _query_text in query_texts)
  assert labels == ("E1",)
  assert not {"E1.1", "E1.2", "E1.3"}.intersection(labels)
  assert all("." not in label for label in labels)
  ```
- 三重防御：精确匹配 `("E1",)`、排除旧 chunk label 集合、排除任何含 `.` 的 label。

**4. durable query_text 断言保留**

- `assert len({query_text for _label, query_text in query_texts}) == 1` — 确认唯一 query_text
- `assert query_texts[0][1] == '工具参数: {"arguments":{"chapter":"MD&A","ticker":"MSFT"}}'` — 确认 query_text 来自 accepted evidence 对应的 durable tool request arguments
- 位置：`test_compaction_operation.py:1725-1726`

**5. 验证通过**

- `pytest tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text -q` → `1 passed in 0.26s`
- `pytest tests/host/test_compact_material.py tests/host/test_llm_compaction.py tests/host/test_compaction_operation.py -q` → `127 passed in 0.51s`（来自 fix artifact 验证记录）

**6. 确认无 production code 在本次 fix 中被修改**

- `git diff HEAD -- dayu/host/compact_material.py dayu/host/compaction.py` 的 diff 内容与 code review 时完全一致，无新增变更。
- `git diff HEAD --stat` 显示 production 文件变更量与 Slice 1 原始 diff 一致（compact_material.py: 193 +/-, compaction.py: 20 +/-）。
- fix artifact 声明 "Production code: 未修改"，与 diff 证据一致。
- `tests/host/test_compact_material.py` 和 `tests/host/test_llm_compaction.py` 在 fix diff 中也无新增变更。

### Validation

逐项核对 accepted finding 的 expected fixed state：

| 预期 | 实际 | 状态 |
|------|------|------|
| 测试 docstring 反映 no-default-chunk 语义 | docstring 已改为 "长 evidence 默认不 chunk，单个 block 使用同一 durable request 的 query_text" | ✅ |
| labels 断言单个 E1 | `assert labels == ("E1",)` | ✅ |
| 无 E1.1/E1.2/E1.3 | `assert not {"E1.1", "E1.2", "E1.3"}.intersection(labels)` | ✅ |
| 无 chunk-style labels | `assert all("." not in label for label in labels)` | ✅ |
| durable query_text 断言保留 | 两行断言完整保留（唯一性 + 具体值） | ✅ |
| 验证通过 | 1 passed in 0.26s | ✅ |
| 无 production code 被修改 | diff 证据确认无新增 production 变更 | ✅ |

## Residual Risks

- **测试函数名仍包含 `chunks`**：`test_evidence_chunks_share_same_durable_query_text` 函数名保留旧 node id，是为了满足 handoff 指定的 pytest node id 验证命令。测试 docstring 和断言语义已明确 no-default-chunk 语义。后续若不再依赖旧 node id，可单独重命名。风险：低，不影响正确性。
- **Fix artifact 记录完整**：`docs/reviews/wu-cm-12-fix-r1-s1-fix-codex-20260618.md` 包含变更清单、验证结果、残余风险，质量合格。

## Conclusion

**fixed**

Accepted finding 已完整修复。测试 docstring、内部标识、label 断言均已迁移到 no-default-chunk 语义；durable query_text 断言完整保留；验证通过；无 production code 在本次 fix 中被修改。fix artifact 记录完整。
