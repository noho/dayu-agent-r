# WU-WAIT-04 Plan Re-Review — AgentMiMo

## Re-Review Target and Scope

- **Target**: `docs/host/wu-wait-04-production-awaiting-e2e-smoke-plan.md`
- **Work unit**: WU-WAIT-04 UI / Service Production-grade Awaiting E2E Smoke
- **Fix report**: `docs/reviews/wu-wait-04-plan-fix-codex.md`
- **Controller adjudication**: `docs/reviews/wu-wait-04-plan-review-controller-adjudication.md`
- **Prior review artifacts**:
  - `docs/reviews/plan-review-20260705-201401.md`
  - `docs/reviews/plan-review-20260705-201420.md`
- **Re-review posture**: 只验证 accepted findings 是否修复、fixed plan 是否 code-generation-ready。不实施修复、不编辑 plan、不修改源码/测试/docs（除本 artifact）。

## Re-Review Method

逐条对照 controller adjudication 中 accepted findings 的 required plan fix，检查 plan 当前文本是否满足要求。同时检查 fix 是否引入新 blocker。

## Accepted Finding Status

### Finding 1: Service/Fins poll adapter registry data flow（DS F1）

- **Controller required**: Specify exact `wait_poll_adapter_registry` construction: only when Fins awaiting runtime and awaiting tool bindings exist; otherwise `None`; reuse the same runtime as wait binding and activation registries; add tests for enabled and disabled cases.
- **Fixed plan text (S1 lines 148-149)**: 明确写出 `_fins_wait_poll_adapter_registry_from_provider_configs(...)` 调用 `_fins_awaiting_registry_inputs_from_provider_configs(provider_configs, available_tool_names=...)`；输入为 `None` 时返回 `None`；输入非 `None` 时要求 `fins_awaiting_runtime is not None` 且是 ingestion runtime，否则 fail fast；随后用同一个 `fins_awaiting_runtime` 构造 poll registry。`_tooling_options_from_discovery` 必须把结果赋给局部变量 `wait_poll_adapter_registry`，并在 `HostToolingOptions(...)` 中显式传入。
- **Code fact verification**: `dayu/service/host_assembly.py:1716-1728` 确认 `wait_adapter_registry` 和 `wait_activation_registry` 在 `_tooling_options_from_discovery` 内部构造，使用 `provider_configs` 和 `available_tool_names`。`FinsIngestionWaitPollAdapter` 构造函数接受 `runtime: FinsObservationRuntime`，与 `wait_adapter_registry` / `wait_activation_registry` 共享同一 runtime 类型。Plan 描述的数据流与现有代码模式一致。
- **Test coverage**: S1 tests 覆盖 "Fins awaiting providers 启用时 `HostToolingOptions.wait_poll_adapter_registry` 非空" 和 "无 awaiting provider / provider disabled / 无 awaiting tool binding 时为 `None`"。
- **Status**: **已修复**

### Finding 2: Deterministic poll adapter synchronization（DS F2 / MiMo 02）

- **Controller required**: Specify a test-controlled synchronization primitive, preferably an event/gate that holds Ready until the public activity callback and/or `get_run` has observed `WAITING`.
- **Fixed plan text (S2 line 181)**: 明确要求 "deterministic adapter 实例必须持有测试控制的同步门控，例如 `asyncio.Event` / gate：gate 未打开时每次 `poll_wait` 返回 `WaitPollNotReady`；测试通过 public activity callback 和/或 `get_run` 观察到 `WAITING` 后才打开 gate；gate 打开后下一次 `poll_wait` 返回 `WaitPollReady(...)`。禁止使用简单无约束的 NotReady-then-Ready 计数器作为唯一同步，因为它可能在 public WAITING 观察前提前 resolve。"
- **Code fact verification**: `dayu/host/wait_adapter.py:186-209` 确认 `WaitPollAdapter` 协议的 `poll_wait` 是无状态方法，返回 `WaitPollNotReady | WaitPollReady | WaitPollLost`。Plan 要求的 `asyncio.Event` gate 是在实例状态层面引入同步，不违反协议签名。
- **Status**: **已修复**

### Finding 3: WAITING activity observation（DS F3）

- **Controller required**: Require `on_activity` to observe `EntrypointActivityStatus.WAITING`, and use `get_run(...).status == WAITING` as an additional public snapshot assertion.
- **Fixed plan text (S2 line 184)**: "`on_activity` 必须收集并断言至少一次 `EntrypointActivityStatus.WAITING`；测试同时用 `host.get_run(accepted_run_id)` 断言 public snapshot 曂观察到 `RunStatus.WAITING`。建议 `on_activity` 在看到 waiting activity 后设置 `waiting_activity_seen` event，测试再确认 `get_run(...).status == RunStatus.WAITING`，最后打开 poll adapter gate。"
- **Code fact verification**: `EntrypointActivityStatus.WAITING` 在 `dayu/service/entrypoint_runtime.py:118` 定义，从 `HostActivityStatus.WAITING` 映射（line 1211-1212）。`RunStatus.WAITING` 是 Host public API 状态。两条路径均为 public contracts。
- **Status**: **已修复**

### Finding 4: Outbox reconnect helper（DS F4 / MiMo 01）

- **Controller required**: Replace with concrete public path: either direct `host.read_outbox_terminal_items` after terminal, or existing `startup_reconnect_entrypoint_session` if the test deliberately covers reconnect semantics.
- **Fixed plan text (S2 line 186)**: "terminal 后调用 `host.read_outbox_terminal_items(ReadOutboxTerminalItemsRequest(session_id=session_id, after=OutboxTerminalCursor(event_sequence=0), limit=...))`，断言 batch 中存在同一 `accepted_run_id` 的 terminal item，并按 `terminal_event_id` / `event_sequence` 去重。不要引用不存在的 reconnect helper；只有刻意测试 Service startup reconnect 语义时才可另用实际存在的 `startup_reconnect_entrypoint_session`，本 smoke 首选直接 public outbox read。"
- **Code fact verification**: `ReadOutboxTerminalItemsRequest`（`dayu/host/api.py:2869`）和 `OutboxTerminalCursor`（`dayu/host/api.py:2731`）均为 Host public API，从 `dayu.host` 可导入。`startup_reconnect_entrypoint_session`（`dayu/service/entrypoint_runtime.py:722`）确实存在。Plan 中已无 `run_entrypoint_startup_reconnect` 这个不存在的函数名。
- **Status**: **已修复**

### Finding 5: S1/S2 dependency framing（MiMo 03）

- **Controller required**: Clarify that S1 is the production assembly slice, while S2 is the public workflow smoke slice; S2 may use direct public opener assembly but must still validate the same public poller contract.
- **Fixed plan text (line 127)**: "S1 是 production assembly slice，目标是让真实 Service/Fins 装配能得到 poller policy 与 poll adapter registry；S2 是 public workflow smoke slice，可直接组装 public `OpenHostOptions` 和 deterministic public poller contract，不需要依赖 S1 的 Service assembly helper，但必须验证与 S1 相同的 public poller contract：`WaitPollerRuntimePolicy` + `HostToolingOptions.wait_poll_adapter_registry` + common `resolve_wait` pipeline。"
- **Status**: **已修复**

### Finding 6: Forbidden-path grep（DS F5 / MiMo 04）

- **Controller required**: Refine validation with import-oriented grep patterns, include `dayu.host.durable`, and state that the implementation report must explain any benign match.
- **Fixed plan text (S2 lines 187, 194)**:
  - Line 187: "测试文件不得 import `dayu.host.durable` 或 `dayu.host.tool_runtime`，不得出现 `open_host_durable_store`、`read_active_wait_records_for_run`、`read_wait_record_by_id`、`ResolveWaitRequest`、`WaitResolutionSource.MANUAL`、`resolve_wait(`、`EngineEvent`、dispatch row / scheduler internals / ToolRuntime internals。测试源码的注释和 docstring 应避免写入这些 forbidden names；若 implementation report 中出现 grep 命中，必须逐条解释是否为 benign match，不能把 benign match 当作已通过证明。"
  - Line 194: grep 命令包含 `from dayu\\.host\\.durable|import dayu\\.host\\.durable|from dayu\\.host\\.tool_runtime|import dayu\\.host\\.tool_runtime` 等 import-oriented patterns。
- **Status**: **已修复**

### Finding 7: WaitPollAdapter typing boundary（controller adjudication row 7）

- **Controller required**: Plan fix must explicitly state the implementation must not read durable rows or import durable helpers in the smoke assertion path. If strict typing requires a public type alias/export or a local protocol-compatible adapter signature, the implementation slice must choose the minimal public-contract-preserving approach and validate with pyright.
- **Fixed plan text (R5 line 248)**: "smoke 的 assertion path 不得读取 durable row 或导入 durable helper；若严格 typing 需要 public type alias/export 或 protocol-compatible adapter signature，implementation 必须选择最小、保持 public contract 的方案并通过 pyright，不得以此授权 durable store query、internal wait mutation 或测试私有 wait id bridging。"
- **Code fact verification**: `WaitPollAdapter` 协议的 `poll_wait(wait_record: WaitRecordRow)` 签名中 `WaitRecordRow` 来自 `dayu/host/durable/state`。deterministic adapter 实现该协议时必须导入 `WaitRecordRow`。Plan R5 正确区分了 "adapter 实现者可以导入协议类型" 和 "smoke assertion path 不得读取 durable rows"，并将 typing 边界决策委托给 implementation agent 以最小 public-contract-preserving 方案处理。
- **Status**: **已修复**

## New Blocker Check

逐项检查 fix 是否引入新 blocker：

1. **public-contract-only violations**: S1 只在 Service/Fins assembly 层操作，S2 只使用 public Host/Service contracts。无新增 internal path 依赖。**无 blocker**。
2. **Code-generation ambiguity**: S1 数据流描述足够具体，implementation agent 可直接按 `_fins_wait_poll_adapter_registry_from_provider_configs` 模式实现。S2 的 deterministic adapter 同步门控有明确的 `asyncio.Event` 规约。outbox 补读有具体 API 调用示例。**无 blocker**。
3. **Overcoupling**: S1 和 S2 保持独立——S2 可直接组装 public `OpenHostOptions`，不依赖 S1 的 Service assembly helper。**无 blocker**。
4. **Scope creep**: Fix 只修改 plan 文本，未引入新的 implementation scope。**无 blocker**。

## Residual Risks

| Risk | Severity | Description |
|---|---|---|
| `WaitRecordRow` typing | Low | deterministic adapter 实现 `WaitPollAdapter` 协议时需导入 `WaitRecordRow`（来自 `dayu/host/durable/state`），该类型未通过 `wait_adapter.__all__` 导出。Plan R5 已将此委托给 implementation agent 以最小方案处理，不构成 blocker。 |
| poller background timing | Low | Plan R3 已缓解：短 poll interval + `asyncio.Event` gate + `asyncio.wait_for` bound。 |
| forbidden grep 误报 | Low | Plan R4 已缓解：import-oriented patterns + implementation report 解释 benign match。 |

## Final Plan Review Conclusion

**pass**

所有 7 条 accepted findings 均已修复。Plan 数据流足够具体，可直接交给 implementation agent 进行 code generation。Fix 未引入新 blocker。Residual risks 均为低严重度且已有缓解措施。
