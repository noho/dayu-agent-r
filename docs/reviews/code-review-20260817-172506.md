# Code Review

## Scope

- Mode: current changes
- Branch or PR: `codex/upload-filing-oracle`，HEAD `91dbf843`
- Base: `94182a0c`（UF-FIX11 work-unit 起点）
- Output file: `docs/reviews/code-review-20260817-172506.md`
- Included scope: base 后 5 个 UF-FIX11 commits；company metadata decision/commit outcome、publication state
  machine、storage atomicity、SEC/CN producer、runtime/durable/direct result、CLI/wait/LLM-facing projection、相关测试与
  README
- Excluded scope: Host/Engine 实现（零 diff）、material rename 行为、真实 CLI/network/calibration/frozen evidence
- Parallel review coverage:
  - MiMo：authoritative state/owner、publication-lock reread、alias、atomicity、concurrency/cancellation/recovery；
    artifact `docs/reviews/uf-fix11-deepreview-state-owner-mimo-20260817.md`
  - DS：typed warning、parser/codec、durable/direct/CLI/wait projection、失败非泄漏、测试/README/类型与耦合；
    artifact `docs/reviews/uf-fix11-deepreview-projection-ds-20260817.md`
  - Controller：复核两路证据，逐行走读关键生产 diff 与真实调用链，去重并裁决 findings

## Findings

### 01-未修复-低-SKIP 执行分支未在 publication 前本地封闭 company decision

- **入口/函数**: `execute_prepared_filing_publication` 的 `SKIP` 分支
- **文件(行号)**: `dayu/fins/pipelines/filing_upload_publication.py:769`；相关远端 predicate 位于同文件
  `:426`，非 stage 静默返回位于 `dayu/fins/pipelines/upload_company_meta.py:149`
- **输入场景**: 当前不可达；若未来 arbitration 与 executor 漂移，使 `SKIP` decision 携带
  `UploadCompanyMetaDecision(disposition="skip", company_meta_intent=None)`
- **实际分支**: executor 只特判 `keep`；`skip` 会落入 stage+commit 路径，stage helper 静默返回，随后无 intent
  batch 完成 physical commit 并返回 `None`，最终 `_require_skip_company_meta_outcome` 在 durable commit 后抛
  `TypeError`
- **预期行为**: `SKIP` executor 应复用唯一 skip-compatibility predicate；非 `keep` 时必须在任何 repository
  stage/commit 前确认 exact `stage + preserve_published intent`，闭集外组合立即 fail closed
- **实际行为**: 当前正确性依赖 `_canonical_skip_requirements_are_met` 的远端不变量；executor 没有本地消费前置
  断言，未来分支演进可能把编程错误推迟到 durable physical swap 之后并投影为 generic failure
- **直接证据**: `filing_upload_publication.py:771` 仅检查 `keep`；`:781-790` 无条件进入 stage/commit；
  `upload_company_meta.py:149-150` 对非 `stage` 直接返回；
  `_canonical_skip_requirements_are_met` 当前在 `filing_upload_publication.py:451-455` 才完整表达
  `keep/no-intent | stage/preserve_published`
- **影响**: 当前 production 输入不可达，无现存用户错误；若后续闭集漂移，会发生无业务变更的 durable swap 后再
  generic failure，破坏 fail-before-publication 的状态机防线
- **建议改法和验证点**: 提取或复用单一 `_company_decision_allows_canonical_skip` predicate；executor 在
  `keep` 以外、repository 调用以前断言 predicate 且 exact stage/preserve intent。新增 owner test 注入闭集外
  `skip` decision，断言 stage/commit 为零、batch 仅由 outer finally rollback 一次
- **修复风险（低）**: 仅加强当前不可达分支的 fail-closed guard 与结构测试，不改变合法状态表
- **严重程度（低）**: 当前不可达，但异常暴露时点晚于 durable commit，且闭集不变量未由 executor 自身锁定

## Open Questions

- 无。

## Residual Risk

- `UploadOperationResult.file_events` 是 frozen dataclass 内的 mutable list；为既有设计味道，本 work unit 未扩大。
- `RESOLVER_VERSION` 仍要求 resolver 规则变化时人工同步 bump；本任务明确不改变 freshness/version 规则。
- 未执行真实 CLI、UF-PF11/UF-PF12、network、scenario/oracle calibration，符合本任务非目标。
- material upload 的同类 name 行为、name-only metadata-only physical swap 成本、post-commit cleanup 可见性维持
  accepted later-work-unit 分类。
- aggregate review 已独立重跑关键 projection/publication 测试组；完整 combined regression 已在 S3 acceptance
  通过 `2155 passed, 1 skipped, 3 warnings`，本 deepreview 未再次重复全量执行。
