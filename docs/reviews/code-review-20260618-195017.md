# Code Review

## Scope

- Mode: current changes
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `main`（未提交 diff，即 WU-CM-12-FIX-R1 aggregate fix）
- Output file: `docs/reviews/code-review-20260618-195017.md`
- Included scope: `dayu/host/compact_material.py`、`tests/host/test_compaction_operation.py`、`docs/reviews/wu-cm-12-fix-r1-aggregate-fix-codex-20260618.md` 的 uncommitted diff
- Excluded scope: 其它 WU-CM-12-FIX-R1 已提交 slice 变更
- Parallel review coverage: 无（单 reviewer focused pass）
- Reviewer: AgentMiMo
- Design truth: `docs/host/design.md` §25 (Context Governance)；`docs/engine/design.md` §15
- Control doc: `docs/host/issues-implementation-control.md`

## Findings

未发现实质性问题。

## Accepted Findings 关闭核对

### Finding 01：移除 `_provenance_from_evidence_blocks` 未使用 `evidence_blocks` 参数

**状态：已关闭。**

- diff 确认：`_provenance_from_evidence_blocks` 签名从 `(evidence_blocks, selected_blocks)` 改为 `(selected_blocks)`，`del evidence_blocks` 已删除（`compact_material.py:2862-2870`）。
- 唯一调用点 `compact_material.py:1036` 已同步更新为 `_provenance_from_evidence_blocks(selected_blocks)`。
- 函数体行为不变：原实现先 `del evidence_blocks` 丢弃该参数，再用 `selected_blocks` 过滤 `EVIDENCE_MATERIAL` section 生成 provenance。移除死参数不改变任何执行路径。
- `grep evidence_blocks` 确认文件中无残留的已删除参数引用。

### Finding 02：旧 chunking 语义测试名改为 no-default-chunk 表达

**状态：已关闭。**

- diff 确认：`test_evidence_chunks_share_same_durable_query_text` 已重命名为 `test_evidence_block_shares_durable_query_text_without_chunking`（`test_compaction_operation.py:1632`）。
- 测试 docstring 与断言未修改，语义一致。
- 运行 `pytest tests/host/test_compaction_operation.py::test_evidence_block_shares_durable_query_text_without_chunking -q` 通过（1 passed）。

## Compact Material 语义未变核对

**结论：PASS。**

- `evidence_blocks` 局部变量（`compact_material.py:1031`）仍正常赋值并消费于 `evidence_material=evidence_blocks`（`compact_material.py:1044`），不受参数移除影响。
- `_provenance_from_evidence_blocks` 函数体的核心逻辑（按 `EVIDENCE_MATERIAL` section 过滤 `selected_blocks`、校验 `accepted_evidence_id` / `tool_result_event_ref` / `tool_call_event_ref` 非空、生成 `PromptLocalProvenanceEntry`）未变更。
- 无新增分支、无条件变更、无数据流变更。

## Typing 核对

**结论：PASS。**

- `pyright dayu/host/compact_material.py tests/host/test_compaction_operation.py` 通过（0 errors）。
- diff 不引入新类型注解，仅移除一个参数。

## AGENTS.md / CLAUDE.md 合规核对

**结论：PASS。**

- 函数 docstring 已同步更新（`:param evidence_blocks:` 行已删除）。
- 无新增 `object`、`Any`、无类型参数或无类型返回值。
- 无胶水 seam、无兼容性代码。
- 无新增魔法数字或魔法字符串。

## Fix Artifact 核对

**结论：PASS。**

- `docs/reviews/wu-cm-12-fix-r1-aggregate-fix-codex-20260618.md` 准确描述了两项修复内容、验证结果（pytest 90 passed、pyright 0 errors）和 README 检查结论。
- 残余风险评估（"只删除死参数和改测试名"）与实际 diff 一致。

## Open Questions

- 无。

## Residual Risk

- 聚合 review artifact `docs/reviews/code-review-20260618-195224.md` §2 仍引用旧测试名 `test_evidence_chunks_share_same_durable_query_text`（第 52 行）。该引用已过时，属于 review artifact 文档 debt，不影响代码 correctness。如需保持 review 文档一致性，可单独更新。

## 结论

**PASS。** 已接受的两项 low findings 已完全关闭；compact material 语义未改变；未引入新的 correctness、typing、测试或 AGENTS.md 违规；fix artifact 足以作为 gate 证据。
