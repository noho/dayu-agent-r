# Plan Re-Review: WU-CLI-SMOKE-01

## Review Metadata

- Reviewed target: `docs/host/wu-cli-smoke-01-dayu-cli-core-usability-plan.md`
- Fix artifact: `docs/reviews/wu-cli-smoke-01-plan-fix-codex.md`
- Prior review artifacts: `docs/reviews/plan-review-20260706-163952.md`, `docs/reviews/plan-review-20260706-164108.md`
- Controller adjudication: `docs/reviews/wu-cli-smoke-01-plan-review-controller-adjudication.md`
- Supporting evidence: `docs/reviews/wu-cli-smoke-01-auto-validation-codex.md`
- Reviewer: AgentMiMo
- Date: 2026-07-06
- Review type: re-review gate (not new broad review)

## Re-Review Scope

This re-review verifies that the three accepted findings from the controller adjudication are correctly fixed in the current plan. No new broad review is performed. The re-review does not modify plan/code/tests/README/control doc and does not enter implementation.

## Finding Verification

### F1: Idle Ctrl+C counter semantics — 已修复

**Controller adjudication requirement**: first idle Ctrl+C at an empty prompt records an exit-pending interrupt and returns to a clean prompt; second consecutive idle Ctrl+C exits; pending-exit state resets when the user performs non-interrupt input activity such as typing/submitting a prompt, EOF handling, or entering a running Run state; prompt redisplay alone does not reset the pending-exit state.

**Current plan verification**:

1. Success signals section (line 22): "Idle input Ctrl+C has an exact two-step counter contract: the first Ctrl+C at an empty prompt records an exit-pending interrupt and redisplays a clean prompt without exiting; the second consecutive idle Ctrl+C exits. Prompt redisplay alone does not reset the pending-exit state." ✓ matches adjudication.

2. Contract / State-machine section (lines 119-123):
   - "First idle Ctrl+C at an empty prompt must clear/cancel the current input attempt, record an exit-pending idle interrupt, redisplay a clean prompt, and continue the REPL without exiting." ✓
   - "Second consecutive idle Ctrl+C at an empty prompt must exit locally with the existing keyboard interrupt exit code." ✓
   - "Prompt redisplay caused by the first idle Ctrl+C does not reset the exit-pending state. This is intentional: two consecutive empty-prompt Ctrl+C inputs mean local exit." ✓
   - "The exit-pending idle interrupt state must reset when the user performs non-interrupt input activity, including submitting a prompt, EOF handling, or entering a running Run state." ✓ matches adjudication reset conditions exactly.

3. Risks / Open Questions section (line 187): The prior open question about counter reset behavior has been replaced with the controller-adjudicated contract, stated as a blocking question only if implementation contradicts the plan's fixed contract. ✓

4. The old open question about "first idle Ctrl+C after an empty prompt should reset the 'second Ctrl+C exits' counter immediately after redisplay" is no longer present. ✓

**Verdict**: The plan now defines the exact idle Ctrl+C counter semantics as adjudicated by the controller. All four behavioral rules (first non-exit, second exit, prompt redisplay does not reset, non-interrupt activity resets) are stated in both success signals and the contract section. The open question is closed.

### F2: Composer protocol overreach — 已修复

**Controller adjudication requirement**: implementation should not change InteractiveComposer protocol or prompt_toolkit key binding unless new direct evidence proves the boundary is false. Default fix target is `_run_interactive_repl` input-loop handling.

**Current plan verification**:

1. Implementation Decisions #4 (line 132): "Do not change the `InteractiveComposer` protocol or prompt-toolkit key binding unless new direct code evidence proves the current boundary is false. The current evidence says the composer already raises `KeyboardInterrupt` for empty-prompt Ctrl+C and clears non-empty input internally." ✓ matches adjudication exactly.

2. Implementation Decisions #3 (line 131): "Default fix target is `_run_interactive_repl` input-loop handling around the existing `InteractiveComposer.read(...)` boundary." ✓ matches adjudication default.

3. Affected Files section (line 99): "`dayu/cli/composer.py` only if new direct code evidence proves the current `InteractiveComposer` `KeyboardInterrupt` boundary is false." ✓ consistent boundary guard.

4. The prior Implementation Decisions #3 text "Prefer extending the input composer boundary to return a typed interrupt result or raising a CLI-local typed exception over catching broad KeyboardInterrupt at the outer command boundary" has been replaced. ✓

**Verdict**: The plan now forbids changing the InteractiveComposer protocol or prompt-toolkit key binding by default, and explicitly defaults to `_run_interactive_repl` input-loop handling. The conditional escape clause ("unless new direct evidence proves the current boundary is false") is present and consistent across implementation decisions and affected files.

### F3: Second Ctrl+C validation — 已修复

**Controller adjudication requirement**: Update validation commands and expected assertions to require PTY coverage for first idle Ctrl+C non-exit, second consecutive idle Ctrl+C exit, and reset after normal input activity where feasible.

**Current plan verification**:

1. Validation commands (lines 155-161): Three named PTY scenario commands are present:
   - `probe_interactive_ctrl_c.py --scenario first-idle-ctrl-c` ✓
   - `probe_interactive_ctrl_c.py --scenario second-consecutive-idle-ctrl-c` ✓
   - `probe_interactive_ctrl_c.py --scenario reset-after-normal-input-activity` ✓

2. Expected assertions (lines 166-168):
   - Scenario `first-idle-ctrl-c`: "sends one Ctrl+C at an empty prompt, records `exited_after_first_ctrl_c=False`, and observes a clean prompt redisplay." ✓
   - Scenario `second-consecutive-idle-ctrl-c`: "sends two consecutive empty-prompt Ctrl+C inputs with no intervening user input activity, records first Ctrl+C non-exit, and records second Ctrl+C local exit with the existing keyboard interrupt exit code." ✓
   - Scenario `reset-after-normal-input-activity`: "proves reset after normal input activity where feasible. Preferred coverage is first idle Ctrl+C non-exit, then a non-interrupt user activity such as typed/submitted input or EOF handling, then a later idle Ctrl+C is treated as a new first interrupt rather than an exit." ✓ matches adjudication "where feasible" qualifier.

3. The reset scenario includes the implementation constraint guard: "If raw typing cannot be observed without changing `InteractiveComposer` protocol or prompt-toolkit binding, the implementation gate must document that direct evidence and still cover the feasible observable reset boundary." ✓ consistent with F2's composer boundary.

**Verdict**: The plan now requires concrete PTY scenarios covering all three adjudicated behavioral cases. The "where feasible" qualifier from the controller adjudication is respected, with an explicit documentation requirement when the reset scenario cannot be fully observed.

## Residual Risks

No new residual risks were introduced by this re-review. The existing residual risks from the controller adjudication remain unchanged:

| Risk | Classification | Owner / destination |
|---|---|---|
| Real provider running-state Ctrl+C UX remains manually validated | deferred-with-owner | MANUAL-02 in goal confirmation; user supplies real environment evidence later in this WU. |
| Optional real Fins download/process remains manual | deferred-with-owner | MANUAL-03 in goal confirmation; user supplies credentials/network evidence if available. |

## Conclusion

**pass**

All three accepted findings from the controller adjudication are verified as correctly fixed in the current plan:

1. F1 (idle Ctrl+C counter semantics): The plan defines the exact two-step contract including all four behavioral rules (first non-exit, second exit, prompt redisplay does not reset, non-interrupt activity resets). The open question is closed.
2. F2 (composer protocol overreach): The plan defaults to `_run_interactive_repl` input-loop handling and forbids changing InteractiveComposer protocol or prompt-toolkit key binding unless new direct evidence contradicts the boundary.
3. F3 (second Ctrl+C validation): The plan requires three named PTY scenarios covering first non-exit, second consecutive exit, and reset after normal input activity where feasible.

The plan is code-generation-ready for the implementation agent. No blocking findings remain.
