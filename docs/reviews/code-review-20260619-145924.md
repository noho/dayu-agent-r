# Code Review (Aggregate Deepreview Re-Review)

## Scope

- Mode: re-review
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `main`
- Output file: `docs/reviews/code-review-20260619-145924.md`
- Reviewed artifacts:
  - `docs/reviews/code-review-20260619-144741.md` (aggregate review — blocking finding)
  - `docs/reviews/code-review-20260619-144524.md` (aggregate review — second pass)
  - `docs/reviews/wu-cm-compact-rejected-diagnostic-aggregate-deepreview-adjudication-20260619-144950.md` (adjudication)
  - `docs/reviews/wu-cm-compact-rejected-diagnostic-aggregate-fix-20260619-145736.md` (fix artifact)
- Changed file: `tests/host/test_public_compact_smoke.py` (unstaged)
- Included scope: blocking finding fix verification; no production code changes
- Excluded scope: 不扩大 scope，不修 production compact/fallback/tier/parser

## Original Blocking Finding

**Finding**: `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` — `assert fake_compactor.prompt_lengths == []` 失败，实际 `[19454]`。原 aggregate review (`code-review-20260619-144741.md`) 将其分类为 blocking for merge。

**Adjudication**: `needs-more-evidence` — 用户确认为旧断言漂移，非 production bug。当前正确语义：超长 current input 应完整进入 compactor `current_input_anchor`。

## Fix Verification

### 1. 测试名称与 docstring

- **名称**: `test_proactive_compact_long_current_input_reaches_compactor_without_lossy_anchor` — 准确描述新语义："超长 current input 完整到达 compactor，不丢失 anchor"
- **Docstring**: `超长 current input 完整进入 compactor current_input_anchor。` — 与名称一致
- **`:raises AssertionError`**: `Run 失败、未调用 compactor 或 current input 被截断。` — 准确描述失败条件

**判断**: 名称和 docstring 符合新语义。

### 2. 断言验证 current_input_anchor 完整性

逐条走读新断言（`test_public_compact_smoke.py:593-611`）：

| 断言 | 验证内容 | 直接证据 |
|---|---|---|
| `len(fake_compactor.prompt_lengths) == 1` | compactor 恰好调用 1 次 | `FakeCompactorRunAgent` 在 `run_agent_request()` 中 append `prompt_lengths`（行 774） |
| `len(fake_compactor.material_jsons) == 1` | 产出 1 份 material JSON | `FakeCompactorRunAgent` 在 `run_agent_request()` 中 append `material_jsons`（行 774） |
| `current_anchor["anchor_label"] == "C1"` | anchor label 为标准 `C1` | `current_input_anchor_label()` 返回 `"C1"`（`compact_material.py`） |
| `current_anchor["text"] == long_prompt` | anchor text 完整等于 long prompt，无截断 | `long_prompt = _DUPLICATE_PROMPT_SENTENCE * 500`（行 565），与 `current_anchor["text"]` 逐字符比较 |
| `material_json["previous_compacted_view"] is None` | 首次 run 无 previous view | 首次 compaction 无历史 compacted view |
| `material_json["trace_material"] == []` | 无 trace material | 首次 run 无 trace |
| `material_json["evidence_material"] == []` | 无 evidence material | 首次 run 无 evidence |
| `material_json["answer_material"] == []` | 无 answer material | 首次 run 无 answer |
| `"truncated" not in material_text` | material JSON 无 truncation 标记 | `material_text = json.dumps(material_json, ...)` 序列化后搜索 |
| `"preview" not in material_text` | material JSON 无 preview 标记 | 同上 |
| `"summary" not in material_text` | material JSON 无 summary 标记（非 compact candidate summary） | 同上 |
| `len(_compact_artifact_files(...)) >= 1` | compact artifact 文件存在 | `_compact_artifact_files` 列出目录下文件 |

**判断**: 断言完整覆盖了用户要求的所有验证点：
- `current_input_anchor.text` 完整等于 long prompt ✅
- `anchor_label` 为 `C1` ✅
- 无 `truncated` / `preview` / `summary` 标记 ✅
- compact artifact 存在 ✅

### 3. Production 行为改动检查

- `git diff --stat -- dayu/`：无输出，无 production 代码改动
- `git diff --stat tests/host/test_public_compact_smoke.py`：仅 1 个测试文件变更（+23/-6）
- 测试中无 `monkeypatch` 对 production 代码的 mock 变更（仍使用 `FakeCompactorRunAgent` 替换 `_run_agent_request`，与旧测试一致）

**判断**: 无 production 行为改动。

### 4. 测试执行验证

```bash
pytest tests/host/test_public_compact_smoke.py::test_proactive_compact_long_current_input_reaches_compactor_without_lossy_anchor
```
Result: `1 passed` ✅

```bash
pytest tests/host/test_public_compact_smoke.py
```
Result: `11 passed, 1 skipped` ✅

```bash
pytest tests/host/test_context_compact_events.py tests/host/test_compaction_operation.py
```
Result: `87 passed` ✅

```bash
pyright
```
Result: `0 errors, 0 warnings, 0 informations` ✅

### 5. `_required_mapping` helper 一致性

新断言使用 `_required_mapping(material_json["current_input_anchor"], field_name="current_input_anchor")`。该 helper 在同一测试文件中已有多处使用（行 204, 238, 420, 734），是既有测试基础设施的一部分。

### 6. `FakeCompactorRunAgent.material_jsons` 属性

`material_jsons` 在 `FakeCompactorRunAgent` 类定义中声明（行 754），在 `run_agent_request()` 中填充（行 774）。同一文件中已有其他测试使用该属性（行 400-401）。非新增属性。

## Open Questions

- 无。

## Residual Risk

- **`"summary" not in material_text` 断言的边界**: 该断言搜索整个 material JSON 序列化文本中不含 `"summary"` 子串。若未来 material 结构中新增含 `"summary"` 的合法字段名（如 `"fact_summary"`），断言会误报。当前 material JSON 结构中 `session_summary` 是 `CompactReadableViewVNext` 的字段名，只出现在 previous_compacted_view 中（当前为 None），不影响。风险极低。
- **其他 aggregate review findings 状态不变**: 原 aggregate review 和 adjudication 中的 11 条 findings（AgentMiMo F1-F5, AgentDS F1-F3, New F1-F3）的处置状态未被本次 fix 改变。它们仍按原分类 deferred / already accepted。

## Original Blocking Finding Status

**已修复。**

修复内容：测试重命名为 `test_proactive_compact_long_current_input_reaches_compactor_without_lossy_anchor`，断言更新为验证 compactor 被调用 1 次、`current_input_anchor.text` 完整等于 long prompt、`anchor_label == "C1"`、无 truncation/preview/summary 标记、compact artifact 存在。无 production 代码改动。全部测试通过。

## Conclusion

**Aggregate deepreview re-review 通过。** 原 blocking finding（`test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` 断言漂移）已修复，新测试正确验证超长 current input 完整进入 compactor 的语义。无 production 行为改动。Aggregate gate 可通过。
