# Plan Review: wu-cli-download-01 Slice 1 test-only amendment

## Reviewed target and scope

- **Target**: `docs/gateflow/wu-cli-download-01-slice1-plan-amendment-20260810-005537.md`
- **Diff base**: `docs/gateflow/wu-cli-download-01-plan-20260809.md` §6 Slice 1、§9、§14.7
- **Direct blocker**: `docs/reviews/code-review-20260810-004602.md` DL-R01
- **Scope**: amendment 是否最小、动机同源、required boolean/owner 反例是否必要、`tests/fins/test_cn_pipeline.py` 三个 call sites 是否真实、是否需要其它 scope、是否引入兼容/过度耦合、当前 dirty product diff 是否被安全冻结
- **Excluded**: 不 review/fix 当前产品实现，不修改 plan/product/test/已有 artifact，不运行真实 CLI、commit/push/PR

## Assumptions tested

1. `test_cn_pipeline.py` 是现有 Slice 1 allowlist 外唯一需要为 DL-R01 修改的测试文件
2. 三个 call sites（337、402、454）真实存在且不传 `start_is_explicit`
3. required boolean 是唯一可行方案（默认值、日期反推、compat overload/wrapper 均不可行）
4. 当前 dirty product diff 未被 amendment 改动
5. DL-R01 修复所需的 production 文件变更已在 Slice 1 allowlist 内

## Findings

### 001-未修复-低-test_cn_download_runtime.py._RecordingPipeline.download 覆写签名隐式依赖

- **位置**: amendment §3 scope expansion；plan §6 Slice 1 allowed test files
- **问题类型**: 测试缺口
- **当前写法**: amendment 仅声明增加 `test_cn_pipeline.py`，未提及 `test_cn_download_runtime.py` 中 `_RecordingPipeline.download` 的签名覆写需要同步更新
- **反例/失败场景**: DL-R01 修复后 `CnPipeline.download` 新增 required `start_is_explicit: bool`，`_RecordingPipeline.download` 签名不匹配将导致 pyright 报错
- **为什么有问题**: `test_cn_download_runtime.py` 已在 Slice 1 allowlist 内，amendment 不需要扩展 scope；但 amendment 未显式指出这一隐式依赖，implementation agent 可能遗漏
- **直接证据**:
  - `tests/fins/test_cn_download_runtime.py:237-248`：`_RecordingPipeline.download` 覆写签名无 `start_is_explicit`
  - `dayu/fins/pipelines/cn_pipeline.py:633-644`：`CnPipeline.download` 当前签名无 `start_is_explicit`，DL-R01 修复后将新增
- **影响**: implementation agent 若只关注 amendment 显式列出的 `test_cn_pipeline.py`，可能遗漏 `_RecordingPipeline` 签名更新，导致 pyright 失败
- **建议改法和验证点**: amendment 或 implementation checklist 应注明 `_RecordingPipeline.download` 签名需同步；或在 §9 static check 中增加 "所有 `CnPipeline` 子类覆写签名与父类一致" 的 pyright 验证
- **修复风险（低）**: 已在 allowlist 内，只需 implementation agent 注意
- **严重程度（低）**: pyright 会捕获此遗漏，不会静默通过

### 002-未修复-低-test_cn_download_workflow.py 辅助函数 _collect_events 隐式依赖

- **位置**: amendment §3 scope expansion
- **问题类型**: 测试缺口
- **当前写法**: amendment 未提及 `test_cn_download_workflow.py` 中 `_collect_events` 辅助函数调用 `pipeline.download_stream(...)` 需要同步更新
- **反例/失败场景**: DL-R01 修复后 `CnPipeline.download_stream` 新增 required `start_is_explicit`，`_collect_events` 调用不传该参数将导致 pyright/运行时失败
- **为什么有问题**: `test_cn_download_workflow.py` 已在 Slice 1 allowlist 内，amendment 不需要扩展 scope；但同 001，未显式标注
- **直接证据**:
  - `tests/fins/test_cn_download_workflow.py:718-724`：`_collect_events` 调用 `pipeline.download_stream(ticker=..., form_type=..., start_date=..., end_date=..., overwrite=..., cancel_checker=...)` 无 `start_is_explicit`
  - `tests/fins/test_cn_download_workflow.py:1210-1217`：直接调用 `pipeline.download(...)` 无 `start_is_explicit`
- **影响**: 同 001，pyright 会捕获
- **建议改法和验证点**: 同 001
- **修复风险（低）**: 已在 allowlist 内
- **严重程度（低）**: pyright 会捕获

## Open Questions

无。amendment 的 scope、motivation、required boolean 必要性和 call sites 真实性均已验证收敛。

## Residual Risks

1. **DL-R01 修复的实际 test 变更范围大于 amendment 显式声明**：amendment 声称 "唯一 scope expansion" 是指 allowlist 新增一个文件，但实际 DL-R01 修复需要更新至少三个测试文件（`test_cn_pipeline.py`、`test_cn_download_runtime.py`、`test_cn_download_workflow.py`）中的多个 call sites。前两个文件已在 allowlist 内，不影响 amendment 正确性，但 implementation agent 需注意完整覆盖。建议在 implementation checklist 中列出所有需更新的 test call sites。

2. **§9 新增 static check 的 production 验证**：amendment 在 §9 新增了 "`FinsDownloadDateRange.start_is_explicit` 从 adapter 经 SEC/CN pipeline 穿透到 workflow" 的 static check。该 check 涉及 production 文件（`cn_pipeline.py`、`cn_download_workflow.py`、`sec_pipeline.py`、`sec_download_workflow.py`），均已在 Slice 1 allowlist 内。验证点清晰，无 scope 越界风险。

## Conclusion

**pass**

Amendment 最小、动机同源（DL-R01）、required boolean 方案正确且必要、三个 call sites 真实存在、无需其它 scope expansion、未引入兼容/过度耦合、当前 dirty product diff 被安全冻结（amendment 仅修改 plan artifact，未触及 production/test）。两个低严重程度 findings 均为 implementation agent 注意事项，不构成 blocker——pyright 会在实现时自动捕获签名不匹配。
