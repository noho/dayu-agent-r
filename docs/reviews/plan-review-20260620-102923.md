# WU-CM-15 Plan Fix Re-Review — AgentDS Focused Verification

## Review Metadata

- **Review type**: Focused plan fix re-review (not a fresh broad review, not implementation review).
- **Target plan**: `docs/host/host-issues/wu-cm-15-public-smoke-reactive-fallback-plan.md`
- **Original review**: `docs/reviews/plan-review-20260620-102145.md` (AgentDS)
- **Other review**: `docs/reviews/plan-review-20260620-102108.md` (AgentMiMo)
- **Controller adjudication**: `docs/reviews/wu-cm-15-plan-review-adjudication-20260620.md`
- **Fix artifact**: `docs/reviews/wu-cm-15-plan-fix-codex-20260620.md`
- **Re-reviewer**: AgentDS
- **Date**: 2026-06-20

## Scope

Verify only whether the seven accepted plan review findings were fixed in the plan, and whether the fix introduced any new blocker. Do not do a fresh broad code review and do not implementation.

## Verification Results

### Finding 1: Fallback pressure mechanism + requested_proactive >= 1 (MiMo 001 / DS F-04)

- **Accepted requirement**: Specify pressure mechanism, assert effective pressure between soft/hard thresholds, require `requested_proactive >= 1`.
- **Plan evidence**:
  - Implementation Decisions item 3 (lines 159-165): explicitly names both `_compact_pressure_padding(...)` / `_estimate_chars_as_tokens(...)` and `_soft_threshold_prompt()` as acceptable pressure mechanisms; requires effective pressure between soft and hard thresholds; requires `requested_proactive >= 1` with suite failure on missing proactive request.
  - Slice S3 (lines 300-302): repeats pressure mechanism options, threshold assertions, and `requested_proactive >= 1` requirement.
- **Status**: **已修复** — pressure mechanism, threshold guard, and proactive-request guard are all explicitly specified.

### Finding 2: Deterministic worker injection via dataclasses.replace(...) (MiMo 002 / DS F-01)

- **Accepted requirement**: Specify `dataclasses.replace(...)` on assembled `OpenHostOptions.worker_factory`, no Host/Service API changes.
- **Plan evidence**:
  - Implementation Decisions item 3 (lines 142-148): calls `compose_open_host_options(...)` normally, then `dataclasses.replace(assembled_options, worker_factory=...)` with a smoke-local `LocalEngineWorkerFactory`; explicitly forbids modifying Host/Service assembly APIs.
  - Slice S2 (line 270): repeats `dataclasses.replace(assembled_options, worker_factory=smoke_factory)`, with explicit "Do not add parameters to Host or Service assembly APIs."
- **Status**: **已修复** — injection path is concrete and API-change prohibition is explicit.

### Finding 3: fallback_input_window audit bounded fields pre-authorized (MiMo 003)

- **Accepted requirement**: Pre-authorize extending `CompactFailedOperationAudit` or equivalent with `selected_block_ids`, `dropped_block_ids`, `current_input_ref`.
- **Plan evidence**:
  - Implementation Decisions item 6 (lines 212-218): pre-authorizes extending `CompactFailedOperationAudit` or an equivalent smoke-local audit object; specifies `selected_block_ids` (non-empty list), `dropped_block_ids` (list, possibly empty), `current_input_ref` (non-empty string); mandates bounded diagnostic refs only, not full fallback material.
  - Slice S3 (lines 305-306): repeats the extension authorization with the same field names.
- **Status**: **已修复** — field names, types, and non-empty/possibly-empty constraints are specified.

### Finding 4: Synthetic helper test data shape specified (MiMo 004)

- **Accepted requirement**: Specify minimal synthetic data for reactive and fallback helper tests, plus a fallback failure case for missing `requested_proactive`.
- **Plan evidence**:
  - Slice S1 (lines 240-244): reactive helper tests use `CompactAuditSummary` with `requested_reactive >= 1`, `compacted_reactive >= 1`, `failed_reactive == 0`; fallback helper tests use `CompactAuditReport` or equivalent failed-operation audit with one `CompactFailedOperationAudit`, `fallback_action="dispatch"`, bounded `fallback_input_window` fields, and no accepted `CONTEXT_COMPACTED` for the same operation; failure case for missing `requested_proactive` is required.
- **Status**: **已修复** — synthetic data shapes for both helpers and the negative case are all specified.

### Finding 5: Compactor stubs smoke-local in utils, no imports from tests (DS F-02)

- **Accepted requirement**: Keep deterministic compactor stubs in `utils/smoke_host_public_conversation_memory_scenarios.py`; runtime `utils/` must not import `tests/`; no production seams.
- **Plan evidence**:
  - Implementation Decisions item 4 (lines 169-177): requires stubs to be "smoke-local in `utils/smoke_host_public_conversation_memory_scenarios.py`, behind a small context manager used only inside deterministic suite execution"; explicitly forbids importing test modules from runtime `utils/`; states "must not extract a production seam just to share compactor stubs"; retains stop condition on unacceptable runtime patching.
  - Slice S2 (line 269): "Keep compactor stubs in `utils/smoke_host_public_conversation_memory_scenarios.py`; do not import tests from runtime `utils/`."
  - Slice S3 (line 298): "Implement the rejecting compactor response with the smoke-local compactor stub context manager created in S2; do not import test-only stubs and do not add production seams."
- **Status**: **已修复** — location, import prohibition, and seam prohibition are all explicit.

### Finding 6: AgentRunRequest and AttemptDispatchSnapshot capture inside LocalEngineWorker.accept(...) (DS F-03)

- **Accepted requirement**: Specify that deterministic workers record every `AgentRunRequest` and `AttemptDispatchSnapshot` inside `LocalEngineWorker.accept(snapshot, request)` before returning the handle.
- **Plan evidence**:
  - Implementation Decisions item 3 (lines 149-150): "The deterministic worker must record every `AgentRunRequest` and `AttemptDispatchSnapshot` inside `LocalEngineWorker.accept(snapshot, request)` before returning a handle. These captured requests/snapshots are the assertion source for one-system-message checks, current input preservation, selected-window rendering, and recovery Attempt identity."
  - Slice S2 (line 271): "Capture ordinary `AgentRunRequest`s and dispatch snapshots in a smoke observation object by recording both arguments at the start of `LocalEngineWorker.accept(snapshot, request)` before returning the handle."
  - Slice S2 completion signal (line 278): capture infrastructure is listed as reusable by S3.
- **Status**: **已修复** — capture mechanism, timing (before returning handle), and assertion uses are specified.

### Finding 7: S2 shared infrastructure reused by S3 (DS F-05)

- **Accepted requirement**: Specify that S2 creates shared deterministic worker/request-capture/compactor-stub infrastructure reused by S3, no duplicate worker implementations.
- **Plan evidence**:
  - Slice S2 (lines 266-268): "Build this shared infrastructure in S2 for reuse by S3: deterministic worker factory, deterministic worker handles, request/snapshot capture, and compactor stub context manager. S3 must extend or parameterize this infrastructure, not duplicate a second worker implementation."
  - Slice S2 completion signal (line 278): "The deterministic worker factory, request/snapshot capture, and compactor stub context manager are reusable by S3 without introducing a second worker implementation."
- **Status**: **已修复** — shared infrastructure items are enumerated and duplication is forbidden.

## New Blocker Check

All seven accepted findings were specification gaps (underspecified implementation details, missing pre-authorizations, or unclear location decisions). The fixes are additive clarifications that fill these gaps with concrete guidance. No fix contradicts:

- The original plan scope (smoke coverage only, no production changes).
- The `memory-compact` preservation constraint.
- The Host/Engine public API non-modification constraint.
- The durable schema / EventLog / provider contract / state-machine non-modification constraint.
- Any non-goal or stop condition.
- Any slice boundary or sequencing decision.

All slice stop conditions remain intact. All risk mitigations remain referenced. The plan's "Why This Is Not Over-Designed" section remains accurate.

**No new blockers introduced.**

## Residual Risks

| ID | Risk | Status post-fix |
|----|------|-----------------|
| RR-01 | Compactor monkeypatching of `dayu.host.llm_compaction._run_agent_request` may break if the private module path changes | Unchanged — plan retains the stop condition. Acknowledge-only risk. |
| RR-02 | Fallback suite pressure tuning may need per-environment adjustment | Reduced — plan now requires explicit soft/hard threshold assertion with diagnostic failure messages. |
| RR-03 | Placeholder `DEEPSEEK_API_KEY` env var required even though no real provider call occurs | Unchanged — plan validation commands use `DEEPSEEK_API_KEY=test-provider-key`. Acknowledge-only risk. |

No new residual risks introduced by the fix.

## Conclusion

**PASS**

All seven accepted findings are verified as 已修复. No new blockers were introduced. The plan is code-generation-ready for the implementation gate.

| # | Original Finding | Status |
|---|-----------------|--------|
| 1 | Fallback pressure mechanism underspecified (MiMo 001 / DS F-04) | 已修复 |
| 2 | Deterministic worker injection path underspecified (MiMo 002 / DS F-01) | 已修复 |
| 3 | fallback_input_window audit infrastructure gap (MiMo 003) | 已修复 |
| 4 | Synthetic helper test data shape unspecified (MiMo 004) | 已修复 |
| 5 | Compactor stub location unspecified (DS F-02) | 已修复 |
| 6 | Request capture mechanism unspecified (DS F-03) | 已修复 |
| 7 | S2 shared infrastructure reuse not called out (DS F-05) | 已修复 |
