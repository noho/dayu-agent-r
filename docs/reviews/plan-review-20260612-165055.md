# WU-CM-08 Plan Review — AgentMiMo

## 审查对象

- Plan artifact: `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md`
- 总控: `docs/host/issues-implementation-control.md`
- 设计真源: `docs/host/design.md`, `docs/engine/design.md`
- GitHub Issue #95 (OPEN)
- 代码: `tests/host/test_compact_material.py`, `tests/host/test_public_compact_smoke.py`

## 审查时间

2026-06-12T16:50:55+08:00

## 结论

**PASS**

---

## 1. 动机是否成立且是否被高估

**动机成立，未被高估。**

直接证据：

- `test_compact_material.py` (2173 行) 内联断言密度高，多个测试重复构造 `CompactMaterialPack` / `ConversationCompactInputVNext` 并逐字段检查，但缺少统一的 shape 摘要 helper。当 section mapping 或 label 边界变更时，失败消息只报告具体值不匹配，不标识是哪个边界（section mapping / current anchor / evidence chunk / provenance）出了问题。
- `test_public_compact_smoke.py` (1207 行) 的 `FakeCompactorRunAgent.__call__` 和各 smoke 测试中散布了对 material shape、instruction contract、forbidden terms、evidence presence、label-only proposal 的断言，但这些断言的失败定位能力取决于测试名称而非 helper 语义。`_assert_compactor_material_instruction_contract` 已存在但其它边界（material section shape、forbidden terms in material、evidence marker retention、fake proposal label-only）仍是 inline。
- Issue #95 body 使用旧命名（`stable_input` / `history_input` / `evidence_input`），plan 正确识别为 stale 并拒绝恢复。当前 production 代码中旧命名零残留（仅 `compaction.py` 的 stale label prefix set 保留旧 `S`/`H` 前缀用于 stale detection，不属于本 WU 范围）。

动机严重性评估合理：这不是 bug fix，而是 test maintainability 投资。当前测试能通过，但边界变更时的 failure localization 成本确实高于必要水平。

---

## 2. Plan 是否严格限定为测试可维护性 / readability / smoke failure localization

**是。Plan scope 边界清晰，non-goals 正确。**

逐项核对：

| 约束 | Plan 是否遵守 | 证据 |
|---|---|---|
| 不改变 memory snapshot schema | 是 | Non-Goals 第 1 条明确；S1/S2 allowed files 只限 tests/ |
| 不改变 compactor JSON 语义 | 是 | Non-Goals 第 2 条明确；S1 限定"不修改 `compact_material.py` 除非新测试暴露真实缺陷" |
| 不改变 public Host API / OpenHostOptions | 是 | Non-Goals 第 4 条明确 |
| 不改变 Conversation Memory 语义 | 是 | Goal Confirmation 第 1 段明确"不负责裁决 Conversation Memory 语义模型" |
| 不引入 broad golden snapshots | 是 | Non-Goals 第 5 条明确；S1 明确"Do not snapshot full material JSON" |
| 不把 test helper 提升为 production code | 是 | Non-Goals 第 6 条明确 |
| 不恢复 Issue #95 body 旧命名 | 是 | Goal Confirmation 直接指出旧命名 stale，当前真源使用新 section 名 |

Plan 没有 scope creep 迹象。两个 slice 的"Exact changes"都聚焦于 assertion helper 提取和 failure message 改善，不涉及 production 逻辑变更。

---

## 3. 两个 slice 是否 code-generation-ready，allowed files 是否足够且不过宽

### Slice 1: Focused Compact Material Test Readability

**code-generation-ready: 是。**

- **Allowed files**: `tests/host/test_compact_material.py` + `docs/reviews/wu-cm-08-s1-implementation-report.md`。范围精确，只触及一个测试文件。
- **变更描述具体**: "Add small test-only assertion helpers that summarize material pack / vNext input shape by section labels and section counts" — 实现 agent 知道要写什么类型的 helper。
- **约束明确**: "typed tuples and concrete dataclasses or local tuples; do not introduce `Any`, `object`, or untyped containers" — 对齐 AGENTS.md 编码硬约束。
- **验证命令**: `pytest tests/host/test_compact_material.py -q` + `pyright dayu/ tests/ utils/` — 完整覆盖。
- **保护措施**: "Do not modify `dayu/host/compact_material.py` unless the new tests expose a real production defect" — 防止 scope creep。

**小问题**: Plan 说"Add or strengthen a focused test proving vNext material JSON uses the current section names ... and excludes stale legacy names"。当前 `test_conversation_compact_input_vnext_maps_material_without_citable_current_anchor` (line 377-382) 已经断言 `"stable_input" not in vnext_json` 和 `"history_input" not in vnext_json`。Plan 应明确这是"strengthen existing"还是"add new standalone"。不阻塞。

### Slice 2: Public Compact Smoke Failure Localization

**code-generation-ready: 是。**

- **Allowed files**: `tests/host/test_public_compact_smoke.py` + `docs/reviews/wu-cm-08-s2-implementation-report.md`。范围精确。
- **变更描述具体**: 五个明确的 helper 提取目标（instruction contract / section shape / forbidden terms / evidence presence / label-only proposal），每个都有 boundary label 要求。
- **约束明确**: "Keep fake compactor behavior deterministic and label-only"、"Do not call real providers"、"Do not change the default skip behavior of optional real compactor smoke"。
- **验证命令**: `pytest tests/host/test_public_compact_smoke.py -q` + `pyright dayu/ tests/ utils/`。

**观察**: `_assert_compactor_material_instruction_contract` (line 771) 已存在且已做 forbidden term 检查。S2 提取的五个 helper 中，"compactor material instruction contract"和"forbidden internal terms in LLM-facing material"与现有 helper 有重叠。Plan 应明确是重构现有 helper 还是新增并行 helper。不阻塞，但实现 agent 需要判断。

---

## 4. S1/S2 验证命令是否覆盖风险

**覆盖。**

| 风险 | 验证覆盖 |
|---|---|
| 断言 helper 类型错误 | `pyright dayu/ tests/ utils/` |
| 断言 helper 引入导致现有测试失败 | `pytest tests/host/test_compact_material.py -q` / `pytest tests/host/test_public_compact_smoke.py -q` |
| Helper 意外改变断言语义（放宽或收紧） | 现有 41 passed + 1 skipped baseline 应保持不变 |
| Production 代码被意外修改 | S1 明确禁止修改 `compact_material.py`（除非暴露真实缺陷）；S2 allowed files 只限 `test_public_compact_smoke.py` |

**注意**: Plan 的 baseline 验证记录"41 passed, 1 skipped"应在实现前由实现 agent 重新验证，确保 baseline 未漂移。这是标准 practice，plan 已隐含（Goal Confirmation 末尾），但未在 S1/S2 validation 中显式要求"先验证 baseline 再开始修改"。不阻塞。

---

## 5. README 判断是否正确

**正确。**

- `tests/README.md`: WU-CM-08 只在已有 `tests/host/` 文件内提取 helper 和补充断言，不引入新 test layer、新命令或新 maintenance rule。不需要更新。
- `dayu/host/README.md`: 不触发，因为 production Host 代码不修改。

---

## 6. 是否存在更好的切分或遗漏的阻断风险

### 切分评估

当前两个 slice 按测试文件切分（`test_compact_material.py` / `test_public_compact_smoke.py`），这是自然的 ownership 边界。两个 slice 之间没有代码依赖（S2 的 `_assert_compactor_material_instruction_contract` 已存在），可以独立实现和验证。

**不需要更好的切分。**

### 遗漏的阻断风险

**无阻断风险。** 以下为非阻断观察：

1. **Issue #95 body 旧命名**: Plan 已正确处理（Goal Confirmation 指出 stale）。但 Issue #95 body 本身未更新。建议实现完成后 comment Issue #95 说明当前 section 命名真源，避免后续 contributor 误解。不阻塞 plan review。

2. **`_CONVERSATION_COMPACT_STALE_LABEL_PREFIXES_VNEXT`**: `compaction.py` line 194 保留了旧 `S`/`H` 前缀用于 stale label detection。这不属于 WU-CM-08 范围（test-only），但如果 S1 新增 stale legacy name 断言，应确认 stale prefix set 的存在不影响断言语义。不阻塞。

3. **S2 helper 与现有 helper 重叠**: 如上 §3 所述。实现 agent 需决定是扩展还是替换 `_assert_compactor_material_instruction_contract`。不阻塞。

---

## 具体修正建议

| # | 严重性 | 建议 |
|---|---|---|
| 1 | Low | S1 "Add or strengthen a focused test proving vNext material JSON uses the current section names" 应明确：当前 `test_conversation_compact_input_vnext_maps_material_without_citable_current_anchor` 已断言旧命名缺失；plan 应说明是新增独立 test case 还是强化现有 test。 |
| 2 | Low | S2 五个 helper 提取目标中，"compactor material instruction contract"和"forbidden internal terms"与现有 `_assert_compactor_material_instruction_contract` 重叠；plan 应明确是重构现有 helper 还是新增并行 helper。 |
| 3 | Low | S1/S2 validation 应显式要求"先运行 baseline 验证确认 41 passed + 1 skipped 未漂移，再开始修改"。 |
| 4 | Info | 实现完成后建议 comment Issue #95 说明当前 section 命名真源。 |

以上建议均为 Low / Info 严重性，不阻塞 plan 通过。

---

## 总结

Plan 动机成立、scope 严格、slice code-generation-ready、验证命令覆盖风险、README 判断正确、无阻断风险。结论: **PASS**。
