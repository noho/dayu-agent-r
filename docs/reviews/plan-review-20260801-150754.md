# Plan Re-Review: `wu-cli-interactive-02-conformance-fixes`

## 元数据

- **Review target**: `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md`
- **Work unit**: `wu-cli-interactive-02-conformance-fixes`
- **Reviewer**: AgentMiMo（独立 re-review）
- **Review date**: 2026-08-01
- **Review type**: adversarial re-review — controller 裁决后的 accepted-finding fix 验证
- **Review posture**: 默认怀疑；验证 accepted findings 真实关闭、rejected-with-reason 合理、修订不引入新 blocker
- **前置 artifacts**:
  - `docs/reviews/plan-review-20260801-143257.md`（AgentMiMo 首轮 review）
  - `docs/reviews/plan-review-20260801-143623.md`（AgentDS 首轮 review）
  - Plan §15 controller 裁决与 fix trace
- **不读取**: AgentDS 本轮 artifact（按指令约束）

## 代码直接证据核验

| 验证项 | 方法 | 结果 |
|---|---|---|
| F11 terminal writers | `grep -rn` | `dispatch.py:3203-3209`（proactive compacted）、`dispatch.py:3448`（proactive failed）、`engine_ingest.py:3201-3209`（reactive compacted）、`engine_ingest.py:3285-3293`（reactive failed）为唯二 writer |
| `_resume_interactive_ticker` 存在 | `grep -rn` | `dayu/cli/commands/session.py:575` 定义、`:290` 调用，确认存在 |
| `RunnerRequestIdentity` 无 response_identity | Agent Explore 首轮已验 | `_FinalDecision`/`FinalAnswerData`/`EngineRunOutcomeFinalAnswer` 均无该字段 |
| 双 namespace 仍存在 | Agent Explore 首轮已验 | `host_context.py` L26-29 存在 `PROMPT_SESSION_SCOPE`/`INTERACTIVE_SESSION_SCOPE` |

## §15 Controller 裁决逐项 Closure 验证

### MiMo-001：F11 reactive compaction terminal writer 不在 S4 allowed files — `accepted` → 已修复

**原始问题**: S4 allowed files 不含 `engine_ingest.py`（reactive terminal writer），F11 修复不完整。

**Plan fix 验证**:

1. §8.1 allowed files 已包含 `engine_ingest.py`（"只收敛 reactive terminal outcome commit，不重写其余 recovery state machine"）— **通过**
2. §8.2.1 代码证据明确列出两路 writer："全仓 terminal writer 核验只有两路：`dispatch.py` 追加 proactive ...，`engine_ingest.py` 追加 reactive 同名 terminal" — **通过**
3. §8.2.1 反例 3 明确描述 reactive 并发/幂等重入场景："两个已取得同一 `_ReactiveCompactPending` 的并发/幂等重入 executor 可在事务外分别得到 accepted 与 failed" — **通过**
4. §8.2.1 反例 4 明确 reactive `_append_reactive_compacted_event()` 先写 artifact 再写 terminal，"late loser 必须在 artifact/descriptor、rejected/terminal event、fallback/fail-close/recovery start 之前就返回 no-op/diagnostic" — **通过**
5. §8.2.2 第 4 点明确两路 writer 复用同一 guard："`dispatch.py` 的 ... 与 `engine_ingest.py` 的 ... 都必须在各自现有 write transaction 中首先调用这一入口" — **通过**
6. §8.5 测试要求包含 reactive 并发反例："reactive 并发/幂等重入确定性反例...每次均 exact 一个 terminal，loser 无 artifact/descriptor/rejected event/fallback/recovery start/fail-close" — **通过**
7. §8.5 writer inventory test："`dispatch.py` 与 `engine_ingest.py` 的全部 `CONTEXT_COMPACTED` / `CONTEXT_COMPACTION_FAILED` append 路径均在同一 transaction 使用 shared owner，不存在第二 guard" — **通过**

**Code fact 对照**: `grep` 确认 `engine_ingest.py` 在 L3201-3209 和 L3285-3293 写 reactive terminal，与 plan §8.2.1 描述一致。

**Closure 判定**: **真实关闭**。Plan fix 从 allowed files、设计语言、反例、guard 复用、测试要求五个维度完整覆盖了 reactive 路径。不存在残留 gap。

---

### MiMo-002 / MiMo-004：Ctrl+C idle 状态机非空 draft 行为 + exit-after-cancel 后续 Ctrl+C — `accepted` → 已修复

**原始问题**: 状态机表缺少"非空 draft Ctrl+C"行和"exit_after_cancel=true 后 Ctrl+C"行。

**Plan fix 验证**:

1. §6.5 状态表已增加 "idle + 非空或纯空白 draft + Ctrl+C → 清空整个 draft 并重绘；不设置 `idle_exit_pending`，仍 idle" — **通过**
2. §6.5 状态表已增加 "idle + 空 draft，`idle_exit_pending=false` → 登记 `idle_exit_pending=true` 并重绘；不退出" — **通过**
3. §6.5 状态表已增加 "idle + 空 draft，`idle_exit_pending=true` → 若两次之间无正常输入，完成 display/composer/attachment cleanup 后 exit 130" — **通过**
4. §6.5 状态表已增加 "cancelling/exit-after-cancel，`exit_after_cancel=true` → 第三次及之后一律 no-op；绝不再次发 cancel、`Task.cancel()` canonical waiter 或强关 Host/attachment" — **通过**
5. §6.5 关键实现要求 2："第二次以及之后的 Ctrl+C 不能 `Task.cancel()` canonical cancel/terminal waiter；`exit_after_cancel=true` 后所有后续 Ctrl+C 都 no-op" — **通过**
6. §6.7 owner-level tests："Ctrl+C deterministic matrix：idle 非空、纯空白 draft 首次只清空且不设 pending；之后空 draft 第一次只登记 exit intent，第二次 cleanup+130；中间穿插正常输入会重置 intent" — **通过**
7. §6.7 tests："active 第一次 Ctrl+C 只建立 single cancel，第二次只设 exit flag，第三次及更多次均 no-op" — **通过**

**Closure 判定**: **真实关闭**。非空 draft → Ctrl+C 只清空不设 pending、exit_after_cancel=true 后 no-op 两个缺口均已明确覆盖。

---

### DS F-01：second Ctrl+C + queued follow-up 语义越界 — `accepted-clarification-only` → 已修复

**原始问题**: exit-after-cancel 等待范围扩展到已 queued follow-up 的完整 terminal，超出冻结 oracle `interactive.12` 语义。

**Controller 裁决**: 保持现有"等待 accepted sole QUEUE terminal"语义，明确 F08+F09 exactly-once。不改为 review 建议的"留 queued 给 fresh writer"。

**Plan fix 验证**:

1. §6.5 状态表 exit-after-cancel 行："先等当前 cancel target canonical terminal，再对本 invocation 的 sole queued submit 等待明确 acceptance 结果；若已 durable accepted，必须使同一 queued Run 恰好执行一次并等其 canonical terminal" — **通过，F08 语义明确**
2. §6.5 关键实现要求 5："F08 + F09 的组合语义是 durable accepted sole `QUEUE` 必须恰好执行一次；第二次 Ctrl+C 只登记 exit-after-cancel，不撤销、取消或改写 queued Run" — **通过，F09 语义明确**
3. §6.5 要求 5 续："特别是无 label fresh Session，不得在进程退出时留下永久 `QUEUED` Run 等待一个不会到来的 fresh invocation" — **通过，确定性反例明确**
4. §6.5 要求 7："exit intent 不取消已发出的 sole submit task，而是等其返回'accepted + exact run id'或'明确未 accepted'，禁止从 task cancellation 推断 Host acceptance" — **通过，admission 语义保护**
5. §15 裁决表："保持现有'等待 accepted sole QUEUE terminal'语义，明确 F08+F09 exactly-once、second Ctrl+C 不 cancel queued Run，并加无 label fresh Session 确定性反例；不改为 review 建议的'留 queued 给 fresh writer'" — **通过，controller 决策记录完整**

**MiMo re-review 判断**: Controller 裁决合理。Oracle `interactive.12` 的 forbidden 条款"第二次 Ctrl+C 先关闭进程而留下 running Run/Attempt 或缺失 RUN_CANCELLED"确实只约束"不得留下 running Run"，而 plan 的"等待已 accepted queued Run terminal"是更安全的语义——不留 orphan queued Run。DS F-01 的"用户期望尽快退出"虽是合理 concern，但 plan 选择了 correctness 优先，且 controller 已裁决。修订未引入新 blocker。

**Closure 判定**: **真实关闭**。Controller 裁决明确，plan fix 完整记录决策理由和确定性反例。

---

### DS F-02 / MiMo-005：F13 Engine identity "attempt" 双关语义 — `accepted` → 已修复

**原始问题**: Engine `RunnerRequestIdentity.attempt_id` 与 Host `CompactionAttempt` number 语义不同，plan 未区分。

**Plan fix 验证**:

1. §9.3 identity taxonomy 表格明确区分七种 identity：
   - `compaction_operation_id`: "一次 proactive 或 reactive durable operation；跨 proposal attempts 不变"
   - `compaction_attempt_number`: "operation 内全局 proposal 序号；每个 number 都构造并执行一个新 `AgentRunRequest`"
   - `compactor_engine_run_id`: "根据 operation id + request digest + Host attempt number 派生"
   - `RunnerRequestIdentity.attempt_id / execution_id`: "表示 ordinary Host Run/Attempt/Execution 注入 Engine 的成对 identity；compactor 不是 ordinary Host Attempt 路径，两字段必须显式为 `None`，绝不得填 Host `compaction_attempt_number`"
   - `runner_call_index`: "单个 AgentRunRequest/Engine run 内从 1 递增；新 Host proposal attempt 创建新 Engine run 并从 1 重新计数"
   — **通过，七种 identity 完整区分**
2. §9.3 文字段明确："每 Host proposal attempt 是新 Engine request，同 request 的 LENGTH continuation 才递增 call index，最终 LENGTH fail closed" — **通过**
3. §9.6 tests 描述："ordinary Run final 身份 A；同 Session compactor Host attempt 1 实际成功 final 身份 B 后 semantic/parse/quality/budget rejected；Host attempt 2 实际成功 final 身份 C 后 accepted。断言 A/B/C 的 engine run/client correlation/provider request id 按设定不同" — **通过，A/B/C 串线反例覆盖**
4. §9.6 tests 续："rejected event 只携 B 且绑定 attempt-1 manifest，accepted event 只携 C 且绑定 attempt-2 manifest，A/B 绝不出现在 accepted，A/C 绝不出现在 rejected" — **通过，泄漏防护明确**
5. §9.2.3："`_classify_iteration()` 只能从同一个 `_IterationState.request_identity`、同一个 `runner_done.provider_request_id` 与本次 `AgentRunRequest.runner_spec.provider/model` 构造" — **通过，Engine 侧绑定同源**
6. §9.3 文字段："代码同时证明每个 Host `compaction_attempt_number` 都会重新调用 `prepare_compactor_proposal_run_input()` 构造一个新 `AgentRunRequest`；其 `run_id` 是该 attempt 的 `compactor_engine_run_id`，`attempt_id/execution_id` 为 `None`" — **通过，代码事实支撑**

**Closure 判定**: **真实关闭**。§9.3 identity taxonomy 表格从七个维度精确区分了 Engine/Host 两套 "attempt" 语义，§9.6 A/B/C 串线反例直接覆盖了原始 concern。修订引入了完整 taxonomy 表，不仅修复了术语歧义，还提供了 code-generation-ready 的 reference。

---

### DS F-03：场景清理 config claim 替换细节缺失 — `accepted` → 已修复

**原始问题**: 保留的 pairwise rows 的 config claim 替换文本不精确。

**Plan fix 验证**:

1. §10.3.2 明确列出五条保留 row ID：`prompt.PC-PW-R2-03`、`prompt.PC-PW-R2-04`、`prompt.PC-PW-R2-06`、`prompt.PC-PW-R2-08`、`prompt.PC-PW-R2-10` — **通过，精确 ID 列表**
2. §10.3.3："对上述五条保留 row，从 `coverage_claims.command_parameter_ids` 和 `coverage_claims.raw_stable_claims` 各删除唯一 `parameter:config:default`；不新增、改名或发明任何替代 parameter claim" — **通过，删除而非替换**
3. §10.3.3 续："每条已有的 `coverage_claims.precondition_state_ids=["init-deepseek-config-explicit"]` 与 `precondition.state_id="init-deepseek-config-explicit"` 保持不变，由 precondition 继续表达 workspace runtime config source evidence；不把系统配置来源伪装为 prompt command parameter" — **通过，precondition 保留理由清晰**
4. §10.5 validation："精确查询五条保留 pairwise row，断言它们全部存在，`command_parameter_ids` / `raw_stable_claims` 都不含 `parameter:config:default`，且两处 `init-deepseek-config-explicit` precondition 仍存在" — **通过，验证命令明确**

**MiMo re-review 判断**: DS F-03 的原始 concern 是"替换文本不精确"，但 plan 修正为"纯删除，不替换"——这比替换更简单、更安全。Implementation agent 只需在五条 row 中各删除一个 claim，不需要发明替代文本。Controller 裁决为 `accepted` 且 fix 已应用。

**Closure 判定**: **真实关闭**。Plan 从"替换"改为"纯删除"，彻底消除了替换文本不确定的问题。

---

### MiMo-003：S4 guard 预埋 optional identity — `rejected-with-reason` → 合理

**原始问题**: 建议 S4 terminal guard 函数签名预留 optional identity payload 参数。

**Controller 裁决**: rejected。理由：禁止 S4 预埋 optional identity；S5 按依赖顺序在 terminal winner writer 上做 required typed payload 扩展，guard 签名无需变更。

**Plan fix 验证**:

1. §8.2.2 第 7 点："S4 guard 的 request/permit 不预埋 optional `successful_response_identity`。S5 依赖 accepted S4 guard 后，再在两路 winner writer 的 typed accepted/rejected payload 上做 required identity 扩展；guard 只决定'能否提交 terminal'，不拥有'terminal payload 写什么'，因而 S5 无需重构或扩展 guard 签名" — **通过，职责分离明确**
2. S5 开头："S5 严格依赖 accepted S4 terminal guard：S4 只决定当前 transaction 是否是 terminal winner，不预埋 optional identity。S5 在 winner 路径上一次性把 accepted/rejected identity 改为 required typed payload；不向 S4 加 nullable 参数、payload bag 或兼容分支" — **通过，依赖方向正确**

**MiMo re-review 判断**: Rejected-with-reason 合理。S4 guard 的职责是"transaction 是否是 terminal winner"，S5 的职责是"winner 写什么 payload"。预埋 optional identity 会让 S4 guard 承担不属于它的 payload 语义，违反单一职责。S4/S5 的依赖方向（S4 guard → S5 payload extension）是干净的。

**Closure 判定**: **合理拒绝**。Rejection 理由正确，plan 不需要修改。

---

### DS F-04：test 文件路径断言 — `non-finding` → 合理

**原始问题**: pytest 命令中的 test 文件名可能不存在。

**Controller 裁决**: non-finding，证据失效。所有 test 文件路径经验证存在。

**MiMo re-review 判定**: 合理。AgentMiMo 首轮 review（assumption A9）已验证全部 30 个 test 文件存在。这不是 plan 缺陷，只是 review 时的合理 concern 被证据消除。

---

### OQ-01：`_resume_interactive_ticker()` 存在性 — `closed-by-code-evidence` → 已关闭

**Code fact**: `grep` 确认 `_resume_interactive_ticker` 在 `dayu/cli/commands/session.py:575` 定义、`:290` 调用。Plan §5.2.5 要求删除该函数和调用路径是正确的。

**Closure 判定**: **已关闭**。代码证据确认。

---

### OQ-02：F11 reactive 路径 terminal guard — `closed-by-MiMo-001` → 已关闭

**Closure 判定**: **已关闭**。MiMo-001 的 fix 已完整覆盖 reactive 路径。

---

### PTY / OQ-03：POSIX PTY smoke 环境依赖 — `allowed-variant` → 已分类

**Plan 位置**: §10.5、§12.1-§12.2。

**MiMo re-review 判定**: 分类正确。POSIX 真实 PTY、非 POSIX 记录 capability/skip 是合理的平台变体处理。不阻塞 plan review。

---

## 五条 Registry Claim 清理精确性验证

Plan §10.3 要求对五条保留 pairwise rows 精确删除 `parameter:config:default` claim。验证如下：

| 验证维度 | Plan 表述 | 判定 |
|---|---|---|
| 保留 row ID 列表 | `prompt.PC-PW-R2-03/-04/-06/-08/-10`（5 条精确 ID） | **通过** |
| 删除目标 | `coverage_claims.command_parameter_ids` 和 `coverage_claims.raw_stable_claims` 各删除唯一 `parameter:config:default` | **通过** |
| 不新增替代 claim | "不新增、改名或发明任何替代 parameter claim" | **通过** |
| Precondition 保留 | `precondition_state_ids=["init-deepseek-config-explicit"]` 保持不变 | **通过** |
| 验证命令 | §10.5 精确查询 5 条 row、断言不含 `parameter:config:default`、断言 precondition 存在 | **通过** |
| 不改 oracle 语义 | §10.4 "F01-F13 accepted predicate 语义保持不变" | **通过** |

**判定**: 五条 registry claim 清理表述精确、可直接实施。

## Shared Compaction Terminal Owner 线性化验证

### 事务内 Fresh Projection 读取作为 CAS 线性化点

Plan §8.2.2 第 6 点："SQLite write transaction 串行 + transaction 内 fresh operation projection read 是 CAS 线性化点；permit 不持久化、不跨 await/事务传递。"

**验证**:
- SQLite write transaction 保证串行执行：第一个 writer 提交 terminal 后，第二个 writer 的 transaction 内 read 必须看到已提交的 terminal — **线性化正确**
- permit 是 in-memory 对象，不持久化、不跨 await/事务传递 — **不会泄漏到其它并发上下文**
- `begin_compaction_terminal_commit_in_transaction()` 使用 `CONTEXT_COMPACTION_REQUESTED` / terminal payload strict parser — **只理解 compaction event，不演化为通用 framework**

### Proactive/Reactive Winner/Loser 裁决

| 场景 | Winner | Loser | Loser 行为 |
|---|---|---|---|
| proactive compacted → reactive late failed | proactive | reactive | §8.2.2.5: "只写有界安全 log/health diagnostic 并返回 no-op" |
| proactive failed → reactive late accepted | proactive | reactive | 同上 |
| reactive accepted → proactive late failed | reactive | proactive | 同上 |
| reactive failed → proactive late accepted | reactive | proactive | 同上 |
| 双 terminal（已存在） | 无 | 两者 | §8.2.2.5: "`INVALID_MULTIPLE`：fail closed 并保持已有 truth" |

**判定**: Winner/loser 裁决逻辑完整，late loser 在任何 artifact/event/fallback/start 前 no-op。线性化是 code-generation-ready 的。

## Ctrl+C + Sole QUEUE 同时满足 F08/F09 验证

| 要求 | Plan 覆盖 | 判定 |
|---|---|---|
| F08: Ctrl+C 表达取消完成后退出 | §6.5 状态表 exit-after-cancel 行："先等当前 cancel target canonical terminal" | **通过** |
| F09: sole QUEUE 恰好执行一次 | §6.5 要求 5："durable accepted sole `QUEUE` 必须恰好执行一次" | **通过** |
| F08+F09 组合 | §6.5 要求 5："第二次 Ctrl+C 只登记 exit-after-cancel，不撤销、取消或改写 queued Run" | **通过** |
| 不留 orphan queued Run | §6.5 要求 5："无 label fresh Session，不得在进程退出时留下永久 `QUEUED` Run" | **通过** |
| 不取消已 accepted submit | §6.5 要求 7："exit intent 不取消已发出的 sole submit task" | **通过** |
| 确定性测试 | §6.7："exit-after-cancel + sole QUEUE 确定性测试：分别在 queued submit acceptance barrier 之前/之后连续 Ctrl+C" | **通过** |

**判定**: F08/F09 组合语义在 plan 中完整且自洽。

## Engine/Host Identity Taxonomy 与 Compactor Request 事实对照

Plan §9.3 的 identity taxonomy 表格与代码事实对照：

| Identity | Plan 语义 | 代码事实 | 一致 |
|---|---|---|---|
| `compaction_operation_id` | 跨 proposal attempts 不变 | `compaction_operation.py` operation 结构 | **是** |
| `compaction_attempt_number` | operation 内全局 proposal 序号 | `run_compaction_operation()` 每次 attempt 递增 | **是** |
| `compactor_engine_run_id` | 由 operation id + request digest + Host attempt number 派生 | `prepare_compactor_proposal_run_input()` 构造新 `AgentRunRequest` | **是** |
| `RunnerRequestIdentity.attempt_id` | compactor 路径必须为 `None` | `agent_run.py` L96-98: attempt_id/execution_id 必须成对 | **是** |
| `runner_call_index` | 同 Engine run 内从 1 递增 | `agent.py` `_AsyncAgent` 每次 Runner call 递增 | **是** |
| `client_correlation_id` | 由完整 request identity canonical tuple 派生 | `runner_identity.py` 现有实现 | **是** |
| `provider_request_id` | 缺失时记 `unavailable` | `_IterationState` + `RunnerDoneData` 现有 optional 字段 | **是** |

**判定**: Identity taxonomy 与代码事实完全一致，attempt_id/execution_id 对 compactor 必须为 `None` 的约束明确。

## 自动 Gateflow 表述验证

Plan §0："Next entry point：`re-review`；本 Agent 子任务在 fix 与验证完成后把控制权交回 controller，由 controller 立即派发独立 re-review；整个 Gateflow 继续按固定 Gate Order 自动推进，不在普通 gate 停止。"

**验证**:
- Gate Order: plan review → fix → re-review → implementation → implementation review → ...
- 当前位置: re-review（本 artifact）
- 下一步: re-review 通过后自动推进到 implementation gate
- "不在普通 gate 停止" 表述正确：整个 flow 是自动推进的

**判定**: Gateflow 表述正确。

## 新 Blocker 检查

Plan §15 的修订是否引入新 blocker：

| 检查维度 | 结果 |
|---|---|
| §8.2.2 新增 `compaction_terminal.py` 是否越界 | 不越界——只理解 compaction request/terminal，不演化为通用 framework |
| §6.5 状态表新增行是否自洽 | 自洽——非空 draft Ctrl+C 只清空、exit_after_cancel=true 后 no-op 与现有行无冲突 |
| §9.3 identity taxonomy 是否引入过度设计 | 不过度——只扩展已有 `RunnerRequestIdentity`，不建立第二套 provider tracing |
| §10.3 五条 claim 清理是否可能漏改 | 不会——plan 给出精确 ID 列表和删除目标，§10.5 有验证命令 |
| S4 不预埋 identity + S5 winner path 扩展是否导致 S4 重写 | 不会——guard 只决定"能否写"，S5 只在 winner 路径扩展 payload，签名无需变更 |
| §6.5 sole QUEUE 语义是否引入不可测试行为 | 不会——§6.7 有确定性测试覆盖 queued submit acceptance barrier 前/后的 Ctrl+C |

**判定**: 未发现新 blocker。

## Findings

### 001-已关闭-中-F11 reactive 路径覆盖

- **原始**: MiMo-001（S4 allowed files 不含 `engine_ingest.py`）
- **Controller decision**: `accepted`
- **Fix 验证**: §8.1 已加入 `engine_ingest.py`；§8.2.1 完整描述两路 writer 反例；§8.2.2.4 两路 writer 复用同一 guard；§8.5 含 reactive 并发反例测试
- **Closure**: 真实关闭

### 002-已关闭-中-Ctrl+C 状态机缺口

- **原始**: MiMo-002（非空 draft Ctrl+C 行为未确定）
- **Controller decision**: `accepted`
- **Fix 验证**: §6.5 状态表新增三行覆盖非空/纯空白 draft、空 draft pending=false、空 draft pending=true 场景
- **Closure**: 真实关闭

### 003-已关闭-中-S4/S5 dispatch.py 重叠

- **原始**: MiMo-003（建议 S4 guard 预埋 optional identity）
- **Controller decision**: `rejected-with-reason`
- **合理性验证**: S4 guard 只决定"能否写 terminal"，S5 在 winner path 扩展 payload。职责分离正确，guard 签名无需变更。
- **Closure**: 合理拒绝，无需修改

### 004-已关闭-低-exit-after-cancel 后续 Ctrl+C

- **原始**: MiMo-004
- **Controller decision**: `accepted`
- **Fix 验证**: §6.5 状态表明确 "exit_after_cancel=true → 第三次及之后一律 no-op"
- **Closure**: 真实关闭

### 005-已关闭-低-F13 串线测试描述

- **原始**: MiMo-005
- **Controller decision**: `accepted`
- **Fix 验证**: §9.6 含完整 A/B/C 串线反例、泄漏防护断言
- **Closure**: 真实关闭

### DS-F01-已关闭-中-queued follow-up 语义

- **原始**: DS F-01
- **Controller decision**: `accepted-clarification-only`
- **Fix 验证**: §6.5 要求 5/7 明确 F08+F09 exactly-once 语义和 orphan 防护
- **Closure**: 真实关闭

### DS-F02-已关闭-中-attempt 术语歧义

- **原始**: DS F-02
- **Controller decision**: `accepted`
- **Fix 验证**: §9.3 identity taxonomy 表格精确区分七种 identity
- **Closure**: 真实关闭

### DS-F03-已关闭-低-config claim 替换

- **原始**: DS F-03
- **Controller decision**: `accepted`
- **Fix 验证**: §10.3.2/3 精确 ID 列表 + 纯删除（非替换）
- **Closure**: 真实关闭

## Open Questions

无新增 open question。原有 OQ-01/OQ-02/OQ-03 均已关闭或分类。

## Residual Risks

| 类别 | 风险 | Plan 控制 | 残余 | 建议跟踪 |
|---|---|---|---|---|
| terminal capability | 不同终端无法区分 Shift+Enter/Enter | 只支持 `\x1b[27;2;13~`，其它记录 capability | 部分终端用户无法使用 Shift+Enter 换行 | plan §12.1 已记录 |
| input race | terminal、Enter、Escape、SIGINT 同批完成 | generation + 固定 terminal-first 裁决 + sole tasks | OS/terminal 实现差异由 PTY/Windows 证据继续观察 | plan §12.1 已记录 |
| queue lifecycle | exit-after-cancel 时已有 durable queued follow-up | 等待 current cancel terminal + sole QUEUE terminal 收口 | G01 跨进程 queued reconnect 仍不裁决 | plan §12.1 已记录 |
| recovery clock | wall clock/heartbeat 临界点 | `<`/`>=` 清晰边界、UTC deadline、fresh now、CAS | 极端系统时钟回拨保守不恢复 | plan §12.1 已记录 |
| compaction concurrency | proactive/reactive late result 与 first terminal 竞争 | 同一 trigger-aware Host owner 在同 write transaction 做 fresh projection guard | 已损坏旧 DB 不兼容 | plan §12.1 已记录 |
| provider evidence | provider 不返回 request id | explicit unavailable + effective identity + canonical client correlation | 行为项 29 需 G06 后续 | plan §12.1 已记录 |
| secrets | RunnerSpec 含 endpoint/credential/header | 窄 identity type、exact payload、canary 扫描 | provider/model 必须由配置 schema 保证安全 | plan §12.1 已记录 |
| coverage/platform | POSIX PTY 与 Windows 差异 | POSIX 真实 PTY + Windows 既有 CI owner boundary | 本地无法替代 Windows runner 证据 | plan §12.1 已记录 |

所有 residual risks 均已在 plan §12.1 中分类，不构成 gate blocker。

## Goal-Bound Minimal Design Review

Plan §15 修订均为 accepted finding fix，未引入新 goal、新验收标准或新 future-slice work。S1-S6 仍然严格映射 F01-F13，non-goals（§2）未扩展。§15 裁决表是 controller 裁决的忠实记录，不包含 plan 自行增加的设计强化。

**判定**: 无 goal drift。

## Architecture Boundary Review

§15 修订未改变分层边界：
- `compaction_terminal.py` 是 Host 内部模块，不暴露给 Service/CLI/Engine
- Engine identity 变更通过 Engine public contract（`runner_identity.py`），Host 只消费
- CLI composer/signal 变更不穿透到 Service/Host

**判定**: 无架构边界违反。

## Overcoupling Review

S4 guard 不预埋 identity + S5 winner path 扩展 payload 的设计避免了 S4→S5 的过度耦合。Guard 签名干净，S5 无需重构 S4 代码。

**判定**: 无过度耦合。

## Plan Review Conclusion

**结论: pass**

Plan 的 accepted-finding fix 全部真实关闭。§15 controller 裁决表准确记录了每个 finding 的决策与 fix 落点。MiMo-003 的 rejected-with-reason 合理。修订未引入新 blocker、新 goal drift 或新架构边界违反。

重点验证项结果：
1. **Shared compaction terminal owner 对 proactive/reactive winner/loser 的线性化**: code-generation-ready，SQLite write transaction 串行 + transaction 内 fresh projection read 是 CAS 线性化点，late loser 在任何 artifact/event/fallback/start 前 no-op
2. **Ctrl+C + sole QUEUE 同时满足 F08/F09**: 完整且自洽，exit-after-cancel 等待 cancel terminal + accepted sole QUEUE terminal 收口
3. **Engine/Host identity taxonomy 符合 compactor request 事实**: 七种 identity 精确区分，attempt_id/execution_id 对 compactor 必须为 None
4. **五条 registry claim 清理精确**: 纯删除 `parameter:config:default`，不发明替代 claim，precondition 保留
5. **自动 Gateflow 表述正确**: re-review 通过后自动推进到 implementation gate

Plan 状态从 `awaiting-re-review` 推进到 `re-review passed`。
