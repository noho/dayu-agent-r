# Code Review

## Scope

- Mode: current changes
- Branch: wu-cm-12-conversation-memory-drift
- Base: main
- Output file: docs/reviews/code-review-20260618-152931.md
- Included scope: WU-CM-12 S2 focused re-review after code review fix。只复核 accepted findings S2-F1（duplicate turn-group helper）和 S2-F2（dead `_is_raw_turn_block`）。
- Excluded scope: S2 核心行为（已在前两轮 review 中通过）、S3/S4 后续 slice。
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Verification

### S2-F1: duplicate turn-group helper — PASS

**检查项**: `context_fallback.py` 不再 import underscored private helpers，改为复用 `compact_material.py` 的非下划线 Host-internal helper。

**直接证据**:

- `context_fallback.py:16-20` import 声明：
  ```python
  from dayu.host.compact_material import (
      RunInputMaterialBlock,
      is_turn_group_material_block,
      protected_recent_turn_group_ids_for_material_blocks,
  )
  ```
  使用非下划线函数名，从 `compact_material.py` 单一来源 import。

- `compact_material.py:1530` 定义 `protected_recent_turn_group_ids_for_material_blocks`（非下划线）。
- `compact_material.py:1571` 定义 `is_turn_group_material_block`（非下划线）。

- `context_fallback.py` 中无 `_protected_recent_turn_group_ids` 或 `_is_turn_group_material_block` 的本地定义。grep 确认唯一 turn-group 相关函数定义为 `_reverse_chronological_turn_group_blocks`（context_fallback 特有的排序/校验 helper，非重复）。

- `context_fallback.py` 中调用共享 helper 的位置：
  - 行 379: `protected_recent_turn_group_ids_for_material_blocks(raw_blocks, ...)`
  - 行 600: `is_turn_group_material_block(block)`
  - 行 602: `protected_recent_turn_group_ids_for_material_blocks(eligible, ...)`
  - 行 680: `is_turn_group_material_block(block)`

- `compact_material.py` 的 `__all__`（行 3471-3499）不包含 `is_turn_group_material_block` 和 `protected_recent_turn_group_ids_for_material_blocks`，确认这些是 Host-internal helper，不作为 public contract 暴露。

### S2-F2: dead `_is_raw_turn_block` — PASS

**检查项**: `_is_raw_turn_block` 已从 `compact_material.py` 删除。

**直接证据**:

- `grep -n "_is_raw_turn_block" dayu/host/compact_material.py` 返回空结果，确认函数定义已删除。
- `compact_material.py` 中无任何对 `_is_raw_turn_block` 的引用。

### S2 behavior unchanged — PASS

- 130 tests passed（`test_compact_material.py`, `test_memory_projection.py`, `test_run_input_builder.py`, 3 个 `test_dispatch_scheduler.py` 用例）。
- pyright: 0 errors, 0 warnings, 0 informations。
- `git diff --check`: 无 whitespace 错误。

### no new architecture/public API drift — PASS

- `context_fallback.py` 的 `__all__`（行 799-818）未变更。
- `compact_material.py` 的 `__all__`（行 3471-3499）未包含新增内部 helper。
- 共享 helper 使用非下划线命名供跨模块 Host-internal 复用，但不进入 `__all__`，不构成 public contract。

## Open Questions

- 无。

## Residual Risk

- 无。S2-F1 和 S2-F2 均已修复并验证。

## Review Conclusion

**PASS** — S2-F1（duplicate turn-group helper）和 S2-F2（dead `_is_raw_turn_block`）均已修复。`context_fallback.py` 正确复用 `compact_material.py` 的单一 Host-internal helper，`_is_raw_turn_block` 已删除，S2 behavior unchanged，tests/pyright/git diff --check 全部通过，无新 architecture/public API drift。
