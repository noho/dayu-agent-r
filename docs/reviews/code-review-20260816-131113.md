# Code Review

## Scope

- Mode: current changes（独立 code review，非 Gateflow 内置 gate）
- Branch: `codex/upload-filing-oracle`
- Base: `c8e75629`（UF-FIX08 accepted plan commit）
- Output file: `docs/reviews/code-review-20260816-131113.md`
- Review 时间：2026-08-16（本机系统时钟生成 timestamp）
- Binding scope: `docs/gateflow/uf-fix08-existing-source-auto-repair-plan-20260816.md` §9 Slice 1（含 Controller 批准的 implementation-time boundary amendment）
- Implementation artifact: `docs/gateflow/uf-fix08-existing-source-auto-repair-slice1-implementation-20260816.md`
- Included scope:
  - `dayu/fins/storage/source_integrity.py`（四态/reason/preflight/comparison 冻结）
  - `dayu/fins/storage/repository_protocols.py`（state required field、repair method shape、rejection helper）
  - `dayu/fins/storage/_fs_filing_upload_state_core.py`（guard 内读取与临时 classifier 接线）
  - `dayu/fins/storage/_fs_source_document_core.py`（仅 2 处机械替换，逐行核对）
  - `dayu/fins/storage/__init__.py`、`dayu/fins/upload_repair_contract.py`（新）
  - `tests/fins/test_fins_storage_atomicity.py`、`tests/fins/test_fins_ingestion_runtime.py`、`tests/service/test_fins_direct.py`、`tests/cli/test_fins_commands.py`
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-plan-20260816.md`（plan amendment 核对）
  - 消费面走读：`dayu/fins/ingestion_runtime.py`、`dayu/fins/service_runtime.py`、`dayu/fins/pipelines/sec_upload_workflow.py`、`dayu/fins/pipelines/cn_pipeline.py`、`dayu/fins/pipelines/sec_pipeline.py`、`fs_source_document_repository.py`（未修改，仅核对受影响链路）
- Excluded scope: 本 slice 未修改的其余生产代码与测试（plan 明确不改 Host/Engine/Service/CLI production、tool schema、registry/oracle/evidence）。
- Parallel review coverage: 无（单 reviewer 全程走读）。
- 验证执行：focused suite 634 passed（与 artifact 声称一致）；全仓 pyright 0 errors/0 warnings（与声称一致）；`git diff --check` 通过；3 条 warning 均为第三方 `edgar` 包 deprecated import，与本 slice 无关。

## Findings

### 1-[未修复]-中-blocking 判定:非阻塞-Protocol rejection stub 的 Slice 4 接管路径缺失，且 stub/helper 移除未纳入 plan

- **入口/函数**: `reset_source_document_for_repair` 经 `FsSourceDocumentRepository` facade 的 runtime 调用（Slice 4 实现后）
- **文件(行号)**: `dayu/fins/storage/repository_protocols.py:706-737`（stub body 调用 `_reject_unimplemented_source_repair()`）；`dayu/fins/storage/fs_source_document_repository.py:221`（`class FsSourceDocumentRepository(SourceDocumentRepositoryProtocol)`，facade 只继承 protocol，不继承 `_FsSourceDocumentMixin`）；plan §9 Slice 4 allowed files（`repository_protocols.py`、`_fs_source_document_core.py` 等，**不含** `fs_source_document_repository.py`）；`dayu/fins/pipelines/sec_pipeline.py:544`、`dayu/fins/pipelines/cn_pipeline.py:392`、`dayu/fins/service_runtime.py:390`（生产默认装配 facade 实例）
- **输入场景**: Slice 4 按 plan 在 `_FsSourceDocumentMixin`（`_fs_source_document_core.py`）实现 `reset_source_document_for_repair` 后，生产 `DoclingUploadService` 通过 `source_repository`（facade 实例）调用该方法
- **实际分支**: facade 的 MRO 为 `[FsSourceDocumentRepository, SourceDocumentRepositoryProtocol]`——mixin 不在其中（facade 通过 `self._repository_set` 组合 core，见 `fs_source_document_repository.py.__init__`）。protocol 的 stub body 被显式子类继承（PEP 544 "for free" 条款，本 review 已用 `test_source_repository_repair_contract_rejects_mutation_before_owner_implementation` 通过证实继承生效）
- **预期行为**: Slice 4 完成后，生产 repair 调用进入真实 staged repair mutation
- **实际行为**: 若 Slice 4 只在 `_fs_source_document_core.py` 的 mixin 实现方法，facade 仍继承 protocol stub body，生产调用继续抛 `NotImplementedError`，repair 功能静默失效；若 Slice 4 移除 stub，facade 则缺方法，调用变 `AttributeError`
- **直接证据**: `grep -n "reset_source_document_for_repair" dayu/` 确认当前仅 protocol 定义该方法，`_FsSourceDocumentMixin`/facade 均无定义；`FsSourceDocumentRepository(SourceDocumentRepositoryProtocol)` 是 facade 的唯一基类（fs_source_document_repository.py:221）；plan Slice 4 allowed files 列表（plan 683-693 行）不含 facade 文件；plan 全文无 "NotImplementedError/stub/helper 移除" 字样（`grep` 已查）；生产装配（sec_pipeline.py:544 等）默认使用 facade
- **影响**: Slice 4 落地即埋入“实现完成但生产路径仍被拒绝”的静默失效，或 AttributeError 崩溃；二者都需要在 Slice 4 之外追加边界裁决
- **建议改法和验证点**: 在 Slice 4 前对 plan 做 boundary amendment：(1) 将 `dayu/fins/storage/fs_source_document_repository.py` 加入 Slice 4 allowed files，并明确 facade 增加委托 override；(2) 明确 Slice 4 将 protocol body 收回为 `...`（或删除）并删除 `_reject_unimplemented_source_repair()`，同时更新本 slice 的 rejection 测试（`test_fins_storage_atomicity.py` 已在 Slice 4 allowed files，可承接）；(3) Slice 4 完成条件增加“facade 实例调用进入真实 mutation、不再抛 NotImplementedError”的 owner-level 断言
- **修复风险（低/中/高）**: 低（plan 层面 amend，不涉及当前代码）
- **严重程度（低/中/高/严重）**: 中（当前 slice 行为正确，但已批准 plan 的结构缺口将直接决定 Slice 4 正确性）

### 2-[未修复]-低-blocking 判定:非阻塞-implementation artifact 的"129 个 reportAbstractUsage 错误"论证无法在真实配置下复现，`_reject_unimplemented_source_repair()` 的 pyright 必要性证据不成立

- **入口/函数**: `_reject_unimplemented_source_repair()`（`repository_protocols.py:61-74`）的设计动机
- **文件(行号)**: `dayu/fins/storage/repository_protocols.py:61-74`；implementation artifact "Validation" 段（"修复为 repository-protocol owner 内的模块级 typed rejection helper后…0 errors"）
- **输入场景**: reviewer 按 artifact 声称的中间态（protocol method body 直接 `raise NotImplementedError`）在隔离目录重建最小复现，用与仓库相同的 pyright 配置与命令运行
- **实际分支**: 4 组独立 probe（`workspace/tmp/review-seam-probe/probe*.py`：字面 raise、`...` body、`@abstractmethod`+raise、protocol-typed 参数一致性调用，均含 `class ConcreteX(P): pass` 显式子类并实例化）在 `python -m pyright` 下全部 `0 errors`
- **预期行为**: 若 artifact 论证成立，字面 raise 变体应触发 `reportAbstractUsage`（子类被视为 abstract 不可实例化）
- **实际行为**: 仓库 `pyrightconfig.json` 无任何 rule override（无 `reportAbstractUsage: error`），默认 basic 模式下四种变体均为 0 errors；pyright 把 protocol 带 body 的方法视为被子类"免费继承"的可用实现，不判 abstract
- **直接证据**: `pyrightconfig.json`（仅 pythonVersion/include/exclude）；probe 输出 4× `0 errors, 0 warnings, 0 informations`；最终态全仓 pyright `0 errors` 与 focused suite 634 passed 均复现成功，唯独中间态论证无法复现
- **影响**: 用户明确质疑"是否为骗过 pyright 的胶水 seam"。证据结论：该 helper 不是欺骗 pyright 的 seam——在本仓库真实配置下 pyright 并不需要被欺骗；helper 的真实、可复现价值只有一个：runtime 上防止显式子类继承 `...` body 后静默返回 `None`（"静默返回成功"风险，artifact 亦提及）。若对照物是字面 `raise NotImplementedError`，helper 与字面 raise 的 runtime 语义完全相同且 pyright 结果相同，helper 是冗余封装。artifact 的 129 错误叙事与其真实配置下的行为不符，属证据失真
- **建议改法和验证点**: 二选一：(a) 保留 helper（显式拒绝意图、有 NoReturn 类型收益），但 artifact 必须修正论证——删除"129 reportAbstractUsage 错误"叙事，改为"防显式子类继承空 body 静默成功"；或 (b) 由 author 提供中间态复现证据（含当时完整 diff 与 pyright 输出），交由 controller 裁决。同时无论何种裁决，helper/stub 的 Slice 4 移除必须写入 plan（见 Finding 1）
- **修复风险（低/中/高）**: 低（文档/裁决层面）
- **严重程度（低/中/高/严重）**: 低（代码行为正确、有测试覆盖；问题在 artifact 论证真实性）

### 3-[未修复]-中-blocking 判定:非阻塞-`read_filing_upload_state` 对损坏既有 source 的行为变化（meta/descriptor 缺失从"静默视为 absent"变为抛异常）未在 contract docstring 与 owner 测试中闭合，SEC/CN workflow fresh re-read 无异常处理

- **入口/函数**: `_FsFilingUploadStateMixin.read_filing_upload_state()`；下游 `service_runtime.py:86`、`ingestion_runtime.py:4436`、`sec_upload_workflow.py:161`、`cn_pipeline.py:790`
- **文件(行号)**: `dayu/fins/storage/_fs_filing_upload_state_core.py:121-200`；`_fs_source_document_core.py:568-664`（当前 classifier）
- **输入场景**: ticker published 目录存在，但 (a) 目标 source 目录存在而 `meta.json` 缺失；或 (b) source 目录存在而 identity descriptor 缺失/损坏
- **实际分支**: (a) classifier `_classify_source_integrity_unguarded` 在 `document_dir.exists()` 为真、`meta_path.exists()` 为假时 `raise ValueError("existing source directory 缺少 meta.json")`（`_fs_source_document_core.py:604`）；(b) `_source_meta_path_for_read` → `_identity_directory_for_read` → descriptor 校验失败 → path-free `FileNotFoundError`（`_fs_identity.py:286-323`，`_new_path_free_filesystem_error` 保留 subclass）
- **预期行为**: 按新 owner contract，损坏状态应 fail closed 并最终收敛为 typed classification/failure（Slice 2/3 落地）；slice 内至少应被文档明确承诺、被 owner 测试断言
- **实际行为**: 两种异常均原样冒出 `read_filing_upload_state`。基线行为对照：旧代码 `try: source_meta = dict(core._get_source_meta_unguarded(...)) except FileNotFoundError: source_meta = None` 将二者都收敛为 `source_meta=None`（静默视为 absent）。方向性结论：新行为（fail closed）优于旧行为（损坏被当作 absent，可诱发 CREATE 路径对残留目录误操作），与本 work unit 目标一致；但 (1) protocol（`repository_protocols.py:207-228`）与 mixin（`_fs_filing_upload_state_core.py:135-140`）的 Raises docstring 仍只写 "ValueError: ticker 或 document identity 非法时抛出"，未承诺 structural-meta/descriptor 异常面；(2) `sec_upload_workflow.py:161` 与 `cn_pipeline.py:790` 的 fresh re-read 无任何 except，若损坏发生在 prevalidation 之后（竞态窗口）或经 job replay/retry 绕过 prevalidation 进入 workflow，raw ValueError/FileNotFoundError 将直接崩出 pipeline（service 层 `service_runtime.py:86` 已有 `except ValueError → FinsUploadPrevalidationError` 的映射，正常 CLI/service 主链路受保护）；(3) owner 测试仅覆盖 fresh-absent（`test_fins_storage_atomicity.py:159`）与单版本 happy path（`:224`），两个新失败分支无断言
- **直接证据**: 上述行号逐行走读；`grep -n "read_filing_upload_state" dayu/` 消费点清单；`tests/fins/test_fins_storage_atomicity.py` 仅两处调用
- **影响**: 主链路无用户可见回归（service 层已映射 typed failure），但 docstring 承诺与实际异常面漂移、pipeline 竞态/replay 路径存在未处理崩溃，owner 测试未闭合新分支
- **建议改法和验证点**: (1) 更新 protocol 与 mixin 的 Raises：明确 "ValueError: published source 结构或 meta 非法（含 meta 缺失）"、"OSError: identity descriptor 或 meta 读取失败"；service_runtime 的映射依赖该承诺，必须写进契约而非靠现状巧合；(2) Slice 3 已含 service prevalidation/path-free failure 测试，应在其 allowed files 内补 "meta.json 缺失 → typed corruption failure" 与 "descriptor 缺失 → typed io failure" 两条真实 filesystem 测试；(3) SEC/CN workflow 的 fresh re-read 异常处理属 Slice 5 scope（"fresh validator 的 FinsUploadPrevalidationError 收敛为 typed failed event"），在 plan 中确认该竞态/replay 路径被 Slice 5 覆盖，或显式声明为 residual
- **修复风险（低/中/高）**: 低（文档+测试；生产逻辑方向正确无需回退）
- **严重程度（低/中/高/严重）**: 中（契约文档漂移 + 未闭合失败路径）

### 4-[未修复]-低-blocking 判定:非阻塞-`SourceIntegrityClassification.__post_init__` 末分支未显式 gate `status is UNSAFE`，未来新增状态会静默继承 UNSAFE 不变量

- **入口/函数**: `SourceIntegrityClassification.__post_init__`
- **文件(行号)**: `dayu/fins/storage/source_integrity.py:118-135`
- **输入场景**: 未来 work unit 为 `SourceIntegrityStatus` 增加第五个成员（plan §8.1 upload_failure 已出现 "unsafe/stale" 语义，stale 状态化是现实演进方向），或传入非四态成员
- **实际分支**: `MISSING`/`COMPLETE`/`REPAIR_REQUIRED` 三个显式分支各自 `return` 后，第 132-135 行两个 `if` 直接以 UNSAFE 规则收尾，没有 `status is UNSAFE` 前置 gate（构造器前段的 `isinstance` 检查只保证是枚举成员）
- **预期行为**: 非四态状态应被显式拒绝（fail closed），不能默认套用任一状态的规则
- **实际行为**: 任何未来新增状态会被静默当作 UNSAFE 校验（`revision=None` + 仅 unsafe reasons），产生错误分类而非显式失败——四态封闭性只对今天的枚举成立
- **直接证据**: source_integrity.py:126-135 分支结构；plan §8.1 "unsafe/stale closed storage failures" 表明状态扩展已在路线图上
- **影响**: 未来状态扩展时静默误分类，违背 closed-enum 设计初衷
- **建议改法和验证点**: 末分支前加 `if self.status is not SourceIntegrityStatus.UNSAFE: raise ValueError("status 必须是封闭四态")`；测试补一条未来状态成员被拒的断言（或至少在 Slice 2/涉及 stale 的 work unit 落地时强制）
- **修复风险（低/中/高）**: 低（纯收紧，不影响现有四态合法构造）
- **严重程度（低/中/高/严重）**: 低

### 5-[未修复]-低-blocking 判定:非阻塞-tests/service 新增对 `dayu.fins.pipelines.docling_upload_service.build_sec_filing_ids` 的依赖，fixture 语义当前为惰性值，且引入 pipeline→docling 导入链

- **入口/函数**: `tests/service/test_fins_direct.py::test_upload_methods_build_union_requests`（新增 import，diff `+from dayu.fins.pipelines.docling_upload_service import build_sec_filing_ids`）
- **文件(行号)**: `tests/service/test_fins_direct.py:48,741-747`；对照 `tests/cli/test_fins_commands.py:62`（该 import 在 base 已存在，非本 slice 新增）
- **输入场景**: 构造 `FilingUploadPublishedState.source_integrity` fixture 时的 document_id 来源
- **实际分支**: fixture 用 `build_sec_filing_ids(ticker=..., fiscal_year=2024, fiscal_period="FY", amended=True)` 推导 `filing_document_id`，仅用于 classification 的 `document_id` 字段；`_internal_document_id` 被丢弃
- **预期行为**: 依赖 owner 应为"request → document_id 推导"的语义真源。全仓证据：`validate_fins_upload_filing_request` 与 `service_runtime` 在本 slice **完全不读** `source_integrity`（`grep -c "source_integrity" dayu/fins/ingestion_runtime.py` = 0），该 fixture 字段当前对被测行为零影响；推导真源是 `ingestion_runtime._filing_upload_request_identity`（`ingestion_runtime.py:1206`，内部才调用 `build_sec_filing_ids`），同文件 test helper `_filing_upload_published_state` 已采用该真源（`tests/fins/test_fins_ingestion_runtime.py:193`）
- **实际行为**: service 测试新增了对 docling pipeline 模块的依赖——该模块顶层 import `docling_process_converter`（docling 重依赖链），使 service 层测试套件 transitively 加载 docling；且所用值在当前 slice 是惰性值（validator 不消费）
- **直接证据**: `grep -n "source_integrity" dayu/fins/ingestion_runtime.py dayu/fins/service_runtime.py` 零命中；`tests/fins/test_fins_ingestion_runtime.py:193` 的 helper 用 `_filing_upload_request_identity`；`docling_upload_service.py:36` import converter 链
- **影响**: 测试层与 pipeline 层的多余耦合（重依赖导入、未来 docling 版本/导入问题波及 service 测试）；语义上绕过推导真源。不构成行为错误
- **建议改法和验证点**: 两种可接受方案：(a) 与 ingestion runtime 测试同源，改用 `ingestion_runtime._filing_upload_request_identity(request)` 取 identity（真正 owner）；(b) 保留 `build_sec_filing_ids`（public `__all__` 函数，Slice 3 validator 开始消费 `source_integrity` 后 fixture 需要精确匹配，提前对齐有合理性），但需在测试内注释说明该依赖的 owner 理由。controller 二选一即可，均无需改生产代码
- **修复风险（低/中/高）**: 低（fixture 层面）
- **严重程度（低/中/高/严重）**: 低

## 已核实的正向结论（无 finding 项）

1. **四态/reason 封闭性**：`MISSING`（无 revision/reasons）、`COMPLETE`（required revision、无 reasons）、`REPAIR_REQUIRED`（required revision、非空且仅 repairable）、`UNSAFE`（无 revision、非空且仅 unsafe）互斥不变量完整，enum 顺序去重校验存在且先于 membership 校验（先 isinstance 后 frozenset 检查，堵住 str-Enum 与裸字符串 hash 相等的旁路）。18 个 reason 恰好 9 repairable + 9 unsafe，测试逐值断言 enum 顺序。
2. **旧 member 清理**：全仓 `PHYSICAL_FILE_MISSING` 零残留（dayu/ tests/ utils/ grep 为空）；无旧名 alias、无兼容 re-export。
3. **UNSAFE fail closed**：`has_same_source_publication_identity` 对任一 UNSAFE 输入先抛 ValueError（含 UNSAFE vs UNSAFE），MISSING 的 `revision=None` 不会与 UNSAFE 在 identity 比较上相等；preflight 在 inventory 出现 UNSAFE 时优先抛 `UNSAFE_PUBLICATION`（先于 MULTIPLE_REPAIR_REQUIRED）。测试覆盖 unsafe-first/second/both 三向。
4. **state/meta 同源**：`FilingUploadPublishedState`（frozen+slots）required `source_integrity` 无 default、无 dual-read、不从 `source_meta` 反推；`__post_init__` 强制 filing kind 与 status/meta presence 对应。全仓 `FilingUploadPublishedState(...)` 构造点仅 owner（`_fs_filing_upload_state_core.py` 两处）与 4 个 allowed 测试文件，production 无任何 fallback/default。
5. **guard 内读取**：classifier、`_source_meta_path_for_read`、company meta、source meta 全部在单一 `_acquire_publication_guard` 段内；private protocol 新增的两个 `_unguarded` 声明与真实方法签名一致（`_fs_source_document_core.py:568`、`_fs_storage_infra.py:3318`）；fresh-absent 路径保持 lock-free、零目录副作用（测试用 monkeypatch 守卫函数断言）。
6. **scope 合规**：修改文件与 plan Slice 1 allowed files 一一对应；`_fs_source_document_core.py` 的 diff 严格只有 Controller 批准的两处机械替换；plan 内 amendment 记录与实际 diff 一致；未越界实现 Slice 2 inspector、validator eligibility、workflow、download、README。
7. **测试/pyright 证据**：focused suite 634 passed（与 artifact 数字一致，3 条 warning 均为第三方 edgar 包）；全仓 pyright `0 errors, 0 warnings, 0 informations`；`git diff --check` 通过。
8. **README 决策**：与 plan §12 及 Slice 6（plan 743-749 行）一致——`dayu/fins/README.md` 与 `tests/README.md` 的更新明确归属 Slice 6，本 slice 不更新属 plan 内既定安排，非遗漏。
9. **repair union**：`ExistingSourceAutoRepair` 只接受 `REPAIR_REQUIRED` filing，测试覆盖 complete/material/unsafe 三组拒绝；storage 不 re-export repair disposition（独立模块导出），符合禁止兼容 re-export 约束。

## Open Questions

1. **Slice 4 facade 接管**（Finding 1）：plan 的 Slice 4 allowed files 是刻意遗漏 `fs_source_document_repository.py`（有意把 delegation 放在其它允许文件？）还是 planning gap？当前 plan 文本无法给出答案，需 controller 向 plan owner 确认。
2. **129 错误复现**（Finding 2）：artifact 声称的中间态（"protocol method 直接 raise NotImplementedError → 129 个 reportAbstractUsage 错误"）在真实 pyrightconfig 下无法复现；中间态是否还包含 artifact 未描述的其它改动（例如同时引入 `@abstractmethod`、修改其它类或换过配置）？需 implementation author 提供中间态完整 diff 与 pyright 输出。
3. **SEC/CN fresh re-read 竞态窗口**（Finding 3）：prevalidation 之后、workflow fresh re-read 之前的损坏窗口与 job replay 路径，是否被 Slice 5 的 "typed failed event" 覆盖？plan 文本未显式点名该窗口。

## Residual Risk

- `read_filing_upload_state` 的 meta/descriptor 损坏失败分支暂无 owner-level 测试（Finding 3）；Slice 3/Slice 5 落地前，pipeline replay 路径存在 raw 异常崩出风险。
- 当前 classifier 尚无 `UNSAFE` producer（`source_meta=None` 的 UNSAFE 分支是 Slice 2 前的死代码），`read_filing_upload_state` 的 UNSAFE 投影未经过真实生产路径验证；已由 artifact 自身列为 "covered by later approved slice"（Slice 2）。
- `_reject_unimplemented_source_repair()` 的移除与 facade 委托未进入 plan（Finding 1），若不 amend，Slice 4 完成后 repair 功能在生产装配下静默失效。
- 本 review 未运行 UF-PF08/UF-PF12、真实 CLI/provider/converter evidence（与 artifact 声明一致，不在本 slice 范围）。
- 未对 `_fs_storage_infra.py` 与 `_fs_identity.py` 全文做逐行走读（仅沿 `read_filing_upload_state` 调用链走读至异常源）；其非本链路部分不在本 review 结论覆盖内。

## 结论

- Blocking findings：**无**。本 slice 的 owner contract（四态/reason 封闭、UNSAFE fail closed、state/meta 同源、guard 内读取、rejection 语义）实现正确、测试覆盖到位、pyright/测试证据可复现，scope 与 README 决策符合已批准 plan。
- Non-blocking findings：5 项（1 中、1 中、1 低、1 低、1 低），其中两项为 plan 层面必须闭合的 forward-risk：
  - Finding 1（中）：Slice 4 必须 amend plan——facade 文件加入 allowed files、stub/helper 显式移除、完成条件加 facade 路径断言；否则 Slice 4 完成后生产 repair 仍被 NotImplementedError 拒绝。
  - Finding 2（低）：`_reject_unimplemented_source_repair()` 不是"骗过 pyright"的 seam（真实配置下 pyright 无需被骗），但 artifact 的 129 错误论证无法复现，需 author 提供证据或修正叙事；helper 本身可保留（防显式子类继承空 body 静默成功），或直接退回字面 raise。
  - Finding 3（中）：`read_filing_upload_state` 对损坏 source 的异常面需写入协议 docstring 并补 owner 测试；SEC/CN fresh re-read 的竞态/replay 路径需确认被 Slice 5 覆盖。
  - Finding 4（低）：classification 末分支加显式 UNSAFE gate，防未来状态静默误分类。
  - Finding 5（低）：tests/service 的 `build_sec_filing_ids` 依赖可选保留，但建议改走 `_filing_upload_request_identity` 真源或注释说明 owner 理由。

综上：**本 slice 可进入下一 gate（Slice 2）**，前提是 controller 对 Finding 1、2 作出裁决并跟踪到对应 slice 的 plan amendment；Finding 3、4 可在 Slice 2/3 的 allowed files 内低成本闭合。
