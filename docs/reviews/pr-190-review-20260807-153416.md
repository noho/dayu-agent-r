# Code Review — PR 190 F17 Re-Review Gate

## Scope

- Mode: PR re-review gate（adjudication closure verification）
- PR: 190 — "fix(cli): close interactive conformance gaps"
- Author: noho
- Head: codex/interactive-oracle (dcc08399)
- Base: main
- URL: https://github.com/noho/dayu-agent-r/pull/190
- Output file: docs/reviews/pr-190-review-20260807-153416.md
- 上游输入：
  - Adjudication: `docs/gateflow/pr-190-f17-pr-review-adjudication-20260807.md`
  - AgentDS review: `docs/reviews/pr-190-review-20260807-152555.md`
- Included scope: 仅验证 adjudication 中每条裁决的闭合状态；不重新走读 745-file PR diff
- Excluded scope: F01–F16 实现细节（已在往期 review 中覆盖）；broader PR 子系统（compaction、interactive CLI、engine runner）本次不重新走读

## 裁决闭合逐项验证

### D3 — branch/baseline/one-entry+pin correction

- **裁决**: accepted（evidence correction）
- **闭合状态**: ✅ 已闭合
- **直接证据**:
  - `git diff e1217811..305c1012` 对两个产品文件各为 1 insertion / 1 deletion：
    - `docs/cli_init_workspace_manifest_v1.json`：仅 `conversation_compaction_user.md` entry 的 `content_sha256` 从 `59b50e13...` 更新为 `22e7bc50...`
    - `tests/cli/test_smoke_cli_init_provider_matrix.py`：`FROZEN_MANIFEST_SHA256` 从 `d95de68e...` 更新为 `064f8066...`
  - 其余 12 个变更文件均为 `docs/gateflow/` 和 `docs/reviews/` 下的 F17 流程产物，非产品代码
  - 当前 manifest raw SHA-256: `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22` ✅
  - 当前 `FROZEN_MANIFEST_SHA256` test pin: `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22` ✅（精确匹配）
  - AgentDS review Finding 3 已完整记录 F17 commit boundary（base=`e1217811`, target=`305c1012`）与范围结论

### D1 — runtime/Engine 层隔离及既有完整值域 invariant test

- **裁决**: rejected-with-reason（invariant test 已存在）
- **闭合状态**: ✅ 已闭合
- **直接证据**:
  - `tests/service/test_host_assembly.py:204`：`test_structured_output_capability_enums_map_mechanically_by_value`
  - 测试断言：`runtime_values == engine_values`（两侧完整 value 集合相等）
  - 测试断言：每个 runtime capability value 可机械构造 Engine capability（`StructuredOutputCapability(runtime_capability.value)`）
  - 运行结果：**1 passed** ✅
  - 架构理由成立：`dayu.runtime` 按架构硬约束不得 import `dayu.engine`；Service 层机械投影是正确的层隔离边界

### D2 — stream + structured_output 互斥 contract

- **裁决**: rejected-with-reason（无现行互斥 contract，无触发证据）
- **闭合状态**: ✅ 已闭合
- **直接证据**:
  - `dayu/engine/contracts/agent_run.py:143-146`：`validate_structured_output_request` 仅校验 structured_output 自身参数，不检查 stream
  - `dayu/engine/runners/openai/payload.py:429`：stream 无条件设置
  - `dayu/engine/runners/openai/payload.py:443`：structured_output 在 stream 之后应用，无互斥 guard
  - 当前触发路径：compactor 使用非流式，interactive 不传 structured_output — **无触发路径**
  - Engine design 合法矩阵仅由 request variant 与 Runner capability 决定，不包含 stream 互斥 contract；provider 层自有拒绝机制

### D4 — README 明确不迁移旧 slot

- **裁决**: rejected-with-reason（README 已明确公开）
- **闭合状态**: ✅ 已闭合
- **直接证据**:
  - `README.md:225-228`：明确声明共享 namespace 为 `cli.agent.<label>`，旧 `cli.prompt.*` / `cli.interactive.*` slot **不会被自动读取或迁移**
  - tests 将旧 scope 明确分类为 `OTHER`
  - 属于 accepted fresh contract，非遗漏；新增 fallback/migration 会违反仓库禁止兼容层的约束

### MiMo M2 — CONTEXT_COMPACTION_FAILED 未来 bounded-storage fallback

- **裁决**: rejected-with-reason（未来假设）
- **闭合状态**: ✅ 已闭合
- **验证**: review 明确承认当前 failed payload 是固定小型封闭结构且当前行为正确；finding 仅假设未来 schema 可能扩张。未来 schema 变更应由其 owner 同步设计 storage。属于 future assumption，不构成当前 defect。

### MiMo M3 — compact_structure.py 没有同名专属测试文件

- **裁决**: rejected-with-reason（文件组织偏好）
- **闭合状态**: ✅ 已闭合
- **验证**: 5 个 public functions 由 `test_compaction_contract.py` 与 `test_llm_compaction.py` 覆盖；仓库要求 owner contract coverage，不要求生产模块与测试文件同名。属于文件组织偏好，不构成 correctness/stability/maintainability defect。

### MiMo M1 — PR 无远端 CI checks

- **裁决**: deferred-with-owner（CI coverage gap）
- **闭合状态**: ✅ 仍为 open CI gap（预期内，不在 F17 关闭）
- **直接证据**: `gh pr checks 190` → `no checks reported on the 'codex/interactive-oracle' branch`
- 本地验证已通过：71/71 owner suite、full pyright、Ruff、compileall、JSON/digest/count 与 committed-range diff check
- Owner: repository CI / merge policy 后续裁决；F17 不擅自新增 workflow

## 远端 PR 状态确认

| 检查项 | 结果 |
|---|---|
| State | OPEN ✅ |
| Draft | true ✅ |
| Head SHA | dcc08399 ✅（`git rev-parse codex/interactive-oracle` 一致） |
| Reviews | []（无 review write）✅ |
| Review requests | [] ✅ |
| Checks | no checks reported ✅（与 M1 CI gap 一致） |

无 GitHub review write、无 comment、无 approve、无 ready-for-review transition、无 merge。

## Findings

### 1-未修复-低-CI gap：PR 分支无远端 status check 配置

- **入口/函数**: GitHub branch protection / CI workflow
- **文件(行号)**: N/A（GitHub repository settings）
- **输入场景**: PR 190 有 745 files changed，合并前应通过自动化 CI 验证
- **实际分支**: `gh pr checks 190` 返回 no checks
- **预期行为**: 至少配置 pyright + 关键测试套件的 status check
- **实际行为**: 无任何 CI 运行
- **直接证据**: `gh pr checks 190` → exit 1, "no checks reported on the 'codex/interactive-oracle' branch"
- **影响**: 745 文件变更无自动化验证门禁；本地 71/71 owner suite 通过但不能伪装成 GitHub CI PASS
- **建议改法和验证点**: 在合并前配置至少 pyright + pytest 的 required status check；不在 F17 work unit 内处理
- **修复风险（低）**: 纯 CI 配置，不影响产品代码
- **严重程度（低）**: 已知 gap，已在 adjudication 中 deferred-with-owner

**其余无新增 product/code finding。** 前次 DS review 的 D1/D2/D4 已被 adjudication 正确拒绝；D3 evidence correction 已闭合；MiMo M2/M3 已被正确拒绝。

## Open Questions

1. 三条 formal replacement scenarios 仍为 `unadjudicated`：`interactive.interactive.g06.tool-trace-formal@2`、`interactive.interactive.g06.rolling-correction-replacement@1`、`interactive.interactive.g06.cap-constrained-memory-replacement@1`。其未裁决状态影响 readiness proof，但不影响 F17 publication truth。Owner: Oracle controller。
2. CI gap 何时关闭：取决于 repository CI / merge policy owner 决策，不在 F17 scope 内。

## Residual Risk

- **CI gap**：分支上无 checks 配置，745 文件变更无自动化验证。建议在合并前至少配置 pyright + 关键测试套件的 status check。本地 71/71 owner suite 通过，但不覆盖 broader PR 190 的其他子系统（compaction、interactive CLI、engine runner）。
- **3 条 replacement scenarios 仍为 `unadjudicated`**：F17 contract 明确不处理，由后续 Oracle controller work unit 处理。
- **stream + structured_output 互斥**：latent risk，当前无触发路径。Provider 层自有拒绝机制。
- **71-test owner suite 仅覆盖 frozen publication contract**：不覆盖 broader PR 190 子系统。
- **745-file PR 规模**：本次 re-review 仅验证 adjudication closure，未重新走读全部 PR diff。F01–F16 实现已在往期 review 中覆盖，但跨版本回归风险仍存在。
