# Code Review — WU-CM-12 S1 DS-F1 Fix Re-Review

## Scope

- Mode: current changes (focused re-review after fix)
- Branch: wu-cm-12-conversation-memory-drift
- Base: HEAD (workspace changes including fix)
- Output file: docs/reviews/code-review-20260618-143944.md
- Included scope: DS-F1 fix verification only — `dayu/host/memory.py::_facts_from_accepted_event` empty-label early return, `tests/host/test_memory_projection.py::test_accepted_compact_preserves_budget_diagnostic_before_invalid_fact`, deferred DS-F2 semantics check, regression surface
- Excluded scope: compact_material.py / run_input.py S1 changes (not part of DS-F1 fix), S2–S5
- Source artifacts:
  - Original finding: `docs/reviews/code-review-20260618-142551.md` finding 1 (DS-F1)
  - Adjudication: `docs/reviews/code-review-wu-cm-12-s1-adjudication-20260618-143543.md`
  - Fix artifact: `docs/reviews/wu-cm-12-s1-fix-codex-20260618.md`

## Verification Matrix

| Check | Result | Evidence |
|---|---|---|
| DS-F1: empty-label early return preserves accumulated diagnostics | PASS | `memory.py:1827` `tuple(diagnostics) + (...)` |
| DS-F1: test covers mixed oversized + empty-label path | PASS | `test_memory_projection.py:743-773` |
| DS-F2: facts clearing semantics unchanged | PASS | `memory.py:1825-1826` returns `()` not `tuple(facts)` |
| No new regressions (pytest) | PASS | 118 passed in 0.80s |
| No new regressions (pyright) | PASS | 0 errors, 0 warnings, 0 informations |
| No new regressions (git diff --check) | PASS | clean, no output |
| `_bounded_text` removal safe (no remaining callers) | PASS | grep confirms no callers in host layer |

## Findings

### DS-F1 — 已验证修复

- **入口/函数**: `dayu/host/memory.py:_facts_from_accepted_event`
- **文件(行号)**: `dayu/host/memory.py:1807-1851`
- **修复内容**: 第 1824-1835 行的 empty-label early return 从 `return ((), (_fact_candidate_invalid_diagnostic(...),))` 改为 `return ((), tuple(diagnostics) + (_fact_candidate_invalid_diagnostic(...),))`。`tuple(diagnostics)` 保留了第 1808 行初始化的 `diagnostics` list 中通过第 1814 行 `diagnostics.append(...)` 累积的 oversized-fact `BUDGET_LIMIT_REACHED` 诊断。
- **直接证据**:
  - 第 1808 行: `diagnostics: list[MemoryDiagnostic] = []`
  - 第 1813-1822 行: oversized fact 通过 `diagnostics.append(_budget_diagnostic(...))` 累积诊断，然后 `continue`
  - 第 1827 行: `tuple(diagnostics)` 将已累积的诊断（可能包含 `BUDGET_LIMIT_REACHED`）注入返回 tuple
  - 第 1827-1834 行: `+ (_fact_candidate_invalid_diagnostic(...),)` 追加 invalid 诊断
  - 第 1261-1262 行 caller 正确累积: `diagnostics = diagnostics + fact_diagnostics`
- **修复质量**: 修复精准，只改返回 tuple 组装方式，不改变分支逻辑。正常路径（第 1851 行 `return tuple(facts), tuple(diagnostics)`）未被触碰。

### DS-F1 回归测试 — 已验证覆盖

- **测试**: `tests/host/test_memory_projection.py:743-773` `test_accepted_compact_preserves_budget_diagnostic_before_invalid_fact`
- **覆盖场景**: 构造 `evidence_fact_char_cap=8` 的 policy，第一个 fact 的 claim_text 为 12 字符（触发 oversized → `continue` + `BUDGET_LIMIT_REACHED`），第二个 fact 设置 `evidence_labels=[]`（触发 empty-label early return → `EVIDENCE_BACKED_FACT_CANDIDATE_INVALID`）
- **断言质量**:
  - 第 769 行: 断言 `evidence_backed_facts == ()` — 确认 DS-F2 行为未改变（facts 仍为空）
  - 第 770-773 行: 断言 diagnostics 顺序为 `(BUDGET_LIMIT_REACHED, EVIDENCE_BACKED_FACT_CANDIDATE_INVALID)` — 确认两个诊断均被保留，且 oversized fact 诊断在前（与代码执行顺序一致）
- **覆盖充分性**: 覆盖了 regression 路径（先 oversized 后 invalid）。未覆盖"先 valid 后 invalid"路径，但该路径属于 DS-F2 语义问题，不在 DS-F1 fix 范围内。

### DS-F2 — 已验证未改变

- **入口/函数**: `dayu/host/memory.py:_facts_from_accepted_event`
- **文件(行号)**: `dayu/host/memory.py:1824-1835`
- **验证**: early return 第 1825-1826 行返回 `()` 而非 `tuple(facts)`。如果 compact event 中第一个 fact 合法（通过 char_cap 和 labels 校验，已被 `facts.append`），第二个 fact 的 evidence_labels 为空，合法 fact 仍被丢弃。此行为与 fix 前完全一致。
- **结论**: 符合 DS-F2 deferred-with-owner 裁决，语义保留至 S3 provenance guard / compact fact rendering review。

### 无新回归

- **pytest**: 118 passed，包含所有 affected 测试文件（`test_compact_material.py`、`test_memory_projection.py`、`test_run_input_builder.py`）
- **pyright**: 0 errors, 0 warnings, 0 informations
- **git diff --check**: clean
- **`_bounded_text` 移除**: 函数 `_bounded_text(text: str, char_cap: int) -> str` 原本仅在 `_session_summary_from_accepted_event` 和 `_facts_from_accepted_event` 中被调用。两个 caller 均已改为 whole-item drop + diagnostic 模式，函数已移除，无其他 caller。
- **`_session_summary_from_accepted_event` 签名变更**: 从返回 `SessionSummaryMemoryView` 改为返回 `tuple[SessionSummaryMemoryView, tuple[MemoryDiagnostic, ...]]`。唯一 caller `project_conversation_memory_event`（第 1256-1260 行）已正确解包并累积 `summary_diagnostics`。
- **`_empty_session_summary_memory` 提取**: 从原 `_session_summary_from_accepted_event` 的 early return 内联构造提取为独立 helper，减少重复。

## Open Questions

无。

## Residual Risk

- **DS-F2 路径缺少测试覆盖**: 合法 fact 后跟 invalid fact 的场景（valid → empty-label）未被测试。这是因为裁决明确将 DS-F2 推迟到 S3，当前测试只需覆盖 oversized → empty-label regression 路径。S3 应补充该测试。
- **Summary budget diagnostic 与 fact budget diagnostic 在同一 compact event 中的并发场景未单独测试**: 当前 S1 测试分别覆盖 oversized summary 和 oversized fact，但不覆盖同一 compact event 中两者同时超出 cap 的场景。`project_conversation_memory_event` 中两者 diagnostic 的累积顺序正确（summary diagnostics 先于 fact diagnostics 加入），但缺少专门的集成级 fixture。S4/S5 smoke 可补偿。

## Verdict

**PASS** — DS-F1 已修复，回归测试覆盖真实 mixed 路径，DS-F2 语义未改变，无新回归。
