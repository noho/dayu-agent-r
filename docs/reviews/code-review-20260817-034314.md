# Final Deep Review — UF-FIX10 same-request-concurrency

## Scope

- Mode: current changes（最终只读深审）
- Branch: `codex/upload-filing-oracle`
- Base: `656b926c`（= `46972e72` 的直接父提交；`656b926c..HEAD` 恰等于 UF-FIX10 五个提交）
- Reviewed commits: `46972e72`（accept plan）→ `5a2f821b`（S1 scope amendment）→ `d97d233b`（S1 second amendment）→ `7e094182`（S1 accepted slice）→ `047691c8`（S2 accepted slice + final fix）
- Review date/time: 2026-08-17 03:43:14（本机时钟）
- Output file: `docs/reviews/code-review-20260817-034314.md`
- Included scope: 上述 5 个提交的全部 diff（60 个文件，+10410/-218），其中生产 12 个文件（`dayu/fins/storage/repository_protocols.py`、`_fs_company_meta_core.py`、`_fs_filing_upload_state_core.py`、`_fs_source_integrity.py`、`fs_filing_upload_state_repository.py`、`storage/__init__.py`、`dayu/fins/pipelines/docling_upload_service.py`、`filing_upload_publication.py`（新增 771 行）、`sec_upload_workflow.py`、`cn_pipeline.py`、`dayu/fins/upload_failure.py`、`dayu/fins/README.md`），测试 13 个文件，`tests/README.md` 与 28 个 gateflow/review artifacts。
- Excluded scope: 更早的 UF-FIX01..08 work unit（base 之前）；oracle/scenario/registry/frozen evidence（未修改，只确认零 diff）；UF-PF10/UF-PF12（未运行）。
- 参考输入：`AGENTS.md`、`docs/host/design.md`（分层 35-43、filelock 层中立 59-72）、`docs/engine/design.md`（tool batch 归属 376-386、typed outcome 397-435、cancellation commit boundary 470-489）、accepted plan 及其两次 amendment、S1/S2 implementation/adjudication/fix/acceptance artifacts、8 份 UF-FIX10 code review 与 12 份 plan review。
- Parallel review coverage: 主 reviewer 逐行通读全部生产 diff 与 README diff，并沿真实调用链复核 `prepare_upload` → `execute_prepared_filing_publication`（begin → checkpoint1 → fresh read → 同 validator → arbitration → checkpoint2 → stage/rollback/commit → finally 单次 rollback）、workflow terminal 投影、batch fresh read 与 durable identity 投影；3 个并行只读子代理分别深挖（A）SEC 并发/取消/spawn/union 测试真实性，（B）publication owner/runtime/CN-HK 测试真实性，（C）Docling/storage owner 测试与 fixture 变更纯度。主 reviewer 已直接复核全部 finding 与关键验证点的代码行证据。

## 结论

**PASS。** 未发现生产代码 correctness、stability 或 maintainability 的实质缺陷；三条 workflow 的 shared publication route、仲裁表、双取消 checkpoint、rollback-first、typed path-free failure、语义 ownership 与分层边界均与 accepted plan 及设计文档一致。存在 3 个低严重度测试证据缺口（accepted plan §10.2 冻结的部分端到端断言未完整落地），不影响生产语义正确性，建议由后续 work unit 或补测闭环。

## Findings

### 1-未修复-低-plan §10.2 test 2 的多文件并发 loser skip 事件与 winner snapshot 一致性未端到端断言

- **入口/函数**: `test_concurrent_identical_auto_upload_has_one_publish_and_one_canonical_skip`（`with_companion=True` 参数化分支）
- **文件(行号)**: `tests/fins/test_sec_pipeline_upload_filing_stream.py:2192-2260`
- **输入场景**: 两个同 ticker、同 document、primary+companion 多文件的 raw auto 请求并发（MISSING 下完成 prepare 后竞争 writer）。
- **实际分支**: 测试断言 `["ok","skipped"]`、counts `[0, 2]`、`begin_tokens == 2`、`commit_tokens == 1`、`rollback_tokens == 1`、company/source stage 仅落在 commit 批次、durable `COMPLETE`。
- **预期行为**: accepted plan §10.2 exact-test 2 冻结："loser skipped events 覆盖全部 input，published role/assets/manifest 与 winner snapshot 相同"。
- **实际行为**: 多文件变体未断言 loser 的 `file_skipped` 事件是否逐 original 覆盖（`upload_filing` 同步结果不携带 file_events，测试也未走事件流断言），未断言 published role/assets/manifest 与 winner prepared snapshot 的一致性。
- **直接证据**: 测试体 2242-2257 的全部断言为 status/count/token/durable COMPLETE；全文件对 skip 事件的唯一精确断言在顺序测试 2527-2531（单文件三事件全序）。生产实现 `build_prepared_filing_skip_result`（docling_upload_service.py:1357-1367）确实只投影 original `file_skipped`，owner 级单测（test_docling_upload_service.py:5233-5296）已覆盖 originals-only 语义；但"与 winner snapshot 相同"的端到端等价断言不存在。
- **影响**: 回归防护面略窄；若未来 skip 投影误漏 companion 或 published 与 winner 树漂移，多文件并发路径不会响亮失败。生产语义正确，方向 fail closed。
- **建议改法和验证点**: 在多文件变体补断言：loser 事件流精确为逐 original 的 `file_skipped`；或读取 winner commit 后的 durable publication identity 与 winner 请求的 prepared identity 精确相等（该等价已由 test_docling_upload_service 跨 owner 测试证明，此处只需在多文件并发路径复现）。只改测试，不触及生产。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-plan §10.2 test 5 的 same-ticker union 未断言 filing IDs/manifest/assets 层 union

- **入口/函数**: `test_concurrent_distinct_targets_preserve_exact_union`（`same_ticker=True` 分支）
- **文件(行号)**: `tests/fins/test_sec_pipeline_upload_filing_stream.py:2390-2464`
- **输入场景**: 同 ticker AAPL、不同 fiscal period（Q1/Q2 → 不同 document_id）的两个 auto 请求并发，各携带不同 alias（MSFT/GOOG）。
- **实际分支**: 断言两 ok、`commit_tokens == 2`、两个 document 的 durable 状态均 `COMPLETE`、canonical company meta `accepted_aliases` 精确 union `{MSFT, GOOG}`。
- **预期行为**: accepted plan §10.2 exact-test 5："最终 filing IDs/manifest/assets 为 exact union，company aliases 按现有 commit owner stable union，无 lost update"。D-F2(c) 修复已补 aliases union；filing IDs/manifest/assets union 部分仍缺。
- **实际行为**: 未读取 filing_manifest.json 断言两个 filing ID 共存，未断言 assets 集合并集；仅以两独立 document `COMPLETE` + 2 次 commit 作为间接证据。
- **直接证据**: 测试体 2445-2461 的断言集合无 manifest 读取、无 assets union 断言；`_tracking_sec_pipeline` 的 source 仓储被丢弃（`_source`）未用于 manifest 验证。
- **影响**: "same ticker different filing 保留并集"的成功信号 6 只被间接证明。生产上 per-ticker writer 串行 + 不同 document_id + fresh company decision 结构性保证 union，且 CN/HK union 已由 Controller 裁决为 non-blocking residual，此处为 SEC 证据完整性缺口。
- **建议改法和验证点**: 读取 SEC filing manifest（或对两个 document 的 durable state 断言 source fingerprint/primary 各自独立且均 COMPLETE，并断言两个 filing ID 的 manifest 条目共存）。只改测试。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-未修复-低-plan §10.2 test 3 的并发 create-overwrite fresh rebase 端到端不变量未在并发测试断言

- **入口/函数**: `test_concurrent_explicit_create_obeys_overwrite_rebase_contract`（`overwrite=True` 参数化分支）
- **文件(行号)**: `tests/fins/test_sec_pipeline_upload_filing_stream.py:2280-2330`
- **输入场景**: 两个 raw explicit `create` + `overwrite=True` 请求并发；后 writer 走 `REBASE_CREATE_OVERWRITE` 分支，按 fresh COMPLETE source meta 重绑后 publish。
- **实际分支**: 测试断言 `["ok","ok"]`、`commit_tokens == 2`、`rollback_tokens == 0`。
- **预期行为**: accepted plan §10.2 exact-test 3 冻结："并再次端到端断言 fresh `first_ingested_at/created_at` 保持、同 fingerprint version 保持/异 fingerprint 递增、storage owner 写入新 revision"。
- **实际行为**: 上述 rebase 不变量在并发端到端路径完全未断言；仅 owner 级单测（test_docling_upload_service.py:5448-5541 的 `_build_upsert_meta`/rebase 字段与 version 断言、5544-5625 的 storage owner 新 revision 断言）间接覆盖。owner 级测试证明的是 helper 行为，未证明并发 rebase 路径上 fresh meta 真源确实传入（`dict(fresh_request.published_state.source_meta)`）。
- **直接证据**: 测试体 2313-2316 只有 status/commit/rollback 计数断言；`overwrite=True` 分支无任何 source meta/version/revision 断言。生产实现 `filing_upload_publication.py:724-733` 与 `rebase_prepared_filing_create_overwrite`（docling_upload_service.py:1279-1337）已正确实现该链路。
- **影响**: 若未来 fresh source_meta 传入被改为 stale meta 或 rebase 版本规则漂移，并发 overwrite 测试不会察觉；生产语义当前正确。
- **建议改法和验证点**: 在 `overwrite=True` 分支后读取 durable source meta，断言 `first_ingested_at/created_at` 与种子 winner 的 fresh meta 一致、同 fingerprint 时 `document_version` 保持、并断言 published revision 与 winner 前 revision 不同（storage owner 新写）。只改测试。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## 逐项审查结论

| 审查维度 | 结论 | 直接证据（当前代码） |
| --- | --- | --- |
| 唯一 owner | 通过 | plan §5 的每个语义均有唯一 owner 且实现未漂移：prepared identity/disposition/rebase/meta 在 `docling_upload_service`；durable identity 在 `_fs_source_integrity._project_filing_upload_publication_identity`；batch fresh read 在 `_fs_filing_upload_state_core` + `_fs_company_meta_core._read_company_meta_from_ticker_dir_unguarded`；arbitration/lifecycle 在 `filing_upload_publication`；typed conflict 在 `upload_failure`。九字段 staging meta 唯一 owner `_build_upsert_meta` 同时服务 prepare 与 rebase，无第二套字段拼装；私有 asset-source alias 已删除并统一消费 contract 常量。 |
| prepared/durable identity | 通过 | 两端投影同一 `FilingUploadPublicationIdentity` strict contract（canonical ticker、canonical lowercase SHA-256、ingest_method=upload、source_provider=user_upload、is_deleted=False、assets 唯一排序、primary 恰一 docling 且 derived_from 命中 primary original、companions 精确分割）；真实 prepare→publish→storage read 的跨 owner exact equality 测试（test_docling_upload_service.py:5299-5380）以磁盘投影对内存投影证明等价，非自证。 |
| batch fresh view / linearization | 通过 | `begin_batch` 先本地 reservation + 跨进程 writer lock 再克隆 staging（_fs_storage_infra.py:434-470，未修改）；batch read 只经 `_resolve_active_batch` 校验后消费 `staging_ticker_dir`（_fs_filing_upload_state_core.py:219-233），company 与 source 来自同一次 writer-owned view；SKIP/CONFLICT 在 rollback 消费 token 前持 writer，无其它 writer 可插入；PUBLISH 的 durable 线性化点保持既有 `_PHASE_COMMITTED` journal。无新增全局锁/retry/sleep/目录扫描。 |
| explicit action | 通过 | 显式 create+overwrite=False 并发 → typed conflict 且 requested/resolved/filing 恒 create（SEC 2322-2327、runtime durable 8765）；create+overwrite=True → rebase 后 publish、绝不 skip；显式 update identical stable 重传 → skip；changed update → conflict；raw action 仅在仲裁中读取一次，`_require_same_request_and_target` 强制同 raw request。 |
| repair / unsafe | 通过 | stable REPAIR_REQUIRED 需 fresh update + 同 expected_integrity（`_require_stable_action_contract` 353-360）；REPAIR_REQUIRED 漂移 → `SOURCE_REVISION_STALE`；fresh UNSAFE → validator prevalidation 保留 `SOURCE_INTEGRITY_UNSAFE` 为 primary（§6.4 行 3）；repair 进 commit owner 后 reset-time revision/blocked 复用既有 code。conflict grid 7 行逐格断言。 |
| cancel / rollback / commit | 通过 | checkpoint1（begin 后、fresh read 前，filing_upload_publication.py:631）与 checkpoint2（arbitration/validation 后、任何 mutation 或 SKIP/CONFLICT terminal 前，:679）均 rollback-first 返回 cancelled 且携带 initial request（C-F1 已闭合）；`batch_terminal_started` + finally（:755-761）保证任一路径恰好一次 rollback；rollback 失败 → path-free `STORAGE_IO`，绝不谎报 skip/cancel；进入 `commit_prepared_upload_batch` 后保持既有 store-time cancel 与 late-cancel 边界（docling_upload_service.py:1377-1404 未改）。waiting-writer 期间取消的三类候选（SKIP/CONFLICT/PUBLISH）owner 测试覆盖且断言 `batch_reads == []`、零 mutation。 |
| same ticker union | 通过（证据见 Finding 2） | per-ticker 串行 + 不同 document_id + fresh company decision 保留并集；aliases exact union 已断言（2458-2461）；filing IDs/manifest/assets union 的端到端断言缺失（低严重度，见 Finding 2）。 |
| different ticker keyed parallel | 通过 | `_BatchReadBarrierFilingUploadStateRepository` 的 barrier 位于真实 `read_filing_upload_state_in_batch` 内（begin_batch 取得各自 writer 之后）；两 entered Event 均 set 且两 ok（2462-2464）；若引入全局锁会 BrokenBarrierError → typed failure → 断言确定性失败。生产侧锁均为 per-ticker keyed（_fs_storage_infra._acquire_ticker_lock 按 ticker lock 文件），无跨 ticker 协调。 |
| single terminal | 通过 | 生产结构保证：每条 workflow stream 的 try 路径恰一个 `UPLOAD_COMPLETED`、异常路径恰一个 `UPLOAD_FAILED`（sec_upload_workflow.py:306-331、cn_pipeline.py:938-960）；runtime durable 每 job 恰一条终态 record。测试侧仅两处全序断言（2527-2531、2176），其余为末事件断言（见 Residual Risk）。 |
| typed path-free failure | 通过 | 新 code `SOURCE_PUBLICATION_CONFLICT` kind=STORAGE、固定文案、`file_label=None`、≤240 字符、无 path/ticker/document/revision/fingerprint/异常文本（upload_failure.py:399-421，测试断言 forbidden 词表）；§6.4 映射表全部实现：4 个 state-dependent usage code → conflict（`_STATE_DEPENDENT_USAGE_CODES` 与 plan 精确一致）、acquire/read `RuntimeError` → IO、validator `ValueError` → corruption（仅该窄边界）、其它 usage/invariant → rollback 后原样传播 → generic `UNEXPECTED_RUNTIME`。workflow 捕获顺序 `FinsUploadFailureError` → `OSError` → `Exception` 保证 typed 先于 generic。 |
| 测试真实性 | 通过（3 个低严重度证据缺口见 Findings） | 并发测试全部使用 ThreadBarrier/Event/Queue/future 有界同步，无 sleep/轮询/时间窗口；spawn 跨进程测试真实（multiprocessing Barrier+Queue，multiset `{ok, skipped}`）；不同 ticker batch 段内 barrier 真实；`_HoldingExecutor` + future 完成通知真实；fake 无 cast 且精确 conform（pyright 全仓 0 error 为直接 signal）。三个 plan §10.2 冻结的端到端断言未完整落地（Findings 1-3）。 |
| README 与 scope | 通过 | `dayu/fins/README.md` 只写已实现事实（shared owner、双 checkpoint、closed state table、skip 零 mutation、keyed 并行、identical 继续转换、skip 只投影 original file_skipped），无 UF-FIX10 流水账；`tests/README.md` 的"无 sleep/polling"表述与修复后实现一致；根 README、dayu/README、Host/Engine README 零 diff。scope 审计：生产改动全部在 `dayu/fins`，Host/Engine/Service/CLI/material/oracle/scenario/registry/evidence 零 diff；S1 零行为承诺由 S1 两路 re-review 与最终 behavior-preserving 边界证实。 |

## Open Questions

- 无。以下三点此前以 open question / residual 形式出现过，本轮已按直接证据闭合：(1) checkpoint2 在 validation-failure 路径上也先观察 cancel，与 plan §6.4 行 1（先形成 closed conflict 裁决、完成第二取消 checkpoint、未取消才 rollback 并抛）字面一致，且零 mutation/durable 副作用，确认为冻结意图；(2) `_read_fresh_state_or_raise` 的 `ValueError → corruption` 映射经 Controller rejected-with-reason 裁决（输入校验 ValueError 在 typed caller 下不可达），不重新计为 finding；(3) `_begin_publication_batch_or_raise` 未捕获 `ValueError`（canonical ticker 输入下不可达）为已记录的 non-blocking residual。

## Residual Risk

- **SEC 取消测试的 observation 耦合**：`cancel_at_observation=[5,6]` 与 prepare 路径取消观察次数耦合（docling_upload_service.py:439/461/985/508 + publication 2 次），checkpoint 1↔2 互换会静默通过；owner 级 `observations == 2` 断言（test_filing_upload_publication.py:1470）已固定权威语义，该耦合影响有限。已由 DS S2 review/re-review 分类，维持原判定。
- **single-terminal 的测试断言强度**：大部分 stream 测试只断言末事件为 terminal；生产结构上每条 stream 恰 yield 一个 terminal，风险低。
- **CN/HK 并发测试共用同一参数化 helper**：HK 非独立测试代码；canonical ticker 直读 durable state 的断言证明 HK 未旁路 shared owner。plan 冻结矩阵下可接受。
- **converter 非确定性 tradeoff**：同一 original 产生不同 derived bytes 时 loser typed conflict 而非近似 skip，是明确的 fail-closed contract，已记录于 README 与 derived mismatch 测试。
- **service runtime 动态 monkeypatch fake**：无 batch-read 可达路径，按 Controller 裁决维持未来触发式 non-blocking residual。
- **`_wait_terminal` 既有 sleep 轮询**：仅存量单任务 runtime 测试使用（非本 work unit 新增），不在 publication matrix 承诺范围内。
- **未独立复跑完整 `pytest tests/fins`**：本 review 独立运行 7 个 focused 文件 `746 passed, 3 warnings`（exit 0）与全仓 `python -m pyright dayu/ tests/ utils/`（exit 0，0 errors）；完整 Fins suite `1916 passed, 1 skipped` 与逐文件 coverage 数据引用自 S2 fix/acceptance artifact 与两路独立 re-review，未独立复跑。
- **manual filesystem writer 绕过仓储锁、SHA-256 理论碰撞、post-COMMITTED guard 释放失败**：均维持 plan §13.2 已分类的 later-work-unit 风险，不在本 work unit 目标内。

## 独立验证记录

- 命令：`source .venv/bin/activate && pytest tests/fins/test_filing_upload_publication.py tests/fins/test_sec_pipeline_upload_filing_stream.py tests/fins/test_cn_pipeline.py tests/fins/test_fins_ingestion_runtime.py tests/fins/test_docling_upload_service.py tests/fins/test_fins_storage_atomicity.py tests/fins/test_upload_failure.py -q`
- 结果：`746 passed, 3 warnings`（exit 0）。
- 命令：`source .venv/bin/activate && python -m pyright dayu/ tests/ utils/`
- 结果：exit 0，无 error/warning/information 输出（仅 pyright 版本更新提示）。
- 未运行 UF-PF10/UF-PF12；未修改生产/测试/计划/oracle/scenario/evidence；未创建 commit 或 PR；工作树仅新增本 review artifact。
