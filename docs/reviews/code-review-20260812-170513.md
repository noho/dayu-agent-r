# Code Re-Review

## Scope

- **Gate**: code re-review S1
- **Mode**: current changes
- **Branch**: `codex/upload-filing-oracle`
- **Base**: `9527c6e0fd430082219b13daee31d300a8b44be4` (accepted plan commit)
- **Reviewer**: AgentMiMo (independent first-pass)
- **Output file**: `docs/reviews/code-review-20260812-170513.md`
- **Target digests (validated)**:
  - `dayu/fins/pipelines/docling_process_converter.py`: `dab1d5ef4fe308bd4a73d9d983dde038fc304430b1d56a762c6c5a29e740cc86` ✓
  - `tests/fins/test_docling_process_converter.py`: `10028e22186162c8a5aab271a490323a5a2bd4bd451baca07c7269c92130c861` ✓
- **Included scope**: 2 untracked files (production module + test module)
- **Excluded scope**: 旧 `cn_docling_process.py`、旧 runner/tests、caller 迁移、ingestion runtime 终态修正（均为 S2/S3 scope）
- **Parallel review coverage**: 无；单 reviewer 全量走读
- **Contract evidence**: accepted plan S1 (§6–§8)、根 `CLAUDE.md`、`dayu.runtime.interruptible_process`、`dayu.documents.docling_runtime`、`dayu.contracts.cancellation`、旧 `cn_docling_process.py` + `test_cn_docling_process.py`
- **Adjudication reference**: `docs/gateflow/uf-fix09-s1-code-review-adjudication-20260812.md`

## Closure Matrix

### Accepted Gaps Verification

#### Gap 1: mkdtemp/write_bytes 及 rmtree 组合的分类/资源/cause (AgentDS F2)

**Status**: ✓ 已正确闭环

| 测试 | 断言 | 结果 |
|------|------|------|
| `test_converter_mkdtemp_failure_is_ipc_without_handle_or_cleanup_leak` | IPC_PROTOCOL kind, __cause__ is creation_failure, 无 handle 创建, 无 cleanup 调用 | ✓ |
| `test_converter_input_write_failure_is_ipc_and_removes_temp_without_handle` | IPC_PROTOCOL kind, __cause__ is write_failure, 无 handle 创建, temp 已清理 | ✓ |
| `test_converter_input_write_and_rmtree_failures_prioritize_cleanup_and_preserve_chain` | CLEANUP 优先级, __cause__ is cleanup_failure, IPC_PROTOCOL 在链中, 无 handle 创建, temp 已清理 | ✓ |

**验证要点**:
- mkdtemp 失败时不创建 handle，直接返回 IPC_PROTOCOL ✓
- write_bytes 失败时仍删除已创建 temp ✓
- rmtree 同时失败时 CLEANUP 按既定优先级覆盖并保留原失败链 ✓
- 只改测试，生产逻辑未修改 ✓

#### Gap 2: terminate/kill 普通异常的 FAILED phase、CLEANUP、close/temp (AgentDS F3)

**Status**: ✓ 已正确闭环

| 测试 | 断言 | 结果 |
|------|------|------|
| `test_converter_terminate_failure_is_cleanup_with_failed_phase_and_full_cleanup` | CLEANUP kind, __cause__ is terminate_failure, terminate < close, 无 kill, close 最后, temp 清理, FAILED phase = ["terminate_completed"] | ✓ |
| `test_converter_kill_failure_is_cleanup_with_failed_phase_and_full_cleanup` | CLEANUP kind, __cause__ is kill_failure, terminate < kill < close, close 最后, temp 清理, FAILED phase = ["kill_completed"] | ✓ |

**验证要点**:
- terminate 抛异常时记录 FAILED phase ✓
- terminate 未退出后 kill 抛异常时记录 FAILED phase ✓
- 两者都映射 CLEANUP ✓
- 两者都调用 handle close ✓
- 两者都清理 temp ✓

### Rejected Findings Verification

| Finding | Status | 证据 |
|---------|--------|------|
| `_close_handle` while-loop 无上界 (AgentMiMo F2, AgentDS F1) | rejected-with-reason, 未实施 | production 文件 SHA-256 未变，L641-667 仍为 `while True` 循环，无新增重试上限或总时限 |
| 测试隐式依赖 Docling 安装 (AgentMiMo F1) | rejected-with-reason, 未实施 | test 文件 L23 仍为 `from dayu.documents.docling_runtime import DoclingRuntimeInitializationError`，未改为条件导入 |
| spawn probe 依赖 pytest (AgentDS F6) | rejected-with-reason, 未实施 | test 文件 L310 仍为 `pytest.MonkeyPatch()`，未改为 `unittest.mock` |
| `wait_result=None` 防御分支 (AgentDS F4) | rejected-with-reason, 未实施 | production 文件 L683-687 未新增测试覆盖 |
| closed JSON 每个私有递归分支 (AgentDS F5) | rejected-with-reason, 未实施 | test 文件未新增 `_is_closed_json_value` 的参数化测试 |

**结论**: 所有被驳回 findings 均未被错误实施 ✓

## Findings

未发现实质性问题。

## Validation

### 测试执行

```
39 passed in 0.63s
```

### pyright

```
0 errors, 0 warnings, 0 informations
```

### Coverage

```
Name                                               Stmts   Miss  Cover   Missing
--------------------------------------------------------------------------------
dayu/fins/pipelines/docling_process_converter.py     331     18    95%   86, 122, 195, 255, 411-412, 470, 592, 684, 696, 708, 821, 832-833, 877, 930, 932, 935
--------------------------------------------------------------------------------
TOTAL                                                331     18    95%
```

**覆盖率提升**: 从首轮 93% 提升至 95%，新增 5 个测试覆盖了 mkdtemp/write_bytes/rmtree 失败路径和 terminate/kill 异常路径。

### Focused Regression

```
110 passed, 1 skipped, 6966 deselected in 9.25s
```

## Docs Decision

S1 尚未迁移任何 caller 或用户可见行为；README 更新归 S2/S3。当前只新增 Gateflow/review artifact，不修改 README。

## Open Questions

无 blocking open question。

## Residual Risks

| 风险 | 分类 | owner / 下一步 |
| --- | --- | --- |
| 旧 CN runner 与新 owner 暂时并存 | covered by later approved slice | S2 migration/delete |
| 真实 Docling 未在 S1 执行 | covered by later approved slice | S2 integration / S3 UF-PF09 |
| 非 POSIX descendant guarantee | assigned to later work unit | runtime platform work unit |
| `_close_handle` while-loop 无上界 | rejected, runtime contract 保护 | 若 runtime 违反 bounded contract，在 runtime owner 修复 |
| 测试隐式依赖 Docling 安装 | rejected, 当前环境可运行 | 若需无 Docling 环境运行，由后续 slice 处理 |

## Completion Status

- **验证结果**: 39 tests passed, pyright 0 errors, coverage 95%, focused regression 110 passed
- **Production 文件 SHA-256**: `dab1d5ef4fe308bd4a73d9d983dde038fc304430b1d56a762c6c5a29e740cc86` ✓ 未变
- **Test 文件 SHA-256**: `10028e22186162c8a5aab271a490323a5a2bd4bd451baca07c7269c92130c861` ✓ 已更新（新增 5 个测试）
- **结论**: **pass**
- **finding 数量**: 0
- **Accepted gaps 闭环**: 2/2 ✓
- **Rejected findings 未实施**: 5/5 ✓
