# Code Review

## Scope

- Mode: current changes
- Branch or PR: `codex/interactive-oracle`，PR 190 F11/F12 S4.1 production fix
- Base: `c824ea9038ecb4084621117c6806764cd63e9a20`
- Output file: `docs/reviews/code-review-20260805-215757.md`
- Included scope: 当前 workspace 中
  `dayu/host/compact_pipeline.py`、`dayu/host/context_fallback.py`、
  `tests/host/test_dispatch_scheduler.py` 与
  `docs/reviews/pr-190-f11-f12-s4-production-fix-20260805.md`；沿真实 proactive selection、
  durable fallback load、reactive recovery start、RunInput strict replay、manifest 与 Run / Attempt
  terminal 路径走读。
- Excluded scope: baseline 以前的 PR 190 已接受提交、harness/oracle/scenario、旧 review/evidence、真实
  provider evidence 重跑、S5 registry/PR body；这些内容没有被当前 workspace 修改。
- Parallel review coverage: 无；当前约束不启用 subagent。主 reviewer 完成 correctness/state-machine/error-path、
  architecture/semantic-owner/LLM-facing/typing/test-gap、adversarial failure 与 overcoupling 检查。

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- `covered by later approved slice`：本 review 只证明 deterministic Host owner contract。S4 mandatory
  Mimo→DeepSeek real-provider evidence 仍须在本 review 后使用全新 evidence root 重跑；旧 bundle 继续是
  immutable / superseded partial evidence，不能据此关闭真实 observation。
- 未发现未分类 implementation residual risk。

## Review Evidence

### Intent 与 owner alignment

- 基线 producer `compact_pipeline._fallback_material_blocks` 手工对 raw current input 计算 size/digest；当前
  diff 改为调用 `context_fallback._fallback_current_input_material_block`，后者唯一委托
  `compact_material.run_input_material_block` 完成 normalizing、size 与 digest 派生。
- `context_fallback.load_context_fallback_in_transaction` 对 proactive/reactive 都调用
  `_fallback_material_blocks_for_window`，从 current Run、canonical `USER_INPUT_ACCEPTED` 与同一
  `build_pre_dispatch_compact_material_view` 重建 selection material；RunInput 只消费 frozen blocks并继续做
  strict id/source/digest/protected-floor 校验，没有新增 fallback 或补偿逻辑。
- 改动没有触及 fallback selection policy、item/char caps、budget、terminal permit、schema、Memory、renderer、
  Engine、Service 或 public export。`context_fallback.__all__` 保持原 contract。

### State-machine 与 failure-path review

- proactive exhausted path：两次 prepared proposal 与两次 rejection 属于同一 operation；唯一
  `CONTEXT_COMPACTION_FAILED` 先于 `DISPATCH_FALLBACK` manifest/start，随后只创建一个 ordinary Attempt。
  typed proactive projection 收敛为 `FAILED/USE_FAILED_FALLBACK`，Run 最终成功且没有 `RUN_LOST`。
- reactive path：source Attempt 先失败并将 Run 推进到 `RECOVERING`；失败 compact terminal 绑定同一
  operation，fallback replay 验证 EventLog-backed selected ids/digest 后创建唯一 recovery Attempt；source
  Attempt 为 `FAILED`、recovery Attempt 与 Run 为 `SUCCEEDED`，active task 清空。
- invalid trigger source、missing Run/current event、current ref mismatch、payload/digest drift 继续 fail closed；
  没有 default、compatibility shim、loose parsing 或 late second terminal。

### Adversarial failure 与 semantic ownership drift pass

- 连续空白、tab 等单行空白与空行由已有 `normalized_material_text` owner contract处理；新增 tests 使用真实
  连续空白/空行输入并先在未修基线上复现两种 production exception。
- reactive protected-recent 反例证明 selection/replay block identity、source refs 与 selected-view digest 均来自
  EventLog material view，不再从 Memory block id 或 renderer output 反推。
- digest 仍由 `selected_material_view_digest` 对 selected block id/source/content digest 计算；manifest 只引用同一
  fallback window digest，不复制或重算 selected-view semantics。
- 没有跨层反向依赖。`compact_pipeline` 原本已经依赖 `context_fallback` 的 selection contract；复用其内部
  current-input owner没有引入新的模块环或更宽公共 interface。

### Tests、typing 与 docs

- owner tests 断言两次 `quality_check_rejected`、唯一 failed compaction terminal、normalized selected-view
  digest、bounded current-only proactive dispatch、fallback manifest ref、sizing stage、protected-recent reactive
  replay、Attempt / Run terminal 与 scheduler task cleanup。
- `dayu/host/README.md` 已覆盖 Host-owned fallback/recovery 与 strict RunInput replay；当前变更恢复既有 contract，
  没有新增稳定接口或状态机，不更新符合其 Agent 更新约束。
- `tests/README.md` 已覆盖 Context Governance、dispatch fallback、reactive recovery 测试层级；没有新增测试层级
  或运行方式，不更新符合其维护边界。

## Validation

- 新增两条 regression tests 在修复前：`2 failed`，精确错误分别为
  `fallback selected material view digest mismatch` 与
  `fallback selected block id is missing from material view`。
- 新增两条 regression tests 在修复后：`2 passed`。
- 受影响 Host tests：`409 passed, 1 skipped`。
- 全量 Host tests：`2423 passed, 1 skipped, 6 deselected`。
- branch coverage：`compact_pipeline.py 90%`、`context_fallback.py 87%`、合计 `88%`。
- 全仓 pyright：`0 errors, 0 warnings, 0 informations`。
- `git diff --check`：通过。

## Gateflow disposition

- Review decision：`PASS`。
- Accepted findings：0。
- Rejected/deferred/needs-more-evidence findings：0。
- Implementation residual：0 unclassified。
- Next Gate Order entry：`fix`；因本 review 无 finding，实际为 zero-change fix/re-review handoff。但用户明确要求
  停在 code review，故本轮不进入 fix、re-review、accepted slice commit、provider evidence 重跑或后续 gate。
