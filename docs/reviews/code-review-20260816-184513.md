# Code Review — UF-FIX08 Slice 6（independent deepreview）

## Scope

- Mode: current changes（当前未提交 diff）
- Branch: `codex/upload-filing-oracle`
- Base: `main`（`256786b2`）
- HEAD: `1e062f6cc13c22232449b4dc80ffcccb93b796d7`（未 commit、未 stage、未 push）
- Output file: `docs/reviews/code-review-20260816-184513.md`
- Included scope:
  - `dayu/fins/pipelines/sec_download_filing_workflow.py`、`dayu/fins/pipelines/cn_download_filing_workflow.py`（production，各 4 处 UNSAFE typed gate + docstring）
  - `tests/fins/test_sec_pipeline_download.py`、`tests/fins/test_cn_download_workflow.py`
  - `README.md`、`dayu/fins/README.md`、`tests/README.md`
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-slice6-implementation-20260816.md`（implementation artifact）
- Excluded scope: 未读 AgentMiMo 本轮新 review artifact（用户指示）；前序 slices 1–5 已 accepted 的 commits 不作为本轮 review 对象，仅在调用链与 contract 层面作为既有事实核对。
- Parallel review coverage: 无 subagent；本轮为主 reviewer 全程独立走读。证据链以静态调用链、测试节点与已复跑的验证为准；line-level coverage 数据因并行 reviewer 曾并发写 `.coverage`，仅作辅助证据（见 Open Questions）。

## 静态调用链与 owner 逐项核对（证据基础）

以下核对基于直接代码走读（行号为当前工作树）：

1. **storage typed integrity/preflight/canonical upsert 唯一 owner**：四个 UNSAFE gate 全部消费 `dayu.fins.storage` 的 `SourceIntegrityClassification` 与 `SourceIntegrityPreflightError`；workflow 不解析 raw meta、不扫描目录、不解释异常字符串。whole-tree 决策仍由 `classify_source_integrity_preflight`（`dayu/fins/storage/source_integrity.py:290`）唯一产生；canonical manifest 重建仍由既有 upsert owner（`sec_download_source_upsert.py`/`cn_download_source_upsert.py`）完成，本 slice 未修改该 owner，测试以 `COMPLETE` 分类 + manifest 文件存在断言其真实执行。
2. **Phase A UNSAFE 在旧 meta/provider 之前**：SEC gate 在 `sec_download_filing_workflow.py:237-238`，位于 `_safe_get_filing_source_meta`（242）与 `list_filing_files`（301）之前；CN gate 在 `cn_download_filing_workflow.py:178-179`，位于 `_safe_get_source_meta`（183）与 PDF transport（227）之前。SEC 测试以真实 undeclared file 种子 + `begin_batch` 拒绝 spy + provider 零调用断言证明。
3. **Phase B UNSAFE 在 identity 比较/reset/blob/commit 之前**：SEC gate `504-505` 在 `has_same_source_publication_identity`（506-509）与 reset（556）之前；CN gate `583-584` 在 identity 比较（585-588）、reset（602-608）、blob（626/640）、commit（691）之前。CN 测试断言 phases 精确等于 `["begin", "rollback"]`、rollback_calls==1、commit_calls==0、无 reset phase。
4. **rollback token owner 与无双 rollback**：CN 由 `_commit_cn_filing_assets_batch` 的 `commit_started` + `finally`（693-699）唯一持有，gate raise 走既有 finally 回滚一次并保留主异常（`_rollback_cn_batch_preserving_primary`）；SEC 由 `finally`（749-751）兜底，gate raise 时 token 未置 None 且未进入 identity-mismatch 显式 rollback 分支，无双 rollback 路径。异常传播不吞掉 typed 类型。
5. **revision=None 不能使 UNSAFE 等价 MISSING**：两处 gate 均以 `status is UNSAFE` 显式 raise，不依赖 revision 比较；`has_same_source_publication_identity` 对任一 UNSAFE 参数抛 `ValueError`（`source_integrity.py:279-280`）；Phase A 的 `MISSING → previous_meta=None` 三元与 Phase B 的 `not MISSING → reset` 分支在 gate 之后，UNSAFE 不可达。storage repair owner 同样在比较前 gate（`_fs_source_document_core.py:438-444`）。
6. **whole-manifest missing 多 actual/多 repair、non-selected、selected rejected、唯一 accepted selected**：多 actual → 每个 actual source 由 inspector 投影为 `REPAIR_REQUIRED + (SOURCE_MANIFEST_MISSING,)`（SEC/CN 测试逐项断言该 owner 投影），preflight 抛 `MULTIPLE_REPAIR_REQUIRED` 且 provider/company/rejection/batch 零 mutation；non-selected → `UNSELECTED_REPAIR_REQUIRED`（SEC/CN 各有节点）；selected rejected → `SELECTED_REJECTED_REPAIR_REQUIRED`（SEC 既有 slice-5 节点保持，CN 无 rejection registry 路径）；唯一 accepted selected → 真实 reset/download/upsert，完成后 `COMPLETE` + manifest 重建（SEC/CN 顶层 repair 测试断言）。全部为真实 filesystem 路径，无 fake 重判。
7. **既有 MISSING/COMPLETE/REPAIR_REQUIRED retry/overwrite/provider/rejection/company-source atomicity 不回退**：diff 只新增 4 个仅在 `status is UNSAFE` 时触发的分支，未改变任何既有分支与结果收敛；`tests/fins` 全套 1850 passed（1 skipped 为环境条件 skip），两个下载测试文件 200 passed。
8. **CN staged fake 是否足够证明 Phase B 边界**：足够。`test_cn_unsafe_phase_b_rolls_back_without_reset` 的 monkeypatch 只替换 `classify_staged_source_integrity` 返回值为 invariant-valid 的 typed UNSAFE classification，其余全部为真实 batching/source repository 与真实 gate/rollback 生产代码；真实 inspector 能产生 staged UNSAFE 的事实由 storage 层测试（`test_fins_storage_atomicity.py:7102` 等）单独证明。两层证据组合闭合 CN Phase B 边界。
9. **严格类型/中文 docstring/无 fallback shim**：pyright `0 errors, 0 warnings`；两个 public workflow docstring 的 Raises 与 `_commit_cn_filing_assets_batch` docstring 均已补充新异常；无 `Any`/`object`/`getattr`/`hasattr`/兼容 shim/魔法数字新增。
10. **README**：根 README 仅写已落地的用户可见 `auto` 重建行为与显式 action 失败提示（与 validator `EXISTING_SOURCE_REPAIR_REQUIRES_AUTO` 文案一致）；`dayu/fins/README.md` 四态、trusted revision、staged recheck、snapshot complete-only、download typed fail-closed 均为现在时、与当前实现一致；`tests/README.md` 命令块实测可运行、owner matrix 与测试事实基本一致（SEC Phase B 表述见 Finding 1）。
11. **scope/frozen/diff guards**：diff 仅含 slice 6 allowed 7 文件 + 本 implementation artifact；`docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`、`docs/host/design.md`、`docs/engine/design.md` diff 为空；`git diff --check` 通过；未执行真实 CLI/provider evidence。

## Findings

### 1-未修复-低-SEC Phase B UNSAFE 真分支无直接测试节点

- **入口/函数**: `run_download_single_filing_stream()` Phase B（SEC 单 filing owner，`_SecDownloadFilingWorkflowHost._download_single_filing_stream` → `sec_pipeline.py:1018` 链路）
- **文件(行号)**: `dayu/fins/pipelines/sec_download_filing_workflow.py:504-505`
- **输入场景**: `begin_batch` 之后、Phase B `classify_staged_source_integrity` 时，staging tree 因外部并发写者/恢复残留等被 storage 分类为 `UNSAFE`（whole-tree preflight clean 之后、Phase B recheck 之前的 race window）
- **实际分支**: `phase_b_integrity.status is SourceIntegrityStatus.UNSAFE` 为 **True** 的分支；本轮所有测试只让该条件为 False（504 的 False 边被既有 Phase B 路径覆盖）
- **预期行为**: 在任何 identity 比较/reset/blob/commit 前抛 typed `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)`，`finally`（749-751）回滚 batch 恰一次，published tree 不变
- **实际行为**: 实现顺序与 rollback 结构正确，但没有任何测试让该条件为 True，失败行为未被证明
- **直接证据**（静态为主，非 coverage）:
  - `tests/fins/test_sec_pipeline_download.py` 无任何 `classify_staged_source_integrity` 引用或 monkeypatch（全文件 grep 无命中）；
  - 新增 `test_sec_unsafe_phase_a_and_whole_tree_preflight_have_zero_mutation` 的 UNSAFE 种子在 Phase A gate（237-238）即 raise，无法到达 Phase B；其 whole-tree 断言由 `classify_source_integrity_preflight`（`sec_download_workflow.py:580`）先 raise，同样不经过 Phase B gate；
  - 全仓 grep 显示 staged-UNSAFE 注入只存在于 CN 测试（`test_cn_download_workflow.py:1964`）与 storage 层测试，SEC 下载路径无此节点；
  - line coverage（504 条件执行、505 raise 未执行）与上述一致，但并行 reviewer 曾并发写 `.coverage`，本 finding 不以 coverage 为唯一证据；
  - 同 slice 的 CN 等价 gate（583-584）有直接节点（`test_cn_unsafe_phase_b_rolls_back_without_reset`），证明该 gate 模式本身可测，SEC 属于漏测。
- **影响**: 该防御分支仅 race-window 可达，实现正确性可由 CN 等价节点与 storage 层 staged-UNSAFE 能力间接支撑；但 SEC 市场该失败路径的 rollback/fail-closed 行为无直接证明；同时 `tests/README.md:431` "Download matrix覆盖 SEC/CN Phase A/Phase B `UNSAFE` 在 meta/reset前失败" 对 SEC Phase B 的表述不准确。
- **建议改法和验证点**: 增加一个 SEC Phase B 节点，镜像 `test_cn_unsafe_phase_b_rolls_back_without_reset`（`tests/fins/test_cn_download_workflow.py:1900`）：seed 一个完整 SEC source，monkeypatch `pipeline._source_repository.classify_staged_source_integrity` 返回 invariant-valid 的 `SourceIntegrityClassification(status=UNSAFE, revision=None, reasons=(META_UNTRUSTED,))`，调用 `pipeline._download_single_filing_stream`，断言 `SourceIntegrityPreflightError` 且 `reason is UNSAFE_PUBLICATION`、batch 恰好 begin+rollback 各一次、无 reset/commit、published source bytes 与 provider 调用零变化；同步修正 tests/README 表述（改为准确覆盖范围，或补齐后保留）。
- **修复风险（低）**: 纯测试/文档补充，不触碰生产。
- **严重程度（低）**: **不阻塞** slice 6 review gate；建议作为 slice 6 fix 或 deferred-with-owner 收口。

### 2-未修复-低-CN 顶层把新 per-filing UNSAFE raise 投影为 generic `filing_execution_failed`，与 SEC typed 传播不对称

- **入口/函数**: `run_cn_download_workflow()` per-filing catch → `project_cn_filing_failure()`
- **文件(行号)**: `dayu/fins/pipelines/cn_download_workflow.py:306-320`；`dayu/fins/pipelines/cn_download_filing_workflow.py:71-88`
- **输入场景**: CN/HK 顶层下载中，whole-tree preflight clean 之后、某 filing Phase A（178-179）或 Phase B（583-584）分类变 `UNSAFE` 的 race window
- **实际分支**: `except Exception`（306）→ `project_cn_filing_failure` 无 `SourceIntegrityPreflightError` 特判 → 落入 generic 返回 `("filing_execution_failed", "财报文档执行失败")`（88）
- **预期行为**: 该 failure 的语义是 typed UNSAFE preflight 拒绝，预期用户可见结果保留封闭、可行动的 reason
- **实际行为**: filing 行标记 failed 且 reason generic；typed reason 只存在于异常边界，未贯穿到 result row。SEC 同场景把 typed 异常沿 `run_download_stream_impl` → `SecPipeline.download_stream` → adapter（`sec_pipeline.py:1779` 链路）原样传播，两市场对同一 failure 的用户可见语义不对称
- **直接证据**: 上述两段代码；`project_cn_filing_failure` 只特判 `FinsDownloadProviderError`/`OSError`；slice 6 新增 CN raise 点（178-179、583-584）位于该 catch 之内；无测试断言 CN 顶层对该新 failure 的 result-row 投影（slice 6 CN 节点均直接调用单 filing stream 断言异常，未走顶层）
- **影响**: race-window 下 CN 用户得到不可行动的 generic 文案（bounded、path-free，不违反最高约束，但丢失 typed 语义）；投影行为未被测试固定，后续可能漂移
- **建议改法和验证点**: 二选一（controller 裁决）：(a) 在 `project_cn_filing_failure` 增加 `SourceIntegrityPreflightError` 特判，投影封闭 reason 与固定安全文案，并补 CN 顶层投影节点（断言 reason_code/message、path-free、company/source 零 mutation）；(b) 确认 generic bounded 投影为有意 policy，补一个断言该投影行为（reason_code=="filing_execution_failed" 且 message path-free）的顶层测试以固化契约
- **修复风险（低）**: 选 (a) 触碰既有 projection 单点，需跑 CN 全套回归；选 (b) 无生产改动。
- **严重程度（低）**: **不阻塞** slice 6 review gate。

## 验证结果（本 reviewer 复跑）

- 两个直接受影响测试文件：`200 passed`（3 条既有 edgar deprecation warnings）
- accepted plan §10 focused matrix：`1229 passed`
- `tests/fins` 全套：`1850 passed, 1 skipped`（环境条件 skip）
- `tests/service/test_fins_direct.py + tests/cli/test_fins_commands.py`：`188 passed`
- pyright（`dayu/ tests/ utils/`）：`0 errors, 0 warnings, 0 informations`
- focused matrix branch coverage：`sec_download_filing_workflow.py` 82%、`cn_download_filing_workflow.py` 83%（均 >=80% 达标）
- `git diff --check` 通过；frozen oracle/scenario/design diff 为空；scope 仅含 slice 6 allowed 文件 + implementation artifact
- 未修改任何 production/test/README，未 commit、未 stage、未 push

## Open Questions

- 无。Finding 2 的 (a)/(b) 是裁决选项而非阻碍判断的问题。

## Residual Risk

- SEC/CN single-filing Phase A/B UNSAFE gate 仅在 whole-tree preflight 与 mutation 之间的 race window 可达；主要保护面（whole-tree typed 拒绝）在 SEC 顶层有直接节点（`test_sec_pipeline_download.py:5458`），storage 层 preflight raise 有节点（`test_fins_storage_atomicity.py:4212/7102`）；CN 顶层 whole-tree UNSAFE 在 workflow 层无直接节点（仅 storage 层覆盖）。
- SEC Phase B UNSAFE 真分支无测试节点（Finding 1）。
- CN 顶层对新 per-filing UNSAFE failure 的 result-row 投影无测试节点（Finding 2）。
- 并行 reviewer 曾并发写 `.coverage`；本 review 的 line-level coverage 仅作辅助证据，aggregate 82%/83% 为本 reviewer 复跑所得。如需更精确 line 级结论，建议在无并行写者时重跑 coverage。
- 未读 AgentMiMo 本轮新 review artifact；若两 review 结论冲突需 controller 复核。
- 未执行真实 CLI/provider evidence（按 work unit 约束，归属 UF-PF08/UF-PF12 evidence owner）。

## 结论

**PASS（无 blocker）**。2 个低严重度 finding（均不阻塞 slice 6 review gate），供 controller 按 accepted / rejected-with-reason / deferred-with-owner 裁决；建议 Finding 1 以 deferred-with-owner（补 SEC Phase B 节点）收口，Finding 2 至少以 deferred-with-owner 固化 CN 顶层投影契约。
