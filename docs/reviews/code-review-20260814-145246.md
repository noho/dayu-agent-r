# Code Review — UF-FIX04 S1 re-review（AgentDS 独立复核 review fix）

## Scope

- Mode: current changes（diff against base `f609a4d8`）
- 本轮性质: S1 review fix 的独立 re-review（Reviewer=AgentDS，只审查不修改）
- Branch: `codex/upload-filing-oracle`
- Base: `f609a4d8238c6b31456c2e1d548079b22b771a68`（gateflow: accept plan for upload-filing-calendar-year-validation）
- Work unit: `UF-FIX04 shared-calendar-year-validation`
- Slice: `S1-domain-calendar-year-owner`
- Output file: `docs/reviews/code-review-20260814-145246.md`
- Included scope:
  - `dayu/fins/domain/filing_semantics.py`（工作区未提交 diff，含 DS-2/DS-4 修复）
  - `tests/fins/test_fiscal_normalization_contracts.py`（工作区未提交 diff，含 DS-2/DS-3 owner 测试）
  - `tests/fins/test_read_runtime_semantic_ownership_guards.py`（工作区未提交 diff，本轮只读复核）
  - `docs/reviews/code-review-20260814-143751.md`（AgentMiMo 初轮，pass，无 finding）
  - `docs/reviews/code-review-20260814-144024.md`（AgentDS 初轮，pass，DS-1~DS-4 四个低严重度 finding）
  - `docs/reviews/wu-upload-filing-calendar-year-validation-s1-review-controller-adjudication.md`（controller 裁决：DS-1 rejected-with-reason，DS-2/3/4 accepted）
  - `docs/reviews/wu-upload-filing-calendar-year-validation-s1-review-fix-codex.md`（AgentCodex fix artifact）
  - `docs/reviews/wu-upload-filing-calendar-year-validation-plan-codex.md`（accepted plan，重点 §5/§10-S1/§11/§12/§13/§14）
  - `dayu/fins/tools/read_runtime.py`（唯一生产 consumer，只读审查调用链）
- Excluded scope: S2/S3 未接线 consumer（按用户指令不作为 finding）；`UF-PF04`（按用户指令不运行）；其余不属于 S1 的文件。
- 并行另一路 re-review artifact（`code-review-20260814-145012.md`）在本次 review 期间出现，为保持独立性未读取。
- Parallel review coverage: 无 subagent。

## 逐项 finding 复核（DS-1 验证 rejected 理由；DS-2/3/4 状态只允许 已修复/未修复/部分修复/证据失效）

### DS-1 — isoformat round-trip 当前不可达（controller: `rejected-with-reason`）

- 复核结论: **rejected 理由与实现保持，成立**。
- 理由核对: 裁决理由为「accepted plan §5 明确要求 `datetime.date` 构造后执行 exact `isoformat()` round-trip，该检查是 canonical output contract 的直接防御，不应删除，也不需加注释」。
- 实现核对: `dayu/fins/domain/filing_semantics.py:395-396` 的 `if parsed.isoformat() != value: raise ...` 原样保留，无删除、无改写、未新增重复注释。
- 证据: 复现 coverage missing 列表仍精确包含行 396（见 Validation），证明没有通过测试替身或行为改写伪造该分支覆盖；与 fix artifact 声明一致。

### DS-2 — year 范围 message 与 bounds 不同源（controller: `accepted`）→ **已修复**

- Required fix 核对:
  - 唯一范围文本真源: `filing_semantics.py:46-53` 定义 `_MIN_CALENDAR_YEAR`（46）/ `_MAX_CALENDAR_YEAR`（49），并由二者派生 `_CALENDAR_YEAR_RANGE_TEXT: Final[str] = f"{_MIN_CALENDAR_YEAR}..{_MAX_CALENDAR_YEAR}"`（52），全部为模块级 `Final` 私有常量。
  - 两个入口共同消费: `parse_calendar_year` 的 raise 在 364 行、`normalize_fiscal_year` 的 narrowing raise 在 421 行，均使用 `f"{field_name} 必须是 {_CALENDAR_YEAR_RANGE_TEXT} 的整数"`；无第二处 message 字面量。
  - owner-level 一致断言: `test_calendar_year_entry_points_share_exact_range_message`（tests 181-201 行）从 required 越界入口（`parse_calendar_year(999, field_name="reporting_year")`）与 optional 非整数入口（`normalize_fiscal_year("2024", field_name="reporting_year")`）取实际异常，断言二者精确等于 `reporting_year 必须是 1000..9999 的整数`。
  - 未在 consumer 重建 message: read_runtime 与 read-runtime 测试本轮未改动（fix artifact 声明，且工作区 read-runtime 测试内容与 S1 初轮复核一致）。
- 对外文本不变核对: 最终文案仍为 `必须是 1000..9999 的整数`，与 S1 初轮已审状态及 plan §5 exact message 完全一致；生产代码 grep 确认 `1000..9999` 无任何 message 字面量残留（仅 docstring 文档性描述，非用户可见文案）。
- 验证: 既有 exact-message 测试（year reject 矩阵、normalize reject 矩阵、read-runtime fail-closed 矩阵）全部通过，新 owner-level 一致断言通过。

### DS-3 — full-date 非 str 防御无测试（controller: `accepted`）→ **已修复**

- Required fix 核对:
  - 生产签名未扩宽: 仍为 `parse_iso_calendar_date(value: str, *, field_name: str = "date") -> datetime.date`（`filing_semantics.py:368`）。
  - 绕过静态类型的 runtime 反例: `test_parse_iso_calendar_date_rejects_non_string_runtime_values`（tests 273-288 行）参数化 `None` 与整数 `2024`，经 `cast(str, value)` 调用 owner，断言统一 `ValueError` message `date 必须是实际存在的 YYYY-MM-DD 日期`。
  - 与既有 year owner 同类测试同模式（`test_parse_calendar_year_rejects_invalid_runtime_values` 使用 `cast(int, value)`），类型上 `JsonValue` 联合含 `None`/`int`，pyright 通过。
- 验证: 复现 coverage missing 列表不再包含非 str raise 行（当前 383 行已覆盖）；focused tests 与定向 pyright 全绿。

### DS-4 — 模块 docstring 未涵盖 calendar/year owner（controller: `accepted`）→ **已修复**

- Required fix 核对: `filing_semantics.py:1-7` 模块概览现声明「四位 fiscal/partial year、canonical Gregorian full-date」由本模块统一拥有 calendar/year 合法性，并保留原消费边界描述。
- Owner 边界核对: 概览未写入 upload/download wrapper-owned shape、partial inclusive expansion、错误投影、调度或上层治理语义；未把 full-date 公历域 `0001..9999` 与 fiscal/partial year `1000..9999` 混写为同一域。
- 验证: 描述与当前模块实际 owner API（`parse_calendar_year` / `parse_iso_calendar_date` / `normalize_fiscal_year`）及边界一致；纯文档变更，无行为变化。

## 用户指定核对项（新 range text 真源 / 外部 message / 非 str 测试与窄签名 / docstring 边界）

| 核对项 | 结论 | 直接证据 |
| --- | --- | --- |
| 新 range text 真源 | 通过 | `_CALENDAR_YEAR_RANGE_TEXT`（52 行）由 bounds 常量（46/49 行）f-string 派生；两处 raise（364/421 行）共用；生产 grep 无 message 字面量残留 |
| 外部 message 不变 | 通过 | 最终文本 `必须是 1000..9999 的整数` 与 S1 初轮已审状态、plan §5 exact message、read-runtime 断言（`fiscal_year 必须是 1000..9999 的整数`）三方一致 |
| 非 str 测试与窄签名 | 通过 | 测试用 `cast(str, value)` + `JsonValue` 参数绕过静态类型锁定 `None`/`2024`；生产签名保持 `value: str` 未扩宽（368 行）；coverage 383 行已覆盖 |
| 模块 docstring owner 边界 | 通过 | 1-7 行概览覆盖 calendar/year 职责且无上层语义泄漏；与 S1 初轮 DS-4 要求的表述范围一致 |

## 修订引入的新问题检查（adversarial pass）

沿真实代码路径逐分支走读修订后代码与测试，重点核对以下风险面：

1. **correctness**: `_CALENDAR_YEAR_RANGE_TEXT` 派生逻辑为纯 f-string 常量，无运行时分支；两入口 raise 行为与修复前逐字节一致（仅文案构造来源变化）。`parse_iso_calendar_date` 四条 raise 路径统一 message 不变。
2. **ownership**: 范围合法性仍只有 bounds 常量一份真源；文案真源在 owner 模块内；consumer（read_runtime）与测试均未重建 message、无 fallback/shim/第二真源。owner 唯一性复查：全仓生产 consumer 仍为 `read_runtime.py:627`（`normalize_fiscal_year`）、`filing_semantics.py:422`（`parse_calendar_year`）；`parse_iso_calendar_date` 无生产 consumer（S2/S3 planned，非 finding）。
3. **fail-closed 保持**: `_parse_source_document_meta` 三个调用点（read_runtime.py 2402/2422/2557 区域）仍只捕获 `FileNotFoundError`；2776 行的 `except (FileNotFoundError, ValueError)` 位于 `_diagnose_cross_document_locator` 的 processed_meta 读取路径，不在 `_parse_source_document_meta` 调用链上，owner `ValueError` 原样传播。
4. **type**: 定向 pyright 0 errors；新测试全部有完整中文 docstring、无 `Any`/`object`/未标注签名。
5. **test**: 新增 3 个 focused 测试（共享 message 断言 + 非 str 参数化 ×2）与 fix artifact 声明一致（95 → 98 passed）；无测试为适配实现而被削弱，无旧测试倒逼兼容分支。
6. **branch ordering / 不可达分支**: `parse_calendar_year` 的 bool 先于 int 检查顺序未变；非 str 防御分支现在有真实测试执行。
7. **docstring 中的 `1000..9999` 字面量**: 仅存在于函数 docstring 的契约性描述中，是文档而非对外 message；若 bounds 变更，docstring 属于契约变更的一部分需同步更新，不构成第二真源或漂移风险。

**结论: 未发现修订引入新的 correctness / ownership / type / test 问题。**

## Findings

未发现实质性问题。

说明：DS-2/DS-3/DS-4 的修复均落在 owner boundary（domain 模块 + owner contract 测试），未在 consumer/展示层/测试夹具引入 fallback、特例、重算或兼容分支；DS-1 按 controller 裁决保持原样。初轮四个低严重度 finding 在本次 re-review 中全部得到符合裁决要求的处理，且没有新的 blocking 或非 blocking finding。

## Open Questions

- 无。
- 说明：DS-1 的恒真防御分支仍属 controller 已裁决事项（`rejected-with-reason`），不是未分类风险。

## Validation（本 reviewer 复现结果）

所有命令均在仓库根目录 `source .venv/bin/activate` 后执行；未运行 `UF-PF04`；未修改任何文件、未 stage/commit。

| 命令 | fix artifact 声明 | 复现结果 |
| --- | --- | --- |
| `pytest tests/fins/test_fiscal_normalization_contracts.py tests/fins/test_read_runtime_semantic_ownership_guards.py -q` | exit 0; 98 passed, 3 warnings | **exit 0; 98 passed, 3 warnings**（一致；warnings 为 `.venv` 中 `edgar` deprecation，与改动无关） |
| 三文件 coverage 集合 + `coverage report --include='dayu/fins/domain/filing_semantics.py' --show-missing --fail-under=80` | exit 0; 211 passed; 141 stmts, 18 missed, 87% | **exit 0; 211 passed; 141 stmts, 18 missed, 87%**（一致；missing 精确为 `212, 238, 262, 266, 289, 311, 335-341, 396, 462, 468, 492, 515`——非 str 行 383 已覆盖，round-trip 行 396 保持未覆盖） |
| `python -m pyright <三文件>` | 0 errors, 0 warnings, 0 informations | **0 errors, 0 warnings, 0 informations**（一致） |
| `git diff --check` | exit 0，无输出 | **exit 0，无输出**（一致） |
| `git status --short` | 3 个 allowed 修改文件 + docs artifacts | 3 个 allowed 修改文件 + 5 个 docs artifacts（其中 `code-review-20260814-145012.md` 为并行另一路 re-reviewer 产物，本轮未读取） |

## Docs decision

- 与 controller adjudication 及 fix artifact 记录一致，维持不变：S1 不修改任何 README（accepted plan §10 S1 Non-goals；plan §13 把 `dayu/fins/README.md` 与根 `README.md` 更新留给 S3 在 consumer contract 稳定后进行）。
- DS-4 的模块 docstring 修正属代码内文档，不触发 README 更新。
- `tests/README.md`、`dayu/README.md`、Host/Engine README 不更新：无新增测试层级、分层装配无变化。

## Residual Risk

1. S2 upload strict admission 接线、S3 download shared-owner 接线与最终 README：`covered by later approved slice`，owner 分别为 `S2-upload-strict-static-admission`、`S3-download-shared-owner-and-closeout`；按用户指令不作为 S1 finding。
2. `UF-PF04` 真实 CLI evidence：`assigned to later work unit`，owner=`UF-PF04`；本轮未运行。
3. 其它 upload findings 与 `upload_filings_from` metadata strictness parity：`assigned to later work unit`，owner 各自既定 work unit。
4. DS-1 round-trip 防御行 396 保持不可达且未覆盖：controller 已 `rejected-with-reason`，属已裁决事项而非未分类风险。
5. `parse_iso_calendar_date` 尚无生产 consumer：S2/S3 接线时必须以直接调用该 owner 的方式消费（S2/S3 测试已有 delegation spy 计划锁定），避免 wrapper 自行解析。

## Conclusion

**Pass。**

UF-FIX04 S1 review fix 的独立 re-review 结论：

- DS-1 rejected 理由与实现保持一致（round-trip 防御按 accepted plan §5 原样保留，无伪造覆盖）。
- DS-2/DS-3/DS-4 均判定为**已修复**：range text 单真源派生且对外 message 逐字节不变、非 str 防御以 `cast` 绕过静态类型锁定且生产窄签名未扩宽、模块 docstring 覆盖 calendar/year owner 且无上层语义泄漏。
- 全部验证声明由本 reviewer 复现一致（98 passed、211 passed、87% coverage、pyright 0 errors、diff check clean）。
- 未发现修订引入新的 correctness / ownership / type / test 问题；无新的 blocking 或非 blocking finding。

## Next Entry Point

controller S1 acceptance（gate: `implementation review fix` 复核 → `S1 accepted`；后续 gate 为 S2 implementation）。
