# Code Review

## Scope

- Mode: PR
- PR: 190
- Title: `fix(cli): close interactive conformance gaps`
- Author: `noho` (Leo Liu)
- Head: `codex/interactive-oracle` (`dcc08399`)
- Base: `main`
- State: OPEN + DRAFT, no review requests, no checks reported
- Mergeable: true, rebaseable: false
- URL: https://github.com/noho/dayu-agent-r/pull/190
- Output file: `docs/reviews/pr-190-review-20260807-152226.md`
- Included scope: 全部 745 个变更文件（+212,069 / -17,158），重点复核：
  - F17 publication digest closure（2 产品 hunk）
  - F17 四次 Gateflow commits（`0d215296..dcc08399`）
  - F13-F16 evidence correction provenance 与 owner 语义
  - 核心 compaction 模块（compact_structure.py、compaction_terminal.py、context_event_payload.py）
  - CLI/session 模块（session_execution.py、composer.py、run_keys.py）
  - Engine contract（structured_output.py）
  - dispatch.py pre-start governance flight
- Excluded scope: docs/gateflow/ 全部 evidence 文件（仅作 evidence provenance 交叉复核，不做逐行 review）
- Parallel review coverage: 3 个 subagent 分别覆盖：
  - F13-F17 Gateflow evidence 与 cli_ci.md design truth
  - 核心 compaction 模块 owner 语义边界
  - CLI/session/dispatch/structured_output 变更

## Findings

### 001-未修复-低-PR 无 CI checks 报告

- **入口/函数**: GitHub PR status check rollup
- **文件(行号)**: PR 190 checks 页面
- **输入场景**: 任何 push 到 `codex/interactive-oracle` 分支
- **实际分支**: `statusCheckRollup` 为空数组
- **预期行为**: 至少应有 pyright、Ruff、compileall 或 pytest 等基础 checks 报告
- **实际行为**: `gh pr checks 190` 返回 "no checks reported on the 'codex/interactive-oracle' branch"；无任何 CI 产出
- **直接证据**: `gh pr checks 190` exit code 1，stderr 为 "no checks reported"；`gh pr view 190 --json statusCheckRollup` 返回 `[]`
- **影响**: CI gap — 无法通过 CI 信号确认 pyright/Ruff/compileall/pytest 在远端通过。当前 review 基于本地证据（gateflow evidence 中记录的 exit 0 命令），但远端未验证
- **建议改法和验证点**: 配置分支保护或 CI workflow，使 `codex/interactive-oracle` 分支的 push 触发基础 checks；或在 merge 前手动确认远端 CI 通过
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低 — 不影响产品代码正确性，但影响 merge 前的信心

### 002-未修复-低-context_event_payload.py 仅覆盖 COMPACTED terminal 的 bounded storage

- **入口/函数**: `store_context_compacted_payload` / `resolve_context_compacted_payload`
- **文件(行号)**: `dayu/host/context_event_payload.py` (141 行)
- **输入场景**: CONTEXT_COMPACTION_FAILED terminal 的 payload 写入
- **实际分支**: `context_event_payload.py` 只处理 CONTEXT_COMPACTED terminal；CONTEXT_COMPACTION_FAILED 走 `payload_object` + `validate_context_compaction_failed_payload` 直接 inline 路径
- **预期行为**: 若 failure payload 未来膨胀超过 inline threshold，应有 bounded storage 路径
- **实际行为**: 当前 failure payload 结构固定且体积小，inline 路径足够；但无显式 fallback
- **直接证据**: `context_event_payload.py` 的 `store_context_compacted_payload` 只接受 `CONTEXT_COMPACTED` event type；`compaction_terminal.py` line 203-213 对 FAILED terminal 调用 `payload_object` + 直接 inline validation
- **影响**: 当前无实际影响（failure payload 结构固定）；未来若 failure payload 扩展，可能在写入时因超限而失败
- **建议改法和验证点**: 确认 failure payload 的结构约束已在其 schema 中固化；若未来扩展，需同步增加 bounded storage 路径
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低 — 当前行为正确，仅作前瞻性观察

### 003-未修复-低-compact_structure.py 缺少专属测试文件

- **入口/函数**: `parse_compact_candidate_v4`、`compact_output_json_schema_v4`、`compact_output_template_v4`、`compact_output_prompt_rules_v4`、`compact_output_json_schema_digest_v4`
- **文件(行号)**: `dayu/host/compact_structure.py` (819 行)
- **输入场景**: 模块级测试覆盖
- **实际分支**: 测试覆盖通过 `tests/host/test_compaction_contract.py` 和 `tests/host/test_llm_compaction.py` 间接提供
- **预期行为**: 新增 819 行的结构 owner 模块应有专属测试文件明确声明测试所有权
- **实际行为**: 无 `tests/host/test_compact_structure.py`；5 个公共函数的测试散落在两个间接测试文件中
- **直接证据**: `rg --files | grep test_compact_structure` 返回空；`test_compaction_contract.py` import `compact_structure` 并测试全部 5 个函数
- **影响**: 测试所有权不明确；若 `test_compaction_contract.py` 被重构，可能意外丢失 `compact_structure.py` 的覆盖
- **建议改法和验证点**: 考虑创建 `tests/host/test_compact_structure.py` 显式声明测试所有权
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低 — 当前覆盖充分，仅作 maintainability 建议

## Open Questions

- 无。

## Residual Risk

1. **CI gap**：PR 无任何远端 CI checks 报告。当前 review 基于 gateflow evidence 中记录的本地验证（pyright exit 0、Ruff exit 0、compileall exit 0、pytest 71/71 pass），但远端未独立确认。
2. **三个 formal replacement scenarios 未裁决**：F13-F17 均明确声明三个 Oracle replacement scenarios（unsupported correction、cap typed reject/repair、stale/late result）为 `unadjudicated`，等待用户驱动的 Oracle 闭环。这不是未修复的 code defect，而是已知的后续工作。
3. **Oracle readiness 仍为 `pending-user-adjudication`**：F14 real observation 确认 FY2025 correction 在 EventLog sequence 201、first accepted compact at 245 未覆盖、second at 515 覆盖，但 formal replacement scenario 未裁决。
4. **REPL 循环组合复杂度**：`session_execution.py` 的 interactive REPL 驱动逻辑（约 300 行）包含 phase/state/cancel/barrier 四层状态管理的嵌套组合。当前每层职责清晰且无 owner 语义冲突，但整体组合复杂度建议后续在架构文档中画出状态转移图。
5. **`resolve_explicit_config_dir` 从 session_execution.py import 中移除**：需确认无残余调用方依赖此符号。
6. **dispatch.py reconciliation 路径 lease 竞争**：reconciliation 改为先释放 lease 再进入 pre-start governance flight，flight 自行获取 fresh lease。当前实现中 flight 的 sole-flight 合并机制和 `asyncio.shield` 保护逻辑正确，但 lease 生命周期从单次扩展为两次，需确认无极端时序下的竞争。

## Gateflow Evidence Provenance 交叉复核

### F13-F16 Owner 语义与 F17 是否冲突

**结论：无冲突。F17 与 F13-F16 操作在完全正交的领域。**

| 维度 | F13-F16 | F17 |
|------|---------|-----|
| 领域 | Host compaction evidence provenance、frontier coverage、canonical views、CLI evidence classification | Frozen workspace publication manifest digest synchronization |
| Owner chain | Host 语义 owner → claim/provenance atoms → `compacted_source_refs` → canonical renderer → EventLog terminal classifier | Prompt bytes (producer) → manifest entry (consumer) → manifest raw bytes (producer) → test pin (consumer) |
| 变更范围 | `dayu/host/`、`dayu/cli/`、compaction schema v3→v4 | 2 个单行 SHA-256 替换 |
| 保护目标 | EvidenceFact provenance integrity across compaction rounds | Publication tree audit integrity for init workspace |

F17 的 `conversation_compaction_user.md` 是 prompt 文件（LLM-facing text），不是 compaction evidence artifact。Manifest entry 追踪其 raw bytes digest 用于 publication validation。F13-F16 的 Host 内部运行时机制（EvidenceFact provenance chain、`compacted_source_refs` frontier、canonical renderer、EventLog terminal classifier）与 init workspace publication manifest 完全独立。

### F17 四次 Gateflow Commits 一致性

**结论：完全一致。**

1. `0d215296` — F17 plan acceptance（8 个 gate evidence 文件，0 产品变更）
2. `305c1012` — F17 implementation（2 产品 hunk + 4 gate evidence 文件）
3. `33f6c16d` — F17 aggregate review（6 gate evidence 文件，0 产品变更；发现并修复 F17-A1）
4. `dcc08399` — F17 evidence formatting fix（1 个 trailing space 修复 + 2 个 verification 文件）

四次 commit 形成连贯的 plan → implementation → aggregate review → formatting fix 流水线。所有 digest values、owner declarations、gate decisions、scope boundaries 跨 commit 对齐。

### F17-A1 Finding 闭合确认

- Finding：aggregate artifact 误用上一 gate 的 working-tree 状态（`git diff HEAD` 描述已提交的变更）
- 裁决：`accepted`，evidence correctness，非产品 defect
- Fix：AgentCodex 修正 artifact 的 aggregate diff/untracked 描述
- Re-review：AgentDS（`code-review-20260807-150638.md`）和 AgentMiMo（`code-review-20260807-150646.md`）均确认 F17-A1 完整闭合，remaining finding 0
- **闭合状态：完整闭合**

### F17 Publication Truth 确认

- Prompt raw SHA-256：`22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`
- Manifest entry：`22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`（一致）
- Manifest raw SHA-256：`064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22`
- `FROZEN_MANIFEST_SHA256`：`064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22`（一致）
- Fresh production `InitMode.FIRST` audit：`valid=true`、`issues=()`
- Inventory：5 directories / 43 files / 16 model pointers
- Owner suite：71/71 passed
- **Publication truth 完全闭合**

## Review 结论

**未发现实质性产品 code defect。**

PR 190 的 F17 publication digest closure 实现正确——精确 2 个单行 hunk，owner chain 清晰，full validation 通过。四次 Gateflow commits 完全一致。F13-F16 的 evidence provenance owner 语义与 F17 无冲突。核心 compaction 模块（compact_structure.py、compaction_terminal.py、context_event_payload.py）的 owner 语义边界清晰，无重叠。CLI 模块（composer.py、run_keys.py、session_execution.py）的接口边界正确。Engine contract（structured_output.py）与现有模式一致。

唯一需要关注的是 PR 无远端 CI checks 报告（001），以及三个 formal replacement scenarios 未裁决（已知后续工作，非 code defect）。
