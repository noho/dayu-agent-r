# F20 RR6 最终独立计划复审

## Reviewed target and scope

- Work unit：F20；review round：RR6。
- Reviewer：AgentDS；未读取、交换或依赖 AgentMiMo RR6 artifact/意见。
- Target plan：`docs/gateflow/pr-190-f20-plan-20260808.md`，SHA-256
  `66074bc59b468c2614e14b7e6840a39b45d09aac9e4454dbff576550ac8b27f7`。
- Plan-review fix SHA-256：`83fe517bd60534429abced41289203ec1bc78bc78db14b84cb57b84946a6f743`。
- Binding goal SHA-256：`9e537fb663e647dccb702056316ad7a9a79c15a14149f8f222c76b289a9c67e0`。
- RR5 Controller adjudication SHA-256：
  `a1926e2c231f8ece5c1a9e8f5aeefb8380f390a7ea12e2bf58293252441e7f0f`。
- Design truth SHA-256：Host
  `7e488b882b4cf739fb3f2f8e66627fe27e7ce79fb2624c6e7a5cd46e4035dc25`，Engine
  `b190e3a8ee2df84d29546ca04d4fb7d81a73877b27a3bddd04d2aaa40db17b1e`。
- 执行边界：provider-free、network-free只读docs/code核验；未修改plan/fix/goal、产品、oracle/scenario、F18/F19，未调用provider，未读取或触碰明确排除的review文件。

## Assumptions tested

1. RR5 finding是否真正关闭：R2/R3 pre-compact compact trigger只依赖产品实际持久化的attempt-free canonical budget cursor、logical projection ref、input digest、stage/prediction/action及request/同operation terminal，不再要求不存在的Host runner manifest、sizing snapshot或Run/Attempt/execution/dispatch identity。
2. provider-free proof如需完整candidate，是否只允许封闭run-owned proof projection，并以budget identity精确绑定；该projection是否被禁止冒充Host manifest或进入formal private/publication schema。
3. 每次accepted后的`post_compact`与R4`ordinary` allow是否仍要求真实Host manifest和完整identity equality；错stage/ref/digest/action与伪造precompact manifest是否逐项fail closed。
4. 双typed port proof、OS process-tree deny与parent-only audit、Host Tool Trace/storage closure是否因RR5修订退化。
5. formal stock production CLI/default factory/production compactor、材料与clean-seed owner、三链调用预算、deadline discriminated union及publication last-writer契约是否仍code-generation-ready。

## Evidence and adversarial results

### RR5 closure与stage语义所有权

- `dayu/host/dispatch.py:1984-2009`直接证明ordinary只有`ALLOW_DISPATCH`才进入`_commit_dispatch_candidate_in_transaction`；compact/hard分支只写`attempt_id=None`、`execution_id=None`的canonical budget fact。
- `dayu/host/dispatch.py:2815-2888`直接证明runner-call manifest、complete sizing snapshot、Attempt/execution/dispatch identity只由allow commit owner原子创建；`dayu/host/context_events.py:103-132`的budget payload拥有cursor、logical projection ref、input digest、stage、policy/estimator、prediction、threshold、pressure与action，但不承诺runner manifest。
- Plan §5.2把R2/R3 trigger精确限制为上述attempt-free budget truth及request/同operation terminal linkage，并明确禁止该compact分支存在或声称Host manifest、sizing snapshot、Attempt/execution/dispatch。§5.5的formal closed variant也强制这些字段absent/null。
- 完整candidate审计被隔离为`projection_kind="run_owned_precompact_candidate_proof"`：只在provider-free proof中使用，以cursor/logical projection ref/input digest与budget fact逐字段相等，显式`host_runner_manifest=false`；计划禁止其进入formal private/publication schema或被stock CLI引用补证。public publication只消费canonical `trigger-proof.json`，不发布该proof-only full projection。

### Post-compact与R4 allow未退化

- `dayu/host/dispatch.py:2539-2679`显示accepted后先追平Memory，再以`POST_COMPACT`重建candidate；allow继续进入同一manifest/start commit，hard则在Attempt/dispatch前关闭。
- Plan仍要求每个accepted terminal后的唯一`post_compact`budget/manifest/candidate直接绑定accepted truth、Memory snapshot与后续Run/Attempt/execution/dispatch；ordinary trigger digest不得替代consumption。
- R4仍需fresh reconnect的独立`ordinary` candidate、真实Host manifest、第二次accepted truth、`allow_dispatch`及operation count=2共同成立。R4 soft/第三operation只能在既有cap/deadline内到terminal后seal为`needs-more-evidence`，不得冒充成功reconnect。
- Corruption matrix明确拒绝四类错误：伪造precompact Host manifest或non-null Attempt/execution、ordinary/post_compact refs互换、precompact digest冒充accepted consumption、R4 compact/第三operation冒充reconnect。

### Proof、formal与publication边界

- `HostLocalExecutionOptions`直接拥有`worker_factory`与`context_compactor`；`HostDispatchScheduler.open`消费同一typed options与transaction runner。计划继续要求两份disjoint ledger分别与allow-only ordinary manifests及compactor proposals/attempts/terminals精确对账，禁止`open_host`、direct SQL/store和ledger交叉。
- `/usr/bin/sandbox-exec`当前SHA-256仍为
  `8290e4be7387a0df83cd1559e86afd880464f269450573d012795761fe298f16`。OS sandbox仍是process-tree唯一deny owner；OS-only matrix只用parent/spawned-child numeric TCP/UDP的`EPERM`与no-contact证明继承。parent audit只拥有parent outbound ledger，三个resolver event均须先ledger再抛run-owned typed exception；无child hook/allowlist，也不解析`resource_tracker`、`spawn_main`、pipe/pickle、argv/PID/order。
- Production Service assembly仍显式构造`DefaultLocalEngineWorkerFactory()`，interactive CLI直接消费assembly并调用public`open_host`；formal继续禁止wrapper/interception/custom opener/endpoint或profile override，只在segment结束后接受canonical evidence。
- 材料只经`dayu.fins.storage`repository owner重算；未公开range保持`not_exposed_by_storage_owner`，clean seed与F18/F19 bundle有same/ancestor/descendant/symlink fail-closed gate。Host tool request/result/canonical terminal与storage identity必须闭合，失败不得direct/in-process fallback。
- 调用预算保持Chain 01=`99`、Chain 02/03各`73`、总计`245`；第三operation已包含在Chain 01上限内。global/allocation/attempt-only active deadline owner及`attempted`/`provider_not_started`封闭union未退化。
- Publication仍从private typed truth独立复算budget/count/terminal/deadline，要求terminal双向穷尽、逐chain Tool Trace、final-byte SHA、`secret-scan.json`最后且唯一public writer及scan后zero writes；stage-boundary corruption与publication corruption都要求provider spawn保持0。

## Findings

没有material findings。RR5唯一High finding已按产品真实owner contract关闭，未通过proof artifact、formal analyzer或publication schema发明下游兼容事实。

## Open questions

没有blocking open question。当前plan已把尚未由执行证明的事项放到正确gate：真实materials能否形成两个soft<hard trigger、caps candidate能否通过production parser/estimator、OS sandbox与process-backed Fins tool能否在proof中共同成立，均由Slice 1 fail-closed验证，而不是plan预设。

## Residual risks and tracking

- `intent_type`/`reason`等未治理字符串仍缺少universal finite serialized upper owner；计划只证明固定bounded canonical metadata下的场景存在性，不外推任意accepted output上界。
- 真实MiMo是否自然产生repair-accepted、budget-exhausted fallback，以及R4是否保持operation count=2，仍是formal observation residual；不得output injection、重跑或重标。
- 若Slice 1发现材料、candidate、network isolation、双port ledger或storage closure任一不成立，必须在provider start=0处停止并回到Controller裁决，不能以产品修改或external fallback补偿。
- 本PASS只覆盖冻结plan revision的code-generation readiness；不构成B2用户裁决、Oracle acceptance或overall readiness。

## Final plan review conclusion

二值结论：`PASS`。

冻结RR6 revision已把pre-compact trigger恢复到产品真实持久化边界，并保留post-compact/R4 allow的真实manifest与identity义务；双port proof、OS deny/parent audit、formal stock CLI、storage、deadline和publication契约均未退化。计划达到进入Slice 1所需的code-generation-ready程度，但只有同一revision的另一独立RR6也为PASS且Controller完成gate裁决后，才可进入实现；本review不授权provider、formal observation或B2状态变更。
