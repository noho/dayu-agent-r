# Code Review — WU-CM-14 Aggregate Focused Re-review

## Scope

- Mode: current changes (focused re-review of dead-code removal after aggregate deepreview)
- Branch: wu-cm-14-final-answer-preservation
- Base: d4b271cb (accepted plan commit)
- Output file: docs/reviews/code-review-20260619-193352.md
- Included scope: `dayu/host/engine_ingest.py` — 删除 `_current_only_material_blocks` 死代码（23 行）
- Excluded scope: 已 committed 的 WU-CM-14 完整实现（commit `921c6219`）— 已在先前 aggregate deepreview 中完成逐链路走读
- Parallel review coverage: 无
- Prior review artifacts:
  - `docs/reviews/code-review-20260619-192740.md` — aggregate deepreview（adversarial pass）
  - `docs/reviews/code-review-20260619-193018.md` — aggregate deepreview（独立 compliance pass）

## Controller Adjudication 响应

### ACCEPT: 192740 Finding 1 — `_current_only_material_blocks` 死代码删除

**核实结论：已关闭。**

逐项验证：

- `grep -rn "_current_only_material_blocks" dayu/ tests/` → 零匹配。函数定义已从 `engine_ingest.py` 完整移除
- `grep -rn "_current_input_anchor_material_block" dayu/host/engine_ingest.py` → 仍存在于 `engine_ingest.py:4030`，且被 `_frozen_reactive_material_blocks` 在 `engine_ingest.py:4022` 调用。保留正确
- Diff 仅包含 23 行删除，零行新增，无其他文件改动
- `python -m pyright dayu/ tests/ utils/` → 0 errors, 0 warnings, 0 informations
- `pytest tests/host/test_run_input_builder.py tests/host/test_compact_material.py tests/host/test_dispatch_scheduler.py -q` → 220 passed
- `git diff --check` → clean

死代码删除不改变任何生产行为或测试行为。无新增风险。

### REJECT as nonblocking: 193018 F1 — reactive compact-success recovery dispatch scheduler E2E 直断言缺失

**独立复核：同意 controller REJECT。**

核实链路：

1. 193018 F1 声称 reactive compact-success recovery dispatch 路径缺少 scheduler E2E 级别的 Engine messages 直断言。
2. Accepted plan §5.2 允许替代覆盖路径："If a full scheduler-level reactive E2E is not used, the focused test must explicitly construct the recovery Attempt state and call the same public/internal builder path used by dispatch."
3. Proactive 路径的 `test_post_compaction_ordinary_build_preserves_protected_recent_raw_tail` 使用 recovery Attempt（`_start_recovery_attempt`）调用 `_build_post_compaction_request` → `RunInputBuilder.build()` no-fallback 分支，覆盖了 `_DurableProtectedRecentRawTailProvider` 的完整 provider → build → Engine messages 路径。
4. Reactive recovery dispatch 走的是**同一个** `RunInputBuilder.build()` 实现、**同一个** `_DurableProtectedRecentRawTailProvider`、**同一个** no-fallback 分支。Plan 的 equivalence proof（§5.2）已声明："reactive compact-success dispatch still creates a new Attempt for the same Run and reaches ordinary `RunInputBuilder.build()`."
5. Reactive 路径特有的行为（frozen material assembly、compact selection floor）已在 `test_reactive_root_compact_selection_passes_protected_recent_floor` 中被直接验证——包括 frozen material block kind composition（USER_INPUT / ASSISTANT_FINAL_ANSWER / ACCEPTED_TOOL_EVIDENCE）和 selection `excluded_reason_codes["protected_recent_raw_floor"]`。

**结论：Controller REJECT 有充分 direct evidence 支撑。193018 F1 不符合 blocker 标准——现有测试覆盖满足 accepted plan 的替代覆盖条款。不持异议。**

## Findings

未发现实质性问题。死代码删除精确、干净，无副作用。

## Open Questions

无。

## Residual Risk

| ID | Risk | Severity | Notes |
|---|---|---|---|
| WU-CM-14-RR-1 | EventLog 双读 | 低 | deferred to WU-CM-13，不影响 WU-CM-14 correctness |
| WU-CM-14-RR-2 | content_digest 规范化不一致 | 低 | source-ref/evidence-ref dedupe 可靠兜底 |

（与先前 aggregate review 一致的 deferred risk，死代码删除不引入新 risk）

## Validation

独立复跑：

- `grep -rn "_current_only_material_blocks" dayu/ tests/` → no matches
- `pytest tests/host/test_run_input_builder.py tests/host/test_compact_material.py tests/host/test_dispatch_scheduler.py -q` → 220 passed
- `python -m pyright dayu/ tests/ utils/` → 0 errors, 0 warnings, 0 informations
- `git diff --check` → clean
- Diff scope: `dayu/host/engine_ingest.py` only, 23 deletions, 0 additions

## Verdict

**PASS.** Accepted dead-code finding (192740 Finding 1) closed with direct verification. Controller's REJECT of 193018 F1 confirmed as well-founded — proactive focused test provides equivalent coverage per accepted plan §5.2 alternative path. No new findings, no new risk. Dead-code removal is clean, minimal, and verified by full test suite + pyright.
