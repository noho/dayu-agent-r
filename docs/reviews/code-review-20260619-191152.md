# Code Review

## Scope

- Mode: current changes
- Branch: wu-cm-14-final-answer-preservation
- Base: d4b271cb (accepted plan commit)
- Output file: docs/reviews/code-review-20260619-191152.md
- Included scope:
  - `dayu/host/run_input.py` — protected recent raw tail provider/view、dedupe、LLM-facing evidence source filter
  - `dayu/host/dispatch.py` — proactive compact selection floor 传递、RunInputBuilder 构造注入
  - `dayu/host/engine_ingest.py` — reactive frozen material assembly 修复、reactive compact selection floor 传递
  - `tests/host/test_run_input_builder.py` — post-compaction raw tail、dedupe、skip 条件
  - `tests/host/test_dispatch_scheduler.py` — proactive/reactive compact selection floor
- Excluded scope: `dayu/host/compact_material.py` 未修改（复用已有 helper）；`tests/host/test_compact_material.py` 无变更
- Parallel review coverage: 无

## Findings

### 01-未修复-低-Reactive frozen material assembly 缺少显式 stop-condition 断言

- **入口/函数**: `_frozen_reactive_material_blocks()` in `dayu/host/engine_ingest.py:4005-4027`
- **文件(行号)**: `tests/host/test_dispatch_scheduler.py` — 无直接测试
- **输入场景**: post-compact delta material 包含至少两个 turn group（user prompt + assistant final answer + accepted evidence），`selected_recent_window_turn_floor >= 1`
- **实际分支**: `test_reactive_root_compact_selection_passes_protected_recent_floor` 通过 `excluded_reason_codes` 间接验证 floor 生效；`test_reactive_overflow_recovers_and_dispatches_new_attempt` 间接驱动完整 reactive overflow → compact → recovery 路径
- **预期行为**: Plan §5.9 要求 "Assert `_frozen_reactive_material_blocks(...)` produces material blocks for the most recent protected turn group(s), including user prompt, assistant final answer, and accepted readable tool evidence" 以及 "Assert it still appends exactly one current input anchor and does not treat that anchor as historical material"
- **实际行为**: 现有测试只断言 compact selection 的 `excluded_reason_codes`，不断言 frozen material blocks 的实际内容
- **直接证据**: `tests/host/test_dispatch_scheduler.py:5675-5731` — `test_reactive_root_compact_selection_passes_protected_recent_floor` 只检查 `request.segment_selection.excluded_reason_codes` 和 `selected_block_ids`，不检查 `pending.frozen_material_blocks`
- **影响**: 若 `_frozen_reactive_material_blocks` 回退到只产出 current input anchor（WU-CM-14 修复前的行为），现有测试不会失败；regression 风险中等
- **建议改法和验证点**: 在 `test_reactive_root_compact_selection_passes_protected_recent_floor` 或新增测试中，通过捕获 `_ReactiveCompactPending.frozen_material_blocks` 或注入 spy 验证 frozen blocks 包含 protected turn-group material（至少 user input、assistant final answer、accepted evidence 三种 kind）且恰好包含一个 current input anchor
- **修复风险（低/中/高）**: 低 — 只加测试断言
- **严重程度（低/中/高/严重）**: 低 — 实现路径已被 scheduler E2E 间接覆盖，但缺少回归守门

### 02-未修复-低-DurableProtectedRecentRawTailProvider 二次读取 EventLog

- **入口/函数**: `_DurableProtectedRecentRawTailProvider._load_protected_recent_raw_tail_tx()` in `dayu/host/run_input.py:1456-1504`
- **文件(行号)**: `dayu/host/run_input.py:1474-1486`
- **输入场景**: 任何 post-compaction ordinary dispatch
- **实际分支**: `_latest_compacted_event_before_attempt` 查询 EventLog 获取 compacted event → `_validate_loaded_compact_view_matches_event` 校验 artifact ref/digest → `build_pre_dispatch_compact_material_view` 再次查询 EventLog 构建 material view
- **预期行为**: Plan §4.2 允许 "Re-reading EventLog in this provider is acceptable for WU-CM-14"
- **实际行为**: `DurableCompactArtifactProvider` 已在前一个独立 read transaction 中读取并校验了同一个 compacted event；`_DurableProtectedRecentRawTailProvider` 在自己的 read transaction 中重复相同查询
- **直接证据**: `run_input.py:1474` 调用 `_latest_compacted_event_before_attempt`，`run_input.py:1483` 调用 `_validate_loaded_compact_view_matches_event`；`run_input.py:1636` 的 `DurableCompactArtifactProvider._load_compact_artifact_tx` 执行相同查询
- **影响**: 每次 post-compaction dispatch 多一次 EventLog SELECT + read；短 transaction 内无并发风险，但增加 dispatch latency
- **建议改法和验证点**: WU-CM-13 跟进时考虑将已构建的 `PreDispatchCompactMaterialView` 通过 dispatch state 传递，避免二次读取；当前 WU-CM-14 不阻塞
- **修复风险（低/中/高）**: 低 — 架构优化，不影响正确性
- **严重程度（低/中/高/严重）**: 低 — Plan 已标记为 WU-CM-14-RR-3

## Open Questions

无。

## Residual Risk

- Reactive frozen material assembly stop-condition 缺少显式测试断言（Finding 01），但 scheduler E2E 间接覆盖了主要路径。
- `_DurableProtectedRecentRawTailProvider` 二次 EventLog 读取（Finding 02）在 WU-CM-14 范围内可接受，WU-CM-13 跟进时统一优化。
- `_llm_facing_evidence_source_text` 的 prefix-based 过滤依赖上游 source text 格式稳定；若新增内部 prefix 需同步更新 `_INTERNAL_EVIDENCE_SOURCE_PREFIXES`。
