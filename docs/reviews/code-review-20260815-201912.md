# Code Review (Re-Review)

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `9d0a2f8a`
- Output file: `docs/reviews/code-review-20260815-201912.md`
- Included scope: Slice 2 全部 9 文件（同首审）
- Excluded scope: Slice 1 owner、Service、Host、Engine、runtime、storage、processor、Docling、renderer、README、registry、oracle、frozen evidence
- Review inputs:
  - `docs/reviews/code-review-20260815-200314.md`（MiMo 首审：pass）
  - `docs/reviews/code-review-20260815-201037.md`（DS 首审：1 low）
  - `docs/gateflow/uf-fix07-slice2-review-fix-20260815.md`（主控 fix gate）
- Parallel review coverage: 无

## DS Finding 1 Fix 复核

### Fix 数据流验证

修复后的唯一数据流：

```text
project_fins_upload_format_text()
  -> FinsUploadFormatTextProjection.upload_tool_material_primary_failure
       = "upload_kind=material 不得提供 primary；请省略 primary 字段"
       |
       +-> upload_tool_primary schema description 内嵌同一字段（L641-642）
       |     assert f"{failure}。" in tool_primary_text  (test L344)
       |
       +-> _upload_primary_selectors_from_arguments() ValueError(同一字段) (L389)
       |     assert str(error) == FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure
       |     (test L1555-1557)
       |
       +-> FinsUploadToolCallable ValueError -> ToolFailedOutcome.result.message (L119)
             assert outcome.result.message == FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure
             (test L1600-1602)
```

**验证结果：完整单真源闭环。** failure 文本仅在 `project_fins_upload_format_text()` 中产生一次，
schema description、adapter ValueError 和 ToolFailedOutcome.message 三处机械消费同一 projection 字段。

### Fix 新增内容逐项检查

| 检查点 | 证据 | 结论 |
| --- | --- | --- |
| 文案双真源 | `upload_tool_material_primary_failure` 仅在 `project_fins_upload_format_text()` L614-616 产生一次；schema L641-642 用 `f"{failure}。"` 内嵌；adapter L389 直接引用；test L344/1555/1600 三处 exact assertion | 无双真源 |
| adapter fallback | `_upload_primary_selectors_from_arguments` L388-389：`if upload_kind == _UPLOAD_KIND_MATERIAL: raise ValueError(FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure)`——条件精确，无 fallback 分支 | 无 fallback |
| hint 漂移 | `_INVALID_ARGUMENT_HINT` L58-60 含 `primary`；callable L123 使用该常量；test L1603-1605 exact 字符串断言 | hint 与常量同源，无漂移 |
| schema/tool outcome 不一致 | schema `upload_tool_primary` 内嵌 failure 文本（L641-642）与 ToolFailedOutcome.message（L119 via L389）来自同一 projection；test L1600-1602 断言 message == projection field | 一致 |
| 新 blocker | fix 范围限于白名单文件；未修改 CLI 行为、Slice 1 owner、Host/Engine/runtime/storage | 无新 blocker |

### 旧版 vs 修复版对比

| 维度 | 旧版（首审时） | 修复版 |
| --- | --- | --- |
| material-primary failure 文本 | 硬编码英文 `"primary must be omitted for material uploads"` | typed projection `FINS_UPLOAD_FORMAT_TEXT.upload_tool_material_primary_failure` 中文 |
| schema 内嵌 | `"upload_kind=material 不使用 primary，必须省略。"` | `f"{upload_tool_material_primary_failure}。"` 同源 |
| hint | 不含 `primary` | `_INVALID_ARGUMENT_HINT` 含 `primary` |
| 测试 | 仅 `pytest.raises(ValueError, match=...)` | exact message + hint + 零副作用 + workspace snapshot |

## 原全部结论复审

### Finding 1-11 Gateflow Final Status

| # | 原结论 | DS Finding | Final Status | 说明 |
| --- | --- | --- | --- | --- |
| 1 | `--primary` occurrence 全保留、append 不吞重复 | 无 | **confirmed** | 不变 |
| 2 | CLI `--primary` 只注册在 `upload_filing` | 无 | **confirmed** | 不变 |
| 3 | raw `primary_selectors` 到 static validator 生效链 | 无 | **confirmed** | 不变 |
| 4 | tool schema `primary` optional string / `files` maxItems=100 | 无 | **confirmed** | 不变 |
| 5 | filing 0/1 selector、material primary fail closed | DS-1 accepted | **confirmed + fixed** | material fail closed 的 failure 文本已归属 typed projection owner |
| 6 | invalid combos 在 state/job/mutation 前失败 | 无 | **confirmed** | 不变 |
| 7 | CLI help 与 tool schema 机械消费唯一文本 owner | DS-1 accepted | **confirmed + fixed** | schema description 现在内嵌同一 failure projection |
| 8 | LLM-facing 文本自足且无内部术语 | 无 | **confirmed** | 不变 |
| 9 | 无首项/顺序推断 | 无 | **confirmed** | 不变 |
| 10 | schema 没有 Host/Engine drift | 无 | **confirmed** | 不变 |
| 11 | 严格类型/docstring/tests | DS-1 accepted | **confirmed + fixed** | 新增 typed projection field、exact assertion、workspace snapshot |

### DS Finding 1 Final Status

**`accepted` → `fixed` → `re-verified`。** 主控修复完整：failure 文本归属 `FinsUploadFormatTextProjection.upload_tool_material_primary_failure`；
schema description 与 adapter ValueError 机械复用同一字段；hint 已包含 `primary`；ToolFailedOutcome exact message/hint 和零副作用测试通过。
未引入文案双真源、adapter fallback、hint 漂移或 schema/tool outcome 不一致。

## 新引入问题检查

| 检查点 | 证据 | 结论 |
| --- | --- | --- |
| 文案双真源 | `upload_tool_material_primary_failure` 仅在 `project_fins_upload_format_text()` 产生一次 | 无 |
| adapter fallback | `_upload_primary_selectors_from_arguments` 条件精确，无 fallback | 无 |
| hint 漂移 | `_INVALID_ARGUMENT_HINT` 模块级 `Final[str]`，test exact assertion | 无 |
| schema/tool outcome 不一致 | schema 内嵌 failure 文本 = adapter ValueError = outcome.message，三处同一 projection | 无 |
| 新 blocker | fix 限于白名单文件，未触碰 CLI/Slice 1/Host/Engine | 无 |

## 主控明确边界（不扩修）

以下由 fix gate 明确标注为 `assigned to later work unit`，本轮不扩修：

- 既有 delete+files adapter 英文消息（非 Slice 2 新增路径）
- schema `files.maxItems=100` 与 ingestion 上限的 owner design
- Slice 3 asset/storage/process 行为
- README 同步与全量 closeout
- UF-FIX08/10/11、registry/evidence 和真实 scenarios

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- 同首审：Slice 3/4 不在本 slice 范围；`--primary` CLI append 允许任意次数由 static validator 拒绝。
- fix 新增的 `_INVALID_ARGUMENT_HINT` 是所有 `ValueError` catch 的通用 hint，不区分具体 failure 原因；当前所有 `ValueError` 路径的 failure message 已足够自足，hint 仅作补充建议，不构成 blocker。
