# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: `main`（实际 diff 取自 HEAD 未暂存变更，因为该 branch 上 committed 内容为 plan artifact，implementation 为 unstaged working tree changes）
- Output file: `docs/reviews/code-review-20260612-143730.md`
- Included scope:
  - `dayu/host/llm_compaction.py`（未暂存变更）
  - `tests/host/test_llm_compaction.py`（未暂存变更）
  - `docs/reviews/wu-cm-05-s1-implementation-report.md`（只读参考）
- Excluded scope:
  - `docs/host/issues-implementation-control.md`：Controller bookkeeping dirty file，只核对状态
  - Accepted plan commit `153c43e3` 及 plan review artifacts：已通过 plan gate
- Parallel review coverage: 无，单人走读全部变更路径

## Findings

### 1-未修复-低-`_required_enum` 错误消息未包含非法实际值

- **入口/函数**: `_required_enum` → `parse_conversation_compact_output_vnext`
- **文件(行号)**: `dayu/host/llm_compaction.py:1108-1109`
- **输入场景**: LLM 返回非法 enum 字符串，例如 `evidence_kind: "unsupported_kind"` 或 `status: "bad_status"`
- **实际分支**: `value not in allowed_values` 为真，进入 `raise ValueError`
- **预期行为**: 错误消息应包含完整字段路径与非法实际值，帮助定位具体哪条数据不对
- **实际行为**: 错误消息为 `"{field_path} has invalid enum value"`，仅包含字段路径（如 `evidence_backed_facts[0].evidence_kind`），不包含实际非法值 `"unsupported_kind"`
- **直接证据**: `dayu/host/llm_compaction.py:1109` — `raise ValueError(f"{field_path} has invalid enum value")`，`value` 变量在作用域内但未被拼入消息
- **影响**: 调试时需额外查看 raw JSON 才能确定 LLM 实际输出了什么非法值；不阻碍功能正确性
- **建议改法和验证点**: 将 `value`（已通过 `_required_string` 确保为 `str`）拼入错误消息，如 `f"{field_path} has invalid enum value: {value!r}"`。更新 S2 中对应测试的 match pattern
- **修复风险（低）**: 仅影响错误消息文本；下游无代码依赖精确消息格式
- **严重程度（低）**:

### 2-未修复-低-测试辅助函数 `_proposal_json` 保留 `cast` 用法

- **入口/函数**: `_proposal_json`（测试私有辅助）
- **文件(行号)**: `tests/host/test_llm_compaction.py:418-419`, `tests/host/test_llm_compaction.py:424`
- **输入场景**: 所有通过 `_proposal_json` 构造可变 proposal 的 S1 测试
- **实际分支**: `cast(Mapping[str, JsonValue], compact_input.to_json())`（行 419）和 `cast(dict[str, JsonValue], parsed)`（行 424）
- **预期行为**: 生产代码已移除 `cast`；测试辅助可在 S3 清理
- **实际行为**: 两处 `cast` 仍在测试辅助中使用
- **直接证据**: `tests/host/test_llm_compaction.py:419` 和 `:424`
- **影响**: 仅测试代码；不影响生产类型安全。但测试中 `cast` 跳过类型检查可能掩盖测试辅助自身的类型错误
- **建议改法和验证点**: S3 中将 `_proposal_json` 改用 `_json_object` / `_required_value` 等与生产代码一致的 helper，或显式标注为 S3 清理项
- **修复风险（低）**: 纯测试代码变更
- **严重程度（低）**:

## Open Questions

无。所有 review focus 问题已通过逐行走读得到明确结论：

1. **是否真正移除了 vNext proposal parsing 的 broad cast / wide mapping cross-function contract**：是。`_parse_vnext_proposal` 返回类型从 `Mapping[str, JsonValue]` 变为 `ConversationCompactOutputVNext`；`cast` 从生产代码 import 中移除；所有 section parser 现在只接收通过 `_json_object` 验证后的 `Mapping[str, JsonValue]` 作为局部输入边界。

2. **JsonValue narrowing 是否无 Any/object/type ignore/broad cast 逃逸**：是。`json.loads(raw)` 显式标注为 `JsonValue`，通过 `_json_object` → `_required_string` / `_required_array` / `_required_enum` / `_optional_non_negative_int` 递归 `isinstance` 窄化到具体类型；pyright 0 errors 确认无类型逃逸。

3. **field-path diagnostics 是否完整、稳定且不破坏现有行为**：是。`_field_path` / `_item_path` 组合产生 `evidence_backed_facts[0].evidence_kind`、`answer_anchors[0].anchor_items[1].ordinal` 等完整嵌套路径；三个新增测试覆盖了嵌套路径稳定性；现有三个测试全部通过。

4. **candidate safety-net wrapping 是否保持 LLMCompactionProposalError**：是。`parse_conversation_compact_output_vnext` 的 `try/except (KeyError, TypeError, ValueError)` 统一包装为 `LLMCompactionProposalError`；新增 `test_parse_conversation_compact_output_vnext_wraps_candidate_safety_net` 覆盖此路径。

5. **source label validation/current anchor/unknown/stale/cross-section 行为是否未回归**：是。`_validate_vnext_candidate_source_labels` 和 `_validate_vnext_labels` 未修改；`test_parse_conversation_compact_output_vnext_rejects_current_anchor_label` 通过。

6. **tests 是否足以覆盖 S1**：是。S1 新增了三个测试——enum 路径诊断、嵌套 ordinal 路径诊断、safety-net wrapping；加上已有的 happy path、old schema fail closed、current anchor rejection 三个测试，覆盖了 S1 目标范围。S2 的完整 invalid matrix 未在 S1 强行要求。

7. **是否违反 AGENTS.md 约束**：否。所有新增和修改的函数均有完整中文 docstring；无 `Any`/`object`/`# type: ignore`/`cast` 出现在生产代码解析路径；分层边界未改变；LLM-facing schema 未修改；内部类名 `ConversationCompactOutputVNext` 仅在 safety-net `__post_init__` 错误中出现，且该错误通过 `LLMCompactionProposalError` 包装后在 Host 内部 repair/retry 流程消费，不进入 LLM 上下文。

## Residual Risk

- S2 完整 invalid proposal matrix（malformed JSON、top-level 非 object、字段类型错误、数组超限、unknown/stale/cross-section label 等）尚未覆盖；计划已明确由 S2 负责，当前风险受控。
- `_validate_vnext_candidate_source_labels` 的字段路径使用硬编码字符串（如 `f"evidence_backed_facts[{index}].evidence_labels"`），未复用 `_field_path`/`_item_path` helper。未来若修改字段名常量，此处需手工同步。当前 parser 路径与 label validator 路径格式一致，无实际冲突。
- `_optional_non_negative_int` 对 `ordinal` 字段新增了负整数拒绝逻辑，旧代码（`_optional_int_or_none`）接受任意 `int`。若存在合法负 ordinal 场景（文档未定义），此收紧可能导致误拒绝。当前测试覆盖确认负 ordinal 被正确拒绝，且 `ordinal` 语义为非负，风险低。
