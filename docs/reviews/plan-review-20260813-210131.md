# Plan Re-Review — UF-FIX03 summary-and-bounded-errors

- **Reviewed target**: `docs/gateflow/uf-fix03-summary-bounded-errors-plan-20260813.md`
- **Binding goal contract**: `docs/gateflow/uf-fix03-summary-bounded-errors-goal-confirmation-20260813.md`
- **Controller adjudication**: `docs/gateflow/uf-fix03-plan-review-adjudication-20260813.md`
- **Plan fix artifact**: `docs/gateflow/uf-fix03-plan-review-fix-20260813.md`
- **First-round reviews**:
  - `docs/reviews/plan-review-20260813-203415.md`（AgentMiMo）
  - `docs/reviews/plan-review-20260813-203826.md`（AgentDS）
- **Reviewer**: Independent re-review（plan re-review gate）
- **Timestamp**: 20260813-210131
- **Lenses applied**: goal-bound minimal design, architecture boundary, best-practice, overcoupling

## 1. Accepted findings 闭合验证

本节逐项验证 controller adjudication 中全部 accepted findings 是否在 plan fix 中正确关闭。

### M1 — exact-key failure schema migration

- **adjudication 要求**: five-field object 五个 key required，`file_label` 值可 null 且字段无 default；fresh intentional breaking change，parser 拒绝旧四字段，不迁移旧 record。
- **plan fix 落实**: §5.4 明确 "fresh breaking schema"、"JSON parser 只接受五个 exact keys，明确拒绝旧四字段"、"不迁移、不删除旧 record、不提供 default、兼容 parser、alias 或 dual writer"、"旧 durable failure record 不在本 WU 的读取契约内"。
- **代码验证**: 当前 `upload_failure.py:17` 的 `_FAILURE_KEYS = frozenset({"kind", "code", "message", "retry_hint"})` 为四字段，`upload_failure_reason_from_json` 在 line 220 做严格 `frozenset(value) != _FAILURE_KEYS` 比较。`FinsUploadFailureReason` 当前无 `file_label` 字段。plan 的 fresh breaking 策略与项目 schema 规则一致。
- **结论**: ✅ 已关闭。

### M2 — summary constructor inventory

- **adjudication 要求**: 显式列出四个 `FinsUploadResultSummary` 构造点及其 function context。
- **plan fix 落实**: §5.3 列出四点：(1) `service_runtime.py::ProductionFinsUploadRunner.run_upload` early-cancelled；(2) `service_runtime.py::_upload_summary_from_result`；(3) `ingestion_runtime.py::_produce_direct_upload` runner-unavailable；(4) `ingestion_runtime.py::_run_upload_job` runner-unavailable。§6.1 补充六个 `UploadOperationResult` 构造点。
- **代码验证**: 通过 Explore agent 确认四个 `FinsUploadResultSummary` 构造点分别在 `service_runtime.py:131`（cancelled）、`service_runtime.py:303`（from result）、`ingestion_runtime.py:3874`（direct runner-unavailable）、`ingestion_runtime.py:4350`（job runner-unavailable）。`UploadOperationResult` 直接构造在 `docling_upload_service.py`（skipped:312、deleted:392、uploaded:547、cancelled via `_build_cancelled_result`:1409）、`sec_upload_workflow.py:287`（conversion-cancelled）、`cn_pipeline.py:907`（conversion-cancelled）。plan 的六点 inventory 覆盖了全部逻辑构造点（`_build_cancelled_result` 作为单一 helper 计为一点）。
- **结论**: ✅ 已关闭。

### M3 — progress payload rename

- **adjudication 要求**: 不改 started/preparing progress 的既有 `file_count`；保留为 requested progress unit。
- **plan fix 落实**: §5.3 明确 "started/preparing/completed progress 的既有 `file_count` 完整保留，继续表示 requested progress unit；本 WU 不改 `_PAYLOAD_FILE_COUNT`、不在 progress 双写/rename count key"。§7 S1 assertion 6 确认 "started/preparing/completed progress 继续只有既有 `file_count` 请求单位"。
- **代码验证**: `_PAYLOAD_FILE_COUNT = "file_count"` 在 `ingestion_runtime.py:247`。plan 不修改此常量。
- **结论**: ✅ 已关闭。

### M4 — provenance/fingerprint coupling

- **adjudication 要求**: closed provenance 值保持 `original`/`docling` 不变，fingerprint bytes 不变。
- **plan fix 落实**: §5.1 明确 "最小方案固定选择私有 `Literal['original', 'docling']` 加两个模块常量"、"provenance 收紧仅是类型化，不改变 `_build_upload_source_fingerprint(...)` 的 canonical payload、排序、JSON 序列化或 bytes"。锁定 fixture digest `099dc9636e306c75f1d5d64dd0210123956ba73888e968088c7279baab1d7fdd`。
- **代码验证**: `_build_upload_source_fingerprint`（`docling_upload_service.py:1000-1023`）的 canonical payload 为 `{"name", "sha256", "size", "source"}`，按 name 排序后 JSON 序列化 + SHA-256。当前 `source` 已只使用 `"original"` 和 `"docling"` 两个值（line 684 和 745）。收紧为 `Literal` 不改变运行期值，fingerprint 不变。
- **结论**: ✅ 已关闭。

### M5 — count invariant owner

- **adjudication 要求**: `FinsUploadPipelineResult.__post_init__` 与 `FinsUploadResultSummary.__post_init__` 分别校验 pipeline/terminal count contract。
- **plan fix 落实**: §5.2 明确 "`FinsUploadPipelineResult.__post_init__()` 必须：拒绝缺失、bool、负数或非 int；`status == 'ok'` 时要求 `stored_file_count >= 1`；`skipped/deleted/failed` 时要求 `stored_file_count == 0`"。§5.3 明确 "`FinsUploadResultSummary.__post_init__()`" 为 "terminal count contract 的唯一 owner"，并给出完整状态矩阵。
- **代码验证**: 当前两个类均无 `__post_init__`。plan 为新增实现，明确放在 constructor 校验层而非 downstream renderer。
- **结论**: ✅ 已关闭。

### M6 — real Docling stability

- **adjudication 要求**: deterministic fake converter owner tests 负责映射；真实 corrupt sample 只断言稳定 public contract；不得加无条件 xfail/skip。
- **plan fix 落实**: §9.2 明确 "deterministic owner tests 覆盖映射，已有真实 sample 保留稳定 contract 测试且不加无条件 xfail/skip"。S2 assertion 2 写明 "只断言稳定 public contract，不依赖第三方原始 exception subtype/text，且禁止因平台差异加无条件 `xfail`/`skip`"。
- **结论**: ✅ 已关闭。

### M7 — per-file conversion control flow

- **adjudication 要求**: 明确顺序、fail-fast：首个 conversion failure 立即包装并终止。
- **plan fix 落实**: §5.4 明确 "filing conversion 顺序固定为 sequential fail-fast：首个失败立即包装并终止，后续 converter call 必须为 `0`"。S2 assertion 3 覆盖 "bad-first 时后续 converter call=`0`；valid-before-bad 时只调用到首个 bad"。
- **代码验证**: 当前 `_build_pending_assets`（`docling_upload_service.py:688`）无 try/catch，`DoclingConversionError` 直接冒泡。plan 要求在此函数内增加逐文件 catch + fail-fast，实现细节清晰。
- **结论**: ✅ 已关闭。

### D1 — material company publication contradiction

- **adjudication 要求**: 从 success signals、S2 tests 和 invariants 中删除/收窄 material zero-publication 承诺。
- **plan fix 落实**: §3.1 明确 "不对 material 增加 empty/corrupt/mixed zero-publication、typed failure、safe label 或 bounded stderr 验收"。S1 assertion 9 写明 "material success/cancel/failure 只新增 required count；existing raw generic failure message/error 与 company-first publication 回归保持，不增加 zero-publication 或 typed failure 预期"。
- **代码验证**: SEC material（`sec_upload_workflow.py:528-543`）和 CN material（`cn_pipeline.py:1135-1150`）均在 `prepare_upload` 前先 `begin_batch` + `upsert_company_meta` + `commit_batch`。plan 正确识别这是既有行为且不在本 WU 修改范围。
- **结论**: ✅ 已关闭。

### D2 — material raw error and missing test scope（accepted component）

- **adjudication 要求**: 加入 material 测试文件，仅断言 count schema 未破坏既有 material 行为。
- **plan fix 落实**: §6.2 加入 `tests/fins/test_sec_pipeline_upload_material_stream.py`。§7 S1 allowed test files 包含该文件。S1 assertion 9 覆盖 material 回归。
- **结论**: ✅ 已关闭。

### D3 — file label/public text guard mismatch

- **adjudication 要求**: 指定单一 owner 级 public display label contract；覆盖 `job_id_notes.pdf`、`财报正文.pdf`、Unicode Cc/Cf。
- **plan fix 落实**: §5.4 指定 `direct_events.py` 内单一 `canonicalize_fins_public_file_label` + `validate_fins_public_file_label`；复用 `_validate_safe_text` 和 `_DISALLOWED_TEXT_FRAGMENTS`；普通 basename 原样保留，命中 fragment/URL/job/path 或含 Unicode Cc/Cf 时返回固定标签 `输入文件（文件名已隐藏）`；five-field parser 只接受 canonical 值。
- **代码验证**: `_DISALLOWED_TEXT_FRAGMENTS`（`direct_events.py:45-60`）包含 `"job_id"`、`"财报正文"` 等 14 个 fragment。`_validate_safe_text`（line 1067）检查 URL、fragment、job ID pattern、绝对路径。plan 的 canonicalizer 在这些 guard 之前运行，确保 "job_id_notes.pdf" 等被 canonicalize 为固定标签后再进入 validator。
- **结论**: ✅ 已关闭。

### D4 — commit failure stored zero

- **adjudication 要求**: 增加 commit failure 精确测试：terminal failed、typed reason 保持既有分类、stored=0、published tree 不变。
- **plan fix 落实**: S1 assertion 8 明确 "filing `commit_batch` 分别抛 `OSError` 与 `RuntimeError`：terminal 均为 failed、stored=`0`；failure kind/code 保持既有 `storage/storage_io` 与 `runtime/unexpected_runtime` 分类，published tree SHA 不变且 single rollback 断言仍成立"。
- **结论**: ✅ 已关闭。

### D5 — no-artifact positive control

- **adjudication 要求**: direct test 先断言成功 terminal 和真实 Fins publication，再做负断言；措辞改 regression guard。
- **plan fix 落实**: S3 assertion 4 写明 "先断言 terminal=`success`、requested=stored，并从 Fins repository 读取真实 source meta、original blob 与 derived Docling asset 作为 positive control；随后才断言 jobs 目录无 `*.json`/`*.jsonl`、workspace `.dayu` 树无 Host/runtime SQLite、EventLog/Memory/ToolTrace"。全文改称 "regression guard"，不声称形式化证明。
- **结论**: ✅ 已关闭。

### C1 — EMPTY_INPUT_FILE content-code closure

- **adjudication 要求**: `EMPTY_INPUT_FILE` 加入 `_CONTENT_FAILURE_CODES`，kind/code 一致性测试。
- **plan fix 落实**: §5.4 末尾明确 "`EMPTY_INPUT_FILE` 必须加入 `_CONTENT_FAILURE_CODES`，并由 kind/code 一致性 owner test 锁定为 `content`"。
- **结论**: ✅ 已关闭。

### C2 — typed exception filing boundary

- **adjudication 要求**: SEC/CN/HK filing workflow 穷尽 catch `FinsUploadFailureError`；runtime string classifier 未调用。
- **plan fix 落实**: §5.4 明确 "SEC/CN/HK `upload_filing` workflow 在 `DoclingConversionError`/`OSError`/generic 之前穷尽显式 catch `FinsUploadFailureError`"。S2 assertion 7 要求 "generic catch、`_classify_direct_error`、`_safe_direct_error_message` 均未调用"。stop condition 要求 "若实现发现另有 filing 入口可绕过这两个 catch，必须停止并在 runtime 增加直接读取 `exc.failure` 的 typed defense"。
- **结论**: ✅ 已关闭。

### C3 — delete-with-files behavior

- **adjudication 要求**: 保持 accept/ignore；requested 保留请求数、stored=0。
- **plan fix 落实**: §5.3 状态矩阵中 `deleted` 行：`requested >= 0`（保留 validated request 事实）、`stored = 0`。§4 明确 "不改变 last-wins/delete ignore 决策"。
- **结论**: ✅ 已关闭。

### C4 — success/non-ok invariant scope

- **adjudication 要求**: 只有 ok 要求 stored=requested；skip/delete/cancelled/failed 为 0。
- **plan fix 落实**: §5.3 状态矩阵完整覆盖五种 status。§5.2 pipeline 校验 "`status == 'ok'` 时要求 `stored_file_count >= 1`；`skipped/deleted/failed` 时要求 `stored_file_count == 0`"。
- **结论**: ✅ 已关闭。

### C5 — remove uploaded_files

- **adjudication 要求**: production/test 零命中，不保留兼容字段。
- **plan fix 落实**: §5.1 "`UploadOperationResult.payload` 删除 `uploaded_files`"。§5.3 "`FinsUploadResultSummary` 删除 `uploaded_files: tuple[str, ...]`"。§8.5 静态审计 "`rg -n '\buploaded_files\b' dayu tests --glob '*.py'` 应零命中"。
- **结论**: ✅ 已关闭。

## 2. 关键 plan 假设验证

| # | 假设 | 判定 | 证据 |
| --- | --- | --- | --- |
| A1 | filing workflow 在 `commit_batch` 返回后才消费 result | 成立 | `commit_prepared_upload_batch`（`docling_upload_service.py:868-892`）在 commit 返回后 return；finally 只在未进入 terminal 时 rollback |
| A2 | filing 空/损坏在 `begin_batch` 前失败 | 成立 | SEC filing `prepare_upload` 在 `begin_batch` 前（`sec_upload_workflow.py:208-253`）；CN filing 同构（`cn_pipeline.py:828-873`） |
| A3 | material company 在 `prepare_upload` 前已 commit | 成立（plan 已正确处理） | SEC material（`sec_upload_workflow.py:528-543`）、CN material（`cn_pipeline.py:1135-1150`）均先 commit company batch；plan §3.1 明确不修改此行为 |
| A4 | `_PendingFileAsset.source` 已只使用 `"original"` 和 `"docling"` | 成立 | line 684 `source="original"`、line 745 `source="docling"`；收紧为 `Literal` 不改变运行期值 |
| A5 | fingerprint canonical payload 不变 | 成立 | `_build_upload_source_fingerprint`（line 1013-1021）包含 `name/sha256/size/source`，与 plan §5.1 一致 |
| A6 | `_PAYLOAD_FILE_COUNT` 为 `"file_count"` | 成立 | `ingestion_runtime.py:247`；plan 不修改 |
| A7 | CLI generic catch 直接渲染 `str(exc)` | 成立 | `fins.py:217-219`：`render_cli_error(f"dayu-cli {args.command_name}: {exc}")` |
| A8 | `_validate_safe_text` 覆盖 fragment/URL/path | 成立 | `direct_events.py:1067-1104`：检查 URL、14 个 fragment、job ID pattern、绝对路径 |

## 3. 特别检查项验证

### 3.1 upload_filing scope

plan §2.1 明确 "业务 scope 严格为 `upload_filing`"。§3.1 列出 material 不做的完整清单。S1/S2 的 material 断言仅限 count schema 回归。**scope 收窄正确。**

### 3.2 material 仅共享 count 机械迁移

plan §3.1 "material 只因共享 count/result type breaking change 机械补齐 `stored_file_count` producer/consumer/test"。S1 assertion 9 "material success/cancel/failure 只新增 required count；existing raw generic failure message/error 与 company-first publication 回归保持"。§6.2 包含 material 测试文件。**机械迁移边界正确。**

### 3.3 fresh breaking schema

§5.4 "这是 intentional fresh breaking schema"、"JSON parser 只接受五个 exact keys，明确拒绝旧四字段"、"不迁移、不删除旧 record、"旧 durable failure record 不在本 WU 的读取契约内"。§5.2 pipeline result 同为 "fresh breaking schema"。**breaking 策略明确。**

### 3.4 constructor inventory

§5.3 列出四个 `FinsUploadResultSummary` 构造点。§6.1 列出六个 `UploadOperationResult` 逻辑构造点。§6.1 补充 pipeline 唯一 `cls(...)` 和 failure reason owner constructors。代码验证确认全部命中。**inventory 完整。**

### 3.5 progress file_count 保留

§5.3 "started/preparing/completed progress 的既有 `file_count` 完整保留"、"`_PAYLOAD_FILE_COUNT` 保持不变"。S1 assertion 6 覆盖。**保留正确。**

### 3.6 count owner

§5.2 "`FinsUploadPipelineResult.__post_init__()` 必须..."。§5.3 "`FinsUploadResultSummary.__post_init__()` 固定不变量..."。所有 producer 先迁移，renderer/progress/durable consumer 不再校验或重算。**owner 唯一且在 constructor 层。**

### 3.7 provenance fingerprint 不变

§5.1 "不改变 `_build_upload_source_fingerprint(...)` 的 canonical payload、排序、JSON 序列化或 bytes"。锁定 fixture digest。代码验证 fingerprint 逻辑（line 1013-1021）不变。**fingerprint 不变。**

### 3.8 filing fail-fast 原子性

§5.4 "filing conversion 顺序固定为 sequential fail-fast：首个失败立即包装并终止，后续 converter call 必须为 `0`"。S2 assertion 3 覆盖 bad-first 和 valid-before-bad 两种场景。"失败前已产生的内容只在内存/临时对象中，不得 `begin_batch`"。**fail-fast 明确。**

### 3.9 单一 public label owner 对 fragment/Cc/Cf 的闭合

§5.4 指定 `direct_events.py` 为唯一 canonicalizer/validator owner。复用 `_validate_safe_text` 和 `_DISALLOWED_TEXT_FRAGMENTS`。Unicode Cc/Cf 返回固定标签。five-field parser 只接受 canonical 值。reason/durable/direct/CLI 消费同一值。raw basename 只进 operator log。**label 闭合正确。**

### 3.10 commit failure stored=0

§5.1 "commit 抛错时不得投影 stored count"。S1 assertion 8 覆盖 OSError/RuntimeError 两种路径：terminal failed、stored=0、failure 分类保持、published tree SHA 不变、single rollback。**commit failure 处理完整。**

### 3.11 direct success positive control 与 no-artifact regression guard

S3 assertion 4 "先断言 terminal=`success`、requested=stored，并从 Fins repository 读取真实 source meta、original blob 与 derived Docling asset 作为 positive control；随后才断言..."。全文 "regression guard" 措辞。**positive control 和 regression guard 均正确。**

## 4. 残留风险

| 风险 | 严重程度 | 跟踪目的地 | 说明 |
| --- | --- | --- | --- |
| 真实 Docling 跨平台底层异常差异 | 中 | UF-PF03 | deterministic owner tests 覆盖映射，真实 sample 只断言稳定 contract |
| material generic raw failure / company-first publication | 中 | 后续 material work unit | owner 为 Fins material workflow，本 WU 只做 shared count 机械迁移 |
| 旧 durable upload summary/failure record 不可读 | 低 | 显式排除 | fresh-breaking schema rule；不兼容读取 |
| goal-confirmation 文本与 plan scope 的措辞差异 | 低 | 无 | goal #5 未显式限定 "filing only"，但 adjudication D1 已裁决收窄；plan §2.1 正确收窄 |

## 5. Plan review conclusion: **pass**

**判断依据**：

1. **全部 16 个 accepted findings 已关闭**：M1-M7、D1-D5、C1-C5 的 plan fix 落实均经代码事实验证，无遗漏。
2. **goal confirmation 与 plan 一致**：plan §2.1 的 goal、§2.3 的 success signals、§3.1 的 non-goals 与 binding goal-confirmation 对齐。唯一措辞差异（goal #5 未显式写 "filing only"）已被 adjudication D1 裁决，plan 正确收窄。
3. **架构边界正确**：Host/Engine/runtime/config no-touch；filing 原子性复用现有 batch；material 只做机械 count 迁移；direct boundary 不创建 Host/Engine artifact。
4. **contract 足够 code-generation-ready**：三个 slice 的 allowed files、exact assertions、data flow、invariants、stop conditions 和 non-goals 均已明确。constructor inventory 完整。fresh breaking schema 策略清晰。单一 label owner 方案最小且可行。
5. **无 goal drift**：plan 未增加 Goal Confirmation 未确认的目标、验收标准或 future-slice work。
6. **无过度耦合**：三个 slice 按可验证业务增量拆分，依赖链清晰（S2 依赖 S1、S3 依赖 S1+S2），每个 slice 只修改列出的文件。
7. **residual risks 已正确分类并跟踪**。

plan 可以进入 implementation gate。

## Artifact path

`docs/reviews/plan-review-20260813-210131.md`
