# Plan Re-Review — WU-SEMANTIC-OWNERSHIP-01 Umbrella Plan

## Review Metadata

- **Review target**: `docs/host/wu-semantic-ownership-01-umbrella-plan.md`（经 AgentCodex plan fix 更新后）
- **Previous reviews**: `docs/reviews/plan-review-20260709-143323.md` (AgentMiMo)、`docs/reviews/plan-review-20260709-143500.md` (AgentDS)
- **Controller adjudication**: `docs/reviews/wu-semantic-ownership-01-plan-review-controller-adjudication.md`
- **Plan fix artifact**: `docs/reviews/wu-semantic-ownership-01-plan-fix-codex.md`
- **Design sources**: `docs/host/design.md`、`docs/engine/design.md`
- **Control source**: `docs/host/issues-implementation-control.md`
- **Project constraints**: `AGENTS.md`
- **Review gate**: plan re-review
- **Review date**: 2026-07-09
- **Review agent**: AgentDS

## Scope

本 re-review 仅判断 plan fix 是否足以关闭 controller adjudication 中全部 accepted findings (C01-C11)。不判断 umbrella WU 是否完成——umbrella 需要 8 个 sub WU 全部通过且至少两轮 full-repository deepreview 无新增 accepted finding 后才算完成。

## C01-C11 Closure Table

### C01: P0-A finish reason authority — **closed**

**Controller 要求**：P0-A 默认 root-cause fix 为从 `RunnerContentCompletedData` 和 `ContentCompleteData` 移除 `finish_reason`，让 `RunnerDoneData.finish_reason` 成为唯一 authority。Implementation 前必须扫描全部消费者。

**Plan 现状**（§6 P0-A "预期 contract"）：

- 明确：`"默认 root-cause fix：从 RunnerContentCompletedData 和 ContentCompleteData 移除 finish_reason。"` ✓
- 明确：`"RunnerDoneData.finish_reason / IterationCompletedData.finish_reason 是唯一 Runner-call completion authority；content-completed 事件只表达内容片段完成，不再携带或投影 finish reason。"` ✓
- 明确：`"Agent-side overwrite 不是主路径，不能作为保留第二个非权威 finish_reason 字段的理由。"` ✓（拒绝了 MiMo 01 建议的方案 b）
- 明确：`"P0-A implementation 前必须扫描 ContentCompleteData.finish_reason 与 RunnerContentCompletedData.finish_reason 的全部消费者。若发现 Engine Agent event bridge 或 parser/tests 之外的预期外生产消费者，controller 必须停下重新裁决 public contract 迁移，不得继续 implementation。"` ✓
- Stop condition 覆盖了迁移风险过大场景 ✓

**验证**：C01 完全闭合。Plan 不再将方案选择推给 implementation agent，而是明确 root-cause fix 为移除字段；同时有消费者扫描作为 implementation 前置条件。

---

### C02: P0-B `ingest_method` coverage — **closed**

**Controller 要求**：P0-B 必须显式要求全量 `rg "ingest_method" dayu/fins/` 扫描，allowed files 必须覆盖扫描发现的所有生产读写点。

**Plan 现状**（§6 P0-B "Root-cause confirmation 必做扫描" 和 "Allowed files/modules"）：

- 明确：`"implementation 前必须运行 rg 'ingest_method' dayu/fins/，把所有读写点归类为 pipeline producer、pipeline rebuild/filter、source upsert、storage serialization/deserialization、maintenance/read path、tests。"` ✓
- 明确：`"allowed files 必须覆盖扫描结果中的全部生产读写点；若新增命中点不属于下列 allowed files，先更新 sub WU plan 或停下让 controller 裁决，不得只改已知路径。"` ✓
- Allowed files 显式列出了 DS 01 finding 指出的全部 6 个缺失文件：`cn_pipeline.py`、`cn_download_rebuild.py`、`cn_download_source_upsert.py`、`sec_download_source_upsert.py`、`_fs_source_document_core.py`、`_fs_maintenance_core.py` ✓

**验证**：C02 完全闭合。DS 01 发现的 allowed files 缺失已全部补齐，且 plan 要求全量扫描 + 动态扩展 allowed files。

---

### C03: P0-B preprocess helper scope — **closed**

**Controller 要求**：P0-B root-cause confirmation 必须先选择 boolean helper 或 typed status helper，列出所有 direct/job/awaiting/direct-stream consumers，裁决 JSON summary 是否新增 `not_supported_count`。

**Plan 现状**（§6 P0-B "Root-cause confirmation 必做扫描"）：

- 明确：`"preprocess success/result helper 必须在 root-cause confirmation 阶段裁决为 boolean helper 或 typed status enum/helper，并列出 direct、job、awaiting、direct-stream consumers。"` ✓
- 明确：`"root-cause confirmation 必须说明 JSON summary 是否新增 not_supported_count；若新增，列出 direct result、job record、awaiting result、CLI/Service rendering 的字段传播路径。"` ✓
- P0-B non-goal 说明不修改 Host wait-resume contract，避免 scope 漂移 ✓

**验证**：C03 完全闭合。helper 类型选择、消费者清单、`not_supported_count` 决策均已作为 P0-B root-cause confirmation 的前置要求。

---

### C04: P1-A consumer migration pressure — **closed**

**Controller 要求**：P1-A S3 必须要求逐消费者 completeness checklist，覆盖全部六个消费者；允许拆分 S3 但不得遗漏消费者。

**Plan 现状**（§6 P1-A "Implementation slices" S3）：

- 明确：`"S3 completeness checklist：implementation artifact 必须逐消费者列出当前 fallback/back-query/blacklist 路径、替换后的 helper、覆盖测试和 residual risk。清单必须至少包含 Tool Trace、Read API、Durable Memory、Conversation Memory、RunInputBuilder、CompactMaterial。"` ✓
- 明确：`"若 S3 上下文过大，允许拆为 S3a/S3b 等较小 semantic slices；拆分只能降低上下文压力，不能漏掉上述任一消费者。"` ✓

**验证**：C04 完全闭合。六个消费者全部强制列入 checklist，允许拆分但不允许遗漏。

---

### C05: P1-B `RUN_LOST` public outbox behavior — **closed**

**Controller 要求**：`RUN_LOST` 是 Host terminal/lifecycle fact，不是 public outbox terminal item。Public outbox watermark 不应因 `RUN_LOST` 要求 outbox item。必须区分三类语义。若 design truth 缺失，先更新 `docs/host/design.md`。

**Plan 现状**（§6 P1-B "预期 contract" 和 "Implementation slices" S0）：

- 明确 controller design decision：`"RUN_LOST 是 Host terminal/lifecycle fact，不是 public outbox terminal item。Public outbox watermark 不应因 RUN_LOST 要求 outbox item。"` ✓
- 明确三类语义区分：`"terminal helper 必须区分三类语义：Host terminal event set（包含 RUN_LOST）、public outbox terminal item event set（排除 RUN_LOST）、非 public terminal fact 的 explicit skip/diagnostic behavior。"` ✓
- 明确：`"若 docs/host/design.md 尚未写清上述区别，P1-B 必须先更新 design truth，再进入 implementation。"` ✓（解决了 DS 07 / MiMo 02 的 "先确认 design" 的不可执行问题）

**验证**：C05 完全闭合。DS 07 / MiMo 02 提出的 "design confirmation 缺乏可执行标准" 已被 controller design decision 和 design truth 前置更新要求解决。

---

### C06: P1-C waiting wording boundary — **closed**

**Controller 要求**：业务级 "等待工具结果返回" 在 tool schema 中允许；治理级 "等待状态"/"未进入等待状态"/"后续调度" 不允许。必须扫描 duplicate-tool/governance messages 是否进入 LLM context。

**Plan 现状**（§6 P1-C "预期 contract"）：

- 明确业务/治理边界：`"业务级'等待工具结果返回'只可用于描述长事务工具会稍后返回结果的模型可见行为；治理级'等待状态''未进入等待状态''后续调度'、Host wait/poll/adapter 术语、Host-governance default text 不得进入 LLM-facing 文本。"` ✓
- 明确：`"P1-C 必须扫描 prompt/config、tool schema/outcome helper、duplicate-tool/governance messages，并明确分类哪些文本进入 LLM context；若 duplicate-tool/governance messages 会进入 LLM context，必须按治理泄漏处理。"` ✓
- Plan 不再承认 DS 02 指出的 Engine design 与 review source 的矛盾——controller C06 裁决已解决该矛盾 ✓

**验证**：C06 完全闭合。DS 02 的 "分类标准对 implementation agent 不充分" 已被 controller classification 和具体扫描要求解决。

---

### C07: P2-A session resume boundary — **closed**

**Controller 要求**：默认采用 Service-owned existing-session execution helper。CLI-public helper 仅当 root-cause confirmation 证明纯 UI/rendering 时才允许。

**Plan 现状**（§6 P2-A "预期 contract"）：

- 明确：`"默认采用 Service-owned existing-session execution helper 承载 prompt/interactive resume 的共享 submit/watch/session execution 语义。"` ✓
- 明确 CLI/Service 职责分离：`"CLI 保留参数解析、终端渲染、SIGINT、本地 exit code 映射和 command-specific output；Service helper 不接收或生成 CLI display concerns。"` ✓
- 明确例外条件：`"只有 root-cause confirmation 证明复用逻辑纯属 UI/rendering，且下沉 Service 会泄漏 CLI display concerns 时，才允许选择 CLI-public narrow helper；该例外必须由 controller 裁决后写入 P2-A sub WU plan。"` ✓
- Plan 不再使用 "或" 并列两个方案 ✓

**验证**：C07 完全闭合。DS 03 / MiMo 04 的 "两种方案以'或'并列导致 implementation agent 需自行做架构决策" 已被解决。

---

### C08: P2-B obsolete finding handling — **closed**

**Controller 要求**：P2-B 必须先产出 finding 状态表（active/obsolete-with-evidence/needs-design-update/deferred-with-owner）。Obsolete finding 可 no-code/no-commit pass 关闭。

**Plan 现状**（§6 P2-B "Root-cause confirmation 必做"）：

- 明确四种状态：`"每个 P2-B finding 的状态只能是 active、obsolete-with-evidence、needs-design-update 或 deferred-with-owner。"` ✓
- 明确各状态处理方式：`"active finding 才进入 normal implementation/review/fix/re-review closure。"` / `"obsolete-with-evidence finding 可通过 controller-accepted confirmation artifact 做 no-code/no-commit pass 关闭，禁止为了制造实现痕迹而假改生产代码或测试。"` / `"needs-design-update 必须先更新设计真源"` / `"deferred-with-owner 必须写清 owner/destination，不得作为 silent skip。"` ✓

**验证**：C08 完全闭合。DS 08 / MiMo 05 的 "obsolete 判定标准不具体，可能浪费 implementation 资源" 已被解决。

---

### C09: P2-C prompt source migration — **closed**

**Controller 要求**：P2-C 必须先 `rg "AgentPolicy\\(" dayu/ tests/` 并按 layer 分类。Engine 不得 import runtime config。production 和 tests 必须显式传 prompt。不允许 test-only default helper 或 compatibility wrapper。

**Plan 现状**（§6 P2-C "Root-cause confirmation 必做扫描" 和 "预期 contract"）：

- 明确：`"P2-C 必须先运行 rg 'AgentPolicy\\(' dayu/ tests/，并按 owner layer 分类构造点：Engine contract/tests、Runtime assembly/config、Service assembly、Host/CLI assembly 或 fixtures、tests。"` ✓
- 明确：`"Engine 不得 import runtime config、config loader 或 execution profile 来恢复默认 prompt；Engine 只接收调用方传入的 prompt。"` ✓——直接解决了 DS 04 的核心担忧
- 明确：`"production 和 tests 都必须从所属 assembly/fixture boundary 显式传入 prompt，不允许 test-only default helper、compatibility wrapper、兼容性 re-export 或 AgentPolicy() fallback。"` ✓
- `AgentPolicy.fallback_prompt` 和 `continuation_prompt` 改为必填，缺少时 TypeError ✓

**验证**：C09 完全闭合。DS 04 的 "Engine → runtime config 反向依赖风险" 被显式禁止；"prompt 双真源" 通过必填化 + assembly boundary 传入解决。

---

### C10: Full-repository deepreview phase — **closed**

**Controller 要求**：每轮至少派 AgentMiMo 与 AgentDS 全仓 review；定义 review dimensions；final closeout 要求至少两轮连续无新增 accepted current-umbrella finding。

**Plan 现状**（§8 "Full-repository deepreview phase"）：

- 明确：`"Each round dispatches at least AgentMiMo and AgentDS over the full repository, not only changed files."` ✓
- 明确：`"Minimum review dimensions: Engine contracts、Host durable truth、Host projections、Fins contracts、CLI/Service boundary、LLM-facing text、config/prompt、tests/import-boundary coverage。"` ✓
- 明确：`"After fixes, repeat full-repository rounds until at least two consecutive rounds produce no new accepted current-umbrella finding, unless the user explicitly changes the exit condition."` ✓
- 明确 adjudication 流程：controller 裁决每个 finding，accepted current-umbrella findings 成为新 sub WU/slices ✓

**验证**：C10 完全闭合。DS 05 的 "deepreview phase 执行细节不足以防止过早 closeout" 已被解决。

---

### C11: Sub WU contract conflict handling — **closed**

**Controller 要求**：后续 sub WU 与已 accepted contract 冲突时，停下、更新设计真源、然后修改早前 contract 或增加 typed mapping。禁止下游 workaround。

**Plan 现状**（§5 "Umbrella execution protocol" 新增 "Sub WU contract conflict handling" 段落）：

- 明确：`"若后续 sub WU 发现与已 accepted sub WU contract 冲突，controller 必须停下，不得在下游消费者、展示层、测试夹具或单入口用 workaround 掩盖冲突。"` ✓
- 明确：`"controller 必须先确认是否需要更新设计真源；若设计真源缺失或与 accepted contract 冲突，先在对应 design doc 中写清事实 owner、校验者、持久化者和投影者。"` ✓
- 明确二选一裁决路径：`"为早前 accepted contract 新开 fix slice 并修改该 contract，或在当前 sub WU 中增加显式 typed mapping"` ✓
- 明确 typed mapping 使用条件：`"只能用于两个 owner boundary 都成立但 public contract 不同的情况；若上游 contract 本身错误，必须修上游，禁止下游 workaround。"` ✓

**验证**：C11 完全闭合。DS 09 的 "umbrella execution protocol 缺少 sub WU 失败的全局回滚策略" 已被解决。

---

## 特别确认项

### P0-A 默认 root-cause 是移除 duplicate finish_reason source ✓

Plan §6 P0-A 明确 `"默认 root-cause fix：从 RunnerContentCompletedData 和 ContentCompleteData 移除 finish_reason"`，`"RunnerDoneData.finish_reason / IterationCompletedData.finish_reason 是唯一 Runner-call completion authority"`。Agent-side overwrite 被显式拒绝。

### P0-B 覆盖所有 fins `ingest_method` 生产消费者 ✓

Plan §6 P0-B 通过两个机制确保覆盖：(1) 实现前强制 `rg "ingest_method" dayu/fins/` 全量扫描并归类；(2) allowed files 显式列出 DS 01 发现的所有缺失文件（cn_pipeline.py、cn_download_rebuild.py、cn_download_source_upsert.py、sec_download_source_upsert.py、_fs_source_document_core.py、_fs_maintenance_core.py）。

### P1-B `RUN_LOST` 不要求 public outbox terminal item ✓

Plan §6 P1-B 明确写入 controller design decision：`"RUN_LOST 是 Host terminal/lifecycle fact，不是 public outbox terminal item。Public outbox watermark 不应因 RUN_LOST 要求 outbox item。"` Terminal helper 区分三类语义。

### P2-A 默认 Service-owned helper ✓

Plan §6 P2-A 明确 `"默认采用 Service-owned existing-session execution helper"`。CLI-public helper 仅限例外且需 controller 裁决。

### P2-C 不引入 Engine → runtime config 依赖 ✓

Plan §6 P2-C 明确 `"Engine 不得 import runtime config、config loader 或 execution profile 来恢复默认 prompt"`。Prompt 由 Service assembly 从 runtime config 解析后传入 Engine。

### Umbrella 后续 deepreview 需要至少两轮连续无新 accepted current-umbrella finding ✓

Plan §8 明确 `"until at least two consecutive rounds produce no new accepted current-umbrella finding, unless the user explicitly changes the exit condition"`。

---

## Additional Findings

本 re-review 对更新后的 plan 做 adversarial check，未发现 C01-C11 范围外的新 blocking issue。以下为 non-blocking observations：

### N01-低-P0-A Implementation slices S3 验证命令的测试覆盖范围偏窄

- **位置**: §6 P0-A S3 "测试/验证命令"
- **观察**: S3 validation/doc sync slice 的测试命令仅覆盖 `tests/engine/runners/openai`、`tests/engine/test_metadata_boundary.py`、`tests/engine/test_agent_phase3_tool_call.py` 和 `tests/host/test_engine_ingest_mapping.py`。如果 content-completed `finish_reason` 移除影响到 EngineEvent `content_completed` 的 Host ingest 消费方（如 `engine_ingest.py` 中直接使用 `data.finish_reason`），仅 `test_engine_ingest_mapping.py` 可能不够。建议 P0-A sub WU plan 阶段检查是否需要补充 Host-side contract tests。
- **严重程度**: 低 — sub WU plan 阶段可以调整测试矩阵。

### N02-低-P2-C `AgentPolicy` 必填化后 Engine 内部构造路径需要确认

- **位置**: §6 P2-C "Root-cause confirmation 必做扫描"
- **观察**: Plan 要求 `rg "AgentPolicy\\(" dayu/ tests/` 按 layer 分类。但 Engine 内部（如 `agent.py`、`_default_runner.py`）如果存在 `AgentPolicy(...)` 直接构造，必填化后这些点需要从 request/assembly 获得 prompt。Plan 没有显式说明 Engine 侧是否存在这种情况——这应该是 `rg` 扫描自动回答的问题。不构成 plan 缺陷，但值得在 sub WU plan 阶段关注。
- **严重程度**: 低 — `rg` 扫描会自然揭示。

---

## Residual Risks（不阻塞 accepted plan commit）

| Risk | 来源 | 缓解措施 |
|------|------|---------|
| P0-A 消费者扫描发现预期外生产消费者 | C01 stop condition | Plan 已要求停下让 controller 裁决 |
| P0-B `rg "ingest_method"` 扫描发现 plan 未知的新读写点 | C02 "allowed files 必须覆盖扫描结果" | Plan 要求先更新 sub WU plan 或停下 |
| P2-C `AgentPolicy(...)` 构造点数量被低估 | DS 04 (resolved) | Plan 要求全量 rg 扫描 + 按层分类 |
| Deepreview phase 执行质量依赖 controller discipline | DS 05 (resolved) | Plan 明确了 dimensions、agent assignment、exit criteria |
| 后续 sub WU 与已接受 sub WU contract 冲突 | DS 09 (resolved) | Plan 新增冲突处理协议 |

---

## Plan Review Conclusion

**pass**

C01-C11 全部闭合。Updated plan 满足 controller adjudication 的全部要求，关键要点：

- P0-A 默认 root-cause fix 为移除 duplicate `finish_reason` 字段，非 Agent 端覆盖。
- P0-B 通过 `rg "ingest_method"` 全量扫描 + allowed files 显式补齐确保覆盖所有生产消费者。
- P1-B `RUN_LOST` 明确为 Host lifecycle fact 而非 public outbox terminal item，三类语义区分清晰。
- P1-C 的 "等待工具结果" 分类标准已由 controller C06 裁决明确，可执行。
- P2-A 默认 Service-owned helper，架构方向明确。
- P2-C 显式禁止 Engine → runtime config 反向依赖。
- Deepreview phase 具有明确的 agent assignment、dimension checklist、两轮连续 no-new-findings exit criteria。
- Sub WU contract 冲突处理协议覆盖了 DS 09 的回滚策略缺失。

无 block remaining finding。Plan 可进入 accepted plan commit。

Additional findings N01/N02 为低严重度观察，不阻塞 plan commit，可在对应 sub WU plan 阶段自然解决。

**重要提醒**：本 re-review 只判定 plan fix 足以关闭 C01-C11。Umbrella WU-SEMANTIC-OWNERSHIP-01 未完成——需 8 个 sub WU 全部通过各自的 plan/implementation/review/fix/re-review gate，且至少两轮 full-repository deepreview 无新增 accepted current-umbrella finding 后，umbrella 才能 final closeout。
