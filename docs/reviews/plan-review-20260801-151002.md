# Plan Re-Review: `wu-cli-interactive-02-conformance-fixes`

## 元数据

- **Review type**: Adversarial plan re-review（AgentDS，独立）
- **Review date**: 2026-08-01
- **Re-review target**: `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md`（已修复 accepted findings 版本）
- **Work unit**: `wu-cli-interactive-02-conformance-fixes`
- **当前 gate**: `plan review -> fix -> re-review`
- **Prerequisite reviews read**:
  - `docs/reviews/plan-review-20260801-143257.md`（AgentDS 原始 review，F-01 至 F-04）
  - `docs/reviews/plan-review-20260801-143623.md`（AgentMiMo 原始 review，001 至 005）
- **Controller adjudication trace**: plan §15（裁决与 fix trace）
- **Code facts verified (direct read, not AgentMiMo artifact)**:
  - `dayu/host/engine_ingest.py`: 确认 reactive compaction terminal writer 存在（L3201 CONTEXT_COMPACTED、L3285 CONTEXT_COMPACTION_FAILED）
  - `dayu/host/dispatch.py`: 确认 proactive compaction terminal writer 存在（L3203 CONTEXT_COMPACTED、L3448 CONTEXT_COMPACTION_FAILED）；`run_queue_promotion()` L1723 与 `reconcile_owned_sessions_once()` L3757 各自独立获取 lease 后调用 `_run_queue_promotion_with_lease()`
  - `dayu/host/llm_compaction.py`: 确认 `_agent_request_vnext()` L436-440 以 `run_id=compactor_engine_run_id`、`attempt_id=None`、`execution_id=None` 构造 compactor Engine request
  - `dayu/engine/contracts/runner_identity.py`: 确认 `RunnerRequestIdentity` 含 `attempt_id`/`execution_id`/`runner_call_index`/`client_correlation_id`，与 plan taxonomy 一致
  - `dayu/cli/commands/session.py`: 确认 `_resume_interactive_ticker()` L575 存在，OQ-01 已由代码证据关闭
  - `docs/cli_ci_scenarios.json`: 确认 5 条保留 pairwise row 均存在且含 `parameter:config:default` claim + `init-deepseek-config-explicit` precondition；17 条待删除 scenario 均存在

## Scope 与 review posture

本次是 plan re-review，不是新 plan review。核心问题：

1. 所有 controller accepted findings 是否真实关闭？
2. controller rejected-with-reason 是否合理？
3. 修订是否引入新 blocker？
4. 五个指定焦点领域是否 code-generation-ready 且不越界？

Review posture：constructively adversarial。默认假设修订可能在微妙处引入新问题，需要逐一验证。

---

## 1. Controller 裁决 trace 完整性验证

plan §15 记录了 10 条裁决项。逐项核对：

| 裁决项 | Controller decision | Plan 落点 | 落点存在？ | 落点语义与裁决一致？ |
|---|---|---|---|---|
| MiMo-001 | accepted | §1.2、§3.1、§8.1-§8.7 | ✓ | ✓ F11 改为 all-trigger invariant，engine_ingest.py 纳入 S4 allowed files |
| MiMo-002 / MiMo-004 | accepted | §6.5-§6.7 | ✓ | ✓ 状态表增加 idle 非空/纯空白 Ctrl+C 行、third+ no-op 行 |
| DS F-01 | accepted-clarification-only | §6.5-§6.7 | ✓ | ✓ F08+F09 exactly-once 语义明确，保留"等待 queued terminal"行为且加 no-label fresh Session 反例 |
| DS F-02 / MiMo-005 | accepted | §9.2-§9.6、§11.2 | ✓ | ✓ identity taxonomy 表区分七种 identity 概念 |
| DS F-03 | accepted | §10.3 | ✓ | ✓ 精确保留 5 条 pairwise row，仅删除两处 `parameter:config:default`，不发明替代 claim |
| MiMo-003 | rejected-with-reason | §8.2.2、§8.6、§9 开头 | ✓ | ✓ S4 guard 不预埋 identity，S5 在 winner 路径做 required typed payload 扩展 |
| DS F-04 | non-finding | §10.5 | ✓ | ✓ "若路径不存在"注释已删除 |
| OQ-01 | closed-by-code-evidence | §5.2.5 | ✓ | ✓ `_resume_interactive_ticker()` 存在且计划删除 |
| OQ-02 | closed-by-MiMo-001 | S4 allowed files / §8.2 | ✓ | ✓ reactive writer 已纳入同一 F11 guard |
| PTY / OQ-03 | allowed-variant | §10.5、§12.1-§12.2 | ✓ | ✓ POSIX 真实 PTY，非 POSIX 记录 capability/skip |

**结论**：10 条裁决项全部在 plan 中有对应落点，语义一致，无遗漏。

---

## 2. Accepted findings 逐项 closure 验证

### 2.1 MiMo-001: F11 reactive compaction terminal writer（原严重程度：高）

**原始 finding**：S4 allowed files 不含 `engine_ingest.py`，reactive 路径的 duplicate terminal 问题无法在 S4 修复。

**Plan 修订**：
- §1.2.4 新增完整 reactive 反例证据（`_execute_reactive_compaction()` 在事务外 await compactor 后进入新 write transaction，仅校验 Run/input 状态，不读取已有 terminal）
- §3.1 semantic owner 表新增 "compaction operation terminal" 行，明确 owner 是 `dayu.host.compaction_terminal` 的专用 trigger-aware transaction-local guard
- §8.1 S4 allowed production files 新增 `dayu/host/compaction_terminal.py`（新增）、`dayu/host/engine_ingest.py`
- §8.2.1 列出全部两路 writer 的代码证据与必须修复的反例
- §8.2.2 设计专用 `begin_compaction_terminal_commit_in_transaction()` 统一入口，reactive 路径在 artifact/event/fallback/start 前首先调用

**Closure 验证**：
- engine_ingest.py 已纳入 S4 allowed files ✓
- 两路 writer 均需在同一 write transaction 首先调用 guard ✓
- guard 的 CAS 线性化点（SQLite write transaction 串行 + transaction 内 fresh projection read）正确 ✓
- reactive 并发/幂等重入反例已明确写入 §8.2.1.3 与 §8.5 ✓

**Closure 状态**：✅ **CLOSED**。reactive 路径已被完整覆盖，all-trigger invariant 成立。

**Closure 风险**：低。唯一实施注意点是 reactive `_append_reactive_compacted_event()` 当前先写 artifact bytes/descriptor 再 append event；plan §8.2.1.4 明确要求 "late loser 必须在 artifact/descriptor、rejected/terminal event、fallback/fail-close/recovery start 之前就返回 no-op/diagnostic"。这是实施顺序要求，plan 已足够明确。

---

### 2.2 MiMo-002 / MiMo-004: Ctrl+C 状态机歧义（原严重程度：中/低）

**原始 finding**：
- MiMo-002：状态表只覆盖"空 draft"的 Ctrl+C，非空 draft Ctrl+C 后 pending exit 状态未确定
- MiMo-004：exit-after-cancel 期间第三次及后续 Ctrl+C 未指定

**Plan 修订**：
- 状态表新增行："idle + 非空或纯空白 draft | Ctrl+C | 清空整个 draft 并重绘；不设置 idle_exit_pending，仍 idle"
- 状态表新增行："cancelling/exit-after-cancel，exit_after_cancel=true | Ctrl+C | 第三次及之后一律 no-op；绝不再次发 cancel、Task.cancel() canonical waiter 或强关 Host/attachment"
- §6.5 关键实现要求 6："第二次以及之后的 Ctrl+C 不能 Task.cancel() canonical cancel/terminal waiter；exit_after_cancel=true 后所有后续 Ctrl+C 都 no-op"

**Closure 验证**：
- idle 非空 draft + Ctrl+C → 清空 + 不设 pending ✓（明确回答了 MiMo-002 的核心歧义）
- 非空 draft Ctrl+C 清空后处于 "idle + 空 draft, idle_exit_pending=false"，后续 Ctrl+C 按该行处理（登记 pending），再后续 Ctrl+C 执行 exit。总计 3 次 Ctrl+C 从非空 draft 退出。这与 oracle interactive.10/12 语义兼容 ✓
- exit_after_cancel=true 后所有 Ctrl+C 均 no-op ✓（关闭了 MiMo-004）

**Closure 状态**：✅ **CLOSED**。状态机所有 Ctrl+C phase 均确定性指定。

**Closure 风险**：低。需注意 §6.5 要求 6 的"第二次以及之后"措辞含义是"进入 cancelling 状态后的第二次 Ctrl+C 及后续"（即整体时序上的第三次 Ctrl+C 起），与状态表的"第三次及之后"一致。若实施 Agent 误解为"第二次 Ctrl+C 就不 cancel canonical waiter"，可能与状态表冲突——但状态表是 authoritative source，"第三次及之后一律 no-op"是精确语义。

---

### 2.3 DS F-01: second-ctrl-c-queued-followup 语义越界（原严重程度：中）

**原始 finding**：plan 将 exit-after-cancel 的等待范围从"当前 Run 的 cancel terminal"扩展到"所有已 queued follow-up 的完整 terminal"，超出冻结 oracle `interactive.12` 的单数 Run 语义。

**Controller 裁决**：`accepted-clarification-only`——保持现有"等待 accepted sole QUEUE terminal"语义，不做 DS 建议的"留 queued 给 fresh writer"改动。

**Plan 修订**：
- §6.5 关键实现要求 5：明确 "F08 + F09 的组合语义是 durable accepted sole QUEUE 必须恰好执行一次；第二次 Ctrl+C 只登记 exit-after-cancel，不撤销、取消或改写 queued Run。当前 invocation 必须先收口当前 cancel terminal，然后把已 accepted sole queued follow-up 提升为 current 并等待其 terminal；这是对现有 plan 语义的精确化，不改变 frozen oracle。特别是无 label fresh Session，不得在进程退出时留下永久 QUEUED Run 等待一个不会到来的 fresh invocation"
- 状态表 "exit-after-cancel" 行完整描述等待顺序

**Closure 验证**：
- 语义边界已显式记录："这是对现有 plan 语义的精确化"—— controller 已裁决此扩展属于 clarification 而非 goal drift ✓
- no-label fresh Session 不得留下永久 QUEUED Run 的反例已加入 §6.5.5 与 §6.7 owner-level tests ✓
- 用户可见行为（连续 Ctrl+C 后等待 queued follow-up 完成）已被显式声明，不再隐含 ✓

**Closure 状态**：✅ **CLOSED**。语义已精确定义，无歧义残留。

**Residual note**：此决策意味着用户在 active Run 期间创建 queued follow-up 后立即连续 Ctrl+C，需要等待 cancel terminal + queued Run 完整执行（可能数十秒）。这是 controller 已裁决的语义选择，不是 plan 缺陷。§12.1 "queue lifecycle" residual risk 已记录。

---

### 2.4 DS F-02 / MiMo-005: F13 identity 术语歧义与串线测试（原严重程度：中/低）

**原始 finding**：
- DS F-02：Engine `RunnerRequestIdentity.attempt_id` 与 Host `CompactionAttempt` number 是两个概念，plan 未明确区分
- MiMo-005：F13 repair/multiattempt 串线测试描述不够精确

**Plan 修订**：
- §9.3 新增完整 identity taxonomy 表，区分七种 identity 概念及其精确语义/绑定
- §9.3.1-§9.3.4 详细说明每种 identity 的 owner、生命周期和不可混用规则
- §9.6 owner-level tests 新增 "operation exact A/B/C 串线反例"：ordinary Run final 身份 A、Host attempt 1 rejected 身份 B、Host attempt 2 accepted 身份 C，断言 A/B/C 不同且不跨 attempt 泄漏

**Closure 验证**：
- taxonomy 表区分了：`compaction_operation_id`、`compaction_attempt_number`、`compactor_engine_run_id`、`RunnerRequestIdentity.attempt_id`/`execution_id`、`runner_call_index`、`client_correlation_id`、`provider_request_id` ✓
- 关键澄清："compactor 的 attempt/execution 必须为 null，Host compaction_attempt_number 继续由外层 event/manifest 独立表达" ✓
- 代码证据确认 `_agent_request_vnext()` L439-440 以 `attempt_id=None`、`execution_id=None` 构造 compactor request，与 taxonomy 一致 ✓
- 测试描述覆盖了 ordinary→rejected→accepted 的完整串线反例 ✓

**Closure 状态**：✅ **CLOSED**。七种 identity 概念已精确区分，code facts 与 design 一致。

**Closure 风险**：低。唯一实施注意点：§9.3.4 说 "repair/multi-pass 循环的每个 compaction_attempt_number 都创建新 prepared input / manifest / AgentRunRequest"。Engine 层面的 repair（同一 AgentRunRequest 内重试）与 Host 层面的 multi-pass（不同 AgentRunRequest）需在实施时仔细区分。plan 已通过 taxonomy 表提供了足够的区分依据。

---

### 2.5 DS F-03: 场景清理 config-claim 替换细节缺失（原严重程度：低）

**原始 finding**：保留的 pairwise rows 中 `parameter:config:default` claim 替换文本不够具体。

**Plan 修订**：
- §10.3.2：精确保留 5 条 pairwise row ID（PC-PW-R2-03/-04/-06/-08/-10）
- §10.3.3：从 `coverage_claims.command_parameter_ids` 和 `coverage_claims.raw_stable_claims` 各删除唯一 `parameter:config:default`；不新增、改名或发明任何替代 parameter claim
- §10.3.3：`init-deepseek-config-explicit` precondition 保持不变
- §10.5 registry 检查：精确查询 5 条保留 row，断言不含 `parameter:config:default` 且 precondition 仍存在

**Closure 验证**：
- 代码证据确认：5 条 row 当前均含 `parameter:config:default` claim + `init-deepseek-config-explicit` precondition ✓
- plan 指定了精确操作（删除，不替换），不再要求实施 Agent 发明替换文本 ✓
- S6 validation 中有精确的 grep/assert 检查 ✓

**Closure 状态**：✅ **CLOSED**。registry claim 清理操作已精确到 field-level。

---

### 2.6 DS F-04: S6 test 命令中的文件名断言（原严重程度：低，non-finding）

**原始 finding**：plan 注释说"文件名若仓库实际不存在"需 fallback——但实际所有路径已验证存在。

**Controller 裁决**：`non-finding`，证据失效。

**Plan 修订**：§10.5 删除了"若路径不存在"注释。

**Closure 状态**：✅ **CLOSED**（non-finding，无需修复动作）。

---

## 3. Rejected-with-reason 合理性验证

### MiMo-003: S4/S5 dispatch.py 重叠（原严重程度：中）

**原始 finding**：S4 和 S5 都修改 `dispatch.py`，S4 的 guard 函数签名可能在 S5 需要扩展。

**Controller 裁决**：`rejected-with-reason`。

**Plan 修订（§8.2.2.7）**：
> "S4 guard 的 request/permit 不预埋 optional successful_response_identity。S5 依赖 accepted S4 guard 后，再在两路 winner writer 的 typed accepted/rejected payload 上做 required identity 扩展；guard 只决定'能否提交 terminal'，不拥有'terminal payload 写什么'，因而 S5 无需重构或扩展 guard 签名。"

**合理性验证**：
- S4 guard (`begin_compaction_terminal_commit_in_transaction`) 只返回 `CompactionTerminalCommitPermit(OPEN)` 或 `CompactionTerminalClosed(COMPACTED/FAILED/INVALID_MULTIPLE)` ✓
- guard 不携带 payload，不负责构造 event 内容 ✓
- S5 的 identity 扩展发生在 winner 路径的 payload 构造阶段（在 guard 返回 OPEN permit 之后），不在 guard 内部 ✓
- 两阶段职责分离清晰：S4 = "能不能写 terminal"，S5 = "terminal 里写什么 identity" ✓

**结论**：rejection 合理。S4 guard 是纯 permission check，S5 identity 是 payload content，二者无签名耦合。实施顺序 S4→S5 不会导致 S4 代码被 S5 重构。

---

## 4. 五个指定焦点领域深度验证

### 4.1 shared compaction_terminal owner 对 proactive/reactive winner/loser 的线性化

**plan 方案**：新增 `dayu.host.compaction_terminal`，定义：
- `begin_compaction_terminal_commit_in_transaction(transaction, event_log_store, *, operation_id, expected_trigger_source) -> CompactionTerminalCommitPermit | CompactionTerminalClosed`
- CAS 线性化点：SQLite write transaction 串行 + transaction 内 fresh operation projection read

**code-generation-ready 检查**：

| 维度 | 状态 | 证据 |
|---|---|---|
| 入口签名完整 | ✓ | operation_id、expected_trigger_source、transaction、event_log_store 均已指定 |
| 返回类型封闭 | ✓ | OPEN / COMPACTED / FAILED / INVALID_MULTIPLE 四种 disposition |
| winner 路径明确 | ✓ | OPEN permit → 在同一 transaction 写 artifact + descriptor + event |
| loser 路径明确 | ✓ | COMPACTED/FAILED → no-op/diagnostic，不写 artifact/event/fallback/start |
| INVALID_MULTIPLE 处理 | ✓ | fail closed，保持已有 truth，不追加第三 terminal |
| 串行性保证 | ✓ | SQLite write lock 天然串行；permit 不持久化、不跨 await/事务传递 |
| 两路 writer 覆盖 | ✓ | dispatch.py（proactive）+ engine_ingest.py（reactive）均需调用同一入口 |
| 不演化为通用 framework | ✓ | §8.2.2 明确"只理解 compaction request/trigger/terminal，不接受任意 event type" |

**不越界检查**：
- guard 只读 EventLog（compaction request + terminal events），不读 Run/Attempt/Session 状态 ✓
- guard 不拥有 "terminal payload 写什么"（那是 S5 的职责） ✓
- guard 不创建 DB unique index、新表或迁移 ✓
- guard 不预埋 optional identity 字段 ✓

**潜在关注点**：
- reactive 路径当前在 `_append_reactive_compacted_event()` 中先写 artifact bytes/descriptor，再 append event。plan §8.2.1.4 要求 "late loser 必须在 artifact/descriptor...之前就返回 no-op"。这意味着 guard 调用必须在 `_append_reactive_compacted_event()` 的最开头、在 artifact write 之前。plan 对此已明确。实施 Agent 需注意调用顺序。
- `expected_trigger_source` 校验：若同一 operation 被两个不同 trigger source 同时到达（理论上不应发生），first-commit-wins 的正确性成立——guard 的 trigger check 使第二个 writer 的 expected_trigger_source 与 request 中的实际 trigger 匹配后，仍会因 terminal 已存在而返回 CLOSED。

**结论**：✅ code-generation-ready。线性化方案正确利用 SQLite write serialization，winner/loser 语义封闭且不越界。

---

### 4.2 Ctrl+C + sole QUEUE 是否同时满足 F08/F09

**F08 语义**：type-ahead Enter 创建一个 QUEUE follow-up，不 STEER。
**F09 语义**：连续 Ctrl+C 表达取消完成后退出，不留下 running/queued Run。

**plan 修订后的状态机覆盖**：

| F 项 | 状态表行 | 满足？ |
|---|---|---|
| F08: Enter → sole QUEUE | "active，尚无 queued follow-up \| 第一份非空 Enter \| 提交恰好一个 QUEUE,target=None follow-up，保存为 sole queued task；不 STEER" | ✓ |
| F08: 第二个 Enter 不创建 | "active，已有 queued follow-up \| Enter \| 不创建第二个 Run；保留当前新 draft 并给出有界中性提示" | ✓ |
| F09: 第二次 Ctrl+C 登记 exit | "cancelling，exit_after_cancel=false \| Ctrl+C \| 只设 exit_after_cancel=true；不取消 cancel task、canonical waiter、watcher、Host或attachment" | ✓ |
| F09: exit-after-cancel 收口 | "exit-after-cancel \| current/queued completion \| 停止接收新输入；先等当前 cancel target canonical terminal，再对本 invocation 的 sole queued submit 等待明确 acceptance 结果；若已 durable accepted，必须使同一 queued Run 恰好执行一次并等其 canonical terminal；只有明确未 accepted 时才可无 queued terminal 继续，最后完成 display/composer/attachment cleanup 并 exit 130" | ✓ |
| F09: no-label fresh Session 安全 | §6.5.5 "无 label fresh Session，不得在进程退出时留下永久 QUEUED Run 等待一个不会到来的 fresh invocation" | ✓ |
| F09: third+ Ctrl+C no-op | "cancelling/exit-after-cancel，exit_after_cancel=true \| Ctrl+C \| 第三次及之后一律 no-op；绝不再次发 cancel、Task.cancel() canonical waiter 或强关 Host/attachment" | ✓ |

**组合语义验证**：
1. 用户在 active Run 期间按 Enter → 创建 sole QUEUE follow-up ✓
2. 用户连续按 Ctrl+C → 第一次进入 cancelling + exit_after_cancel=false，第二次设 exit_after_cancel=true ✓
3. exit-after-cancel 阶段：先等当前 cancel terminal，再把 accepted sole QUEUE 提升为 current 并等其 terminal ✓
4. queued Run 恰好执行一次（Host 幂等保证 + CLI sole slot 保证） ✓
5. 无 label fresh Session 退出后不留永久 QUEUED Run ✓

**结论**：✅ F08 和 F09 同时满足。状态机在 active/queued/cancelling/exit-after-cancel 四个关键阶段的转移均确定性指定。

---

### 4.3 Engine/Host identity taxonomy 是否符合当前 compactor request 事实

**plan taxonomy（§9.3）vs 代码事实**：

| Taxonomy claim | 代码事实 | 匹配？ |
|---|---|---|
| `compaction_attempt_number` 每个 number 都构造新 `AgentRunRequest` | `run_compaction_operation()` L1105 每个 attempt 调用 `prepare_compactor_proposal_run_input()` | ✓ |
| `compactor_engine_run_id` 作为 `AgentRunRequest.run_id` | `_agent_request_vnext()` L437 `run_id=compactor_engine_run_id` | ✓ |
| `RunnerRequestIdentity.attempt_id` 与 `execution_id` 在 compactor 路径必须显式为 `None` | `_agent_request_vnext()` L439-440 `attempt_id=None, execution_id=None` | ✓ |
| `runner_call_index` 在单 Engine request 内从 1 递增，新 Host proposal attempt 创建新 Engine run 后重置 | `RunnerRequestIdentity` L46 `runner_call_index: int`，L100 docstring "从 1 起" | ✓ |
| `client_correlation_id` 由完整 request identity canonical tuple 派生 | `runner_identity.py` L67-77 `_build_client_correlation_id(...)` 用 canonical tuple 做 SHA-256 | ✓ |
| `provider_request_id` 缺失时记 `unavailable`，不从 client correlation 或 manifest 伪造 | plan §9.3 provider_request_id 行 "vendor 返回的可选 request id；缺失时只记 unavailable" | ✓ |
| Host `compaction_attempt_number` 与 Engine `attempt_id` 是不同字段，不互相填充 | code 已证明 compactor 路径 attempt_id=None；plan 明确 "绝不得填 Host compaction_attempt_number" | ✓ |
| LENGTH continuation 最终 fail closed | plan §9.3.2 "LLMContextCompactor 对最终 finish_reason=LENGTH 已明确 fail closed" | ✓ |

**结论**：✅ taxonomy 与当前 compactor request 代码事实完全一致。七种 identity 的 owner、生命周期和不可混用规则均有代码证据支撑。

---

### 4.4 五条 registry claim 清理是否精确

**plan §10.3 指定操作**：

1. 保留 5 条 row：`prompt.PC-PW-R2-03`、`-04`、`-06`、`-08`、`-10`
2. 对每条保留 row，从 `coverage_claims.command_parameter_ids` 删除 `parameter:config:default`
3. 对每条保留 row，从 `coverage_claims.raw_stable_claims` 删除 `parameter:config:default`
4. 不新增、改名或发明任何替代 parameter claim
5. `coverage_claims.precondition_state_ids` 中的 `init-deepseek-config-explicit` 保持不变

**代码证据验证**：

```
prompt.PC-PW-R2-03:
  has parameter:config:default = True       ← 删除目标
  precondition_state_ids = ['init-deepseek-config-explicit']  ← 保留

prompt.PC-PW-R2-04: (同上)
prompt.PC-PW-R2-06: (同上)
prompt.PC-PW-R2-08: (同上)
prompt.PC-PW-R2-10: (同上)
```

**精确性检查**：
- 5 条 row ID 全部存在于 `cli_ci_scenarios.json` ✓
- 每条当前均含 `parameter:config:default` ✓
- 每条当前均含 `init-deepseek-config-explicit` precondition ✓
- 操作是纯删除（两个 array 各删一个 string），不涉及 JSON 结构变更 ✓
- 不发明替代 claim，不把 config source 伪装成 parameter claim ✓
- S6 validation 中有精确断言检查（grep 查询返回数、precondition 保留） ✓

**结论**：✅ 五条 registry claim 清理精确到 field-level，实施 Agent 无需自行判断或发明替代文本。

---

### 4.5 自动 Gateflow 表述是否正确

**plan §0 中的 Gateflow 表述**：

> "当前 Gateflow entry：`plan review -> fix` 中的 accepted-finding fix"
> "Next entry point：`re-review`；本 Agent 子任务在 fix 与验证完成后把控制权交回 controller，由 controller 立即派发独立 re-review；整个 Gateflow 继续按固定 Gate Order 自动推进，不在普通 gate 停止"

**验证**：
- 当前 gate 确实是 `plan` gate，处于 `plan review -> fix` 子状态（两份 review 完成、accepted findings 已修复） ✓
- "Next entry point: re-review" 正确——本 re-review 正是该 entry point 的产物 ✓
- "由 controller 立即派发独立 re-review" 正确——controller（用户）派发了本 AgentDS re-review ✓
- "整个 Gateflow 继续按固定 Gate Order 自动推进" 正确——plan gate 通过后应进入 accepted plan commit → implementation gate ✓
- "不在普通 gate 停止" 正确——plan review → fix → re-review 是 plan gate 内的子流程，不创建新 gate ✓

**结论**：✅ Gateflow 表述正确，与实际 gate 状态和推进顺序一致。

---

## 5. 修订是否引入新 blocker

逐领域检查：

### 5.1 S4 compaction_terminal guard 的边界

- guard 要求 reactive 路径在 artifact write 之前调用：plan 已明确要求，实施时需注意调用顺序，但 plan 规格足够精确 ✓
- `expected_trigger_source` 校验：若同一 operation 同时被 proactive 和 reactive 触发（理论上不应发生），first-commit-wins，行为正确 ✓
- guard 不持久化 permit：permit 只在单个 write transaction 内有效，不会跨事务泄漏 ✓

**无新 blocker。**

### 5.2 S2 Ctrl+C 状态机的完整性

- 非空 draft Ctrl+C 清空后不设 idle_exit_pending，进入 "idle + 空 draft + idle_exit_pending=false" 状态 ✓
- 后续 Ctrl+C 行为由该行接管（登记 pending），再后续 Ctrl+C 执行 exit ✓
- §6.5 要求 6 的 "第二次以及之后" 措辞与状态表的 "第三次及之后" 一致（均为 cancelling 状态内的 Ctrl+C 序数） ✓
- `_InteractiveExitIntent` 的 idle_exit_pending 重置规则明确：任何非 Ctrl+C composer event 均清除 ✓

**无新 blocker。** 有一个轻微的措辞歧义（"第二次以及之后" vs "第三次及之后"），但状态表是 authoritative source，以状态表为准。

### 5.3 S5 identity payload 与 S4 guard 的分离

- S4 guard 返回 OPEN/CLOSED，不携带 payload ✓
- S5 在 winner 路径（OPEN permit 之后）构造带 identity 的 accepted/rejected payload ✓
- `CONTEXT_COMPACTED` 的 `successful_response_identity` required，`CONTEXT_COMPACTION_ATTEMPT_REJECTED` 的 nullable ✓
- Engine `FinalAnswerData` 和 `EngineRunOutcomeFinalAnswer` 新增 required `response_identity` ✓

**无新 blocker。** S4/S5 的职责边界干净。

### 5.4 S3 delayed recovery task 生命周期

- 每个 live attachment/Session 至多一个 delayed task ✓
- task 在 sleep 前可被 cancel 且不写事实 ✓
- task 在 actor 已提交后 shield 收口再释放 attachment mutex ✓
- attachment close / Host close 取消并 join 未开始 task ✓

**无新 blocker。**

### 5.5 S1 parser/public contract 清理

- `--config` 在 prompt/interactive 完全不可达（含 root parser 前置 `--config`） ✓
- interactive `--ticker` 删除，session resume --mode interactive 拒绝 ticker ✓
- 双 namespace → 单 `cli.agent.<label>`，旧 namespace 不读取/不迁移/不兼容 ✓
- `--kind` 删除 ✓

**无新 blocker。**

---

## 6. New findings

经过 adversarial review，**无新增 material finding**。以下是一个轻微观察，不构成 blocker：

### N-01-观察-低-interactive_submit_client_request_id 规格未完全展开

- **位置**: §6.5 关键实现要求 7
- **问题类型**: 实施细节未展开（非契约缺失）
- **当前写法**: "queued follow-up使用新的稳定 interactive_submit_client_request_id(turn_index)"
- **为什么不是 blocker**: `client_request_id` 是 Host admission/idempotency 的 correlation 标识，不是 durable schema 或 public contract。其格式（如 `f"interactive-submit-{turn_index}"`）是实施细节。plan 不需要指定精确格式，因为：
  - Host 幂等/admission 只要求 `client_request_id` 在同 Session 内不重复
  - `turn_index`（monotonic counter）已提供唯一性保证
  - 调用方（`_start_interactive_turn`）和消费方（Host dispatch）都在 CLI/Host 边界内，不涉及跨系统协议
- **建议**: implementation agent 可自由选择格式；plan 无需修订。

---

## 7. Open questions

无阻塞 open question。以下所有原 OQ 已关闭：

| OQ | 状态 | 关闭方式 |
|---|---|---|
| OQ-01: `_resume_interactive_ticker()` 存在性 | 已关闭 | 代码证据确认存在于 `session.py` L575 |
| OQ-02: reactive 路径是否需 F11 guard | 已关闭 | MiMo-001 裁决 accepted，engine_ingest.py 纳入 S4 |
| OQ-03: POSIX PTY smoke CI 环境 | 已关闭 | plan §12.1-§12.2 分类为 allowed platform/capability variant |

---

## 8. Residual risks（更新后）

以下 residual risks 来自 plan §12.1，经 re-review 确认仍然有效且不阻塞 plan gate：

| 类别 | 风险 | plan 内控制 | 残余 | 变化？ |
|---|---|---|---|---|
| terminal capability | 不同终端无法区分 Shift+Enter/Enter | 只支持证实的 exact sequence | 未知终端不宣称支持 | 不变 |
| input race | terminal/Enter/Escape/SIGINT 同批完成 | generation + terminal-first 裁决 | OS/终端实现差异 | 不变 |
| queue lifecycle | exit-after-cancel 需等待 queued follow-up 完成 | 停止新输入，不取消 sole submit | 用户可能期望更快退出 | 不变（controller 已裁决） |
| recovery clock | wall clock/heartbeat 临界点 | `<`/`>=` 边界、UTC deadline、CAS | 极端时钟回拨保守不恢复 | 不变 |
| process proof | pid 存在但无 identity | 继续 inconclusive，不恢复 | G02 完整矩阵仍是 gap | 不变 |
| compaction concurrency | proactive/reactive late result 竞争 | 同一 trigger-aware Host owner + SQLite write serialization | 已损坏旧 DB 不兼容 | **降低**（F11 从 proactive-only 扩展为 all-trigger） |
| live/fresh owner | in-memory flight crash 消失 | durable request/manifests + fresh RW recovery | 不提供跨 opener 共享 single-flight | 不变 |
| provider evidence | provider 不返回 request id | explicit unavailable + effective identity | 行为项 29 需 G06 后续 | 不变 |
| secrets | RunnerSpec 含 endpoint/credential/header | 窄 identity type、exact payload、canary 扫描 | provider/model 本身需配置 schema 保证 | 不变 |
| coverage/platform | POSIX PTY 与 Windows 差异 | POSIX 真实 PTY + Windows CI owner boundary | 本地无法替代 Windows runner 证据 | 不变 |

---

## 9. Validation checklist（re-review 视角）

以下验证项已通过 re-review 确认：

- [x] Controller 裁决 trace（10 条）全部在 plan 中有对应落点
- [x] 6 条 accepted findings 全部真实关闭
- [x] 1 条 rejected-with-reason（MiMo-003）理由成立
- [x] 1 条 non-finding（DS F-04）证据失效确认
- [x] 3 条 OQ 全部关闭
- [x] S4 compaction_terminal owner 对 proactive/reactive winner/loser 的线性化 code-generation-ready 且不越界
- [x] Ctrl+C + sole QUEUE 同时满足 F08/F09
- [x] Engine/Host identity taxonomy 与 compactor request 代码事实一致
- [x] 五条 registry claim 清理精确到 field-level
- [x] 自动 Gateflow 表述正确
- [x] 修订无新 blocker
- [x] plan §16 closeout 状态声明（`accepted-finding fix complete; awaiting re-review`）准确

---

## 10. Final re-review conclusion

**结论：PASS**

Plan 已对所有 controller accepted findings 完成修复。修订精准、边界清晰、无新增 blocker。五个指定焦点领域均 code-generation-ready 且不越界。

**Gate blocker 判断**：无。原 DS review 的 F-01 和 F-02 两个 accepted gate blocker 已关闭；MiMo review 的 001（高严重度）已关闭。当前 plan 无剩余 accepted gate blocker。

**建议**：
1. Controller 可批准 plan 进入 accepted plan commit → implementation gate
2. Implementation agent 应注意 N-01 中的 `interactive_submit_client_request_id` 为实施细节，可自由选择格式
3. Implementation agent 应特别注意 S4 reactive 路径的 guard 调用顺序（必须在 artifact write 之前）
4. §12.1 的 residual risks 继续有效，在 implementation completion report 中应如实报告

**下一 entry point**：controller review → accepted plan commit → implementation gate。

---

## Appendix: 与原始 review artifact 的关系

本 re-review artifact 是 plan review gate 内的 `re-review` 产物。它与两份原始 review（`plan-review-20260801-143257.md`、`plan-review-20260801-143623.md`）的关系：

- **不修改**原始 review artifact
- **不代替**原始 review 的 findings——原始 findings 的 closure 状态由本 re-review §2 逐项记录
- **独立裁决** plan 是否通过 plan gate
- Plan §15 的 controller fix trace 是本 re-review 的输入，不是 review target

本 artifact 也不代替后续 implementation gate 的 implementation review、deepreview 或 final closeout review。
