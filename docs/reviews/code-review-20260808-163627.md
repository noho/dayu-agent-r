# F18 Slice 1 Current Changes Deep Review（MiMo）

## Findings

未发现实质性问题。

## Binary verdict

**PASS**

## Review scope

- 模式：deepreview Current Changes Mode。
- Selected base：`HEAD`。
- 审查对象仅限：`docs/cli_ci_scenarios.json`、`docs/cli_ci_oracles.json`、`docs/cli_ci.md`、`docs/reviews/pr-190-oracle-adjudication-20260808.md` 的未提交差异。
- 明确排除：生产代码、测试、B2 实现/证据、provider 调用、提交与推送，以及 ownership 不明的 `docs/reviews/plan-review-20260808-095346.md`；未读取该文件。
- 审查时间：2026-08-08 16:36:27 +0800。

## Direct evidence and contract checks

### 1. B1 裁决语义与 owner 边界

用户 / Oracle controller 是 B1 裁决 owner；本 Slice 只把已确认裁决投影到 stable scenario、accepted Oracle history/summary/notes、handbook 与裁决记录，没有改写产品行为或把 observation artifact 自行当作裁决 owner。

- `tool-trace-formal@2` 在 `docs/cli_ci_scenarios.json:113044` 从 `unadjudicated` 投影为 `accepted`，并记录新的用户裁决 identity。
- `cli.interactive.core-execution@2` 只新增一条用户裁决 history，并同步 summary/notes；accepted predicate、适用版本和其它 Oracle 对象未改。
- `docs/cli_ci.md:538` 与 `docs/reviews/pr-190-oracle-adjudication-20260808.md:49` 对 mandatory evidence、limitation 和 B2 pending 的表述与用户已确认目标一致。

结构级 HEAD/working-tree 比对结果：

- changed scenario objects：仅 `interactive.interactive.g06.tool-trace-formal@2`；
- changed Oracle objects：仅 `cli.interactive.core-execution@2`；
- `interactive.interactive.g06.cap-constrained-memory-replacement@1` 对象 exact equal；
- 两个 registry 除记录列表外的全部顶层对象 exact equal；scenario registry 的 `registry_status` 与 `readiness_proof` 未变。

因此没有 B2 裁决提前发生、整体 readiness 翻转或 semantic ownership drift。

### 2. 六个 immutable identity 与 manifest 传播

对只读 bundle `pr190-formal-g1a2xu` 重新计算 SHA-256，六项均与本次文档登记值精确一致：

| Evidence | SHA-256 | Registry / document propagation |
|---|---|---|
| Human report | `de7ee64de11140add816facf9926c2cf17aa13a4176bd873bc0f91ed20b70f79` | `report_sha256`、相对 ref、裁决记录 |
| B1 summary | `dfe3604bba8c7f8bda6b0d8a80639a87ac77a2d1c03f0e7baa28236e554d7c0a` | 相对 ref `#b1`、digest manifest、裁决记录 |
| Cold analysis JSON | `f1b162c185c977bdb24882cdc7b1eb8d273defe02cfa1dbf920248d07754ec3a` | 相对 ref、digest manifest、裁决记录 |
| Cold analysis Markdown | `2bba07d27ec3307a44cc0d6531c73cb7565bcd72eea7304c7f56d1e0961c41da` | 相对 ref、digest manifest、裁决记录 |
| Public digest | `567d5539d9e745e78379093117275231c7b96ab5ac03536f13aec9475e476153` | 带 SHA 的相对 ref、裁决记录 |
| Final secret/path scan | `6f33e30ced5dbbcc96c6cadcc93bf1c5c2ad3d261c5f3386ccbec74c3ba52ba8` | 带 SHA 的相对 ref、裁决记录 |

Public digest 内 report、summary、cold JSON/Markdown 的路径、大小和 SHA 精确匹配；final scan 报告 `secret_scan.status=complete`、零 hits，`path_hygiene.status=complete`、零 violations。旧 bundle 内容未被改写，registry 只引用 immutable identity。

### 3. Process outcome 与 canonical Run terminal 正交

Scenario 保留 raw PTY owner 的 `execution_outcome=error` 与 `exit_codes=[1]`（`docs/cli_ci_scenarios.json:113133`），没有用业务成功覆盖进程结果。独立 public report 记录 10 个 accepted Run 全部为 typed `RUN_SUCCEEDED`，hot owner 对 canonical terminal identity 的六字段比较为 6/6 exact match。

本次裁决据 mandatory Host/Tool Trace contract 接受 B1，同时原样保留 raw process outcome；两者 owner、对象和判断问题不同，不构成矛盾。`evidence_status=sufficient`、`gap_kind=none` 与 `conformance_at_observation=matches-accepted-oracle` 只回答 B1 mandatory conformance，不把 exit code 重标为成功。

### 4. Limitation 与 B2/readiness 隔离

Public summary 直接记录 `cold_compactor_response_count=0`、`cold_identity_disposition=needs-more-evidence`、`provider_request_id_availability=unavailable`；cold analysis 的 `compactor_responses` 为空，并保留 typed limitation。四个 owner 文档均将这些事实描述为 limitation/residual evidence question，而非该 scenario 的 mandatory readiness gap。

同时，`cap-constrained-memory-replacement@1` 保持 `unadjudicated`，handbook 与裁决记录均明确 B2 仍需 fresh observation 和用户单独裁决；registry 继续为 `calibration`，overall readiness 未标 ready。

### 5. Adversarial boundary checks

- JSON：两个 registry 均通过 `.venv` 下 `python -m json.tool`。
- Diff hygiene：四个目标文件的 `git diff --check HEAD` 通过。
- Scope attack：递归对象比对未发现其它 scenario、Oracle 或顶层 readiness 数据变化。
- Evidence substitution attack：公开 summary 的 `conformance_disposition` 仍是 observation-only；accepted 状态来自新增用户裁决 history，不是 evidence artifact 自我裁决。
- Path/secret attack：HEAD 到 working tree 的新增行未引入私有绝对路径；B1 的旧绝对 `report_id` 反而被 bundle-relative public ref 替换。新增内容未包含 credential、raw SQLite 或私有 evidence 路径。
- Coupling check：B1 status、Oracle history、handbook 和 adjudication report 都引用同一裁决事实与同一 evidence identities，没有由 profile digest、事件顺序或错误字符串反推业务结论。

## Open questions

无阻断问题。

## Residual risks

- Current Changes Mode 只裁决 `HEAD..working tree` 差异。裁决文档中 HEAD 已存在的 F15 历史 Root 行不属于本 Slice 新增内容；本 review 不把它计为 F18 回归，也不对其另行清理。
- Cold analyzer 无 payload resolution、provider-native request id unavailable 仍是已知 evidence limitation；它们不影响本次 B1 mandatory contract，但后续若改变 mandatory evidence 定义，需要新的独立 work unit 与 fresh evidence。
- B2 formal observation、裁决与 readiness refresh 均未完成，不能从本 Slice 的 PASS 推导整体 readiness。
