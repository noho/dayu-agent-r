# Code Review — upload-filing-ticker-alias-contract Aggregate Deepreview Re-review

## Scope

- Mode: current changes（aggregate deepreview re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `2b11cb21`（accepted S2 commit）
- HEAD: working tree（uncommitted S2 fix diff，+933/-345，26 files）
- Output file: `docs/reviews/code-review-20260815-032921.md`
- Included scope: 前序 aggregate deepreview（`code-review-20260815-023958.md`）的 4 项 findings 修复验证。S2 fix codex（`wu-upload-filing-ticker-alias-contract-s2-fix-codex.md`）全文已读。uncommitted diff 全部 14 个生产文件、11 个测试文件、1 个 artifact 已逐文件走读。
- Excluded scope: 未修改的 Host/Engine 文件；UF-PF05、oracle/scenario registry、冻结 evidence（controller 已裁决排除）。
- Parallel review coverage: 主 reviewer 独立走读全部 diff hunk，验证 4 项 finding 修复的直接代码证据。

## 验证结论（独立复现）

- `python -m pytest tests/fins tests/tools/test_combined_tools_acceptance.py tests/cli/test_fins_commands.py -x -q` → **1753 passed, 1 skipped, 3 warnings**
- `pyright` 全部 14 个修改生产文件 → **0 errors, 0 warnings**
- residue scan：`resolve_existing_ticker`、`_canonicalize_ticker_alias`、`_normalize_company_ticker_aliases`、`upsert_company_meta_for_upload`、`_company_meta_path`、`SecDownloader.normalize_ticker`、`_normalize_ticker`（docling）全部零命中
- `SecDownloader` 中剩余 `.upper()` 均为 SEC ticker map 比较（已 normalized canonical vs 大写数据源）、section name/form type/fiscal period 归一化，不承担用户输入 ticker 归一化

## Finding 1 验证 — durable corruption 唯一 typed owner

### 修复内容

**storage owner boundary 升级：**

- `_fs_company_meta_core.py:_get_company_meta_unguarded` 不再直接读 `meta.json` + `CompanyMeta.from_dict`（会产生裸 `ValueError`）。改为调用 `_read_published_company_identity(ticker_dir, ...)`，descriptor/meta schema、symlink/non-regular、identity mismatch 分别进入 closed `invalid_descriptor`/`invalid_meta`/`identity_mismatch` corruption kind。meta-less corpus 仍返回 `FileNotFoundError`（不被误判为 corruption）。
- `_fs_filing_upload_state_core.py:read_filing_upload_state` 不再用 `_ticker_dir_if_present_for_read`（`Path.exists` 隐式判断）。改为显式 `_lstat_optional_storage_path`，仅 `ENOENT` 视为 missing；target symlink/regular file 直接抛 `CompanyTickerIdentityCorruptionError(kind="invalid_descriptor")`。
- `_fs_storage_infra.py:begin_batch` target 非目录/symlink 判定从 `ValueError` 改为 `CompanyTickerIdentityCorruptionError(kind="invalid_descriptor")`。
- `_fs_storage_infra.py:_commit_batch_with_publication_guard` commit-time backup target 非目录/symlink 判定同上。
- `_fs_storage_infra.py:_require_copyable_ticker_tree` symlink/special file 判定从 `ValueError` 改为 `CompanyTickerIdentityCorruptionError(kind="invalid_descriptor")`。
- `_fs_storage_infra.py:_ticker_dir_for_read` 改为 `_lstat_optional_storage_path` + `_read_published_company_identity`，corruption 投影统一。
- `_fs_storage_infra.py:begin_batch` 存在 target 时改用 `_read_published_company_identity` 替代旧 `_read_identity_descriptor`。

**SEC/CN material workflow failure boundary 前移：**

- `sec_upload_workflow.py:run_upload_material_stream` 和 `cn_pipeline.py:upload_material_stream` 将 `_safe_get_document_meta` 和 `resolve_upload_action` 移入 `try` 块内，使 published corruption 在 job observation 之前被捕获。
- material terminal catch 统一调用 `fins_upload_failure_from_exception(...)`（与 filing 一致），`CompanyTickerIdentityCorruptionError` → `storage/storage_io`，不再落入 `unexpected_runtime`。

**tool 入口 typed corruption branch 可达：**

- 旧测试用 monkeypatch 注入 `CompanyTickerIdentityCorruptionError`，未走真实 corruption 路径。新测试 `test_upload_tool_projects_real_workspace_identity_corruption` 参数化 5 种真实 corruption 形态（malformed meta、meta symlink/directory、target symlink/regular file），直接构造 durable state 而非 monkeypatch。
- tool 的 `except CompanyTickerIdentityCorruptionError` 分支现在由真实 `_normalize_upload_request → _validate_fins_upload_filing_static → read_filing_upload_state` 链路可达，且早于宽 `except ValueError` 分支。

**CLI 入口统一投影：**

- `test_upload_filing_prevalidation_identity_corruption_is_typed_and_path_free` 参数化 6 种 corruption 形态（descriptor malformed、meta malformed、meta symlink/directory、target symlink/regular file），断言单行 bounded failure，不含路径、traceback 或内部异常类型。

**material 入口统一投影：**

- `test_upload_material_identity_corruption_projects_storage_terminal` 参数化 5 种 corruption 形态，断言 failed terminal 为 `storage/storage_io`、`stored_file_count=0`、无 source publication 或内部 schema 泄漏。

### 直接证据

- `_fs_company_meta_core.py:66-93` — `_get_company_meta_unguarded` 改为 `_read_published_company_identity` 路径
- `_fs_filing_upload_state_core.py:83-103` — `read_filing_upload_state` 改为 `_lstat_optional_storage_path` + `stat.S_ISDIR` 判定
- `_fs_storage_infra.py:546-547` — `begin_batch` target 非目录 → `CompanyTickerIdentityCorruptionError`
- `_fs_storage_infra.py:562-570` — `begin_batch` 存在 target 时用 `_read_published_company_identity`
- `_fs_storage_infra.py:777` — commit backup target 非目录 → `CompanyTickerIdentityCorruptionError`
- `_fs_storage_infra.py:2715-2735` — `_require_copyable_ticker_tree` 用 `_lstat_optional_storage_path`
- `sec_upload_workflow.py:452-490` — material failure boundary 前移
- `cn_pipeline.py:1067-1103` — CN material failure boundary 前移
- `test_fins_ingestion_tools.py:1089-1164` — 真实 corruption tool 投影测试（5 形态参数化）
- `test_fins_commands.py:1976-2040` — 真实 corruption CLI 投影测试（6 形态参数化）
- `test_sec_pipeline_upload_material_stream.py:292-386` — 真实 corruption material 投影测试（5 形态参数化）

### 结论

**Finding 1 已修复。** 同一 durable corruption 事实现在统一投影为 typed `CompanyTickerIdentityCorruptionError`，storage owner boundary（`get_company_meta`/`read_filing_upload_state`/`begin_batch`/commit backup/`_require_copyable_ticker_tree`/`_ticker_dir_for_read`）全部使用同一 `_read_published_company_identity` 或 `_lstat_optional_storage_path` 分型。tool/CLI/material 三个入口的 typed corruption branch 由真实路径可达，不再泄露内部 schema 术语。

## Finding 2 验证 — filing/material 共用 ticker identity 准入

### 修复内容

**共享 admission owner：**

- `ingestion_runtime.py` 新增 `_admit_fins_upload_ticker_identity(ticker, ticker_aliases)`，只拥有 `<=100` 数量边界与 typed usage 投影（`EMPTY_TICKER`/`INVALID_TICKER`/`INVALID_TICKER_ALIAS`/`TOO_MANY_TICKER_ALIASES`）。canonical/alias grammar、market normalization、canonical-equivalent 去重仍只由 `ticker_normalization.normalize_ticker` 与 `build_company_ticker_identity` 负责。
- `_validate_fins_upload_filing_static` 改为调用 `_admit_fins_upload_ticker_identity`，不再自行逐 alias 校验。
- `_normalize_upload_request`（material 入口）在 action/source_kind 校验前调用 `_admit_fins_upload_ticker_identity`，确保 material 非法 ticker/alias 在 observation/job 创建前被拒绝。

**SecDownloader.normalize_ticker 删除：**

- `sec_downloader.py:normalize_ticker` 方法（含 `strip().upper()` fallback）已完全删除。
- `sec_downloader.py:resolve_company` 改为 `normalize_ticker(ticker).canonical`（公共 normalizer）。
- `sec_downloader.py:_resolve_company_via_browse_edgar_ticker` 参数从 `ticker` 改为 `normalized_ticker`，不再自行 `strip().upper()`。
- `sec_download_workflow.py` 删除 `_DownloadWorkflowDownloader.normalize_ticker` protocol 方法。
- `sec_upload_workflow.py` 删除 `SecUploadWorkflowHost._downloader` protocol 属性。
- `sec_upload_workflow.py:run_upload_material_stream` 改为 `normalized.canonical`（`normalize_ticker` 已在上方调用）。
- `docling_upload_service.py:_normalize_ticker` 私有函数（含 `strip().upper()` fallback）已完全删除，改为 `normalize_ticker(ticker).canonical`。

**CLI material 超量 alias 投影：**

- `test_upload_material_alias_count_uses_typed_upload_admission` 断言 101 aliases → `EXIT_USAGE_ERROR` + `dayu-cli upload_material: --ticker 别名数量不能超过 100 个`（正确 command name 前缀）。

**tool material ticker/alias 投影：**

- `test_upload_tool_material_ticker_identity_usage_is_bounded_and_typed` 参数化非法 ticker（`"Apple Inc."`）、非法 alias（`"a apl"`）、101 aliases，断言 `invalid_argument`、精确业务文案、零 observation/job。

### 直接证据

- `ingestion_runtime.py:859-903` — `_admit_fins_upload_ticker_identity` 实现
- `ingestion_runtime.py:916-920` — `_validate_fins_upload_filing_static` 调用 admission
- `ingestion_runtime.py:7224` — `_normalize_upload_request` 调用 admission
- `sec_downloader.py` — `normalize_ticker` 方法已删除（grep 零命中）
- `sec_downloader.py:1180` — `resolve_company` 改为 `normalize_ticker(ticker).canonical`
- `sec_download_workflow.py:362` — download workflow 改为 `normalized.canonical`
- `sec_upload_workflow.py:436` — material workflow 改为 `normalized.canonical`
- `docling_upload_service.py:284,603` — Docling service 改为 `normalize_ticker(ticker).canonical`
- `test_fins_commands.py:2372-2416` — CLI material 超量 alias 测试
- `test_fins_ingestion_tools.py:1240-1304` — tool material ticker/alias 测试

### 结论

**Finding 2 已修复。** `_admit_fins_upload_ticker_identity` 是 upload admission 的唯一 owner，filing 和 material 共用。`SecDownloader.normalize_ticker` 及其 `strip().upper()` fallback 已完全删除，所有 SEC/CN/Docling workflow 直接消费公共 normalizer。material 非法 ticker/alias/超量 aliases 在 observation/job 创建前被 typed usage 拒绝。

## Finding 3 验证 — 零 mutation batch rollback

### 修复内容

- `sec_company_meta.py:upsert_company_meta` 返回类型从 `None` 改为 `CompanyMetaCommitIntent | None`。fresh 且 identity 未变化时返回 `None`（旧行为是 `return` 隐式 None）。
- `sec_download_workflow.py:_publish_sec_post_repair_mutations` 检查 `intent` 返回值：`None` 时 `rollback_batch`，非 None 时 `commit_batch`。
- `cn_download_workflow.py:_publish_cn_company_after_repair` 同上。
- `cn_pipeline.py` 和 `sec_upload_workflow.py` 的 material upload stream 将 `_safe_get_document_meta` 和 `resolve_upload_action` 移入 `try` 块，确保 corruption 在 job observation 前被捕获（与 Finding 1 修复协同）。

### 直接证据

- `sec_company_meta.py:83-110` — 返回 `CompanyMetaCommitIntent | None`
- `sec_download_workflow.py:810-827` — `intent is None` → rollback
- `cn_download_workflow.py:427-440` — `intent is None` → rollback
- `test_cn_download_workflow.py:951-1010` — `test_repeat_cn_company_publication_rolls_back_zero_mutation_batch` 断言 commit_calls=1、rollback_calls=1、published meta bytes 不变
- `test_cn_download_workflow.py:1726-1791` — 既有 CN replacement 测试断言从 `commit+commit` 改为 `rollback+commit`
- `test_cn_download_workflow.py:1777-1831` — CN complete phase A 测试断言从 `commit` 改为 `rollback`

### 结论

**Finding 3 已修复。** `upsert_company_meta` 返回明确 mutation signal（`CompanyMetaCommitIntent | None`），caller 在 `None` 时 rollback 而非 commit。SEC 和 CN 两条 download 路径均已修复。测试覆盖首次 commit + 重复 rollback 的 durable 断言。

## Finding 4 验证 — 死代码清除

### 修复内容

- `_fs_storage_infra.py:_company_meta_path(ticker, state)` 方法（18 行）已完全删除。
- `_company_meta_path_for_read` 保留（不同的 read locator contract）。

### 直接证据

- `grep -n "_company_meta_path\b" dayu/fins/storage/_fs_storage_infra.py` → 零命中（仅 `_company_meta_path_for_read` 存在）

### 结论

**Finding 4 已修复。** 死代码已删除，staging CompanyMeta 写路径唯一真实入口是 `_prepare_company_identity_commit` 的 `state.staging_ticker_dir / _SOURCE_META_FILENAME`。

## 并发/锁序/Recovery 验证

uncommitted diff 不改变锁序（recovery → identity → publication → writer），不改变 `_commit_batch_with_identity_guards` 结构，不改变 `_PHASE_STARTED` recovery 语义。`begin_batch` 和 `_commit_batch_with_publication_guard` 的 lstat 升级只替换 `Path.exists/is_symlink` 为显式 `_lstat_optional_storage_path`，不影响锁获取/释放时序。

`_require_copyable_ticker_tree` 改为 `_lstat_optional_storage_path` + `CompanyTickerIdentityCorruptionError`，不改变其在 publication guard 内的调用位置。

## 语义 Ownership 验证

- `CompanyTickerIdentity`（ticker_normalization）✓ — 无变更
- `CompanyMeta`（document_models）✓ — 无变更
- commit intent/authoritative merge（company_meta_contract）✓ — 无变更
- storage route/unique index/锁序（storage）✓ — lstat 升级不改变 owner
- typed failure（upload_failure + error_contract）✓ — corruption 统一投影为 `storage/storage_io`
- upload admission（`_admit_fins_upload_ticker_identity`）✓ — 新增唯一 owner，filing/material 共用

## 过度耦合检查

- `sec_upload_workflow.py` 删除 `SecUploadWorkflowHost._downloader` protocol 属性 → 解耦 upload workflow 与 downloader 实例
- `sec_download_workflow.py` 删除 `_DownloadWorkflowDownloader.normalize_ticker` protocol 方法 → 解耦 download workflow 与 downloader ticker 归一化
- `docling_upload_service.py` 删除私有 `_normalize_ticker` → 消除 duplicate normalizer
- `sec_company_meta.py:upsert_company_meta` 返回 `CompanyMetaCommitIntent | None` → mutation signal 显式化，不依赖 caller 猜测

无新增反向 import，无新增共享可变状态。

## LLM-facing 验证

- tool 投影：`fins_upload_start_failed` + `"工作区公司代码身份数据损坏，上传任务未启动。"` + `"请修复工作区公司元数据后重试。"` — 不含内部 schema 术语
- CLI 投影：单行 bounded failure，不含路径、traceback 或内部异常类型
- material terminal：`storage/storage_io` + `"工作区公司代码身份数据损坏，无法安全提交"` — 与 filing 一致
- material usage：`"--ticker 无法识别，请提供有效公司代码"` / `"--ticker 别名无法识别，请提供有效公司代码"` / `"--ticker 别名数量不能超过 100 个"` — CLI 前缀使用实际 command name（`upload_material` 而非 `upload_filing`）

## 测试充分性验证

| 测试类别 | 新增/修改 | 覆盖面 |
|---|---|---|
| 真实 corruption tool 投影 | 5 形态参数化 | malformed meta, meta symlink/directory, target symlink/regular file |
| 真实 corruption CLI 投影 | 6 形态参数化 | descriptor malformed, meta malformed, meta symlink/directory, target symlink/regular file |
| 真实 corruption material 投影 | 5 形态参数化 | malformed meta, meta symlink/directory, target symlink/regular file |
| material ticker/alias usage | 3 形态参数化 | 非法 ticker, 非法 alias, 101 aliases |
| CLI material alias count | 1 测试 | 101 aliases → typed usage |
| CN zero-mutation rollback | 1 测试 | 首次 commit + 重复 rollback + durable meta bytes |
| SEC zero-mutation rollback | 由既有 CN 替换测试覆盖 | rollback+commit phases |
| `_admit_fins_upload_ticker_identity` | 由 filing static validation 测试间接覆盖 | grammar + 数量边界 |

总测试数：**1753 passed, 1 skipped**。

## README Trigger 检查

本轮修改不新增 CLI 参数、用户工作流、workspace 位置、测试入口或分层关系。ticker CSV 与 typed conflict/corruption 的用户 contract 已由既有 README 覆盖。不触发 README 更新。

## Open Questions

无。

## Residual Risk

1. **ACL/NFS 跨平台未验证**：`os.lstat` 在 Linux NFS 上的行为可能与 macOS APFS 不同（特别是 inode/nlink 语义）。当前测试仅在 macOS 运行。pre-existing，本 WU 未引入。
2. **SEC ticker map `.upper()` 比较**：`sec_downloader.py:1189` 的 `str(row_mapping.get("ticker", "")).upper() != normalized` 是对 SEC EDGAR 数据源的 case-insensitive 比较（数据源条目为大写），不承担用户输入归一化，不属 loose fallback。
3. **identity route 全量扫描**：`resolve_company_ticker` 每次调用都全量扫描 published identities。当前 corpus 规模下性能无问题。pre-existing，本 WU 未引入。
4. **material company-name admission 未扩展**：controller defer，不在本轮 scope。
5. **resolver_version 常量不一致**：`market_resolver_v1.0.0`（upload）vs `market_resolver_v1`（SEC download）vs `cn_download_v1`（CN download）。既有行为，本 WU 未恶化，但 download/upload 交替互相刷新非身份字段。建议后续 WU 收敛为单一 owner 常量。

## 结论

**PASS** — 前序 aggregate deepreview 的 4 项 findings 全部修复验证通过：

- **Finding 1（中→已修复）**：durable corruption 统一投影为 typed `CompanyTickerIdentityCorruptionError`，storage owner boundary 全部使用 `_read_published_company_identity` 或 `_lstat_optional_storage_path` 分型。tool/CLI/material 三个入口的 typed corruption branch 由真实路径可达，不泄露内部 schema 术语。
- **Finding 2（中→已修复）**：`_admit_fins_upload_ticker_identity` 是 upload admission 唯一 owner，filing/material 共用。`SecDownloader.normalize_ticker` 及 `docling_upload_service._normalize_ticker` 的 `strip().upper()` fallback 已完全删除。
- **Finding 3（低→已修复）**：`upsert_company_meta` 返回 `CompanyMetaCommitIntent | None`，caller 在 `None` 时 rollback。
- **Finding 4（低→已修复）**：`_company_meta_path` 死代码已删除。

无新增 blocker。1753 tests passed。pyright 0 errors。residue scan 零命中。
