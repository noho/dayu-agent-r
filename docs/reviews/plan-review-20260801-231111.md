# Plan Review: WU-CLI-INTERACTIVE-02 S5/F13 Durable Builder Amendment — Final Independent Re-Review

## 0. Review metadata

- Provider: MiMo
- Reviewer: AgentMiMo
- Review type: final independent re-review
- Reviewed target: `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md` §9.1 / §10.5 / §13 及
  `docs/reviews/wu-cli-interactive-02-s5-f13-durable-builder-plan-amendment-proposal-codex.md`
- Adjudication reference: `docs/reviews/gateflow-wu-cli-interactive-02-s5-durable-builder-amendment-review-adjudication-20260801.md` §7
- Base HEAD: `ec9342ed9e5584123618f6b5c5eba8e93e2aed94`
- Branch: `codex/interactive-oracle`
- Review timestamp: `2026-08-01T23:11:11+08:00`
- Gate: `re-review` — 不得修改 plan、proposal、代码或任何既有 artifact

## 1. Review scope 与假设

### 1.1 Review scope

本次 re-review 聚焦以下验证目标：

1. **Controller §7 accepted AgentDS finding 闭合验证**：plan §9.1 / §10.5 / §13 与 proposal §3.2
   是否已将 AgentDS final finding 001（`accepted-low-clarification`）正确落实为可执行规则——fixture
   caller 仅使用 call site 真实已有的显式非敏感唯一上下文，不虚构维度，同时保持
   deterministic / event-unique / sibling-consistent 与无 default / 共享万能 helper / 反推。
2. **Controller §7 rejected findings 未引入副作用**：MiMo final finding 001
   （`rejected-duplicate-inventory`）与 MiMo final finding 002（`rejected-already-covered`）
   是否未导致 30-file duplicate list 或新抽象。
3. **独立只读 inventory 复核**：builder call 数量、file union、allowed-file delta 与 §9.1 声明一致。
4. **scope / owner / sequencing / 反例 / overengineering / validation / residual risk 全面检查**。

### 1.2 Assumptions tested

- Base HEAD `ec9342ed` 与 plan/proposal 声明一致 ✓（`git rev-parse HEAD` 确认）
- S5 implementation 尚未执行，两个 builder 当前无 `successful_response_identity` 参数 ✓（读取
  `dayu/host/context_events.py` 第 1092 行 / 第 1352 行确认）
- §9.1 的 builder inventory 数量（8 accepted / 7 rejected / 8 union / 15 total）与 repo 现状一致 ✓
  （独立 `rg` 复核）
- §9.1 的第一 amendment 25-file identity/typed-return closure 与 repo 现状一致 ✓
  （独立 `rg` 复核：FinalAnswerData 35 calls / 19 files，EngineRunOutcomeFinalAnswer 4 calls / 3 files，
  ContextCompactor 7 files，union 25 files）
- 5 个 gap files 在 call site 有足够的显式上下文可供 file-local factory 构造 deterministic identity ✓
  （逐文件读取确认）

## 2. AgentDS final finding 001 闭合验证

### 2.1 Finding 内容

Controller 裁决 `accepted-low-clarification`：当前文字把输入名写死为 `case_label` / `operation_label` /
`attempt_label`，但部分 helper 只有 `operation_id`。修订为：caller 使用当前 helper/call site 实际已有的
显式、非敏感、足以区分 event 的上下文。

### 2.2 §9.1 落实验证

Plan §9.1 已修订为（原文摘录）：

> "contract、projection、material、run-input 等未执行真实 Engine 的测试，若 helper 没有 run
> context，每个受影响文件可在自己的 fixture owner 内定义 private typed identity
> factory。test/case caller 必须使用当前 helper/call site 实际已有的显式、非敏感且足以区分
> event 的上下文（例如 case label、`operation_id`、attempt/run id 或显式 ordinal），由该
> factory 构造 deterministic 且对该 event 唯一的 `SuccessfulRunnerResponseIdentity`；具体输入
> 维度与参数名以现有 helper/call site 为准，不要求为统一形状虚构不存在的维度。"

### 2.3 §10.5 落实验证

Plan §10.5 已修订为（原文摘录）：

> "无 run context helper 的 caller 必须使用当前 helper/call site 实际已有的显式、非敏感且
> 足以区分 event 的上下文（例如 case label、`operation_id`、attempt/run id 或显式 ordinal），
> 由所在文件的 private typed factory 构造 deterministic、event-unique identity，再作为 required
> 参数显式传给 payload helper；具体输入维度与参数名以现有 helper/call site 为准，不要求虚构
> 不存在的维度。"

### 2.4 §13 checklist 落实验证

Plan §13 S5 checklist 第三项已修订为（原文摘录）：

> "caller 使用当前 helper/call site 实际已有的显式、非敏感且足以区分 event 的上下文（例如
> case label、`operation_id`、attempt/run id 或显式 ordinal），由该文件 private typed factory
> 生成 deterministic、event-unique typed identity，再作为 required 参数显式传给 payload
> helper；具体输入维度与参数名以现有 helper/call site 为准，不要求虚构不存在的维度"

### 2.5 独立 call site 上下文验证

对 5 个 gap files 的实际 call site 读取确认，每个文件均有足够显式上下文：

| 文件 | call site 上下文 | 可用维度 |
|---|---|---|
| `test_memory_projection.py:449` | `operation_id="event-context-compaction-requested-1"`, `accepted_attempt_number=1` | operation_id + attempt_number |
| `test_compaction_terminal.py:650` | `operation_id`（参数）, `accepted_attempt_number=1`, `ordinal`（参数） | operation_id + ordinal |
| `test_run_input_builder.py:7134` | `operation_id`, `accepted_attempt_number` | operation_id + attempt_number |
| `test_compact_material.py:3605` | `operation_id` | operation_id |
| `test_proactive_compaction_operation.py:268` | `operation_id`（参数）, `attempt_number`（参数） | operation_id + attempt_number |

所有 call site 均有 `operation_id`，部分还有 `attempt_number` / `ordinal`。这些维度均是当前 helper
签名的真实参数，不是虚构。file-local factory 可用 `(operation_id, attempt_number/ordinal)` 构造
deterministic、event-unique identity，无需虚构不存在的维度。

**结论：AgentDS final finding 001 已在 plan 三层（§9.1 / §10.5 / §13）完整闭合。**

## 3. Rejected findings 副作用验证

### 3.1 MiMo final finding 001（`rejected-duplicate-inventory`）

Controller 裁决理由：Plan §9.1 已完整列出第一 amendment 的 25-file closure，本次 builder inventory
又完整列出 8 files 并明确 3 个既有文件与 5-file delta；§10.5 还要求重跑去重 inventory。再复制第三份
30-file 清单会增加文档漂移源，不提升可执行性。

**验证结果**：Plan 文档中不存在第三份 30-file 全清单。§9.1 分别维护两组 inventory（25-file identity
closure 与 8-file builder closure），并明确"总枚举 mechanical union 从 25 增至 30 个去重
test/test-support files"。§10.5 要求 implementation 前后重跑两组 inventory 并比对。没有 duplicate
list。✓

### 3.2 MiMo final finding 002（`rejected-already-covered`）

Controller 裁决理由：File-local factory 的 event uniqueness、sibling consistency、required
argument、无 default / 共享 singleton / 反推均已由 §9.1、§10.5、§13 约束；一般"依赖实施纪律"
不是尚缺的 plan action。

**验证结果**：Plan 中没有新增额外抽象层、wrapper helper、通用 factory framework 或跨文件共享
identity fixture。§9.1 明确允许"每个受影响文件可在自己的 fixture owner 内定义 private typed
identity factory"，并立即列出六条禁止项。§10.5 的 focused test 要求验证"helper 内无 default、
无硬编码共享 singleton、无跨文件万能 helper 或从 manifest/sibling fields 反推"。没有新抽象。✓

## 4. 独立只读 Inventory 复核

### 4.1 Durable builder inventory（独立 `rg` 复核）

`build_context_compacted_payload(` 在 `tests/host` 下：

```
tests/host/test_memory_projection.py:449         # gap
tests/host/test_compaction_terminal.py:650       # gap
tests/host/test_context_compact_events.py:494    # existing
tests/host/test_context_compact_events.py:515    # existing
tests/host/test_context_compact_events.py:1116   # existing
tests/host/test_compact_material.py:3605         # gap
tests/host/test_run_input_builder.py:7134        # gap
tests/host/test_dispatch_scheduler.py:10559      # existing
```

8 calls / 6 files ✓

`build_context_compaction_attempt_rejected_payload(` 在 `tests/host` 下：

```
tests/host/test_proactive_compaction_operation.py:268  # gap
tests/host/test_context_compact_events.py:785          # existing
tests/host/test_context_compact_events.py:833          # existing
tests/host/test_context_compact_events.py:856          # existing
tests/host/test_context_compact_events.py:1020         # existing
tests/host/test_compaction_operation.py:914            # existing
tests/host/test_dispatch_scheduler.py:11422            # existing
```

7 calls / 4 files ✓

Union: 8 files ✓，total 15 calls ✓

Previously allowed 3 files: `test_context_compact_events.py`、`test_compaction_operation.py`、
`test_dispatch_scheduler.py` ✓

5-file gap: `test_memory_projection.py`、`test_compaction_terminal.py`、`test_run_input_builder.py`、
`test_compact_material.py`、`test_proactive_compaction_operation.py` ✓

**与 §9.1 table 完全一致。**

### 4.2 First amendment identity/typed-return closure（独立 `rg` 复核）

- `FinalAnswerData(`: 35 calls / 19 files ✓
- `EngineRunOutcomeFinalAnswer(`: 4 calls / 3 files ✓
- `ContextCompactor`-related: 7 files ✓
- Union: 25 files ✓（19 + 1 new + 5 new，逐文件确认）

**与 §9.1 table 完全一致。**

## 5. Architecture / Sequencing / Overengineering 检查

### 5.1 Owner 判定

`dayu/host/context_events.py` 是两个 strict builder 的唯一 owner。S5 必须先收紧 owner
signature，再迁移 consumer。这正确遵循了"语义 owner 优先"原则。✓

### 5.2 Sequencing

S4 (terminal guard) → S5 (identity payload) 依赖顺序清晰。S4 guard 不预埋 optional identity；
S5 在 winner writer 路径上做 required typed payload 扩展。✓

### 5.3 Overengineering 检查

- 没有引入通用 identity framework、通用 factory registry、通用 payload template
- file-local private factory 是最小侵入方案：每个文件只定义自己需要的 fixture helper
- 没有跨文件共享万能 helper
- 没有增加 optional/default/compatibility path

**无 overengineering。** ✓

### 5.4 Overcoupling 检查

- identity factory 只在各 test file 内部定义，不跨文件共享
- payload helper 签名收紧是 owner-level change，consumer 机械迁移
- 没有引入新的层间依赖

**无 overcoupling。** ✓

## 6. 反例与 Residual Risk 检查

### 6.1 Mapping / null 分类

`_rejected_payload()` 始终使用 `failure_category="quality_check_rejected"`。读取
`test_proactive_compaction_operation.py` 第 268 行确认。orphan（第 540 行）、incomplete（第 669 行）、
exhausted（第 709 行）三个测试均调用此 helper。

按 event semantic，`quality_check_rejected` 表示成功 response 后的质量拒绝，identity 应为 mapping。
Adjudication §2 与 plan §9.1 / §9.4 一致。✓

### 6.2 Residual risks

| 风险 | 分类 | 评估 |
|---|---|---|
| file-local identity 与 sibling 语义不一致 | fixed in amendment | §9.1 / §10.5 / §13 已冻结一致性规则 |
| HEAD 新增 builder call file | fail-closed | §10.5 pre-inventory 要求停止并退回 controller |
| test identity 被误当 provider evidence | deferred to G06 | §9.7 / §12.1 明确分类 |
| pytest/pyright/coverage 未运行 | covered by later slice | 本 gate 禁止 implementation |
| optional/default/manifest 反推掩盖缺口 | fixed in amendment | §9.1 / §10.5 明确禁止 |

**无 unclassified residual risk。** ✓

## 7. Open questions

无 blocking open question。

## 8. Findings

无 actionable finding。

具体验证结论：

1. **AgentDS final finding 001**（`accepted-low-clarification`）已在 plan §9.1 / §10.5 / §13 完整闭合：
   - "caller 使用当前 helper/call site 实际已有的显式、非敏感、足以区分 event 的上下文"
   - "不要求为统一形状虚构不存在的维度"
   - 禁止 default / 共享 singleton / 跨文件万能 helper / manifest 反推
   - 5 个 gap files 的 call site 上下文逐文件验证通过

2. **Rejected MiMo findings** 未引入副作用：
   - 无 30-file duplicate list
   - 无新抽象层

3. **Inventory 独立复核**：8 accepted calls / 6 files、7 rejected calls / 4 files、8-file union、
   15 total calls、5-file delta、25-file identity closure、30-file total union — 全部与 §9.1 一致。

4. **Mapping / null 语义**：`quality_check_rejected` → mapping，与 adjudication §2 一致。

5. **Sequencing / owner / overengineering / overcoupling**：无问题。

## 9. Final plan review conclusion

**pass**

Plan §9.1 / §10.5 / §13 与 proposal §3.2 已正确落实 Controller §7 的全部 accepted findings。
Rejected findings 未引入副作用。独立 inventory 复核与代码事实一致。Scope、owner、sequencing、
反例、overengineering、validation 和 residual risk 均无 material issue。本次 re-review
无 actionable finding。

---

Artifact path: `docs/reviews/plan-review-20260801-231111.md`
