# WU-CM-15 Plan Fix Re-Review — AgentMiMo Focused Verification

## Review Metadata

- **Review target**: `docs/host/host-issues/wu-cm-15-public-smoke-reactive-fallback-plan.md`（post-fix version）
- **Work unit**: WU-CM-15 Conversation memory public smoke reactive compact and fallback coverage
- **Gate**: focused plan re-review after AgentCodex fix
- **Reviewer**: AgentMiMo
- **Review date**: 2026-06-20
- **Scope**: verify only whether accepted plan review findings were fixed; do not perform a fresh broad review

## Sources

- Original review: `docs/reviews/plan-review-20260620-102108.md`
- Context review: `docs/reviews/plan-review-20260620-102145.md`
- Controller adjudication: `docs/reviews/wu-cm-15-plan-review-adjudication-20260620.md`
- Fix artifact: `docs/reviews/wu-cm-15-plan-fix-codex-20260620.md`
- Updated plan: `docs/host/host-issues/wu-cm-15-public-smoke-reactive-fallback-plan.md`

## Accepted Findings Verification

### Finding 1: Fallback pressure mechanism and requested_proactive >= 1

**Original**: MiMo 001 / DS F-04 — fallback pressure injection mechanism underspecified.

**Adjudication requirement**: Specify the fallback suite pressure mechanism; use existing smoke pressure helpers or an explicit long prompt pattern; assert effective pressure between soft and hard thresholds; require `requested_proactive >= 1`.

**Plan text evidence**:

- Implementation Decisions item 3, lines 162-164:
  > "Pressure must use either existing smoke pressure helpers such as `_compact_pressure_padding(...)` / `_estimate_chars_as_tokens(...)`, or an explicit long `RoundSpec.prompt` pattern equivalent to the focused `_soft_threshold_prompt()` approach."
  > "Before executing fallback assertions, compute or assert the effective prompt pressure lands between the active soft and hard context thresholds: high enough to require proactive compact and below the hard fail-closed threshold."
  > "Fallback coverage requires `requested_proactive >= 1`; if no proactive compact was requested, the fallback suite must fail rather than report a pass."
- Slice S3 exact changes, lines 300-302: same pressure mechanism, threshold assertion, and `requested_proactive >= 1` requirement repeated.
- Slice S3 completion signal, line 311: "Fallback acceptance proves `requested_proactive >= 1`, the effective pressure sits between soft and hard thresholds."

**Verdict**: **已修复**. The plan now specifies the concrete pressure mechanism (existing helpers or long prompt pattern), the threshold assertion requirement, and the `requested_proactive >= 1` gate. An implementation agent has enough information to implement without design ambiguity.

---

### Finding 2: Deterministic worker injection via dataclasses.replace(...)

**Original**: MiMo 002 / DS F-01 — deterministic worker injection path not documented in plan.

**Adjudication requirement**: Specify that deterministic suites replace assembled `OpenHostOptions.worker_factory` with a smoke-local `LocalEngineWorkerFactory` by using `dataclasses.replace(...)` after `compose_open_host_options(...)`; do not modify Host / Service assembly APIs.

**Plan text evidence**:

- Implementation Decisions item 3, lines 144-147:
  > "call `compose_open_host_options(...)` exactly as the current smoke script does;"
  > "use `dataclasses.replace(assembled_options, worker_factory=...)` to replace only the assembled `OpenHostOptions.worker_factory`;"
  > "the replacement factory must be a smoke-local `LocalEngineWorkerFactory`;"
  > "do not modify Host public API, Service assembly API, `compose_open_host_options(...)`, or production worker factory wiring."
- Slice S2 exact changes, line 270: "After `compose_open_host_options(...)`, inject the deterministic factory with `dataclasses.replace(assembled_options, worker_factory=smoke_factory)`. Do not add parameters to Host or Service assembly APIs."

**Verdict**: **已修复**. The plan now explicitly specifies the `dataclasses.replace(...)` injection pattern, the target field, and the constraint against API changes. This is the one-line clarification that DS F-01 requested.

---

### Finding 3: fallback_input_window audit bounded fields pre-authorized

**Original**: MiMo 003 — fallback input window assertion requires infrastructure gap.

**Adjudication requirement**: Pre-authorize extending `CompactFailedOperationAudit` or an equivalent smoke-local audit object to carry bounded `fallback_input_window` fields: `selected_block_ids`, `dropped_block_ids`, and `current_input_ref`.

**Plan text evidence**:

- Implementation Decisions item 6, lines 212-218:
  > "To support the fallback assertions, implementation is pre-authorized to extend `CompactFailedOperationAudit` or an equivalent smoke-local audit object with bounded fields extracted from `fallback_input_window`:"
  > "- `selected_block_ids`: non-empty list of selected recent material refs or ids;"
  > "- `dropped_block_ids`: list of dropped material refs or ids, possibly empty only when the scenario genuinely has nothing old enough to drop;"
  > "- `current_input_ref`: non-empty current input ref."
  > "The smoke audit must keep these as diagnostic refs/countable bounded fields only."
- Slice S1 exact changes, line 242: fallback helper tests construct with "bounded `fallback_input_window` fields (`selected_block_ids`, `dropped_block_ids`, `current_input_ref`)."
- Slice S3 exact changes, line 305: "Extend `CompactFailedOperationAudit` or an equivalent smoke-local audit object to carry bounded `fallback_input_window.selected_block_ids`, `fallback_input_window.dropped_block_ids`, and `fallback_input_window.current_input_ref`."

**Verdict**: **已修复**. The plan pre-authorizes the infrastructure extension, specifies the exact fields and their semantics, and constrains them to bounded diagnostic refs. The implementation agent knows exactly what to add and where.

---

### Finding 4: Synthetic helper test data shape specified

**Original**: MiMo 004 / DS implied — synthetic helper test data shape unspecified.

**Adjudication requirement**: Specify minimal synthetic data for helper tests: reactive tests use `CompactAuditSummary`; fallback tests use a `CompactAuditReport` / failed-operation audit with `fallback_action="dispatch"`, a non-empty fallback window, and no accepted compact for the same operation.

**Plan text evidence**:

- Slice S1 exact changes, lines 240-243:
  > "reactive helper tests construct `CompactAuditSummary` with `requested_reactive >= 1`, `compacted_reactive >= 1`, and `failed_reactive == 0`;"
  > "fallback helper tests construct `CompactAuditReport` or an equivalent failed-operation audit with one `CompactFailedOperationAudit`, `fallback_action="dispatch"`, bounded `fallback_input_window` fields (`selected_block_ids`, `dropped_block_ids`, `current_input_ref`), and no accepted `CONTEXT_COMPACTED` for the same operation."
  > "Add a fallback helper failure case proving the suite does not pass unless `requested_proactive >= 1`."

**Verdict**: **已修复**. The plan now specifies the exact synthetic data shapes for both reactive and fallback helper tests, including the failure case for missing `requested_proactive`.

---

### Finding 5: Compactor stubs smoke-local; runtime utils do not import tests

**Original**: DS F-02 — compactor stub code location unspecified.

**Adjudication requirement**: Specify location choice. Controller preference: keep minimal deterministic compactor stubs smoke-local in `utils/smoke_host_public_conversation_memory_scenarios.py`; do not make runtime `utils/` depend on `tests/`.

**Plan text evidence**:

- Implementation Decisions item 4, lines 173-175:
  > "Implementation must keep the minimal deterministic compactor stubs smoke-local in `utils/smoke_host_public_conversation_memory_scenarios.py`, behind a small context manager used only inside deterministic suite execution. Runtime `utils/` code must not import test modules, including `tests/host/test_public_compact_smoke.py`, and this WU must not extract a production seam just to share compactor stubs."
  > "The smoke-local stubs may reuse the same semantic shape as `tests/host/fake_compaction.py`, but they should stay minimal."
- Slice S2 exact changes, line 269: "Keep compactor stubs in `utils/smoke_host_public_conversation_memory_scenarios.py`; do not import tests from runtime `utils/`."

**Verdict**: **已修复**. The plan explicitly records the controller decision: stubs live in the smoke script, not in tests, and runtime utils must not import test modules.

---

### Finding 6: AgentRunRequest and AttemptDispatchSnapshot capture inside LocalEngineWorker.accept(...)

**Original**: DS F-03 — request capture mechanism not described.

**Adjudication requirement**: Specify that the deterministic worker records every `AgentRunRequest` and `AttemptDispatchSnapshot` inside `LocalEngineWorker.accept(snapshot, request)` before returning the handle.

**Plan text evidence**:

- Implementation Decisions item 3, line 149:
  > "The deterministic worker must record every `AgentRunRequest` and `AttemptDispatchSnapshot` inside `LocalEngineWorker.accept(snapshot, request)` before returning a handle. These captured requests/snapshots are the assertion source for one-system-message checks, current input preservation, selected-window rendering, and recovery Attempt identity."
- Slice S2 exact changes, line 271: "Capture ordinary `AgentRunRequest`s and dispatch snapshots in a smoke observation object by recording both arguments at the start of `LocalEngineWorker.accept(snapshot, request)` before returning the handle."
- Slice S3 exact changes, line 303: "Capture fallback final dispatch request through the shared S2 `LocalEngineWorker.accept(snapshot, request)` recorder."

**Verdict**: **已修复**. The plan specifies the exact capture point (`accept(...)` method), what to capture (both arguments), and where the captured data is used (assertion source).

---

### Finding 7: S2 shared infrastructure reused by S3

**Original**: DS F-05 — S2 and S3 share worker infrastructure not called out.

**Adjudication requirement**: Specify that S2 creates shared deterministic worker/request-capture/compactor-stub infrastructure reused by S3, avoiding duplicate worker implementations.

**Plan text evidence**:

- Slice S2 exact changes, line 267:
  > "Build this shared infrastructure in S2 for reuse by S3: deterministic worker factory, deterministic worker handles, request/snapshot capture, and compactor stub context manager. S3 must extend or parameterize this infrastructure, not duplicate a second worker implementation."
- Slice S2 completion signal, line 278:
  > "The deterministic worker factory, request/snapshot capture, and compactor stub context manager are reusable by S3 without introducing a second worker implementation."
- Slice S3 exact changes, line 298: "Implement the rejecting compactor response with the smoke-local compactor stub context manager created in S2."
- Slice S3 exact changes, line 303: "Capture fallback final dispatch request through the shared S2 `LocalEngineWorker.accept(snapshot, request)` recorder."

**Verdict**: **已修复**. The plan explicitly identifies S2 as the shared infrastructure builder and S3 as the consumer, with clear completion signals and cross-references.

---

## New Blocker Check

The fix changes are additive specifications that clarify existing plan decisions. I verified:

1. **No new API surface introduced**: `dataclasses.replace(...)` uses existing `OpenHostOptions.worker_factory` public field. No Host/Service API changes.
2. **No new production dependency**: compactor stubs remain smoke-local in `utils/`.
3. **No scope expansion**: all fixes address specification gaps within the already-authorized plan scope.
4. **No contradictory instructions**: the new specifications are consistent with each other and with the existing plan structure.

**New blockers**: none.

---

## Residual Risks (carried from original review, unchanged)

1. Smoke-local monkeypatching of `dayu.host.llm_compaction._run_agent_request` remains a private-API dependency. Plan stop condition covers this.
2. Fallback suite pressure tuning may need per-environment adjustment. Plan now requires explicit threshold assertions, making this visible.
3. Smoke command requires placeholder `DEEPSEEK_API_KEY`. Plan validation commands handle this.

---

## Conclusion

**pass**

All seven accepted findings are verified as fixed in the updated plan. Each finding's required specification is present in the plan text with sufficient detail for an implementation agent to proceed without design ambiguity. No new blockers were introduced by the fix.

| Finding | Status |
|---------|--------|
| MiMo 001 / DS F-04: fallback pressure mechanism and requested_proactive >= 1 | 已修复 |
| MiMo 002 / DS F-01: deterministic worker injection via dataclasses.replace(...) | 已修复 |
| MiMo 003: fallback_input_window audit bounded fields pre-authorized | 已修复 |
| MiMo 004: synthetic helper test data shape specified | 已修复 |
| DS F-02: compactor stubs smoke-local; runtime utils do not import tests | 已修复 |
| DS F-03: AgentRunRequest and AttemptDispatchSnapshot capture inside accept(...) | 已修复 |
| DS F-05: S2 shared infrastructure reused by S3 | 已修复 |

New blockers: none.
