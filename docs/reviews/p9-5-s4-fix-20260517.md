# P9.5 S4 Review Finding Fix

## Scope

- Source review artifacts:
  - `docs/reviews/p9-5-s4-code-review-mimo-20260517.md`
  - `docs/reviews/p9-5-s4-code-review-ds-20260517.md`
- Finding addressed: AgentDS F1 / AgentMiMo F1 info observation about Python precondition checks and SQL CAS checks both existing in `mark_dispatching_after_lane_row`.

## Fix

Added a Chinese docstring note to `_invalid_dispatching_after_lane_precondition` explaining that:

- Python-level precondition checks provide structured `NOT_FOUND` / `INVALID_STATE` diagnostics.
- The SQL `WHERE` clause in `mark_dispatching_after_lane_row` remains the final CAS defense.
- The two checks are intentionally complementary rather than redundant.

No behavior, schema, public contract, state machine, or test expectation changed.

## Validation

Controller reruns validation after this comment-only fix before accepting S4.
