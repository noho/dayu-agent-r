# Code Review — S2 Re-Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `c5446b770d238aafd8c42552dadbe132cba94ad2`
- Output file: `docs/reviews/code-review-20260815-020224.md`
- Included scope: 全部未提交 S2 diff（31 修改文件 + 5 新增文件），含 S2 implementation 与 S2 fix
- Excluded scope: S1 已接受内容、docs/reviews 下本 WU 的 gate artifacts、UF-PF05/oracle/evidence
- Parallel review coverage: 无。主 reviewer 逐文件走读了全部生产与测试 diff。

已读取上下文：根 `AGENTS.md`、accepted plan、两份 S2 review（`code-review-20260815-011809.md`、`code-review-20260815-011944.md`）、controller adjudication、S2 fix artifact、S2 implementation artifact + addendum。

## 结论

**PASS**。controller accepted F1/F2/F3 全部正确修复，核心路径未引入新 drift，完整回归通过。

## Accepted Finding 逐条验证

### F1 — begin_batch lstat fail closed ✓

**验证路径**：`_fs_storage_infra.py` begin_batch diff（行 538-555）→ `_lstat_optional_storage_path` 实现（行 1097-1121）→ 测试 `test_begin_batch_lstat_failure_never_reclassifies_existing_corpus_as_new`

**逐条确认**：

1. **仅 ENOENT 返回 missing**：`_lstat_optional_storage_path` 内 `os.lstat` 抛 `OSError` 时，`exc.errno == errno.ENOENT` 返回 `None`；其余 errno 走 `_raise_path_free_error`。✓
2. **EACCES/EIO fail closed**：`_OneShotLstatFailure` 注入 `errno.EACCES` / `errno.EIO`，测试断言 `exc_info.value.errno == error_number`，`begin_batch` 抛出 `OSError` 而非构造空 staging。✓
3. **symlink/non-directory 拒绝**：begin_batch 在 lstat 成功后检查 `stat.S_ISDIR(target_stat.st_mode)`，非目录抛 `ValueError`。✓
4. **tree 不变**：测试断言 `_tree_bytes(target_dir) == tree_before`。✓
5. **retry/rollback 正常**：测试补 `retry_batch = batching.begin_batch("DELTA"); batching.rollback_batch(retry_batch); assert core._active_batches == {}`。✓
6. **path-free 错误**：`_raise_path_free_error` + `_project_filesystem_error` 确保不泄露 target path；测试断言 `str(target_dir) not in str(exc_info.value)`。✓

**实现正确性**：`begin_batch` 使用 `_lstat_optional_storage_path` 取代 `Path.exists()` / `Path.is_symlink()`，`publishes_new_corpus=not target_is_published` 仅在明确 `ENOENT` 时为 `True`。

### F2 — §11.3/§11.4 测试矩阵 11 组 ✓

逐组验证每项测试是否为有效 owner/durable-state 断言，而非仅测试名：

| # | plan 条目 | 测试函数 | owner/durable 断言 | 有效 |
|---|----------|---------|-------------------|------|
| 1 | 跨进程 same-canonical stale/changed-but-still-stale/material union | `test_cross_process_same_canonical_stale_intent_preserves_alias_union` + `test_cross_process_same_canonical_changed_but_still_stale_is_rejected` + `test_two_material_processes_on_same_canonical_union_aliases` | P2 `company_id/company_name` 不被 P1 覆盖；`frozenset(aliases) == {"OLD","CHILD","PARENT"}`；`CompanyMetaConcurrentUpdateError` + tree 不变；两个 spawn 进程 `stage_company_meta_for_upload` 都成功且 `frozenset({"OLD","MAT-A","MAT-B"})` | ✓ |
| 2 | document-only commit 不取 global guards | `test_existing_corpus_document_only_commit_does_not_take_global_guards` | monkeypatch `_acquire_recovery_lock` 和 `_acquire_company_identity_guard` 为必抛；commit 仍成功 | ✓ |
| 3 | alias read 锁序 identity → sorted publications | `test_alias_resolution_lock_order_is_identity_then_sorted_publications` | `_LockAcquisitionRecorder` 断言 `names[0] == "company_identity.lock"`；`publication_names == sorted(publication_names)`；全部以 `.publication.lock` 结尾；无 `batch_recovery.lock` | ✓ |
| 4 | acquire failure 在首次 replace 前失败 | `test_identity_commit_acquire_failures_happen_before_first_replace` | 参数化 4 点（recovery/identity/scan_publication/target_publication）；`_ReplaceDirectoryForbidden` 断言 `calls == []`；`_tree_bytes(target_dir) == tree_before` | ✓ |
| 5 | conflict + identity/publication release failure | `test_alias_conflict_survives_identity_release_failure_with_bounded_note` + `test_alias_conflict_survives_publication_release_failure_with_bounded_note` | `CompanyTickerAliasConflictError` 为主异常；notes 有界且不含 workspace path；winner tree 不变；loser absent | ✓ |
| 6 | swap 后 orphan 与 interleaving conflict | `test_swapped_orphan_is_recovered_before_interleaving_conflict_validation` | `_leave_batch_swapped_before_committed` 构造真实 orphan evidence；B commit 先恢复 A（`assert "restore backup ticker=ALPHA" in actions`），再 typed conflict；A OLD tree 恢复、NEW 不可路由、B 不发布 | ✓ |
| 7 | meta-less canonical 与 healthy alias 双向冲突 | `test_meta_less_canonical_and_healthy_alias_conflict_in_both_directions` | 参数化 `healthy_first`；两方向均 `CompanyTickerAliasConflictError`；winner tree 不变；loser absent；唯一 route 指向 winner | ✓ |
| 8 | recovery-read barrier | `test_recovery_holds_identity_barrier_across_physical_restore` | `_BlockingFirstReplace` 阻塞 recovery replace；`_IdentityAcquireSignal` 记录 read 开始取 identity guard；`assert not read_future.done()` 在 blocker 未放行时；放行后 read 只观察 restored OLD | ✓ |
| 9 | orphan identity acquire/release failure | `test_orphan_identity_acquire_failure_preserves_recovery_evidence` + `test_orphan_identity_release_failure_preserves_primary_and_completed_restore` | acquire failure：`replace_forbidden.calls == []`、`_tree_bytes(workspace_root) == tree_before`、token_dir/backup_dir 保留；release failure：restore 已完成（OLD 可路由、NEW 不可见）、release 异常抛出、evidence 清理完成 | ✓ |
| 10 | meta-less list_documents 与 healthy alias 共存 | `test_list_documents_meta_less_corpus_coexists_with_healthy_alias_corpus` | DELTA 通过 public API 发布真实 document；`DELTA/delta.us` 返回 same result；`AAPL/APPLE` 保持 same result；`DLTA` 不获隐式 alias；DELTA 无 `meta.json` | ✓ |
| 11 | SEC/CN filing/material terminal projection | `test_upload_filing_alias_conflict_projects_exact_typed_terminal` × 2 + `test_upload_material_alias_conflict_projects_exact_typed_terminal` × 2 + `test_alias_conflict_failure_is_identical_across_direct_durable_and_observation` | 四项 pipeline 级测试断言 exact `failure` JSON（`kind=storage, code=ticker_alias_conflict`）、`stored_file_count=0`、loser absent；第五项从同一 `FinsUploadFailureReason` 构造 direct/durable/observation，断言 exact JSON 相等且 path/lock key/internal job id 不进入 public surface | ✓ |

**全部 11 组均为有效 owner/durable/e2e 测试，非仅测试名。**

### F3 — snapshot 不透明性断言归位 ✓

**验证路径**：`test_fins_ingestion_tools.py` diff

- upload 测试 `test_upload_tool_returns_external_job_awaiting_outcome`：行 1092 `assert "finsjob_" not in outcome.snapshot.snapshot_id`。✓
- download 测试 `test_download_tool_returns_external_job_awaiting_outcome`：无重复断言（S2 原有的一行保持不变）。✓
- preprocess 测试：未受影响。✓

### SEC/CN material typed mapper 修复 ✓

**验证路径**：`sec_upload_workflow.py` diff + `cn_pipeline.py` diff + 测试断言

- SEC material `run_upload_material_stream`：`except Exception` 分支新增 `failure_reason = fins_upload_failure_from_exception(exc, file_label=None)`；`message=failure_reason.message`、`failure=failure_reason.to_json()`。✓
- CN material `upload_material_stream`：相同修改。✓
- 测试 `test_upload_material_failure_uses_shared_typed_failure_owner`（SEC + CN）：旧断言 `result["message"] == "create/update 时必须提供 --company-name"` 改为 `result["message"] == "上传执行失败，请检查运行日志后重试"`；新增 `result["failure"]` exact JSON 断言。✓
- 无新 drift：`fins_upload_failure_from_exception` 中 `CompanyTickerAliasConflictError`、`CompanyTickerIdentityCorruptionError`、`CompanyMetaConcurrentUpdateError` 均在 `OSError` 之前捕获。✓

## 完整回归审查

### Correctness ✓

- **commit 路径分流**：`commit_batch` 以 `company_meta_intent is not None or state.publishes_new_corpus` 决定走 identity guards 还是 publication-only。document-only commit 不取 recovery/identity guards。✓
- **authoritative merge**：`_prepare_company_identity_commit` 在 incoming publication guard 内重读 current meta，`merge_company_meta_for_commit` 按 `expected_non_identity` snapshot / resolver version 分支选择非身份字段。✓
- **pure merge 规则**：`company_meta_contract.py` 的 `merge_company_meta_for_commit` 实现 `preserve_published`（保留 current 非身份事实）与 `refresh_if_stale`（snapshot 相等或同 resolver 时刷新，否则 typed 拒绝）。✓
- **unique index 构建**：`_build_unique_company_identity_index` 先注册 canonical，再注册 valid CompanyMeta aliases；`_register_company_identity_owner` 对 duplicate owner 抛 typed corruption。✓
- **meta-less canonical**：descriptor canonical 无条件进 index，meta 缺失不贡献 alias、非 corruption。✓

### Atomicity ✓

- **identity-changing commit**：recovery → identity → publication 三层嵌套 try/finally，每层释放失败保留 earliest primary 并 append secondary note。✓
- **publication guard release failure**：COMMITTED 后释放失败抛为 terminal error，不撤销已完成 commit。✓
- **conflict 在 backup/swap 前拒绝**：`_prepare_company_identity_commit` 在 identity guard 内完成 scan/conflict 校验后才进入 `_commit_batch_with_publication_guard`。✓

### Lock ✓

- **锁序固定**：identity-changing = `writer → recovery → identity → publication`；document-only = `writer → publication`；read = `identity → sorted publication`。✓
- **release failure 语义**：`_release_lock_after_operation` 在 primary_error 非 None 时 append note；primary_error 为 None 时 re-raise。✓
- **deadlock freedom**：recovery 只做 nonblocking ticker lock；identity guard 是进程级独占；publication guard 是 per-ticker。同进程内无嵌套同锁风险。✓

### Recovery ✓

- **orphan batch 恢复**：`_recover_single_batch_dir` 现在在 recovery guard 内取得 identity guard → publication guard 后执行 physical restore。✓
- **orphan backup 恢复**：`_recover_orphan_backup_dirs` 同样取得 identity → publication guards。✓
- **active batch 跳过**：`_recover_single_batch_dir` 新增 `with self._batch_condition: if token_dir.name in self._active_batches: return actions` 防止同进程 writer 被 recovery 清理。✓
- **_PHASE_STARTED orphan**：无 backup、无 target swap，直接 cleanup；无需 identity/publication guards。✓

### Read Route ✓

- **单一路由**：`_fs_company_meta_core.resolve_company_ticker` 在 identity guard 内扫描 → 构建 unique index → `index.get(normalized.canonical)`。✓
- **无 fallback**：read runtime `_resolve_canonical_ticker` 只调用 `resolve_company_ticker`，无 `strip().upper()` 或 `try_normalize_ticker` fallback。✓
- **typed error 投影**：`CompanyTickerIdentityCorruptionError` → `WORKSPACE_IDENTITY_CORRUPTED`；`RuntimeFileLockError/OSError` → `STORAGE_UNAVAILABLE`；`None` → `NOT_FOUND`。✓

### Failure ✓

- **upload failure mapper**：conflict/corruption/concurrent-update 三个 typed 分支位于 `OSError` 之前。`_STORAGE_FAILURE_CODES` 包含 `STORAGE_IO` 和 `TICKER_ALIAS_CONFLICT`。`upload_failure_reason_from_json` 用 `code in _STORAGE_FAILURE_CODES` 替代旧 `code is FinsUploadFailureCode.STORAGE_IO`。✓
- **upload_tools catch 顺序**：`FinsIngestionStartCancelledError` → `CompanyTickerIdentityCorruptionError` → `ValueError` → `OSError` → `Exception`。✓

### Schema ✓

- **tool schema**：`_ticker_parameter_schema` 更新为"公司财报代码。可传工作区已接收的主代码或该公司的任一已接收别名"。✓
- **error_contract**：新增 `WORKSPACE_IDENTITY_CORRUPTED` 和 `STORAGE_UNAVAILABLE`。✓
- **repository protocol**：`upsert_company_meta` → `stage_company_meta_intent`；`resolve_existing_ticker` → `resolve_company_ticker`。✓

### README ✓

- 根 `README.md`：更新 `--ticker` 逗号分隔说明、alias 冲突拒绝行为。✓
- `dayu/fins/README.md`：更新 descriptor canonical + valid CompanyMeta aliases owner 分解、meta-less、唯一 route、intent merge、固定锁序、recovery 与 typed failures；修正 FMP result 接口。✓
- `tests/README.md`：不触发（只增同层测试与 fixture helper）。✓

### Scope ✓

- 旧 `resolve_existing_ticker`、旧 alias-index helpers、`upsert_company_meta` contract：diff 中已全部删除。✓
- read runtime `normalize/upper` fallback：已删除。✓
- `_STORAGE_FAILURE_CODES` 替代旧单值 `FinsUploadFailureCode.STORAGE_IO` 检查。✓
- 无新增 durable index/cache、journal/schema 字段或下游补偿。✓

## Open Questions

无。

## Residual Risk

- 全仓非 Fins 测试未运行（Host/Engine 未修改）。
- permission failure 测试为 monkeypatch 注入，真实 ACL/NFS 跨平台行为未验证。
- identity route/commit 每次全量扫描 workspace corpus，无真实规模性能证据（plan 已分类 later work）。
- UF-PF05 真实 CLI evidence 未执行（用户明确排除）。
