# Code Review — 全仓库逐文件深度审查

## Scope

- **Mode**: all repository (逐文件走读，零抽样)
- **Branch**: `phaseflow/host-issues-control`
- **Review date**: 2026-07-12 09:11:26 UTC+8 — 09:16:54 UTC+8
- **Output file**: `docs/reviews/repo-review-20260712-091126.md`
- **Full manifest**: `/tmp/full_manifest.txt` (641 files total)

### Included File Manifest

| Slice | Directory | Files | Covered | Partially-Covered | Not-Covered |
|-------|-----------|-------|---------|-------------------|-------------|
| S1 | `dayu/host/` (含 `durable/`) | 81 | 55 | 26 | 0 |
| S2 | `dayu/engine/` + `dayu/contracts/` | 48 | 42 | 6 | 0 |
| S3 | `dayu/fins/processors/` + `dayu/fins/pipelines/` | 75 | 57 | 12 | 6 |
| S4 | `dayu/fins/` 其余 | 66 | 63 | 3 | 0 |
| S5 | `dayu/runtime/` + `dayu/service/` + `dayu/tools/` | 42 | 42 | 0 | 0 |
| S6 | `dayu/cli/` + `dayu/web/` + `dayu/wechat/` + `dayu/render/` + `dayu/documents/` + `dayu/__init__.py` | 52 | 52 | 0 | 0 |
| S7a | `tests/host/` | 111 | 98 | 5 | 0 (+8 support files) |
| **Production Total** | | **364** | **311** (85.4%) | **47** (12.9%) | **6** (1.6%) |
| **Tests Total** | | **111** (of 277) | **98** (88.3%) | **5** (4.5%) | **0** |
| **Grand Total** | | **475** (of 641) | **409** | **52** | **6** |

### Excluded Scope
- `tests/engine/`, `tests/runtime/`, `tests/fins/`, `tests/cli/`, `tests/service/`, `tests/contracts/`, `tests/tools/`, `tests/documents/` (166 test files — 超出本轮 token budget，标记为 not-reviewed)
- `.venv/`, `.git/`, `.pytest_cache/`, `.claude/`, `__pycache__/`, `workspace/`, `utils/`, `constraints/`, `dayu_agent.egg-info/`

### Parallel Review Coverage
9 个并行 subagent，每个按固定文件清单逐文件打开阅读。主 reviewer 负责去重、复核证据链、裁决 severity、合并跨 slice 重复发现。

---

## Findings

### 严重度分布

| 严重度 | 数量 |
|--------|------|
| 严重 (CRITICAL) | 1 |
| 高 (HIGH) | 10 |
| 中 (MEDIUM) | 18 |
| 低 (LOW) | 16 |
| **总计** | **45** |

---

### 1-CRITICAL — Fins 层反向依赖 Host 层（架构硬约束违规）

- **Slice**: S4 (已确认), S1 cross-reference
- **入口/函数**: `dayu/fins/ingestion/wait_adapter.py` 模块级 import
- **文件(行号)**: `dayu/fins/ingestion/wait_adapter.py:49-74`
- **输入场景**: 任何导入 `dayu.fins.ingestion.wait_adapter` 的代码路径
- **实际分支**: 模块顶层无条件 import
- **预期行为**: 按分层架构 `UI → Service → Host → Engine`，Fins 层不应依赖 Host 层
- **实际行为**: 直接 import `dayu.host.api` (ResolveWaitCompletedOutcome 等)、`dayu.host.durable.state` (WaitRecordRow, WaitResumePolicy)、`dayu.host.wait_adapter` (WaitPollAdapterRegistration, WaitActivationRegistry 等)
- **直接证据**: 行 49-55: `from dayu.host.api import ResolveWaitCancelledOutcome, ...`；行 56: `from dayu.host.durable.state import WaitRecordRow, WaitResumePolicy`；行 57-74: `from dayu.host.wait_adapter import ...`
- **影响**: 下层(Fins)依赖上层(Host)，形成循环依赖风险；Fins 无法独立测试和替换；违反项目架构硬约束
- **建议改法和验证点**: 将本模块移至 `dayu.host/` 层（如 `dayu.host.wait_fins_adapter.py`）或 `dayu.service/` 层；或在 `dayu.contracts` 中定义 Host-facing adapter interface。验证: `grep -rn "from dayu.host" dayu/fins/` 为空
- **修复风险**: 中 — 需更新所有 import 引用
- **严重程度**: 严重

---

### 2-HIGH — `sec_download_filing_workflow.py`: `not_modified` skip 路径缺少 `download_version` 校验

- **Slice**: S3
- **入口/函数**: `run_download_single_filing_stream`
- **文件(行号)**: `dayu/fins/pipelines/sec_download_filing_workflow.py:384`
- **输入场景**: `previous_meta` 非 None、`overwrite=False`、`downloaded_files==0`、`skipped_files==len(file_results)`、`has_same_file_name_set` 为真
- **实际分支**: 直接 rollback 并 skip，不校验 `previous_meta.download_version`
- **预期行为**: 与 `_can_skip_fast` 保持一致 — fast-skip 路径会因为 version mismatch 拒绝 skip
- **实际行为**: 文件下载后判定路径接受 skip，版本不一致时期的旧 filing 被错误标记为已完成
- **直接证据**: `_can_skip_fast` 对 download_version 的校验 vs `not_modified` 路径的缺失
- **影响**: 版本升级后旧数据被跳过，丢失新版本文件
- **建议改法和验证点**: 在 `not_modified` skip 判定中增加 download_version 校验
- **修复风险**: 低
- **严重程度**: 高

---

### 3-HIGH — `docling_upload_service.py`: `commit_batch` 失败导致 batch token 泄漏

- **Slice**: S3
- **入口/函数**: `_store_upload_assets`
- **文件(行号)**: `dayu/fins/pipelines/docling_upload_service.py` (~L400-450)
- **输入场景**: `begin_batch` 成功 → 数据写入成功 → `commit_batch` 时抛异常
- **实际分支**: `commit_batch` 失败后 token 已被置为 None，batch 未提交也未回滚
- **预期行为**: `commit_batch` 失败应回滚 batch
- **实际行为**: batch 处于中间态，仓储状态泄漏
- **直接证据**: `finally` 块之外处理异常，token 置 None 后无 rollback 保障
- **影响**: 仓储状态不一致，可能留下半写入数据
- **建议改法和验证点**: `commit_batch` 调用也纳入 try/except，失败时执行 rollback
- **修复风险**: 低
- **严重程度**: 高

---

### 4-HIGH — `upload_company_meta.py`: ticker alias 归一化未走 `try_normalize_ticker` 真源

- **Slice**: S3
- **入口/函数**: `_normalize_ticker_aliases`
- **文件(行号)**: `dayu/fins/pipelines/upload_company_meta.py:216`
- **输入场景**: 上传路径中处理 ticker alias
- **实际分支**: 仅做 `strip().upper()`，不走 `try_normalize_ticker` 真源归一化
- **预期行为**: 与 `cn_download_company_meta.py` 和 `sec_company_meta.py` 保持一致，走同一真源归一化
- **实际行为**: 上传路径 alias 与 download 路径 canonical 形态不一致
- **直接证据**: `cn_download_company_meta.py` 使用 `try_normalize_ticker` vs `upload_company_meta.py` 的 `strip().upper()`
- **影响**: 同一 ticker 在不同路径下有不同 canonical 形态，导致数据匹配失败
- **建议改法和验证点**: 统一使用 `try_normalize_ticker` 作为 ticker 归一化唯一真源
- **修复风险**: 低
- **严重程度**: 高

---

### 5-HIGH — dispatch.py drain loop 在事务重试耗尽时自我关闭，导致级联不可用

- **Slice**: S1
- **入口/函数**: `HostDispatchScheduler._drain_loop()`
- **文件(行号)**: `dayu/host/dispatch.py:2756-2814`
- **输入场景**: 数据库持续繁忙导致 dispatch write 事务重试耗尽
- **实际分支**: 行 2781-2798: 捕获 `HostTransactionRetryExhaustedError` → `self._closed = True`、cancel 所有 active workers、标记 instance stopped
- **预期行为**: 重试耗尽应进入 graceful degradation — 将当前 record 标记 failed，继续处理下一轮
- **实际行为**: scheduler 自我关闭，所有后续 dispatch 永久失败
- **直接证据**: 行 2792 (`self._closed = True`)；对比 `drain_once` 中行 2502-2504 对同一错误的正确处理（将 record 放回队列）
- **影响**: 单个 dispatch 事务重试耗尽 → 整个 scheduler 不可用 → 所有 Run 永久卡在队列中
- **建议改法和验证点**: 移除 `_drain_loop` 中的 `except HostTransactionRetryExhaustedError`（`drain_once` 已正确处理）
- **修复风险**: 低
- **严重程度**: 高

---

### 6-HIGH — `admission.py` 与 `command.py` 间重复定义的 helper（双 owner 语义漂移）

- **Slice**: S1
- **入口/函数**: `_pending_dispatch_from_row`、`_call_context_digest`
- **文件(行号)**: `dayu/host/admission.py:4377-4393, 4896-4921`; `dayu/host/command.py:1721-1737`
- **输入场景**: `admission.py` 和 `command.py` 各自独立定义语义完全相同的 helper 函数
- **实际分支**: 两个模块各自维护自己的实现
- **预期行为**: 同一语义只能有一个 owner；应从 admission.py 导出，command.py 导入复用
- **实际行为**: 双 owner，未来修改可能只更新一处
- **直接证据**: 两个文件的同名函数实现完全一致
- **影响**: 行为漂移风险；违反 semantic ownership 原则
- **建议改法和验证点**: 将 helper 归入 admission.py 或独立 projection helper 模块，command.py 导入复用
- **修复风险**: 低
- **严重程度**: 高

---

### 7-HIGH — `_event_payload.py` 与 `_execution_config_projection.py` 间重复的 validation helper

- **Slice**: S1
- **入口/函数**: `required_payload_text` vs `required_json_text`
- **文件(行号)**: `dayu/host/_event_payload.py:412`; `dayu/host/_execution_config_projection.py:446`
- **输入场景**: 两个模块各自实现"读取必填非空文本字段"的 helper，语义相同但签名不同
- **实际分支**: 各自独立实现
- **预期行为**: 应统一到 shared validation module
- **实际行为**: 两个 owner 各自维护，签名差异增加误用风险
- **直接证据**: 两个函数语义完全相同，参数名不同
- **影响**: 若未来改 field_name 格式或错误类型，需要改两处
- **建议改法和验证点**: 统一到 `durable/_validation.py`
- **修复风险**: 低
- **严重程度**: 高

---

### 8-HIGH — `cancel_run` deferred cancel 路径存在并发竞态

- **Slice**: S1
- **入口/函数**: `cancel_run` → `_is_deferred_cancel_state`
- **文件(行号)**: `dayu/host/command.py:1655-1689`
- **输入场景**: 写事务 commit 后、deferred cancel 读事务执行期间，Run 状态被另一个并发事务改变
- **实际分支**: 用 read transaction 重读状态
- **预期行为**: 写事务已提交的判定应保持一致性；deferred 路径不应依赖读事务的快照
- **实际行为**: 读事务可能看到与写事务不一致的状态，返回错误的 `UNSUPPORTED_OPERATION`
- **直接证据**: 行 1655-1689 的 deferred cancel 逻辑
- **影响**: 极端并发下返回错误码，但不导致数据损坏
- **建议改法和验证点**: 将 deferred cancel 判定纳入写事务内
- **修复风险**: 中 — 需调整事务边界
- **严重程度**: 高

---

### 9-HIGH — `durable/memory.py` 从 host 层 import（durable → host 反向依赖）

- **Slice**: S1
- **入口/函数**: 模块级 import
- **文件(行号)**: `dayu/host/durable/memory.py:41-46`
- **输入场景**: durable 子目录中的模块 import host 层的 `compact_payload` 和 `accepted_result_projection`
- **实际分支**: 模块级 top-level import
- **预期行为**: durable 层不应依赖上层 host 模块
- **实际行为**: durable/memory.py 反向 import host 层
- **直接证据**: 行 41-46 import 语句
- **影响**: 架构边界模糊，durable 层不独立
- **建议改法和验证点**: 提取 shared contract 到 `dayu.contracts` 或提升 memory.py 到 host 包根
- **修复风险**: 中
- **严重程度**: 高

---

### 10-HIGH — `_store_downloaded_document` batch rollback 无法恢复已删除的物理文件

- **Slice**: Previous review Fins+Tools retry (confirmed)
- **入口/函数**: `FinsIngestionRuntime._store_downloaded_document()`
- **文件(行号)**: `dayu/fins/ingestion_runtime.py:3747-3813`
- **输入场景**: overwrite 路径：`reset_source_document` 成功删除原文件后，后续 `store_file` 写入失败
- **实际分支**: 异常被 catch → `rollback_batch(token)` 被调用，但 `reset_source_document` 已物理删除原文档
- **预期行为**: Rollback 应完全恢复批处理前状态
- **实际行为**: 若 FS batch kernel 不做 backup-before-modify，原文档和新文档同时丢失
- **直接证据**: 行 3790-3812 — `reset_source_document` 与后续写入之间无原子性保证
- **影响**: 磁盘故障时可能丢失已完成的 source document 物理文件
- **建议改法和验证点**: FS batch kernel 在 `begin_batch` 时做完整 backup，或改用 staging→原子 rename 的两阶段提交
- **修复风险**: 中
- **严重程度**: 高

---

### 11-HIGH — `_normalize_optional_string` 在 3 个 processor 模块中重复定义

- **Slice**: S3
- **入口/函数**: 模块级函数
- **文件(行号)**: `dayu/fins/processors/sec_section_build.py`, `dayu/fins/processors/sec_table_extraction.py`, `dayu/fins/processors/sec_xbrl_query.py`
- **输入场景**: 三个模块各自定义完全相同的 `_normalize_optional_string`（均额外处理 pandas NaN/NaT）
- **实际分支**: 各自独立维护
- **预期行为**: 应提取到公共工具模块复用
- **实际行为**: 三份拷贝，任何修改需同步三次
- **直接证据**: 三个文件中同名函数的实现完全一致
- **影响**: 维护漂移风险
- **建议改法和验证点**: 提取到 `dayu/fins/` 下的公共工具模块
- **修复风险**: 低
- **严重程度**: 高

---

### 12-HIGH — `admission.py` 多处分发 `released_active_slot` 语义注释不一致

- **Slice**: S1 (合并原 F3/F4)
- **入口/函数**: `_CancelRunOperation._cancel_predispatch_starting_or_none()` / `_cancel_recovering()`
- **文件(行号)**: `dayu/host/admission.py:1648-1776`
- **输入场景**: 取消 pre-dispatch STARTING 或 RECOVERING 状态 Run
- **实际分支**: 两个路径均返回 `released_active_slot=True`，但注释描述不同业务语义
- **预期行为**: 相同语义应有统一注释
- **实际行为**: 行 1711 vs 1774-1775 注释不一致
- **影响**: 维护困惑
- **建议改法和验证点**: 统一注释
- **修复风险**: 低
- **严重程度**: 中

---

### 13-MEDIUM — dispatch.py watchdog wakeup 队列容量为 1，静默丢弃

- **Slice**: S1
- **文件(行号)**: `dayu/host/dispatch.py:963, 1092-1105`
- **输入场景**: 多个 cancel commit 在 watchdog 处理完之前快速连续发生
- **实际行为**: `asyncio.Queue(maxsize=1)` 满时 `QueueFull` → `pass` 静默丢弃
- **影响**: CANCELLING 状态 Run 延迟到下一次 periodic scan 才收口
- **严重程度**: 中

---

### 14-MEDIUM — dispatch.py proactive compaction 事务/异步边界 TOCTOU 竞态

- **Slice**: S1
- **文件(行号)**: `dayu/host/dispatch.py:1463-1490`
- **输入场景**: compaction 事务提交后、异步 LLM 调用执行期间 Run 被 cancel/steer
- **实际行为**: LLM token 已消耗但因 stale preconditions 在第二事务中被丢弃
- **影响**: LLM token 浪费
- **严重程度**: 中

---

### 15-MEDIUM — recovery.py 启动恢复扫描将所有非终态 Run 放入单个写事务

- **Slice**: S1
- **文件(行号)**: `dayu/host/recovery.py:180-234`
- **输入场景**: 崩溃重启后存在大量非终态 Run（1000+）
- **实际行为**: 所有分类和 closeout 在同一写事务内
- **影响**: 大规模部署下可能超时、锁竞争加剧
- **严重程度**: 中

---

### 16-MEDIUM — `compact_material.py` tool_call_event_ref fallback 值可能误导下游

- **Slice**: S1
- **文件(行号)**: `dayu/host/compact_material.py:2565-2567`
- **输入场景**: `tool_call_event_ref` 为 None 时使用 `row.event_id` 作为 fallback
- **实际行为**: provenance entry 中的 `tool_call_event_ref` 可能指向 TOOL_RESULT_ACCEPTED 而非 TOOL_CALL_REQUESTED
- **影响**: 下游按 ref 查 TOOL_CALL_REQUESTED 时找不到对应事件
- **严重程度**: 中

---

### 17-MEDIUM — `sec_download_state.py` 缓存读写对 OSError 静默吞错

- **Slice**: S3
- **文件(行号)**: `dayu/fins/pipelines/sec_download_state.py` (~L520)
- **输入场景**: `_read_sec_cache` / `_write_sec_cache` 遇到 OSError
- **实际行为**: bare `pass` 吞掉错误，缓存写入失败不被发现
- **影响**: 静默缓存失效
- **严重程度**: 中

---

### 18-MEDIUM — `sec_fiscal_fields.py` lazy import edgar 失败时静默返回，语义丢失

- **Slice**: S3
- **文件(行号)**: `dayu/fins/pipelines/sec_fiscal_fields.py:499`
- **输入场景**: `from edgar.xbrl import XBRL` 因库不可用而失败
- **实际行为**: 静默返回 `(None, None)`，调用方无法区分"无 XBRL"与"edgar 库不可用"
- **影响**: 排障困难
- **严重程度**: 中

---

### 19-MEDIUM — `sec_6k_rules.py` 分类链缺少回归测试锚点

- **Slice**: S3
- **文件(行号)**: `dayu/fins/pipelines/sec_6k_rules.py`
- **输入场景**: SEC 变更 6-K 提交格式
- **实际行为**: `_classify_6k_text` 约 30 个顺序信号判断，失败静默返回 `EXCLUDE_NON_QUARTERLY`，丢失季度财务数据
- **影响**: 6-K 分类错误可能静默丢失关键财务数据
- **严重程度**: 中

---

### 20-MEDIUM — `cn_download_filing_workflow.py` PDF 复用但 Docling 不可复用的 fingerprint 边界

- **Slice**: S3
- **文件(行号)**: `dayu/fins/pipelines/cn_download_filing_workflow.py` (~L490)
- **输入场景**: PDF blob 缓存复用成功但 Docling JSON 需要重新转换
- **实际行为**: `source_fingerprint` 基于 `pdf_bytes + docling_json_bytes` 计算，pdf_bytes 来自 blob 缓存，但 docling_json_bytes 是新转换的，若 blob PDF 与 re-downloaded PDF 字节不同，fingerprint 不一致
- **影响**: 实际风险低但语义不完美
- **严重程度**: 中

---

### 21-MEDIUM — `cn_pipeline.py` / `sec_pipeline.py` facade 类职责膨胀

- **Slice**: S3
- **文件(行号)**: `dayu/fins/pipelines/cn_pipeline.py`, `dayu/fins/pipelines/sec_pipeline.py`
- **实际行为**: 两个 facade 类同时承担 Protocol 实现、仓储装配、Docling 转换适配、upload 编排、download adapter 桥接和 helper 函数
- **影响**: 未来替换 Docling 转换器或切换仓储后端时修改面广
- **严重程度**: 中

---

### 22-MEDIUM — `ingestion_runtime.py` (6825行)、`sec_form_section_common.py` (~3000行)、`sec_table_extraction.py` (~2200行) 模块过长

- **Slice**: S3, S4
- **文件**: `dayu/fins/ingestion_runtime.py`, `dayu/fins/processors/sec_form_section_common.py`, `dayu/fins/processors/twenty_f_form_common.py`
- **严重程度**: 中 — 有明确的不拆分理由（docstring 已说明内部调用图高度耦合），但影响可维护性

---

### 23-MEDIUM — Engine `_classify_iteration` finish_reason 缺失处理与 SSE parser 策略不一致

- **Slice**: S2
- **文件(行号)**: `dayu/engine/agent.py:1756`; `dayu/engine/runners/openai/sse_parser.py:685`
- **实际行为**: agent 侧 `finish_reason is None` 默认 `FinishReason.STOP`；SSE parser 侧 `finish_reason is None` fail-closed (`_MISSING_TERMINAL_FINISH_REASON_CODE`)
- **影响**: 两条路径策略不一致，缺乏统一的 finish_reason 缺失策略
- **严重程度**: 中

---

### 24-MEDIUM — `runner_identity.py` 编码方案未对文本字段中的分隔符做转义

- **Slice**: S2
- **文件(行号)**: `dayu/engine/contracts/runner_identity.py` (分隔符 `|` / `:`)
- **实际行为**: run_id/iteration_id 等字段若包含 `|` 或 `:`，canonical encoding 产生歧义
- **影响**: 当前系统生成的 ID 不含这些字符，实际风险极低，但应在契约文档中明确约束
- **严重程度**: 中

---

### 25-MEDIUM — `error_classifier.py` 的 context overflow message markers 为硬编码英文短语

- **Slice**: S2
- **文件(行号)**: `dayu/engine/runners/openai/error_classifier.py:39-46`
- **实际行为**: `_CONTEXT_OVERFLOW_MESSAGE_MARKERS` 不可配置，不支持多语言 provider 或措辞变更
- **影响**: 已标注为 fallback 路径（首选 structured code），但 marker 矩阵缺乏扩展性
- **严重程度**: 中

---

### 26-MEDIUM — `_infer_filename_from_uri` 在 storage 模块中重复定义

- **Slice**: S4
- **文件(行号)**: `dayu/fins/storage/_fs_storage_utils.py:172`; `dayu/fins/storage/fs_source_document_repository.py:220`
- **实际行为**: 两个模块级函数实现相同逻辑
- **影响**: 维护漂移风险
- **严重程度**: 中

---

### 27-MEDIUM — `service_runtime.py` 一次性装配成本偏高

- **Slice**: S4
- **文件(行号)**: `dayu/fins/service_runtime.py:421-514`
- **实际行为**: `get_ingestion_runtime()` 同时构造 SecPipeline 和 CnPipeline，即使只需要 download adapter
- **影响**: 不必要的全量装配
- **严重程度**: 中

---

### 28-MEDIUM — `document_models.py` 中多处 `Any` 类型扩散

- **Slice**: S4
- **文件(行号)**: `dayu/fins/domain/document_models.py:33, 337, 441, 649, 1031`
- **实际行为**: `DocumentMeta = dict[str, Any]`、`from_dict(data: dict[str, Any])` 等使用 `Any`
- **影响**: 序列化/反序列化边界（JSON → typed），评估为必要但可改进
- **严重程度**: 中

---

### 29-MEDIUM — Engine `agent.py` 非 CancelledError 异常覆盖已有 failure_candidate

- **Slice**: S2
- **文件(行号)**: `dayu/engine/agent.py:1320-1355`
- **实际行为**: Runner 异常无条件覆盖 `state.failure_candidate`，`provider_request_id` 强制 None
- **影响**: 丢失具体错误诊断信息
- **严重程度**: 中

---

### 30-LOW — `bs_twenty_f_processor.py` 未使用的局部变量

- **Slice**: S3
- **文件(行号)**: `dayu/fins/processors/bs_twenty_f_processor.py:177`
- **实际行为**: `section_titles = [section.title for section in self._virtual_sections]` 赋值后未使用
- **严重程度**: 低

---

### 31-LOW — `financial_enhancer.py` `setattr` 动态修改表格对象

- **Slice**: S3
- **文件(行号)**: `dayu/fins/processors/financial_enhancer.py:428-437`
- **实际行为**: 使用 `setattr` 将 Dayu 语义写入外部表格对象
- **严重程度**: 低

---

### 32-LOW — CLI `main.py` 临时日志文件泄漏

- **Slice**: S6
- **文件(行号)**: `dayu/cli/main.py:130-138`
- **实际行为**: `_open_default_log_file()` 使用 `tempfile.mkstemp` 但进程退出时不清理
- **严重程度**: 低

---

### 33-LOW — CLI SIGINT Monitor 代码重复（`agent_entrypoint.py` vs `commands/fins.py`）

- **Slice**: S6
- **文件(行号)**: `dayu/cli/agent_entrypoint.py:47`; `dayu/cli/commands/fins.py:112`
- **实际行为**: `CliSigintMonitor` 和 `_FinsSigintMonitor` 功能同构
- **严重程度**: 低

---

### 34-LOW — CLI `session_terminal_cursor.py` `__all__` 类型不一致（list vs tuple）

- **Slice**: S6
- **文件(行号)**: `dayu/cli/session_terminal_cursor.py:299`
- **实际行为**: 使用 `__all__ = [...]` 而非项目统一的 tuple
- **严重程度**: 低

---

### 35-LOW — `Optional[X]` vs `X | None` 风格不一致（多个 documents/processors 文件）

- **Slice**: S6
- **文件**: `base.py`, `_doc_processor_factory.py`, `docling_processor.py`, `html_extraction.py`, `local_file_source.py`, `markdown_processor.py`, `source.py`
- **严重程度**: 低

---

### 36-LOW — `_doc_processor_factory.py` 模块级单例竞态

- **Slice**: S6
- **文件(行号)**: `dayu/documents/processors/_doc_processor_factory.py:21`
- **实际行为**: `_DOCUMENTS_PROCESSOR_REGISTRY` 是模块级可变 global 变量，多线程下有竞态
- **严重程度**: 低

---

### 37-LOW — `text_utils.py` docstring 风格不统一（Args vs :param）

- **Slice**: S6
- **文件(行号)**: `dayu/documents/processors/text_utils.py`
- **实际行为**: `normalize_optional_string` 使用 Google 风格 `Args:`，项目其余使用 RST `:param`
- **严重程度**: 低

---

### 38-LOW — CLI `host_context.py` 两个函数实现完全相同

- **Slice**: S6
- **文件(行号)**: `dayu/cli/host_context.py`
- **实际行为**: `prompt_create_session_client_request_id` 和 `interactive_create_session_client_request_id` 实现完全相同
- **严重程度**: 低

---

### 39-LOW — `web_playwright_backend.py` 使用 `except BaseException`

- **Slice**: S5
- **文件(行号)**: `dayu/tools/web/web_playwright_backend.py:431, 489`
- **实际行为**: 可能吞噬 KeyboardInterrupt/SystemExit
- **严重程度**: 低

---

### 40-LOW — `read_runtime.py` `getattr(source, "media_type", None)` 因 Source Protocol 未声明属性

- **Slice**: S4
- **文件(行号)**: `dayu/fins/tools/read_runtime.py:2558`
- **实际行为**: 因 Source Protocol 未声明 `media_type` 属性而使用 getattr
- **严重程度**: 低

---

### 41-LOW — 测试 monkeypatching 过于集中（3 文件 ~80 处）

- **Slice**: S7a
- **文件**: `tests/host/test_dispatch_scheduler.py` (52处), `tests/host/test_engine_ingest_mapping.py` (16处), `tests/host/test_toolruntime_executor.py` (11处)
- **实际行为**: 大量 monkeypatch.setattr 替换模块级函数，重构内部函数签名会静默破坏测试
- **严重程度**: 低

---

### 42-LOW — 跨测试文件导入共享 helper 耦合

- **Slice**: S7a
- **影响**: 多个 public smoke 测试从 `test_public_retry_replay.py` 导入 helper，修改会级联破坏
- **严重程度**: 低

---

### 43-LOW — 5 个测试文件覆盖不足

- **Slice**: S7a
- **文件**: `test_submit_followup_public_contract.py` (3 test), `test_tool_runtime_schema_projection.py` (3 test), `test_public_resolve_wait_resume.py` (1 test), `test_public_tool_wiring_smoke.py` (2 test), `test_public_steer.py` (4 test)
- **严重程度**: 低

---

### 44-LOW — `command.py` resolve_wait service 每次新建实例

- **Slice**: S1
- **文件(行号)**: `dayu/host/command.py:774-815`
- **实际行为**: 每次 resolve_wait 创建新 service 实例，与 admission_service 单例模式不一致
- **严重程度**: 低

---

### 45-LOW — `admission.py` ATTACH_ACTIVE 分支 contract 不一致

- **Slice**: S1
- **文件(行号)**: `dayu/host/admission.py:998-1077`
- **实际行为**: ACCEPTED 分支返回 `attempt=None, dispatch_record=None`，非 ACCEPTED 分支返回具体值
- **严重程度**: 低

---

## Open Questions

1. **大文件拆分的架构方案** — God files (`ingestion_runtime.py` 6825行, `sec_form_section_common.py` 3048行, `tool_runtime.py` 7597行, `engine_ingest.py` 7136行) 拆分需在设计层面确定新模块边界
2. **Fins wait_adapter 迁移路径** — 移至 `dayu.host` 还是 `dayu.service`，哪种方案对 Service 层 cascading impact 最小？
3. **Provider 厂商行为差异** — Engine finish_reason 策略不一致（Finding 23）在不同 provider 上的实际行为需验证
4. **FS batching repository 原子性** — Finding 10 的修复需要明确 backup-before-modify 或两阶段提交方案
5. **166 个测试文件未审查** — `tests/engine/`, `tests/runtime/`, `tests/fins/`, `tests/cli/`, `tests/service/`, `tests/contracts/`, `tests/tools/`, `tests/documents/` 因 token budget 限制未覆盖

## Residual Risk

1. **测试密度不均**: God files 的测试文件非常大（`test_dispatch_scheduler.py` 8260行, `test_engine_ingest_mapping.py` 5724行），倾向于集成测试而非单元测试
2. **166 个测试文件未审查** — tests/engine, tests/runtime, tests/fins 等模块的测试质量、边界覆盖和 monkeypatching 情况未被评估
3. **`except BaseException` 在 `web_playwright_backend.py:431,489`** — 需立即修复
4. **大量 `Any` 类型在 fins 数据正则化层** — 累积的类型不安全在数据管线中传播
5. **`ingestion_runtime.py` 剩余约 6000 行未深度审查** — 该文件在 S4 中被标记为 partially-covered（仅阅读了关键路径）
6. **6-K 分类准确率无回归测试锚点** — SEC 格式变更可能导致静默数据丢失
7. **S1 中 26 个 partially-covered 文件** — 多为小文件 (<200行) helper/protocol，风险较低但未逐行验证
