# PR 190 F11/F12 S4 Harness Fix

## Gate

- Work unit: PR 190 F11/F12 S4 harness/evidence gate
- Current gate: `fix`
- Next entry point: `re-review`
- Base: `321893e423beeb20acf2768c03b2be3477c92903`
- Completion status: `FIX_COMPLETE_AWAITING_REVIEW`
- Artifact path: `docs/reviews/pr-190-f11-f12-s4-harness-fix-20260805.md`

## Scope 与边界

本 fix 只执行 `docs/reviews/pr-190-f11-f12-s4-harness-review-adjudication-20260805.md` 接受的 harness 项：

1. real-provider suites 缺少 `--evidence-output-dir` 时 parser fail closed，并独立测试 pressure-mode 与 evidence-dir 约束；
2. 为新增 evidence export contract 补 deterministic owner tests：fresh file/dir 不可覆盖、digest 排除自身且校验实际内容、public/canonical equality 的 equal/mismatch、failure finally export 不遮蔽原业务异常；
3. 修正本 Agent 自有 `docs/reviews/code-review-20260805-210138.md` 的 base 与旧 evidence bundle 状态描述。

明确未修改：

- `dayu/` 下任何生产代码；
- oracle、scenario、MiMo review artifact、DS review artifact、总控 adjudication；
- external evidence root `/Users/leo/workspace/.dayu-cli-ci/interactive-memory-v3-20260805T-s4-HeHeLm/`。旧 root 继续作为 `immutable / superseded partial evidence` 保留，本 gate 未回写 metadata、observations、secret scan 或 digest，也未重算其 scan/digest；
- `DS-004`；
- `S4-001` 的生产 owner contract。

## First-principles 与 owner 判断

- real-provider suite 的唯一正式输出是 fresh evidence bundle；允许无 `--evidence-output-dir` 运行会静默丢失不可替代的 provider observation，因此 parser fail-closed 动机成立。该规则由 harness CLI parser 拥有，不能在运行结束或报告层补救。
- fresh-write、digest、public/canonical equality 已由 harness helper 直接承诺；deterministic tests 必须断言这些 owner contract，不能只依赖外部 smoke bundle 间接证明。
- failure finally 的异常优先级由 `run_smoke` evidence-finally 边界拥有；新增私有错误处理 helper 只封装“业务异常优先、正常路径导出异常可见”语义，不改变 evidence 内容或生产行为。
- `S4-001` 是 fallback current-input material construction/digest 的生产 owner 问题；harness/test 不做 normalization、digest fallback 或 fixture 补偿。

## Changed files

### `utils/smoke_host_public_conversation_memory_scenarios.py`

- 定义完整 real-provider suite 集合，包含 baseline、boundary、replacement、repair、reconnect probe、fallback。
- parser 在其它前置约束满足后，对缺少 `--evidence-output-dir` 的 real-provider suite 给出可操作错误并 fail closed。
- `run_smoke` 的 evidence-finally 继续保留原语义，改由 `_handle_s4_evidence_export_error` 显式拥有异常优先级：存在 active business exception 时记录导出失败但不替换原异常；无 active exception 时原样抛出 export error。

### `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`

- 将 real pressure-mode 与 evidence-dir 两个 parser contract 拆成独立 parametrized tests；pressure 测试显式提供 evidence dir，evidence 测试先满足 pressure 约束，保证 `SystemExit` 来源唯一。
- 覆盖全部六个 real-provider suite 缺 evidence dir 的 fail-closed 行为。
- 新增 fresh file 与既有 invocation dir 不可覆盖测试，并断言原内容/目录保持不变。
- 新增 digest 测试，验证 `digest.json` 自身被排除，索引只包含真实文件，且 size/SHA-256 与实际 bytes 一致。
- 通过 canonical event builder 构造 rejected terminal，同源验证 public/canonical equality；修改 public manifest digest 后验证 exact mismatch finding。
- 验证 evidence export error 不遮蔽正在传播的同一个业务异常对象，同时验证无业务异常时 export error 仍原样可见。

### `docs/reviews/code-review-20260805-210138.md`

- Base 修正为实际 `321893e423beeb20acf2768c03b2be3477c92903`。
- 明确旧 bundle 是 `immutable / superseded partial evidence`：因错误 base metadata 与产品 blocker 归档，不得回写、补写或重算；后续只能写入新的独立 evidence root。

## Finding 状态

| Finding | 状态 | 证据/处置 |
|---|---|---|
| `S4-REVIEW-001` | 已修复 | evidence export owner-level deterministic tests 已补齐 accepted 边界。 |
| `DS-002` | 已修复 | 六个 real-provider suites 缺 evidence dir 均 parser fail closed。 |
| `DS-003` | 已修复 | pressure-mode 与 evidence-dir 已拆成独立测试，失败来源被显式断言。 |
| Review/report base SHA | 已修复（本 Agent artifact） | `code-review-20260805-210138.md` 使用实际 base；按用户边界未改 MiMo/DS artifacts 与 adjudication。 |
| `DS-001` | 证据失效 | 不作为本 gate 代码 finding，不加临时 debug；待 `S4-001` 修复后在全新 root 重跑，验证 `compactor-attempts.json` 数量与 canonical attempts 一致。 |
| `DS-004` | 证据失效 | adjudication 已不采纳，本 gate 未修改。 |
| `S4-001` | 未修复 | 继续归 production fallback current-input material construction/digest owner 的独立 work unit。 |

## Validation

- `source .venv/bin/activate && pytest tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py -q`：`36 passed, 3 warnings`；warnings 均来自 `edgar` 已弃用模块。
- `source .venv/bin/activate && python -m pyright`：`0 errors, 0 warnings, 0 informations`。
- `git diff --check`：通过。
- real-provider campaign：本 harness fix gate 未重跑。`DS-001` 只保留为 `S4-001` 修复后从全新 evidence root 执行的验证项；未加入临时 debug。

## Docs decision

- 未更新 README。测试仍位于既有 runtime test 层，测试运行方式与维护规则未变化，不触发 `tests/README.md` 的更新边界；本次也没有用户可见安装、CLI 正式入口或分层装配变化。
- 新增本独立 fix artifact；未修改不属于本 Agent 的 review/adjudication artifact。

## Residual risks 与 uncovered areas

1. `S4-001`：`assigned to later work unit`。生产 fallback current-input material 的 normalization/digest 仍不同源，S4 mandatory exhausted fallback/single terminal 继续阻塞。
2. `DS-001`：`covered by later approved slice`。生产修复后必须使用新的独立 evidence root 重跑，核对 capture 与 canonical attempt 数量；若仍不一致，再基于新 root 直接证据定位，禁止向 harness 加临时 debug 噪声。
3. MiMo fallback no-downgrade 与完整 mandatory campaign：`covered by later approved slice`。旧 bundle 只能作为 superseded partial evidence，不能复用或补写。
4. 本 gate 只做 deterministic owner tests，没有真实 provider 重跑：`covered by later approved slice`，原因是 `S4-001` 的生产 blocker 尚未修复。

## Closeout

accepted harness findings 已完成 fix 与本地验证；没有 stage、commit 或 push。按用户指定，本轮停在 `re-review`，未进入 accepted commit 或 `S4-001` production slice。
