# Plan Rereview: wu-cli-download-01 Slice 1 test-only amendment

## Reviewed target and scope

- **Target**: `docs/gateflow/wu-cli-download-01-slice1-plan-amendment-20260810-005537.md`（最新 revision，含 §4 implementation checklist、§7 plan review adjudication、upload hard stop、typed owner 不变量）
- **Diff base**: `docs/gateflow/wu-cli-download-01-plan-20260809.md` §6 Slice 1、§9、§14.7
- **Direct blocker**: `docs/reviews/code-review-20260810-004602.md` DL-R01
- **原 artifact**: `docs/reviews/plan-review-20260810-010145.md`（MiMo 首轮）
- **DS artifact**: `docs/reviews/plan-review-20260810-010500.md`（DS 首轮）
- **Scope**: 核验原两个注意项及 DS 低风险项已收敛

## Rereview checklist

### 1. 三个现有 call site 固定 True

- **状态**: **resolved**
- **直接证据**: amendment §2 line 21 — "三处当前都显式传入非空 `start_date`，恢复实现时必须逐处传入 `start_is_explicit=True`，不得由实现者再次推断或自行分流"
- **代码验证**: `test_cn_pipeline.py:337`（`start_date="2025-01-01"`）、`:402`（`start_date="2024-01-01"`）、`:454`（`start_date="2025-01-01"`）均显式传入非空日期，`start_is_explicit=True` 是唯一正确映射
- **结论**: DS DL-PA-01 已收敛

### 2. _RecordingPipeline / _collect_events 等已在 checklist

- **状态**: **resolved**
- **直接证据**: amendment §4 implementation checklist #2 — "同步 `tests/fins/test_cn_download_runtime.py::_RecordingPipeline.download` 的 required boolean 签名"；#3 — "同步 `tests/fins/test_cn_download_workflow.py::_collect_events`、该文件直接 `pipeline.download` 及全部相关调用的显式 boolean"
- **代码验证**:
  - `test_cn_download_runtime.py:237-248`：`_RecordingPipeline.download` 覆写签名无 `start_is_explicit`，checklist 已覆盖
  - `test_cn_download_workflow.py:718-724`：`_collect_events` 调用 `pipeline.download_stream(...)` 无 `start_is_explicit`，checklist 已覆盖
  - `test_cn_download_workflow.py:1210-1217`：直接 `pipeline.download(...)` 无 `start_is_explicit`，checklist 已覆盖
- **结论**: MiMo 001/002 已收敛。checklist #4 "全量 pyright 捕获所有漏迁签名/call site" 提供二次兜底

### 3. False + 非空 date 是 pipeline direct owner test

- **状态**: **resolved**
- **直接证据**: amendment §3 line 37 — "第二项是 pipeline-layer direct contract test：它绕过当前 builder 直接调用 pipeline，目的是证明下游消费 typed explicitness 事实，而不是从非空日期反推。它不是公开端到端合法输入示例，不授权改变当前 builder observable behavior"
- **代码验证**: `download_contract.py:155` 定义 `start_is_explicit=start is not None`，因此 `start_is_explicit=False` ⇔ `start=None`。测试 `start_date="..."` + `start_is_explicit=False` 必须绕过 builder 直接调用 `CnPipeline.download`
- **结论**: DS DL-PA-02 已收敛。测试性质已明确标注

### 4. FinsDownloadDateRange owner 校验：explicit=>bound、start<=end 且允许 default bound

- **状态**: **resolved**
- **直接证据**: amendment §4 —
  - "`start_is_explicit=True` 时，`start_bound` 必须非空"
  - "`end_is_explicit=True` 时，`end_bound` 必须非空"
  - "`start_bound` 与 `end_bound` 均存在时，必须满足 `start_bound <= end_bound`"
  - "允许 bound 非空且对应 explicit 为 False，用于未来默认 bound 和本次 pipeline-layer direct contract test"
- **代码验证**: 当前 `FinsDownloadDateRange`（`download_contract.py:52-92`）无 `__post_init__`；`build_fins_download_request` 在 line 146 做 `start_bound > end_bound` 检查。amendment 要求新增 `__post_init__` 拥有不变量并删除 builder 重复检查
- **implementation note**: builder 删除 `start > end` 检查与 `__post_init__` 新增应在同一实现步骤完成，避免中间状态缺失校验。amendment §4 已覆盖此要求
- **结论**: DS OQ-1 已收敛

### 5. builder 委托 owner 且保持错误行为

- **状态**: **resolved**
- **直接证据**: amendment §4 — "`build_fins_download_request` 必须删除自身重复的 `start > end` 判断，改为构造 `FinsDownloadDateRange` 并由 typed owner 执行 range 校验；owner 继续抛同一 `FinsDownloadUsageError` 中文消息，保持当前 builder 的可观察错误类型与文本行为"
- **代码验证**: 当前 builder line 147 抛 `FinsDownloadUsageError("--start 不能晚于 --end，请检查下载日期范围")`。`__post_init__` 需抛相同类型和等价中文消息
- **结论**: 错误行为保持已明确约束

### 6. upload 测试区域 hard stop

- **状态**: **resolved**
- **直接证据**: amendment §3 line 39 — "`tests/fins/test_cn_pipeline.py` 的全部现有 `test_upload_*` 测试函数及其 fixture/helper 区域必须零 diff；code rereview 必须检查该文件 unified diff 的每个 hunk，任何 upload 测试区域变更都是 hard stop"
- **风险表**: amendment §6 新增行 — "upload 测试被 allowlist 扩张波及 → upload 测试区域零 diff；code rereview 逐 hunk 核对，命中即退回"
- **结论**: DS DL-PA-03 已收敛。enforceability 从 "依赖 review 执行" 升级为 "hard stop + 逐 hunk 核对"

## Original MiMo findings rereview

### 001-_RecordingPipeline.download 覆写签名隐式依赖

- **状态**: **resolved**
- **resolution**: amendment §4 checklist #2 显式列出 `_RecordingPipeline.download` 同步。文件已在 allowlist 内，pyright 兜底

### 002-_collect_events 辅助函数隐式依赖

- **状态**: **resolved**
- **resolution**: amendment §4 checklist #3 显式列出 `_collect_events`、直接 `pipeline.download` 及全部相关调用。文件已在 allowlist 内，pyright 兜底

## Open Questions

无。DS OQ-1（非法组合 production 侧 invariant）已由 §4 `FinsDownloadDateRange.__post_init__` 收敛。

## Residual Risks

1. **`__post_init__` 与 builder 删除重复检查的实现顺序**：builder 当前在 line 146 做 `start_bound > end_bound` 检查。实现时应先在 `__post_init__` 中添加不变量校验，再删除 builder 重复检查，避免中间状态。amendment §4 已隐含此要求，implementation agent 按 checklist 顺序执行即可。风险低。

2. **`__post_init__` 抛出的错误消息与 builder 当前消息的精确一致性**：amendment 要求 "保持当前 builder 的可观察错误类型与文本行为"。`__post_init__` 抛出的 `FinsDownloadUsageError` 消息应与当前 `download_contract.py:147` 的消息一致或等价。implementation agent 需注意。风险低。

## Conclusion

**pass**

Amendment 最新 revision 已全面收敛首轮 review 的全部注意项：

- MiMo 001/002：implementation checklist #2/#3 显式覆盖，pyright 兜底
- DS DL-PA-01：三个 call site 锁定 `start_is_explicit=True`
- DS DL-PA-02：False+非空 date 测试标注为 pipeline-layer direct contract test
- DS DL-PA-03：upload 测试区域 hard stop + 逐 hunk 核对
- DS OQ-1：`FinsDownloadDateRange.__post_init__` 拥有 explicit/bound 与 range 不变量

Amendment 可安全加入 accepted plan。
