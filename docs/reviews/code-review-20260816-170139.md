# Code Review（Slice 4 re-review）

## Scope

- Mode: current changes（re-review）
- Branch: `codex/upload-filing-oracle`
- Base: HEAD `1fd52c96f5e07f6007c3c6442d20635f3d37182b`
- Output file: `docs/reviews/code-review-20260816-170139.md`
- Included scope:
  - 初审 artifacts 全文：`docs/reviews/code-review-20260816-162505.md`（PASS 路）、
    `docs/reviews/code-review-20260816-163507.md`（FAIL 路，本路）
  - 修复 artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-code-review-fix-20260816.md`
  - 更新后的 implementation artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-implementation-20260816.md`
  - 当前完整未提交 diff（10 个修改文件 + 3 个 untracked artifacts）；重点增量：
    `dayu/fins/storage/_fs_source_integrity.py`（+37）、`_fs_source_document_core.py`（helper 重写 +
    material whole-kind 接入）、两个 owner test 文件的 +1010/+493 增量
  - 只读复核 Slice 2/3 accepted artifacts 中被本次 fix 触及的行为契约（Slice 2 fix D 的 whole-kind
    `repair_blocked_reason` 语义裁决；Slice 2 `test_damaged_fixture_classifier_snapshot_and_commit_are_consistent`）
- Excluded scope: 无；SEC/CN/HK workflow、download、README 属 later slices，不 review。
- Parallel review coverage: 无（单路走读 + 定向测试/pyright 复现）。

## 结论

**PASS**（附 1 个中 severity finding 需 Controller 显式 adjudicate + 3 条 residual risk）

初审（163507）三类 findings（高/中/低）全部有直接证据闭环；Controller 指定的 7 项严格验证项逐项通过
（其中验证项 2/3/4/5/6 均有 owner-level 直接测试锁定）。唯一需要 Controller 裁决的是 Finding 1：
本次 fix 在未显式引用/反驳 Slice 2 fix D 裁决的情况下，回退了 whole-kind `repair_blocked_reason` 的
reason 值语义并修改了 Slice 2 的 direct payload 断言——行为本身 fail-closed 且当前无按值消费者，不构成
correctness defect，但冻结契约的变更记录不完整。

---

## Findings

### 1-未修复-中-Slice 2 冻结的 whole-kind repair_blocked_reason 语义被回退，未经显式 re-adjudicate

- **入口/函数**: `_fs_source_integrity._derive_repair_blocked_reason()`（whole-kind 分支，
  `target is None` 时 `non_target_inventory = 全部 inventory`）
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:1280-1311`；
  既有断言被修改处 `tests/fins/test_fins_storage_atomicity.py:6964-6969`
  （`test_damaged_fixture_classifier_snapshot_and_commit_are_consistent`）
- **输入场景**: whole-kind mode（`requested_document_id=None`）下 source content 损坏
  （如 original/declared file missing → `content_classification=REPAIR_REQUIRED`）。
- **实际分支**: 新谓词步骤 4：`content_classification.status is not COMPLETE` → 返回
  `NON_TARGET_SOURCE_INCOMPLETE`。
- **预期行为**: Slice 2 fix D 的已接受裁决明确："whole-kind repairable incomplete固定
  `CANONICAL_MANIFEST_UNAVAILABLE`，unsafe仍为`CROSS_SOURCE_PUBLICATION_UNSAFE`"，理由是 whole-kind
  没有 repair target，"NON_TARGET_SOURCE_INCOMPLETE 语义失真"；该行为由 direct payload test 锁定。
- **实际行为**: 同一场景现在返回 `NON_TARGET_SOURCE_INCOMPLETE`；Slice 2 的 direct payload 断言被同步
  修改为
  `whole.repair_blocked_reason is SourceIntegrityRepairBlockedReason.NON_TARGET_SOURCE_INCOMPLETE`。
  修复 artifact 的 Finding 2 描述给出了新规则（"whole-kind mode把全部 source视为non-target"），但未引用、
  反驳或重新裁决 Slice 2 fix D 的"语义失真"理由；Controller 的 fix 授权只覆盖"允许修改该文件"，没有
  显式 adjudicate 该 reason 值的语义变更。
- **直接证据**: Slice 2 implementation artifact "Code review adjudication" 表 D 行原文与本次 diff
  `tests/fins/test_fins_storage_atomicity.py` 行 6964-6969 的断言替换；fix artifact 全文无 Slice 2 fix D
  引用。
- **影响**: 当前无按值消费者（commit validator 仅检查 `is not None`，`_fs_storage_infra.py:1117`；
  Phase B 与 service 只消费"是否有 blocked"并统一投影 `SOURCE_REPAIR_BLOCKED`），因此无运行时行为
  错误、fail-closed 不变量未破坏。风险是：(a) 冻结 owner-level 行为契约在裁决记录缺失下变更，
  (b) 未来任何按 reason 值分支的消费者（如 download preflight、诊断投影）会拿到与 Slice 2 裁决理由
  相反的语义。从 Phase B material preflight 视角，新语义有其合理性（material 相对 filing repair 确实是
  non-target），这正是需要 Controller 在两者间做显式裁决的点。
- **建议改法和验证点**: Controller 在 adjudication 中二选一：(a) 接受新语义——在 fix/implementation
  artifact 中显式记录"推翻 Slice 2 fix D 的 whole-kind reason 语义及理由"，并同步更新 Slice 2 相关
  owner test 的锁定意图；(b) 拒绝新语义——恢复 whole-kind repairable incomplete →
  `CANONICAL_MANIFEST_UNAVAILABLE`（Phase B material content missing 场景随之投影该 reason），恢复原
  direct payload 断言。两种方案都需重新运行两份 owner test 文件。
- **修复风险（低）**: 纯 reason 值归属裁决 + 断言更新；不改 Phase B 顺序、不改 reset/rollback 语义。
- **严重程度（中）**:

---

## Controller 7 项严格验证逐项证据

### 1) 初审三类 findings 全部闭环

| 初审 finding | 闭环证据 |
| --- | --- |
| 163507-F1（高）：material damage 对 Phase B 不可见 | 已修复。`reset_source_document_for_repair()` 在 identity comparison 之后追加 `MATERIAL + requested_document_id=None` whole-kind inspection 恰一次（`_fs_source_document_core.py:455-466`）；`SourceIntegrityPreflightError` 精确映射 `CROSS_SOURCE_PUBLICATION_UNSAFE`（行 463-466）；material `repair_blocked_reason` 在 reset 前消费（行 472-475）。service 真实 filesystem 测试 `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once` 三场景（content_missing / manifest_missing / structural_unsafe）全部断言 `SOURCE_REPAIR_BLOCKED`、`rollback_calls == 1`、`commit_calls == 0`、old tree SHA 与 filing/material meta、company meta 不变、batch root 清空。初审 probe 场景现已被真实测试覆盖。 |
| 163507-F2（中）：blocked predicate 双派生且闭合不一致 | 已修复。`_derive_repair_blocked_reason()` 成为唯一 predicate owner（`_fs_source_integrity.py:1257-1311`）：exact mode 排除 target 后逐 non-target 检查 public UNSAFE / content 完整性 / `classification.reasons != shared_manifest_reasons` / canonical item；whole-kind 将全部 inventory 视为 non-target 并额外关闭 shared manifest 问题。core helper 重写为纯机械消费者（`_fs_source_document_core.py:108-111`：`repair_blocked_reason is not None` → 原样抛出；行 119-126：clean payload 的 item shape/unique 违约抛 producer-invariant `RuntimeError`，不再重建业务 blocked reason）。初审的 divergent 场景（sibling projection mismatch）由 `test_filing_sibling_manifest_mismatch_owner_matches_phase_b_block` 用 synthetic payload 直接锁定 owner 产生 `NON_TARGET_SOURCE_INCOMPLETE` 且 collector 机械传播同一 reason。 |
| 163507-F3（低）：三个 fail-closed 分支缺测试 | 已修复。`test_prepare_upload_rejects_invalid_repair_disposition`（union 之外 disposition → 固定 `ValueError`）；`test_prepare_upload_rejects_delete_with_existing_repair`（delete×repair → 固定 `ValueError`）；`test_existing_filing_repair_manifest_rewrite_failure_rolls_back_once`（patch 真实 `source_document_core_module._write_json` 抛 `OSError`，证明 target reset 后 manifest rewrite 失败 → rollback exactly once、commit zero、old tree/meta/company meta 不变、batch cleanup）。 |

### 2) repair_blocked_reason 仅由 storage inspector owner 派生，core 只机械消费

- core 中 `_canonical_remaining_manifest_items_for_repair()` 第一分支只做
  `inspection.repair_blocked_reason is not None → raise`（`_fs_source_document_core.py:108-111`）；
  后续只收集 item，shape/unique 违约抛 `RuntimeError`（producer invariant），不派生任何 blocked reason。
- `reset_source_document_for_repair()` 对 material 同样只消费
  `material_inspection.repair_blocked_reason`（行 472-475）。
- 全仓 grep：`repair_blocked_reason` 消费者仅 core 两处与 commit validator 的 `is not None` 检查，无第三派生点。

### 3) staged batch 内 filing exact inspection 恰一次、material whole-kind inspection 恰一次

- 代码路径：`reset_source_document_for_repair()` 行 427-433（FILING + `requested_document_id=external_document_id`
  恰一次）、行 456-462（MATERIAL + `requested_document_id=None` 恰一次），无循环、无重扫。
- `test_source_repository_repair_facade_resets_target_and_rewrites_canonical_sibling_manifest` 用
  `_InspectorCallCounter`（委托真实 production inspector 并记录 `(source_kind, requested_document_id)`）
  monkeypatch 真实 core 模块后调用真实 facade，断言 `counter.calls == [(FILING, target_id), (MATERIAL, None)]`。

### 4) material 内容/manifest/root 损坏均在 reset 前投影 SOURCE_REPAIR_BLOCKED，clean/empty 不阻断

- 三场景 service 测试（见上表）分别锁定 `NON_TARGET_SOURCE_INCOMPLETE` / `CANONICAL_MANIFEST_UNAVAILABLE` /
  `CROSS_SOURCE_PUBLICATION_UNSAFE`（structural_unsafe 额外断言 `__cause__` 为
  `SourceIntegrityPreflightError`），且全部发生在 target reset 前（`rollback_calls == 1` 证明异常在
  publish 内抛出、未到 commit）。
- clean material：成功路径测试在 fixture 中保留一个 complete material，repair 正常 commit 且 material 最终
  COMPLETE（`test_source_repository_repair_facade_resets_target_and_rewrites_canonical_sibling_manifest`）。
- empty material：`_inspect_source_kind_unguarded` 对 material root/ticker 不存在返回空 payload、
  `repair_blocked_reason=None`（`_fs_source_integrity.py:207-213,232-238`），既有 filing-only fixture 的
  成功 repair 测试覆盖该路径。

### 5) target local unsafe 与 shared filing manifest untrusted 的 stale/conflict 优先级保持

- 代码顺序：filing exact inspect → target presence gate（行 435-436）→ staged status/content gate
  （行 438-444）→ identity comparison（行 445-453）→ 才进入 material inspect 与 blocked 消费。target
  public `UNSAFE`（含 `_apply_manifest_facts` 对 manifest untrusted 的全 inventory 覆盖）与
  content 非 {COMPLETE, REPAIR_REQUIRED} 在 material 扫描之前即抛 `SourceIntegrityRevisionConflictError`，
  因此即使同批 staging 存在 material 损坏也不会误投影 blocked。
- storage drift grid 新增 `shared_untrusted`（staging filing_manifest.json 写为 `{` → untrusted → conflict）；
  service grid 新增 `target_unsafe` 与 `shared_untrusted`，均断言
  `SOURCE_REVISION_STALE`（非 `SOURCE_REPAIR_BLOCKED`）、rollback once、old tree 不变。

### 6) invalid disposition、delete+repair、manifest rewrite OSError rollback 测试有效

- 三个测试均存在且断言固定 `ValueError` 文案 / 真实 `_write_json` patch 后的 `OSError` 传播与
  `rollback_calls == 1`（见 Finding 闭环表）。定向运行验证通过（见下）。

### 7) 原 Slice 4 九项语义、scope、pyright、覆盖率与冻结文档约束无回退

- 九项语义：初评已验证的 disposition 贯穿/identical-skip bypass、storage 唯一 owner 与 gate 顺序、
  target-local damage 不误判 blocked、staging 污染边界、facade 纯 delegate、stale/blocked 精确映射、
  Controller caller 机械接线、tests 真实命中、无兼容/default/Any/反射——本次 diff 未回退其中任何一项
  （facade/protocol/service caller diff 与初评完全一致；core 顺序只增加了 material inspect 与机械消费）。
- scope：新增 `_fs_source_integrity.py` 系 Controller 明确授权；其余文件仍在原 allowed + 扩展集合内，
  `git status` 无越界文件。
- pyright：本 reviewer 独立运行全 10 个修改文件 `0 errors, 0 warnings, 0 informations`。
- 测试：本 reviewer 独立运行 `test_fins_storage_atomicity.py -k "repair or material_whole_inspection or
  damaged_fixture"` → 23 passed；`test_docling_upload_service.py -k repair` → 15 passed。覆盖率未独立重跑，
  依据 fix artifact 声称的 80%–97% 逐文件 branch coverage（详见 residual）。
- 冻结文档：`git diff -- README.md dayu/fins/README.md tests/README.md docs/cli_ci_oracles.json
  docs/cli_ci_scenarios.json docs/host/design.md docs/engine/design.md` 输出为空。
- 唯一回退面即 Finding 1 所述 Slice 2 whole-kind reason 值语义（行为契约变更记录不完整，fail-closed
  不变量未破坏）。

---

## Open Questions

- 无。Finding 1 是需要 Controller 裁决的语义归属事项，不阻碍对本 diff 的技术判断。

## Residual Risk

- Finding 1 的裁决（接受新语义或恢复 Slice 2 reason 值）需 Controller 在 adjudication 中显式闭环并同步
  artifact 记录；两种方案都需重跑两份 owner test 文件。
- `_derive_repair_blocked_reason()` 内部存在防御性分支：exact mode 的 `CROSS_SOURCE_PUBLICATION_UNSAFE`
  （unassignable_root_fact / non-target public UNSAFE）在 Phase B 可达路径上大多被 target conflict gate
  前置覆盖；步骤 7（`len(canonical_manifest_items) != len(inventory)`）在步骤 4/6 之后不可达；exact mode
  的 `CANONICAL_MANIFEST_UNAVAILABLE` 同样不可达（non-target item 为空 ⇔ content 非 COMPLETE，已被步骤 4
  先行命中）。plan §6.2 step 5 的 filing Phase B `CANONICAL_MANIFEST_UNAVAILABLE` 仍无实际触发路径——
  reason 只影响内部细分，不改变 blocked 结果，无需本 slice 处理。
- 完整 affected matrix（345 passed）与完整 Fins suite（1827 passed）由 fix artifact 声称，本 reviewer 仅
  独立复现了 repair 相关 38 个用例与 10 文件 pyright；coverage 数字未独立重跑。
- Slice 2 语义变更（Finding 1）的潜在下游消费者（download preflight、诊断/evidence 投影）目前都不按
  reason 值分支；若未来接入，需先消费 Controller 对 Finding 1 的裁决结论。
