# Code Review

## Scope

- Mode: current changes
- Branch or PR: `codex/interactive-oracle` / PR 190，F11/F12 S4
- Base: `321893e423beeb20acf2768c03b2be3477c92903`
- Output file: `docs/reviews/code-review-20260805-210138.md`
- Included scope: `utils/smoke_host_public_conversation_memory_scenarios.py`、`tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`、真实 provider evidence root、失败时只读追踪到 fallback owner 路径
- Excluded scope: 生产 contract 修改、oracle/scenario/旧 evidence、S5 registry/PR body、未受本 slice 影响的仓库代码
- Parallel review coverage: 无；当前约束不启用 subagent。主 reviewer 分别完成 correctness/error-path 与 architecture/semantic-owner/LLM-facing/typing/test-gap 两轮检查。

## Findings

### S4-001-未修复-[高]-真实 exhausted fallback 的 current-input material digest 非同源

- **入口/函数**: 真实 proactive compaction 两次候选耗尽后进入 fallback；`_fallback_material_blocks`、`_current_input_material_block_for_fallback`、`run_input_material_block`、`_selected_material_render_view`
- **文件(行号)**: `dayu/host/compact_pipeline.py:1113`、`dayu/host/context_fallback.py:487`、`dayu/host/compact_material.py:706` 与 `:734`、`dayu/host/run_input.py:5071`
- **输入场景**: DeepSeek `capability=json_object`，最大 attempts=2，各语义 char cap=1；真实 provider 两次返回可解析但 low-information 的空语义候选；current pressure input 含连续空白/换行。
- **实际分支**: attempt 1 `quality_check_rejected` 且 retry；attempt 2 `quality_check_rejected` 且 `fail_compaction`；Host 选择 recent-window fallback，随后在提交 terminal/dispatch 时比较 selected material view digest。
- **预期行为**: selection 与 replay 从唯一 current-input material block 真源派生相同 text/size/digest，提交一个 exhausted terminal 并执行 bounded fallback dispatch。
- **实际行为**: promotion critical task 抛出 `HostDurableError: fallback selected material view digest mismatch`；预期 rejected/failed terminal 和 fallback dispatch 没有提交，调用没有正常收口。
- **直接证据**: `screen/10-deepseek-exhausted-fallback.txt:71-72` 保存两次真实拒绝，`:73-117` 保存精确异常与堆栈。`_fallback_material_blocks` 对 raw `source_snapshot.current_input_text` 直接计算 size/digest；replay 侧 `_current_input_material_block_for_fallback` 调用 `run_input_material_block`，该 helper 先经 `normalized_material_text` 折叠空白再计算 digest；`_selected_material_render_view` 对二者比较并抛出本次异常。输入与两个代码路径共同直接解释精确错误，不依赖日志猜测。
- **影响**: 真实 exhausted fallback 在 durable commit 边界失败，Host critical task fatal，S4 mandatory exhausted fallback/single terminal 无法取证；属于生产错误状态与不可正常收口，不是 provider unavailable/rejection/timeout。
- **建议改法和验证点**: 在独立 implementation slice 让 selection/replay 复用同一个 current-input material block owner（共享构造 helper 或 frozen typed handoff），禁止在 run-input consumer/harness fallback 补偿；新增含可折叠空白的 proactive exhausted fallback owner test，断言两次 rejected、唯一 failed terminal、manifest bindings、bounded fallback dispatch 与 reconnect。修复双 review 后必须从全新 evidence root 重跑 MiMo→DeepSeek mandatory rows。
- **修复风险（低/中/高）**: 中；改动触及 fallback selection digest 与 durable replay contract，需要同时验证 proactive/reactive、protected recent 与 recovery。
- **严重程度（低/中/高/严重）**: 高
- **Gateflow 裁决**: `deferred-with-owner`；owner 为生产 fallback current-input material construction/digest boundary。S4 allowed scope 禁止修生产，故停止取证并阻塞本 gate。

## Open Questions

- 无。根因 owner 与复现场景已有直接代码/数据证据；待办不是澄清，而是另开允许修改生产 contract 的 implementation slice。

## Residual Risk

- 旧 bundle `/Users/leo/workspace/.dayu-cli-ci/interactive-memory-v3-20260805T-s4-HeHeLm/` 是 immutable / superseded partial evidence：screen 保留两次真实 rejection，但 in-memory request captures 缺失，transaction 也回滚了 rejected/failed terminal；同时 bundle metadata 使用了错误 base。该 bundle 不得回写、补写或重算，不能据此推断该 invocation 的完整 attempt manifest/binding；产品修复后只能写入新的独立 evidence root。
- `04` 证明 MiMo accepted path 无 structured-output payload；产品 bug 后按要求停止，MiMo fallback no-downgrade 子项没有实测。
- `07` accepted replacement 后 selected recent window 仍暂含明确纠正旧值的原始轮次；`08` 跨进程 reconnect/后续窗口证据才证明完整 Memory 与 RunInput 中旧 marker/value 均为 0。报告按时间边界区分这两项，不把 `07` 单独冒充 reconnect closure。
- 当前 harness owner tests 覆盖 CLI/suite assembly、Host owner token estimator 与 repair caps；真实 provider 状态机只能由外部 smoke evidence 覆盖。失败路径已改为在 Host lifecycle `finally` 导出，避免产品修复后重跑再次丢失 capture。

## S4 Evidence Gate

- Evidence root: `/Users/leo/workspace/.dayu-cli-ci/interactive-memory-v3-20260805T-s4-HeHeLm/`
- Human-readable report: `report/observed-behavior.md`
- Reconnect equality: `report/reconnect-equality.json`
- Secret scan: `report/secret-scan.json`
- Campaign digest: `report/digest.json`
- Bundle status: `immutable / superseded partial evidence`；因 base metadata 错误与产品 blocker 归档，禁止回写或重算。
- Real observation: `partial`
- Implementation/harness validation: `PASS`
- S4 gate: `BLOCKED_BY_PRODUCT_CONTRACT_FAILURE`
- Oracle: `pending`；本 slice 未修改 registry，也未把任何新 scenario 标为 accepted/ready。

## Review Passes

- Correctness/state-machine/error-path：检查真实 MiMo/DeepSeek screen、compactor captures、canonical EventLog、public Tool Trace equality、Memory/artifact/RunInput/reconnect；accepted/rejected identity equality finding=0；接受 S4-001。
- Architecture/semantic-owner/LLM-facing/typing/test-gap：harness 只镜像 typed request 后调用原真实 runner；response format 由正式 payload builder 投影；public #59 只经 public resolver/analysis；证据导出不改变生产 contract；prompt/caps/repair 观测来自实际 request；没有 fake provider substitute、compatibility shim、下游 semantic fallback。接受 S4-001 owner 判断。
- Adversarial failure pass：验证 no-boundary、harness-preflight、target-before-dispatch、assembly-mismatch、same-boundary rejection/repair、exhaustion fatal、证据导出失败不遮蔽原异常、旧 marker 时间边界与 secret leakage。
- 项目指令检查：修改范围限定为允许的 harness/test 与本独立 artifact；无生产/oracle/scenario/旧 evidence 修改；未 stage/commit/push；README 触发检查后无职责/用户工作流变化，且用户明确禁止额外文件，故不改 README。

## Validation

- `pytest tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py -q`：`21 passed, 3 warnings`。
- `pyright`：`0 errors, 0 warnings, 0 informations`。
- `git diff --check`：通过。
- final secret scan：以 `report/secret-scan.json` 为准，finding 必须为 0；SQLite/WAL/SHM 排除且未复制。

## Closeout

S4 没有通过。可执行的 real-provider observation 已诚实保存，但 mandatory exhausted fallback/single terminal 被生产 owner bug 阻塞。不得进入把 S4 写成 complete/ready 的 S5 收口；应先开新 implementation slice 修复 S4-001，双 review 后用新的不可复用 evidence root 从 MiMo 首先运行重新取证。
