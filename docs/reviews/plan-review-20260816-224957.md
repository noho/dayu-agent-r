# UF-FIX10 same-request-concurrency Plan Re-Review

## Reviewed target

- **文件**: `docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md`（fix 后版本）
- **范围**: 同请求并发 filing upload 的 batch-time recheck、canonical skip 与 typed conflict 裁决
- **日期**: 2026-08-16 22:49:57（本机系统时钟）
- **分支**: `codex/upload-filing-oracle`
- **前置 artifacts**: `docs/reviews/plan-review-20260816-221939.md`（MiMo 首轮）、`docs/reviews/plan-review-20260816-222742.md`（DS 首轮）、`docs/gateflow/uf-fix10-plan-review-fix-20260816.md`（fix artifact）
- **re-review 方式**: 只读审查。未修改 plan、生产代码、测试；未 commit。

## 1. Re-review 范围与方法

本轮验证 Controller 接受的全部11个 finding（MiMo 01-04、DS F1-F7）及2个 open question（DS OQ1-2）是否在 plan fix 后真实闭合，同时攻击修正是否引入新 blocker。逐项使用 Gateflow 状态：`已修复` / `未修复` / `部分修复` / `证据失效`。

直接证据来源：fix artifact、更新后 plan、`docling_upload_service.py`、`sec_upload_workflow.py`、`cn_pipeline.py`、`_fs_company_meta_core.py`、`_fs_storage_infra.py`、`_fs_filing_upload_state_core.py`、`_fs_source_integrity.py`、`repository_protocols.py`、`upload_failure.py`。

## 2. Accepted findings 逐项验证

### DS F1：preparation early skip 绕过 final arbitration — 已修复

- **首轮问题**: filing identical fingerprint 时 `prepare_upload()` 返回 `status="skipped"` 终态，workflow 短路不进入 `begin_batch()`，绕过 fresh arbitration。
- **代码事实**: `_can_skip_upload()`（`docling_upload_service.py:1264-1296`）对 filing identical 返回 `True`，call site（`:388-410`）构造 `UploadOperationResult(status="skipped")`。SEC（`:231`）和 CN（`:862`）的 `isinstance(prepared_upload, UploadOperationResult)` 短路终结。
- **plan fix**: §6.3 冻结 "filing 的 pre-prepare identical check 不再产生终态 `UploadOperationResult(status="skipped")`"；§7.1 步骤1-2 明确 "filing identical fingerprint 也必须执行 primary Docling conversion"、"所有 prepared filing candidate 都进入同一 publication owner"。
- **验证**: plan 正确识别当前代码行为并指定删除路径。新 §7.1 执行顺序要求所有 filing candidate（含 identical 命中）走 `begin_batch()` → batch fresh read → arbitration。与 MiMo 01/03 合并为 state-machine bundle，§7.2-§7.4 的封闭表覆盖 MISSING→COMPLETE 专用 skip 分支。
- **结论**: **已修复**。plan 级规格完整，实施 agent 有明确路径。

### DS F2：batch-time failure 映射与 failed action 真源未冻结 — 已修复

- **首轮问题**: §7.1.5 的 "或" 字把映射决策留给实施者；explicit create loser 可能投影为 `UNEXPECTED_RUNTIME`；failed terminal 的 `resolved_action` 真源未指定。
- **代码事实**: `SOURCE_PUBLICATION_CONFLICT` 当前不存在于 `FinsUploadFailureCode` 枚举（`upload_failure.py:37-53`）。`FileExistsError` → `OSError` → `STORAGE_IO`（SEC `:296`、CN `:927` 的 `except OSError` 先于 `except Exception`）。
- **plan fix**: §6.4 新增完整封闭映射表（10行），覆盖：state-dependent usage → `SOURCE_PUBLICATION_CONFLICT`；UNSAFE → `SOURCE_INTEGRITY_UNSAFE`；I/O/corruption → `STORAGE_IO`；repair revision/blocked → existing codes；cancel → cancelled terminal；rollback failure → `STORAGE_IO`。每行指定 action 投影真源：failed terminal 的 `requested_action` 始终取 raw request，`resolved_action/filing_action` 取 initial prepared action。显式 create 恒为 `create/create`。
- **验证**: 映射表封闭、无歧义。四个 state-dependent usage code（`CREATE_TARGET_EXISTS`、`UPDATE_TARGET_MISSING`、`EXISTING_SOURCE_REPAIR_REQUIRES_AUTO`、`COMPANY_NAME_REQUIRED`）明确映射到 conflict。其它 usage code 不伪装 concurrency。与 S2 test 3 断言一致。
- **结论**: **已修复**。映射表完整，action 真源冻结。

### DS F3：required field 导致 S1 allowed files 不闭合 — 已修复

- **首轮问题**: `FilingUploadPublishedState` 新增 required `publication_identity` 字段后，4个测试夹具文件不在 S1 allowed 清单。
- **plan fix**: §10.1 allowed tests 显式加入4个文件（`test_fins_ingestion_runtime.py`、`test_fins_service_runtime.py`、`test_fins_commands.py`、`test_fins_direct.py`），约束为 "仅机械补 `publication_identity=None`"。S1 exact tests 断言12："repo 中全部既有 `FilingUploadPublishedState(...)` direct constructor 都显式提供 `publication_identity`；四个 fixture 文件的 diff 只有机械 `None` 参数"。
- **验证**: 文件清单与约束明确，S1 闭环。
- **结论**: **已修复**。

### DS F4：staging company meta owner/实现位置未指定 — 已修复

- **首轮问题**: `_get_company_meta_unguarded` 只读 published tree（`_fs_company_meta_core.py:58-90` 通过 `_target_ticker_dir` 定位）；`_read_published_company_identity`（`_fs_storage_infra.py:889-962`）接受 `directory: Path` 参数但当前只被 published 路径调用。S1 allowed files 不含 `_fs_company_meta_core.py`。
- **plan fix**: §6.2 冻结新增 `_read_company_meta_from_ticker_dir_unguarded(external_ticker, ticker_dir)` helper，位于 `_fs_company_meta_core.py`。该 helper 对 caller 传入的 exact ticker root 调用现有 `_read_published_company_identity(ticker_dir, ...)` 做 strict 解析。现有 `_get_company_meta_unguarded()` 改为委托该 helper。§8.1 S1 allowed files 已含 `_fs_company_meta_core.py`。
- **验证**: 设计正确——`_read_published_company_identity()` 已接受任意 `directory: Path`，传入 `staging_ticker_dir` 语义兼容。唯一 parser owner 不变，staging 不读 published path，filing state core 不重复 parse。与 DS F4 fix 一致。
- **结论**: **已修复**。

### DS F5：FileExistsError 投影证据错误 — 已修复

- **首轮问题**: plan 声称 `FileExistsError` → `UNEXPECTED_RUNTIME`，实际是 `STORAGE_IO`。
- **代码事实**: SEC（`:296-304`）和 CN（`:927-935`）的 `except OSError` 先于 `except Exception`。`FileExistsError` 是 `OSError` 子类，命中 `STORAGE_IO`。
- **plan fix**: §2.1.4 已修正为 "投影为 `STORAGE_IO`（`storage_io`）"。
- **验证**: 修正与代码事实一致。
- **结论**: **已修复**。

### DS F6：缺少跨进程 winner/loser 覆盖 — 已修复

- **首轮问题**: 全部新并发测试为线程级，跨进程 winner/loser 仲裁路径从未被真实跑过。
- **plan fix**: §10.2 test 9 新增 `multiprocessing.get_context("spawn")` + process-safe `Barrier/Queue` 的 real-FS exact-auto 测试，断言 `{ok, skipped}` / `{1,0}`。
- **验证**: 测试规格遵循现有 `test_fins_storage_atomicity.py` 的 spawn 基建模式（`:3480/3709/8131`）。
- **结论**: **已修复**。

### DS F7：SKIP 路径缺 cancellation checkpoint — 已修复

- **首轮问题**: SKIP 分支直接 rollback 并构造 skip result，无取消检查。
- **plan fix**: §6.4 映射表增加 "canonical SKIP terminal 前观察到 cancel" 行；§7.1 步骤8 冻结 "在构造 terminal 前观察 `cancellation`。已取消则先 rollback 并返回 cancelled；rollback 失败是 `STORAGE_IO`，绝不报 cancelled/skip"。
- **验证**: 与现有 `docling_upload_service.py:354/376/424` 的三处取消 checkpoint 模式一致。cancel-before-skip 语义正确：rollback 成功 → cancelled，rollback 失败 → `STORAGE_IO`。
- **结论**: **已修复**。

### MiMo 01：MISSING→COMPLETE 与 publish/skip 规则矛盾 — 已修复

- **首轮问题**: §7.2 "任一 MISSING 与 present 切换 -> 已变" 要求走 CONFLICT，但 §9.8 暗示 "changed-but-equivalent exact auto" 路径。
- **plan fix**: §7.2 明确 "`MISSING -> COMPLETE` 不 stable，但在 §7.3 专用 exact-auto predicate 成立时明确 `SKIP`。该分支中 prepared resolved action=`create`、fresh resolved action=`update` 是合法的专用例外"。§7.4 封闭表有独立 `MISSING → COMPLETE` 行。§7.3 步骤6："该 predicate 对 source observation 是 stable 还是 changed 无例外地生效"。
- **验证**: 规则不再矛盾。MISSING→COMPLETE 在 §7.3 全部条件成立时只能 SKIP；不满足时按 §7.2 走 CONFLICT。与 MiMo 01 fix 一致。
- **结论**: **已修复**。

### MiMo 02：batch company meta 来源未指定 — 已修复

- 与 DS F4 同一 owner fix。§6.2 指定 helper、strict parse、allowed file 与不得读 published path 的测试证据。
- **结论**: **已修复**。

### MiMo 03：resolved-action invariant 与 exact-auto 例外矛盾 — 已修复

- **首轮问题**: §7.2 要求 resolved action 一致，否则 invariant failure；§9.8 说 changed-but-equivalent exact auto 是 skip 例外。
- **plan fix**: §7.2 明确 "`MISSING -> COMPLETE` exact-auto 专用分支中 prepared=`create`/fresh=`update` 合法，不触发 action invariant"。§7.4 封闭表独立行。
- **验证**: §7.2 末尾："不得把此规则误用到上述 `MISSING -> COMPLETE` exact-auto 专用分支"。
- **结论**: **已修复**。

### MiMo 04：asset source 隐式跨模块字面量对齐 — 已修复

- **首轮问题**: `_ASSET_SOURCE_ORIGINAL`（`docling_upload_service.py:76`，`Final[_AssetSource]`）与 `_FILE_SOURCE_ORIGINAL`（`_fs_source_integrity.py:63`，`Final[str]`）值相同但定义独立，无契约保证。
- **plan fix**: §6.1 冻结 contract-owned `FilingUploadAssetSource` alias 与 typed constants，由 `repository_protocols.py` 唯一定义并从 `dayu.fins.storage` 导出。§6.1 明确 "删除各自的 filing asset-source private alias/constant，不得只依赖字面量恰好相同"。
- **验证**: 当前代码确认两组常量值恰好相同（`"original"`、`"docling"`），但类型标注不同（`Final[_AssetSource]` vs `Final[str]`）。plan 要求统一到 contract 层是正确修复。
- **结论**: **已修复**。

## 3. Accepted open questions 逐项验证

### DS OQ1：`primary_original_asset_name` 命中 name 还是 original filename — 已修复

- **plan fix**: "精确命中 original descriptor 的 storage `name`；docling `derived_from` 与 primary 使用同一 storage identity，绝不使用用户 basename"。
- **验证**: §6.1 明确 "`primary_original_asset_name` 存的也是该 storage `name`，绝不是用户 basename / `original_filename`"。
- **结论**: **已修复**。

### DS OQ2：prepared assets/companions 排序 — 已修复

- **plan fix**: "prepared/durable `assets` 和 companions 都按 storage `name` 字典序排序，exact dataclass equality 不依赖偶然输入/文件顺序"。
- **验证**: §6.1 "`assets` 在 prepared/durable 两端都按 `descriptor.name` 字典序排序"。§6.3 "`assets` 与 companions 均按 storage `name` 字典序排序"。
- **结论**: **已修复**。

## 4. Fix 引入新 blocker 攻击

### 4.1 `_read_company_meta_from_ticker_dir_unguarded` 不存在

- **攻击**: 该 helper 当前不存在于代码库（grep 零结果）。plan 要求新增但未提供完整函数体。
- **评估**: 不是 blocker。plan §6.2 明确指定：(1) 位于 `_fs_company_meta_core.py`；(2) 接受 `(external_ticker, ticker_dir)` 参数；(3) 委托现有 `_read_published_company_identity(ticker_dir, expected_storage_key=ticker_dir.name, known_directory_stat=...)`；(4) 做 strict canonical ticker 校验。`_read_published_company_identity` 已接受任意 `directory: Path`（`_fs_storage_infra.py:889`），staging 目录语义兼容。实施 agent 有足够信息生成该 helper。
- **结论**: 非 blocker，plan 规格足够 code-generation-ready。

### 4.2 `publication_identity` required field 对现有构造点的影响

- **攻击**: 新增 required field 可能遗漏构造点。
- **评估**: 不是 blocker。plan §10.1 已列出4个 fixture 文件 + 2个 production 文件（`_fs_filing_upload_state_core.py:94/134`），S1 断言12 要求验证所有 direct constructor。`FilingUploadPublishedState` 是 `repository_protocols.py` 的 public contract，grep 可定位全部构造点。
- **结论**: 非 blocker，plan 已覆盖。

### 4.3 §6.4 映射表四个 usage code 的完整性

- **攻击**: 是否存在未被映射表覆盖的 batch-time 错误路径？
- **评估**: 映射表覆盖：(1) state-dependent usage（4个 code）→ CONFLICT；(2) 其它 usage → `UNEXPECTED_RUNTIME`；(3) UNSAFE → `SOURCE_INTEGRITY_UNSAFE`；(4) I/O/corruption → `STORAGE_IO`；(5) repair revision/blocked → existing codes；(6) cancel → cancelled；(7) rollback failure → `STORAGE_IO`；(8) programming invariant → `UNEXPECTED_RUNTIME`。这是穷尽分类——validator 只产生 usage/format/operational 三类错误，加上 state inspector 的 UNSAFE 和 I/O。无遗漏。
- **结论**: 非 blocker，映射表封闭。

### 4.4 cross-process test 的 process-safe barrier 可行性

- **攻击**: `multiprocessing.Barrier` 在 spawn 模式下是否可靠？
- **评估**: `test_fins_storage_atomicity.py` 已有 spawn multiprocessing 基础设施（`:3480/3709/8131`），`Barrier` 是标准库 `multiprocessing` 组件。plan 指定 "process-safe barrier 确保两进程在 initial MISSING 下完成 prepare 后才竞争"，与现有模式一致。
- **结论**: 非 blocker。

### 4.5 SKIP 前 cancel 与 rollback 语义

- **攻击**: cancel 后 rollback 失败是否可能泄漏状态？
- **评估**: plan §6.4 映射表明确 "rollback 失败则是 `STORAGE_IO`，绝不报 cancelled/skip"。与现有 `rollback_prepared_upload_batch()` 的 primary/secondary error 规则一致。
- **结论**: 非 blocker。

## 5. Re-review 结论

**pass**

全部11个 accepted findings（MiMo 01-04、DS F1-F7）及2个 accepted open questions（DS OQ1-2）均在 plan fix 中真实闭合，状态均为 **已修复**。修正未引入新 blocker。

关键闭合验证：

| 用户指定核对项 | 状态 | plan 位置 |
| --- | --- | --- |
| MISSING→COMPLETE exact auto 专用 skip | 已修复 | §7.2-§7.4 封闭表 |
| early skip 删除 | 已修复 | §6.3、§7.1 步骤1-2 |
| staging company reader | 已修复 | §6.2 `_read_company_meta_from_ticker_dir_unguarded` |
| contract-owned source constants/排序 | 已修复 | §6.1 `FilingUploadAssetSource` + 字典序排序 |
| failure/action 映射 | 已修复 | §6.4 封闭映射表（10行） |
| cancel checkpoint | 已修复 | §6.4、§7.1 步骤8 |
| cross-process tests | 已修复 | §10.2 test 9 |

plan 已 code-generation-ready，可进入 implementation authorization。
