# WU-CLI-INIT-01 Plan Rereview (AgentMiMo Final)

## Review metadata

- reviewer：AgentMiMo
- review type：final independent plan rereview
- reviewed target：`docs/reviews/wu-cli-init-01-plan-codex.md`
- date：2026-07-30
- timestamp：20260730-143852
- scope：完整审查 plan artifact，重点核验 R01-R03，继续寻找 material finding
- inputs：
  - `AGENTS.md`（根目录约束）
  - `docs/reviews/wu-cli-init-01-goal-confirmation-controller.md`
  - `docs/reviews/wu-cli-init-01-plan-review-adjudication-controller.md`
  - `docs/reviews/wu-cli-init-01-plan-codex.md`（当前 plan）

## R01-R03 核验结果

### R01 — PRESERVE copytree symlink 行为 ✅

**当前 plan 完整落实：**

1. §6.5 已明确：
   > "显式调用 `shutil.copytree(public_config, staged_config_root, symlinks=True, ignore_dangling_symlinks=False)` 把用户 config 复制到 staged config，保留用户额外内容及 symlink/dangling symlink 的原始 shape；不得使用默认 `symlinks=False` follow symlink，也不得把 workspace 外部目标内容复制成 staged regular file"

2. S4 exact changes 已明确：
   > "PRESERVE 显式以 `shutil.copytree(..., symlinks=True, ignore_dangling_symlinks=False)` 把用户 config 复制到 staged config，禁止 dereference"

3. S4 tests 已明确：
   > "用户 config 内指向 workspace 外部 regular file 的 symlink 经 copytree 后在 staged tree 仍是 symlink shape，外部目标 bytes 未被复制成 regular file，随后 no-follow validation fail closed；dangling symlink 同样保留 shape 并失败"

4. §17 completion 已确认：
   > "R01 固定 PRESERVE copytree 的 `symlinks=True` / `ignore_dangling_symlinks=False`，并以 staged shape 与外部目标 bytes 断言防 dereference"

**代码事实验证：**
- 当前 `init_workspace.py` 第 822-826 行和第 868-871 行已有 `symlinks=True` 用法
- plan 要求的 `ignore_dangling_symlinks=False` 参数明确

**结论：** R01 在 plan 中完整落实，implementation agent 有明确执行路径。

---

### R02 — cleanup shape 判定 ✅

**当前 plan 完整落实：**

1. §6.5 已明确：
   > "expected shape 的产生、actual shape lock 与 quarantine 后 shape recheck 都只能读取同一次对应 no-follow identity/stat 结果的 `PathIdentity.mode`，分别通过 `stat.S_ISREG(mode)` / `stat.S_ISDIR(mode)` 判定；不得使用 `Path.is_file()`、`Path.is_dir()` 或任何 follow-symlink API"

2. §6.5 还详细描述了 dispatch 顺序（6步）：
   - direct-child containment
   - no-follow actual identity 读取并与 expected identity 精确锁定
   - 从 actual no-follow identity 的 mode 通过 `stat.S_ISREG` / `stat.S_ISDIR` 得到 actual shape，并与 typed expected shape 精确匹配
   - `os.replace` 到 same-parent quarantine
   - 对 quarantine 再做 identity/shape lock，并确认原名 no-follow absent
   - 按 expected shape 分派

3. S4 exact changes 已明确：
   > "expected、actual、quarantine 三处 shape 都只从各自同一次 no-follow identity/stat 的 `mode` 通过 `stat.S_ISREG` / `stat.S_ISDIR` 判定，禁止 `Path.is_file()` / `Path.is_dir()`"

4. S4 tests 已明确：
   > "quarantine recheck 中 symlink-to-regular-file 与 symlink-to-directory 的 no-follow mode 都不满足 `stat.S_ISREG` / `stat.S_ISDIR` expected shape，证明 cleanup 不会 follow target"

**代码事实验证：**
- 当前 `init_workspace.py` 已使用 `os.stat(path, follow_symlinks=False)` 和 `stat.S_ISREG` / `stat.S_ISDIR`
- `_path_identity()` 函数（第1411-1436行）使用 `os.stat(path, follow_symlinks=False)`
- `_path_exists_no_follow()` 函数（第1439-1453行）使用 `os.stat(path, follow_symlinks=False)`
- 当前 `_cleanup_private_path()` 实现（第1267行起）缺少 `_PrivatePathShape` 参数，需要按 plan 扩展

**结论：** R02 在 plan 中完整落实，implementation agent 有明确执行路径。

---

### R03 — PRESERVE 的 target execution profile ✅

**当前 plan 完整落实：**

1. §4.3 已明确：
   > "init 不复制 profile 分档常量；唯一读取 API 是 `ConfigLoader(package_config_dir=_PACKAGE_CONFIG_ROOT).load_execution_profiles(...)`"
   > "FIRST / OVERWRITE / confirmed RESET 传 `workspace_config_dir=None`，目标实际生效 profile 来自即将发布的 package layer"
   > "PRESERVE 传真实 workspace config dir，使用 ConfigLoader 的 layered typed load"
   > "workspace `execution_profiles.json` 缺失时由 loader 的 package layering 规则取得 package layer"
   > "存在但 JSON/schema/default-profile 引用非法时 fail closed，明确提示 `rerun with --overwrite`，不得改传 `None` 或以 package 值掩盖损坏"

2. S2 exact changes 已明确：
   > "在 `run_init_command(...)` 已取得 lock、复核 locked snapshot、确定 target mode 后且调用 `_select_model(...)` / 输出首个 model-choice prompt 前，只调用一次 `ConfigLoader(package_config_dir=_PACKAGE_CONFIG_ROOT)` 的 `load_execution_profiles(...)`"
   > "FIRST / OVERWRITE / confirmed RESET 传 `workspace_config_dir=None`；PRESERVE 传真实 workspace config dir，复用 loader 的 typed layering，文件缺失时取得 package layer，文件存在但非法时 fail closed 并提示 `--overwrite`，不得以 `None` 重试"

3. S2 fail-closed boundary 已明确：
   > "package-only source 或 PRESERVE 缺失 workspace profile 后的 package-layer failure，只说明 package execution profile 配置无效、异常类型以及 reinstall/repair package config 的动作；PRESERVE 已存在 workspace profile 的 typed load failure 只说明 workspace execution profile 无效并提示 `rerun with --overwrite`"
   > "exit 1；不进入 model/secret prompt、transaction prepare、environment persistence 或 managed-root publication"
   > "workspace profile 缺失只走 ConfigLoader 的 package layering；workspace profile 存在但非法时不得重试 package-only load"
   > "不使用 262144 fallback，不自动选其它 profile"

4. S2 tests 已明确：
   > "按 FIRST / OVERWRITE / confirmed RESET 参数化证明 package typed default profile minimum 被精确下传；即使 destructive mode 面对已有 workspace profile，source 仍是 `workspace_config_dir=None`"
   > "PRESERVE 的 workspace layered typed default profile minimum 被精确下传；workspace minimum 高于 package minimum 时，Custom/Ollama 较低 context 在原 context 步骤重试，随后输入满足 workspace minimum 的值可继续"
   > "PRESERVE 缺失 `execution_profiles.json` 时通过 loader package layer 取得 package minimum，不把缺失误报为损坏"
   > "PRESERVE 存在但 malformed/schema-invalid/default-id-invalid workspace profile 时通过唯一 typed API fail closed：exit 1、提示 `--overwrite`、诊断脱敏、package-only fallback 未发生、model/secret prompt 未发生、config / `.dayu` digest 不变"

5. §17 completion 已确认：
   > "R03 固定 target effective typed profile：FIRST / OVERWRITE / RESET 使用 package，PRESERVE 使用 workspace layered load；缺失取得 package layer，非法 workspace profile fail closed 并提示 `--overwrite`，更高 workspace minimum 使低 context 在原步骤重试"

**代码事实验证：**
- `ConfigLoader.load_execution_profiles()` API 已存在（`config_loader.py` 第770行），接受 `workspace_config_dir: Path | None = None` 参数
- plan 中描述的调用方式与现有 API 签名一致

**结论：** R03 在 plan 中完整落实，implementation agent 有明确执行路径。

---

## Material findings

经过完整审查，包括：

1. **架构边界审查**：plan 明确划分了 semantic owner（§5），`dayu.runtime` 不 import 业务层，与 AGENTS.md 一致。
2. **过度耦合审查**：plan 只新增一个四字段 value object 和一个窄职责 smoke，复用现有组件，不新增配置字段或第二套 loader。
3. **状态机完整性审查**：§6.4 完整定义了 init interaction state machine，覆盖所有 `input()` / `getpass` EOF 和 `KeyboardInterrupt` 场景。
4. **测试覆盖审查**：S1-S6 都有详细的 tests 要求，覆盖正常路径和各种失败场景。
5. **rollback safety 审查**：plan 复用现有 workspace transaction 的 backup/rollback 机制，S4 明确要求 "each backup / publish / fsync fault rollback 原 digest"。
6. **data loss prevention 审查**：PRESERVE 不覆盖已有文件，OVERWRITE / RESET 有 backup 和 rollback，EOF / SIGINT / persistence refusal 都不写半成品 config。
7. **credential security 审查**：errors 只输出 field rule / env name，不输出 secret、authorization、endpoint query；evidence 报告只保存 redacted endpoint identity。
8. **idempotency 审查**：FIRST 只在 config 不存在时执行，PRESERVE 已有文件零改写，OVERWRITE / RESET 是 whole-tree replace。
9. **platform compatibility 审查**：plan 覆盖 POSIX 和 Windows，有 no-follow / fd-safe capability gate。
10. **version skew 审查**：plan 不修改 ConfigLoader schema，不做 migration，只修改 package defaults 和添加新 API。
11. **observability 审查**：plan 要求每个 matrix row 记录完整的 evidence 和脱敏诊断。
12. **test gaps 审查**：plan 的 tests 覆盖了 happy path 和各种 failure paths，包括 identity drift、quarantine fault、rollback fault 等。

**未发现 material findings。**

---

## Open questions

None。用户已冻结：
- compactor 的 family identity 四字段
- 允许的 runner-option/extension 差异
- package fallback 同源
- 主 Run override isolation
- provider external failure 与 internal bug 的分类边界

---

## Residual risks

1. **外部 provider 当次不可用**：`assigned to environment/provider owner`，只要 row 的 unavailable contract 完整，不阻止产品实现通过。
2. **Windows junction/reparse 与 setx 的真实 node**：`covered by approved cross-platform validation`；本地非 Windows skip 不能关闭该风险。
3. **Custom endpoint/model 未提供**：`requiring explicit smoke invocation input`，不是产品 fallback。
4. **argparse command 前/后 global option 行为容易回归**：S1 用两种位置的正反测试封闭。
5. **package execution profile baseline 改为 Mimo 可能暴露依赖旧 DeepSeek baseline 的偶然测试**：S3 已冻结 pre-change `rg` inventory 与三类处置。
6. **Service 若用 ordinary effective selection 做 family anchor，会错误拒绝跨-family `--model`**：必须使用无 run override的 primary default selection。
7. **endpoint 可能含敏感 query**：错误和 evidence 只输出 redacted identity。
8. **ordinary-root file repair 扩大 cleanup shape**：identity drift、quarantine fault、rollback fault 必须分别测试。
9. **真实 provider 非确定性、限流与 preview model 下线可能使可用性变化**：每次 run 重新分类，不把 plan-time snapshot 当 verdict。

所有 residual risks 均已由 plan 中对应的 tests 和 fail-closed boundary 覆盖。

---

## Plan review conclusion

**`pass`**

理由：
1. R01-R03 三个 Controller rereview 要求均已在当前 plan 中完整落实
2. 未发现其它 material findings
3. plan 是 code-generation-ready 的，implementation agent 有明确执行路径
4. 语义 owner 清晰，public contract 完整，state machine 明确
5. 测试覆盖详细，包括正常路径和各种失败场景
6. 与 AGENTS.md 的架构硬约束、编码硬约束、语义所有权约束一致

---

## Output file

`docs/reviews/plan-review-20260730-143852.md`
