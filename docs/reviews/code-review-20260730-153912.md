# Code Review

## Scope

- Mode: current changes
- Branch: ci/pr-179-first-ci-readiness
- Base: 53f6b7f6
- Output file: docs/reviews/code-review-20260730-153912.md
- Included scope: WU-CLI-INIT-01 S2 fix 后独立 re-review。6 modified files — `dayu/cli/commands/init.py`, `dayu/cli/init_catalog.py`, `dayu/runtime/assembly.py`, `tests/cli/test_init_catalog.py`, `tests/cli/test_init_command.py`, `tests/runtime/test_assembly_helpers.py`
- Excluded scope:
  - S1 changes（已 merge 到 base）
  - S3–S6（尚未实现）
  - `docs/reviews/wu-cli-init-01-s2-implementation-codex.md`（implementation artifact）
  - `docs/reviews/code-review-20260730-152426.md`（AgentDS review，按指令不读取）
- Parallel review coverage: 无

## Review method

完整读取了：

- 根 `AGENTS.md` / `CLAUDE.md` 项目约束
- AgentMiMo 初审 `code-review-20260730-152220.md`
- Controller adjudication `wu-cli-init-01-s2-code-review-adjudication-controller.md`
- Codex fix artifact `wu-cli-init-01-s2-fix-codex.md`
- Codex implementation artifact `wu-cli-init-01-s2-implementation-codex.md`
- 当前完整 diff（~1700 行 workspace changes vs 53f6b7f6）
- 6 个变更文件的完整源码
- `ConfigLoader.load_execution_profiles` 与 `_load_layered_config_file` / `_overlay_roots` 的 layering 实现

沿以下 7 个 adversarial dimension 逐一走读真实 call path：

1. no-follow stat 对 symlink / dangling / directory / FIFO 的静态分类
2. FileNotFoundError 边界与其它 OSError 传播
3. loader 未调用验证（非普通 shape 在 ConfigLoader 前 fail closed）
4. 外部 symlink target 零读取、零修改
5. 错误脱敏（path / value / secret 不进入诊断）
6. fix 是否引入新 finding
7. S2-MIMO-001 / S2-CTRL-001 / S2-MIMO-002 修复完整性

## Findings

未发现实质性问题。

## Finding status

### S2-MIMO-001 — `_confirm_reset` stale raises docstring

- **status**: `已修复`
- **evidence**: `init.py:351-357` 的 docstring 现在正确标注 `:raises CliInitOperationError: 确认输入 EOF 时抛出`、`:raises KeyboardInterrupt: 用户中断时透传`、`:raises OSError: 输入或诊断 I/O 失败时透传`。与 `_confirm`（`init.py:925-948`）的真实 EOF → `CliInitOperationError`、非法输入 → retry、`KeyboardInterrupt` / `OSError` 透传 contract 完全一致。无运行时改动。
- **test coverage**: `test_reset_invalid_confirmation_retries_same_prompt_then_cancels`（`test_init_command.py:1450-1469`）覆盖非法输入原步骤 retry；`test_reset_eof_and_interrupt_map_to_failure_and_interrupt_without_mutation`（`test_init_command.py:1427-1449`）覆盖 EOF → EXIT_FAILURE。

### S2-CTRL-001 — PRESERVE workspace profile no-follow 普通文件边界

- **status**: `已修复`
- **evidence**:
  - 新增 `_workspace_execution_profile_is_regular_file`（`init.py:445-466`）使用 `os.stat(path, follow_symlinks=False)`，只把 `FileNotFoundError` 归为 absent（返回 `False`），只接受 `stat.S_ISREG(...)` 为 ordinary regular（返回 `True`），其它 shape 抛 value-free `CliInitOperationError` 并提示 `--overwrite`。
  - `_load_target_min_context_window`（`init.py:396-442`）在构造 `ConfigLoader` 前调用上述 helper；非普通 shape 在 loader 前 fail closed。
  - `run_init_command`（`init.py:164-167`）在 `_select_model` / `_collect_environment_persistence_plan` / `prepare_workspace_transaction` / `publish_workspace_transaction` 前调用 `_load_target_min_context_window`。
  - 错误诊断不含路径、目标内容、原始配置值或底层异常文本（`init.py:462-465`）。

- **攻击验证**:

  | 攻击面 | 结果 | 证据 |
  |--------|------|------|
  | symlink → 外部 regular file | ✅ 拒绝 | `os.stat(follow_symlinks=False)` 返回 `S_ISLNK` → `S_ISREG` 为 False → `CliInitOperationError` |
  | dangling symlink | ✅ 拒绝 | `os.stat(follow_symlinks=False)` 返回 `S_ISLNK` → `S_ISREG` 为 False → `CliInitOperationError` |
  | directory | ✅ 拒绝 | `os.stat(follow_symlinks=False)` 返回 `S_ISDIR` → `S_ISREG` 为 False → `CliInitOperationError` |
  | FIFO (special file) | ✅ 拒绝 | `os.stat(follow_symlinks=False)` 返回 `S_ISFIFO` → `S_ISREG` 为 False → `CliInitOperationError` |
  | FileNotFoundError (absent) | ✅ 允许 package layer | `except FileNotFoundError: return False` → loader 走 package layering |
  | 其它 OSError (permission etc.) | ✅ 原样透传 | docstring 承诺 `:raises OSError`，不在 `except` 中捕获 |

- **零副作用验证**（`test_preserve_non_regular_workspace_profile_stops_before_all_consumers`，`test_init_command.py:1290-1431`）:
  - `ConfigLoader` mock 未构造：`loader_factory.assert_not_called()`
  - model prompt 未发生：`select_model.assert_not_called()`
  - secret input 未发生：`secret_input.assert_not_called()` / `collect_secrets.assert_not_called()`
  - transaction prepare 未发生：`prepare_transaction.assert_not_called()`
  - publication 未发生：`publish_transaction.assert_not_called()`
  - 外部 symlink target 字节未修改：`external_target.read_bytes() == external_before`
  - 外部 symlink target secret 未进入诊断：`external_secret not in captured.out` / `captured.err`
  - 既存 `models.json` 逐字节保持：`models_path.read_bytes() == models_before`
  - durable sentinel 保持：`dayu_sentinel.read_text(...) == "durable-state"`
  - 零 transaction 残留：`not tuple(workspace_root.glob(".dayu-init-transaction-*"))`

### S2-MIMO-002 — profile 存在性检查与加载之间 TOCTOU

- **status**: `部分修复 — residual 合理`
- **evidence**: 静态 symlink / special 误分类已消除。`_workspace_execution_profile_is_regular_file` 使用 no-follow stat 确保 symlink / dangling / directory / FIFO 在 loader 前被拒绝。
- **residual**: stat 成功后、ConfigLoader 真正读取前，外部进程仍可替换 `execution_profiles.json`。最坏场景：文件在 stat 后消失 → loader 的 `workspace_path.exists()` 返回 False → 静默回退到 package layer 而非报 workspace 错误。`workspace_profile_exists=True` 但无 `ConfigLoadError` → 无法触发 workspace 错误分支。
- **classification**: `covered by later approved slice`（WU-CLI-INIT-01 S4 no-follow transaction/path-safety）
- **合理性评估**: 当前 S2 的 fix 消除了静态 shape 误分类这一高概率、用户可触发的失败模式。残余 TOCTOU 需要 fd-based no-follow read contract（超出 S2 approved files），且在 init lock + 单用户场景下触发概率极低。按 controller 裁决归 S4 是正确的风险分层。

### S2-MIMO-003 — Ollama template default 低于 minimum

- **status**: `rejected-with-reason`（controller 裁决，未修改）
- **evidence**: accepted plan 明确要求保留 Ollama template default 并让低于 target minimum 的默认或显式输入在 context 原步骤 retry。`_read_context_window`（`init.py:619-656`）的 `value < minimum` 检查精确满足。`test_preserve_workspace_minimum_retries_low_dynamic_context`（`test_init_command.py:1201-1261`）覆盖 Ollama 和 Custom 两种 provider 的低值重试行为。

### S2-DS residual — execution profile 文件名重复

- **status**: `deferred-with-owner`（controller 裁决，未修改）
- **evidence**: `init.py:74` 的 `_EXECUTION_PROFILES_FILE_NAME = "execution_profiles.json"` 与 `ConfigLoader._EXECUTION_PROFILES_FILE` 是同一字面量。当前只用于 upstream shape classification。S4 会统一 managed-file manifest。

### S2-DS residual — 非数值 port

- **status**: `证据失效`（controller 裁决）
- **evidence**: Python 3.11 的 `urlsplit(...).port` 对非数值 port 抛 `ValueError`；`validate_dynamic_endpoint`（`init_catalog.py:438-442`）已捕获并转为 `InitCatalogError`。

## Fix 是否引入新 finding

逐项检查 fix 新增代码：

1. **`_workspace_execution_profile_is_regular_file`**（`init.py:445-466`）: 纯函数，只依赖 `os.stat` / `stat.S_ISREG` / `FileNotFoundError`。无新增依赖、无副作用、无隐藏状态。`stat` 模块已在 `init.py:15` 导入。

2. **`_load_target_min_context_window`**（`init.py:396-442`）: 调用顺序正确 — no-follow stat → ConfigLoader → profile lookup。`workspace_profile_exists` flag 用于区分 error source，语义清晰。

3. **`_read_model_choice` / `_read_dynamic_model_name` / `_read_dynamic_endpoint` / `_read_context_window` / `_read_environment_persistence_entry`**: 每个 retry loop 只捕获对应 owner exception，EOF / `KeyboardInterrupt` / `OSError` 不被捕获。`_report_recoverable_input_error` 只输出 `exc` 的 str（value-free）。

4. **`_confirm` while-True loop**（`init.py:935-948`）: EOF → `CliInitOperationError`；非法输入 → retry；`KeyboardInterrupt` / `OSError` 透传。`_NEGATIVE_ANSWERS` 包含 `""`，Enter 正确返回 `False`。

5. **`ModelFamilyIdentity` 与 `model_family_identity`**（`assembly.py:90-104, 275-288`）: frozen dataclass，纯投影函数，无副作用。`__all__` 已更新。

6. **`validate_dynamic_model_name` / `validate_dynamic_endpoint`**（`init_catalog.py:399-452`）: 从 `_validate_dynamic_model_inputs` / `_validate_endpoint` 拆分为独立 public validators，被 `init.py`（交互 retry loop）和 `OllamaModelSettings.__post_init__` / `CustomOpenAIModelSettings.__post_init__` 复用。单一真源，无重复实现。

7. **`_validate_resolved_choice` 新增 family identity 检查**（`init_catalog.py:625-642`）: 四字段比较，mismatched_fields 只收集字段名，错误消息只报告 model ids + field names，不回显字段值。测试 `test_static_pair_family_mismatch_reports_only_ids_and_field_names` 以 sentinel 值验证脱敏。

**结论：fix 未引入新 finding。**

## Open Questions

无。

## Residual Risk

1. **stat → loader read TOCTOU**（已知，归 S4）: 外部进程在 no-follow stat 成功后、ConfigLoader 读取前替换 profile 文件。完全消除需要 fd-based no-follow read contract，超出 S2 approved files。

2. **`_EXECUTION_PROFILES_FILE_NAME` 重复**（已知，归 S4）: `init.py:74` 与 `ConfigLoader._EXECUTION_PROFILES_FILE` 同一字面量两个副本。S4 统一。

3. **S3–S6 未实现**: package defaults / Service runtime family comparison / workspace transaction repair / README 同步不在本 slice scope。

## Validation

- focused tests：`120 passed`
- 单文件 coverage：`init.py: 95%`, `init_catalog.py: 90%`, `assembly.py: 92%`，合计 `92%`
- pyright：`0 errors, 0 warnings, 0 informations`
- ruff：`All checks passed!`
