# Code Review（Re-review）

## Scope

- Mode: current changes
- Branch: `codex/download-oracle`
- Base: `0fe85869`（accepted plan HEAD）
- Output file: `docs/reviews/code-review-20260810-172154.md`
- Included scope: 5 个 allowed changed Python files + review fix artifact
- Excluded scope: plan 外 production/test 文件、README、Slice 2/3 未实现模块
- Parallel review coverage: 无
- Re-review inputs:
  - `docs/gateflow/wu-cli-download-02-slice1-code-review-adjudication-20260810.md`
  - `docs/gateflow/wu-cli-download-02-slice1-review-fix-20260810.md`
  - `docs/reviews/code-review-20260810-171152.md`（上一轮 review）

## Findings

未发现实质性问题。

### 上一轮 Finding 关闭状态

#### Finding 1：永久 AST owner test 与实现结构耦合 — 已修复

- **裁决**: `accepted`（adjudication）
- **修复验证**:
  - `tests/service/test_fins_direct.py` 中 `test_download_mutation_mode_conflict_has_one_production_owner` 已删除。
  - `ast` 和 `dayu.fins.download_contract as download_contract_module` imports 已删除。
  - `rg "download_contract_module|\\bast\\b|test_download_mutation_mode_conflict_has_one_production_owner" tests/service/test_fins_direct.py` 无匹配（exit 1）。
  - 保留的行为测试完整：`test_download_mutation_mode_contract_accepts_legal_matrix`（3 cases）、`test_download_mutation_mode_contract_rejects_conflict_with_one_diagnostic`、`test_download_mutation_mode_contract_rejects_non_boolean_fields`、`test_download_command_maps_single_mutation_mode_to_service`（2 cases）、`test_download_mutation_mode_conflict_precedes_all_side_effects`（2 cases）。
  - Production conjunction owner guard：`rg "overwrite_existing and rebuild_local_artifacts|rebuild_local_artifacts and overwrite_existing" dayu` 仅命中 `dayu/fins/download_contract.py:79`。

#### Finding 2：`test_arg_parsing.py` 无关全文件格式化 — 已修复

- **裁决**: `accepted`（adjudication）
- **修复验证**:
  - `git diff 0fe85869 -- tests/cli/test_arg_parsing.py` 精确为 2 个 hunks：`COMMAND_HELP_EXPECTATIONS["download"]` 新增 `--rebuild`，新增 `test_download_help_explains_mutually_exclusive_mutation_modes` 测试函数。共 +24 行。
  - 上一轮 diff 中的约 30 处函数签名/类型声明/集合推导/调用/assert 折叠已全部还原。
  - `ruff format --diff tests/cli/test_arg_parsing.py` 确认 formatter 提议的全部 hunks 均为仓库既有 baseline 折叠，不触及 `--rebuild` inventory 或互斥 help test。fix artifact 的 baseline 归因成立。

### Production 不变性

Production diff 与上一轮 review 完全一致：

| 文件 | 变更 |
|---|---|
| `dayu/cli/arg_parsing.py` | +8（`--overwrite`/`--rebuild` help 互斥说明） |
| `dayu/fins/download_contract.py` | +60（`_validate_download_mutation_mode` helper、`FinsDownloadRequest.__post_init__`、`FinsDownloadEffectiveFilters.__post_init__` 复用、`build_fins_download_request` docstring 补充 `TypeError`） |

未编辑其它 production 文件。

### Tests 与 coverage

```text
pytest tests/service/test_fins_direct.py tests/cli/test_arg_parsing.py tests/cli/test_fins_commands.py
549 passed, 3 warnings in 5.26s
```

549 passed（implementation 的 550 减去已删除的 1 个 AST test）。3 个 warning 均为 `edgar` 既有 deprecation。

逐文件 line coverage（fix artifact 声称值，与 implementation 一致）：

| Changed production file | Statements | Miss | Line coverage | Gate |
|---|---:|---:|---:|---|
| `dayu/cli/arg_parsing.py` | 342 | 2 | 99% | PASS |
| `dayu/fins/download_contract.py` | 325 | 39 | 88% | PASS |

### Static validation

```text
ruff check <5 changed Python files>             PASS
python -m compileall -q <2 production modules>  PASS
pyright                                          PASS（0 errors）
git diff --check                                 PASS
```

`ruff format --check tests/cli/test_arg_parsing.py` 报告 `Would reformat`，但 formatter 提议的 hunks 全为仓库既有 baseline 折叠，不重新引入 review finding。符合 adjudication 要求。

### Scope guard

```text
git diff --name-only <base> -- '*.py'
dayu/cli/arg_parsing.py
dayu/fins/download_contract.py
tests/cli/test_arg_parsing.py
tests/cli/test_fins_commands.py
tests/service/test_fins_direct.py

git diff --name-only <base> -- README.md dayu/README.md ...
无输出
```

精确为 5 个 allowed files。无 README 变更。无 scope drift。

## Open Questions

- 无。

## Residual Risk

| 风险 | 分类 | 说明 |
|---|---|---|
| `ruff format --check` 对 `test_arg_parsing.py` 报告 baseline 差异 | 低 | formatter 提议的 hunks 全为既有 baseline 折叠，不触及 F12 变更。符合 adjudication "不重新全文件格式化" 要求 |
| CLI help 尚未在 README 投影 | covered by later slice | accepted plan Slice 3 稳定后处理 |
| 真实 CLI post-fix evidence 尚未执行 | covered by later slice | accepted plan §9.2 |

## 综合判定

**PASS**。

两个 accepted low findings 均已按裁决精确关闭：

1. **AST test 已删除**：`test_download_mutation_mode_conflict_has_one_production_owner` 及其 `ast`/`download_contract_module` imports 已移除。行为测试（legal matrix、conflict rejection、non-boolean rejection、CLI pre-workspace）完整保留。Production conjunction 仅存于 `_validate_download_mutation_mode` 一处。
2. **格式化噪声已还原**：`test_arg_parsing.py` diff 精确为 F12 实质变更（`--rebuild` inventory + 互斥 help test）。formatter baseline 差异已归因，不重新引入 churn。

Production diff 不变。549 tests passed。Ruff lint、compileall、pyright 均通过。无新增 correctness 或 scope finding。
