# Code Review

## Scope

- Mode: current changes（AgentDS independent re-review，CR-DS-001 only）
- Branch: `feat/wu-ctx-04`
- Base: `1f032b5e2d1aba974304ee4537be76ed4a1174e6`（accepted plan commit）
- Output file: `docs/reviews/code-review-20260722-125901.md`
- Included scope:
  - `dayu/runtime/native_mutex.py`（Codex fix 修改后的当前状态）
  - `tests/runtime/test_native_mutex.py`（Codex fix 修改后的当前状态）
  - `dayu/runtime/__init__.py`（diff vs base）
  - `dayu/host/api.py`（diff vs base）
  - `tests/runtime/test_import_boundary.py`（diff vs base）
  - `dayu/host/session_attachment.py`（new，回归检查）
  - `tests/host/test_session_attachment_registry.py`（new，回归检查）
- Excluded scope:
  - `docs/host/issues-implementation-control.md`（controller 独占，不修改）
  - 首轮 review artifacts（CR-MIMO、CR-DS 原始报告）
  - AgentMiMo 本轮 re-review artifact
  - Slice 2/3 范围内文件
- Parallel review coverage: 无

## Findings

### CR-DS-001 — fixed — 低 — native prior failure + partial close failure 结构化异常链

- **入口/函数**: `try_acquire_strict_native_mutex` → `_lock_file_descriptor` → `_close_partial_file_descriptor`
- **文件(行号)**: `dayu/runtime/native_mutex.py:339-364`
- **输入场景**: non-busy native lock 操作抛出 `StrictNativeMutexUnavailableError`（如 POSIX `EIO`、backend 中途消失），随后 `os.close(fd)` 也失败
- **实际分支**: `_close_partial_file_descriptor` 行 354 `except Exception`，`prior_error is not None` 分支（行 359-364）
- **controller 修复要求**:
  1. 保持外层异常类型仍为 `StrictNativeMutexUnavailableError` ✓
  2. 当 prior native error 与 partial close error 同时存在时，异常 cause 必须结构化保留两个原始异常及 traceback ✓
  3. 新增精确单测同时触发 native lock failure 与 partial FD close failure ✓
  4. 普通 busy + close failure 仍 fail-closed ✓
- **逐项验证**:

  **1. 外层异常类型**（行 355, 364）：
  `cleanup_error` 始终构造为 `StrictNativeMutexUnavailableError("关闭 partial strict-native mutex file descriptor 失败")`。
  双失败路径（行 359-364）与单 close 失败路径（行 357）使用同一类型。✓

  **2. 结构化 cause 保留两个原始异常**（行 360-364）：
  ```python
  combined_error = ExceptionGroup(
      "strict-native mutex native 操作与 partial descriptor cleanup 均失败",
      [prior_error, exc],
  )
  raise cleanup_error from combined_error
  ```
  - `prior_error` 是 native lock 操作的 `StrictNativeMutexUnavailableError`，其自有 `__cause__`（原始 `OSError`）完整保留
  - `exc` 是 `os.close` 的原始 `OSError`，identity 完整保留
  - `ExceptionGroup` 是 Python 3.11 内建类型，无需额外 import，不扩大 public surface
  - 外层 `cleanup_error.__cause__` → `ExceptionGroup` → `[prior_error, exc]`，两层可达 ✓

  **3. 新增精确单测**（`test_native_lock_and_partial_close_failures_preserve_structured_chain`，行 421-451）：
  - 注入 `_UnexpectedPosixLocking`（non-busy `EIO`）与 `_CloseAfterFailure`（真实 close + 抛 `EIO`）
  - `assert isinstance(raised.value, StrictNativeMutexUnavailableError)` — 外层类型 ✓
  - `assert isinstance(combined_error, ExceptionGroup)` + `len(...) == 2` — 结构化 cause ✓
  - `assert prior_error.__cause__ is locking.error` — prior wrapper 的 cause 是原始 lock `OSError` identity ✓
  - `assert close_error is closer.error` — close 成员是原始 close `OSError` identity ✓
  - 三个 traceback 均 `is not None` — traceback 可达 ✓
  - `assert len(closer.file_descriptors) == 1` — partial fd 只 close 一次 ✓

  **4. 普通 busy + close failure fail-closed**（`test_partial_fd_cleanup_failure_overrides_busy_outcome`，行 401-418）：
  既有测试未删除、未弱化。`prior_error=None` → 行 356-357 直接 `raise cleanup_error from exc`，不经过 `ExceptionGroup` 分支。
  `StrictNativeMutexUnavailableError` 传播替代 `return None`。✓

- **实现约束检查**:
  - `prior_error` 类型从 `BaseException | None` 收窄为 `Exception | None`（行 342），与全部调用点 `except Exception` 真源一致 ✓
  - 未新增 public type、schema、Host state、fallback、`hasattr`/`getattr`、兼容 shim ✓
  - 未新增 import；`ExceptionGroup` 为 Python 3.11 内建 ✓
  - `_close_partial_file_descriptor` 始终真实调用 `_close_file_descriptor`（行 352-353），descriptor 消费后不重试 ✓
- **直接证据**: 见上述行号引用
- **影响**: 无——修复正确完成
- **建议改法和验证点**: 无需进一步修改
- **修复风险**: 已消除
- **严重程度**: 低（原始 controller 裁决 severity）

### 回归检查结论

对 Slice 1 全部 included scope 做 adversarial failure pass，覆盖以下检查面：

- **异常契约**: `try_acquire_strict_native_mutex` 的 public contract 不变（返回 `StrictNativeMutexHandle | None`，raise `StrictNativeMutexUnavailableError`）；`StrictNativeMutexHandle.close()` contract 不变；Host value types 仅新增封闭 enum/dataclass，不改变既有 behavior。无新 public exception type、无异常语义漂移。✓
- **typing**: `prior_error: Exception | None` 与全部调用点类型一致；`ExceptionGroup` 无额外类型标注需求；`HostSessionMutationErrorDetail` 与 `HostSessionAttachmentConflictDetail` 的 `__post_init__` 类型守卫使用 `isinstance` 精确校验。pyright `0 errors, 0 warnings, 0 informations`。✓
- **资源清理**: `_close_partial_file_descriptor` 始终先执行真实 `os.close`，后构造异常；`_CloseAfterFailure` test fake 同步执行真实 close。`StrictNativeMutexHandle.close()` 先消费 `_file_descriptor` 再 unlock/close。所有 descriptor 路径不重试已消费 fd。✓
- **测试固化**: 新增测试使用 identity（`is`）断言验证原始异常对象保留，不依赖字符串匹配或间接推断；既有测试未为适配新实现而弱化。未发现测试为实现便利而固化偶然行为的证据。✓
- **首轮 rejected findings 未重开**: CR-MIMO-001、CR-MIMO-002、CR-MIMO-OQ-001、CR-DS-002、CR-DS-003 均已被 controller `rejected-with-reason`；当前代码与 rejected 时的代码在这些 finding 对应的代码路径上没有实质变化，无新证据触发重开。✓
- **其他 modified files**（`dayu/runtime/__init__.py`、`dayu/host/api.py`、`tests/runtime/test_import_boundary.py`）：均为 Slice 1 既有新增（docstring 更新、value type 定义、import boundary 测试），与 CR-DS-001 修复无交互。无新问题。✓

未发现新实质问题。

## Open Questions

无。

## Residual Risk

- **真实 Windows 环境验证**: 保留，owner=`Slice 3 final matrix / CI`。本次 fix 不改 Windows backend 分支，只改跨 backend 共用的 `_close_partial_file_descriptor`。
- **`dayu/host/api.py` 全文件 coverage**: 保留到 Slice 3，本 Slice 新增 value type 分支已执行。
- **`ExceptionGroup.__traceback__` 未断言**: 新增测试断言了 `prior_error.__traceback__`、`locking.error.__traceback__`、`close_error.__traceback__`，但未单独断言 `ExceptionGroup` 自身的 `__traceback__`。`ExceptionGroup` 的 traceback 在 `raise cleanup_error from combined_error` 时由 Python runtime 自动设置，其可达性低于两个成员异常的 traceback。不构成 correctness 或 diagnostic gap。
