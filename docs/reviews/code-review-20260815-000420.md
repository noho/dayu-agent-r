# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `5508d044`（`gateflow: accept plan for upload-filing-ticker-alias-contract`）
- Output file: `docs/reviews/code-review-20260815-000420.md`
- Included scope: 仅 `upload-filing-ticker-alias-contract` S1 workspace changes——16 个生产文件 + 1 个新增测试文件 + 18 个更新测试文件（`git diff 5508d044` 全部 35 个文件）；已完整读取 accepted plan、第二轮 plan re-review controller adjudication、S1 implementation artifact 与 `AGENTS.md`。
- Excluded scope: S2 的 concurrency/unique route/typed conflict/lock 图是 accepted residual，按用户指令不作为 S1 blocker；`docs/reviews/wu-upload-filing-ticker-alias-contract-goal-confirmation-controller.md` 与第一轮 plan review/fix 只作背景，不作为本次裁决依据。
- Parallel review coverage: 无；主 reviewer 逐文件走读全部 diff，并独立复跑 residue scan、pyright、focused tests、6-K 单文件 branch coverage 与两项回归失败 spot-check。

## Findings

### 1-未修复-中-sec_6k_primary_document_repair.py 单文件 branch coverage 76%，低于 80% 强制门槛，必须进入 S1 fix gate
- **入口/函数**: `reconcile_active_6k_primary_documents` 全文件（S1 变更点在 `_resolve_target_tickers` 的 inventory discovery 分支）
- **文件(行号)**: `dayu/fins/pipelines/sec_6k_primary_document_repair.py`（S1 变更行为 347）
- **输入场景**: 无特定输入——这是逐文件覆盖率合同违约，不是行为缺陷。
- **实际分支**: `target_tickers=None` 的 discovery 分支（含本次变更行 347）已被新测试覆盖；未覆盖的是既有异常/恢复/CLI 分支。
- **预期行为**: `AGENTS.md` 规定"单文件测试覆盖率目标为 >= 80%"，accepted plan §12.3 明确"若任一文件低于80%，补 owner行为测试；不得以 omit、pragma、降低阈值或无意义执行行绕过"。
- **实际行为**: 独立复跑 `coverage run --branch -m pytest tests/fins/test_sec_pipeline_download.py` 后 `coverage report --include=dayu/fins/pipelines/sec_6k_primary_document_repair.py` 得 76%（181 stmts / 60 branches）。Missing 与 implementation artifact §5.2 完全一致：`117, 122, 136, 182, 186, 193, 291, 301-303, 305->289, 334-341, 365, 368, 401-402, 420, 425, 428, 430, 489-495, 568, 636, 675`。变更行 347 不在 missing 中，即"本次要求的 public discovery path 已实际覆盖"为真。
- **直接证据**: 复跑 coverage 输出（本 review 实测 76%）+ `AGENTS.md` 测试与验证节 + plan §12.3。
- **影响**: 计划合同未达成；该文件是 S1 allowed-files 成员，不能以"缺口多为既有分支"绕过强制门槛。对当前 diff 的回归风险本身有限（变更行已覆盖），但 80% 门槛是逐文件合同。
- **建议改法和验证点**: S1 fix gate 内为该文件补齐 owner-level 测试，至少把未覆盖的异常/恢复分支（如 301-303、305->289、334-341、489-495）与 401-402/420/425/428/430 等分支触达；补测后复跑单文件 `coverage report --include=<file> --fail-under=80`。implementation artifact 已正确把 76% 记为 fail，此处只裁决"必须修复"，不是新发现。
- **修复风险（低）**: 补测试不触碰生产代码；若发现某分支需生产代码配合才能触达，停止并回 plan review。
- **严重程度（中）**:

### 2-未修复-中-S1 新增 identity mismatch `ValueError` 被 blanket catch 误标为 COMPANY_NAME_REQUIRED，plan 要求的专用 `UploadCompanyNameRequiredError` 未实现
- **入口/函数**: `_validate_fins_upload_filing_request` → `resolve_upload_company_meta_decision`
- **文件(行号)**: `dayu/fins/pipelines/upload_company_meta.py:68-73`（新 raise）；`dayu/fins/ingestion_runtime.py:1051-1060`（`except ValueError: _raise_upload_usage(FinsUploadUsageCode.COMPANY_NAME_REQUIRED)`）
- **输入场景**: filing upload 时 `published_state.company_meta` 存在且 strict-valid，但其 canonical/market/exchange 与本次 incoming canonical 不一致（published identity 与请求 target 不一致的 workspace 状态，如 descriptor/meta 手工编辑或混合状态）。
- **实际分支**: 走到决策函数新增的三元比较 `existing_identity.canonical_ticker != proposed_identity.canonical_ticker or market != or exchange !=` → raise `ValueError("既有 CompanyMeta 与本次 canonical ticker identity 不一致")` → 被 ingestion_runtime 的 blanket `except ValueError` 捕获。
- **预期行为**: plan §6.1 冻结的 owner 契约："缺失时抛专用`UploadCompanyNameRequiredError`。`ingestion_runtime.py` 只捕获 `UploadCompanyNameRequiredError` 并映射 `COMPANY_NAME_REQUIRED`。builder的`ValueError`不会被该分支捕获……任一其它domain/storage异常不能借偶然调用顺序伪装成company-name-required。" 即 identity 不一致应走 published-state corruption 类失败（S2 为 typed corruption），绝不能投影成"缺少 --company-name"。
- **实际行为**: 该新 raise 是 S1 新增的唯一可在静态校验后到达 blanket catch 的 ValueError（invalid alias 已在 `_validate_fins_upload_filing_static:886-892` 先行投影 `INVALID_TICKER_ALIAS`，不可达此处）。用户/LLM 看到 `FinsUploadUsageCode.COMPANY_NAME_REQUIRED` 文案"当前公司缺少有效元数据；create/update 必须提供 --company-name"——错误指引，把 published 状态不一致伪装成用户输入缺失。全仓 `grep UploadCompanyNameRequiredError` 零命中：plan 点名的专用异常在 S1 中完全不存在。
- **直接证据**: `upload_company_meta.py:73` 的 raise 语句；`ingestion_runtime.py:1059-1060` 的 blanket catch；`grep -rn UploadCompanyNameRequiredError dayu/ tests/` 返回空。触发面窄（fresh schema + strict read 下很难出现），但错误投影语义漂移是确定的：错误原因 owner（domain decision）与投影 owner（usage code）对同一事实给出互斥解释。
- **影响**: 错误 answer（LLM 被引导去补 company-name 而不是修 identity 不一致）/ 错误状态解释。不丢数据、不产生错误持久化。
- **建议改法和验证点**: 二选一，建议前者：① S1 fix gate 内把该 raise 改为专用 `UploadCompanyNameRequiredError` 之外的 typed 错误并收窄 catch（把 S2 item 6 的"收窄捕获"提前完成最小子集），或 ② 把 identity-mismatch raise 推迟到 S2 与 typed corruption 一起落地（S1 恢复旧行为：不比较 identity，仅按 freshness 决策）。补 owner 测试断言 mismatch 场景不投影 COMPANY_NAME_REQUIRED。
- **修复风险（中）**: ①会小幅越过 S1/S2 的"收窄捕获"划分；②需回退一行逻辑并保证 fresh/stale union 不变。两者均不触碰 storage/锁图。
- **严重程度（中）**:

### 3-未修复-低-read_runtime.py 一行 field 迁移越过 S1 allowed-files 字面枚举（裁决：接受为最小必要 consumer 迁移）
- **入口/函数**: `FinsReadRuntime._resolve_company_name_and_market`（company meta 投影辅助）
- **文件(行号)**: `dayu/fins/tools/read_runtime.py:2753`
- **输入场景**: read route 消费 CompanyMeta 时。
- **实际分支**: `return company_meta.company_name, company_meta.ticker_identity.market`。
- **预期行为**: S1 allowed-files 明确排除 `read_runtime.py`（plan §10 S1："§9.1 中除……`dayu/fins/tools/read_runtime.py`以外的……文件"；S2 item 5 才切换 read route）。
- **实际行为**: 该一行把 `company_meta.market` 机械迁移为 `company_meta.ticker_identity.market`。plan §5.2 禁止保留 `market` compatibility property，而 S1 删除了 `CompanyMeta.market` 字段——不迁此行则 pyright 报错且运行期 AttributeError，即该迁移是字段删除的强制下游收敛，不是提前实现 S2 read 语义（route、normalize/upper fallback、schema、错误投影均未改）。
- **直接证据**: diff 仅一行；`rg -n 'company_meta\.(ticker|market|ticker_aliases)' dayu tests` 零命中证明没有第二处遗漏或扩散。
- **影响**: 若拒绝，S1 无法通过 pyright 与运行期回归；若接受，read_runtime 的 S2 归属声明需在 closeout 中显式记录。接受后无行为风险。
- **建议改法和验证点**: 裁决为 accepted-with-note：S1 fix gate 不修改此行，S2 把 read route 完整切换。任何后续 read_runtime 变更仍必须由 S2 门控。
- **修复风险（低）**:
- **严重程度（低）**:

### 4-未修复-低-upload_material CLI help 未按 plan §8.1 明确"aliases 由 material CompanyMeta producer 可靠持久化"
- **入口/函数**: `_add_upload_ticker_argument`
- **文件(行号)**: `dayu/cli/arg_parsing.py:1080-1101`
- **输入场景**: 用户执行 `dayu-cli fins upload_material --help`。
- **实际分支**: 三个 upload 命令共用同一 help 文案"……成功保存公司元数据后均查询同一归档"，没有 material 专属持久化声明。
- **预期行为**: plan §8.1："其中`upload_material`明确其aliases由material CompanyMeta producer可靠持久化"；§11.5："upload tool/CLI material文案明确aliases与filing一样由成功CompanyMeta commit持久化……不得描述成canonical-only或后续filing才生效"。
- **实际行为**: 共享文案对 material 是否也覆盖到"可靠持久化"这一层，但未点明 material producer；`tests/cli/test_arg_parsing.py` 对 upload_material 只断言通用句"成功保存公司元数据后均查询同一归档"。行为（aliases 实际持久化）已由 material owner tests 证明正确，这是 LLM-facing/用户可见文本与 plan 字面要求的偏差。
- **直接证据**: arg_parsing.py diff 的 help 字符串；test_arg_parsing.py 的 COMMAND_HELP_EXPECTATIONS 断言项。
- **影响**: 帮助文本对 material 场景的说服力略弱于 plan 要求；不改变运行时行为。
- **建议改法和验证点**: S1 fix gate 中给 `_register_upload_material_command` 追加或替换一句 material 专属说明（如"material 上传的别名同样由公司元数据可靠持久化，成功后与 filing 查询同一归档"），并同步该命令的 help 断言。
- **修复风险（低）**:
- **严重程度（低）**:

## Open Questions

- `CompanyMeta.from_dict` 校验 persisted `ticker` 与 builder canonical 一致，但未校验"persisted `ticker` 字符串 == builder 输出 canonical"（如 meta.json 持久化 `ticker="AAPL.US"` + `market="US"` 会静默 canonical 化为 `"AAPL"` 通过，round-trip 不稳定）。plan §5.2 fail-closed 清单未包含此项，且 fresh schema 下唯一写入方 `to_dict` 只写 canonical，生产路径不可达；但 S2 的 `identity_mismatch` 检测发生在 from_dict canonical 化之后，观测不到 raw 不一致。建议 S2 实现时明确：descriptor 双向校验是否需要 raw ticker 字段级比较。
- material 路径没有静态 alias validator：invalid material alias 由 `build_company_ticker_identity` 抛 `ValueError`，沿用旧 `_normalize_ticker_aliases` 相同的泛化失败投影（S1 无回归，但也无 `INVALID_TICKER_ALIAS` 静态投影）。plan §6.1 的"invalid alias仍在static validation唯一投影"目前只覆盖 filing；material 侧是否由 S2 item 6 的 failure projection 一并收口，建议在 S2 plan 落地时显式确认。
- `test_upload_validator_accepts_v_dot_ba_alias_from_identity_grammar` 使用 `action="delete"` 且 `company_meta=None` 验证 V.BA 静态通过——刻意避开 decision 路径，测试名与 intent 一致，但 filing decision 路径对 `V.BA` alias 的 keep/stage 行为未在 filing stream 测试中直接覆盖（material stream 已覆盖）。

## Residual Risk

- 新 grammar 的多字符点分节与市场后缀 token 邻接的负向测试缺失：`AA.SS`→`AA-SS`（fallback 为 US 分节）、`V.N`/`V.O`→`V`（单字母交易所后缀优先）与 `V.BA`→`V-BA` 的边界对照没有 owner test；`SHEL`/`SHOP` 前缀回退（docstring 明示行为）在 tests 中零引用（grep 证实）。`_split_market_token` 逻辑本身未改，属既有行为，但 grammar 放宽后这些歧义点首次成为可争议边界。
- S1 临时 snapshot 窗口、`resolve_existing_ticker` 半契约、late multi-owner `ValueError` 为 accepted residual，由 S2 收口（用户明确不作 S1 blocker）。
- 6 项回归失败经 spot-check 2 项（`test_service_does_not_import_forbidden_layers`、`test_upload_filings_from_usage_empty_and_write_failures`）确认失败内容与 implementation artifact 描述一致且相关文件不在本 diff；其余 4 项（test_init_workspace 3 项参数化 + 1 项）按同类证据接受为既有问题，未逐项复跑。
- 独立复跑验证：residue scan（duplicate grammar helpers / legacy field access / 旧 FmpCompanyInfo 构造 / `upsert_company_meta_for_upload`）全部零命中，S1 临时 route 命中与 plan 允许清单一致；`python -m pyright dayu/ tests/ utils/` 0 errors；`git diff --check` 通过；focused 核心 6 文件 193 passed。
