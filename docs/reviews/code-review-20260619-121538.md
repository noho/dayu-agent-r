# Code Review — Rereview

## Gate

- Gate: code review / rereview (adjudication + fix validation)
- Work unit: Conversation Memory smoke/log diagnostics and smoke coverage boundary
- Prior review: `docs/reviews/code-review-20260619-121033.md`
- Adjudication: `docs/reviews/wu-cm-conversation-memory-smoke-log-diagnostics-code-review-adjudication-20260619-121419.md`
- Output file: `docs/reviews/code-review-20260619-121538.md`

## Finding Status

### Mimo-1: unused `TextIOWrapper` import — **已修复** ✅

`from io import TextIOWrapper` 已从 `utils/smoke_host_public_conversation_memory_scenarios.py` 中移除。`grep -c` 返回 0。

### DS-1: `_print_round()` / `_print_session_observation()` / assembly diagnostics 缺少 `flush=True` — **已修复** ✅

以下函数的全部 `print` 调用均已添加 `flush=True`：
- `_print_assembly_diagnostics()` — 全部 15 个 print
- `_print_compact_pressure_plan()` — 全部 3 个 print（含早退路径）
- `_print_round()` — 全部 2 个 print
- `_print_session_observation()` — 1 个 print
- `_print_compact_audit_summary()` — 1 个 print（行尾 flush=True）
- `_assert_compact_acceptance()` — 1 个 print（行尾 flush=True）
- `run_smoke()` 内 `SMOKE PASS`、`SMOKE WORKSPACE_KEPT` — 均已 flush

### DS-2: `_compact_payload_int()` docstring 未说明 JSON 浮点数 — **已修复** ✅

行 3893 docstring 已追加 "JSON 浮点数" 说明：`字段缺失、JSON 浮点数或其它类型不匹配时返回 None`。

### Mimo-2: `_compact_pressure_reserve_tokens` 分支返回值相同 — **rejected-with-reason** ✅

拒绝理由成立：这是 pre-existing 代码，不属于本 work unit。修改 compact pressure sizing 行为超出 smoke/log diagnostics 范围。

### Mimo-3: `CompactOperationAudit` docstring missing fields — **rejected-with-reason** ✅

拒绝理由成立：当前代码（行 700-715）已完整文档化 `request_event_sequence`、`run_id`、`compacted_event_sequences`、`failed_events` 等字段。re-review 确认 docstring 与字段列表一致。

### DS-3: per-operation failure/diagnostic histograms 不打印 — **deferred-with-owner** ✅

延期合理：per-operation histogram 保留在 `CompactOperationAudit` dataclass 中（`failure_categories`、`diagnostic_histogram` 字段），用于聚合全局 histogram。当前 work unit 的验收要求已由全局 histogram 满足。如需 per-operation 细分，可后续增强。

### DS-4: `_compact_artifact_files()` 递归 `rglob("*")` — **rejected-with-reason** ✅

拒绝理由成立：pre-existing helper 行为，非本 work unit 引入。修改 artifact discovery 语义可能影响现有 smoke acceptance。

### DS-5: `main()` 不打印 traceback — **rejected-with-reason** ✅

拒绝理由成立：traceback policy 是更广泛的 CLI/logging 行为决策，超出当前 work unit 的 failure line separation 和 hard-fail visibility 目标。

## 无新增 material issue

对 fix diff 的逐行审查确认：

1. 移除 `TextIOWrapper` import 干净、无残留引用。
2. 所有 `flush=True` 添加均为机械修改，无逻辑变更，无副作用。
3. docstring 补充为纯文档修改，不影响行为。
4. `_print_compact_audit_report` / `_print_compact_operation` / `_print_compact_histogram` / `_print_compact_reject_detail` 作为新函数加入，内部逻辑与已有的 `_compact_audit_report_from_rows` -> dataclass 数据流一致。`CompactFailedOperationAudit.fallback_policy_decision` 字段在 payload 读取（`_compact_failed_operation_audit` 行 3600）、dataclass 定义（行 687）和打印（行 3139-3140）三方一致。`_COMPACT_MANIFEST_PRESENT`/`_COMPACT_MANIFEST_MISSING` 常量在定义（行 215-216）、`_compact_manifest_presence()` 返回（行 3958-3959）、`_print_compact_reject_detail()` 使用（行 3193）和 `_compact_rejected_manifest_presence_histogram()` 聚合（行 3694）四处一致。

## Validation

```
pytest tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py: 16 passed
pyright utils/smoke_host_public_conversation_memory_scenarios.py \
       tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py: 0 errors, 0 warnings
```

## Residual Risk

无变化。此前记录的 residual risk（production memory compact failure 未修、per-operation histogram 不独立打印、`_print_round` 无 flush 的故障丢失）中，flush 风险已消除；其余两项仍属于后续 work unit 或 deferred enhancement。
