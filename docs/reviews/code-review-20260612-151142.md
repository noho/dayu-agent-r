# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: `main` (via committed S1/S2 slices; current dirty diff is S3)
- Output file: `docs/reviews/code-review-20260612-151142.md`
- Review date: 2026-06-12T15:11:42+08:00
- Included scope:
  - `tests/host/test_llm_compaction.py` (dirty diff: cast 移除、TypeGuard 引入、helper 重构)
  - `docs/host/issues-implementation-control.md` (dirty diff: control doc 状态更新)
- Excluded scope:
  - `tests/host/fake_compaction.py`（未在本次 dirty diff 中，仍含 `cast` 调用，属独立 clean-up 范围）
  - 生产代码 `dayu/host/compaction.py`、`dayu/host/llm_compaction.py`（本次未变更）
- Parallel review coverage: 无
- Untracked: `docs/reviews/wu-cm-05-s3-implementation-report.md`

## Context

S3 目标是 Contract and boundary cleanup，关闭 DS-F02：`_proposal_json` 不应使用 `cast` 祝福 JSON。
本次改动移除 `tests/host/test_llm_compaction.py` 中全部 `cast` 导入和调用，新增 `_compact_input_json`
与 `_is_json_mapping`，用 `_required_mapping` + `TypeGuard` 做显式 runtime narrowing。生产代码未变更。

## Summary

- DS-F02 **已修复**：`cast` 从测试文件完全移除，`_proposal_json` 改用 `_required_mapping`（TypeGuard + assert）做显式 JSON object 收窄。
- 未发现 `cast`/`Any`/`object`/裸弱类型逃逸，`TypeGuard` 用法合法，非伪装绕过。
- 所有 helper 签名含完整类型标注，docstring 含 :param/:returns/:raises（中文），符合项目约束。
- 测试全部通过 `parse_conversation_compact_output_vnext` public entry point，不依赖私有 parser。
- 无生产代码变更，README 不更新判断成立。
- pyright: 0 errors, 0 warnings, 0 informations；pytest: 37 passed。

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- `tests/host/fake_compaction.py:465` 仍含 `cast(Mapping[str, JsonValue], value)`，属同一测试目录下的遗留弱类型模式。该文件不在本次 S3 dirty diff 范围内，建议后续 slice 一并清理。
- `_proposal_json` docstring 中 `:raises TypeError:` 在改后已不准确（现由 `_required_mapping` 抛出 `AssertionError`）：`json.loads` 可能抛 `json.JSONDecodeError`、`_required_mapping` 抛 `AssertionError`，docstring 未反映。不影影响测试行为，属微小编写问题。
