# Code Review（Final Re-review）

## Scope

- Mode: current changes
- Branch: `codex/download-oracle`
- Base: `0fe85869`（accepted plan HEAD）
- Output file: `docs/reviews/code-review-20260810-173314.md`
- Included scope: 4 个实际 changed Python files + validation fix artifact
- Excluded scope: `tests/cli/test_arg_parsing.py`（已完全还原，零 diff，退出 changed-files 集合）、plan 外 production/test 文件、README、Slice 2/3 未实现模块
- Parallel review coverage: 无
- Re-review inputs:
  - `docs/gateflow/wu-cli-download-02-slice1-code-review-adjudication-20260810.md`（含 validation 裁决）
  - `docs/gateflow/wu-cli-download-02-slice1-validation-fix-20260810.md`
  - `docs/reviews/code-review-20260810-172154.md`（上一轮 re-review）

## Findings

未发现实质性问题。

### Validation 裁决关闭状态

#### `test_arg_parsing.py` 零 diff — 已验证

- `git diff 0fe85869 -- tests/cli/test_arg_parsing.py`：exit 0，无输出。
- 该文件完全回到 accepted-plan base，退出 changed-files 集合。
- `COMMAND_HELP_EXPECTATIONS["download"]` 中的 `--rebuild` inventory 和 `test_download_help_explains_mutually_exclusive_mutation_modes` 已从该文件删除。

#### Help owner test 迁移等价 — 已验证

- `test_download_help_explains_mutually_exclusive_mutation_modes` 已迁移到 `tests/cli/test_fins_commands.py:782`。
- 迁移后测试使用 `cli_main.main(("download", "--help"))` 走真实 CLI 入口，比原 `_capture_help` helper 更严格。
- 断言覆盖：`exit_code == EXIT_SUCCESS`、`--overwrite` in output、`覆盖已有原始文档；不可与 --rebuild 同时使用。` in output、`--rebuild` in output、`不访问远端来源；不可与 --overwrite 同时使用。` in output、`captured.err == ""`。
- 原测试的 4 个 help 文案断言全部保留，额外增加 exit code 和 stderr 断言。

#### Production 不变 — 已验证

| 文件 | 变更 | 与上一轮一致 |
|---|---|---|
| `dayu/cli/arg_parsing.py` | +8（`--overwrite`/`--rebuild` help 互斥说明） | ✅ |
| `dayu/fins/download_contract.py` | +60（`_validate_download_mutation_mode`、`FinsDownloadRequest.__post_init__`、`FinsDownloadEffectiveFilters.__post_init__` 复用、docstring 补充） | ✅ |

未编辑其它 production 文件。

#### 4 个实际 changed Python files 的 Ruff format 真实 exit 0 — 已验证

```text
git diff --name-only <base> -- '*.py'
dayu/cli/arg_parsing.py
dayu/fins/download_contract.py
tests/cli/test_fins_commands.py
tests/service/test_fins_direct.py

ruff format --check dayu/cli/arg_parsing.py dayu/fins/download_contract.py tests/cli/test_fins_commands.py tests/service/test_fins_direct.py
4 files already formatted
exit 0
```

`tests/cli/test_arg_parsing.py` 不在 changed-files 集合中，不参与 format gate。

#### Tests / coverage / pyright — 已验证

```text
pytest tests/service/test_fins_direct.py tests/cli/test_arg_parsing.py tests/cli/test_fins_commands.py
549 passed, 3 warnings in 5.77s

coverage report --include='dayu/cli/arg_parsing.py,dayu/fins/download_contract.py'
dayu/cli/arg_parsing.py            342      2    99%
dayu/fins/download_contract.py     325     39    88%

ruff check <4 files>             PASS
python -m compileall -q <4 files>  PASS
pyright                            0 errors, 0 warnings, 0 informations
```

完整 focused owner union：1020 passed, 3 warnings in 13.68s。

#### 静态 guards — 已验证

| Guard | 结果 |
|---|---|
| `git diff --check` | exit 0 |
| conjunction owner `rg` | 仅 `dayu/fins/download_contract.py:79` |
| AST test 删除 `rg` | exit 1（无匹配，符合预期） |
| README diff guard | exit 0（无 README 变更） |

### Adversarial pass

- 无新增 correctness finding。Production diff 与首轮 review 完全一致。
- 无 scope drift。Changed-files 精确为 4 个 Slice 1 allowed files。
- 无 semantic ownership drift。唯一 owner 仍为 `_validate_download_mutation_mode`。
- 无 test ownership 退化。行为测试（legal matrix、conflict rejection、non-boolean rejection、CLI pre-workspace、help owner）完整保留。

## Open Questions

- 无。

## Residual Risk

| 风险 | 分类 | 说明 |
|---|---|---|
| CLI help 尚未在 README 投影 | covered by later slice | accepted plan Slice 3 稳定后处理 |
| 真实 CLI post-fix evidence 尚未执行 | covered by later slice | accepted plan §9.2 |
| F13/F14 尚未实现 | covered by later slice | Slice 2 / Slice 3 |

## 综合判定

**PASS**。

Validation 裁决要求的全部 gate 均已真实通过：

1. **`test_arg_parsing.py` 零 diff**：exit 0，完全回到 accepted-plan base，退出 changed-files 集合。
2. **Help owner test 迁移等价**：`test_download_help_explains_mutually_exclusive_mutation_modes` 已迁入 `test_fins_commands.py`，覆盖原有 4 个 help 文案断言 + exit code + stderr。
3. **Production 不变**：`arg_parsing.py` +8、`download_contract.py` +60，与首轮 review 完全一致。
4. **Ruff format 真实 exit 0**：4 个实际 changed Python files 全部 `already formatted`。
5. **Tests / coverage / pyright**：549 affected tests passed、1020 focused union passed、逐文件 coverage 99%/88%、pyright 0 errors。
6. **无新增 finding**：无 correctness、scope、semantic ownership 或 test ownership 问题。
