# Code Review（Slice 4 re-review · 第二轮：170139 Finding 1 裁决后终审）

## Scope

- Mode: current changes（re-review）
- Branch: `codex/upload-filing-oracle`
- Base: HEAD `1fd52c96f5e07f6007c3c6442d20635f3d37182b`
- Output file: `docs/reviews/code-review-20260816-170922.md`
- Included scope:
  - 上一轮 re-review artifact：`docs/reviews/code-review-20260816-170139.md`（本路，PASS + 1 中 finding）
  - 更新后的 fix artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-code-review-fix-20260816.md`
    （新增 "Re-review 170139 Finding 1：不得隐式推翻 Slice 2 fix D" 裁决段）
  - 更新后的 implementation artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-implementation-20260816.md`
    （新增 "Re-review 170139：Slice 2 fix D frozen contract 恢复" 段）
  - 当前完整未提交 diff 的增量：`_fs_source_integrity.py`（+42，`_derive_repair_blocked_reason` whole-kind
    分支恢复 Slice 2 语义）、两个 owner test 文件的 reason 期望更新与 Slice 2 direct payload 断言恢复
  - 只读复核：core 的机械消费实现（`_fs_source_document_core.py`）、Slice 2 fix D 原裁决文本
- Excluded scope: 无。
- Parallel review coverage: 无（单路走读 + 定向测试/pyright 复现）。

## 结论

**PASS（无 material findings）**

Controller 对 170139 Finding 1 的裁决（accepted / blocker；恢复 Slice 2 frozen contract）已完整落地并写入
两个 gateflow artifact；`_derive_repair_blocked_reason()` 的 whole-kind / exact-target 语义与 Slice 2 fix D
及 Controller 裁决完全一致；core 保持纯机械消费无重算；此前三项初审 findings 与九项 Slice 4 语义无回退。
所有定向验证独立复现通过。

---

## Findings

未发现实质性问题。

---

## 逐项验证证据

### 1) whole-kind（target=None）repairable incomplete 固定 CANONICAL_MANIFEST_UNAVAILABLE

- `_fs_source_integrity.py:1296-1307`：unsafe gate 之后，`target is None` 分支把
  `shared_manifest_reasons` 非空、任一 item content 非 COMPLETE / public reasons 与 shared 不一致 /
  canonical item 缺失、aggregate 长度不匹配，全部收敛为 `CANONICAL_MANIFEST_UNAVAILABLE` 并返回。
- Slice 2 direct payload 断言已恢复：
  `test_damaged_fixture_classifier_snapshot_and_commit_are_consistent`
  （`tests/fins/test_fins_storage_atomicity.py:6967-6969`）重新断言 whole-kind content 损坏 →
  `CANONICAL_MANIFEST_UNAVAILABLE`（本 reviewer 通过 `-k "damaged_fixture"` 运行确认 23 passed 含该用例）。
- material whole-kind owner 测试 `test_material_whole_inspection_owns_repair_blocked_reason` 参数化
  content_missing / manifest_missing 均为 `CANONICAL_MANIFEST_UNAVAILABLE`（行 7319-7327）。

### 2) whole-kind unsafe 固定 CROSS_SOURCE_PUBLICATION_UNSAFE

- `_fs_source_integrity.py:1286-1293`：unassignable_root_fact 或任一 item public UNSAFE →
  `CROSS_SOURCE_PUBLICATION_UNSAFE`；`SOURCE_MANIFEST_UNTRUSTED in shared_manifest_reasons` 同样闭合为该
  reason。whole-kind root structural 无法归属时仍由 `_inspect_source_kind_unguarded` 直接抛
  `SourceIntegrityPreflightError`，core 在 `_fs_source_document_core.py:463-466` 精确映射为
  `CROSS_SOURCE_PUBLICATION_UNSAFE`（service structural_unsafe 用例断言
  `blocked_error.__cause__` 为 `SourceIntegrityPreflightError`）。
- Slice 2 fix D 的 "unsafe仍为CROSS_SOURCE_PUBLICATION_UNSAFE" 无变化。

### 3) exact-target sibling incomplete 仍为 NON_TARGET_SOURCE_INCOMPLETE

- `_fs_source_integrity.py:1309-1315`：exact 分支（target 非 None）排除 target 后，任一 non-target
  content 非 COMPLETE 或 public reasons 与 shared 不一致 → `NON_TARGET_SOURCE_INCOMPLETE`。
- synthetic payload 测试 `test_filing_sibling_manifest_mismatch_owner_matches_phase_b_block`（sibling
  projection mismatch）断言 owner 派生 `NON_TARGET_SOURCE_INCOMPLETE` 且 collector 机械传播同一 reason——
  本 reviewer 单独运行该用例通过。
- 关键可修复场景无回退：whole manifest missing 时（exact mode，shared 仅 `SOURCE_MANIFEST_MISSING`，
  non-targets content COMPLETE 且 reasons == shared）exact 分支返回 `None` 不阻断，repair 仍可重写整份
  canonical manifest（成功路径 facade 测试锁定）。

### 4) `_derive_repair_blocked_reason` 仍是唯一 owner，core 无重算

- core 两处消费均只做 `repair_blocked_reason is not None → raise`（`_fs_source_document_core.py:108-111`、
  行 472-475），无任何 status/reasons/shared 二次判断。
- `_canonical_remaining_manifest_items_for_repair()` 收集阶段只校验 canonical item shape/unique 违约并抛
  producer-invariant `RuntimeError`（行 119-126），不派生业务 blocked reason。
- 全仓 grep：`repair_blocked_reason` 生产者仅 `_fs_source_integrity.py` 一处；消费者仅 core 两处与 commit
  validator 的 `is not None` 检查。

### 5) material content/manifest/root 均继续在 reset 前投影 public SOURCE_REPAIR_BLOCKED 且 rollback 不变

- service 三场景参数化测试 `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once`
  期望 reason 已同步为 `CANONICAL_MANIFEST_UNAVAILABLE`（content_missing）、
  `CANONICAL_MANIFEST_UNAVAILABLE`（manifest_missing）、`CROSS_SOURCE_PUBLICATION_UNSAFE`（structural_unsafe）；
  全部断言 public code `SOURCE_REPAIR_BLOCKED`、`rollback_calls == 1`、`commit_calls == 0`、old tree SHA /
  filing / material meta / company meta 不变、batch cleanup 完成。
- Phase B 顺序未变：filing exact inspect → target presence/status/content gate → identity comparison →
  material whole-kind inspect → filing non-target blocked → material blocked → reset → rewrite。所有 blocked
  均发生在 reset 前。

### 6) 此前三项初审 findings 与九项 Slice 4 语义无回退

- 163507-F1/F2/F3 闭环状态与本轮 diff 均未回退（facade/protocol/service caller diff 与初评完全一致；
  helper 机械消费形态不变；material blocked 测试矩阵完整）。
- 九项语义（disposition 贯穿与 identical-skip bypass、storage 唯一 owner 与 gate 顺序、target-local damage
  不误判 blocked、staging 污染边界、facade 纯 delegate、stale/blocked 精确映射、Controller caller 机械接线、
  tests 真实命中、无兼容/default/Any/反射）逐项复核无回退。
- controller 指定的 inspection 次数不变：counter 测试仍锁定 `[(FILING, target_id), (MATERIAL, None)]`
  （`test_source_repository_repair_facade_resets_target_and_rewrites_canonical_sibling_manifest`）。

### 7) Artifacts、测试、pyright、scope、frozen guards

- artifacts：fix artifact 新增 "Re-review 170139 Finding 1" 段，含 Controller 裁决原文（第一性原理：
  whole-kind `target=None` 无 non-target 参照）、最小修复描述与 reason 同步说明；implementation artifact 行
  229-236 同步记录。裁决记录完整、可追溯。
- 测试（本 reviewer 独立复现）：`test_fins_storage_atomicity.py -k "repair or material_whole_inspection or
  damaged_fixture"` → 23 passed；`test_docling_upload_service.py -k repair` → 15 passed；三个关键 owner 用例
  （sibling mismatch / shape violation / facade success）单独运行 3 passed。
- pyright（本 reviewer 独立运行全部 10 个修改文件）：`0 errors, 0 warnings, 0 informations`。
- scope：`git status` 变更文件仍限于原 allowed + Controller 授权集合 + 4 个 review artifacts + 2 个 gateflow
  artifacts，无越界。
- frozen guards：`git diff -- README.md dayu/fins/README.md tests/README.md docs/cli_ci_oracles.json
  docs/cli_ci_scenarios.json docs/host/design.md docs/engine/design.md` 输出为空；production diff 静态扫描无
  `Any/object/getattr/hasattr` 新增。

---

## Open Questions

- 无。

## Residual Risk

- `_derive_repair_blocked_reason()` 的 exact 分支中 `CROSS_SOURCE_PUBLICATION_UNSAFE`（unassignable_root_fact
  / non-target public UNSAFE）与 `CANONICAL_MANIFEST_UNAVAILABLE`（non-target canonical item 缺失）在 Phase B
  可达路径上大多为防御性（前者多数被 target conflict gate 前置覆盖；后者因 content 非 COMPLETE 已被
  `NON_TARGET_SOURCE_INCOMPLETE` 先行命中）。不影响 correctness，若未来需要 plan §6.2 step 5 的 filing
  `CANONICAL_MANIFEST_UNAVAILABLE` 细分语义，需另立 work unit 明确触发条件。
- 完整 affected matrix（345 passed）与完整 Fins suite（1827 passed）及逐文件 coverage 数字仍依据 artifact
  声称，本 reviewer 未独立重跑全量。
- 未 commit、未 push、未创建 PR，与 gateflow gate 状态一致。
