# Code Review

## Scope

- Mode: current changes (diff against base `f609a4d8`)
- Branch: `codex/upload-filing-oracle`
- Base: `f609a4d8238c6b31456c2e1d548079b22b771a68`
- Reviewer: AgentMiMo
- Review date: 2026-08-14
- Output file: `docs/reviews/code-review-20260814-143751.md`
- Included scope:
  - `dayu/fins/domain/filing_semantics.py`
  - `tests/fins/test_fiscal_normalization_contracts.py`
  - `tests/fins/test_read_runtime_semantic_ownership_guards.py`
  - `docs/reviews/wu-upload-filing-calendar-year-validation-s1-implementation-codex.md`
  - `AGENTS.md` / `CLAUDE.md` (project instructions)
  - `dayu/fins/tools/read_runtime.py` (唯一生产 consumer，只读审查)
- Excluded scope: S2/S3 未接线 consumer；UF-PF04；其它不属于 S1 的文件。
- Parallel review coverage: 无。

## Validation

- 完整读取 accepted plan S1、implementation artifact、AGENTS.md/CLAUDE.md。
- 完整读取 diff（3 files, +323/-17）。
- 完整读取 `dayu/fins/tools/read_runtime.py` 中 `_parse_source_document_meta`（唯一生产 consumer），确认调用链。
- 全仓 `normalize_fiscal_year(` 和 `parse_calendar_year(` grep 确认唯一生产 consumer。

## Findings

未发现实质性问题。

### 逐项审查结论

**1. Correctness — bool/non-int guard 顺序**
- `parse_calendar_year` 第 354-356 行先检查 `isinstance(value, bool)` 再检查 `isinstance(value, int)`，正确。Python 的 `bool` 是 `int` 子类，顺序颠倒会导致 `True/False` 通过 int 检查。
- `normalize_fiscal_year` 第 416 行同样先 bool 后 int，且在委托 `parse_calendar_year` 前完成窄化，保证 owner 只收到已确认的 `int`。
- 测试矩阵覆盖 `True, False` 两条路径。**通过。**

**2. Strict ASCII YYYY-MM-DD**
- `_STRICT_ISO_CALENDAR_DATE_PATTERN` 使用 `re.ASCII` flag + `[0-9]` 字符类，精确拒绝全角数字 `２０２４`、空白、非补零、错误分隔符。
- `fullmatch` 确保无前导/尾随字符。
- 测试矩阵包含 17 个负例，覆盖空串、空白、非补零、`/` 分隔、全角数字。**通过。**

**3. 公历 0001..9999 vs 财年 1000..9999 语义解耦**
- `parse_iso_calendar_date` 不调用 `parse_calendar_year`，独立使用 `datetime.date()` 验证实际 Gregorian 日期。
- monkeypatch 守护测试 `test_parse_iso_calendar_date_does_not_delegate_to_calendar_year_owner` 用 `"0999-12-31"` 证明 year<1000 的合法 Gregorian 日期不经过 year owner。
- 正向测试包含 `("0001-01-01", datetime.date(1, 1, 1))` 和 `("0999-12-31", datetime.date(999, 12, 31))`。**通过。**

**4. isoformat round-trip 防御**
- 第 391 行 `parsed.isoformat() != value` 防御 `datetime.date` 构造器对非规范输入的静默接受（理论上 regex + datetime.date 已足够，但 round-trip 是成本极低的额外保险）。
- `datetime.date.isoformat()` 返回零填充 `YYYY-MM-DD` 格式，与 regex 要求一致。**通过。**

**5. Owner 唯一性**
- `parse_calendar_year`: 全仓唯一非-test 调用方是 `normalize_fiscal_year`（`filing_semantics.py:418`）。
- `normalize_fiscal_year`: 全仓唯一非-test 调用方是 `_parse_source_document_meta`（`read_runtime.py:627`）。
- 无第二真源、无 fallback、无 hasattr/getattr、无兼容 shim。**通过。**

**6. Read-runtime fail-closed**
- `_parse_source_document_meta` 直接调用 `normalize_fiscal_year(raw_meta.get("fiscal_year"))`，不捕获 ValueError。
- 历史非法值（999, 10000, bool, "2025"）在 read-runtime 测试中确认抛出 ValueError。**通过。**

**7. 类型/docstring**
- 两个新函数均有完整中文 docstring（Args/Returns/Raises）。
- 签名无 `object`、`Any`、无类型参数。
- `parse_calendar_year(value: int) -> int`，`parse_iso_calendar_date(value: str) -> datetime.date`。**通过。**

**8. 测试是否锁定真实 owner**
- `test_normalize_fiscal_year_narrows_json_before_delegating_year_owner`: monkeypatch `parse_calendar_year`，验证非整数不进入 owner、合法整数正确委托。
- `test_parse_iso_calendar_date_does_not_delegate_to_calendar_year_owner`: monkeypatch `parse_calendar_year`，验证 full-date parser 独立于 year owner。
- read-runtime 测试通过 `_parse_source_document_meta` 端到端验证 fail-closed。**通过。**

**9. Coverage 证据**
- Implementation artifact 报告：`coverage report --include='dayu/fins/domain/filing_semantics.py' --fail-under=80` → 139 statements, 19 missed, **86%**。
- 新增 `parse_calendar_year`（13 行，纯路径覆盖）和 `parse_iso_calendar_date`（17 行，含 try/except 两条路径）均有正负矩阵覆盖。**通过。**

**10. 过度耦合/重复校验/compatibility shim**
- `normalize_fiscal_year` 的 bool/int guard 与 `parse_calendar_year` 的 bool/int guard 存在逻辑重复，但这是合理的分层设计：`normalize_fiscal_year` 负责 `JsonValue | None → int | None` 窄化，`parse_calendar_year` 负责 `int → 1000..9999 int` 校验。两者各自独立正确，不构成 compatibility shim。
- 无新增类、协议、factory、re-export、shim。**通过。**

**11. Docstring 和常量命名**
- `_MIN_CALENDAR_YEAR` / `_MAX_CALENDAR_YEAR` 为模块级 `Final[int]` 私有常量，docstring 说明用途。
- `_STRICT_ISO_CALENDAR_DATE_PATTERN` 同上。**通过。**

**12. `datetime.date` 年份域**
- Python `datetime.date` 支持 year 1..9999。regex `[0-9]{4}` 允许 0000，但 `datetime.date(0, 1, 1)` 会抛出 `ValueError`，被第 389-390 行的 except 捕获。**通过。**

## Open Questions

无。

## Residual Risk

- `parse_iso_calendar_date` 当前无生产 consumer，仅作为 domain owner 建立。S2/S3 接线时需确保 consumer 直接调用此 owner 而非自行解析。
- `filing_semantics.py` 模块 docstring 未提及新增的 calendar year/date 解析能力，但 S1 scope 不要求更新 README，由后续 slice 处理。
- 19 missed statements 来自 `filing_semantics.py` 中非 S1 范围的其它函数（如 `sanitize_fiscal_period_by_sec_form` 等），不影响 S1 新增代码覆盖。

## Docs Decision

- `dayu/fins/README.md`: S1 scope 明确排除，由后续 approved slice 更新。
- 根 `README.md`: 同上。
- `tests/README.md`: 不更新，未新增测试层级。
- 其它 README: 分层/装配未变化，不触发。

## Conclusion

**Pass — 未发现实质性问题。**

S1 实现正确建立了 Fins domain calendar/year owner：
- `parse_calendar_year` 和 `parse_iso_calendar_date` 作为纯函数，语义边界清晰，不耦合。
- `normalize_fiscal_year` 正确窄化 `JsonValue | None` 后委托 owner，无 fallback。
- read-runtime 作为唯一生产 consumer，fail-closed 行为通过测试锁定。
- 测试覆盖正负矩阵、边界、monkeypatch owner 守护，coverage 86% ≥ 80% gate。
- 无 compatibility shim、无第二真源、无过度耦合。

## Next Entry Point

`dual S1 deepreview` — 需要第二独立 reviewer 完成后，由 controller 裁决 S1 是否 accepted。
