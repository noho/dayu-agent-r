# UF-FIX08 existing-source-auto-repair：Aggregate Re-Review

## Gate 元数据

- work unit：`UF-FIX08 existing-source-auto-repair`
- gate：`aggregate deepreview re-review`
- base：`5859856e46af42c9ae5a2a5c07fab1ba59dc91d3`（UF-FIX07 closeout）
- HEAD：`65d352e6` + workspace aggregate fix（未提交）
- 输入 artifacts：
  - `docs/reviews/deep-review-20260816-191226.md`（first aggregate review）
  - `docs/reviews/deep-review-20260816-192359.md`（second aggregate review，未读取其 re-review 产出）
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-aggregate-deepreview-fix-20260816.md`（fix artifact）
- workspace diff：3 files（`_fs_source_integrity.py` +34/-6、`test_fins_storage_atomicity.py` +130、`test_sec_pipeline_download.py` +57）
- 日期：2026-08-16
- reviewer：AgentMiMo（re-review 独立复核）

## 结论

**PASS**

M2 fix 正确，M1 证据失效确认，docstring 改进到位，finding/residual 分类合理，scope/frozen guards 无越界。

## 验证摘要

| 验证项 | 结果 |
| --- | --- |
| 新增 5 个精确测试 | 5 passed |
| focused matrix（含新增） | 1250 passed |
| 全仓 pyright | 0 errors, 0 warnings |

## M2：trusted manifest 悬空 IDs 独立验证

### Fix 逻辑走读

`_apply_manifest_facts`（`_fs_source_integrity.py:1122-1149`）：

1. `dangling_ids = tuple(sorted(manifest_ids - source_ids))` — 稳定排序，确定性
2. `unsafe_inventory = tuple(_with_unsafe_classification(item, reason) for item in inventory)` — 全部已有 item 标 UNSAFE
3. `dangling_inventory = tuple(_unsafe_source_inspection(...) for document_id in dangling_ids)` — 每个悬空 ID 生成 synthetic UNSAFE inspection，revision=None，不伪造 meta/provenance/files/canonical item
4. `unsafe_by_id = {item.classification.document_id: item for item in (*unsafe_inventory, *dangling_inventory)}` — 合并去重
5. `tuple(unsafe_by_id[document_id] for document_id in sorted(unsafe_by_id))` — 按 document ID 稳定排序返回

### 场景矩阵验证

| 场景 | inventory | manifest | dangling | 结果 | 验证 |
| --- | --- | --- | --- | --- | --- |
| empty inventory + dangling manifest | () | trusted, 有 items | 有 | synthetic UNSAFE items | ✅ test_dangling_trusted_manifest_projects_stable_unsafe_inventory |
| non-empty + dangling manifest | 有 items | trusted, 有额外 items | 有 | 全部 UNSAFE + synthetic UNSAFE | ✅ same test (source_dir.rename 后 inventory 为空但 manifest 保留) |
| non-empty + no dangling | 有 items | trusted, 完全匹配 | 无 | 正常处理 | ✅ existing tests |
| empty + no manifest | () | missing | N/A | empty inventory | ✅ existing tests |
| non-empty + untrusted manifest | 有 items | untrusted | N/A | 全部 UNSAFE | ✅ existing tests |
| non-empty + missing manifest + all complete | 有 items | missing | N/A | 全部 REPAIR_REQUIRED | ✅ existing tests |
| non-empty + missing manifest + not all complete | 有 items | missing | N/A | 全部 UNSAFE | ✅ existing tests |

### 下游消费一致性

- **exact-target**（`classify_source_integrity`）：`_inspect_source_kind_unguarded` exact mode → `_select_exact_target` 在 inventory 中找到 synthetic UNSAFE item → 返回 UNSAFE classification ✅
- **whole-kind**（`list_source_integrity`）：inventory 包含 synthetic items → public projection 包含 UNSAFE ✅
- **preflight**（`classify_source_integrity_preflight`）：`any(item.status is UNSAFE for item in inventory)` → `UNSAFE_PUBLICATION` fail closed ✅
- **commit validator**（`_validate_complete_source_tree`）：`repair_blocked_reason` 非空（`CROSS_SOURCE_PUBLICATION_UNSAFE`）→ reject ✅
- **canonical_manifest_items**：`_canonical_manifest_items` 要求 `_all_content_complete` → UNSAFE item 的 content_classification 也是 UNSAFE → 不 complete → 返回空 → 不影响 repair ✅
- **SEC download**：`test_sec_dangling_manifest_preflight_has_zero_company_or_provider_mutation` 验证 zero provider/company/rejection mutations ✅

### dict 去重安全性

`unsafe_inventory` 的 document IDs 来自 `source_ids`（实际 source），`dangling_inventory` 的 document IDs 来自 `dangling_ids = manifest_ids - source_ids`。两集合互斥（集合差集定义），无 key 冲突。

## M1：foreign descriptor 本就 IDENTITY_UNTRUSTED 核验

### _fs_identity 直接证据

`_read_identity_descriptor`（`_fs_identity.py`）在枚举模式下使用 `directory.name` 与 `derive(namespace, descriptor external_identity)` 做双向校验。当 descriptor 声明的 identity 与 canonical locator 不一致时，必定抛 `ValueError`。

### 测试验证

`test_canonical_source_directory_with_foreign_descriptor_is_unsafe`（parametrized filing/material）：
1. 创建 complete source → commit
2. 修改 descriptor `external_identity` 为 `"foreign-document"`
3. `classify_source_integrity` → `UNSAFE(IDENTITY_UNTRUSTED), revision=None` ✅
4. `classify_staged_source_integrity` → 同结果 ✅
5. `list_source_integrity` → `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)` ✅

**结论**：M1 确认为证据失效。identity owner 已有双向校验，inspector 正确投影为 UNSAFE。不增加冗余 production 校验是正确决策。

## Docstring 改进验证（L3/L4）

### L3：revision 私有性

`_SourcePublicationInspection.revision` docstring 更新为：「content 扫描对应的可信 opaque revision；当公共 classification 为 `UNSAFE` 时仅是私有 content fact，不得对外投影」。这正确反映了 `_with_unsafe_classification` 将 revision 设为 None 的设计，以及即使 `content_classification` 保留 content-level facts，public classification 为 UNSAFE 时 revision 不对外暴露。

### L4：canonical items gating

`_SourceKindPublicationInspection.canonical_manifest_items` docstring 更新为：「由全部 complete content 产生的 canonical manifest 项目；消费前必须先确认 `repair_blocked_reason` 为 `None`」。`repair_blocked_reason` docstring 更新为：「其它 source 或 canonical aggregate 对 repair 的阻断原因；非空时 `canonical_manifest_items` 不构成可执行 repair 的授权」。这正确反映了 `_canonical_remaining_manifest_items_for_repair` 先 gate `repair_blocked_reason` 的设计。

## Finding 裁决复核

| Finding | Fix artifact 裁决 | Re-review 裁决 | 依据 |
| --- | --- | --- | --- |
| 1 / M1 canonical 目录被异源 descriptor 占据 | 证据失效 | **确认** | _fs_identity 双向校验 + 新增 owner regression tests |
| 2 / M2 空 inventory + trusted manifest 悬空 ID | 已修复 | **确认** | synthetic UNSAFE inspections + 5 个新测试 + SEC zero-mutation |
| 3 / private revision 在 public UNSAFE 后保留 | 已修复（docstring） | **确认** | docstring 明确「不得对外投影」 |
| 4 / canonical items 与 blocked reason 并存 | 已修复（docstring） | **确认** | docstring 明确「消费前必须先 gate」 |
| 5 / `_ordered_reasons` 非 enum 值 | 证据失效 | **确认** | 签名只接受 typed list/tuple |
| 6 / raw runtime `RuntimeFileLockError` | deferred | **确认** | 不违反 UF-FIX08 invariant |
| 7 / 手工构造 validated request | deferred | **确认** | production producer 均经唯一 validator |
| 8 / SEC/CN 顶层 download generic 差异 | deferred | **确认** | 归属 future owner |

## Low 项裁决复核

| 项 | Fix artifact 裁决 | Re-review 裁决 |
| --- | --- | --- |
| L1 inspector 两次 physical read TOCTOU | rejected | **确认** — caller-held guard 是当前一致性边界 |
| L2 catch 闭集依赖跨模块异常面 | rejected | **确认** — 无可达 typed 输入产生闭集外异常 |
| L3 public classification 不校验 ticker/document 值域 | rejected | **确认** — production 输入由 identity owner 校验 |
| L4 public repository docstring 保留结构 ValueError | rejected | **确认** — operational ValueError 仍是 public Raises |
| L5 upload-state 早返回不校验 document ID | rejected | **确认** — caller 已通过 identity owner 校验 |
| L6 snapshot kind 自动探测改为双 inspection | rejected | **确认** — accepted fail-closed 方向 |
| L7 repair manifest 重写只保留当前三字段 | rejected | **确认** — schema owner 只定义该字段集 |
| L8 空 selection 防御分支 | rejected | **确认** — producer-invariant fail-loud 防御 |
| L9-L15 | rejected | **确认** — 均不影响 typed contract |

## README / Scope / Frozen Guards

- **README**：fix 不改变行为语义（恢复已文档化的 contract），不更新 README ✅
- **Scope**：tracked diff 仅 3 个 production/test 文件 ✅
- **Oracle/scenario**：零 diff ✅
- **Host/Engine design**：零 diff ✅
- **Host/Engine/Service/CLI production**：零 diff ✅

## Residual Risks

与 fix artifact 一致，无新增：

| 风险 | 分类 | owner / destination |
| --- | --- | --- |
| 同 request 并发 success/skip 收敛 | assigned to later | `UF-FIX10` |
| fresh company meta warning | assigned to later | `UF-FIX11` |
| material existing-source repair | assigned to later | 后续独立 work unit |
| 旧 schema corpus 读取/迁移 | assigned to later | 后续 migration work unit |
| UF-PF08/UF-PF12 真实 evidence | assigned to later | evidence work unit |
| registry/oracle 仍描述 fix 前状态 | assigned to later | registry adjudication |
| SEC/CN download typed terminal projection | assigned to later | future failure projection owner |
| raw runtime lock operational failure | assigned to later | operational failure owner |
| 手工构造 validated request 强化 | assigned to later | contract hardening owner |
| manual filesystem writer 绕过 lock | operational policy | storage operational policy |

无 unclassified residual risk，无 blocking open question。

## Final Decision

**PASS**。

M2 fix 正确：trusted manifest 悬空 IDs 现在在 empty/nonempty inventory、filing/material、exact/list/preflight/commit 中均稳定 fail-closed，synthetic inspections 去重排序且不把缺失误判为可修复。M1 确认证据失效：`_fs_identity` 双向校验已保证 foreign descriptor 为 `IDENTITY_UNTRUSTED`。Docstring 改进到位。所有 finding 已裁决。scope/frozen guards 无越界。1250 tests passed，pyright 0 errors。
