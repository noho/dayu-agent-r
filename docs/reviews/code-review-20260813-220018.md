# Code Review — UF-FIX03 S1 re-review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `662c9ad4`（gateflow: accept plan for UF-FIX03）
- Output file: `docs/reviews/code-review-20260813-220018.md`
- Included scope: 全部 11 个未提交变更文件（5 生产 + 6 测试），plus review-fix artifact 与 implementation artifact
- Excluded scope: S2/S3/UF-PF03 范围文件；Host/Engine/runtime/config/storage/Service/CLI/README/frozen JSON
- Parallel review coverage: 无
- Review 时间: 2026-08-13 22:00（本机时钟生成）

## 结论（Verdict）

**PASS — F1/F2 均已闭合，无实质性 findings。**

## Review 输入

- 裁决文档：`docs/gateflow/uf-fix03-s1-code-review-adjudication-20260813.md`
- 修复记录：`docs/gateflow/uf-fix03-s1-review-fix-20260813.md`
- 原 review 1：`docs/reviews/code-review-20260813-213823.md`（PASS，无 findings）
- 原 review 2：`docs/reviews/code-review-20260813-214459.md`（FAIL，F1+F2）
- 当前 diff：`git diff 662c9ad4`

## F1 验证：S1 修改分支 owner 行为覆盖已闭合

裁决要求补齐三个缺失场景的行为测试。逐一验证：

### 1. service early-cancelled summary

`test_production_upload_runner_early_cancel_uses_request_count`（`test_fins_service_runtime.py`）：

- 入口：`ProductionFinsUploadRunner.run_upload` + `_AlwaysCancelledChecker`（始终 `return True`）
- 断言：`summary.status == "cancelled"`、`summary.requested_file_count == len(request.files)`、`summary.stored_file_count == 0`
- 生产代码：`service_runtime.py:131-137` — `requested_file_count=len(raw_request.files)`, `stored_file_count=0`
- **结论**：✓ 行为测试触达真实 owner，值语义已锁定

### 2. direct runner-unavailable summary

`test_direct_upload_without_runner_reports_requested_and_zero_stored_counts`（`test_fins_ingestion_runtime.py`）：

- 入口：`FinsIngestionRuntime.upload` + 无 `upload_runner` + `FinsUploadMaterialRequest(files=(Path("first.pdf"), Path("second.pdf")))`
- 断言：`result.status == FAILURE`、`details["requested files"] == "2"`、`details["stored files"] == "0"`
- 生产代码：`ingestion_runtime.py:3945-3950` — `requested_file_count=len(_raw_upload_request(request).files)`, `stored_file_count=0`
- **结论**：✓ integration 行为测试触达真实 owner，count 与文案均已锁定

### 3. skipped + requested_file_count=0 拒绝

`test_upload_summary_count_owner_rejects_invalid_status_matrix`（`test_fins_ingestion_runtime.py`）：

- 参数化用例 `("skipped", 0, 0)` 覆盖拒绝分支
- 生产代码：`ingestion_runtime.py:1310-1312` — `elif self.status == _UPLOAD_RESULT_STATUS_SKIPPED: if self.requested_file_count < 1: raise ValueError(...)`
- 构造时 `requested_file_count=0` 触发 ValueError
- **结论**：✓ 拒绝分支已被行为测试覆盖

## F2 验证：workflow 级 conversion-cancelled catch 删除后 cancellation 语义完整性

### 删除确认

- `sec_upload_workflow.py`：`DoclingConversionCancelledError` import 已删除；catch 块（原 287-325 行）及 inline `UploadOperationResult(...)` 构造已删除
- `cn_pipeline.py`：同形删除已完成

### cancellation owner 确认

`DoclingUploadService.prepare_upload(...)` 仍完整拥有 cancellation 语义：

- `docling_upload_service.py:336-340`：`except DoclingConversionCancelledError: return _build_cancelled_result(...)`
- `_build_cancelled_result`（line 1419-1430）构造 `UploadOperationResult(status="cancelled", stored_file_count=0, ...)`
- `docling_upload_service.py:341-342`：precommit cancellation 路径也投影相同 cancelled result

### AST inventory 收紧确认

`test_production_upload_count_constructors_are_explicit_and_complete` 断言：

- `UploadOperationResult(...)`: 恰 4 点，全部位于 `docling_upload_service.py`（317, 398, 557, 1419）
- `FinsUploadResultSummary(...)`: 恰 4 点（service_runtime:131, service_runtime:305, ingestion_runtime:3945, ingestion_runtime:4423）
- `cls(...)` parser: 1 点

**结论**：✓ 不可达 catch 已删除，cancellation 语义完整保留在 DoclingUploadService，AST inventory 从六点收紧为四点。

## 四点 constructor inventory 逐项验证

| # | 文件 | 行号 | status | stored_file_count | 场景 |
| --- | --- | --- | --- | --- | --- |
| 1 | `docling_upload_service.py` | 317 | skipped | 0 | fingerprint match skip |
| 2 | `docling_upload_service.py` | 398 | deleted | 0 | delete action |
| 3 | `docling_upload_service.py` | 557 | uploaded | stored_original_count | commit 后 publish |
| 4 | `docling_upload_service.py` | 1419 | cancelled | 0 | converter cancelled / precommit cancelled |

四点均显式传入 `stored_file_count`，无 default。✓

## S1 计数真链验证

1. **original-only stored count**：`_store_upload_assets` 仅当 `asset.source == _ASSET_SOURCE_ORIGINAL` 时 `stored_original_count += 1`。✓
2. **commit 后消费**：`publish_prepared_upload` 只在 `commit_prepared_upload_batch` 成功后构造 `stored_file_count=stored_original_count`。✓
3. **durable/direct 同源**：同一 `FinsUploadResultSummary` 投影 `to_json_summary()`（job record）与 `_upload_result_details()`（direct RESULT）。✓

## 状态矩阵验证

`FinsUploadPipelineResult.__post_init__()`（ingestion_runtime:1193-1219）：
- ok: `stored_file_count >= 1` ✓
- skipped/deleted/failed/cancelled: `stored_file_count == 0` ✓
- 非 ok + positive stored: rejected ✓

`FinsUploadResultSummary.__post_init__()`（ingestion_runtime:1294-1320）：
- ok: `requested >= 1`, `stored == requested` ✓
- skipped: `requested >= 1`, `stored == 0` ✓
- deleted/cancelled/failed: `stored == 0` ✓
- skipped + requested=0: rejected（line 1311-1312）✓

## material 仅机械迁移验证

- SEC material：success 路径 `stored_file_count=upload_result.stored_file_count`，failure 路径 `stored_file_count=0` ✓
- CN material：同形 ✓
- `test_upload_material_failure_preserves_existing_user_visible_semantics` 仍通过，未修改 material 行为 ✓

## no-touch 验证

- `git diff --check`：通过
- `rg -n '\buploaded_files\b' dayu tests --glob '*.py'`：零命中
- `DoclingConversionCancelledError` in SEC/CN workflows：零命中
- `_PAYLOAD_FILE_COUNT == "file_count"`：未迁移
- frozen SHA-256：`a357e5a1…efb` / `88b04ca4…70b8`，与 plan §1 一致
- Host、Engine、runtime、config、storage、Service production、CLI、README 均未修改

## 验证复跑记录

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| focused tests | `pytest -q` (6 files) | `286 passed, 3 warnings` |
| pyright | `python -m pyright dayu/ tests/ utils/` | `0 errors, 0 warnings, 0 informations` |
| old-field audit | `rg -n '\buploaded_files\b'` | 零命中 |
| constructor inventory | `rg -n 'UploadOperationResult\('` | 恰 4 点，全部 `docling_upload_service.py` |
| workflow catch | `rg -n 'DoclingConversionCancelledError'` in workflows | 零命中 |
| whitespace | `git diff --check` | 通过 |
| frozen SHA | `shasum -a 256` | 与 plan 一致 |

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- `cn_pipeline.py` 整文件 coverage 69%，remaining missing 为既有未修改的 conversion-error/download/facade/helper 路径。分类为 **assigned to later work unit**，不阻塞 S1。
- S2/S3/UF-PF03 未实施。分类为 **covered by later approved slice**。
- material generic raw failure / company-first publication。分类为 **assigned to later work unit**，本轮仅保留 S1 shared count 机械迁移。
