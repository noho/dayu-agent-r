# Code Review

## Scope

- Mode: all repository
- Branch: wu-cm-12-conversation-memory-drift
- Base: main
- Output file: docs/reviews/repo-review-20260619-165328.md
- Included scope: 全部生产代码、测试、配置、prompt、README；重点关注当前分支 WU-CM-12 conversation memory / compact / smoke 相关改动
- Excluded scope: `.venv/`、`__pycache__/`、`.mypy_cache/`、`.pyright/`、生成文件、`workspace/output/`、`workspace/tmp/`、`workspace/portfolio/`
- Parallel review coverage:
  - Subagent 1: `dayu/host/` 核心改动（compact_material、compaction、compaction_operation、context_events、context_fallback、dispatch、engine_ingest、memory、run_input）
  - Subagent 2: `dayu/engine/` 与 `dayu/contracts/`（agent.py、contracts 层）
  - Subagent 3: `tests/` 测试覆盖与质量（WU-CM-12 相关测试文件）
  - Subagent 4: 全仓架构边界（import 边界、LLM-facing 文本合规性）
  - 未覆盖区域: `dayu/documents/`、`dayu/tools/`（非本次改动焦点，风险低）

## Findings

### 1-未修复-高-`normalized_material_text` 规范化行为变更缺少直接测试覆盖

- **入口/函数**: `normalized_material_text`、`_normalized_material_line`、`_normalized_structured_field_text`
- **文件(行号)**: `dayu/host/compact_material.py` 第 760-778 行
- **输入场景**: 系统需要规范化 previous compacted view 的 material block 文本
- **实际分支**: c3ffa72c 将规范化逻辑从 `" ".join(text.split())`（折叠所有空白包括换行为单空格）改为逐行规范化后用 `\n` 重新拼接
- **预期行为**: 新增核心规范化函数应有直接单元测试覆盖各种边界条件
- **实际行为**: 在整个 `tests/` 目录下搜索 `normalized_material_text`、`_normalized_structured_field_text`、`_normalized_material_line` 无任何匹配。只有 `test_pre_dispatch_second_compact_rolls_from_latest_accepted_candidate` 间接覆盖 happy path（单行文本）
- **直接证据**: `compact_material.py` 第 760-778 行定义了新的规范化逻辑；`grep -r normalized_material_text tests/` 返回零结果
- **影响**: 若规范化行为 regression（如空行过滤、连续空白折叠、纯空白输入抛异常），当前测试套件无法捕获
- **建议改法和验证点**: 为 `normalized_material_text` 补充边界测试：纯空白输入应抛 `ValueError`；多行文本中含空行时应正确过滤；行内连续空白应折叠为单空格；单行超长文本保持完整
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

### 2-未修复-高-multi-item per-type previous view block 拆分缺少 pre_dispatch 端到端测试

- **入口/函数**: `_previous_compacted_view_from_compacted_event` → `_candidate_facts_texts`、`_candidate_answer_anchor_texts`
- **文件(行号)**: `dayu/host/compact_material.py` 第 2484-2511 行（`_candidate_facts_texts`）、第 2751-2771 行（`_snapshot_fact_texts`）；`tests/host/test_compact_material.py` 第 1807 行（`test_pre_dispatch_second_compact_rolls_from_latest_accepted_candidate`）
- **输入场景**: 第二次 compact 时，需要从 accepted compact payload 构造 previous compacted view，每个 semantic item 独立成 block
- **实际分支**: c3ffa72c 将 previous view 从 per-type 单 block（所有 facts 合并为一个 block）改为 per-item 多 block（每个 fact 独立一个 block）
- **预期行为**: 测试应使用包含 2+ facts、2+ anchors 的 compact payload，验证每个 item 独立成 block 且顺序正确、`event_sub_index` 递增
- **实际行为**: `test_pre_dispatch_second_compact_rolls_from_latest_accepted_candidate` 使用的 `_accepted_candidate()` helper 只构造 1 个 fact、1 个 anchor、1 个 intent、1 个 reference，无法验证多 item 拆分正确性
- **直接证据**: `test_compact_material.py` 第 2452 行 `_accepted_candidate()` helper 只构造单 item；断言长度为 5 恰好等于单 item 拆分后的数量
- **影响**: 若 `_candidate_facts_texts` 返回 tuple 时 item 顺序错误、或 `event_sub_index` 计算有误，当前测试无法捕获
- **建议改法和验证点**: 补充 pre_dispatch 端到端测试，使用 2+ facts、2+ anchors 的 compact payload，验证 `view.previous_compacted_view` 中每个 item 独立成 block 且顺序正确
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

### 3-未修复-中-`normalized_material_text` 对已有 material 的 digest 漂移风险

- **入口/函数**: `normalized_material_text`
- **文件(行号)**: `dayu/host/compact_material.py` 第 760-778 行
- **输入场景**: 包含换行的已有 material 文本需要规范化
- **实际分支**: 旧行为 `" ".join(text.split())` 将 `"\n"` 也视为空白折叠为单空格；新行为逐行规范化后用 `"\n"` 重新拼接
- **预期行为**: 对于 `line1\nline2`，旧结果是 `"line1 line2"`，新结果是 `"line1\nline2"`。所有依赖 `content_digest` 的下游（segment selection digest、material pack digest、compact candidate digest）会产生不同值
- **实际行为**: 若系统重启后需要从旧 EventLog payload 重新构造 previous compacted view 的 material blocks，其 `content_digest` 会与旧 compact candidate 中记录的 `content_digest` 不一致
- **直接证据**: `compact_material.py` 第 760-778 行规范化逻辑变更；`content_digest` 由 `normalized_material_text` 输出计算
- **影响**: duplicate section owner guard 可能误判。不过由于 previous compacted view 的 blocks 来自 snapshot 或 compact candidate payload 而非原始文本重新规范化，风险受控
- **建议改法和验证点**: 确认所有构造 `RunInputMaterialBlock` 的路径的输入文本是否已经处于规范化后状态；确认 snapshot 反序列化路径是否也经过此规范化函数
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 4-未修复-中-Engine 内部 `RuntimeError` 被误归类为 `runner_exception`

- **入口/函数**: `_run_runner_iteration` → `_consume_runner_event`
- **文件(行号)**: `dayu/engine/agent.py` 第 1161-1198 行（except 块）、第 1213-1214 行（RuntimeError 抛出点）
- **输入场景**: Engine 迭代状态未初始化时 `_consume_runner_event` 抛出 `RuntimeError`
- **实际分支**: `except Exception as exc` 捕获所有异常，统一归类为 `_ERROR_RUNNER_EXCEPTION`
- **预期行为**: Engine 内部逻辑错误（迭代状态未初始化）应使用与 Runner / provider 异常不同的 error_code
- **实际行为**: `RuntimeError("iteration state is not initialized")` 被归类为 `runner_exception`，误导 Host 的诊断和重试策略
- **直接证据**: `agent.py` 第 1213-1214 行 `raise RuntimeError("iteration state is not initialized")`；第 1163-1198 行 `except Exception as exc` 统一使用 `_ERROR_RUNNER_EXCEPTION`
- **影响**: Host 会认为是 provider 侧问题而非 Engine 内部 bug，可能导致错误的重试策略
- **建议改法和验证点**: 在 `except Exception` 块中区分 `RuntimeError`（内部逻辑错误）与 Runner / provider 异常，使用不同 error_code
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 5-未修复-中-`CONTEXT_COMPACTION_ATTEMPT_REJECTED` 在 memory projection 中无过滤行为测试

- **入口/函数**: `project_conversation_memory_event`
- **文件(行号)**: `dayu/host/durable/memory.py` 第 96-101 行（`_EVENT_TYPE_FILTER`）；`tests/host/test_memory_projection.py`
- **输入场景**: `CONTEXT_COMPACTION_ATTEMPT_REJECTED` 事件到达 memory projection consumer
- **实际分支**: `_EVENT_TYPE_FILTER` 只包含 `USER_INPUT_ACCEPTED`、`RUN_SUCCEEDED`、`TOOL_RESULT_ACCEPTED`、`CONTEXT_COMPACTED`，不包含 `CONTEXT_COMPACTION_ATTEMPT_REJECTED`
- **预期行为**: 应有测试验证 attempt rejected 事件被 projection consumer 正确忽略（不 crash、不写入 snapshot、不写入 diagnostic）
- **实际行为**: `test_memory_projection.py` 中没有针对 `CONTEXT_COMPACTION_ATTEMPT_REJECTED` 的过滤测试。虽然有 `test_projection_consumer_skips_failed_compact_without_memory_snapshot` 测试 `CONTEXT_COMPACTION_FAILED`，但 `CONTEXT_COMPACTION_ATTEMPT_REJECTED` 是不同的事件类型且是 WU-CM-12 新增的
- **直接证据**: `memory.py` 第 96-101 行 `_EVENT_TYPE_FILTER` 定义；`grep -r ATTEMPT_REJECTED tests/host/test_memory_projection.py` 返回零结果
- **影响**: 若 `_EVENT_TYPE_FILTER` 意外包含 `CONTEXT_COMPACTION_ATTEMPT_REJECTED`，或 `project_conversation_memory_event` 对未覆盖事件类型的行为改变，当前测试无法捕获 regression
- **建议改法和验证点**: 补充测试验证 `CONTEXT_COMPACTION_ATTEMPT_REJECTED` 事件不进入 memory projection
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 6-未修复-中-compaction operation 中 `previous_compacted_view` parse failure 的 diagnostic 只覆盖 `reference_continuity` 一种 failure

- **入口/函数**: `_previous_compacted_view_from_compacted_event`
- **文件(行号)**: `tests/host/test_compaction_operation.py` 第 910-1007 行（`test_rejected_attempt_diagnostic_captures_invalid_previous_reference`）
- **输入场景**: previous compacted view 的各 section 发生 parse failure
- **实际分支**: 测试只验证 `_request_with_previous_reference_continuity(raw_text)` 构造的 reference continuity parse failure
- **预期行为**: `_candidate_facts_texts`、`_candidate_answer_anchor_texts`、`_candidate_forward_intent_texts` 的 parse failure 也应有对应的 diagnostic 测试
- **实际行为**: 只有 reference_continuity 的 parse failure 被测试覆盖
- **直接证据**: `test_compaction_operation.py` 第 910-1007 行只测试 reference_continuity parse failure
- **影响**: 若其他 previous view section 的 parse failure 未被正确捕获或 diagnostic 信息不完整，当前测试无法发现
- **建议改法和验证点**: 至少为 fact claim_text parse failure 和 anchor title parse failure 各补充一个 diagnostic 验证测试
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 7-未修复-中-dispatch scheduler recovery tier 测试不验证 `previous_compacted_view` 中 block 的 per-item 结构

- **入口/函数**: `_proactive_compaction_recovery_attempts`
- **文件(行号)**: `tests/host/test_dispatch_scheduler.py` 第 4700-4754 行（`test_proactive_compaction_recovery_tier1_uses_fallback_caps`）
- **输入场景**: proactive compaction recovery tier 1 执行后验证 compacted payload
- **实际分支**: 测试验证 `compacted_payload["accepted_attempt_number"] == 2` 和 `CONTEXT_COMPACTED` 事件写入，但不检查 `accepted_candidate` 的 `evidence_backed_facts` 是否正确对应 per-item block
- **预期行为**: 至少一个 recovery tier 测试应增加对 `accepted_candidate` 内容的断言（如 fact 数量、claim_text 非空）
- **实际行为**: tier 2 和 tier 3 测试只验证 event sequence 和 event types，不验证 material pack 内容
- **直接证据**: `test_dispatch_scheduler.py` 第 4700-4754 行只验证 event type 和 sequence，不验证 payload 内容
- **影响**: 若 recovery path 中 previous view block 拆分出错，当前测试无法捕获
- **建议改法和验证点**: 在至少一个 recovery tier 测试中增加对 `accepted_candidate` 内容的断言
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 8-未修复-中-Fins 对 Host 的反向依赖

- **入口/函数**: `FinsIngestionWaitPollAdapter`
- **文件(行号)**: `dayu/fins/ingestion/wait_adapter.py` 第 44、51、52 行
- **输入场景**: Fins ingestion 需要将 observation handle 适配到 Host 的 wait-resume 等待契约
- **实际分支**: `dayu/fins/ingestion/wait_adapter.py` 直接 import `dayu.host.api`、`dayu.host.durable.state`、`dayu.host.wait_adapter`
- **预期行为**: Fins 不应依赖 Host 实现；wait adapter 的接口类型应定义在层中立契约包中
- **实际行为**: Fins 通过 `FinsIngestionWaitPollAdapter` 直接依赖 Host 的 wait adapter 类型（`WaitAdapterBinding`、`WaitAdapterRegistry`、`WaitPollResult` 等）
- **直接证据**: `wait_adapter.py` 第 44 行 `from dayu.host.api import (ResolveWaitCancelledOutcome, ..., WaitAdapterKey)`；第 51 行 `from dayu.host.durable.state import WaitRecordRow, WaitResumePolicy`；第 52 行 `from dayu.host.wait_adapter import (WaitAdapterBinding, WaitAdapterRegistry, ...)`
- **影响**: 违反分层架构约束；Fins 和 Host 被绑定为必须同步修改的整体
- **建议改法和验证点**: wait adapter 的接口类型应提取到 `dayu.contracts` 或新的 `dayu.contracts.wait_adapter` 中
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 9-未修复-中-`_parse_previous_forward_intent_text` 和 `_parse_previous_reference_continuity_text` 的 `"; "` 分隔符脆弱性

- **入口/函数**: `_parse_previous_forward_intent_text`、`_parse_previous_reference_continuity_text`
- **文件(行号)**: `dayu/host/compact_material.py` 第 3509-3555 行
- **输入场景**: LLM 生成的 compact candidate 中 `text`、`intent_type`、`status`、`reason` 等字段值包含 `"; "`
- **实际分支**: `text.split("; ")` 依赖 `"; "` 作为字段分隔符
- **预期行为**: 分隔符不应出现在自然语言字段值中，或应有转义机制
- **实际行为**: `_normalized_structured_field_text` 只做行内空白折叠，不消除 `"; "` 本身。`intent_type` 和 `status` 是枚举值不太可能含 `"; "`，但 `text` 字段来自 LLM 输出理论上可能包含
- **直接证据**: `compact_material.py` 第 3509 行 `parts = text.split("; ")`；第 3510 行 `if len(parts) != 3: raise ValueError(...)`
- **影响**: LLM 输出含 `"; "` 时解析失败。这是旧 debt，非本次回归
- **建议改法和验证点**: 考虑在 `_normalized_structured_field_text` 中对 `"; "` 做转义或替换，或改用不会出现在自然语言中的分隔符
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 10-未修复-低-`_CompactorProposalExecutionError` 是 dataclass 但被用作 Exception

- **入口/函数**: `_CompactorProposalExecutionError`
- **文件(行号)**: `dayu/host/compaction_operation.py` 第 544-553 行
- **输入场景**: compactor proposal 执行失败时抛出该异常
- **实际分支**: `@dataclass(frozen=True, slots=True)` 同时继承 `Exception`
- **预期行为**: `str(exc)` 应返回有意义的错误消息
- **实际行为**: dataclass 的 `__init__` 覆盖 `Exception.__init__`，`str(exc)` 返回 dataclass 的默认 `__repr__` 而非有意义的消息
- **直接证据**: `compaction_operation.py` 第 544-553 行定义；`_safe_exception_message` 使用 `str(exc)` 并截断
- **影响**: 日志中异常消息不具诊断价值，但不会崩溃
- **建议改法和验证点**: 为 `_CompactorProposalExecutionError` 添加 `__str__` 或 `__post_init__` 中调用 `super().__init__(...)` 传递有意义的消息
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 11-未修复-低-`compact_material.py` 的 `_candidate_facts_texts` / `_snapshot_fact_texts` 对 evidence_refs 去重行为差异

- **入口/函数**: `_candidate_facts_texts`、`_snapshot_fact_texts`
- **文件(行号)**: `dayu/host/compact_material.py` 第 2484-2511 行、第 2751-2771 行
- **输入场景**: snapshot 中的 `evidence_refs` 存在重复时
- **实际分支**: `_candidate_facts_texts` 通过 `_required_json_text_tuple` 内置 `dict.fromkeys` 去重；`_snapshot_fact_texts` 直接使用 `fact.evidence_refs` 未去重
- **预期行为**: snapshot path 和 candidate path 应产出相同的 fact 文本
- **实际行为**: 若 snapshot 中 `evidence_refs` 存在重复，两条路径会产出不同的 fact 文本，导致 content_digest 不一致
- **直接证据**: `compact_material.py` 第 2498-2501 行（candidate path，内置去重）vs 第 2761-2764 行（snapshot path，未去重）
- **影响**: content_digest 不一致可能导致 duplicate section owner guard 误判。实际影响取决于上游是否保证无重复
- **建议改法和验证点**: 在 `_snapshot_fact_texts` 中也对 `evidence_refs` 做去重，或确认上游保证无重复
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 12-未修复-低-`_make_failed_or_cancelled_terminal_with_close` 命名与行为不一致

- **入口/函数**: `_make_failed_or_cancelled_terminal_with_close`
- **文件(行号)**: `dayu/engine/agent.py` 第 2148-2160 行
- **输入场景**: Engine 需要创建 failed 或 cancelled terminal event
- **实际分支**: 方法名含 `_with_close`，但走 `_make_terminal_failed` 路径时不关闭 Runner
- **预期行为**: 方法名应准确反映行为，或方法应显式关闭 Runner
- **实际行为**: Runner 关闭依赖 `run_messages` 的 `finally` 块，功能正确但命名有误导性
- **直接证据**: `agent.py` 第 2148-2160 行；`_make_cancelled_terminal_with_close`、`_make_tool_timeout_terminal_with_close`、`_make_suspended_terminal_with_close` 这三个确实关闭 Runner
- **影响**: 可能误导维护者，但功能正确无资源泄漏
- **建议改法和验证点**: 重命名为 `_make_failed_or_cancelled_terminal`，或在非取消路径也显式关闭 Runner
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 无

## Residual Risk

- `dayu/documents/`、`dayu/tools/` 未在本次 review 中深入覆盖，因为它们不是当前分支 WU-CM-12 改动的焦点
- `dayu/service/` 的 host_assembly 和 fins_direct 未深入 review，但已验证其 import 边界合规
- `dayu/config/prompts/` 的 LLM-facing 文本合规性已由 subagent 4 初步覆盖，发现多处内部术语泄漏（smoke scene prompt、tools.md、conversation_compaction_user.md、agents.md），但这些属于 prompt 质量问题而非代码 correctness 问题，未单独列为 finding
- `dayu/engine/contracts/` 中的共享类型（`AgentPolicy`、`RunnerSpec`、`EngineEvent`、`AgentRunRequest` 等）被 Host 8 个文件 import，但这是正确的依赖方向（Host → Engine），不构成架构违规。AGENTS.md 明确将 `dayu.engine.contracts` 定义为"Engine 专属契约和函数式执行入口，定义 Host -> Engine 的单次 run contract"。是否将这些共享类型迁移到 `dayu.contracts` 是设计演进问题，不是当前架构违规
