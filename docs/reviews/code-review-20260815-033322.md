# Code Review — upload-filing-ticker-alias-contract aggregate deepreview re-review

## Scope

- Mode: current changes（aggregate deepreview re-review；review 对象为上一轮 aggregate review 四项 finding 的未提交修复）
- Branch: `codex/upload-filing-oracle`
- Base of this pass: `2b11cb21`（accepted S2）→ 未提交 worktree diff（26 个文件，+933/−345）
- Output file: `docs/reviews/code-review-20260815-033322.md`
- 输入：完整读取上一轮 artifact `code-review-20260815-023958.md`、`wu-upload-filing-ticker-alias-contract-s2-fix-codex.md`（含 §9 aggregate fix addendum）、accepted plan 与全部 controller adjudications。
- Included scope：HEAD..worktree 全部生产改动（14 个生产文件）与全部测试改动（12 个测试文件）逐 hunk 走读；并复核被改动边界的既有调用链。
- Excluded scope：无生产改动被排除；`code-review-20260815-023308.md`（另一 agent 产物）不在本 pass 范围。
- 验证方式：主 reviewer 直接走读全部 diff + 独立复跑测试/pyright/coverage/residue。

## 验证结论（独立复现）

- `coverage run --branch -m pytest -q tests/fins tests/tools/test_combined_tools_acceptance.py tests/cli/test_fins_commands.py` → **1753 passed, 1 skipped**（与 fix addendum §9.6 声称一致；首跑即全绿，未复现 addendum 提到的偶发 flake）。
- `pytest -q tests/fins/test_company_identity_storage_contract.py tests/fins/test_fins_storage_atomicity.py` → 190 passed。
- 全量 `python -m pyright dayu/ tests/ utils/` → **0 errors**。
- 逐文件 branch coverage（本次生成数据）：14 个修改生产文件全部 `>=81%`，与 addendum 表格一致（`fins.py=81%`、`sec_pipeline.py=81%`、`_fs_company_meta_core.py=82%`、`docling_upload_service.py=84%`、`_fs_storage_infra.py=85%`、`sec_downloader.py=88%`、`_fs_filing_upload_state_core.py=100%` 等）。
- residue 独立扫描：`SecDownloader.normalize_ticker` / downloader、workflow 层 `.normalize_ticker(...)` proxy 零命中；ticker `strip().upper()` fallback 零命中（残留 `strip().upper()` 均为财期/表单名/小节名等非 ticker 业务字段）；`resolve_existing_ticker` 零命中；storage `_company_meta_path(` 零命中（仅测试文件内有一个同名无关 helper）；全部 `.normalize_ticker(` 消费者均为公共 owner `ticker_normalization.normalize_ticker`。
- `git diff --check` 通过；本轮无 README 变更（见 Finding 1 的 README 触发说明）。

## Findings

### 1-未修复-低-根 README 未同步 upload_material 新增的启动前 usage 校验行为
- **入口/函数**: `FinsIngestionRuntime._normalize_upload_request` → `_admit_fins_upload_ticker_identity`；CLI `run_fins_direct_command` 的 `FinsUploadUsageError` 分支
- **文件(行号)**: `dayu/fins/ingestion_runtime.py:7227`（material admission 调用点）；`dayu/cli/commands/fins.py:195-196`（usage 前缀改用实际 command name）；根 `README.md:316`（usage 校验段落只点名 `upload_filing`）
- **输入场景**: 用户在 `upload_material --ticker "Apple Inc."` 或 `start_fins_upload(upload_kind=material, ticker_aliases=[非法项])` 时。
- **实际分支**: 本轮修复后，material 的非法 ticker/alias/超量 aliases 在 observation/job 创建前被共享 usage owner 拒绝：CLI 退出码 2、单行具体原因（"dayu-cli upload_material: ..."）；tool 侧 `invalid_argument` + bounded usage 文案。这是相对 base 的用户可见行为变化（base 上 material 非法 ticker 会进入 job 并落为 `unexpected_runtime`）。
- **预期行为**: 按 AGENTS.md README 触发规则，用户可见参数校验与排障方式变化应检查并更新根 README；README:310 已有一般性说明"允许上传的文件后缀和每个 action 的必填字段由命令在执行前校验"，但 README:316 的 usage 校验段落只点名 `upload_filing`，未把 material 纳入同一"启动前校验、可修正输入退出 2"的表述。
- **实际行为**: fix addendum §9.5 判定"不机械更新 README"，未同步该用户可见行为。
- **直接证据**: `README.md:316` 原文只含 `upload_filing`；`test_upload_material_alias_count_uses_typed_upload_admission`（CLI 测试）证明 material usage 拒绝行为已落地。
- **影响**: 最终用户手册对 material 的排障说明滞后于实现；无正确性影响。
- **建议改法和验证点**: 在 README:316 段把 `upload_filing` 泛化为 upload_filing/material 的启动前 usage 校验（或补一句 material 同样在启动前拒绝非法 ticker/别名）；不引入内部术语。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## 上一轮 Finding 逐项复核（全部闭环）

### F1（原中，blocker）durable corruption 唯一 typed 投影 — 已修复，闭环
- **storage owner 统一分型**：`_get_company_meta_unguarded` 改经显式 `os.lstat` + `_read_published_company_identity`（`_fs_company_meta_core.py:71-90`），descriptor/meta schema/symlink/non-regular/mismatch 分别进入 closed corruption kind；合法 meta-less 仍 `FileNotFoundError` 不被误判。`read_filing_upload_state` 对 target symlink/regular file 直接 `invalid_descriptor`（`_fs_filing_upload_state_core.py:86-103`）。`begin_batch`（`:549`）、commit backup 判定（`:780`）、`_require_copyable_ticker_tree`（`:2718-2740`）、`_ticker_dir_for_read`（`:3175-3200`）全部改抛 `CompanyTickerIdentityCorruptionError`，首次 replace 前 fail closed，未动 publication/recovery 锁序。
- **真实路径投影逐入口验证**：tool 的 typed branch 现在由真实 prepare 链可达——新测试 `test_upload_tool_projects_real_workspace_identity_corruption`（5 种真实 corruption fixture，删除 monkeypatch helper）断言 `fins_upload_start_failed` + bounded message/hint、无 "CompanyMeta"/"ticker_aliases" 内部原文、零 durable job；`test_start_upload_projects_real_corrupt_company_meta_before_job_creation` 断言真实 malformed meta 在 start 边界透出 `invalid_meta` 且 job store 未创建；CLI 测试 `test_upload_filing_prevalidation_identity_corruption_is_typed_and_path_free`（6 种 fixture）断言单行 bounded 输出无路径/traceback/内部类型；material 测试 `test_upload_material_identity_corruption_projects_storage_terminal`（5 种 fixture）断言 failed terminal 为 exact `storage/storage_io` 文案、`stored_file_count=0`、无 source publication。SEC/CN material workflow 的 failure boundary 前移到 published meta 观察之前（`sec_upload_workflow.py:455-468`、`cn_pipeline.py:1070-1088`），统一经 `fins_upload_failure_from_exception` 投影。
- 已核实无 `invalid_argument`/`unexpected_runtime`/内部 schema 泄露残留路径。注意：同一 corruption 在 tool 与 CLI 的文案不同（"工作区公司代码身份数据损坏，上传任务未启动。" vs "上传状态已损坏，请检查工作区存储状态"），但两者均为 typed、bounded、path-free 的 corruption 投影，属不同入口自有 bounded 文案，不构成语义漂移。

### F2（原中，blocker）material/filing 共用准入 + loose fallback 清零 — 已修复，闭环
- **共用准入**：`_admit_fins_upload_ticker_identity`（`ingestion_runtime.py:862-905`）只拥有数量边界（`<=100`）与 typed usage 投影，grammar/归一化/去重只委托公共 `normalize_ticker` 与 `build_company_ticker_identity`；filing static validation（`:919`）与 material normalization（`:7227`）都消费它。material 非法 ticker/alias/超量在 observation/job 创建前分别投影 `INVALID_TICKER`/`INVALID_TICKER_ALIAS`/`TOO_MANY_TICKER_ALIASES`——测试 `test_material_upload_reuses_ticker_identity_admission_before_job_creation`（零 executor operations、零 jobs）、`test_upload_tool_material_ticker_identity_usage_is_bounded_and_typed`（exact 文案）、`test_upload_material_alias_count_uses_typed_upload_admission`（CLI）均通过。
- **fallback 清零**：`SecDownloader.normalize_ticker` 及其 `strip().upper()` fallback 删除（`sec_downloader.py`）；`resolve_company` 与 `run_download_stream_impl`/`run_upload_material_stream` 直接消费公共 `normalize_ticker(ticker).canonical`；browse-edgar 私有 fallback 移除且只接收已 canonical 输入；`DoclingUploadService._normalize_ticker` 删除、三个调用点改公共 owner；`SecUploadWorkflowHost._downloader` property 与 `_DownloadWorkflowDownloader.normalize_ticker` protocol 声明删除；测试 stub 的重复 `normalize_ticker` 实现同步删除。repo-wide residue 扫描确认无任何 ticker loose fallback 残留，所有 `.normalize_ticker(` 消费公共 owner。
- CLI usage 前缀改用实际 command name（`fins.py:196`），material 不再误报为 filing。

### F3（原低）zero-mutation 下载 rollback — 已修复，闭环
- SEC `upsert_company_meta` 与 CN `stage_company_meta_for_cn_download` 以 `CompanyMetaCommitIntent | None` 作为明确 mutation signal（`sec_company_meta.py:86-130`、`cn_download_company_meta.py:70-71`）；`_publish_sec_post_repair_mutations`（`sec_download_workflow.py:813-826`）与 `_publish_cn_company_after_repair`（`cn_download_workflow.py:430-443`）在 intent 为 `None` 时 rollback，只在存在 intent 时 commit。storage 不猜测 mutation、无新增 durable 字段或锁。
- 测试 `test_repeat_sec_company_publication_rolls_back_zero_mutation_batch` 与 CN 同名测试：首次 publication commit 一次、重复 publication 只 rollback 一次（phases 为 begin→rollback）、published `meta.json` bytes 不变。失败路径（`except BaseException → rollback → raise`）保持不变：company batch 与 document/registry mutation 的 batch 相互独立，失败/registry/document mutation 不被误回滚；异常仍向上传播。

### F4（原低）死代码 — 已修复，闭环
- `_FsStorageInfra._company_meta_path(...)` 已删除（`_fs_storage_infra.py`）；staging CompanyMeta locator 唯一真实写入点仍是 `_prepare_company_identity_commit` 的内联 `staging_ticker_dir / _SOURCE_META_FILENAME`；`_company_meta_path_for_read` 是不同 read locator contract，按上轮建议保留。residue 扫描零命中。

## 并发/锁序/recovery 回归核对

- 锁图未变：本轮没有新增锁获取点或释放点；`begin_batch` 的 typed 判定仍发生在 same-ticker writer 持有之后、staging 创建之前；`_read_published_company_identity` 的复用没有改变 identity → sorted publication 的 read 锁序；recovery 路径未动。
- `_require_copyable_ticker_tree` 改显式 lstat 后语义与原有 is_symlink/is_file 判定等价，仅把 ValueError 收口为 typed corruption，不引入新 I/O 顺序。
- `read_filing_upload_state` 的 outer lstat 只是"corpus 不存在"快速路径；authoritative descriptor/meta 读取仍在 publication guard 内完成（guard 内二次 lstat），不存在 TOCTOU 语义漂移。

## 语义 ownership / 过度耦合 / LLM-facing 复核

- 语义 owner 未漂移：corruption 分型统一收敛到 storage 的 `_read_published_company_identity` 与显式 lstat helper；usage 准入收敛到 `_admit_fins_upload_ticker_identity`；grammar 仍唯一属于 `ticker_normalization`。没有新增下游 catch、字符串解析或重算。
- 过度耦合：无新增反向依赖；`_fs_company_meta_core`/`_fs_filing_upload_state_core` 复用 `_read_published_company_identity` 是同一 storage owner 内部复用，不穿透层边界；pipeline 只消费 `CompanyMetaCommitIntent | None` 信号。
- LLM-facing：所有新增 projection 文案 bounded、path-free、不含内部类型名/schema 术语（测试显式断言 "CompanyMeta" / "ticker_aliases 必须" 不出现）；tool/CLI material usage 文案与 filing 同源模板。
- 测试有效性：上一轮的 monkeypatch 测试已替换为真实 workspace corruption fixture；zero-mutation 测试用真实 workflow + 真实 storage（spy 只计数）；所有新断言面向 owner/durable state（job store、published meta bytes、terminal failure JSON），无 event-payload 代替 durable 证据。
- scope exclusions 保持：未触碰 UF-PF05、oracle/scenario registry、冻结 evidence、Host/Engine、material 缺 company-name（controller 已 defer）、旧 schema 迁移。

## Open Questions

- 无阻塞性问题。`_validate_runtime_upload_request` docstring 的 Raises 仍写"ValueError: material request 非法或 published state 损坏"——typed corruption 是 ValueError 子类故字面仍真，但建议后续把 `CompanyTickerIdentityCorruptionError` 显式写进 docstring（纯文档精度，不作为 finding）。
- `begin_batch` 现在对既有 corpus 也 strict 读取 CompanyMeta：corrupt meta 的 document-only commit 从"允许 swap"收窄为"typed 拒绝"。该行为与 commit-time 既有 fail-closed（`_read_current_company_meta_for_commit`）一致，且 corrupt meta 本就无法通过正常 intent 修复（commit 读同样失败）、属 operator 手动修复契约，故判定为一致性收窄而非回归；若 controller 认为 document-only 修复路径需要豁免，可另立裁决。

## Residual Risk

- 空 alias 复用 `EMPTY_TICKER` 文案（pre-existing，本 pass 未动）；NOT_FOUND 错误文本无界回显（pre-existing）。
- SEC upload/download/CN download resolver_version 字面量不一致（controller 已分类 later work unit）。
- ACL/NFS 真实外部文件系统行为未做跨平台验证（errno 注入已覆盖分型逻辑）。
- 本轮未运行 Host/Engine 全仓非 Fins 测试（本 pass 未修改 Host/Engine）。
- `test_company_identity_storage_contract.py` 新增的 20 行改动为既有用例的 typed-kind 断言增强，已包含在 190 passed 独立复跑中。

## 结论

**PASS**。上一轮两项 blocker（durable corruption 多入口投影、material 准入不对称与 loose fallback 残留）与两项 low finding（zero-mutation swap、死代码）均已在正确 owner boundary 闭环，且由真实路径测试（非 monkeypatch）、独立复现的全量验证（1753 passed / pyright 0 errors / 逐文件 coverage ≥81% / residue 零命中）支撑；并发/锁序/recovery、语义 ownership、LLM-facing 与 scope exclusions 复核无新增问题。

- **blocker：无。**
- 唯一 non-blocking 低 severity finding：根 README 未同步 upload_material 新增的启动前 usage 校验行为（Finding 1），建议 controller 裁决 accept（补一句 README）或 defer；不影响 final closeout 的代码正确性。
