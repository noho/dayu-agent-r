# Code Review (Re-review)

## Scope

- Mode: current changes (re-review gate)
- Branch: wu-cm-12-conversation-memory-drift
- Base: main
- Output file: docs/reviews/code-review-20260619-144239.md
- Re-review of: `docs/reviews/code-review-20260619-143711.md` (AgentMiMo F1-F5), `docs/reviews/code-review-20260619-143853.md` (AgentDS F1-F3)
- Adjudication artifact: `docs/reviews/wu-cm-compact-rejected-diagnostic-code-review-adjudication-20260619-144111.md`
- Included scope: `dayu/host/compaction_operation.py` ordinal boundary fix; adjudication decisions on all 8 original findings
- Excluded scope: recovery-tier production audit semantics (deferred to later work unit); orphan artifact lifecycle (deferred to existing storage maintenance model); helper extraction (rejected)
- Parallel review coverage: 无

## Findings

### Original Finding Status Summary

| 原 Finding | 原严重程度 | 当前状态 | 处置 |
|---|---|---|---|
| AgentMiMo F1 - Recovery tier rejected attempts not written to EventLog | 高 | 未修复 | deferred-with-owner |
| AgentMiMo F2 - Recovery tier missing tests | 中 | 未修复 | deferred-with-owner |
| AgentMiMo F3 - Session stale recovery result protection | 中 | 未修复 | deferred-with-owner |
| AgentMiMo F4 - Completed attempt count readability | 低 | 未修复 | deferred-with-owner |
| AgentMiMo F5 - Recovery tier operation_id attribution | 低 | 未修复 | deferred-with-owner |
| AgentDS F1 - Orphan diagnostic artifact after SQL rollback | 中 | 未修复 | deferred-with-owner |
| AgentDS F2 - Duplicated diagnostic helpers | 低 | 未修复 | rejected-with-reason |
| AgentDS F3 - Offending block ordinal boundary guard | 低 | 已修复 | accepted |

### 1-已修复-低-`_offending_block_artifact_json` ordinal 边界校验

- **入口/函数**: `_offending_block_artifact_json()`
- **文件(行号)**: `dayu/host/compaction_operation.py:1802-1806`
- **原 finding**: AgentDS F3，无保护索引访问 `previous_blocks[offending_block.block_ordinal]` 可能导致 IndexError 穿透到 `_proposal_failure_diagnostic()` 的兜底异常处理，丢失整个 diagnostic
- **修复内容**: 新增两行守卫：`if offending_block.block_ordinal < 0 or offending_block.block_ordinal >= len(previous_blocks): return None`
- **验证**: 
  - 守卫检查负数（防止 Python 负索引从尾部回绕）和越界（`>=` 而非 `>`，对 0-based 索引正确）
  - `return None` 使 artifact JSON 中 `offending_block` 字段为 `null`，但 `CompactionRejectedAttemptDiagnostic.offending_block` 仍保留 locator 结构（`failure_stage`、`parser_or_validator`、`offending_block_kind` 等摘要字段仍在 EventLog payload 和日志中可用），仅丢失 artifact 内的 raw text
  - 87 tests 全部通过
- **直接证据**: 行 1802-1806 的显式守卫；行 1800-1801 的 `if offending_block is None: return None` 先检查，守卫作为第二层防御
- **残余风险**: 无直接测试覆盖该守卫分支（`grep` 未找到 ordinal 越界的测试用例）。建议在后续 work unit 的测试补充中增加：构造一个 `CompactionRejectedAttemptOffendingBlock` 使其 `block_ordinal` 超出 `previous_blocks` 长度，断言 `_offending_block_artifact_json` 返回 `None`
- **严重程度（低）**: 修复正确且安全

### 2-未修复-中-Orphan diagnostic artifact 文件（AgentDS F1）— deferred 合理

- **原 finding**: AgentDS F1，`write_compaction_rejected_attempt_diagnostic_artifact()` 中 artifact 文件写入先于 SQL descriptor 写入，事务回滚后产生孤儿文件
- **Adjudication decision**: `deferred-with-owner`，owner 为 existing storage maintenance / artifact orphan model
- **Judgment**: **合理**。Plan 已将此列为 residual risk："Artifact file orphan after SQL rollback: tracked by existing artifact maintenance model." 当前设计确保 descriptor 和 EventLog 共享同一 SQLite 事务（避免 descriptor-without-event），文件级孤儿是 `LocalArtifactStore` 的已有 tradeoff，不是本 slice 引入的新问题。现有 `DurableCompactorProposalManifestRecorder`（`compaction_operation.py:231-374`）使用完全相同的先写文件后写 SQL 模式，diagnostic 路径仅复用了同一模式
- **直接证据**: `compaction_operation.py:980-982`（diagnostic）与 `compaction_operation.py:290-297`（proposal manifest）使用相同的 `write_artifact_bytes()` → `write_payload_descriptor_for_artifact()` 调用顺序

### 3-未修复-低-Duplicated diagnostic helpers（AgentDS F2）— rejected 合理

- **原 finding**: AgentDS F2，`dispatch.py:4482-4557` 与 `engine_ingest.py:6251-6326` 的 6 个 `_diagnostic_offending_*` helper 逐字重复
- **Adjudication decision**: `rejected-with-reason`，理由：提取会扩大公共 helper surface（或引入跨模块私有依赖），收益小于风险
- **Judgment**: **合理**。6 个 helper 各为 3-4 行纯投影函数（`None if reference is None else reference.diagnostic.offending_block` → `None if offending is None else offending.<field>`），不依赖事务或 I/O。提取到 `compaction_operation.py` 需要将它们加入 `__all__` 或改为非下划线前缀的模块级函数（扩大了公共 API），或者在 `compaction_operation.py` 中作为私有函数导入到 dispatch/engine_ingest（跨层私有 import）。在当前只有两处调用的规模下，维护成本低于额外抽象成本。若出现第三处调用，则提取的收益会超过成本
- **直接证据**: `dispatch.py:4482-4557` 与 `engine_ingest.py:6251-6326` 的函数体完全相同，均访问 `CompactionRejectedAttemptDiagnosticReference` 的 frozen dataclass 字段

### 4-未修复-高至低-Recovery tier findings（AgentMiMo F1-F5）— deferred 合理

- **原 findings**: AgentMiMo F1 (高), F2 (中), F3 (中), F4 (低), F5 (低)，均关于 `dispatch.py` recovery tier 的 rejected attempt EventLog 缺失、测试缺失、stale result、attempt numbering、operation_id 归属
- **Adjudication decision**: 全部 `deferred-with-owner`，owner 为后续 recovery-tier compact audit diagnostics work unit
- **Judgment**: **合理**。这 5 个 findings 的证据链是真实的——`_execute_proactive_compaction()` recovery loop（`dispatch.py:1343-1376`）确实不写入 recovery tier 的 rejected attempts，transaction 内只遍历 `result.rejected_attempts`（初始 operation）。但修复这些 findings 跨越了从 "diagnostic artifact 字段定义与持久化" 到 "recovery-tier audit 语义" 的边界。修复需要：
  - 定义跨 tier 的 attempt numbering 语义（recovery tier 的 attempt_number 如何累加）
  - 决定 `CONTEXT_COMPACTION_FAILED` payload 的 `attempt_count` 是否计入 recovery tier
  - 处理 stale-result 场景下 recovery tier rejected attempts 的写入
  - 补充 recovery tier 全路径测试
  - 这些决策会影响 `_execute_proactive_compaction()` 的事务结构和 `_append_compaction_failed_event()` 的语义，超出当前 narrow slice

  当前 slice 的边界是：*为已写入的 `CONTEXT_COMPACTION_ATTEMPT_REJECTED` 事件增加 diagnostic artifact 字段*。Recovery tier 的 rejected attempts 是否/如何写入 EventLog 是独立问题，应在后续 work unit 统一处理。

  **唯一需要注意的交叉点**：若 recovery tier audit work unit 修改了 `CompactionAttemptRejected` 的结构（例如增加 `recovery_tier_reason` 字段），当前 diagnostic artifact 的 `write_compaction_rejected_attempt_diagnostic_artifact()` 调用不受影响——它只读取 `rejected.diagnostic`（`CompactionRejectedAttemptDiagnostic | None`），不依赖 recovery-tier 元数据。EventLog payload builder `build_context_compaction_attempt_rejected_payload()` 同样独立于 recovery context。**当前 slice 与 deferred work unit 之间无耦合阻塞**。

- **直接证据**: `dispatch.py:1401` 只遍历 `result.rejected_attempts`；recovery loop 行 1343-1376 不收集 rejected attempts；plan scope 明确排除 recovery-tier behavior changes

## Open Questions

- 无。

## Residual Risk

- **Ordinal 守卫无直接测试**: Fix 在 `_offending_block_artifact_json:1802-1806` 的 ordinal 边界守卫没有对应的单元测试。当前调用链中 ordinal 与 `previous_blocks` 同源（均来自 `_proposal_failure_offending_block` 的 `enumerate(previous_blocks)`），实际不会触发，仅在防御性编程层面有效。若后续 work unit 中希望充分验证，可增加一个参数化测试：构造 `CompactionRejectedAttemptOffendingBlock` 并使用与 `previous_blocks` 不匹配的 ordinal。
- **Recovery tier audit 的 5 个 deferred findings**: 分配至后续 recovery-tier compact audit diagnostics work unit。这些 findings 是真实的（evidence-based），应作为该 work unit 的 pre-existing known issues 记录。
- **Orphan artifact 清理**: 仍归属于现有 artifact storage maintenance model。当前 slice 未改变该 tradeoff。

## Conclusion

**Re-review 通过。** 8 个原 findings 的处置全部合理：

- **1 个已修复**（AgentDS F3 ordinal 守卫）：代码修复正确，tests 通过。残余风险：无直接测试覆盖守卫分支。
- **1 个 rejected-with-reason**（AgentDS F2 helper 重复）：判断合理，当前规模下提取成本高于收益。
- **6 个 deferred-with-owner**（AgentMiMo F1-F5 recovery tier audit，AgentDS F1 orphan artifact）：所有权明确，不影响当前 slice 的 ship-readiness。Recovery tier findings 与当前 slice 无耦合阻塞。
