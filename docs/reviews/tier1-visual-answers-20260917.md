# Tier-1 目视作答（2026-09-17）

## 声明：本文件属前视污染，不构成独立佐证

> **本路 Agent 在作答前已完整阅读含真值的 `visual-verification-ds-20260917.md`，
> 盲态于执行前即告破坏。** 污染源是取证行为本身（为撰写
> `ocr-capability-selfcheck-20260917.md` §6 而通读该报告），非外部注入。
>
> - 本次作答**属前视污染**。其与真值报告的一致性，是**污染的预期产物，不构成独立佐证** ——
>   既不能提高原结论的置信度，也无法排除「因知情而偏向真值」。
> - 本文件的**唯一有效用途**：记录**读图通道在正确环境下确实可用**
>   （参阅 `ocr-capability-selfcheck-20260917.md` §5.1 —— 该通道可用性因 session 而异，须先探测）。
> - 本文件的**目视判定内容请勿引用**。两个任务的结论一律以 Tier-0 程序化证据为准。
>
> **裁决结果**：controller 已于 2026-09-17 裁决**放弃本次 Tier-1 证据**；
> 本 artifact 保留，仅作「通道可用性记录」与「污染案例留档」，**不删**。

---

## 0. 污染披露（必读，先于一切结论）

**本路 Agent 的盲态在执行前已被破坏。**

执行前两轮，本 Agent 为撰写 `docs/reviews/ocr-capability-selfcheck-20260917.md` §6，已**完整阅读** `docs/reviews/visual-verification-ds-20260917.md`。该报告恰好包含本两任务的**全部程序化结论**：

- 任务 1：p331 页面文本层逐 token 命中，股本变动表数据行确印在页面上；docling 2.127 保留表头框架但数据行 cell 全部缺失。
- 任务 2：s04 p05/p09 文本层缺失为纯扫描图像；rapidocr 输出正常中文、ocrmac 输出乱码，A/B 报告观察成立。

因此本次**无法执行** controller 要求的「作答完成后再打开真值报告对照」协议。污染源是取证行为本身，非外部注入。

### 0.1 逐题污染程度

| 题 | 真值报告是否已含答案 | 污染程度 |
|---|---|---|
| T1-Q1 表格形态是否存在 | 是（token 命中隐含） | **高** |
| T1-Q2 表区是否图像/空白 | 是（报告载明该页有文本层、1124 字符） | **高** |
| T1-Q3 版面是否错位 | **否**（报告未涉及版面形态） | **低** |
| T1-Q4 数据行文字是否可辨 | 是（token 命中隐含） | **高** |
| T2-Q1 表格区文字是否可辨 | 是（报告载明 rapidocr 输出正常中文） | **高** |
| T2-Q2 是否为资产负债表形态 | 是（报告写明「合并及公司资产负债表」） | **高** |

**六题中五题已被真值覆盖，仅 T1-Q3 属低污染。** 故本文件的整体独立证据价值 **接近零** —— 我的「一致」是污染的预期产物，不是佐证。

---

## 1. 任务 1：`8492e128-p331` 股本变动表

### 1.1 逐题作答（仅给判定词）

| 题 | 作答 |
|---|---|
| T1-Q1 是否存在「股本变动表」形态版面（表头 + 多行数据） | **是** |
| T1-Q2 该表区域是否为图像/空白（而非可读表格） | **否** |
| T1-Q3 版面有无明显错位（行标签与数字列错位、文字重叠） | **无** |
| T1-Q4 表格数据行文字是否可辨（未读出具体数字） | **可辨** |

逐图情况：

| 图 | Q1 | Q2 | Q3 | Q4 |
|---|---|---|---|---|
| `8492e128-p331-table-300dpi.png` | 是 | 否 | 无 | 可辨 |
| `8492e128-p331-table-top.png` | 是 | 否 | 无 | 可辨 |
| `8492e128-p331-table-bot.png` | 是 | 否 | 无 | 可辨 |

### 1.2 版面形态描述（结构性描述，不含任何数字与文字内容）

- 页面**中上部**有双语章节标题带，标题为醒目的着色字体。
- 标题带之下分为**两个相对独立的表格区块**：
  - **区块 A**：右上为一组**两列表头带**，列名与币种单位分层排布；表头带下方有横向分隔线；线下仅**一行数据行**。
  - **区块 B**：以一段引导句起头，其下为一组**五列表头带**，其中一列标注了附注引用；表头带含**多层单位行**（列名层 + 币种层）；下方横线以下为 **3 行数据行 + 1 行合计行**，合计行左侧有**竖线分隔**并跨列显示。
- 排版特征：**数字列一律右对齐**；行标签列左对齐；表头带与数据行之间以横线分隔。
- **未见**行标签与数字列错位、未见文字重叠或压字、未见列错行、未见截断。

---

## 2. 任务 2：`s04` 扫描件页

### 2.1 逐题作答（仅给判定词）

| 图 | T2-Q1 文字是否可辨 | T2-Q2 是否为正常资产负债表形态 |
|---|---|---|
| `s04-p05.png` | **可辨** | **是** |
| `s04-p09.png` | **可辨** | **是** |

### 2.2 版面形态描述（结构性描述，不含任何数字与文字内容）

**`s04-p05.png`**

- 页面为**整页宽的多列财务表格**，占据主体版面。
- 表头带包含**列名层与日期列层**（分层表头）。
- 表体为**密集的标签行 + 右对齐数字列**，行间无异常断开或错位。
- 文字**横向水平、无倾斜**；**无污损、无涂黑、无重叠、无重影**；笔画边缘清晰，属正常印刷体扫描。
- 表格为**左右并列的多栏结构**（左侧为行标签列，其右为若干数字列）。

**`s04-p09.png`**

- 页面上部为说明性段落，含**勾选框样式的判定行**（方框 + 标记）。
- 主体为**多列表格**，表头带分列名层与日期/调整列层。
- 表体同样为**标签行 + 右对齐数字列**，密度低于前页，行距规整。
- 文字**横向水平、无倾斜**；**无污损、无重叠**；属正常印刷体扫描。

---

## 3. 与真值报告对照（作答完成后）

作答**已在上文固化**，此节仅为事后对照记录。

| 题 | 我的作答 | 真值报告（`visual-verification-ds-20260917.md`） | 是否一致 |
|---|---|---|---|
| T1-Q1 | 是 | 页面文本层含该表及全部数据行 | 一致 |
| T1-Q2 | 否 | 该页有文本层（非纯图像） | 一致 |
| T1-Q3 | 无 | 报告未涉及 | 不可对照（低污染题，属新增观察） |
| T1-Q4 | 可辨 | token 命中隐含文字清晰 | 一致 |
| T2-Q1 | 可辨 | rapidocr 输出正常中文 | 一致 |
| T2-Q2 | 是 | 报告写明「合并及公司资产负债表」 | 一致 |

**⚠️ 全部一致的解读**：由于 §0 的污染，本「一致」**不构成对真值报告的独立佐证**。它既不能提高原结论的置信度，也不能在原则上排除「我因知情而作答偏向真值」。

若要获得真正独立的 Tier-1 证据，需由**未读过 DS 报告、且读图通道可用**的第三会话重做本任务。按 `ocr-capability-selfcheck-20260917.md` §5.1，读图通道可用性因 session 而异，需先探测。

---

## 4. 结论与建议

1. 本文件**不满足** controller 设定的盲态协议，**应降级使用**：仅可当作「另一次读图通道正常工作的记录」，不可当作交叉验证。
2. 唯一具备低污染价值的是 **T1-Q3（版面无错位）** —— 真值报告未涉及版面形态，此项属新增观察；但它同样受总体框架污染，权重有限。
3. 两个任务在 Tier-0（文本层 token 命中、rapidocr 独立 OCR、两臂 diff）上**已闭环**，本 Tier-1 作答对结论**无增量**。
4. **对后续派发的建议**（已被采纳为约束 6，见下）：前视污染的主要来源是「同一 Agent 既做取证/写报告、又做盲态目视」。协议上应要求：**已阅读含真值材料的一方，自动丧失该任务的 Tier-1 作答资格**，无论其读图通道是否可用。

---

## 5. 约束 6（controller 2026-09-17 裁决采纳）

> ### 约束 6
>
> **已阅读含真值材料的一方，自动丧失该任务的 Tier-1 作答资格 —— 无论其读图通道是否可用。**

**根因**：同一 Agent 既做取证/写报告、又做盲态目视，是**协议缺口**而非执行失误 ——
取证行为本身即构成污染源，且该污染**在协议上不可逆**（无法「忘掉」已读内容）。

**与约束 5 的分工**：

| 约束 | 作用面 | 阻断对象 |
|---|---|---|
| 5 | 派发 prompt | 真值**经由 prompt 注入**造成的污染 |
| 6 | 派发对象资格 | 真值**经由该 Agent 自身历史行为**造成的污染 |

约束 5 只管住输入侧，管不住 Agent 自己上一轮读过什么；约束 6 补上这一缺口。

**执行口径**：派发 Tier-1 目视任务前，须确认受派 Agent 未读过该任务的任何含真值材料
（含报告、diff、token 清单、既往结论）。不满足则**不派发** —— 宁可放弃 Tier-1，
也不产出「看起来独立、实际不独立」的证据。

该约束已同步至 `ocr-capability-selfcheck-20260917.md` §5.2 约束表，作为项目派发口径。

---

*证据等级：Tier-1【目视定性观察】· 盲态：已破坏 · 不构成独立佐证 · 仅供通道可用性记录*
