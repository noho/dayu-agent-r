# Code Review

## Scope

- Mode: current changes
- Branch: codex/upload-filing-oracle
- Base: 892be915
- Output file: docs/reviews/code-review-20260815-211702.md
- Included scope:
  - README.md
  - dayu/fins/README.md
  - tests/README.md
  - docs/gateflow/uf-fix07-slice4-implementation-20260815.md
- Excluded scope: 无
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## 核对清单

### README 职责边界、一致性与自足性

- **README.md**：
  - 正确反映 `--primary` 的用户可见行为：单文件可省略、多文件必须恰好指定一次、selector 必须属于 files。
  - 正确说明 usage failure 场景：重复规范路径、多个 selector、集合外 selector、超过 100 个文件、delete 携带 files/primary。
  - 正确说明 companions 只原样保存、只有 primary 执行 Docling 转换。
  - 正确说明不同目录同 basename、同 stem 不同后缀可共存。
  - 正确说明失败时 `stored files` 为 `0`。
  - 未暴露内部 asset digest 或 storage schema。
  - 未引用 UF-PF07、UF-PF12、UF-FIX08、UF-FIX10、UF-FIX11。
  - 未预写未来能力。

- **dayu/fins/README.md**：
  - 正确更新 raw request 保留 selector cardinality、static admission 在 workspace read 前产生 validated selection。
  - 正确更新 `FinsUploadFilingFiles` 持有 authoritative primary 与保序 companions，不从 index/basename/stem 推断。
  - 正确更新 filing asset identity 与 `original_filename` 分责、derived identity、`derived_from`、storage `primary_document` 与 snapshot primary 同源。
  - 正确更新 `process_filing` 与 read runtime 只消费 snapshot `get_primary_source()` 返回的 exact primary derived path。
  - 正确更新 filing fingerprint 排除 path-derived identity；同 basename/同内容移动目录保持 identical-skip，basename 改名或内容改变触发 update。
  - 正确更新 material 的 name、derived name、fingerprint、metadata、事件与 failure 行为保持不变。
  - 已删除所有"首项决定 filing primary"的旧事实。
  - 未暴露内部 digest 或 schema。

- **tests/README.md**：
  - 正确增加计划列出的 13 文件 focused command。
  - 正确更新 admission、CLI/tool、asset collision、fingerprint、atomicity、storage primary 与 processor/read consistency 覆盖面。
  - 正确说明没有把真实 scenario 写成已执行或已通过。
  - 已删除"首项建模为 primary"旧事实。

### --primary/cardinality/membership/100/delete/duplicate

- **--primary**：CLI 使用 `action="append"`，`ParsedCliArgs.primary` 保留所有 occurrence；`_prevalidate_upload_filing_request()` 正确构造 `primary_selectors` tuple。
- **cardinality**：`_select_fins_upload_filing_files()` 正确实现：单文件无 selector 时唯一文件成为 primary；单文件有 selector 时要求属于 files；多文件无 selector 时抛 `MISSING_MULTI_FILE_PRIMARY`；多文件有 selector 时要求属于 files；多个 selector 时抛 `MULTIPLE_PRIMARY_SELECTORS`。
- **membership**：selector 与 files 使用同一 `_fins_upload_path_identity()` 比较，正确实现 case-sensitive exact path-string equality。
- **100**：`_validate_fins_upload_filing_static()` 在 duplicate 检查前先检查 `len(request.files) > 100`，正确实现 raw entries 计数。
- **delete**：delete precedence 正确实现：`FILES_NOT_ALLOWED_FOR_DELETE` 先于 `PRIMARY_NOT_ALLOWED_FOR_DELETE`；delete 携带 files 时立即返回 files error，即使同时携带 primary。
- **duplicate**：`_fins_upload_path_identity()` 产生 normalized path string，`set(file_identities)` 正确判重。

### companions/only-primary conversion

- **companions**：`FinsUploadFilingFiles.for_upsert(primary=..., companions=...)` 正确保存保序 companions；companions 不产生 `conversion_started` 事实或 Docling 派生资产。
- **only-primary conversion**：`_UploadSelectionPreparation` 对 filing 只包含 `selection.require_primary()`，正确实现只转换 authoritative primary。

### collision-free identities

- **original identity**：`_build_filing_original_asset_identity()` 使用 `fins-upload-asset-v1` + NUL + `normalized_path.as_posix()` 的 SHA-256，正确实现 path-free deterministic identity。
- **derived identity**：`_build_filing_derived_asset_identity()` 直接追加 `_docling.json`，正确实现从 exact original identity 派生。
- **uniqueness assertion**：`_require_unique_filing_original_identities()` 在 converter/publication 前 fail closed。
- **different directory same basename**：不同 normalized path 产生不同 identity，正确实现 collision isolation。
- **same stem different suffix**：不同 suffix 产生不同 identity，正确实现 collision isolation。

### atomic stored=0

- **success**：`stored_file_count` 只统计成功 commit 的 original 文件数，derived 不计数。
- **failure**：primary conversion failure、original 读取失败、blob/final source/commit 失败时 `stored_file_count=0`，正确实现原子失败。
- **cancelled**：`_build_cancelled_result()` 返回 `stored_file_count=0`。

### fingerprint move/rename

- **filing fingerprint**：`_build_upload_source_fingerprint()` 对 filing 使用 `original_filename`、`sha256`、`size`、`source`，正确排除 path-derived identity。
- **identical-skip**：同 basename、同内容从另一目录重传时 fingerprint 相同，`auto` 保持 identical-skip。
- **rename update**：basename 改名即使内容不变也会 fingerprint 不同，触发 update/version increment。
- **content change update**：内容改变同样触发 update。
- **material fingerprint**：material 使用 `name`、`sha256`、`size`、`source`，保持现状。

### storage/process exact primary

- **primary_document**：`_build_pending_assets()` 对 filing 使用 `_build_filing_derived_asset_identity(original_asset.name)`，正确实现从 exact original identity 派生。
- **storage snapshot**：`get_primary_source()` 返回 derived path，正确实现 exact primary。
- **process_filing**：只消费 `get_primary_source()` 返回的 path，不扫描 originals 或 companions。
- **read runtime**：只消费 `get_primary_source()` 返回的 path，不重新选择 primary。

### material unchanged

- **material name**：`_build_original_assets()` 对 material 使用 `file_path.name`，保持现状。
- **material derived name**：`_build_pending_assets()` 对 material 使用 `{stem}_docling.json`，保持现状。
- **material fingerprint**：`_build_upload_source_fingerprint()` 对 material 使用 `name`、`sha256`、`size`、`source`，保持现状。
- **material metadata**：`_build_stored_file_entry()` 对 material 不写 `original_filename` 或 `derived_from`，保持现状。
- **material event**：`file_uploaded.name` 对 material 使用 `asset.name`，保持现状。
- **material failure path**：material 的 failure 行为保持不变。

### Implementation artifact 证据复核

- **1358 passed**：implementation artifact 声称 `1358 passed, 1 skipped, 3 warnings in 48.39s`，与 affected suite 一致。
- **pyright 0**：implementation artifact 声称 `0 errors, 0 warnings, 0 informations`，与 full pyright 一致。
- **六文件 coverage 88/89/99/81/92/85**：implementation artifact 声称六文件均通过 `--fail-under=80`，与 branch coverage 一致。

### 残留检查

- **首项 primary**：三个 README 均无"首项决定 filing primary"旧事实。
- **内部 digest/schema**：三个 README 均未暴露 `_FILING_ASSET_IDENTITY_NAMESPACE`、`path_digest`、`original-` prefix 等内部实现细节。
- **未来能力**：三个 README 均未预写 UF-FIX08、UF-FIX10、UF-FIX11 或其它未实现能力。
- **UF-PF07/PF12**：implementation artifact 明确声明 UF-PF07、UF-PF12 未执行，不将其写成通过。

## Open Questions

无。

## Residual Risk

- optional real Docling integration 未启用，分类：`assigned to later explicitly authorized evidence gate`。
- UF-PF07、UF-PF12 未执行，分类：`assigned to later explicitly authorized evidence gate`。
- material basename/stem collision 行为不在本 work unit 修复范围，分类：`accepted scope boundary`。
- 3 条第三方 `edgar` deprecation warning，分类：`accepted external non-blocking warning`。

所有当前 residual risks 已分类；无 validation failure、unclassified residual risk 或 blocking open question。
