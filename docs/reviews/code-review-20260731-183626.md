# Code Review

## Scope

- Mode: current changes（Gateflow implementation S2 独立审查）
- Branch or PR: `codex/prompt-oracle-adjudication`
- Base: `2f045e51`（S1 accepted commit）
- Output file: `docs/reviews/code-review-20260731-183626.md`
- Included scope: `dayu/cli/session_execution.py`、`tests/cli/test_prompt_command.py`、`docs/reviews/wu-cli-prompt-01-s2-implementation-controller.md`；同时沿真实入口读取 `dayu/cli/agent_entrypoint.py::CliSigintMonitor` 与 `dayu/service/entrypoint_runtime.py::cancel_entrypoint_run_and_wait`，并运行既有 runtime-display / interactive 回归以核对共享边界未漂移。
- Excluded scope: S1、S3-S6 及其它未改动生产语义；不审查 frozen oracle/scenario 的内容变更，因为本 slice 没有修改这些文件。
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Evidence / Adversarial Review

- **Host lifecycle owner**：`dayu/cli/session_execution.py:946-1014` 只在 `accepted_run.run_id` 存在时构造一次 `CancelMode.GRACEFUL` public request，并直接 await `cancel_entrypoint_run_and_wait`；canonical terminal 的产生、持久化与投影仍由 Host/Service owner 完成，CLI 未补写 SQLite、未制造 terminal，也未引入 timeout 或 sleep。
- **连续 SIGINT**：`dayu/cli/session_execution.py:860-938` 在等待 cancel terminal 的整个阶段保持 `CliSigintMonitor` 已安装；第二次及后续 SIGINT 仅通过 `dayu/cli/agent_entrypoint.py:85-112` 增加本地计数，不再参与 task competition，也不会取消 canonical cancel-and-wait。只有 helper 返回 terminal 后，caller 才进入 `_close_prompt_lifecycle`。
- **accepted 前边界**：`dayu/cli/session_execution.py:965-980` 先回收 submit task，再读取 accepted Run id；无 run id 时返回 `None` 且不发 Host cancel。既有 `test_prompt_sigint_before_run_id_returns_local_interrupt` 与 cursor 回归仍断言 cancel 零调用和 cursor 不推进。
- **Escape 与 terminal race**：Esc 与 SIGINT 共用 `_cancel_prompt_turn_after_local_request`，不存在单独的本地强退分支；submit 在 thinking-display finish 窗口先完成时仍优先返回 live terminal 且不发 cancel。
- **cleanup / cursor 顺序**：新增 barrier 测试在 Host terminal 释放前断言 execution 未完成且 cursor 为 0；释放 typed CANCELLED terminal 后才得到 130，并断言 SIGINT monitor、watcher 已关闭和 cursor 推进到 terminal event。`execute_prompt_on_session` 的生产顺序仍是 terminal 返回后 render、advance cursor，最后 shield-close attachment。
- **interactive 未漂移**：diff 未触及 `_submit_interactive_turn_handling_sigint`、`_wait_for_run_id_or_local_exit` 或 `_cancel_run_waiting_for_terminal_or_second_sigint`；interactive 既有二次 SIGINT 本地退出测试随回归通过。
- **项目纪律**：新增/修改函数均保留完整中文 docstring；未发现 `getattr`/`hasattr` fallback、字符串匹配修补、CLI 假终态、下游 durable-state 补写或新增反向依赖。

## Validation

- `.venv/bin/pytest -q tests/cli/test_prompt_command.py tests/cli/test_runtime_display.py tests/cli/test_interactive_command.py --cov=dayu.cli.session_execution --cov-report=term-missing`
  - 结果：`102 passed`。
  - `dayu/cli/session_execution.py`：`80%` coverage。
- `.venv/bin/pyright dayu/cli/session_execution.py tests/cli/test_prompt_command.py`
  - 结果：`0 errors, 0 warnings, 0 informations`。
- `git diff --check`
  - 结果：通过。

## Open Questions

- 无。

## Residual Risk

- 本 slice 的 deterministic fake Host 已证明第二次 SIGINT 不越过 terminal barrier；真实 POSIX subprocess 的精确按键时序、Host/runtime SQLite、EventLog 与 Run/attempt terminal state 尚待最终 P44/P45/P46、PC-BD-02、PC-CN-07 frozen scenario 重放。该项属于 accepted plan 已明确的最终验证，不构成当前实现 finding。

## Conclusion

- Decision: `pass`
- Required fixes: 无。
- Gateflow disposition: S2 可进入 accepted slice commit。
