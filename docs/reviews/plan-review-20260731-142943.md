# AAPL 下载与 canonical ticker 目录计划复审

## Reviewed target and scope

- target：`docs/reviews/aapl-download-canonical-ticker-layout-plan.md`
- prior review：`docs/reviews/plan-review-20260731-142835.md`
- fix artifact：
  `docs/reviews/aapl-download-canonical-ticker-layout-plan-review-fix.md`
- scope：PR-01、PR-02 修复有效性，以及完整计划是否 code-generation-ready
- reviewed at：`2026-07-31 14:29:43 +0800`

## Finding re-review

### PR-01-已修复-[高]-primaryDocument 非法路径 fail-closed

- 最终状态：已修复
- 证据：计划现要求拒绝 absolute/backslash/leading/trailing slash；所有 `/` segment
  必须非空且不得为 `.` / `..`，最后一个已验证 segment 才能成为文件名。
- 验证充分性：目标 transform path、普通单文件名、dot/dotdot、double separator 与
  trailing separator 均有明确测试要求。

### PR-02-已修复-[高]-company alias 不进入 canonical ticker locator

- 最终状态：已修复
- 证据：计划现把 direct lookup 限制为 `try_normalize_ticker(...)` 成功的 candidate；
  公司名/普通 alias 跳过 locator 并进入现有 alias index。
- 验证充分性：`AAPL`、`aapl.us`、`apple`、unknown alias 以及九个 read tools 均列入
  contract test。

## Assumptions re-tested

- canonical ticker grammar 由 `dayu.fins.ticker_normalization` 唯一拥有，storage 只做
  exact canonical validation，不重建业务规则。
- document ID private mapping、descriptor、snapshot、revision 和 transaction state
  machine 均保留；ticker locator 特例不要求 consumer 修改。
- SEC helper 只识别已经有直接证据的 relative transform path，不为未知输入建立 loose
  fallback。
- read tools 先获得 canonical ticker，再走 repository protocol；综合九工具测试足以捕获
  ticker target lookup、snapshot 和 processor 构造回归。

## Open questions

- 无。

## Residual risks

- 旧 `portfolio/id-*` ticker workspace 不兼容：已分类为 fresh workspace operation。
- 未来真实嵌套 SEC archive 文件：待出现直接证据后由后续 SEC collection work unit
  处理。

## Special lenses conclusion

- Goal-bound minimal design：通过。
- Architecture boundary：通过；alias、ticker identity、document identity 与 filename
  channel 已分离。
- Best-practice / optimal solution：通过；在现有 transaction/descriptor 基础上做最小
  namespace-specific 修正。
- Overengineering：通过；无 migration、dual-read 或新 public abstraction。
- Overcoupling：通过；两个行为 slice 独立，read tools 仅作为消费面验证。

## Final plan review conclusion

`pass-with-risks`

两个高风险 finding 均已修复。已分类 residual risks 不阻塞 fresh-schema implementation。
