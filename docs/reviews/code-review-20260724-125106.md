# Code Review — Re-Review (AgentMiMo 第一路)

```text
status=re-review complete
work_unit=WU-OBS-00
slice=S1
gate=implementation re-review
review_mimo=docs/reviews/code-review-20260724-124007.md (初审)
review_ds=docs/reviews/code-review-20260724-123859.md (初审)
controller_adjudication=docs/reviews/wu-obs-00-slice-1-implementation-review-controller-adjudication.md
fix_artifact=docs/reviews/wu-obs-00-slice-1-implementation-review-fix-codex.md
accepted_plan_commit=e1799abc3341872ba19ff609de15b236813a3533
branch=work/wu-obs-00
output_file=docs/reviews/code-review-20260724-125106.md
```

## Scope

- Mode: current changes (re-review after fix gate)
- Branch: `work/wu-obs-00`
- Base: `e1799abc3341872ba19ff609de15b236813a3533` (accepted plan commit)
- Output file: `docs/reviews/code-review-20260724-125106.md`
- Included scope: fix gate 后全部 dirty files，含 staged、unstaged 与 untracked 文件
- Excluded scope: 无（全部改动均在 review 范围内）
- Parallel review coverage: 无

## Context

本 re-review 已完整阅读：
- `AGENTS.md`（项目级约束）
- 初审 artifact：`docs/reviews/code-review-20260724-124007.md`（AgentMiMo 初审）
- 初审 artifact：`docs/reviews/code-review-20260724-123859.md`（AgentDS 初审）
- Controller adjudication：`docs/reviews/wu-obs-00-slice-1-implementation-review-controller-adjudication.md`
- Fix artifact：`docs/reviews/wu-obs-00-slice-1-implementation-review-fix-codex.md`

本 re-review 的目的是确认：
1. CTRL-S1-IMPL-01 是否只在 `tests/host/test_package_exports.py` 精准同步三个 Tool Trace Analysis public exports
2. production 是否完全未被 fix gate 改动
3. CTRL-S1-IMPL-02 是否保持 rejected
4. 完整 Host 全绿证据是否可信
5. 是否产生新 actionable finding

## Finding Closure

### CTRL-S1-IMPL-01 — 已修复，验证通过

**问题**：`tests/host/test_package_exports.py::test_host_all_matches_current_public_contracts` 断言旧 `dayu.host.__all__` 集合，未包含 accepted plan 与当前实现新增的 `ToolTraceAnalysisPolicy`、`ToolTraceAnalysisSource`、`ToolTraceInputMode`。

**Fix 验证**：
- `tests/host/test_package_exports.py:195-201`：新增 `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS`，包含且仅包含三个 class
- `tests/host/test_package_exports.py:222`：将其纳入 `EXPECTED_HOST_EXPORTS`
- 测试结果：`1 passed in 0.32s` ✅

**Production 影响**：无。fix gate 仅修改测试文件，未修改任何 production 文件。

**结论**：CTRL-S1-IMPL-01 已修复，精准同步三个 Tool Trace Analysis public exports。

### CTRL-S1-IMPL-02 — 保持 rejected

**Controller 裁决**：Controller 不接受删除 `_read_exact_prefix` 中 `len(chunk) > remaining` 防御分支。

**Fix 行为**：fix gate 未修改任何 production 文件，未删除、改写或绕过该 exact-prefix fail-closed invariant。

**代码验证**：`dayu/host/tool_trace_analysis_input.py:847-848` 防御分支仍然存在：
```python
if len(chunk) > remaining:
    raise OSError("cold snapshot read exceeded captured prefix")
```

**结论**：CTRL-S1-IMPL-02 保持 rejected，符合 Controller 裁决。

## Production 改动确认

**Fix gate scope audit**：相对 fix gate preflight，新增修改仅为 `tests/host/test_package_exports.py` 与 fix artifact，均在 Controller allowlist 内。

**Production files 状态**：当前 worktree 的 production dirty files 均为进入 fix gate 前已存在的 Slice 1 implementation：
- `dayu/host/__init__.py`（新增 3 个 public export）
- `dayu/host/durable/connection.py`（新增 read-only store）
- `dayu/host/durable/tool_trace.py`（新增 `read_tool_trace_page`）
- `dayu/host/durable/transaction.py`（新增 read-only PRAGMA helper）
- `dayu/host/open_host.py`（删除重复 lock helper）
- `dayu/host/tool_trace.py`（新增 lock helper）

**结论**：production 完全未被 fix gate 改动。

## 完整 Host 全绿证据验证

**测试结果**：
- Package export test: `1 passed in 0.32s` ✅
- Slice 1 focused tests: `112 passed in 1.23s` ✅（fix artifact 记录 111 passed，差异为新增 package export test）
- Complete Host tests: `2296 passed, 2 skipped, 6 deselected in 64.80s` ✅（与 fix artifact 一致）

**Pyright 结果**：
- Targeted pyright: `0 errors, 0 warnings, 0 informations` ✅
- Full pyright: `0 errors, 0 warnings, 0 informations` ✅

**结论**：完整 Host 全绿证据可信，所有测试通过，pyright 无报错。

## 新 Actionable Finding 检查

### Semantic Ownership Drift 检查

- **Public Source 五字段**：`ToolTraceAnalysisSource` 仅含 `requested_path`、`mode`、`cold_jsonl_path`、`hot_db_path`、`artifact_root`，无 `cold_lock_path`。**通过**。
- **Lock 路径 owner**：`_tool_trace_cold_lock_path` 归属 `dayu.host.tool_trace`，producer 与 reader 在 Host 内部复用。**通过**。
- **Cold record digest/ref**：由 producer 唯一产生，reader 只做 recompute-and-compare strict 校验。**通过**。
- **Resolver 失败不降级**：`resolve_tool_trace_hot_row_payloads` 失败进入 `PAYLOAD_UNRESOLVABLE` diagnostic，不 fallback。**通过**。

### 测试覆盖检查

- **Package export test**：已同步三个 Tool Trace Analysis public exports。**通过**。
- **Slice 1 focused tests**：112 passed，覆盖 hot/cold join、resolver、parser、read-only store 等。**通过**。
- **Complete Host tests**：2296 passed，无 regression。**通过**。

### Docs 检查

- **Fix artifact**：记录 finding closure、验证、docs decision、blocker 与 next entry point。**通过**。
- **Scope audit**：明确记录 fix gate scope 与 production files 状态。**通过**。

**结论**：未产生新 actionable finding。

## Open Questions

无。

## Residual Risk

1. **Live schema blocker 保持不变**：真实 workspace DB schema=`20`，current fresh schema=`24`。Slice 1 acceptance、保护提交与 Slice 2 继续冻结。fix gate 未读取或修改真实 workspace，未增加 schema 兼容、raw SQLite、fallback、跳过 schema validation 或 cold-only 降级。

2. **Live resolver smoke 未执行**：由于 schema 20 vs 24 blocker，当前 workspace 的 descriptor 在 resolver 层尚未验证。Unit/fixture 层 resolver contract 已由 Slice 1 focused tests 覆盖。

3. **跨平台 residual**：same-handle exact-prefix、path replacement、truncate/short read 与 live producer non-interference 已在 macOS/Python 3.11 验证；无其他 OS CI 证据。

4. **`_read_exact_prefix` 防御分支**：`len(chunk) > remaining` 分支在当前 Python 实现中不可达，但 Controller 裁决保留为 fail-closed invariant。不影响正确性。

## Verdict

**PASS — 0 新 actionable findings。**

Re-review 确认：
- CTRL-S1-IMPL-01 已修复：`tests/host/test_package_exports.py` 精准同步三个 Tool Trace Analysis public exports
- production 完全未被 fix gate 改动
- CTRL-S1-IMPL-02 保持 rejected，符合 Controller 裁决
- 完整 Host 全绿证据可信
- 未产生新 actionable finding

唯一阻塞项为 live workspace schema 20 vs current fresh schema 24 的 validation environment blocker，已由 Controller 裁决为独立于代码正确性的问题。
