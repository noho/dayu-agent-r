# Code Review — PR190 F17 Aggregate

## Scope

- **Mode**: current changes (aggregate)
- **Branch**: `codex/interactive-oracle`
- **Base**: `e1217811ad57e48c90e3763994930e53378ba060` (F15/F16 follow-up，F17 起点)
- **Output file**: `docs/reviews/code-review-20260807-150010.md`
- **Included scope**: 完整 F17 work unit（两个 commits + clean working tree），含 Goal/Plan/reviews/implementation 全部门禁证据与两个单行 digest 变更；排除 base 之前 F14/F15/F16 历史
- **Excluded scope**: 无；所有 committed 变更与 clean working tree 均已纳入
- **Parallel review coverage**: 无（单一 reviewer 完整走读）

## Commit / file inventory

| Commit | Message | Product/Test | Gateflow Evidence |
| --- | --- | --- | --- |
| `0d215296` | docs(gateflow): accept PR 190 F17 plan | 0 files | 8 files (goal-confirmation, plan, 4 plan reviews, plan acceptance, adjudication) |
| `305c1012` | test(cli): refresh frozen init publication digest | 2 files (manifest + test pin) | 4 files (implementation, implementation review acceptance, 2 code reviews) |

**Product/test files（2）:**
- `docs/cli_init_workspace_manifest_v1.json` — 1 hunk, 1 insertion / 1 deletion
- `tests/cli/test_smoke_cli_init_provider_matrix.py` — 1 hunk, 1 insertion / 1 deletion

**Gateflow evidence files（12）:**
- `docs/gateflow/pr-190-f17-goal-confirmation-20260807.md`
- `docs/gateflow/pr-190-f17-plan-20260807.md`
- `docs/gateflow/pr-190-f17-plan-acceptance-20260807.md`
- `docs/gateflow/pr-190-f17-plan-review-adjudication-20260807.md`
- `docs/gateflow/pr-190-f17-implementation-20260807.md`
- `docs/gateflow/pr-190-f17-implementation-review-acceptance-20260807.md`
- `docs/reviews/plan-review-20260807-143241.md`
- `docs/reviews/plan-review-20260807-143636.md`
- `docs/reviews/plan-review-20260807-144241.md`
- `docs/reviews/plan-review-20260807-144253.md`
- `docs/reviews/code-review-20260807-145315.md`
- `docs/reviews/code-review-20260807-145453.md`

Working tree: **clean**；无 untracked files。

## Independent Verification Results

以下所有验证均由 aggregate reviewer 独立执行，不引用既有 review artifact 或 implementation artifact 中的声称值。

### 1. Raw digest chain — 四条链路全部独立闭合

| 链路 | 源 | 目标 | 独立验证方式 | 结果 |
| --- | --- | --- | --- | --- |
| prompt → manifest entry | `dayu/config/prompts/scenes/conversation_compaction_user.md` raw bytes | manifest `files[].content_sha256` | `shasum -a 256` + Python `hashlib.sha256(path.read_bytes())` | `22e7bc50...` ≡ `22e7bc50...` ✓ |
| manifest entry 唯一性 | manifest 全量 43 entries | target path 精确命中 1 次 | Python 结构化断言按精确 `path` 定位 | 1 hit ✓ |
| 旧 digest 消除 | manifest 全量 43 entries | 旧值 `59b50e13...` | Python 结构化遍历 43 entries | 出现 0 次 ✓ |
| manifest → test pin | `docs/cli_init_workspace_manifest_v1.json` 保存后 raw bytes | `FROZEN_MANIFEST_SHA256` | `shasum -a 256` + Python AST 提取常量 + `hashlib.sha256(path.read_bytes())` | `064f8066...` ≡ `064f8066...` ✓ |

### 2. Production FIRST strict publication tree

- `test_frozen_manifest_matches_fresh_real_publication_tree`：**PASSED**；production `InitMode.FIRST`、file lock、workspace transaction owner 在 fresh 临时 workspace 发布完整树；`validate_publication_tree` 独立递归枚举 actual tree 并与 manifest 逐项比较；断言 `report.valid=True`、`report.issues=()`
- `test_checked_in_manifest_digest_is_stable_across_validation`：**PASSED**；证明 manifest raw digest 与 pin 一致且每次验证不改写 manifest
- Inventory：actual/manifest 均为 5 directories / 43 files / 16 model owners
- Lock file：`.dayu-init.lock` 存在

### 3. OpenAI / Ollama projected validation

- `test_projected_publication_accepts_only_role_model_pointer_changes`：**PASSED**（OpenAI choice 的 16 个 role owner 投影通过冻结 oracle）
- `test_projected_publication_validates_ollama_dynamic_owner_fields`：**PASSED**（Ollama strict 模式正确拒绝 drift；`allow_ollama_dynamic=True` 正确接受动态字段）
- `test_ollama_expected_identity_uses_init_owned_dynamic_truth`：**PASSED**（expected identity 来自 init publication 而非 package placeholder）

### 4. 71-test owner suite

- `pytest --collect-only`：**71 tests collected**
- `pytest ... -q`：**71 passed**（3 个第三方 edgar deprecation warnings，非本项目）

### 5. Static checks

| 检查 | 命令 | Exit | 结果 |
| --- | --- | --- | --- |
| pyright | `python -m pyright dayu/ tests/ utils/` | 0 | 0 errors, 0 warnings, 0 informations |
| ruff | `python -m ruff check tests/cli/... utils/...` | 0 | All checks passed |
| compileall | `python -m compileall -q ...` | 0 | 通过 |
| JSON parse | `python -m json.tool docs/cli_init_workspace_manifest_v1.json` | 0 | 通过 |
| git diff --check | `git diff --check` | 0 | 通过 |

### 6. Exact diff guard

- Product/test diff vs base：精确 2 files / 2 hunks / 1 insertion + 1 deletion each
- Word diff：各为一个完整 SHA-256 hex string 替换，无 whitespace、formatting 或相邻行变化
- Protected paths zero-diff guard（均确认零 diff）：
  - `dayu/config/prompts/scenes/conversation_compaction_user.md`（prompt 内容真源）
  - `utils/smoke_cli_init_provider_matrix.py`（validator/fixture）
  - `dayu/` 所有 source（schema、public contract、product code）
  - `dayu/config/` 全部（prompt、配置）
  - `docs/cli_ci.md`（Oracle/scenario/readiness）
  - `tests/README.md`、`dayu/README.md`、根 `README.md`

### 7. Schema / public contract 零变化

`dayu/`、`dayu/config/`、`utils/` 下所有 `.py`、`.json`、`.md` 相对 base 零 diff。无 schema 字段增减、类型变更、枚举变更、接口签名变更或 public export 变更。

## Adversarial Attack Assessment

### Duplicate truth

**未发现。** Owner chain 为严格单真源派生链：

1. `dayu/config/prompts/scenes/conversation_compaction_user.md` raw bytes → prompt 内容真源（producer）
2. `docs/cli_init_workspace_manifest_v1.json` target entry → consumer，记录预期 prompt digest
3. `docs/cli_init_workspace_manifest_v1.json` raw bytes → manifest 身份真源（producer）
4. `FROZEN_MANIFEST_SHA256` → consumer，记录预期 manifest digest

每个事实（prompt digest、manifest digest）有且仅有一个 producer；所有 consumer 均从唯一 producer 派生，无第二条写路径、无 fallback 推导、无 loose matching。manifest 的用途是 drift detection（fail-closed），而非 alternative truth。

### Artifact misrepresentation

**未发现。** 逐项核对：

- **Implementation artifact** (`pr-190-f17-implementation-20260807.md`) 声称的 pre-state digests、post-state digests、5/43/16 counts、71 passed、exact two-hunk diff 全部与 aggregate reviewer 独立验证一致。
- **Code review artifacts** (`code-review-20260807-145315.md`, `code-review-20260807-145453.md`) 对 base `0d215296` 的 working tree 做的独立 digest 验证与 aggregate reviewer 对 base `e1217811` 的 committed state 做的独立验证在数值上完全一致（digest 由文件 raw bytes 决定，不依赖 base）。
- **Plan review artifacts** 独立复现的 pre-state singleton mismatch、5/43/16 inventory、prompt digest、manifest raw digest、71-test count 全部与 aggregate reviewer 验证一致。
- **Adjudication** 中 F17-P3 被 `rejected-with-reason` 的理由（现有 rollback 已充分）经独立审视成立：精确反向 patch + raw digest 重算 + changed-file guard 组合能检测并恢复任何非目标 bytes 变化。
- **Implementation review acceptance** 的 controller evidence audit 逐项与 aggregate reviewer 独立验证一致。

### Scope / commit pollution

**未发现。** 

- Commit `0d215296`：仅 gateflow plan evidence（8 files），无产品/测试变更。
- Commit `305c1012`：2 product/test 单行 hunks + 4 gateflow implementation evidence files。
- 两个 commit 之间的 gateflow 证据无交叉污染：plan-phase evidence 全部在 `0d215296`；implementation-phase evidence 全部在 `305c1012`。
- Working tree clean，无未提交变更，无 untracked files。
- 无 protected path（prompt、validator、fixture、Oracle、schema、README）被意外修改。

### Residual risk classification

现有 residual risk 分类经独立审视成立：

| 风险 | 分类 | 独立验证 |
| --- | --- | --- |
| 非目标 bytes 被 formatter 改写 | `fixed in current slice` | numstat=1/1 per file + word diff=完整 hex string 替换 + JSON parse 通过 + manifest raw digest 重算匹配 ✓ |
| 存在第二个 publication mismatch | `fixed in current slice` | `validate_publication_tree` → `issues=()` + 独立结构化断言 43 entries 中旧 digest 出现 0 次 ✓ |
| 测试数量被意外减少 | `fixed in current slice` | collect-only=71 + 71 passed ✓ |
| 真实 provider / Oracle replacement scenarios 未重跑 | `assigned to later work unit` | F17 未改变 provider、Oracle/scenario/readiness 或产品行为；三条 replacement scenarios 保持 `unadjudicated`；属于 Oracle 闭环而非 publication consumer fix ✓ |
| 未来 package asset 再次漂移 | `assigned to later work unit` | `validate_publication_tree` + `test_checked_in_manifest_digest_is_stable_across_validation` 会 fail-closed；本 slice 不新增自动更新写路径 ✓ |

### Additional adversarial checks

| 攻击面 | 验证 | 结论 |
| --- | --- | --- |
| LLM-facing regression | prompt raw bytes 相对 base 零 diff；manifest/test pin 不进入 LLM 上下文 | 无风险 |
| validation weakening | `FROZEN_MANIFEST_SHA256` 比较逻辑未被修改或放松；`validate_publication_tree` 零 diff | 无风险 |
| semantic ownership drift | 修复在 manifest（publication truth consumer）边界，未在下游用 fallback/特例/重复计算/loose parsing 补救 | 无漂移 |
| commit message accuracy | commit `305c1012` 标题为 "test(cli): refresh frozen init publication digest"，实际变更包含 manifest entry 修复与 test pin 更新两项；标题侧重 test 侧，未提及 manifest 修复 | 可接受（commit body 可通过 diff 自明；不影响 correctness/stability/maintainability） |
| minimum-design compliance | 2 single-line hunks，无新增 helper/abstraction/schema/test/compat shim；满足 owner contract 的最小方案 | 无过度设计 |

## Findings

未发现实质性问题。

本 aggregate review 对所有 claims 执行了独立于既有 artifact 的完整验证：四条 digest 链全部独立闭合、production FIRST strict audit 通过、OpenAI/Ollama projected validation 全绿、71-test suite 全通过、全部 static checks 通过、exact two-hunk diff guard 通过、protected paths 零 diff、schema/public contract 零变化。未发现 duplicate truth、artifact misrepresentation、scope/commit pollution 或 residual risk 误分类。

## Open Questions

无。

## Residual Risk

- 上述已分类的五项 residual risk 中，三项已由当前 slice 修复，两项已明确分配到后续 work unit。无未分类或 orphan risk。
- 本 aggregate review 未覆盖真实 provider 端到端行为或 Oracle replacement scenario 的运行时正确性——这是 F17 明确定义的非目标，且 `docs/cli_ci.md` 中三条 replacement scenarios 继续为 `unadjudicated`。该风险已在 plan 和 implementation 中明确分配 owner，不构成本 slice 的遗漏。
