# Code Review

## Scope

- Mode: current changes
- Branch: codex/upload-filing-oracle
- Base: 56d159cb4bf13baf82858bb237b2f73075eaf717
- Output file: docs/reviews/code-review-20260813-175624.md
- Included scope:
  - `dayu/fins/pipelines/docling_upload_service.py`（S1 生产文件）
  - `dayu/fins/ingestion_runtime.py`（S1 生产文件）
  - `tests/fins/test_docling_upload_service.py`（S1 测试文件）
  - `tests/fins/test_fins_ingestion_runtime.py`（S1 测试文件）
  - `tests/cli/test_fins_commands.py`（S1 测试文件）
  - `docs/gateflow/uf-fix02-action-and-update-identity-s1-implementation-20260813.md`（implementation artifact）
- Excluded scope:
  - committed plan 历史
  - S2 scope（publication mutation、`_resolve_upsert_mode` 删除、跨市场 workflow 集成）
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Analysis

### 1. evaluate_upload_overwrite_precondition 变更审查

**变更**（`docling_upload_service.py:185`）：
- 旧代码：`if action == "update" and previous_meta is None and not overwrite:`
- 新代码：`if action == "update" and previous_meta is None:`

**语义**：update missing 现在无条件返回 `UPDATE_TARGET_MISSING`，不再允许 `overwrite=True` 把 explicit update 变成 upsert。

**正确性验证**：
- `overwrite` 参数现在只对 `create-existing` 有意义（第183行条件）
- `update + previous_meta=None + overwrite=True` 旧行为返回 `ALLOWED` 是 bug，新行为返回 `UPDATE_TARGET_MISSING` 是正确修复
- 测试覆盖：runtime 层（`test_validate_fins_upload_filing_request_rejects_missing_explicit_update`）、Service 层（`test_prepare_upload_rejects_missing_update_before_shared_conversion`）、CLI 层（`test_upload_filing_state_conflict_exits_before_service_factory_without_mutation`）均覆盖 `overwrite=False/True`

### 2. _can_skip_upload 变更审查

**变更**（`docling_upload_service.py:1001-1002`）：
- 新增：`if _require_source_deleted_flag(previous_meta): return False`

**语义**：deleted 的 source 即使指纹相同也不能 skip，必须进入 conversion 恢复。

**正确性验证**：
- 调用点（第304行）在 `evaluate_upload_overwrite_precondition` 之后，`previous_meta` 已确认不为 `None`
- `_require_source_deleted_flag` 是严格 reader：字段缺失抛 `KeyError`、非精确 `bool` 抛 `ValueError`
- 无 `get`、默认值、fallback 或 loose truthiness
- 测试覆盖：`test_execute_upload_deleted_equal_fingerprint_enters_conversion` 验证 create→delete→update 恢复路径，`test_prepare_upload_requires_canonical_boolean_deleted_state` 验证 fail-closed 行为

### 3. _require_source_deleted_flag 新函数审查

**函数**（`docling_upload_service.py:1007-1026`）：
- 严格读取 canonical boolean deletion state
- 无 `get`、默认值、fallback 或 loose truthiness
- 符合 CLAUDE.md "禁止 fallback" 约束
- 类型签名正确：`Mapping[str, JsonValue]` → `bool`，异常为 `KeyError | ValueError`
- docstring 完整：参数、返回值、异常

### 4. _USAGE_MESSAGES 文案变更审查

**变更**（`ingestion_runtime.py:753`）：
- 旧文案："update 目标不存在；请改用 create 或允许覆盖"
- 新文案："update 目标不存在；请改用 create"

**正确性**：文案与行为一致，`overwrite` 不再提供 update upsert 权限，因此移除"或允许覆盖"。

### 5. 测试 fixture 迁移审查

**变更**（`test_fins_commands.py:1713,1756`）：
- 旧参数：`action="update"` + `--overwrite`
- 新参数：`action="create"` + `--overwrite`

**正确性**：
- 旧测试验证参数透传，不是 admission 逻辑
- 新参数组合 `create + overwrite` 是合法的，不会触发 `UPDATE_TARGET_MISSING`
- 没有增加生产兼容分支，只是避免测试被 admission 逻辑拦截
- 不削弱语义：测试目的不变，只是使用合法参数

### 6. Semantic Ownership Drift 检查

- `evaluate_upload_overwrite_precondition`：拥有 create/update admission matrix ✓
- `_can_skip_upload`：拥有 active identical input 的 no-op 条件 ✓
- `_USAGE_MESSAGES`：拥有 `FinsUploadUsageCode.UPDATE_TARGET_MISSING` 的唯一 public 文案 ✓
- `_require_source_deleted_flag`：canonical logical deletion reader，只在 `_can_skip_upload` 中使用 ✓
- 无 CLI/Service duplicated rule、字符串异常分类或 `str(exc)` public projection 增量

### 7. UF-FIX01 Contracts 检查

- typed validation：保持 ✓
- fresh authoritative workflow：未修改 ✓
- zero mutation：CLI 测试验证 `before_tree == after_tree` ✓
- atomic batch：未修改 ✓
- bounded stderr：测试验证 `len(captured.err) <= _MAX_PUBLIC_CONTENT_FAILURE_STDERR_CHARS` ✓
- cancellation：未修改 ✓

### 8. Active 旧 Meta/Material 兼容性

- `_require_source_deleted_flag` 在 `is_deleted` 缺失时抛 `KeyError`，不使用默认值
- 旧 meta 如果没有 `is_deleted` 字段，会在 `_can_skip_upload` 中 fail closed
- 这是正确的设计：旧 meta 应该有 `is_deleted` 字段，如果没有，说明数据损坏

## Open Questions

无。

## Residual Risk

1. **existing full-input update 仍未执行 exact complete-set reset/create**：renamed update 可能残留旧文件。已分配给 S2。
2. **`_resolve_upsert_mode` 与 overwrite-only reset 尚未收敛**：已分配给 S2。
3. **prevalidation/fresh recheck 到 publication 的同请求竞争**：已分配给 UF-FIX10。
4. **existing source corruption auto repair**：已分配给 UF-FIX08；S1 仅对损坏 deletion flag fail closed。
5. **frozen `UF-A08` observed evidence 在修复后 intentionally stale**：已分配给 later unified conformance refresh。

所有 residual 均已有 approved destination；无未分类或需要本 slice 扩 scope 的风险。
