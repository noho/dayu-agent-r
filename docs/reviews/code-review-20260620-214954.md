# Code Review — WU-ENG-02-R1 Re-review Gate

## Scope

- Mode: current changes (re-review gate — verify controller-accepted findings are closed)
- Branch: `phase/wu-eng-02-r1`
- Base: `913875da`
- Output file: `docs/reviews/code-review-20260620-214954.md`
- Control doc: `docs/host/issues-implementation-control.md`
- Accepted plan artifact: `docs/host/host-issues/wu-eng-02-r1-provider-debugging-correlation-plan.md`
- Implementation artifact: `docs/reviews/implementation-wu-eng-02-r1-codex-20260620.md`
- Initial code review artifacts:
  - `docs/reviews/code-review-20260620-213746.md`
  - `docs/reviews/code-review-20260620-214050.md`
- Fix artifact: `docs/reviews/fix-wu-eng-02-r1-code-review-codex-20260620.md`
- Included scope:
  - `dayu/host/_terminal_diagnostics.py` — message="" fix (line 35)
  - `tests/host/test_terminal_diagnostics.py` — new direct helper tests
  - `tests/host/test_tool_trace_projection.py` — raw_payload_ref fallback test addition
  - `dayu/host/read_api.py` — _failed_host_event diagnostic suffix (verified _lost_host_event untouched)
  - `dayu/host/outbox.py` — _error_message diagnostic suffix
  - `dayu/host/tool_trace.py` — diagnostic skip condition narrowed to both-absent
  - Affected test suites: `test_terminal_diagnostics`, `test_read_api_terminal_policy`, `test_tool_trace_projection`, `test_tool_trace_queries`
- Excluded scope: same as initial reviews; no new modules or test layers added
- Parallel review coverage: 无

## Controller-Adjudicated Finding Status

### Finding 1: terminal diagnostics helper direct tests — ✅ CLOSED

- **Original finding**: `_terminal_diagnostics` 模块缺少独立单元测试（code-review-20260620-213746.md #1）和双 id 场景无测试覆盖（code-review-20260620-214050.md #01）。
- **Fix**: 新增 `tests/host/test_terminal_diagnostics.py`，8 个测试覆盖：
  - `test_terminal_diagnostic_suffix_includes_only_provider_id` — 仅 provider id
  - `test_terminal_diagnostic_suffix_includes_only_client_id` — 仅 client id
  - `test_terminal_diagnostic_suffix_includes_both_ids_in_stable_order` — 双 id，锁定顺序
  - `test_terminal_diagnostic_suffix_returns_original_message_when_ids_absent` — 双 id 缺失
  - `test_terminal_diagnostic_suffix_returns_none_when_message_and_ids_absent` — message=None 且双 id 缺失
  - `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_none` — message=None 有 id
  - `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_empty` — message="" 有 id
  - `test_terminal_diagnostic_suffix_truncates_ids` — id 截断
- **验证**: `pytest tests/host/test_terminal_diagnostics.py -q` → 8 passed.
- **判定**: 所有要求的分支均被直接参数化测试覆盖，通过。

### Finding 2: double-id suffix format locked with provider_request_id before client_correlation_id — ✅ CLOSED

- **Original finding**: 双 id 输出格式无测试锁定（code-review-20260620-214050.md #01）。
- **Fix**: `_terminal_diagnostics.py:53-63` 按 provider_request_id、client_correlation_id 顺序构建 parts 列表，`"\n".join(parts)` 输出。测试 `test_terminal_diagnostic_suffix_includes_both_ids_in_stable_order` 断言精确输出格式：
  ```
  {message}\nprovider_request_id={provider}\nclient_correlation_id={client}
  ```
- **验证**: 测试断言精确字符串，无模糊匹配；代码行 54-62 的 append 顺序与断言一致。
- **判定**: 格式锁定，通过。

### Finding 3: Tool Trace diagnostic with provider_request_id=None, client_correlation_id present, raw_payload_ref present — ✅ CLOSED

- **Original finding**: 测试仅覆盖 raw_payload_ref 缺失场景，未覆盖存在时的 diagnostic_ref 行为（code-review-20260620-213746.md #2）。
- **Fix**: 新增 `test_diagnostic_trace_uses_raw_payload_ref_without_provider_id`，payload 含 `raw_payload_ref="raw-ref-fallback"`。断言：
  - hot row `diagnostic_ref == "raw-ref-fallback"`
  - cold JSONL `diagnostic_refs == ["raw-ref-fallback"]`
  - `client_correlation_id` 在 hot/cold 两侧均保留
- **验证**: 测试已加入 `tests/host/test_tool_trace_projection.py:1666-1725`；`pytest tests/host/test_tool_trace_projection.py -q` 通过（含该测试）。
- **判定**: plan 要求的 raw payload ref fallback 行为已覆盖，通过。

### Finding 4: message="" helper behavior no longer creates leading blank line — ✅ CLOSED

- **Original finding**: `message=""` 时产生前导空行 `"\nclient_correlation_id=xxx"`（code-review-20260620-214050.md #02）。
- **Fix**: `_terminal_diagnostics.py:35` 改为 `if message is None or message == ""`，空字符串与 None 一致处理，返回纯 suffix。
- **验证**: 测试 `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_empty` 传入 `message=""` + `client_correlation_id`，断言结果为 `"client_correlation_id=..."`，无前导换行。
- **判定**: 空字符串不再产生前导空行，durable payload 语义不变（helper 仅影响投影输出），通过。

### Rejected finding: _lost_host_event suffix — ✅ RESPECTED

- **Controller decision**: 不在本 WU 要求 `_lost_host_event` 追加诊断后缀。
- **当前状态**: `read_api.py:1033-1060` 的 `_lost_host_event` 未调用 `_append_terminal_diagnostic_suffix`，直接从 payload 读取 `message` 字段。`_append_terminal_diagnostic_suffix` 仅在 `_failed_host_event`（line 986）和 `outbox._error_message`（line 398）调用，仅对 `RUN_FAILED` 生效。
- **验证**: `git diff 913875da -- dayu/host/read_api.py | grep "_lost_host_event"` 返回空（exit 1），确认 `_lost_host_event` 未被修改。
- **判定**: 尊重 controller 裁决，通过。

## Findings

未发现实质性问题。四项 accepted findings 均已闭环，一项 rejected finding 被尊重。

## Open Questions

- 无。

## Residual Risk

- `_lost_host_event` 未来同步风险（已在 code-review-20260620-213746.md 记录）：若后续 `_lost_lifecycle_plan` 开始携带 correlation ids，`_lost_host_event` 不会自动展示。该风险由 controller 明确接受为本 WU 范围外，建议在触发 `_lost_lifecycle_plan` 变更的 WU 中复核。
- Tool Trace 的 `_extract_diagnostic_trace` skip 条件已从"仅 provider_request_id is None"收紧为"两者都 None"，与 plan scope 一致；其他 event type 的 skip 逻辑未受影响，不存在回归风险。

## Verification Summary

| 检查项 | 结果 |
|--------|------|
| `pytest tests/host/test_terminal_diagnostics.py tests/host/test_read_api_terminal_policy.py tests/host/test_tool_trace_projection.py tests/host/test_tool_trace_queries.py` | 51 passed |
| `pytest tests/engine/runners/openai/test_runner_diagnostics.py tests/engine/runners/openai/test_streaming_capability_and_content_type.py tests/service/test_host_assembly.py` | 62 passed |
| `pyright` | 0 errors, 0 warnings, 0 informations |
| `git diff --check` | passed |
