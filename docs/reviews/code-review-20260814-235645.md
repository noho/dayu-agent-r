# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `5508d044` (accepted plan commit)
- Output file: `docs/reviews/code-review-20260814-235645.md`
- Included scope: S1 `upload-filing-ticker-alias-contract` workspace changes — 17 production files, 19 test files (含 1 新增 `test_ticker_normalization.py`)
- Excluded scope: S2 domain commit contract、storage unique route、typed conflict、read schema/failure projection、README；均按 accepted plan 冻结于 S2
- Parallel review coverage: 无

## Findings

### 1-accepted-[低]-sec_6k_primary_document_repair.py branch coverage 76%

- **入口/函数**: `sec_6k_primary_document_repair` 模块全局
- **文件(行号)**: `dayu/fins/pipelines/sec_6k_primary_document_repair.py` (lines 117, 122, 136, 182, 186, 193, 291, 301-303, 305→289, 334-341, 365, 368, 401-402, 420, 425, 428, 430, 489-495, 568, 636, 675)
- **输入场景**: 正常执行路径已覆盖；未覆盖行主要属于异常/恢复/CLI分支
- **实际分支**: `target_tickers=None` public discovery path 已由 `test_sec_pipeline_download.py::test_standalone_6k_reconcile_publishes_source_and_processed_together` 覆盖
- **预期行为**: 每个修改生产文件 branch coverage >= 80%
- **实际行为**: 76%，低于 80% 门槛
- **直接证据**: S1 implementation report §5.2 coverage table；精确缺行清单已列出
- **影响**: 非本次 S1 identity migration 引入的行为缺口；主要影响既有异常/恢复/CLI路径的覆盖
- **建议改法和验证点**: 在 S1 fix gate 补充 owner-level tests 覆盖关键未触达分支，至少补齐到 80%；不得以 omit/pragmap/降低阈值绕过
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-accepted-[低]-read_runtime.py 一行 field projection 机械迁移

- **入口/函数**: `FinsReadRuntime._read_company_info`
- **文件(行号)**: `dayu/fins/tools/read_runtime.py:2753`
- **输入场景**: 所有 read 工具调用经过 `_resolve_canonical_ticker` → `_read_company_info`
- **实际分支**: `company_meta.ticker_identity.market` 替代 `company_meta.market`
- **预期行为**: CompanyMeta 删除旧 `market` property 后，所有 consumer 必须使用 `ticker_identity.market`
- **实际行为**: 一行机械替换，route/fallback/schema/error projection 均未改变
- **直接证据**: `git diff 5508d044 -- dayu/fins/tools/read_runtime.py` 显示单行变更；S1 implementation report §3.3 确认"仅一行 CompanyMeta field projection 机械迁移"
- **影响**: 若不迁移，pyright 会在 CompanyMeta 删除旧 field 后报错；迁移后行为完全等价
- **建议改法和验证点**: 接受为 strict CompanyMeta S1 的必要 consumer 迁移；不实现 S2 read 语义
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## S1 Residual Items（accepted plan 明确允许，非 findings）

以下项目按 accepted plan §10 S1 scope 明确保留为 S2 临时允许项：

1. **S1 route 半契约**: `resolve_existing_ticker`、`_resolve_existing_ticker_by_company_alias`、`_build_company_alias_index`、`_build_company_alias_index_from_meta` 与 read runtime 旧 fallback 仍有命中。S2 将删除前两个 route symbol 并用 `_build_unique_company_identity_index` 替换两个 list-index helpers。
2. **Late conflict**: alias index 仍为 `dict[str, list[str]]`，多 owner 在 read 时抛既有 `ValueError`。S2 实现 unique route 与 typed conflict。
3. **Snapshot/lost-update window**: `UploadCompanyMetaDecision.company_meta` 与 `CompanyMetaRepositoryProtocol.upsert_company_meta` 暂存 final snapshot。S2 切换为 `CompanyMetaCommitIntent` 与 authoritative merge。
4. **Read contract**: read route/schema/failure projection 未修改。S2 随唯一 storage route 一起切换。

## Adversarial Failure Pass

### Compat shim scan

- `_canonicalize_ticker_alias`、`_normalize_company_ticker_aliases`、`_normalize_ticker_aliases`、`normalize_sec_ticker_aliases`、`_merge_ticker_aliases`、`_normalize_ticker_token`、`_dedupe_ticker_aliases`：**零命中**。
- multiline old `FmpCompanyInfo(canonical_ticker=..., ticker_aliases=...)`：**零命中**。
- `upsert_company_meta_for_upload`：**零命中**。
- exact legacy `company_meta.ticker/market/ticker_aliases` 与 `existing_meta.*` field access：**零命中**；原计划宽 regex 命中条目均为合法 `ticker_identity.*` 前缀。
- material scan: request aliases 仍经 service runtime 进入 SEC/CN `stage_company_meta_for_upload`，并进入统一 decision/stage，**未发生 accept-ignore**。

### V.BA / 跨市场 / DELTA,MSFT / canonical-equivalent 反例

- `V.BA` grammar: `_US_SYMBOL_PATTERN` 接受 `[A-Z]+(?:[.-][A-Z0-9]+)?`，`V.BA → V-BA` 正确。测试覆盖于 `test_ticker_normalization.py::test_normalize_ticker_supports_identity_grammar`。
- `AAPL.SW`: 同样接受为单分节 US ticker，`AAPL.SW → AAPL-SW`。测试覆盖于同一参数化用例。
- `DELTA,MSFT`: `build_company_ticker_identity("DELTA", ("MSFT",))` 保留 `MSFT` 为 accepted alias。测试覆盖于 `test_build_company_ticker_identity_trusts_distinct_declared_alias`。
- canonical-equivalent dedupe: `build_company_ticker_identity("AAPL", ("AAPL", "US.AAPL"))` 产生零 accepted aliases。测试覆盖于 `test_build_company_ticker_identity_stably_deduplicates_equivalent_aliases`。
- material `V.BA` alias 贯通: `test_upload_material_stream_uploads_docling_files` 断言 `accepted_aliases == ("APC", "V-BA")` 且 `resolve_existing_ticker(["V.BA"]) == "AAPL"`。

### Fresh / stale material alias 丢失

- Fresh meta new alias stage: `test_upload_filing_stream_preserves_same_version_company_meta` 断言 `accepted_aliases == ("OLD", "NEW")`。
- Fresh meta no-new-alias keep: `test_upload_filing_stream_uploads_docling_files` 中 fresh meta 无新增 alias 时 disposition 为 `keep`。
- Stale meta alias merge: `test_upload_filing_stream_refreshes_stale_company_meta` 断言 `accepted_aliases == ("STALE", "APC")`。
- Material fresh/stale: `test_upload_material_stream_auto_action_and_overwrite_reset` 断言 `accepted_aliases == ("OLD", "NEW")` 且 `company_name == "Apple Inc."`（不被 update 的 company_name 覆盖）。

### 6-K branch

- `_resolve_target_tickers` 机械迁移到 `entry.company_meta.ticker_identity.canonical_ticker`。
- `test_standalone_6k_reconcile_publishes_source_and_processed_together` 已扩展为 `target_tickers=None` public-path discovery test，断言 canonical projection。

### 测试弱化检查

- 删除的 `test_upload_company_meta_ticker_aliases_use_canonical_owner` 测试的是已被删除的 `_normalize_ticker_aliases` 函数；新 `test_ticker_normalization.py` 通过 `build_company_ticker_identity` 覆盖相同行为且断言更严格（canonical 不进 aliases）。
- 所有旧 `CompanyMeta(ticker=..., market=..., ticker_aliases=...)` 构造已迁移为 `CompanyMeta(ticker_identity=build_company_ticker_identity(...), ...)`。
- 旧 `company_meta.ticker_aliases == ["AAPL", "APC"]` 断言已迁移为 `company_meta.ticker_identity.accepted_aliases == ("APC",)`（canonical 不再重复）。
- 未发现为适配实现而削弱的 assertion。

## Open Questions

无。S1 scope、owner boundary、residue 允许项与 coverage gap 均由 accepted plan 和 S1 implementation report 明确定义。

## Residual Risk

1. **6-K coverage 76%**: 精确缺行已列出（§1 finding）；需在 S1 fix gate 裁决是否补测试。
2. **S2 强制后续**: S1 只是 reviewed local commit checkpoint，不满足 workspace atomicity success signal。S2 必须实现 commit intent、authoritative merge、unique route、typed conflict 与 read contract 切换。
3. **`_resolve_target_tickers` raw normalization**: 使用 `strip().upper()` 而非 `try_normalize_ticker`；对 S1 inventory discovery 场景安全（inventory 返回 canonical），但 S2 应统一。
4. **`UploadCompanyMetaDecision.company_meta` snapshot window**: S1 单进程 fresh/stale merge 正确，但无 commit-time authoritative 保证。S2 切换为 intent 后关闭。
5. **Regression baseline failures**: 6 项既有失败不在本次 changed path，按 controller triage 处理。

## Review Conclusion

S1 implementation 与 accepted plan 的 owner contract 一致：`CompanyTickerIdentity` builder 是 grammar/canonicalization/stable dedupe 的唯一 owner；`CompanyMeta.ticker_identity` 是 durable meta 的结构化真源；FMP/SEC/CN/CLI/tool 全链复用同一 builder；material aliases 数据流从 request 贯通到统一 decision/stage；storage 消费 `lookup_tickers()` 投影。未发现 compat shim、accept-ignore、S1 scope 越界或测试弱化。`read_runtime.py` 一行机械迁移接受为 strict CompanyMeta 的必要 consumer。6-K 76% coverage 作为 accepted fix finding 进入 S1 fix gate。

**结论**: S1 checkpoint 通过 code review，可创建 accepted S1 local commit 并继续 S2。
