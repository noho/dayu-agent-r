# Code Review 2026-05-06 10:06

## 1. Findings

1. [Important] `dayu/engine/README.md` 与 Phase 5 当前代码事实不一致，且 README 守护测试没有随能力边界更新。

   位置：

   - `dayu/engine/README.md:28`
   - `dayu/engine/README.md:97`
   - `dayu/engine/README.md:171`
   - `tests/engine/test_engine_readme_phase2.py:47`

   问题：

   当前 `dayu/engine/agent.py` 已实现 `finish_reason=length` continuation，并通过 `AgentPolicy.continuation_prompt`、`continuation_max_attempts`、`tools=()` 禁工具续写和 partial content 拼接收口。但 Engine README 仍写着：

   - Engine 不负责 `context budget、continuation 或语义压缩`。
   - `LENGTH -> final_answer(filtered=False)`，当前不 continuation。
   - `context budget / continuation 尚未落地`。

   同时 `tests/engine/test_engine_readme_phase2.py` 仍把 `"当前支持 continuation"` 放在 forbidden claims 中，却没有要求 README 描述当前已落地的 `finish_reason=length` continuation、`tools=()`、次数限制和 `content_filter` 不续写边界。因此测试可以通过，但它守住的是旧事实。

   影响：

   这违反 `AGENTS.md` 的 README 同步规则：`dayu/engine/` 修改后，属于 Engine README 职责范围的当前接口、状态机和事件流事实必须同步。若直接合入，后续 Host / review Agent / 人类维护者会基于 README 误判 Engine 当前不支持 continuation，也可能继续按旧 Phase 2 行为设计调用与测试。

   建议：

   - 更新 `dayu/engine/README.md`，只写当前已落地事实：`finish_reason=length` 会在预算内触发 final-answer text continuation；continuation 轮固定 `tools=()`；达到 `continuation_max_attempts` 或 `max_iterations` 边界时以 degraded final 收口；`content_filter` 不 continuation；context overflow / context compaction / OLD `TruncationManager` / fetch_more 仍未落地。
   - 更新 `tests/engine/test_engine_readme_phase2.py` 的命名和断言，使其要求 README 覆盖 Phase 5 continuation 当前事实，同时继续禁止把 context budget、context compaction、fetch_more、run_suspended 等未来能力写成已可用。

## 2. 结论

原始 review 结论为不通过。未发现 Phase 5 continuation 生产代码本身存在明确正确性缺陷；原阻塞点是文档与文档守护测试没有跟上已落地能力。修复状态见下一节，当前阻塞项已收口。

## 2.1 修复状态

1. [Important] `dayu/engine/README.md` 与 Phase 5 当前代码事实不一致，且 README 守护测试没有随能力边界更新。

   状态：已修复。

   修复文件：

   - `dayu/engine/README.md`
   - `tests/engine/test_engine_readme_phase2.py`

   修复内容：

   - README 已同步 `finish_reason=length` 的 final-answer text continuation 当前事实。
   - README 已明确 continuation 轮固定 `tools=()`，不进入普通 tool loop。
   - README 已明确达到 `continuation_max_attempts` 或 `max_iterations` 边界时以 `final_answer(degraded=True)` 收口。
   - README 已明确 `CONTENT_FILTER` 不 continuation。
   - README 仍明确 context overflow / context compaction、OLD `TruncationManager` / `fetch_more`、awaiting / `run_suspended`、Host resume 未落地。
   - README 守护测试已改为要求覆盖 Phase 5 continuation 当前事实，并继续禁止把后续能力写成当前可用。

   验证：

   ```bash
   source .venv/bin/activate && pytest tests/engine/test_engine_readme_phase2.py -q
   ```

   结果：2 passed。

   ```bash
   source .venv/bin/activate && pytest tests/engine/test_agent_phase2.py tests/engine/test_agent_phase3_tool_call.py tests/engine/test_engine_readme_phase2.py -q
   ```

   结果：45 passed。

   ```bash
   source .venv/bin/activate && pyright
   ```

   结果：0 errors, 0 warnings, 0 informations。

   ```bash
   git diff --check
   ```

   结果：通过，无 whitespace 问题。

## 3. Review 范围

本轮 review 范围为当前仓库工作区相对 `main` 的全部变更，当前分支为 `refactory/phase_5`，包含：

- 当前分支相对 `main` 已提交但未合入的 Phase 5 计划/设计文档。
- 当前工作区未提交的 Phase 5 continuation 代码与测试。
- 当前工作区未跟踪的 Phase 5 review 产物。

重点查看文件：

- `dayu/engine/agent.py`
- `dayu/engine/contracts/agent_policy.py`
- `tests/engine/test_agent_phase2.py`
- `tests/engine/test_agent_phase3_tool_call.py`
- `dayu/engine/README.md`
- `tests/engine/test_engine_readme_phase2.py`
- `docs/engine/migration-plan.md`
- `docs/engine/phase5-plan.md`
- `docs/engine/design.md`
- `docs/host/design.md`

## 4. 检查摘要

- Phase 5 实现保持在 `finish_reason=length` continuation 范围内，未发现偷跑 context overflow、`context_compaction_requested`、trigger ratio、Engine compact/retry、OLD `TruncationManager` / fetch_more、Host wait record / resume / `run_suspended`。
- continuation 轮固定 `tools=()`，非法 tool call fail-closed，不执行 ToolExecutor。
- 多轮 partial content 拼接、次数限制、`max_iterations` 边界、`content_filter` 不续写和取消优先均有测试覆盖。
- `AgentPolicy` 新增 `continuation_prompt`，签名严格类型化，未发现新增 `Any` / `object` / 无类型参数。
- 当前剩余问题集中在 README 当前事实与 README 测试仍停留在旧状态。

## 5. 验证

已运行：

```bash
source .venv/bin/activate && pytest tests/engine/test_agent_phase2.py tests/engine/test_agent_phase3_tool_call.py tests/engine/test_engine_readme_phase2.py -q
```

结果：45 passed。

```bash
source .venv/bin/activate && pyright
```

结果：0 errors, 0 warnings, 0 informations。

```bash
git diff --check
```

结果：通过，无 whitespace 问题。
