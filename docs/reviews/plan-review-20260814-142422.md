# UF-FIX04 shared-calendar-year-validation plan re-review（AgentDS 独立复核）

## Review record

- Reviewer: AgentDS（独立 re-review；不与 AgentMiMo 的 re-review artifact 共享证据，全部证据本机独立采集）
- Gate: `plan re-review`
- Work unit: `UF-FIX04 shared-calendar-year-validation`
- Reviewed target: `docs/reviews/wu-upload-filing-calendar-year-validation-plan-codex.md`（plan fix 完成后版本）
- Review inputs（完整读取）:
  - `docs/reviews/wu-upload-filing-calendar-year-validation-plan-codex.md`
  - `docs/reviews/wu-upload-filing-calendar-year-validation-plan-fix-codex.md`
  - `docs/reviews/plan-review-20260814-134334.md`（AgentMiMo 原 review）
  - `docs/reviews/plan-review-20260814-135603.md`（AgentDS 原 review）
  - `docs/reviews/wu-upload-filing-calendar-year-validation-plan-review-controller-adjudication.md`
  - `docs/reviews/wu-upload-filing-calendar-year-validation-goal-confirmation-controller.md`
  - `docs/reviews/plan-review-20260814-141854.md`（AgentMiMo 的 re-review，用于交叉核对其 R1 finding）
- Scope: 只读复核。逐项复核 controller 已接受 findings 的修复状态（只允许 已修复/未修复/部分修复/证据失效）；验证 rejected/deferred 项的理由与 owner 是否仍成立；重点核对六项：strict full-date `0001..9999` 与 fiscal year `1000..9999` 解耦、tool raw date 不 strip、单一中立 message、read-runtime consumer、真实可达 coverage、UF-FIX01 baseline 处理。
- 未修改 plan、产品代码、测试、README 或冻结文件；未 stage、未 commit；未运行 UF-PF04。
- Artifact path: `docs/reviews/plan-review-20260814-142422.md`

## Assumptions tested（本机独立验证）

| # | assumption | 验证方式 | 结论 |
| --- | --- | --- | --- |
| A1 | `_filing_upload_request_identity` 在 workspace state read 前完成 static 校验 | 读 `ingestion_runtime.py:968-984`（先调 `_validate_fins_upload_filing_static`）；`service_runtime.py:80-86`（identity 之后才构造 repository 并 read）；`_validate_runtime_upload_request` `4195-4204`（同序）；`prepare_observed_upload`（3311 起，validate 在 observation 创建前） | 成立 |
| A2 | 当前 year 校验缺口是 `0/999/10000` 与仅长度校验的日期（`-1`/bool 已拒） | 读 `ingestion_runtime.py:892-909` | 成立，plan §4 现状描述准确 |
| A3 | tool 路径当前在 domain admission 前 strip 日期 | 读 `upload_tools.py:312-313` + `_ingestion_tool_helpers.py:138-157`（返回 `value.strip()`，空串抛 ValueError） | 成立 |
| A4 | CLI 日期可 raw 传递（argparse 无预处理） | 读 `arg_parsing.py:1107-1108`（`--filing-date`/`--report-date` 无 type=） | 成立 |
| A5 | strict full-date `0001..9999` 由 `datetime.date` 真实决定 | Python 3.11 语义：`date(1,1,1)`/`date(999,12,31)` 合法且 round-trip canonical，`date(0,...)` 拒绝 | 成立 |
| A6 | `normalize_fiscal_year` 唯一生产 direct consumer 是 read_runtime | `grep -rn normalize_fiscal_year dayu/`：仅 `read_runtime.py:627` + domain 模块自身 | 成立 |
| A7 | read path 收紧后 fail closed（ValueError 不被吞） | 读 `read_runtime.py:2410-2416`、`2550-2557`（只 catch FileNotFoundError）、`3061`（无守卫） | 成立 |
| A8 | plan §12 S1 三文件 coverage 集合对 `filing_semantics.py` 真实可达 >=80% | 本机运行 plan §12 S1 集合（`test_fiscal_normalization_contracts.py` + `test_read_runtime_semantic_ownership_guards.py` + `test_sec_pipeline_download.py`）：160 passed；`filing_semantics.py` 115 stmts/17 miss = **85%** statement（branch 口径 81%） | 成立 |
| A9 | UF-FIX01 baseline 失败节点当前仍稳定失败，且 node 名与 plan §12 字符串精确一致 | 本机运行 `tests/fins/test_fins_ingestion_tools.py::test_upload_tool_accepts_local_file_outside_workspace_without_source_side_effect`：FAILED（1 failed） | 成立 |
| A10 | 删除 `FILING_DATE_TOO_LONG`/`REPORT_DATE_TOO_LONG` 无其它消费者 | `grep -rn` 全仓库：仅 `ingestion_runtime.py` 引用（测试断言旧 message 位于 `test_fins_ingestion_runtime.py:1154`，plan 已要求 closed mapping test 同步更新） | 成立 |
| A11 | download year-only/year-month 收紧到 `1000` 下界不破坏冻结 evidence | 扫 `docs/cli_ci_scenarios.json`：全部显式年份仅 `2024/2025/2026`，无 <1000 的 date bound | 成立 |
| A12 | CLI `upload_filings_from` / material 入口不在改动范围 | 读 `fins.py:683-684`（prevalidate 仅处理 `COMMAND_UPLOAD_FILING`）、`351-352`（batch 入口保持 `_optional_stripped_text`）、`734-735`（material stream 保持） | 成立 |
| A13 | delete 动作的 fiscal_year 要求与现状一致（plan S2 invariant 无行为变化） | 读 `ingestion_runtime.py:892-895`（fiscal_year 检查无条件，无 delete 豁免） | 成立 |
| A14 | 工作树未被本 review 改动 | `git status --short`：仅 untracked review docs；coverage 文件写入 /tmp 已删除 | 成立 |

## Finding disposition — controller accepted findings

### AgentMiMo 01 — `parse_calendar_year(JsonValue)` API 过宽
- **状态**: `已修复`
- **验证证据**: plan §5 签名为 `parse_calendar_year(value: int, *, field_name: str = "year") -> int`；§5 明确 `normalize_fiscal_year(JsonValue | None)` 在同一 owner 先拒绝 bool/非 int、完成 `JsonValue -> int` narrowing 再机械委托；§10 S1 exact changes #3 落实。当前代码 `filing_semantics.py:327` 确为宽 `JsonValue | None` 签名，与修订后 plan 对比成立。

### AgentMiMo 05 — read-runtime direct consumer 回归缺口
- **状态**: `已修复`
- **验证证据**: `tests/fins/test_read_runtime_semantic_ownership_guards.py` 存在且直接 import `_parse_source_document_meta`（第 51 行）并有 meta 级测试（937/965 行）；该文件已列入 plan §10 S1 allowed files、§12 S1 coverage 集合与命令；本机运行 3 文件集合 160 passed 包含该文件。

### AgentDS 1 — tool 路径 strip 与 strict admission 矛盾
- **状态**: `已修复`
- **验证证据**: 当前代码 drift 事实本机确认（`upload_tools.py:312-313` 用 `_optional_nullable_text`，helper 返回 strip 值）。plan §6 定义 filing-only 窄 raw nullable reader（missing/null -> `None`，string 原样，非 string 抛 ValueError），§7 production files #5 列入 `upload_tools.py`，§9 tool assertions 覆盖 empty/blank/padded whitespace 原样送达 admission，§10 S2 allowed changes #5 明确；material/company 继续旧 helper。原 S2 "必然触发" 的 stop condition 已被已收敛决策替换。

### AgentDS 2 — full date 与 fiscal-year 范围过度耦合
- **状态**: `已修复`
- **验证证据**: plan §5 `parse_iso_calendar_date` "不调用 `parse_calendar_year`"，公历域 `0001..9999`；§6 download full-date 分支只委托 date owner（`0999-12-31` 合法）；§8 download contract 明确 "year-only/year-month 的 year <1000 由 shared year owner 判为 invalid；full-date 的 0001..0999 继续合法"；§11 矩阵接受 `0001-01-01`/`0999-12-31`、拒绝 `0000-12-31`。与 controller decisions #1/#2/#3 完全一致。

### AgentDS 3 — coverage 测量集不可达
- **状态**: `已修复`
- **验证证据**: **本机独立实测** plan §12 S1 三文件集合对 `filing_semantics.py` 达 **85%** statement（>=80% 门槛）。原 review 的 63% 出自不含 `test_read_runtime_semantic_ownership_guards.py` 与 `test_sec_pipeline_download.py` 的旧 4 文件集合；修订后 plan §12 为五个修改生产文件分别绑定真实直接测试集合，每个 coverage 命令保留自身 exit code、不用 pipeline 掩盖。

### AgentDS 4 — affected tool test file 预存失败
- **状态**: `已修复`
- **验证证据**: **本机独立复现** baseline node 仍稳定 FAILED；plan §12 记录的 node 字符串与实际测试函数名精确一致；S2 完整文件命令保留真实非零 exit 且要求失败集合与唯一 baseline 精确相等、任何变化立即停止；coverage 命令才允许 deselect 该 node；§14 residual risk #4 记录 owner=`UF-FIX01 follow-up`。fixture 顺手修复按裁决明确不接受。

### AgentDS 5 — tool outcome 暴露 CLI flag 文案
- **状态**: `已修复`
- **验证证据**: 当前 `_USAGE_MESSAGES`（`ingestion_runtime.py:762-767`）确带 `--fiscal-year`/`--filing-date`/`--report-date` 前缀，`FinsUploadUsageError.__init__` 以 message 初始化 str，tool 路径 `message=str(exc)`（`upload_tools.py:104-110`）直达 LLM——finding 证据成立。plan §8 三个 year/date code 各只有一份业务中立中文 message（`财年（fiscal_year）必须是 1000..9999 的整数` 等），不含 `--`，CLI 与 LLM tool outcome 共同消费，closed mapping test 同步更新（旧 message 断言位置已识别）。

### AgentDS 7 — `normalize_fiscal_year` consumer 表述不准确
- **状态**: `已修复`
- **验证证据**: 本机 grep 确认唯一生产 direct consumer 为 `read_runtime.py:627::_parse_source_document_meta`；plan §3/§4 已更正，processor/pipeline 表述为 fiscal facts producer 而非 parser consumer。

## Finding disposition — controller rejected findings（理由复核）

### AgentMiMo 02 — `1000..9999` 缺少直接裁决
- **Decision**: `rejected-with-reason`。**复核结论**: 理由仍成立。goal artifact 第 25 行逐字确认 "required upload filing `fiscal_year` 必须是非 bool 的 `1000..9999` 整数"；plan §4 已补充直接来源引用而非推断。不伪报修复，正确。

### AgentMiMo 03 — download full-date canonicalize 规格不足
- **Decision**: `rejected-with-reason`。**复核结论**: 理由仍成立。plan §6 明确 `f"{year:04d}-{month:02d}-{day:02d}"` canonical 文本后委托 owner；§11 download 矩阵含 `2024-2-9` 接受回归。不伪报修复，正确。

### AgentMiMo 04 — CLI test file 跨 S2/S3
- **Decision**: `rejected-with-reason`。**复核结论**: 理由仍成立。plan §10 S3 prerequisites 仍为 "S1、S2 accepted"，Gateflow 按序实施，同一测试文件有序追加无并行 ownership 冲突。不伪报修复，正确。

### AgentMiMo 06 — service_runtime 文件定位
- **Decision**: `rejected-with-reason`。**复核结论**: 理由仍成立。plan §4 direct evidence #2 保留完整 path/function；`service_runtime.py` 不在 allowed production files 且 implementation 不修改它（本 review A1 确认其调用链无需改动）。不伪报修复，正确。

## Finding disposition — controller deferred findings（owner 复核）

### AgentDS 6 — `upload_filings_from` 仍 strip metadata date
- **Decision**: `deferred-with-owner`。**复核结论**: owner 仍成立。plan §2 non-goals 明确不改 batch 入口 strip 行为；§13 README 只承诺 direct `upload_filing` / filing tool admission、不泛化 batch parity；§14 residual risk #7 记录 owner=`upload_filings_from metadata strictness parity`。本 review A12 独立确认 batch 入口（`fins.py:351-352`）与 material stream（734-735）不在 S2 改动范围。deferred 项忠实保留，正确。

## 交叉复核：AgentMiMo re-review 的 R1

### R1-未修复-低-§6 raw reader 的"非 string 拒绝"措辞与 §10 S2 #5 精度不一致
- **我的独立评估**: 证据成立但严重程度低、不阻塞。§10 S2 #5（"Exact allowed changes"，即实施指令）明确 "非 string 抛 `ValueError`"，无歧义；§6 是裁决语义层描述。tool 边界 `except ValueError -> ToolFailedOutcome(error="invalid_argument")`（`upload_tools.py:104-110`）是既有投影机制，与其它字段 helper 模式一致。可选文案微调（§6 补 "抛 ValueError"），不要求 plan fix gate 重开。
- **状态**: `未修复`（低，非阻塞；实施指令层面无歧义）。

## 本 review 新 observation（非 finding，已分类）

1. **goal artifact 措辞与 download year 域裁决的文字张力**
   - goal artifact 成功信号 "download 的合法 year/year-month/full-date…保持成立"，而 controller decision #1 将 download year-only/year-month 的 year 域收紧为 `1000..9999`（当前代码 `dt.date(999,...)` 接受 year-only "0999"，修复后拒绝）。controller 已裁决并声明来源为用户原始目标闭区间，plan 忠实实现；A11 确认冻结 scenarios 无 <1000 用例、不破坏冻结 evidence。
   - 建议：implementation closeout 时在 goal artifact 或 control doc 记录该裁决引用，避免后续工作误读 "保持成立"。
   - 分类：controller 已裁决；非 plan 缺陷。

2. **S1 coverage 边际为 85%（statement），依赖新增 owner matrix 测试守住 >=80%**
   - 本机实测是未实现状态下的 85%；实现新增两个函数后语句基数增加，plan §10 S1 #4 / §11 矩阵要求 exact positive/negative 全覆盖。plan 的 `--fail-under=80` 与 S1 stop condition 使门槛自执行。
   - 分类：`fixed in current slice`（验证边界内自执行）；低。

3. **其它 usage codes 仍带 `--` 前缀（如 INVALID_TICKER）**
   - 本 work unit 只对三个 year/date code 承诺业务中立 message，其它 code 的 LLM-facing 风格统一不在 scope；plan 未声称修复，非 goal drift。
   - 分类：范围外；如需统一建议另立 work unit。

## Open questions

无阻塞性 open question。三处 observation 均已给出分类与去向（见上）。

## Residual risks and suggested tracking destination

| # | 风险 | 分类 | Owner / 去向 |
| --- | --- | --- | --- |
| 1 | `UF-PF04` 真实 CLI evidence 未执行 | assigned to later work unit | `UF-PF04` |
| 2 | 其它 upload findings | assigned to later work unit | `UF-FIX01/02/03/05...` |
| 3 | `upload_filings_from` raw-date parity | assigned to later work unit | `upload_filings_from metadata strictness parity` |
| 4 | tool 预存 fixture failure（baseline） | assigned to later work unit | `UF-FIX01 follow-up`；本 work unit 只证明无新增/扩散 |
| 5 | 历史非法 fiscal year read fail-closed | fixed in current slice | owner contract + read-runtime regression（A6/A7 已验证路径） |
| 6 | CLI 静默 strip upload date 从接受变 usage error | fixed in current slice | strict admission contract（A4 已验证 raw 传递可行） |
| 7 | download 与 upload 格式严格度差异 | fixed in current slice | wrapper-owned shape + shared owner 解耦（A5 已验证） |
| 8 | goal artifact 措辞与 download year 域裁决张力 | controller 已裁决 | closeout 时回写 goal artifact 或 control doc 引用 |
| 9 | S1 coverage 边际 85%，依赖新 owner matrix | fixed in current slice | S1 `--fail-under=80` + stop condition 自执行 |

没有 `unclassified residual risk`。

## Docs decision

- `dayu/fins/README.md`: 需要更新。区分 full calendar date `0001..9999` 与 fiscal/download partial year `1000..9999`；strict raw admission 限定到直接 `upload_filing` 与 `start_fins_upload` filing branch。与 plan §13 一致。
- 根 `README.md`: 需要更新。只写最终用户可见 download 与直接 upload filing 规则及 exit 2；明确不覆盖 `upload_filings_from` batch parity。与 plan §13 一致。
- `tests/README.md` / `dayu/README.md`: 不更新；Host/Engine README 不触发。理由符合各自写作边界（A10 无代码层变化）。

## Final plan review conclusion

**pass-with-risks**

复核结论：
- controller 接受的 8 项 findings 全部为 `已修复`，且每一项均有本机独立采集的直接证据支撑（含 §12 S1 coverage 三文件集合实测 85%、UF-FIX01 baseline 失败独立复现、tool strip 链路与 message 投影代码级确认、read-runtime 唯一 consumer 与 fail-closed 路径确认）。
- 4 项 rejected findings 的驳回理由与 1 项 deferred finding 的 owner 均仍成立，fix artifact 未伪报修复。
- AgentMiMo R1 交叉复核为低严重度措辞差异，不阻塞。
- 三项新 observation（goal 措辞张力、coverage 边际、其它 code 的 `--` 文案）均低严重度且已分类，无 unclassified residual risk。
- 修订后 plan 是 code-generation-ready：owner/API/输入裁决/错误 contract/consumer boundary/测试位置/README 决策全部收敛，无目标漂移、无过度耦合、无过度设计。

Risk 均不要求 plan fix gate 重开；controller 可按裁决确认 accepted plan 并进入 implementation S1。

## Next entry point

controller 确认 accepted plan -> implementation `S1-domain-calendar-year-owner`
