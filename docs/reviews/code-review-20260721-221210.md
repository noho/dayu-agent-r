# Code Review

## Scope

- Mode: current changes
- Branch: `phaseflow/wu-host-session-event-delivery-01`
- Base: `64383186` (accepted Slice 1 commit)
- Output file: `docs/reviews/code-review-20260721-221210.md`
- Included scope: `dayu/host/engine_ingest.py`、`dayu/host/transient_delta.py`、`dayu/host/open_host.py`、`tests/host/test_engine_ingest_mapping.py`、`tests/host/test_transient_delta.py`、`tests/host/test_watch_session_events.py`、`tests/host/test_open_host_runtime.py`
- Excluded scope: `docs/host/issues-implementation-control.md`（Controller-owned，不产生 code finding）
- Parallel review coverage: 无

## Findings

未发现实质性问题。

逐项 adversarial 验证结论：

### 1. Same-transaction Attempt fence

**结论：PASS。** `_validated_transient_delta_candidate(...)` 在 `engine_ingest.py:5270-5272` 直接读取 `context.attempt.started_event_sequence`，该 context 来自同一 validation write transaction 内的 current `AttemptRow`。`_AttemptFenceReadSpy` 测试（`test_engine_ingest_mapping.py:2645-2693`）monkeypatch `read_attempt_by_id` 并注入唯一 sentinel fence，断言 `attempt_read_spy.call_count == 1` 且 publisher candidate 精确携带该值。不存在 post-transaction latest/max readback、Run 字段反推、时间戳重算或其它间接路径。

### 2. Candidate 到每订阅独立 entry 原样复制

**结论：PASS。** `transient_delta.py:900-908` 在 hub `publish` 中对 fanout snapshot 中每个 subscription 独立构造 `HostTransientDeltaMailboxEntry`，共享同一个 immutable `HostTransientDelta` event 引用与 candidate 原样 fence 值。`test_hub_fanout_reuses_envelope_and_late_attach_has_no_replay`（`test_transient_delta.py:153-194`）断言每订阅 entry 对象独立（`shared_first_entry is not shared_second_entry`）、event 引用相同（`shared_first_entry.event is shared_second_entry.event`）、fence 值完全一致（`== 1`）。late attach 不 replay 已发布增量。

### 3. Retained accounting

**结论：PASS。** `pop_next_nowait` 把 entry 从 mailbox 转移到 `_in_flight`，不降低 `retained_items`（`len(self._mailbox) + (1 if self._in_flight is not None else 0)`）。`release_in_flight` 清除 `_in_flight` 并调用 `_refresh_readiness`。overflow 时 `prospective_retained_items` check 使用 `self.retained_items + 1`，超限设置 `_overflowed = True` 并从 fanout 排除，不入队。entry 整体只计一个 retained item。

### 4. Head 不得提前 pop

**结论：PASS。** `_watch_session_events_after` 在 pop 前先 `peek_next_nowait()` 检查 head fence：`next_cursor < head.durable_causal_fence_event_sequence` 时保留 head 并继续读页追平（`open_host.py:1110-1118`）。只有 `next_cursor >= fence` 时才 pop yield。cursor 通过逐 event / 逐页 `advance_durable_cursor` 严格单调推进。

### 5. 逐页 cursor 语义

**结论：PASS。** 非 terminal event 在 yield 时立即推进 cursor（`open_host.py:1098-1099`）。terminal event 在实际 yield 时推进（`open_host.py:1085-1086`）。页边界处理后推进到 `pending_durable_page_next_cursor`（`open_host.py:1103-1105`）。空页立即推进到 `batch.next_cursor`（`open_host.py:1165-1167`）。`advance_durable_cursor` 拒绝倒退（`transient_delta.py:597-598`）。page size 64 保持不变，`read_api.py` 无 diff。

### 6. Same-Run prefix / terminal / different-Run barrier

**结论：PASS。** `current_terminal_event` 持有已读出但未 yield 的 terminal（`open_host.py:1069-1088`）。yield 前先交付 mailbox head 中同 Run 连续 prefix：peek head 且 `head.event.run_id == current_terminal_event.run_id` 时 pop yield，不同 Run 原位保留。terminal yield 后 `current_terminal_event = None` 并推进 cursor，下一次 `anext()` 才重新评估该 head。`test_same_run_prefix_hands_off_to_terminal_before_different_run_head`（`test_watch_session_events.py:1324-1401`）精确断言顺序为 `A delta -> A terminal -> B delta`。

### 7. Overflow prefix 后立即错误优先级

**结论：PASS。** overflow 检查（`open_host.py:1059-1065`）在 terminal、durable page 和 mailbox pop 之前执行。已接受的 mailbox prefix 逐项 pop yield，prefix 耗尽后下一次 `anext` 立即 `raise overflow_error`。即使 durable page 已读出 terminal，也不得插入 prefix 与 error 之间。测试 `test_capacity_slow_watcher_overflow_does_not_block_fast_watcher_or_terminal`（`test_watch_session_events.py:1278-1309`）要求前 512 项全部为按 `worker_event_index=1..512` 排序的 `HostTransientDelta`，下一次拉取立即得到 overflow error。

### 8. 每 timeout 最多一页

**结论：PASS。** mailbox 空且无 local watermark 领先时，`wait_for_readiness` 返回 `False`（timeout）后设置 `should_read_durable_page = True`（`open_host.py:1138-1139`）。读取一页后回到循环顶部，再次进入 wait 分支。空页时 `next_cursor` 不前进（`_ReadSessionHostEventsAfterOperation` 对空扫描返回 `self.cursor`），`advance_durable_cursor` 幂等接受相同值。`test_dual_opener_empty_mailbox_reconciles_one_page_per_timeout_and_close_wakes`（`test_watch_session_events.py:1563-1711`）精确断言 `waiter_c.timeout_count == len(page_read_spy.cursors)`。

### 9. Host close 立即 EOF

**结论：PASS。** `_close_owned_resources` 调用 `self._health_gate.begin_closing()`，设置 readiness 并唤醒等待。iterator loop 中 `self._health_gate.state in (CLOSING, CLOSED)` 分支（`open_host.py:1141-1152`）`await wait_for_readiness`，readiness 已 set 则立即返回。loop 重新检查 `subscription.is_closed` 并退出。close 测试断言 `next_task` 在 0.5 秒 gate 内正常 `StopAsyncIteration`，不依赖真实 sleep 缩短。

### 10. 双 opener 只共享 DB 且 local hook 零调用

**结论：PASS。** `test_dual_opener_b_fence_catches_up_pages_before_terminal_handoff`（`test_watch_session_events.py:1404-1560`）构造两个独立 `open_host` context：`shared_db_path` / `shared_lane_db_path` 相同，artifact roots、worker factories、Host handles、scheduler、actor/store、hub、waiter 与 lifecycle 彼此独立。monkeypatch `advance_committed_terminal_event_sequence_high_watermark` 为 `_TerminalWatermarkHookCallCounter`，断言 `local_hook_calls.call_count == 0`。跨 opener correctness 来自 B entry 的 fence 与共享 durable DB，而非 local hook 或 periodic clock。opener C 的 `committed_terminal_event_sequence_high_watermark` 始终为 0。

### 11. 无第三 sequence / 后台 task / S3 接线 / scope 越界

**结论：PASS。** `durable_causal_fence_event_sequence` 引用既有 durable EventLog sequence，不进入 public delta、trace、memory、prompt、schema 或日志。`_terminal_run_ids` 和 `mark_run_terminal` 已从 subscription 删除，production 为 0 命中。local watermark hook 在 production 只有定义与 subscription owner 推进，不存在 terminal producer/coordinator/promotion 调用。`read_api.py` 无 diff，page size 保持 64。无新增 timer loop、per-watcher background task 或第三 sequence。Service/CLI 无 diff。`_SessionEventReconciliationWaiter` Protocol 和 `_TimedSessionEventReconciliationWaiter` 只使用 `subscription.wait_ready`，不创建后台 task。

## Open Questions

无。

## Residual Risk

1. **local watermark 接线属于 S3**：`advance_committed_terminal_event_sequence_high_watermark` 在 production 中 0 命中（terminal producer 未接线）。跨 opener 低延迟 terminal 发现当前完全依赖 B fence 与 durable page catch-up，而非 local watermark hint。S3 完成后该路径将有 production 覆盖。当前风险已由 accepted plan 归属，不构成 S2 stop condition。

2. **`read_events_after` 全局扫描 + session 过滤**：`_ReadSessionHostEventsAfterOperation` 的 `next_cursor` 基于全局扫描的最后一行 `event_sequence`，而非 session 过滤后的最后一行。当多 Session 并发写入 EventLog 时，扫描的 64 行可能全属其它 Session，导致 `events` 为空但 `next_cursor` 仍前进。这是 S1 之前的既有架构行为，S2 未引入也未改变，且测试场景中单一 Session 不触发此边界。不在本 slice scope 内。

3. **S2 测试覆盖已充分**：S2 focused 159 passed、affected suites 3410 passed、production stress 5 passed、transient delta stress 1 passed、单文件 coverage 84.28%–92.00%、pyright 0 errors。`_advance_committed_terminal_event_sequence_high_watermark` 路径的 owner-level 测试由 `test_local_terminal_watermark_is_scalar_and_tracks_durable_cursor` 覆盖。

## Gate Conclusion

**PASS。** 未发现实质性问题。所有 adversarial 验证点均通过，无 blocking open questions，residual risks 均有 accepted plan 后续 slice 归属。
