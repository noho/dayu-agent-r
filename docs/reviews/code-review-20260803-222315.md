# Code Review — S1 re-review

## Scope

- Mode: current changes
- Branch: `codex/interactive-oracle`
- Base: `21b602c1`
- Output file: `docs/reviews/code-review-20260803-222315.md`
- Review role: S1 code-review-fix re-review，聚焦 `docs/gateflow/pr-190-compactor-output-business-semantics-s1-code-review-fix-20260803.md` 与 `tests/host/test_llm_compaction.py` 的 F01/F02 修复
- Included scope:
  - `tests/host/test_llm_compaction.py`（F01 补齐 7 个 frozen semantic fragments + F02 扩展 forbidden guard）
  - `docs/gateflow/pr-190-compactor-output-business-semantics-s1-code-review-fix-20260803.md`（fix artifact）
  - prompt、manifest、CLI hash test、public smoke（验证 hash/scope 未漂移）
- Excluded scope: 已提交 plan gate 全量历史；frozen production files（`compaction.py`、`context_governance.py`、`memory.py`）；README/design；前两路 review artifacts
- Parallel review coverage: 无

## Context read

- `docs/gateflow/pr-190-compactor-output-business-semantics-s1-code-review-fix-20260803.md`（fix artifact，记录 F01/F02/F03 controller 裁决与 fix 内容）
- `docs/reviews/code-review-20260803-221641.md`（AgentDS 第二路 review，识别 F01/F02/F03，SHA-256 `d62942a5…`）
- `docs/reviews/code-review-20260803-220950.md`（AgentMiMo 第一路 review，无 finding，pass，SHA-256 `3d1547d2…`）
- `tests/host/test_llm_compaction.py`（fix 后全文，重点 line 371-436）
- `dayu/config/prompts/scenes/conversation_compaction_user.md`（验证 prompt 未漂移）
- `docs/cli_init_workspace_manifest_v1.json`（验证 manifest hash 未漂移）

## Findings

未发现实质性问题。

## Adversarial checklist

### 1. F01 — 7 个 frozen semantic fragments 是否补齐

DS review 识别的 7 项缺失 fragment 全部已在 owner test 中补齐：

| # | Fragment | Section | Test line | 验证 |
|---|---|---|---|---|
| 1 | `不得发明新结论` | `answer_anchors.detail` | 400 | ✅ grep 命中 |
| 2 | `继续保留旧内容会过时、冲突或误导` | `superseded` reason | 407 | ✅ grep 命中 |
| 3 | `replacement 中保留的是替代后的当前内容` | `superseded` reason | 408 | ✅ grep 命中 |
| 4 | `丢弃它不会损失独立业务信息` | `redundant` reason | 410 | ✅ grep 命中 |
| 5 | `不得加入材料没有的事实、结论或任务` | `session_summary.text` | 377 | ✅ grep 命中 |
| 6 | `不得把 \`trace_material\` 或 \`answer_material\` 当作事实依据` | `evidence_facts.claim` | 388 | ✅ grep 命中 |
| 7 | `不把工具证据、未来动作或新推断伪装成既有结论` | `answer_anchors` section | 396 | ✅ grep 命中 |

每项 fragment 均有独立 `assert ... in user_prompt` 断言，不再是同一 sentence 前缀匹配的间接覆盖。

### 2. F02 — forbidden guard 是否覆盖当前活跃内部术语

forbidden list 从 5 个 v1 历史术语扩展为 13 项：

| 术语 | 类别 | 覆盖方式 |
|---|---|---|
| `schema_version` | v1 历史 | 直接匹配 |
| `current_input_anchor` | v1 历史 | 直接匹配 |
| `previous_compacted_view` | v1 历史 | 直接匹配 |
| `evidence_backed_facts` | v1 历史 | 直接匹配 |
| `reference_continuity_items` | v1 历史 | 直接匹配 |
| `Compact` | 当前 typed contract 前缀 | 大小写敏感前缀，覆盖 `CompactCandidateV2`、`CompactDropReasonV2`、`CompactSessionSummaryV2`、`CompactEvidenceFactV2`、`CompactAnswerAnchorV2`、`CompactValidationIssueCodeV2`、`CompactAcceptedTruthV2` 等全部 `Compact*` 类型 |
| `compaction.py` | 内部模块名 | 直接匹配 |
| `context_governance` | 内部模块名 | 直接匹配 |
| `memory.py` | 内部模块名 | 直接匹配 |
| `MemoryProjectionPolicy` | Host 治理类型 | 直接匹配 |
| `SessionSummaryMemoryView` | Host 治理类型 | 直接匹配 |
| `event_id` | 内部治理标识 | 直接匹配 |
| `payload_ref` | 内部治理标识 | 直接匹配 |

独立验证确认 prompt 中 8 项新增 forbidden 术语全部 absent。`Compact` 使用大小写敏感匹配，不会误禁业务必需的小写 schema labels（如 `dayu.context_compaction.output.v2` 中的 `compaction`）。

### 3. F03 — duplicate 裁决是否合理

fix artifact 记录 F03 `rejected-with-reason`：F03 描述的 substring-fragment 覆盖缺口与 F01 同根，没有独立 root cause 或额外修复动作。F01 的 7 项独立语义 assertion 补齐后，F03 所述证据不再成立。

**裁决合理。** F03 的核心论点是"同一 sentence 前后半句只测前半句"，这与 F01 的"7 项 missing fragment"完全重叠。F01 的修复（逐 fragment 独立 assert）已同时关闭 F01 和 F03。不重复增加第二套测试结构是正确选择。

### 4. prompt drift 检查

- `sha256sum dayu/config/prompts/scenes/conversation_compaction_user.md` → `a2f5711c…`，与 implementation artifact 和 fix artifact 记录一致
- 独立验证 8 项新增 forbidden 术语全部 absent
- prompt 内容未被 fix 修改

### 5. hash/scope drift 检查

- `docs/cli_init_workspace_manifest_v1.json` SHA-256 → `fb6d0ba8…`，与基线一致
- `tests/cli/test_smoke_cli_init_provider_matrix.py` 中 `FROZEN_MANIFEST_SHA256` → `fb6d0ba8…`，与基线一致
- `tests/host/test_public_compact_smoke.py` 未被 fix 修改
- fix 只修改了 `tests/host/test_llm_compaction.py`，与 fix artifact 的 "Changed files" 一致
- 无 scope drift

### 6. test execution

- `pytest tests/host/test_llm_compaction.py::test_prompt_assets_are_self_contained_for_fresh_v2_contract` → `1 passed`
- `pytest tests/host/test_public_compact_smoke.py::test_default_compactor_prompt_is_llm_facing_and_self_contained` → `1 passed`
- 两个测试均在 fix 后通过，无回归

### 7. owner test 结构完整性

fix 后 owner test 的四个 section for loop 覆盖：

| Section | 断言数量 | 覆盖范围 |
|---|---|---|
| `session_summary_semantics` | 8 | 职责、source 同源、text 约束（含负向）、直接来源、null 清除（含先前摘要、其它四类） |
| `evidence_fact_semantics` | 6 | accepted evidence 直接支持、双来源、claim 负向约束、support_labels 角色、context_labels 限制 |
| `answer_anchor_semantics` | 8 | 职责（含负向伪装禁止）、title、detail（含负向发明禁止、条件/边界）、结论限制、source_labels、来源枚举 |
| `drop_reason_semantics` | 14 | 四个 reason 各有正向定义 + 负向约束，`policy_limit` 额外覆盖 cap 来源和禁止猜测，互斥声明 |

加上 forbidden guard 的 13 项和既有的 schema/source-kind/repair/open-field/example 断言，owner test 现在完整锁定 accepted plan 的全部 frozen semantics。

## Open Questions

无。

## Residual Risk

- `assigned to later work unit`：真实 provider 对字段分类、drop reason 与 repair cap 的稳定遵循度。
- `assigned to later work unit`：frozen oracle/scenario 的 current-head readiness refresh。
- `assigned to later work unit`：`forward_intents.status` 与 `reference_continuity.reason` 的 LLM-facing 业务语义。

所有 residual risk 均已分类并有 owner。无未分类 residual risk。

## Verdict

`pass`

F01 的 7 项 frozen semantic fragments 已全部补齐，每项有独立 assert。F02 的 forbidden guard 已扩展至 13 项，覆盖当前活跃的 typed contract 前缀、内部模块名、Host 治理类型和内部治理标识。F03 作为 F01 同根重复的 `rejected-with-reason` 裁决合理。prompt SHA-256、两级 publication hash、frozen files 与 public smoke 均未漂移。fix 只修改 `tests/host/test_llm_compaction.py`，无 scope drift。
