# Aggregate Deep Review Re-review：UF-FIX08 existing-source-auto-repair

## Scope

- Mode: current changes（aggregate deepreview re-review gate）
- Branch: `codex/upload-filing-oracle`
- Base: `5859856e46af42c9ae5a2a5c07fab1ba59dc91d3`
- HEAD: `65d352e6`；workspace 含 aggregate fix（未提交）：`dayu/fins/storage/_fs_source_integrity.py`、`tests/fins/test_fins_storage_atomicity.py`、`tests/fins/test_sec_pipeline_download.py` 及 fix artifact `docs/gateflow/uf-fix08-existing-source-auto-repair-aggregate-deepreview-fix-20260816.md`
- Output file: `docs/reviews/deep-review-20260816-195058.md`
- 输入：本 reviewer 的原 aggregate review（`deep-review-20260816-192359.md`）、fix artifact（含 Controller 逐项裁决）；未读 AgentMiMo 本轮 re-review artifact
- 主 reviewer 独立验证动作：
  - 全仓 pyright：`0 errors, 0 warnings, 0 informations`（亲自运行）
  - 5 个新增 fix 节点：`5 passed`（亲自运行）
  - 相关 matrix（storage_atomicity + SEC/CN download + docling service + processor consistency）：`555 passed, 3 warnings`（亲自运行）
  - full-suite evidence（`tests/fins` 1856 passed）复用 fix artifact 记录，未重跑
  - 两个真实文件系统实证实验（见 R1 直接证据）
  - scope guards：tracked diff 仅为上述 3 个授权文件；untracked 仅 docs artifact；oracle/scenario/design/evidence/registry/Host/Engine/Service/CLI 生产零 diff
- Parallel review coverage: 本轮未使用子代理；全部由主 reviewer 逐行走读 + 实证实验完成（范围集中在 ~250 行生产 diff 与 ~190 行测试 diff）

## 结论

**FAIL**。原因：M2 修复不完整 —— `manifest exists 且 trusted=false 且 actual inventory 为空` 的 whole-kind 场景中，public `list_source_integrity` 仍返回空 inventory、`classify_source_integrity_preflight` 仍返回 clean，同一 payload 的 kind-level unsafe 事实（`shared_manifest_reasons=(SOURCE_MANIFEST_UNTRUSTED,)`、`repair_blocked_reason=CROSS_SOURCE_PUBLICATION_UNSAFE`）在公开投影层被丢弃，违反 accepted plan §5.1「无法归属 target 的 root/manifest structural corruption 在任何副作用前抛 typed preflight error」的冻结承诺。该缺口与已修复的 M2（trusted manifest 悬空 ID）同根同源、差一个分支；按 Controller 对 M2 的裁决口径，属同类 blocker。其余全部 finding 已闭环或已分类，无第二处实质问题。

## Findings

### R1-未修复-中-`manifest trusted=false + actual inventory 为空` 的 whole-kind 公开投影仍 fail-open（M2 修复不完整）

- **入口/函数**: `_apply_manifest_facts`（not-trusted 分支）→ `_inspect_source_kind_unguarded`（whole-kind）→ `list_source_integrity` → `classify_source_integrity_preflight`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:1113-1118`（not-trusted 分支只把 unsafe 叠加到已有 inventory item，不生成任何 kind-level 载体）；`dayu/fins/storage/source_integrity.py:310-325`（preflight 空 inventory 无 UNSAFE item、无 repair target → clean）
- **输入场景**: source root 无任何实际 source 目录，且 manifest 存在但结构不可信（`_inspect_source_manifest` 的 trusted=False 闭集：非 regular file/symlink、JSON 不可解析、ticker 不匹配、documents 非 list、item 非 mapping、document_id 非法/重复等，`_fs_source_integrity.py:1046-1074`）
- **实际分支**: `not manifest.trusted` 分支对空 inventory 返回 `((), (SOURCE_MANIFEST_UNTRUSTED,))`；`list_source_integrity` 投影空 tuple；preflight 机械返回 `NoSourceRepairRequired`（clean）
- **预期行为**: 按 accepted plan §5.1 与 goal「manifest 与 published tree 一致性、结构不可唯一重建 fail-closed」，该场景是「无法归属 target 的 manifest structural corruption」，应在任何副作用前抛 typed `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)`——与现有 `unassignable_root_fact` 的处理（299-302 行）同口径
- **实际行为**: public list/preflight 报 clean。真实 SEC pipeline 实证（见下）越过 preflight 后，直到 company-meta batch 的 commit validator 才以裸 `ValueError("filing source publication 不满足 complete canonical manifest contract")` 失败并 rollback——fail-closed 最终达成，但发生在错误边界、错误异常类型，且不经过 designated typed gate
- **直接证据**:
  - 代码：`_fs_source_integrity.py:1113-1118` 与 `source_integrity.py:310-325`；`_fs_storage_infra.py:1115-1122`（commit validator 因 `shared_manifest_reasons` 非空拒绝——说明同一 payload 已识别该事实，仅公开投影层丢弃）
  - 实证实验 1（storage 层，真实 repository API）：`list_source_integrity("AAPL") == ()`；`classify_source_integrity_preflight((), ...) == NoSourceRepairRequired(kind='clean')`；exact `classify_source_integrity` 返回 `UNSAFE/revision=None/SOURCE_MANIFEST_UNTRUSTED`；`commit_batch` 抛上述 ValueError
  - 实证实验 2（真实 SEC `SecPipeline.download`）：preflight clean 后 pipeline 越过 whole-tree gate（provider 零调用），在 company 阶段 commit validator 抛裸 ValueError，`company_meta exists=False`、`rejections exists=False`（零 durable 副作用，但失败类型与位置均违约）
  - CN 路径未执行验证，但共享同一 commit validator；CN top-level 对 company 阶段异常无 typed 捕获，该 ValueError 将裸穿出 stream（与既有「一般 download typed terminal projection」residual 叠加）
- **影响**: designated typed whole-tree gate 被绕过；manifest structural corruption 的拒绝被推迟到 commit 边界、以裸 ValueError 呈现；SEC/CN 行为不对称（SEC 终止、CN 裸穿出）。无 durable 数据损坏（commit 兜底），但违反冻结 typed-preflight 契约与「同一事实在 preflight 与其它入口结论一致」的封闭性要求
- **建议改法和验证点**:
  - owner：保持唯一 owner 在 `dayu.fins.storage`。修复方向：`_inspect_source_kind_unguarded` whole-kind 模式在 `_apply_manifest_facts` 之后，若 `inventory` 为空且 `shared_manifest_reasons` 含 unsafe reason（`SOURCE_MANIFEST_UNTRUSTED`/`CROSS_SOURCE_INCONSISTENCY`），按现有 `unassignable_root_fact` 同口径抛 `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)`（该分支可并入 299-302 行的既有 raise）
  - 明确不得复用 dangling-id synthetic 方案：untrusted manifest 无法产出可信 document ID，伪造 identity 会违反 identity owner 语义；kind-level 事实的正确载体是 raise 路径，不是 fabricated per-item classification
  - 验证点：storage owner 测试（empty inventory + 各类 untrusted manifest → `list_source_integrity` 抛 typed preflight error）；SEC/CN workflow 测试（镜像 dangling 测试：零 provider/company/rejection 副作用、typed 异常）；commit/repair 路径回归（material whole-kind inspection 的 `except SourceIntegrityPreflightError` 转换已存在，`_fs_source_document_core.py:463-466`，不受影响）
- **修复风险（低/中/高）**: 低。raise 点在既有 whole-kind unassignable raise 机制内，非空 inventory 场景不受影响（items 已携带 unsafe 覆盖，preflight 经 item 拒绝），exact/commit 路径不变
- **严重程度（低/中/高/严重）**: 中（blocker-class：与 Controller 已接受的 M2 同类同根，typed-preflight 契约违约有直接实证）

## 原 findings 逐项最终状态

### M1（原 finding 1，canonical 目录被异源 descriptor 占据）— 最终状态：证据失效（closed）

主 reviewer 独立核验 `dayu/fins/storage/_fs_identity.py:280-282`：`_read_identity_descriptor` 在枚举模式（无 `expected_storage_key`）下执行 `storage_key = directory.name; storage_key != _derive_storage_key(namespace, identity) → ValueError("identity descriptor 与 private locator 不一致")`。因此「B 的 canonical 目录含 A 的 descriptor 且读取成功被当作 source A」的原始前提不成立——descriptor 读取必然抛 ValueError，inspector 在 277-285 行将 exact requested 目录投影为 `UNSAFE(IDENTITY_UNTRUSTED)/revision=None`，whole-kind 走 `unassignable_root_fact` → typed preflight error。新增回归 `test_canonical_source_directory_with_foreign_descriptor_is_unsafe`（filing/material × exact/staged/whole 三入口）为真实文件系统注入，5 节点全部通过。回归覆盖充分。

### M2（原 finding 2，trusted manifest 悬空 ID）— 最终状态：已修复（trusted 分支），sibling 分支残留见 R1

修复核实：`_apply_manifest_facts` 对 `manifest_ids - source_ids` 生成稳定排序 synthetic `UNSAFE(SOURCE_MANIFEST_UNTRUSTED)/revision=None` inspection（`_unsafe_source_inspection` 不伪造 meta/revision/provenance/files/canonical item，`content_classification=UNSAFE`），与 actual unsafe 投影合并后按 document ID 排序、dict 去重（悬空集与 actual 集由集合减法保证不相交）。下游逐项核实：synthetic 不可被误判为可修复（status=UNSAFE + unsafe reason + revision=None + canonical_manifest_item=None，`_all_content_complete` 为 False → `_canonical_manifest_items` 为空）；exact target 经 inventory 命中 synthetic → UNSAFE；preflight 经 UNSAFE item 抛 `UNSAFE_PUBLICATION`；commit validator 经 `shared_manifest_reasons`/非 COMPLETE item 拒绝（`_fs_storage_infra.py:1115-1122`）；guard 路径无新锁交互。新增测试 `test_dangling_trusted_manifest_projects_stable_unsafe_inventory`（filing/material）与真实 SEC workflow `test_sec_dangling_manifest_preflight_has_zero_company_or_provider_mutation`（零 provider 调用、零 company/rejection 文件、旧字节不变）均真实命中并通过。残余测试缺口：多悬空 ID + 非空 inventory 的合并排序无直接节点（排序/去重由代码确定性保证，属低风险缺口，归 storage coverage hardening）。

### Finding 3（private revision docstring）— 最终状态：已修复

`_SourcePublicationInspection.revision` docstring 已改为「content 扫描对应的可信 opaque revision；当公共 classification 为 UNSAFE 时仅是私有 content fact，不得对外投影」（diff 确认）。

### Finding 4（canonical items 与 blocked reason 并存无 gating 声明）— 最终状态：已修复

`_SourceKindPublicationInspection` docstring 已明确「消费前必须先确认 repair_blocked_reason 为 None；非空时 canonical_manifest_items 不构成可执行 repair 的授权」（diff 确认）。

### Finding 5（_ordered_reasons 非 enum 值）— 最终状态：rejected-with-reason（无 residual）

核验：签名只接受 `tuple[SourceIntegrityReason, ...] | list[SourceIntegrityReason]`，全部调用点受全仓 pyright 严格类型约束，非 enum 输入在当前 typed call sites 不可达。裁决成立。

### Finding 6（raw runtime RuntimeFileLockError）— 最终状态：deferred-with-owner（已分类）

residual 表已登记「raw runtime lock operational failure projection → Fins upload operational failure projection owner」。事实存在（job 创建前停止、无 durable 污染），不违反 UF-FIX08 integrity/repair invariant，不扩 scope 的裁决成立。

### Finding 7（手工构造 validated request 的 MISSING+update）— 最终状态：deferred-with-owner（已分类）

residual 表已登记「Fins validated-request contract hardening owner」。生产 producer 均经唯一 validator，裁决成立。

### Finding 8（SEC/CN 顶层 download generic 差异）— 最终状态：deferred-with-owner（已分类）

residual 表已登记「future general download failure projection owner」。R1 的 CN 裸 ValueError 穿出路径与该 residual 同 owner，可合并处理。

### L1-L15 低/Info 项 — 最终状态：全部 rejected-with-reason（复核成立）

逐项复核：L1（TOCTOU）caller-held guard 是 plan 冻结边界，manual writer 归 operational policy；L2（catch 闭集）无可达输入证据；L3（classification 值域）生产构造输入由 identity owner 校验，不重复值域 owner；L4（public docstring ValueError 条款）operational/capability ValueError 仍是 public Raises 的真实部分，未证明用户可见契约破坏；L5（state 早返回）公开 caller 已先行校验、不可达；L6（snapshot 双 inspection）为 accepted fail-closed 方向；L7（manifest 三键）当前 schema owner 只定义该字段集，不预埋 compatibility；L8（空 selection 死代码）保留 producer-invariant fail-loud 防御；L9-L11（AssertionError/format-error/不可达分支）fail-loud 或不可达；L12-L15（测试面）既有 storage 真实注入与 workflow 分支测试已分层覆盖。各项 rejection 理由与代码事实一致，无 residual 溢出。

## Open Questions

- 无。R1 的缺口、owner 与修复方向均有直接代码与实证证据。

## Residual Risk

| 风险 | 分类 | owner / destination |
| --- | --- | --- |
| R1 untrusted-manifest + 空 inventory 的 typed-preflight 缺口 | 当前 gate blocker | 本 gate：storage whole-kind inspector 修复 + owner/workflow 回归测试 |
| 多悬空 ID + 非空 inventory 合并排序无直接测试节点 | covered by deterministic code / low | storage coverage hardening（可选） |
| CN company 阶段裸 ValueError 穿出 stream（R1 场景下） | assigned | 与 future general download failure projection owner 合并 |
| 一般同请求并发 success/skip 收敛 | assigned | UF-FIX10 |
| fresh company meta warning | assigned | UF-FIX11 |
| material existing-source repair / 旧 schema migration | assigned | 后续独立 work unit |
| UF-PF08/UF-PF12 真实 evidence 与 registry/oracle 状态 | assigned | evidence / registry adjudication work unit |
| raw runtime lock operational failure projection | assigned | Fins upload operational failure projection owner |
| 手工构造 validated request 的非 repair 状态↔action 强化 | assigned | Fins validated-request contract hardening owner |
| SEC/CN Phase B identity churn 真实注入、material exception conversion 直接节点、repair 专用 barrier | covered by existing mechanism tests / uncovered direct nodes | 后续 download concurrency / storage coverage hardening |
| manual filesystem writer 绕过 repository lock | assigned | storage operational policy |

## Validation 记录

- 全仓 pyright（主 reviewer 亲自运行）：`0 errors, 0 warnings, 0 informations`
- 5 个新增 fix 节点（主 reviewer 亲自运行）：`5 passed, 3 warnings`（warnings 为第三方 edgar deprecated imports）
- 相关 matrix（storage_atomicity + SEC/CN download + docling service + processor consistency，主 reviewer 亲自运行）：`555 passed, 3 warnings`
- `tests/fins` 全量 1856 passed 复用 fix artifact 记录（按用户授权复用 full-suite evidence）
- 两个实证实验（storage 层与真实 SEC pipeline，主 reviewer 亲自执行）输出已写入 R1 直接证据
- scope/frozen guards：tracked diff 仅 3 个授权文件；oracle/scenario/design/evidence/registry 与 Host/Engine/Service/CLI 生产零 diff；未运行 coverage、真实 CLI/evidence；未修改 production/tests、未 commit
