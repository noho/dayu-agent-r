# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: S1 `a9f77611`, S2 `77c32c32`
- Output file: `docs/reviews/code-review-20260612-182602.md`
- Included scope: S3 uncommitted docs/control changes only
  - `docs/host/design.md`
  - `dayu/host/README.md`
  - `tests/README.md`
  - `docs/reviews/wu-cm-09-s3-implementation-report.md` (new file)
  - `docs/host/issues-implementation-control.md`
- Excluded scope: S1/S2 runtime code (already committed)
- Parallel review coverage: 无

## Review Focus

按用户指定审查维度逐项验证：

1. docs 是否准确描述已实现 behavior
2. 是否误称 snapshot 为 truth
3. 是否明确 no quarantine/rebuild/overwrite
4. maintenance issue JSON 是否不泄漏 payload
5. README 更新是否符合各 README 约束
6. tests README 是否不过度扩写
7. validation 51 passed / pyright 0 / diff clean

## Findings

未发现实质性问题。

### 验证详情

#### 1. docs 准确描述已实现 behavior

- `design.md` 新增 "memory snapshot integrity issues" 到 `run_storage_maintenance` 返回描述，与 `HostStorageMaintenanceResult.memory_snapshot_integrity_issues` 字段一致（`storage_maintenance.py:166`）。
- `design.md` 列出五类 failure kind：`invalid_json`、`schema_mismatch`、`digest_mismatch`、`unsupported_item_kind`、`storage_read_failed`，与 `MemorySnapshotIntegrityFailureKind` 枚举（`memory.py:99-110`）完全匹配。
- `host README` 新增段落描述 integrity issues "只报告分类、短错误摘要和 row identity"，与 `MemorySnapshotIntegrityIssue.json_value()`（`memory.py:178-192`）返回字段一致：`failure_kind`、`message`、`snapshot_id`、`session_id`、`consumer_id`、`policy_digest`、`checkpoint_event_sequence`。
- `host README` 新增 "memory snapshot integrity issues" 到返回描述，与 `run_storage_maintenance` 实现（`storage_maintenance.py:307-317`）一致。

#### 2. 未误称 snapshot 为 truth

- `design.md` 新增段落："Operator-facing storage maintenance 可以只读分类 damaged snapshot row，分类结果用于诊断，不是 repair authorization"。这明确将 integrity classification 定位为诊断而非 truth source。
- `inspect_memory_snapshot_integrity()`（`memory.py:500-527`）是纯只读函数，不修改任何 SQLite row。
- `host README` 使用 "只读诊断" 措辞，与实现一致。

#### 3. 明确 no quarantine/rebuild/overwrite

- `design.md` 新增："不 quarantine / rebuild / overwrite memory snapshot"。
- `host README` 新增："不 quarantine / rebuild / overwrite memory snapshot"。
- `design.md` 补充："自动 overwrite、quarantine 或 rebuild 必须另行通过显式 maintenance / operator policy 设计，不能静默发生在 command path"。
- 实现验证：`run_storage_maintenance()`（`storage_maintenance.py:248-317`）不调用任何 quarantine/rebuild/overwrite 函数；`inspect_memory_snapshot_integrity()`（`memory.py:500-527`）不修改任何 row。

#### 4. maintenance issue JSON 不泄漏 payload

- `MemorySnapshotIntegrityIssue.json_value()`（`memory.py:178-192`）只返回：`failure_kind`（枚举值）、`message`（短错误摘要）、`snapshot_id`、`session_id`、`consumer_id`、`policy_digest`、`checkpoint_event_sequence`（row identity）。
- 不包含 snapshot JSON、prompt、tool payload 或大内容。
- `HostStorageMaintenanceResult.json_value()`（`storage_maintenance.py:210-227`）对 integrity issues 只调用 `issue.json_value()`，不额外注入 payload。

#### 5. README 更新符合各 README 约束

- **host README**（约束：只写已实现的设计意图、架构边界、开发接口、公共契约；不写用户手册、测试清单、review/work unit 过程状态；不写未来计划或未落地能力）：
  - 更新描述 `run_storage_maintenance` 已实现的 integrity issues 字段和只读语义，属于"已实现的开发接口和公共契约"。
  - 未引入用户手册、测试清单或过程状态内容。

- **tests README**（约束：只记录当前 `tests/` 下已经存在的测试分层、运行方式与维护约定）：
  - 新增的测试覆盖描述对应 `test_memory_projection.py` 和 `test_storage_maintenance.py` 中已存在的测试用例。
  - 未引入不存在的测试或过度扩写。

- **design.md**：更新 `run_storage_maintenance` 行为描述和 snapshot integrity policy 段落，属于设计文档正常维护。

- **issues-implementation-control.md**：更新 gate 状态和 implementation status，属于 control doc 正常维护。

#### 6. tests README 未过度扩写

- 新增内容只描述已存在的测试：`test_storage_maintenance.py` 的 `test_storage_maintenance_reports_memory_snapshot_integrity_issue` 和 `test_memory_projection.py` 的 integrity classification 系列测试。
- 测试 README 的 bullet point 长度与既有条目一致，未引入新的测试层级或分类。

#### 7. validation 结果

- `pytest tests/host/test_memory_projection.py tests/host/test_storage_maintenance.py tests/host/test_package_exports.py -q` → 51 passed ✓
- `python -m pyright dayu/ tests/ utils/` → 0 errors, 0 warnings ✓
- `git diff --check` → clean ✓

## Open Questions

- 无

## Residual Risk

- S1 residual：identity read failure defensive branch 仍为 low-risk uncovered branch，已记录不阻塞。
- S3 无新增 runtime risk，全部为 docs-only 变更。
