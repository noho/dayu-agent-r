# Plan Review Rereview — wu-cli-download-01 Slice 1 test-only amendment

## Review Metadata

- **Reviewer**: AgentDS（独立 rereview，原 artifact `docs/reviews/plan-review-20260810-010500.md`）
- **Date**: 2026-08-10T01:15:25+0800
- **Target**: `docs/gateflow/wu-cli-download-01-slice1-plan-amendment-20260810-005537.md`（已修订）与主 plan `docs/gateflow/wu-cli-download-01-plan-20260809.md` §14.7（已修订）
- **Original review**: `docs/reviews/plan-review-20260810-010500.md`（DL-PA-01/02/03、OQ-1）
- **Exact HEAD**: `0e18abdcd1fb7edbc2fbd2e6a366580beccf5ee8`
- **Output**: `docs/reviews/plan-review-20260810-011525.md`

## Rereview Scope

逐项核验原 review 四个 findings/open questions 的收敛状态，确认 amendment 与 plan §14.7 最新 revision 已解决所有 blocker。同时检查 amendment 的 implementation checklist（§4 / plan §14.7）是否穷举了受影响 call sites、typed contract owner 校验是否归还到正确 owner、builder 行为是否保持一致。

## Convergence Verification

### DL-PA-01 → RESOLVED

**原 finding**: 三个已有 call sites 的 `start_is_explicit=True` 映射未在 amendment 中显式锁死。

**收敛证据**:

- Amendment §2 新增："三处当前都显式传入非空 `start_date`，恢复实现时必须逐处传入 `start_is_explicit=True`，不得由实现者再次推断或自行分流。"
- Plan §14.7 Scope expansion disposition 新增："三处均已显式传入非空 `start_date`，迁移时必须分别传 `start_is_explicit=True`。"
- `rg` 已验证 line 337/402 均传 `start_date="2025-01-01"`（非空），line 454 传 `start_date="2025-01-01"`（非空），语义唯一映射为 `True`。

**裁决**: 已收敛。三处 call sites 的 `start_is_explicit=True` 映射已锁死，implementation agent 无需推断。

### DL-PA-02 → RESOLVED

**原 finding**: `start_date` 非空且 `start_is_explicit=False` 的 owner 反例测试场景不可通过当前公开 API 构造，需标注为 pipeline-layer direct contract test。

**收敛证据**:

- Amendment §3 新增（第二段）:"第二项是pipeline-layer direct contract test：它绕过当前builder直接调用pipeline，目的是证明下游消费typed explicitness事实，而不是从非空日期反推。它不是公开端到端合法输入示例，不授权改变当前builder observable behavior。"
- Amendment §6 风险表新增 pipeline direct 反例行。
- Plan §14.7 新增 "Pipeline-layer反例 — 接受并限定语义" disposition。
- Plan §9 AST/static check 项更新为："pipeline-layer direct owner tests绕过builder覆盖 `start_date` 非空但 `start_is_explicit=False` 时SEC仍可按policy扩窗且CN仍启用默认业务限制；date-range owner tests断言explicit=True要求对应bound非空、双bound要求start<=end、非空bound+explicit=False合法，非法组合抛`FinsDownloadUsageError`"

**裁决**: 已收敛。反例测试语义已明确标注为 pipeline-layer direct contract test，不会误导为公开端到端示例。

### DL-PA-03 → RESOLVED

**原 finding**: `test_cn_pipeline.py` 包含 6 个 upload 测试，范围约束依赖 review 执行。

**收敛证据**:

- Amendment §3 新增："`tests/fins/test_cn_pipeline.py` 的全部现有 `test_upload_*`测试函数及其fixture/helper区域必须零diff；code rereview必须检查该文件unified diff的每个hunk，任何upload测试区域变更都是hard stop。"
- Amendment §6 风险表新增 upload 波及行。
- Plan §14.7 新增 "Upload test freeze — 接受并设hard stop" disposition。
- `rg "def test_" tests/fins/test_cn_pipeline.py` 确认 6 个 upload 测试函数（lines 484/543/593/646/702）可被 diff hunk 精确识别。

**裁决**: 已收敛。Upload 测试区域 zero-diff 约束已升级为 hard stop，code rereview 逐 hunk 强制执行。

### OQ-1 → RESOLVED

**原 open question**: 非法组合（`start_is_explicit=True` 且 start bound 为空）的 production 侧 invariant 是否已在 builder 端防护。

**收敛证据**:

- Amendment §4（新增）完整定义 `FinsDownloadDateRange.__post_init__` 不变量：
  - `start_is_explicit=True` → `start_bound` 必须非空
  - `end_is_explicit=True` → `end_bound` 必须非空
  - 双 bound 存在 → `start_bound <= end_bound`
  - 允许 bound 非空 + explicit=False（默认 bound / pipeline direct test）
  - 非法组合统一抛 `FinsDownloadUsageError`
- Amendment §4 明确 builder 委托 owner："`build_fins_download_request`必须删除自身重复的`start > end`判断，改为构造`FinsDownloadDateRange`并由typed owner执行range校验；owner继续抛同一`FinsDownloadUsageError`中文消息，保持当前builder的可观察错误类型与文本行为。"
- Plan §5.1 `FinsDownloadDateRange` 描述更新为含 `__post_init__` owner 校验。
- Plan §5.1 `build_fins_download_request` 描述更新为："builder不保留重复判断。错误抛 `FinsDownloadUsageError`，携带中文 actionable message，并保持当前builder可观察文本。"
- Plan §14.7 新增 "Typed contract invariant — 接受OQ-1并归还owner" disposition。

**裁决**: 已收敛。Typed contract owner 正式拥有 explicit/bound 与 range 不变量，builder 删除重复判断并委托 owner，保持可观察错误行为不变。没有把校验下放到 adapter/pipeline/workflow。

### Additional Verification: Implementation Checklist Completeness

Amendment §4 / plan §14.7 的 implementation checklist 共 4 项。逐项核验每项的覆盖范围：

| Checklist Item | 覆盖文件 | 文件 allowlist 状态 | 直接证据 |
|---|---|---|---|
| 1. `test_cn_pipeline.py:337/402/454` 传 `True` + 新增 False 反例 | `tests/fins/test_cn_pipeline.py` | 本 amendment 加入 | `rg` 已确认三个 call sites；反例标注为 pipeline-layer direct |
| 2. `_RecordingPipeline.download` required boolean 签名 | `tests/fins/test_cn_download_runtime.py` | 已在 original allowlist | `test_cn_download_runtime.py:237-289` — `_RecordingPipeline.download` 重写 `CnPipeline.download`，签名须兼容 |
| 3. `_collect_events` 及直接 `pipeline.download/download_stream` 同步 | `tests/fins/test_cn_download_workflow.py` | 已在 original allowlist | `_collect_events_async:718` 调用 `pipeline.download_stream()`（不含 `start_is_explicit`）；`_collect_events:681-682` 传入 `start_date="2024"`（非空）；另有 3 处直接调用（lines 1210/1466/1508） |
| 4. `FinsDownloadDateRange.__post_init__` owner 校验 + builder 委托 | `dayu/fins/download_contract.py` | 已在 original production allowlist | Amendment §4 完整定义不变量；builder 删除重复判断 |

**结论**: Checklist 已穷举当前 `rg` 可识别的全部受影响 call sites。全量 pyright 兜底遗漏项。

### Additional Verification: No Scope Expansion Beyond Amendment Claim

| 检查项 | 结果 |
|---|---|
| Production allowlist 是否被扩大？ | **否** — §6 production files 列表未变 |
| Test allowlist 是否仅增加 `test_cn_pipeline.py`？ | **是** |
| 是否引入 compat shim / default / wrapper？ | **否** — amendment §2/§6/plan §14.7 明确拒绝 |
| 是否新增 `--source` / multi-ticker / prune 等能力？ | **否** — 不在本 amendment 或 Slice 1 scope |
| `FinsDownloadDateRange.__post_init__` 是否向下层泄漏？ | **否** — 校验在 owner boundary；builder 委托而非绕过 |
| Builder 的可观察错误行为是否改变？ | **否** — "owner继续抛同一`FinsDownloadUsageError`中文消息" |

## Frozen Diff Verification

| Check | Result |
|---|---|
| `tests/fins/test_cn_pipeline.py` 是否在 dirty diff？ | **否** — `git diff --name-only HEAD | grep test_cn_pipeline` 无输出 |
| Product/test dirty diff 是否保持原样？ | **是** — 24 files unchanged since original review |
| Plan artifact 修改范围？ | 仅 §5.1（两处行修改）、§6（一行行修改）、§9（两处新增）、§14.7（新增整节）。均为 amendment 自身内容 |
| Amendment artifact 状态？ | Untracked new file，内容已修订（§2→§8 含 plan review adjudication） |
| Code review artifacts 不可修改？ | **是** — 两份 code review 未在 diff |
| Implementation artifact 不可修改？ | **是** — `wu-cli-download-01-slice1-implementation-20260810-003603.md` 未在 diff |

## Findings

无新增 findings。四个原 review items 全部收敛：

- **DL-PA-01** (call-site 映射): RESOLVED — 三处锁死 `start_is_explicit=True`
- **DL-PA-02** (pipeline direct test 语义): RESOLVED — 明确标注为绕过 builder 的内部 contract test
- **DL-PA-03** (upload hard stop): RESOLVED — zero-diff 约束升级为 code rereview 逐 hunk hard stop
- **OQ-1** (production invariant owner): RESOLVED — `FinsDownloadDateRange.__post_init__` 拥有所有不变量，builder 委托 owner

## Open Questions

无。原 OQ-1 已收敛为 typed contract owner 方案。

## Residual Risks

| Risk | Status | Tracking |
|---|---|---|
| Amendment 在 `CnPipeline.download/download_stream` 签名实际变更前授权 test 文件，若实现方案调整为不修改 pipeline 公开签名则 allowlist 扩张变为多余 | **维持** — 概率不变（adapter→pipeline→workflow 链路仍需参数流经公开方法） | DL-R01 fix commit 中验证 `test_cn_pipeline.py` 确实被修改；若未修改则回退 allowlist 条目 |
| `FinsDownloadDateRange.__post_init__` owner tests 未指定具体宿主管文件 | **降低** — amendment 明确"使用既有Slice 1 allowed test files"，至少有 `test_fins_ingestion_runtime.py`（request/adapter schema）可为宿主 | Implementation agent 需选择合适宿主；code review 验证 owner tests 不寄生在无关测试文件中 |
| 全量 pyright 兜底遗漏 call sites 可能导致"先失败再补漏"的迭代 | **维持** — 属合理兜底策略；checklist 已穷举已知站点 | Implementation agent 应优先按 checklist 主动修复，不应依赖 pyright 报错才发现遗漏 |

## Conclusion

**Pass**

原 review 的四个 findings/open questions（DL-PA-01、DL-PA-02、DL-PA-03、OQ-1）均已在 amendment v2 与 plan §14.7 最新 revision 中收敛：

- 三个已有 call sites 的 `start_is_explicit=True` 已锁死（DL-PA-01）
- Pipeline direct contract test 语义已明确标注，不会误导为公开端到端示例（DL-PA-02）
- Upload 测试区域 zero-diff 约束已升级为 code rereview hard stop（DL-PA-03）
- `FinsDownloadDateRange.__post_init__` 正式拥有 explicit/bound/range 不变量，builder 委托 owner 并保持可观察行为不变（OQ-1）
- Implementation checklist 已穷举 `_RecordingPipeline.download`、`_collect_events`/`_collect_events_async` 及所有直接 `pipeline.download/download_stream` 调用点
- 无 production scope expansion，无 compat shim/default/wrapper，无新增 findings

Amendment 可安全加入 accepted plan。

下一合法动作：AgentMiMo rereview 通过后，形成 accepted amendment commit，恢复 Slice 1 code-review fix（DL-R01、DL-R02）。
