# Code Review

## Scope

- Mode: current changes
- Branch: work/wu-obs-00
- Base: 179520e08e8c6b59cdf49aefc59bc4463c9698c2
- Output file: docs/reviews/code-review-20260724-163232.md
- Included scope:
  - Production: `dayu/cli/commands/tool_trace.py`, `dayu/service/tool_trace_analysis.py`, `dayu/cli/arg_parsing.py`, `dayu/cli/main.py`
  - Tests: `tests/cli/test_tool_trace_command.py`, `tests/service/test_tool_trace_analysis.py`, `tests/cli/test_arg_parsing.py`, `tests/cli/test_import_boundary.py`
  - README: `README.md`, `dayu/README.md`, `dayu/host/README.md`, `dayu/service/README.md`, `tests/README.md`
- Excluded scope: `docs/host/issues-implementation-control.md`, `docs/reviews/wu-obs-00-slice-4-implementation-codex.md`, `docs/reviews/wu-obs-00-slice-4-implementation-controller-adjudication.md`
- Parallel review coverage: 无

## Verification

- 93 tests passed (0 failures)
- pyright: 0 errors, 0 warnings on all four production files
- subprocess module entry (`python -m dayu.cli tool_trace analyze`) verified by `test_module_entrypoint_publishes_reports_in_subprocess`

## Findings

未发现实质性问题。

以下为 adversarial pass 中逐项检查的直接证据与结论：

### 1. 四种 input mode 唯一发现

- **文件**: `dayu/service/tool_trace_analysis.py:143-285`
- `discover_tool_trace_analysis_source` 对文件输入检查 `.jsonl` 后缀（L163），对目录输入构造 `workspace_candidate`、`dayu_candidate`、`trace_candidate` 三个候选（L239-267），空候选抛 `ServiceToolTraceAnalysisUsageError`（L269），多候选抛含 mode 列表的 usage error（L274）。
- 直接证据：`test_discovers_all_four_explicit_input_modes` 覆盖四种 mode 的 source/mode/path 断言；`test_directory_discovery_rejects_ambiguous_and_unsupported_paths` 覆盖 ambiguous（同时匹配 workspace+trace）与 unsupported（空目录）fail closed。

### 2. Symlink / TOCTOU fail closed

- **文件**: `dayu/service/tool_trace_analysis.py:158,174,497-505`
- `_path_lexically_exists` 使用 `os.path.lexists`（L505），dangling symlink 返回 `True` 使 discovery 继续；下游 `ToolTraceAnalysisSource` 构造或 `analyze_tool_trace` 读取时会因目标不存在抛出 `OSError`/`ValueError`，被 `_build_source` 捕获转为 `ServiceToolTraceAnalysisUsageError`（L345-348）。
- `requested_path.is_file()` / `requested_path.is_dir()`（L162, L174）follow symlink；symlink-to-directory 会被目录发现逻辑处理。
- `_absolute_normalized_path` 使用 `os.path.abspath(os.path.normpath(...))`（L494），词法归一化不解析 symlink，保持 operator 显式输入语义。
- 直接证据：`test_existing_non_directory_output_fails_without_changing_input` 覆盖非目录 output 路径；目录发现的 hot-only/cold-only 测试覆盖部分布局。无显式 symlink 测试，但 fail-closed 路径由下游读取错误保证。

### 3. CLI→Service→Host public API 分层与 import boundary

- **文件**: `dayu/cli/commands/tool_trace.py:14-27`, `tests/cli/test_import_boundary.py:42-69`
- `tool_trace.py` 只 import `dayu.cli.arg_parsing`、`dayu.cli.exit_codes`、`dayu.service.tool_trace_analysis`，不 import `dayu.host`、`dayu.engine`、`dayu.fins`、`dayu.runtime`。
- `test_tool_trace_command_only_depends_on_service_public_boundary` 通过 AST 扫描确认无 forbidden import。
- `tool_trace_analysis.py` import `dayu.host` 的 public symbols（`ToolTraceAnalysisSource`、`analyze_tool_trace` 等），符合 Service→Host 依赖方向。
- 直接证据：import boundary 测试通过，AST 扫描确认。

### 4. JSON/Markdown 同一 report 真源

- **文件**: `dayu/service/tool_trace_analysis.py:202-204`
- `analyze_and_publish_tool_trace` 调用 `analyze_tool_trace(source, ToolTraceAnalysisPolicy())` 获取单一 `report`，再分别调用 `tool_trace_analysis_report_to_json(report)` 和 `render_tool_trace_analysis_markdown(report)`。
- 直接证据：`test_analysis_calls_host_public_api_and_renders_same_report` 用 `id(report)` 追踪，断言 `rendered_report_ids == [id(result.report), id(result.report)]`。

### 5. Same-directory temp write / UTF-8 / 固定 replace 顺序

- **文件**: `dayu/service/tool_trace_analysis.py:351-407,409-429`
- `_write_temporary_text` 在 `output_dir`（最终目录）内创建 `NamedTemporaryFile`，`mode="w", encoding="utf-8", errors="strict"`（L418-425）。
- `_publish_report_pair` 先写 JSON 临时文件，再写 Markdown 临时文件（L371-377），然后按 `(json_temporary, json_path)` → `(markdown_temporary, markdown_path)` 固定顺序 replace（L384-387）。
- 直接证据：`test_first_replace_failure_keeps_old_reports_and_publishes_nothing` 覆盖第一次 replace 失败；`test_second_replace_failure_keeps_new_json_and_existing_markdown_state` 覆盖第二次 replace 失败（参数化 old_markdown_exists）。

### 6. 第一次 / 第二次 replace 失败、cleanup secondary failure 时 published_paths/failed_path/temporary_paths_cleaned 稳定性

- **文件**: `dayu/service/tool_trace_analysis.py:382-407`
- 第一次 replace 失败：`published_paths=()`，`failed_path=json_path`，cleanup 清理两个临时文件（L391-404）。
- 第二次 replace 失败：`published_paths=(json_path,)`，`failed_path=markdown_path`，cleanup 清理剩余临时文件。
- `temporary_paths_cleaned` 在 `cleanup_error is None` 时为 `True`（L403）。
- cleanup 失败时 `ServiceToolTraceAnalysisPublishError` 的 `published_paths`、`failed_path`、`primary_publish_error` 不因 `cleanup_error` 改变（L398-404）。
- 直接证据：`test_cleanup_secondary_failure_does_not_change_primary_publication_truth` 参数化覆盖两个 replace 位置 × cleanup 失败，断言 primary paths 不漂移。

### 7. write/mkdir/analysis failure 保留既有报告且退出码正确

- **文件**: `dayu/service/tool_trace_analysis.py:206`, `dayu/cli/commands/tool_trace.py:44-57`
- `mkdir(parents=True, exist_ok=True)` 后检查 `is_dir()`（L206-210），非目录抛 `NotADirectoryError`。
- `analyze_and_publish_tool_trace` 的 `OSError`（mkdir/write 失败）透传到 CLI，被 `except Exception` 捕获，输出错误消息并返回 `EXIT_FAILURE`（L55-57）。
- 分析失败（`ServiceToolTraceAnalysisUsageError`）返回 `EXIT_USAGE_ERROR`（L49-51），发布失败（`ServiceToolTraceAnalysisPublishError`）返回 `EXIT_FAILURE`（L52-54）。
- 直接证据：`test_existing_non_directory_output_fails_without_changing_input` 断言输入和 output 文件不变；`test_cli_usage_error_returns_two` / `test_cli_analysis_failure_returns_one` / `test_cli_publication_failure_prints_published_and_failed_paths` 覆盖三种退出码。

### 8. broad Exception 是否掩盖 KeyboardInterrupt / SystemExit

- **文件**: `dayu/cli/commands/tool_trace.py:55`, `dayu/cli/main.py:140-143`
- `run_tool_trace_command` 的 `except Exception`（L55）位于 `ServiceToolTraceAnalysisUsageError` 和 `ServiceToolTraceAnalysisPublishError` 之后，作为 safety net。
- `KeyboardInterrupt` 和 `SystemExit` 继承 `BaseException`，不被 `except Exception` 捕获，会传播到 `main.py` 的 `except KeyboardInterrupt`（L140）和 `except SystemExit`（L142）。
- 直接证据：`test_main_maps_keyboard_interrupt` 覆盖 KeyboardInterrupt→130 映射；`test_main_closes_log_file_when_restoring_stderr_fails` 覆盖 ValueError 传播。

### 9. parser / help / unknown action / subprocess / module entry

- **文件**: `dayu/cli/arg_parsing.py:507-553`, `tests/cli/test_arg_parsing.py:279-311`
- `_register_tool_trace_command` 注册 `tool_trace` 子命令和 `analyze` 二级 action，`required=True`（L531）。
- `TOOL_TRACE_INPUT` 为 positional required，`--output-dir` 为 required（L543-552）。
- 直接证据：`test_tool_trace_analyze_help_and_required_arguments` 覆盖 help 输出和 missing input/output 用法错误；`test_tool_trace_parser_rejects_unknown_action` 覆盖 unknown action；`test_module_entrypoint_publishes_reports_in_subprocess` 覆盖 subprocess 入口。

### 10. README 与代码一致

- **文件**: `README.md:355-389`, `tests/cli/test_arg_parsing.py:419-473`
- README 第 7 节描述了 `tool_trace analyze` 的四种输入、输出文件、exit code 语义、ambiguous 拒绝和 `--base`/`--config` 不参与 Analyzer 输入发现。
- `test_root_readme_matches_current_cli_public_contract` 通过字符串匹配验证 README 与 CLI 公共契约一致。
- 直接证据：测试通过，README 描述与实现代码一致。

### 11. 过度耦合 / semantic ownership drift

- `tool_trace_analysis.py` import `dayu.host` 的 6 个 public symbols，符合 Service→Host 依赖方向。
- `ToolTraceInputMode` enum 定义在 `dayu.host`，Service 使用同一 enum 不产生 ownership drift。
- `_directory_candidate` 返回裸 tuple 而非 named type，但作为模块私有 helper 不扩散到公共契约。
- 无 evidence-based ownership drift 发现。

### 12. Owner-level test gap

- 无显式 symlink-to-file / symlink-to-directory / dangling-symlink 输入测试。当前 fail-closed 由下游读取错误保证，但显式测试可提高回归安全。
- 无 output directory 是 symlink 的测试。`mkdir(parents=True, exist_ok=True)` 在 symlink 目标是目录时会成功，但 `os.replace` 的跨文件系统行为可能不同。
- `_publish_report_pair` 的 `OSError` during temp write 路径由 `test_first_replace_failure_keeps_old_reports_and_publishes_nothing` 间接覆盖（monkeypatch `_replace_temporary_file`），但真实 temp write 失败（如磁盘满）未被直接测试。

## Open Questions

- 无。

## Residual Risk

- symlink 输入（file / directory / dangling）未被显式测试覆盖。当前 fail-closed 路径由下游读取错误保证，风险低。
- output directory 是 symlink 时的 `os.replace` 行为未被测试。同一文件系统内通常安全，但跨文件系统 `os.replace` 可能抛 `OSError`（Linux）或成功（macOS APFS）。
- `_publish_report_pair` 的真实 temp write 失败（磁盘满、权限）未被直接测试，由 monkeypatch 间接覆盖。

## VERDICT

**PASS** — 未发现实质性问题。代码实现与架构约束一致，异常处理层次正确，publication 失败路径的 primary/secondary 分离稳定，README 与代码契约对齐。
