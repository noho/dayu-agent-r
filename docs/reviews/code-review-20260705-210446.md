# Code Review

## Scope

- Mode: current changes
- Branch: `phaseflow/host-issues-control`
- Base: `main`
- Output file: `docs/reviews/code-review-20260705-210446.md`
- Included scope: WU-WAIT-04 S2 code review — `tests/service/test_entrypoint_runtime_awaiting_smoke.py`（新增）、`tests/README.md`（状态更新）、`docs/reviews/wu-wait-04-s2-implementation-codex.md`（新增）、`docs/reviews/wu-wait-04-s2-controller-validation.md`（新增）、`docs/host/issues-implementation-control.md`（状态更新）。S1 committed changes in `dayu/service/host_assembly.py`、`dayu/fins/ingestion/wait_adapter.py`、`tests/service/test_host_assembly.py` are reviewed as S2 dependency context.
- Excluded scope: production code changes (S1 already accepted), Engine internals, Host durable internals
- Parallel review coverage: 无

## Findings

未发现实质性问题。

以下为低严重度 observations，不构成 blocking findings：

### 编号-1-未修复-低-GatedReadyPollAdapter 入参类型使用内部 Protocol 而非 public WaitRecordRow

- **入口/函数**: `_GatedReadyPollAdapter.poll_wait`
- **文件(行号)**: `tests/service/test_entrypoint_runtime_awaiting_smoke.py:267`
- **输入场景**: Host poller 调用 `poll_wait(wait_record=WaitRecordRow(...))`
- **实际分支**: 测试定义 `_OpaqueWaitInput(Protocol)` 作为参数类型，接收任意对象
- **预期行为**: 作为 `WaitPollAdapter` protocol 的 structural 实现，参数类型应与 protocol 声明一致或更宽
- **实际行为**: `_OpaqueWaitInput` 是无字段的空 Protocol，比 `WaitRecordRow` 更宽；pyright 接受此方向的 contravariant 参数类型，测试运行通过
- **直接证据**: `dayu/host/wait_adapter.py:189` 定义 `poll_wait(self, wait_record: WaitRecordRow) -> WaitPollResult`；测试 `:267` 定义 `poll_wait(self, wait_record: "_OpaqueWaitInput") -> WaitPollResult`；adapter 体内 `del wait_record` 不读取任何字段
- **影响**: 无运行时影响。若 `WaitRecordRow` 未来新增 protocol-level required field（非 dataclass field），测试 adapter 不会自动捕获不兼容变更。当前为 maintainability concern
- **建议改法和验证点**: 可将 `_OpaqueWaitInput` 替换为直接使用 `WaitRecordRow` 类型标注，或保留 `_OpaqueWaitInput` 但增加注释说明其为有意的宽类型以避免测试对 durable row 产生隐式依赖。验证：pyright 通过、测试通过
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 编号-2-未修复-低-_AwaitingTool 类级 docstring 不完整

- **入口/函数**: `_AwaitingTool`
- **文件(行号)**: `tests/service/test_entrypoint_runtime_awaiting_smoke.py:547-548`
- **输入场景**: 代码审查
- **实际分支**: 类有单行概述 docstring `"""返回等待 outcome 的业务工具。"""`
- **预期行为**: AGENTS.md 要求"类与模块应提供中文概述 docstring"；`__call__` 方法已有完整 docstring 含参数、返回值、异常
- **实际行为**: 类级 docstring 只有一行概述，无 `:param` / `:returns` / `:raises`；但 `__call__` 方法 docstring 完整覆盖了调用签名
- **直接证据**: `:547-548` 类 docstring；`:550-561` `__call__` docstring 含 `:param call`、`:param context`、`:returns`、`:raises`
- **影响**: 无功能影响；类本身无独立构造参数，`__call__` docstring 已覆盖完整调用契约
- **建议改法和验证点**: 类级 docstring 可补充 `:param` / `:returns` / `:raises` 以严格匹配 AGENTS.md 约束，或确认当前单行概述对于无构造参数的 callable 类已足够
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/严重）**: 低

## Open Questions

无。

## Residual Risk

- S2 使用 deterministic business tool 与 deterministic poll adapter 验证 UI / Service public workflow 与 production poller runtime，不覆盖真实 Fins ingestion provider 的外部系统状态转换；真实 Fins poll adapter assembly 已由 S1 / existing service assembly tests 覆盖。
- 当前 smoke 不覆盖 callback endpoint path；accepted plan 明确选择 poller path，因为 ordinary UI / Service path 没有 public wait id discovery contract。
- 测试依赖短 poll interval 和 bounded timeout；gate 在 public WAITING 观察后才打开，降低提前恢复造成的 flake 风险。

## Verification Summary

| 检查项 | 结果 |
|---|---|
| `pytest tests/service/test_entrypoint_runtime_awaiting_smoke.py -q` | 1 passed, 3 warnings |
| `pytest tests/service/test_host_assembly.py tests/service/test_entrypoint_runtime_awaiting_smoke.py -q` | 55 passed, 3 warnings |
| `pyright tests/service/test_entrypoint_runtime_awaiting_smoke.py` | 0 errors, 0 warnings, 0 informations |
| Forbidden-path grep | no matches |
| Weak typing guard | only JSON schema `"type": "object"` (allowed exception) |
| Public contract boundary check | all 50+ imported symbols verified in package `__all__` |
| `EngineEvent` usage scope | only in `LocalWorkerHandle.events()` return type and construction, no behavior assertions |
| `git diff --check` | passed |

## Public Contract Enforcement Summary

- 测试主体只使用 public `open_host(options)`、`ensure_session`、Service `submit_entrypoint_turn_and_wait`、`on_run_accepted`、`on_activity`、`host.get_run(...)` 与 `host.read_outbox_terminal_items(...)`。
- 等待恢复只通过 production wait poller policy 与 public wait poll adapter registry 触发。
- 未导入 `dayu.engine.agent`，未使用 `_AsyncAgent` 或其它 private Engine agent path。
- `EngineEvent` 只用于 public `LocalWorkerHandle.events` 协议的返回类型和载荷，以便 Host public opener 消费 worker output；测试不对 Engine events 做行为断言。
- 未读取 durable wait rows、dispatch rows、scheduler internals、ToolRuntime internals。
- 未使用测试私有 wait id bridge。
- 未调用 manual resolve。
- 新测试文件通过 forbidden-path grep，无命中。
