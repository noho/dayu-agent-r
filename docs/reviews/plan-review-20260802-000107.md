# Plan Review — wu-cli-interactive-02 S5/F13 第三次 utils required-identity closure plan amendment

## 0. Review metadata

- Review type: adversarial plan review
- Reviewed target: `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md` §9.1, §10.5, §13, §15（第三次 plan amendment）
- Amendment proposal: `docs/reviews/wu-cli-interactive-02-s5-f13-utils-closure-plan-amendment-proposal-codex.md`
- Base HEAD: `e7f578dc7bdfafb51a859be2db584300e08f81fb`
- Branch: `codex/interactive-oracle`
- Provider: DeepSeek
- Reviewer: AgentDS
- Timestamp: 2026-08-02T00:01:07+08:00
- Artifact path: `docs/reviews/plan-review-20260802-000107.md`
- Review scope: adversarial independent verification of the third plan amendment's claims, with focus on exact delta, identity sourcing, cardinality, and validation closure
- Working tree: 20-file S5 implementation dirty set（只读，未修改）

## 1. Review method summary

1. 从 accepted HEAD 独立复核全部 inventory 计数（FA/OA/CR/CB/RB），不依赖 proposal 自述。
2. 从 accepted HEAD 独立复核 4 个 utils call site 的代码上下文与可用的 `AgentRunRequest` 字段。
3. 从 accepted HEAD 独立复核 `RunnerSpec`、`AgentRunRequest`、`FinalAnswerData`、`EngineRunOutcomeFinalAnswer` 的实际结构。
4. 应用全部 5 个 review lens：goal-bound minimal design、architecture boundary、best-practice、optimal-solution、overengineering、overcoupling。
5. 按用户指定的 10 个重点维度逐项 adversarial 检查。

## 2. Assumptions tested

| # | Assumption | Verdict | Direct evidence |
|---|-----------|---------|-----------------|
| A1 | 两个 utils 共有 4 个 required-contract direct constructors | ✅ 成立 | `rg -c`：awaiting FA×1，conversation_memory FA×1 + OA×2 |
| A2 | utils 无 CR hit | ✅ 成立 | `rg -l` 在 `utils/` 搜索 ContextCompactor/FakeContextCompactor/… 返回空（exit 1） |
| A3 | 完整 closure FA 37/21、OA 6/4、CR 7、union 27 | ✅ 成立 | 独立 `rg` 计数完全匹配 |
| A4 | Builder inventory 8 CB/6、7 RB/4、union 8、5-file delta | ✅ 成立 | 独立 `rg -c` 与去重计算完全匹配 |
| A5 | Total mechanical union = 32 (27 identity + 5 builder-only) | ✅ 成立 | 独立去重：27 + 5 = 32，无遗漏/重复 |
| A6 | 每个 utils call site 已持有同一次 `AgentRunRequest` | ✅ 成立 | 逐行读取：`_AnswerHandle._request`、`_AcceptingSmokeCompactorRunner.__call__(request)`、`_RejectingSmokeCompactorRunner.__call__(request)`、`_DeterministicCompactWorker.accept(snapshot, request)` |
| A7 | `AgentRunRequest.runner_spec` 在 accepted HEAD 有 `provider`/`model` | ✅ 成立 | `git show e7f578dc:runner_spec.py` L283-284：`provider: str`、`model: str` |
| A8 | accepted HEAD `FinalAnswerData` 无 `response_identity` | ✅ 成立 | `git show e7f578dc:engine_events.py` L432-442：仅 `content/filtered/degraded/finish_reason` |
| A9 | accepted HEAD `EngineRunOutcomeFinalAnswer` 无 `response_identity` | ✅ 成立 | `git show e7f578dc:agent_run.py` L138-154：仅 `session_id/run_id/content/filtered/degraded/finish_reason` |
| A10 | 两个 utils 与既有 25-file tests closure 无重叠 | ✅ 成立 | 文件列表逐项比对，无交集 |

**所有 10 项关键 assumption 均通过独立复核。**

## 3. Findings

### 3.1 重点维度逐项 adversarial 检查

以下按用户指定的 10 个维度逐一报告检查结果。

#### 维度 1：exact 2-file/4-call allowed delta

- **检查方法**：`rg -c` 精确计数 + 逐行阅读 4 个 call site
- **结果**：✅ **精确匹配**
  - `utils/smoke_host_public_awaiting_entrypoint.py` L2010：`FinalAnswerData(...)` — 1 call
  - `utils/smoke_host_public_conversation_memory_scenarios.py` L1748：`EngineRunOutcomeFinalAnswer(...)` — accepting compactor
  - 同上 L1794：`EngineRunOutcomeFinalAnswer(...)` — rejecting compactor
  - 同上 L1843：`FinalAnswerData(...)` — `_final_answer_event()` 内
- **总数**：2 files / 4 calls，与 proposal §3.1 完全一致
- **边界检查**：proposal §4.2-§4.3 对每个 call site 的 exact delta 描述（从哪个字段取 identity、如何构造、禁止项）与代码实际上下文一致

#### 维度 2：synthetic identity 是否从同一 AgentRunRequest 产生

- **检查方法**：逐行阅读 4 个 call site 的调用链
- **结果**：✅ **全部同源**
  - `_AnswerHandle.events()`：`self._request`（L1976-1978 构造期注入）持有 `AgentRunRequest`
  - `_AcceptingSmokeCompactorRunner.__call__(request, ...)`：参数 `request: AgentRunRequest`（L1733）
  - `_RejectingSmokeCompactorRunner.__call__(request, ...)`：参数 `request: AgentRunRequest`（L1779）
  - `_DeterministicCompactWorker.accept(snapshot, request)`：参数 `request: AgentRunRequest`（L1644），调用 `_final_answer_event()` 时透传
- **字段映射验证**：
  - `run_id` → `request.run_id` ✅
  - `attempt_id/execution_id` → `request.attempt_id/execution_id`（成对语义，compactor 为 None/None）✅
  - `effective_provider/effective_model` → `request.runner_spec.provider/model` ✅
  - proposal §4.1 表与代码实际字段完全一致，无遗漏/多余字段
- **反例验证**：`_final_answer_event()` 当前签名 `(snapshot, content)` 不含 request，proposal 要求的新 required typed 参数必须由 `accept()` 从 request 构造后显式传入 — 数据流方向正确，不从 snapshot 反推

#### 维度 3：iteration/call cardinality

- **检查方法**：分析 smoke 的 synthetic 调用语义
- **结果**：✅ **cardinality 正确**
  - `iteration_index=0`：每个 synthetic request 模拟单 iteration（Engine iteration 从 0 起），正确
  - `runner_call_index=1`：每个 synthetic request 模拟单次成功 Runner 调用（Runner call 从 1 起），正确
  - 这些值在 proposal §4.1 表与 §4.2-§4.3 常量定义中冻结，不随 smoke invocation 变化
- **非平凡验证**：`runner_call_index=1` 而非 `0` — `build_runner_request_identity()` 的校验（`runner_identity.py` L231-232）要求 `runner_call_index >= 1`，proposal 的值符合 contract invariant

#### 维度 4：UNAVAILABLE + None

- **检查方法**：验证所有 4 个 call site 的身份构造策略
- **结果**：✅ **全部 UNAVAILABLE + None**
  - proposal §4.1 表明确写死：`ProviderRequestIdAvailability.UNAVAILABLE` + `provider_request_id=None`
  - 理由成立：这些 smoke stub 没有真实 provider 调用，没有 provider request id 可记录
  - `SuccessfulRunnerResponseIdentity.__post_init__`（dirty 工作树 L135-152）强制 `unavailable` 与 `None` 成对、`present` 与非空成对 — proposal 的值符合 contract invariant
  - 四个 call site 无一会伪装 `present`

#### 维度 5：无 config/manifest/相邻 request inference

- **检查方法**：proposal §4.4 禁止项逐条对照代码
- **结果**：✅ **prohibition 完整且可执行**
  - §4.4 明确禁止 6 类反推来源：runtime/workspace config、model id CLI 参数、manifest、provider family、相邻 event、snapshot string/日志/输出文本/偶然调用顺序
  - §4.1 表冻结每个 identity 字段的唯一来源（run_id、attempt_id/execution_id、effective_provider/model、iteration_id 显式常量、UNAVAILABLE）
  - 禁止项与允许项完备互斥，无歧义空间

#### 维度 6：smoke zero behavior/output/provider/oracle delta

- **检查方法**：proposal §4.2-§4.4 的 zero-delta 约束逐条对照
- **结果**：✅ **zero-delta 约束完备**
  - §4.2 末尾：不修改 `_AwaitingHandle`、tool-awaiting handshake、11-phase state flow、worker id、event 顺序、timeout/backoff、stdout marker、public Host/Service oracle
  - §4.3 末尾：不修改 proposal JSON、semantic rejection、pressure policy、reactive/fallback state path、tool payload、suite dispatch、provider assembly、artifact/EventLog audit、stdout marker、CLI oracle；不得新增 identity 输出
  - §4.4：禁止跨文件共享 helper、共享 singleton identity、改变 smoke invocation 数量/场景/输出/CLI oracle/provider 配置/失败语义
- **风险**：zero-delta 依赖 code review 逐一核对，plan 未提供自动化 regression 机制。但三个既有 smoke 命令（§6.2/§10.5）作为 behavior golden test 可提供基本保护

#### 维度 7：pyright/inventory/smoke validation

- **检查方法**：plan §10.5 验证命令对照 proposal §6.2
- **结果**：✅ **validation closure 完整**
  - Pyright：`python -m pyright dayu/ tests/ utils/` — 包含 utils（第一次 amendment 遗漏）
  - Pre/post inventory：5 条 `rg` 命令覆盖 FA/OA/CR/CB/RB — 完整
  - Smoke：3 条命令覆盖 public awaiting、memory-reactive-compact、memory-compact-fallback — 覆盖 4 个 call site 的运行时路径
  - 2-file utils delta + 5-file builder delta 都参与对应 allowed-file scope 检查

#### 维度 8：utils 测试/coverage 豁免

- **检查方法**：AGENTS.md 规则对照 proposal §6.2
- **结果**：✅ **豁免正确适用**
  - AGENTS.md L102：`dayu/render/` 和 `utils/` 下的脚本默认无需测试、无覆盖率要求
  - proposal §6.2：正确引用该规则，并明确"该规则不免除 full pyright、post-inventory、上述既有 smoke 或后续 code review"
  - 豁免范围窄：仅 utils 文件不新增测试/不计覆盖率；production modified files 的 ≥80% 覆盖率要求不变
  - 两条规则之间无冲突

#### 维度 9：scope/owner/sequencing

- **检查方法**：对照 plan §3.1 semantic owner 表与 proposal §2
- **结果**：✅ **scope 正确、owner 不变**
  - Engine `SuccessfulRunnerResponseIdentity` contract 仍是唯一业务 owner
  - 两个 utils 只是 synthetic smoke fixture consumer，不成为第二套 identity owner
  - proposal 不扩大 S5 scope、不新增 F 项、不改变 S4→S5 依赖顺序
  - proposal §7 non-goals 边界完整：不修改 production/test/utils 代码、不改变 schema、不新增共享 helper
- **Sequencing**：第三次 amendment 在 S5 implementation 之前、plan gate 之内，位置正确

#### 维度 10：overengineering/residual risk

- **检查方法**：对照 plan §3.2 与 proposal §4、§8
- **结果**：✅ **无 overengineering，residual risk 已分类**
  - 新增内容仅限于 2 个 file-local private helper + 常量 + 4 个 constructor 的 required identity 参数 — 最小化满足需求
  - 不新增 abstraction、framework、shared module、通用 builder、migration 或 compatibility layer
  - proposal §8 分类 5 项 residual risk，每项有处理策略（`fixed in current amendment` 或 `covered by later approved slice`）
  - 无 unclassified residual risk

### 3.2 Material findings

**无 material findings。**

经独立复核，第三次 plan amendment 的以下 claims 全部成立：

1. Premise invalidation 动机成立：第一 amendment 的 pyright scope 只覆盖 `tests/`，遗漏 `utils/`，与项目全量 type-check 命令 `python -m pyright dayu/ tests/ utils/` 不同源。
2. Root cause 定位正确：不是 utils smoke 的业务缺陷，而是 plan consumer inventory 与全量 type-check scope 不同源。
3. Semantic owner 判定正确：Engine identity contract 仍是 owner，utils 是 consumer。
4. Exact delta 精确：2 files / 4 calls，全部可追溯到 accepted HEAD 的行证据。
5. Identity sourcing 正确：每个 call site 已持有同一次 `AgentRunRequest`，可从其 `run_id/attempt_id/execution_id` 与 `runner_spec.provider/model` 构造 required identity。
6. Validation closure 完整：pyright + pre/post inventory + 3 smoke commands 覆盖全部变更路径。

## 4. Open questions

无阻塞 open question。以下项目已在 plan/proposal 中固定，implementation 不得自行改向：

- 两个 utils file-local helper 的函数签名、参数 required 且无 default（proposal §4.1 冻结）
- 4 个 call site 的 identity 字段映射（proposal §4.1 表冻结）
- `UNAVAILABLE + None` 策略（proposal §4.1 表冻结）
- Zero behavior/output/provider/oracle delta（proposal §4.2-§4.4 冻结）

## 5. Residual risks

| 风险 | 分类 | 处理 |
|---|---|---|
| `RunnerSpec` 包含 `endpoint`/`api_key_ref`/`headers` 敏感字段，与 `provider`/`model` 同对象 | 已由 plan §9.2/§10.5 覆盖 | identity type 只取 `provider`/`model`；canary 扫描验证零泄漏；code review 复核 |
| accepted HEAD 后出现新的 tests/utils constructor | 已由 plan §9.1 覆盖 | implementation pre-inventory fail closed，先退回 controller 修订 |
| 3 条 smoke 命令因环境/API key 问题无法运行 | 已知 gap | plan §10.5 明确"真实环境未覆盖项被明确分类而未伪造" |
| synthetic smoke identity 被误当真实 provider continuity evidence | 已由 plan §9.7/§12.1 覆盖 | 行为项 29/G06 外部 evidence owner 不变；smoke 不关闭该 gap |
| 两个 utils 未新增测试/coverage | 已由 AGENTS.md 规则覆盖 | 项目规则明确豁免；pyright + inventory + smoke + code review 是 required closure |

## 6. Final plan review conclusion

**结论：PASS**

第三次 plan amendment 的动机成立、root cause 定位正确、semantic owner 判定准确、exact delta 精确、identity sourcing 正确、validation closure 完整。所有 10 项关键 assumption 均通过独立复核，无 material findings。

amendment 将 S5 mechanical total union 从 30 修正为 32 的算术校正正确。proposal 的 §4 exact delta 冻结、§6.2 validation closure、§7 non-goals 与 §8 residual risks 构成 code-generation-ready 的 implementation spec。

implementation 前需确认：
- [ ] 全量 pre-inventory 重现 FA 37/21、OA 6/4、CR 7、union 27（与本次独立复核结果一致）
- [ ] Builder pre-inventory 重现 8 CB/6、7 RB/4、union 8
- [ ] Total mechanical union 确认为 32 files（27 identity + 5 builder-only delta）
- [ ] 无新增 constructor 或 typed-return consumer 超出 allowed boundary
