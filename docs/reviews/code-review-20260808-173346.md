# F18 final tracked closeout 独立 code/evidence review

- 时间：2026-08-08 17:33:46 CST
- Reviewer：AgentMiMo
- Review target：`codex/interactive-oracle` 当前 F18 tracked closeout diff、`docs/reviews/plan-review-20260808-172014.md`、`docs/reviews/plan-review-20260808-172234.md`，以及冻结 public bundle `pr190-f18-b2-fixed-XUmH8YBg`
- Base：`HEAD=1c92a5bebdf6dd428eaf7577c5d97f5105079d6e`
- 约束：provider-free；未修改被审查文件或外部 bundle；未读取、触碰或暂存 ownership 不明的 `docs/reviews/plan-review-20260808-095346.md`

## Binary verdict

**FAIL**

## Findings

### [High] F18 冻结 public bundle 未满足 plan 自己声明的 minimum publication contract

直接证据：

1. `docs/gateflow/pr-190-f18-plan-20260808.md:255-263` 把以下项目定义为“public tree 最少保存”，且范围是“每条 attempted chain”：
   - 每条执行链的 path-redacted public Host resolver/Tool Trace analysis JSON/Markdown；
   - 每个 accepted target 的 accepted-truth/equality projection；
   - `execution-index.json`；
   - report、summary、digest 与 final scan。
2. 同一 plan 的 `:265-269` 进一步要求 publication writer 先完成最终 `execution-index.json`，再生成覆盖此前全部 public files 的 `digest.json`，最后独占写入 `secret-scan.json`。
3. 冻结 public tree 实际恰好只有六个文件：
   - `observed-behavior-report.md`
   - `observation-summary.json`
   - `material-audit-summary.json`
   - `pre-provider-calibration-summary.json`
   - `digest.json`
   - `secret-scan.json`
4. 因而 public tree 不含两条已执行 non-covering chain 的 public Host resolver/Tool Trace analysis，也不含 plan 要求的 `execution-index.json`；`observation-summary.json` 也只有聚合 ledger 和简化的 outcome/reason/ref，没有 plan 要求的 per-chain budgets、actual counts 与 terminal refs。
5. `docs/cli_ci.md:1400-1420` 附近仍要求 final publication 先落盘 execution index，并要求 public Tool Trace 由 production analyzer 产生；当前 closeout 没有给出这些 owner-produced public artifacts 或明确的 not-applicable contract。
6. review 进行期间更新后的 `docs/gateflow/pr-190-f18-b2-material-amendment-20260808.md:76-90` 和 `docs/gateflow/pr-190-f18-final-closeout-20260808.md:8-10,34-37` 已诚实把该 bundle 冻结为 `FAIL/nonconforming`，没有把缺件隐藏成 PASS；这正确记录了 finding，但不能让 immutable evidence retroactively 符合原 publication contract。

影响：

- 两条 canonical-success-but-non-covering 运行与一条 provider-not-started 运行的处置结论在 summary/report 中被诚实保留，但 public reviewer 无法按本 WU 已确认 plan，从 path-redacted owner output 和 execution index 独立核验 attempted-chain identity、terminal/skip/seal reason 与 relative refs。
- 当前六个文件的 byte SHA、final scan 和 `needs-more-evidence` verdict 本身都是真实的；问题不是伪造或重标，而是 frozen publication 未满足 owner contract。material/closeout 已把它投影成 publication FAIL，不能再作为 B2 mandatory evidence/readiness proof。

最小修复边界：

- 当前 `secret-scan.json` 已是 final writer，不能原地补文件或改 summary。现有 bundle 只能保持 immutable `FAIL/nonconforming`；F19 必须在新 bundle 中补齐 per-attempt public analyzer、execution index 与 per-chain summary fields，再重新生成 digest/final scan。不得覆盖旧结果或把 non-covering/provider-not-started 重标 PASS。

### [High] `material_audit_sha256` 没有声明 digest domain，不能与目标 artifact identity 对齐

直接证据：

1. `observation-summary.json.material_audit_sha256` 是 `37eee3c9665c21874b8caf545acd0a49af0e2d5faa69a52a257ff4702acf10e4`。
2. `material-audit-summary.json` 的实际 file-byte SHA-256 是 `6399d1280a7c567ed96e563739f0338b32371f01ea3c578357582ff38c15af8f`，也是 `digest.json`、material amendment 和 final closeout 冻结的 artifact identity。
3. 对该 JSON 执行 sorted compact serialization 并保留 writer newline 时，SHA-256 是 `18284f2ecd59a08ce1ad2887e6d9c6f3cdefc88a3053f082c8408511f058f2c8`；去掉末尾 newline 时可以复现 `37eee3c...`。但 summary schema 没有声明 digest 针对 file bytes、canonical object，还是某个 newline policy，也没有记录 canonicalization algorithm/version。
4. 因此 `37eee3c...` 不是不可复现的随机值，但它不能由字段名或当前 public contract唯一解释；消费者无法确定它与被审计 artifact 的 owner identity 关系。reviewer 不能用猜测的 serializer 规则替代 owner 声明。

影响：

- summary 的 cross-file integrity claim 无法 fail-closed 校验，违反“业务事实/协议字段必须有唯一 owner 与自解释 contract”的项目约束。
- 顶层六文件 byte SHA 与 final scan 仍然正确；finding 只否定该 summary 字段及 publication conformance，不否定 material audit 内容本身。

最小修复边界：

- 现有 bundle final scan 后不可写，保持 `FAIL/nonconforming`。F19 的 summary producer 应从目标 artifact 的 owner bytes 直接计算 file SHA，或通过显式字段名/schema 声明 canonical object digest 的 domain、serialization 与版本，并在 final scan 前做 cross-file exact validation。

## 已通过的独立核验

### 1. Tracked plan/material/handbook/oracle/closeout 同源

review finding 回写完成后，以下 tracked projections 已统一陈述同一事实，而没有改写 immutable public bundle：

- plan `:309-312`：`executed-needs-more/publication-nonconforming`，并把 digest、per-chain summary、public analysis/index 缺口移交 F19；
- material amendment `:76-90`：publication `FAIL/nonconforming`，final scan 后不回写；
- handbook `docs/cli_ci.md:541-547`：real observation `needs-more-evidence`、publication `nonconforming`、B2/registry/readiness 不升级；
- oracle adjudication `:78-84`：B2 仍 `unadjudicated`，private capture/final scan 不能替代 publication 缺口；
- final closeout `:5-15,34-45`：implementation PASS、evidence publication FAIL、real observation needs-more、B2 unadjudicated/not ready，并给出 F19 新 bundle 修复边界。

这些文档现在对 implementation、real observation、publication 与 Oracle 四类 verdict 的语义分工一致。binary review 仍为 FAIL，是因为 evidence publication 本身不可回写且不 conform；不是 tracked closeout 隐藏了该失败。

### 2. 六个 public SHA 与 final scan

六个文件的 byte SHA-256 已逐一重算，均与 material/closeout 完全一致：

| Public artifact | SHA-256 |
| --- | --- |
| observed report | `0969738ba5d75b4749b785fc6d8203cfd53e55a6cf850cbd3ad2408eacc3f8aa` |
| observation summary | `1bc5eeb1f2c5537e0e5ebd60ad7f6e06c244f8d10802bb2cb6a42cd6d65d2457` |
| material audit | `6399d1280a7c567ed96e563739f0338b32371f01ea3c578357582ff38c15af8f` |
| calibration summary | `2b855885e5820bf2e902b64b3352425cef8486498bf17ae8655c14031436baa1` |
| digest | `36fd642ad1700a7386ad7263bd1ef52496ba465ffea5dd367abcc2f175e4a627` |
| secret scan | `40f7885d6d1c517c2db40289d636b02a2cc3837bc5c04081f15432ad294e4f47` |

`secret-scan.json` 的扫描集合是 digest 加此前四个 public files，共 `5` files、`27,450` bytes；secret hits、absolute-private-path violations、validation errors 均为 `0`。五文件的独立 size 求和同样为 `27,450`。

补充：`37eee3...` 可由 sorted compact JSON 且不带末尾 newline 的一种序列化复现，但 bundle 未声明这一 digest domain；因此它属于上述 publication finding，不能视为与 file-byte artifact identity 已建立合同关系。

### 3. 失败与 non-covering 处置未隐藏、未重标

`observation-summary.json.attempts` 精确保留：

- `formal-chain-01/R1`：`canonical_success_non_covering` / `setup_source_contamination`；
- `formal-chain-02/R1`：`canonical_success_non_covering` / `typed_filter_zero_facts`；
- `formal-valid-chain-01/R1`：`provider_not_started` / `empty_prompt_rejected_before_cli_spawn`。

private 直接证据中第三条的 actions 为空、PTY 为空、event sequence 未增加、before/after 均无 durable state，支持“CLI/provider 未启动”，而不是 provider failure。两条实际运行的 ledger 合计 `7` ordinary calls、`42.5896s`、`0` compactor calls；report、summary、material、oracle 与 closeout 均未把它们改标 PASS 或覆盖成 mandatory evidence。

### 4. B2 Oracle/registry/readiness 状态

- `docs/cli_ci_scenarios.json` 和 `docs/cli_ci_oracles.json` 相对 HEAD 无 diff。
- B2 scenario 对象与 HEAD 精确相等：`status=unadjudicated`，adjudication identity 仍 pending，applicable identity 仍在用户裁决之后。
- registry 顶层仍为 `calibration`；public summary、handbook、oracle adjudication 与 closeout 均为 overall `not_ready` / not ready。
- 未替用户接受 B2，也未把 F18 的 `needs-more-evidence` 提升为 readiness proof。

### 5. F19 handoff 与 durable-state isolation

`docs/gateflow/pr-190-f18-final-closeout-20260808.md:39-45`、`docs/reviews/pr-190-oracle-adjudication-20260808.md:78-89` 明确要求 F19 使用新 work unit、新 deadline、新 bundle、fresh workspace，并禁止复制或复用 F18 durable state、禁止覆盖两条 non-covering 结果。handoff 没有把旧 Trial/F18 durable state 当作合法 setup。

### 6. Scope、path 与 secret hygiene

- 当前 tracked diff 仅涉及 F18 handbook/plan/oracle closeout 文本；产品代码、schema、runtime、harness、registry JSON 均未修改，符合 `no product change / setup correction` implementation verdict。
- 新增/修改 tracked 行未发现 raw 绝对私有路径；public final scan 的 secret/path/errors 也是 `0/0/0`。
- 两路 172014/172234 plan review 均对 material correction 给出 PASS，但 reviewer 自报没有替代本次对文件 SHA、typed summary、private precondition evidence、registry diff 和 publication tree 的直接核验。

## Adversarial failure pass

- 检查了把 exit 0 / `RUN_SUCCEEDED` 误当 scenario PASS：未发现。
- 检查了把 compactor `0` calls 隐藏为覆盖：未发现。
- 检查了 provider-not-started 被包装为 provider failure 或正式观察成功：未发现。
- 检查了通过修改 registry JSON 提前接受 B2：未发现。
- 检查了 F19 复用 F18/Trial durable state：未发现。
- 检查了六 SHA、扫描文件数、字节数与零命中的算术/byte-level mismatch：未发现。
- 检查了 publication minimum 与实际 frozen tree：发现缺少 per-attempt public artifacts/index/summary fields。
- 检查了 summary cross-file digest 与 file/canonical identity：发现 digest domain 未声明且不能唯一对齐。

## Open questions / residual risk

1. 当前 closeout 已将 F18 public bundle 冻结为 immutable `FAIL/nonconforming`；如果未来需要允许 needs-more partial publication，必须先在 plan/handbook owner contract 中定义该分支，reviewer 不代替 owner 发明例外。
2. B2 mandatory compactor evidence 仍全部未观察；这已如实移交 F19，不是本次 finding，但在用户裁决前必须继续保持 `unadjudicated` / not ready。
3. cold analyzer `compactor_responses=0` 与 provider-native request id unavailable 继续作为 residual evidence question；本 review 未把它们升级为 B1 mandatory readiness gap。
