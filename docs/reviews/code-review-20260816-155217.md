# Code Review (Re-Review)

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `a29c8eb5`（UF-FIX08 Slice 2 HEAD）
- Output file: `docs/reviews/code-review-20260816-155217.md`
- Included scope: 5 个生产文件、7 个测试文件的全部 workspace changes（相对 `a29c8eb5`）
- Excluded scope: 无
- Parallel review coverage: 无
- 参考输入：前 review `docs/reviews/code-review-20260816-153600.md`（4 findings）；
  fix artifact `docs/gateflow/uf-fix08-existing-source-auto-repair-slice3-code-review-fix-20260816.md`；
  updated implementation artifact `docs/gateflow/uf-fix08-existing-source-auto-repair-slice3-implementation-20260816.md`；
  accepted plan §5.3/§5.4/§5.5、§9 Slice 3、§11 validator tests；`AGENTS.md` 语义 owner 约束

## 前 Review Findings 状态

| Finding | 前 review 状态 | 本 re-review 验证结论 |
| --- | --- | --- |
| 1：selection owner duplicated | 未修复（中） | **已修复** |
| 2：Raises 遗漏 | 未修复（低） | **已修复** |
| 3：workflow identity guard 测试空洞 | 未修复（低） | **已修复** |
| 4：矛盾 derived projection 无测试 | 未修复（低） | **已修复** |

## 8 项 Adversarial Focus 验证

### 1. `_project_fins_upload_filing_selection` 是否成为唯一 owner

**通过。** `_project_fins_upload_filing_selection()`（`ingestion_runtime.py:914-960`）是 duplicate/cardinality/implicit-primary/membership/companions 的唯一纯 projection owner。返回 `_FinsUploadFilingSelectionProjection`（immutable success）或 `_FinsUploadFilingSelectionFailure`（封闭 enum，5 个成员）。旧 `_select_fins_upload_filing_files()` 已删除（diff 中 `-42 行`）。

- **static admission**（`_validate_fins_upload_filing_static`，`ingestion_runtime.py:1289-1294`）调用 `_project_fins_upload_filing_selection`，将 closed failure 通过 `_FILING_SELECTION_FAILURE_USAGE_CODES` 映射为 `FinsUploadUsageCode`。
- **validated constructor**（`_validate_filing_selection_matches_request`，`ingestion_runtime.py:996-1004`）调用同一 `_project_fins_upload_filing_selection`，将 closed failure 投影为 `ValueError`。
- 两处调用共享同一 projection 函数，无第二套 raw 推导。`_FILING_SELECTION_FAILURE_USAGE_CODES`（`ingestion_runtime.py:878-894`）完整覆盖 5 个 failure enum 成员。

### 2. static validation/path/capability precedence 与 exact primary/companions 行为未漂移

**通过。** static admission 顺序不变：ticker/action/fiscal/delete → path normalization → projection → basename/exists/regular/capability → `projection.to_file_selection()`。`_FinsUploadFilingSelectionProjection.to_file_selection()`（`ingestion_runtime.py:858-875`）调用 `FinsUploadFilingFiles.for_upsert(primary, companions)`，与旧 `_select_fins_upload_filing_files` 返回后构造 selection 的行为等价。delete 路径仍走 `FinsUploadFilingFiles.for_delete()`。

### 3. direct constructor 对 duplicate/missing/invalid selection 仍 fail closed

**通过。** `__post_init__` 调用 `_validate_filing_selection_matches_request`，该函数：
- delete 空状态校验不变
- upsert 调用 `_project_fins_upload_filing_selection`，failure 投影为 `ValueError`（封闭 failure enum value 内嵌消息）
- `replace()` 伪造 empty selection 或 incomplete companions 被 `__post_init__` 拦截（测试 `test_validated_filing_repair_contract_rejects_incomplete_selection_and_target` 验证）

异常面为 typed/bounded：`TypeError`（类型违约）、`ValueError`（identity/selection/repair 不变量）、`FinsUploadUsageError`（path normalization failure）。

### 4. Raises 完整

**通过。** 三处 docstring 已补全：
- `read_filing_upload_state`（`_fs_filing_upload_state_core.py:83`）：补 `RuntimeError`
- `ValidatedFinsUploadFilingRequest.__post_init__`（`ingestion_runtime.py:795`）：补 `FinsUploadUsageError`
- `_validate_filing_selection_matches_request`（`ingestion_runtime.py:981`）：补 `FinsUploadUsageError`

### 5. SEC/CN identity mismatch 测试命中 workflow guard

**通过。** SEC/CN 两处 `test_upload_filing_authoritative_identity_mismatch_fails_closed` 的 `mismatched_validator` 从注入 `document_id` 漂移改为注入 `internal_document_id` 漂移（SEC `test_sec_pipeline_upload_filing_stream.py:1513`、CN `test_cn_pipeline.py:1832`）。`__post_init__` 的 `integrity.document_id != self.document_id` 校验不再拦截（`internal_document_id` 不参与该检查），异常真正由 workflow `_assert_authoritative_filing_identity` 产生。测试继续断言 fail-closed 与 `begin_tokens == []`。

### 6. derived=A/basename=B 为 REPAIR_REQUIRED，ambiguous fallback 仍 UNSAFE

**通过。** `test_source_integrity_uses_storage_name_for_same_basename_asset_identity` 现有三个 phase：
1. 同 basename 合法 → `COMPLETE`
2. derived_from 命中 A、Docling original_filename 为 B 的 basename（矛盾派生）→ `REPAIR_REQUIRED` + `DERIVED_PROJECTION_MISMATCH` + revision 保持
3. derived_from 缺失 + 同 basename 命中多个 originals → `UNSAFE` + `FILE_DECLARATION_UNTRUSTED` + revision=None

Phase 2 直接覆盖了前 review Finding 4 识别的行为面变化。

### 7. 原 8 项 Slice 3 adversarial focus 无回归

**通过。** 逐项验证：

1. 同 guard 单一 inspection：未变更（`_fs_filing_upload_state_core.py` diff 仅删旧 seam、补 docstring）
2. repair contract 封闭性：未变更（`upload_repair_contract.py` 无新 diff）
3. validator precedence：未变更（`validate_fins_upload_filing_request` 逻辑不变）
4. 三个 failure factory：未变更（`upload_failure.py` 无新 diff）
5. raw runtime start 传播：未变更（`start_upload`/`prepare_observed_upload` 逻辑不变）
6. same-basename owner：增强（新增 phase 2 测试覆盖）
7. fixture 迁移：未变更（SEC/CN fixture diff 与前 review 一致）
8. scope/typing/README：未变更（pyright 0 errors，README 无 diff）

### 8. scope/typing/coverage/README/oracle/design guards

**通过。**
- `pyright dayu/ tests/ utils/`：0 errors, 0 warnings, 0 informations
- `pytest tests/fins`：1802 passed, 1 skipped, 3 warnings
- `git diff --check`：干净
- README/oracle/scenario/design/registry diff：无输出
- 新增 diff 签名扫描：无 `object`/`Any`/`hasattr`/`getattr`/伪 callable cast

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- 与前 review 相同的 residual items（Slice 4/5 序列、material repair、旧 schema migration、UF-PF08/UF-PF12 evidence）不变。
- `_project_fins_upload_filing_selection` 的 `primary_selectors[0]`（`ingestion_runtime.py:948`）在 `primary_selectors` 非空时取第一个元素；上游 `_normalize_fins_upload_paths` 保证 tuple 类型，`len(primary_selectors) > 1` 已在 944 行拦截，不存在 IndexError 风险。

## 复验证据（本 re-review 独立执行）

```text
pytest -q tests/fins/test_fins_ingestion_runtime.py tests/fins/test_fins_service_runtime.py tests/fins/test_upload_failure.py
  333 passed

pytest -q tests/fins/test_fins_storage_atomicity.py::test_source_integrity_uses_storage_name_for_same_basename_asset_identity \
  tests/fins/test_sec_pipeline_upload_filing_stream.py::test_upload_filing_authoritative_identity_mismatch_fails_closed \
  tests/fins/test_cn_pipeline.py::test_upload_filing_authoritative_identity_mismatch_fails_closed
  3 passed

pytest -q tests/fins
  1802 passed, 1 skipped, 3 warnings

python -m pyright dayu/ tests/ utils/
  0 errors, 0 warnings, 0 informations

git diff --check：干净
README/oracle/scenario/design/registry diff：无输出
新增 diff 签名扫描：无 object/Any/hasattr/getattr/伪 callable cast
```

## Verdict

**PASS**。前 review 4 个 findings 全部修复验证通过；8 项 adversarial focus 无回归；无新 findings。可进入 Slice 3 acceptance。
