# UF-FIX08 existing-source-auto-repair：Aggregate Re-Review（R1 fix）

## Gate 元数据

- work unit：`UF-FIX08 existing-source-auto-repair`
- gate：`aggregate deepreview re-review`
- base：`5859856e46af42c9ae5a2a5c07fab1ba59dc91d3`（UF-FIX07 closeout）
- HEAD：`65d352e6` + workspace aggregate fix + R1 fix（未提交）
- 输入 artifacts：
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-aggregate-rereview-fix-20260816.md`（R1 fix artifact）
- workspace diff：3 files（`_fs_source_integrity.py` +45/-6、`test_fins_storage_atomicity.py` +184、`test_sec_pipeline_download.py` +65）
- 日期：2026-08-16
- reviewer：AgentMiMo（独立复核，未读取其它 reviewer re-review 产出）

## 结论

**PASS**

R1 fix 正确，production condition 最小且不误杀 clean empty workspace，M2 trusted dangling 路径不回退，全部场景 typed fail-closed。

## 验证摘要

| 验证项 | 结果 |
| --- | --- |
| R1 精确节点（filing/material owner + SEC trusted/untrusted） | 6 passed |
| focused matrix（含全部新增） | 1253 passed |
| 全仓 pyright | 0 errors, 0 warnings |

## R1 Fix 独立验证

### Production 逻辑走读

`_inspect_source_kind_unguarded`（`_fs_source_integrity.py:319-330`），在 `_apply_manifest_facts` 之后、`_canonical_manifest_items`/`_select_exact_target`/`_derive_repair_blocked_reason` 之前：

```python
if (
    external_document_id is None           # whole-kind mode
    and not inventory                      # empty inventory
    and SourceIntegrityReason.SOURCE_MANIFEST_UNTRUSTED in shared_manifest_reasons
):
    raise SourceIntegrityPreflightError(SourceIntegrityPreflightReason.UNSAFE_PUBLICATION)
```

条件最小化：
- `external_document_id is None` → 只在 whole-kind mode 触发，exact mode 不受影响
- `not inventory` → 只在空 inventory 触发，有 actual source 时不触发
- `SOURCE_MANIFEST_UNTRUSTED in shared_manifest_reasons` → 只在 manifest 不可信时触发

### 不误杀 clean empty workspace 验证

Clean empty workspace（无 manifest、无 source）：
1. ticker_dir 不存在 → `_empty_source_kind_inspection` → empty inventory, no shared reasons
2. ticker_dir 存在但 source_root 不存在 → 同上
3. source_root 存在但为空 + manifest 不存在 → `_inspect_source_manifest` → `exists=False, trusted=True, items={}`
4. `_apply_manifest_facts` → `not manifest.exists` + `not inventory` → `return inventory, ()`
5. shared_manifest_reasons = () → R1 条件 `SOURCE_MANIFEST_UNTRUSTED in ()` = **False**
6. 返回 empty inventory, no blocked reason ✅

### 场景矩阵

| 场景 | inventory | manifest | R1 条件 | 结果 | 测试 |
| --- | --- | --- | --- | --- | --- |
| clean empty workspace | () | missing | False（no untrusted reason） | empty, clean | existing tests ✅ |
| empty + untrusted manifest | () | untrusted | **True** | `UNSAFE_PUBLICATION` | test_empty_inventory_with_untrusted_manifest_raises_whole_preflight ✅ |
| empty + trusted dangling | () | trusted, dangling IDs | False（nonempty after synthetic） | synthetic UNSAFE items | test_dangling_trusted_manifest_projects_stable_unsafe_inventory ✅ |
| nonempty + untrusted manifest | 有 items | untrusted | False（inventory 非空） | per-item UNSAFE | existing tests ✅ |
| nonempty + trusted dangling | 有 items | trusted, dangling | False（inventory 非空） | per-item UNSAFE + synthetic | existing + M2 tests ✅ |
| nonempty + trusted matched | 有 items | trusted, matched | False | normal | existing tests ✅ |
| exact + untrusted manifest | N/A | untrusted | False（exact mode） | UNSAFE(reason) | test_empty_inventory_with_untrusted_manifest... (exact part) ✅ |
| exact + missing target + trusted | N/A | trusted, not in items | False（exact mode） | MISSING | existing tests ✅ |

### R1 不影响 repair/revision/guard/commit

R1 在 inspector 层面抛出 `SourceIntegrityPreflightError`，在以下所有处理之前中断：
- `_canonical_manifest_items` → 不执行
- `_select_exact_target` → 不执行
- `_derive_repair_blocked_reason` → 不执行
- caller 的 publication guard → finally 块正常释放
- repair reset → 不执行（guard 后 inspector 失败）
- commit validator → 不执行（batch 未开始或已 rollback）
- revision 比较 → 不执行

对 repair/revision/guard/commit 无影响 ✅

### SEC 真实 workflow 验证

`test_sec_empty_inventory_manifest_preflight_has_zero_mutation`（parametrized `trusted_dangling`/`untrusted`）：

1. `_seed_complete_sec_source` → 创建 complete source
2. `source_dir.rename(detached)` → 移除 actual source directory
3. 若 `untrusted`：`manifest_path.write_text("{")` → manifest 不可信
4. `pipeline.download(ticker="AAPL")` → 触发 whole-tree preflight
5. `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)` ✅
6. `downloader.list_filing_files_call_count == 0` → 零 provider 调用 ✅
7. `downloader.download_files_called is False` → 零下载 ✅
8. `manifest_path.read_bytes() == old_manifest` → manifest 未修改 ✅
9. `not _company_meta_path(...)` → 零 company mutation ✅
10. `not _download_rejections_path(...)` → 零 rejection mutation ✅

trusted_dangling 和 untrusted 两种 corruption 均零副作用 ✅

## M2 trusted dangling 路径回退检查

M2 fix 仍在 `_apply_manifest_facts`（`:1132-1149`）中保留：
- `dangling_ids = tuple(sorted(manifest_ids - source_ids))`
- `unsafe_inventory` + `dangling_inventory` → `unsafe_by_id` → sorted merged inventory

R1 gate 在 `_apply_manifest_facts` **之后**执行。当 inventory 非空（M2 合成的 synthetic items）时，R1 条件 `not inventory` = False，不触发。M2 路径完整保留 ✅

## Finding 裁决

| Finding | 裁决 | 状态 | 依据 |
| --- | --- | --- | --- |
| R1 untrusted manifest + empty inventory fail-open | accepted | **已修复** | filing/material owner tests + SEC trusted/untrusted 零副作用 tests |

## 原有 Finding 状态确认

| Finding | 状态 |
| --- | --- |
| M1 foreign descriptor | 证据失效（不变） |
| M2 trusted dangling | 已修复（不变） |
| 3/4 docstring | 已修复（不变） |
| 5-8 | rejected/deferred（不变） |
| L1-L15 | rejected（不变） |

## Scope / Frozen Guards

- **tracked diff**：仅 3 个 production/test 文件 ✅
- **oracle/scenario**：零 diff ✅
- **Host/Engine design**：零 diff ✅
- **Host/Engine/Service/CLI production**：零 diff ✅
- **review artifacts**：全部未跟踪只读 ✅

## Residual Risks

与前次一致，无新增。R1 无 residual。

| 风险 | 分类 | owner |
| --- | --- | --- |
| 同 request 并发 success/skip 收敛 | assigned to later | `UF-FIX10` |
| fresh company meta warning | assigned to later | `UF-FIX11` |
| material existing-source repair | assigned to later | 后续独立 work unit |
| 旧 schema 读取/迁移 | assigned to later | migration work unit |
| UF-PF08/UF-PF12 真实 evidence | assigned to later | evidence work unit |
| registry/oracle 状态 | assigned to later | registry adjudication |
| SEC/CN download terminal projection | assigned to later | failure projection owner |
| raw runtime lock failure | assigned to later | operational failure owner |
| validated request hardening | assigned to later | contract hardening owner |
| manual filesystem writer | operational policy | storage policy |

无 unclassified residual risk，无 blocking open question。

## Final Decision

**PASS**。

R1 fix 条件最小（whole-kind + empty inventory + untrusted manifest），不误杀 clean empty workspace，不回退 M2 trusted dangling 路径，不影响 repair/revision/guard/commit。SEC trusted/untrusted 两种 corruption 零 provider/company/rejection mutation 且无裸 ValueError。1253 tests passed，pyright 0 errors。
