# Plan Re-Review — WU-SEMANTIC-OWNERSHIP-01 Umbrella Plan

## Review Target

- **Plan artifact**: `docs/host/wu-semantic-ownership-01-umbrella-plan.md`
- **Plan fix artifact**: `docs/reviews/wu-semantic-ownership-01-plan-fix-codex.md`
- **Controller adjudication**: `docs/reviews/wu-semantic-ownership-01-plan-review-controller-adjudication.md`
- **Previous reviews**: `docs/reviews/plan-review-20260709-143323.md` (AgentMiMo), `docs/reviews/plan-review-20260709-143500.md` (AgentDS)
- **Review date**: 2026-07-09
- **Gate**: plan re-review
- **Reviewer**: AgentMiMo

## Re-Review Scope

本次 re-review 复核 controller adjudication 中 C01-C11 共 11 项 accepted findings 是否已在 plan fix 中闭合，并检查 plan 是否仍有 blocker。

## C01-C11 Closure Table

| ID | Subject | Controller Decision | Plan Fix Status | Closure |
|---|---|---|---|---|
| C01 | P0-A finish reason authority | 默认 root-cause fix：移除 `RunnerContentCompletedData.finish_reason` 与 `ContentCompleteData.finish_reason`；Agent-side overwrite 被拒绝为主方案 | Plan §6 P0-A "预期 contract" 已写明默认 root-cause fix 为移除 finish_reason，Agent-side overwrite 明确标注"不是主路径"。S0 消费者扫描已加入 implementation slices，stop condition 已覆盖预期外消费者场景。 | **closed** |
| C02 | P0-B `ingest_method` coverage | 必须全量 `rg "ingest_method" dayu/fins/`，allowed files 覆盖所有读写点 | Plan §6 P0-B "Root-cause confirmation 必做扫描" 已要求 implementation 前运行全量扫描并归类。Allowed files 已显式列出 `cn_pipeline.py`、`cn_download_rebuild.py`、`cn_download_source_upsert.py`、`sec_download_source_upsert.py`、`_fs_source_document_core.py`、`_fs_maintenance_core.py`，并保留 glob 兜底扫描新增点。 | **closed** |
| C03 | P0-B preprocess helper scope | root-cause confirmation 必须选择 boolean helper 或 typed status helper，列出 consumers，裁决 `not_supported_count` | Plan §6 P0-B "Root-cause confirmation 必做扫描" 第 2-3 条已明确要求裁决 helper 类型、列出 direct/job/awaiting/direct-stream consumers、说明 JSON summary 是否新增 `not_supported_count`。 | **closed** |
| C04 | P1-A consumer migration pressure | S3 需逐消费者 completeness checklist，覆盖 Tool Trace / Read API / Durable Memory / Conversation Memory / RunInputBuilder / CompactMaterial | Plan §6 P1-A S3 completeness checklist 已逐消费者列出要求，允许拆 S3a/S3b 但不得漏消费者。 | **closed** |
| C05 | P1-B `RUN_LOST` behavior | `RUN_LOST` 是 Host terminal/lifecycle fact，不是 public outbox terminal item；watermark 不应要求 outbox item | Plan §6 P1-B "预期 contract" 已写入 controller design decision：Host terminal event set（含 `RUN_LOST`）与 public outbox terminal item event set（排除 `RUN_LOST`）的区分。S0 design truth confirmation 已要求先检查 `docs/host/design.md`。 | **closed** |
| C06 | P1-C waiting wording boundary | 业务级"等待工具结果返回"允许；治理级"等待状态""后续调度""宿主取消"等禁止 | Plan §6 P1-C "预期 contract" 已明确 LLM-facing waiting wording boundary：业务级允许条件和治理级禁止清单。S0 LLM-facing wording classification 已要求列出分类标准。 | **closed** |
| C07 | P2-A session resume boundary | 默认 Service-owned existing-session execution helper；CLI-public helper 仅在 root-cause confirmation 证明纯 UI/rendering 时允许 | Plan §6 P2-A "预期 contract" 已默认采用 Service-owned helper，CLI 只保留参数解析/终端渲染/exit code 映射。S0 root-cause confirmation 已要求裁决复用逻辑归属。 | **closed** |
| C08 | P2-B obsolete finding handling | 必须产出 finding status table：`active` / `obsolete-with-evidence` / `needs-design-update` / `deferred-with-owner` | Plan §6 P2-B S2 已要求先产出 finding status table，obsolete finding 可用 controller-accepted no-code/no-commit pass 关闭。 | **closed** |
| C09 | P2-C prompt source migration | 必须 `rg "AgentPolicy\\("` 全量扫描按层分类；Engine 不得 import runtime config | Plan §6 P2-C S0 已要求运行全量扫描按 layer 分类构造点。"非目标"明确"不把 Engine 反向依赖 runtime config"。"预期 contract"明确 production/tests 都必须从所属 assembly/fixture 显式传 prompt。 | **closed** |
| C10 | Full-repository deepreview phase | 每轮至少 AgentMiMo + AgentDS 全仓 review；8 个 review dimensions；final closeout 要求至少两轮连续无新增 accepted finding | Plan §8 已重写：每轮派 AgentMiMo + AgentDS 全仓 review，列出 8 个维度，exit 条件为"至少两轮连续无新增 accepted current-umbrella finding"。Umbrella completion report 格式（§9）已增加 deepreview rounds table。 | **closed** |
| C11 | Sub WU contract conflict handling | 后续 sub WU 与已 accepted contract 冲突时：停下 → 更新设计真源 → 修改早前 contract 或增加 typed mapping | Plan §5 "Sub WU contract conflict handling" 已新增完整冲突处理流程：停下裁决、更新设计真源、二选一路径（amend commit 或 typed mapping）、禁止下游 workaround。 | **closed** |

**结论：C01-C11 全部 closed。**

## 用户指定复核项

| 复核项 | 结论 | 证据 |
|---|---|---|
| P0-A 默认 root-cause 是移除 duplicate finish_reason source | **确认** | Plan §6 P0-A "预期 contract" 第 1-3 条：移除 `RunnerContentCompletedData.finish_reason` 和 `ContentCompleteData.finish_reason`，`RunnerDoneData.finish_reason` 是唯一 authority，Agent-side overwrite "不是主路径"。 |
| P0-B 覆盖所有 Fins ingest_method 生产消费者 | **确认** | Plan §6 P0-B allowed files 已显式列出 CN pipeline、CN rebuild/source upsert、SEC source upsert、storage core、maintenance。S0 要求全量 `rg "ingest_method"` 扫描并归类。 |
| P1-B RUN_LOST 不要求 public outbox terminal item | **确认** | Plan §6 P1-B "预期 contract" 已写入 controller design decision：`RUN_LOST` 是 Host terminal/lifecycle fact，不是 public outbox terminal item。 |
| P2-A 默认 Service-owned helper | **确认** | Plan §6 P2-A "预期 contract" 第 1 条："默认采用 Service-owned existing-session execution helper"。CLI-public helper 仅在 root-cause confirmation 证明纯 UI/rendering 时允许。 |
| P2-C 不引入 Engine → runtime config 依赖 | **确认** | Plan §6 P2-C "非目标"："不把 Engine 反向依赖 runtime config"。"预期 contract"："Engine 不得 import runtime config、config loader 或 execution profile 来恢复默认 prompt"。 |
| Umbrella 后续 deepreview 需至少两轮连续无新 accepted finding | **确认** | Plan §8："至少两轮连续无新增 accepted current-umbrella finding，除非用户改变退出条件"。§9 umbrella completion report 已增加 deepreview rounds table。 |

## Blocker 检查

### Scope 漂移

无。8 个 sub WU 均围绕 semantic ownership hardening，各自 owner boundary 独立，不混入非相关功能。

### Owner boundary 不清

无。每个 sub WU 的 owner boundary 已明确定义（产生事实 / 校验事实 / 持久化事实 / 投影事实），与 Host / Engine / Fins / CLI / Config 分层一致。

### 下游止血

无。C11 已引入 sub WU contract conflict handling，禁止下游 workaround。

### Review 后仍缺 design decision

无。C01（finish reason authority）、C05（RUN_LOST public outbox）、C06（waiting wording boundary）、C07（session resume boundary）的 design decision 已写入 plan。

### Sub WU 顺序不可执行

无。依赖链 P0-A → P1/P2 Host work、P0-B → P1-C Fins schema、P1-A → P1-C evidence contract、P1-B → P2-B terminal/cancel truth、P2-C last 减少 rebase churn，均真实存在且符合设计真源。

### Deepreview exit criteria 不可验证

Plan §8 的 exit criteria 为"至少两轮连续无新增 accepted current-umbrella finding"。这是可验证的：controller 只需记录每轮 deepreview 的 accepted finding 数，连续两轮为零即满足退出条件。§9 umbrella completion report 要求记录 deepreview rounds table，提供审计轨迹。

### 违反 AGENTS.md

无。plan 不引入反向依赖、不引入兼容性代码、不修改 schema 时做兼容读取、不把 Engine 内部治理信息暴露给 LLM-facing 文本。

## 风险（不阻塞 accepted plan commit）

### R1: P1-A S3 消费者迁移上下文压力

P1-A S3 需要同时修改 5 个消费者的 back-query / blacklist / heuristic fallback 模式。plan 已通过 completeness checklist 和允许拆 S3a/S3b 缓解。implementation agent 需逐消费者验证替换完整性。

### R2: P2-C test migration breadth

大量 tests 直接构造 `AgentPolicy`，迁移是机械但广泛的。plan 已识别此风险并放在最后一个 sub WU 以减少 rebase churn。

### R3: Deepreview phase 执行质量

deepreview 的深度依赖 agent 和 controller 的执行质量。plan 已定义 8 个 review dimensions 和 exit criteria，但实际执行质量需要 controller 在运行时把控。这不是 plan 设计缺陷，而是流程执行风险。

## Plan Re-Review Conclusion

**pass**

C01-C11 全部在 plan fix 中闭合。plan 无 blocker：scope 无漂移、owner boundary 清晰、无下游止血、design decision 已写入、sub WU 顺序可执行、deepreview exit criteria 可验证、不违反 AGENTS.md。

用户指定的 6 项复核全部确认。3 项风险不阻塞 accepted plan commit。
