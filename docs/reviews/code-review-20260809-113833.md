# Aggregate Deep Review

## Scope

- Mode: current changes
- Branch: `codex/interactive-oracle`
- Base: `23097230861fe4acad730054e0ba9818ca42bd4f`
- Reviewed commits:
  - `c7a937e1` — accepted G06 fallback-grounding plan and plan review
  - `907a6ab3` — accepted S1 implementation, tests, design and code review
- Output file: `docs/reviews/code-review-20260809-113833.md`
- Included scope: complete G06-5 work unit, both production call paths, affected tests, design truth, package README and Gateflow artifacts.
- Excluded scope: pre-existing PR 190 changes, unrelated untracked review artifact, pre-fix observation report and Issue #193 implementation.
- Parallel review coverage: 无；the complete work unit is narrow and was traced locally end to end.

## Findings

未发现实质性问题。

## Adversarial aggregate review

### Goal alignment

- Every production change maps to the confirmed behavior: explicit degraded-context grounding only when a typed dispatch fallback is active.
- No compaction prompt/schema, attempt budget, accepted truth, Memory, provider transport, tool schema, terminal state or Oracle registry was changed.
- Test threshold adjustments only preserve the original purpose of successful synthetic fallback cases after the required system guidance became part of the authoritative complete input budget.

### Real entry points and branch ordering

- Initial/queued/recovery pre-start dispatch calls `prepare_runner_call_candidate_in_transaction`; fallback is loaded before message assembly and drives the guidance directly.
- The older/public `RunInputBuilder.build` path uses the same helper after its scene provider and before bounded context.
- Both paths pass through `_normalize_ordinary_run_messages`, which classifies the fixed prefix into the existing `Execution Guidance` section and enforces one system envelope.
- The `None` branch is explicit and precedes message creation; no broad condition can cause normal paths to receive the text.

### State, durability and consistency

- The helper is pure and has no write, I/O, mutable cache or retry behavior.
- Final AgentRunRequest messages, prepared candidate projection and runner-call manifest remain same-source because the guidance is inserted before normalization and candidate freezing.
- Fallback material refs/digests and durable `CONTEXT_COMPACTION_FAILED` facts remain unchanged; the new text is a projection of the active fact, not a new truth.
- Complete-candidate sizing still sees the actual final messages and can fail closed near the hard threshold.

### LLM-facing and trust boundary

- The model receives actions, evidence-use rules and a safe recovery decision, not implementation identifiers.
- Prior assistant text can still provide conversational continuity but is explicitly not evidence for missing facts.
- User/tool authority remains intact: tools are used only when available and permitted; no-tool or user-forbidden calls must report insufficiency.
- The change does not consume rejected compactor proposals or upgrade unverified text into Memory.

### Architecture, overcoupling and maintainability

- Host owns the fallback fact and its LLM-facing projection; UI, Service, Engine and provider adapters remain unchanged.
- No public API, shared contract, protocol, callback, builder, wrapper, compatibility path or second formatter was introduced.
- The module-level text and one private helper are the minimum reusable seam required by two existing Host call paths.

### Validation quality

- Direct builder and production scheduler paths are covered.
- Existing no-fallback exact-content tests provide negative coverage.
- Full affected suite: `238 passed`.
- `dayu/host/run_input.py` affected-suite coverage: `83%`, above the project target.
- Full Pyright: zero errors/warnings/informations.
- Changed Python Ruff, compileall and diff check: PASS.

## Open Questions

无。

## Residual Risk

- Model adherence is probabilistic. A post-fix real G06 operation is still required before user adjudication.
- Repeated stability and prompt A/B measurement remain assigned to Issue #193.
- No GitHub checks have been evaluated at this gate; PR gate will report their actual state without inferring pass.

## Conclusion

`pass`

All work-unit findings are closed; no fix/re-review loop is required.
