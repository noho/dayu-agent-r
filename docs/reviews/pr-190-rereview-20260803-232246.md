# PR 190 Re-review — MiMo Independent Verification

## Scope

- Mode: PR re-review
- PR: 190 — fix(cli): close interactive conformance gaps
- Author: noho
- Head: codex/interactive-oracle (b819309c)
- Base: main (113ea34d)
- URL: https://github.com/noho/dayu-agent-r/pull/190
- Output file: docs/reviews/pr-190-rereview-20260803-232246.md
- Re-review mandate: 逐项复核控制器对 F-01 至 F-07 的 disposition，以直接代码/设计/测试证据判断；不得仅复述控制器，也不得因两路初审一致而投票。
- Preservation artifacts（原样保留并完整读取）:
  1. `docs/reviews/pr-190-review-20260803-225333.md`（第一路）
  2. `docs/reviews/pr-190-review-20260803-225520.md`（第二路）
  3. `docs/gateflow/pr-190-pr-review-adjudication-no-code-resolution-20260803-231451.md`（控制器裁决）
- Parallel verification coverage: 4 subagents
  - Subagent A: F-01/F-02 VNext naming debt pre-existence verification
  - Subagent B: F-06 intent_type/reason downstream consumer audit
  - Subagent C: F-07 source_boundary_refs position contract verification
  - Subagent D: New critical/high/medium blocker sweep (15 automated checks)

---

## Adjudication Disposition Verification

### F-01: VNext class names — `deferred-with-owner` ✅ CONFIRMED

**Adjudication claim**: 9 个 VNext 类在 merge base 已存在，PR 190 未引入新的 VNext 命名债务。

**Independent verification evidence**:
- Merge base SHA: `113ea34d47b95812d79aa31705949bbb46bc6061`
- Merge base `compaction.py`: 9 个 `class.*VNext` 定义，95 处 `vNext` 引用
- Current HEAD `compaction.py`: 9 个 `class.*VNext` 定义，61 处 `vNext` 引用
- PR diff 中 `+.*VNext` 命中 4 行：2 处是 `field_name=` 字符串引用已有类型，2 处是函数签名格式重排引用已有类型——无一为新类定义
- PR diff 中 `+.*vNext` 命中 1 行：修改已有 docstring 措辞，非新增
- **净效果**: vNext 引用从 95 降至 61（净减少 34 处），PR 实际上缩小了命名债

**Verdict**: 裁决成立。命名债是历史遗留，PR 190 未扩大反而缩小了它。deferred-with-owner 是正确 disposition。

---

### F-02: vNext docstrings — `deferred-with-owner` ✅ CONFIRMED

**Adjudication claim**: 与 F-01 同一 destination，不创建第二 owner。

**Independent verification**: 与 F-01 共享同一证据链——merge base 95 处 → HEAD 61 处，PR 净减少 34 处。剩余 docstring 不进入 LLM-facing prompt，不改变 schema/durable state/Memory 行为。

**Verdict**: 裁决成立。与 F-01 同一后续 work unit 处理。

---

### F-03: Empty snapshot Protocol — `rejected-with-reason` ✅ CONFIRMED

**Adjudication claim**: 空 Protocol 是 RunInput adapter 绑定的 opaque lower-layer seam，不是 compact pipeline 可解释的数据 contract。

**Independent verification**:
- `compact_pipeline.py:1-6` 模块 docstring 明确限定为薄 helper contract
- `CompactPipelineProtectedRawTailProvider.load_ordinary_raw_tail` 的 snapshot 参数是 opaque handoff
- 生产 concrete value 是 `api.py:715-740` 的 `AttemptDispatchSnapshot`
- 两个生产 provider implementation 均 `del snapshot`，不读取其成员
- raw-tail 所需真实数据由另外三个窄协议参数提供
- 添加字段会把上层 concrete shape 泄漏进不消费它的下层 helper

**Verdict**: 裁决成立。空 Protocol 是有意的边界表达，不是缺陷。

---

### F-04: Interactive completion union — `rejected-with-reason` ✅ CONFIRMED

**Adjudication claim**: union 精确描述 `asyncio.wait` 容器的异构 task result 上界，不是业务结果的模糊分支。

**Independent verification**:
- `session_execution.py:2144` 的 union 覆盖 4 类真实 task result
- 两处集合确实是异构 task 集合
- driver 在 await 前已通过 task identity 判断类型，不从 union value 猜测语义
- 改为 `set[asyncio.Task[object]]` 会引入项目禁止的 `object`
- 为每个 task 创建独立 wait 集合会破坏 `FIRST_COMPLETED` 的单一竞争集合

**Verdict**: 裁决成立。union 是 asyncio.wait 的结构性需求，不是类型安全缺陷。

---

### F-05: Composer/driver phase ownership — `rejected-with-reason` ✅ CONFIRMED

**Adjudication claim**: phase state 的唯一 storage owner 是 composer；driver 只通过 public Protocol 调用 transition。

**Independent verification**:
- `_phase` 字段在 `composer.py:323` 声明、constructor 初始化
- 所有读取通过 composer 自身的 `_current_phase` / key-binding condition
- composer 对同步输入语义拥有必须早于 event-loop 调度的内部 transition（`_record_submit_intent` 在 Enter chord 解码时立即进入 `SUBMITTING`）
- driver 没有读取或写入 `_phase`，只通过 `set_phase` 通知 lifecycle transition
- `accept_submit(record_history=True)` 后 driver 再确认 `RUNNING` 是同值幂等 transition

**Verdict**: 裁决成立。finding 把"transition 的调用者"误判成"状态 owner"。

---

### F-06: intent_type/reason free-text — `rejected-with-reason` ✅ CONFIRMED

**Adjudication claim**: 两个字段是已冻结的 LLM-facing 业务语义，所有生产消费者均机械透传，没有按旧 enum token 分支。

**Independent verification**:
- `ForwardIntent.intent_type`（memory.py:595）：类型 `str`，校验仅 `_require_non_empty`
- `ReferenceContinuityItem.reason`（memory.py:406）：类型 `str`，校验仅 `_require_non_empty`
- 旧枚举值（`open_question`/`pending_clarification`/`pending_user_visible_task`/`next_step_note`/`local_reference`/`ordinal_reference`/`ellipsis_recovery`/`recent_state`）在 `dayu/host/` 生产代码中 **零命中**
- `ForwardIntentTypeVNext` 和 `ReferenceContinuityReasonVNext` 已被彻底移除
- 消费模式：Context Governance 用 `intent_type+text` 做重复 identity；Memory/durable/RunInput 纯透传
- prompt 中 `intent_type` 描述为"业务可读的后续动作类别"，`reason` 描述为"说明为何仍需保留"——均为开放文本

**Verdict**: 裁决成立。开放字符串是有意的业务语义，不是缺少防护。

---

### F-07: source_boundary_refs[0] position contract — `rejected-with-reason` ✅ CONFIRMED

**Adjudication claim**: 位置契约在 design.md 中有文档记录，strict parser 双重强制执行，无消费者索引 raw list。

**Independent verification**:
- `docs/host/design.md:3429` 明确规定："第一项必须是 `current_input_ref`，其余去重 refs 是 `compacted_source_refs`"
- design.md 同时禁止消费者索引 raw list、按 ref 前缀猜角色
- `compact_payload.py:204`: `current_input_ref=source_refs[0]` —— 位置解析
- `compact_payload.py:209-210`: `if source_refs != (semantics.current_input_ref, *semantics.compacted_source_refs): raise ValueError` —— 反向一致性校验
- `grep "source_boundary_refs\[" dayu/host/` 非测试文件零命中——无消费者索引 raw list
- `memory.py:1238-1239` 消费者通过 parser 投影的 `compacted_semantics.current_input_ref` 和 `.compacted_source_refs` 访问

**Verdict**: 裁决成立。位置契约已文档化、已强制执行、无消费者绕过投影层。

---

## New Blocker Sweep

15 项自动化扫描，结果全部 clean：

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | TODO/FIXME/HACK/XXX in diff | ✅ zero hits |
| 2 | Bare `except:` / `except Exception:` | ✅ zero hits |
| 3 | `# type: ignore` additions | ✅ zero hits |
| 4 | `assert` in production code | ✅ zero hits |
| 5 | Blocking I/O in async code | ✅ 3 hits，全部在同步上下文（editor helper、pipe reader、stdin reader） |
| 6 | Hardcoded secrets/credentials | ✅ 全部为 test fixtures 或文档示例 |
| 7 | New public exports missing `__all__` | ✅ `ProviderRequestIdAvailability` 和 `SuccessfulRunnerResponseIdentity` 已添加 |
| 8 | `typing.Any` usage | ✅ 仅 `Keys.Any`（prompt_toolkit 键盘绑定） |
| 9 | Bare `object` type annotation | ✅ 仅在 docstring/error messages 中 |
| 10 | `hasattr`/`getattr` | ✅ zero hits |
| 11 | Lazy imports | ✅ zero hits |
| 12 | Compatibility/fallback shims | ✅ 全部为业务领域 fallback |
| 13 | `dayu.runtime` layer violations | ✅ zero hits |
| 14 | New packages missing `__init__.py` | ✅ zero hits |
| 15 | New production file without test | ✅ `compaction_terminal.py` 有对应 `test_compaction_terminal.py` |

**未发现新的 critical/high/medium correctness 或 conformance blocker。**

---

## Verdict

**所有 7 项裁决均经独立直接证据验证成立。未发现新的阻塞问题。**

| Finding | Adjudication | Re-review | 阻塞 |
|---------|-------------|-----------|------|
| F-01 VNext class names | deferred-with-owner | ✅ confirmed | 否 |
| F-02 vNext docstrings | deferred-with-owner | ✅ confirmed | 否 |
| F-03 empty Protocol | rejected-with-reason | ✅ confirmed | 否 |
| F-04 completion union | rejected-with-reason | ✅ confirmed | 否 |
| F-05 phase ownership | rejected-with-reason | ✅ confirmed | 否 |
| F-06 free-text fields | rejected-with-reason | ✅ confirmed | 否 |
| F-07 position contract | rejected-with-reason | ✅ confirmed | 否 |

**Re-review 结论: pass。** PR 190 当前 head (b819309c) 无 correctness、stability 或 conformance blocker。deferred naming debt（F-01/F-02）由后续专门 work unit 处理，不阻塞 merge。

---

## Residual Risks

1. **CI checks**: PR 仍无 reported checks。merge 前需确认 CI pipeline 配置。
2. **Naming debt tracking**: F-01/F-02 的 deferred work unit 需要在 merge 后创建并跟踪。
3. **PR 体量**: 390 files 已通过两路完整 review + 控制器裁决 + re-review 四轮覆盖，但体量本身仍是长期维护风险。
