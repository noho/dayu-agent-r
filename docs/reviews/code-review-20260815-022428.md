# Code Review — S2 Final Narrow Re-Review

## Scope

- Mode: current changes（Gateflow S2 final narrow re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `c5446b770d238aafd8c42552dadbe132cba94ad2`（accepted S1 local commit；HEAD == base，全部 S2 变更未提交）
- Output file: `docs/reviews/code-review-20260815-022428.md`
- Included scope: controller accepted 两个 low findings 的窄修复——commit-time backup 存在性判定（`_commit_batch_with_publication_guard`）与 begin-time symlink/regular-file owner tests；以及对两处修复的影响范围回归（`_commit_batch_with_publication_guard` 两个调用方、rollback 语义、错误投影链、lock 释放、reservation 释放）
- Excluded scope: S2 implementation / S2 fix 其余内容（已由前两轮 re-review 覆盖并通过）、S1 已接受内容、docs/reviews 下本 WU gate artifacts、UF-PF05/oracle/evidence
- Parallel review coverage: 无。主 reviewer 逐行走读了 commit/begin/rollback/error-projection 全链路与四个测试源码。

已读取上下文：根 `AGENTS.md`、`wu-upload-filing-ticker-alias-contract-s2-rereview-controller-adjudication.md`、S2 fix artifact（含 §8 follow-up）、上轮两份 re-review（`code-review-20260815-020224.md`、`code-review-20260815-021015.md`）。

## 结论

**PASS / 无 blockers**。controller accepted 两个 low findings 均已按 adjudication 要求真实修复；四个新测试为有效 owner-level 断言；影响范围回归未发现新 defect；独立复跑全部验证通过。

## Accepted Finding 逐条核验

### Finding 1 — commit-time backup 显式 lstat + replace 前 fail closed ✓

**验证路径**：`dayu/fins/storage/_fs_storage_infra.py:751-790`（`_commit_batch_with_publication_guard`）→ `:1110-1134`（`_lstat_optional_storage_path`）→ `dayu/fins/storage/_fs_storage_utils.py:425-474`（`_project_filesystem_error` / `_raise_path_free_error`）→ 测试 `test_commit_backup_lstat_io_failure_precedes_replace_and_preserves_evidence`、`test_commit_backup_rejects_non_directory_before_replace_and_preserves_locator`。

逐条确认：

1. **显式 lstat 且位置正确**：`:769-772` 在取得 target publication guard（`:766`）之后、第一次 `_replace_directory`（`:776`）之前调用 `_lstat_optional_storage_path(state.target_ticker_dir, action=...)`；旧 `Path.exists()` 判定已删除（全文件 grep 确认 commit backup 决策处无残留 `exists()`）。
2. **仅 ENOENT 表示 missing**：helper `:1129-1134` 显式 `os.lstat`，`exc.errno == errno.ENOENT` 返回 `None`；`None` 时跳过 backup 直接 replace（`:775` 分支不命中），与旧 `exists()==False` 行为逐 case 等价。
3. **EACCES/EIO fail closed**：其余 errno 走 `_raise_path_free_error(_project_filesystem_error(...))`；`_new_path_free_filesystem_error` 保留原 `errno`（`_fs_storage_utils.py:420-422`），`_raise_path_free_error` 显式清除 exception context，错误图无 locator。异常在任一 replace 前抛出。
4. **non-directory/symlink 在 replace 前拒绝**：`:773-774` `not stat.S_ISDIR(target_stat.st_mode)` 抛 `ValueError("commit backup target ticker root 必须为非 symlink 目录")`。lstat 不跟随 symlink，symlink 的 mode 为 `S_IFLNK`，与 regular file 同样命中拒绝分支。
5. **evidence/tree 不变**：失败进入 `:781-790` except → `_rollback_precommit_batch`；此时 staging 存在、target 存在、backup 不存在，`:1577-1585` 不触发任何 replace，写 `_PHASE_ROLLED_BACK` 后移除 staging root。published tree 与 backup_root 均零触碰；`commit_batch`（`:647`）因 phase 已为 ROLLED_BACK 跳过二次 rollback，`_close_active_batch` 消费 capability 后重抛。
6. **测试断言有效性**：
   - `test_commit_backup_lstat_io_failure_precedes_replace_and_preserves_evidence`（EACCES/EIO 参数化）：begin 成功后才注入 exact-path 一次性 `os.lstat` 失败（`_OneShotLstatFailure` 仅首次命中 target 抛错、之后透传，`:152-202`）；monkeypatch `_replace_directory` 为 `_ReplaceDirectoryForbidden`（记录调用并抛 AssertionError，`:389-422`），`replace_forbidden.calls == []` 直接证明 replace 为零次；断言 errno 保留、错误不含 target path、published tree bytes 不变、backup_root bytes 不变、staging 清理、`_active_batches` 清空。
   - `test_commit_backup_rejects_non_directory_before_replace_and_preserves_locator`：begin 后把真实 published 目录停放到 `parked-delta`、在 exact target 放 regular file；commit 抛 `ValueError`（match 文案）、replace 零次、foreign bytes 不变、parked tree byte-exact、backup evidence 不变、staging 清理、capability 消费。

### Finding 2 — begin-time symlink/regular-file owner tests ✓

**验证路径**：`test_begin_batch_rejects_non_directory_locator_and_releases_writer`（`tests/fins/test_company_identity_storage_contract.py:1160-1213`）→ 生产分支 `_fs_storage_infra.py:541-547`。

逐条确认：

1. **symlink/regular_file 参数化**：`:1160` `parametrize("locator_kind", ("symlink", "regular_file"))`；symlink 用例为 `target_is_directory=True` 的真实目录 symlink，regular-file 用例为带 foreign bytes 的真实文件。
2. **断言异常**：`pytest.raises(ValueError, match="batch target ticker root")`，与生产 `:547` 文案一致。
3. **published locator 不变**：`:1196-1202` 用 `os.lstat` 分别断言 mode 仍为 `S_ISLNK`/`S_ISREG`，symlink 断言 `os.readlink` target 不变、regular file 断言 exact bytes 不变。
4. **reservation/writer 可恢复**：`:1203-1206` 断言 `_active_batches`、`_active_transaction_by_ticker`、`_reserved_batch_tickers` 均空且 batch_root 无泄漏；`:1208-1213` 移除非法 fixture 后同 ticker 重试 `begin_batch` + `rollback_batch` 成功。writer 恢复证明有效——`_acquire_ticker_lock` 经 `_acquire_lock_token(blocking=True)`（`:1992-2010`）取阻塞式跨进程文件锁，若 `:593-601` 的锁释放失败，重试会永久阻塞而非通过；reservation 释放由 `:612-614` finally 的 `_release_batch_ticker_reservation` 保证并被直接 set 断言覆盖。

## 影响范围回归

- **`_commit_batch_with_publication_guard` 仅两个调用方**：`commit_batch`（`:644`，document-only 既有 corpus）与 `_commit_batch_with_identity_guards`（`:719`，identity-changing）。两路径均走读：新增 lstat 只是 backup 前的 fail-closed stat，不改变锁序（仍为 writer → [recovery → identity →] publication），ENOENT 语义与旧 `exists()==False` 逐 case 等价；identity 路径中 `_read_current_company_meta_for_commit`（`:882`）的既有 lstat 调用不受影响。
- **行为差异仅发生在 commit 窗口内 target 被外部篡改的异常场景**：旧代码对 regular-file target 会 `os.replace` 把文件移入 backup 再替换 staging（静默吞掉 foreign locator）；新代码在 replace 前 `ValueError` fail closed。属严格改进且被测试 pin 住。
- **错误投影链完整**：commit-time lstat 失败已是 path-free + errno 保留的 OSError，经 `commit_batch` 重抛后由既有 `fins_upload_failure_from_exception` 映射为 `STORAGE_IO`，无新 mapper 或下游补偿。
- **rollback/cleanup/recovery 中残留的 `Path.exists()`**（`:1577`、`:1580`、`:1601`、`:2342`、`:2381` 等）为 S1 既有语义，不在本次 accepted findings 范围内，前两轮已 review；未因本 fix 改变。
- **测试助手的有效性**：`_OneShotLstatFailure` 的 exact-path 比较（`Path(path) == self._failed_path`）由测试实际通过证明与生产 locator 精确一致；`_ReplaceDirectoryForbidden` 在 rollback 若误触 replace 时会先记录 calls 再抛 AssertionError，`calls == []` 是直接证据而非间接推断。

## 独立复跑验证

全部命令均在 `source .venv/bin/activate` 后执行：

- 四个聚焦测试（含参数化）：`7 passed`。
- `test_company_identity_storage_contract.py + test_company_meta_contract.py + test_fins_storage_atomicity.py`：`198 passed`。
- 完整 relevant 套件 `tests/fins + tests/tools/test_combined_tools_acceptance.py`：`1604 passed, 1 skipped, 3 warnings`，与 fix artifact §8.4 声明完全一致；skip 为既有环境条件，warnings 为 `edgar` dependency deprecation。
- 全量 `pyright`：`0 errors, 0 warnings, 0 informations`。
- `git diff --check`：通过。

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- commit-time non-directory 测试只参数化了 regular file；commit-time symlink 与 regular file 共用同一 `S_ISDIR` 拒绝分支，且 begin-time 测试已覆盖 lstat 对 symlink 的分型，属组合覆盖充分，未再单独补 commit-time symlink 用例。
- filesystem 权限/I/O 测试为 monkeypatch 注入 errno，真实 ACL/NFS 跨平台行为未验证（与 fix artifact 声明一致）。
- 全仓非 Fins 测试未运行（Host/Engine 未修改）。
