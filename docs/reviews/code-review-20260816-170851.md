# Code Review（Slice 4 re-review，Controller 最小修复后）

## Scope

- Mode: current changes（re-review after Controller minimal fix）
- Branch: `codex/upload-filing-oracle`
- Base: HEAD `1fd52c96f5e07f6007c3c6442d20635f3d37182b`
- Output file: `docs/reviews/code-review-20260816-170851.md`
- Included scope:
  - 被拒绝的 review：`docs/reviews/code-review-20260816-170139.md`（PASS-with-finding，Finding 1 为 Slice 2 语义漂移）
  - 修复 artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-code-review-fix-20260816.md`
  - 当前完整未提交 diff（10 files，+1857/-55）
  - 重点增量：`_fs_source_integrity.py`（`_derive_repair_blocked_reason` whole-kind 分支重写 + `shared_manifest_reasons` 参数新增）、`test_fins_storage_atomicity.py`（断言修正为 `CANONICAL_MANIFEST_UNAVAILABLE`）、`test_docling_upload_service.py`（material damage expected reason 同步修正）
- Excluded scope: SEC/CN/HK workflow、download、README 属 later slices
- Parallel review coverage: 无

## 结论

**PASS**（0 findings）

被拒绝 review 的 Finding 1（Slice 2 whole-kind reason 语义漂移）已由 Controller 最小修复闭环。当前 diff 中 `_derive_repair_blocked_reason` 的 whole-kind 分支精确匹配 Slice 2 fix D 裁决：repairable incomplete → `CANONICAL_MANIFEST_UNAVAILABLE`，unsafe → `CROSS_SOURCE_PUBLICATION_UNSAFE`。九项 Slice 4 语义与三项初审 findings 无回退。

---

## 被拒绝 Finding 1 闭环验证

### 问题

被拒绝 review（170139）发现：修复后 whole-kind mode（`target is None`）下 content 损坏返回 `NON_TARGET_SOURCE_INISMATCH`，违反 Slice 2 fix D 裁决"whole-kind repairable incomplete 固定 `CANONICAL_MANIFEST_UNAVAILABLE`"。

### 修复证据

`_derive_repair_blocked_reason()`（`_fs_source_integrity.py:1294-1307`）新增 `target is None` 专用分支：

```python
if target is None:
    if (
        shared_manifest_reasons
        or any(
            item.content_classification.status
            is not SourceIntegrityStatus.COMPLETE
            or item.classification.reasons != shared_manifest_reasons
            or item.canonical_manifest_item is None
            for item in inventory
        )
        or len(canonical_manifest_items) != len(inventory)
    ):
        return SourceIntegrityRepairBlockedReason.CANONICAL_MANIFEST_UNAVAILABLE
    return None
```

- whole-kind mode 下所有 content 非 COMPLETE、reasons 不匹配、item 缺失、shared manifest 问题、aggregate 不完整均返回 `CANONICAL_MANIFEST_UNAVAILABLE`。
- `SOURCE_MANIFEST_UNTRUSTED` 在 `shared_manifest_reasons` 中 → `CROSS_SOURCE_PUBLICATION_UNSAFE`（line 1291-1292），在 `target is None` 分支前已拦截。
- non-target public `UNSAFE` → `CROSS_SOURCE_PUBLICATION_UNSAFE`（lines 1286-1290），同样在 `target is None` 分支前。

精确匹配 Slice 2 fix D 裁决：
- ✅ whole-kind repairable incomplete → `CANONICAL_MANIFEST_UNAVAILABLE`
- ✅ whole-kind unsafe → `CROSS_SOURCE_PUBLICATION_UNSAFE`

### exact-target mode 不受影响

`target is not None` 分支（lines 1309-1318）仍对 `non_target_inventory` 检查：
- content 非 COMPLETE / reasons ≠ shared → `NON_TARGET_SOURCE_INCOMPLETE`（line 1314）
- item 缺失 → `CANONICAL_MANIFEST_UNAVAILABLE`（line 1316）

### 测试断言同步修正

- `test_material_whole_inspection_owns_repair_blocked_reason`：`content_missing` 和 `manifest_missing` 均断言 `CANONICAL_MANIFEST_UNAVAILABLE`（非 `NON_TARGET_SOURCE_INCOMPLETE`）。
- `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once`：`content_missing` 和 `manifest_missing` 均断言 `CANONICAL_MANIFEST_UNAVAILABLE`。

---

## Controller 7 项严格验证逐项证据

### 1) 初审三类 findings 全部闭环

| Finding | 状态 | 证据 |
|---|---|---|
| F1（高）：material damage 对 Phase B 不可见 | ✅ 已修复 | material whole-kind inspection 在 identity comparison 后、reset 前（`_fs_source_document_core.py:455-475`）；`SourceIntegrityPreflightError` → `CROSS_SOURCE_PUBLICATION_UNSAFE`；`repair_blocked_reason` 直接消费。service 测试三场景均 `SOURCE_REPAIR_BLOCKED`、rollback once、old tree 不变。 |
| F2（中）：blocked predicate 双 owner | ✅ 已修复 | `_derive_repair_blocked_reason` 为唯一 owner；core helper 只 `inspection.repair_blocked_reason is not None → raise` + producer invariant `RuntimeError`。 |
| F3（低）：fail-closed 缺测试 | ✅ 已修复 | invalid disposition / delete×repair / manifest rewrite OSError 三测试已补齐。 |

### 2) repair_blocked_reason 仅由 storage inspector owner 派生，core 无重算

- `_derive_repair_blocked_reason()` 是唯一赋值点（`_fs_source_integrity.py:1290,1292,1306,1314,1316`）。
- core 两处消费均为 `inspection.repair_blocked_reason is not None → raise`（`_fs_source_document_core.py:108,472`），无任何 content/status/reasons 二次判断。
- producer invariant 违约抛 `RuntimeError`（lines 120,125），非 `SourceIntegrityRepairBlockedError`。

### 3) staged batch 内 filing exact inspection 恰一次、material whole-kind inspection 恰一次

- `_InspectorCallCounter` monkeypatch 断言：`counter.calls == [(FILING, target_id), (MATERIAL, None)]`（`test_fins_storage_atomicity.py:7281-7284`）。
- 无循环、无重扫。

### 4) material content/manifest/root 损坏均在 reset 前投影 SOURCE_REPAIR_BLOCKED，rollback 不变

- `content_missing` → `CANONICAL_MANIFEST_UNAVAILABLE`（owner whole-kind 分支）
- `manifest_missing` → `CANONICAL_MANIFEST_UNAVAILABLE`（owner whole-kind 分支）
- `structural_unsafe` → `CROSS_SOURCE_PUBLICATION_UNSAFE`（`SourceIntegrityPreflightError` 映射）
- 三场景均断言 `SOURCE_REPAIR_BLOCKED`、`rollback_calls == 1`、`commit_calls == 0`、old tree SHA/filing meta/material meta/company meta 不变、batch root 清空。

### 5) target local unsafe 与 shared filing manifest untrusted 的 stale/conflict 优先级保持

- target conflict gate（lines 438-444）在 material inspection（line 455）之前。
- `target_unsafe` → `SOURCE_REVISION_STALE`（service 测试 line 3229）
- `shared_untrusted` → `SOURCE_REVISION_STALE`（service 测试 line 3230；storage drift grid line 7530）
- 均非 `SOURCE_REPAIR_BLOCKED`。

### 6) invalid disposition、delete+repair、manifest rewrite OSError rollback 测试有效

- `test_prepare_upload_rejects_invalid_repair_disposition`：`ValueError("repair_disposition 必须是封闭 repair contract")`
- `test_prepare_upload_rejects_delete_with_existing_repair`：`ValueError("delete 上传不得携带 existing source repair 授权")`
- `test_existing_filing_repair_manifest_rewrite_failure_rolls_back_once`：patch `_write_json` → `OSError`，rollback once、commit zero、old tree 不变。

### 7) 原 Slice 4 九项语义、scope、pyright、覆盖率与冻结文档约束无回退

- 九项语义：disposition 贯穿/identical-skip bypass、storage 唯一 owner 与 gate 顺序、target-local damage 不误判 blocked、staging 污染边界、facade 纯 delegate、stale/blocked 精确映射、Controller caller 机械接线、tests 真实命中、无兼容/default/Any/反射——均无回退。
- scope：10 个 modified files 全在 allowed + Controller 扩展 + Controller 授权的 `_fs_source_integrity.py` 内。
- frozen guards：`git diff -- README.md dayu/fins/README.md tests/README.md docs/cli_ci_oracles.json docs/cli_ci_scenarios.json docs/host/design.md docs/engine/design.md` 输出为空。
- pyright：`0 errors, 0 warnings, 0 informations`（fix artifact 声称；本 reviewer 独立运行 10 文件定向检查通过）。

---

## Open Questions

无。

## Residual Risk

- 完整 affected matrix（345 passed）与完整 Fins suite（1827 passed）由 fix artifact 声称，本 reviewer 仅独立复现 repair 相关用例与定向 pyright。
- `_derive_repair_blocked_reason` 的 exact-mode `CANONICAL_MANIFEST_UNAVAILABLE`（line 1316）在当前 Phase B 可达路径上不可达（non-target item 缺失 ⇔ content 非 COMPLETE，已被 line 1309-1314 先行命中 `NON_TARGET_SOURCE_INCOMPLETE`）。该分支为防御性代码，不影响行为。
- Slice 5/6 的 SEC/CN/HK fresh reread、download unsafe、README 不在本 slice scope。
