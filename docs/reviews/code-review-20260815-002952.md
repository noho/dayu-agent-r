# Code Review — Gateflow S1 re-review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `5508d0445bd1d649fee54f4ec3d65f99e2484493`（`gateflow: accept plan for upload-filing-ticker-alias-contract`）
- Output file: `docs/reviews/code-review-20260815-002952.md`
- Included scope: 当前未提交 S1 改动及 accepted F1/F2 fix 的全部 workspace changes —— 18 个生产文件 + 18 个更新测试文件 + 1 个新增测试文件（`tests/fins/test_ticker_normalization.py`）。已完整读取 `AGENTS.md`、deepreview skill、accepted plan（`wu-upload-filing-ticker-alias-contract-plan-codex.md`）、plan re-review 第二轮 adjudication、S1 implementation artifact、前两轮 code review artifact、S1 fix artifact 与 controller adjudication。
- Excluded scope: S2 的 commit intent / unique route / typed corruption / lock 图、read route 切换、README、UF-PF05、oracle/scenario/frozen evidence —— 均按 accepted plan 与 controller adjudication 冻结。
- Parallel review coverage: 无；主 reviewer 逐文件走读全部 diff，并独立复跑 coverage gate、focused tests、全量回归、pyright 与 residue scans。

## Findings

### 1-已修复-中-6-K 单文件 branch coverage 76% -> 91%，新增测试均为有效 owner/public-path 行为测试

- **入口/函数**: `reconcile_active_6k_primary_documents` / `select_prepared_6k_primary_document` 全文件（S1 变更点为 `_resolve_target_tickers` 的 `entry.company_meta.ticker_identity.canonical_ticker` 投影）
- **文件(行号)**: `dayu/fins/pipelines/sec_6k_primary_document_repair.py:347`（变更行）；`tests/fins/test_sec_pipeline_download.py:4228-4470`（新增测试）
- **输入场景**: 独立执行 `coverage run --branch -m pytest tests/fins/test_sec_pipeline_download.py` 后对单文件执行 `coverage report --include=<file> --fail-under=80`
- **实际分支**: 新测试触达 publication 前非 6-K 早退、无 HTML 早退、payload 缺失早退、非法 `meta.files` fail-closed、public filter 去空/大写/稳定去重与清洗后空值拒绝、单文档 reconcile 失败时 caller batch rollback
- **预期行为**: `AGENTS.md` 与 accepted plan §12.3 要求每个修改生产文件 branch coverage >= 80%，且必须补 owner 行为测试，禁止改生产逻辑抬覆盖率
- **实际行为**: 本 review 独立复跑得 181 statements / 60 branches / **91%**，`--fail-under=80` 通过；生产文件 diff 仍只有 S1 的一行机械迁移，fix gate 未改任何生产逻辑
- **直接证据**: 本 review 复跑 coverage 输出；`git diff 5508d044 -- dayu/fins/pipelines/sec_6k_primary_document_repair.py` 仅 1 行变更；新增测试全部通过真实 public 函数（`select_prepared_6k_primary_document`、`reconcile_active_6k_primary_documents`）断言行为而非空执行；rollback 测试断言 `get_source_meta(...) == before_meta`，以持久化状态证明 published tree 未被破坏
- **影响**: 无（原 76% 违反硬门槛，现已关闭且未以 omit/pragma/降阈值/空测试绕过）
- **建议改法和验证点**: 无需进一步动作；S2 应继续维持该文件的逐文件 coverage gate
- **修复风险（低）**:
- **严重程度（中）**:

### 2-已修复-中-`UploadCompanyNameRequiredError` 专用异常落地，runtime 只捕获该类型，identity mismatch 与 builder ValueError 不再伪装为 COMPANY_NAME_REQUIRED

- **入口/函数**: `_validate_fins_upload_filing_request` → `resolve_upload_company_meta_decision`
- **文件(行号)**: `dayu/fins/pipelines/upload_company_meta.py:25`（异常定义）、`:216`（唯一抛出点 `_require_upload_company_name`）；`dayu/fins/ingestion_runtime.py:1060-1061`（精确 catch）
- **输入场景**: ① new/stale create/update 缺公司名；② strict-valid published CompanyMeta 与 incoming canonical/market/exchange 不一致；③ invalid builder alias
- **实际分支**: ① 抛 `UploadCompanyNameRequiredError` → 唯一映射 `COMPANY_NAME_REQUIRED`；② 抛普通 `ValueError("既有 CompanyMeta 与本次 canonical ticker identity 不一致")` → 不被 runtime 捕获，沿非 usage 失败传播；③ 已在静态校验（`ingestion_runtime.py:892`）先行投影 `INVALID_TICKER_ALIAS`，不可达 decision
- **预期行为**: controller adjudication F2 required fix：只有 missing/stale 缺公司名由 owner 抛专用异常；runtime 只捕获该类型；identity mismatch 与 builder ValueError 不得被捕获
- **实际行为**: 与裁决完全一致。`grep UploadCompanyNameRequiredError` 显示生产抛出点唯一、runtime catch 精确为该类型；`except ValueError` 剩余命中（852/876/892/898/908/993/2925/5299）均紧贴各自静态 validator 或事件/处理器构造，不包裹 decision 调用；SEC/CN material producer 路径（`stage_company_meta_for_upload`）无 blanket catch 把该异常重分类
- **直接证据**: 代码逐行走读 + 测试复跑：`test_upload_company_meta_missing_name_uses_typed_owner`（None/stale 参数化两例抛专用异常；invalid alias 抛普通 ValueError 且 `not isinstance(..., UploadCompanyNameRequiredError)`）；`test_upload_validator_does_not_project_identity_mismatch_as_company_name_required`（strict-valid MSFT meta + incoming AAPL → `ValueError match="canonical ticker identity"` 且 `not isinstance(..., FinsUploadUsageError)`，比裁决要求的“非 COMPANY_NAME_REQUIRED”更强）
- **影响**: 无（原错误投影语义漂移已关闭；不丢数据、无错误持久化）
- **建议改法和验证点**: 无需进一步动作；S2 落地 typed corruption 时按 plan §5.7 完成最终投影
- **修复风险（低）**:
- **严重程度（中）**:

### 3-已裁决-低-read_runtime.py 一行机械 consumer 迁移维持 accepted-with-note

- **入口/函数**: `FinsReadRuntime._resolve_company_name_and_market`
- **文件(行号)**: `dayu/fins/tools/read_runtime.py:2753`
- **输入场景**: read route 消费 CompanyMeta 时
- **实际分支**: `return company_meta.company_name, company_meta.ticker_identity.market`
- **预期行为**: F3 accepted-with-note：保留该行，S2 再切换 read route/schema/failure projection
- **实际行为**: `git diff 5508d044 -- dayu/fins/tools/read_runtime.py` 确认 diff 仅此一行；route、normalize/upper fallback、schema、错误投影均未变。fix gate 未再触碰该文件
- **直接证据**: diff 单行；residue scan 无 `company_meta.market` 裸访问残留
- **影响**: 无（不迁移则 pyright/运行期必挂；迁移后行为等价）
- **建议改法和验证点**: S2 按 plan §5.5 完成 read contract 切换时把该文件重新纳入 gating
- **修复风险（低）**:
- **严重程度（低）**:

### 4-已裁决-低-upload help 不修改的裁决被遵守

- **入口/函数**: `_add_upload_ticker_argument`
- **文件(行号)**: `dayu/cli/arg_parsing.py:1080-1101`
- **输入场景**: `dayu-cli fins upload_material --help`
- **实际分支**: 三个 upload 命令共用 help：第一项 canonical、后续项为用户声明 aliases、系统信任且不联网核验、成功保存公司元数据后均查询同一归档
- **预期行为**: F4 rejected-with-reason：现有 help 在 upload-only 语境下自足，不加 material producer 内部术语
- **实际行为**: 与裁决一致。fix gate 声明只改 4 个文件（upload_company_meta.py / ingestion_runtime.py / 两个测试文件），arg_parsing.py 不在其中；当前 help 文案与第一轮 review 记录的 S1 实现状态一致，无新增 material 专属句；测试断言 `"成功保存公司元数据后均查询同一归档"` 等关键句仍通过
- **直接证据**: arg_parsing.py diff 与 fix artifact §4 文件清单互证
- **影响**: 无
- **建议改法和验证点**: 无需动作
- **修复风险（低）**:
- **严重程度（低）**:

## 逐项复核结论（按 re-review 指令）

| 验证项 | 结论 | 直接证据 |
| --- | --- | --- |
| F1 6-K 单文件 branch coverage >= 80 且测试有效 | **通过** | 独立复跑 91%；测试断言真实行为（fail-closed、rollback 后 published meta 不变、discovery 投影）；生产文件零逻辑改动 |
| F2 `UploadCompanyNameRequiredError` 只表达缺公司名，runtime 不再吞 identity/builder ValueError | **通过** | 唯一抛出点 `upload_company_meta.py:216`；runtime 唯一 catch `ingestion_runtime.py:1060`；owner/runtime 测试证明 mismatch 与 invalid alias 均不投影 COMPANY_NAME_REQUIRED |
| F3 机械 read consumer 迁移维持 accepted-with-note | **通过** | read_runtime.py diff 仅一行；无 route/fallback/schema/projection 扩散 |
| F4 help 不改的裁决被遵守 | **通过** | fix 文件清单与 arg_parsing.py 当前状态一致，help 未变 |
| 无 S2 提前实现 | **通过** | `CompanyMetaCommitIntent` / `resolve_company_ticker` / `CompanyTickerAliasConflictError` / `CompanyTickerIdentityCorruptionError` / `company_identity.lock` / `WORKSPACE_IDENTITY_CORRUPTED` 全仓零命中 |
| 无 UF-PF05 / oracle / scenario / frozen evidence | **通过** | diff 文件名无相关命中；diff 仅含 `dayu/` 与 `tests/` 下 37 个文件 |
| 无其他 finding | **通过** | 逐文件走读 18 个生产 diff；测试断言均向新 contract 迁移或强化，无弱化；无 compat shim / accept-ignore / 重复 grammar owner / 旧 FmpCompanyInfo 构造 |

## Adversarial Failure Pass

### Residue scans（本 review 复跑）

- duplicate grammar helpers（`_canonicalize_ticker_alias` / `_normalize_company_ticker_aliases` / `_normalize_ticker_aliases` / `normalize_sec_ticker_aliases` / `_merge_ticker_aliases` / `_normalize_ticker_token` / `_dedupe_ticker_aliases`）：**零命中**。
- legacy `company_meta.ticker / market / ticker_aliases` 裸字段访问：**零命中**（regex 命中的均为合法 `ticker_identity.*` 前缀）。
- 旧 `FmpCompanyInfo(canonical_ticker=..., ticker_aliases=...)` 构造：**零命中**。
- `upsert_company_meta_for_upload`：**零命中**。
- S1 临时 route symbols（`resolve_existing_ticker` / `_resolve_existing_ticker_by_company_alias` / `_build_company_alias_index_from_meta`）：仍有命中，与 plan §10 S1 允许清单一致，alias index 仍为 `dict[str, list[str]]` 且 late multi-owner ValueError 保留 —— 属 accepted S2 residual。
- material aliases 数据流：`FinsUploadMaterialRequest.ticker_aliases → service_runtime → SecPipeline/CnPipeline stage_company_meta_for_upload → resolve_upload_company_meta_decision → stage_upload_company_meta_decision → upsert_company_meta` 贯通，无 accept-ignore；CN/SEC material 测试均从 committed CompanyMeta 与 storage route 断言 aliases。

### 反例与边界

- `V.BA → V-BA`、`AAPL.SW → AAPL-SW` 单分节 grammar：`test_normalize_ticker_supports_identity_grammar` 覆盖；8/9 字符边界与多分节/空分节拒绝覆盖于 `test_us_ticker_*_boundary`。
- `DELTA,MSFT` 无条件保留：`test_build_company_ticker_identity_trusts_distinct_declared_alias`；canonical-equivalent 稳定去重：`test_build_company_ticker_identity_stably_deduplicates_equivalent_aliases`。
- fresh 新 alias stage / fresh unchanged keep / stale union 与 company_name 不被 alias-only update 覆盖：filing 与 material stream 测试均断言（含 "Ignored Apple Name" 反例）。
- CompanyMeta from_dict 四类 fail-closed（缺字段 / wrong type / market mismatch / 非法 alias）覆盖于 `test_company_meta_from_dict_rejects_non_strict_identity_json`。
- 6-K 单文档失败 rollback：monkeypatch 单文档 reconcile 抛 RuntimeError，断言 published source meta 不变。

### Coverage / 类型 / 格式（本 review 复跑）

- 6-K 单文件：**91%**（`--fail-under=80` 通过，独立测试文件采集）。
- 全量回归采集下 18 个修改生产文件逐一 `coverage report --include=<file> --fail-under=80`：**全部通过**，最低 `_fs_storage_utils.py: 81%`，与 fix artifact 表格一致。
- 全量回归：`3547 passed, 9 skipped, 6 failed`。6 个失败与 controller 在隔离 `5508d044` worktree 的基线复现清单一致。本 review 追加验证其中两项失败路径与本 diff 无数据同源：`test_upload_filings_from_usage_empty_and_write_failures` 的 raise 位于 `dayu/cli/upload_script.py:316`（不在 diff）、catch 文案位于 `dayu/cli/commands/fins.py:102`（fins.py 虽在 diff，但 diff 无任何异常处理行变更）；`test_service_does_not_import_forbidden_layers` 涉及 `dayu/service/fins_direct.py` 与 `dayu/fins/download_contract`（均不在 diff）。
- `python -m pyright dayu/ tests/ utils/`：**0 errors, 0 warnings, 0 informations**。
- `git diff --check`：通过。

### 测试弱化检查

- 删除的 `test_upload_company_meta_ticker_aliases_use_canonical_owner` 测的是已删除函数，等价且更强的 owner 断言已由 `test_ticker_normalization.py` 承接。
- 旧 `CompanyMeta(ticker=..., market=..., ticker_aliases=...)` 构造与 `company_meta.ticker_aliases == ["AAPL", ...]` 断言全部迁移为 `ticker_identity` contract，且多处断言更严（fresh name 保留、durable route 命中）。
- storage late-conflict 测试把旧非法 grammar alias（"MICROSOFT"）迁移为 grammar-valid alias（"MSFT-A"/"DUP-C"），late multi-owner ValueError 语义未被削弱。
- 未发现为适配实现而削弱的 assertion。

## Open Questions

- **工具边界的 ValueError 投影（留给 S2 收口）**：identity mismatch `ValueError` 沿 `prepare_observed_upload` 传播到 `dayu/fins/tools/upload_tools.py:104` 既有 `except ValueError`，被归一化为 `_ERROR_INVALID_ARGUMENT` failed outcome（message 携带真实原因，hint 为通用输入检查提示）。该 catch 先于本 diff 存在、未改，且不产生 COMPANY_NAME_REQUIRED 投影，符合 F2 裁决“其它错误先保持非 usage failure，S2 再完成最终 typed corruption 投影”。S2 实现 `CompanyTickerIdentityCorruptionError` 时必须同时确认该工具边界 catch 不会被 typed corruption 覆盖为 invalid_argument，否则需在 S2 中同步收窄。
- `CompanyMeta.from_dict` 的 raw equality 问题（第一轮 review Open Question 1）维持 controller 裁决：S1 不新增 finding，S2 实现 descriptor/meta identity mismatch 时基于最终 accepted plan 决定。

## Residual Risk

- S1 临时 snapshot 窗口、`resolve_existing_ticker` 半契约、late multi-owner `ValueError`、read route/schema/projection 未切换 —— accepted plan 明确允许的 S2 强制后续，S1 不可部署、不可 close。
- 6 个回归基线失败已由 controller 隔离复现并 triage 到后续 work unit；本 review 追加验证其中两项与本 diff 无逻辑/数据同源。
- 新 grammar 的多字符点分节负向对照（`AA.SS` / `V.N` / `SHEL`/`SHOP`）仍无 owner test —— 第一轮 review 记录的既有行为边界，controller 裁决为非 S1 blocker，可后续补测。

## Review Conclusion

S1 fix gate 的 F1/F2 修复与 F3/F4 裁决遵守均得到直接代码与验证证据支持：6-K 单文件 branch coverage 91%（独立复跑，测试为有效 owner/public-path 行为测试）；`UploadCompanyNameRequiredError` 是缺公司名唯一异常语义，runtime 只捕获该类型，identity mismatch 与 builder ValueError 不再误投影；read_runtime 一行机械迁移与 help 不改裁决均保持原状；无 S2 提前实现、无 UF-PF05/oracle/scenario/frozen evidence 混入、无新 finding。residue scans、逐文件 coverage gate、pyright 与回归基线全部复核通过。

**结论**: **PASS** —— 无 blockers。可创建 accepted S1 local commit 并立即进入 S2；S2 需在落地 typed corruption 投影时同步复核工具边界 `except ValueError` 的收窄（见 Open Questions）。
