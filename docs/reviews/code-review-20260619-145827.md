# Code Review — Aggregate Deepreview Re-Review

## Scope

- **Mode**: current changes (re-review gate)
- **Branch**: `wu-cm-12-conversation-memory-drift`
- **Base**: `main`
- **Output file**: `docs/reviews/code-review-20260619-145827.md`
- **Re-review of**: aggregate blocking finding fix for `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor`
- **Original blocking finding source**: `docs/reviews/code-review-20260619-144741.md` Finding 1
- **Adjudication**: `docs/reviews/wu-cm-compact-rejected-diagnostic-aggregate-deepreview-adjudication-20260619-144950.md`
- **Fix artifact**: `docs/reviews/wu-cm-compact-rejected-diagnostic-aggregate-fix-20260619-145736.md`
- **Included scope**: `tests/host/test_public_compact_smoke.py` — renamed test + updated assertions
- **Excluded scope**: 不扩大；不做 production behavior 改动；不修 compact root cause / fallback / tier / recovery tier
- **Parallel review coverage**: 无

## Original Blocking Finding

来自 `docs/reviews/code-review-20260619-144741.md` Finding 1：

> `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` 回归失败。测试断言 `fake_compactor.prompt_lengths == []`，实际为 `[19454]`。Compaction 行为从 "fallback" 变为 "compactor call"。严重程度：严重。

## Fix Summary

- **Change**: 重命名测试并重写断言，匹配当前正确语义。
- **Root cause**: 旧断言漂移 — 旧测试期望超长 current input 绕过 compactor 走 dispatch fallback；当前设计允许超长 current input 完整进入 compactor `current_input_anchor`，不恢复 Host 字段长度/schema guard。
- **Files changed**: `tests/host/test_public_compact_smoke.py` (1 file, +23/-6).

## Verification

### 1. 测试名称/docstring 是否符合新语义

**直接证据**: `tests/host/test_public_compact_smoke.py:554-562`

- **旧名称**: `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor`
- **新名称**: `test_proactive_compact_long_current_input_reaches_compactor_without_lossy_anchor`
- **旧 docstring**: `"超长 current input 不能无损进入 compact schema 时走 dispatch fallback。"`
- **新 docstring**: `"超长 current input 完整进入 compactor current_input_anchor。"`
- **旧 raises**: `"Run 失败或错误调用 compactor 时抛出。"`
- **新 raises**: `"Run 失败、未调用 compactor 或 current input 被截断。"`

**判断**: ✅ 通过。名称、docstring、raises 全部准确反映新语义："超长 current input 完整进入 compactor" 而非 "绕过 compactor 走 fallback"。

### 2. 断言是否证明 current_input_anchor.text 完整等于 long prompt

**直接证据**: `tests/host/test_public_compact_smoke.py:593-610`

关键断言逐条走读：

| 行号 | 断言 | 意图 | 判断 |
|---|---|---|---|
| 593 | `terminal.kind is HostEventKind.SUCCEEDED` | Run 成功 | ✅ |
| 594 | `len(fake_compactor.prompt_lengths) == 1` | compactor 被调用恰好一次 | ✅ |
| 595 | `len(fake_compactor.material_jsons) == 1` | 恰好一份 material JSON | ✅ |
| 601 | `current_anchor["anchor_label"] == "C1"` | anchor label 为 C1 | ✅ |
| 602 | `current_anchor["text"] == long_prompt` | text 完整等于 long prompt（`_DUPLICATE_PROMPT_SENTENCE * 500`） | ✅ |
| 603 | `material_json["previous_compacted_view"] is None` | 无 previous view | ✅ |
| 604-606 | `trace/evidence/answer_material == []` | 空 material | ✅ |
| 608 | `"truncated" not in material_text` | 无截断标记 | ✅ |
| 609 | `"preview" not in material_text` | 无预览标记 | ✅ |
| 610 | `"summary" not in material_text` | 无摘要标记 | ✅ |
| 611 | `len(_compact_artifact_files(...)) >= 1` | compact artifact 文件存在 | ✅ |

**细节验证**: 行 565 `long_prompt = _DUPLICATE_PROMPT_SENTENCE * 500` 被提取为局部变量，同时用于 `followup_request()` 调用（行 588）和 `current_anchor["text"]` 断言（行 602）。两者引用同一对象，确保断言比较的是进入 compactor 的原始文本，不会因间接引用引入偏差。

**判断**: ✅ 通过。所有断言准确证明完整 long prompt 进入 `current_input_anchor.text`，anchor_label 为 C1，无截断/预览/摘要标记，且有 compact artifact 文件。

### 3. compact artifact 存在

**直接证据**: 行 611 `assert len(_compact_artifact_files(tmp_path / "compact-artifacts")) >= 1`

`_compact_artifact_files()` 定义在 `tests/host/test_public_compact_smoke.py` 内部，在其他测试中被广泛使用（行 645, 728），返回符合 artifact root 下 compact artifact 文件的 tuple。断言 `>= 1` 而非 `== 1` 允许 recovery tier 产生额外 artifact（与前序 work unit 的 recovery tier 行为兼容）。

**判断**: ✅ 通过。

### 4. 没有 production 行为改动

**直接证据**: `git diff --stat HEAD` 输出:

```
 tests/host/test_public_compact_smoke.py | 29 +++++++++++++++++++++++------
 1 file changed, 23 insertions(+), 6 deletions(-)
```

**判断**: ✅ 通过。唯一变更文件是测试文件。未触及 `dayu/host/` 下任何 production 代码。未恢复 Host 字段长度 guard 或 schema guard。未修改 compact accept/reject/fallback/tier 行为。

### 5. 测试通过

```bash
source .venv/bin/activate && pytest tests/host/test_public_compact_smoke.py::test_proactive_compact_long_current_input_reaches_compactor_without_lossy_anchor -x -q
```
结果: `1 passed in 0.32s`

**判断**: ✅ 通过。

### 6. 旧测试名称已消除

`grep` 确认旧测试函数名 `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` 在当前文件中不再存在，已被 rename 为新名称。

**判断**: ✅ 通过。不存在新旧测试共存或幽灵旧断言。

## Open Questions

- 无。

## Residual Risk

- **无新增风险**: 本次变更仅为 smoke 测试断言对齐当前 production 行为，不引入新路径、新状态或新 I/O。
- **既有 deferred 项**: Recovery tier audit diagnostics、production compact root cause、orphan artifact cleanup 等 deferred 项不受本次 fix 影响。

## Original Blocking Finding Status

**已修复。**

原 finding（`code-review-20260619-144741.md` Finding 1, severity 严重）的根因是测试断言漂移：旧测试期望 production 走 fallback 路径，但当前 production 设计已将超长 current input 完整送入 compactor `current_input_anchor`。修复后用新测试名称和新断言准确反映当前 production 行为，compactor 被正确调用、current input 完整、无截断标记、compact artifact 存在。

## Aggregate Gate Status

**可通过。** 原始 aggregate review（`code-review-20260619-144741.md` / `code-review-20260619-144524.md`）的唯一 blocking finding 已修复。所有 deferred 项归属明确，与当前 slice 无耦合阻塞。
