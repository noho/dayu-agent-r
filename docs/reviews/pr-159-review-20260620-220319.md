# Code Review — PR #159 WU-ENG-02-R1 Provider Debugging Correlation Default

## Scope

- Mode: PR review
- PR: #159 https://github.com/noho/dayu-agent-r/pull/159
- Title: WU-ENG-02-R1 provider debugging correlation default
- Head: phase/wu-eng-02-r1
- Base: main
- Output file: docs/reviews/pr-159-review-20260620-220319.md
- Included scope: Full PR diff relative to main — production code, tests, README updates, plan artifact, control doc updates, review artifacts
- Excluded scope: 无
- Parallel review coverage: 无，单人审查

## Findings

### F-1-未修复-低-`runner.http.response` 日志行 `client_correlation_id=None` 时输出字面量 `None`

- **入口/函数**: `_do_attempt` / `_LOGGER.debug("runner.http.response ...")`
- **文件(行号)**: `dayu/engine/runners/openai/runner.py:617-623`
- **输入场景**: `request_identity` 为 `None` 时（例如非 Service assembly 路径或测试中未提供完整 request identity），`client_correlation_id` 传入 `None`
- **实际分支**: debug 日志行输出 `client_correlation_id=None`
- **预期行为**: 与同日志行中 `provider_request_id=None` 语义一致，均为"该次调用无此诊断 id"
- **实际行为**: 输出 `client_correlation_id=None`
- **直接证据**: runner.py:387-391（`request_identity` 为 `None` 时 `client_correlation_id` 为 `None`），runner.py:622（`%s` 格式化 `None` 输出 `None`）
- **影响**: 仅影响日志可读性，不影响状态、持久化或 LLM-facing material。与已有 `provider_request_id=None` 行为一致
- **建议改法和验证点**: 控制文档已裁决 `rejected-with-reason`，当前行为可接受；如需改善可读性，在后续 WU 中用格式化 helper 统一处理 `None` -> `-` 或省略
- **修复风险（低）**:
- **严重程度（低）**:

## Open Questions

无。

## Residual Risk

### RR-1: lost/cancelled terminal 不追加 diagnostic suffix

`_lost_host_event` (read_api.py:1033-1060) 和 `_cancelled_host_event` (read_api.py:1003-1030) 当前不调用 `_append_terminal_diagnostic_suffix`。

- **当前原因**: accepted plan scope 是 failed terminal；direct evidence 显示 `_lost_lifecycle_plan` 当前写入 `provider_request_id=None` 与 `client_correlation_id=None`，不存在可展示的诊断 id
- **风险**: 若后续 WU 让 lost/cancelled lifecycle payload 携带 provider/client correlation ids，终端展示不会自动同步
- **Owner**: 后续修改这些 lifecycle payload 的 WU plan reviewer 需复核 terminal projection 是否需要同步追加 suffix
- **严重程度**: 非阻塞；当前无实际缺失数据

### RR-2: GitHub 无 CI checks 报告

`gh pr checks 159` 报告 "no checks reported on the 'phase/wu-eng-02-r1' branch"。PR body 和 control doc 记录的本地验证（53 + 38 + 51 + 69 tests passed，pyright 0 errors）是当前唯一验证证据。

- **Owner**: pre-merge gate / repository branch protection
- **严重程度**: 非阻塞；本地验证充分

## Review Conclusion

**PASS — 无阻断性 findings。**

本 PR 忠实执行了 accepted plan `docs/host/host-issues/wu-eng-02-r1-provider-debugging-correlation-plan.md` 的全部五个 slice：

1. **Slice 1 (Service 默认启用)**: `_runner_spec_from_model` 中 `client_correlation_policy` 从 `DISABLED` 改为 `OPENAI_X_CLIENT_REQUEST_ID`；测试覆盖 ordinary baseline、compactor baseline 和静态 header 冲突 fail fast
2. **Slice 2 (Runner 诊断日志)**: `client_correlation_id` 从 `request_identity` 传入 `_do_attempt`，在既有 `runner.http.response` debug 日志行追加 `client_correlation_id=%s`；`_extract_provider_request_id` 保持 `x-request-id` 单一路径，测试覆盖基础设施 header 不被映射
3. **Slice 3 (Tool Trace fallback)**: `_extract_diagnostic_trace` 的 skip 条件增加 `client_correlation_id is None`，使仅含 client fallback 的 diagnostic 仍保留；测试覆盖 hot row / cold JSONL 和 `diagnostic_ref=raw_payload_ref` 保留
4. **Slice 4 (Terminal 诊断可见性)**: 新增 `dayu/host/_terminal_diagnostics.py` 私有 helper，在 `read_api.py`（live watcher）和 `outbox.py`（outbox fallback）的 failed terminal 投影边界追加有界诊断 id 后缀；测试覆盖 live/outbox 一致性、id 存在/缺失/截断、`message=None`/`""` 边界
5. **Slice 5 (README/docs 同步)**: `dayu/engine/README.md`、`dayu/host/README.md`、`dayu/service/README.md`、`tests/README.md` 同步更新

关键架构约束全部满足：
- 分层边界：Service assembly 负责 typed policy 映射，Host projection 只展示已存在 payload 字段，不改写 durable truth
- provider request id 提取保持 `x-request-id` 单一路径，不做 speculative header mapping
- 不新增配置项、provider 字符串分支、safety_identifier 或用户治理字段
- 不新增日志点、日志行或日志等级
- durable terminal payload `message` 和 payload digest 不被修改（有测试证实）

PR body 和 control doc 对 validation 和 residual risks 的陈述与代码证据一致。

### Findings count: 1
### Blocking findings count: 0
### Residual risks: 2 (均为非阻塞)
