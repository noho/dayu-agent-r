# Code Review

## Scope

- Mode: current changes
- Branch: wu-cm-12-conversation-memory-drift
- Base: HEAD (reviewing uncommitted untracked file only)
- Output file: docs/reviews/code-review-20260618-192722.md
- Included scope: `docs/reviews/wu-cm-12-fix-r1-s3-validation-codex-20260618.md`
- Excluded scope: 无（仅一个未跟踪文件）
- Parallel review coverage: 无
- Reviewer: AgentDS (focused validation artifact review)

## Review Context

本 review 只审查 Slice 3 validation artifact 的文档准确性，不重新审查 Slice 1/Slice 2 production/test 变更。

对照真源：
- `docs/host/host-issues/wu-cm-12-fix-r1-material-guard-plan.md` Section 6 (Slice 3 目标、允许文件、具体改动、验证命令)
- `docs/host/issues-implementation-control.md` WU-CM-12-FIX-R1 行和第 1651-1678 行的 WU-CM-13 边界声明

## Findings

### 01-未修复-低-Slice 3 跳过 Plan 明确要求的 combined focused regression 测试

- **入口/函数**: Slice 3 的 implementation 决策点
- **文件(行号)**: `docs/reviews/wu-cm-12-fix-r1-s3-validation-codex-20260618.md` Section 1（行 13）、Section 2.1（行 39）、Section 2.2（行 59）
- **输入场景**: Plan Section 6 明确要求 "加一个 focused regression，覆盖 `CompactionRequest -> LLMContextCompactor.prepare_compactor_proposal_run_input(...)`，输入含长 current input、长 evidence response、超过 8 条 accepted evidence 候选时不因私有 guard 抛错。"
- **实际分支**: Validation report 走的是 "本轮不修改 production，不修改 tests" 分支（行 13），理由是两个子系统（compaction DTO validation 在 `compaction.py` + evidence material provider 在 `run_input.py`）独立，已有单独测试覆盖各自的回归入口，combined test 不会覆盖新的代码路径。
- **预期行为**: Plan 要求新增一个 combined focused regression 测试。
- **实际行为**: 未新增。Validation report 声明 "不需要新增 duplicate focused regression"（行 39/59）。
- **直接证据**: Plan 第 193 行 "加一个 focused regression" vs. validation report 第 13 行 "本轮不修改 production，不修改 tests"。Report 在 Section 2.1 提供了现有测试覆盖的证据，在 Section 2.2 提供了第二条入口的独立覆盖证据，但没有提供 combined path 无新增代码路径的直接证据。
- **影响**: 低。两个子系统（compaction DTO validation 和 evidence material provider）在代码层面确实由不同模块独立处理，combined 测试不会触发新的条件分支。但 plan 要求本测试作为 Slice 3 的交付物之一，跳过意味着 Slice 3 交付物与 plan 的 "具体改动" 列表不完全匹配。这不影响 behavior correctness，但影响 plan-implementation traceability。
- **建议改法和验证点**: 两种可选方向：(a) 在 validation report 中补充更明确的 combined-path 代码路径分析，解释为什么两个独立子系统在同一个 compact flow 中不会产生新的分支或交互；(b) 按 plan 要求新增 combined regression 测试。方向 (a) 改文档即可；(b) 需改测试代码。
- **修复风险（低）**: 方向 (a) 无风险；方向 (b) 低风险，新增测试不会破坏现有行为。
- **严重程度（低）**: Plan deviation，但被 validation report 显式记录并给出了第一性原理判断，不影响 correctness。

## 逐项核对结论

以下逐项核对 validation report 是否准确记录 plan 要求的 Slice 3 内容：

| Plan 要求 | Report 记录 | 准确性 |
|---|---|---|
| 合并验证 Slice 1 + Slice 2 回归入口 | Section 2.1 + 2.2 逐条列出已有测试覆盖及关键断言，结论为无需新增 | 准确：覆盖描述与 Slice 1/Slice 2 accepted commits 的测试文件一致 |
| Combined pytest 验证 | Section 3.1：`240 passed in 1.03s`，测试文件集合为 plan 要求的超集（多了 `test_compaction_operation.py`） | 准确：命令为 plan 指定命令的超集 |
| Pyright 全量检查 | Section 3.2：`0 errors, 0 warnings, 0 informations` | 准确 |
| Old symbol rg audit | Section 3.3：rg 搜索词覆盖 plan 要求的 4 个常量 + Slice 2 额外 3 个符号，结果 exit code 1（无匹配） | 准确：搜索范围超过 plan 要求，结果符合预期 |
| git diff whitespace | Section 3.4：`git diff --check` PASS，新增文件单独检查 PASS | 准确 |
| Preliminary constant audit | Section 4：4.1 已删除符号（7 个），4.2 MAX_VNEXT 保留为 parser safety guard（12 个常量），4.3 memory_projection_policy 管理的 production caps，4.4 未发现的剩余私有常量 | 准确：分类与 plan Section 5 和 design doc Section 25 一致 |
| README/docs decision | Section 5：正确判定不触发 README 更新（未修改 `dayu/host/`、`tests/`、用户可见入口/路径/分层关系），未修改 control doc | 准确：符合 CLAUDE.md README 更新触发规则 |
| 不声称 final closeout 完成 | Section 1 "未进入 review gate，未修改 control doc，未 stage/commit/push/PR"；Section 6 "本报告是 preliminary constant audit prep，不替代 final closeout" | 准确：多处显式声明 |
| 不修改 control doc | Section 5 "未修改 `docs/host/issues-implementation-control.md`" | 准确 |

## 跨文档一致性

- Validation report 的 4.3 节描述的 `MemoryProjectionPolicy` 与 `execution_profiles.json` 的关系，与 plan Section 5 第 5 条 "不扩大架构：不新增 policy schema 字段" 一致。
- Report 的 4.2 节对 `MAX_VNEXT_*` 常量的 "保留为 parser safety guard" 分类，与 plan Section 5 决策 1 第 3 条 "ConversationCompactOutputVNext 及其 LLM-generated candidate 的 MAX_VNEXT_* parser safety guards 保留" 一致。
- Report Section 6 的 residual risk 关于 `default_memory_projection_policy(...)` fallback defaults 与 `execution_profiles.json` 数值不同，与 plan Section 8 的 deferred performance residual 不冲突——这是不同的风险类别（code fallback vs. packaged config），report 正确将其标记为 final closeout 需处理的独立事项。
- WU-CM-13 边界：report 没有提及 WU-CM-13，没有错误地将其声称的 residual 委派给 WU-CM-13。这与 control doc 第 1651 行 `WU-CM-13 不得阻塞 WU-CM-12-FIX-R1` 和第 1669 行 `不在当前 WU-CM-12-FIX-R1 中实现 reactive compact recovery` 一致。

## Open Questions

- 无。

## Residual Risk

- 本次 review 未重新运行 validation report 中声明的 pytest/pyright/rg 命令。若需独立复现，应执行 report Section 3 中的命令并核对结果。
- Finding 01 的 plan deviation 是否需要在 final closeout 前补齐 combined regression 测试，由 controller 或用户裁决。
