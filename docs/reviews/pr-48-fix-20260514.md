# PR #48 Review Fix 2026-05-14

## Accepted Findings Fixed

- A1：调整 `RuntimeFileLockToken.release()` 状态顺序与 marker 恢复语义。第三方 `release()` 成功后立即设置 `released=True`；后续 marker 恢复失败按 best-effort 压制，不向调用方抛出 release failure，也不破坏重复 `release()` 幂等性。
- A3：补充 `RunSnapshot` source id / relation 不一致的失败路径测试：
  - `source_run_id=None` 且 `source_run_relation=RETRY` 抛 `ValueError`。
  - `source_run_id` 存在且 `source_run_relation=None` 抛 `ValueError`。
- A4：补充 `FollowupSnapshot` behavior 与 target / queued id 不一致的失败路径测试：
  - steer 缺 `target_run_id`。
  - steer 携带 `queued_run_id`。
  - queue 携带 `target_run_id`。
  - queue 缺 `queued_run_id`。
- A5：更新 `dayu/README.md`，把已实现的 `filelock` 描述移入 `dayu.runtime` 当前已有能力段，并保持 `ToolsDiscovery` / `ScenePrepare` 在 deferred / design-boundary 段。
- A6：补充 lane failure-path / 幂等测试：
  - `LaneOwner` 空 `owner_id`。
  - `LaneOwner` 非法 `pid`。
  - `LaneController.acquire(..., timeout_seconds=-1)` 抛 `RuntimeLaneConfigError`。
  - `LaneController.close()` 连续调用两次不抛错。

## Changed Files

- `dayu/runtime/filelock.py`
- `dayu/README.md`
- `tests/runtime/test_filelock.py`
- `tests/runtime/test_lane.py`
- `tests/host/test_public_contracts.py`
- `docs/reviews/pr-48-fix-20260514.md`

## Validation Results

- `source .venv/bin/activate && pytest tests/runtime/test_filelock.py tests/runtime/test_lane.py tests/host/test_public_contracts.py -q`
  - 41 passed
- `source .venv/bin/activate && pytest tests/host tests/runtime -q`
  - 112 passed
- `source .venv/bin/activate && python -m pyright dayu/ tests/ utils/`
  - 0 errors, 0 warnings, 0 informations
- `git diff --check`
  - passed

## Stop Condition Status

- 未触发 stop condition。
- 未改变 filelock public API。
- 未引入 logging / diagnostic policy。
- 未改变 `__exit__` exception priority。
- 未修改 `dayu/host/api.py` 或 `dayu/runtime/lane.py` 生产逻辑。
- 未触碰禁止文件或禁止范围。

## Unfixed Items

- 只修复 controller accepted findings A1 / A3 / A4 / A5 / A6。
- Controller rejected items 未修改。
