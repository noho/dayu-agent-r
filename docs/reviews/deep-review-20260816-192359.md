# Deep Review：UF-FIX08 existing-source-auto-repair aggregate

## Scope

- Mode: current changes（aggregate deepreview，Gateflow deepreview gate）
- Branch: `codex/upload-filing-oracle`
- Base: `5859856e46af42c9ae5a2a5c07fab1ba59dc91d3`（UF-FIX07 closeout，非 main，按用户要求）
- HEAD: `65d352e6`（UF-FIX08 Slice 6 accepted）
- Output file: `docs/reviews/deep-review-20260816-192359.md`
- Included scope: base..HEAD 全部 83 个 changed files（20 个生产文件、15 个测试文件、3 份 README、14 份 gateflow artifacts、32 份 review artifacts）
- Excluded scope: 按用户要求未执行 coverage、真实 CLI/evidence（UF-PF08/UF-PF12）；未读本轮 AgentMiMo 新 artifact
- 主 reviewer 亲自验证：全仓 pyright（0 errors / 0 warnings / 0 informations）、`tests/fins` 全量（1851 passed, 1 skipped, 3 warnings；skip 为环境条件 skip，warnings 来自第三方 edgar deprecated imports）
- 输入读取：AGENTS.md、docs/host/design.md（关键边界章节）、docs/engine/design.md、UF-FIX08 goal confirmation / accepted plan / plan adjudication / Slice 1 adjudication / Slice 3-6 code-review-fix artifacts 全部完整读取；Slice 1-6 implementation artifacts 已读
- Parallel review coverage:
  - 子代理 A：`source_integrity.py` + `_fs_source_integrity.py`（四态封闭性、revision=None 不等价、inspector 覆盖、precedence、canonical items、guard precondition）
  - 子代理 B：`_fs_source_document_core.py` / `_fs_storage_infra.py` / `_fs_source_snapshot.py` / `_fs_filing_upload_state_core.py` / `fs_source_document_repository.py` / `repository_protocols.py` / `fs_batching_repository.py`（Phase B 顺序、commit validator、snapshot、state read、batch 原子性）
  - 子代理 C：`ingestion_runtime.py` / `upload_repair_contract.py` / `upload_failure.py` / `_filing_upload_fresh_validation.py`（repair eligibility、validator precedence、failure contract、传播边界）
  - 子代理 D：`docling_upload_service.py` / SEC/CN upload / SEC/CN download workflows（wiring、失败映射、UNSAFE gate、CN generic residual）
  - 子代理 E：13 个测试文件（五类行为真实命中矩阵、断言强度、fixture 契约）
  - 子代理 F：README/scope/frozen/evidence 禁令、编码硬约束、兼容性残留、未授权文件清单
  - 主 reviewer 复核了全部中危及以上 finding 的证据链（行号、冻结语义、代码顺序），并裁决了子代理之间的冲突结论

## 结论

**PASS（无 blocking findings）**。UF-FIX08 的 10 条成功信号在代码与测试层面成立：四态/reasons/invariants 封闭、MISSING/UNSAFE revision=None 不等价、published/staged/commit/snapshot 同源 inspector、repair eligibility 唯一 producer 且 fail-closed、Phase A/fresh/Phase B recheck 抗 stale、same-batch old-or-new 原子提交、identical fingerprint 不 skip、manifest missing selected/multi/nonselected 分流、non-target/cross-source 阻断、runtime/durable failure typed JSON 无 path/raw exception。CN top-level generic terminal projection residual 经证据链复核**不阻塞**（详见下方专项裁决）。2 个中危 finding 为 inspector 边缘场景的分类封闭性缺口，建议在后续 work unit 或本 gate 后续修复，但不构成 ship 阻断。

## Findings

### 1-未修复-中-exact-target 分类对「canonical 目录被异源 descriptor 占据」返回 MISSING，后续 auto-create 可覆盖错位 source 数据

- **入口/函数**: `_inspect_source_kind_unguarded` 根目录枚举 → `_select_exact_target`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:264-297、1158-1198`
- **输入场景**: requested=B；B 的 canonical 目录（`_identity_directory_path` 派生）物理存在，其中 identity descriptor 可读但声称 document_id=A。触发条件为外部 writer 手动移动/篡改目录（plan residual 表中「manual filesystem writer」场景的变体），非正常 workflow 可达。
- **实际分支**: 枚举循环读取 descriptor 得 A，直接把该目录当 source A 检查（274-297 行对 descriptor 可读的正常分支**不比对 child 与 requested_directory 或 A 的 canonical 路径**）；`_select_exact_target` 在 inventory 找不到 B，manifest trusted 且不含 B → 返回 MISSING（1187-1192 行）；327-330 行的 unassignable→unsafe 收口因 target 为 MISSING 不生效；`repair_blocked_reason=None`。
- **预期行为**: plan §5.2 矩阵 step 2：identity 无法建立可信事实必须 UNSAFE；goal 的 UNSAFE 定义包含「存在未声明额外业务文件」「结构状态不封闭」。B 的 canonical 路径被异源身份目录占据属于结构不封闭，不应投影为「目标完全不存在」。
- **实际行为**: B=MISSING + revision=None + 无 blocked reason。validator 按 MISSING+auto → create 放行，`_store_upload_assets` 的 create/reset 路径作用在 B 的 canonical 目录上，错位存放的 source A 数据被静默清除或引发 commit 期失败。
- **直接证据**: 253-261 行 requested_directory 仅用于 277-285 行 descriptor 读取失败分支；274-297 行成功分支无位置↔身份交叉校验（`_inspect_source_directory` 822-829 行只核对目录内 declared_names，不核对目录名）；1184-1192 行 target 选择不感知 requested_directory 物理存在性。
- **影响**: 分类封闭性缺口 + 潜在数据丢失（错位 source 被当作不存在目标覆盖）；违反「MISSING 只表示 exact target 完全不存在」的语义承诺。
- **建议改法和验证点**: 枚举循环中当 `child == requested_directory`（或按 descriptor identity 反推的 canonical 路径与 child 不一致）时，将该目录闭合为 `IDENTITY_UNTRUSTED`/`CROSS_SOURCE_INCONSISTENCY` 而非按 descriptor 身份入 inventory；至少使 exact-target 不再返回 MISSING。补单测：requested canonical 路径放含其它 document_id 的 descriptor，断言 `target.status is UNSAFE`（或 preflight error），禁止 MISSING+blocked=None。
- **修复风险**: 中。触及 identity/location 语义 owner，需与 `_fs_identity` 对齐并确认正常双 source 目录布局不受影响（whole-kind mode 同样按目录枚举，修复应共享同一判定）。
- **严重程度**: 中

### 2-未修复-中-`classify_source_integrity_preflight` 对「空 inventory + manifest 悬垂声明」返回 clean，与同一 payload 的 kind-level unsafe 事实不一致

- **入口/函数**: `classify_source_integrity_preflight`
- **文件(行号)**: `dayu/fins/storage/source_integrity.py:290-325`；`_fs_source_integrity.py:1122-1127`
- **输入场景**: whole-kind 扫描：source root 无任何实际 source 目录，但 trusted manifest 仍声明 source X（全部 source 被外部删除、manifest 遗留）。
- **实际分支**: `_apply_manifest_facts` 走 `manifest_ids - source_ids` 非空分支 → inventory=() + shared=(SOURCE_MANIFEST_UNTRUSTED,)；preflight 对空 inventory：无 UNSAFE item（310 行不触发）、无 repair target → 返回 `NoSourceRepairRequired`（315-316 行）。
- **预期行为**: manifest 悬垂是 kind-level unsafe 事实，whole-tree preflight（plan §7.2「whole-tree preflight 同样拒绝 unsafe」）应 typed fail closed；同一 payload 的 `repair_blocked_reason` 已为 CROSS_SOURCE_PUBLICATION_UNSAFE，public disposition 不应与其矛盾。
- **实际行为**: public disposition=clean。download whole-tree 在 company/maintenance/rejection mutation 前放行；仅当 provider 本轮恰好返回被声明 filing 时，per-filing Phase A 才经 `_select_exact_target` 1193-1198 行闭合为 UNSAFE 抛 typed preflight error（CN 折叠为 generic terminal、SEC 终止 stream）。
- **直接证据**: source_integrity.py:310-325 只消费逐 item status；函数签名不接收 shared manifest facts；_fs_source_integrity.py:1122-1127。
- **影响**: whole-tree gate 语义不闭合：空 workspace + 悬垂 manifest 可被判定 clean 并继续 company publication；分类粒度不一致（同一事实在 preflight 与 per-filing 两个入口不同结论）。
- **建议改法和验证点**: preflight 契约显式声明「inventory 必须来自同一 whole-kind inspection；kind-level facts 由调用方另行消费并 fail closed」并修正两个 download 调用点，或给 preflight 增加 shared facts 入参并拒绝含 kind-level unsafe 的 inventory。补单测：空 inventory + manifest 悬垂 → typed preflight error 或调用方零 mutation。
- **修复风险**: 中。修改 public 契约影响 download 两个消费点与既有测试；需与 Slice 6 已冻结的 download 行为协调。
- **严重程度**: 中

### 3-未修复-低-UNSAFE 转换后私有 inspection.revision 保留非 None，docstring 未区分公开/私有语义

- **入口/函数**: `_with_unsafe_classification`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:91、1351-1382`
- **输入场景**: 已信任 inspection 叠加 unsafe reason（CROSS_SOURCE_INCONSISTENCY 等）。
- **实际行为**: public classification.revision=None（四态不变量正确），但 `dataclasses.replace` 保留 `inspection.revision` 非 None；91 行 docstring「revision: 与 classification 同版的可信 opaque revision」与实际语义矛盾。
- **直接证据**: 1374-1382 行；1362 行注释确认有意保留私有 content fact。
- **影响**: 下游误把私有 revision 当公开事实的风险；当前消费点均消费 classification.revision，无实际破坏。
- **建议改法和验证点**: docstring 改为「content 扫描对应的可信 opaque revision；public classification 为 UNSAFE 时仅作私有 content fact，不得公开投影」。
- **修复风险**: 无（纯文档）。
- **严重程度**: 低

### 4-未修复-低-`_canonical_manifest_items` 非空与 `repair_blocked_reason` 非空可并存，payload 无 gating 声明

- **入口/函数**: `_canonical_manifest_items` / `_derive_repair_blocked_reason`（whole 分支）
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:319、1201-1224、1294-1307`
- **输入场景**: whole mode 单一 source 的 manifest 投影 mismatch 且 content 全 COMPLETE。
- **实际行为**: canonical items 返回非空（只看 content complete），同时 blocked=CANONICAL_MANIFEST_UNAVAILABLE（额外要求 reasons==shared 等更严格条件）——「canonical 已可得」与「canonical unavailable」并存。
- **直接证据**: 1201-1224 vs 1294-1307。
- **影响**: `_SourceKindPublicationInspection` 未声明「blocked 非 None 时 canonical items 无效」的 gating 规则，下游误用风险；当前消费点（core 468-475 行先判 blocked 再消费）顺序正确。
- **建议改法和验证点**: dataclass docstring 明确 gating 规则，或统一消费契约「先看 blocked reason」。
- **修复风险**: 低。
- **严重程度**: 低

### 5-未修复-低-`_ordered_reasons` 静默丢弃非 enum 成员

- **入口/函数**: `_ordered_reasons`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:1490-1506`
- **输入场景**: 传入混入裸字符串等非成员的可迭代对象。
- **实际行为**: `set` 后按 enum 迭代过滤，非成员被静默丢弃（1505-1506 行）。
- **直接证据**: 1505-1506 行。当前全部调用点类型安全，属防御性缺口。
- **影响**: helper 契约是「规范化」而非「校验」，未来误用会静默欠报 reasons。
- **建议改法和验证点**: `selected - set(SourceIntegrityReason)` 非空时抛 ValueError，补负面单测。
- **修复风险**: 低。
- **严重程度**: 低

### 6-未修复-低-runtime 启动边界 Raises 未覆盖 RuntimeFileLockError，与同库其它边界不一致

- **入口/函数**: `FinsIngestionRuntime._validate_runtime_upload_request`（upload/start_upload/prepare_observed_upload）
- **文件(行号)**: `dayu/fins/ingestion_runtime.py:4678、3617、4631、3813`
- **输入场景**: raw filing request 启动边界读 published state 时 publication guard 获取失败。
- **实际分支**: `RuntimeFileLockError` 继承 `Exception`（dayu/runtime/filelock.py:24），不受 Raises 的 OSError 覆盖，裸抛出；CLI 落入 generic except → 通用未知失败文案。行为上仍无 job 落盘污染（job 创建前抛出）。
- **直接证据**: filelock.py:24；对照 service_runtime.py:87-90 与 `_filing_upload_fresh_validation.py:64-66` 均已把 RuntimeFileLockError 投影为 typed io failure。
- **影响**: 同一 storage 读取失败在三个入口产生三种表面（typed io failure / 裸 RuntimeFileLockError / OSError），用户可见文案不一致，契约文档不准确。
- **建议改法和验证点**: 三处 Raises 补 `RuntimeFileLockError`，或按设计在入口包装为 `FinsUploadPrevalidationError(fins_upload_prevalidation_io_failure())`；补锁失败单测。
- **修复风险**: 低。
- **严重程度**: 低

### 7-未修复-低-手构造 ValidatedFinsUploadFilingRequest 可绕过 validator 构造 MISSING+update 矛盾组合

- **入口/函数**: `ValidatedFinsUploadFilingRequest.__post_init__` + `_validate_runtime_upload_request`
- **文件(行号)**: `dayu/fins/ingestion_runtime.py:809-812、4682-4683`
- **输入场景**: 调用方手工构造 validated request（MISSING + resolved_action=update + NoExistingSourceRepair）直传 start_upload/upload。
- **实际分支**: post_init 的 NoExistingSourceRepair 分支仅禁 REPAIR_REQUIRED 即 return；runtime 对 validated 实例原样放行并落 job record。
- **预期行为**: repair 分支（815-822 行）对 status↔action 一致性做了严格校验，非 repair 分支应有对称的不变量（MISSING→create、COMPLETE→{update,delete}）。
- **直接证据**: 809-812 vs 815-822；4682-4683。当前生产入口（service prevalidate + fresh validation）均不会产生此类对象，属信任边界缺口。
- **影响**: 绕过 validator 的调用方可构造语义矛盾 request 进入 runner；非现存 bug。
- **建议改法和验证点**: NoExistingSourceRepair 分支补 status↔resolved_action 校验（需先确认无合法路径构造 MISSING+update，如 delete 后重传场景）；补 post_init 单测。
- **修复风险**: 低。
- **严重程度**: 低

### 8-未修复-低-SEC download per-filing 无异常边界，单 filing UNSAFE 终止整个 stream，与 CN 不对称

- **入口/函数**: `sec_download_workflow.py` per-filing 循环
- **文件(行号)**: `dayu/fins/pipelines/sec_download_workflow.py:649-669`
- **输入场景**: SEC 单 filing Phase A/B 抛 `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)` 或 churn 耗尽抛 `SourceIntegrityRevisionConflictError`。
- **实际分支**: 无 try/except，异常穿透终止整个 download stream，后续 accepted filings 全部丢弃。
- **直接证据**: 649 行无 try；对照 cn_download_workflow.py:302-326 有 per-filing catch + FILING_FAILED 事件。
- **预期行为**: UF-FIX08 对 download 面只要求 typed fail closed（已满足：异常消息为封闭 path-free 文案），SEC/CN 收敛对称性属「一般 download typed terminal projection」后续 work unit 范围。
- **影响**: 单 UNSAFE 目标使 SEC ticker 级下载整体中断；行为与 CN 不一致；非 UF-FIX08 引入（沿既有传播路径）。
- **建议改法和验证点**: 与 CN generic projection 合并归入后续 download failure projection work unit，统一 SEC/CN per-filing typed 收敛。
- **修复风险**: 中（涉及 SEC download top-level 控制流与结果收敛语义，不宜在本 work unit 内改）。
- **严重程度**: 低（范围外既有结构）

### 低/Info 汇总（子代理证据链已复核，按 severity 降序，详细证据见子代理报告）

| # | Finding | 位置 | 影响与建议 |
| --- | --- | --- | --- |
| L1 | inspector 结构校验与 physical facts 两次 lstat/hash 之间存在 TOCTOU（size 与 hash 来自不同内容） | `_fs_source_integrity.py:799-832、951-989` | caller-held guard 是唯一防线且 plan 已承诺；不修，记录 |
| L2 | inspector catch 闭集依赖跨模块 owner 异常面（IndexError 等穿透为未分类 crash） | `_fs_source_integrity.py:222、276、471、494、552-563、1049` | 建议在 owner 模块 docstring 固化异常闭集 |
| L3 | `SourceIntegrityClassification` 不校验 ticker/document_id 值域 | `source_integrity.py:93-137` | 生产构造方已校验；docstring 或值校验二选一 |
| L4 | `classify_source_integrity`/`classify_staged_source_integrity`/`list_source_integrity` docstring 仍承诺旧 ValueError 语义，与实际 UNSAFE 收敛矛盾 | `_fs_source_document_core.py:603-610、651-656、688-693` | 纯文档修复 |
| L5 | `read_filing_upload_state` 早期返回分支不校验 document_id（ticker 目录缺失时） | `_fs_filing_upload_state_core.py:93-105` | 协议承诺与实现分支级不一致；上游 identity 校验使不可达；入口统一校验即可 |
| L6 | snapshot source_kind=None 从存在性探测变为全量双 inspection，filing 侧 operational 失败会使 material 读取整体失败 | `_fs_source_snapshot.py:878-896` | fail-closed 方向正确；建议 docstring 补说明，不修 |
| L7 | `_rewrite_source_manifest_for_repair` 只写 ticker/updated_at/documents 三键，未来扩展字段会被静默丢弃 | `_fs_source_document_core.py:1425-1458` | 当前无契约字段可丢；注释声明或改走 upsert owner |
| L8 | REPAIR_REQUIRED 分支两处空 selection 防御死代码（static 校验先抛） | `ingestion_runtime.py:1477-1478、821-822` | 删除或注释不变量依据；若未来可达会被误分类为 corruption |
| L9 | `resolve_fresh_filing_request` Raises 未覆盖 AssertionError（upstream `resolve_upload_action` 闭集外违约时裸抛） | `_filing_upload_fresh_validation.py:45-47` | fail loud 行为正确；docstring 补「其它异常不捕获原样抛出」 |
| L10 | `FinsUploadFormatError` 在 fresh validation 潜在被归类为 corruption（当前 static/post_init 双重校验相同路径使不可触发） | `ingestion_runtime.py:1007` + `_filing_upload_fresh_validation.py:67-69` | latent；注释说明或显式捕获投影 usage |
| L11 | inspector 277-279 行「exact target directory 缺少 requested document ID」分支不可达（253-261 行保证 id 非 None 才有 requested_directory） | `_fs_source_integrity.py:277-279` | 删除或标注不可达防御 |
| L12 | download Phase B UNSAFE 测试用 seam fake 注入 staged fact（staged tree 实为 COMPLETE copy），真实 staged UNSAFE 检测仅 storage 层覆盖 | `test_sec_pipeline_download.py:5482-5729`、`test_cn_download_workflow.py:1900-1980` | 分支与副作用断言为真（begin/rollback/reset/blob/commit 精确计数）；可选增强为真实 staged 注入 |
| L13 | clear preflight 测试用宽异常元组 `(SourceIntegrityPreflightError, ValueError, OSError)`，异常类型回归可能被掩盖 | `test_fins_storage_atomicity.py:1664、1760、1829` | 物理零部分删除断言仍精确；可选按 corruption 参数拆分 |
| L14 | CLI renderer 测试 fixture 用 `build_sec_filing_ids` 直建 identity，绕过 `_filing_upload_request_identity` 静态校验 | `tests/cli/test_fins_commands.py:3058-3081` | renderer 单测目标不受影响；service/ingestion fixture 已消费真源 |
| L15 | 无 repair 专用 barrier 测试（barrier 全部作用于 generic batch commit） | `test_fins_storage_atomicity.py:3443-3740` | repair 复用同一 begin/commit 机制，机制覆盖间接成立；可选增强 |

## 已裁决为「非缺陷/正确实现」的子代理质疑

- **Phase B 顺序偏差质疑（子代理 B F-01）：驳回**。主 reviewer 逐行核对 Slice 4 code-review-fix artifact 冻结顺序（filing exact inspect → presence gate → status/content gate → identity comparison → **material whole-kind inspect → filing non-target blocked → material blocked** → reset → rewrite）与 `_fs_source_document_core.py:427-487` 完全一致。子代理 B 将冻结顺序读反。残余观察：material inspection 的 operational OSError 会先于 filing blocked 表面化，两者均在 mutation 前 fail closed，无原子性风险。
- **单点 canonical item 空/重复应抛 CANONICAL_MANIFEST_UNAVAILABLE 的质疑（子代理 B F-05 / Open Q1）：裁决为 producer-invariant RuntimeError 正确**。producer（`_fs_source_integrity.py:1309-1316`）承诺 clean 时 non-target 单点必存在，consumer 侧见到空/重复只能是 producer 违约，与 Slice 4 fix 冻结的 producer-invariant 测试语义一致，不改。

## 专项裁决：CN top-level generic terminal projection residual 是否阻塞

**裁决：不阻塞（PASS），属已分类的后续 work unit，且不 invalidate UF-FIX08 goal。** 主 reviewer 亲自核实的证据链：

1. CN top-level per-filing `except Exception`（`cn_download_workflow.py:306-326`）→ `project_cn_filing_failure` fallback 返回固定 `("filing_execution_failed", "财报文档执行失败")`（`cn_download_filing_workflow.py:71-88`）。三个分支全部返回固定/owner 化安全文案，**不消费 str(exc)/args/path/traceback**，path-free bounded 契约成立，仅分类粒度粗。
2. lower single-filing owner 已 typed fail closed：Phase A/B UNSAFE 抛封闭 `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)`（cn_download_filing_workflow.py:178-179、583-584），churn 耗尽抛 typed conflict。
3. upload 面 goal（§3.1 成功信号 9）要求的 typed bounded failure 已完整贯穿：fresh prevalidation → 唯一 UPLOAD_FAILED typed event；service 层精确映射 SOURCE_REVISION_STALE / SOURCE_REPAIR_BLOCKED / SOURCE_INTEGRITY_UNSAFE，无 UNEXPECTED_RUNTIME 误落。
4. goal 对 download 面只承诺「复用同一 classification」与「UNSAFE typed fail closed」（§3.1 第 1、10 条 + §7.2），均已满足；从未承诺 per-filing durable 事件携带 typed integrity reason。
5. Slice 6 adjudication 已将该面记录为 `rejected-with-reason` + 「一般 download typed terminal projection contract/schema → assigned to later work unit」，并明确拒绝固化 generic fallback 测试；本 review 不做无授权的 schema 扩张。
6. 该 fallback 与 top-level except 在 base 版本已存在，本次 diff 未触碰，非 UF-FIX08 引入。

## Open Questions

1. SEC/CN download Phase B identity churn 三轮重试（`sec_download_filing_workflow.py:512-534`，`_MAX_SOURCE_IDENTITY_ROUNDS`）在全测试套件无真实注入覆盖（grep 无 identity_round 测试引用）。既有缺口还是本 work unit 范围外，需 controller 确认。
2. Service 层对 upload fresh 期 `FinsUploadUsageError` 与 identity-mismatch RuntimeError 的投影在本审查范围（五个 pipeline 文件）外未能验证；fresh TOCTOU usage 失败若 Service 无 typed 投影会绕过 closed failure 契约。
3. inspector 声明的 size/digest 为可选字段（`_fs_source_integrity.py:642-647`），二者都缺失时完整性退化为存在性检查；declared physical_path 的 containment 完全依赖 `_normalize_filename`（`_fs_storage_utils` 内）。是否属于 owner 契约预期，需对照 plan 确认。
4. `reset_source_document_for_repair` 的 material inspection 两个异常转换分支（`_fs_source_document_core.py:455-466`）无直接单测节点（helper 级有覆盖），轻微覆盖缺口。

## Residual Risk

| 风险 | 分类 | owner / destination |
| --- | --- | --- |
| 一般同请求并发 success/skip 收敛 | assigned | UF-FIX10 |
| fresh company meta warning | assigned | UF-FIX11 |
| material existing-source repair | assigned | 后续独立 work unit |
| 旧 schema corpus 读取/迁移 | assigned | 后续显式 migration work unit |
| UF-PF08/UF-PF12 真实 CLI evidence、registry/oracle 状态 | assigned | evidence work unit（本轮禁止执行，已确认无 diff） |
| 一般 download typed terminal projection（CN generic fallback、SEC stream 终止、typed reason 折叠） | assigned | 后续独立 download failure projection contract/schema work unit；本 gate 明确不固化 generic fallback 测试 |
| Finding 1/2 的 inspector 边缘场景无测试覆盖（location↔identity 错位目录、空 inventory + 悬垂 manifest） | 本 work unit 新增缺口 | 建议本 gate 后续修复或显式 defer |
| manual filesystem writer 绕过 repository lock | 既有 | storage operational policy |
| barrier 测试无 repair 专用变体 | 机制覆盖间接成立 | 可选增强，非阻断 |

## Validation 记录

- `python -m pyright dayu/ tests/ utils/`：0 errors, 0 warnings, 0 informations（主 reviewer 后台实际运行）
- `python -m pytest tests/fins -q`：1851 passed, 1 skipped, 3 warnings（主 reviewer 实际运行；skip 为环境条件 skip，warnings 为第三方 edgar deprecated imports）
- `git diff --check`：通过（子代理 F 验证）
- 未运行 coverage、真实 CLI/provider/converter evidence（用户明确禁止）
- 子代理 E 实测各 owner test 文件全部通过；五类行为（corruption/stale/unsafe/atomicity + identical fingerprint + manifest missing 分流 + non-target blocked + snapshot/process_filing 同源）均有真实命中证据，无断言被削弱、无 fake 固化偶然行为
- 子代理 F 确认：禁止文件（oracle/scenario/design/evidence/registry/Host/Engine/Service/CLI 生产）零 diff；README 三份均按职责合规更新；新增生产代码 0 处 Any/hasattr/getattr/兼容 re-export；83 个 changed files 全部归入 allowed lists 或已文档化 Controller amendment
