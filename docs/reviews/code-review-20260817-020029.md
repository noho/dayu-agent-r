# Code Review

## Scope

- Mode: current changes（UF-FIX10 S1 独立 code-review re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `d97d233b`
- Review date/time: 2026-08-17 02:00:29（本机时钟）
- Output file: `docs/reviews/code-review-20260817-020029.md`
- Included scope: 相对 base 的全部 workspace diff（19 个文件）：
  - 生产：`dayu/fins/storage/repository_protocols.py`、`_fs_company_meta_core.py`、`_fs_filing_upload_state_core.py`、`_fs_source_integrity.py`、`fs_filing_upload_state_repository.py`、`__init__.py`、`dayu/fins/pipelines/docling_upload_service.py`、`dayu/fins/pipelines/filing_upload_publication.py`（新增）、`dayu/fins/upload_failure.py`
  - 测试：`tests/fins/test_filing_upload_publication.py`（新增）、`test_docling_upload_service.py`、`test_fins_storage_atomicity.py`、`test_fins_ingestion_runtime.py`、`test_fins_ingestion_tools.py`、`test_upload_failure.py`、`test_fins_service_runtime.py`、`tests/cli/test_fins_commands.py`、`tests/service/test_fins_direct.py`
  - 文档：`docs/gateflow/uf-fix10-s1-implementation-20260816.md`、`docs/gateflow/uf-fix10-s1-code-review-adjudication-20260817.md`
- Excluded scope: 无生产越界文件；SEC/CN/HK workflow、workflow tests、README、oracle/scenario/registry/evidence 经 `git diff --name-only` 逐一核对零 diff
- 参考文档：`docs/reviews/code-review-20260817-012504.md`、`docs/reviews/code-review-20260817-013652.md`、`docs/gateflow/uf-fix10-s1-code-review-adjudication-20260817.md`、accepted plan（含两次 amendment）、两次 amendment acceptance artifact
- Parallel review coverage: 无 subagent；主 reviewer 逐行走读全部生产 diff、全部测试新增/修改，并独立复核裁决要求挑战的五项修复与 F1/S2 边界

## 结论摘要（供 Controller 裁决）

本 re-review 以 adjudication 的五项修复为挑战对象逐一核实，全部通过，无 blocker。真实 prepare→publish→durable identity 交叉测试两侧数据源独立（内存 prepared plan 投影 vs 磁盘 manifest/inspector 投影），经真实生产 publish/read 路径，不是同实现自证。§7.4 conflict grid 五格构造合法、断言准确。UNSAFE 入口拒绝与 stable REPAIR_REQUIRED invariant 测试均命中实际 owner 的 fail-closed 分支。strict text 校验覆盖完整、合法输入不误拒绝。空 staging MISSING batch read 确走 staging 单视图且零 mutation。F1 defer 可接受：accepted plan §6.3 冻结的 rebase 版本规则自洽且实现精确符合，S2 的 SKIP 双 predicate 与 create-overwrite rebase 分支均不依赖 sequential/rebase version 数值一致，defer 不影响 S2 冻结规则。无 S2 接线/behavior drift。

## Findings

未发现实质性问题。

以下为本次 re-review 要求的五项挑战与 F1/S2 专项核查的逐项证据记录。

### 挑战 1 — 真实 prepare→publish→durable identity 交叉测试是否同实现自证

- **入口/测试**: `tests/fins/test_docling_upload_service.py::test_prepared_identity_exactly_matches_real_published_storage_projection`
- **prepared 端数据源**: 真实 `service.prepare_upload()`（含真实 Docling conversion）产生 base mutation，经 `_as_prepared_filing_candidate()` 显式收窄后由 `describe_prepared_filing_publication()` 从**内存** `pending_assets` + `candidate.meta` 投影。
- **durable 端数据源**: `_publish_prepared_upload()` 调用生产 `begin_batch()` + `commit_prepared_upload_batch()` 完成真实 durable 写入；随后 `FsFilingUploadStateRepository.read_filing_upload_state()` 走 published guard → `_inspect_source_kind_unguarded()` → `_project_filing_upload_publication_identity()`，从**磁盘** manifest、file descriptors、persisted source meta 投影。
- **判定**: 两侧是两段独立实现的投影（docling_upload_service.py:1157-1245 vs _fs_source_integrity.py:1014-1170），数据源一内存一磁盘，断言 `durable_state.publication_identity == prepared_identity` exact dataclass equality，并逐资产七元组（name/original_filename/derived_from/sha256/size/content_type/source）再次断言。不是同实现自证。
- **覆盖边界核查**: adjudication F2 修复边界为 primary、companion、多文件 role 与 metadata，不枚举 mutation variant。测试覆盖两 original + 一 Docling 派生资产、primary 命中、companion 分割、`primary_original_asset_name == _build_filing_original_asset_identity(primary)`（name 规则一致性，非投影同源）。符合边界。
- **残余观察（非缺陷）**: 该测试未覆盖 primary flip（companion 变 primary）；`_build_publication_identity` fixture 驱动的 identity inequality 测试（test_filing_upload_publication.py:272-291）已覆盖 flip/hash/derived/content_type 不等价方向，两者互补。

### 挑战 2 — §7.4 conflict grid 构造合法性与准确性

- **入口/测试**: `tests/fins/test_filing_upload_publication.py::test_arbitration_conflict_grid_is_closed_and_typed`（五格参数化）
- **格 1 MISSING→REPAIR_REQUIRED**（auto/no-ow）: initial MISSING、fresh REPAIR_REQUIRED 均由真实 `validate_fins_upload_filing_request` 产出；status 不同 → stable=False → 不命中 MISSING→COMPLETE 与 REPAIR_REQUIRED 分支 → fallthrough conflict。格合法。
- **格 2/3 COMPLETE→MISSING、COMPLETE→REPAIR_REQUIRED**: initial COMPLETE 带 identity + company keep，fresh 由同一 raw request 真实验证；同样 fallthrough conflict。格合法。
- **格 4 MISSING→COMPLETE auto+overwrite=True**: raw auto 不满足 `overwrite is False` 收敛条件、不满足 `raw_action == "create"` rebase 条件 → conflict。无论真实 validator 对 auto+ow=True+COMPLETE 产出何种 resolved/repair，均不影响该格裁决。格合法。
- **格 5 MISSING→COMPLETE explicit create+overwrite=False**: 真实 validator 对 fresh COMPLETE 的 create+no-ow 会抛 `CREATE_TARGET_EXISTS` usage error（§6.4 映射表路径），无法产出 ValidatedFinsUploadFilingRequest；测试以 `replace(initial, published_state=donor.published_state, ...)` 显式构造 fresh create 观察（resolved 为 donor 的 update），并用 `_require_same_request_and_target` 的 raw request equality 保证目标一致。该格证明"explicit create + overwrite=False 绝不进入 skip"即使 identity 精确相等、company keep、resolved=update 也固定 conflict。构造合法且正是 plan §11 点名的语义。
- **断言质量**: 每格断言 CONFLICT、非 SKIP、`publish_mode is None`、`failure_reason.code is SOURCE_PUBLICATION_CONFLICT`。typed 且非 skip 双断言齐备。
- **残余观察（非缺陷）**: 格 5 的 S2 真实路径（validator usage error → §6.4 冲突映射）与纯仲裁 fallthrough 是两条不同 owner 边界，S2 route tests 需在映射表路径上补断言；两条边界当前都 fail closed 到同一 code，无分歧。

### 挑战 3 — UNSAFE 与 stable action test 是否命中实际 owner

- **UNSAFE 入口拒绝**: `test_arbitration_rejects_unsafe_observation_at_either_entry` 用 `object.__setattr__` 越过 frozen dataclass 与 validator（真实 validator 拒绝 UNSAFE published_state），向 initial/fresh 任一侧注入 UNSAFE，命中 `arbitrate_filing_upload_publication()` :347-348 的入口 raise，断言 `ValueError match="UNSAFE source observation"`。命中实际 owner（pure arbitration 防线），且两侧参数化。合法。
- **stable REPAIR_REQUIRED invariant**: `test_arbitration_rejects_stable_repair_action_invariant_drift` 构造 initial/fresh 均 REPAIR_REQUIRED + 同 revision（stable 成立），再 `object.__setattr__` 把两侧 `resolved_action` 伪造为 `"create"`（两侧相等，通过 :261 前置 equality），命中 `_require_stable_action_contract()` :278-286 REPAIR_REQUIRED 分支的 fresh 检查 → raise。命中实际 owner。
- **诚实观察（非缺陷）**: 由于 :261 前置 equality（`initial_request.resolved_action != fresh_request.resolved_action` 先 raise），C1 修复（:280 改查 `fresh_request.resolved_action`）在行为上与检查 initial 等价，任何测试都无法区分检查源。测试固定的是该分支 fail-closed 语义；owner 精确性由代码本身体现。C1 属精确性修复，测试固定分支行为，可接受。

### 挑战 4 — strict text 类型校验完整性与误拒绝

- **实现**: `repository_protocols.py` 的 `FilingUploadPublicationIdentity.__post_init__` 对 12 个 required text 字段 loop 内先 `isinstance(value, str)` 再空值检查；`filing_date`/`report_date` 可选字段 `isinstance` + 非空；`source_fingerprint` 单独 `isinstance`；`FilingUploadAssetDescriptor` 对 name/original_filename/sha256/derived_from 均 `isinstance`，`content_type` 在 `is not None` 前提下 `isinstance` + 非空；`size`/`fiscal_year` 用 `type() is int` 精确拒绝 bool。
- **误拒绝核查**: `content_type=None`、`filing_date=None`、`report_date=None` 合法（None 分支不进入 str 校验）；`fiscal_year` 为 int 字段不进 required_texts loop；`amended/is_deleted` 用 `type() is` 精确 bool。全部合法构造经 944 条 focused + 1884 条全量测试通过。
- **测试覆盖**: `test_publication_identity_rejects_non_string_business_text` 覆盖 fiscal_period（loop 代表）、asset name、content_type 三个非 str 分支。
- **残余观察（低风险）**: `filing_date`/`report_date` 非 str、`source_fingerprint` 非 str、`original_filename` 非 str 三分支无直接测试；均为同一 isinstance 模式的不同字段分支，且生产两个 producer（`_require_prepared_filing_meta_text` 强制 str、`_required_publication_identity_text` 只返回 str）不会注入非 str，不影响当前等价判断。不构成 blocker。

### 挑战 5 — MISSING batch read 是否同 staging 且零 mutation

- **入口/测试**: `tests/fins/test_fins_storage_atomicity.py::test_filing_upload_state_batch_reader_returns_missing_from_empty_staging_without_mutation`
- **同 staging**: 空 workspace 上 `begin_batch("AAPL")` 后调用 `read_filing_upload_state_in_batch(batch, document_id)`，生产实现经 `_resolve_active_batch` 后只传 `state.staging_ticker_dir` 给共享 `_read_filing_upload_state_from_ticker_dir_unguarded`；staging 路径由前一测试的 monkeypatch（`_target_ticker_dir` 拒绝 published locator、company reader/strict parser 调用根精确等于 `staging_ticker_dir`）固定。
- **零 mutation**: `before_read = _snapshot_workspace_tree(workspace_root)` 在 begin_batch 之后拍摄（覆盖目录结构与全部 regular file bytes，含 batch journal），read 后快照精确相等，再 `rollback_batch`。断言完整。
- **MISSING 投影**: `company_meta is None`、ticker/document_id 精确、status MISSING、revision None、reasons 空、source_meta None、publication_identity None。全部断言齐备。
- **补充**: published 空 document_id fail-fast（`test_filing_upload_state_published_reader_rejects_empty_document_before_mutation`）与零 mutation、batch read 空 document_id/foreign core/wrong ticker/closed token fail-fast（`test_filing_upload_state_batch_reader_rejects_invalid_capabilities_without_mutation`）均有直接测试。F5(a)/F5(b) 修复完整。

### 专项裁决 — F1 defer 是否可接受且不影响 S2 冻结规则

- **F1 主张**: rebase 用硬编码 `identical_skip_safe=True` 伪造 skip 语义 flag，造成 sequential prepare（role-ambiguous 时无条件递增）与 concurrency rebase（同指纹保持）的 document_version 路径分歧。adjudication 以 accepted plan §6.3 已冻结 rebase 规则为由 `rejected-with-reason`，并把分歧登记为 deferred residual。
- **实现核对**: `_resolve_document_version()`（docling_upload_service.py:1712-1737）在 `identical_skip_safe=True` 时同指纹保持 fresh version、异指纹递增；`rebase_prepared_filing_create_overwrite()`（:1293-1297）硬编码 True，恰好精确实现 plan §6.3 冻结的 rebase 规则。`_resolve_document_version`/`identical_skip_safe` 语义为既有基线代码（不在本 diff 内），sequential 行为未被本 work unit 改变。
- **S2 影响链核查**:
  1. SKIP predicate A（stable retransmission，COMPLETE→COMPLETE）: 不经过 rebase；prepare 版本从 durable previous_meta 计算，stable observation 下 prepared/durable version equality 闭环。
  2. SKIP predicate B（changed convergence，MISSING→COMPLETE auto）: 不经过 rebase；winner/loser 均在 MISSING previous state 上 prepare，`_resolve_document_version` 对 `previous_meta is None` 提前返回 `"v1"`（:1729-1730），不经过 identical_skip_safe 分支，两侧 version 一致，equality 可命中。
  3. REBASE_CREATE_OVERWRITE 分支（explicit create+overwrite=True）: 只做 fresh rebase 后 PUBLISH，不比较 identity，version 数值分歧不影响 skip/conflict/publish 裁决正确性，仅影响 role-ambiguous 多文件场景下 sequential-vs-concurrent 的 audit 数值一致性。
- **裁决**: F1 defer 可接受。accepted plan 冻结的 rebase 规则自洽且实现精确符合；F1 要求的"sequential 与 rebase version 必须一致"是 plan 冻结规则之外的新 contract 假设，拒绝理由成立；deferred residual 已登记 owner（未来独立 document-version policy work unit），不影响 S2 冻结规则与 S2 裁决正确性。

### 专项核查 — 无 S2 接线 / behavior drift

- `prepare_upload()` 仍返回 base `_PreparedAssetMutation`（docling_upload_service.py:511），filing identical 仍在 `_can_skip_upload` 命中时 early skip（:481-490），只做了 skip result 构造的语义等价提取（`_build_skipped_result` 与旧内联构造逐字段一致）。
- `_PreparedFilingAssetMutation` 在生产代码中仅出现于类型定义与四个 helper 的收窄处，`prepare_upload()` 不构造 filing subtype、不产生 typed disposition。
- `execute_prepared_filing_publication` 在 `dayu/` 与 `tests/` 全库零命中；`filing_upload_publication.py` 无 begin/stage/commit/rollback 调用。
- SEC/CN/HK workflow 与 workflow tests 零 diff（`git diff --name-only` 核对）；`_fs_storage_infra.py`（batch lifecycle owner）零 diff，`_resolve_active_batch` 为既有实现。
- material 行为不变：material 路径的 `_build_skipped_result` 提取为等价重构；四个机械 fixture 文件 diff 仅为 `publication_identity=None` 单行；runtime/tools 两个 forbidden fake 与一个 fixed fake 的 conformance 修订符合两次 amendment 冻结边界（无新增 cast/default/getattr/wrapper）。
- 越界核对：全部 workspace 文件均在 S1 allowlist 内；`git diff --check` 通过。

## Open Questions

- 无。

## Residual Risk

1. **C1 修复的行为不可区分性**: `_require_stable_action_contract()` 的 REPAIR_REQUIRED 分支检查 fresh 而非 initial，因 :261 前置 equality 与检查 initial 行为等价，无测试可区分检查源；现有测试固定分支 fail-closed 语义。未来若重构移除前置 equality，该测试会开始真正守卫 fresh 检查。风险低。
2. **strict text 三分支无直接测试**: `filing_date`/`report_date`/`source_fingerprint` 非 str 与 `content_type=None` 合法通过无直接断言；同一 isinstance 模式，生产 producer 不会注入非法值。风险低。
3. **conflict grid 格 5 的 S2 真实路径差异**: 纯仲裁的手动构造防御与 S2 真实 validator usage error 映射是两条 owner 边界，S2 route tests 需覆盖 §6.4 映射表路径（S2 职责，非 S1 缺陷）。
4. **交叉测试场景广度**: 真实交叉 owner 测试覆盖单 primary + 单 companion；primary flip 的等价性由 fixture 驱动的 inequality 测试互补覆盖，S2 route tests 会再覆盖多文件 primary flip 真实路径。
5. **验证复核范围**: 本 review 独立运行 focused 九文件 `944 passed, 3 warnings`、完整 `pytest tests/fins -q` `1884 passed, 1 skipped, 3 warnings`、全仓 pyright `0 errors, 0 warnings`、`git diff --check` 通过；coverage 数据未独立复跑，沿用 implementation artifact 声明（四模块合计 88%、逐文件均 ≥80%）。frozen pytest-cov NumPy duplicate-load 问题未复现验证，不在本 review 结论依赖内。
6. **未覆盖区域**: `test_fins_service_runtime.py` 动态 monkeypatch fake 维持 Controller 已裁决的未来触发式 non-blocking residual；Host/Engine 侧无 `FilingUploadPublishedState` 新字段消费点（已确认零命中）。
