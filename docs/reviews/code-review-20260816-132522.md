# Code Review — UF-FIX08 Slice 1 Re-review

## Scope

- Mode: current changes（re-review，复审 adjudication 闭环）
- Branch: `codex/upload-filing-oracle`
- Base: `c8e75629`（UF-FIX08 accepted plan commit）
- Output file: `docs/reviews/code-review-20260816-132522.md`
- Review inputs:
  - `docs/reviews/code-review-20260816-130711.md`（首轮 review）
  - `docs/reviews/code-review-20260816-131113.md`（二轮 review）
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-slice1-code-review-adjudication-20260816.md`（Controller 裁决）
- Included scope: 同前两轮（Slice 1 allowed files + plan amendments + 被影响消费面走读）
- Parallel review coverage: 无

## Findings 闭环

### CR-130711-001：Protocol concrete rejection helper 非典型

- **裁决**: `accepted`
- **fix 要求**: 保留 typed `NoReturn` rejection helper，runtime 确定性拒绝；accepted plan Slice 4 新增删除 transitional body/helper、production facade delegate 真实 core 的强制步骤
- **复审结论**: **已修复**
- **直接证据**:
  - `repository_protocols.py:61-74`：`_reject_unimplemented_source_repair()` 仍存在，签名为 `() -> NoReturn`，body 为 `raise NotImplementedError("staged source repair mutation 尚未实现")`
  - `repository_protocols.py:737`：`reset_source_document_for_repair()` 方法体调用该 helper
  - plan diff：Slice 4 allowed files 新增 `fs_source_document_repository.py`（`+| dayu/fins/storage/fs_source_document_repository.py | Slice 4删除protocol transitional rejection后，由production facade显式delegate真实core repair owner`）；Slice 4 步骤 4 明确删除 body/helper 并新增 facade override
  - A/B 实验已由 Controller 复现：字面 raise → 130 errors，helper 恢复 → 0 errors

### CR-130711-002：Service/CLI fixture 跨层依赖

- **裁决**: `accepted`
- **fix 要求**: Service fixture 改用 `ingestion_runtime._filing_upload_request_identity()`，删除新增 Docling pipeline import
- **复审结论**: **已修复**
- **直接证据**:
  - `tests/service/test_fins_direct.py` diff：新增 `import dayu.fins.ingestion_runtime as ingestion_runtime`；`build_sec_filing_ids` import 已删除；fixture 改为 `canonical_ticker, filing_document_id = ingestion_runtime._filing_upload_request_identity(raw_filing_request)`
  - `tests/cli/test_fins_commands.py`：保留既有 `build_sec_filing_ids` import（base 已存在，非本 slice 新增），未增加新依赖

### CR-131113-01：Slice 4 缺少 production facade 接管路径

- **裁决**: `accepted`
- **fix 要求**: plan Slice 4 allowed files 新增 `fs_source_document_repository.py`；明确删除 body/helper、facade 显式 override delegate、owner test
- **复审结论**: **已修复**
- **直接证据**:
  - plan production affected-files 新增 `fs_source_document_repository.py` 行
  - plan Slice 4 allowed files 新增 `fs_source_document_repository.py`
  - plan Slice 4 步骤 4 新增：删除 transitional rejection body/helper、protocol 恢复纯 method contract、facade 显式 override 并 delegate 共享 core
  - plan Slice 4 步骤 8 新增：storage owner test 从真实 `FsSourceDocumentRepository` facade 调用 repair，证明进入共享 core 真实实现

### CR-131113-02：129 个 pyright 错误叙事缺少可复现证据

- **裁决**: `accepted`
- **fix 要求**: implementation artifact 回写精确命令、数量、首尾错误、差异来源与 helper 恢复理由
- **复审结论**: **已修复**
- **直接证据**:
  - Controller A/B 实验在 adjudication 中记录：字面 raise → 130 errors（`reportAbstractUsage`），首个 `cn_pipeline.py:392:56`，末个 `test_combined_tools_acceptance.py:890:25`；相对 implementation 初轮 129 条增加的 1 条来自新增 test 实例化
  - implementation artifact 已回写修正后的叙事（adjudication 裁决记录）

### CR-131113-03：damaged upload-state read 异常面与 owner tests 未闭合

- **裁决**: `accepted`
- **fix 要求**: (1) 更新 protocol/mixin Raises docstring；(2) 新增两个真实 filesystem owner tests；(3) plan Slice 2/3/5 明确 damaged state 收敛路径
- **复审结论**: **已修复**
- **直接证据**:
  - Protocol Raises 更新（`repository_protocols.py` diff）：`ValueError: ticker/document identity 非法，或 published source directory、meta、identity descriptor 结构损坏（含 required meta/descriptor 缺失）时抛出`；`OSError: identity descriptor、meta 或其它 published state operational 读取失败时抛出 path-free 文件系统异常`
  - Mixin Raises 更新（`_fs_filing_upload_state_core.py` diff）：同步更新
  - 新增 test `test_filing_upload_state_fails_closed_for_missing_meta_without_mutation`：发布完整 filing 后删除 `meta.json`，断言 `ValueError(match="缺少 meta.json")`、path-free、零 mutation
  - 新增 test `test_filing_upload_state_fails_closed_for_missing_identity_descriptor_without_mutation`：删除 identity descriptor，断言 `ValueError(match="identity descriptor")`、path-free、零 mutation
  - plan Slice 2 步骤 1 新增：`meta/identity descriptor 缺失或损坏必须归属 UNSAFE/revision=None typed inspection`
  - plan Slice 3 步骤 1 新增：`required meta/identity descriptor 缺失或损坏返回 source_integrity=UNSAFE/revision=None + source_meta=None typed state`
  - plan Slice 5 步骤 5 新增：fresh re-read 仍可能产生的 path-free `ValueError/FileNotFoundError` 必须经 upload-failure owner 收敛为唯一 typed failed event

### CR-131113-04：classification 末分支缺少显式 UNSAFE gate

- **裁决**: `accepted`
- **fix 要求**: UNSAFE invariant 前新增显式 gate；contract test 用 FUTURE member 证明未来状态被拒绝
- **复审结论**: **已修复**
- **直接证据**:
  - `source_integrity.py` diff：UNSAFE 分支前新增 `if self.status is not SourceIntegrityStatus.UNSAFE: raise ValueError("status 必须是封闭四态")`
  - 新增 test helper `_FutureSourceIntegrityStatus(str, Enum)`：包含 FUTURE = "future" member
  - 新增 test `test_source_integrity_classification_rejects_future_status_member`：monkeypatch `SourceIntegrityStatus` 为含 FUTURE 的 enum，断言 `ValueError(match="封闭四态")`

### CR-131113-05：Service fixture 绕过 request identity owner

- **裁决**: `accepted`
- **fix 要求**: 同 CR-130711-002
- **复审结论**: **已修复**（与 CR-130711-002 同一 fix 覆盖）

## Plan Amendments 核对

| amendment | 位置 | 状态 |
| --- | --- | --- |
| Slice 1 `_fs_source_document_core.py` boundary amendment | plan §9 Slice 1 allowed files + 步骤 1 | ✅ diff 中已记录 |
| Slice 2 damaged state typed 收敛 | plan §9 Slice 2 步骤 1 + 完成条件 | ✅ 新增 `UNSAFE/revision=None` 归属、`ValueError/FileNotFoundError` 不再旁路 |
| Slice 3 state core 机械消费 inspection | plan §9 Slice 3 步骤 1 | ✅ 新增 `UNSAFE + source_meta=None` 返回、raw exception 不冒出 |
| Slice 4 protocol helper 删除 + production facade 接管 | plan production affected-files + Slice 4 allowed files + 步骤 4/8 | ✅ 新增 `fs_source_document_repository.py`、明确删除 body/helper、facade override、owner test |
| Slice 5 fresh re-read failure boundary | plan §9 Slice 5 步骤 5 | ✅ 新增 path-free `ValueError/FileNotFoundError` 经 upload-failure owner 收敛 |

## 新 blocking correctness/ownership/scope finding 检查

**未发现新 blocking finding。**

逐项检查：
- **correctness**: 四态 invariant 封闭（含显式 UNSAFE gate）、UNSAFE fail closed、state/meta 同源、guard 内读取、damaged state fail closed + 零 mutation——均通过 test 证据确认。
- **ownership**: `source_integrity` 由 storage owner 产生、protocol/mixin docstring 已更新承诺异常面、`_filing_upload_request_identity` 为 request identity 真源——无 semantic ownership drift。
- **scope**: 修改文件与 plan Slice 1 allowed files 一一对应；plan amendments 落在对应 slice 的 allowed files 内；无越界实现 Slice 2-6。

## Validation

- focused suite: `637 passed, 3 warnings`（较首轮 634 新增 3 个 finding-specific tests）
- pyright: `0 errors, 0 warnings, 0 informations`
- `git diff --check`: 通过
- plan/README/registry/oracle/evidence forbidden-scope diff: 均为空

## Residual Risk

| residual / uncovered area | 分类 |
| --- | --- |
| transitional rejection helper 仍存在，production facade 尚未 delegate 真实 repair owner | `covered by later approved slice`：plan 已 amend Slice 4 |
| Slice 1 damaged-state read 仍以 path-free structural exception fail closed，尚未产出 `UNSAFE` typed state | `covered by later approved slice`：Slice 2 inspector + Slice 3 state 接入 |
| fresh SEC/CN/HK re-read/runtime event 尚未收敛全部 failure | `covered by later approved slice`：plan 已 amend Slice 5 |
| 当前 classifier 仍不产生完整 structural/role/manifest UNSAFE reasons | `covered by later approved slice`：Slice 2 |

没有未分类 residual risk。

## 结论

**PASS**。全部 7 项既有 findings 已按 adjudication 闭环修复，plan amendments 已落地，无新 blocking finding。本 slice 可进入下一 gate（Slice 2）。
