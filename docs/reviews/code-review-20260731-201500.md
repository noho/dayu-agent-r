# Code Review

## Scope

- Mode: `WU-CLI-PROMPT-01` aggregate deepreview re-review
- Reviewed target: exact P46 final-replay fix after commit `9021cc43`
- Files: `dayu/cli/__main__.py`, `dayu/cli/agent_entrypoint.py`, `pyproject.toml`, `tests/cli/test_prompt_command.py`, `tests/cli/test_public_package_entrypoints.py`, `tests/README.md`
- Reviewer: independent sub-agent `review_sigint_restore`
- Excluded: frozen oracle/scenario semantics、interactive/Fins 业务取消语义、native launcher/interpreter pre-bytecode signal ownership

## Findings

未发现实质性问题。Gate verdict：`pass`。

## Evidence

- `CliSigintMonitor` 仍是 command-local handler 的单一 owner；保存并恢复既有 `asyncio.Runner` handler，没有在下游 prompt 分支重算 signal policy。
- `exit_module` 只拥有 canonical exit code 确定后的 process teardown；仅对 130 安装 `SIG_IGN`，普通 0/1/2 退出不改变 signal policy。
- pyproject console script 与 module invocation 共用同一公共入口，不是 prompt-only shim。
- 新增/修改函数具备中文 docstring、参数/返回/异常说明，严格 pyright 通过。
- owner tests 覆盖 handler identity 恢复与 130/0 分支，public package test 覆盖 wheel metadata entrypoint。
- fresh exact P46 真实 probe 中两个 frozen SIGINT 都实际发送，exit 从 `-2` 变为 `130`；Host Run/attempt 均 `cancelled`，EventLog 含 `ATTEMPT_CANCELLED` 与 `RUN_CANCELLED`，UI 不含内部 reason。
- shared regression：`142 passed`；总控扩大受影响集合后为 `1229 passed, 7 skipped`；完整 pyright 0 errors；`git diff --check` 通过。
- final target `174f65cb` 的 321 条 focused-real replay 全部通过，failure count 为 0。

## Docs Decision

- 根 README 的用户工作流语义未变化，无需再次修改。
- `tests/README.md` 已按职责记录 process teardown 与 handler restoration 覆盖。

## Residual Risk

- 非冻结 0.005 秒级 interpreter pre-bytecode SIGINT 归 native launcher/distribution owner；需要新的 work unit 或用户显式决策，不属于当前 approved scope。frozen `PC-CN-01` exact 0.05 秒已通过。

## Completion

- Re-review status: pass
- Required fix: none
- Artifact path: `docs/reviews/code-review-20260731-201500.md`
