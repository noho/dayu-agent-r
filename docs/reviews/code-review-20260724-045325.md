# Code Review

## Scope

- Mode: current changes
- Branch: `feat/wu-ctx-01`
- Base: `ed43bcf2`
- Output file: `docs/reviews/code-review-20260724-045325.md`
- Included scope: current workspace diff relative to `ed43bcf2`，覆盖全部 60 个变更文件（18 production + 40 test + 2 README）。
- Excluded scope: Controller-owned `docs/host/issues-implementation-control.md`、Controller adjudication artifact、既有 review artifacts（`code-review-20260724-035007.md`、`code-review-20260724-034122.md`）。
- Parallel review coverage: 3 个 subagent 分别覆盖 CTRL-IMPL-01（effective tool facts）、CTRL-IMPL-02/03/04（NoBudgetDispatchStart + typed source failure + row/hot identity）、REVIEW-IMPL-05/06/07/08/09（5×3 matrix + compactor invariant + dead code + docstring）。主 reviewer 整合、去重、复核证据链并独立执行 adversarial failure pass。

## Decision

**pass**

## Accepted Finding 关闭矩阵

以下逐项复核 Controller 裁决的 `CTRL/REVIEW-IMPL-01..09` 是否真正关闭。

### CTRL-IMPL-01（高）：durable effective tool facts 精确消费

**已关闭。** 直接证据：

- `admission.py` 建立共享 typed contract：`EffectiveBusinessToolSelector`（StrEnum，line 230）、`EffectiveToolFacts`（frozen dataclass，line 238）、`effective_tool_facts_json()`（line 3884）、`parse_effective_tool_facts()`（line 3958）、`validate_effective_tool_facts_runtime()`（line 4032）。
- `dispatch.py` 从 admission 导入上述五项（lines 32-37），pre-start 路径（line 2603）和 worker dispatch 路径（line 4700）均调用 `validate_effective_tool_facts_runtime()` 获取 frozen exact names，不再以 `None` 表示"选择当前全部工具"。
- `effective_tool_facts_json(None, tooling_options=...)` 对 `selector=all` 冻结 exact enumerated names（line 3904-3906），selector 只保留 caller intent。
- `validate_effective_tool_facts_runtime()` 做六点校验：bundle digest、source refs、tool name subset、schema digest、snapshot ref、display names。任一 drift 抛出 `HostDurableError`，transaction rollback，零 Attempt/manifest/start。
- repair replay 使用 `effective_tool_facts_json(frozenset(), tooling_options=None)`（line 2732-2735），不读取当前 tooling。
- steer 在 `_strict_steer_candidate_inputs`（line 3284-3304）保留 SUBSET/ALL/NONE caller intent：SUBSET+non-empty+policy-disabled fail closed；ALL 被 policy 禁用时 graceful 降级；NONE 保持 no-tool。

### CTRL-IMPL-02（中）：NoBudgetDispatchStart stage

**已关闭。** 直接证据：

- `NoBudgetDispatchStart` 显式携带 `stage: ContextSizingStage`（dispatch.py:722-731）。
- ordinary producer 传 `ContextSizingStage.ORDINARY`（line 1827-1829）；post-compact 传 `POST_COMPACT`（line 2504）；dispatch-fallback 传 `DISPATCH_FALLBACK`（line 1996-1999, 3297-3300）。
- commit owner 消费 `plan.stage`（line 2738-2742），不硬编码。

### CTRL-IMPL-03（中）：continuation typed closed reason

**已关闭。** 直接证据：

- `run_input.py` 新增 `PreparedRunnerCallSourceFailureCategory`（StrEnum，line 411-416）和 `PreparedRunnerCallSourceError(HostDurableError)`（line 419-444），携带 typed category。
- `engine_ingest.py::_continuation_frozen_sources`（line 6175-6254）捕获 `PreparedRunnerCallSourceError`（非宽泛 `HostDurableError`，line 6198），通过 `_continuation_reason_from_source_failure(exc.category)`（line 6257-6277）精确映射。
- 固定优先级：projection（line 6190-6201）→ tool schema（line 6204-6213）→ policy（line 6215-6225）→ request semantics（line 6226-6241）。首个 failure wins。

### CTRL-IMPL-04（中）：EventLog row / hot identity

**已关闭。** 直接证据：

- `_find_existing_runner_call_manifest_event`（run_input.py:6221-6299）校验 event_class、event_type、session_id、run_id（line 6258-6264）。
- hot payload 交叉校验 session_id、host_run_id、attempt_id、execution_id（line 6271-6279）。
- continuation limited manifests（iteration_id/iteration_index 非空，line 6288-6289）仍执行 row/hot identity 检查但不参与 pre-start duplicate 判断。
- compactor proposals 无条件跳过（line 6280-6281）。
- 双 pre-start manifest 触发 duplicate error（line 6294-6297）。

### REVIEW-IMPL-05（中）：5×3 owner matrix

**已关闭。** 直接证据：

- `ContextSizingStage` 包含 5 个成员：ORDINARY、POST_COMPACT、REACTIVE_POST_COMPACT、DISPATCH_FALLBACK、CONTINUATION（context_budget.py:83-90）。
- test_context_budget.py:66-158 参数化 5×3=15 cells，覆盖全部 CONTINUATION × normal/soft/hard。
- docstring 确认"5×3 stage-aware action 矩阵"（line 167）。

### REVIEW-IMPL-06（中）：compactor proposal COMPLETE invariant

**已关闭。** 直接证据：

- `_runner_call_manifest.py:1383-1387` 明确拒绝 `compactor_proposal + COMPLETE`，抛出 `HostDurableError("compactor proposal sizing must be not_applicable")`。
- 同时拒绝 UNAVAILABLE（line 1421-1424），唯一合法 sizing 为 `not_applicable`（line 1430）。
- test_runner_call_hot_payload_contract.py:402-431 覆盖正反 contract。

### REVIEW-IMPL-07（低）：dead source policy loader

**已关闭。** 直接证据：

- `_source_policy_snapshot_in_transaction` 在 engine_ingest.py 和整个 `dayu/` 下零命中。相关 imports/constants 已同步删除。

### REVIEW-IMPL-08（低）：dead admission direct-running wrapper

**已关闭。** 直接证据：

- `_create_running_admission_result` 在 admission.py 零命中。`CreateRunningRunInput`、`PromoteQueuedRunInput`、`PromotionSkipReason` 的 admission imports 已删除。

### REVIEW-IMPL-09（低）：estimator digest docstring

**已关闭。** 直接证据：

- `BudgetEstimate.estimator_digest` docstring（context_budget.py:279）已改为"估算器契约、完整估算输入与固定估算常量的 digest"。

## 四项途中复核

### 1. repair replay 永久 no-tool 且不读当前 tooling

**通过。** `from_source_run_replay()`（admission.py:2695-2736）调用 `effective_tool_facts_json(frozenset(), tooling_options=None)`，`tooling_options=None` 导致 `business_definitions=()`、`known_names=frozenset()`。dispatch 路径对 replay 调用 `validate_effective_tool_facts_runtime(effective_tool_facts, tooling_options=None)`（line 2598-2601），`None` 产生空 definitions 匹配空 frozen facts，不做任何当前工具校验。

### 2. continuation limited manifest 不误判 pre-start duplicate 但仍校验 row/hot

**通过。** `_find_existing_runner_call_manifest_event`（run_input.py:6221-6299）先执行 row identity（line 6258-6264）和 hot cross-validation（line 6271-6279），再跳过 iteration_id/iteration_index 非空的 continuation manifests（line 6288-6289），最后对 surviving pre-start kinds 做 duplicate 检测（line 6290-6298）。

### 3. steer 保留 SUBSET/ALL/NONE 在 policy-disabled 下的 caller intent

**通过。** `_strict_steer_candidate_inputs`（admission.py:3284-3304）：
- SUBSET+non-empty+non-replay → `HostDurableError`（fail closed，line 3294）。
- ALL+policy-disabled → `disable_tools=True`，`NO_TOOL_DISABLED`（graceful 降级，line 3297-3304）。
- NONE+policy-disabled → `disable_tools=True`，`NO_TOOL_DISABLED`（保持 no-tool）。
- Replay → `NO_TOOL_REPLAY`（不论 selector）。

### 4. source failure 固定 projection→tool→policy→request typed precedence

**通过。** `_continuation_frozen_sources`（engine_ingest.py:6175-6254）：
1. projection/source（line 6190-6201）：`load_prepared_runner_call_source_in_transaction` → 捕获 `PreparedRunnerCallSourceError`，按 typed category 映射。
2. tool schema（line 6204-6213）：`_continuation_tool_schema_fragments` → `CONTINUATION_TOOL_SCHEMA_UNAVAILABLE`。
3. policy（line 6215-6225）：检查 sizing status、context_window_size、policy_ref、policy_snapshot_digest → `CONTINUATION_POLICY_UNAVAILABLE`。
4. request semantics（line 6226-6241）：检查 provider、model、request_semantics_digest、estimator contract → `CONTINUATION_REQUEST_SEMANTICS_UNAVAILABLE`。

## Findings

未发现实质性问题。

## Adversarial Failure Pass

### Semantic Ownership Drift

未发现新增 semantic ownership drift。主要检查点：

- effective tool facts contract 由 admission（语义真源）定义，dispatch 只消费不重解释。selector 字段只保留 caller intent，effective names 是 admission 时冻结的 exact set。
- PreparedRunnerCallSourceFailureCategory 由 run_input（strict source loader owner）产生，engine_ingest 只做封闭映射。
- NoBudgetDispatchStart.stage 由各 producer site 显式传入，commit owner 只消费不反推。
- ContextSizingStage/ContextSizingResult 由 context_budget（sizing owner）定义，dispatch/engine_ingest 只消费。

### Overcoupling

未发现过度耦合。主要检查点：

- admission ↔ dispatch 的 effective tool facts 依赖方向正确（admission 是 contract owner，dispatch 是 consumer）。
- run_input → engine_ingest 的 typed source failure 依赖方向正确（run_input 是 strict loader owner，engine_ingest 是 consumer）。
- _runner_call_manifest 被 run_input、engine_ingest、dispatch 三方共用，是正确的 manifest contract owner。
- 未发现跨层穿透、双向依赖或共享可变状态。

### Boundary Violations

- `CONTEXT_BUDGET_EVALUATED`：production/test 代码零命中，Slice 2 未提前混入。
- `USAGE_ANCHORED`：仅出现在 context_budget.py enum 定义（line 71），无执行路径，Slice 3 未提前混入。
- direct promotion：production/test 代码零 stale symbol，`PromoteQueuedRunInput` 已从 admission imports 删除。
- dead code：`_source_policy_snapshot_in_transaction`、`_create_running_admission_result` 均零残留。

## 验证

### Focused tests

```text
pytest -q tests/host/test_dispatch_scheduler.py tests/host/test_context_budget.py tests/host/test_runner_call_hot_payload_contract.py tests/host/test_engine_ingest_mapping.py tests/host/test_run_input_builder.py

397 passed in 4.00s
```

### Full Host tests

```text
pytest -q tests/host --cov=dayu.host --cov-branch --cov-report=

2202 passed, 2 skipped, 6 deselected in 69.99s
```

默认 `pyproject.toml` 排除 6 个 stress tests；2 个测试按各自 skip 条件未执行。

### Full pyright

```text
python -m pyright dayu/host/ tests/host/

0 errors, 0 warnings, 0 informations
```

### Static audits

- `CONTEXT_BUDGET_EVALUATED` 新增符号 grep：零命中。
- `USAGE_ANCHORED` 执行路径 grep：零命中（仅 enum 定义）。
- direct promotion stale symbol grep：零命中。
- dead source-policy loader / admission direct-running wrapper grep：零命中。
- effective tool payload fixtures audit：执行路径使用 shared current producer；corruption fixtures 只用于 strict failure 测试。

### README audit

- `dayu/host/README.md`：已补充 admission effective tool facts、`all` exact consumption、pre-start drift fail-closed、repair replay no-tool 边界、manifest-before-start 不变量、五 stage 十五格 matrix、wait completed/cancelled only、failed/lost 零新 Attempt。
- `tests/README.md`：已补充 focused 命令与 Slice 1 实现覆盖说明。
- 未改变用户安装、CLI、Web/WeChat、根 README、分层装配或 Engine public contract，其余 README trigger 未命中。

## Open Questions

无。

## Residual Risk

- optional real compactor smoke 未启用。
- Gemini 等真实 provider quota / endpoint smoke 未执行。
- 默认 pytest 配置排除 6 个 stress tests。

这些是 Controller 明确保留的外部/可选验证风险，不以 mock 或本地 deterministic tests 冒充。
