# Code Review — WU-CM-06-S1 Fix Re-Review

## Scope

- Mode: current changes（fix re-review，仅 review accepted finding 的 fix）
- Branch: `work/cm-05-06-08-09`
- Base: HEAD（dirty workspace diff）
- Output file: `docs/reviews/code-review-20260612-162004.md`
- Previous S1 code review artifacts: `docs/reviews/code-review-20260612-160858.md`、`docs/reviews/code-review-20260612-161139.md`
- Included scope: accepted finding fixes in `tests/host/test_memory_projection.py`、`tests/host/test_terminal_summary_payload.py`、`docs/host/issues-implementation-control.md`
- Excluded scope: 非 fix 范围的其他 S1 diff（已在第一次 review 中覆盖）
- Parallel review coverage: 无

## Accepted Findings to Verify

### Finding 001（来自 code-review-20260612-161139.md）

> `test_memory_direct_consumer` 测试命名存在轻微偏差：`test_projection_consumer_hydrates_terminal_content_as_final_answer` 暗示 consumer 做 hydration，实际 hydration 由 durable projection / run-input adapter 完成。

**预期 fix**: 更名为 `test_durable_projection_hydrates_terminal_content_as_selected_recent_window`，docstring 强调 durable projection adapter 归属。

### Finding 002（来自 code-review-20260612-161139.md）

> 缺少 malformed `terminal_summary_digest` 的单独覆盖。当前 `test_continuity_resolver_rejects_malformed_terminal_descriptor` 只覆盖了 `terminal_summary_ref` 为 int 的场景，缺少 `terminal_summary_digest` 为 int 的对称覆盖。

**预期 fix**: 新增 `test_continuity_resolver_rejects_malformed_terminal_digest`，覆盖 `terminal_summary_digest` 为 int 的场景。

## Fix Verification

### Fix 001: 测试重命名

- **文件(行号)**: `tests/host/test_memory_projection.py:386`
- **旧名**: `test_projection_consumer_hydrates_terminal_content_as_final_answer`
- **新名**: `test_durable_projection_hydrates_terminal_content_as_selected_recent_window`
- **docstring**: 从 `"durable projection 只把 digest-checked terminal content 合并为 final_answer"` 改为 `"durable projection adapter hydrate terminal content 后进入 continuity"`
- **断言重构**: 从单一 `selected_recent_window[0].text == "artifact final answer"` 改为结构化断言：
  - `len(selected) == 1`
  - `selected[0].text == "artifact final answer"`
  - `selected[0].included_reason is MemoryIncludedReason.SELECTED_RECENT_WINDOW`
  - `evidence_fact_memory.evidence_backed_facts == ()`
- **结论**: 命名精确反映 durable projection adapter 归属，断言强化（从单一 text 到 structured check），无覆盖退化。✅

### Fix 002: malformed terminal_summary_digest 对称覆盖

- **文件(行号)**: `tests/host/test_terminal_summary_payload.py:319-349`
- **新测试**: `test_continuity_resolver_rejects_malformed_terminal_digest`
- **输入场景**: `terminal_summary_ref="payload-terminal-summary"`（合法字符串），`terminal_summary_digest=123`（int 非法）
- **实际分支**: `assistant_final_answer_continuity_text` → `_optional_descriptor_text(run_payload, field_name="terminal_summary_digest")` → `isinstance(123, str)` is `False` → `raise HostDurableError("payload field terminal_summary_digest must be text")`
- **直接证据**: `dayu/host/_terminal_answer.py:83-84` — `_optional_descriptor_text` 对任意 `field_name` 使用相同类型检查逻辑，`terminal_summary_digest` 参数路径与 `terminal_summary_ref` 共享同一 helper
- **断言**: `pytest.raises(HostDurableError, match="terminal_summary_digest")`
- **对称性检查**: 现有 `test_continuity_resolver_rejects_malformed_terminal_descriptor`（行 286）测试 `terminal_summary_ref=123` + `pytest.raises(HostDurableError, match="terminal_summary_ref")`，两者完全对称
- **执行顺序验证**: 新测试中 `terminal_summary_ref` 为合法字符串，先通过 `_optional_descriptor_text` 检查并成功返回，执行到 digest 检查时触发异常。如果代码顺序改变导致 ref 检查先抛错，match `"terminal_summary_digest"` 会失败，测试不会静默通过
- **结论**: 对称覆盖完成，与现有测试共享同一验证模式。✅

### Fix 003: Control doc 更新

- **文件(行号)**: `docs/host/issues-implementation-control.md:145-148`、`docs/host/issues-implementation-control.md:893`
- **变更内容**:
  - gate 从 `"accepted plan commit"` 更新为 `"re-review"`
  - implementation status 更新为 `"WU-CM-06-S1 code review completed；accepted low findings fixed，awaiting re-review"`
  - next entry point 更新为 `"WU-CM-06-S1 re-review gate"`
  - WU-CM-06 状态追加了 S1 code review artifacts 和 fix 描述
- **结论**: control doc 正确反映当前 gate 和状态。✅

## Adversarial Failure Pass

### 重命名后断言不退化

原断言 `selected_recent_window[0].text == "artifact final answer"` 在新断言中保持不变，额外新增 `len(selected) == 1`（防止空 selected 时 `[0]` IndexError 被静默跳过）、`included_reason` 验证（确保 reason 语义正确）和 `evidence_backed_facts == ()`（确保 terminal content 不污染 evidence layer）。新断言集合是原断言的严格超集，无退化风险。

### 新测试的 match 精度

生产代码 `_optional_descriptor_text`（`_terminal_answer.py:84`）的异常消息为 `f"payload field {field_name} must be text"`。当 `field_name="terminal_summary_digest"` 且 `value=123` 时，消息为 `"payload field terminal_summary_digest must be text"`。`re.match("terminal_summary_digest", ...)` 会成功匹配。若匹配改为严格字符串相等或包含检查失败则不会静默通过。

### 无新增类型问题

- `test_continuity_resolver_rejects_malformed_terminal_digest`: `(tmp_path: Path) -> None` ✅
- 所有嵌套函数均标注完整类型 ✅
- 无 `Any`、`object`、无类型参数 ✅

### 测试隔离

新测试 `test_continuity_resolver_rejects_malformed_terminal_digest` 使用临时 `tmp_path` 和 `open_host_durable_store`，不依赖全局状态或顺序执行，可独立运行。✅

## Validation

- **pytest**: `tests/host/test_terminal_summary_payload.py tests/host/test_memory_projection.py tests/host/test_engine_ingest_mapping.py tests/host/test_read_api_terminal_policy.py -q` → **95 passed in 0.82s**（比 S1 review 的 94 passed 增加 1 个，即新增的 `test_continuity_resolver_rejects_malformed_terminal_digest`）
- **pyright**: `dayu/ tests/ utils/` → **0 errors, 0 warnings, 0 informations**

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- **复合 malformed+missing 场景未覆盖**: `test_continuity_resolver_requires_complete_terminal_descriptor` 覆盖了 descriptor 缺一侧（missing），`test_continuity_resolver_rejects_malformed_terminal_descriptor` 和新增的 `test_continuity_resolver_rejects_malformed_terminal_digest` 覆盖了字段类型非法。但"一侧 missing + 另一侧 malformed"的复合场景（如 `terminal_summary_ref` 缺失但 `terminal_summary_digest` 为 int）未被覆盖。此场景行为取决于 `_terminal_answer.py:49-56` 的字段检查顺序（ref 先于 digest），先命中的 malformed 检查会抛错，后命中的 missing 检查不可达。此路径顺序依赖未被测试固化。风险极低，非本次 accepted finding 范围。
- **run-input adapter hydration 路径未直接测试**: `test_durable_projection_hydrates_terminal_content_as_selected_recent_window` 通过 fixture 间接覆盖 hydration 结果，未直接测试 `run_input` 模块中的 hydration 逻辑。

## Conclusion

**PASS**

两个 accepted finding 的 fix 均已正确完成：

1. **Finding 001 fix**: `test_projection_consumer_hydrates_terminal_content_as_final_answer` → `test_durable_projection_hydrates_terminal_content_as_selected_recent_window`，命名精确反映 durable projection adapter 归属，断言强化为 structured check，无覆盖退化。
2. **Finding 002 fix**: 新增 `test_continuity_resolver_rejects_malformed_terminal_digest`，与已有 `test_continuity_resolver_rejects_malformed_terminal_descriptor` 对称覆盖 `_optional_descriptor_text` 的两条 field_name 路径。
3. **Control doc**: gate/status/next entry point 正确更新。

pytest 95 passed / pyright 0 errors，均可信。未引入新问题。
