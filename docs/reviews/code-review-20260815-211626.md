# Code Review

## Scope

- Mode: current changes
- Branch or PR: `codex/upload-filing-oracle`
- Base: `892be915`（== HEAD；scope = working tree changes：三份 README + Slice 4 implementation artifact）
- Output file: `docs/reviews/code-review-20260815-211626.md`
- Included scope:
  - `README.md`（working tree diff）
  - `dayu/fins/README.md`（working tree diff）
  - `tests/README.md`（working tree diff）
  - `docs/gateflow/uf-fix07-slice4-implementation-20260815.md`（新增 artifact，含验证记录）
  - 核对依据：`docs/gateflow/uf-fix07-multi-file-primary-collision-plan-20260815.md`（Slice 4 与 §6/§7 契约）以及 base 内三个实现 commit 的生产/测试代码：
    `9d0a2f8a`（Slice 1 admission）、`3911c790`（Slice 2 CLI/tool）、`892be915`（Slice 3 asset identity/fingerprint/atomicity）
- Excluded scope: 无（base 内 Slices 1–3 已有各自 review artifact；本 review 只对 README/artifact 声称做实现核对）
- Parallel review coverage: 无 subagent，主 reviewer 全程走读。

## Findings

未发现实质性问题。

本 review 的结论是 **PASS**。以下逐项记录对抗性检查的证据链，供 controller 裁决留档。

### 三份 README 读者职责边界

- `README.md` 更新约束要求"只写最终用户操作，不写内部架构/公共契约/测试清单"。diff 只新增用户命令示例（多文件 `--primary`）、文件集合/角色规则、usage failure 清单与"恰好 100 个允许"，未出现 `primary_selectors`、`FinsUploadFilingFiles`、identity、digest、`derived_from` 等内部术语。合规。
- `dayu/fins/README.md` 更新约束允许写"当前代码已实现的公共契约、主要组件、状态机、关键机制"。diff 使用 `FinsUploadFilingRequest.primary_selectors`、`FinsUploadFilingFiles`、`original_filename`/`derived_from`、`get_primary_source()` 等 owner 级术语，逐条与代码一致（见下）；未写用户手册内容、测试清单或未来能力。合规。
- `tests/README.md` 未定义自身更新约束，按 CLAUDE.md 触发规则属于 tests 变更 → 只更新测试覆盖描述与 focused command。diff 未把 UF-PF07/UF-PF12 写成已执行/已通过（"这是 deterministic owner-level 回归，不替代真实全格式 fixture 矩阵或全量 CLI scenario evidence"）。合规。

### raw/validated primary owner 投影准确性

- raw：`FinsUploadFilingRequest.primary_selectors: tuple[Path, ...] = ()`（`dayu/fins/ingestion_runtime.py:645`），CLI `action="append"` 保留全部 occurrence（`dayu/cli/arg_parsing.py:928`），`_prevalidate_upload_filing_request` 机械投影全部 occurrence（`dayu/cli/commands/fins.py:683`）；LLM tool 单值映射为 0/1 元 tuple、material 携带 primary 在 union 边界 ValueError（`dayu/fins/tools/upload_tools.py:364-389`）。Fins README "只保留调用方声明的 selector 次数，不提前选择角色" 属实。
- validated：`_select_fins_upload_filing_files`（`ingestion_runtime.py:993`）单文件隐式/显式、多文件显式唯一 primary + 保序 companions；`FinsUploadFilingFiles.for_upsert` 只接收已确定角色，`from_upsert_paths` 仅存于 material contract（`upload_format_contract.py:486`，符合计划"material 保持现状"）。全仓扫描无 `paths[0]`/`files[0]` 首项分配残留，三个 README 无"首项/首个 primary"旧事实（仅剩 ticker CSV"首项是主代码"与 material 语义，均非 filing primary）。

### CLI/tool usage failure

- CLI：`_prevalidate_upload_filing_request` 在 `FINS_DIRECT_SERVICE_FACTORY` 之前调用（`dayu/cli/commands/fins.py:239-243`），`FinsUploadUsageError` → `EXIT_USAGE_ERROR=2`（`exit_codes.py:10`）一行 stderr。六个新 closed code 全部进入 `_USAGE_MESSAGES`（`ingestion_runtime.py:798-803`），消息无本地路径、≤240 字符。
- 测试 `test_upload_filing_usage_matrix_precedes_service_factory_and_workspace_mutation` 冻结矩阵覆盖：重复 `--primary` 两个 occurrence（argparse 未吞证据，validator 报 `MULTIPLE_PRIMARY_SELECTORS`）、multi 缺 primary、primary 集合外、duplicate files，断言 exit 2、factory/service/stream 零调用、workspace tree 不变。与根 README 的 usage 清单逐项对应。
- tool：`FinsUploadUsageError(ValueError)` 在 `except ValueError` 归一为 invalid_argument failed outcome（`upload_tools.py:117-124`），发生在 `prepare_observed_upload` 的 observation 创建前；测试 `test_upload_tool_invalid_primary_fails_before_state_or_observation_registration`、`test_upload_tool_material_primary_failure_is_owned_and_has_zero_side_effects`、`test_upload_tool_delete_rejects_unnecessary_files_before_job_creation` 证实。

### 100 文件边界

- `TOO_MANY_FILES` 在 `len(request.files) > _MAX_TUPLE_ITEMS(=100)` 触发（`ingestion_runtime.py:150,1069`），先于 delete/files 组合与任何 path normalize；delete 精确 precedence 为 TOO_MANY_FILES → FILES_NOT_ALLOWED_FOR_DELETE → PRIMARY_NOT_ALLOWED_FOR_DELETE（`ingestion_runtime.py:1092-1096`），有测试 `test_filing_delete_over_raw_limit_precedes_files_rejection`。
- 100 接受/101 拒绝有测试 `test_filing_file_count_limit_counts_raw_entries_before_duplicates`（`tests/fins/test_fins_ingestion_runtime.py:1989`：100 accepted，101 先计 raw 再判 duplicate）；`test_filing_one_hundred_originals_publish_with_one_conversion`（`tests/fins/test_docling_upload_service.py:2965`）断言 primary 位于 index 73（非首项）时 stored=100、entries=101、converter 恰好一次、`primary_document` 精确等于 derived identity。根 README "恰好 100 个不同文件允许上传" 属实。

### same basename/stem collision-free publication

- filing identity = `original-` + sha256(namespace + NUL + posix(normalized_path)) + lower suffix（`docling_upload_service.py:1090-1114`），derived = original identity + `_docling.json`（`1117-1132`）；同 request 内 identity 唯一性 fail-closed 在 converter/batch 前（`1135-1153`，测试 `test_filing_generated_identity_collision_fails_before_converter_and_batch`）。不同目录同 basename、同 stem 不同后缀均可共存；测试 `test_filing_same_basename_assets_are_collision_free_and_path_private`（1244-1249 精确断言 entry 元数据）与 `test_filing_identity_is_stable_and_request_order_independent` 证实。根 README 与 Fins README 表述属实。

### original filename / derived association

- original entry：`original_filename=file_path.name`、`derived_from=None`；derived entry：`original_filename` 继承、`derived_from=exact original identity`（`docling_upload_service.py:851-858`，测试 1427 精确断言三字段 tuple）。`file_uploaded.name` filing 用 `original_filename`（`569-573`），`conversion_started.name` 用 basename（`818-826`）。Fins README "basename 只通过 `original_filename` 保存为业务可读文件名" 属实。

### only explicit primary conversion/process

- `_prepare_upload_selection` filing 分支：`converter_inputs = (selection.require_primary(),)`（`docling_upload_service.py:1075`），companions 无 `conversion_started`、无 derived。`process_filing`（`_preprocess_one_document`，`ingestion_runtime.py:5498`）与 read runtime（`dayu/fins/tools/read_runtime.py:3042`）只消费 `snapshot.get_primary_source()`，生产代码未改；spy 测试（`test_fins_ingestion_runtime.py:6711-6760`）断言 processor 只收到 primary derived 内容，`test_read_runtime_consumes_exact_opaque_primary_instead_of_original_or_companion` 覆盖 read runtime。测试 `test_filing_converts_only_primary_and_publishes_all_companions` 证实 companions 全部发布但不转换。

### atomic zero publication

- `_store_upload_assets` 在 caller-owned batch 内 blob-first 写全部资产、单次 `_create_source_document` 写入 files+`primary_document`（`docling_upload_service.py:547-601`）；`primary_document` 由 `_build_pending_assets` 的唯一 derived identity 直接给出（`858-859`），不扫描 pending 或 stem 反推。primary conversion 失败发生在 `begin_batch` 前（`FinsUploadFailureError`），`stored_file_count` 只计 `_ASSET_SOURCE_ORIGINAL`。测试覆盖 primary conversion 失败零发布（`test_corrupt_primary_with_valid_companions_fails_without_publication`）、第 N 次 blob/final/commit 失败 rollback 且 existing tree SHA 不变、`primary_document` 精确命中（`test_primary_uri_owner_requires_exact_explicit_primary_name`、`test_fins_ingestion_runtime.py:6701-6702`）。根 README "失败时不会留下只发布一半的结果" 属实。

### fingerprint move identical-skip / rename update

- filing fingerprint 只含 `original_filename`、sha256、size、source，明确排除 path identity 与请求顺序（`docling_upload_service.py:1223-1249`）；material 分支保持原 name 公式。测试 `test_filing_fingerprint_excludes_path_identity_but_tracks_filename_and_content`（2846）完整证明：同 basename 同内容跨目录 → `skipped` 且 `document_version` 保持 v1；改名同内容 → update v2；内容改变 → update v3。Fins README 表述属实。同 fingerprint 字段完全相同的重复条目排序稳定性不影响序列化（条目全等 → JSON 相同），无顺序敏感问题。

### material unchanged

- material 三个生产分支保持：`name=file_path.name`、`original_filename/derived_from=None`、derived name=`<stem>_docling.json`、逐项转换、首 derived 为 primary、`file_uploaded.name=asset.name`、fingerprint 原公式、entry 不写新字段（`_build_stored_file_entry` 仅在 `SourceKind.FILING` 分支追加，`1626-1629`）。回归测试 `test_execute_upload_material_converts_every_selected_file`（1535）与 1586-1587（material entries 不含 `original_filename`/`derived_from`）、material conversion failure 部分产物丢弃（1641）、material cancellation（1596）均存在。三份 README 对 material 的表述与计划"不把 filing schema 扩到 material"一致。

### 首项 primary 残留 / 未实现承诺 / 内部术语泄漏 / 虚假 UF-PF evidence

- 首项残留：生产代码与三个 README 扫描干净（`from_upsert_paths` 仅剩 material；"首项"仅指 ticker CSV 主代码与 material 语义）。Slice 4 artifact 声称的 `tests/README.md` 旧残留修复可在 diff 中确认（旧句"Fins 把 filing 首项建模为 primary"已替换为 raw selector/validated selection 表述）。
- 未实现承诺：未发现。README 未预写迁移、UF-FIX08/10/11 或未落地能力；`upload_filings_from` 仍生成单文件命令、不携带 `--primary`（`dayu/cli/commands/fins.py:380-410`），与计划非目标一致。
- 内部术语泄漏：LLM-facing 文本 `filing_primary`/`upload_tool_primary` 由同一 `_project_filing_primary_rules` 生成（`upload_format_contract.py:571-607`），只含单文件省略、多文件恰好一个、membership、顺序非角色、delete/material 禁止规则，无 Python 类型名、digest 或 Host 术语；测试逐字断言两字段与规则同源（`tests/fins/test_upload_format_contract.py:291-369`、`tests/fins/test_fins_ingestion_tools.py:1419-1422`、`tests/cli/test_arg_parsing.py:402`）。tool schema `files.maxItems=100` 已落地并有断言。
- 虚假 UF-PF evidence：三份 README 与 Slice 4 artifact 均未声称 UF-PF07/UF-PF12 已执行或通过；artifact 明确分类为 `assigned to later explicitly authorized evidence gate`，符合 plan 禁止边界。

### 验证记录复核（本 review 实测）

- affected pytest（plan Slice 4 13 文件命令）：`1358 passed, 1 skipped, 3 warnings`（本机 35.71s），与 artifact 记录一致；skip 为 optional real Docling env gate。
- 全仓 pyright：`0 errors, 0 warnings, 0 informations`，与 artifact 一致。
- branch coverage（同一 13 文件 suite + `--branch`）：六文件实测 88% / 89% / 99% / 81% / 92% / 85%，全部通过 `--fail-under=80`，与 artifact 表格完全一致；coverage 下 pytest 复跑 `1358 passed, 1 skipped`。
- `git diff --check` 无输出。scope 复核：workspace 仅 `M README.md`、`M dayu/fins/README.md`、`M tests/README.md`、`?? docs/gateflow/uf-fix07-slice4-implementation-20260815.md`，无 registry/oracle/scenario/frozen evidence 修改，无 commit/PR 动作。

## Open Questions

无。

## Residual Risk

- UF-PF07、UF-PF12 真实 mandatory evidence 未执行（plan 明确禁止边界，归属后续授权 evidence gate）；本 review 只验证 deterministic owner 测试与 README 投影一致性。
- optional real Docling integration 环境 gate 未启用（1 skipped），真实转换器行为不在本 slice 证据内。
- 冻结 UF-F11–F13/UF-D04–D06/UF-C05–C06/UFS-006 argv 不含 `--primary` 的 staleness、`UF-A14-delete-with-files-ignored` 与 `UF-PF03.blocked_by_finding_ids` 的 registry 更新，均为 plan §12 已分类的后续 work unit 事项，本 slice 未触碰。
- material duplicate-path 处理仍保持现状（accepted scope boundary），不在本 work unit 范围。
