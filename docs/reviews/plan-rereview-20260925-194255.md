# Plan Re-review：cninfo 空公告列表解析修复（fix 后复核）

- Review target: `docs/gateflow/cninfo-empty-announcement-parse-plan-20260925.md`（fix 后版本）
- 前次 review: `docs/reviews/plan-review-20260925-194055.md`（结论 `fail`，2 findings）
- 日期：2026-09-25 19:42:55（本机系统时钟）
- 范围：仅复核 2 个 accepted findings 的修复是否落实、是否引入新问题

## Finding 最终状态

| Finding | 严重程度 | 建议改法 | fix 后状态 |
|---|---|---|---|
| 1. 终态必现 `Fins missing periods` 未纳入验收预期与交付解释 | 中 | §9 写明预期行 + 解释口径 + 禁止越权修 missing；§12 加解释项 | **已修复** |
| 2. 元数据核验字段真源未钉死 | 低 | §9 钉死取值口径与来源字段标注 | **已修复** |

## 复核证据

**Finding 1**：

- §9 现已写明：终态预期出现 `Fins missing periods: H1,Q1,Q3`；语义为“主源在该 filing 窗口没有这些财期的候选”
  （既有 designed semantics，非 document outcome）；属可解释缺失；**禁止**为消除该行修改期间策略 / missing 投影 /
  workflow 语义。与建议改法逐项一致。
- §12 交付格式已加入该解释项，口径与 §9 一致。
- §11 风险表新增一行，分类为 `covered by current slice`（解释口径钉死），分类正确。

**Finding 2**：

- §9 现已钉死：`source_fingerprint` / `pdf_sha256` 直接取 meta 同名字段（引用 `cn_download_source_upsert.py:187-191`
  真源）；SHA 校验 = 对 meta 指名 PDF 重算比对；财务报表正文路径取 meta primary 文档字段，“不得从 workspace 目录
  结构反推”；revision 取 meta/仓储真源；交付逐项标注来源字段。与建议改法一致。

**新问题检查**：fix 仅补充验收口径与核验映射，未改设计、slice、测试范围；无 goal drift、无新增耦合、无新增假设。

## Open questions

- 无。

## Residual risks

与前次 review 的 residual risks 表一致（含 fix 后新增的“missing periods 解释口径”已 covered），无新增、无未分类项。

## Final plan review conclusion

`pass`

两个 accepted findings 均已修复并有 plan 文本证据；plan 现为 code-generation-ready，可进入
`accepted plan commit` 与 implementation。
