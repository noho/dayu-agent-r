# PR 58 Fix - AgentCodex - 2026-05-16

## Scope

- PR: `https://github.com/noho/dayu-agent-r/pull/58`
- Accepted finding: `PR58-F1 RuntimeFileLock.__exit__ release failure clears active token too early`
- Controller adjudication: `docs/reviews/pr-58-review-controller-adjudication-20260516.md`

## Fix

- Updated `RuntimeFileLock.__exit__` so it calls `token.release()` before clearing `self._active_token`.
- `self._active_token` is now cleared only after `release()` returns successfully.
- Added a runtime test that simulates token release failure and verifies:
  - `RuntimeFileLock.__exit__` propagates the release error.
  - The active token remains attached to the lock instance after release failure.
  - A later `acquire(timeout_seconds=0)` on the same instance fails fast through the active-token guard instead of attempting a fresh underlying acquire.

## Validation

```bash
source .venv/bin/activate && pytest tests/runtime/test_filelock.py -q
```

Result: `13 passed in 0.05s`

```bash
source .venv/bin/activate && pytest tests/host tests/runtime -q
```

Result: `530 passed in 7.14s`

```bash
source .venv/bin/activate && python -m pyright dayu/ tests/ utils/
```

Result: `0 errors, 0 warnings, 0 informations`

```bash
git diff --check
```

Result: passed with no output.

## Residual Risk

- This fix covers the accepted runtime file lock state-consistency defect only.
- Deferred PR 58 findings and test gaps listed in the controller adjudication were not changed in this fix gate.
