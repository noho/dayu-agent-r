# Re-Review — UF-FIX10 final deepreview fix

## Scope

- Mode: current changes（只读 re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `047691c8`（S2 accepted commit）
- Output file: `docs/reviews/code-review-20260817-035840.md`
- Included scope: `047691c8` 之后的 workspace 变更（2 文件，+440/-18），即 final deepreview fix 对 `tests/fins/test_sec_pipeline_upload_filing_stream.py` 的补强与 `docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md` 的 gate metadata 更新
- Excluded scope: 全部生产代码（`dayu/` 零 diff）；其它测试文件；README；oracle/scenario/registry/evidence；UF-PF10/UF-PF12
- Parallel review coverage: 无

## 验证目标

按 `docs/gateflow/uf-fix10-final-deepreview-adjudication-20260817.md` 裁决，验证 D-F1、D-F2、D-F3 三项 accepted-low finding 的 test-only 修复是否正确关闭根因，且未引入 scope 违规。

## 结论

**PASS。** 三项 accepted-low finding 的 test-only 修复均正确闭合，未发现实质性问题。修复严格限定在测试文件内，全部使用 production public API/helper 读取 durable facts，无业务逻辑复制、无目录扫描、无 sleep/polling、无生产 hook、无 scope 越界。

## Findings

未发现实质性问题。

### 逐项根因关闭验证

#### D-F1 — multi-file concurrent loser events / winner identity snapshot

- **修复位置**: `test_sec_pipeline_upload_filing_stream.py:2600-2629`
- **根因关闭证据**:
  1. **loser 事件序列精确断言**（2608-2616）：loser event types 精确为 `UPLOAD_STARTED` → 每个 original 各一条 `FILE_SKIPPED` → `UPLOAD_COMPLETED`；`CONVERSION_STARTED` 不出现在 loser 流中（`all(... is not ...)`）。skip event 的 `name` payload 精确匹配 `(primary, *companions)` 文件名顺序。
  2. **prepared identity 一致性**（2600-2603）：两个并发 caller 各自的 prepared identity 由 production `describe_prepared_filing_publication()` 产生（`_PreparedIdentityRecordingDoclingUploadService:388`），`prepared_identities[0] == prepared_identities[1]` 且 `durable.publication_identity == prepared_identity`。
  3. **durable 与 source snapshot 对照**（2618-2629）：经公开 `source.read_source_snapshot()` 读取 durable facts，断言 `revision`、`source_meta`、`primary_filename`、`asset names` 与 prepared identity 精确一致。
- **scope 合规**: `_PreparedIdentityRecordingDoclingUploadService` 是 `DoclingUploadService` 子类，`prepare_upload` 委托 `super().prepare_upload()` 后调用 production helper `describe_prepared_filing_publication()` 记录 identity，无 identity 计算逻辑复制。`pipeline._upload_service = ...` 是 test-held instance 上的注入，非生产 hook。
- **修复质量**: 高。直接证据链完整：production prepare → production descriptor → durable write → public read → exact equality。

#### D-F2 — same-ticker different-filing exact union

- **修复位置**: `test_sec_pipeline_upload_filing_stream.py:2847-2879`
- **根因关闭证据**:
  1. **filing IDs exact union**（2847-2848）：经公开 `source.list_source_document_ids("AAPL", FILING)` 读取 ticker-level inventory，精确等于 `(Q1 document_id, Q2 document_id)` 排序后 tuple，无额外条目。
  2. **per-document source snapshot 与 identity 对照**（2859-2878）：对两个 document 分别经公开 `source.read_source_snapshot()` 读取，断言 `document_id`、`revision`、`primary_filename`、asset count 与 exact asset-name set 与各自 `publication_identity` 一致。
  3. **asset union 精确断言**（2852-2879）：`expected_asset_union` 从两个 `publication_identity.assets` 构建 `(document_id, asset_name)` 对集合；`actual_asset_union` 从两个 source snapshot 构建同构集合；`assert actual_asset_union == expected_asset_union` 精确相等。
- **scope 合规**: 全部读取经公开 `SourceDocumentRepositoryProtocol` 方法，无目录扫描或 raw path 猜测。
- **修复质量**: 高。直接证据链完整：public list IDs → public read snapshot per document → per-asset union equality。

#### D-F3 — concurrent create-overwrite durable rebase invariants

- **修复位置**: `test_sec_pipeline_upload_filing_stream.py:2703-2712`
- **根因关闭证据**:
  1. **两个 commit snapshot 精确记录**（2703-2704）：`_CommitSnapshotTrackingBatchingRepository` 在 `commit_batch` 后经公开 `source_repository.read_source_snapshot()` 读取同版 durable facts，记录 `document_id`、`source_meta`、`revision`。
  2. **fresh meta 保持断言**（2707-2711）：`first_ingested_at`、`created_at`、`source_fingerprint`、`document_version` 在第二次 commit 与第一次 commit 完全一致，证明 rebase 使用了 fresh winner meta。
  3. **revision 递增断言**（2712）：`second_commit.revision != first_commit.revision`，证明 storage owner 写入了新 revision。
  4. **确定性时序**（450-455）：`_begin_lock` + `_first_snapshot_recorded` Event 保证第二个 writer 在第一个 durable snapshot 已记录后才取得 batch，不使用 sleep/polling。
- **scope 合规**: `_CommitSnapshotTrackingBatchingRepository` 是 `TrackingBatchingRepository` 子类，`commit_batch` 委托 `super().commit_batch()` 后经公开 source repository 读取 snapshot，无 rebase 计算逻辑复制。
- **修复质量**: 高。直接证据链完整：real commit → public read snapshot → exact field equality + revision inequality。

### Scope 合规审计

| 检查项 | 结论 | 直接证据 |
| --- | --- | --- |
| 生产代码零变更 | 通过 | `git diff 047691c8 --stat -- dayu/` 零输出 |
| 测试文件限定 | 通过 | diff 仅 `tests/fins/test_sec_pipeline_upload_filing_stream.py`（+440/-18） |
| plan metadata 限定 | 通过 | diff 仅 `docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md`（+18/-18 gate metadata） |
| 无目录扫描 | 通过 | diff 无 `os.walk`/`glob`/`listdir`/`Path.iterdir` |
| 无 sleep/polling | 通过 | diff 无 `time.sleep`/`asyncio.sleep`；`Event.wait(timeout=10)` 是确定性 handoff |
| 无生产 hook | 通过 | `pipeline._upload_service = ...` 是 test-held instance 注入，非生产代码可感知 |
| 无业务逻辑复制 | 通过 | identity 读取经 `describe_prepared_filing_publication()`（production helper）；source snapshot 读取经公开 `SourceDocumentRepositoryProtocol` 方法 |
| 无 generic exception fallback | 通过 | diff 中无 bare `except`/`except Exception` |
| README 零变更 | 通过 | `git diff 047691c8 -- dayu/fins/README.md tests/README.md` 零输出 |

### 独立验证

- 6 个 D-F1/D-F2/D-F3 定点测试：`6 passed, 40 deselected`（exit 0）
- 完整 SEC stream 测试：`46 passed, 3 warnings`（exit 0）
- 全仓 pyright：`0 errors, 0 warnings, 0 informations`（exit 0）

## Open Questions

无。

## Residual Risk

final deepreview（`docs/reviews/code-review-20260817-034314.md`）记录的存量低风险全部维持原 owner，不因本轮 test-only fix 扩大：

| Residual | 分类 / owner | 影响 |
| --- | --- | --- |
| SEC 取消测试的 observation 耦合 | DS S2 review 已分类，维持原判定 | 非 blocker |
| single-terminal 的测试断言强度 | 大部分 stream 测试只断言末事件为 terminal | 非 blocker |
| converter 非确定性 tradeoff | fail-closed contract，README 已记录 | 非 blocker |
| service runtime 动态 monkeypatch fake | 无 batch-read 可达路径 | 非 blocker |
| `_wait_terminal` 既有 sleep 轮询 | 非本 work unit 新增 | 非 blocker |

无未分类 residual risk。
