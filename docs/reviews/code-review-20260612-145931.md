# Code Review（Re-Review）

## Metadata

- Reviewer: AgentDS
- Review type: WU-CM-05-S2 fix re-review
- Previous review artifact: docs/reviews/code-review-20260612-144954.md
- Previous review Finding 1 under review: missing explicit `forward_intents[0].intent_type` invalid enum diagnostic test
- Fix agent: AgentCodex
- Artifact path: docs/reviews/code-review-20260612-145931.md

## Scope

- Mode: current changes（re-review，仅关注 fix 增量）
- Branch: work/cm-05-06-08-09
- Base: S2 working tree state at code-review-20260612-144954.md
- Included scope:
  - `tests/host/test_llm_compaction.py` fix diff：新增 `test_parse_conversation_compact_output_vnext_reports_forward_intent_type_enum_value`（1 个测试函数）
  - `docs/reviews/wu-cm-05-s2-implementation-report.md` Fix Gate Update 章节
- Excluded scope:
  - S2 原始 production diff（`_required_enum` 变更，已在初次 review 中通过）
  - S2 其他 16 个新增/更新测试（已在初次 review 中通过）
  - `docs/host/issues-implementation-control.md`（Controller bookkeeping）
- Parallel review coverage: 无

## Verification

- **pyright**: 0 errors, 0 warnings, 0 informations（dayu/host/llm_compaction.py, tests/host/test_llm_compaction.py）
- **pytest**: 37 passed in 0.27s（S2 前次 review 时为 36 passed；+1 为新增 `intent_type` 测试）
- **fix diff 范围**: 仅 `tests/host/test_llm_compaction.py`（+1 测试函数），生产代码 `dayu/host/llm_compaction.py` 未变

## Finding 1 修复验证

### 1-已修复-forward_intents[0].intent_type 枚举值诊断测试已补齐

- **原始 Finding**: docs/reviews/code-review-20260612-144954.md Finding 1 指出 4 个 enum 字段中仅 `evidence_kind`、`status`、`reason` 有显式枚举值诊断测试，`intent_type` 缺少对应测试
- **fix 内容**: 新增 `test_parse_conversation_compact_output_vnext_reports_forward_intent_type_enum_value`（test_llm_compaction.py:480-507）
- **直接证据**:
  - 测试通过 public entry point `parse_conversation_compact_output_vnext(compact_input, json.dumps(...))` 调用（line 504-507），与 S2 其他 enum 测试保持一致入口规范
  - 测试设置 `intent_type: "bad_intent_type"`（line 494），合法 status `"pending"`（line 496）
  - match pattern 为 `r"forward_intents\[0\]\.intent_type.*bad_intent_type"`（line 502），同时断言字段路径和非法值
  - 测试 docstring 明确说明"返回完整路径与非法值"（line 481）
- **修复状态**: 已修复

## S2 Enum Diagnostic Coverage 自洽性

4 个 enum 字段全部有显式路径+非法值诊断测试：

| enum 字段 | 测试函数 | match pattern | 覆盖 |
|---|---|---|---|
| `evidence_kind` | `test_..._reports_fact_evidence_kind_path` | `evidence_backed_facts[0].evidence_kind.*bad_evidence_kind` | ✓ |
| `intent_type` | `test_..._reports_forward_intent_type_enum_value` | `forward_intents[0].intent_type.*bad_intent_type` | ✓ |
| `status` | `test_..._reports_forward_status_enum_value` | `forward_intents[0].status.*bad_status` | ✓ |
| `reason` | `test_..._reports_reference_reason_enum_value` | `reference_continuity_items[0].reason.*bad_reason` | ✓ |

4/4，覆盖自洽。

## Fix 范围检查

- fix 仅新增 1 个测试函数，未改生产代码（`dayu/host/llm_compaction.py`），未改已有测试逻辑
- 新增测试与 S2 其他 3 个 enum 测试保持相同的结构模式：构造合法 proposal → mutate 特定 enum 字段为非法值 → `pytest.raises` 断言 `LLMCompactionProposalError` + `字段路径.*非法值` regex
- 未引入新 import、新 helper、新 fixture

## 新增 correctness/stability/maintainability 问题检查

无。纯测试添加，不改变任何生产语义，不引入新类型错误、新耦合或新维护负担。

## Open Questions

无。

## Residual Risk

- 与原始 review 一致的 residual risks 保持有效：DS-F02（`_proposal_json` 使用 `cast`）延期至 Slice 3；`redact_sensitive_diagnostic_values` 仅覆盖凭证类敏感值。
- 新增测试无额外 residual risk。

## Conclusion

- **结论**: PASS
- Finding 1 已修复，fix 仅触及预期测试范围，S2 enum diagnostic coverage 自洽，无新增问题，pyright/pytest 验证通过
- **Remaining findings**: 初次 review 记录的 4 项 Residual Risk 保持不变，均已在 plan 中明确延期至 Slice 3 或评估为可接受风险
