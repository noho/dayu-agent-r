# Code Review

## Scope

- Mode: current changes
- Branch: `phase/wu-cm-15`
- Base: `97518e93` (accepted plan commit)
- Output file: `docs/reviews/code-review-20260620-112301.md`
- Included scope: workspace diff of `utils/smoke_host_public_conversation_memory_scenarios.py`, `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`, `docs/host/issues-implementation-control.md` against plan commit `97518e93`.
- Excluded scope: production `dayu/host/`, `dayu/engine/`, `dayu/runtime/`, `dayu/service/`, prompt assets, durable schema, config files.
- Parallel review coverage: 无。

## Findings

### 1-未修复-低-`_compact_pressure_reserve_tokens` 成为死代码

- **入口/函数**: `_compact_pressure_padding_with_reserve` (line 4233)
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:4287`
- **输入场景**: 任何 suite 调用 `_compact_pressure_padding` 或 `_fallback_compact_pressure_padding`
- **实际分支**: `_compact_pressure_padding` 被重构为直接委托 `_compact_pressure_padding_with_reserve`，将 `_COMPACT_PRESSURE_RESERVE_TOKENS` 作为 `reserve_tokens` 传入
- **预期行为**: 重构后，不再使用的 `_compact_pressure_reserve_tokens` 应被删除
- **实际行为**: `_compact_pressure_reserve_tokens` (line 4287) 仍然保留，但整个文件中无任何调用点。`grep -c` 确认只出现 1 次（定义处）
- **直接证据**: diff 删除了 `_compact_pressure_reserve_tokens(context_window_size=policy.context_window_size)` 的调用（原 line 2828 附近），替换为 `_compact_pressure_padding_with_reserve` + 固定常量 `_COMPACT_PRESSURE_RESERVE_TOKENS`。函数定义 (line 4287) 未被删除
- **影响**: 不影响运行时行为，仅增加维护噪音
- **建议改法和验证点**: 删除 `_compact_pressure_reserve_tokens` 函数定义。运行 pyright 确认无引用
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-compactor runner 私有 API monkey-patch 产生维护耦合

- **入口/函数**: `_patched_compactor_runner` (line 1835)
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:1835`
- **输入场景**: deterministic suite 执行时，通过 context manager 替换 `llm_compaction._run_agent_request`
- **实际分支**: `llm_compaction._run_agent_request` 被临时替换为 smoke-local `_AcceptingSmokeCompactorRunner` 或 `_RejectingSmokeCompactorRunner`，`finally` 块恢复原值
- **预期行为**: patch 边界应足够稳定，不随生产代码重构静默失效
- **实际行为**: patch 目标是 `dayu.host.llm_compaction` 模块的私有函数 `_run_agent_request`。如果该模块内部重命名或移动此函数，monkey-patch 会静默指向不存在的属性或失效，而 smoke 仍可能因其他原因通过
- **直接证据**: line 1841 `llm_compaction._run_agent_request = runner`；line 1844 `llm_compaction._run_agent_request = original_runner`。`tests/host/test_public_compact_smoke.py` 也使用相同 patch 路径
- **影响**: 维护耦合，非运行时正确性问题。smoke 本身不修改生产代码，且 plan 明确接受此 trade-off（"If review determines that runtime patching the compactor runner boundary is unacceptable... the implementation must stop and report blocker"）
- **建议改法和验证点**: 当前方案可接受，但应在 smoke 中添加一个最小 sanity check：patch 后验证 `llm_compaction._run_agent_request` 确实指向注入的 runner，而非静默失败。如果未来 Host 提供公开 compactor runner seam，应迁移
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

无。

## Residual Risk

- **`memory-compact` real-provider 残留**: implementation artifact 记录 `DEEPSEEK_API_KEY=test-provider-key` 运行 `--suite memory-compact` 时，真实 compactor provider 认证失败导致 `compacted_proactive=0`、`failed_proactive=2`。这不是本次实现引入的回归——确定性 reactive/fallback suite 不依赖真实 provider。此残留的分类合理：它是 `memory-compact` 现有 real-provider smoke 的环境依赖问题，不属于 WU-CM-15 scope。
- **compactor stub 单元覆盖**: `_AcceptingSmokeCompactorRunner` 和 `_RejectingSmokeCompactorRunner` 的 JSON proposal 生成逻辑（`_fake_compaction_proposal_from_material_json`、`_invalid_current_anchor_citation_proposal`、`_material_json_from_compactor_prompt`）没有直接单元测试。它们通过 smoke integration flow 间接覆盖。考虑到这些是 smoke-local deterministic stub，风险可接受。
- **并发安全**: `_DeterministicCompactWorkerFactory.requests` 和 `.snapshots` 是可变 list，由 `_DeterministicCompactWorker.accept` 追加。当前 smoke 脚本是顺序执行，不存在并发风险。如果未来 smoke 改为并行 dispatch，需要加锁或改为线程安全容器。
