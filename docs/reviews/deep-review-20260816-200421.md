# Aggregate Deep Review Re-review（R1 修复复核）：UF-FIX08 existing-source-auto-repair

## Scope

- Mode: current changes（aggregate deepreview re-review gate，第二轮）
- Branch: `codex/upload-filing-oracle`
- Base: `5859856e46af42c9ae5a2a5c07fab1ba59dc91d3`
- HEAD: `65d352e6`；workspace 未提交 fix：`dayu/fins/storage/_fs_source_integrity.py`、`tests/fins/test_fins_storage_atomicity.py`、`tests/fins/test_sec_pipeline_download.py`
- Output file: `docs/reviews/deep-review-20260816-200421.md`
- 输入：`docs/gateflow/uf-fix08-existing-source-auto-repair-aggregate-rereview-fix-20260816.md`（R1 fix artifact）、`docs/gateflow/uf-fix08-existing-source-auto-repair-aggregate-deepreview-fix-20260816.md`、上一轮本 reviewer 的 `docs/reviews/deep-review-20260816-195058.md`（R1 的 source）；未读 AgentMiMo 新产物（194657 仅按其 fix artifact 中「首版 PASS 未覆盖 R1、不作为 gate pass」的只读记录知悉其存在）；4 份既有 review artifacts 保持只读未改写
- 主 reviewer 独立验证动作（全部亲自执行）：
  - 8 个新增节点（6 owner + 2 SEC 参数化）：`8 passed`
  - 相关 matrix（storage_atomicity + SEC/CN download + docling service + processor consistency）：`558 passed, 3 warnings`
  - 全仓 pyright：`0 errors, 0 warnings, 0 informations`
  - 4 场景 storage 层实证实验（clean / empty+untrusted / nonempty+untrusted / trusted dangling）
  - 真实 SEC pipeline 两参数实证实验（trusted_dangling / untrusted）
  - scope/frozen guards、`git diff --check`
- Parallel review coverage: 无子代理；全部由主 reviewer 逐行走读 + 实证实验完成

## 结论

**PASS**。R1 已修复且经真实代码路径实证闭环；本次 fix 的 gate 条件精确、不误杀 clean workspace、不回退 trusted-dangling synthetic 路径、不改变 repair/revision/guard/commit 语义。UF-FIX08 aggregate deepreview 至此无未闭合 finding，全部 residual 已分类 owner。

## Findings

未发现新的实质性问题。原 R1 最终状态与验证证据如下：

### R1（untrusted manifest + 空 inventory 的 whole-kind 公开 fail-open）— 最终状态：已修复（closed，无 residual）

- **修复核实**：`_inspect_source_kind_unguarded` 在 `_apply_manifest_facts` 之后、canonical/target/repair-blocked 派生之前新增封闭 gate（`_fs_source_integrity.py:322-331`）：`external_document_id is None`（whole-kind）且 `inventory` 为空且 `shared_manifest_reasons` 含 `SOURCE_MANIFEST_UNTRUSTED` → 抛 `SourceIntegrityPreflightError(UNSAFE_PUBLICATION)`。修复方向与本 reviewer 上轮给出的 owner/fix direction 一致（复用 `unassignable_root_fact` 的 typed raise 路径，不伪造 synthetic identity）
- **条件精确性验证（主 reviewer 实证 + 代码走读）**：
  - S1 root/manifest 均 missing 的 clean workspace：`list_source_integrity=()`、preflight=clean，未被误杀（该场景 shared reasons 为空，gate 不触发）✓
  - S2 empty + untrusted manifest：`list_source_integrity` 抛 `SourceIntegrityPreflightError(unsafe_publication)`；exact 仍 `UNSAFE/revision=None/SOURCE_MANIFEST_UNTRUSTED` ✓
  - S3 nonempty + untrusted manifest：逐 item 投影 `UNSAFE/revision=None/source_manifest_untrusted`，preflight 经 item 抛 `unsafe_publication`（gate 不触发，行为不变）✓
  - S4 trusted dangling（M2 synthetic 路径）：inventory 含 synthetic `UNSAFE` item、preflight 抛 typed error——不回退 ✓
  - 其它含 `SOURCE_MANIFEST_UNTRUSTED` shared reason 的分支（1122-1127 悬空、1151-1154 多源 mismatch）inventory 均非空，gate 不触发；`manifest missing + 空 inventory` 返回空 shared reason，不触发 ✓
- **下游影响复核**：repair 路径 material whole-kind inspection 的 raise 被既有 `except SourceIntegrityPreflightError → CROSS_SOURCE_PUBLICATION_UNSAFE` 精确转换（`_fs_source_document_core.py:463-466`），语义不变；commit validator 该场景失败类型从裸 ValueError 变为 typed preflight error（同为 fail-closed，且正是 designated type）；publication guard 无新锁交互（raise 发生在 caller-held guard 内）；public API 签名、revision 比较、batch capability 均未变 ✓
- **SEC 真实 workflow 实证（两参数）**：`trusted_dangling` 与 `untrusted` 均抛 `SourceIntegrityPreflightError(unsafe_publication)`，provider 调用 0、company meta 不存在、rejection 不存在——零副作用且无裸 ValueError ✓
- **测试核实**：owner 测试 `test_empty_inventory_with_untrusted_manifest_raises_whole_preflight`（filing/material，真实 fs 注入：移动 source 目录 + 写坏 manifest）断言 exact UNSAFE/no revision + whole typed raise；SEC 测试 `test_sec_empty_inventory_manifest_preflight_has_zero_mutation` 参数化 trusted_dangling/untrusted 断言 typed 异常 + provider/company/rejection 零副作用 + 旧字节不变。8 节点全部真实命中并通过

## 其余 findings / residual 状态（与上一轮 artifact 一致，未变化）

| 项 | 最终状态 |
| --- | --- |
| M1（异源 descriptor） | 证据失效（closed）：`_fs_identity.py:280-282` 双向校验 + filing/material exact/staged/whole 回归 |
| M2（trusted dangling） | 已修复（closed）：synthetic UNSAFE 去重排序、不可误判可修复、exact/list/preflight/commit 同源 fail-closed |
| Finding 3/4（docstring） | 已修复（closed） |
| Finding 5（_ordered_reasons） | rejected-with-reason（无 residual） |
| Finding 6（RuntimeFileLockError） | deferred-with-owner：Fins upload operational failure projection owner |
| Finding 7（validated request hardening） | deferred-with-owner：Fins validated-request contract hardening owner |
| Finding 8（download terminal projection） | deferred-with-owner：future general download failure projection owner |
| L1-L15 | rejected-with-reason（复核成立） |
| 多悬空 ID + nonempty inventory 合并排序无直接节点 | 由集合差 + 稳定排序实现覆盖；storage coverage hardening（可选） |

## Open Questions

- 无

## Residual Risk

| 风险 | 分类 | owner / destination |
| --- | --- | --- |
| 一般同请求并发 success/skip 收敛 | assigned | UF-FIX10 |
| fresh company meta warning | assigned | UF-FIX11 |
| material existing-source repair / 旧 schema migration | assigned | 后续独立 work unit |
| UF-PF08/UF-PF12 真实 evidence 与 registry/oracle 状态 | assigned | evidence / registry adjudication work unit |
| 一般 download typed terminal projection | assigned | future general download failure projection owner |
| raw runtime lock operational failure projection | assigned | Fins upload operational failure projection owner |
| 手工构造 validated request 强化 | assigned | Fins validated-request contract hardening owner |
| manual filesystem writer 绕过 repository lock | assigned | storage operational policy |
| 多悬空 ID + nonempty inventory 排序直接节点、SEC/CN Phase B churn 真实注入、material exception conversion 直接节点、repair 专用 barrier | covered by deterministic code / existing mechanism tests | storage / download coverage hardening |

## Validation 记录

- 8 个新增节点：`8 passed, 3 warnings`（主 reviewer 亲自运行；warnings 为第三方 edgar deprecated imports）
- 相关 matrix：`558 passed, 3 warnings`（主 reviewer 亲自运行）
- 全仓 pyright：`0 errors, 0 warnings, 0 informations`（主 reviewer 亲自运行）
- `tests/fins` 全量 1859 passed 复用 rereview-fix artifact 记录（按用户授权复用 full-suite evidence）
- 实证实验输出（storage 4 场景 + SEC 2 参数）已写入上文；与测试断言一致
- scope/frozen guards：tracked diff 仅 3 个授权文件；untracked 仅 2 个 gateflow fix artifacts + 4 份 review artifacts（只读）；oracle/scenario/Host/Engine design、Host/Engine/Service/CLI 生产零 diff；`git diff --check` 通过
- 未运行 coverage、真实 CLI/provider/converter evidence；未修改 production/tests、未改写旧 artifact、未 commit/push/PR
