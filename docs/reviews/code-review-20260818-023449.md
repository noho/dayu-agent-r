# Code Review

## Scope

- Mode: current changes（UF-FIX01 S2 review-fix 后 re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `f6b2d04c`
- Output file: `docs/reviews/code-review-20260818-023449.md`
- Included scope:
  - `dayu/fins/tools/upload_tools.py`（schema description 变更）
  - `tests/fins/test_fins_ingestion_tools.py`（recording runner 装配、canonical boundary 测试、schema 断言、helper 签名扩展）
  - `tests/cli/test_fins_commands.py`（CLI 三市场非法财期 + canonical handoff 测试）
  - `README.md` / `dayu/fins/README.md` / `tests/README.md`（contract 文档更新）
- Excluded scope: S1 已提交内容、`ingestion_runtime.py`、`docling_upload_service.py`（不在本 diff 范围）
- Parallel review coverage: 无
- 验证对象：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-s2-code-review-adjudication-20260818.md` 的四项 accepted findings

## Adjudication Verification

### Accepted Finding 1（DS 001）：schema filing/material 限域 — ✅ 通过

- **变更**：`upload_tools.py:253-259` — `fiscal_period` description 从「财报期间，例如 FY、Q1、Q2、Q3 或 Q4。上传 filing 时必填；上传 material 时可选。」改为「财报期间。上传 filing 时必填且只支持 FY、H1、Q1、Q2、Q3、Q4；上传 material 时可选。」
- **验证**：六值闭集精确限域到 filing；material 仅保留 optional 声明。未添加 `enum` 字段（MiMo 001 rejected-with-reason 裁决一致）。schema test `test_upload_tool_calendar_year_schema_and_usage_messages_are_business_neutral` 行 1718-1720 新增 exact description 断言。`tests/README.md` 同步更新。
- **直接证据**：`upload_tools.py:255-258`、`test_fins_ingestion_tools.py:1718-1720`

### Accepted Finding 2（MiMo 002）：invalid 已装配 runner 零调用 — ✅ 通过

- **变更**：`_runtime_with_static_admission_guard` 行 2726 新增 `_RecordingFinsUploadRunner()` 装配；测试行 1436-1438 将 `assert runtime.upload_runner is None` 替换为 `isinstance(upload_runner, _RecordingFinsUploadRunner)` + `assert upload_runner.calls == ()`
- **验证**：runner 已装配但 calls 为空，证明非法输入路径未触及 runner。原有 `_ForbiddenFilingUploadStateRepository`（state read 禁止）、`executor.submitted_job_ids == ()`（executor 零提交）、`runtime._observations == {}`（observation 零创建）、workspace 快照不变（零文件 mutation）四重断言保持。
- **控制流验证**：非法 `fiscal_period`（如 `"BANANA"`）经 `_upload_request_from_arguments`（只做 `strip()` 检查非空）成功构造 `FinsUploadFilingRequest`，进入 `prepare_observed_upload` → `_validate_runtime_upload_request` → `_validate_fins_upload_filing_static` → `normalize_fiscal_period` 抛出 `ValueError`，由 `except ValueError` 捕获返回 `ToolFailedOutcome`。runner 已装配但从未被调用，converter 不可达。
- **直接证据**：`test_fins_ingestion_tools.py:2726`（runner 装配）、`test_fins_ingestion_tools.py:1436-1438`（零调用断言）、`upload_tools.py:102-117`（ValueError catch）

### Accepted Finding 3（DS 002）：valid runner-boundary canonical 与 observation cleanup — ✅ 通过

- **变更**：新测试 `test_upload_tool_raw_period_is_canonical_at_runner_contract_boundary` 行 1444-1510
- **验证**：
  - 使用 `_HoldingExecutor`（不立即执行后台任务）+ `_RecordingFinsUploadRunner`（实现 `FinsUploadRunner` Protocol）
  - tool 调用返回 `ToolAwaitingOutcome` 后，手动 `activate_observation` → `executor.run_next()` → 断言 `upload_runner.calls[0]`
  - 断言 `isinstance(validated_request, ValidatedFinsUploadFilingRequest)` + `validated_request.normalized_fiscal_period == expected_period`
  - `finally: runtime.abandon_observation(record.handle)` 确保 observation 清理
  - 最终 `assert runtime._observations == {}` 验证清理完成
- **私有表示 pin 已删除**：无 `_DirectUploadProducer` import、无 `record.producer` 链访问、无 raw != canonical 反契约断言
- **直接证据**：`test_fins_ingestion_tools.py:1503-1509`（runner contract 断言 + try/finally cleanup）

### Accepted Finding 4（private/raw pin 删除）：— ✅ 通过（与 Finding 3 同一变更）

- `_DirectUploadProducer` import 已删除
- `runtime._observations` → `record.producer` → `producer.request.request` 三层私有链访问已删除
- `!= expected_period` 反契约断言已删除

## Findings

未发现实质性问题。

## Open Questions

- 无。adjudication 中 `deferred-with-owner` 的 `_upload_request_summary` raw fiscal period 问题与 `rejected-with-reason` 的 `--fiscal-period` CLI 前缀问题均为既有行为，不属于本 S2 diff 引入，controller 已裁决。

## Residual Risk

- `_upload_request_summary`（`ingestion_runtime.py:7781`）对 filing 请求使用 `raw_request.fiscal_period`（raw 值如 `"fy"`）而非 `normalized_fiscal_period`（canonical 值如 `"FY"`），durable summary 中财期与 pipeline 持久化的 canonical 值不一致。这是既有行为，adjudication 已裁决为 `deferred-with-owner`。
- `_ForbiddenFilingUploadStateRepository` 的 fail-fast 文案仍写「tool calendar/year admission 前禁止读取 filing state」，未随财期范围更新；只在测试失败时可见，不影响证明强度。
- `_RecordingFinsUploadRunner.run_upload` 返回硬编码 `FinsUploadResultSummary(status="deleted")`；该摘要只用于证明 runner 被调用，不执行真实 converter/storage，符合 accepted finding 要求的边界。
- CLI 入口级大小写覆盖只取小写+首尾空白各一形态（`" fy "` / `" q2 "` / `" h1 "`）；owner 级 `test_normalize_fiscal_period_canonicalizes_case_and_outer_whitespace` 已覆盖 normalize 本身，入口测试只需证明委托。
- `_runtime_with_executor` 新增可选参数 `upload_runner` 默认 `None`，既有调用方不受影响。
- 按声明未运行真实 CLI/网络/Docling calibration。

## Verification Summary

- accepted findings 逐项验证：4/4 通过
- fix 是否引入新 material finding：未发现
- helper 签名变更对既有测试影响：`_runtime_with_executor` 新增可选参数，默认 `None`，backward compatible
- try/finally observation cleanup：正确，确保异常路径也能清理
- 结论：**pass**
