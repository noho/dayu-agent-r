# Code Review

## Scope

- Mode: current changes（Gateflow S1 slice code review）
- Branch or PR: `codex/upload-material-oracle`
- Base: S1 工作区 diff（相对 accepted plan commit `72e5a370`）；branch 上既有 upload-material work unit 的历史提交不在本次 slice 范围
- Output file: `docs/reviews/code-review-20260925-195434.md`
- Included scope: `dayu/fins/downloaders/cninfo_downloader.py`（解析分支 + 模块 docstring）、`tests/fins/test_cninfo_downloader.py`（4 组新增回归测试）
- Excluded scope: branch 上其它 work unit 的既有提交；`workspace/tmp/probe_cninfo_announcement_shape.py`（gitignored 取证脚本）
- Parallel review coverage: 无（diff 小，主 reviewer 全量走读）

## Findings

未发现实质性问题。

主链路逐行走读摘要（entry：`run_cn_download_stream_impl` → `CninfoDiscoveryClient.list_report_candidates`
→ `_query_announcements` → `_query_announcement_page` / `_http_post_form`；消费方 `select_cninfo_report_candidates`）：

1. **入参 → 条件 → 下游**（`cninfo_downloader.py:471-490`）：`_http_post_form` 在 HTTP 非 2xx / JSON 不可解析时
   已分别抛 `HTTP_STATUS` / `JSON 内容无法解析` 协议失败；本次改动只作用于“JSON 已解析成功”后的 shape 判定。
   四分支互斥且顺序正确：缺 key（475-478）→ null（479-483）→ list（484-485）→ 空 list（486-487）。
2. **分支由真实事实驱动**：缺 key 与 null 显式分离（旧 `payload.get(...)` 将两者混同为 None 后一律 raise）；
   判定只消费 `announcements` 字段本身，未借用 `totalRecordNum` 等间接信号（注释仅记录证据，不参与决策）。
   非法 falsy 值（`false`/`0`/`""`/`{}`）不落入空页分支，仍抛协议失败——不构成 loose falsy parsing。
3. **返回值/副作用一致性**：`items is None → break` 与既有 `[] → break` 同路径结束分页、保留已收集公告；
   函数无状态写入；`raw_by_period` 对每个 discovery period 都有 entry，selection 端 `get(period, ())` 消费一致。
4. **semantic ownership drift**：null→空页的 wire 语义归一发生在唯一 owner（响应解析边界），docstring 已记录该
   provider 事实；无下游补偿、无 fallback、无测试固化旧行为（既有 raise 测试均为 HTTP 503 路径，不与新语义冲突）。
5. **测试证明力**：`test_list_report_candidates_null_empty_period_does_not_block_other_periods`（`test_cninfo_downloader.py:1057`）
   复刻事故形态（FY 摘要+正文双公告、H1/Q1/Q3 null、bare 四财期、filing 窗口 2025-01-01~2025-03-31），
   断言收敛到唯一正文 FY2024 候选（`source_id=="FY_BODY"`）；缺 key（:1118）与非 list 非 null（:1155，dict/str 参数化）
   断言 owner 级 contract（`FinsDownloadProviderError` + `PROTOCOL` + `retryable=False` + 安全文案），非仅断言“抛了异常”。
6. **AGENTS.md 约束核对**：中文 docstring/行内注释齐备；无 `Any`/`object`/无类型签名；无魔法值扩张（wire 字段
   字面量与解析器现状一致）；测试风格与文件既有约定一致（嵌套 handler、单行 docstring）。
   `pyright` 对两文件 0 errors；`tests/fins` 2124 passed（1 failed 为沙箱禁 AF_UNIX bind 的环境伪失败，非沙箱通过）。

## Open Questions

- 无。

## Residual Risk

- `announcements: null` 但 `hasMore=true` 或 `totalRecordNum>0` 的矛盾响应会被当空页静默截断；与既有 `[]` 分支同语义，
  实测（两次取证）未出现该形态。若未来出现，需要在解析 owner 引入一致性校验——现不预实现。
  【分类：assigned to later work unit，仅当出现矛盾响应证据时】
- 空页分支不校验 `hasMore` 字段类型（与 `[]` 分支一致的既有行为，本次未引入）。
  【分类：assigned to later work unit，仅当需要收紧契约时】
- pagination page≥2 的 null 形态未单独建测（与 page 1 同代码路径，覆盖价值低）。
  【分类：assigned to later work unit（测试增强），非当前 slice 阻塞项】
- 协议失败仍不记录响应特征（plan 已 deferred）。
  【分类：requiring new issue or explicit user decision，owner：Dayu 维护方】
