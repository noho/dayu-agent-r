# Code Review Re-Review

## Scope

- Mode: re-review
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `main`
- Output file: `docs/reviews/code-review-20260619-144206.md`
- Reviewed artifacts:
  - `docs/reviews/code-review-20260619-143711.md` (AgentMiMo review)
  - `docs/reviews/code-review-20260619-143853.md` (AgentDS review)
  - `docs/reviews/wu-cm-compact-rejected-diagnostic-code-review-adjudication-20260619-144111.md`
- Excluded scope: 不修改代码、不 stage/commit/push

## Finding Status

### AgentMiMo 1 — Recovery tier rejected attempts 不写入 EventLog

- **原严重程度**: 高
- **Adjudication**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit
- **复核状态**: 证据有效，deferred 合理
- **复核理由**: 该 finding 的直接证据（`dispatch.py:1401` 只遍历 `result.rejected_attempts`）仍然成立。但当前 slice 的 boundary 明确限定为 "diagnostic artifact generation and EventLog small-field projection for attempts already emitted as rejected events"。Recovery tier 的 rejected EventLog emission 是一个独立的审计语义问题，涉及跨 tier attempt numbering、failed payload `attempt_count` 语义、stale-result 处理等，超出了 diagnostic artifact wiring 的 scope。当前 diagnostic artifact 主链路（`run_compaction_operation` → `CompactionRejectedAttemptDiagnostic` → `write_compaction_rejected_attempt_diagnostic_artifact` → EventLog payload）已被 `test_rejected_attempt_diagnostic_captures_invalid_previous_reference` 覆盖，不依赖 recovery tier 的 EventLog 写入。Deferred 到后续 work unit 是合理的

### AgentMiMo 2 — Recovery tier 无测试覆盖

- **原严重程度**: 中
- **Adjudication**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit
- **复核状态**: 证据有效，deferred 合理
- **复核理由**: Recovery tier 代码（~180 行）确实无测试覆盖，但这些代码不属于当前 diagnostic artifact slice 的实现范围。当前 slice 的测试覆盖了 diagnostic artifact 的核心路径：`test_rejected_attempt_diagnostic_captures_invalid_previous_reference` 覆盖了 `proposal_manifest_ref=None` 的 material projection failure → diagnostic → artifact → payload 完整链路。Recovery tier 的测试应在其实现 slice 中补充

### AgentMiMo 3 — Session stale recovery result protection

- **原严重程度**: 中
- **Adjudication**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit
- **复核状态**: 证据有效，deferred 合理
- **复核理由**: Session state check 的 TOCTOU 问题是 recovery tier 执行模型的固有属性，当前 slice 不改变 recovery tier 的执行逻辑。Transaction 内的 `_run_session_allows_proactive_compaction` 检查提供了最终防线，stale result 不会被提交为 compacted event。该问题与 Finding 1 同属 recovery tier 审计语义范畴

### AgentMiMo 4 — `_completed_compaction_proposal_attempt_count` 语义不一致

- **原严重程度**: 低
- **Adjudication**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit
- **复核状态**: 证据有效，deferred 合理
- **复核理由**: 该函数是 recovery tier attempt numbering 的组成部分，其语义应在 recovery tier 整体审计设计中统一定义

### AgentMiMo 5 — Recovery tier 共享 operation_id

- **原严重程度**: 低
- **Adjudication**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit
- **复核状态**: 证据有效，deferred 合理
- **复核理由**: 当前因 Finding 1 不写入 recovery tier EventLog，此问题暂不可见。需与 Finding 1 一并解决

### AgentDS 1 — Artifact file orphan after SQL rollback

- **原严重程度**: 中
- **Adjudication**: `deferred-with-owner`，owner = existing storage maintenance / artifact orphan model
- **复核状态**: 证据有效，deferred 合理
- **复核理由**: Plan 已明确将此列为 residual risk。Descriptor 和 EventLog 写入共享同一 SQLite transaction（`write_compaction_rejected_attempt_diagnostic_artifact` 使用调用方的 `transaction`），避免了 descriptor-without-event 的不一致。File-only orphan 是 content-addressed artifact store 的已知 tradeoff，不是当前 slice 引入的新风险

### AgentDS 2 — Duplicated diagnostic offending helper functions

- **原严重程度**: 低
- **Adjudication**: `rejected-with-reason`
- **复核状态**: 证据有效，rejection 合理
- **复核理由**: 6 个 trivial projection helper（每个 ~5 行）在 dispatch.py 和 engine_ingest.py 各定义一次。提取到公共位置会为两处调用创建一个新的公共 helper surface，维护收益不足以证明 API 膨胀合理。当前重复不导致行为差异，且实现 artifact 已将其归类为 "non-blocking helper duplication"。若第三处调用出现或发生行为 drift，应重新评估

### AgentDS 3 — Offending block ordinal boundary guard

- **原严重程度**: 低
- **Adjudication**: `accepted` → `已修复`
- **复核状态**: 已修复，代码验证通过
- **复核理由**: `compaction_operation.py:1802-1806` 已增加边界守卫：`if offending_block.block_ordinal < 0 or offending_block.block_ordinal >= len(previous_blocks): return None`。该守卫防止了负数索引和越界访问，确保 `_offending_block_artifact_json` 在 ordinal 异常时 fail-safe 返回 `None` 而非抛出 `IndexError`。注意：当前无专项测试覆盖此边界守卫（`test_rejected_attempt_diagnostic_captures_invalid_previous_reference` 只覆盖 happy path），但守卫逻辑简单且调用链中 ordinal 始终从同一个 `enumerate(previous_blocks)` 派生，实际触发概率极低

## Open Questions

- 无。

## Residual Risk

- **Offending block boundary guard 无专项测试**: 修复已到位但无直接测试覆盖。建议在后续迭代中补充一个 ordinal 越界的 unit test。
- **Recovery tier 审计语义**: Findings 1-5 的 deferred 项需要在后续 recovery-tier compact audit diagnostics work unit 中统一解决，包括：cross-tier rejected EventLog emission、attempt_count 语义、stale-result 保护、operation_id 归属。
- **Production compact root cause**: `previous reference continuity text is invalid` parser 根因仍未修复，不在当前 scope。
