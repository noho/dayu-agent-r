# Code Review — WU-OBS-00 Slice 1 Implementation Re-Review（第二路独立复核）

## Scope

- Mode: current changes（re-review，非初次 review）
- Branch: `work/wu-obs-00`
- Base: `e1799abc3341872ba19ff609de15b236813a3533`（accepted plan commit）
- Output file: `docs/reviews/code-review-20260724-125418.md`
- Reviewer: AgentDS（第二路独立复核）
- Included scope:
  - Slice 1 全部 implementation diff（8 production + 3 test + 2 new production + 1 new test）
  - Fix gate diff（仅 `tests/host/test_package_exports.py`）
  - 初审 artifacts: `code-review-20260724-124007.md`（AgentMiMo）、`code-review-20260724-123859.md`（AgentDS 初审）
  - Controller adjudication: `wu-obs-00-slice-1-implementation-review-controller-adjudication.md`
  - Fix artifact: `wu-obs-00-slice-1-implementation-review-fix-codex.md`
- Excluded scope: Slice 2/3/4（未实现）；README（按 plan defer 到 Slice 4）；live workspace schema 20 vs 24（validation environment blocker，非 code finding）
- Parallel review coverage: 无（单路独立复核）

## Re-review 独立验证结果

以下各项均基于从 accepted plan commit 到当前 worktree 的完整 diff 逐行走读，独立于初审结论。

### CTRL-S1-IMPL-01 — CLOSED

**直接证据：**

- `tests/host/test_package_exports.py` 新增 `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS`（行 195-201），精确包含 `ToolTraceAnalysisPolicy`、`ToolTraceAnalysisSource`、`ToolTraceInputMode` 三个符号。
- 该集合通过 set union 纳入 `EXPECTED_HOST_EXPORTS`（行 221）。
- `dayu/host/__init__.py` 行 171-175 从 `dayu.host.tool_trace_analysis_contracts` 导入完全相同的三个符号。
- `dayu/host/__init__.py` 行 298-300 在 `__all__` 中列出完全相同的三个符号。
- `dayu.host.tool_trace_analysis_contracts.__all__`（行 356-365）额外包含 5 个 S1 阈值常量，但 `dayu/host/__init__.py` **未**导入这些常量，符合 contract 边界——常量仅供模块内部使用。

**验证命令与结果：**

```text
pytest -q tests/host/test_package_exports.py
15 passed in 0.33s

pytest -q tests/host
2296 passed, 2 skipped, 6 deselected in 57.27s

python -m pyright dayu/ tests/ utils/
0 errors, 0 warnings, 0 informations
```

**结论：CTRL-S1-IMPL-01 已正确关闭，owner-level expected set 与 production `__all__` 精确同步，无兼容导出、fallback 或 loose parsing。**

### Export expected set 唯一 owner 与集合组合

**owner 链验证：**

| 符号 | 定义 owner | 导入路径 | `__init__.__all__` | test expected |
|---|---|---|---|---|
| `ToolTraceAnalysisPolicy` | `dayu.host.tool_trace_analysis_contracts:78` | `__init__.py:172` | 行 298 | `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS` |
| `ToolTraceAnalysisSource` | `dayu.host.tool_trace_analysis_contracts:49` | `__init__.py:173` | 行 299 | `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS` |
| `ToolTraceInputMode` | `dayu.host.tool_trace_analysis_contracts:39` | `__init__.py:174` | 行 300 | `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS` |

- 集合组合正确：`EXPECTED_HOST_EXPORTS = (EXPECTED_API_EXPORTS - ROOT_INTERNAL_API_NAMES) | EXPECTED_TOOLING_EXPORTS | EXPECTED_COMMAND_EXPORTS | EXPECTED_WAIT_CALLBACK_EXPORTS | EXPECTED_STORAGE_MAINTENANCE_EXPORTS | EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS`
- 无重复：三个符号仅出现在 `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS` 中，不与其他子集重叠。
- `cold_lock_path` 未泄漏：`_tool_trace_cold_lock_path` 保持在 `dayu.host.tool_trace` 模块内部（函数名 `_` 前缀），不在 `dayu.host.__all__` 中，不在 `ToolTraceAnalysisSource` 五字段中。

### Fix scope audit — 无误触 production/其它文件

**fix gate 变更文件清单：**

```text
git diff HEAD --name-only
dayu/host/__init__.py                        # Slice 1 原始实现
dayu/host/durable/connection.py              # Slice 1 原始实现
dayu/host/durable/tool_trace.py              # Slice 1 原始实现
dayu/host/durable/transaction.py             # Slice 1 原始实现
dayu/host/open_host.py                       # Slice 1 原始实现
dayu/host/tool_trace.py                      # Slice 1 原始实现
docs/host/issues-implementation-control.md   # Slice 1 原始实现
tests/host/test_durable_connection.py        # Slice 1 原始实现
tests/host/test_package_exports.py           # ← fix gate 唯一修改
tests/host/test_tool_trace_projection.py     # Slice 1 原始实现
tests/host/test_tool_trace_queries.py        # Slice 1 原始实现
```

fix gate 仅修改 `tests/host/test_package_exports.py`（加 `docs/reviews/wu-obs-00-slice-1-implementation-review-fix-codex.md` artifact），均在 Controller allowlist 内。

`test_package_exports.py` 的实际 diff 仅为：
1. 新增 `EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS` 定义块（+7 行）
2. 在 `EXPECTED_HOST_EXPORTS` union 中追加 `| EXPECTED_TOOL_TRACE_ANALYSIS_EXPORTS`（+1 行）

**零 production 文件被 fix gate 修改。零其他测试文件被修改。** 符合 Controller fix scope 约束。

### CTRL-S1-IMPL-02 — 未处理（符合裁决）

Controller 在 adjudication 中明确拒绝将 `_read_exact_prefix` 的 `len(chunk) > remaining` 防御分支（`tool_trace_analysis_input.py:847-848`）作为 fix item，理由：

- 该检查表达 exact-prefix 边界的 fail-closed invariant；
- private binary opener/test seam 的静态契约为 `BinaryIO`，保留防御不产生 fallback 或语义漂移；
- 删除只会产生无必要 production churn。

**当前代码验证：** 行 847-848 的 `if len(chunk) > remaining:` 分支**未被修改**，fix gate 未触及任何 production 文件。符合 Controller 裁决。

### Full Host/pyright 证据

| 验证项 | 命令 | 结果 |
|---|---|---|
| Slice 1 focused tests | `pytest -q tests/host/test_durable_connection.py tests/host/test_tool_trace_projection.py tests/host/test_tool_trace_queries.py tests/host/test_tool_trace_analysis_input.py` | **111 passed** |
| Package export tests | `pytest -q tests/host/test_package_exports.py` | **15 passed** |
| Complete Host tests | `pytest -q tests/host` | **2296 passed, 2 skipped, 6 deselected** |
| Targeted pyright | `python -m pyright dayu/host/tool_trace_analysis_contracts.py ... tests/host/test_package_exports.py` | **0 errors, 0 warnings, 0 informations** |
| Full pyright | `python -m pyright dayu/ tests/ utils/` | **0 errors, 0 warnings, 0 informations** |

全部独立验证通过，无新增或扩散错误。

### Lock helper owner 迁移确认

- `_tool_trace_cold_lock_path` 定义在 `dayu/host/tool_trace.py:263`（Tool Trace projection owner）。
- `_TOOL_TRACE_LOCK_FILE_SUFFIX` 定义在 `dayu/host/tool_trace.py:225`（同 owner）。
- `open_host.py` 的旧 `_default_tool_trace_lock_path` 和 `_TOOL_TRACE_LOCK_FILE_SUFFIX` 已删除。
- Producer（`open_host.py:2604`）和 reader（`tool_trace_analysis_input.py:738`）均导入并调用同一 helper。
- Reader lock timeout 使用 producer 同源的 `_LOCK_TIMEOUT_SECONDS`（`dayu.host.tool_trace:224`，值 `5.0`）。
- 无重复定义、无 dual ownership、无 loose coupling。

## Findings

未发现新的 actionable finding。

以下是对初审已识别项目与 Controller 裁决的独立逐项复核结果：

| 检查项 | 初审状态 | 独立复核结果 |
|---|---|---|
| public Source 严格五字段 | ✓ | ✓ — `ToolTraceAnalysisSource` 五字段，`cold_lock_path` 不在其中，测试断言五字段顺序 |
| Host internal lock helper 唯一 owner | ✓ | ✓ — producer/reader 复用 `_tool_trace_cold_lock_path`，旧重复定义已删除 |
| hot-first 同一 read transaction 和 watermark | ✓ | ✓ — 同一 `run_read` 内完成全量分页+resolver+reconstruction |
| cold 锁内 O(1)，锁外 same-handle exact-prefix | ✓ | ✓ — 锁内仅 open+fstat；锁外同 handle 精确读取；identity invariant 复核 |
| short read/truncate/replacement/close/lock 全 fail closed | ✓ | ✓ — 全部 fatal，无静默降级或 fallback |
| 物理 mode=ro/query_only 且无 WAL/bootstrap/DDL/write | ✓ | ✓ — URI mode=ro + PRAGMA query_only=ON 双保险 |
| strict current schema parser、digest/ref、hot-cold join | ✓ | ✓ — 28 字段精确匹配，digest recompute-and-compare，event_id primary join |
| hot missing/invalid/empty/cold-late 矩阵 | ✓ | ✓ — 仅有实际缺失可 limited；已存在但非法 fatal；cold-late → limitation |
| 无 S2/Service/CLI creep | ✓ | ✓ — diff 中无 `dayu/service/`、`dayu/cli/`；无 report/finding 预定义 |
| 中文 docstring、严格类型、无 hasattr/getattr/Any/兼容分支 | ✓ | ✓ — 全部新增函数有完整中文 docstring；零 `hasattr`/`getattr`/`Any` |
| CTRL-S1-IMPL-01 — test_package_exports 同步 | ✓（fix 后） | ✓ — 已验证通过 |
| CTRL-S1-IMPL-02 — `len(chunk) > remaining` 防御分支 | rejected（Controller） | ✓ — 未处理，符合裁决 |

## Open Questions

无。

## Residual Risk

1. **Live schema blocker 持续**：真实 workspace DB schema=20，current fresh schema=24。该 validation environment blocker 继续冻结 Slice 1 acceptance、保护提交与 Slice 2。代码行为正确（fail closed via `HostSchemaMismatchError` → `HOT_STORE_READ_FAILED` fatal）。

2. **Live resolver smoke 未执行**：由于上述 schema gate 在 resolver 之前 fail closed，当前 workspace descriptor 的 live resolver 验证尚未发生；unit/fixture resolver contract 已通过。

3. **Platform residual**：same-handle exact-prefix、path replacement、truncate/short read 与真实 producer non-interference 已在 macOS/Python 3.11 验证；无其他 OS CI 证据。但测试使用 Python 标准 `io`/`os` 模块，不依赖平台特定行为。

4. **Large cold file**：prefix capture 机制对 multi-GB JSONL 的内存影响由 plan 中记录的 Issue #36 cold rotation 负责，不在 S1 scope。

## Verdict

**PASS — 0 new actionable findings。CTRL-S1-IMPL-01 已正确关闭。**

Slice 1 实现 + fix gate 在以下各维度均通过第二路独立复核：

- CTRL-S1-IMPL-01（test_package_exports allowlist 同步）已正确关闭，owner-level expected set 与 production `__all__` 精确一致。
- Export expected set 唯一 owner 为 `dayu.host.tool_trace_analysis_contracts`，集合组合正确，无符号泄漏。
- Fix gate 未误触任何 production 文件或其他测试文件，严格在 Controller allowlist 内。
- CTRL-S1-IMPL-02 未处理，符合 Controller 拒绝裁决。
- 全部 Host 测试（2296 passed）、Slice 1 focused 测试（111 passed）、完整 pyright（0 errors）独立验证通过。
- Lock helper owner 迁移完整，producer/reader 共用同一真源，旧重复定义已删除。
- Live schema 20 vs 24 仍为 validation environment blocker，代码行为正确 fail closed。

**无新 actionable finding。Slice 1 acceptance 的代码正确性无争议，唯一阻塞项为独立于代码的 live schema mismatch environment blocker。**
