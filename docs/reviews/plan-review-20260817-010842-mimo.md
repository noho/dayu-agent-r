# UF-FIX10 S1 second blocker plan amendment — plan re-review

## Review metadata

- reviewed target：`docs/gateflow/uf-fix10-s1-second-blocker-plan-amendment-20260817.md` 及其引用的 amended plan `§8.2`/`§10.1`/`§13.2`
- reviewer：MiMo
- date：2026-08-17
- scope：验证第三个 Forbidden fake 的最小 owner 修复路径是否正确、是否还有未纳入的同类无 cast structural implementer、S1/S2/production scope 是否不变
- artifact path：`docs/reviews/plan-review-20260817-010842-mimo.md`

## Assumptions tested

1. `FilingUploadStateRepositoryProtocol` 是否确实新增了 `read_filing_upload_state_in_batch` required method。
2. `test_fins_ingestion_tools.py` 的 `_ForbiddenFilingUploadStateRepository` 是否确实缺少该 method 且无 cast 注入。
3. 全仓是否还有其它缺少该 method 的 structural fake。
4. `test_fins_ingestion_runtime.py` 的两个 fake 是否已真实 conform（首次 amendment 产物）。
5. amendment 的修改路径是否最小且不引入 cast/default/fallback。
6. S1/S2/production scope 是否未被 amendment 改变。

## Evidence gathered

### Protocol definition（`dayu/fins/storage/repository_protocols.py:426-477`）

```python
class FilingUploadStateRepositoryProtocol(Protocol):
    def read_filing_upload_state(self, ticker: str, document_id: str) -> FilingUploadPublishedState: ...
    def read_filing_upload_state_in_batch(self, batch: BatchToken, document_id: str) -> FilingUploadPublishedState: ...
```

Protocol 确实要求两个 method。`read_filing_upload_state_in_batch` 为新增 required method。

### 全仓 structural fake census

| # | Class | File | has `read_filing_upload_state` | has `read_filing_upload_state_in_batch` | 注入方式 |
|---|-------|------|:---:|:---:|---|
| 1 | `FsFilingUploadStateRepository` (production) | `fs_filing_upload_state_repository.py:18` | YES | YES | N/A |
| 2 | `_FsFilingUploadStateMixin` (core) | `_fs_filing_upload_state_core.py:136` | YES | YES | N/A |
| 3 | `_FixedFilingUploadStateRepository` (test fake) | `test_fins_ingestion_runtime.py:10748` | YES | YES | 直接 typed 参数（无 cast） |
| 4 | `_ForbiddenFilingUploadStateRepository` (test fake) | `test_fins_ingestion_runtime.py:10686` | YES | YES | 直接 typed 参数（无 cast） |
| 5 | **`_ForbiddenFilingUploadStateRepository` (test fake)** | **`test_fins_ingestion_tools.py:772`** | YES | **NO** | **直接 typed 参数（无 cast）** ← **pyright 阻塞** |
| 6 | `_UnsafeFilingUploadStateRepository` (test fake) | `test_fins_service_runtime.py:40` | YES | NO | `monkeypatch.setattr` class 替换（非 typed 参数） |

### Pyright 验证事实

- `tests/fins/test_fins_ingestion_runtime.py`：`0 errors`（首次 amendment 已修复两个 fake）。
- `tests/fins/test_fins_ingestion_tools.py:2474:40`：`1 error` — `_ForbiddenFilingUploadStateRepository` 不兼容 `FilingUploadStateRepositoryProtocol`，缺少 `read_filing_upload_state_in_batch`。这是 amendment 声称的 blocker，已确认。
- `tests/fins/test_fins_service_runtime.py`：`0 errors`。`_UnsafeFilingUploadStateRepository` 通过 `monkeypatch.setattr` 在 class 级别替换 `FsFilingUploadStateRepository`，不经过 typed parameter 注入，pyright 不检查其 structural conformance。**当前不阻塞 pyright。**

### Injection site 验证（`test_fins_ingestion_tools.py:2450-2480`）

```python
def _runtime_with_static_admission_guard(*, workspace_root: Path) -> ...:
    base_runtime = DefaultFinsRuntime.create(workspace_root=workspace_root)
    executor = _NoOpExecutor()
    state_repository = _ForbiddenFilingUploadStateRepository()
    runtime = FinsIngestionRuntime.create(
        ...
        filing_upload_state_repository=state_repository,  # line 2474: 直接传入，无 cast
        ...
    )
```

确认：无 cast、无 default、无 hasattr/getattr、无兼容 wrapper。fake 直接作为 typed parameter 传入 `FinsIngestionRuntime.create()`，pyright 在此处检查 structural conformance。

### test_fins_ingestion_runtime.py 首次 amendment 产物验证

两个 fake 已确认：
- `_FixedFilingUploadStateRepository`（:10748）：有 `batch_calls`、`read_filing_upload_state_in_batch`，记录后返回固定 state。
- `_ForbiddenFilingUploadStateRepository`（:10686）：有 `batch_calls`、`read_filing_upload_state_in_batch`，记录后抛 `AssertionError`。
- `_build_static_admission_guarded_runtime()`（:10882）和 `_build_fixed_state_guarded_runtime()`（:10921）均无 cast。
- 六处 `batch_calls` 断言已存在：四处 `== []`（零行为断言），两处 `== [(batch, document_id)]`（direct conformance signal）。

## Findings

### 001-unfixed-low-第四 structural fake（`_UnsafeFilingUploadStateRepository`）未纳入 census

- **位置**：amendment §2 "Direct full-pyright evidence"、§3 "Second amendment decision"；amended plan §8.2、§10.1
- **问题类型**：census 仍不完整
- **当前写法**：amendment 只识别三个 fake（runtime 的 `_Fixed...` 和 `_Forbidden...`，tools 的 `_Forbidden...`），未提及 `test_fins_service_runtime.py:40` 的 `_UnsafeFilingUploadStateRepository`。
- **反例/失败场景**：S2 接线后，若 `prevalidate_fins_upload_filing_request_for_workspace()` 的代码路径调用 `read_filing_upload_state_in_batch`，该 fake 在运行时会抛 `AttributeError`。当前 S1 不调用该 method，因此不会触发。
- **为什么有问题**：该 fake 同样只实现 `read_filing_upload_state`，缺少 `read_filing_upload_state_in_batch`。与 tools 文件的第三个 fake 属于同一类 structural conformance gap。
- **直接证据**：
  - `test_fins_service_runtime.py:40-91`：`_UnsafeFilingUploadStateRepository` 只有 `read_filing_upload_state`。
  - `test_fins_service_runtime.py:313-317`：通过 `monkeypatch.setattr(service_runtime, "FsFilingUploadStateRepository", _UnsafeFilingUploadStateRepository)` 注入，不经过 typed parameter。
  - `python -m pyright tests/fins/test_fins_service_runtime.py`：`0 errors`（monkeypatch 不触发 structural check）。
- **影响**：S1 无影响（pyright 不阻塞，S1 代码路径不调用 batch method）。S2 接线后若该代码路径触及 batch read 会 runtime fail，但 S2 的 allowed tests 已包含 `test_fins_service_runtime.py`，届时可同步修复。
- **建议改法**：不需要在本 amendment 中修复。S2 implementation 时同步为该 fake 新增 `read_filing_upload_state_in_batch` method（record-then-return-UNSAFE-state 或 record-then-raise，取决于 S2 测试意图）。当前 amendment 的 scope 只针对 pyright 直接阻塞的第三个 fake，这是正确的。
- **修复风险**：低
- **严重程度**：低（S1 不阻塞，S2 自然覆盖）

### 002-unfixed-low-amendment 未要求运行 amendment 涉及文件的 focused pyright

- **位置**：amendment §3 第 6 点
- **问题类型**：验证信号不完整
- **当前写法**：amendment 要求 "focused pyright 扩展为同时检查 runtime/tools 两个文件"，但 amendment gate 本身不运行 pyright（§5 "本 second amendment gate 不运行 pytest、pyright 或 coverage"）。
- **反例/失败场景**：无实际风险。implementation gate 恢复后会运行 focused pyright 验证。
- **为什么有问题**：这是一个 sequencing 问题而非 correctness 问题。amendment 只修订 plan 文本，不修改代码，因此不运行 pyright 是合理的 gate boundary。
- **直接证据**：amendment §5 明确声明 "本 second amendment gate 不运行 pytest、pyright 或 coverage"。
- **影响**：无。re-review pass 后 implementation gate 恢复时会运行完整验证命令。
- **建议改法**：无需修改。这是 gate boundary 设计，不是遗漏。
- **修复风险**：无
- **严重程度**：低

## 无 blocker finding 验证

对 amendment 声称的修复路径逐项验证：

| 验证项 | 结论 |
|--------|------|
| 第三个 fake 确实缺少 required batch method | ✅ 已确认：`test_fins_ingestion_tools.py:772` 的 `_ForbiddenFilingUploadStateRepository` 只有 `read_filing_upload_state` |
| 注入点无 cast | ✅ 已确认：`test_fins_ingestion_tools.py:2474` 直接 typed parameter，无 cast |
| 修复路径为新增 `batch_calls` + `read_filing_upload_state_in_batch` record-then-AssertionError | ✅ 与首次 amendment 的 runtime fake 同型 |
| 现有 static-admission cases 只增 `batch_calls == []` 断言 | ✅ amendment §3 第 5 点明确 |
| 不引入 cast/default/hasattr/fallback | ✅ amendment §3 第 6 点明确禁止 |
| 全仓无其它 pyright 阻塞的 structural fake | ✅ `_UnsafeFilingUploadStateRepository` 通过 monkeypatch 注入，pyright 不报错 |
| S1 scope 不变 | ✅ amendment 只扩充 allowed tests，不改 production、不改 S2 boundary |
| S2 scope 不变 | ✅ amendment 不触碰 S2 files/behavior |
| production scope 不变 | ✅ amendment 不修改任何生产代码 |

## Open questions

无。

## Residual risks

| 风险 | 分类 | Owner |
|------|------|-------|
| `_UnsafeFilingUploadStateRepository`（`test_fins_service_runtime.py:40`）同样缺少 batch method，但因 monkeypatch 注入不触发 pyright；S2 接线后需同步修复 | S2 natural scope | S2 implementation |
| amendment gate 不运行 pyright，依赖 implementation gate 恢复后验证 | gate sequencing | implementation gate |

## Plan review conclusion

**pass**

第二个 blocker amendment 的修复路径正确且最小：

1. **root cause 准确**：首次 amendment 的 structural implementer census 漏列 `test_fins_ingestion_tools.py` 的第三个 fake，不是 production protocol 过严，也不是 tool runtime 需要 fallback。
2. **修复路径最小**：只对该 fake 新增 `batch_calls` + `read_filing_upload_state_in_batch`（record-then-AssertionError），并在现有 static-admission cases 增加 `batch_calls == []` 零行为断言。与首次 amendment 的 runtime fake 完全同型。
3. **无 cast/default/fallback**：amendment 明确禁止且 injection site 已确认无 cast。
4. **全仓 census 完整**：第四个 fake（`_UnsafeFilingUploadStateRepository`）通过 monkeypatch 注入，pyright 不报错，不属于 S1 blocker；S2 自然覆盖。
5. **S1/S2/production scope 不变**：amendment 只扩充 `test_fins_ingestion_tools.py` 到 S1 allowed tests，不改变 production、S2 boundary 或 validation contract。

无 blocker。S1 implementation 可恢复。
