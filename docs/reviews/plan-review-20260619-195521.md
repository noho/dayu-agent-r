# WU-CM-13 Plan Re-Review — Focused Fix Verification

## Reviewed Target

- **Plan**: `docs/host/host-issues/wu-cm-13-unified-compact-pipeline-plan.md`（已修订版）
- **Previous reviews**: `docs/reviews/plan-review-20260619-194515.md`（fail），`docs/reviews/plan-review-20260619-194657.md`（pass-with-risks）
- **Review type**: focused re-review — 验证 controller accepted fixes 是否已落实，以及修订 plan 是否仍有新 blocker
- **Review date**: 2026-06-19

## Controller Accepted Fixes — Verification

| # | Expected Fix | Status | Evidence |
|---|---|---|---|
| F1 | thin helper owner instead of thick coordinator/god pipeline | **已修复** | §1 收窄为"薄的 shared helper owner，目标小于 500 行"；§4 明确"不得 import dispatch scheduler、engine ingest class、durable state transition helpers 或 worker/lifecycle owner"；§5 无 lifecycle coordinator 语义。 |
| F2 | concrete dataclass/function signatures | **已修复** | §5.1 定义 8 个 frozen dataclass（`CompactPipelineSourceSnapshot`、`CompactPipelineRequestPlan`、`CompactPipelineRecoveryRequestPlan`、`CompactPipelinePassQueuePlan`、`CompactPipelineAcceptedPayloadInput`、`CompactPipelineFailedPayloadInput`、`CompactPipelineFallbackDecisionInput`、`CompactPipelineOrdinaryRawTailHandoff`、`CompactPipelineFallbackSelectedMaterialHandoff`）。§5.2 定义 6 个函数签名。§6 给出 3 个 caller + 1 个 fallback handoff 的伪代码。 |
| F3 | no lifecycle expected_run_status in pipeline; caller owns guards | **已修复** | §7 明确"compact_pipeline.py 不输出 expected_run_status、expected_attempt_status、dispatch record status 或 session active slot 字段"；§6.1/6.2 伪代码中 caller 各自做 commit guard。 |
| F4 | tier 5 current-input-only fallback out of WU-CM-13 scope/deferred | **已修复** | §8 明确"本 WU 不新增未实现的 tier 5 current-input-only fallback，也不新增 fallback_tier payload 字段"；§8 最后一段"Design 中 tier 5 current-input-only fallback 是 deferred future owner"；§16 blocking questions 列出"Implementing tier 5 current-input-only fallback in this WU"；§5.1 `CompactPipelineRecoveryRequestPlan` 只允许 tier 1/2/3。 |
| F5 | WU-CM-14 provider choice settled as pipeline-owned audited second-read provider | **已修复** | §9 开头"本 WU 明确选择 pipeline-owned audited second-read provider"；§9 列出 7 条具体校验要求；§5.2 定义 `CompactPipelineProtectedRawTailProvider` Protocol；§9 明确"不能只把 provider 文件位置挪到 compact_pipeline.py 而保留独立 selection 逻辑"。 |
| F6 | ordinary raw-tail handoff split from fallback selected-material handoff | **已修复** | §5.1 分别定义 `CompactPipelineOrdinaryRawTailHandoff`（§201-207）和 `CompactPipelineFallbackSelectedMaterialHandoff`（§212-220）；§6.3 伪代码明确 ordinary branch 消费 raw-tail handoff；§6.4 说明 fallback handoff 不进入 ordinary branch。 |
| F7 | fallback unified scope limited to selection/payload input/action hint; caller owns dispatch/recovery action | **已修复** | §5.1 `CompactPipelineFallbackDecisionInput` 包含 `action_hint: str`；§5.1 语义说明"proactive caller 决定是否 start dispatch；reactive caller 决定是否 create recovery Attempt 或 fail recovering Run"；§6.1/6.2 伪代码确认 caller 消费 `action_hint` 后各自决定后续动作。 |
| F8 | compaction_evidence test migration mapping table | **已修复** | §10 包含 17 行 mapping table，每行有"当前测试"、"当前覆盖"、"裁决"（迁移到 `test_compact_pipeline.py` 或 `test_compact_material.py` 或删除）。§10 末尾给出迁移后验证命令。 |
| F9 | five Session Semantic Memory equivalence standard | **已修复** | §11 定义 11 个具体 semantic projection fields 必须逐字段等价（session_summary_memory.summary_text、evidence_backed_facts[*].claim_text 等）；明确 lifecycle-only fields 可以不同；§13 item 8 引用 §11 作为测试标准。 |

**全部 9 项 controller accepted fixes 已落实。**

## New Blocker Check

### 检查项 1：`compact_pipeline_source_snapshot_from_pre_dispatch_view` 函数未定义签名

§6 伪代码中调用 `compact_pipeline_source_snapshot_from_pre_dispatch_view(trigger_source, run, material_view)`（行 346、375），但 §5.2 的函数签名列表中没有该函数。

**评估**：不构成 blocker。该函数的输入（`ContextCompactionTriggerSource`、`Run`、`PreDispatchCompactMaterialView`）和输出（`CompactPipelineSourceSnapshot`）从上下文完全可推断。Implementation agent 可以直接实现它。但建议在 §5.2 补一行签名以保持完整性。

### 检查项 2：§4 声明 "不得新增 fallback_tier payload 字段" 与 §16 blocking questions 一致

§4 明确 "不新增 fallback_tier"；§16 列出 "Implementing tier 5 current-input-only fallback in this WU" 为 blocking question；§8 说 "deferred future owner"。三处一致。

### 检查项 3：Slice 2 拆分为 2a/2b/2c

前一轮 review 建议 S2 拆分。修订 plan §12 将 Slice 2 拆为 2a（wire proactive dispatch）、2b（wire reactive ingest）、2c（wire RunInput protected raw tail）。每个 sub-slice 有具体的替换点描述。

**评估**：已修复。Sub-slice 拆分合理，每个有明确的 "Replace X with Y" 描述。

### 检查项 4：Smoke hard gate 边界说明

§13 item 12 明确 "This smoke verifies public Host conversation memory behavior does not regress; helper convergence itself is verified by focused unit/integration tests above"。

**评估**：已修复。Smoke 覆盖边界已明确。

### 检查项 5：Caller wiring pseudocode 是否足够具体

§6 给出 3 段伪代码：
- §6.1 proactive dispatch caller（25 行）
- §6.2 reactive engine ingest caller（35 行）
- §6.3 RunInputBuilder ordinary branch（22 行）

每段伪代码标注了 caller 保留的职责（EventLog append、artifact write、dispatch start 等）。

**评估**：已修复。伪代码足够让 implementation agent 定位替换点。

### 检查项 6：是否存在隐性 schema/contract 变更

- §4 明确 "不改 EventLog event type"、"不改 compact artifact schema"、"不新增 fallback_tier payload 字段"。
- §5.1 所有 dataclass 标注 `frozen=True, slots=True`，是 Host 内部类型，不进入 durable schema。
- §9 选择 second-read provider 不改变 EventLog 读取语义。

**评估**：无隐性 schema 变更。

### 检查项 7：`CompactPipelineProtectedRawTailProvider` Protocol 定义是否完整

§5.2 定义 Protocol class：
```python
class CompactPipelineProtectedRawTailProvider(Protocol):
    def load_ordinary_raw_tail(self, snapshot, current_facts, memory, compact) -> CompactPipelineOrdinaryRawTailHandoff: ...
```

§9 列出 7 条校验要求（current-run compact event、input cursor、source boundary、material digest 等）。

**评估**：Protocol 接口清晰，校验要求在 §9 以 implementation requirements 形式列出。不构成 blocker。

## Residual Risks

| ID | 风险 | 严重程度 | 建议追踪 |
|---|---|---|---|
| RR-1 | Tier 5 deferred — 后续实现时可能需要 `fallback_tier` payload 字段 | 低 | Future WU plan gate |
| RR-2 | `compact_pipeline_source_snapshot_from_pre_dispatch_view` 未在 §5.2 给出正式签名 | 低 | Implementation agent 直接实现 |
| RR-3 | Slice 2a/2b/2c 的 "Replace X with Y" 描述仍依赖 implementation agent 在大文件中定位具体行号 | 低 | Implementation review |

## Open Questions

无新 open question。前一轮 4 个 open questions 已全部在修订 plan 中收敛：

1. ~~Tier 4/5 scope~~ → §8 deferred，§16 blocking question
2. ~~CompactPipelineRenderHandoff consumption~~ → §5.1 拆为两个独立 handoff，§6.3/6.4 伪代码
3. ~~WU-CM-14 provider choice~~ → §9 "pipeline-owned audited second-read provider"
4. ~~Fallback unified scope~~ → §5.1 `action_hint` + §6 caller 伪代码

## Final Plan Review Conclusion

**pass**

修订 plan 已落实全部 9 项 controller accepted fixes，无新 blocker。Plan 现在是 code-generation-ready：

- **类型系统**：8 个 frozen dataclass + 6 个函数签名 + 1 个 Protocol，字段名和类型具体可实现。
- **Scope 收窄**：薄 helper（<500 行），不控制 lifecycle，不写 EventLog，tier 5 deferred。
- **Caller wiring**：3 段伪代码覆盖 proactive / reactive / RunInput，每段标注 caller 保留职责。
- **WU-CM-14**：明确选择 pipeline-owned audited second-read provider，7 条校验要求。
- **测试**：17 行 evidence migration table + 12 项 test plan + memory equivalence standard。
- **Blocking questions**：无 open question；tier 5 实现列为 blocking question。

Residual risks 均为低严重程度，可在 implementation review 中跟踪。
