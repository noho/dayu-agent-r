# Code Review

## Scope

- Mode: current changes（Gateflow UF-FIX07 Implementation Slice 2 code review）
- Branch: `codex/upload-filing-oracle`
- Base: `9d0a2f8a`（Slice 1 accepted commit：`fix(fins): enforce UF-FIX07 filing admission contract`）
- Output file: `docs/reviews/code-review-20260815-201037.md`
- Included scope: 仅审查当前未提交的 Slice 2 文件：
  - `dayu/cli/arg_parsing.py`
  - `dayu/cli/commands/fins.py`
  - `dayu/fins/tools/upload_tools.py`
  - `dayu/fins/upload_format_contract.py`
  - `tests/cli/test_arg_parsing.py`
  - `tests/cli/test_fins_commands.py`
  - `tests/fins/test_fins_ingestion_tools.py`
  - `tests/fins/test_upload_format_contract.py`
  - `docs/gateflow/uf-fix07-slice2-implementation-20260815.md`
- Excluded scope: Slice 1 已提交代码（`ingestion_runtime` 静态 validator、`service_runtime`）、Slice 3/4 计划内容（asset identity、storage、README）、Host/Engine/runtime/storage/processor 生产代码（本 slice 未修改，按计划不视为 defect）。
- Parallel review coverage: 无。本 review 由单一 reviewer 完成。
- 已读输入：`AGENTS.md`、accepted plan（`uf-fix07-multi-file-primary-collision-plan-20260815.md`，含 §6.2/§6.4/§6.5/§7 不变量与 §11 stop conditions）、plan re-review adjudication、Slice 2 implementation artifact、Slice 1 已提交实现（`ingestion_runtime._validate_fins_upload_filing_static` / `_filing_upload_request_identity` / `FinsUploadUsageCode` / `_USAGE_MESSAGES`、`service_runtime.prevalidate_fins_upload_filing_request_for_workspace`）、`ToolParametersSchema` 声明期校验、`dayu/runtime/tool_call_projection` 的 `maxItems` 运行时校验、`dayu/host/run_input._tool_schema_json` 的 LLM-facing JSON 投影。
- 验证执行：affected tests（`720 passed, 3 warnings`，与 implementation doc 一致）、targeted pyright（`0 errors, 0 warnings, 0 informations`）、`git diff --check`（exit 0）、单生产文件 branch coverage（`arg_parsing.py 99%` / `commands/fins.py 81%` / `upload_tools.py 92%` / `upload_format_contract.py 89%`，与 implementation doc 一致）、真实 `python -m dayu.cli upload_filing --help` 输出核对。

## Findings

### 1-未修复-低-tool 失败投影未随新 primary 字段更新，LLM-facing 失败文案与 hint 存在两处缺口
- **入口/函数**: `FinsUploadToolCallable.__call__` → `_upload_request_from_arguments` → `_upload_primary_selectors_from_arguments` / `_failed_outcome`
- **文件(行号)**: `dayu/fins/tools/upload_tools.py:386`（material-primary 错误消息）、`dayu/fins/tools/upload_tools.py:114-121`（ValueError → `message=str(exc)` 与固定 hint）
- **输入场景**: (a) LLM 对 `upload_kind=material` 请求携带 `primary` 字段；(b) 任一 primary 相关 `invalid_argument`（material 携带 primary、primary 空串/非字符串、owner 级 `--primary 只能指定一次` / `--primary 必须精确匹配 --files 中的一个文件` / `delete 不得提供 --primary` 经 `prepare_observed_upload` 抛出）。
- **实际分支**: (a) `_upload_primary_selectors_from_arguments` 在 `upload_kind == material` 分支抛 `ValueError("primary must be omitted for material uploads")`，被 `__call__` 的 `except ValueError` 捕获后以 `message=str(exc)` 原样写入 `ToolFailedOutcome.result.message` 投影给 LLM；(b) 同一 except 分支对所有 ValueError 附固定 hint。
- **预期行为**: 按 AGENTS.md LLM-facing 文本约束，投影给 LLM 的 tool message 应业务可读、与同工具其它 LLM-facing 文案同源且一致；同一业务规则（material 不使用 primary）在 schema description 中已是中文（`upload_tool_primary`：「upload_kind=material 不使用 primary，必须省略」），owner 级 usage 文案（`_USAGE_MESSAGES`）也全部为中文；失败恢复提示应覆盖新增字段。
- **实际行为**: (a) LLM 收到的失败消息为英文「primary must be omitted for material uploads」，与同一 schema 内的中文规则文案及全中文 owner usage 消息不一致；同一条规则出现三种文本形态（schema 中文 / adapter 英文 / filing delete 路径 owner 中文）；(b) hint「请检查 ticker、upload_kind、action、文件路径、会计期间和材料字段。」未提及新增的 `primary` 字段，owner 级 primary 错误投影时同样携带该不完整 hint。
- **直接证据**: `upload_tools.py:386` 的英文字面量；`upload_tools.py:119-120` 的 `message=str(exc)` 与固定 hint；`_ingestion_tool_helpers.py:69` `_failed_outcome` docstring 明确 `message` 是「面向模型的错误说明」；`ingestion_runtime.py:800-803` 的 owner 中文文案；测试 `tests/fins/test_fins_ingestion_tools.py:1607` 只断言 message 未断言 hint。
- **影响**: 仅 LLM 失败恢复体验层面的不一致与提示缺口，不影响 fail-closed 正确性、状态或数据。低。
- **建议改法和验证点**: (a) material-primary 拒绝消息改为中文可行动文案（如「upload_kind=material 不得提供 primary」），与 schema/owner 文案口径一致；(b) ValueError 分支 hint 加入 primary 字段名。验证点：在 `test_upload_tool_adapter_projects_zero_or_one_filing_primary_selector` 或新的 failed-outcome 测试中逐字断言 message 与 hint。若裁决为按计划保留 adapter 英文惯例，应记录理由并至少在 hint 中补入 primary。
- **修复风险（低）**: 仅改两个字符串；若被外部测试固化需同步断言。
- **严重程度（低）**:

## Open Questions

- 无。

## Residual Risk

- tool 路径 delete+files 由 adapter 英文消息（`upload_tools.py:476`「files must be omitted for delete uploads」）拒绝，filing delete+primary 由 owner 中文消息（`delete 不得提供 --primary`）拒绝——同一条「delete 必须省略 files 和 primary」规则在两个 enforcement 边界产生不同文本。delete+files 部分是 Slice 2 之前的既有代码，本 slice 新增的 material-primary 沿用 adapter 边界，属计划 §6.5 允许但未指定文案的盲区；建议在后续 registry/evidence 或文案收敛 work unit 统一（或确认 adapter 边界英文为既定惯例）。
- schema `files.maxItems=100`（`upload_tools.py:240`）与 enforcement 真源 `ingestion_runtime._MAX_TUPLE_ITEMS`（=100，`_validate_fins_upload_filing_static:1069` 的 `TOO_MANY_FILES`）是两份独立字面量，无共享真源，未来调整上限需两处同步且无跨边界一致性测试。该形态由 accepted plan §3.1 goal 4 明确裁定（schema 明确 maxItems=100），实现符合计划，故不列为 defect，仅提示维护风险。
- CLI usage matrix（`test_upload_filing_usage_matrix_precedes_service_factory_and_workspace_mutation`）新增的 4 个 UF-FIX07 case 覆盖 repeated/missing/outside/duplicate，未覆盖 delete+primary 与 delete+files 的 CLI 级 case；owner 级（`test_fins_ingestion_runtime.py:1599-1612, 1787-1795`）与 tool 级（`test_fins_ingestion_tools.py:1607`）已覆盖，CLI 为纯委托路径，风险很低。
- 新增的 tool 零副作用测试未做 workspace 树快照对比（兄弟测试 `test_upload_tool_filing_calendar_year_invalid_input_has_zero_side_effects` 有），但 `_ForbiddenFilingUploadStateRepository.calls == []` + 无 observation/job 文件的断言已充分证明 state/job/mutation 边界为零，覆盖强度等价。
- 100/101 文件上限的 CLI 与 tool 完整链路测试在 Slice 2 未新增（Slice 1 validator 级已覆盖 100 接受/101 拒绝；tool schema maxItems 由 runtime 投影与 owner `TOO_MANY_FILES` 双重 fail-closed）。
- 本 slice 未修改 README（accepted plan 归属 Slice 4）；`dayu/cli/`、`dayu/fins/` 的 README 触发规则在 Slice 4 closeout 时应一并核对。
- plan §12 已分类的 assigned-to-later-work-unit 风险（UF-FIX08/10/11、frozen UF-A14 stale、registry/evidence 更新、UF-PF07/UF-PF12 未执行）均不在本 slice，按约定不视为 defect。

## 逐项核查结论（对照 review 重点清单）

| 重点项 | 结论 | 直接证据 |
| --- | --- | --- |
| `--primary` 只注册 upload_filing 且 append 不吞重复 occurrence | 通过 | `arg_parsing.py:928-933` 仅 `upload_filing` 注册 `action="append"`；`test_upload_filing_primary_is_append_only_on_filing_command` 断言两 occurrence 全保留、其它两命令 exit 2；`UF-FIX07-repeated-primary` CLI case 证明 validator 看到 cardinality=2 |
| path projection 不承载业务判断 | 通过 | CLI `fins.py:683-686` 与 tool `upload_tools.py:387` 均为机械 `expanduser().resolve(strict=False)`；无 membership/cardinality/role 逻辑 |
| raw primary_selectors 到 static validator 生效链 | 通过 | CLI：`args.primary` → `FinsUploadFilingRequest.primary_selectors` → `prevalidate_fins_upload_filing_request_for_workspace`（`service_runtime.py:80` 静态校验先于 `read_filing_upload_state:86`）→ `_validate_fins_upload_filing_static:1101-1111`；tool：`prepare_observed_upload` → `_validate_runtime_upload_request`（`ingestion_runtime.py:4435` 同样先静态后 state read）→ observation/job 前失败 |
| tool schema primary optional string / files maxItems=100 | 通过 | `upload_tools.py:236-245`；`primary` 不在 required；`ToolParametersSchema._validate_count_bounds` 支持 maxItems；Host `_tool_schema_json`（`run_input.py:7065`）逐字透传 properties |
| filing 0/1 selector、material primary fail closed | 通过 | adapter 0/1 投影 + material 边界拒绝；`test_upload_tool_adapter_projects_zero_or_one_filing_primary_selector` |
| invalid combos 在 state/job/mutation 前失败 | 通过 | CLI matrix 断言 factory_calls==[]、workspace tree 不变、workspace root 未创建；tool 测试断言 forbidden state repo 零调用、observation/job 零副作用 |
| CLI help 与 tool schema 机械消费唯一文本 owner | 通过 | 两入口均消费 `FINS_UPLOAD_FORMAT_TEXT`；唯一构造点 `upload_format_contract.py:626`；测试以 `==` 逐字断言 |
| LLM-facing 自足且无内部术语 | 通过 | schema/help 文案自足说明单/多文件、membership、顺序、delete、material 规则；测试负向断言不含 Path/tuple/Host/Engine/asset identity |
| 无首项/顺序推断 | 通过 | 生产代码 grep 无「首文件是主文件」残留；`_select_fins_upload_filing_files` 仅在 owner 级单文件隐式规则用 `next(iter(files))`；`test_upload_filing_non_first_primary_is_preserved_into_validated_service_request` 证明非首位 primary 进入 validated request |
| schema 没有 Host/Engine drift | 通过 | 本 slice 未改 `dayu/host|engine|service|runtime`；maxItems 声明期/运行时投影/Host JSON 投影均为既有能力，未触发计划 §8 Slice 2 stop condition |
| 严格类型/docstring/tests | 通过 | pyright 0/0/0；新函数 docstring 完整（参数/返回/异常）；720 tests 通过；单文件 branch coverage 99%/81%/92%/89% ≥80% |

未发现实质性问题（除 Finding 1 的文案缺口外）。
