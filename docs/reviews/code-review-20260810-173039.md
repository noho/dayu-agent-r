# Code Review (Final Re-review — Validation Fix)

## Verdict

**PASS**

## Scope

- Mode: current changes (final re-review of Slice 1 post-re-review validation fix)
- Branch: `codex/download-oracle`
- Base: `0fe85869` (accepted-plan HEAD)
- Previous reviews: `code-review-20260810-171054.md` → `code-review-20260810-172003.md` → `code-review-20260810-172307.md`
- Adjudication: `docs/gateflow/wu-cli-download-02-slice1-code-review-adjudication-20260810.md`（含 Re-review 后总控 validation 裁决）
- Fix artifact: `docs/gateflow/wu-cli-download-02-slice1-validation-fix-20260810.md`
- Output file: `docs/reviews/code-review-20260810-173039.md`
- Included scope: 核验 validation fix 的 5 个 claim——`test_arg_parsing.py` 零 diff、help owner test 迁移等价、production 不变、4 changed Python files 的 Ruff format exit 0、tests/coverage/pyright 通过且无新增 finding
- Excluded scope: Slice 2/3、README、post-fix CLI evidence、accepted slice commit

## Findings

### 先前 Finding 状态汇总

| # | 来源 | Finding | 裁决 | 状态 |
|---|---|---|---|---|
| 1 | `code-review-20260810-171054.md` #2 | AST 单 owner 测试对 helper 内部重构敏感 | accepted (review fix) | ✅ 已修复 |
| 2 | `code-review-20260810-171054.md` #1 | `test_arg_parsing.py` 无关全文件格式化 | accepted (review fix) | ✅ 已修复 |
| 3 | 总控 re-review 后 validation | `test_arg_parsing.py` 留在 changed-files 集合导致 formatter gate 失败（`ruff format --check` 对该文件 report Would reformat），归因不闭合 gate | accepted (validation fix) | ✅ 已修复 |

### Finding 3 关闭证据

**要求**: `test_arg_parsing.py` 完全回到 accepted-plan base（零 diff），help owner test 等价迁移到 Slice 1 allowed file。

**直接证据**:

1. **`test_arg_parsing.py` 零 diff**:
   ```
   git diff --exit-code 0fe85869 -- tests/cli/test_arg_parsing.py
   exit=0
   ```
   文件完全回到 accepted-plan base，退出 changed-files 集合。

2. **Help owner test 迁移等价**:
   - 原位置 (`test_arg_parsing.py`): 已删除 `--rebuild` inventory 与 `test_download_help_explains_mutually_exclusive_mutation_modes`
   - 新位置 (`tests/cli/test_fins_commands.py:782`): 同名测试通过 `cli_main.main(("download", "--help"))` 断言 `exit_code == EXIT_SUCCESS`、`"--overwrite"` 存在、互斥 help 文案存在、`"--rebuild"` 存在、`captured.err == ""`
   - 新测试比原测试更强：原测试仅 `_capture_help(capsys, ("download",))`，不验证 exit code 和 stderr；新测试走真实 CLI entry point 并额外断言 exit 0 与空 stderr
   - `rg` 确认：`test_download_help_explains_mutually_exclusive_mutation_modes` 仅在 `tests/cli/test_fins_commands.py` 一处命中；两条互斥 help 文案也仅在该文件命中

3. **Production 不变**:
   - `dayu/cli/arg_parsing.py` 与 `dayu/fins/download_contract.py` 仍在 changed-files 集合，diff 与首轮 review 完全一致
   - 单 owner 守卫仍为 `dayu/fins/download_contract.py:79`: `if overwrite_existing and rebuild_local_artifacts:`
   - `_validate_download_mutation_mode` 仍被 `FinsDownloadEffectiveFilters.__post_init__` (line 225) 和 `FinsDownloadRequest.__post_init__` (line 641) 复用

4. **4 changed Python files 的 Ruff format 真实 exit 0**:
   ```
   Changed files: dayu/cli/arg_parsing.py, dayu/fins/download_contract.py,
                  tests/cli/test_fins_commands.py, tests/service/test_fins_direct.py
   ruff check <4 files>    → All checks passed! exit 0
   ruff format --check <4 files> → 4 files already formatted exit 0
   ```

5. **Tests/coverage/pyright** (来源 fix artifact §4):
   - 549 passed (affected tests); 1020 passed (full focused union)
   - `arg_parsing.py` 99%, `download_contract.py` 88% (均 ≥ 80%)
   - compileall PASS; pyright 0 errors, 0 warnings

### 无新增 Finding

沿完整 call chain 重新确认:
- `_validate_download_mutation_mode` 唯一 semantic owner
- `_prevalidate_download_request` → `build_direct_download_request` → `build_fins_download_request` → `FinsDownloadRequest.__post_init__` 调用链未变
- 冲突拒绝仍在 workspace resolution 与 service factory 之前
- 无新增 import、类型、异常类型、状态机或 I/O 路径
- 无 scope drift（仅 4 allowed Python files，无 README/Slice 2/3 文件进入 diff）

## Open Questions

无。

## Residual Risk

| 风险 | 状态 |
|---|---|
| detached clean worktree / installed CLI evidence | deferred to later approved slice (accepted plan §9.2) |
| README F12 互斥语义投影 | deferred to Slice 3 |
| F13/F14 未实现 | deferred to Slice 2/3 |
