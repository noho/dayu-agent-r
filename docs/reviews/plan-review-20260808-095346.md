# PR 190 / F17 Plan Review

- **Review artifact**: `docs/reviews/plan-review-20260808-095346.md`
- **Review time**: 2026-08-08 09:53:46 +0800
- **Reviewed target**: `docs/gateflow/pr-190-f17-plan-20260807.md`
- **Baseline commit**: `e1217811ad57e48c90e3763994930e53378ba060`（plan 所依据的 goal confirmation baseline）
- **Current HEAD**: `ce0c171a022a073c6355ace44e7c5e34a668d4bb`
- **Review type**: adversarial plan review，按 Gateflow plan review gate 执行

## 1. Reviewed Scope

Plan 声称的目标：修复冻结 workspace publication manifest 中 `config/prompts/scenes/conversation_compaction_user.md` 的单一 digest 漂移，使 prompt raw bytes、manifest asset entry、manifest raw bytes digest pin 三者重新一致。

Plan 声称的修复边界：恰好两个 hunk——
1. `docs/cli_init_workspace_manifest_v1.json`：替换目标 asset 的 `content_sha256`
2. `tests/cli/test_smoke_cli_init_provider_matrix.py`：替换 `FROZEN_MANIFEST_SHA256`

Plan 声称的非目标：不修改 prompt 内容、产品行为、validator 逻辑、Oracle/Scenario registry、5/43/16 库存常量。

## 2. Binding Contract

Goal confirmation artifact：`docs/gateflow/pr-190-f17-goal-confirmation-20260807.md`，状态 `pass`。Plan review 以此 goal confirmation 为 binding scope contract。

## 3. Assumptions Tested

| 假设 | 验证方法 | 结果 |
|---|---|---|
| 当前 prompt raw SHA-256 为 `22e7bc...c1c88` | `shasum -a 256` 对当前文件计算 | **成立**：实际值 `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88` |
| 基线 manifest entry 旧值为 `59b50e...87b76` | `git show e1217811:docs/cli_init_workspace_manifest_v1.json` 逐项核对 | **成立**：基线 commit 中该 entry 确为 `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76` |
| 基线 manifest raw SHA-256 为 `d95de6...a9f8` | `git show e1217811:tests/...` 核对 | **成立**：基线测试常量为 `d95de68e69b0aacc712ec6bf468c8604a91460a17f3e2497f397182517a6a9f8` |
| 修复后预测 manifest raw SHA-256 为 `064f8066...cae22` | 对已修复文件计算 `read_bytes()` digest | **成立**：实际值 `064f80660b2cba0f16db392a46e8dc68ac45fdcd31252f96423c854e342cae22` |
| 当前清单库存为 5/43/16 | JSON parse 后独立计数 | **成立**：directories=5, files=43, model_projection_owner_paths=16 |
| files 与 owners 严格排序且唯一 | 程序化逐项比对 | **成立**：两者均按 path 排序且无重复 |
| 仅目标 entry 含旧 digest | 逐 43 entries 扫描旧 digest `59b50e...87b76` | **成立**：基线中仅目标 entry（index 39）命中；当前零命中 |
| 其余 42 entries 均不含旧 digest | 同上 | **成立**：0 matches |
| 71-test owner suite | `pytest --collect-only -q` | **成立**：71 tests collected |
| 生产 publication 行为无误 | fixture 代码审查 | **成立**：`snapshot_managed_roots → prepare_workspace_transaction → publish_workspace_transaction` 原样发布 package asset |
| `validate_publication_tree` 独立枚举 actual tree | 源码审查 | **成立**：不依赖 manifest file list，独立 rglob + read_bytes |
| `load_manifest` fail-closed | 源码审查 | **成立**：quantity/sort/uniqueness/schema digest regex/json_pointer 全量 fail-closed |
| OpenAI/Ollama projected paths 覆盖 | 源码审查 | **成立**：`openai_publication_tree` 与 `ollama_publication_tree` fixtures 均通过 `validate_projected_publication_tree` 验证 |

## 4. Architecture Boundary Review

### 4.1 Owner chain

```
prompt raw bytes (LLM-facing 内容真源，只读)
  → manifest asset entry (冻结 publication consumer，修复点)
    → FROZEN_MANIFEST_SHA256 (二级派生 consumer，从保存后 raw bytes 推导)
```

- **裁决：正确。** 语义 owner 唯一清晰。修复在 manifest（consumer boundary）执行，不在 prompt（upstream source）、production transaction（behavior owner）或 validator（audit owner）执行。
- 没有跨层穿透、反向依赖或共享可变状态引入。

### 4.2 Public contract / schema / state machine

- Contract：不变（恢复既有 frozen publication contract 一致性）
- Schema：不变（manifest keys、结构、版本、排序保持）
- State machine：不变（FIRST/PRESERVE/OVERWRITE/RESET 不受影响）
- **裁决：无架构边界违反。**

### 4.3 依赖方向

- `docs/cli_init_workspace_manifest_v1.json` 是用户冻结的 publication truth consumer，依赖 prompt raw bytes
- `tests/cli/test_smoke_cli_init_provider_matrix.py::FROZEN_MANIFEST_SHA256` 依赖 manifest raw bytes
- 依赖方向正确：consumer → source，无循环

## 5. Goal-bound Minimal Design Review

### 5.1 Scope vs Goal Confirmation

Plan 的 scope 与 goal confirmation 精确对齐：
- Goal：闭合 prompt/manifest/test 三级 digest chain ✓
- Non-goal：不修改 prompt、产品行为、validator、Oracle/Scenario/readiness ✓
- 没有 goal drift 或 scope creep

### 5.2 Overengineering Check

Plan 不引入新抽象、helper、schema、测试层级或兼容分支。编辑序列为纯机械操作：JSON parse → digest 计算 → 单值替换 → 保存 → 再计算 → 单值替换。**没有过度设计。**

### 5.3 Overcoupling Check

两个 hunk 是相互依赖的（manifest 变更后 manifest raw digest 必然变化），拆分会制造 manifest 已更新但测试 pin 未同步的中间 contract。合并更多变更则扩大风险。当前方案是满足 owner contract 的最小耦合。

## 6. Execution Semantics Review

### 6.1 Edit Sequencing

Plan 的六步编辑序列是完整的：

1. **Pre-state 与 mismatch audit**：`git status --short` + `shasum` + focused owner test → 预期 singleton digest mismatch
2. **只更新 manifest entry**：JSON parser 精确定位 → 断言旧 digest → 计算新 digest → 替换 → 保存（禁止 formatter）
3. **从已保存 raw bytes 更新二级 pin**：`Path.read_bytes()` → `hashlib.sha256()` → 交叉检查预测值 → 替换
4. **Post-state strict audit**：重跑 focused test → `issues==()` / 5/43/16 ✓
5. **完整验证与 changed-file guard**：71-test suite + JSON/pyright/Ruff/compileall + `git diff --word-diff`
6. 最后检查 `git diff --stat` 恰为两个单行 digest 替换

**裁决：编辑序列完整，每一步有明确的停止条件。**

### 6.2 Stop Conditions

Plan 列举了以下停止条件，全部在正确位置：

| 条件 | 位置 | 裁决 |
|---|---|---|
| pre-state git dirty paths 超出枚举的 5 个 evidence artifacts | Step 1 | **充分** |
| `shasum` 结果不符合预期 | Step 1 | **充分** |
| focused test 出现第二项 mismatch | Step 1 | **充分** |
| JSON parser 找不到目标 entry 或 digest 不等于旧值 | Step 2 | **充分** |
| 其余 42 entries 含旧 digest | Step 2 | **充分** |
| 保存后计算的 manifest digest 与预测交叉检查值不同且非目标 bytes 变化 | Step 3 | **充分** |
| post-state focused test 未通过 | Step 4 | **充分** |
| collect-only 不足 71 或 passed 不足 71 | Step 5 | **充分** |
| pyright/Ruff/compileall 非零 | Step 5 | **充分** |
| `git diff --check` 非零 | Step 5 | **充分** |
| changed-file guard 发现额外文件变更 | Step 5 | **充分** |

**裁决：停止条件与验证项充分覆盖。**

### 6.3 Rollback

Plan 的 rollback 程序：精确反向 patch 仅恢复两个 digest hunk — manifest entry 回旧值，测试 pin 回旧值。然后重算 digest、`git diff --check`、changed-file guard。不使用覆盖整个文件的命令。记录失败证据并回到 goal confirmation。

**裁决：Rollback 完整且安全。**

## 7. Test Coverage / Validation Sufficiency

### 7.1 Owner Test（修复后直接验证）

`test_frozen_manifest_matches_fresh_real_publication_tree` 通过 production `InitMode.FIRST` + production file lock + `validate_publication_tree` 独立枚举，断言：
- `report.valid` ✓
- `report.issues == ()` ✓
- 5 directories, 43 files, `.dayu-init.lock` 存在, 16 model owners ✓

**裁决：这是修复的直接 owner 级 assertion，覆盖充分。**

### 7.2 二级 Pin 稳定性测试

`test_checked_in_manifest_digest_is_stable_across_validation` 断言 digest before == pin == digest after。

**裁决：覆盖 manifest 不被 validation 改写。**

### 7.3 71-Test Suite

71 tests 覆盖：frozen manifest validation、loader 严格 schema 拒绝（非法 JSON、未知 key、错误版本、非法 digest、错误 json_pointer）、preflight classifier 全分支、availability classifier 全分支、endpoint redaction、bounded text summary、secret scan 全类别、persisted scan credential/canary/symlink/special-file 边界、no-fallback 全裁决分支、projected publication（OpenAI/Ollama）、reconciliation、live matrix 入口。

**裁决：71-test owner suite 对 manifest contract 的覆盖足以捕获回归。**

### 7.4 OpenAI/Ollama Projected Paths

`test_projected_publication_accepts_only_role_model_pointer_changes`（OpenAI）与 `test_projected_publication_validates_ollama_dynamic_owner_fields`（Ollama）分别验证非冻结默认模型的 role-based model pointer 投影。

**裁决：两个 projected path 均已覆盖，manifest 变更不会导致已修复的 owner test 在 projected 场景失败。**

## 8. Findings

经过 adversarial review，未发现 material finding。Plan 在以下维度均通过：

- **动机成立**：prompt bytes 变更后 frozen publication consumer 未同步是确定性错误
- **Root cause 同源**：直接证据来自 production `InitMode.FIRST` → `validate_publication_tree` → focused owner test 的 singleton mismatch
- **Owner chain 正确**：修复在唯一正确的 consumer boundary（manifest entry + 二级 pin）
- **Scope 精确**：恰好两个 hunk，无 scope creep
- **编辑序列可实施**：每一步有明确的输入、操作、预期输出和停止条件
- **停止条件充分**：覆盖 pre-state mismatch、非目标 bytes 变化、count 漂移、测试非全绿、changed-file 漂移
- **Rollback 安全**：精确反向 patch，不改其它文件
- **验证充分**：owner test + full suite + JSON/pyright/Ruff/compileall + changed-file guard
- **无过度设计/过度耦合**：不新增抽象、schema、测试或兼容分支

### 附加观察（非 finding）

Plan artifact 描述的是基线 commit `e1217811` 时的 pre-fix 状态。当前 HEAD（`ce0c171a`）已包含 commit `305c1012 test(cli): refresh frozen init publication digest` 执行的修复——manifest entry 已从 `59b50e...87b76` 更新为 `22e7bc...c1c88`，测试 pin 已从 `d95de6...a9f8` 更新为 `064f8066...cae22`。Plan 本身对 pre-fix 状态的描述经 `git show e1217811` 验证准确无误；当前状态差异是 plan→implementation gate 正常推进的结果，不属于 plan 缺陷。

## 9. Open Questions

无。

## 10. Residual Risks

| 风险 | Owner/Destination |
|---|---|
| 未来 package asset 再次漂移 | 现有 strict validator + frozen digest test 会 fail closed；由后续 work unit 处理 |
| 真实 provider / Oracle replacement scenarios 未重跑 | 由 PR 190 后续 work unit 处理；F17 只修 deterministic frozen publication consumer |

## 11. Final Conclusion

**`pass`**

Plan 的目标成立、owner chain 正确、scope 精确、编辑序列可实施、停止条件充分、验证完整、无过度设计或过度耦合。经过 adversarial review 未发现 material finding。
