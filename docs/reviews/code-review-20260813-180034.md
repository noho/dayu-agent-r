# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `56d159cb4bf13baf82858bb237b2f73075eaf717`
- Output file: `docs/reviews/code-review-20260813-180034.md`
- Included scope:
  - `dayu/fins/ingestion_runtime.py`（`_USAGE_MESSAGES` 一处文案）
  - `dayu/fins/pipelines/docling_upload_service.py`（admission matrix、skip owner、strict deleted reader）
  - `tests/cli/test_fins_commands.py`、`tests/fins/test_docling_upload_service.py`、`tests/fins/test_fins_ingestion_runtime.py`
  - `docs/gateflow/uf-fix02-action-and-update-identity-s1-implementation-20260813.md`
  - 沿真实调用链走读的既有生产文件（只读，无 diff）：`dayu/cli/commands/fins.py`、`dayu/fins/service_runtime.py`、`dayu/fins/pipelines/sec_upload_workflow.py`、`dayu/fins/pipelines/cn_pipeline.py`、`dayu/fins/storage/_fs_source_document_core.py`、`dayu/fins/storage/_fs_source_snapshot.py`、`dayu/fins/upload_failure.py`
- Excluded scope: committed plan 历史（`uf-fix02-action-and-update-identity-plan-20260813.md` 未读取）、S2、AgentMiMo 本轮 artifact/结论
- Parallel review coverage: 无（未启动子 agent）

已沿真实路径逐行确认的链路：

- CLI `upload_filing`：`_prevalidate_upload_filing_request`（`dayu/cli/commands/fins.py:240`）→ `prevalidate_fins_upload_filing_request_for_workspace`（`dayu/fins/service_runtime.py:61`，`create_directories=False` 只读仓储）→ `validate_fins_upload_filing_request`（admission matrix，`ingestion_runtime.py:969-977`）→ `FinsUploadUsageError` → `run_fins_direct_command` 捕获后 `EXIT_USAGE_ERROR`、精确单行 stderr（`dayu/cli/commands/fins.py:205-207`）。prevalidation 位于 `FINS_DIRECT_SERVICE_FACTORY`（`fins.py:244`）之前，冲突路径零 factory、零 workspace 写入——与新增 CLI 测试断言一致。
- workflow fresh recheck：`run_upload_filing_stream`（`sec_upload_workflow.py:167-186`）/ CN 等价路径（`cn_pipeline.py:787-806`）在 conversion 前重读 `read_filing_upload_state` 并重新调用同一 owner validator，`authoritative_request.resolved_action` 覆盖 stale preflight action，`_assert_authoritative_filing_identity` 守 identity。
- `prepare_upload`（`docling_upload_service.py:226-356`）：update-missing 现在无条件（±overwrite）在 `_validate_source_files` 后、converter 前抛 `FileNotFoundError`，对 filing/material 均生效；`_can_skip_upload` 消费 canonical `is_deleted`。
- publication：`commit_prepared_upload_batch` 的 rollback linearization 未受影响；`_resolve_upsert_mode` 的 upsert 分支在 S1 后经 `prepare_upload` 入口不可达（遗留删除归 S2）。
- storage canonical：`_fs_source_document_core.py:1703` create/update `setdefault("is_deleted", False)`，`:1791` delete 写精确 bool；`fins_upload_failure_from_exception` 对未知异常产出 closed RUNTIME 文案，无原始异常文本外泄。

## Findings

### 1-未修复-中-evaluate_upload_overwrite_precondition 的 CREATE_TARGET_EXISTS 对 material 被 prepare_upload 静默忽略，material create-existing 冲突未在 conversion 前 fail closed

- **入口/函数**: `run_upload_material_stream`（SEC）/ CN material stream → `DoclingUploadService.prepare_upload`
- **文件(行号)**: `dayu/fins/pipelines/docling_upload_service.py:295-296`（`source_kind is SourceKind.FILING` 守卫）、`:304`（`_can_skip_upload`）、`:1078-1108`（`_resolve_upsert_mode`）；`dayu/fins/pipelines/sec_upload_workflow.py:498-504`（material previous_meta 为 fresh 读）、`:606-633`（`str(exc)` 投影）
- **输入场景**: 显式 `--action create` + stable material ID 已存在（previous_meta 非空）+ 无 `--overwrite`；两种子场景：输入指纹与既有相同 / 不同
- **实际分支**: owner 已返回 `CREATE_TARGET_EXISTS`，但 `prepare_upload` 的 raise 仅对 `SourceKind.FILING` 生效，material 落入 `_can_skip_upload`：同指纹 → `is_deleted=False` 且指纹相等 → 返回 True → 结果 `status="skipped"`；不同指纹 → Docling conversion 执行完毕后，publication 阶段 `_resolve_upsert_mode(action="create", previous_meta 非空, overwrite=False)` 才抛 `FileExistsError("创建目标已存在")`，由 material stream `except Exception` 以 `str(exc)` 写入 failed event
- **预期行为**: 按 goal confirmation「fresh published state 可判定的 create-existing/update-missing 冲突在 Docling conversion 前 fail closed」与 shared admission owner 的 closed matrix，material create-existing 应在 converter 前以同一 typed conflict 语义失败
- **实际行为**: 同指纹时显式 create 被静默吸收为 `skipped` 成功（`skip_reason="already_uploaded"`），与 owner 判定的 conflict 矛盾；不同指纹时浪费一次完整 Docling conversion 后以内部文案 `"创建目标已存在"` 失败，投影面与 filing 的 exit-2 typed usage error（`"create 目标已存在；请改用 update 或允许覆盖"`）不一致。S1 测试仅固定 filing create-existing pre-conversion（`test_prepare_upload_rejects_existing_filing_create_before_conversion`），material 无对应 converter-zero-call 覆盖；implementation artifact 声称 shared owner 拥有 admission matrix，但未在 residual 表登记该 material 缺口
- **直接证据**: `docling_upload_service.py:295` 的条件守卫使 `:294` 评估出的 `CREATE_TARGET_EXISTS` 对 material 不产生 raise；`:304-305` skip 决策只看 `is_deleted`/指纹，不消费 precondition 结果；`_resolve_upsert_mode:1104-1108` 的 `FileExistsError` 位于 `_store_upload_assets`（conversion 之后）调用链内
- **影响**: admission 语义按 source_kind 分裂；显式 create 被静默报告成功（用户无法区分 no-op 与冲突）或高成本转换后失败；「filing/material shared owner」承诺未闭合
- **建议改法和验证点**: 在 `prepare_upload` 中删除 FILING 守卫，`CREATE_TARGET_EXISTS` 无条件 raise；material 路径补 converter-zero-call 与精确错误断言（与 filing parity 对齐）；material stream 的失败投影改走 typed reason 而非 `str(exc)`。若 approved plan 已将该缺口显式划归 S2（如「跨市场 workflow 集成改造」），则本 finding 应由 Controller 裁决为 deferred-with-owner，但 S1 artifact 需补登记该 residual，否则 shared-owner 声明不成立
- **修复风险（中）**: material create 幂等重传（同指纹 skip）若被既有调用方依赖，行为会从静默成功变为 usage 冲突；需确认 batch 工具与既有 material 用例
- **严重程度（中）**:

### 2-未修复-低-canonical is_deleted 严格读取规则被逐字复制，同一语义在代码库已有三个独立 reader

- **入口/函数**: `_require_source_deleted_flag`（本次新增）与既有 `dayu/fins/storage/_fs_source_snapshot.py:996` `_require_deleted_flag`
- **文件(行号)**: `dayu/fins/pipelines/docling_upload_service.py:1007-1026`；`dayu/fins/storage/_fs_source_snapshot.py:996-1012`；`dayu/fins/ingestion_runtime.py:5102`
- **输入场景**: 维护演进场景——UF-FIX08 或后续 fail-closed 规则调整时，需同步修改所有 reader
- **实际分支**: skip owner 与 storage snapshot reader 用两份逐字相同（含错误文案）的实现读取同一 canonical 字段；第三处 `ingestion_runtime.py:5102` `bool(meta.get("is_deleted", False))` 仍是 loose truthiness + 默认 False 的旧读法
- **预期行为**: 多个消费者复用同一 source of truth / public contract / projection helper，读取规则单一 owner（CLAUDE.md 语义所有权约束）
- **实际行为**: 新增了第二份严格 reader 复制体；storage 版本为私有模块符号，未通过 `dayu.fins.storage` 公共面导出，服务层无法复用。规则（含错误文案）今后演进时两份实现存在漂移风险，loose reader 的存在证明该漂移模式已真实发生过
- **直接证据**: 两函数体与两条错误消息逐字相同（`"source meta 缺少 is_deleted"` / `"source meta is_deleted 必须为布尔值"`）；`grep` 确认 `_require_deleted_flag` 未被 `dayu/fins/storage/__init__.py` 导出
- **影响**: 语义读取规则多真源，fail-closed 契约演进成本与漂移风险增加（维护性/语义所有权）
- **建议改法和验证点**: 将 strict reader 提为 `dayu.fins.storage` 公共契约符号，`docling_upload_service` 直接 import 复用；`ingestion_runtime.py:5102` 的 loose 读法登记给 UF-FIX08（S1 不动）；验证点：全仓仅一处读取实现
- **修复风险（低）**: 公共导出 + 替换 import，无行为变化
- **严重程度（低）**:

### 3-未修复-低-CLI 测试 fixture 迁移后不再有任何测试证明 action="update" 被投影到 Service

- **入口/函数**: `test_upload_commands_map_args_and_validate_files`
- **文件(行号)**: `tests/cli/test_fins_commands.py:1713-1716`（`update`→`create`）、`:1759`（期望调用 `action="create"`）、`:1774`（`action="auto"`）；冲突矩阵 `:1208/:1215` 的 update 仅出现在被拒绝路径（factory 未调用）
- **输入场景**: 未来 CLI 胶水层回归——例如 action 字段规范化、`--action update` 分支条件或 overwrite 与 update 的组合投影被改坏
- **实际分支**: 全部 CLI 测试中 `--action update` 均以 usage 冲突退出（exit 2、factory 前），唯一进入 Service 的成功投影测试现使用 create/auto
- **预期行为**: 迁移 fixture 不应损失对既有语义的回归兜底；update（含 update+overwrite 成功路径）是 CLI 公开契约的一部分，应有 CLI 层证据
- **实际行为**: action="update" 与 update+overwrite=True 的 Service 投影在 CLI 层无任何测试覆盖；该覆盖此前由旧 fixture（fresh missing + update + overwrite）提供，迁移时随禁止行为一并移除，未以 seeded existing target 的成功 case 补回
- **直接证据**: 全文件 `action=` 断言仅剩 `create`（1759）与 `auto`（1774）；迁移 diff 显示旧 `"update"` 期望被替换
- **影响**: CLI 层 update 投影回归无测试兜底（service 层 update 行为已覆盖，风险局限于 CLI 胶水）
- **建议改法和验证点**: 新增基于 seeded existing target（复用 `_seed_cli_filing_source`）的 CLI update / update+overwrite 成功映射断言，或至少在现有冲突矩阵旁增加「update-existing 到达 service」case
- **修复风险（低）**: 纯测试补充
- **严重程度（低）**:

### 4-未修复-低-prepare_upload 的 Raises docstring 未登记损坏 meta 的新增异常面

- **入口/函数**: `DoclingUploadService.prepare_upload`
- **文件(行号)**: `dayu/fins/pipelines/docling_upload_service.py:259-265`（Raises 列表）vs `:1007-1026`（`_require_source_deleted_flag` 实际抛 KeyError/ValueError）
- **输入场景**: 既有 source meta 缺 `is_deleted` 或值为非布尔（corruption）时执行任何非 overwrite 的既有目标上传
- **实际分支**: `_can_skip_upload` → `_require_source_deleted_flag` 抛 `KeyError("source meta 缺少 is_deleted")` / `ValueError(...)`，经 `prepare_upload` 向调用方传播
- **预期行为**: public 函数的 docstring 完整登记参数、返回、异常（CLAUDE.md 编码硬约束）；新增契约在 helper 层已登记，但对外入口未同步
- **实际行为**: `prepare_upload` 的 Raises 仅列 ValueError（描述为「参数非法」）、FileNotFoundError、FileExistsError、RuntimeError、OSError；新路径的 KeyError 与 corruption ValueError 语义未被登记
- **直接证据**: `docling_upload_service.py:259-265` 原文与 `:1016-1018`、`:1024-1025` 实际抛出对照
- **影响**: 对外异常契约文档不完整，调用方无法从契约层面区分参数错误与 corruption fail-closed
- **建议改法和验证点**: 在 `prepare_upload` Raises 中补 `KeyError`/`ValueError`（corruption）条目
- **修复风险（低）**: 仅 docstring
- **严重程度（低）**:

## Open Questions

- material create-existing admission 是否已在 approved plan 中显式划归 S2？Finding 1 的裁决（fix now / deferred-with-owner）取决于此；本 review 被禁止读取 committed plan，S1 implementation artifact 未登记该缺口，需 Controller 对照 plan 裁定。
- 历史 published meta 是否可能合法缺 `is_deleted`（非 upload 生产路径写入）？storage create/update `setdefault(False)`、delete 写 True 已覆盖经仓储的写入；但若存在绕过仓储或旧版本写入的存量 meta，`_can_skip_upload` 将 KeyError fail-closed——按 fresh-schema 规则与 UF-FIX08 归属是设计决策，静态阅读无法证实存量分布。

## Residual Risk

- 未独立复跑测试/pyright（按任务指示不做与判断无关的重验证）；GREEN/RED 证据来自 implementation artifact，未经本 review 独立执行确认。
- Finding 1 的「投影不一致」部分基于 workflow 层 `str(exc)` 证据；material failed event 在 `fins_direct` Service 层与 CLI 的最终用户可见面（退出码/打印内容）未逐行走完，若 Controller 需要可作为 Finding 1 的补证项。
- `_resolve_upsert_mode` 的旧 upsert 分支（update+missing+overwrite→create）在 S1 后经 `prepare_upload` 入口不可达，但仍是活代码；`rg` 显示其唯一调用方为 `_store_upload_assets`，S2 删除前风险低。
- prevalidation → fresh recheck → publication 同请求竞争的 usage-error 投影归属 UF-FIX10，本 review 未覆盖该竞争窗口。
