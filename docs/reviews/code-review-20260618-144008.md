# WU-CM-12 S1 Focused Re-Review — DS-F1 Fix Verification

## Scope

- Mode: current changes (focused re-review)
- Branch: wu-cm-12-conversation-memory-drift
- Base: HEAD (uncommitted workspace changes)
- Output file: docs/reviews/code-review-20260618-144008.md
- Reviewed artifacts:
  - Original review: `docs/reviews/code-review-20260618-142551.md`
  - Adjudication: `docs/reviews/code-review-wu-cm-12-s1-adjudication-20260618-143543.md`
  - Fix artifact: `docs/reviews/wu-cm-12-s1-fix-codex-20260618.md`
- Focus: DS-F1 accepted finding fix verification only
- Parallel review coverage: 无

## Verification Checklist

### DS-F1: `_facts_from_accepted_event` empty-label early return preserves accumulated diagnostics

**Status: PASS**

**Evidence chain:**

1. **Original finding** (`code-review-20260618-142551.md` Finding 1):
   - `_facts_from_accepted_event` at line 1824-1834 performed `return ((), (...))` when encountering empty evidence labels
   - This discarded previously accumulated `BUDGET_LIMIT_REACHED` diagnostics from oversized facts
   - Impact: operator看不到 oversized-fact 被 skip 的诊断信号

2. **Adjudication** (`code-review-wu-cm-12-s1-adjudication-20260618-143543.md`):
   - DS-F1 accepted, fix required before S1 acceptance
   - Required fix: preserve accumulated diagnostics in early return + add mixed regression fixture

3. **Fix implementation** (current workspace):
   - `dayu/host/memory.py:1824-1835`: early return now uses `tuple(diagnostics) + (...)`
   - This preserves all previously accumulated diagnostics (including `BUDGET_LIMIT_REACHED` from oversized facts)

4. **Regression test** (`tests/host/test_memory_projection.py:743-773`):
   - `test_accepted_compact_preserves_budget_diagnostic_before_invalid_fact`
   - Creates fixture with oversized fact (triggers `BUDGET_LIMIT_REACHED`) + empty-label fact (triggers `EVIDENCE_BACKED_FACT_CANDIDATE_INVALID`)
   - Asserts diagnostics tuple contains both reasons in order
   - Asserts facts remain empty (consistent with DS-F2 deferred semantics)

5. **Validation results:**
   - `pytest tests/host/test_compact_material.py tests/host/test_memory_projection.py tests/host/test_run_input_builder.py -q`: **118 passed in 0.78s**
   - `pyright dayu/host/compact_material.py dayu/host/memory.py dayu/host/run_input.py tests/host/test_compact_material.py tests/host/test_memory_projection.py tests/host/test_run_input_builder.py`: **0 errors, 0 warnings, 0 informations**
   - `git diff --check`: clean

### DS-F2: facts clearing semantics unchanged

**Status: PASS (deferred semantics preserved)**

**Evidence:**
- `dayu/host/memory.py:1826`: early return still returns empty facts `()`
- This matches the deferred DS-F2 semantic: "invalid candidate invalidates fact materialization for this compact event"
- No change to the pre-existing behavior where a single invalid candidate drops all accumulated valid facts

### No new regressions

**Status: PASS**

**Evidence:**
- All 118 S1-affected tests pass
- pyright clean on all changed files
- No whitespace errors (`git diff --check` clean)
- Fix only modifies the return tuple assembly in one branch, no logic changes elsewhere

## Findings

未发现实质性问题。

DS-F1 fix correctly preserves accumulated diagnostics in the empty-label early return path, as required by the adjudication. The mixed oversized + empty-label regression fixture covers the exact failure scenario. DS-F2 deferred semantics (facts clearing) remain unchanged. No new regressions detected.

## Open Questions

- 无。

## Residual Risk

- DS-F2 deferred finding remains open for S3 provenance guard / compact fact rendering review, as planned.
- S2/S3/S4/S5 subsequent slice risks are tracked by the WU-CM-12 control document.

---

## Verdict

**PASS** — DS-F1 fix verified. Ready for S1 acceptance gate.
