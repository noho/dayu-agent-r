# Plan Review: wu-cli-interactive-02 S5/F13 Durable Builder Amendment — Final Independent Re-Review

## 0. Artifact metadata

- **Review type**: Final independent adversarial re-review（plan amendment）
- **Provider**: DeepSeek
- **Reviewer**: AgentDS
- **Timestamp**: 2026-08-01T23:11:20+08:00
- **Base HEAD**: `ec9342ed9e5584123618f6b5c5eba8e93e2aed94`
- **Branch**: `codex/interactive-oracle`
- **Reviewed artifacts**:
  - `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md`（目标 plan，第二次 amendment 后）
  - `docs/reviews/wu-cli-interactive-02-s5-f13-durable-builder-plan-amendment-proposal-codex.md`（Codex proposal）
  - `docs/reviews/gateflow-wu-cli-interactive-02-s5-durable-builder-amendment-review-adjudication-20260801.md`（Controller adjudication）
- **Review scope**: 验证 Controller §7 accepted AgentDS finding 已在 §9.1/§10.5/§13 与 proposal 同步闭合；验证拒绝的两条 MiMo 意见没有导致 30-file duplicate list 或新抽象；独立重跑只读 inventory；检查 scope、owner、sequencing、反例、overengineering、validation/residual risk
- **Review constraints**: 只读；不修改 plan、proposal、代码或任何既有 artifact；不 commit/push/PR

## 1. Review summary

本次 re-review 对第二次 S5/F13 durable-builder plan amendment 做最终独立 adversarial 验证。核心结论：**pass** — Controller §7 的 accepted AgentDS finding 已在 plan §9.1/§10.5/§13 与 proposal §3.2 同步闭合；拒绝的两条 MiMo 意见未导致 30-file duplicate list 或新抽象；独立 inventory 与 plan/proposal 声称一致；scope/owner/sequencing 正确；无反例遗漏；无 overengineering；residual risk 分类完整。

## 2. Assumptions tested

| # | Assumption | Evidence | Verdict |
|---|---|---|---|
| A1 | AgentDS final finding 001 已闭合：不再硬编码 `case_label`/`operation_label`/`attempt_label` | `rg` 确认 plan 正文无一硬编码；仅 fix trace §15 记载变更历史 | ✅ 成立 |
| A2 | MiMo final finding 001 (rejected) 未导致 30-file duplicate list | `rg` 确认仅 fix trace 提及 "30-file"，无第三份枚举清单 | ✅ 成立 |
| A3 | MiMo final finding 002 (rejected) 未导致新抽象 | plan/proposal 无新增 abstraction/helper type/万能 factory | ✅ 成立 |
| A4 | 8-file / 15-call builder inventory 与 HEAD 一致 | 独立 `rg` 复现：accepted `8/6`、rejected `7/4`、union 8 | ✅ 成立 |
| A5 | 25-file identity/typed-return closure 完整保留 | 独立 `rg` 复现：35 FA + 4 OA + 7 CR = 25 dedup | ✅ 成立 |
| A6 | 5-file delta 各 call site 确有足够真实上下文构造 event-unique identity | 逐文件读取确认：均至少有 `operation_id`；proactive 另有 `attempt_number` | ✅ 成立 |
| A7 | orphan/incomplete/exhausted 三者均为 `quality_check_rejected` → mapping | 代码确认三者共用 `_rejected_payload()` 且 failure_category 硬编码为 `quality_check_rejected` | ✅ 成立 |
| A8 | S5 validation commands 覆盖全部 8 builder files | §10.5 的 focused/full Host commands 逐文件核对 | ✅ 成立 |

## 3. Independent inventory verification

### 3.1 Strict durable builder calls（独立 `rg` 复现）

**Accepted builder (`build_context_compacted_payload`): 8 calls / 6 files**

| 文件 | 行号 | 计数 |
|---|---|---|
| `tests/host/test_memory_projection.py` | 449 | 1 |
| `tests/host/test_compaction_terminal.py` | 650 | 1 |
| `tests/host/test_context_compact_events.py` | 494, 515, 1116 | 3 |
| `tests/host/test_run_input_builder.py` | 7134 | 1 |
| `tests/host/test_compact_material.py` | 3605 | 1 |
| `tests/host/test_dispatch_scheduler.py` | 10559 | 1 |

**Rejected builder (`build_context_compaction_attempt_rejected_payload`): 7 calls / 4 files**

| 文件 | 行号 | 计数 |
|---|---|---|
| `tests/host/test_proactive_compaction_operation.py` | 268 | 1 |
| `tests/host/test_context_compact_events.py` | 785, 833, 856, 1020 | 4 |
| `tests/host/test_compaction_operation.py` | 914 | 1 |
| `tests/host/test_dispatch_scheduler.py` | 11422 | 1 |

**Union**: 8 deduplicated files. **Delta** (previously not in S5 allowed tests): 5 files. 与 plan §9.1 和 proposal §2.3 完全一致。

### 3.2 Identity/typed-return closure（独立 `rg` 复现）

- `FinalAnswerData(...)` direct constructors: 35 calls across 19 files ✅
- `EngineRunOutcomeFinalAnswer(...)` direct constructors: 4 calls across 3 files (1 overlap) ✅
- `ContextCompactor` typed-return files: 7 files (3 overlap) ✅
- Deduplicated union: 25 files ✅
- Grand total (25 + 5 builder delta): 30 deduplicated test/test-support files ✅

### 3.3 Call site context availability（逐文件抽样）

| 文件 | 可用上下文 | 能否构造 deterministic event-unique identity |
|---|---|---|
| `test_memory_projection.py:449` | `operation_id="event-context-compaction-requested-1"` | ✅ 是 |
| `test_compaction_terminal.py:650` | `operation_id` (函数参数) | ✅ 是 |
| `test_run_input_builder.py:7134` | `operation_id` (函数参数) | ✅ 是 |
| `test_compact_material.py:3605` | `operation_id="operation-compact-test"` | ✅ 是 |
| `test_proactive_compaction_operation.py:268` | `operation_id` + `attempt_number` (函数参数) | ✅ 是 |
| `test_context_compact_events.py:*` | 各 call site 有独立 `operation_id` | ✅ 是 |
| `test_compaction_operation.py:914` | `operation_id="operation-quality-rejected"` + `rejected.attempt_number` | ✅ 是 |
| `test_dispatch_scheduler.py:10559/11422` | `operation_id` (局部变量) | ✅ 是 |

无一 call site 需要虚构不存在的维度。所有 call site 至少有 `operation_id`，部分另有 `attempt_number` 或 Engine run context。

## 4. Controller §7 accepted finding closure verification

### 4.1 AgentDS final finding 001: `accepted-low-clarification`

**Controller 要求**: 将写死的 `case_label` / `operation_label` / `attempt_label` 改为 caller 使用当前 helper/call site 实际已有的显式、非敏感、足以区分 event 的上下文。

**Closure evidence**:

- **Plan §9.1** (lines 638-650): "caller 必须使用当前 helper/call site 实际已有的显式、非敏感且足以区分 event 的上下文（例如 case label、`operation_id`、attempt/run id 或显式 ordinal），由该 factory 构造 deterministic 且对该 event 唯一的 `SuccessfulRunnerResponseIdentity`；具体输入维度与参数名以现有 helper/call site 为准，不要求为统一形状虚构不存在的维度。"
- **Plan §10.5** (lines 956-959): 同样弹性措辞，不硬编码参数名。
- **Plan §13 S5 checklist** (line 1121): 同样弹性措辞。
- **Proposal §3.2** (lines 160-168): 同样弹性措辞，并明确 "已有 proposal manifest / compactor Engine run context 时，caller 必须显式传入对应 `compactor_engine_run_id` 给 factory"。

硬编码 `case_label` / `operation_label` / `attempt_label` 仅出现在 plan §15 fix trace (line 1220) 作为变更历史记录。✅ **已闭合。**

### 4.2 MiMo final finding 001: `rejected-duplicate-inventory`

**Controller 决定**: 拒绝新增第三份 30-file 全清单。

**Closure evidence**: `rg '30.*file.*list|30.*file.*清单'` 仅在 plan §15 fix trace (line 1218) 命中，该处明确记载 "不复制第三份 30-file 全清单"。plan §9.1 完整保留 25-file closure table 与 8-file builder inventory table，仅以一句 "S5 枚举的机械闭包总 union 从 25 增至 30 个去重 test/test-support files" 说明总数，不枚举。✅ **已闭合。**

### 4.3 MiMo final finding 002: `rejected-already-covered`

**Controller 决定**: 拒绝新增抽象；现有规则已覆盖 event uniqueness、sibling consistency、required argument 及禁止项。

**Closure evidence**: 搜索 plan/proposal 无新增 abstraction type、helper class、万能 factory、shared singleton 定义。所有 identity 构造规则均以具体约束形式写入 §9.1/§10.5/§13，不引入新的抽象层。✅ **已闭合。**

## 5. Architecture boundary review

### 5.1 Semantic ownership

| 语义 | Owner | 验证 |
|---|---|---|
| Engine success response identity | `dayu/engine/contracts/runner_identity.py` → `_FinalDecision` → `FinalAnswerData` → `EngineRunOutcomeFinalAnswer` | 单向流，不反推 |
| Host compactor typed proposal | `dayu/host/compaction.py` `CompactorProposal` + `LLMContextCompactor` | 只消费 Engine typed outcome |
| Host durable payload builder | `dayu/host/context_events.py` | required typed 参数，无 default/fallback |
| Test fixture identity | 各 file-local private typed factory | 不跨文件共享，不伪装 provider evidence |

Owner 链清晰：Engine → Host compactor → Host durable builder。无循环依赖、无跨层穿透。✅

### 5.2 Dependency direction

S4 terminal guard → S5 required identity payload extension。S4 guard 不预埋 optional identity；S5 在 winner 路径上做 required typed 扩展。这是正确的分层依赖：guard 只决定 "能否提交 terminal"，不拥有 "terminal payload 写什么"。✅

## 6. Overengineering review

逐项检查 plan/proposal 是否引入不必要的抽象、builder、wrapper、protocol、migration 或 generalization：

| 检查项 | 结果 |
|---|---|
| 新增 abstraction type / helper class | ❌ 无 — file-local private factory 是最小机械迁移 |
| 新增跨文件万能 helper / shared singleton | ❌ 无 — plan 多处明确禁止 |
| 新增 builder/wrapper/facade | ❌ 无 — 只扩展现有 `context_events.py` owner signature |
| 新增 migration / compatibility layer | ❌ 无 — 明确 "fresh schema 直接起库，不兼容旧 payload" |
| 新增 protocol / interface | ❌ 无 |
| 新增 DB schema / table / index | ❌ 无 |
| S4 guard 预埋 optional identity | ❌ 无 — plan §9 开头明确禁止 |
| 5-file delta 借机扩展 scope | ❌ 无 — proposal §3.2 明确 "只允许补齐 required typed 参数或同步 exact payload fixture" |

**结论**: 无 overengineering。✅

## 7. Sequencing review

| 依赖 | 顺序 | 验证 |
|---|---|---|
| S4 terminal guard → S5 identity payload | S4 先完成，S5 在其 winner 路径扩展 | plan §9 开头明确此依赖 |
| Owner signature → consumer migration | 先改 `context_events.py` 两个 builder signatures，再迁移 8 files / 15 calls | plan §9.1 + proposal §3.2 明确 |
| 25-file identity/typed-return → 8-file builder | 第一 amendment 的 25-file closure 不受影响，本次只加 5-file builder delta | plan §9.1 明确两套 inventory 独立 |

Sequencing 正确，无循环依赖，无 slice 间竞争。✅

## 8. Counter-example coverage

Plan §9.6 覆盖了以下关键反例：

| 反例 | Plan 覆盖 | 状态 |
|---|---|---|
| ordinary Run final identity A ≠ compactor attempt 1 identity B ≠ attempt 2 identity C | §9.6 "operation exact A/B/C 串线反例" | ✅ |
| LENGTH continuation: 仅最后终结 run 的 call identity 进入 final | §9.2 + §9.6 length continuation exact test | ✅ |
| LENGTH 预算耗尽仍以 LENGTH 结束 → compactor fail closed | §9.3 + §9.6 | ✅ |
| Engine success but parse/schema fail → rejected attempt 仍携带 identity | §9.3.2 | ✅ |
| provider_request_id unavailable → explicit `unavailable` + `null` | §9.6 present/unavailable pair test | ✅ |
| effective provider/model mismatch → fail closed | §9.6 LLM compactor test | ✅ |
| engine run mismatch (run_id 不匹配 manifest) → fail closed | §9.6 | ✅ |
| endpoint/credential/header/secret canary 泄漏 | §10.5 secret canary scan + §9.6 | ✅ |
| proactive orphan/incomplete/exhausted → mapping not null | §9.1/§9.6/§13 明确三者均为 mapping | ✅ |
| transport/timeout/cancel/Engine failed no-final → null | §9.4 + §9.6 | ✅ |

反例覆盖充分。✅

## 9. Validation plan review

### 9.1 Focused test coverage

§10.5 的 S5 focused Host command 包含全部 8 builder files（3 既有 + 5 delta）。Full affected Host/Engine 回归也实际收集这些文件。✅

### 9.2 Pre/post inventory

§10.5 定义了 5 条 `rg` inventory 命令覆盖第一 amendment 的 identity/typed-return closure 和本次 builder closure。Pre-inventory baseline（8/6、7/4、8-file union）已冻结。Post-inventory 要求重现同一 baseline 且无新文件 hit。✅

### 9.3 Pyright / coverage

§10.5 要求 `python -m pyright dayu/ tests/ utils/` 零新增错误，逐文件 branch coverage ≥ 80%。覆盖了 required field 遗漏和类型不匹配的检测。✅

### 9.4 Residual risk in validation

Plan §12.1 分类了所有 residual risk：terminal capability、input race、queue lifecycle、recovery clock、process proof、compaction concurrency、live/fresh owner、provider evidence、secrets、coverage/platform。分类完整，均为已知且分类准确。✅

## 10. Open questions

经逐项检查 plan §12.2 及全文，无阻塞 open question：

- 所有 §12.2 列出的决策已固定，implementation 不得自行改向
- PTY 环境依赖已按 §12.1 分类为 allowed platform/capability variant
- 无未分类的设计空白

## 11. Residual risks

| 风险 | 分类 | 评估 |
|---|---|---|
| file-local private factory 实现不一致（5 个文件各自定义 factory，可能对同一语义产生不同形状） | 低 — plan §9.1/§10.5/§13 已冻结 deterministic、event-unique、sibling-consistent 约束 | 后续 code review gate 可检测 |
| `test_dispatch_scheduler.py:11422` 的 `failure_category="proposal_failed"` 是否应填 mapping 或 null | 低 — plan 规则已明确：仅 transport/timeout/cancel/Engine failed no-final → null；`proposal_failed` 语义需 implementation 时按此规则判断 | 不影响 plan correctness |
| pytest/pyright/coverage 尚未运行（plan gate 设计如此） | 已分类 — 由获批后的 S5/S6 validation 执行 | 标准 Gateflow 流程 |
| HEAD 之后新增 builder call file | 已分类 — §10.5 pre-inventory fail closed | 标准 defense |

无 unclassified residual risk。无 blocking issue。

## 12. Findings

经完整 adversarial review，**零 material finding**。

补充说明（非 finding，仅观察）：

1. **File-local factory 一致性依赖实施纪律**：5 个 delta 文件各自定义 private typed identity factory，plan 已用具体约束（deterministic、event-unique、sibling-consistent、无 default、无共享 singleton、无跨文件万能 helper、无 manifest/sibling 反推）限定形状。这在 plan 层面已足够具体；实际一致性由 S5 implementation 后的 focused tests + pyright + code review gate 验证。MiMo 曾提出此担忧（final finding 002），Controller 已正确裁决为 "already covered"。

2. **`test_dispatch_scheduler.py` 的两处 builder call 位于超大文件深处（行 10559/11422）**：caller 的 `operation_id` 来自外层 seeded fixture，需确认 implementation 时该变量在调用点确实在作用域内。这不影响 plan correctness，属于 implementation 级别的机械检查。

## 13. Final conclusion

**Verdict: PASS**

理由：

1. Controller §7 的唯一 accepted finding（AgentDS final finding 001）已在 plan §9.1/§10.5/§13 与 proposal §3.2 中完整闭合。弹性措辞 ("使用当前 helper/call site 实际已有的显式、非敏感、足以区分 event 的上下文") 替代了硬编码参数名，同时保持了 deterministic、event-unique、sibling-consistent 约束。

2. Controller 拒绝的两条 MiMo 意见（final finding 001: 30-file duplicate list; final finding 002: 新抽象）未在 plan 或 proposal 中产生任何对应的不良后果。无第三份 30-file 枚举清单；无新增 abstraction/万能 helper/shared singleton。

3. 独立 `rg` inventory 复现与 plan/proposal 声称完全一致：accepted builder 8 calls / 6 files，rejected builder 7 calls / 4 files，union 8 files，delta 5 files。25-file identity/typed-return closure 完整保留。Grand total 30 files。

4. 全部 8 个 builder call sites 至少有 `operation_id` 作为真实可用上下文；proactive 另有 `attempt_number`。无一 call site 需要虚构不存在的维度。

5. orphan/incomplete/exhausted 三者均通过 `_rejected_payload()` 产生 `failure_category="quality_check_rejected"` event → 三者 `successful_response_identity` 均为 mapping，符合 durable event 语义。

6. Scope、owner、sequencing、反例覆盖、overengineering 检查均通过。Residual risk 分类完整。无 blocking open question。

7. Plan 已处于 `code-generation-ready` 状态，可安全进入 accepted plan amendment commit 及后续 S5 implementation gate。
