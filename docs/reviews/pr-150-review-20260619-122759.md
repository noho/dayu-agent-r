# PR Review — Deepreview Gate

## Scope

- Gate: PR review / deepreview (gateflow PR review gate)
- Repository: `noho/dayu-agent-r`
- PR: 150
- URL: https://github.com/noho/dayu-agent-r/pull/150
- Title: WU-CM-12 Conversation Memory drift repair
- State: OPEN
- Draft: true
- Base: `main`
- Head: `wu-cm-12-conversation-memory-drift`
- Output file: `docs/reviews/pr-150-review-20260619-122759.md`
- Reviewed work unit: Conversation Memory smoke/log diagnostics (latest 4 commits)
  - `60d895ee` gateflow: accept plan for Conversation Memory smoke/log diagnostics
  - `69d47ecd` gateflow: accept Conversation Memory smoke/log diagnostics slice
  - `5714358f` gateflow: accept deepreview for Conversation Memory smoke/log diagnostics
  - `9413ca1c` gateflow: accept PR review for Conversation Memory smoke/log diagnostics
- Included scope:
  - `utils/smoke_host_public_conversation_memory_scenarios.py` — compact audit report construction, per-operation timeline, rejected attempt histograms, manifest missing stage diagnostics, stdout flush
  - `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py` — assembly/helper tests for report construction, histograms, hard fail, malformed payload, empty rows, manifest stage
  - `README.md` §5.3 — suite rename (`core`→`memory-core`), diagnostic smoke boundary, hard fail语义, correctness coverage disclaimer
  - `tests/README.md` — runtime test boundary update
  - `docs/host/conversation-memory-smoke-compact-followup.md` — implementation status appendix
  - Prior gate artifacts: plan, plan review, code review, adjudication, re-review, aggregate deepreview, PR review
- Excluded scope:
  - Production Host/Engine compact behavior files (`dayu/host/compact_material.py`, `dayu/host/compaction.py`, `dayu/host/dispatch.py`, `dayu/host/memory.py`, `dayu/host/context_fallback.py` 等)
  - Pre-existing WU-CM-12 PR contents from earlier phases (S1-S5, FIX-R1)
  - Production memory compact failure repair (deferred to later work unit)
- Parallel review coverage: 无（单一 reviewer，全量走读）

## Constraint Verification

以下为用户给定约束的逐条独立验证，每条沿实际代码路径走读。

### 1. CONTEXT_COMPACTION_FAILED 仍 hard fail ✅

**代码层验证**（`utils/smoke_host_public_conversation_memory_scenarios.py:3216-3254`）：

`_assert_compact_acceptance()` 的逻辑链：

```python
# 行 3235-3236: memory-core 不要求 compact，直接返回
if suite is not SuiteMode.MEMORY_COMPACT:
    return

# 行 3238: failed_total 聚合 proactive + reactive
failed_total = audit.failed_proactive + audit.failed_reactive

# 行 3243-3244: failed_total > 0 即 hard fail
if failed_total > 0:
    raise RuntimeError("memory-compact observed CONTEXT_COMPACTION_FAILED")
```

关键证据：
- `failed_total` 无条件聚合 `failed_proactive` 与 `failed_reactive`（行 3238），不检查 `fallback_action`
- hard fail 条件 `failed_total > 0` 在 `compacted_proactive >= 1` 检查之后独立执行（行 3243），不受 compact accepted 数量影响
- 即使 `fallback_action=dispatch` 使当前 Run 继续完成，`failed_total` 计数不变，`RuntimeError` 仍会抛出
- `main()` 的 `except Exception`（行 4083-4085）打印 `SMOKE FAIL ...` 到 stderr 并 flush，不会静默吞掉

**测试层验证**（`tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`）：

- `test_compact_acceptance_requires_event_log_audit_summary` 行 431-446：`failed_proactive=1` 时 `pytest.raises(RuntimeError, match="CONTEXT_COMPACTION_FAILED")` ✅
- `test_compact_audit_report_prints_operation_histograms_and_manifest_stage` 行 584-589：含 `CONTEXT_COMPACTION_FAILED` event 的 report.summary 触发硬失败 ✅

**文档层验证**（`README.md` §5.3）：

> 若出现 `CONTEXT_COMPACTION_FAILED`，即使 fallback dispatch 让当前回答完成，`memory-compact` 仍会失败

**验证结论**：三层一致，无回归。

### 2. README 不声称完整 memory correctness eval 已实现 ✅

**README.md** §5.3 明确声明：

> 当前 smoke 不覆盖 conversation memory correctness 的全量验证

`rg` 搜索确认：
- README.md 中无 `issue 80`、`完整 issue`、`已实现 eval`、`memory correctness 全量` 等内部引用
- tests/README.md 同样无内部 issue 引用

**文档边界检查**：
- `memory-core`: "轻量日常 smoke，不要求 compact" ✅
- `memory-compact`: "压力与诊断 smoke" ✅
- audit 范围声明："只审计 compact event type、trigger source、operation timeline、rejected attempt histogram、proposal manifest ref 有无、fallback action 和失败阶段标记，不读取 memory / compact material 正文" ✅
- 覆盖边界声明在 README 中而非隐藏于 follow-up doc ✅

**验证结论**：README 准确反映当前实现边界，未声称已实现 conversation memory correctness 全量验证。

### 3. 不要求 production memory 行为修改 ✅

**实现文件边界检查**：

work unit 计划（`docs/reviews/wu-cm-conversation-memory-smoke-log-diagnostics-plan-20260619-115520.md`）明确定义了 Forbidden implementation files：

- `dayu/host/compact_material.py` — 未修改 ✅
- `dayu/config/prompts/` 下 compactor prompt — 未修改 ✅
- Host accept barrier — 未修改 ✅
- memory projection — 未修改 ✅
- production Host/Engine compact 行为 — 未修改 ✅

实际修改文件全部在 allowed 范围内：
- `utils/smoke_host_public_conversation_memory_scenarios.py` — 仅 smoke 诊断工具 ✅
- `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py` — 仅测试 ✅
- `README.md` / `tests/README.md` — 仅文档 ✅
- `docs/host/conversation-memory-smoke-compact-followup.md` — 仅实施状态补充 ✅

**生产代码未受影响的证据**：
- compact payload 读取仅通过 `EventLogRow.payload_json`（已在 durable store 中持久化），不创建新的 production event
- `_compact_row_payload()` 不修改 EventLog 写入路径
- `_assert_compact_acceptance()` 不修改 Host 的 compact accept/reject barrier
- smoke 的 durable store 连接为只读事务（`transaction_runner.run_read`）

**验证结论**：work unit 严格限定在 smoke/log diagnostics，未触碰任何 production memory 行为。

### 4. PR gate artifact 足够性评估

现有 PR gate artifact `docs/reviews/pr-150-review-20260619-122248.md` 是 gateflow PR review gate 产物，结构包含 scope、findings、evidence、validation、open questions、residual risk、conclusion。其 "未发现实质性问题" 结论通过引用 intermediate artifacts 的 chain-of-trust 建立：

```
Plan review (AgentMiMo + AgentDS) → adjudication
  → Implementation → code review (AgentDS + AgentMiMo)
  → adjudication + fix → re-review → aggregate deepreview
  → PR review
```

对 gateflow PR review gate 而言，该 artifact 的结构充分性成立（gate 只需确认 prior gates 通过且当前状态干净）。但从 deepreview skill 标准衡量，它缺少独立代码路径逐行走读和 adversarial failure pass。本次 deepreview 即为补充该独立验证。

## Findings

### 1-未修复-低-现有 PR gate artifact 缺少独立约束逐条验证

- **入口/函数**: N/A（artifact 结构层面）
- **文件(行号)**: `docs/reviews/pr-150-review-20260619-122248.md`
- **输入场景**: gateflow PR review gate 仅依赖 chain-of-trust 而不做独立代码路径验证
- **实际分支**: Findings 写 "未发现实质性问题"，Evidence 仅列出 intermediate artifact 路径，无独立代码引用
- **预期行为**: 按 deepreview skill 的 output contract，PR review 应包含基于直接代码路径证据的独立约束验证和 adversarial failure pass
- **实际行为**: artifact 将 substance 全部委托给 intermediate artifacts，自身仅做汇总
- **直接证据**: 该 artifact 行 25-38 无任何代码行号引用；行 40-60 validation 仅展示命令结果，未关联代码路径
- **影响**: 如果 intermediate artifact 链中任一环节存在未检测的 gap（如某次 code review 基于 outdated diff），PR gate 无法捕获。对 gateflow 流程的 gate 间 chain-of-trust 而言风险低，但对独立审计而言追溯性不足
- **建议改法和验证点**: 本次 artifact（`pr-150-review-20260619-122759.md`）已补充独立约束验证。后续 gateflow PR review gate 建议至少对最高风险约束（hard fail 语义、README 声称边界、production 代码未触碰）做独立逐行走读验证，而非仅委托 prior gates
- **修复风险（低）**: 仅影响 review artifact 质量，不影响代码正确性
- **严重程度（低）**: gate 间委托在 gateflow 中是可接受的模式；该 finding 仅指向独立审计追溯性不足，不影响本 work unit 的 gate-pass 结论

## Evidence Checked

以下为本次 deepreview 独立走读的代码路径和验证对象：

### Compact audit report 数据流完整性

```
run_smoke() 行 2110: _compact_audit_report(options, session_id=session_id)
  → _durable_options_from_open_host_options(options) 行 3278
  → open_host_durable_store(durable_options) 行 3280  # 只读事务
  → _read_compact_event_rows(transaction, ...) 行 3380-3412  # 分页读取
  → _compact_audit_report_from_rows(rows) 行 3288  # 纯函数构造
    → _compact_audit_summary_from_rows(rows) 行 3299
      → _compact_request_trigger_sources(rows) 行 3770-3785
      → _compact_row_count × 8 行 3719-3767
    → per-operation grouping 行 3306-3320
      → _compact_operation_key(row) 行 3415-3428
      → _compact_rejected_attempt_audit(row, ...) 行 3542-3576
      → _compact_failed_operation_audit(row, ...) 行 3579-3612
    → _compact_operation_audit(...) × N 行 3321-3340
    → 三种全局 histogram 行 3344-3346
→ _print_compact_audit_summary(compact_audit.summary) 行 2111
→ _print_compact_audit_report(compact_audit, ...) 行 2112
→ _assert_compact_acceptance(...) 行 2113-2117
```

每一层都有 typed dataclass 收口，无 `Any`/`object` 签名泄漏。

### Payload 类型安全

五个 typed payload helper 均有严格类型守卫：

- `_compact_payload_str()` 行 3882-3884: `isinstance(value, str)` ✅
- `_compact_payload_int()` 行 3899: `isinstance(value, int) and not isinstance(value, bool)` ✅（正确处理 Python bool 子类）
- `_compact_payload_bool()` 行 3914: `isinstance(value, bool)` ✅
- `_compact_payload_str_tuple()` 行 3929: `isinstance(value, Sequence) or isinstance(value, str)` ✅（防止字符串被当序列迭代）
- `_compact_row_payload()` 行 3867-3869: 非 object payload 抛出 `ValueError` ✅

类型不匹配时所有 helper 返回 `None` 或空元组，不静默吞掉字段。

### Operation grouping 正确性

- `_compact_operation_key()` 行 3415-3428：
  - REQUESTED 行以 `row.event_id` 为键（Host 保证唯一）
  - 非 REQUESTED 行通过 `operation_id` payload 字段回指
  - `operation_id` 缺失时使用 `<missing-operation-id>` 占位符，不静默丢弃事件
- `_compact_operation_sort_key()` 行 3431-3457：按最早 EventLog sequence 稳定排序，所有 operation_id 均为 `<missing-operation-id>` 时仍确定性地归到同一排序桶

### Trigger source 归因链

`_compact_operation_trigger_source()` 行 3507-3539 三级 fallback：

1. request row payload `trigger_source`（直接读取）
2. `request_trigger_sources` 映射（通过 `operation_id` 回指）
3. rejected/failed event payload `trigger_source`（自身携带）
4. `<unknown>` 占位符（无法归因时）

`_compact_row_effective_trigger_source()` 行 3813-3834 为 non-request rows 实现回指逻辑。无 trigger source 的 row 在 `_compact_row_count` 中 `None != "proactive"` 判定为不匹配，不被计入任何类别——这是安全的 fail-close 语义。

### Histogram 正确性

三种全局 histogram 均通过 per-operation Counter 聚合：

- `_compact_rejected_failure_histogram()` 行 3648-3662: 聚合 `operation.failure_categories`
- `_compact_rejected_diagnostic_histogram()` 行 3665-3679: 聚合 `operation.diagnostic_histogram`
- `_compact_rejected_manifest_presence_histogram()` 行 3682-3695: 聚合 `_compact_manifest_presence(attempt.proposal_manifest_ref)`

所有 histogram 通过 `_compact_counter_items()` 稳定排序输出（按 key 字典序）。

### Diagnostic suffix 归一化

`_compact_normalized_diagnostic_suffix()` 行 3935-3946：

- `diagnostic_ref.split(":")` 后取 `parts[4:]` 重新拼接
- 示例：`compact:operation:attempt:ValueError:previous reference continuity text is invalid` → `previous reference continuity text is invalid`
- 分段不足时保留完整 ref，不截断 ✅

### Stdout flush 覆盖

所有关键 stdout 输出均已 `flush=True`：

- `_print_compact_audit_summary()` 行 3080 ✅
- `_print_compact_operation()` 行 3143 ✅
- `_print_compact_histogram()` 行 3160/3168 ✅
- `_print_compact_reject_detail()` 行 3199 ✅
- `_print_round()` / `_print_session_observation()` ✅（DS-1 fix 后）
- `_assert_compact_acceptance()` 行 3253 ✅
- `run_smoke()` SMOKE PASS 行 2118, WORKSPACE_KEPT 行 2120/2122 ✅
- `main()` SMOKE FAIL 行 4084 ✅

### Test coverage

16 个测试覆盖：

- 正常路径：多 event 混合（requested/rejected/compacted/failed）+ 三种 histogram + stdout 输出格式
- 边界路径：空 rows、缺失 operation_id、类型不匹配 payload（字符串 attempt_number、字符串 repairable、空 diagnostic_refs）
- 错误路径：malformed payload（非 object）→ ValueError
- hard fail 语义：两个测试独立验证 `failed_total > 0` 触发 RuntimeError
- CLI 参数边界：suite 默认值、memory-compact 需 --pressure-mode、long-rounds 20-25 范围

未覆盖区域（已记录为 residual risk）：
- `_compact_audit_report()` 的真实 I/O 路径（依赖 durable store 事务）
- 真实 LLM long25 端到端路径（成本高/不稳定，由集成 smoke 运行覆盖）
- `_compact_normalized_diagnostic_suffix()` 的独立边界测试（在 histogram 断言中间接覆盖）

### 无 TextIOWrapper dead import ✅

`grep -n "TextIOWrapper" utils/smoke_host_public_conversation_memory_scenarios.py` 返回空。Mimo-1 已修复。

## Adversarial Failure Pass

以下为沿真实代码路径的 adversarial 检查：

### 空输入防御

- `_compact_audit_report_from_rows(())` → 空 operations、空 histograms → 测试覆盖（`test_compact_audit_report_handles_empty_missing_and_malformed_payloads`）
- `_assert_compact_acceptance(suite=MEMORY_COMPACT, summary=all_zero)` → `requested_proactive < 1` 触发 RuntimeError ✅

### 畸形输入防御

- payload 非 JSON object → `_compact_row_payload` 抛出 `ValueError` → 测试覆盖 ✅
- 字段类型不匹配（`attempt_number="1"`）→ `_compact_payload_int` 返回 None → 上游渲染为 `<none>` → 不触发异常 ✅
- 缺失 `operation_id` → `<missing-operation-id>` 占位符 → 不静默丢弃事件 ✅

### 不一致输入防御

- COMPACTED row 的 `operation_id` 指向不存在的 REQUESTED row → 创建独立 operation 组，无 request sequence/run_id，trigger source 为 `<unknown>` → 被 `_assert_compact_acceptance` 的 `requested_proactive < 1` 守卫捕获 ✅
- 多个 REQUESTED row 同 operation_id（不应发生的场景）→ `request_rows` 收集所有，`_compact_operation_audit` 只用第一个 → 计数正确但可能掩盖重复，当前 Host 不产生重复 request ✅

### 资源泄漏防御

- `_compact_audit_report` 使用 `with open_host_durable_store(durable_options) as store`（context manager）→ 连接正确关闭 ✅
- `run_smoke` 使用 `async with open_host(assembly.options) as host` → Host handle 正确关闭 ✅

### 并发/重入防御

- smoke 脚本单线程运行，无并发竞争 ✅
- `_compact_audit_report` 使用只读事务，不写入 durable store ✅

### 状态一致性

- `_assert_compact_acceptance` 在 `_print_compact_audit_report` 之后调用 → 先输出诊断再断言，确保 failure 原因可见后再 hard fail ✅
- `main()` 的 `except Exception` 输出到 stderr（`flush=True`），不依赖 stdout 缓冲区 ✅

### 无 production 行为泄漏

- `_compact_audit_report` 仅通过 `EventLogRow.payload_json` 读取已持久化的事实，不调用任何 Host/Engine production API ✅
- `_compact_row_payload` 不修改 `EventLogRow`、不创建新 event、不写 durable store ✅
- 所有 compact 相关常量和 helper 均为模块私有（`_` 前缀），不通过 `__all__` 或 re-export 暴露 ✅

## Open Questions

1. `_compact_artifact_files()` 使用 `rglob("*")` 无深度/文件数限制（行 4047）。compact artifact root 由 Host assembly 设置为专用目录，正常使用无风险。若未来 workspace 配置错误导致 artifact root 指向大型共享目录，`rglob` 可能消耗大量 I/O。建议后续 smoke 维护时加文件数上限保护——但不属于本 work unit 范围。

2. `_compact_audit_summary_from_rows()` 对全量 rows 做 8 次全扫描（每次 `_compact_row_count` 都会遍历全部 rows）。对 smoke 诊断场景（≤ 数百行）可忽略；若未来扩展到 prod 审计，可改为单次遍历。

## Residual Risk

- **Production memory compact failure 未修复**：long25 暴露的两个 durable `CONTEXT_COMPACTION_FAILED` 的主链路（`_parse_previous_reference_continuity_text` → `ValueError`）仍存在。当前 work unit 仅增强 smoke 诊断，使下次 long25 失败时能从 stdout 直接定位 failed operation、trigger source、rejected attempt histogram 和 manifest missing stage。production memory 修复属于后续独立 work unit。
- **Real LLM long25 未在本 work unit 运行**：单元/assembly 测试覆盖了所有 no-real-LLM 诊断路径（report 构造、histogram、stdout 格式、hard fail 语义），但 compaction 真实触发与真实 LLM compact proposal 行为的集成验证未在本次完成。
- **Per-operation histogram 不独立打印**：`CompactOperationAudit` 保留 `failure_categories` 和 `diagnostic_histogram`，但 `_print_compact_audit_report` 仅打印全局 histogram。多数场景下全局 histogram 足够诊断；如需 per-operation failure pattern 细分，当前需回 EventLog 查询。
- **Offending material block 仍然不可用**：当 `proposal_manifest_ref` 缺失时，smoke 明确输出 `failure_stage=prepare_or_material_projection` 和 `log_insufficient=offending_material_block_unavailable`，而非假装有 block 数据。这一 log insufficiency 标记正确反映了当前 EventLog payload 的观察上限。
- **GitHub checks 未报告**：`gh pr checks 150` 返回空，无 CI status 作为额外证据。不影响代码正确性判断。

## Conclusion

**PR review gate status: pass**（当前 work unit additions 独立验证通过）。

三个用户约束全部独立验证通过：
1. `CONTEXT_COMPACTION_FAILED` 仍是 hard fail — 代码、测试、文档三层一致 ✅
2. README 未声称完整 memory correctness eval 已实现 — `rg` 搜索确认，边界声明准确 ✅
3. 不要求 production memory 行为修改 — 实现严格限定在 `utils/`、`tests/`、文档 ✅

现有 PR gate artifact（`pr-150-review-20260619-122248.md`）作为 gateflow gate 间产物结构充分；本次 deepreview 补充了独立代码路径逐行走读和 adversarial failure pass。

Product memory compact failure（follow-up doc 第 3 个问题）仍属于后续 production memory compact failure work unit，不在本 work unit 修复范围。当前 smoke/log diagnostics 已实现日志不足问题的目标：下次 long25 再失败时，stdout 可直接看到 failed operation、rejected attempt histogram、manifest missing stage 和 log insufficiency 标记，无需查 SQLite。
