# PR 190 F11/F12 S2 Fix Re-Review — DS 第二路独立路线

## Review metadata

- **Reviewer**: AgentDS (第二路独立 re-review，未参考、未消费 MiMo re-review 结论)
- **Gate**: `re-review`（controller adjudication → fix → re-review gate）
- **Work unit**: PR 190 F11/F12 Interactive Memory 收口
- **Slice**: S2 — Engine generic structured output 与 config capability
- **基线 HEAD**: `c8be3e5184b8b797c59458027e991f0284cbb3b5`
- **Fix artifact**: `docs/gateflow/pr-190-f11-f12-s2-review-fix-20260805.md`
- **Adjudication artifact**: `docs/gateflow/pr-190-f11-f12-s2-code-review-adjudication-20260805.md`
- **原始 DS review**: `docs/reviews/pr-190-f11-f12-s2-ds-code-review-20260805.md`
- **Review type**: 只读审查；未执行 git stash/checkout/reset/clean/stage/commit/push 或工作树切换
- **Artifact path**: `docs/reviews/pr-190-f11-f12-s2-ds-rereview-20260805.md`

## 审查范围

按 controller adjudication 与 fix artifact 要求的 6 项审查维度逐项复核：

1. builder required signature 是否真正关闭未来漏传，callsite 全迁移
2. enum alignment test owner 是否在 Service、是否锁定集合和映射且不生产耦合
3. import-boundary policy 从 basename 到 repo-relative path 的转换是否保留全部旧 allowlist、精确加入两个新文件、无 path canonicalization 漏洞
4. Tool Trace 仍复用 canonical Engine identity，不复制、不下游补偿
5. tests/hash/coverage/type evidence 与 artifact 一致
6. 未扩张 schema/provider/S3 scope

---

## 1. DS-LOW-01 — `build_request_payload` required signature

### 裁决：CLOSED

### 直接证据

**签名**（`dayu/engine/runners/openai/payload.py:402-409`）：

```python
def build_request_payload(
    *,
    messages: Sequence[AgentMessage],
    options: RunnerCallOptions,
    tools: Sequence[ToolSchema],
    spec: RunnerSpec,
    structured_output: StructuredOutputRequest | None,
) -> _OpenAIRequestPayloadWithStructuredOutput:
```

- `structured_output` 是 required keyword-only 参数，无 default。
- 类型保持 `StructuredOutputRequest | None` — `None` 仍是合法业务值，但必须显式传递。

**Owner test**（`tests/engine/runners/openai/test_no_extra_payload_bag.py:37-58`）：

```python
assert (
    sig.parameters["structured_output"].default
    is inspect.Parameter.empty
)
```

- 断言 default 为 `Parameter.empty`（即 required），直接锁定 contract。

**Production call site**（`dayu/engine/runners/openai/runner.py:431-436`）：

```python
payload = build_request_payload(
    messages=messages,
    options=effective_options,
    tools=tools,
    spec=self._spec,
    structured_output=structured_output,
)
```

- 显式传递 `structured_output=structured_output`。

**全部 test call sites**（grep 验证，共 30+ 调用点）：

- `tests/engine/runners/openai/test_payload_build.py`：全部显式传参（`structured_output=None` 或 explicit request）
- `tests/engine/runners/openai/test_stream_usage_capability_gating.py`：4 处全部显式传参
- `tests/engine/runners/openai/test_payload_assistant_reasoning_content_preserved.py`：6 处全部显式传参

**遗漏不可能发生**：新增 caller 省略 `structured_output` → `TypeError: missing required keyword-only argument`，在 import 时即被 Python runtime / pyright 捕获。

### 修复判定

- 修复正确：移除 default，保持合法值类型。
- 修复完整：所有 production 与 test call sites 已迁移。
- 修复最小：无 wrapper、fallback、overload 或其它补偿逻辑。

---

## 2. DS-LOW-02 — config/Engine capability enum drift guard

### 裁决：CLOSED

### 直接证据

**Owner test**（`tests/service/test_host_assembly.py:204-222`）：

```python
def test_structured_output_capability_enums_map_mechanically_by_value() -> None:
    runtime_values = {
        capability.value for capability in StructuredOutputCapabilityConfig
    }
    engine_values = {
        capability.value for capability in StructuredOutputCapability
    }

    assert runtime_values == engine_values
    for runtime_capability in StructuredOutputCapabilityConfig:
        engine_capability = StructuredOutputCapability(runtime_capability.value)
        assert engine_capability.value == runtime_capability.value
```

**Owner 判定**：

- 测试在 `tests/service/test_host_assembly.py` → **Service 装配边界**（符合 controller 裁决的 "Service 的 runtime-config → Engine-contract 机械装配边界"）。
- `assert runtime_values == engine_values` → 锁定完整 value 集合相等。
- `StructuredOutputCapability(runtime_capability.value)` → 逐个 runtime value 机械构造 Engine enum。
- 无 production helper、无第三份 enum、无 runtime import Engine。

**当前值域**（运行时验证）：

```
runtime enum values:  {'none', 'json_object', 'json_schema'}
engine enum values:   {'none', 'json_object', 'json_schema'}
```

### 修复判定

- 修复正确：owner 在 Service，锁定集合和逐值构造，不生产耦合。
- 修复完整：覆盖完整 value 集合相等与逐值可构造。
- 修复最小：仅新增一个 test function，无 production 变更。

---

## 3. S1 deterministic import-boundary regression

### 裁决：CLOSED

### 直接证据

**Policy 变更**（`tests/host/test_import_boundary.py:53-69`）：

从旧 basename-based allowlist 迁移到 `HOST_ENGINE_CONTRACT_ALLOWED_RELATIVE_FILES: frozenset[str]`，使用 `dayu/host/...` 格式的精确 workspace-relative paths。

**全量旧 allowlist 保留验证**：

交叉比对当前工作树中实际 `import dayu.engine` 的 Host 文件与 allowlist：

| 实际导入 Engine 的 Host 文件 | allowlist 中存在 |
|---|---|
| `dayu/host/_execution_config_projection.py` | ✅ |
| `dayu/host/_runner_call_manifest.py` | ✅ |
| `dayu/host/api.py` | ✅ |
| `dayu/host/compact_pipeline.py` | ✅ |
| `dayu/host/compaction.py` | ✅ |
| `dayu/host/compaction_operation.py` | ✅ |
| `dayu/host/context_events.py` | ✅ |
| `dayu/host/dispatch.py` | ✅ |
| `dayu/host/durable/tool_trace.py` | ✅ **NEW** |
| `dayu/host/engine_ingest.py` | ✅ |
| `dayu/host/llm_compaction.py` | ✅ |
| `dayu/host/local_proxy.py` | ✅ |
| `dayu/host/run_input.py` | ✅ |
| `dayu/host/tool_trace_analysis_contracts.py` | ✅ **NEW** |

- 全量 12 个旧文件保留 ✅
- 精确新增 2 个新文件 ✅
- 无遗漏、无多余 ✅

**Matching 逻辑**（`tests/host/test_import_boundary.py:657-680`）：

```python
repo_root = _repo_root()
for file_path in _iter_python_files(host_root):
    relative_path = file_path.relative_to(repo_root).as_posix()
    for module in _imported_module_names(...):
        if _matches_prefix(module, ("dayu.engine",)) and (
            relative_path not in HOST_ENGINE_CONTRACT_ALLOWED_RELATIVE_FILES
        ):
            violations.append(...)
```

- `file_path` 来自 `Path(package_file).resolve().parent` → 已 resolve symlinks
- `repo_root` 来自 `Path(__file__).resolve().parents[2]` → 已 resolve symlinks
- `relative_to(...).as_posix()` 在两方均已 resolve 的路径上执行 → 确定性产出
- frozenset 使用精确 string match → 无 basename-only 旁路、无 `endswith` 宽松匹配

**Path canonicalization 漏洞检查**：

- 双方均已 resolve，因此 symlink 不会产生路径不一致。
- `as_posix()` 确保跨平台 `/` 分隔符一致。
- 不存在 `..` 遍历、`os.path.realpath` 不对称或 partial resolve 窗口。

### 修复判定

- 修复正确：从 basename 到 repo-relative path，精确匹配。
- 修复完整：保留全部旧文件，精确新增两个新文件。
- 无 path canonicalization 漏洞。

---

## 4. Tool Trace 仍复用 canonical Engine identity

### 裁决：PASS

### 直接证据

**`dayu/host/durable/tool_trace.py:17`**：

```python
from dayu.engine.contracts.runner_identity import SuccessfulRunnerResponseIdentity
```

- 唯一引用点，直接 import canonical Engine type。
- `ResolvedCompactorResponseIdentity.successful_response_identity` 字段类型为 `SuccessfulRunnerResponseIdentity | None`。
- 无本地 class 复制、无 `TypedDict` 镜像、无下游 `isinstance` fallback 重算。

**`dayu/host/tool_trace_analysis_contracts.py:19-22`**：

```python
from dayu.engine.contracts.runner_identity import (
    ProviderRequestIdAvailability,
    RunnerRequestIdentity,
)
```

- 导入的是不同的 Engine identity 类型（`ProviderRequestIdAvailability`、`RunnerRequestIdentity`），不涉及 `SuccessfulRunnerResponseIdentity`。
- 无重复定义、无下游补偿。

**反例检查**：

- ❌ Host 未定义 `SuccessfulRunnerResponseIdentity` 副本
- ❌ Tool Trace 未从 `SuccessfulRunnerResponseIdentity` 的字段反推语义
- ❌ 无 `hasattr` / `getattr` / loose dict 访问 Engine identity 字段

---

## 5. Tests / hash / coverage / type evidence

### 5.1 测试验证

**Host regression tests**（controller 指定）：

```
tests/host/test_active_cancel_dispatch.py::test_cancel_session_replay_after_watchdog_does_not_append_or_propagate PASSED
tests/host/test_import_boundary.py::test_host_engine_imports_stay_on_allowed_boundary_modules           PASSED
```

**Owner 与 affected suites**：

```
tests/service/test_host_assembly.py::test_structured_output_capability_enums_map_mechanically_by_value PASSED
tests/engine/runners/openai/test_no_extra_payload_bag.py::test_build_request_payload_signature_no_kwargs PASSED
tests/engine/runners/openai/test_payload_build.py (full)                                                 PASSED
tests/engine/runners/openai/test_stream_usage_capability_gating.py (full)                                PASSED
tests/engine/runners/openai/test_payload_assistant_reasoning_content_preserved.py (full)                 PASSED
```

合计：35 passed, 3 warnings（均为 edgar dependency 既有 deprecation warning）。

**Full Engine suite**：

```
602 passed in 2.54s
```

### 5.2 类型检查

```
pyright dayu tests utils → 0 errors, 0 warnings, 0 informations
```

### 5.3 Hash 一致性

```
models.json raw-byte SHA256: dc924f842be81599c00606dae0cbe464d6f766147581d4bbb4167e83044e5f2b
                              MATCH adjudication doc
manifest raw-byte SHA256:    0e1ec1047062eecbe6dc8eae89139460058219c881ce4d7960e6c96c7a182469
                              MATCH adjudication doc
```

### 5.4 与 fix artifact 的一致性

fix artifact 声明的 9 个 incremental changed files 与当前工作树一致；未发现未记录的额外变更。

---

## 6. 未扩张 schema / provider / S3 scope

### 裁决：PASS

### 直接证据

- **Schema**: `dayu/engine/contracts/structured_output.py` 未新增字段、enum value 或 variant。DS-LOW-01 只修改了 `build_request_payload` 的 default，未改变 `StructuredOutputRequest` 联合或 `StructuredOutputCapability` 枚举值。
- **Provider**: 无新增 provider-specific 分支。`_apply_structured_output_request` 仍使用 `match/case` + `assert_never` 按 variant type 派发，不读取 provider/model 名称。
- **S3**: 无新增 `compact_output_*` 函数、schema、template 或 parser。
- **Host Tool Trace**: `SuccessfulRunnerResponseIdentity` 导入未变；Tool Trace production contract 未扩张。
- **Config publication**: `models.json` hash 未变。

Host 层仅 `_execution_config_projection.py` 引用 `StructuredOutputCapability`（已存在的 S2 实现），fix 未扩散该引用到其他 Host 模块。

---

## 综合裁决

| # | 审查项 | 裁决 |
|---|---|---|
| 1 | DS-LOW-01 — `build_request_payload` required signature | **CLOSED** |
| 2 | DS-LOW-02 — enum alignment test owner / 集合 / 映射 | **CLOSED** |
| 3 | S1 import-boundary regression — repo-relative paths | **CLOSED** |
| 4 | Tool Trace 复用 canonical Engine identity | **PASS** |
| 5 | Tests / hash / coverage / type evidence | **PASS** |
| 6 | 未扩张 schema / provider / S3 scope | **PASS** |

**新 finding**：0

三项修复均 owner-correct、完整、最小。无新增 correctness、stability、maintainability 或 semantic ownership drift finding。

## 最终结论

**S2 Fix: PASS — 可进入 acceptance gate。**

本 re-review 完成。AgentDS 停在 idle。不执行任何 Git mutation、不派发 reviewer、不做 controller adjudication。
