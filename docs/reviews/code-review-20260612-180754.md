# Code Re-Review — WU-CM-09-S1 Focused Fix Verification

## Scope

- Mode: focused re-review — 仅复核上一轮 S1 code review 中 4 个低风险修复项的关闭状态
- Reference artifacts:
  - `docs/reviews/code-review-20260612-180244.md`（首次 S1 review）
  - `docs/reviews/code-review-20260612-180250.md`（S1 adversarial review）
- Branch: `work/cm-05-06-08-09`
- Uncommitted changes: `dayu/host/durable/memory.py`, `tests/host/test_memory_projection.py`, `docs/host/issues-implementation-control.md`

## Fix Verification

### 1. `_SNAPSHOT_INTEGRITY_SCAN_ID` unused constant removed

**来源**: 180244-F01 / 180250-F01 — 模块级常量声明后未被任何代码引用。

**验证**: `grep -rn '_SNAPSHOT_INTEGRITY_SCAN_ID' dayu/ tests/` → NOT FOUND。

**结论**: ✅ 已关闭。

### 2. Row `snapshot_digest` column mismatch test added

**来源**: 180250-F03 — `memory.py` line 1302 `row_snapshot_digest != snapshot.snapshot_digest` 分支未经测试覆盖。

**验证**:
- 新增测试 `test_memory_snapshot_integrity_classifies_row_digest_column_mismatch`（`tests/host/test_memory_projection.py:959-975`）。
- 通过 `_replace_snapshot_digest` helper 直接 SQL UPDATE `snapshot_digest` 列为 `"sha256:manual-corrupt"`，不改 JSON 内容。
- 断言 `issues[0].failure_kind is MemorySnapshotIntegrityFailureKind.DIGEST_MISMATCH` 且 `snapshot_id` 正确。
- 测试单独运行 PASS（`pytest ...::test_memory_snapshot_integrity_classifies_row_digest_column_mismatch` → 1 passed）。

**结论**: ✅ 已关闭。该分支现被覆盖（当前 coverage missing 列表中不含 1300-1303 行）。

### 3. Unknown item kind test added

**来源**: 180250-F04 — `memory.py` line 1372 `item_kind not in allowed_kinds` 分支（完全未知 kind，非 `verified_fact`）未经测试覆盖。

**验证**:
- 新增测试 `test_memory_snapshot_integrity_classifies_unknown_item_kind`（`tests/host/test_memory_projection.py:1005-1019`）。
- 通过 `_insert_unsupported_memory_item_kind(store, snapshot, "mystery_memory_kind")` 插入一个既不是 `verified_fact` 也不是当前 allowed_kinds 六种之一的完全未知 kind。
- 断言 `issues[0].failure_kind is MemorySnapshotIntegrityFailureKind.UNSUPPORTED_ITEM_KIND` 且 `snapshot_id` 正确。
- 辅助 helper `_insert_unsupported_memory_item_kind` 正确复用 `_insert_unsupported_memory_item_kind_in_transaction` 带 `PRAGMA ignore_check_constraints = ON` 绕过 DDL CHECK 约束。
- 测试单独运行 PASS（`pytest ...::test_memory_snapshot_integrity_classifies_unknown_item_kind` → 1 passed）。

**结论**: ✅ 已关闭。该分支现被覆盖。

### 4. Validation checks

| Check | Expected | Actual | Result |
|---|---|---|---|
| `pytest tests/host/test_memory_projection.py -q` | 26 passed | 26 passed in 0.34s | ✅ |
| `pytest --cov=dayu.host.durable.memory --cov-report=term-missing` | ≥ 80% | 80% (332 stmts, 66 missed) | ✅ |
| `python -m pyright dayu/ tests/` | 0 errors, 0 warnings | 0 errors, 0 warnings, 0 informations | ✅ |
| `git diff --check` | clean | clean | ✅ |

**结论**: ✅ 全部通过。

## Conclusion

**PASS** — 4 项低风险修复全部关闭，无 blocker。

- `_SNAPSHOT_INTEGRITY_SCAN_ID` 已删除，全仓零引用。
- Row `snapshot_digest` column mismatch 测试已添加并通过。
- Unknown item kind 测试已添加并通过。
- 测试从 24 passed 增至 26 passed，coverage 保持 80%，pyright 0 errors，diff 无 whitespace 问题。
