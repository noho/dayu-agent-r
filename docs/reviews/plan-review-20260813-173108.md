# UF-FIX02 action-and-update-identity — Delta Re-Review (AgentMiMo)

## Reviewed target

- **Revised plan artifact**: `docs/gateflow/uf-fix02-action-and-update-identity-plan-20260813.md`
- **Binding Goal Confirmation**: `docs/gateflow/uf-fix02-action-and-update-identity-goal-confirmation-20260813.md`
- **Controller adjudication**: `docs/gateflow/uf-fix02-action-and-update-identity-plan-review-adjudication-20260813.md`
- **Original review**: `docs/reviews/plan-review-20260813-171637.md`
- **Reviewer**: AgentMiMo，独立 re-reviewer
- **Review date**: 2026-08-13
- **Review type**: delta re-review，验证 Controller accepted findings 是否被精确修复，再做全面 audit

## Scope

逐项验证 MIMO-001/002/003 与 Controller 接受的 DS-01/02/03 修复状态，重点关注：
- registry no-touch 与后续 refresh owner 声明
- failure/cancel rollback 路径文档化
- previous_meta/version/first_ingested_at 语义契约
- material shared-owner parity 边界
- 旧 upsert pin 迁移

再做一次完整 goal drift / owner / slice / validation / UF-PF02 / static audit，发现新 material blocker 才报告。

## Finding verification

### MIMO-001 — 已修复

- **原问题**: plan 声称"不刷新 registry"，但未说明 UF-A08 的 stale 状态与后续 owner，导致实现 agent 无法判断是否应更新 scenario status。
- **Controller 裁决**: accepted（bounded remedy）；只在 plan/Gateflow artifacts 说明 UF-A08 intentionally stale，等待后续统一 conformance refresh。
- **修订验证**:
  - §3.1 增加："UF-A08-update-missing-overwrite-creates 只是在 frozen scenario 中保存的修复前 observed evidence；它将在本 WU 后 intentionally stale。本 WU 只以 upload_filing.action-core oracle predicate 判定正确性，绝不修改 registry/evidence；其 status/evidence 更新由后续统一 conformance refresh owner 负责。"
  - §12 residuals 表格增加 UF-A08 stale 行，classification=intentionally stale，destination=后续统一 conformance refresh。
  - §11 static audit 增加："UF-A08 只被标注为 intentionally stale observed evidence；本 WU 无 registry/evidence/status 写入，后续统一 conformance refresh 是唯一状态更新 owner。"
- **判定**: 精确覆盖 Controller bounded remedy 要求。实现 agent 现在可以明确判断：不碰 registry/evidence，UF-A08 stale 是预期的，后续 owner 已锁定。

### MIMO-002 — 已修复

- **原问题**: publication transition 流程图缺少错误分支，实现 agent 可能遗漏 batch rollback 语义。
- **Controller 裁决**: accepted；在 publication transition 写出错误/取消分支，并绑定 rollback assertions。
- **修订验证**:
  - §5.2 publication transition 增加两个显式错误分支：
    1. "on any store/create failure: discard/rollback whole batch → published old state remains byte-for-byte unchanged"
    2. "on precommit cancellation: discard/rollback whole batch → published old state remains byte-for-byte unchanged"
  - §5.2 尾段增加："任何 reset 后的 store/create failure 与 precommit cancellation 都必须让 caller-owned batch 走既有 discard/rollback，禁止提交半成品或另起补偿 batch；已发布 old source/meta/manifest/company state 保持不变。"
  - §8 validation matrix 增加 atomic batch 行："existing failure/cancel tests + reset 后 store/create/precommit cancel assertions → whole batch discard/rollback；published old state不变；成功只见完整 new。"
- **判定**: 错误/取消路径已在 transition 流程图中显式文档化，并与 S2 rollback assertions 绑定。

### MIMO-003 / DS-03 — 已修复

- **原问题**: reset→create 路径下 `previous_meta` 存活语义、version 解析规则和 `first_ingested_at` 保持规则未显式说明。
- **Controller 裁决**: accepted（consolidated）；明确 transition invariant；S2 对改名 update 与 deleted restore 断言 first_ingested_at 保持，version 规则不回退。
- **修订验证**:
  - §5.2 增加完整 invariant 段落："previous_meta 在 reset 前由 caller 持有，是 version 与 first_ingested_at 派生的唯一真源；reset 只清 staging identity tree，不得清空、重读或以 reset 后 missing 状态替代该引用。final create 继续用同一个 previous_meta 解析 version，并在同一 filing/material identity 的改名 update 与 deleted restore 中保持原始 first_ingested_at；changed fingerprint 按既有规则递增 version，不得回退到 v1。"
  - §5.3 state transitions 中 renamed update 行明确："version 按 reset 前 previous_meta 派生，first_ingested_at 保持。"
  - §5.3 deleted restore 行明确："version 不回退且 first_ingested_at 保持。"
  - §7 S2 tests-first 增加对应 assertions："version 按 reset 前 previous_meta 派生，first_ingested_at 等于初次 create 的值且不被本次 updated_at 替代"，以及 "version 不回退，first_ingested_at 等于 logical delete 前的初次 create 值。"
- **判定**: previous_meta 存活语义、version 解析规则和 first_ingested_at 保持规则已完整文档化，并由 S2 测试矩阵固定。

### DS-01 — 已修复

- **原问题**: filing/material 共用 Docling owner 的一致性效果未被声明和测试，可能违反唯一 owner 约束。
- **Controller 裁决**: accepted；plan 声明 material 同步获得 update-missing 不 upsert、deleted auto 不 skip、existing update 完整替换；增加最小 material parity owner tests。
- **修订验证**:
  - §3.1 non-goals 增加："material 只接受 shared owner 的一致性效果与最小 owner parity tests；不新增 material typed usage/public error projection，不修改 CLI/Service/SEC/CN/HK material workflow 生产代码，也不把 material 扩入 UF-PF02 focused-real。"
  - §4 owner table "完整文件集合替换" 行增加："filing/material 共享效果，不逐文件补偿删除。"
  - §6.2 test files 增加 material parity 说明："S1/S2 各一个 filing/material shared-owner 最小 parity test"。
  - §7 S1 tests-first 增加："另加一个最小 material parity owner test，证明 material update-missing 在 overwrite false/true 下同样于 converter 前失败且 converter call count 为 0。"
  - §7 S2 tests-first 增加："一个最小 material publication parity owner test：deleted + equal full input 的 auto 不得 skip，成功后 active、完整且 first_ingested_at 保持。"
  - §8 validation matrix 增加 material shared-owner parity 行。
  - §11 static audit 增加："material 变化仅来自 filing/material shared Docling owner，并只有 S1/S2 两个最小 owner parity tests；无 material typed usage、workflow 生产代码或 UF-PF02 focused-real 扩面。"
- **判定**: material shared-owner parity 声明、边界、测试全部到位，边界明确（不新增 typed usage/workflow/focused-real）。

### DS-02 — 已修复

- **原问题**: 旧 `_resolve_upsert_mode` import/assertion 精确固化了要移除的 upsert bug，未点名迁移会诱导 compat shim。
- **Controller 裁决**: accepted；S2 明确删除 import 与旧断言，以 owner action/replacement matrix 取代，并要求全仓零命中。
- **修订验证**:
  - §7 S2 新增 "Test migration" 段落："从 test_docling_upload_service.py 删除 _resolve_upsert_mode import 与 assert _resolve_upsert_mode('update', None, True) == 'create' 错误 upsert pin；不得保留 compat shim。由 S1 action/admission owner matrix 与 S2 complete-replacement owner matrix 取代该 internal helper 断言。完成后运行 rg -n '_resolve_upsert_mode' --glob '*.py' .，要求全仓生产/测试 Python 源码零命中。"
  - §6.1 production files 中 `_resolve_upsert_mode` 条目明确："删除；不得保留 update-missing→create 或 reset 后 update 的第二套决策。"
  - §11 static audit 增加："rg -n '_resolve_upsert_mode' --glob '*.py' . 在全仓生产/测试 Python 源码零命中；不得用 compat shim 保留旧 import/assertion。"
- **判定**: 删除点、迁移策略、验证命令全部到位。

## Fresh audit (goal drift / owner / slice / validation / UF-PF02 / static)

### Goal drift audit

- §2.1 goals 与 Goal Confirmation binding scope 完全对齐，无新增 goal。
- §3.1 non-goals 明确排除 UF-FIX03–08/10/11、UF-PF03–12，无 scope creep。
- material 只做 shared owner parity，不新增 typed usage/workflow/focused-real，符合 minimality 约束。
- **无 goal drift。**

### Owner adjudication audit

- §4 owner table 覆盖 filing identity、published existence/deleted、action resolution、complete file set replacement、fresh authoritative decision、CLI/Service、Host/Engine。
- 每个 owner 有直接代码证据和本 WU 决策。
- Host/Engine 明确 no-touch。
- `_resolve_upsert_mode` 被标记为删除，不保留第二套决策。
- **无 owner 冲突或遗漏。**

### Slice boundary audit

- S1（action admission + deleted no-skip）和 S2（complete replacement + cross-market propagation）按行为增量切分，不按文件机械拆分。
- S1 prerequisite 为 UF-FIX01 closeout；S2 prerequisite 为 S1 accepted。
- S2 不重定义 S1 action matrix。
- 两 slice 的 focused validation 命令覆盖对应测试文件。
- **slice 切分合理，可独立验证。**

### Validation matrix audit

- §8 matrix 覆盖 stable identity、update missing ± overwrite、create existing、deleted no-skip、same-name update、renamed update、material parity、stale action discard、atomic batch、bounded stderr、cancellation。
- 每个 contract 有 owner test 和 integration/boundary test 双层覆盖。
- §8.3 per-file coverage 要求逐文件 ≥80%，不接受 aggregate 替代。
- §8.4 pyright 完整检查。
- **validation matrix 完整。**

### UF-PF02 audit

- §9.1 执行边界：独立 root、真实 CLI/runtime/storage/Docling、无 mock/fake。
- §9.2 命令序列：A（同名/改名 update + create-existing）、B（update missing ± overwrite）、C（delete→auto），覆盖 Goal Confirmation 全部 success signal。
- §9.3 每步 8 类 observation 完整。
- §9.4 bundle publication 要求 manifest/report/digests/completion/secret-scan。
- §9.5 pass conditions 明确。
- **UF-PF02 设计完整，无遗漏。**

### Static audit checklist

- [x] 无新增 `hasattr/getattr`、`Any/object`、裸 dict 公共签名、lazy import、compat re-export/wrapper
- [x] 无 basename/stem filing identity、字符串异常分类、`str(exc)` public projection、默认 deleted state、下游 fallback
- [x] 无 commit 后补偿删除、跨 batch replacement、ticker 级清空
- [x] 无 CLI/Service duplicated action rule、Host/Engine/Fins 反向依赖
- [x] 无 registry/evidence refresh、UF-FIX03–08/10/11 或 UF-PF03–12 内容
- [x] UF-A08 只被标注为 intentionally stale observed evidence
- [x] `_resolve_upsert_mode` 全仓零命中要求已写入
- [x] material 变化仅限 shared owner parity tests
- [x] docstring/注释要求已声明

## New findings

**无新 material finding。**

revised plan 精确覆盖了 Controller 裁决的全部 accepted findings，无引入新问题。

## Residual risks

| Risk | Classification | Destination |
| --- | --- | --- |
| prevalidation/fresh recheck 后到 batch publication 前的同请求竞争 | deferred | UF-FIX10 |
| existing source corruption + auto repair | deferred | UF-FIX08 |
| multi-file basename/stem collision | deferred | UF-FIX07 |
| format/XBRL companion | deferred | UF-FIX06 |
| summary/stored counts | deferred | UF-FIX03 |
| fresh company name/alias warning | deferred | UF-FIX11 |
| full upload_filing conformance | deferred | UF-PF12 |
| UF-A08 frozen scenario stale status | intentionally stale | 后续统一 conformance refresh |
| material broader public typed error projection / full-real | deferred | UF-PF12 或后续明确 WU |
| real converter runtime cost/机器差异 | covered by UF-PF02 bounded timeout | 外部运行失败如实 classified |

所有 residual risk 已有明确 destination，无未分类项。

## Open questions

**无。** 原 3 个 finding 与 Controller accepted 的 DS-01/02/03 全部精确修复，无新增 blocking question。

## Plan review conclusion

**pass**

revised plan 精确修复了 MIMO-001/002/003 与 DS-01/02/03 全部 6 个 accepted findings：

1. MIMO-001：UF-A08 intentionally stale 声明 + 后续 conformance refresh owner 已明确。
2. MIMO-002：failure/cancel rollback 路径已显式文档化并绑定 rollback assertions。
3. MIMO-003/DS-03：previous_meta 存活语义、version 解析、first_ingested_at 保持规则已完整文档化。
4. DS-01：material shared-owner parity 声明、边界、最小 tests 全部到位。
5. DS-02：_resolve_upsert_mode 删除点、迁移策略、零命中验证全部到位。

全面 audit（goal drift / owner / slice / validation / UF-PF02 / static）未发现新 material blocker。plan 可进入 implementation。
