# Code Review

## Scope

- Mode: current changes
- Branch: `codex/interactive-oracle`
- Base: `21b602c1`
- Output file: `docs/reviews/code-review-20260803-220950.md`
- Included scope: PR 190 S1 implementation slice 五个 dirty files — prompt owner、两个 host tests、publication manifest、CLI manifest hash test；加 implementation artifact `docs/gateflow/pr-190-compactor-output-business-semantics-s1-implementation-20260803.md`。只 review 当前未提交 changes，不 review 已提交 plan gate 全量历史。
- Excluded scope: 已提交的 plan/acceptance/review artifacts 全量历史；`dayu/host/compaction.py`、`dayu/host/context_governance.py`、`dayu/host/memory.py` 等 frozen production files；`docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`、`docs/cli_ci.md`；所有 README/design。
- Parallel review coverage: 无。

## Context read

- `AGENTS.md`（LLM-facing 文本约束、语义所有权、编码硬约束、测试与验证、schema 变更）
- `docs/gateflow/pr-190-compactor-output-business-semantics-plan-20260803.md`（accepted plan，SHA-256 `23b8951e…`）
- `docs/gateflow/pr-190-compactor-output-business-semantics-plan-review-acceptance-20260803-215810.md`（plan review acceptance，frozen semantics）
- `docs/reviews/pr-190-review-20260803-203709.md`（外部 follow-up review，识别 F01 residual finding，SHA-256 `e7add55e…`）
- `docs/reviews/plan-review-20260803-212134.md`（plan review，识别 `session_summary:null`、previous evidence、`policy_limit` 三项纠正，SHA-256 `1d592ae4…`）
- `dayu/host/memory.py:1229-1255,1720-1739`（Memory replacement projection、`session_summary is None` 返回 empty view）
- `tests/host/test_memory_projection.py:1408-1442`（"without summary clears prior session summary" regression）
- `dayu/host/compaction.py:157-163`（`CompactDropReasonV2` 闭集枚举）
- `dayu/host/context_governance.py:492-576`（具体 item/char cap 只在 candidate 超限后进入 repair issue message）

## Findings

未发现实质性问题。

## Adversarial checklist

### 1. LLM-facing 文案是否自足且最低认知负担

prompt 在现有字段定义旁就地补充业务语义。`session_summary`、`evidence_facts`、`answer_anchors`、`explicitly_dropped_sources.reason` 四组定义均：
- 使用业务可读中文，不暴露 `CompactCandidateV2`、`CompactDropReasonV2`、`compaction.py`、`context_governance.py`、`memory.py` 等内部类型名或模块名；
- 自足说明字段含义、允许值、引用限制和禁止事项；
- 不复制 Host 实现规则（如 typed schema shape、accept barrier 阈值）；
- 保留现有完整同源示例和 repair feedback schema，未改动 fence/marker/枚举。

grep `schema_version`、`current_input_anchor`、`previous_compacted_view`、`evidence_backed_facts`、`reference_continuity_items`、`CompactDropReason`、`CompactCandidate`、`compaction.py`、`context_governance`、`memory.py` 均无命中。

### 2. `session_summary:null` 是否准确清除

prompt 第 34 行：
> null 表示本次完整 replacement 不包含 session summary；candidate 被接受后，当前会话摘要变为空，包括清除先前已接受的摘要，但不影响同一 candidate 中其它四类业务语义项。

与 Memory owner 同源：
- `dayu/host/memory.py:1229-1255`：`CONTEXT_COMPACTED` 整体替换五类 memory；
- `dayu/host/memory.py:1720-1739`：`session_summary is None` 返回 empty view；
- `tests/host/test_memory_projection.py:1408-1442`：锁定 "without summary clears prior session summary"。

prompt 承诺的 clear 行为与 Memory owner 的实际执行一致。plan review F01 纠正（原 Goal Confirmation 错误声称 preserve）已正确修复。

### 3. claim 双来源

prompt 第 39-40 行同时包含：
- "accepted `evidence_material` 或 `previous_evidence_fact` 直接支持"（`claim` 定义）
- "只能引用 kind 为 `evidence_material` 或 `previous_evidence_fact` 的 source"（`support_labels` 定义）

与 typed contract `CompactSourceKindV2` 允许的两种 support kind 同源。plan review F02 纠正（原 claim 只提 "工具证据" 隐式排除 previous fact）已正确修复。

### 4. `answer_anchors` 字段

prompt 第 43-46 行定义：
- section 职责：保存既有回答、判断或结论锚点；不伪装成新推断。
- `title`：识别主题的简短业务标题。
- `detail`：可独立阅读的既有结论；保留条件、边界或不确定性；不发明新结论。
- `source_labels`：只引用 `answer_material` 或 `previous_answer_anchor`。

边界清晰，无内部术语。

### 5. 四种 drop reason 区分与 `policy_limit` 仅当前 feedback 具体 cap

prompt 第 63-68 行分别定义：
- `superseded`：已被更新/更完整/更权威替代，保留会过时/冲突/误导。
- `redundant`：仍有效但已完整表达，不掩盖冲突。
- `out_of_scope`：与当前任务/可预见后续无关，不因难分类/冲突/依据不足滥用。
- `policy_limit`：仍相关且原本应保留，当前 repair feedback 明示具体 cap，为满足 cap 必须舍弃；首次请求/无 feedback/无具体 cap 时禁止猜测使用。

第 68 行明确"四种 reason 是对 source 实际业务关系的互斥解释，不是固定优先级"。

`policy_limit` 的可见来源与 `dayu/host/context_governance.py:492-576` 同源：具体 cap 只在 candidate 超限后经 repair feedback 投影给模型。首次请求无 repair feedback，因此无具体 cap，禁止使用 `policy_limit`。

### 6. tests 是否锁定正确 owner 而非脆弱偶然字符串

- `test_llm_compaction.py::test_prompt_assets_are_self_contained_for_fresh_v2_contract`：按 section 分组断言业务语义子串存在（如 "整体任务背景、已完成进展、当前状态与关键约束"、"accepted evidence 直接支持"、"四种 reason 是对 source 实际业务关系的互斥解释"）。每个断言绑定到 frozen plan acceptance 中的精确语义，不是偶然出现的随机字符串。同时保留 forbidden-term 和完整 example parser/accept 断言。
- `test_public_compact_smoke.py::test_default_compactor_prompt_is_llm_facing_and_self_contained`：最小哨兵（`session_summary:null` 清除、previous evidence support、`policy_limit` 禁止条件、四个 reason 定义），加上完整 example 通过 production parser/accept barrier 证明。不做 owner-level 全量复刻。
- 两个测试均绑定 prompt owner 文件，不依赖 Host 内部实现。

### 7. hash 同源

独立验证：
- `sha256sum dayu/config/prompts/scenes/conversation_compaction_user.md` → `a2f5711c84f6fdd51f921e5d266d05cdb3f6a34a6c8321ffc42f0c5dc75a0dce`
- `docs/cli_init_workspace_manifest_v1.json` 中该 asset 的 `content_sha256` → `a2f5711c…`（一致）
- `sha256sum docs/cli_init_workspace_manifest_v1.json` → `fb6d0ba8fbf01b093419d178daf09c145bc8643e03b900703a91f2a3ff005f6c`
- `tests/cli/test_smoke_cli_init_provider_matrix.py::FROZEN_MANIFEST_SHA256` → `fb6d0ba8…`（一致）

两级 hash（asset → manifest → test constant）同源，且与 implementation artifact 记录一致。

### 8. frozen files preserved

`git diff 21b602c1 -- dayu/host/compaction.py dayu/host/context_governance.py dayu/host/memory.py` 无输出。implementation artifact 记录的 frozen-file SHA-256 与 plan 一致。

### 9. README/design no-change

implementation artifact 的 docs decision 逐条列出 no-change 理由。本 slice 只修改 prompt owner 和 tests，不改变目录职责、装配、schema、Host contract、状态机或用户工作流。触发判断正确。

### 10. scope / semantic ownership

- 实现严格限于 plan 定义的五个 allowed files + implementation artifact。
- 唯一 LLM-facing 语义 owner：`conversation_compaction_user.md`。
- typed schema / strict parser / accept barrier / repair feedback / Memory projection 均未修改。
- 没有新增 semantic verifier、fallback、loose parsing、默认值或兼容分支。

## Open Questions

无。

## Residual Risk

- `assigned to later work unit`：真实 provider 对字段分类、drop reason 与 repair cap 的稳定遵循度；owner 为 real Compactor conformance evidence work unit。deterministic prompt/parser tests 不能冒充真实模型行为。
- `assigned to later work unit`：frozen oracle/scenario 的 current-head readiness refresh；owner 为独立 readiness refresh work unit。
- `assigned to later work unit`：`forward_intents.status` 与 `reference_continuity.reason` 的 LLM-facing 业务语义；owner 为后续独立 LLM-facing schema work unit（controller 裁决 deferred-with-owner）。

所有 residual risk 均已分类并有 owner。无未分类 residual risk。

## Verdict

`pass`

实现正确兑现 accepted plan 的全部 frozen semantics。prompt owner 文案自足、无内部术语泄露、`session_summary:null` 清除语义与 Memory owner 同源、claim 双来源覆盖完整、四种 drop reason 互斥且 `policy_limit` 可见来源正确、两级 publication hash 同源、tests 锁定正确 owner。五个 dirty files + implementation artifact 形成完整 intended diff，无 scope drift。
