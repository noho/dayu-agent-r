# PR-190 F13 S1 C3 — AgentDS Adversarial Review

## Metadata

- reviewer: AgentDS
- date: 2026-08-06
- checkpoint: `docs/gateflow/pr-190-f13-s1-c3-checkpoint-20260806-165756.md`
- base commit: `62445b59`
- reviewed diff identity: `d82dd47e`
- scope: rolling, reactive multi-pass, Memory per-fact projection, reconnect, RunInput, call sites, repair exhaustion/fallback, stale/late single terminal, residue

## Verdict: **ACCEPTED** — zero blocking/high/medium findings

C3 实现与 accepted plan、design.md F13/v4、C3 checkpoint frozen expectations 一致。所有 adversarial 反例检查均未命中。以下逐项裁决。

## Adversarial Pass Results

### 1. Rolling projection — clean

`_previous_compacted_view_pair_from_replacement` (compact_material.py:2319–2330):
- 每个 previous fact atom 从 `replacement.evidence_facts` 逐项读取 `fact.canonical_evidence_refs`，不共享其他 atom 的 refs。
- `canonical_source_refs=(event_id,)` 是 compact event ref，与 `canonical_evidence_refs` 完全分离。
- **反例未命中**：两 fact refs 交叉污染、previous source event ref 冒充 evidence ref 均不成立。

### 2. Reactive multi-pass aggregation — clean

`_aggregate_pass_candidates` (compaction_operation.py:1229–1300):
- 从 `truth.replacement`（非 proposal）提取 retained labels 与 new facts。
- retained labels 通过 `selection_labels[0]` 且 source_kind 为 `PREVIOUS_EVIDENCE_FACT` 识别；new facts 使用完整 `fact.selection_labels` / `fact.context_labels`。
- 聚合 candidate 经 `accept_compact_candidate_v4` root revalidation。
- **反例未命中**：pass 间退回 proposal / flat union 不成立。

### 3. Memory per-fact projection — clean

`_facts_from_accepted_event` (memory.py:1740–1774):
- 每条 `EvidenceBackedFactView` 的 `evidence_refs` 来自 `fact.canonical_evidence_refs`（该 atom 自身 refs）。
- 同 event 内多 fact 各自独立取 refs，不共享 aggregate union。
- RunInput 消费 `snapshot.evidence_fact_memory.evidence_backed_facts`，逐 fact 迭代渲染。
- **反例未命中**：aggregate union 写所有 facts、omit 后仍传播均不成立。

### 4. Reconnect — clean

run_input.py:1975、4250 均通过 `parse_context_compacted_semantic_payload` 恢复 replacement。
该 parser 是 durable strict parser 的唯一 owner，重验 proposal / replacement / source boundary / 逐 fact binding / coverage / policy audit / aggregate-union 与 request-boundary membership。
- **反例未命中**：failed / rejected candidate 无 accepted replacement 读取入口；reconnect 不经 loose parse。

### 5. RunInput call sites — clean

`CompactArtifactView` (run_input.py:1978–1985、4253–4261) 的 `represented_evidence_refs` 来自 `semantic_payload.accepted_evidence_mapping_refs`。
- `accepted_evidence_mapping_refs` 在 `compact_pipeline.py:290–296` 是 derived property，从 `self.accepted_truth.replacement.canonical_evidence_refs` 派生。
- `CompactAcceptedReplacementV4.canonical_evidence_refs` (compaction.py:1592–1606) 是 per-fact refs 的 ordered unique union。
- **反例未命中**：aggregate 用于整体 represented refs，未用于重建逐 fact refs。

### 6. Repair exhaustion / fallback — clean

`_run_compaction_operation` (compaction_operation.py:751–1226):
- repair 每次重新执行完整 `accept_compact_candidate_v4` 链（line 1023–1027）。
- budget exhaust → `_failed_operation_result` (accepted_truth=None, no next_repair_feedback leak for partial pass truth)。
- `_non_repairable_contract_failure_result` 不携带 repair feedback。
- **反例未命中**：repair 旧 truth 复用、partial pass truth 泄漏均不成立。

### 7. Stale / late single terminal — clean

- `_CompactorProposalCancelledError` 在 Host cancellation 生效后直接终止 operation (line 954–981)，不产生第二 terminal。
- `_CompactionAttemptCancellationToken` 保证每次 attempt 独立 token，parent lifetime 优先。
- **反例未命中**：late 第二 terminal 路径不存在。

### 8. Fake duplicate repair — clean

`_typed_repair_omits_evidence_facts` (fake_compaction.py:630–645):
- 检查 `CompactValidationIssueCodeV4.DUPLICATE_SEMANTIC_ITEM` 且 `json_path` 以 `$["evidence_facts"]` 开头。
- 使用 typed issue code，不经 claim 文本、关键词或 subject matching 预判。
- **反例未命中**：claim heuristic 不存在。

### 9. Residue — clean

- `rg -n -i 'accepted_candidate|candidate_binding_v4'` → 零命中。
- active Python v3 symbol/schema → 仅 `tests/service/test_host_assembly.py` 两条 negative assertion。
- `PromptLocalProvenanceEntry` 无 `accepted_evidence_id` 字段，已替换为 `canonical_evidence_refs`。
- 所有 durable key、constructor、reader、fixture 均使用 v4/schema-5。
- **反例未命中**：v3 residue、旧 `accepted_evidence_id` 残留均不成立。

### 10. S2 boundary drift — clean

C3 production 文件（compact_material、compaction_operation、memory、run_input、dispatch、engine_ingest、compact_pipeline）中：
- 无 `ResolvedCompactorEvidenceFact` 新定义。
- 无 public Tool Trace 新 typed fact projection。
- `test_tool_trace_queries.py` 仅做 v3/schema-5 fixture 迁移，未引入 S2 新语义。
- **反例未命中**：S2 边界漂移不成立。

### 11. Runtime smoke — acknowledged

`utils/smoke_host_public_conversation_memory_scenarios.py` 与 `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py`：
- 使用 Host public API 与 fake compactor，不调用真实 LLM provider。
- checkpoint 明确标记为 "owner/integration/Host smoke tests，不是实际 provider 或 interactive CLI scenario"。
- **反例未命中**：smoke 未伪装为真实 CLI；该限制在 checkpoint 中已显式声明。

### 12. Test evidence

```
source .venv/bin/activate && pytest -q [C3 focused suite]
304 passed, 1 skipped, 3 warnings in 8.77s
```
1 skip 为既有条件；3 warnings 为第三方 `edgar` deprecation。

## Summary

| 检查项 | 结果 |
|--------|------|
| rolling per-fact provenance | clean |
| multi-pass replacement-only aggregation | clean |
| Memory per-fact refs (no aggregate union) | clean |
| reconnect strict parser | clean |
| RunInput aggregate-only represented refs | clean |
| repair exhaustion / no partial truth leak | clean |
| stale/late single terminal | clean |
| two-fact refs cross-contamination | not hit |
| previous source ref → evidence ref | not hit |
| omit-then-propagate | not hit |
| inter-pass proposal/flat union fallback | not hit |
| repair old truth reuse | not hit |
| failed artifact reconnected | not hit |
| fake claim heuristic | not hit |
| runtime smoke → real CLI | not hit |
| S2 boundary drift | not hit |
| v3 / accepted_candidate / schema-4 residue | clean |

**Verdict: ACCEPTED. 无需修复。**
