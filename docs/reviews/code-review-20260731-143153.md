# Code Review

## Scope

- Mode: current changes / Gateflow Slice 1
- Branch: `codex/prompt-oracle-adjudication`
- Base: accepted plan commit `b3f90c5d`
- Output file: `docs/reviews/code-review-20260731-143153.md`
- Included scope:
  - `dayu/fins/pipelines/sec_filing_collection.py`
  - `tests/fins/test_sec_pipeline_download.py`
  - `docs/reviews/aapl-download-canonical-ticker-layout-plan.md` 的 Slice 1 contract
- Excluded scope:
  - 尚未实施的 canonical ticker locator Slice 2
  - 已接受计划 artifacts 的文本质量
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Review evidence

- 真实入口链：
  `collect_filings_from_table -> FilingRecord.primary_document ->
  SecDownloader.list_filing_files -> RemoteFileDescriptor.name ->
  download_files_stream store callback -> source upsert primary_document`。
- owner 对齐：转换发生在 SEC submissions 进入 typed `FilingRecord` 时；downloader、
  workflow、storage 与测试夹具没有 fallback 或二次 basename。
- adversarial failure pass：
  - non-string/empty；
  - POSIX absolute；
  - backslash/platform path；
  - dot/dotdot；
  - empty segment/double separator；
  - trailing separator；
  - Windows drive-like filename。
- semantic ownership drift pass：未发现下游补偿、兼容 shim、loose parsing 或多真源。
- overcoupling pass：helper 只依赖 JSON contract 和标准库，不依赖 storage private helper，
  没有反向依赖。
- tests：
  - 目标 accession/form/路径通过真实 collection table 证明 integrated projection；
  - 普通单文件名不变；
  - 非法路径全部 fail closed；
  - 全文件 99 tests passed；
  - focused pyright 0 errors。

## Open Questions

无。

## Residual Risk

- 没有运行真实 SEC 网络下载作为自动测试；目标 submissions/index/HTTP 已在 goal
  confirmation 期间只读取证。确定性测试覆盖导致原异常的数据边界。
- 未来若 SEC 出现真实嵌套 archive 文件，需要新的直接证据和独立 contract 变更；当前不做
  fallback。

## Conclusion

`pass`

Slice 1 可进入 accepted slice commit。
