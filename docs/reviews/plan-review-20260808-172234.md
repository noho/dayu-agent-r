# F18 B2 Material Amendment 独立 Plan Review（DS）

- 审查时间：2026-08-08 17:22:34 CST
- 审查目标：`docs/gateflow/pr-190-f18-plan-20260808.md`、`docs/gateflow/pr-190-f18-b2-material-amendment-20260808.md`
- 审查基线：当前工作树，HEAD `1c92a5bebdf6dd428eaf7577c5d97f5105079d6e`
- 模式：provider-free adversarial plan review
- 最终结论：**PASS**

## Assumptions tested

1. B2 registry/oracle 只要求 previous/new EvidenceFact 在真实 cap 下竞争，不绑定 FY2024/FY2025。
2. R1 `s_0003` 与修正后的 R2 `s_0013` 都可经 public typed Fins 工具合法、完整读取，并提供隔离且足够大的真实 material。
3. R1 tool-result continuation 与 R2/R3 pre-start `ORDINARY` boundary 的 Host 时序正确。
4. effective ordinary loop owner 是 scene override 20，而不是 profile baseline 24；force-answer 最多额外一次。
5. 两次 non-covering 运行没有被隐藏、覆盖或冒充正式 coverage，后续链数与 wall/provider budget 仍有统一硬边界。

## Findings

未发现实质性问题。

## Direct evidence

### Registry / Oracle

`interactive.interactive.g06.cap-constrained-memory-replacement@1` 的 precondition 是 `relevant-material-over-initial-output-caps`，required evidence 是 real caps、Host cap/usage audit、accepted provenance/omitted complement、repair/fallback、durable/reconnect equality。predicate `interactive.29-compactor-output-accept-repair-fallback` 与 `interactive.30-compaction-semantic-memory-closure` 约束 previous/new EvidenceFact、typed provenance、caps、same-boundary repair 与同源持久化；没有 FY2024/FY2025 配对条件。因此 amendment 删除 FY2024-only setup 是收窄到 owner contract，不是放松 Oracle。

### R1 / R2 material 与 Fins typed legality

对同一真实 AAPL processed `sections.json` 直接核验：

- `s_0003`：Part I Item 1A，68,039 chars / 9,782 words，content SHA-256 `563b193a9c31061fa48f25b1b27709e94150d5c9cfceace80ce2a961b6a23b36`；不含 `416,161`、`133,050`、`391,035`、`123,216`、`21.7`、`18.2`。
- `s_0013`：Part II Item 8，61,252 chars / 9,744 words，content SHA-256 `d1a11c06db1a08e644946e6869cc152aa55353c5437214c1c2b85a1e62065607`；包含目标 `416,161`、`133,050`，不含 unsupported `21.7`、`18.2`。

`dayu/fins/tools/fins_tools.py` 的 `read_section` schema 要求 `ticker/document_id/ref`，并明确 ref 必须来自同 document 的 `get_document_sections`/search grounding；`dayu/fins/tools/read_runtime.py` 对跨文档或未知 ref fail closed。packaged `read_section_max_chars=80,000`，工具声明把该显式 limit 写入 effective truncation spec；runtime policy 只补缺省值，不覆盖显式 80,000。因此 68,039 与 61,252 都可完整进入 canonical tool outcome。修正后的 plan 禁止使用 358-word `s_0013_c03` 再 padding，避免用人工材料替代真实 completed R2 evidence。

### Host boundary 时序

`dayu/host/context_budget.py` 的五阶段 owner matrix直接规定：`CONTINUATION` 在 normal/soft/hard pressure 均为 `ALLOW_DISPATCH`；只有 `ORDINARY + SOFT_THRESHOLD_EXCEEDED` 触发 `COMPACT_SOFT_THRESHOLD`。`docs/host/design.md` 与 compact material builder 又规定 latest committed delta 在当前 input 之前构成 source，当前 `USER_INPUT_ACCEPTED` 只作为独立 current anchor且不可成为 evidence source。

因此 R1 tool result 继续在同 Run `CONTINUATION` 完成，R2 pre-start `ORDINARY` 首次把 completed R1 material作为 baseline source；R2完成 `s_0013` 后，R3 pre-start `ORDINARY` 才把 previous business-risk atom与新 FY2025 evidence置于同一 immutable target boundary。plan 对任何 reactive operation 立即 seal，防止把不含同 Run accepted tool result 的 reactive snapshot误当 target。

### Iteration owner 与 ceiling

`dayu/config/prompts/manifests/interactive.json` 明确 scene `max_iterations=20`；execution profile baseline是24且 `fallback_mode=force_answer`。`dayu/runtime/assembly.py` 的优先级为 `run_override > scene_override > execution_profile > base`，现有 pre-provider typed calibration也记录 `max_iterations` source 为 `scene_override`。`dayu/engine/agent.py` 最多运行20个ordinary iterations；只有普通预算耗尽或失败批次阈值触发时再执行一次 disabled-tools force-answer。因此 `21/Run` 是 typed hard cap，25只能是更宽的 external stop ceiling；plan没有改 scene 去凑24。

### Non-covering attempts 与统一预算

对现有 run-owned evidence 直接核验：

- `formal-chain-01/R1` 有5个 `RUNNER_CALL_INPUT_ASSEMBLED` / context-budget facts、0 compaction、canonical `RUN_SUCCEEDED`；actions/screen owner elapsed为27.5583s。
- `formal-chain-02/R1` 有2个 runner calls、0 compaction、canonical `RUN_SUCCEEDED`；actions/screen owner elapsed为15.0313s。

合计7 calls / 42.5896s，与 amendment 完全一致。plan保留其raw evidence并明确标 non-covering；最多三个新的 fresh workspace slots，每个只运行一条候选链。旧消耗继续进入同一个call ledger、1620s provider execution与1800s scenario deadline；monotonic remaining-time allocator、每链540s cap和最后180s reserve共同阻止重置预算或无界试错。invalid/non-covering 不占“有效覆盖”slot，但仍占统一时间/call ledger，且总 fresh workspace slots仍固定为3。

## Special lenses

- Goal-bound minimal design：只修正 material selection和预算解释，没有扩展产品、profile、scene或 harness。
- Architecture boundary：事实来自 registry/oracle、Fins storage/read tool、Host context matrix与Engine policy各自 owner；没有下游 fallback 或重算。
- Optimal / overengineering：复用现有真实 section与fixed profile，不增加 padding、动态 profile、synthetic replay或新工具。
- Overcoupling：R1/R2 acquisition、R3 target、R4 reconnect按依赖顺序分离；任何 prerequisite 或 reactive deviation立即seal。
- Failure handling：natural non-trigger、runner candidate invalid、digest drift、非MiMo、tool drift、预算耗尽都有明确停止语义，不用后续成功覆盖失败。

## Open Questions

无阻断问题。

## Residual risks

1. MiMo 的自然输出仍可能不形成单个160字符内、具有正确 provenance 的 R1/R2 EvidenceFact，或不触发指定 repair/fallback branch；按 plan 记录 `needs-more-evidence`，不得重跑同链或注入输出。
2. 历史 Trial2 sequence 327 root cause仍不可恢复；保持独立 unresolved residual，不由本次 fresh path反推。
3. B2 即使取得完整 observation，在用户裁决前仍须保持 `unadjudicated`、overall non-ready。

## Final conclusion

**PASS**。最新 material amendment 已消除过窄 R2 section 的压力缺口；registry/oracle、typed grounding、Host时序、effective iteration owner与统一预算均有直接 owner 证据支持。没有会阻止 provider gate 的实质 finding。
