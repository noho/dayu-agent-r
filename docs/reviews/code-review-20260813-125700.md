# Code Review

UF-FIX01 R1/R2 第二路独立 delta re-review（DS 路）。

## Scope

- Mode: current changes（delta re-review，严格限界）
- Branch: codex/upload-filing-oracle
- Base: 0391b589de075f47a2c13f8e173e48e3ae0f1c5e（gateflow: adjudicate UF-FIX01 fix re-review）
- Target: ef4e432427a8e670fed83904118f9efe39b94473（fix(fins): type prevalidation repository construction）
- Output file: docs/reviews/code-review-20260813-125700.md
- Included scope（delta 5 文件）:
  - dayu/fins/service_runtime.py（R1 production 变更）
  - dayu/cli/commands/fins.py（R2 docstring 变更）
  - tests/fins/test_fins_service_runtime.py（新增 owner-level 定点测试）
  - tests/cli/test_fins_commands.py（新增 CLI boundary 定点测试）
  - docs/gateflow/uf-fix01-validation-atomic-boundary-implementation-fix-20260813.md（fix artifact）
- Excluded scope: 非 delta 文件；两路 prior review artifact（禁止参考 MiMo）；未运行任何测试（约束）。
- Parallel review coverage: 无（禁止子 agent，本路全部由主 reviewer 逐行核销）。
- 核销输入: rereview adjudication、implementation fix artifact、delta diff 全文、5 个 delta 文件及直接依赖 production symbol（`FsFilingUploadStateRepository`、`build_fs_repository_set`、`_FsStorageInfra.__init__`、`_project_filesystem_error`/`_raise_path_free_error`、`FinsUploadPrevalidationError`/`fins_upload_prevalidation_io_failure`/`fins_upload_prevalidation_corruption_failure`、CLI `run_fins_direct_command` 与 `_run_fins_direct_command_async` 主链路）。

## 裁决核销

### R1 — repository construction + read 同 typed try：**CLOSED**

核销证据链（全部为直接读取的生产代码）：

1. **construction 与 read 同 typed try**：`dayu/fins/service_runtime.py:81-90`，`FsFilingUploadStateRepository(workspace_root, create_directories=False)` 构造与 `read_filing_upload_state(ticker, document_id)` 同处一个 `try`；构造期 workspace `resolve()` 的 `OSError` 不再绕过 prevalidation owner。
2. **I/O/corruption mapping 复用既有真源，未新增映射**：`except (OSError, RuntimeFileLockError)` → `fins_upload_prevalidation_io_failure()`；`except ValueError` → `fins_upload_prevalidation_corruption_failure()`。两条映射均为既有代码，delta 只是把构造纳入既有 typed boundary，无重复实现、无新 reason。
3. **cause chain 与 path-free 投影同源**：构造链 `FsFilingUploadStateRepository.__init__`（fs_filing_upload_state_repository.py:42-47，docstring 已声明 `OSError: storage core 初始化失败`）→ `build_fs_repository_set` → `FsStorageCore` → `_FsStorageInfra.__init__`（_fs_storage_infra.py:434-439：`workspace_root.resolve()` 包在 `except OSError` 内，走 `_raise_path_free_error(_project_filesystem_error(exc, action="解析 storage workspace"))`）。`_project_filesystem_error`（_fs_storage_utils.py:474-498）构造两层 path-free 异常：外层 `{action}失败`、`__cause__` 为 `{action}底层文件系统失败`，保留有意义 subclass 与 errno、丢弃 raw args，并 `__suppress_context__`；`_raise_path_free_error` 以 `from error.__cause__` 抛出并清空 `__context__`。与 owner 测试断言 `value.__cause__` 为 `PermissionError("解析 storage workspace失败")`、其 `__cause__` 为 `PermissionError("解析 storage workspace底层文件系统失败")` 完全一致。
4. **CLI exit 1 / exact path-free stderr / operator log 保留根因**：`dayu/cli/commands/fins.py:208-212`，typed `except FinsUploadPrevalidationError`（位于 generic `except Exception` 之前）→ `_LOGGER.exception("upload_filing prevalidation operational failure")`（logging 标准链路输出完整 `__cause__` 链，operator evidence 含 `PermissionError` 类型、errno 与“底层文件系统”层）→ `render_cli_error("dayu-cli upload_filing: {exc.failure.message}")` → `EXIT_FAILURE`。`fins_upload_prevalidation_io_failure()` message 为“上传状态读取失败，请检查工作区存储状态”（upload_failure.py:179），且 reason 构造经 `_validate_failure_reason_text` 禁止路径分隔符，stderr 无路径。
5. **无 CLI fallback / 字符串分类 / 兼容分支**：delta diff 中 `fins.py` 仅 docstring 3 行变更，generic branch 与其余生产文件零改动；production delta 仅为一次 owner-boundary try 移动，无 `hasattr/getattr`、无 `str(exc)` 分类、无 fallback。
6. **owner-level 与 CLI boundary 测试均断言 owner 级 contract**：owner 测试断言 exact typed reason（`== fins_upload_prevalidation_io_failure()`）、两层 `PermissionError` cause chain 消息、fresh workspace 零 mutation（`not workspace_root.exists()`，`create_directories=False` 下 `_FsStorageInfra` 不会 mkdir）；CLI 测试经真实 `cli_main.main` 入口注入第二次 workspace resolve 失败（对应主链路 `_resolve_workspace_root(args.workspace_root)` 后 `_prevalidate_upload_filing_request` → service_runtime → 仓储构造），断言 exit `1`、stdout 空、exact stderr 一行、operator log 三项、`workspace_resolve_count == 2`、workspace 零 mutation。断言为 exact 断言，未被削弱。

### R2 — docstring typed contract：**CLOSED**

- `dayu/cli/commands/fins.py:668-671`：`_prevalidate_upload_filing_request` docstring 的 `Raises` 由裸 `OSError` / `ValueError` 修正为 `FinsUploadUsageError`（既有行保留）与 `FinsUploadPrevalidationError`，与实际运行契约一致：该函数把 workspace_root 直接交给 `prevalidate_fins_upload_filing_request_for_workspace`，后者只可能经 `validate_fins_upload_filing_request` 抛 `FinsUploadUsageError`、经 typed try 抛 `FinsUploadPrevalidationError`。
- delta 对该文件仅改 docstring，无运行语义变更（diff 确认只有 Raises 两行删除一行新增）。

## Findings

未发现实质性问题。R1/R2 均 CLOSED；delta 无 CLI fallback、无字符串分类、无 non-goal 文件 diff（diff 全文确认 5 文件范围内 production 仅上述两处变更）。

## Open Questions

- 无。

## Residual Risk

- 本路按约束未运行测试、未做扩展检索；以下 delta 未改动、但未在本轮直接打开读体的既有链路，依赖其自身先前已验收状态与 fix artifact 记录的 tests-first 红灯→绿灯及 79 passed suite：`_resolve_workspace_root` 函数体（单次 resolve 假设）、`_prevalidate_download_request` 函数体（不 resolve workspace 假设）、`render_cli_error` 输出目标与换行、`cli_main` `--log-file` 到 `dayu.cli.commands.fins` logger 的接线。其中 CLI 测试对 `workspace_resolve_count == 2` 与 exact stderr 的断言一旦与上述既有行为不符会直接失败，属于可自我暴露的 contract 断言，风险低。
- operator evidence 保留的是投影后的 `PermissionError` 类型 + errno + “底层文件系统”两层 cause，原始 OS 消息在 storage projection 层被有意丢弃（path-free 闭合要求）；该投影设计为 round 1 已验收的既有 read-path 语义，本 delta 只是把构造纳入同一边界，未改变解释。
