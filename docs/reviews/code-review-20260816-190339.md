# Code Review Re-review — UF-FIX08 Slice 6（code review fix 后独立复核）

## Scope

- Mode: current changes（当前未提交 diff）
- Branch: `codex/upload-filing-oracle`
- Base: `main`（`256786b2`）
- HEAD: `1e062f6cc13c22232449b4dc80ffcccb93b796d7`（未 commit、未 stage、未 push）
- Output file: `docs/reviews/code-review-20260816-190339.md`
- Reviewed inputs:
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-slice6-code-review-fix-20260816.md`（fix artifact）
  - `docs/gateflow/uf-fix08-existing-source-auto-repair-slice6-implementation-20260816.md`（更新后）
  - `docs/reviews/code-review-20260816-183959.md`（AgentMiMo，frozen）
  - `docs/reviews/code-review-20260816-184513.md`（DS 上轮，frozen）
  - 当前 uncommitted diff（7 个 modified 文件 + 2 个 gateflow artifact）
- Fix delta: 仅 `tests/fins/test_sec_pipeline_download.py`（新增 `_SecPhaseBMutationCalls` 计数类型与 `test_sec_unsafe_phase_b_rolls_back_without_reset_blob_or_commit`，+265 行）
- 未读/未修改其它 reviewer 未公开的中间产物；两份 review artifact 均按 fix artifact 声明保持 frozen
- 本轮未运行 coverage（按用户指示避免并行写 `.coverage`）；coverage 数字引用 fix artifact 单进程采集

## 变更增量与裁决前提

上轮 DS Finding 1（SEC Phase B UNSAFE 真分支缺直接节点）被判 `accepted` 并在本 fix gate 修复；DS Finding 2（CN 顶层 generic terminal projection）被判 `rejected-with-reason` 并 assigned 到后续 download failure projection work unit；AgentMiMo Focus 5 的证据表述不准确（"SEC Phase B 已由 whole-tree 真实测试证明"），frozen artifact 不修改，由新直接节点消解证据缺口。以下逐项独立复核。

## Findings

未发现实质性问题。

## 逐项复核结论

### 1. 新 SEC Phase B 测试的执行前提：真实 Phase A COMPLETE + overwrite/prefetch

- `_seed_complete_sec_source()` 在真实 filesystem 上 seed 完整 SEC source（meta/payload/manifest 齐全，`old_manifest.read_bytes()` 成功即证 manifest 存在）。
- `overwrite=True`：`_can_skip_fast` 首条规则 `if overwrite ... return None`（`sec_pipeline.py:1375`），`_can_skip` 首条规则 `if overwrite ... return None`（`sec_pipeline.py:1404`），故 Phase A `COMPLETE` 不 fast-skip、不 fingerprint-skip，真实执行 provider `list_filing_files` 与 `prefetch_files_stream`。
- 测试断言 `downloader.download_files_called is True`（`prefetch_files_stream` 内 `StubDownloader:938` 置位），证明失败发生在 provider/prefetch 之后、即 Phase B，而非 Phase A 或 whole-tree 提前失败。`_download_single_filing_stream` 直调绕开顶层 preflight，Phase A classify 未被 patch 且返回真实 `COMPLETE`，故 Phase A gate 不可能 raise。
- 注入点为 `pipeline._source_repository.classify_staged_source_integrity` 单点，返回 invariant-valid `SourceIntegrityClassification(status=UNSAFE, revision=None, reasons=(META_UNTRUSTED,))`（`__post_init__` 全量通过）。若 gate 缺失，`has_same_source_publication_identity(COMPLETE, UNSAFE)` 会抛 `ValueError` 而非 typed preflight error，exact-reason 断言会失败——测试对 gate 位置有真实判别力。

### 2. exact typed reason、begin/rollback 各一次、reset/blob/commit 零、published 不变

- `assert exc_info.value.reason is SourceIntegrityPreflightReason.UNSAFE_PUBLICATION`（identity 比较，exact）。
- `calls == _SecPhaseBMutationCalls(begin=1, staged_classify=1, reset=0, blob=0, commit=0, rollback=1)`：dataclass 全字段相等。`staged_classify=1` 证明注入 fact 恰好被消费一次；begin/rollback observer 均调用真实 owner（`original_begin`/`original_rollback`）；reset/blob/commit rejector 任一被调用即 `AssertionError` 且计数 +1，最终计数为 0。
- `meta_path/payload_path/manifest_path` 三处 bytes 与 seed 前完全相等；`classify_source_integrity(...).status is COMPLETE` 用未 patch 的真实 storage 重分类证明 published publication 语义不变（不只字节不变）。
- SEC `finally`（`sec_download_filing_workflow.py:749-751`）在 gate raise 时 token 未置 None，回滚恰一次，无双 rollback 路径——与断言 `rollback=1` 互证。

### 3. test instrumentation 的签名、owner 与耦合

- 签名核对：`observe_begin(ticker)` / `reject_commit(batch)` / `observe_rollback(batch)` 对应 `BatchingRepositoryProtocol`；`classify_staged_unsafe(ticker, document_id, source_kind, *, batch)` 对应 `classify_staged_source_integrity`；`reject_reset(ticker, document_id, source_kind, *, batch)` 对应 `reset_source_document`；`reject_blob(handle, filename, data, *, batch, content_type=None, metadata=None)` 与 `DocumentBlobRepositoryProtocol.store_file`（`repository_protocols.py:1252-1261`）逐参一致。唯一收窄是 `reject_blob.handle: SourceHandle`（协议 union `SourceHandle | ProcessedHandle`），本流程运行时只传 `SourceHandle`，无实际风险。
- owner 核对：monkeypatch 目标是 `pipeline._batching_repository` / `pipeline._source_repository` / `pipeline._blob_repository`，与 `_SecDownloadFilingWorkflowHost` 实际消费的同一批实例一致（`_build_store_file` → `_build_store_file_impl(self._blob_repository, ...)`，`sec_pipeline.py:1560-1564`）。测试没有触达 workflow 内部私有状态。
- 耦合判断：6 个闭包各对应最终 `calls ==` 断言的一个字段，无冗余；采用与本文件既有 `reject_begin`（Phase A UNSAFE 测试）同一 instrumentation 风格；改用 spy 仓储子类需要绕过 `SecPipeline` 装配注入自定义 repository set，耦合更高而非更小。嵌套闭包为该文件既有模式，且必须捕获局部 `calls` 计数。不构成过度耦合，无需缩小。

### 4. production/README fix-gate 零新增

- fix delta 仅 `tests/fins/test_sec_pipeline_download.py`（diff stat 从 +199 → +464）。
- production 与三份 README 的 diff 与上轮完全相同：blob index（如 `README.md d74d13dc..36f05555`、`dayu/fins/README.md b8761e41..ce5143f9`）与全部 hunk 内容逐一比对无变化；四个 UNSAFE gate、docstring、README 表述与上轮 review 版本一致。

### 5. tests/README matrix 表述已被新节点支撑

`tests/README.md:431` "Download matrix覆盖 SEC/CN Phase A/Phase B `UNSAFE` 在 meta/reset前失败" 现在由四个直接节点支撑：SEC Phase A（`test_sec_unsafe_phase_a_and_whole_tree_preflight_have_zero_mutation`）、SEC Phase B（新节点）、CN Phase A（`test_cn_unsafe_phase_a_fails_before_meta_transport_or_reset`）、CN Phase B（`test_cn_unsafe_phase_b_rolls_back_without_reset`）。fix artifact 不修改 tests/README 的决策正确。

### 6. Finding 2 rejected-with-reason 与 plan/allowlist/unique failure owner 的一致性

- 不扩 CN 顶层/schema：`cn_download_workflow.py` 不在 accepted plan §8.1 Slice 6 production allowlist；plan §7.2 明确"不改变 download retry次数、provider transport、overwrite、rejection registry 或结果收敛"。修改 CN 顶层 projection 超出本 work unit 授权。
- 不固化 generic：为 `SourceIntegrityPreflightError` 补测断言 `filing_execution_failed` 会把当前 generic fallback 固化为稳定业务 contract，违反 AGENTS.md "禁止让测试固化偶然行为，倒逼生产代码保留兼容分支"；现有 closed download terminal reason 集合（`provider_*`/`storage_failed`/`filing_execution_failed`）没有可安全新增的 integrity code，借 upload failure code 或 preflight enum 值作 terminal code 会制造错误 owner。裁决理由与 unique failure owner 原则一致。
- residual 明确：fix artifact residual 表第一行 `一般 download typed terminal projection contract/schema → assigned to later work unit：后续独立 download failure projection owner`，owner 与 work unit 边界清晰。

### 7. 旧 MiMo 错误证据不修改且现已消解

- `docs/reviews/code-review-20260816-183959.md` mtime `18:39:53`，早于 fix artifact（`18:56:56`）与更新后的 implementation artifact（`18:55:39`）；当前内容仍含原 Focus 5 不准确表述，未被改写，frozen 状态成立。
- 证据缺口已由新直接节点消解：SEC Phase B gate 真分支现在有测试证明，frozen artifact 中的历史表述不再影响 gate 裁决；controller 裁决表已显式记录该消解。

## 验证结果（本 reviewer 独立复跑，未运行 coverage）

| 验证项 | 结果 |
| --- | --- |
| 新增 node `test_sec_unsafe_phase_b_rolls_back_without_reset_blob_or_commit` | 1 passed |
| `tests/fins/test_sec_pipeline_download.py` + `tests/fins/test_cn_download_workflow.py` | 201 passed |
| accepted focused matrix（plan §10） | 1230 passed |
| pyright（`dayu/ tests/ utils/`） | 0 errors, 0 warnings, 0 informations |
| `git diff --check` | 通过 |
| frozen oracle/scenario/design diff | 空（0 行） |
| scope | 仅 slice 6 allowed 7 文件 + 2 个 gateflow artifact + 2 个 frozen review artifact |

计数与 fix artifact 声明一致（1230/201/1）；完整 Fins suite（1851）与 Service+CLI（188）未独立复跑，其增量 = 上轮已验证计数 + 恰好 1 个新测试节点，算术一致。coverage（SEC 83%、CN 83%，branch-aware）引用 fix artifact 单进程采集，本轮按用户指示未重跑。

## Open Questions

- 无。

## Residual Risk

- CN 顶层 per-filing UNSAFE 的 generic terminal projection（DS Finding 2）未在本 slice 修改，属于有意的 bounded/path-free 政策；其 typed 化归属后续 download failure projection contract/schema work unit（fix artifact 已登记 owner）。
- SEC/CN single-filing Phase A/B UNSAFE gate 的生产触发面仍是 whole-tree preflight 与 mutation 之间的 race window；主要保护面（whole-tree typed 拒绝）与四个 gate 边界现均有直接节点。
- coverage 数字未在本轮独立复跑（避免并行写 `.coverage`）；如需独立 line 级确认建议在无并行写者时执行。
- 未执行真实 CLI/provider evidence（归属 UF-PF08/UF-PF12 evidence owner）。
- 未读 MiMo 之外的任何未声明中间产物；frozen review artifact 的历史表述以 controller 裁决为准。

## 结论

**PASS（无 findings）**。DS Finding 1 已由 `test_sec_unsafe_phase_b_rolls_back_without_reset_blob_or_commit` 正确修复并独立验证（真实 COMPLETE/overwrite/prefetch 前提、invariant-valid staged UNSAFE 注入、exact typed reason、begin/rollback 各一次、reset/blob/commit 零、published 完整不变）；DS Finding 2 的 rejected-with-reason 与 plan allowlist、unique failure owner 原则一致且 residual 明确；旧 MiMo 错误证据保持 frozen 且缺口已消解；production/README 零新增；tests/README 表述被新节点支撑；验证计数、pyright、scope/frozen guards 全部通过。Slice 6 re-review gate 可放行。
