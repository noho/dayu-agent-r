# Code Review — UF-FIX07 Aggregate Re-Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `64050349`
- HEAD: `6b804001`（实施基线）+ 未提交 aggregate fix diff
- Output file: `docs/reviews/code-review-20260815-222906.md`
- Included scope: amendment §7 四个 allowed paths 的 aggregate fix diff
  - `dayu/fins/pipelines/docling_upload_service.py`（production）
  - `tests/fins/test_docling_upload_service.py`（tests）
  - `dayu/fins/README.md`（docs）
  - `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md`（artifact）
- Excluded scope: 原 plan、goal confirmation、四份 code/plan review inputs、registry/oracle/frozen evidence、UF-PF07/PF12
- Parallel review coverage: 无。单 reviewer 沿真实链路逐文件走读：
  `_prepare_upload_selection` → `_UploadSelectionPreparation.filing_primary` →
  `_build_original_assets` → `_build_upload_source_fingerprint` →
  `_can_skip_upload` / `_resolve_document_version` → `prepare_upload` 控制流 →
  storage snapshot `primary_document` / `get_primary_source()` →
  material fingerprint 回归；README role order 描述。
- Contract 依据：`docs/gateflow/uf-fix07-aggregate-review-plan-amendment-20260815.md`（authoritative amendment）

## 独立只读验证结果

- 13-file affected suite：`1362 passed, 1 skipped, 3 warnings`（exit 0）。
- 全仓 `python -m pyright dayu/ tests/ utils/`：`0 errors, 0 warnings, 0 informations`。
- 六个修改生产文件 branch coverage：
  - `arg_parsing 99%`
  - `commands/fins 81%`
  - `ingestion_runtime 88%`
  - `docling_upload_service 85%`
  - `upload_tools 92%`
  - `upload_format_contract 89%`
  全部 ≥80%。
- `git diff --check 64050349...HEAD`：无输出。
- 实施期间 HEAD 保持 `6b804001`。
- 四份 code/plan review inputs、原 plan 与 goal confirmation 的 `git diff` 为空。
- 未执行 UF-PF07/UF-PF12；未修改 registry/oracle/frozen evidence；未 stage/commit/push/建 PR。

## Finding 1 复核：role-aware fingerprint 与 primary-only flip update

### 修复状态：已修复

**amendment 要求**：
- `_UploadSourceFingerprint` typed result 含 `value` 与 `identical_skip_safe`
- `_UploadSelectionPreparation.filing_primary` 显式承接 authoritative primary
- single-file 保持 v1 公式，multi-file 使用 `filing-primary-role-v2` envelope
- primary descriptor 与 companion 完全相同时 `identical_skip_safe=False`
- `_can_skip_upload()` 先检查 overwrite/previous/deleted，再要求 `identical_skip_safe=True` 且 digest 相等
- `_resolve_document_version()`：无 previous 为 `v1`；existing + unsafe 无条件 increment；safe 时保持既有规则

**实现证据**：

1. **`_UploadSourceFingerprint` 定义**（`docling_upload_service.py:141-151`）：
   ```python
   @dataclass(frozen=True, slots=True)
   class _UploadSourceFingerprint:
       value: str
       identical_skip_safe: bool
   ```
   ✅ 与 amendment §4.1 一致。

2. **`_UploadSelectionPreparation.filing_primary`**（`docling_upload_service.py:184-191`）：
   ```python
   @dataclass(frozen=True)
   class _UploadSelectionPreparation:
       ordered_files: tuple[Path, ...]
       converter_inputs: tuple[Path, ...]
       filing_primary: Path | None
   ```
   ✅ 与 amendment §4.1 一致。

3. **`_prepare_upload_selection` 中 `filing_primary` 赋值**（`docling_upload_service.py:1087-1097`）：
   ```python
   if source_kind is SourceKind.FILING:
       ...
       filing_primary = None if selection.is_empty else selection.require_primary()
       converter_inputs = () if filing_primary is None else (filing_primary,)
       return _UploadSelectionPreparation(
           ordered_files=ordered_files,
           converter_inputs=converter_inputs,
           filing_primary=filing_primary,
       )
   ```
   ✅ 直接从 validated selection 取 `require_primary()`，不从 `ordered_files[0]` 或下游反推。

4. **`_build_upload_source_fingerprint` 签名与实现**（`docling_upload_service.py:1244-1335`）：
   - 签名包含 `filing_primary: Path | None`
   - filing 分支：单文件保持 v1 公式（`identical_skip_safe=True`），multi-file 使用 `filing-primary-role-v2` envelope
   - role-to-asset 关联用 `_build_filing_original_asset_identity(filing_primary)` 在 `assets[].name` 中 exact 命中
   - `identical_skip_safe = primary_descriptor not in companion_descriptors`
   - material 分支：保持现有 name-sorted payload，`identical_skip_safe=True`
   ✅ 与 amendment §4.2-§4.4 一致。

5. **`_can_skip_upload` 实现**（`docling_upload_service.py:1214-1241`）：
   ```python
   if overwrite or previous_meta is None:
       return False
   if require_source_meta_is_deleted(previous_meta):
       return False
   if not source_fingerprint.identical_skip_safe:
       return False
   previous_fingerprint = _text_meta(previous_meta, "source_fingerprint")
   return bool(previous_fingerprint) and previous_fingerprint == source_fingerprint.value
   ```
   ✅ 与 amendment §4.2 一致。

6. **`_resolve_document_version` 实现**（`docling_upload_service.py:1338-1363`）：
   ```python
   if previous_meta is None:
       return "v1"
   previous_version = _text_meta(previous_meta, "document_version") or "v1"
   if not source_fingerprint.identical_skip_safe:
       return _increment_document_version(previous_version)
   previous_fingerprint = _text_meta(previous_meta, "source_fingerprint")
   if previous_fingerprint and previous_fingerprint != source_fingerprint.value:
       return _increment_document_version(previous_version)
   return previous_version
   ```
   ✅ 与 amendment §4.2 一致：existing + unsafe 无条件 increment。

7. **`prepare_upload` 中 data flow**（`docling_upload_service.py:361-420`）：
   ```python
   source_fingerprint = _build_upload_source_fingerprint(
       original_assets,
       source_kind=source_kind,
       filing_primary=selection_preparation.filing_primary,
   )
   if _can_skip_upload(normalized_previous_meta, source_fingerprint, overwrite):
       ...
       return UploadOperationResult(status="skipped", ...)
   ...
   current_version = _resolve_document_version(normalized_previous_meta, source_fingerprint)
   ...
   return _PreparedAssetMutation(
       ...
       source_fingerprint=source_fingerprint.value,  # 只持久化 .value
       document_version=current_version,
   )
   ```
   ✅ 同一个 typed fingerprint 传给 skip/version；只持久化 `.value`。

### Finding 1 结论：PASS

实现完整满足 amendment §4 的所有 contract 要求。filing fingerprint 正确编码 authoritative primary 角色，primary 翻转会触发 update/version increment 并切换 downstream primary。

## Finding 2 复核：README role order

### 修复状态：已修复

**amendment 要求**：
- 将"全部 originals 按输入顺序读取"改为"按角色顺序读取：authoritative primary 在前，companions 保持原请求相对顺序"

**实现证据**（`dayu/fins/README.md:572`）：
```
filing selection 的全部 originals 会在 publication batch 开始前按角色顺序读取：authoritative primary 在前，companions 保持原请求相对顺序；converter inputs 只包含 authoritative primary...
```
✅ 与 amendment §6 一致。

### Finding 2 结论：PASS

README 已正确描述 role order contract。

## Amendment §5 Tests 复核

### §5.1 可区分 primary flip

**测试**：`test_distinguishable_filing_primary_flip_updates_v2_then_skips_replay`（`test_docling_upload_service.py:2929-3029`）

**断言覆盖**：
- create `v1`，flip 后 `v2`，replay skipped 保持 `v2` ✅
- `source_fingerprint` 改变后相同 ✅
- `primary_document` 切换到 second derived ✅
- `derived_from` 回指 second original ✅
- snapshot `get_primary_source()` 读取 second derived bytes ✅
- converter calls == `[first.name, second.name]`（flip 后只转换新 primary）✅
- companions 重排后 fingerprint 相同并可 skip ✅

### §5.2 不可区分 duplicate descriptors

**测试**：`test_ambiguous_filing_primary_forces_versions_then_recovers_safe_skip`（`test_docling_upload_service.py:3032-3211`）

**断言覆盖**：
- 两次 v2 digest 相同且 `identical_skip_safe=False` ✅
- companions-only duplicate `identical_skip_safe=True` ✅
- create `v1`，same-primary replay `v2`，primary flip `v3` ✅
- flip 后 `primary_document` 指向 second derived ✅
- `derived_from` 回指 second original ✅
- `identical_skip_safe` 不在持久化 meta 中 ✅
- 改变 descriptor 后 `v4`，replay skipped 保持 `v4` ✅
- converter calls 正确反映每次转换 ✅

### §5.3 Move/rename/content/material regressions

**测试**：
- `test_safe_multifile_whole_set_move_keeps_v1_and_published_tree`（`test_docling_upload_service.py:3214-3312`）✅
- `test_filing_fingerprint_excludes_path_identity_but_tracks_filename_and_content`（`test_docling_upload_service.py:3404+`）✅
- material fixed digest `099dc963...1d7fdd` 保持不变 ✅

**断言覆盖**：
- 整组 move：storage identities 全变，fingerprint 相同且 safe，skip 保持 `v1` ✅
- published originals 保持旧 identities ✅
- single-file move skip、rename update、content update ✅

### §5.4 Old v1 multi-file -> v2 transition

**测试**：`test_old_v1_multifile_fingerprint_transitions_once_to_v2_and_then_skips`（`test_docling_upload_service.py:3315-3401`）

**断言覆盖**：
- 独立旧公式 fixture 不调用 production builder ✅
- v2 `.value != old_digest` ✅
- `identical_skip_safe=True` ✅
- `_can_skip_upload` 返回 False ✅
- `prepare_upload` 返回 update mutation，`document_version="v2"` ✅
- replay skipped 保持 `v2` ✅
- single-file old/current digest 相等 ✅

### Tests 结论：PASS

所有 amendment §5 要求的测试矩阵均已实现并通过。

## 全范围 Semantic Ownership / Atomicity / 100 / Help / Tool Schema / Material / No-Shim Regression Pass

### Semantic Ownership

- **fingerprint owner**：`_build_upload_source_fingerprint` 是唯一构造 `_UploadSourceFingerprint` 的位置，safety bool 由该 owner 裁决，不由 caller、storage 或 README 重算。✅
- **filing_primary owner**：`_prepare_upload_selection` 从 validated selection 的 `require_primary()` 取得，不从 `ordered_files[0]`、converter input 或下游反推。✅
- **skip/version owner**：`_can_skip_upload` 和 `_resolve_document_version` 职责分离，各自只消费 typed fingerprint，不读取 overwrite/deleted/primary pointer。✅
- **material fingerprint**：继续使用现有 name-sorted payload，不混入 filing 参数。✅
- **no fallback/shim**：production diff 无 `hasattr/getattr`、loose parsing、legacy helper、dual-read 或 compatibility branch。✅

### Atomicity

- `_build_pending_assets` 在 skip 判定之后执行，skip 不会触发转换或 publication。✅
- `prepare_upload` 返回的 `_PreparedAssetMutation` 包含完整 `source_fingerprint.value` 和 `document_version`，caller 在短事务中一次性 commit。✅
- material fingerprint 不受 filing role-aware 变更影响。✅

### 100-File Limit

- `ingestion_runtime.py` 的 `_validate_upload_file_count` 继续限制 100 个文件上限。✅
- fingerprint builder 不引入新的文件数量限制或性能问题。✅

### Help / Tool Schema

- CLI help 和 LLM-facing upload tool schema 由 `upload_format_contract` 的 `_project_filing_primary_rules` 产生，不暴露内部 type 名或 fingerprint 公式。✅
- README 不暴露实际 path digest、内部 dataclass/type 名或旧迁移兼容方案。✅

### Material Regression

- material fingerprint formula、`identical_skip_safe=True`、skip/update/version 行为完全不变。✅
- material fixed digest `099dc963...1d7fdd` 保持不变。✅
- `FinsUploadMaterialRequest` 无 selector 字段，material 不可能携带 `filing_primary`。✅

### No-Shim / No-Dual-Read

- old v1 -> v2 transition 通过 update/version increment 完成，不做 dual-read fallback。✅
- `_resolve_document_version` 在 `identical_skip_safe=False` 时无条件 increment，不再比较 digest。✅
- production diff 无 legacy helper、migration shim 或 compatibility branch。✅

### Regression Pass 结论：PASS

全范围 semantic ownership/atomicity/100/help/tool schema/material/no-shim 回归检查未发现问题。

## Open Questions

- 无。

## Residual Risk

- R1：primary 与 companion descriptor 完全相同时无法在不引入 path/order identifier 下区分；ambiguous replay 会持续 version churn。
  **分类**：accepted conservative boundary。恢复 non-ambiguous 后可再次 safe skip。
- R2：当前已持久化的旧无角色 multi-file digest 首次按 v2 upsert 会 update/version increment。
  **分类**：fixed in current aggregate fix。独立 old-v1 seed test 固定 fail-safe transition；不做 dual-read 或 compatibility shim。迁移需求归 `UF-FIX08`。
- R3：旧 basename-based source schema 的兼容、首次重传或自动修复。
  **分类**：assigned to later work unit `UF-FIX08`。
- R4：同 request/同 document 并发 writer。
  **分类**：assigned to later work unit `UF-FIX10`。
- R5：fresh company meta warning。
  **分类**：assigned to later work unit `UF-FIX11`。
- R6：optional real Docling 与 UF-PF07/UF-PF12 未执行。
  **分类**：assigned to later evidence work。本 fix 只完成 deterministic owner/aggregate validation。
- R7：registry/oracle/frozen evidence 仍记录修复前观察。
  **分类**：assigned to later evidence work。当前 gate 明令只读。

所有 residual risks 已分类；没有 blocking open question，也没有未分类 residual risk。

## 结论

**Finding 1（role-aware fingerprint）**：已修复。filing fingerprint 正确编码 authoritative primary 角色，primary 翻转触发 update/version increment 并切换 downstream primary；不可区分 descriptor 使用 conservative unsafe contract；single-file/multi-file 公式分离；skip/version owner 职责清晰。

**Finding 2（README role order）**：已修复。README 已改为 primary-first role order 描述。

**全范围 regression**：PASS。semantic ownership、atomicity、100-file limit、help/tool schema、material、no-shim/no-dual-read 全部通过。

**验证证据**：
- 1362 passed, 1 skipped, 3 warnings ✅
- pyright 0 errors ✅
- 六文件 branch coverage 全部 ≥80% ✅
- diff check clean ✅
- 只读 review/plan/goal confirmation 无 diff ✅

**Decision**：`AGGREGATE RE-REVIEW PASS`
