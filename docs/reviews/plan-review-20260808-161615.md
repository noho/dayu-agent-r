# PR 190 F18 最终独立 Plan Re-review（AgentMiMo）

## Reviewed target and scope

- Target：`docs/gateflow/pr-190-f18-plan-20260808.md`、
  `docs/gateflow/pr-190-f18-plan-fix-20260808.md` 与
  `docs/gateflow/pr-190-f18-plan-third-rereview-adjudication-20260808.md`。
- Prior finding baseline：`docs/reviews/plan-review-20260808-155519.md`。
- Binding authority：F18 goal/amendment、`docs/host/design.md`、`docs/engine/design.md`、
  `docs/cli_ci.md`、scenario/oracle registry、production owner code与Trial2只读typed rows。
- Focus：predicted-only ratio=0.018；R1/R2/R3 source boundary与current anchor；
  `POST_COMPACT`/`CONTINUATION`；reactive non-covering；reactive cap=1；ordinary
  25 calls/Run；expected/hard compactor budgets；replacement/repair/fallback role；
  B1/B2 publication identity与全局stop。
- 本轮未读取另一reviewer的最终review，也未读取或触碰
  `docs/reviews/plan-review-20260808-095346.md`；未运行provider，未修改plan、产品、
  registry或evidence，未commit/push。

## Assumptions tested

| Assumption | 结论 | Direct owner evidence |
| --- | --- | --- |
| ratio=0.018派生soft threshold 18,874 | 成立 | `floor(1,048,576 * 0.018)=18,874`；typed policy要求soft<hard且ratio合法 |
| feasibility band只使用pressure owner字段 | 成立 | Trial2 canonical rows：seq5 ordinary predicted=9,048；seq104/115 continuation=19,174/20,316；seq125 ordinary=20,363；plan不再使用conservative field |
| R1 continuation越soft不会抢先compact | 成立 | Host 15-case matrix对`CONTINUATION`的normal/soft/hard全部`ALLOW_DISPATCH` |
| R2 pre-start baseline source只含completed R1，R2 current input不成为citable evidence | 成立 | pre-dispatch builder以R2 current input sequence作排他终点；selection保护current anchor，`CompactInputV4.current_input`与source boundary分离 |
| R2 baseline accepted后能在同Run继续取FY2025 | 成立 | `POST_COMPACT`在soft下allow；后续active-Run calls为`CONTINUATION`且soft/hard均allow；hard threshold 524,288远高于现有candidate band |
| R3 pre-start是首个可同时拥有previous FY2024与completed R2 FY2025 evidence的target | 成立 | accepted baseline投影previous compacted view；R2 completed group位于R3 current input排他终点之前，且current_input_ref不属于consumed compacted_source_refs |
| 同Runreactive snapshot不能覆盖FY2025 target | 成立 | reactive复用pre-dispatch view，delta终点仍是active Run input；同Run后续tool result不进入boundary；plan将全部reactive明确标为non-covering并seal |
| reactive cap=1与hard operation/call矩阵同源 | 成立 | R1最多1 reactive；R2/R3各最多1 proactive+1 reactive，正式hard=5 operations/25 calls；R4再加2/10，总hard=7/35 |
| ordinary call硬上界75/100 | 成立 | Engine最多24个loop calls；standard-256k `force_answer`最多再调用一次，故R1-R3为3*25=75，含R4为4*25=100 |
| expected 2/10不冒充hard cap | 成立 | plan分别记录mandatory baseline+target expected=2/10与typed hard=5/25、7/35 |
| exhausted-fallback不会冒充replacement accepted | 成立 | R3 role闭合：replacement/repair必须accepted；fallback必须exact 5 rejects、`retry_repair_budget_exhausted`与deterministic fallback；execution index按未覆盖branch继续分配fresh slot |
| B1 frozen evidence不会被B2 publication改写 | 成立 | B1 bundle/tree明确immutable read-only；B2在首次writer前冻结全新bundle id与独立public/private root，只生成B2-only report/index/digest/scan |
| 全局stop不被其它chain成功覆盖 | 成立 | 再次`runner_candidate_invalid`明确停止整个B2并重开owner investigation；profile drift、非MiMo、terminal/cap/hygiene failure均fail closed |

## Findings

No material findings.

上一轮四个owner blockers已经在当前plan中关闭：

1. pressure calibration只使用`predicted_input_tokens`，不再把conservative estimate冒充decision truth；
2. FY2025 acquisition被重排为R2 baseline accepted后的同Run执行，R3成为唯一合法target；
3. reactive cap与per-Run proactive/reactive hard bound分开计算，不再宣称per-chain=2；
4. force-answer额外call已计入25 calls/Run。

复审过程中发现的B1/B2 publication root冲突也已关闭：B1旧bundle保持immutable，B2使用新bundle及独立finalization。

## State-machine and role verdict

```text
R1 completed FY2024
  -> R2 pre-start ORDINARY baseline (source=completed R1; current=FY2025 request)
  -> accepted FY2024 atom
  -> same R2 POST_COMPACT/CONTINUATION acquires FY2025
  -> R2 RUN_SUCCEEDED
  -> R3 pre-start ORDINARY unique target
       source = previous FY2024 atom + completed R2 FY2025 evidence
       current/trace = unsupported 21.7% / 18.2%
       caps = evidence item cap 1
  -> role-specific accepted repair/replacement or exact exhausted fallback
  -> replacement accepted时才允许R4 reconnect
```

每个箭头都有明确terminal/material prerequisite；缺项、non-trigger、reactive或自然branch不符合role时均seal并保留
`needs-more-evidence`，没有fallback、analyzer或回答文本补真源。

## Open questions

1. fresh MiMo的R2/R3 prediction是否仍落在旧typed band的可触发一侧，只能由正式运行自然观察；plan已禁止调参、padding与同链重跑。
2. reject→accept与五次reject→fallback是否自然出现仍不确定；最多三个fresh workspace slot可能仍留下branch gap。
3. 旧Trial2 seq327原始owner exception未durably捕获，继续是不可恢复的独立residual，不影响fixed-profile scenario合法性。

这些问题都已有bounded、fail-closed disposition，不要求implementation agent重新设计，也不构成provider gate blocker。

## Residual risks and suggested tracking destination

- fresh chain natural non-trigger或branch不齐：写入B2 observed report与execution index，状态保持`needs-more-evidence`/
  `unadjudicated`，不得追加第四个workspace。
- 任一合法fixed-profile chain再次`runner_candidate_invalid`：停止整个B2，回到owner investigation；不得以其它chain覆盖。
- B1 cold analyzer `compactor_responses=0`与provider-native request id unavailable：继续留在B1 limitation/residual question。
- cross-opener profile contract与旧seq327 cause：留在F18 final closeout residual；若要定义或修复，进入独立goal/design gate。
- B2用户裁决前overall readiness保持non-ready。

## Validation performed

- 只读核对Trial2 canonical sizing rows：seq5/104/115/125/300。
- 只读核对pre-dispatch material builder、stage/pressure矩阵、Engine force-answer与reactive policy owner。
- 对最新plan、plan-fix、third adjudication执行`git diff --check`，通过。
- 未运行provider；plan-only review未运行产品测试来冒充formal observation。

## Final plan review conclusion

Binary provider gate verdict：`PASS`

当前plan已足够具体且owner一致，可以进入下一gate。剩余风险来自真实MiMo是否自然命中触发与repair/fallback分支，而不是setup、
material ownership、预算或publication设计缺陷；计划已为所有non-trigger与异常路径规定bounded seal、evidence preservation及
Oracle/readiness不变量。这些项目只保留在Residual Risk，不阻塞计划，也不把尚未发生的formal observation提前标为成功。
