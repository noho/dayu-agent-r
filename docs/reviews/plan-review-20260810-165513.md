# Plan Second Re-Review: `WU-CLI-DOWNLOAD-02-DL-F12-F14`（fix loop 2 后）

## Reviewed Target

- **Artifact**: `docs/gateflow/wu-cli-download-02-plan-20260810.md`（经 fix loop 1 + fix loop 2 修订）
- **Fix loop 2 artifact**: `docs/gateflow/wu-cli-download-02-plan-fix-2-20260810.md`
- **Adjudication**: `docs/gateflow/wu-cli-download-02-plan-review-adjudication-20260810.md`（含 first re-review 裁决 §"第一轮 re-review 裁决"）
- **Previous DS re-review**: `docs/reviews/plan-review-20260810-164911.md`（2 findings: N1 blocking, N2 low; conclusion=fail）
- **Previous MiMo re-review**: `docs/reviews/plan-review-20260810-164812.md`（2 findings: C01 low, C07 low; conclusion=pass）
- **Work unit**: `WU-CLI-DOWNLOAD-02-DL-F12-F14`
- **Branch**: `codex/download-oracle`
- **HEAD**: `3811f95c82fbf0daf15740a5d217eed4d8b49df5`

## Re-review Scope

重点核验第一轮 re-review 裁决的 4 项（DS N1, DS N2, MiMo C01, MiMo C07）是否真正关闭，并再次检查 fix loop 2 修订没有引入新的 blocking gap。

---

## 逐 Finding 关闭核验

### DS N1: `cn_report_selection.py` 不在 Slice 2 allowed files（原 blocking）

- **裁决**: "接受，blocking。必须将该文件加入 Slice 2，并在共有文件表明确 Slice 2 只做 query rename、Slice 3 才做 classification/projection"
- **状态**: **已关闭** ✓
- **直接证据**:
  - Slice 2 allowed production files 新增 `dayu/fins/pipelines/cn_report_selection.py`（plan line 313）
  - Slice 2 exact change 2（line 328）: "其中 `cn_report_selection.py` 当前两处 `query.target_periods` 访问必须在本 slice 只机械改名为 `query.discovery_periods`，protocol/docstring 只使用 discovery 语义，不提前修改 classifier 或 candidate projection"
  - §6 共有文件边界表新增第 5 行（lines 358-359）: `cn_report_selection.py` — Slice 2 "仅将两处 `query.target_periods` 机械重命名为 `query.discovery_periods`；不改分类/候选语义"，Slice 3 "重写 category-first classification，构造/消费 `CnReportPeriodProjection`，selection sort 复用既有 `CN_FISCAL_PERIOD_ORDER`"
  - 表后说明（line 360）: "`cn_report_selection.py` 是 Slice 2/3 共有文件：Slice 2 只做 query field rename...Slice 3 才做 classification/projection"
  - Test 覆盖: `tests/fins/test_cn_report_selection.py` 已在 Slice 2 allowed test files（line 321）与 §7.1 focused union 中，无需新增

### DS N2: `CN_FISCAL_PERIOD_ORDER` slice 归属不清（原 low）

- **裁决**: "接受。Slace 2 首次由 policy 使用；共有文件表必须明确 Slice 2 定义常量，Slice 3 的 projection/selection 只复用"
- **状态**: **已关闭** ✓
- **直接证据**:
  - §5.2 line 139: "CN/HK 的 canonical period order 在 **Slice 2** 由 `cn_download_models.py` 新增唯一常量 `CN_FISCAL_PERIOD_ORDER = (FY,H1,Q1,Q2,Q3,Q4)`，供同 slice 的 form policy canonicalization 使用；Slice 3 的 `CnReportPeriodProjection` 与 selection sort 只复用该既有常量"
  - §6 共有文件边界表 `cn_download_models.py` 行（line 354）: Slice 2 列 "定义唯一 `CN_FISCAL_PERIOD_ORDER`；完成 `CnReportQuery.target_periods -> discovery_periods` 与 query docstring"；Slice 3 列 "复用既有 order 做 canonical validation"
  - Slice 2 exact change 1（line 327）: "在 `cn_download_models.py` 先定义唯一 `CN_FISCAL_PERIOD_ORDER`；新增 `CnDownloadPeriodPolicy`、语义化默认常量与 `resolve_download_period_policy(...)` 并复用该顺序"
  - §5.3 line 248: 明确 `CN_FISCAL_PERIOD_ORDER` 与 domain `FISCAL_PERIODS` 职责不重叠，前者只拥有 CN/HK download canonical order

### MiMo C01: `FISCAL_PERIODS` 引用可能不存在（原 low）

- **裁决**: "按直接代码证据关闭。`dayu/fins/domain/filing_semantics.py::FISCAL_PERIODS` 已存在"
- **状态**: **已关闭** ✓
- **直接证据**:
  - 代码基线确认: `dayu/fins/domain/filing_semantics.py` line 79: `FISCAL_PERIODS: Final[frozenset[FiscalPeriod]] = frozenset(cast(tuple[FiscalPeriod, ...], ("FY", "H1", "Q1", "Q2", "Q3", "Q4")))`
  - Fix loop 2 §3 记录了该路径核验
  - Revised plan §5.3 line 248: "当前基线已存在 `dayu/fins/domain/filing_semantics.py::FISCAL_PERIODS`（定义于该模块现有第 79 行附近），公共类型直接复用它做成员校验，并校验 tuple 与无重复；无需创建新模块、常量或替代真源"
  - Ownership 分离: `FISCAL_PERIODS` 拥有通用 membership；`CN_FISCAL_PERIOD_ORDER` 拥有 CN/HK download canonical order，两者不重叠

### MiMo C07: `commit_cn_filing_source_document` 删除 `form_type` 参数后的表面矛盾（原 low）

- **裁决**: "接受文字澄清。删除的是 caller 暴露的冗余参数；内部 `_build_upsert_request(...)` 仍显式接收从 candidate identity 派生的局部值"
- **状态**: **已关闭** ✓
- **直接证据**:
  - Revised plan §5.3 line 245: "`commit_cn_filing_source_document(...)` 与 `_build_base_meta(...)` 只删除**对各自 caller 暴露**的冗余 `form_type` 参数；`commit_cn_filing_source_document(...)` 内部唯一取局部值 `identity_period = candidate.period_projection.identity_period`，将 candidate 交给 `_build_base_meta(...)` 派生 meta 的 `form_type`/`fiscal_period`/`report_kind`，并继续显式调用 `_build_upsert_request(..., form_type=identity_period)`。`_build_upsert_request(...)` 的内部参数不删除，因为它机械构造 storage request；该值来自 candidate identity，不是 caller 可提供的第二真源"
  - 代码基线确认: `cn_download_source_upsert.py` line 131 当前 `commit_cn_filing_source_document` 有 caller 传入的 `form_type` 参数；line 185 传给 `_build_base_meta`；line 199 传给 `_build_upsert_request`。修订后 design: 外层删除 `form_type`，内层从 `candidate.period_projection.identity_period` 派生，传给 `_build_upsert_request` 的仍是明确派生值
  - 不存在"保留外部双真源"问题 — `_build_upsert_request` 收到的 `form_type` 来自 candidate identity，不是 caller 可独立提供的第二值

---

## 无新增 Finding — 全量 adversarial sweep

对 fix loop 2 修订做全量 adversarial sweep，未发现新的 material finding：

### Slice allowed files 完整性

- Slice 1: 2 production + 3 test — 仅 mode invariant/help，无变化 ✓
- Slice 2: 8 production（新增 `cn_report_selection.py`）+ 5 test — 覆盖全部 `target_periods → discovery_periods` rename consumers ✓
- Slice 3: 13 production + 10 test — 覆盖全部 coverage/projection/classification/JSON consumers ✓
- 共有文件边界表: 5 行，每行 Slice 2/3 修改范围互不重叠 ✓

### `cn_report_selection.py` Slice 2 边界安全性

- Slice 2 只允许机械 rename，exact change 2 明确 "不提前修改 classifier 或 candidate projection"
- Slice 2 non-goals（line 339）: "本 slice 不改变 HKEX category code或 candidate period schema"
- 两个 stop condition 共同约束：若实现越界立即停止 ✓

### Goal drift / scope creep

- Fix loop 2 未引入新类型、新函数、新 policy、新验收标准
- 4 项修复全部属于 slice 边界/措辞精度调整，无语义变化
- Goal confirmation 的 success signals、non-goals 均未改变 ✓

### Test union 覆盖

- §7.1 focused union（lines 423-437）现包含 13 个 test 文件，覆盖 §4.3 全部 constructors/consumers
- `tests/fins/test_cn_report_selection.py` 同时在 Slice 2 和 Slice 3 的 allowed test files 中 — 合理，因为该文件需在 Slice 2 rename 后通过、在 Slice 3 classification 重写后再次通过 ✓

### 前序 findings 未退化

- 原 Finding 1-6（第一轮 161417）: 全部保持关闭 ✓
- 原 DS N1/N2（第二轮 164911）: 本次关闭 ✓
- 原 MiMo C01/C07（第二轮 164812）: 本次关闭 ✓

---

## Assumptions Tested

| 假设 | 验证结果 |
|---|---|
| `cn_report_selection.py` 加入 Slice 2 后 Slice 2 可独立通过 pyright | **成立**。所有 `query.target_periods` consumers 均在 Slice 2 allowed files 中 |
| `CN_FISCAL_PERIOD_ORDER` 在 Slice 2 定义、Slice 3 复用 | **成立**。§5.2 + §6 共有文件表 + Slice 2 item 1 三处一致 |
| `FISCAL_PERIODS` 在当前基线存在且可用 | **成立**。`filing_semantics.py:79` 直接代码证据 |
| `form_type` 参数边界澄清后无歧义 | **成立**。caller-facing 删除 vs internal builder 保留的区分由代码基线支持 |
| Fix loop 2 未引入新 scope/新类型/新验收标准 | **成立**。仅 slice 边界和措辞修订 |

---

## Open Questions

无。前序所有 open questions 已在第一轮裁决中关闭，本轮未产生新问题。

---

## Residual Risks

| 风险 | 跟踪 |
|---|---|
| implementation 开始前代码基线漂移（新增 query consumer） | plan §6 前言要求 Slice 2 开始前重跑相同 `rg`，发现漂移即停止 |
| 第二轮 MiMo/DS 独立 re-review 结果未知 | 总控下一 gate；本 artifact 为 DS 侧结论 |
| `ingestion_runtime.py` 整文件 80% coverage 实施成本 | plan §7.2 拒绝豁免，implementation 承担 |

---

## Plan Review Conclusion: **pass**

4 项第一轮 re-review finding（DS N1 blocking, DS N2 low, MiMo C01 low, MiMo C07 low）全部已在 fix loop 2 修订中关闭，各有直接 plan 文本与代码基线证据。全量 adversarial sweep 未发现新增 blocking gap。Plan 达到 code-generation-ready。

前序全部 findings 关闭链：

```text
Round 1 (161417): F1-F6 (1H/3M/2L) → fix loop 1 → 全部关闭
Round 2 (164812/164911): N1(MiMo C01/C07) + N2(DS N1/N2) → fix loop 2 → 全部关闭
Round 3 (this): 核验 4 项关闭 + adversarial sweep → 无新 finding → pass
```

---

## Review Metadata

- **Review 时间**: 2026-08-10T16:55:13+08:00
- **Review 类型**: adversarial plan second re-review
- **Base commit**: `3811f95c82fbf0daf15740a5d217eed4d8b49df5`
- **Reviewer**: Claude (planreview skill, 独立于 MiMo)
- **Previous DS review**: `docs/reviews/plan-review-20260810-164911.md`
- **Previous MiMo review**: `docs/reviews/plan-review-20260810-164812.md`
- **Output**: `docs/reviews/plan-review-20260810-165513.md`
