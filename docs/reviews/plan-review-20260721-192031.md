# Plan Review — WU-HOST-SESSION-EVENT-DELIVERY-01（AgentDS 第二轮独立复审）

## 元数据

- **review target**: `docs/host/wu-host-session-event-delivery-01-plan.md`（Codex 修复后版本）
- **review 类型**: adversarial plan review（planreview skill），只审阅不修改
- **reviewer**: AgentDS（独立复审，未读取 AgentMiMo 本轮复审产物）
- **timestamp**: 20260721-192031
- **基线 commit**: `2c02079a82c049b49914be412178006ccd354049`（main HEAD）
- **参考 artifact**:
  - 上一轮 DS review: `docs/reviews/plan-review-20260721-185406.md`（conclusion=`pass-with-risks`）
  - Controller adjudication: `docs/reviews/wu-host-session-event-delivery-01-plan-review-controller-adjudication.md`
  - Codex fix record: `docs/reviews/wu-host-session-event-delivery-01-plan-fix-codex.md`
  - 设计真源: `docs/host/design.md`
  - goal confirmation: `docs/reviews/wu-host-session-event-delivery-01-goal-confirmation.md`

## Review Scope

本轮复审只针对上一轮 review + Controller adjudication + Codex fix 形成的闭合链路，逐项核验：

1. 8 个 accepted findings（MIMO-001、MIMO-002+DS-F2、MIMO-003 narrowed、DS-F1、DS-F4、DS-F5、DS-F6、DS-O1）是否在 plan 中闭环；
2. 2 个 rejected findings（DS-F3 timeout-abandon、DS-O2 wider scan）是否未被错误写入；
3. 用户裁决（item-only、512/4、不设 byte bound）是否保持不变。

若发现新 material finding，按 planreview skill 格式报告。

## Accepted Finding 逐项核验

### MIMO-001 — S1 对 `entrypoint_runtime.py` 的边界冻结

- **要求**: S1 只做 async factory/public iterator 机械传播；旧 relay symbols 冻结到 S4；S1 tests 不固化 relay 行为。
- **plan 当前位置**: §7 S1 Exact changes #9
- **直接证据**: plan 逐名冻结 `_ENTRYPOINT_LIVE_EVENT_BUFFER_CAPACITY`、`_WatcherFailure`、`_WatcherQueueItem`、`_WatchAndWaitRuntime.queue`、`_WatchAndWaitRuntime.drain_task`、`_drain_host_events`、`_close_watch_and_wait_runtime`、`_drain_available_watcher_items`、`_drain_available_startup_terminal_items`，并明确 "S1测试只断言async/public iterator传播与类型正确，不新增或固化relay行为断言"。S1 Non-goals 写清 "本slice不删除、不简化、不'顺手迁移'Service relay；S4是上述冻结symbols及queue/drain consumer的唯一语义修改slice"。
- **代码事实核对**: `dayu/service/entrypoint_runtime.py` 当前存在 `_ENTRYPOINT_LIVE_EVENT_BUFFER_CAPACITY`（2 处）、`_WatchAndWaitRuntime`（4 处），与 plan 冻结清单一致。
- **判定**: **CLOSED**。冻结清单逐名闭合，S1/S4 边界无歧义。

### MIMO-002 + DS-F2 — scheduler/coordinator 构造环与生命周期 barrier

- **要求**: factory/bind 先于 critical tasks 启动；bind failure 时 tasks 从未启动；Host close 先 stop/await 全部 scheduler-owned producers，再 close coordinator/port；closing race notice 固定低基数 diagnostic；禁止临时 port/runtime rebind。
- **plan 当前位置**: §4.6 lifecycle、§7 S3 Exact changes #10-11、§7 S3 Tests、§10.1
- **直接证据**:
  - §4.6: "HostDispatchScheduler.open 先构造不可运行、不会启动 critical task、不会暴露给任何 caller 的 scheduler；随后 construction-only typed factory取得其 ordinary promotion capability并创建 opener-local coordinator，non-optional terminal port 只允许在该私有构造阶段完成一次 bind。bind 成功后才允许启动 heartbeat、active-cancel watchdog、dispatch/promotion drain、worker/ingestor等 scheduler-owned producer；禁止临时 no-op port、先运行后替换、public setter或任何 runtime rebind。"
  - §4.6: "factory/coordinator allocation/bind任一步失败时，heartbeat/watchdog及其它 scheduler critical task必须从未启动；按已分配资源的逆序关闭 coordinator（若已创建）、未启动 scheduler 的 lane/Host-instance resource，再由外层 opener关闭其scheduler store，并且不返回 scheduler或Host handle。"
  - §4.6: "Host close先关闭 public new-work gate，停止并 await wait-poller / durable actor intake及所有 scheduler-owned terminal producers；HostDispatchScheduler.close()必须取消并 await heartbeat、active-cancel watchdog、dispatch/promotion drain、active worker task/handle与scheduler-owned ingestor，证明之后不会产生新 notice，才允许 close coordinator/terminal port，随后再关闭 Session Event Delivery owner及其 iterators。不得先 close coordinator再让 producer补交 notice。"
  - §4.6: "coordinator close是owner-loop barrier：close开始前已经进入 owner loop的 notice必须在正常 watermark-before-promotion顺序下幂等完成；若 notice与closing竞态且已经不能完成本地动作，则只发固定低基数 operator diagnostic。两种路径都不得创建新subscription、不得把已commit terminal伪装成rollback，也不得从durable state重算notice。"
  - §7 S3 Tests 覆盖 "scheduler factory/bind failure barrier断言heartbeat/watchdog/dispatch/promotion/worker/ingestor task从未启动"、"Host close ordering recorder断言...全部stop并await，随后才调用coordinator close与delivery owner close"、"closing race固定diagnostic且不产生subscription/promotion/rollback"。
- **判定**: **CLOSED**。构造顺序、失败路径、Host close stop-before-port-close、closing race diagnostic 全部冻结。

### MIMO-003（Controller 收窄版本）— UI callback 执行域隔离

- **要求**: 使用 Service-defined typed callback execution port + CLI-owned private single-thread executor，不与 event loop 默认 executor 或 Host/runtime blocking work 共享；同一 consumer 至多一个 submitted/in-flight callback；不建 Host event-copy queue、第二 observation channel、executor registry 或跨 Session quota。
- **plan 当前位置**: §3.1.5、§4.1 owner map、§4.7、§7 S4 Exact changes #7-10、§10.1
- **直接证据**:
  - §4.1 owner map: "callback / renderer专用执行域与 caller-finally close | Service 定义 typed callback execution port；dayu.cli.runtime_display 实现并拥有显式单线程 executor；dayu.cli.session_execution 拥有其 lifecycle | 不使用事件循环默认 executor"
  - §4.7: "Service定义只含 activity/thinking 两个精确 typed invocation 的 callback execution port，CLI RuntimeDisplayController 提供 concrete owner：每个 prompt/interactive display lifecycle 显式创建一个 ThreadPoolExecutor(max_workers=1)，通过 loop.run_in_executor(explicit_executor, ...) 执行 callback、toggle、finish、cancel/local-exit render与最终renderer close。该 executor 不设置为event-loop default，不被 Host、dayu.runtime、其它 Session或其它 watcher复用；不新增跨 Session registry/quota。"
  - §4.7: "execution domain在提交前持有 async serial gate，只有取得gate后才向其executor提交工作，因此同一consumer任一时刻至多一个 submitted/in-flight callback"
  - §7 S4.7: "Service定义EntrypointCallbackExecutionPort，只含精确typed的invoke_activity(callback, activity)与invoke_thinking(callback, thinking) async方法"
  - §10.1: "UI-owned explicit single-thread executor + submit-before serial gate，一次只允许一个awaited callback/display job；与Host/runtime default executor隔离，不建event-copy relay。"
- **判定**: **CLOSED**。显式 executor、serial gate、typed port、不共享默认 executor 全部冻结。

### DS-F1 — periodic reconciliation 驱动语义

- **要求**: reconcile 由 sole iterator `__anext__()` 的 readiness wait timeout 分支驱动；每次 timeout 最多一页后重新等待；无 per-watcher timer/background task；interval 为 Host-internal 有界常量，不进入 public policy。
- **plan 当前位置**: §4.5.6、§7 S2 Exact changes #7、§7 S2 Tests
- **直接证据**:
  - §4.5.6: "mailbox空时，唯一 iterator 的当前 __anext__() 在同一个调用栈内等待 level readiness，并以 Host-internal bounded reconcile interval 作为这次 readiness wait 的 timeout；只有该 timeout 分支触发一次、且恰好一次最多一页的 bounded durable reconciliation，处理完该页或确认空页后重新进入 readiness wait。不得创建 per-watcher timer/background task，不得在一次 timeout 中循环追多页，也不得复用 wait-resolution poll/idle cadence、Service poll interval或 public delivery policy；持续调用中的后续 timeout 才驱动下一页"
  - §7 S2.7: 相同语义，并明确 "interval是Host-internal有界常量，不进入public policy，不复用wait-resolution cadence、Service poll interval或其它runtime timer"
  - §7 S2 Tests: "mailbox空、无local notice的独立用例使用同一个可注入Host-internal clock/readiness barrier：A提交terminal后，C每次只推进一个interval并断言每轮最多读取一页，最终交付跨opener terminal；另在interval尚未推进时发起Host C close，断言close立即打断wait并按正常EOF收口。"
- **判定**: **CLOSED**。`__anext__()` 内 timeout 分支驱动、单页、无后台 task、Host-internal 常量、可控 clock test 全部明确。

### DS-F4 — 双 opener fixture 范围收窄

- **要求**: 只在 `tests/host/test_watch_session_events.py` 内直接构造；两个独立 `open_host` contexts 共享 Host DB/lane DB paths；写清 no-local-notice barrier、逐页 clock 推进与关闭顺序；若必须改其它 fixture，触发 stop condition 回 plan gate。
- **plan 当前位置**: §7 S2 Allowed tests、§7 S2 Tests #4、§7 S2 Stop conditions
- **直接证据**:
  - §7 S2 Allowed tests: "双openerfixture只能直接写在tests/host/test_watch_session_events.py；本slice未授权其它fixture或support file。若实现证据证明必须修改其它fixture或support file，立即触发stop condition并回到plan gate明确文件与owner，不得在S2临时扩scope。"
  - §7 S2 Tests #4: "在 tests/host/test_watch_session_events.py 内直接构造最小concurrent dual-opener fixture：两个独立open_host context使用各自的Host handle、scheduler、durable actor/store、Session Event Delivery owner、worker factory与lifecycle，但OpenHostOptions.db_path和lane_db_path显式指向同一Host DB/lane DB；不共享in-memory hub、coordinator、worker handle或local notice port。"
  - §7 S2 Tests #4: "cleanup固定为：先cancel/await未完成的anext()并aclose() C watcher，再退出opener C context，最后退出opener A context；各自worker/task/resource只能由所属context关闭。"
  - §7 S2 Stop conditions: "双opener测试若不能完全在tests/host/test_watch_session_events.py以内用两个独立context和共享DB/lane DB options完成，停止S2并回到plan gate"
- **判定**: **CLOSED**。单文件约束、fixture 设计、关闭顺序、stop condition 全部明确。

### DS-F5 — CLI renderer close 同步点

- **要求**: caller-finally close flow 固定为 stop new display work → await current job → serial renderer close → await → shutdown executor → release caller-local resources；有 deterministic ordering test。
- **plan 当前位置**: §7 S4 Exact changes #9、§7 S4 Failure/release paths
- **直接证据**:
  - §7 S4.9: "dayu.cli.session_execution是该controller/executor与renderer的唯一caller lifecycle owner。唯一finally close flow固定为：在event loop标记controller closing并拒绝新display work -> 等待已取得serial gate或已submitted的当前callback/renderer job完成 -> 在同一explicit executor串行提交renderer close并await -> job返回event loop后shutdown该executor -> 再释放monitor、runtime-line guard、renderer handle等caller-local resource。"
  - §7 S4.9: "取消、local-exit、terminal和异常路径不得直接调用底层renderer close()，只可提交非terminal finish/cancel render work，最终都进入同一aclose()；renderer close恰好一次。"
  - §7 S4 Failure/release paths: "caller-finally ordering test记录callback_started -> close_requested -> callback_released -> callback_finished -> renderer_close_started -> renderer_close_finished -> executor_shutdown -> caller_local_release；close不能越过current callback。"
- **判定**: **CLOSED**。finally 顺序、renderer close 串行化、ordering test 全部冻结。

### DS-F6 — no-backpressure 边界澄清

- **要求**: 不反压承诺只属于 Host publisher/Agent/Engine/promotion/其它 watcher；Service callback 耗时只减速当前 consumer 并通过 item-bound overflow 收口；不增加 relay/drop queue/byte quota/Host-global/cross-Session quota。
- **plan 当前位置**: §3.3.5、§4.7、§7 S4 Non-goals
- **直接证据**:
  - §3.3.5: "Service sole consumer 必须等待当前 callback 返回后才调用下一次 anext()；因此慢 callback 可以只减速当前 Service consumer，并可能使当前 subscription 按 item-bound policy overflow。不得为了声称端到端「零背压」增加 event-copy relay、drop queue、第二 observation channel、byte quota、Host-global quota或跨 Session quota。"
  - §4.7: "Callback仍是快速、同步、非阻塞的 typed callable；Service sole consumer一次只处理一个callback，不复制Host event。"
  - §7 S4 Non-goals: "不为慢callback增加relay/drop queue、第二observation channel、byte quota、Host-global/cross-Session quota或「限时后遗留thread」的伪cleanup"
- **判定**: **CLOSED**。边界精确：Host publish 不等待，slow callback 只减速当前 consumer 并通过 item-bound overflow 收口。

### DS-O1 — standalone command handle 与 manifest 关系

- **要求**: static manifest 覆盖 producer qualified callsites 与 composition path 无关；standalone 显式注入 private no-local-delivery port；runtime fake 证明 producer 仍调用 port。
- **plan 当前位置**: §5.3、§7 S3 Exact changes #9、§7 S3 Tests
- **直接证据**:
  - §5.3: "standalone create_host_command_handle 必须显式注入一个 Host-private、语义固定为「本composition没有local delivery owner」的 no-local-delivery terminal port；它只消费调用而不推进watermark/promotion，不public export、不兼容转发、不承担跨opener correctness，也不能作为完整 opener 构造期的临时 port。tests/host/test_command_handle.py 以recording runtime fake替换该private port，证明standalone路径的admission/waiting terminal producer在commit return后仍调用port并携带exact notice。"
  - §5.3: "manifest冻结的是上述 terminal writer / producer qualified callsite，不以 composition path 为筛选条件。同一个 admission / waiting producer无论由完整 open_host opener、wait-poller command handle还是 standalone create_host_command_handle 装配，都必须留在同一 static manifest"
  - §7 S3.9: "standalone create_host_command_handle没有Session Event Delivery runtime，必须显式注入private no-local-delivery port；该port是standalone composition的最终语义端点，不是临时占位"
- **判定**: **CLOSED**。manifest 不以 composition 分类、standalone 显式注入 private port、runtime fake 验证 dataflow 全部明确。

## Rejected Finding 边界核验

### DS-F3（timeout-abandon callback thread）— 确认未写入

- **Controller 拒绝理由**: Python thread job 无法安全取消；让仍运行 callback 晚于 iterator/controller close 会破坏串行 lifecycle 与 late-commit safety。
- **plan 当前状态**: §4.7 明确 "专用executor只提供执行域隔离，不承诺安全终止违反快速同步非阻塞contract的无限阻塞任意代码；不得通过限时等待后继续cleanup而把仍运行的thread遗留在renderer/iterator关闭之后。deterministic test必须使用可释放blocking barrier：阻塞期间证明Host publisher、Agent、Engine、terminal commit、promotion和第二watcher继续；释放后严格验证callback、consumer、iterator与renderer cleanup顺序。"
- **定向 scan**: plan 全文无 "timeout" + "callback" + "abandon" 组合、无 "限时等待后继续cleanup"（除上述否定句外）、无 thread-job await 超时收口。
- **判定**: **NOT PRESENT**。✓ 未写入。

### DS-O2（wider source scan）— 确认未写入

- **Controller 拒绝理由**: 当前精确扫描已覆盖本 WU 旧语义；扩大组合扫描会误报真实 availability owner。
- **plan 当前位置**: §8.4 source scan pattern 保持精确：`_TRANSIENT_WATCH_BUFFER_CAPACITY|_ENTRYPOINT_LIVE_EVENT_BUFFER_CAPACITY|session_live_stream|reason_code="slow_consumer"|transient_mailbox_max_bytes|delivery_size_bytes|cumulative_byte|byte_full|oversized.*mailbox`
- **判定**: **NOT PRESENT**。✓ 未扩展。

## 用户裁决保持核验

| 裁决 | plan 当前证据 | 判定 |
|---|---|---|
| item-only（不计 byte） | §2.2 "容量 contract 只有 retained item 与 per-Session subscription admission；不实现 logical-byte/resident-heap accounting"；§3.3 non-goals "不增加 transient_mailbox_max_bytes、delivery_size_bytes..."；§4.2.1 policy 只有两个 int 字段 | ✓ 保持 |
| 512/4 packaged defaults | §2.2 冻结 "512/4"；§5.1 packaged JSON "只写512/4"；§5.5 "packaged 512/4"；§8.1 S1 tests "packaged exact 512/4" | ✓ 保持 |
| 不设 byte bound | §2.2 "不承诺其上界"；§3.3 非目标逐项列出 byte 字段不增加；§8.4 scan 包含 byte 残留检测 | ✓ 保持 |

## 新 Material Finding 检查

本轮对 Codex 修复后 plan 做完整 adversarial pass，重点检查：

1. **修复是否引入新矛盾**: S1 对 `entrypoint_runtime.py` 的冻结清单（§7 S1.9）与 S4 删除清单（§7 S4.1）逐名一致，无遗漏或多删。✓
2. **S3 coordinator lifecycle 与 S1 Host close 顺序是否冲突**: §7 S1.7 声明 "S1测试只冻结'producer停止后才close delivery'与reservation release；S3在二者之间插入coordinator in-flight drain/port close，不得把delivery close重新提前"。S3 §4.6 将 coordinator close 插入在 producer stop 之后、delivery close 之前。无冲突。✓
3. **S2 reconcile 与 S1 不变量是否冲突**: §4.5.6 reconcile 在 `__anext__()` 调用栈内，不创建 per-watcher task。§4.3 不变量 "Host subscription不创建 per-watcher background task" 保持。✓
4. **MIMO-003 显式 executor 创建/销毁频率**: 每个 prompt/interactive display lifecycle 创建一个 executor。S4.9 在 finally 中 shutdown。对于 CLI 的 prompt 模式，每次命令一个 lifecycle；对于 interactive 模式，每个 session 一个 lifecycle。频率合理。✓
5. **S4 execution domain 的 serial gate 与 asyncio 语义**: §4.7 "execution domain在提交前持有 async serial gate，只有取得gate后才向其executor提交工作"。asyncio gate 在 event loop 内串行化提交，executor 内单线程串行化执行。双重串行化无冲突。✓

**未发现新 material finding。**

## Open Questions

无。上一轮 O1/O2 已由 Controller 裁决并落地；本轮复审未产生新的 open question。

## Residual Risks

上一轮 5 个 residual risks 的当前状态：

| ID | 风险 | 状态 |
|---|---|---|
| R1 | callback thread 阻塞导致 consumer cleanup 悬挂 | 降级。Controller 拒绝 timeout-abandon，plan 明确 physical guarantee boundary：不承诺终止无限阻塞代码；可释放 barrier 测试证明隔离。实施时应严格按 plan boundary 实现，不发明伪修复。 |
| R2 | 双 opener fixture 基础设施未确认 | **已关闭**。DS-F4 修复后 fixture 设计、文件约束、stop condition 全部明确。 |
| R3 | coordinator/scheduler 生命周期竞争 | **已关闭**。MIMO-002+DS-F2 修复后 lifecycle barrier 全部冻结。 |
| R4 | reconcile 实现机制可能引入后台 task | **已关闭**。DS-F1 修复后 `__anext__()` timeout 分支驱动、禁止后台 task 全部明确。 |
| R5 | CLI renderer close 与 caller finally 竞争 | **已关闭**。DS-F5 修复后 finally flow、ordering test 全部冻结。 |

当前唯一 residual risk 是 R1（callback 无限阻塞），且已由 plan 显式声明为 physical guarantee boundary，不是设计缺陷。实施 agent 必须严格遵守该 boundary，不得发明 timeout-abandon 伪修复。

## Architecture Boundary & Overcoupling Re-check

Codex 修复未改变任何 architecture boundary。快速复检：

- `dayu.runtime` 层中立 config 不 import Host ✓
- Engine contract 不变 ✓
- Host 不反向依赖 Service/UI ✓
- Service 不绕过 Host ✓
- 4 slices 沿 owner boundary 切分，无跨 owner 合并 ✓

## Final Plan Review Conclusion

**PASS**

理由：

1. **全部 8 个 accepted findings 已闭环**：MIMO-001、MIMO-002+DS-F2、MIMO-003 narrowed、DS-F1、DS-F4、DS-F5、DS-F6、DS-O1 均在 plan 中有精确、可实施、可验证的修改。每个 finding 的 required fix 与 plan 实际内容逐一匹配，无遗漏、无偏离。
2. **全部 2 个 rejected findings 未被写入**：DS-F3 timeout-abandon 未出现，plan 明确拒绝该方向并保留 physical guarantee boundary；DS-O2 wider scan 未扩展。
3. **3 项用户裁决保持不变**：item-only、512/4 packaged defaults、不设 byte bound 在 plan 各章节中一致贯彻。
4. **无新 material finding**：Codex 修复未引入矛盾、未产生新 gap、未改变 architecture boundary。
5. **Plan 已达到 code-generation-ready 标准**：4 slices 的 allowed modules、exact changes/dataflow、failure paths、tests、stop conditions 均精确可执行。

Plan 可进入 implementation gate。

## Artifact Path

`docs/reviews/plan-review-20260721-192031.md`
