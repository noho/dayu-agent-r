# Code Review — wu-cli-download-01 Slice 1 rereview

## Scope

- Mode: current changes (rereview after accepted review fix)
- Branch: `codex/download-oracle`
- Base: `85e71774075e07d5b8f24a35930664fe80ffe322`
- Output file: `docs/reviews/code-review-20260810-013615.md`
- Reviewer: AgentMiMo (原 reviewer)
- Included scope:
  - 原 review artifact: `docs/reviews/code-review-20260810-004038.md`
  - DS review artifact: `docs/reviews/code-review-20260810-004602.md`
  - Fix evidence: `docs/gateflow/wu-cli-download-01-slice1-review-fix-20260810-013105.md`
  - 全部 workspace diff（25 production/test files, +1368/−766）
  - 新增 untracked `dayu/fins/download_contract.py`
- Excluded scope: README closeout（按 plan 留待 closeout）；Slice 2/3/4 未实施代码
- Parallel review coverage: 无（单 reviewer 全覆盖）

## Findings

未发现实质性问题。

## 原 findings disposition 核验

### MiMo 001 — rejected-with-reason ✓ 确认

`cn_form_utils.resolve_target_periods` 从聚合错误变为 fail-fast。controller 裁决 rejected-with-reason，理由：新 domain owner `parse_fiscal_period_filter_value` 仍以中文 actionable 错误 fail closed，仅错误格式从聚合变为首错，不违反任何已承诺 contract。

**核验结论**: 确认无代码修改。当前 `cn_form_utils.py:198` 循环调用 `parse_fiscal_period_filter_value`，首个非法 token 立即抛 `ValueError`。行为正确，无需修复。

### MiMo 002 — rejected ✓ 确认

`_parse_form_types` 去重行为确认。controller 裁决 rejected，理由：预期与实际一致，影响为无。

**核验结论**: 确认无代码修改。`download_contract.py:252` 的 `if canonical not in seen` 逻辑正确。

### DS DL-R01 — accepted/fixed ✓ owner-level 修好

**原问题**: `start_is_explicit` typed field 未穿透 adapter 链，下游从字符串反推。

**修复核验**:

1. **`FinsDownloadDateRange.__post_init__` 不变量** (`download_contract.py:68-90`):
   - `start_is_explicit=True` → `start_bound` 非空 ✓
   - `end_is_explicit=True` → `end_bound` 非空 ✓
   - 双 bound 存在 → `start_bound <= end_bound` ✓
   - 非空 bound + `explicit=False` 合法（供未来默认窗口）✓
   - 非法组合统一抛 `FinsDownloadUsageError` ✓

2. **builder 委托** (`download_contract.py:170-182`):
   - builder 不再自行判断倒序范围，全部委托给 `__post_init__` ✓
   - 倒序输入仍保留中文错误 `--start 不能晚于 --end`（由 `__post_init:90` 产生）✓

3. **`start_is_explicit` required bool 全链穿透**:
   - `FinsSourceDownloadAdapterRequest.date_range: FinsDownloadDateRange` (`ingestion_runtime.py:405`) ✓
   - SEC adapter: `start_is_explicit=request.date_range.start_is_explicit` (`sec_pipeline.py:1831`) ✓
   - CN adapter: `start_is_explicit=request.date_range.start_is_explicit` (`cn_pipeline.py:1388`) ✓
   - SEC workflow: `start_is_explicit: bool` required keyword-only (`sec_download_workflow.py:279`) ✓
   - SC13 retry: `start_is_explicit: bool` required (`sec_sc13_filtering.py:647`) ✓
   - CN workflow: `start_is_explicit: bool` required (`cn_download_workflow.py:56`) ✓

4. **SEC/CN direct 反例**:
   - SEC SC13 retry: `if start_is_explicit: return list(filings)` (`sec_sc13_filtering.py:665`) — 直接消费 typed boolean ✓
   - CN default limits: `use_default_business_limits=not start_is_explicit` (`cn_download_workflow.py:255`) — 直接消费 typed boolean ✓

5. **无反推残留**:
   - `rg "start_date is not None|start_date is None" dayu/fins/pipelines/sec_download_workflow.py dayu/fins/pipelines/cn_download_workflow.py` — 无结果 ✓
   - `sec_6k_rules.py` 中的 `start_date is None` 是 form date range 校验，与 download explicitness 无关 ✓

6. **owner tests**:
   - `test_download_non_explicit_nonempty_start_keeps_default_business_limit` (`test_cn_pipeline.py:488`) — 证明 `start_is_explicit=False` + 非空 `start_date` 时 CN 仍启用 FY 五年限制 ✓
   - `test_download_date_range_rejects_invalid_contract_combinations` (`test_fins_direct.py:325`) — 参数化 3 种非法组合 ✓
   - `test_download_date_range_allows_non_explicit_nonempty_bounds` (`test_fins_direct.py:369`) — 非空 bound + `explicit=False` 合法 ✓

### DS DL-R02 — accepted/fixed ✓ owner-level 修好

**原问题**: `_cleanup_stale_filing_dirs` 函数体仍存在但无调用者。

**修复核验**:

1. **删除**: `_cleanup_stale_filing_dirs` 从 `sec_pipeline.py` 和 `sec_download_workflow.py` 完全删除 ✓
2. **`_CleanupStaleFilingDirs` Protocol**: 从 `sec_download_workflow.py` 删除 ✓
3. **`cleanup_stale_filing_dirs` 参数**: 从 `run_download_stream_impl` 签名删除 ✓
4. **rg 验证**: `rg -n "_cleanup_stale_filing_dirs" dayu tests` — 无结果 ✓
5. **替代测试**: `test_sec_ordinary_download_keeps_unselected_historical_document` (`test_sec_pipeline_download.py:2863`) — 证明普通 SEC 下载不删除非目标历史文档 ✓

### DS DL-R03 — deferred-with-owner ✓ 确认

process CLI `--overwrite → rebuild_processed` 命名不一致。controller 裁决 deferred-with-owner，由后续 process-command 专项 WU 处理。

**核验结论**: `rebuild_processed` 仅存在于 preprocess/upload 路径（`fins.py:722,747,772`、`ingestion_runtime.py:470-477`、`preprocess_tools.py`、`sec_download_source_upsert.py`、`cn_download_source_upsert.py`），download 路径已完全切换到 `rebuild_local_artifacts`。未改 preprocess/upload 字段或代码，符合 plan §5.3 约束。

## `tests/fins/test_cn_pipeline.py` 逐 hunk 核验

| 行范围 | 变更类型 | 内容 |
| ---: | --- | --- |
| 50-53 | download fake | `_PipelineDownloadFakeDiscoveryClient` 新增 `candidates` 字段 |
| 99-115 | download fake | `discover_reports` 改为 `self.candidates if self.candidates is not None else default_candidates` |
| 342-345 | download test | `test_download_runs_cn_workflow...` 新增 `start_is_explicit=True` |
| 408-411 | download test | `test_download_runs_hk_workflow...` 新增 `start_is_explicit=True` |
| 461-464 | download test | `test_download_stream_runs_cn_workflow...` 新增 `start_is_explicit=True` |
| 485-543 | 新增 download test | `test_download_non_explicit_nonempty_start_keeps_default_business_limit` |
| 546+ | upload tests | 零 diff ✓ |

**结论**: 所有 diff hunks 止于新 direct download test（第 543 行），首个 upload test 从第 546 行开始。upload tests、fixtures、helpers 零 diff。符合预期。

## 全部 current diff 新问题扫描

### `_filter_filings_to_windows` 新增函数 (`sec_download_workflow.py:762-794`)

该函数在 SC13 retry 前后各调用一次，确保扩展后的 filing 仍在 inclusive 窗口内。逻辑正确：
- 遍历 filings，检查 `form_windows.get(filing.form_type)` lower bound
- `parse_date` 由 SEC 日期 owner 提供
- `lower_bound <= filing_date <= end_date` inclusive 范围检查
- 不在 `form_windows` 中的 form 被跳过

无 correctness/stability/ownership 问题。

### CN adapter source 枚举校验改进 (`cn_pipeline.py:1368-1373`)

从 `request.source not in {self._source, "auto"}` 改为 `request.source is not expected_source`，使用 `FinsDownloadSource` 枚举比对替代字符串比对。`expected_source` 由 `self._market` 唯一确定。正确。

### CN `_build_result` 新增 `missing_periods` (`cn_download_workflow.py:754`)

`missing_periods` tuple 作为独立字段存入结果 dict，不再为每个 missing period 发射 `FILING_COMPLETED` 事件。这是 DL-F06 的实现，正确区分 document outcome 与 source-level 信息。

### 删除 `_build_missing_period_result` (`cn_download_workflow.py:554-570`)

不再需要为 missing period 构造 fake filing result。正确。

### SEC rebuild `reprocess_required` 行为变更

`test_sec_pipeline_rebuild_local_meta_manifest_without_redownload` 断言 `reprocess_required` 从 `True` 变为 `False`。这是因为 download rebuild（local-only）不再标记 processed 需要重处理，而是由 `sec_download_source_upsert._should_mark_processed_reprocess_required` 根据 source fingerprint 变化决定。download rebuild 不改变 source fingerprint，因此 `reprocess_required` 保持 `False`。行为正确。

## Open Questions

- 无

## Residual Risk

1. **Slice 2/3/4 未实施**: provider policy (UA gate)、public terminal summary、canonical cancellation、Docling process boundary、storage concurrency 与 integrity repair 仍为计划内后续工作。
2. **DL-R03 deferred**: process CLI `--overwrite → rebuild_processed` 命名不一致留待 process-command 专项 WU。
3. **README 未更新**: 按 plan 留到实现完成后的 closeout 统一更新。
4. **测试均使用 fake/mock**: 未覆盖真实 SEC EDGAR / CNINFO / HKEX 路径，这是 Slice 1 设计预期。

## Review Summary

**结论**: Slice 1 review fix 已 owner-level 修好 DS DL-R01 和 DL-R02。MiMo 001/002 的 controller disposition 确认正确。全部 current diff 未引入新 correctness/stability/ownership 问题。

**验证结果**:
- 受影响测试: 340 passed, 0 failed, 3 warnings (edgartools deprecation)
- pyright: 0 errors, 0 warnings, 0 informations
- `test_cn_pipeline.py` upload tests/fixtures/helpers: 零 diff ✓
- `_cleanup_stale_filing_dirs` rg: 无结果 ✓
- `start_date is not None` 反推 rg: download workflow 无结果 ✓
- stale helpers (`FinsDirectUsageError`、`_normalize_download_source`、`mark_downloaded_processed_rebuild_required`、`_build_missing_period_result`、`_TOKEN_TO_PERIOD`): download 路径无残留 ✓
- 新增 `hasattr`/`getattr`/兼容 shim/compatibility re-export: 无 ✓

**逐 DS finding 核验**:
- **DL-R01**: `FinsDownloadDateRange.__post_init__` 是日期组合不变量唯一 owner；`start_is_explicit` 以 required `bool` 从 adapter 穿透到 SEC SC13 retry 和 CN 默认限制；无反推、无默认值、无兼容 wrapper。owner tests 证明三种非法组合被拒绝、非空 bound + `explicit=False` 被接受、SEC/CN 下游直接消费 typed boolean。
- **DL-R02**: `_cleanup_stale_filing_dirs` 从 production 和 test 完全删除；普通 SEC 下载 non-deletion owner test 保留并验证通过。
- **DL-R03**: 既有 process CLI `--overwrite → rebuild_processed` 命名不一致未改，符合 plan deferred 声明。

**裁决**: pass，无 blocker。
