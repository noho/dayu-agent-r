# Code Review

## Scope

- Mode: current changes
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `main`
- Output file: `docs/reviews/code-review-20260619-121125.md`
- Included scope: `utils/smoke_host_public_conversation_memory_scenarios.py` (committed diff + working tree diff), `tests/runtime/test_smoke_host_public_conversation_memory_scenarios_assembly.py` (committed diff + working tree diff), `README.md` §5.3, `tests/README.md` runtime section, `docs/host/conversation-memory-smoke-compact-followup.md`
- Excluded scope: `dayu/host/compact_material.py`, `dayu/host/compaction.py`, `dayu/host/dispatch.py`, `dayu/host/memory.py`, `dayu/host/run_input.py`, `dayu/host/context_fallback.py` 等 production 文件（不在本次 smoke/log diagnostics work unit 范围内）
- Parallel review coverage: 无

## Findings

### 1-未修复-低-`TextIOWrapper` import 未使用

- **入口/函数**: 模块级 import
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:28`
- **输入场景**: 任何导入该模块的路径
- **实际分支**: `from io import TextIOWrapper` 被执行
- **预期行为**: 只 import 实际使用的符号
- **实际行为**: `TextIOWrapper` 在整个模块中没有被引用，仅出现在 import 行
- **直接证据**: `grep -n "TextIOWrapper" utils/smoke_host_public_conversation_memory_scenarios.py` 只命中第 28 行 import
- **影响**: 死代码；pyright 不报错因为它是合法 import，但违反 CLAUDE.md "禁止胶水 seam" 精神
- **建议改法和验证点**: 删除 `from io import TextIOWrapper`；运行 pyright 和 pytest 确认无回归
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-`_compact_pressure_reserve_tokens` 条件分支无效

- **入口/函数**: `_compact_pressure_reserve_tokens`
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:2856-2866`
- **输入场景**: 任何调用该函数的路径
- **实际分支**: `if context_window_size >= _COMPACT_PRESSURE_LARGE_WINDOW_TOKENS` 条件的两个分支都返回 `_COMPACT_PRESSURE_RESERVE_TOKENS`
- **预期行为**: 大窗口和小窗口应使用不同的 reserve token 策略（或条件应被移除）
- **实际行为**: 条件判断无效，两个分支返回值完全相同
- **直接证据**:
  ```python
  if context_window_size >= _COMPACT_PRESSURE_LARGE_WINDOW_TOKENS:
      return _COMPACT_PRESSURE_RESERVE_TOKENS
  return _COMPACT_PRESSURE_RESERVE_TOKENS
  ```
- **影响**: 逻辑上无害（行为等价于无条件返回），但条件判断造成虚假的分支覆盖假象，后续维护者可能误以为大小窗口有不同策略
- **建议改法和验证点**: 要么移除条件直接返回，要么为小窗口补充不同的 reserve token 值；需与 `_compact_pressure_padding` 的压力估算逻辑对齐
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-未修复-低-`CompactOperationAudit` docstring 缺失新增字段

- **入口/函数**: `CompactOperationAudit` dataclass
- **文件(行号)**: `utils/smoke_host_public_conversation_memory_scenarios.py:700-730`
- **输入场景**: 代码阅读 / 类型理解
- **实际分支**: N/A
- **预期行为**: CLAUDE.md 要求 "类与模块应提供中文概览 docstring"，dataclass 字段应有 docstring 条目
- **实际行为**: `request_event_sequence`、`run_id`、`compacted_event_sequences`、`failed_events` 四个字段在 docstring 中缺失条目
- **直接证据**: 对比 `CompactOperationAudit` 的 docstring（行 694-715）与实际字段定义（行 717-730），缺失四个字段的 `:param` 说明
- **影响**: 不影响运行时行为，但违反编码硬约束
- **建议改法和验证点**: 为缺失的四个字段补充 `:param` docstring 条目
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Confirmations

### CONTEXT_COMPACTION_FAILED 仍是 hard fail — 确认

`_assert_compact_acceptance`（行 3208-3246）中：
```python
failed_total = audit.failed_proactive + audit.failed_reactive
if failed_total > 0:
    raise RuntimeError("memory-compact observed CONTEXT_COMPACTION_FAILED")
```
即使 `fallback_action=dispatch` 让当前 Run 继续执行，`memory-compact` suite 仍会 hard fail。测试 `test_compact_acceptance_requires_event_log_audit_summary` 覆盖了 `failed_proactive=1` 的 hard fail 路径。`test_compact_audit_report_prints_operation_histograms_and_manifest_stage` 覆盖了 `CompactAuditReport.summary` 含 failed 时的 hard fail 路径。确认无回归。

### Compact audit report 数据流 — 正确

- `_compact_audit_report_from_rows` → `_compact_audit_summary_from_rows` + per-operation grouping → `CompactAuditReport`
- operation grouping 以 `event_id`（REQUESTED rows）或 `operation_id` payload 字段（accepted/rejected/failed rows）为分组键，缺失时使用 `<missing-operation-id>` 占位符
- trigger source 归因链：request payload `trigger_source` → request_trigger_sources map → rejected/failed payload `operation_id` 回指 → `<unknown>` fallback
- histogram 聚合正确：`failure_categories`（per-operation）、`diagnostic_histogram`（per-operation）、`rejected_failure_histogram`（全局）、`rejected_diagnostic_histogram`（全局）、`rejected_manifest_presence_histogram`（全局）

### Payload typing — 正确

- `_compact_payload_str` / `_compact_payload_int` / `_compact_payload_bool` / `_compact_payload_str_tuple` 均做 `isinstance` 类型守卫，类型不匹配时返回 `None` 或空元组
- `_compact_row_payload` 对非 object payload 抛出 `ValueError`，测试 `test_compact_audit_report_handles_empty_missing_and_malformed_payloads` 覆盖了该路径
- `_compact_payload_int` 正确排除 `bool`（`isinstance(value, int) and not isinstance(value, bool)`）

### Stdout line flush — 已修复

工作树 diff 在以下关键 stdout 行添加了 `flush=True`：
- `_print_compact_audit_summary`（SMOKE COMPACT_AUDIT）
- `_print_compact_operation`（SMOKE COMPACT_OPERATION）
- `_print_compact_histogram`（SMOKE COMPACT_REJECT_HISTOGRAM）
- `_print_compact_reject_detail`（SMOKE COMPACT_REJECT_DETAIL）
- `_assert_compact_acceptance`（SMOKE COMPACT_ACCEPTANCE）
- `run_smoke` 中的 SMOKE PASS、SMOKE WORKSPACE_KEPT、calls_by_key_summary

解决了 follow-up doc 中提到的 "stdout 粘连问题"。

### 测试覆盖 — 符合 work unit 边界

新增测试覆盖：
- `test_compact_acceptance_requires_event_log_audit_summary`: compact suite 基于 EventLog audit 摘要验收；CONTEXT_COMPACTION_FAILED hard fail；memory-core 不要求 compact
- `test_compact_audit_summary_maps_operation_id_to_request_trigger_source`: operation_id 归属到 request trigger source
- `test_compact_audit_report_prints_operation_histograms_and_manifest_stage`: operation timeline、histogram、manifest 缺失阶段、failure_stage、log_insufficient、CONTEXT_COMPACTION_FAILED hard fail
- `test_compact_audit_report_handles_empty_missing_and_malformed_payloads`: 空 rows、缺失 operation id（类型不匹配 `attempt_number="1"` / `repairable="true"`）、malformed payload

### README 边界 — 正确

- `README.md` §5.3 更新了 suite 名称（`core` → `memory-core`、`long/all` → `memory-compact`），描述了 `memory-compact` 的 EventLog 摘要审计能力，符合 README 更新约束 "只写用户完成操作所需的当前可用操作"
- `tests/README.md` runtime section 更新了 `test_smoke_host_public_conversation_memory_scenarios_assembly.py` 的覆盖描述，增加了 `memory-core` / `memory-compact` suite 解析、pressure mode、compact EventLog audit 摘要验收，符合 "只描述当前 tests/ 已存在的事实"

## Open Questions

- 无

## Residual Risk

- `_compact_pressure_reserve_tokens` 的条件分支无效问题当前不影响 smoke 行为，但如果未来小窗口确实需要不同策略，当前代码会造成误导
- `CompactOperationAudit` docstring 缺失字段不影响运行时，但可能在后续 review 中被标记
- `TextIOWrapper` import 是纯死代码，不影响功能但违反编码约束
- 工作树 diff 中 `_PAYLOAD_FIELD_FALLBACK_TIER`、`_COMPACT_MANIFEST_PRESENT`、`_COMPACT_MANIFEST_MISSING` 等新增常量和 `_compact_manifest_presence`、`_compact_failure_stage`、`_compact_log_insufficient` 等新函数已通过测试覆盖，但它们在 committed diff 中不存在，属于未提交的工作树改动
- production memory compact failure（conversation-memory-smoke-compact-followup.md 中的第 3 个问题）不在本 work unit 范围内，未修复
