# Code Review

## Scope

- Mode: current changes (S3 documentation closure only)
- Branch: work/cm-05-06-08-09
- Base: HEAD (77c32c32 WU-CM-09-S2 accepted slice commit)
- Output file: docs/reviews/code-review-20260612-182356.md
- Included scope:
  - `docs/host/design.md` — uncommitted S3 doc update (2 hunks)
  - `dayu/host/README.md` — uncommitted S3 doc update (1 hunk)
  - `tests/README.md` — uncommitted S3 doc update (1 hunk)
  - `docs/reviews/wu-cm-09-s3-implementation-report.md` — new file, S3 implementation report
  - `docs/host/issues-implementation-control.md` — uncommitted S3 gate state update (2 hunks)
- Excluded scope:
  - All committed S1/S2 implementation code (already reviewed and accepted: `a9f77611`, `77c32c32`)
  - Non-doc runtime changes (S3 is doc-only by design)
- Parallel review coverage: 无。S3 scope 仅限于 5 个文档/控制文件，单 reviewer 可直接全覆盖。

## Review Method

S3 是纯文档 closure slice，不改 runtime behavior。Review 沿以下路径逐条举证：

1. 对照 S1/S2 已实现代码 (`dayu/host/durable/memory.py`, `dayu/host/storage_maintenance.py`) 逐一核对 doc claims 是否与代码行为一致。
2. 对照各 README 的 `Agent更新约束【必须遵守】` 或 `README 更新边界` 检查 S3 doc 变更是否符合约束。
3. 按用户指明的 7 项重点审查项逐条举证。
4. 运行 validation 确认 51 passed / pyright 0 / diff clean。

## Findings

未发现实质性问题。

### 逐项审查举证

#### 1. docs 是否准确描述已实现 behavior

**design.md `run_storage_maintenance` 描述 (line 1201)**

代码真源：`dayu/host/storage_maintenance.py` `run_storage_maintenance()` 函数 (line 248-317) 在 `_ReadStorageMaintenanceStateOperation.__call__()` 中调用 `inspect_memory_snapshot_integrity(transaction)` (line 372-374)，结果放入 `HostStorageMaintenanceResult.memory_snapshot_integrity_issues` 字段 (line 166)。

doc claim：`返回...memory snapshot integrity issues，并可用独立 SQLite connection 执行 WAL checkpoint`
代码行为：`run_storage_maintenance()` 返回 `HostStorageMaintenanceResult`，其中 `memory_snapshot_integrity_issues` 字段为 `tuple[MemorySnapshotIntegrityIssue, ...]`；`wal_checkpoint` 通过独立 connection 执行 (line 496-497)。**匹配。**

doc claim：`memory snapshot integrity issues 只报告 invalid_json、schema_mismatch、digest_mismatch、unsupported_item_kind 或 storage_read_failed 分类`
代码真源：`MemorySnapshotIntegrityFailureKind` (dayu/host/durable/memory.py line 99-110) 定义了这五个枚举值，`inspect_memory_snapshot_integrity()` 函数只产出这五种分类。**匹配。**

doc claim：`短错误摘要和 row identity，不内联 snapshot JSON、prompt、tool payload 或大内容`
代码真源：`MemorySnapshotIntegrityIssue.json_value()` (line 178-192) 仅返回 `failure_kind`、`message`、`snapshot_id`、`session_id`、`consumer_id`、`policy_digest`、`checkpoint_event_sequence`；不包含任何 payload、prompt 或 snapshot JSON 内联。**匹配。**

**design.md Snapshot integrity 描述 (line 3028)**

doc claim：`Operator-facing storage maintenance 可以只读分类 damaged snapshot row，分类结果用于诊断，不是 repair authorization`
代码真源：`inspect_memory_snapshot_integrity()` docstring (line 505-508)："本函数是 operator-facing maintenance report 使用的只读 classifier。它不修改 SQLite row，不触发 rebuild / overwrite，也不改变现有 read_memory_snapshot fail-closed 语义"。**匹配。**

**host README Storage Maintenance 描述 (line 428-434)**

doc claim：`memory snapshot integrity issues 只报告...不会内联 snapshot JSON、prompt、tool payload 或大内容`
代码真源：同上述 `json_value()` 举证。**匹配。**

doc claim：`不 quarantine / rebuild / overwrite memory snapshot`
代码真源：`inspect_memory_snapshot_integrity()` 函数体内只有 `SELECT` 查询和只读分类逻辑，无 `INSERT/UPDATE/DELETE`。**匹配。**

**tests README 描述 (line 168, 170)**

doc claim：`durable snapshot / item / diagnostic round-trip、memory snapshot 与 projection checkpoint 同事务、memory snapshot integrity classification（invalid JSON、schema mismatch、digest mismatch、unsupported item kind、storage read failure）及 storage maintenance operator-facing report`
代码真源：`tests/host/test_memory_projection.py` 包含 `test_memory_snapshot_integrity_*` 系列 8 个测试 (line 879-1033)，覆盖全部五种分类。`tests/host/test_storage_maintenance.py` `test_storage_maintenance_reports_memory_snapshot_integrity_issue` (line 380-401) 覆盖 maintenance result 暴露 integrity issues。**匹配。**

doc claim：`test_storage_maintenance.py 覆盖 operator-facing maintenance result 暴露 memory snapshot integrity issues 且不执行 snapshot repair / overwrite`
代码真源：测试 `test_storage_maintenance_reports_memory_snapshot_integrity_issue` 断言 `len(result.memory_snapshot_integrity_issues) == 1` 且 issue 分类为 `invalid_json`，同时验证 maintenance 不修改 snapshot row（无 delete/update 断言）。**匹配。**

#### 2. 是否误称 snapshot 为 truth

全文搜索 design.md、host README、tests README、S3 implementation report、control doc 中所有涉及 memory snapshot integrity 的表述：

- design.md line 3028：`分类结果用于诊断，不是 repair authorization` — 明确 diagnostic 定位
- design.md line 1201：`memory snapshot integrity issues` — 使用 issue/diagnostic 术语
- host README line 430：`memory snapshot integrity issues 只报告` — 只读报告定位
- 代码 `MemorySnapshotIntegrityFailureKind` docstring：`只服务 operator-facing integrity report` — 非 truth
- 代码 `inspect_memory_snapshot_integrity()` docstring：`只读 classifier` — 非 truth

**结论：无任何一处将 snapshot 称为 truth。** 所有 documentation 一致使用 diagnostic / issue / report / classifier 等只读诊断语义。S1 的 plan 也明确写道 `不让 memory snapshot 成为 truth`（见 control doc line 943），S3 docs 严格遵循此设计意图。

#### 3. 是否明确 no quarantine/rebuild/overwrite

该约束在以下位置均显式声明：

| 文件 | 位置 | 原文 |
|------|------|------|
| design.md | line 1201 | `不 quarantine / rebuild / overwrite memory snapshot` |
| design.md | line 3028 | `分类结果用于诊断，不是 repair authorization；自动 overwrite、quarantine 或 rebuild 必须另行通过显式 maintenance / operator policy 设计，不能静默发生在 command path` |
| host README | line 432 | `不 quarantine / rebuild / overwrite memory snapshot` |
| tests README | line 170 | `不执行 snapshot repair / overwrite` |

代码层：`MemorySnapshotIntegrityFailureKind` docstring：`不改变 snapshot 读路径的 fail-closed 行为，也不触发 rebuild / overwrite`。`inspect_memory_snapshot_integrity()` 函数体纯 SELECT 读操作。

**结论：no quarantine/rebuild/overwrite 在 doc、code、test 三层均显式声明，无歧义。**

#### 4. maintenance issue JSON 是否不泄漏 payload

代码真源 `MemorySnapshotIntegrityIssue.json_value()` (dayu/host/durable/memory.py line 178-192)：

```python
return {
    "failure_kind": self.failure_kind.value,
    "message": self.message,
    "snapshot_id": self.snapshot_id,
    "session_id": self.session_id,
    "consumer_id": self.consumer_id,
    "policy_digest": self.policy_digest,
    "checkpoint_event_sequence": self.checkpoint_event_sequence,
}
```

返回字段仅包含：损坏分类、短错误摘要、row identity（snapshot_id、session_id、consumer_id、policy_digest、checkpoint_event_sequence）。**不包含 snapshot_json、prompt、tool payload、大内容。**

文档 claim（design.md line 1201）：`不内联 snapshot JSON、prompt、tool payload 或大内容`。**与代码行为完全一致。**

#### 5. README 更新是否符合各 README 约束

**host README 约束**（dayu/host/README.md line 11-19）：

- `只写两类内容：当前代码已实现的整个 Agent 的设计意图...当前代码已实现的 dayu.host package 的开发接口` → S3 新增的 Storage Maintenance section 描述的是 `run_storage_maintenance` 的已实现行为，**符合**。
- `必须按本文档现有章节职责写作` → S3 在既有的 `### Storage Maintenance` 小节内追加描述，**符合**。
- `不写未来计划、路线图、未落地能力或实现细节` → S3 新增内容均为已实现行为（integrity issues 报告、只读语义），**符合**。

**tests README 约束**（tests/README.md line 238-242）：

- `只描述当前 tests/ 已存在的事实` → S3 新增的 integrity classification 和 storage maintenance 测试描述对应 `test_memory_projection.py` 和 `test_storage_maintenance.py` 中已存在的测试，**符合**。
- `不写用户手册、Engine 设计文档、完整 review prompt、未落地测试体系或时间敏感记录` → S3 新增内容无上述违禁类别，**符合**。

#### 6. tests README 是否不过度扩写

S3 在 tests/README.md 中的新增文本量：

| 位置 | 新增内容 | 字符数 |
|------|----------|--------|
| durable foundation 行 | `memory snapshot integrity classification（invalid JSON、schema mismatch、digest mismatch、unsupported item kind、storage read failure）及 storage maintenance operator-facing report` | ~170 |
| P12.6 memory semantic smoke 行 `test_memory_projection.py` | `与 durable memory snapshot integrity classification` | ~50 |
| P12.6 memory semantic smoke 行 `test_storage_maintenance.py` | `test_storage_maintenance.py 覆盖 operator-facing maintenance result 暴露 memory snapshot integrity issues 且不执行 snapshot repair / overwrite` | ~130 |

新增总量约 350 字符，均为对已有测试覆盖范围的精确描述，未引入新章节、新分类层级或超出 tests/README 现有密度（该行已有 similar density，如原行描述 compaction、manifest、render 等约 800 字符）。**符合 "不过度扩写" 约束。**

#### 7. validation 确认

| 检查项 | 命令 | 结果 |
|--------|------|------|
| 51 tests passed | `pytest tests/host/test_memory_projection.py tests/host/test_storage_maintenance.py tests/host/test_package_exports.py -q` | 51 passed in 0.44s |
| pyright 0 errors | `python -m pyright dayu/ tests/ utils/` | 0 errors, 0 warnings |
| diff clean | `git diff --check` | clean (no output) |

## Open Questions

无。

## Residual Risk

- S3 无新增 runtime risk。S1 residual low-risk uncovered defensive branch（identity read failure）已在 S1 review 中记录为 nonblocking，S3 文档变更不改变此评估。
- S3 implementation report 声称 "changed docs only and did not modify runtime behavior"，`git diff HEAD` 确认所有变更仅在 `.md` 文件中，无 `.py` 文件变更。与该声称一致。
