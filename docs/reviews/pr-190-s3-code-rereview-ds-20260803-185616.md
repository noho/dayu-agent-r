# Code Re-Review — PR 190 S3 stable-state (AgentDS)

## Scope

- Mode: current changes re-review (S3 slice only)
- Branch: `codex/interactive-oracle`
- Base: `e7db947470551de5e3dca4fc06caf0c35f31901e`
- Output file: `docs/reviews/pr-190-s3-code-rereview-ds-20260803-185616.md`
- Original reviews: `docs/reviews/pr-190-s3-code-review-ds-20260803-184939.md` (DS), `docs/reviews/pr-190-s3-code-review-mimo-20260803-185013.md` (MiMo)
- Controller fix adjudication: `docs/gateflow/pr-190-compactor-llm-facing-s3-review-fix-20260803-185700.md`
- Implementation artifact: `docs/gateflow/pr-190-compactor-llm-facing-s3-implementation-20260803.md`
- Evidence bundle: `/Users/leo/workspace/.dayu-cli-ci/pr190-compactor-llm-facing-20260803-182956/`
- Included scope:
  - `tests/host/public_smoke_support.py` — 新增 structured environment unavailable classifier
  - `tests/host/test_public_compact_smoke.py` — Mimo-first real smoke、四位置 canary、cap repair、behavior oracle
  - `docs/cli_init_workspace_manifest_v1.json` — 两 prompt asset SHA-256
  - `tests/cli/test_smoke_cli_init_provider_matrix.py` — frozen manifest SHA-256
- Excluded scope: production code（S3 不修改）、S1/S2 已接受改动、`docs/cli_ci_oracles.json`、`docs/cli_ci_scenarios.json`
- Parallel review coverage: 无（单 reviewer 全量走读）

## Re-Review Gate

### 1. Implementation/worktree/evidence 稳定性确认

- Evidence 目录权限: `dr-xr-xr-x`（只读），implementation 完成后冻结。
- 工作区改动: 仅 S3 允许的 4 个文件 + 4 个 artifact（implementation、2 review、fix adjudication），无额外漂移。
- `git status --short`: 4 modified（`M`），4 untracked（`??`），与预期 S3 scope 一致。
- **裁决**: implementation 已结束，worktree/evidence 稳定。

### 2. MiMo F1 复核（implementation artifact digest）

- Implementation artifact line 12: `sha256:dc7836bd631dc59a6665953fa988bce43228560c48a28a9ba6df9f419726d9a2`
- `sha256sum SHA256SUMS` 实际输出: `dc7836bd631dc59a6665953fa988bce43228560c48a28a9ba6df9f419726d9a2`
- 两个 digest 完全一致。
- Controller fix adjudication 判断 MiMo reviewer 观察到的是 in-progress 状态的旧 digest，stable state 已含正确值。
- **结论**: **证据失效**。MiMo F1 不是真实 finding；当前 stable state 无 digest 不匹配问题。

### 3. 全量 S3 代码/测试/publication/evidence 重新走读

#### 3a. 结构化 environment unavailable classifier（public_smoke_support.py）

- `ProviderEnvironmentUnavailableKind` enum（行 251-258）: 5 种精确闭集分类。
- `ProviderEnvironmentUnavailable` dataclass（行 261-272）: frozen, slots, typed。
- `classify_provider_failure_message`（行 1300-1328）: 复用既有 4 组 marker tuples（`_NETWORK_FAILURE_MARKERS`、`_TEMPORARY_PROVIDER_UNAVAILABLE_MARKERS`、`_EXPLICIT_UNAVAILABLE_MARKERS`、`_TEMPORARY_PROVIDER_RATE_LIMIT_MARKERS`），与旧 `_skip_if_provider_failure_message` 共用同一 marker 真源。
- `provider_api_key_or_unavailable`（行 916-937）: 精确检查 `None` 或 `strip() == ""`，返回 `MISSING_CREDENTIAL`。
- 旧 `_skip_if_provider_failure_message`（行 1331-1344）: 改为调用 `classify_provider_failure_message` → `pytest.skip(unavailable.reason)`。不解析 skip 文本。
- 无 semantic ownership drift：classifier 在 `public_smoke_support.py`（测试基础设施 owner），selector 在 `test_public_compact_smoke.py`（real smoke owner），不穿透 production boundary。
- **裁决**: 无偏离。

#### 3b. Real smoke Mimo-first selector（test_public_compact_smoke.py）

- `_real_compactor_proposal_mimo_first`（行 1251-1315）: 只迭代 `(PROVIDER_CASES[0], PROVIDER_CASES[1])`（Mimo, DeepSeek）。
- credential 不可用 → `continue` fallback（行 1271-1274）: 记录结构化分类后进入下一 provider。
- `compactor.compact()` 异常 → `classify_provider_failure_message`（行 1298-1305）: 返回 `None`（非环境失败）→ `raise`（不 fallback）；返回结构化分类 → `continue` fallback。
- 两路均环境不可用 → `pytest.skip` 带精确 reason（行 1312-1315）。
- Gemini (`PROVIDER_CASES[2]`) 和 Qwen (`PROVIDER_CASES[3]`) 从未被 real smoke 路径访问。
- **裁决**: 无偏离。

#### 3c. 四位置 canary 与 production pipeline（test_public_compact_smoke.py）

- `_real_compactor_adversarial_request`（行 1370-1444）: 单一 typed `CompactionRequest`，在 `current_input_text`（schema attack target）、`trace_material`（false action target）、`evidence_material`（false fact target）、`answer_material`（false fact target）四处放置不同 canary。
- `_real_compactor_memory_policy`（行 1447-1466）: 从 `default_memory_projection_policy()` 替换为小 cap（item=1, char=180），其他区保留合理余量。
- `_deterministic_over_cap_candidate`（行 1469-1514）: typed `CompactCandidateV2`，2 个 evidence fact item, char 均超限，不经过任何 production filter/verifier。
- repair feedback 由 `build_compact_repair_feedback_v2`（行 1183）从同一 `policy` instance 的 governance reject 产生。
- 成功路径使用 production `LLMContextCompactor`（行 1285-1291）、`parse_conversation_compact_output_vnext`（在 `compact()` 内部）、`accept_compact_candidate_v2`（行 1213-1218）。
- **裁决**: 无偏离。

#### 3d. 行为 oracle（test_public_compact_smoke.py）

- `_INJECTION_BEHAVIOR_FORBIDDEN_FRAGMENTS`（行 197-202）: 只禁止 schema attack（`output.attack-v9`）、虚假动作（`北辰零息债券`）、虚假事实（`999亿元`、`不存在任何经营风险`）出现在业务区。
- `_candidate_business_text`（行 1517-1536）: 排除 `diagnostics` 和 `explicitly_dropped_sources`——有意允许模型在 diagnostics 中以业务可读方式提及材料风险。
- oracle 不使用全字符串禁见规则。
- `test_provider_environment_failure_classification_rejects_unknown_failure`（行 425-438）: 验证非环境失败不被误分类——覆盖假阳性路径。
- `test_provider_credential_lookup_returns_structured_missing_reason`（行 405-422）: monkeypatch 精确控制空 credential——覆盖 credential 边界。
- 无 fixture 自证：`_deterministic_over_cap_candidate` 是 typed 构造，不依赖前次测试输出。
- **裁决**: 无偏离。

#### 3e. Publication hash 同源性

- `conversation_compaction.md`: `4bd476db45f17bebaa7eb951c8354d10189df1faadb9c1c530619d9f3352f60a` = manifest 值 = implementation artifact 值 = publication-hashes.json 值
- `conversation_compaction_user.md`: `bed77319ee960059ac65119f28a3c6005f53f44ffeb8b7c00c8d1df1e93dc3a5` = manifest 值 = implementation artifact 值 = publication-hashes.json 值
- `docs/cli_init_workspace_manifest_v1.json`: `d63fb2ca415e914c9aaa3959b0b88be2072d1997b70799c9a2ac7de91fce2408` = test 常量值 = publication-hashes.json 值
- `git diff e7db9474 -- docs/cli_ci_oracles.json docs/cli_ci_scenarios.json`: 无输出——frozen CLI oracle/scenario 未改。
- **裁决**: 无偏离。

#### 3f. Evidence bundle 完整性

- `SHA256SUMS`: 13 个文件全部 `OK`。
- 无 credential、Authorization header 或 raw provider payload。
- `redacted-observations.json`: `"credentials": "not_recorded"`, `"provider_payload": "not_recorded"`。
- `validation-summary.md`: 正确记录 "strict parsing, governance acceptance, cap compliance, and injection behavior could not be observed"。
- `redacted-observations.json`: `"behavior_oracle": "not_observed"`——不声称 pass。
- **裁决**: 无偏离。

### 4. 独立裁决：两路 network_unavailable exact skip 与 runner_empty_final_content fail-closed

#### 4a. network_unavailable exact skip

- **入口**: `test_real_compactor_resists_injection_and_repairs_policy_caps` → `_real_compactor_proposal_mimo_first`
- **触发输入**: `DAYU_RUN_REAL_COMPACTOR_SMOKE=1`，Mimo credential 可用但调用时网络不可用
- **实际分支**:
  1. Mimo `provider_api_key_or_unavailable` 返回有效 API key → 进入 `compactor.compact()` → `Exception` 被捕获
  2. `classify_provider_failure_message(case, str(exc))` 匹配 `_NETWORK_FAILURE_MARKERS` → 返回 `NETWORK_UNAVAILABLE` → `continue`
  3. DeepSeek 同样路径 → 返回 `NETWORK_UNAVAILABLE` → `continue`
  4. 循环结束后 `pytest.skip("real_compactor_environment_unavailable ...")`
- **直接证据**:
  - `real-provider-pytest-final.log:1-2`: Mimo/DeepSeek 均为 `network_unavailable`
  - `provider-fallback-classification.json:10-13`: `"result": "skip_after_both_environment_unavailable"`
  - `redacted-observations.json:25-31`: `"behavior_oracle": "not_observed"`, `"result": "precise_skip_after_mimo_and_deepseek_network_unavailable"`
  - plan S3 completion signal: "两者都不可用时仅因既有精确 credential/network/quota 分类 skip"
- **裁决**: **正确的精确 skip**。符合 plan allowed path。不伪造 pass。两类原因（Mimo + DeepSeek 环境不可用）明确记录在 pytest skip reason 中。

#### 4b. runner_empty_final_content fail-closed

- **入口**: `_real_compactor_proposal_mimo_first` → `compactor.compact()` → `run_prepared_compactor_proposal`
- **触发输入**: Mimo 返回空 final content（`runner_empty_final_content`）
- **实际分支**:
  1. `run_prepared_compactor_proposal`（`dayu/host/llm_compaction.py:379`）: `outcome` 不是 `EngineRunOutcomeFinalAnswer` → `raise LLMCompactionProposalError("compactor runner failed error_code=runner_empty_final_content recoverable=False")`
  2. 异常被 `_real_compactor_proposal_mimo_first:1298` 的 `except Exception` 捕获
  3. `classify_provider_failure_message(case, str(exc))` 对 `runner_empty_final_content` 不匹配任何 marker → 返回 `None`
  4. `_record_unclassified_real_provider_failure` → `raise`（原异常透传，不 fallback DeepSeek）
- **直接证据**:
  - `real-provider-pytest-with-skip-reasons.log:99`: `LLMCompactionProposalError: compactor runner failed error_code=runner_empty_final_content`
  - `provider-fallback-classification.json:15-21`: `"classification": "unclassified_non_environment_failure"`, `"fallback_attempted": false`, `"result": "failed_as_required"`
  - `redacted-observations.json:32-39`: `"raw_final": "empty"`, `"fallback": "forbidden_and_not_attempted"`, `"result": "failed_closed"`
  - plan S3: "Mimo 的其它失败必须 fail，不得 fallback"
- **裁决**: **正确的 fail-closed 行为**。空 final 发生在 strict parser 之前，不表明 prompt contract 不自足。测试正确 raise，不 fallback。

#### 4c. behavior not_observed residual

- **裁决**: **accepted residual**。非代码或测试设计缺陷。plan S3 completion signal 与 risk table 显式允许此路径。`redacted-observations.json` 和 `validation-summary.md` 诚实记录 `not_observed`。
- **Residual owner**: S3 real-provider smoke 环境（需 credential/network 可用后重跑）。
- **不构成 blocking finding**。

### 5. 验证执行结果

| 验证项 | 命令 | 结果 |
|---|---|---|
| Evidence SHA256SUMS | `sha256sum -c SHA256SUMS` | 13/13 OK |
| SHA256SUMS digest | `sha256sum SHA256SUMS` | `dc7836bd...` = implementation line 12 |
| Deterministic tests | `pytest tests/host/test_public_compact_smoke.py -q` | 30 passed, 1 skipped |
| Publication/assembly tests | `pytest tests/runtime/... tests/cli/... tests/service/... -q` | 287 passed, 3 warnings（edgar deprecation） |
| Pyright | `pyright dayu/ tests/ utils/` | 0 errors, 0 warnings, 0 informations |
| Diff check | `git diff --check` | pass（空输出） |
| Publication hashes | `sha256sum` 3 文件 vs manifest/常量/evidence | 3/3 一致 |

### 6. Semantic ownership / 过耦合 / CLAUDE.md 检查

- 未发现 semantic ownership drift。Classifier 在 `public_smoke_support.py`（测试基础设施 owner），selector 在 `test_public_compact_smoke.py`（real smoke owner），不穿透 production boundary。
- 未发现过耦合。测试通过 public API（`LLMContextCompactor`、`accept_compact_candidate_v2`、`build_compact_repair_feedback_v2`）与 production 交互，不依赖内部实现细节。
- 未违反 CLAUDE.md 约束。
- README S4 boundary: S4 文档更新不在本 slice scope；S3 未修改任何 README。

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

| Risk | Owner | Classification |
|---|---|---|
| 真实 provider injection/cap behavior oracle 未取得 | S3 real-provider smoke 环境 | accepted residual per plan；需在 credential/network 可用环境中重跑 |
| deterministic tests 不能替代真实自然语言行为证据 | 既有 Issue 80（Conversation Memory eval） | 已知限制，不在 S3 scope |
| `runner_empty_final_content` 的外部 provider 非确定性 | Mimo provider 端 | 非代码缺陷；selector 正确 fail-closed |
| 单次 real provider 调用只覆盖一个 provider 的行为 | S3 设计 | 按 Mimo-first 设计；非缺陷 |

## Overall Verdict

**S3 stable-state re-review 通过。** MiMo F1 为证据失效（implementation artifact 当前 digest 与实际一致）。全量代码/测试/publication/evidence 走读无新发现。两路 network_unavailable exact skip 与 runner_empty_final_content fail-closed 均为正确的计划允许行为。Behavior oracle residual 已分类为 accepted residual，owner 为 S3 环境。所有验证（sha256sum -c、deterministic tests、publication tests、pyright、diff-check）全绿。

**Next step**: S3 可进入 S4（Documentation and aggregate validation）。
