# Code Review — Targeted Re-Review（最终复核）

## Scope

- Mode: current changes（targeted re-review，非全量）
- Branch: `codex/upload-filing-oracle`
- Base: `f6b2d04c`（HEAD 即 base；全部改动均在 workspace，`git log f6b2d04c..HEAD` 为空）
- Output file: `docs/reviews/code-review-20260818-025120.md`
- 对照工件: DS re-review `docs/reviews/code-review-20260818-024134.md`（2 low findings）
- 输入裁决: `docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-s2-rereview-adjudication-20260818.md`
- 本轮 fix 声明: `docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-s2-code-review-fix-20260818.md`（「Final re-review fix」段）
- Included scope（只复核以下两项，不扩大搜索）:
  - finding 001 清理范围：`tests/fins/test_fins_ingestion_tools.py:1452` 测试的 try/finally 边界及 `abandon_observation` 语义
  - finding 002 UF-024 锚点恢复：`tests/cli/test_fins_commands.py` usage matrix case id 与 seeded 判定
- Excluded scope:
  - US/HK scenario 的 cli-ci registry/oracle 注册——按用户明确边界 deferred，本轮不得要求修改 registry
  - production diff、README diff、其余测试内容——024134 已覆盖，且自 024134 以来未再变化（生产文件 diff 与 024134 复测状态逐字一致）
- Parallel review coverage: 无
- 证据复用: 按指令复用已完成的 822/pyright/coverage 证据，本复核未重跑任何测试；fix artifact「Final re-review fix」段声明的 focused `48 passed`、affected suite `822 passed`、全仓 pyright `0/0/0`、`git diff --check` 通过均产生于 DS 001/002 修复之后（即当前 workspace 状态），非过期证据。

## Targeted Re-Check

### Finding 001（try/finally 清理范围）— 已修复 ✓

- **核查点**: `activate_observation` 与 `executor.run_next()` 是否已移入 try 块，使激活/执行异常路径都经过 finally abandon。
- **当前代码**（`tests/fins/test_fins_ingestion_tools.py:1498-1510`）: `assert isinstance(outcome, ToolAwaitingOutcome)`（1498）→ `record = next(iter(...))`（1500）→ `try:`（1501）→ `runtime.activate_observation(record.handle)`（1502）→ `executor.run_next()`（1503）→ runner 断言（1504-1507）→ `finally:`（1508）→ `asyncio.run(runtime.abandon_observation(record.handle))`（1509）→ `assert runtime._observations == {}`（1510）。
- **结论**: activate/run_next 已位于 try 内，任意激活、执行或断言异常都会执行 finally abandon，清理断言保持。与裁决「把 activate_observation 与 executor.run_next() 移入 try，使任何激活/执行异常都经 finally abandon」逐项一致。
- **finally 语义核验**: `abandon_observation`（`ingestion_runtime.py:3938`）声明 `Raises: 无`——`pop(handle.handle_id, None)` 后 best-effort 请求取消；对已收口为 failed 或已不存在的 record 是安全无抛出的清理，不会掩盖 activate/run_next 的原异常。修复后的清理路径在异常路径上自洽。

### Finding 002（UF-024 锚点恢复）— 已修复 ✓（deferred 部分按用户边界维持）

- **核查点**: CN invalid-period case id 是否恢复为原值 `UF-024`，seeded 判定是否继续使用该 id；US/HK registry 注册是否按边界未被要求且未被改动。
- **当前代码**:
  - `tests/cli/test_fins_commands.py:1631`: case id 恢复为 `"UF-024"`，与 base `f6b2d04c` 原值一致（`git show f6b2d04c:tests/cli/test_fins_commands.py:1626` 同为 `"UF-024"`）。
  - `tests/cli/test_fins_commands.py:1800-1803`: `seed_workspace = case_id in {"UF-S2-seeded-invalid-report-date", "UF-024"}`，seeded 判定继续使用 `UF-024`，符合裁决要求。
  - US/HK 新 case（`:1626` `UF-FIX01-US-invalid-period-fresh`、`:1636` `UF-FIX01-HK-invalid-period-fresh`）为 fresh（非 seeded）场景，与既有 matrix 断言结构一致。
- **registry 锚点状态**: `docs/cli_ci_scenarios.json:127157-127158` 与 `docs/cli_ci_oracles.json:2794` 引用 `UF-024-cn-invalid-period`。`UF-024` 恢复后，tests 目录内 `UF-024` grep 命中恢复（`:1631`、`:1802`），中间态 rename（`UF-FIX01-CN-invalid-period-seeded`）造成的悬空引用已消除。registry 的 `-cn-invalid-period` 后缀命名在 base 即如此，属 cli-ci calibration 既有命名约定；当前状态与 base 锚点关系一致，未引入相对 base 的新漂移。
- **边界核验**: git status 确认本轮未修改 `docs/cli_ci_scenarios.json`、`docs/cli_ci_oracles.json` 或任何 frozen evidence 文件；US/HK 注册维持 deferred，符合用户明确边界。

### 新 material finding 扫描（限于 024134 → 当前的增量）

- fix artifact「Final re-review fix」声明且 git diff 证实：本轮只改 `tests/fins/test_fins_ingestion_tools.py`、`tests/cli/test_fins_commands.py` 与该 artifact；生产文件、README 的 diff 与 024134 复测状态完全一致，无新增生产改动。
- 两项修复自身的实现路径（finally 清理、seeded 判定）均已在上述核查中走读，无引入新缺陷。
- 新增 US/HK case 的 registry 未注册状态按用户边界 deferred，不构成 finding。

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- finding 001 的裁决未要求在测试中新增「让 `run_next` 抛异常以证明 abandon 仍执行」的 failure-injection case；现修复为结构性防护，且 `abandon_observation` 声明 `Raises: 无`，不存在异常掩盖风险，残留风险可接受。
- US/HK 新场景的 cli-ci registry/oracle 注册仍悬空——按用户明确边界 deferred，归属后续真实 CLI calibration workstream，本 work unit 不得处理。
- 本复核按指令复用 fix round 声明的 `48 passed` / `822 passed` / pyright `0/0/0` / diff-check 证据，未本机重跑；该证据产生于 DS 001/002 修复完成之后，覆盖当前 workspace 状态。
- 024134 中既有 deferred 项（material admission、durable summary raw fiscal period、入口级大小写覆盖形态）不在本轮授权内，维持原裁决。

## Verdict

**PASS** — finding 001 与 finding 002 均按裁决修复到位；US/HK registry 注册按用户边界正确维持 deferred；当前 diff 相对 024134 无新 material finding。
