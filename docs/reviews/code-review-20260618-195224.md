# Code Review

> **Path correction**：本文件是 `docs/reviews/deepreview-wu-cm-12-fix-r1-aggregate-20260618-194217.md` 的命名合规副本。原文件因未遵循 deepreview current changes mode 的 `code-review-YYYYMMDD-HHMMSS.md` 命名契约而补写至此。内容保持原 aggregate review 语义，仅更新 Output file 路径。

## Scope

- Mode: current changes
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `b4899a7a`
- Output file: `docs/reviews/code-review-20260618-195224.md`
- Included scope: WU-CM-12-FIX-R1 aggregate changes from commit `d904445e` through `3ae3c605` (HEAD)，覆盖 Slice 1、Slice 2、Slice 3 全部 production 代码、测试代码与 review artifacts
- Excluded scope: 更早的 WU-CM-12 实现与 WU-CM-13 定义，仅作为上下文参考
- Parallel review coverage: 无（单 reviewer aggregate pass）
- Reviewer: AgentMiMo
- Design truth: `docs/host/design.md` §25 (Context Governance)、§24.6 (ordinary path 不做字段级 silent truncation)；`docs/engine/design.md` §15
- Control doc: `docs/host/issues-implementation-control.md`
- Plan: `docs/host/host-issues/wu-cm-12-fix-r1-material-guard-plan.md`

## Findings

### 01-未修复-低-test_compaction_operation.py 函数名残留 "chunks" 措辞

- **入口/函数**: `test_evidence_chunks_share_same_durable_query_text`
- **文件(行号)**: `tests/host/test_compaction_operation.py:1632`
- **输入场景**: 读者按函数名推断测试意图
- **实际分支**: 函数 docstring 已更新为"长 evidence 默认不 chunk"语义，但函数名仍含 `chunks`
- **预期行为**: 函数名应反映当前测试意图（默认不 chunk）
- **实际行为**: 函数名 `test_evidence_chunks_share_same_durable_query_text` 暗示 evidence 被 chunk，但实际断言验证 `labels == ("E1",)` 且 `"." not in label`
- **直接证据**: `tests/host/test_compaction_operation.py:1632` 函数名 vs. `tests/host/test_compaction_operation.py:1705` `assert labels == ("E1",)`
- **影响**: 仅可读性。不影响 correctness——断言本身正确验证了不 chunk 行为
- **建议改法和验证点**: 重命名为 `test_evidence_block_shares_durable_query_text_without_chunking`；运行 `pytest tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text -q` 确认重命名后通过
- **修复风险（低）**: 仅重命名
- **严重程度（低）**: 可读性 debt，不影响行为

## Cross-Slice Aggregate Correctness 核对

### 1. EventLog-derived LLM-facing input material 不再被私有 guard 拒绝

**结论：PASS**

- `CurrentInputAnchorVNext.__post_init__` (compaction.py:662): `_require_bounded_non_empty_text(... max_chars=CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS)` 已改为 `_require_non_empty(self.text, ...)`。常量 `CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS = 1200` 已删除。
- `EvidenceReadableItemVNext.__post_init__` (compaction.py:1045): `_require_bounded_non_empty_text(... max_chars=EVIDENCE_BLOCK_CHUNK_VNEXT_TEXT_MAX_CHARS)` 已改为 `_require_non_empty(self.response_text, ...)`。常量 `EVIDENCE_BLOCK_CHUNK_VNEXT_TEXT_MAX_CHARS = 4096` 已删除。
- 测试覆盖: `test_current_input_anchor_keeps_whole_text_without_private_cap` (test_compact_material.py) 验证长 current input 通过 pack 和 vNext DTO 不截断；`test_conversation_compact_input_vnext_maps_evidence_to_evidence_material` 验证长 evidence `response_text` 完整进入 vNext；`test_llm_context_compactor_prepares_same_source_runner_input` (test_llm_compaction.py) 验证 `prepare_compactor_proposal_run_input` 不因长 material 抛错。

### 2. 默认 evidence chunking 已移除

**结论：PASS**

- `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS = 4096` 已从 `compact_material.py` 删除。
- `_EvidenceChunk` dataclass、`_evidence_chunks` 函数、`evidence_chunk_label` 函数已删除。
- `_evidence_blocks` (compact_material.py:1310) 不再调用 `_evidence_chunks`，直接输出单个 `CompactEvidenceBlock`。
- `_evidence_provenance` (compact_material.py:1396) 不再为 chunk 生成 `chunk_parent_label` / `chunk_ordinal`。
- `_pack_evidence_blocks` (compact_material.py:2817) 和 `_provenance_from_evidence_blocks` (compact_material.py:2880) 同步移除 chunk 调用。
- 测试覆盖: `test_single_large_evidence_block_stays_whole_with_same_provenance` 验证 5000 字符 evidence 保持单个 `E1`、全文 digest、payload/artifact/locator refs 完整、`chunk_parent_label is None`、`chunk_ordinal is None`。`test_evidence_chunks_share_same_durable_query_text` 断言 `labels == ("E1",)` 且无 `"."` 分隔符。

### 3. Accepted evidence provider 使用 EventLog-backed material view，无私有 row limit

**结论：PASS**

- `_ACCEPTED_TOOL_EVIDENCE_MATERIAL_LIMIT = 8` 已删除。
- `DurableAcceptedToolEvidenceMaterialProvider.__init__` 不再接受 `max_evidence_blocks` 参数。
- `_load_accepted_tool_evidence_materials_tx` (run_input.py:1365) 调用 `build_pre_dispatch_compact_material_view(transaction, self._event_log_store, run=current_facts.run, current_display_text=current_facts.user_prompt)`——符合 plan 要求的精确映射。
- 从返回 view 的 `material_blocks` 中只选取 `kind is CompactMaterialBlockKind.ACCEPTED_TOOL_EVIDENCE` 的 whole blocks，然后用 `represented_evidence_refs` 做第二道 whole-block 排除。
- `build_accepted_tool_evidence_material_blocks` 公开函数和 `_recent_accepted_tool_result_rows` 私有函数（含 SQL `LIMIT ?` 查询）已删除。
- `compaction_evidence` import 已从 `run_input.py` 移除。
- 测试覆盖: `test_run_input_builder_accepted_tool_evidence_material_has_no_private_row_cap` 写入 10 条 prior accepted evidence，通过真实 provider 读取，断言 10 条全部进入 material blocks。`test_run_input_builder_represented_refs_exclude_whole_accepted_evidence_blocks` 验证 memory/compact 已表示的 refs 被 whole-block 排除。`test_run_input_builder_accepted_tool_evidence_uses_raw_outcome_text` 验证 raw outcome 全文、readable tool name、readable query text、accepted evidence id、canonical refs、payload refs、tool call event ref。

### 4. Retained MAX_VNEXT parser safety guards 确认为 output-only

**结论：PASS**

- `compaction.py` 保留 12 个 `MAX_VNEXT_*` 常量。全部用于 `__post_init__` 中校验 LLM-generated output candidate 的字段（`claim_text`、`display_text`、`anchor_title`、`text`、`session_summary`、`answer_text`、`summary_text` 等）。
- 这些常量不用于拒绝 EventLog-derived input material，不是 current input / evidence material 的输入预算真源。
- 常量审计 (validation report §4.2) 正确分类为"保留为 parser safety guard / output accept barrier"。

### 5. 不引入 public API、durable schema、EventLog canonical semantics、Engine contract、WU-CM-13 或过度设计

**结论：PASS**

- 删除的公开符号：`EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS`、`evidence_chunk_label`、`build_accepted_tool_evidence_material_blocks`。均为 Host 内部符号，非 public API。
- 新增导入：`build_pre_dispatch_compact_material_view` 在 `run_input.py` 中导入自 `compact_material.py`——同一 Host 子系统内的内部依赖，不改变分层边界。
- 无 durable schema 变更。
- 无 EventLog canonical semantics 变更。
- 无 Engine contract 变更。
- 未实现 WU-CM-13 reactive compact recovery。
- 不新增 policy schema 字段、Host/Service seam 或第二套 material budget。

### 6. 常量审计完整性

**结论：PASS**

rg 搜索确认以下符号在 `dayu/` 和 `tests/` 中均无残留：

- `CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS`
- `EVIDENCE_BLOCK_CHUNK_VNEXT_TEXT_MAX_CHARS`
- `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS`
- `_ACCEPTED_TOOL_EVIDENCE_MATERIAL_LIMIT`
- `max_evidence_blocks`
- `build_accepted_tool_evidence_material_blocks`
- `_recent_accepted_tool_result_rows`

## Open Questions

- 无。

## Residual Risk

- `PromptLocalProvenanceEntry` (compaction.py:313-314) 仍保留 `chunk_parent_label: PromptLocalMaterialLabel | None = None` 和 `chunk_ordinal: int | None = None` 字段。默认 chunking 移除后这些字段恒为 `None`，属于 dead field。不影响 correctness，但增加后续维护者理解成本。建议作为后续 cleanup 跟踪。
- `build_pre_dispatch_compact_material_view` 读取 EventLog delta 全量，无分页。长 session 下 evidence scan 成本可能上升。Plan §8 已裁决为 non-blocking residual，deferred-with-owner 到未来 Host material source performance hardening WU。
- 本次 review 未独立运行 pytest/pyright/rg 验证命令。验证结果引用 Slice 3 validation report 的记录数据（240 passed、0 pyright errors、rg exit 1）。
