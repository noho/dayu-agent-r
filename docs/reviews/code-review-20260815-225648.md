# Code Review — UF-FIX07 Aggregate Re-Review（定点复核轮，独立执行）

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `64050349`
- HEAD: `6b80400139aba1ba43d950635a6e735467db4316`（未移动）
- 上轮 artifact（本次证据基准）：`docs/reviews/code-review-20260815-225405.md`（只读）
- Output file: `docs/reviews/code-review-20260815-225648.md`
- Included scope：仅定点复核 `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md` 对上轮 LOW finding 的修正
  （全仓 pre-existing 9 失败记录的措辞），以及所有 aggregate findings 的闭环状态
- Excluded scope: production/tests/README 代码复读（前两轮已闭环且本轮 diff 无变化）、13-file gate/pyright/coverage 复跑
  （用户明确不跑测试）、UF-PF07/PF12、registry/oracle/evidence、commit/PR；并发 artifact 只读未动
- Parallel review coverage: 无

## Findings

### 1-未修复-低-修正后的 8/1 证据未回引其来源 review artifact，gate 记录证据链不完整
- **入口/函数**: `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md` 元数据与 Validation 节
- **文件(行号)**: 第 10 行（`Aggregate re-review input` 仅列 `code-review-20260815-224341.md`）；第 114-115 行
  （“9 个 failure test IDs 的 base `64050349` 临时 worktree 定向复跑结果为 8 failed, 1 passed”）
- **输入场景**: 后续 gate/controller 沿证据链追溯“8/1”与“flaky”结论的来源时
- **实际分支**: 修正后的措辞本身准确，但未注明该证据出自
  `docs/reviews/code-review-20260815-225405.md`（base 9-ID 定向复跑与 stash 隔离实验均在该 artifact 记录）
- **预期行为**: gate 记录中新增的验证证据应可回溯到产生它的 review artifact；该 work unit 的合同要求
  evidence 链条完整（fix artifact 已为全仓数字回引了 `224341.md`，8/1 结论应同样回引）
- **实际行为**: 第 113 行回引了 `224341.md`（全仓 `7704 passed, 9 failed, 10 skipped, 6 deselected`），
  但第 114-115 行的 `8 failed, 1 passed`、双处通过与 flaky 分类未回引 `225405.md`；元数据第 10 行也未把
  `225405.md` 列入 aggregate re-review input
- **直接证据**: 逐行比对当前 artifact 第 10、112-117 行与 `docs/reviews/code-review-20260815-225405.md`
  的「Finding 1 直接证据」字段：后者记录了两轮独立复跑（stash 隔离 + base worktree 9-ID），前者采纳结论但无出处
- **影响**: 仅文档追溯性；结论本身正确，不影响 PASS 判定
- **建议改法和验证点**: 将第 10 行改为列出两份 re-review inputs（`224341.md`、`225405.md`），或在第 114-115 行
  句末注明“证据见 `code-review-20260815-225405.md`”；无需重跑任何验证
- **修复风险（低）**: 纯文档修订
- **严重程度（低）**:

## 定点复核逐项状态

| # | 复核项 | 状态 | 直接证据 |
| --- | --- | --- | --- |
| 1 | 全仓结果仍记录 `7704 passed / 9 failed` | PASS | 第 112-113 行：“`7704 passed, 9 failed, 10 skipped, 6 deselected`”，与本 reviewer 实际全仓 run 输出一致 |
| 2 | 9 个 failure IDs 在 base 定向复跑记为 `8 failed / 1 passed` | PASS | 第 114 行精确记录“8 failed, 1 passed”，与本 reviewer 在 base `64050349` worktree 的 9-ID 定向复跑结果一致 |
| 3 | open_host watchdog 在 base 与去 working diff HEAD 均通过，分类 unrelated flaky | PASS | 第 115 行明确“在 base 与移除当前 working diff 的 HEAD 均通过，属于全仓运行中的 flaky failure”；第 142 行 residual 行“1/9 为与当前 diff 无关的 host flaky failure”。与 225405 的两轮隔离证据一致（stash 隔离 pass + base 9-ID run pass，仅全仓 run 偶发失败） |
| 4 | 只有 8 个声明为 base 稳定复现 | PASS | 第 117 行“只有 8 个可声明为 base 稳定复现”；第 142 行“8/9 在 base `64050349` 稳定复现”。上一轮“同样复现”的过度声称已被移除 |
| 5 | 措辞与 `code-review-20260815-225405.md` 证据一致 | PASS | 逐项对照：8/1 拆分、flaky 双处通过、pre-existing unrelated 总分类、路径归属（host/service/cli 四文件）与“不属于 base..HEAD 或当前 aggregate fix diff”均与 225405 的 Finding 1 证据链一致；唯一缺口是未回引来源（见 Finding 1） |
| 6 | 所有 aggregate findings 已闭环 | PASS | Finding 状态表（第 21-27 行）：原 Finding 1 `已修复`、原 Finding 2 `已修复`、Re-review Finding 1（LOW，guard 测试）`已修复`；明确“没有 `部分修复`、`未修复` 或 `证据失效` finding”。上轮 225405 的 LOW（措辞精度）虽未单列行，其内容已在 Validation 节（112-117 行）与 Residual 表（142 行）完成修正 |
| 7 | 其余承诺未变 | PASS | Decision 仍 `AGGREGATE FIX COMPLETE / RE-REVIEW PENDING`；HEAD 仍 `6b80400139aba1ba43d950635a6e735467db4316`；工作树仅 3 个 tracked 修改 + fix artifact 与 review artifacts（untracked），本轮未改任何已有文件 |

## Open Questions

- 无。

## Residual Risk

- Finding 1（证据未回引）若 controller 选择 deferred，可在下一 gate 顺手补引，不阻塞。
- open_host watchdog 的 flaky 分类基于两次隔离通过（base、去工作 diff HEAD）与代码独立性，未做过“去工作 diff
  的全仓 run”交叉验证；该验证成本高且非本 work unit 义务，留作 host owner 观察项。
- 未重跑测试/UF-PF，未修改任何实现、测试、README 或已有 artifact，未提交/PR。

## 结论

**PASS**。fix artifact 对上轮 LOW 的修正准确：全仓数字保持 `7704/9`，9-ID base 定向复跑记为 `8 failed / 1 passed`，
open_host watchdog 明确为双处通过的 unrelated flaky，且只有 8 个声明为 base 稳定复现——全部与本 reviewer 在
`code-review-20260815-225405.md` 记录的证据一致。三个 aggregate findings 全部 `已修复` 闭环。
仅 1 项 LOW 追溯性建议：把 `225405.md` 加入 aggregate re-review input 或为 8/1 结论标注出处。
