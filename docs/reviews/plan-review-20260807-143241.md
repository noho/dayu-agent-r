# Plan Review: PR 190 / F17

- Reviewed target: `docs/gateflow/pr-190-f17-plan-20260807.md`
- Review time: 2026-08-07 14:32:41 +0800
- Review mode: adversarial plan review

## Scope

- Goal confirmation: `docs/gateflow/pr-190-f17-goal-confirmation-20260807.md` ✅ 已读取
- Design source: `docs/cli_ci.md` ✅ 已读取
- Production validator: `utils/smoke_cli_init_provider_matrix.py` ✅ 已读取
- Test file: `tests/cli/test_smoke_cli_init_provider_matrix.py` ✅ 已验证
- AGENTS.md: ✅ 已读取
- tests/README.md: ✅ 已读取

## Assumptions Tested

### 1. Fresh production FIRST strict tree

**Plan 声称**: 通过 production `InitMode.FIRST`、production file lock、workspace transaction owner 在 fresh 临时 workspace 发布完整树。

**独立验证**:
- 运行 `pytest tests/cli/test_smoke_cli_init_provider_matrix.py::test_frozen_manifest_matches_fresh_real_publication_tree -q`
- 结果: `FAILED`，输出显示 `PublicationValidationReport(valid=False, issues=('file_digest_mismatch:config/prompts/scenes/conversation_compaction_user.md', ...))`
- 结论: ✅ 确认当前存在 mismatch，且来自 production publication

### 2. 唯一 mismatch

**Plan 声称**: 完整差异仅为 `file_digest_mismatch:config/prompts/scenes/conversation_compaction_user.md`

**独立验证**:
- 测试输出: `issues=('file_digest_mismatch:config/prompts/scenes/conversation_compaction_user.md', ...)`
- 结论: ✅ 确认唯一 mismatch 是 `conversation_compaction_user.md`

### 3. 5/43/16 inventory

**Plan 声称**: actual inventory 与 manifest inventory 均为 5 directories / 43 files / 16 model pointers

**独立验证**:
- 运行 `python -c "from pathlib import Path; from utils.smoke_cli_init_provider_matrix import load_manifest; m = load_manifest(Path('docs/cli_init_workspace_manifest_v1.json')); print(f'dirs={len(m.directories)}, files={len(m.files)}, owners={len(m.model_projection_owner_paths)}')"`
- 结果: `dirs=5, files=43, owners=16`
- 结论: ✅ 确认 inventory 为 5/43/16

### 4. Prompt raw SHA-256

**Plan 声称**: 当前 prompt raw SHA-256 是 `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`

**独立验证**:
- 运行 `shasum -a 256 dayu/config/prompts/scenes/conversation_compaction_user.md`
- 结果: `22e7bc5015cb369ff228a754b557493594b8313c99877944b5a7c08da0dc1c88`
- 结论: ✅ 确认 prompt SHA-256 一致

### 5. Manifest raw SHA-256

**Plan 声称**: 当前 manifest raw SHA-256 是 `d95de68e69b0aacc712ec6bf468c8604a91460a17f3e2497f397182517a6a9f8`

**独立验证**:
- 运行 `shasum -a 256 docs/cli_init_workspace_manifest_v1.json`
- 结果: `d95de68e69b0aacc712ec6bf468c8604a91460a17f3e2497f397182517a6a9f8`
- 结论: ✅ 确认 manifest SHA-256 一致

### 6. Manifest entry 旧 digest

**Plan 声称**: manifest 中旧值是 `59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76`

**独立验证**:
- 运行 Python 脚本读取 manifest 并查找 `conversation_compaction_user.md` 的 digest
- 结果: `path=config/prompts/scenes/conversation_compaction_user.md, digest=59b50e13ea636c434fcabe26adf6d9ed22665dfcba03533ebcf5e9b524b87b76`
- 结论: ✅ 确认旧 digest 一致

### 7. FROZEN_MANIFEST_SHA256 测试常量

**Plan 声称**: 测试中的 `FROZEN_MANIFEST_SHA256` 与当前 manifest raw SHA-256 相等

**独立验证**:
- 运行 `grep -n "FROZEN_MANIFEST_SHA256" tests/cli/test_smoke_cli_init_provider_matrix.py`
- 结果: `FROZEN_MANIFEST_SHA256 = "d95de68e69b0aacc712ec6bf468c8604a91460a17f3e2497f397182517a6a9f8"`
- 结论: ✅ 确认测试常量与 manifest SHA-256 一致

### 8. 71-test validation

**Plan 声称**: `pytest --collect-only -q tests/cli/test_smoke_cli_init_provider_matrix.py` 当前收集 71 tests

**独立验证**:
- 运行 `python -m pytest tests/cli/test_smoke_cli_init_provider_matrix.py --collect-only -q`
- 结果: `71 tests collected in 1.10s`
- 结论: ✅ 确认测试数量为 71

## Findings

**未发现实质性问题。**

Plan 的所有关键假设都已通过独立验证，owner chain 清晰，scope 精确，sequencing 合理。

---

### Detailed Analysis

#### 1. Goal-bound minimal design review

**当前写法**: Plan 只修复 frozen manifest 中的单一 digest 漂移，不修改 prompt、validator、fixture 或产品行为。

**直接证据**:
- Goal confirmation 明确列出非目标："不修改 prompt 内容、init/prompt/interactive 产品行为或 validation 逻辑"
- Plan 第 3 节明确允许的产品/测试变更只有两个精确 hunk
- Plan 第 5 节明确禁止修改 prompt、validator、fixture 逻辑、Oracle/scenario/readiness

**结论**: ✅ 无 goal drift，plan 严格绑定到 Goal Confirmation

#### 2. Architecture boundary review

**当前写法**: Plan 遵循语义 owner chain：
1. `conversation_compaction_user.md` raw bytes: LLM-facing 内容真源；只读
2. `dayu.cli.init_workspace` production transaction: publication 行为 owner；不改
3. `docs/cli_init_workspace_manifest_v1.json`: frozen publication truth consumer；唯一修复点
4. `FROZEN_MANIFEST_SHA256`: 二级派生 consumer；只能在第 3 步保存后推导更新
5. `utils/smoke_cli_init_provider_matrix.py`: 严格 schema / audit owner；只用于验证，不改

**直接证据**:
- Plan 第 3 节完整描述了 owner chain
- AGENTS.md 要求 "每个业务事实、状态、错误原因、格式化规则、派生值、LLM-facing 语义、schema 字段或协议字段，都必须有唯一清晰 owner"
- Plan 遵循了这一要求

**结论**: ✅ 架构边界清晰，无反向依赖或语义漂移

#### 3. Best-practice review

**当前写法**: Plan 使用最小化修复方案，只修改两个文件的两个单行 digest。

**直接证据**:
- Plan 第 5 节："该 work unit 只有一个相互依赖的两-hunk slice；拆成多个 slice 会制造 manifest 已更新但 digest pin 未同步的中间 contract"
- Plan 第 5 节："当前方案不新增抽象、helper、schema、测试或兼容分支，因而是满足现有 owner contract 的最小方案，没有过度设计"

**结论**: ✅ 符合最佳实践，最小化修复

#### 4. Optimal-solution review

**当前写法**: Plan 选择直接同步 manifest entry 和测试常量，而不是倒改 prompt、动态生成 expected 或放宽 validator。

**直接证据**:
- Goal confirmation 明确禁止："不得倒改 prompt、动态生成 expected、放宽 validator 或添加兼容分支"
- Plan 第 2 节："倒改 prompt、修改 production transaction、动态生成 expected 或放宽 validator 都会破坏语义 owner，不能实施"

**结论**: ✅ 方案最优，避免了破坏语义 owner 的替代方案

#### 5. Overengineering review

**当前写法**: Plan 不新增抽象、helper、schema、测试或兼容分支。

**直接证据**:
- Plan 第 5 节明确说明
- 只修改两个文件的两个单行 digest

**结论**: ✅ 无过度设计

#### 6. Overcoupling review

**当前写法**: Plan 只修改 manifest 和测试常量，不引入跨层耦合。

**直接证据**:
- 只涉及两个文件：`docs/cli_init_workspace_manifest_v1.json` 和 `tests/cli/test_smoke_cli_init_provider_matrix.py`
- 不修改 prompt、validator、fixture、产品行为或 schema
- 不引入新的依赖关系

**结论**: ✅ 无过度耦合

## Open Questions

无

## Residual Risks

### 1. 非目标 bytes 被 formatter 改写

**风险**: JSON formatter 或全文件 rewrite 可能改变非目标 bytes。

**缓解措施**: Plan 第 6 节明确禁止 JSON formatter、排序或全文件 rewrite；第 7 节要求 `git diff --word-diff=plain` 和 `git diff --numstat` 验证只有两个单行 digest 替换。

**结论**: 风险已缓解

### 2. 存在第二个 publication mismatch

**风险**: 可能存在第二个未发现的 mismatch。

**缓解措施**: Plan 第 6 节要求 post strict audit 必须 `issues == ()`，否则 slice 不通过并回滚。

**结论**: 风险已缓解

### 3. 测试数量被意外减少

**风险**: 修改可能意外减少测试数量。

**缓解措施**: Plan 第 7 节要求 `collect-only` 必须仍为 71，再要求 71 passed。

**结论**: 风险已缓解

### 4. 真实 provider / Oracle replacement scenarios 未重跑

**风险**: F17 只修 deterministic frozen publication consumer，不改变 provider、Oracle/scenario/readiness 或产品行为。

**缓解措施**: Plan 第 9 节明确说明："三条 replacement scenarios 继续由 PR 190 后续既有 owner 处理"。

**结论**: 风险已明确分配

### 5. 其它未来 package asset 再次漂移

**风险**: 现有 strict validator 与 frozen digest test 会 fail closed，本 slice 不引入自动更新 manifest 的新写路径。

**缓解措施**: Plan 第 9 节明确说明："本 slice 不引入自动更新 manifest 的新写路径"。

**结论**: 风险已明确分配

## Plan Review Conclusion

**PASS**

Plan 的所有关键假设都已通过独立验证：

1. ✅ fresh production FIRST strict tree：测试确认当前存在 mismatch
2. ✅ 唯一 mismatch：`conversation_compaction_user.md`
3. ✅ 5/43/16 inventory：manifest 库存正确
4. ✅ prompt bytes owner 不改：plan 明确只读
5. ✅ manifest entry 到 raw-byte pin 单一派生链：owner chain 清晰
6. ✅ exact two-hunk scope：只允许两个文件的两个单行修改
7. ✅ 71-test validation：测试数量正确

Plan 遵循了 AGENTS.md 的所有约束：
- 语义所有权清晰
- 架构边界明确
- 最小化修复
- 无过度设计或过度耦合
- 无 goal drift

所有 residual risks 都已有明确的缓解措施或分配。

**无 blocking findings，plan 可安全交给 implementation agent。**
