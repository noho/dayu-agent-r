# UF-FIX01 fiscal-period prevalidation residual — Plan Re-Review（fix 后）

## 元数据

- reviewed target：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-plan-20260817.md`（fix 后版本）
- work unit：`UF-FIX01-fiscal-period-prevalidation-residual`
- gate：`plan review -> re-review`
- review 日期：2026-08-18（本机时钟）
- 原 review：MiMo `docs/reviews/plan-review-20260818-000654.md`（findings M-001..M-005）、
  DS `docs/reviews/plan-review-20260818-001109.md`（findings D-F1..D-F3）
- adjudication：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-plan-review-adjudication-20260818.md`
- fix artifact：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-plan-review-fix-20260818.md`
- goal artifact：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-goal-confirmation-20260817.md`（`confirmed-by-user`）
- 结论：**pass-with-risks**

## Reviewed target and scope

本次 re-review 的目标是验证 8 个 accepted findings（M-001..M-005、D-F1..D-F3）是否真实写回 plan、
以及修订后的 plan 是否引入新的 material finding 或 slice 红窗口。逐项用实际代码/测试证据判定终态
（未修复/已修复/部分修复/证据失效），并复检 owner / LLM-facing / coverage / pyright / README 风险。
未修改 plan 或任何代码，未提交。

## Assumptions tested

| # | Assumption | 验证方式 | 结果 |
|---|---|---|---|
| R1 | owner 签名支持 plan §6 伪代码的 `field_name` kwarg | `dayu/fins/domain/filing_semantics.py:293`：`normalize_fiscal_period(value: Optional[str], *, field_name: str = "fiscal_period")` | 成立 |
| R2 | owner 对空白输入返回 None（None-invariant 可达性） | `filing_semantics.py:307-311`：`None` 与 strip 后为空均返回 `None` | 成立 |
| R3 | 空白-only period 不会落入 AssertionError 分支 | `ingestion_runtime.py:1270`：`is None or strip()==""` 先映射 `MISSING_FISCAL_PERIOD`，domain 调用在其后 | 成立（invariant 为不可达 fail-closed，无行为回归） |
| R4 | 两个 upload ID builder 的 production 调用方只有 upload admission | 全仓 grep：`docling_upload_service.build_cn_filing_ids` 仅 `ingestion_runtime.py:1329`；`build_sec_filing_ids` 仅 `:1322` | 成立 |
| R5 | CN download 使用独立的 `cn_form_utils.build_cn_filing_ids`，不在收窄范围 | `cn_download_workflow.py:33-38`、`cn_download_filing_workflow.py:43` 均 import 自 `cn_form_utils` | 成立 |
| R6 | `derive_report_kind` 两个消费者在收窄后类型成立 | `cn_pipeline.py:816/860`、`sec_upload_workflow.py:185/229` 均取自 `authoritative_request.normalized_fiscal_period`（收窄后为 `FiscalPeriod`） | 成立 |
| R7 | `ValidatedFinsUploadFilingRequest` / `_StaticFinsUploadFilingValidation` 构造点唯一 | grep：`:1523`（从 static 字段）、`:1336`（admission 返回），全仓无其它构造 | 成立 |
| R8 | 收窄后 Literal→str 消费点无 pyright 破坏 | `cn_pipeline.py:1862`、`sec_upload_workflow.py:397` 传入 `**payload: JsonValue`；`tests/cli/test_fins_commands.py:397` fake 记录字段为 str | 成立 |
| R9 | 旧 code/value/message 的全部引用都在 S1 allowed files 内 | 全仓 grep：`ingestion_runtime.py:688/1040/1279`、`tests/fins/test_fins_ingestion_runtime.py:1419/1452`、`tests/cli/test_fins_commands.py:1628`；tests 内 `仅支持` 仅上述两文件 | 成立 |
| R10 | tool schema 现状与 D-F3 声称一致（无 enum、缺 H1） | `dayu/fins/tools/upload_tools.py:253-256`：仅 description，列举 FY/Q1/Q2/Q3/Q4，无 H1 | 成立 |
| R11 | schema contract test 先例存在 | `tests/fins/test_fins_ingestion_tools.py:1433` `test_upload_tool_calendar_year_schema_and_usage_messages_are_business_neutral` | 成立 |
| R12 | 无其它测试断言旧 schema description 文案 | grep `财报期间，例如` / `例如 FY、Q1` 在 tests 无命中 | 成立 |
| R13 | `_StaticFinsUploadFilingValidation.normalized_fiscal_period` 与 `ValidatedFinsUploadFilingRequest` 字段确为 `str` | `ingestion_runtime.py:835`、`:780` | 成立 |
| R14 | owner 契约测试文件存在 | `tests/fins/test_fiscal_normalization_contracts.py` 存在 | 成立 |

## Finding 终态验证

| ID | 终态 | 证据 |
|---|---|---|
| M-001 | **已修复** | plan §5/§7 change #5：`derive_report_kind` 参数收窄为非 optional `FiscalPeriod`，只投影 report kind，不再调用 optional normalizer。代码证据：现实现 `docling_upload_service.py:2008-2026` 内部调用 `normalize_cn_fiscal_period`（返回 str、不返回 None）；两个消费者（R6）在收窄后传入 `FiscalPeriod`，None 泄漏洞由类型关闭。 |
| M-002 | **已修复** | plan §5：`_StaticFinsUploadFilingValidation.normalized_fiscal_period`（`:835`）与 `ValidatedFinsUploadFilingRequest.normalized_fiscal_period`（`:780`）及两个 upload ID builder 的 period 参数同步收窄为 `FiscalPeriod`。R4/R5/R7/R8 证明构造点唯一、production 调用方只有 admission、download 走独立 owner 不受影响。 |
| M-003 | **已修复** | plan §6 #4、§7 change #5：删除 `normalize_cn_fiscal_period`（`docling_upload_service.py:1989`）及其 `__all__` 导出（`:2217`），删除两个 upload ID builder 内 period 的 `strip().upper()`（`:1944`/`:1979`）。R9 扩展扫描：该函数全部引用（`ingestion_runtime.py:121/1277`、`docling_upload_service.py:2021`、`tests:56/5030`）均在 S1 allowed files 内。 |
| M-004 | **已修复** | plan §6 已给出 exact try/except 伪代码：`normalize_fiscal_period(request.fiscal_period, field_name="--fiscal-period")`，`ValueError` 映射 `UNSUPPORTED_FISCAL_PERIOD`，`None` 为 required invariant breach。R1/R2/R3 证明伪代码与 owner 签名一致，且 required check（`ingestion_runtime.py:1270`）先于 domain 调用，`None` 分支不可达，空白-only 输入仍走既有 `MISSING_FISCAL_PERIOD` exit 2，无行为回归、无 ValueError 逃逸为 runtime failure 的路径。 |
| M-005 | **已修复** | plan §7 change #2 与 §7 扫描清单明列：closed enum set、exact message tests、旧 enum 名称/旧值/旧文案的全仓扫描（`UNSUPPORTED_CN_FISCAL_PERIOD`、`unsupported_cn_fiscal_period`、`CN/HK --fiscal-period 仅支持`）。R9 证明旧引用仅存在于 S1 已列文件（含 `tests/fins/test_fins_ingestion_runtime.py:1419/1452` 与 `tests/cli/test_fins_commands.py:1628`），扫描模式覆盖 enum 名、值字符串与中文文案前缀。 |
| D-F1 | **已修复** | plan §7 allowed files 已加入 `tests/cli/test_fins_commands.py`（仅同步现有 UF-024 exact reason），S1 Validation commands 的 pytest 与 pyright 均已包含该文件。代码证据：`:1628` 断言旧文案 exact、usage matrix `:1799-1806` 为 exact equality；R9 证明 repo 内无其它旧文案断言，S1 accepted commit 不再携带已知红。 |
| D-F2 | **已修复** | plan §7 change #1 明确「不修改 `filing_semantics.py` production；在 owner tests 参数化断言」；§8 Coverage 段给出固定 include 集合 `dayu/fins/ingestion_runtime.py,dayu/fins/pipelines/docling_upload_service.py,dayu/fins/tools/upload_tools.py`，并写明 `filing_semantics.py` production 不修改故不纳入 include、不为无关 sec-form/date/quality 分支扩 scope。 |
| D-F3 | **已修复** | plan §8 allowed files 已加入 `dayu/fins/tools/upload_tools.py`；change #5 要求 description 自足列出 `FY、H1、Q1、Q2、Q3、Q4` 并保持 filing 必填/material 可选，schema 不从 runtime enum 反向拼装、不新增兼容 alias，并在现有 schema contract test 增加 exact assertion。R10/R11/R12 证明现状与先例、无其它旧描述断言。 |

全部 8 项均通过代码/测试证据验证为 **已修复**，无「部分修复」或「证据失效」项。

## Amendment 引入的新风险检查

按用户指定维度逐一复检修订后 plan：

- **slice 红窗口**：无。S1 变更 usage enum/message 与删除 duplicate parser 的全部受影响引用（R9 + `normalize_cn_fiscal_period` 全仓 grep）都在 S1 allowed files 内，S1 的 pytest/pyright 命令覆盖这些文件；S1→S2 之间不存在其它依赖旧契约的测试或 production 引用。README 中无旧文案残留（grep 无命中），S2 change #6 按触发规则更新根 README。
- **owner 风险**：无新增真源。domain owner（`normalize_fiscal_period`）仍是唯一 strip/uppercase/闭集 owner；S1 明确不改 `filing_semantics.py` production；CN download 的 `cn_form_utils.build_cn_filing_ids` 由 plan §5 显式排除（R5 证明其为独立 download owner）。
- **LLM-facing 风险**：D-F3 修复后 tool schema description 自足列全六值；usage reason `--fiscal-period 仅支持 FY、H1、Q1、Q2、Q3、Q4` 为 market-neutral 精确文案；schema 与 runtime 闭集同一业务事实，双投影一致。
- **coverage 风险**：include 集合固定且与「最终实际修改的 production 文件」一致。残留低风险：`upload_tools.py` 的覆盖率未在两份原 review 中实测（仅实测 ingestion 91%、docling 89%、filing_semantics 70%），`--fail-under=80` 为三文件合并门槛，若 `upload_tools.py` 覆盖率显著偏低可能拖累合并值；该文件有大量既有 tool 测试（R11 同文件），S2 验证命令会实测并暴露，不构成 material finding。
- **pyright 风险**：R7/R8 证明类型收窄构造点唯一、下游消费点均为 Literal→str/JsonValue 兼容方向；S1 七文件 pyright + S2 全仓 pyright 命令均在 plan 内。
- **README 风险**：plan §10 docs decision 与 CLAUDE.md 触发规则一致（fins/tests/根 README 更新在 S2，Host/Engine/dayu README 不更新）；fix gate 未提前改写行为文档，正确。
- **goal drift / 过度设计**：amendments 全部落在 goal confirmation 的 scope boundary 内（D-F3 是同一闭集契约的 LLM-facing 投影，由 controller 按 LLM-facing 硬约束裁决）；未新增层、类、协议、alias、migration。

## 未构成 material finding 但需记录的点

1. `build_cn_filing_ids` 内部除 `fiscal_period.strip().upper()`（`:1944`）外还有 `form_type.strip().upper()`（`:1943`），且 upload 调用路径（`ingestion_runtime.py:1331`）中 form_type 与 fiscal_period 传入同一 `FiscalPeriod` 值。plan change #5「删除两个 upload ID builder 的 strip/uppercase」未逐行点名 1943；无论实现只删 period 行还是两行都删，canonical 输入下 seed 输出一致（strip/upper 幂等）、ID 算法不变，故不影响正确性，仅建议 implementation 在 artifact 中写明删除范围。
2. plan §6 的 `None -> AssertionError` 为不可达 fail-closed（R3），实现时保留即可，勿改为宽松 fallback。

## Open questions

无 blocking open question。

## Residual risks and suggested tracking destination

| Risk | 分类 | Tracking destination |
|---|---|---|
| `upload_tools.py` 覆盖率未实测，S2 合并 80% 门槛的理论达标风险 | `covered by later approved slice`（S2 验证命令实测） | S2 implementation/closeout |
| 真实 subprocess exit 2 / no-traceback / no-mutation 证据 | `assigned to later work unit`（goal 已确认排除） | 用户 UF-PF01/UF-PF12 校准流程 |
| 冻结 evidence/oracle/scenario 未刷新 | `assigned to later work unit` | 用户校准流程 |
| material optional fiscal metadata、`upload_filings_from` 未统一 | `assigned to later work unit`（goal 排除） | 后续 work unit |
| download filter aliases 多于 filing closed values | `rejected-with-reason`（独立 filter owner 有意承诺） | 无 |
| 旧 durable 非法 fiscal period 兼容读取 | `assigned to later work unit`（schema 规则 + 无升级授权） | 后续 work unit |
| AgentCodex plan/fix turn 无产出停滞 | workflow execution risk，controller 已机械落盘 | 本次 re-review 独立验证收口 |

## Final conclusion

**pass-with-risks**

8 个 accepted findings（M-001..M-005、D-F1..D-F3）经实际代码与测试证据逐项验证，终态均为「已修复」：
修订后的 plan 已把 `FiscalPeriod` 类型收窄贯穿 validated/static dataclass、`derive_report_kind` 与两个
upload ID builder，删除 duplicate parser 与 builder 内第二份 normalization，给出与 owner 真实签名一致的
exact try/except 映射与不可达 None invariant，旧 code/value/message 的全仓扫描清单与 S1 范围闭合
（CLI UF-024 exact reason 与 closed code set/message tests 同 slice 更新，S1 accepted commit 不再携带已知红），
coverage include 集合固定且 `filing_semantics.py` production 被明确排除，tool schema description 纳入 S2
并按 LLM-facing 约束自足列全六值。

修订未引入新的 material finding、slice 红窗口或 owner/LLM-facing 风险；残留项均为已分类的低风险
（`upload_tools.py` 覆盖率待 S2 实测、builder 内 form_type 行删除范围建议在 implementation artifact 写明）。
plan 已达到 code-generation-ready，可进入两路 re-review 后的 accepted plan commit 与 S1 实施。
