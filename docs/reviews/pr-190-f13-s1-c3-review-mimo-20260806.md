# PR-190 F13 S1 C3 AgentMiMo Review

## Verdict: ACCEPTED

## Review scope

- checkpoint: `docs/gateflow/pr-190-f13-s1-c3-checkpoint-20260806-165756.md`
- diff identity: `d82dd47e26fd32537bb6d80780bbf5c97ede0a97`
- base: `62445b59`
- 裁决范围: rolling/multi-pass/Memory/reconnect/RunInput/call sites、repair/fallback/stale/late 及对应 owner/integration tests/residue

## 验证结果

- C3 focused/integration: 595 passed, 1 skipped, 3 warnings (第三方 edgar deprecation)
- v3 residue: `grep -rn 'accepted_candidate\|candidate_binding_v4' dayu tests utils --include '*.py'` 零命中
- active v3 symbol: 仅 `tests/service/test_host_assembly.py:335-336` 两条负断言（允许项）
- `PromptLocalProvenanceEntry` 无 `accepted_evidence_id` 字段；`RunInputMaterialBlock.accepted_evidence_id` 仅用于 current evidence admission，不进入 PromptLocal/read path

## Finding summary

无 blocking/high/medium finding。以下为逐项 accepted 裁决：

### 1. Rolling previous fact claim+refs atom — accepted

`compact_material.py:2319-2327`: `_previous_compacted_view_pair_from_replacement` 对每个 `replacement.evidence_facts` atom 同时取 `text=fact.claim` 与 `canonical_evidence_refs=fact.canonical_evidence_refs`，来源为同一 accepted replacement atom。`canonical_source_refs=(event_id,)` 保存 compact EventLog ref，不与 evidence refs 交叉。

### 2. Multi-pass root selector ordering/union — accepted

`compaction_operation.py:1229-1301`: `_aggregate_pass_candidates` 按 frozen pass queue 顺序迭代 pass_truths，retained atoms 由 `source_kind is PREVIOUS_EVIDENCE_FACT` 确定（非 claim/refs 形状猜测），new atoms 按 pass queue / atom 顺序拼接。root revalidation 在 `accept_compact_candidate_v4` 重新执行完整 binding/duplicate/caps/coverage/aggregate 校验。

`compaction_operation.py:1331-1378`: `_route_root_validation_report` 把首个 root issue 路由到最后一个稳定贡献 pass，用于定向 repair。

### 3. Memory per-fact refs（无 aggregate 污染）— accepted

`memory.py:1728-1774`: `_facts_from_accepted_event` 对每个 `fact` 构造 `EvidenceBackedFactView(evidence_refs=fact.canonical_evidence_refs)`，逐 atom 独立 refs。

`compact_pipeline.py:290-296`: `accepted_evidence_mapping_refs` 是从 replacement aggregate 派生的只读 property，用于 RunInput represented refs；不用于重建逐 fact refs。

`compact_payload.py:620-655`: strict parser 逐 fact 恢复 `canonical_evidence_refs`，round-trip integrity 确认。

### 4. Reconnect strict terminal only — accepted

`run_input.py` 中 reconnect 路径经 `parse_context_compacted_semantic_payload` strict parser 恢复 replacement，再复用 Memory fact projection。`memory.py` 的 `project_conversation_memory_event` 对 `CONTEXT_COMPACTED` 事件严格要求 `compacted_semantics` 非空。

### 5. Rejected/failed/stale/late 无二次 terminal/Memory — accepted

`compaction_operation.py:1447-1472`: `_failed_operation_result` 返回 `accepted_truth=None`；下游 dispatch/engine_ingest 在 accepted_truth 为空时不写 `CONTEXT_COMPACTED`。

stale/cancellation 由 `_CompactionAttemptCancellationToken` 的 parent 优先逻辑与 `_ensure_compactor_proposal_active` 保证丢弃。

### 6. Fake repair typed reject（非 heuristic）— accepted

`fake_compaction.py:630-680`: `_typed_repair_omits_evidence_facts` 检查 `CompactValidationIssueCodeV4.DUPLICATE_SEMANTIC_ITEM` + `json_path.startswith('$["evidence_facts"]')`，走真实 governance typed reject path。`_prompt_repair_omits_evidence_facts` 从 repair prompt 的 typed JSON block 读取，不使用 claim/关键词 heuristic。

### 7. S2 Tool Trace 边界未提前 — accepted

`test_tool_trace_queries.py` 使用 `COMPACT_OUTPUT_SCHEMA_V4` 和 v4 constructs，只做 v3/schema-5 fixture 迁移，未引入 S2 的 `ResolvedCompactorEvidenceFact` 新语义或 README。

### 8. 过度耦合/兼容/第二 truth/下游 fallback — accepted

- 无兼容层 re-export 或 wrapper
- 所有 consumer（artifact/EventLog/audit/rolling/Memory/RunInput/reconnect）从同一 accepted replacement 投影
- `RunInputMaterialBlock.accepted_evidence_id` 只用于 current evidence admission，不进入 PromptLocal provenance
- dispatch/engine_ingest 中无下游 fallback 补偿

## Risk / uncovered

- C3 有 1 个既有 skip；未改变其 skip 条件
- 测试覆盖为 owner/integration/Host smoke tests，非实际 provider 或 interactive CLI scenario
- README 与 public Tool Trace 最终语义归 S2
