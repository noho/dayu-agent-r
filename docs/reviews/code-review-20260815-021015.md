# Code Review

## Scope

- Mode: current changes（Gateflow S2 re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `c5446b770d238aafd8c42552dadbe132cba94ad2`（accepted S1 local commit）
- Output file: `docs/reviews/code-review-20260815-021015.md`
- Included scope: 当前全部未提交 S2 diff（S2 implementation + S2 fix，相对 base 的 31 个文件、+1789/-607），含新增 `dayu/fins/domain/company_meta_contract.py`、`tests/fins/company_meta_test_support.py`、`tests/fins/test_company_meta_contract.py`、`tests/fins/test_company_identity_storage_contract.py`
- Excluded scope: S1 已接受内容、docs/reviews 下本 work unit 的 gate artifacts、UF-PF05/oracle/scenario/frozen evidence（用户明确排除）、Host/Engine 非 Fins 测试
- Parallel review coverage: 无。主 reviewer 逐文件走读了 domain merge contract 全文、`_fs_storage_infra.py` 的 begin_batch/commit/identity 准备/scan/index/lstat/recovery 全部关键路径、`_fs_company_meta_core.py` route、repository protocols、fs_company_meta_repository、storage `__init__`、read_runtime/error_contract、upload_tools/fins_tools、upload_failure mapper、SEC/CN filing/material 终端 catch、upload/sec/cn download producer、两份新测试文件全文与全部 S2 测试 diff。
- 已读取上下文：根 `AGENTS.md`、accepted plan（`wu-upload-filing-ticker-alias-contract-plan-codex.md`）及两轮 plan fix、首次两份 S2 review（`code-review-20260815-011809.md`、`code-review-20260815-011944.md`）、S2 controller adjudication、S2 fix artifact、S2 implementation artifact（含 fix addendum）。

## 结论摘要

- 决策：**PASS / 无 blocker**。accepted F1/F2/F3 逐条验证全部真实修复，11 组矩阵测试均为有效 owner/durable/e2e 测试；完整回归审查未发现新功能性 defect。
- 独立复验：`test_company_identity_storage_contract.py + test_company_meta_contract.py` = 43 passed；4 个 SEC/CN filing/material conflict terminal tests passed；`test_list_documents_meta_less_corpus_coexists_with_healthy_alias_corpus` 与 `test_alias_conflict_failure_is_identical_across_direct_durable_and_observation` passed；完整 relevant suite `tests/fins + tests/tools/test_combined_tools_acceptance.py` = **1599 passed, 1 skipped**（与 fix artifact 声明一致）；全量 `pyright dayu/ tests/ utils/` = **0 errors**；4 项 residue scan 零命中（scan2 命中均为合法 `ticker_identity` 前缀访问）；`git diff --check` 通过。

## 逐条验证 accepted findings

### F1 — 验证通过：begin_batch lstat 语义正确，fail closed 且 tree 不变

- **生产代码**（`dayu/fins/storage/_fs_storage_infra.py:541-559`）：`begin_batch` 在取得 same-ticker writer lock 后调用 storage-owned `_lstat_optional_storage_path`（`:1103-1127`）派生 `publishes_new_corpus`。该 helper 仅 `errno.ENOENT` 返回 `None`（missing）；`EACCES`/`EIO` 及其它 OSError 经 `_project_filesystem_error` + `_raise_path_free_error` 原样 fail closed（path-free、errno 保留）；`target_stat is not None 且非 S_ISDIR`（lstat 不跟随 symlink，故 symlink/non-directory 均命中）在 `:546-547` 抛 `ValueError`。任一异常走 `:577-604` 的 staging 清理 + writer lock 释放 + 重抛，published tree 零触碰。仅明确 missing 才冻结 `publishes_new_corpus=True`。
- **测试**（`test_company_identity_storage_contract.py:1110-1156`）：`test_begin_batch_lstat_failure_never_reclassifies_existing_corpus_as_new` 对 EACCES/EIO 参数化，用 `_OneShotLstatFailure` 在 exact target path 首次 lstat 注入失败；断言 errno 保留、错误不含 target path、published tree 全文件 bytes 不变、retry begin/rollback 正常且 `_active_batches` 清空（writer/active state 释放）。
- **独立验证**：该测试在 43 passed 中通过。

### F2 — 验证通过：§11.3/§11.4 的 11 组矩阵全部为有效 owner/durable/e2e 测试

逐一打开测试源码核对断言（不是只数测试名）：

1. **跨进程 same-canonical**（`test_company_identity_storage_contract.py:773-928`）：`test_cross_process_same_canonical_stale_intent_preserves_alias_union` 用 `multiprocessing.get_context("spawn")` + Pipe 真实跨进程，父进程先冻结 stale intent、子进程提交较新同版本事实，断言 durable `CompanyMeta` 保留 P2 非身份事实与 `("OLD","CHILD","PARENT")` stable union；`test_cross_process_same_canonical_changed_but_still_stale_is_rejected` 断言 `CompanyMetaConcurrentUpdateError`、P2 tree bytes 不变、PARENT 未发布；`test_two_material_processes_on_same_canonical_union_aliases` 用两个真实 `stage_company_meta_for_upload(...)` producer 进程，断言两次 commit 均成功且 durable aliases 精确保留 `OLD/MAT-A/MAT-B`。前两个测试的 P2 用 fixture-based intent 而非 material producer，但 merge 语义与 producer 无关，且第 3 个测试以真实 producer 覆盖同路径——等价覆盖成立。
2. **document-only 不取 global guards**（`:1159-1183`）：把 recovery/identity acquire seam 替换为必抛 callable 后 document-only commit 仍成功，证明只走 target publication guard。
3. **alias read 锁序**（`:1186-1218`）：`_LockAcquisitionRecorder` 记录真实 `_acquire_lock_token` 调用，断言首锁为 `company_identity.lock`、后续全为按名排序的 `*.publication.lock`、无 recovery 锁。
4. **四层 acquire failure**（`:1221-1286`）：参数化 recovery/identity/scan-publication/target-publication 四个注入点（target 用 `_NthPublicationAcquireFailure(..., 2)` 精确命中 authoritative 重读后的最终 target guard），`_ReplaceDirectoryForbidden` 断言 replace 调用为零、published tree bytes 不变。
5. **conflict primary + release failure**（`:1289-1366`）：两个测试分别注入 identity 与 publication guard release failure，断言 `CompanyTickerAliasConflictError` 保持 earliest primary、note 有界且无 workspace path、loser absent、winner tree 不变。
6. **swap 后崩溃交错**（`:1414-1449`）：`_leave_batch_swapped_before_committed` 通过真实 core 操作构造 target 已 swap、journal 为 `swapped_target` 的 orphan evidence；B 持不同 ticker writer 提交时先恢复 A 的 OLD alias 再以 typed conflict 拒绝；断言 A exact old tree 恢复、NEW 不可路由、B 不发布。
7. **双向 canonical conflict**（`:1369-1411`）：参数化 healthy-first/meta-less-first，两个方向均在 publication 前 typed conflict，winner tree exact 不变且唯一 route 指向 winner。
8. **recovery-read barrier**（`:1452-1498`）：`_BlockingFirstReplace` 在 recovery 第一次 physical replace 前建立 Event barrier；并发 read 已尝试 identity acquire（`_IdentityAcquireSignal.entered`）但不得完成，放行后只观察 restored OLD route，NEW 不可见。
9. **orphan identity guard acquire/release failure**（`:1501-1583`）：acquire failure 断言 physical mutation 为零、workspace 全树 bytes + journal token dir + backup 均保留；release failure 断言 restore 已完成、release failure 可见、fresh repository 一致读取 OLD、evidence 清理完成。
10. **meta-less e2e 共存**（`test_fins_storage_provider.py:3212-3293`）：通过真实 public batching/source/blob repository API 发布带完整 document 的 DELTA meta-less corpus（非伪造 material create）；断言 `list_documents(DELTA)` == `list_documents(delta.us)`、matched==1、`AAPL`==`APPLE`（healthy alias 不受阻断）、DLTA 不获隐式 alias、DELTA 无 `meta.json`。
11. **failure projection e2e**：SEC filing（`test_sec_pipeline_upload_filing_stream.py:1338`）、SEC material（`test_sec_pipeline_upload_material_stream.py:243`）、CN filing（`test_cn_pipeline.py:1417`）、CN material（`test_cn_pipeline.py:1041`）四个测试均先发布真实 MSFT corpus 再让另一 canonical 声明 `MSFT` alias，断言 failed terminal、`stored_file_count==0`、loser absent、exact `storage/ticker_alias_conflict/message/retry_hint/file_label=None`；`test_alias_conflict_failure_is_identical_across_direct_durable_and_observation`（`test_fins_ingestion_runtime.py:6259`）断言 direct RESULT 字段、durable `result_summary["failure"]`、durable `failure_summary`、awaiting observation exact JSON 相等，且 path/lock key/`finsjob_` 不进入 public surface。

**F2 连带暴露并已修复的生产缺口**：SEC/CN material terminal catch 原先使用 `str(exc)`，现已与 filing 一样机械调用 `fins_upload_failure_from_exception(...)`（`sec_upload_workflow.py:571`、`cn_pipeline.py:1179`），`failure` 字段经既有 `_build_result(..., **payload)` builder 写入（filing 路径同款既有 pattern，非新增 extra-payload 设计），并有 `test_upload_material_failure_uses_shared_typed_failure_owner`（SEC/CN 各一）pin 住有界 projection。

### F3 — 验证通过：snapshot 不透明性断言归位

- `test_fins_ingestion_tools.py` 三个 awaiting owner 测试各恰有一条 `assert "finsjob_" not in outcome.snapshot.snapshot_id`：download（`:947`）、preprocess（`:973`）、upload（`:1092`）。upload 断言已恢复，download 重复已删除。

## 回归审查结论（correctness/atomicity/lock/recovery/read-route/failure/schema/README/scope）

- **merge contract**（`company_meta_contract.py` 全文）：与 plan §5.3 规则 1-6 逐一比对一致——preserve_published 保留 current 非身份字段、current 缺失抛 `CompanyMetaConcurrentUpdateError`；refresh_if_stale 的 create/apply-unchanged-snapshot/keep-fresh-current/reject-changed-stale 四分支正确；current aliases 在前、intent aliases 在后 stable union；`updated_at` 仅真实 mutation 时更新；intent builder 校验 mode/optional-field 不变量；`_require_identity_matches_observed` 校验 canonical/market/exchange。无新 drift。
- **原子性与锁图**：三条路径锁序逐一核对——identity-changing commit = writer → recovery → sweep → identity → incoming publication（读后释放）→ sorted scan publication → target publication（`_commit_batch_with_identity_guards` :693、`_prepare_company_identity_commit` :804、`_commit_batch_with_publication_guard` :751）；alias read = identity → 逐 candidate publication（逐个释放，:938-962）；recovery = recovery → nonblocking ticker writer → identity → publication（:2197-2312、:2387-2414，所有 phase 无条件取 identity guard，符合 R1 不猜 mutation）。未发现 publication→identity 或 identity→recovery 反向获取；`company_identity.lock` 位于 `.dayu` 根（:469），不会被 ticker candidate 扫描误判。锁内扫描与 swap 间无交错窗口。
- **recovery**：earliest-primary/note 规则在 `_release_lock_after_operation`（:1957）与 commit 双 finally 中实现；post-`COMMITTED` release failure 不回滚 durable tree（:795-802）。
- **read route**：`resolve_company_ticker` 单一路由（identity guard → scan → unique index → lookup），`_read_published_company_identity` 对 descriptor/meta 的 missing/symlink/non-regular/schema/namespace/locator/canonical 校验分型正确，`invalid_descriptor`/`invalid_meta`/`identity_mismatch`/`duplicate_owner` closed kinds 与 plan 一致；missing meta 合法 canonical-only；permission/I/O 经 `_lstat_optional_storage_path` 保持 OSError 类别不被抹平成 corruption（`test_descriptor_scan_preserves_permission_io_category` 覆盖）。read runtime 无 normalize/upper fallback，corruption→`workspace_identity_corrupted`、lock/OSError→`storage_unavailable`、`None`→`NOT_FOUND`，路径与文案 path-free 且固定。
- **failure**：mapper 三分型（conflict→`ticker_alias_conflict`、corruption/concurrent→`storage_io` 各自固定文案、其余不变）；`RuntimeFileLockError` 从 `unexpected_runtime` 收敛为 `storage_io`（语义改进，与 read 侧 `storage_unavailable` 一致，且被 e2e 测试 pin 住）；JSON kind/code 一致性从 `_STORAGE_FAILURE_CODES` 派生（R2 计划内新增）。`upload_tools.py` typed corruption catch 位于宽泛 `except ValueError` 之前（:105-112）。
- **schema 与 LLM-facing**：九个 read tools 共用 ticker schema 文案自足说明主代码/别名同归档语义（`fins_tools.py:1764-1770`），未暴露 lock/index/intent 等内部术语；`test_fins_read_tool_schemas_do_not_expose_execution_context` pin 住 exact 文案。
- **README**：根 README 只增用户可见 CSV 主代码/别名与冲突拒绝行为；`dayu/fins/README.md` 更新 descriptor canonical + CompanyMeta aliases owner 分解、meta-less、唯一 route、intent merge、锁序与 typed failures，并修正 FMP result 接口。tests/README、dayu/README 不触发，判断正确。
- **scope**：全部改动落在 accepted plan §9 allowed files 内；无 Host/Engine/runtime 修改；无 compat shim/旧符号残留（residue 四项独立复跑零命中）；测试迁移全部走真实 intent contract（`company_meta_test_support.stage_company_meta_fixture` 使用真实 builder + stage 接口，非兼容 seam）。
- **下载 producer 迁移**（`sec_company_meta.py`、`cn_download_company_meta.py`）：均以 public read 取 observed meta 后用同一 intent builder 形成 preserve/refresh intent，并复用 commit-time authoritative merge；旧 `upsert_company_meta`/`upsert_company_meta_for_cn_download` 语义已被 `stage_company_meta_intent`/`stage_company_meta_for_cn_download` 原子替换，无重复 final-meta snapshot 写入。

## Findings

### 1-未修复-低-commit-time backup 决策仍用 `Path.exists()`，F1 修复后为 fail-closed 但非 typed 失败

- **入口/函数**: `_commit_batch_with_publication_guard`
- **文件(行号)**: `dayu/fins/storage/_fs_storage_infra.py:768`（S1 既有代码，本次 diff 未改动该行）
- **输入场景**: begin_batch 的 lstat 正常判定 target 存在并复制 staging；commit 窗口内 target stat 因瞬时 EACCES/EIO 失败（`Path.exists()` 折叠为 False）
- **实际分支**: `if state.target_ticker_dir.exists():` 为 False → 跳过 backup → `_replace_directory(staging, target)`
- **预期行为**: 存在性事实应同样由显式 lstat 分型；stat 失败时 fail closed 并保留 typed 语义
- **实际行为**: 对非空 target，POSIX `os.replace` 因 ENOTEMPTY 抛 OSError → 走 `_rollback_precommit_batch` → published tree 不变（fail closed，无静默数据丢失，但 backup 缺失且失败表现为原始 OSError）；对空 target 的极端情形才可能静默覆盖
- **直接证据**: `:768` 使用 `Path.exists()`，而同文件 `:541`（F1 修复点）与 scan/read 路径均已改用 `_lstat_optional_storage_path`；base 版本 `git show c5446b77:...:529/613` 证明该行为 pre-existing，不在 accepted F1 scope 内
- **影响**: 无当前可触发数据丢失路径；仅留下"未获得 typed 失败 + backup 证据缺失"的健壮性缺口
- **建议改法和验证点**: 后续 WU 把 `:768` 迁移为 `_lstat_optional_storage_path` 并按 S_ISDIR 分型；本次不改（F1 已按 adjudication scope 修复 begin_batch）
- **修复风险（低）**:
- **严重程度（低）**:

### 2-未修复-低-begin_batch 的 symlink/non-directory fail-closed 分支无专项测试

- **入口/函数**: `begin_batch`
- **文件(行号)**: `dayu/fins/storage/_fs_storage_infra.py:546-547`
- **输入场景**: target ticker 路径为 symlink 或 non-directory regular file 时 begin_batch
- **实际分支**: `target_stat is not None and not stat.S_ISDIR(...)` → `ValueError("batch target ticker root 必须为非 symlink 目录")`
- **预期行为**: 按 F1 adjudication，非目录、symlink 必须 fail closed
- **实际行为**: 生产代码正确 fail closed（staging 清理 + writer 释放），但全仓无针对该分支的测试（grep `batch target ticker root` 在 tests/ 零命中）；F1 的 EACCES/EIO regression 已按要求补，本分支仅靠代码走读保证
- **直接证据**: `:546-547` 分支；`rg "batch target ticker root" tests/` 零命中
- **影响**: 该分支未来被重构破坏时无测试拦截
- **建议改法和验证点**: 补一个 target 为 symlink（或 regular file）时 begin_batch 抛 ValueError 且 published tree 不变的测试
- **修复风险（低）**:
- **严重程度（低）**:

## Open Questions

- `test_upload_material_failure_uses_shared_typed_failure_owner` 中 material pipeline-direct 缺 `company_name` 场景最终投影为 `runtime/unexpected_runtime` 而非 `COMPANY_NAME_REQUIRED`。该分类属于 material admission 的既有行为（ingestion_runtime 静态校验在 pipeline 之上拦截，S2 未修改 material request 校验），且相对修复前的 `str(exc)` 泄漏是纯改进；是否需要 material 侧也产生 name-required usage failure 不属于本 WU 判断范围，留待 controller 确认是否另立。

## Residual Risk

- 全仓非 Fins 测试未运行（Host/Engine 未修改；与 fix artifact 声明一致）。
- filesystem 权限测试为 monkeypatch 注入 errno，真实 ACL/NFS 跨平台行为未验证（artifact 已声明）。
- identity route/commit 每次全量扫描 workspace corpus，无真实规模性能证据（plan 已分类 later work）。
- UF-PF05 真实 CLI evidence 未执行（用户明确排除）。
- finding 1/2 为低严重度健壮性/测试覆盖缺口，不阻断本 gate；若 controller 选择 defer，需记录 owner 与完成 gate。
