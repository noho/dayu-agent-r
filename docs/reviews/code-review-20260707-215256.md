# Code Review

## Scope

- Mode: current changes
- Branch or PR: `phase/host-issues-control`
- Base: current workspace diff
- Output file: `docs/reviews/code-review-20260707-215256.md`
- Included scope: aggregate deepreview for the full work unit diff and validation evidence.
- Excluded scope: unrelated repository files, historical review artifacts not used as current documentation truth.
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Adversarial Failure Pass

- Prompt 工具边界：`prompt.json` 删除 `"utils"` 后，`prepare_scene("prompt")` 不再选择 `get_current_time`，对应条件块不会进入 system prompt；CLI prompt submit request 也不再包含该工具。
- Interactive / WeChat 工具边界：`interactive` 与 `wechat` manifest 未修改，真实 scene asset 测试继续断言二者选择并渲染 `get_current_time`。
- 静态时间文本：`current_time()` 仍由同一 helper 生成，新增文本只说明模型应如何使用该时间，没有暴露 Host、run input、context slot、scene、tool selection 或模块名。
- 工具 description：description 明确了“调用这一刻”“明确要求此刻最新时间”“动作完成后再确认时间”“普通问题不调用”的边界，没有改变参数 schema 或执行函数。
- `fins_default_subject`：manifest、helper、scene placement 和相关断言未改变；prompt 仍要求并消费财报主体。
- README：`dayu/config/README.md` 和 `tests/README.md` 已按触发规则更新当前配置与测试职责，不回写历史 review artifact。

## Validation Reviewed

- `source .venv/bin/activate && pytest tests/runtime/test_scene_assets_migration.py tests/service/test_entrypoint_runtime.py tests/service/test_entrypoint_runtime_prompt_path.py tests/service/test_entrypoint_runtime_interactive_path.py tests/runtime/test_scene_prepare.py tests/cli/test_prompt_command.py tests/cli/test_interactive_command.py -q`
  - Result: 179 passed, 3 dependency deprecation warnings.
- `source .venv/bin/activate && python -m pyright dayu/ tests/ utils/`
  - Result: 0 errors, 0 warnings, 0 informations.
- `git diff --check`
  - Result: passed.

## Open Questions

无。

## Residual Risk

- 未运行真实 LLM 端到端 smoke；本 work unit 的核心风险是模型可见输入装配，已由真实 `ScenePrepare` 与 CLI / Service 入口组装测试覆盖。分类：accepted residual risk for local validation scope。

## Conclusion

pass
