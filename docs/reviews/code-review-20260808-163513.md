# F18 Slice 1 B1 Oracle 投影独立代码审查

- 审查模式：DeepReview Current Changes Mode
- 审查时间：2026-08-08 16:35:13 CST
- Selected base：`HEAD`（`2b14a6aeaf426ec84bf8dcf9e08e40ad2b302b33`）
- 审查范围：
  - `docs/cli_ci_scenarios.json`
  - `docs/cli_ci_oracles.json`
  - `docs/cli_ci.md`
  - `docs/reviews/pr-190-oracle-adjudication-20260808.md`
- 明确排除：产品实现、provider 调用、旧 evidence bundle 写入、B2 裁决与 readiness 提升、未跟踪且 ownership 不明的 plan review
- 结论：**PASS**

## Findings

未发现实质性问题。

## 审查证据

### 1. B1 用户裁决与语义 owner

`interactive.interactive.g06.tool-trace-formal@2` 是 B1 scenario 状态、evidence 指针、原始进程结果与适用时间的 owner；本次只把它从 `unadjudicated` 更新为 `accepted`，并指向 immutable bundle `pr190-formal-g1a2xu`。`cli.interactive.core-execution@2` 是相应 accepted oracle 与用户决定的 owner；本次新增一条 2026-08-08 user-decision，并同步 observed behavior 与 adjudication notes。handbook 和 adjudication report 只做同源的人类可读投影，没有另造 B1 事实或独立重算 verdict。

用户已确认的 mandatory evidence 在 scenario 中仍是三项：public Host Tool Trace resolver/analysis response identity、canonical terminal response identity equality、secret scan。公开 summary 直接记录 `hot_owner_canonical_equality=true` 与 `hot_owner_exact_comparison_count=6`；human report 明确列出 terminal sequence、operation id、attempt number、proposal manifest ref、proposal manifest digest、successful response identity 六字段 6/6 exact match。裁决投影与直接 owner evidence 一致。

### 2. Immutable evidence identity

对 immutable bundle 中六个公开文件重新计算 SHA-256，结果与 scenario/adjudication 记录逐项一致：

| Evidence | SHA-256 | 核验 |
| --- | --- | --- |
| `evidence/public/observed-behavior-report.md` | `de7ee64de11140add816facf9926c2cf17aa13a4176bd873bc0f91ed20b70f79` | exact |
| `evidence/public/observation-summary.json` | `dfe3604bba8c7f8bda6b0d8a80639a87ac77a2d1c03f0e7baa28236e554d7c0a` | exact |
| `evidence/public/b1-cold-tool-trace/tool-trace-analysis.json` | `f1b162c185c977bdb24882cdc7b1eb8d273defe02cfa1dbf920248d07754ec3a` | exact |
| `evidence/public/b1-cold-tool-trace/tool-trace-analysis.md` | `2bba07d27ec3307a44cc0d6531c73cb7565bcd72eea7304c7f56d1e0961c41da` | exact |
| `evidence/public/digest.json` | `567d5539d9e745e78379093117275231c7b96ab5ac03536f13aec9475e476153` | exact |
| `evidence/public/secret-scan.json` | `6f33e30ced5dbbcc96c6cadcc93bf1c5c2ad3d261c5f3386ccbec74c3ba52ba8` | exact |

`digest.json` 的 `dayu.s4.real_provider_evidence.v1` manifest 又逐项固定 human report、summary、cold JSON 与 cold Markdown；digest 与 secret scan 自身 SHA 由 scenario/adjudication 固定。`secret-scan.json` 直接记录 `status=complete`、`validation_errors=[]`、`secret_scan.hits=[]`、`path_hygiene.violations=[]`。本次审查只对 bundle 做只读读取与散列核验，没有改写旧 bundle 或新 bundle 内容。

### 3. 原始进程结果与 canonical Host truth 正交

scenario 原样记录 raw PTY `execution_outcome=error`、`exit_codes=[1]`，没有为了 accepted verdict 改写为成功。human report 同时记录 child 在 cleanup EOT 后退出 1，以及 canonical required Run 为 10 个 typed `RUN_SUCCEEDED`。adjudication report 明确说明 raw process outcome、canonical terminal truth、evidence sufficiency 与 Oracle conformance 是正交维度。因此 accepted verdict 没有从 exit code 反推，也没有掩盖原始失败。

### 4. Limitation 与 mandatory gap 分离

公开 summary 直接记录 `cold_compactor_response_count=0`、`cold_identity_disposition=needs-more-evidence`、`provider_request_id_availability=unavailable`。cold analyzer 的 typed limitations 进一步给出 `hot_store_unavailable`、`payload_resolution_unavailable` 与 `provider_request_id_unavailable`，没有用顺序、字符串或私有数据库补偿。scenario、oracle、handbook 与 adjudication report 均保留这些 limitation/residual question，同时没有把它们误写成该 scenario 的 mandatory readiness gap，符合用户 B1 裁决。

### 5. Scope、B2 与 readiness 不变量

对 HEAD 与工作树做 canonical JSON 投影比较：

- scenario 顶层 `schema_version`、`registry_status`、`readiness_proof` 完全相同，canonical SHA-256 均为 `f432d3cd112590d2b11f6a5eb944a6d874063b9f6dce1279c0e10f1db45566c6`；
- `cap-constrained-memory-replacement@1` 整体对象完全相同，canonical SHA-256 均为 `54bccd449790bd1d9866555cd620a573fa03fea08da1bc6544a85d9909dc943d`；
- 排除 `tool-trace-formal@2` 后的全部 scenario 完全相同；
- oracle 顶层 `schema_version`、`registry_status` 完全相同；排除 `cli.interactive.core-execution@2` 后的全部 oracle 完全相同。

B2 仍为 `unadjudicated`，用户裁决 identity 仍为 pending，`applicable_from` 仍为 adjudication 后；两个 registry 仍为 `calibration`，overall readiness 没有提前标 ready。没有修改 Issue 192 duplicate-governance 行为、Fins tool schema 或 cold analyzer。

### 6. 路径与发布边界

对四个目标文件的新增 diff 行做私有绝对路径扫描，无 `/Users/`、`/home/`、Windows drive path 或 `.dayu-cli-ci/` 命中；F18 B1 新引用全部是 bundle-relative public refs。`docs/reviews/pr-190-oracle-adjudication-20260808.md` 第 29 行仍存在 HEAD 已有的 F15 绝对 Root，它不是本次 diff 引入或修改的内容，因此不构成 Current Changes finding；若后续做历史文档净化，应由独立 scope 处理。

## Adversarial failure pass

- 伪造成功：未发生；raw error/exit 1 保留。
- 仅凭 report 文本接受：未发生；summary、cold typed analysis、digest manifest 与 secret/path scan 均独立核验。
- SHA 传递错位：未发生；六个文件实算值、manifest child 值与 registry/report 值一致。
- B1 acceptance 污染 B2：未发生；B2 object exact equal。
- readiness 偷跑：未发生；readiness proof 与 registry status exact equal。
- limitation 被删除或升级为事实：未发生；cold/provider-id limitation 在四层投影中保留。
- 下游补偿语义：未发生；handbook/report 没有覆盖 scenario/oracle owner 的原始状态。
- 私有路径或 secret 新增：未发生；added-lines scan 与 public secret/path scan 均为零命中。
- 过度耦合或 semantic ownership drift：未发现；变更限定在 scenario owner、oracle owner 及其人类可读投影。

## 验证

- `jq empty docs/cli_ci_scenarios.json docs/cli_ci_oracles.json`：通过。
- `git diff --check -- <四个目标文件>`：通过。
- HEAD/工作树 canonical JSON equality checks：通过。
- 六个公开 evidence 文件 `shasum -a 256`：全部 exact。
- digest manifest child SHA comparison：全部 exact。
- secret/path scan typed fields：complete，零 hit/violation，validation errors 为空。
- added-lines absolute private path scan：零命中。
- 未运行 provider；纯文档/registry 变更不需要产品测试、pyright、Ruff 或 compileall。

## Open Questions

无阻断问题。

## Residual Risk

1. 本审查证明的是当前本机 immutable bundle 与登记 SHA 的一致性；bundle 的跨环境可获取性仍由 handbook 的 external publication 流程负责。
2. cold analyzer 仍不能解析 compactor response identity，provider-native request id 仍不可用；这是用户明确保留的 residual evidence question，不影响 B1 mandatory verdict。
3. B2 仍未完成 fresh formal observation 与用户裁决；整体 readiness 继续保持非 ready。
4. adjudication 文档中 HEAD 已有的 F15 绝对 Root 未在本 slice 改动；本次 F18 B1 新增内容没有绝对私有路径。

## 最终结论

**PASS**。本次变更准确投影了用户对 B1 的 accepted 裁决，immutable evidence identity、raw/canonical 正交关系和 limitation 均被保留；变更没有扩展到 B2、readiness、产品实现或 analyzer enhancement。
