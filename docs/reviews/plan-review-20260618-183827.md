# Focused Re-Review: WU-CM-12-FIX-R1 Accepted Findings Closure

- **Reviewer**: AgentMiMo
- **Review date**: 2026-06-18
- **Review type**: focused re-review — verify accepted findings are closed
- **Reviewed artifact**: `docs/host/host-issues/wu-cm-12-fix-r1-material-guard-plan.md`
- **Original reviews**: `docs/reviews/plan-review-20260618-182749.md` (AgentDS), `docs/reviews/plan-review-20260618-182916.md` (AgentMiMo)
- **Branch**: `wu-cm-12-conversation-memory-drift`

## Scope

Only verify the following four accepted findings are closed in the current plan text. Do not re-review architecture, overcoupling, or other dimensions.

---

## Finding 1: Slice 2 mapping for `build_pre_dispatch_compact_material_view` is explicit

**Original**: FS-1 (AgentDS), F1 (AgentMiMo) — underspecified integration mapping.

| Closure Item | Status | Evidence |
|---|---|---|
| `current_display_text=current_facts.user_prompt` explicit | ✅ | §5 item 3: "`current_display_text` 必须来自 `current_facts.user_prompt`"; Slice 2 第 3 条重复 |
| Filtering by `CompactMaterialBlockKind.ACCEPTED_TOOL_EVIDENCE` | ✅ | §5 item 3: "从返回 view 的 `material_blocks` 中只选择 `kind == CompactMaterialBlockKind.ACCEPTED_TOOL_EVIDENCE` 的 whole blocks" |
| Represented refs second filter via `_represented_evidence_refs(memory, compact)` | ✅ | §5 item 3: "再计算 `represented_refs = _represented_evidence_refs(memory, compact)`，把 represented refs 作为第二道 whole-block filter" |
| EventLog-backed material view declared authoritative | ✅ | Slice 2 第 5 条: "新的 EventLog-backed material view 是 accepted evidence material 的权威路径" |
| No requirement to keep old direct-SQL path for comparison | ✅ | Slice 2 第 5 条: "删除旧 direct-SQL builder 后，不为新旧字段对比保留旧路径" |
| Tests validate key fields / provenance / represented refs exclusion | ✅ | Slice 2 测试要求: explicitly lists `text`/`raw_result_text`, `readable_tool_name`, `readable_query_text`, `accepted_evidence_id`, `canonical_source_refs`/provenance refs, "represented refs 命中的 evidence 被 whole-block 排除" |

**Verdict**: **Closed.**

---

## Finding 2: Chunk helper ambiguity closed

**Original**: FS-2 (AgentDS), F2 (AgentMiMo) — tension between retain-as-budget-helper vs. delete-if-no-caller.

| Closure Item | Status | Evidence |
|---|---|---|
| `_evidence_chunks` deleted when no production caller | ✅ | Slice 1 第 5 条: "如果 `_evidence_chunks`、`_EvidenceChunk`、`evidence_chunk_label` 与 `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` 没有生产调用方，必须删除" |
| `_EvidenceChunk` deleted | ✅ | Same clause |
| `evidence_chunk_label` deleted | ✅ | Same clause |
| `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` deleted | ✅ | Same clause |
| No dead explicit budget helper retained or renamed | ✅ | Same clause: "不得重命名后保留为未接入的显式 budget chunk helper" |

**Verdict**: **Closed.** Original tension resolved — plan mandates deletion.

---

## Finding 3: Former chunking test migration assertions are concrete

**Original**: FS-3 (AgentDS) — migration test may miss chunk ordinal / provenance / digest assertions.

| Closure Item | Status | Evidence |
|---|---|---|
| Single E1 | ✅ | Slice 1 测试要求: "仍是单个 `E1` evidence item" |
| Full text / response | ✅ | Slice 1 测试要求: "`response_text` 完整，不生成默认 `E1.1` / `E1.2`" and "`raw_result_text` / vNext `response_text` 保持全文" |
| Full digest | ✅ | Slice 1 测试要求: "`content_digest` 基于全文" |
| Provenance / payload refs | ✅ | Slice 1 测试要求: "payload / provenance refs 保留" |
| No E1.1 / E1.2 | ✅ | Slice 1 测试要求: "没有 `E1.1` / `E1.2` label" |
| No chunk_parent_label / chunk_ordinal semantics | ✅ | Slice 1 测试要求: "没有 `chunk_parent_label` / `chunk_ordinal` 语义残留" |

**Verdict**: **Closed.**

---

## Finding 4: Performance residual is deferred

**Original**: F3 (AgentMiMo) — performance regression risk without specific owner.

| Closure Item | Status | Evidence |
|---|---|---|
| Deferred to future Host material source performance hardening WU | ✅ | §8 risk #2: "deferred-with-owner 到未来 Host material source performance hardening WU" |
| Explicitly not WU-CM-13 | ✅ | §8 risk #2: "WU-CM-13 是 reactive compact recovery owner，不是该性能 hardening owner" |
| Does not reintroduce private row limit / page correctness cap | ✅ | §8 risk #2: "本 WU 不为性能风险重引入私有 row limit 或 page correctness cap" |

**Verdict**: **Closed.**

---

## Conclusion

**Pass.**

All four accepted findings from the original reviews are closed in the current plan text. No new material issues found in the focused scope. Plan remains code-generation-ready.
