# WU-CM-08-S2 Code Review — AgentDS

## 结论：PASS

无 findings。

## 审查范围

Branch: `work/cm-05-06-08-09`
Dirty workspace diff:

- `tests/host/test_public_compact_smoke.py`
- `docs/host/issues-implementation-control.md`

Plan: `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md`

## 审查维度与结论

### 1. S2 保持 test-only，无生产语义/schema/API 变化

- `git diff HEAD --name-only` 仅触碰 `tests/host/test_public_compact_smoke.py` 和 `docs/host/issues-implementation-control.md`。
- 零生产代码修改。无 schema、public API、memory semantic model 变化。

### 2. 扩展/拆分已有 `_assert_compactor_material_instruction_contract`，无新增重叠并行 helper

- `_assert_compactor_material_instruction_contract` 被扩展为调用两个子 helper：
  - `_assert_compactor_material_section_shape` — 校验顶层 section key、stale legacy key 排除、list section 类型。
  - `_assert_no_forbidden_compactor_material_terms` — 校验 LLM-facing material 未泄漏内部术语。
- 原有的 forbidden term 内联断言被移入 `_assert_no_forbidden_compactor_material_terms`，调用语义等价。
- 另两个独立 helper `_assert_material_evidence_contains_marker` 和 `_assert_label_only_fake_proposal` 覆盖 evidence material 和 proposal 边界，与 instruction contract helper 不重叠。

### 3. helper 正反例覆盖 section shape、stale key、forbidden internal terms、evidence marker、proposal canonical ref 泄漏

正例（1 个）：

- `test_compactor_material_assertion_helpers_accept_valid_material`：覆盖 instruction contract（含 section shape + forbidden terms）、evidence marker、label-only proposal 三条路径。

反例（4 个）：

- `test_compactor_material_section_shape_rejects_stale_legacy_key`：注入 `stable_input` stale key，匹配 `material pack / labels boundary`。
- `test_compactor_material_forbidden_terms_rejects_internal_text`：注入 `EventLog` 内部术语，匹配 `LLM-facing material boundary`。
- `test_compactor_material_evidence_marker_rejects_missing_marker`：构造无 marker 的 material，匹配 `evidence material marker boundary`。
- `test_label_only_fake_proposal_rejects_canonical_ref_leakage`：构造含 `payload:event-tool-result-accepted` 的 proposal，匹配 `compactor proposal boundary`。

覆盖完整。

### 4. helper 失败消息定位各边界

每个 helper 的 AssertionError 消息均包含明确的边界标识：

| Helper | 边界标识 |
|---|---|
| `_assert_compactor_material_section_shape` | `material pack / labels boundary` |
| `_assert_no_forbidden_compactor_material_terms` | `LLM-facing material boundary` |
| `_assert_material_evidence_contains_marker` | `evidence material marker boundary` |
| `_assert_label_only_fake_proposal` | `compactor proposal boundary` |

所有反例测试均通过 `pytest.raises(AssertionError, match=...)` 精确匹配对应边界消息。

### 5. 未修改 public_smoke_support，未引入共享维护规则，README 决策正确

- `tests/host/public_smoke_support.py` 未被触碰。
- 新增 helper 均为 `tests/host/test_public_compact_smoke.py` 模块级私有函数，compact-specific，不适合下沉到 `public_smoke_support`。
- `tests/README.md` 未更新 —— 未引入新测试层、命令、共享 helper 模块或维护规则，与 plan 决策一致。
- `dayu/host/README.md` 未触发 —— 无生产代码变更。

### 6. pyright / pytest / git diff --check 通过

- `pytest tests/host/test_public_compact_smoke.py -q`：11 passed, 1 skipped。
- `pytest tests/host/test_compact_material.py tests/host/test_public_compact_smoke.py -q`：46 passed, 1 skipped（S1 35 + S2 11，基线无回退）。
- `python -m pyright dayu/ tests/ utils/`：0 errors, 0 warnings, 0 informations。
- `git diff --check`：clean。

## 对抗性失效遍历

对每个变更点进行了 adversarial pass：

- `_valid_compactor_material_json` 返回值比旧 `_llm_material_with_long_tool_evidence` 多了 `schema_version`、`answer_material`、`instruction` 字段，且 `previous_compacted_view` 从 `[]` 改为 `None`。已验证：`fake_compaction_proposal_from_material_json` 的 `_previous_labels` 对 `None` 和 `[]` 均返回 `()`；`_answer_items` 对 `[]` 返回 `()`；`instruction` 和 `schema_version` 不影响 proposal 生成逻辑。`_llm_material_with_long_tool_evidence` 调用方（唯一）行为不变。
- 内联 `result_preview` / `payload:` / `event-tool-result` 检查（行 406-409）与 `_assert_no_forbidden_compactor_material_terms` 覆盖的 forbidden terms 集合正交，无冗余。
- `FakeCompactorRunAgent.__call__` 中 `_assert_compactor_material_instruction_contract` 现在额外调用 section shape 和 forbidden term 检查，使 fake compactor 路径更严格，无假阴性风险。
- `_assert_compactor_material_section_shape` 不校验 `previous_compacted_view` 的具体类型（可以是 `None`、空列表或对象），这是正确的 —— 不同 compaction 阶段其形状不同。
- `test_compactor_material_assertion_helpers_accept_valid_material` 中 proposal 的 `cast(JsonValue, json.loads(...))` 是延续了 WU-CM-05 residual 既有的 `cast` 模式，不属于 S2 引入的问题。

## 残留风险

无。
