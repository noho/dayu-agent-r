# Code Review

## Scope

- Mode: current changes（Gateflow aggregate deepreview）
- Branch or PR: `codex/upload-material-oracle`
- Base: `ade529a8`（本 work unit 起点；含 commit `72e5a370` plan 系列与 `ee46801e` S1）
- Output file: `docs/reviews/code-review-20260925-203518.md`
- Included scope: `dayu/fins/downloaders/cninfo_downloader.py`、`tests/fins/test_cninfo_downloader.py`、
  `docs/gateflow/cninfo-empty-announcement-parse-plan-20260925.md`、
  `docs/gateflow/cninfo-empty-announcement-parse-s1-implementation-20260925.md`、
  `docs/reviews/plan-review-20260925-194055.md`、`docs/reviews/plan-rereview-20260925-194255.md`、
  `docs/reviews/code-review-20260925-195434.md`，以及本 work unit 的运行时验证证据（CLI 复验输出、
  source integrity inventory、目标文档 meta 对账）。
- Excluded scope: `ade529a8` 之前分支携带的其它 work unit 提交（upload-material / docling 工作流，各有自己的
  review 链）；消费方 `portfolio-manager-v2` 仓库内容（任务书禁止修改）；gitignored 取证脚本
  `workspace/tmp/probe_*.py`。
- Parallel review coverage: 无（范围小，主 reviewer 全量走读；S1 slice code review 已逐行走读解析路径，
  本轮以 work-unit 级一致性与验证证据为主并复核关键代码路径）

## Findings

未发现实质性问题。

Aggregate 走读要点（含对 slice review 的独立复核）：

1. **plan ↔ 实现一致性**：实现与 plan §7.2 三分支设计逐条一致（缺 key fail-closed / null=空页 / list 照旧 /
   其它类型协议失败），位置、范围、禁止事项无偏差；无 goal drift（诊断增强与 HK 同型检查均按 plan 留在
   deferred，未越权实现）。
2. **代码路径复核**（`cninfo_downloader.py:471-490`）：四分支互斥；`False`/`0`/`""`/`{}` 等 falsy 非法值仍抛
   协议失败（无 loose falsy parsing）；null 与 `[]` 同路径 `break` 保持既有分页语义；判定只消费
   `announcements` 字段本身。AGENTS.md 约束核对通过（中文 docstring、类型签名、无魔法值扩张、owner 边界修复）。
3. **测试证明力**：4 组新测试覆盖事故回归（FY 摘要+正文双公告 + H1/Q1/Q3 null → 唯一 FY2024 正文候选）、
   null 空页、缺 key、非 list 非 null；断言 owner 级 contract（异常类型/transport/retryable/安全文案）。
   `tests/fins` 全量 2124 passed（1 failed 为沙箱禁 AF_UNIX bind 的环境伪失败，非沙箱通过，与本改动无交集）；
   `pyright` 两文件 0 errors。
4. **运行时验证证据一致性**（acceptance 逐项对账）：
   - 复验命令与事故同参（仅新日志名），退出码 0；
   - 终态 `discovered=1 downloaded=0 skipped=1 rejected=0 failed=0 omitted=0`，disposition=skipped、
     reason=`integrity_complete`（本地 source 完整，跳过远端传输）；
   - **跳过本身不是恢复证据**：恢复证据是 discovery/selection 走通（discovered=1，来源为巨潮实时响应中
     经解析的 FY 候选）+ 下方元数据核验全项一致；
   - 目标文档 `fil_cn_95d26c810c326725ece2cc478a6a4c012fe1c9ce`：ticker=000333、company=美的集团、
     form FY、fiscal_year=2024、filing_date=2025-03-28 ∈ [2025-01-01, 2025-03-31]、`ingest_complete=true`、
     `_published_source_revision=7d79bbc27992444fb50380c122669116`（与 inventory revision 一致）、
     `source_fingerprint`/`pdf_sha256` 与磁盘重算 SHA-256 双文件一致、`primary_document` 取自 meta；
   - `Fins missing periods: "H1,Q1,Q3"` 为既有 missing 语义 + 窗口内无此类报告的业务事实（plan §9 已钉死
     解释口径，非缺陷、非未解释缺失）。
5. **环境阻塞（第二根因）处置核对**：workspace 中 Q1 2026 filing 目录被工具残留 `.claude/.cc-writes`（0 文件）
   污染 → storage owner 判 `unsafe_filesystem_entry` → 预检拒绝整个 ticker。经用户点名授权后删除该非来源/
   非元数据残留，来源文件与 meta 未动，inventory 7/7 恢复 complete。该动作在任务书“修复运行环境问题”授权范围内，
   并保留了完整证据链（分类 inventory 前后对照、目录清单）。
6. **semantic ownership drift 复核**：修复位于 wire→领域语义唯一 owner；无下游补偿、无测试固化旧行为；
   空结果双编码归一与本模块既有 `_format_announcement_date` 双形态先例一致。

## Open Questions

- 无。

## Residual Risk

- 本次为 skip 路径验收：全新 PDF 下载 + docling 落盘链路未在本窗口端到端重跑（既有 target 完整，删除重下
  违反“不手工触碰”约束）；该链路由 `tests/fins` 既有集成测试与本次不受影响的 `download_report_pdf` 单测覆盖。
  【分类：assigned to later work unit（仅当需要端到端下载演练时）】
- `SourceIntegrityPreflightError` 等 storage 预检异常被 producer 兜底投影为 `classification=execution`
  “下载执行失败”，掩盖真实原因（本次排障实证其代价）；属既有可观测性缺陷，非本 change 引入。
  【分类：requiring new issue or explicit user decision，owner：Dayu 维护方】
- 其它 ticker / 其它 filing 目录可能存在同类工具残留污染（本次仅核验 000333；消费方 CMB/YUMC 尚未下载）。
  【分类：assigned to later work unit（消费方 fixture-refresh 排障时按同口径处理）】
- 巨潮空结果编码若未来改为省略 `announcements` key → 再次协议失败（fail-closed 设计使然）。
  【分类：fixed in current slice 的已知残留边界，接受】
- `announcements: null` 与 `hasMore=true` 的矛盾响应会静默截断翻页（与 `[]` 同语义，实测未出现）。
  【分类：assigned to later work unit，仅当出现证据】
