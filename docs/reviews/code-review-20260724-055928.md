# Code Review

## Scope

- Mode: current changes
- Branch: feat/wu-ctx-01
- Base: b6f297b4
- Output file: docs/reviews/code-review-20260724-055928.md
- Included scope: dayu/host/ (production), dayu/service/entrypoint_runtime.py (production), dayu/host/README.md, dayu/service/README.md, tests/host/, tests/cli/, tests/service/ — all uncommitted production/test/README changes relative to b6f297b4
- Excluded scope: docs/host/issues-implementation-control.md (Controller-owned), docs/reviews/wu-ctx-01-slice-2-implementation-codex.md (implementation artifact), other existing review artifacts
- Parallel review coverage: context_events.py canonical schema/strict-parser/idempotent-append/conflict-fail-closed (subagent 1), dispatch.py ordering/CAS/rollback (subagent 2), recovery.py + engine_ingest.py startup/continuation/reactive (subagent 3), waiting.py + read_api.py wait-resume/public-projection (subagent 4). Main reviewer covered admission.py steer path, entrypoint_runtime.py Service projection, pyright, test execution, and cross-cutting integration verification.

## Reference Documents Read

- AGENTS.md (project instructions)
- docs/host/design.md §25 Context Governance (lines 3556-3855)
- docs/reviews/wu-ctx-01-plan-codex.md §5.3.1, §5.5-§5.7, §6.5, §8.3
- Implementation artifact (excluded from scope but read for context)

## Findings

未发现实质性问题。

### Detailed Evidence Summary

以下是对用户指定的八个 adversarial 检查维度的逐项证据报告：

#### 1. CONTEXT_BUDGET_EVALUATED canonical schema / strict parser / deterministic identity / idempotent append / conflict fail-closed

**schema**: `context_events.py:54-78` 定义 `_BUDGET_EVALUATED_FIELDS` 为22字段 tuple。`_require_exact_fields` (line 1509-1526) 先检查所有必填字段存在，再检查无意外字段，隐式强制字段计数。

**strict parser**: `parse_context_budget_evaluated_payload` (line 305-471) 对每个字段做类型/范围校验（`_required_text`/`_required_digest`/`_required_enum`/`_required_non_negative_int`/`_required_positive_int`），并重新推导 identity digest (line 406-407)、utilization (line 415-418，整数除法无浮点漂移)、pressure 与 decision (line 419-428，调用 `context_sizing_pressure_and_decision` 纯函数)。对 CONSERVATIVE_FALLBACK (line 429-435) 要求 fallback_reason 非空、anchor_diagnostic 为空、predicted==conservative；对 USAGE_ANCHORED (line 436-445) 要求 fallback_reason 为空、anchor_diagnostic 非空且内部字段一致。else 分支为 AssertionError 防御。

**deterministic identity**: `context_budget_evaluated_decision_id` (line 206-228) 使用7个 str/int 原子（run_id, candidate_input_cursor, candidate_input_digest, sizing_stage.value, policy_snapshot_digest, estimator_id, estimator_version）通过 `sha256_digest_json`（`sort_keys=True, separators=(",",":"), allow_nan=False`）生成。无时间戳、无随机值、无浮点。

**idempotent append**: `append_context_budget_evaluated_in_transaction` (line 474-555) 先按 deterministic event_id 读取；已存在时调用 `_require_matching_context_budget_row(expected=parsed)` 做 frozen dataclass 结构相等比较（line 735: `if expected is not None and parsed != expected`），同时校验 event_class/session_id/run_id/attempt_id/execution_id/event_type/actor/source/client_request_id/policy_decision_json/reason_json/payload_ref/payload_digest/idempotency_key/event_id。

**conflict fail-closed**: line 738-743 捕获 HostDurableError/TypeError/ValueError，`conflict=True` 时包装为 `HostEventIdentityConflictError` 并 `from exc` 链式抛出。

#### 2. ordinary/postcompact/fallback/reactive/startup/steer/wait/Engine continuation 的 manifest→fact→transition 同事务顺序与 CAS/precondition rollback

**ordinary allow** (dispatch.py `_commit_dispatch_candidate_in_transaction`): manifest (line 2765) → fact (line 2777, gated by `isinstance(plan, BudgetedDispatchStart)`) → start (line 2787)。在同一 `run_write` transaction。

**ordinary soft/hard** (dispatch.py `_evaluate_governance_in_transaction`): fact (line 1893) 在 compact/fail-closed 之前。ALLOW 返回早于 line 1893。

**post-compact/fallback hard** (dispatch.py `_prepare_and_commit_start_in_transaction`): fact (line 2546) → hard_threshold_closeout → fail_unstarted。ALLOW 走 `_commit_dispatch_candidate_in_transaction`。

**reactive recovery** (engine_ingest.py `_StartReactiveRecoveryOperation`): manifest (line 859) → fact (line 889) → start (line 899)。CAS miss 抛 `_ReactiveRecoveryStartCasMissRollback`，整笔 transaction rollback。

**startup recovery** (recovery.py `_start_recovery_dispatch_or_ready`): sizing_result from `_startup_continuation_budget_result` (line 833) → manifest (line 854) → rebuild sizing with new manifest cursor (line 869-895) → fact (line 901) → start (line 907)。HostDurableError → `_lose_unrecoverable_source` → LOST。

**steer** (admission.py `_create_steer_attempt_result`): sizing (line 3086) → manifest (line 3110) → rebuild sizing with manifest cursor (line 3122) → fact (line 3139) → RUN_STARTED (line 3149) → ATTEMPT_STARTED (line 3160)。同一 transaction。

**wait resume** (waiting.py `_resolve_resume`): source budget load (line 1268) → sizing (line 1303) → manifest (line 1326) → rebuild sizing (line 1337) → fact (line 1365) → resume transition (line 1375)。同一 transaction。

**Engine continuation** (engine_ingest.py `_append_limited_runner_call_manifest_event`): manifest (line 3752) → re-parse → sizing from frozen sources (line 3785) → fact (line 3802)。返回 `(manifest, fact)` 给 caller，caller 在同一 transaction 内追加 iteration link。

**CAS/precondition rollback**: `_StartCandidateCasMissRollback` (dispatch.py line 738) 由 `_commit_dispatch_candidate_in_transaction` 在 start 返回非 UPDATED 或缺 rows 时抛出（line 2793/2799）。`_evaluate_governance_in_transaction` (line 2104) 和 `_start_after_compact_accepted_in_transaction` (line 2440) 捕获并返回空结果。底层 CAS lost 以 `HostDurableError` 传播。测试 `test_budgeted_start_failure_rolls_back_candidate_fact_and_rows` 参数化覆盖 precondition 和 cas_lost 两种场景，断言零孤立写入。

#### 3. startup strict-load matching source manifest + source budget fact

`_startup_continuation_budget_result` (recovery.py line 1050-1141):
- sizing.status == UNAVAILABLE → return None（零 fact）
- sizing.status == COMPLETE 但任何 required atom 为 None → raise HostDurableError
- 调用 `load_matching_context_budget_evaluation_in_transaction` strict 匹配 identity + 所有 manifest/candidate atoms
- 检查 `fallback_reason is None` → raise HostDurableError
- 使用 `source_budget.soft_threshold_tokens` / `hard_threshold_tokens`（冻结自 source fact），不重读当前配置
- 调用方捕获 HostDurableError → `_lose_unrecoverable_source` → LOST

#### 4. Host 七字段 public view → Service typed pass-through

`read_api.py::_context_usage_activity` (line 1395-1429): strict parse payload → 投影 `HostContextUsageView` 七字段（predicted_input_tokens, context_window_size, utilization_basis_points, soft_threshold_tokens, hard_threshold_tokens, estimate_method, pressure_level）。不暴露 decision_id/candidate_input_cursor/sizing_stage/estimator_id/anchor_diagnostic 等内部字段。malformed payload → HostDurableError fail closed。

`api.py::HostContextUsageView` (line 3055-3118): `__post_init__` 校验非负/正数/阈值顺序/enum 类型。`HostActivityView` (line 3130-3194): kind==CONTEXT_USAGE 时 context_usage 必须非空、tool/counts 必须为空；其它 kind context_usage 必须为空。

`entrypoint_runtime.py::_entrypoint_context_usage_from_host` (line 2193-2219): 逐字段复制，不执行算术。exhaustive enum mapper `_entrypoint_context_estimate_method_from_host` (line 2222-2236) 和 `_entrypoint_context_pressure_from_host` (line 2239-2255) 对未覆盖成员抛 AssertionError。`EntrypointContextUsage` (line 180-252) 同样做 `__post_init__` 校验。

raw usage/policy/action/anchor 不泄漏：HostContextUsageView 只含7字段；`_entrypoint_context_usage_from_host` 只读 HostContextUsageView；test `test_submit_entrypoint_turn_maps_context_usage_without_recalculation` 断言逐字段相等且不重算。

#### 5. policy missing / failed-lost wait / unavailable continuation 零伪造 fact

**policy missing**: dispatch.py `_evaluate_governance_in_transaction` (line 1818-1842) 和 `_prepare_and_commit_start_in_transaction` (line 2509-2521) 使用 `NoBudgetDispatchStart`，`isinstance(plan, BudgetedDispatchStart)` 为 False → 不写 fact。测试 `test_no_budget_manifest_preserves_actual_dispatch_stage` 断言 `CONTEXT_BUDGET_EVALUATED not in event_types`。

**failed/lost wait**: waiting.py `_resolve_failed` (line 1428) 和 `_resolve_lost` (line 1505) 不调用 manifest/fact/attempt 创建，只执行 terminal transition。测试 `test_failed_lost_wait_add_no_budget_fact_manifest_or_attempt` 断言 before/after event count 不变。

**unavailable continuation**: engine_ingest.py `_continuation_frozen_sources` (line 6368-6373) 在 HostDurableError 时返回 `_unavailable_continuation_sources`（source_budget=None）。caller `_append_limited_runner_call_manifest_event` (line 3766-3767) 在 sizing 非 COMPLETE 时返回 `(manifest, None)`。

#### 6. CONTEXT_BUDGET_EVALUATED 与 context_budget 新预估算法是独立修改，Slice 2 只能 conservative

所有 producer 只调用 `build_conservative_context_sizing_result` 或 `build_conservative_context_sizing_result_from_atoms`，两者都强制 `estimate_method=ContextEstimateMethod.CONSERVATIVE_FALLBACK`。`ContextEstimateMethod` 只有两个成员：`USAGE_ANCHORED` 和 `CONSERVATIVE_FALLBACK`。parse_context_budget_evaluated_payload 的 USAGE_ANCHORED 分支 (line 436-445) 在 Slice 2 不会被触发，因为没有任何 producer 生成 USAGE_ANCHORED 方法的 payload。anchor_diagnostic 始终为 None。测试覆盖五个 stage 的 conservative fallback roundtrip。

#### 7. local durable imports 是否有直接循环证据

`context_events.py` 使用 `if TYPE_CHECKING:` (line 22-24) 导入 `EventLogRow` 和 `HostTransaction`，运行时不导入。`append_context_budget_evaluated_in_transaction` 内部 (line 492-495) 使用 lazy import `from dayu.host.durable.event_log import EventClass, EventLogAppendRequest`，注释说明 "api -> memory -> context_events 的初始化链早于 durable schema 完成"。`_require_matching_context_budget_row` 内部 (line 702-703) lazy import `EventClass` 和 `event_payload_object`。运行时 `from dayu.host.context_events import append_context_budget_evaluated_in_transaction` 验证通过（无 ImportError）。

这些 lazy import 有充分理由：context_events 是 context_budget 的下游，而 context_budget 被 api/memory 等早期初始化模块导入；如果 context_events 在模块顶层导入 durable primitives，会形成 api → context_budget → context_events → durable.event_log → durable.schema → api 的循环。lazy import 把实际加载推迟到首次 append 调用时，此时整个模块图已初始化完成。

#### 8. AGENTS 类型/docstring/README/coverage/testing 约束

**类型**: pyright 0 errors, 0 warnings, 0 informations（10个变更文件全部通过）。

**docstring**: 所有新增公共函数/类/方法均有完整中文 docstring，包含参数、返回值、异常说明。`context_events.py` 新增约790行，所有函数/类/dataclass 均有 docstring。

**README**: `dayu/host/README.md` 更新了 Context governance 段落（line 419）和 Context budget 与 compaction 段落（line 696），反映 Slice 2 新增的 manifest→fact→transition 顺序、startup strict-load、policy missing 行为和七字段 public view。`dayu/service/README.md` 更新了 `CONTEXT_USAGE` 通过同形 `EntrypointContextUsage` 逐字段复制的说明。

**coverage**: `tests/host/test_context_budget_evaluated.py` 新增10个测试覆盖：五 stage pressure/action roundtrip、unclamped utilization + public 七字段精确集合、deterministic append 幂等复用 + 矛盾 reject、source loader manifest atom mismatch reject、unknown stage fail closed + durable corruption fail closed、event identity frozen atoms only。`test_dispatch_scheduler.py` 新增3个参数化测试（budgeted allow 三 stage、budgeted hard 三 stage、budgeted rollback 两 failure kind）。`test_public_steer.py` 新增1个 steer hard continuation 测试。`test_resolve_wait_command.py` 新增2个参数化测试（completed/cancelled budgeted resume、failed/lost zero-fact）。`test_recovery_scan.py` 新增2个测试（startup recovery reuse source budget、source budget missing/mismatch fail closed）。`test_engine_ingest_mapping.py` 更新6个现有测试适配新 event 顺序和 budget fact 断言。`test_entrypoint_runtime.py` 新增1个 Service 投影测试。`test_activity_renderer.py` 新增1个 CLI formatter 兼容测试。`test_lifecycle_events.py`、`test_durable_schema.py`、`test_package_exports.py` 各更新1个断言。

**producer 顺序与零残留**: 所有7类 producer（ordinary/postcompact/fallback/reactive/startup/steer/wait/Engine continuation）的 manifest→fact→transition 顺序均有测试覆盖。rollback 测试验证零孤立写入。failed/lost wait 测试验证零 fact/manifest/attempt。

## Open Questions

无。

## Residual Risk

1. **`context_sizing_pressure_and_decision` 和 `build_conservative_context_sizing_result_from_atoms` 无独立单元测试**：这两个新增公共函数通过 test_context_budget_evaluated.py 的 roundtrip 测试、test_dispatch_scheduler.py、test_resolve_wait_command.py、test_recovery_scan.py、test_engine_ingest_mapping.py 间接覆盖。risk 低，但如果未来有人修改 `_pressure_and_decision` 内部逻辑而不更新集成测试，可能引入回归。建议在后续迭代中补充独立单元测试。

2. **`HOST_SCHEMA_VERSION` 23→24 无迁移脚本**：`CONTEXT_BUDGET_EVALUATED` 事件使用现有 `event_log` 表的 `payload_json` inline 存储，不需要 DDL 变更。schema version bump 仅用于标识新 event type 的存在。如果未来有需要按 schema version 做数据迁移的场景，需注意24版本的含义。

3. **`ContextSizingResult.fallback_reason` 类型为 `ContextSizingFallbackReason`（非 optional），但 `ContextBudgetEvaluatedPayload.fallback_reason` 类型为 `ContextSizingFallbackReason | None`**：builder 始终设置 `result.fallback_reason.value`（line 298），parse 时 `_optional_enum` 允许 None（用于未来 USAGE_ANCHORED 路径）。Slice 2 中 fallback_reason 始终非空，类型差异无实际影响，但 Slice 3 实现 anchor 路径时需注意此过渡。

## Verdict

**pass**
