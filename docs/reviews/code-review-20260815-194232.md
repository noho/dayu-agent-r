# Code Review

## Scope

- Mode: current changes（Gateflow UF-FIX07 Slice 1 code review re-review，针对
  `docs/gateflow/uf-fix07-slice1-review-fix-20260815.md` 的修复后复查）
- Branch: `codex/upload-filing-oracle`
- Base: `64050349`（accepted plan commit checkpoint）
- 前序 artifact: `docs/reviews/code-review-20260815-193342.md`（本 reviewer 第一轮）
- Output file: `docs/reviews/code-review-20260815-194232.md`
- Included scope:
  - `dayu/fins/ingestion_runtime.py`（本轮 delta：`_normalize_fins_upload_path` 的 typed catch）
  - `tests/fins/test_fins_ingestion_runtime.py`（本轮 delta：loop 双入口零副作用测试 + delete+101 precedence 测试）
  - 其余 Slice 1 文件复查确认无新 delta：`dayu/fins/upload_format_contract.py`、`tests/fins/test_fins_service_runtime.py`、`tests/fins/test_upload_format_contract.py`
  - Gateflow artifacts：`uf-fix07-slice1-review-fix-20260815.md`、`uf-fix07-slice1-implementation-20260815.md`（更新后）
- Excluded scope: 同第一轮（Slice 2/3/4 内容、oracle/scenario/frozen evidence、Host/Engine/runtime/storage 生产代码均未改动，已核实 `git status --short` 只含 6 个 Slice 1 文件与 2 份 review artifact）
- Parallel review coverage: 无（未参考另一 reviewer 结论，独立复查）
- 独立验证结果:
  - `pytest tests/fins/test_upload_format_contract.py tests/fins/test_fins_ingestion_runtime.py tests/fins/test_fins_service_runtime.py -q` → `319 passed, 3 warnings in 4.83s`
  - `pyright`（同 5 文件）→ `0 errors, 0 warnings, 0 informations`
  - `coverage report --include='dayu/fins/ingestion_runtime.py,dayu/fins/upload_format_contract.py'` → 88% / 88%
  - `git diff --check` → 无输出，exit 0
  - 均与 fix artifact 声称一致。

## 修复复查结论（按复查指令逐项）

| 复查项 | 结论 | 直接证据 |
| --- | --- | --- |
| OSError/RuntimeError 由正确 owner 映射 | 是 | catch 位于 `_normalize_fins_upload_path`（`ingestion_runtime.py:979-982`），即 plan §5 指定的 static admission 规范化 owner；全仓库仅此一处 `except (OSError, RuntimeError)`（rg 确认），files（line 1101）与 selectors（line 1103）共用同一映射点，无第二套分类 |
| 只暴露安全 basename | 是 | `file_name=path.name`（line 982）进入既有 `fins_upload_usage_failure`，其 guard（`ingestion_runtime.py:842-851`）强制 basename 无路径分隔符且等于 `Path(name).name` 后才格式化 `上传文件不存在：{file_name}`；测试断言 message 精确等于 `上传文件不存在：loop.pdf` 且不含 `tmp_path` |
| files 与 selector symlink-loop 均 typed fail | 是 | `test_filing_symlink_loop_is_typed_file_not_found_before_all_side_effects`（`test_fins_ingestion_runtime.py:1702-1751`）parametrize `loop_is_selector=(False, True)`，两分支均断言 `FILE_NOT_FOUND` 与精确 message |
| 零副作用 | 是 | 同一测试经 `runtime.start_upload` 走 guarded runtime：`state_repository.calls == []`、`executor.operations == []`、`runner.requests == []`、`runtime._observations == {}`、workspace tree SHA 快照不变、无 jobs 目录、无 portfolio |
| delete+101 → TOO_MANY_FILES precedence 锁定 | 是 | `test_filing_delete_over_raw_limit_precedes_files_rejection`（`test_fins_ingestion_runtime.py:1672-1699`）：101 个 raw files + 1 个 selector + `action="delete"`，经 `_filing_upload_request_identity` 精确断言 `TOO_MANY_FILES / --files 数量不能超过 100 个`；证明 raw count gate（line 1069）先于 delete files/primary 分支（line 1092-1096） |
| 过宽 catch | 否 | try 块只包裹 `path.expanduser().resolve(strict=False)` 两个库调用，无项目代码；`RuntimeError` 在该调用内的唯一来源是 pathlib symlink-loop 检测与 home 目录确定失败，`OSError` 为跨平台防御；`KeyboardInterrupt`/`SystemExit` 不受影响 |
| 误分类 | 否（相对 base 无回归） | 第一轮已实测：symlink loop 在 base 实现下 `exists()` 返回 False → 同一 `FILE_NOT_FOUND` code/message；本修复恢复同一分类。loop 作为 selector 时 message 指向 selector 的 raw basename，语义可行动 |
| 新回归 | 未发现 | catch 只改变 `_normalize_fins_upload_path` 的行为（唯一调用方是 static admission），其余 316 个既有测试保持绿色（本轮 319=316+3 新增） |

## Findings

### 原 review finding 的 Gateflow 最终状态

| 来源 | Finding / residual | Gateflow final status | 复核证据 |
| --- | --- | --- | --- |
| DS（第一轮） | Finding 1：normalize 的 OSError/RuntimeError 逃出 closed usage contract | `已修复` | catch 落在正确 owner、单一映射点、双入口 typed fail、basename 安全、零副作用，全部由 owner test 固化并经本轮独立运行验证 |
| DS（第一轮） | R3：delete+101 precedence 无测试固化 | `已修复` | `test_filing_delete_over_raw_limit_precedes_files_rejection` 精确断言 code+message，独立运行通过 |
| DS（第一轮） | R1：Slice 2 前 LLM-facing 文案窗口期 | 维持 `covered by later approved slice`（非本 gate 闭集项） | 旧文案仍在 `upload_format_contract.py:574`，属 plan §8 Slice 2 分配 |
| DS（第一轮） | R2：Slice 3 测试残留旧 constructor | 维持 `covered by later approved slice`（非本 gate 闭集项） | 残留引用位置不变，本轮未新增 |
| DS（第一轮） | R4：真实 evidence 未执行 | 维持 `assigned to later work unit`（非本 gate 闭集项） | 禁令未解除 |
| DS（第一轮） | R5：helper 无重复 input invariant | 维持 `accepted boundary` | docstring 明示 invariant，唯一 caller 先判重，本轮未改动 |

本轮新增 finding：**无**。未发现新 blocker。

## Open Questions

- 无 blocking open question。
- 一个非阻塞方向性观察（供主控知晓，不要求修复）：normalize 阶段失败（loop）现在以 `FILE_NOT_FOUND` 在 selector cardinality/membership 之前触发，而 exists 阶段缺失文件按 plan §6.2 点 9 位于 selector 检查之后——即同类“文件缺失”错误在不同失败阶段有不同的相对优先级。两分支均为 closed typed error；与 base 行为一致（base 对 loop 同样先于一切 selector 语义产生 `FILE_NOT_FOUND`，且 base 无 selector 概念）；plan 冻结的 precedence 表从未覆盖 normalize 失败。若后续要显式化，属于 plan amendment 而非当前修复缺陷。

## Residual Risk

- R1/R2/R4/R5 维持第一轮分类（见上表），无新增未分类风险。
- 跨平台边界：`OSError` 分支在当前 darwin/Linux 上实际很少由 `resolve(strict=False)` 抛出（realpath 会吞掉大部分 EACCES 等错误），主要面向 Windows 防御；若未来在 Windows 上把权限错误映射为“文件不存在”引起困惑，属可后续观察项，当前无证据表明会发生。
- 未执行 UF-PF07/UF-PF12 与真实 CLI evidence；全量测试集在 Slice 3 前不可绿（R2），属 accepted plan 的 slice 中间态，merge 策略仍需主控注意。
