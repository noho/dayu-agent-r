# Code Review — WU-CTX-01 Slice 2 Implementation Re-Review

## Scope

- Mode: current changes（re-review，仅审查当前 diff 与 fix）
- Branch: `feat/wu-ctx-01`
- Base: `b6f297b4`（accepted Slice 1 protected commit）
- Output file: `docs/reviews/code-review-20260724-060842.md`
- Included scope:
  - 所有 production diff（`dayu/host/` + `dayu/service/entrypoint_runtime.py`）
  - 所有 test diff（`tests/host/` + `tests/cli/` + `tests/service/`）
  - Host/Service README diff
  - 本次唯一 fix：`tests/host/test_durable_schema.py`（CTRL-S2-IMPL-01）
- Excluded scope:
  - `docs/host/issues-implementation-control.md`（Controller-owned control doc）
  - `docs/reviews/wu-ctx-01-slice-2-implementation-codex.md`（implementation artifact）
  - `docs/reviews/code-review-20260724-055928.md`（AgentMiMo 初审 artifact）
  - `docs/reviews/code-review-20260724-055648.md`（AgentDS 初审 artifact）
  - `docs/reviews/wu-ctx-01-slice-2-implementation-review-controller-adjudication.md`（Controller adjudication artifact）
  - `docs/reviews/wu-ctx-01-slice-2-implementation-review-fix-codex.md`（fix artifact）
  - 其他既有 review artifacts
- 审查依据：
  - 根 `AGENTS.md`
  - accepted plan `docs/reviews/wu-ctx-01-plan-codex.md` §8.3
  - 初审 artifacts（AgentMiMo `code-review-20260724-055928.md`、AgentDS `code-review-20260724-055648.md`）
  - Controller adjudication `wu-ctx-01-slice-2-implementation-review-controller-adjudication.md`
  - fix artifact `wu-ctx-01-slice-2-implementation-review-fix-codex.md`

---

## 1. CTRL-S2-IMPL-01：schema version 测试名/docstring 对齐验证

**Controller 要求**：

1. 只在 `tests/host/test_durable_schema.py` 更新测试名与中文 docstring，使其准确描述 version 24 的 context budget canonical fact contract；
2. 不修改 production、public contract、control doc 或 Slice 3 代码。

**当前状态**：

| 检查项 | 原状态（初审时） | 当前状态 | 证据 |
| --- | --- | --- | --- |
| 测试函数名 | `test_host_schema_version_is_queue_policy_check_version` | `test_host_schema_version_is_context_budget_canonical_fact_version` | `test_durable_schema.py:837` |
| 中文 docstring | "当前 committed Host schema version 是 queue policy CHECK schema 23" | "当前 committed Host schema version 是 context budget canonical fact schema 24" | `test_durable_schema.py:838` |
| 断言 | `assert HOST_SCHEMA_VERSION == 24` | `assert HOST_SCHEMA_VERSION == 24`（不变） | `test_durable_schema.py:840` |
| HOST_SCHEMA_VERSION 声明 | `HOST_SCHEMA_VERSION = 24` | `HOST_SCHEMA_VERSION = 24`（不变） | `durable/schema.py:39` |

**语义 owner 对齐验证**：

- `HOST_SCHEMA_VERSION = 24` 的语义 owner 是 `dayu/host/durable/schema.py`，version bump 的直接驱动是新增 `CONTEXT_BUDGET_EVALUATED` canonical fact contract（属于 `dayu/host/context_events.py`）。
- 测试名 `test_host_schema_version_is_context_budget_canonical_fact_version` 准确引用了 context budget canonical fact 这一 owner 语义——而非已被替换的 queue policy CHECK schema。
- docstring "context budget canonical fact schema 24" 与 `HOST_SCHEMA_VERSION = 24` 的 owner 级语义同源。

**结论**：CTRL-S2-IMPL-01 已正确修复。✅

---

## 2. Fix scope drift 验证

**检查项**：fix 是否改变了 production、public contract、schema 或 Slice 3 代码。

| 文件类别 | 是否被 fix 修改 | 证据 |
| --- | --- | --- |
| Production（`dayu/host/*.py`） | 否 | `git diff b6f297b4 -- dayu/` 中 production diff 与初审时完全相同 |
| Public contract（`dayu/host/api.py`） | 否 | `HostContextUsageView` / `HostActivityView` / public DTO 无变化 |
| Schema（`dayu/host/durable/schema.py`） | 否 | `HOST_SCHEMA_VERSION = 24` 为原实现产物，fix 未触及 |
| Service（`dayu/service/entrypoint_runtime.py`） | 否 | `EntrypointContextUsage` / exhaustive mapper 无变化 |
| Controller control doc | 否（excluded scope；Controller-owned，仅在 gate status 行更新 metadata） | 不纳入 review |
| Slice 3 代码 | 否 | 无 `context_anchor.py`、无 anchor resolver、无 USAGE_ANCHORED production 路径 |
| 测试（除 `test_durable_schema.py` 外） | 否 | 其他 test diff 与初审时完全相同 |

**结论**：fix 零 production/public/schema 行为 scope drift。✅

---

## 3. DS-F1：anchored builder 是否被提前实现

**Controller 裁决**（驳回，accepted Slice 3 计划内必做项）：
> "只在 Slice 2 给 `.value` 增加 unreachable `None` 分支，既不能形成可用 anchored fact，又会把两项独立修改混在一起。"

**当前状态验证**：

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| `context_anchor.py` 文件存在 | 不存在 | `git diff b6f297b4 --name-only` 中无 `context_anchor` |
| anchor resolver 实现 | 无 | diff 中无 `resolve_context_anchor`、`CompatibleContextAnchor` |
| USAGE_ANCHORED 生产路径 | 无 | 所有 producer 固定使用 `conservative_fallback` |
| `fallback_reason.value` 判空处理 | 未修改（`context_events.py:310` 仍为 `result.fallback_reason.value`） | Controller 驳回要求保留 |
| `ContextAnchorDiagnostic` 类 | 作为 frozen nullable schema field 定义存在，仅 parser 中使用 | plan §8.3 #2 允许 strict schema 定义，不含 resolver |
| anchored formula / delta 计算 | 无 | diff 中 `predicted = conservative_input_tokens` 无 delta |
| `anchor_diagnostic` 在 test 中的使用 | 仅验证 `is None`（conservative fallback 边界断言）和 `not in public_fields`（public secrecy 断言） | `test_context_budget_evaluated.py:124,193` |

**结论**：DS-F1 没有被提前实现，明确仍属于 Slice 3。Controller 驳回边界完整保持。✅

---

## 4. DS-F2/F3：Controller 驳回边界验证

### DS-F2：`AssertionError` exhaustive branch

**Controller 裁决**（驳回）：
> "raw payload 的 unknown enum 已在 `_required_enum` 边界统一以 `ValueError` fail closed。后续若代码显式扩展 `ContextEstimateMethod` 却遗漏 typed exhaustive branch，属于开发期不完整穷举，使用 `AssertionError` 与项目现有 typed mapper/state-machine 风格一致。"

**当前状态**：`context_events.py` 中 `parse_context_budget_evaluated_payload` 的 `else: raise AssertionError("context estimate method is not exhaustive")` 未修改。Controller 驳回边界保持。✅

### DS-F3：public projector 重复 `Mapping` 检查

**Controller 裁决**（驳回）：
> "`parse_context_budget_evaluated_payload` 是 canonical payload shape 的唯一 owner，其入口已显式检查 `Mapping`；在 read projector 重复 shape validation 会产生第二份边界规则，违背唯一 owner 约束。"

**当前状态**：`read_api.py:_context_usage_activity` 中 `_payload_object(row)` 调用未添加 `isinstance(payload, Mapping)` pre-check。Controller 驳回边界保持。✅

**结论**：DS-F2 与 DS-F3 均保持 Controller 驳回边界，fix 未反向修改。✅

---

## 5. 此前结论复审：canonical fact、producer ordering/rollback、startup strict replay、public secrecy

### 5.1 Canonical fact 结论

初审确认成立的各项 canonical fact 属性，经逐项复验仍成立：

| 属性 | 初审证据 | 当前状态 |
| --- | --- | --- |
| v1 schema 22 fields 全量 | `_BUDGET_EVALUATED_FIELDS` tuple | 未变化 ✅ |
| strict parser（类型/范围/enum/recompute cross-check） | `parse_context_budget_evaluated_payload` | 未变化 ✅ |
| deterministic identity（7 atoms，无时间戳/随机/浮点） | `context_budget_evaluated_decision_id` | 未变化 ✅ |
| 幂等 append（同 identity + 同 result → 复用） | `append_context_budget_evaluated_in_transaction` | 未变化 ✅ |
| 冲突 fail-closed（同 identity + 矛盾 result → `HostEventIdentityConflictError`） | `conflict=True` 分支 | 未变化 ✅ |
| `_require_matching_context_budget_row` 12 项 row 元数据校验 | frozen dataclass 结构相等 | 未变化 ✅ |

### 5.2 Producer ordering/rollback 结论

初审验证的 15 个 producer 的 manifest→fact→transition 同事务顺序、CAS/precondition rollback 零孤立契约，所有 production 代码未变化。关键 producer：

| producer | 初审证据位置 | 当前状态 |
| --- | --- | --- |
| ordinary allow | `dispatch.py` manifest → fact → start | 未变化 ✅ |
| ordinary soft/hard | `dispatch.py` fact → compact/fail | 未变化 ✅ |
| post-compact allow/hard | `dispatch.py` manifest/fact → start/fail | 未变化 ✅ |
| tier fallback allow/hard | `dispatch.py` manifest/fact → start/fail | 未变化 ✅ |
| reactive accepted/recovery | `engine_ingest.py` manifest → fact → start | 未变化 ✅ |
| startup replay | `recovery.py` strict-load → manifest → fact → start | 未变化 ✅ |
| steer continuation | `admission.py` manifest → fact → start | 未变化 ✅ |
| wait completed/cancelled | `waiting.py` manifest → fact → resume | 未变化 ✅ |
| wait failed/lost | `waiting.py` 零 fact/manifest/Attempt | 未变化 ✅ |
| Engine iteration>0 | `engine_ingest.py` manifest → fact → link | 未变化 ✅ |
| policy missing | `dispatch.py` no-budget manifest → start（零 fact） | 未变化 ✅ |

**CAS/precondition rollback**：`_StartCandidateCasMissRollback`、`_ReactiveRecoveryStartCasMissRollback` 逻辑未变化。`test_dispatch_scheduler.py` 参数化 rollback 测试断言零孤立写入。✅

### 5.3 Startup strict replay 结论

`recovery.py:_startup_continuation_budget_result` 的全部 strict-load 逻辑未变化：

- `sizing.status == COMPLETE` → strict-load matching fact via `load_matching_context_budget_evaluation_in_transaction`
- source fact missing/mismatch → `HostDurableError` → `_lose_unrecoverable_source` → LOST
- unavailable source → 返回 `None`（零 fact）
- 新 fact identity 绑定 new Attempt/candidate，不复用 source fact identity
- 要求 `CONSERVATIVE_FALLBACK` method + 非空 `fallback_reason`

`test_recovery_scan.py` 相关断言未变化。✅

### 5.4 Public secrecy 结论

Host→Service 七字段投影链未变化：

- `HostContextUsageView` 精确 7 字段（`predicted_input_tokens`、`context_window_size`、`utilization_basis_points`、`soft_threshold_tokens`、`hard_threshold_tokens`、`estimate_method`、`pressure_level`）
- `HostActivityView.__post_init__` 双向 invariant 校验未变化
- `read_api._context_usage_activity` 仅 strict-parse `CONTEXT_BUDGET_EVALUATED`，raw `USAGE_REPORTED` 保持 `activity=None`
- `EntrypointContextUsage` 同形 7 字段，逐字段复制，exhaustive enum mapper
- test `test_submit_entrypoint_turn_maps_context_usage_without_recalculation` 断言逐字段相等且不重算

不包含 `policy_ref`、`anchor_diagnostic`、`budget_decision`、`sizing_stage`、`usage refs`、`delta`。✅

---

## 6. Plan §8.3 allowlist 合规检查

当前 diff 中的 production 文件与 accepted plan §8.3 allowed production files 对照：

| 实际变更文件 | plan allowlist 中存在 | 备注 |
| --- | --- | --- |
| `dayu/host/context_budget.py` | ✅ | |
| `dayu/host/context_events.py` | ✅ | |
| `dayu/host/lifecycle_events.py` | ✅ | |
| `dayu/host/durable/schema.py` | ✅ | |
| `dayu/host/dispatch.py` | ✅ | |
| `dayu/host/engine_ingest.py` | ✅ | |
| `dayu/host/admission.py` | ✅ | |
| `dayu/host/recovery.py` | ✅ | |
| `dayu/host/waiting.py` | ✅ | |
| `dayu/host/api.py` | ✅ | |
| `dayu/host/read_api.py` | ✅ | |
| `dayu/host/__init__.py` | ✅ | |
| `dayu/service/entrypoint_runtime.py` | ✅ | |

无 allowed files 外的 production 修改。plan §8.3 stop condition "任何 allowed files 外 production 修改" 未触发。✅

---

## 7. Test 文件变更范围检查

| 实际变更 test 文件 | plan §8.3 allowlist 中存在 | 备注 |
| --- | --- | --- |
| `tests/host/test_context_budget_evaluated.py` | ✅（新增） | untracked，plan 要求新增 |
| `tests/host/test_dispatch_scheduler.py` | ✅ | |
| `tests/host/test_engine_ingest_mapping.py` | ✅ | |
| `tests/host/test_recovery_scan.py` | ✅ | |
| `tests/host/test_public_steer.py` | ✅ | |
| `tests/host/test_resolve_wait_command.py` | ✅ | |
| `tests/host/test_lifecycle_events.py` | ✅ | |
| `tests/host/test_durable_schema.py` | ✅ | |
| `tests/host/test_package_exports.py` | ✅ | |
| `tests/service/test_entrypoint_runtime.py` | ✅ | |
| `tests/cli/test_activity_renderer.py` | ✅ | |

无 allowed tests 外的 test 修改。✅

---

## Findings

### Accepted Finding Closure

| Finding ID | Controller 裁决 | 本 re-review 状态 | 证据 |
| --- | --- | --- | --- |
| CTRL-S2-IMPL-01 | accepted，低风险，必须在本 gate 修复 | **已修复，verified pass** | test 函数名/docstring 已与 `HOST_SCHEMA_VERSION=24` context budget canonical fact owner 同源 |
| DS-F1（anchored builder `fallback_reason=None`） | 驳回，accepted Slice 3 计划内必做项 | 未实现（符合 Controller 裁决） | `context_events.py:310` 未修改；无 anchor resolver；无 USAGE_ANCHORED 生产路径 |
| DS-F2（`AssertionError` exhaustive branch） | 驳回 | 未修改（符合 Controller 裁决） | `context_events.py` `else: raise AssertionError(...)` 保持 |
| DS-F3（projector 重复 `Mapping` 检查） | 驳回 | 未修改（符合 Controller 裁决） | `read_api.py` 未添加 `isinstance(payload, Mapping)` pre-check |

### 新增 Actionable Finding

无。

---

## Open Questions

无。

---

## Residual Risk

1. **DS-F1 anchored builder roundtrip**：属于 accepted Slice 3 计划内必做项。当前 `ContextSizingResult.fallback_reason` 为非 optional typed field，Slice 3 扩展该类型时需要同步处理 `build_context_budget_evaluated_payload` 中 `result.fallback_reason.value` 的判空。风险属于 Slice 3，不在本 Slice 2 gate 范围内。

2. **初审 residual risks 状态不变**：
   - `context_events` 局部 durable import — 仍为已知架构约束，未变化。
   - `context_sizing_pressure_and_decision` / `build_conservative_context_sizing_result_from_atoms` 无独立单元测试 — 由各 producer owner-level tests 间接覆盖，未变化。
   - `HOST_SCHEMA_VERSION` 23→24 无迁移脚本 — 不需要 DDL 变更，未变化。
   - `_append_limited_runner_call_manifest_event` 返回 tuple 类型 — 仍为 internal helper，未变化。
   - `waiting.py` 中 sizing_result 重复构造 — 模式与 recovery.py/admission.py 一致，未变化。

3. **Controller-owned control doc**：`docs/host/issues-implementation-control.md` 的 diff 为 Controller 在 gate status 行更新 metadata（gate 状态从 Slice 1 re-review pass 推进到 Slice 2 review fix），不纳入本 review scope，无 residual risk。

---

## Verdict

**pass**

CTRL-S2-IMPL-01 已正确修复，测试名与 docstring 已与 `HOST_SCHEMA_VERSION=24` / context budget canonical fact owner 同源。fix 零 production/public/schema 行为 scope drift。DS-F1 没有被提前实现且明确仍属于 Slice 3。DS-F2/F3 保持 Controller 驳回边界。此前 canonical fact、producer ordering/rollback、startup strict replay、public secrecy 结论经逐项复验仍成立。无新增 actionable finding。
