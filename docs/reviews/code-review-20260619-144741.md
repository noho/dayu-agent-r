# Code Review (Aggregate Deepreview)

## Scope

- Mode: current changes
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `main`
- Output file: `docs/reviews/code-review-20260619-144741.md`
- Included scope: 当前分支相对 `main` 的完整未合并变化（148 files, +22571/-1265）
- Review focus: 聚合 compact rejected attempt diagnostic artifact slice 与已有大 PR（WU-CM-12 主体、WU-CM-12-FIX-R1、Conversation Memory smoke/log diagnostics、recovery tier）的交互风险
- Parallel review coverage: 无（单 reviewer 聚合审查）

## Review Artifacts Consumed

- `docs/reviews/wu-cm-compact-rejected-diagnostic-plan-20260619-124435.md` (plan)
- `docs/reviews/wu-cm-compact-rejected-diagnostic-implementation-20260619-143551.md` (implementation)
- `docs/reviews/wu-cm-compact-rejected-diagnostic-code-review-adjudication-20260619-144111.md` (adjudication)
- `docs/reviews/code-review-20260619-143711.md` (AgentMiMo review)
- `docs/reviews/code-review-20260619-143853.md` (AgentDS review)
- `docs/reviews/code-review-20260619-144206.md` (re-review: AgentMiMo findings)
- `docs/reviews/code-review-20260619-144239.md` (re-review: AgentDS findings + conclusion)

## Findings

### 1-blocking-严重-smoke 测试 `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` 回归失败

- **入口/函数**: `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor`
- **文件(行号)**: `tests/host/test_public_compact_smoke.py:593`
- **输入场景**: 超长重复 prompt (`_DUPLICATE_PROMPT_SENTENCE * 500`) 触发 proactive compaction
- **实际分支**: `fake_compactor.prompt_lengths == [19454]`，compactor 被调用
- **预期行为**: 测试断言 `fake_compactor.prompt_lengths == []`，即超长 current input 不能无损进入 compact schema 时应走 dispatch fallback，不调用 compactor
- **实际行为**: compactor 被调用（prompt length 19454），说明 proactive compaction 路径现在会触发 compactor 调用而非走 fallback
- **直接证据**: `assert fake_compactor.prompt_lengths == []` 失败，实际为 `[19454]`；该测试在 commit `2c46631b`（WU-CM-12 S5 regression reconciliation）中被修改为期望 compaction 不触发
- **影响**: 分支存在回归。可能是 recovery tier 逻辑（`_proactive_compaction_recovery_attempts`）、context fallback caps（`_fallback_caps_allow_append`）、或 compact material view 构建变更导致 proactive compaction 行为改变。此回归不在 diagnostic artifact slice 内，但在同一分支中
- **建议改法和验证点**: 定位导致 compaction 从 "fallback" 变为 "compactor call" 的具体 commit 和代码路径；确认是预期行为变更（测试断言需更新）还是非预期回归（代码需修复）
- **修复风险（中）**: 需要理解 proactive compaction 与 fallback 的完整交互链路
- **严重程度（低/中/高/严重）**: 严重 — 分支存在明确测试失败

### 2-deferred-高-Recovery tier rejected attempts 不写入 EventLog，审计链断裂

- **入口/函数**: `HostDispatchScheduler._execute_proactive_compaction()` → recovery loop → `_operation()` transaction
- **文件(行号)**: `dayu/host/dispatch.py:1343-1407`
- **输入场景**: proactive compaction 首次 operation 全部 rejected，进入 recovery tier 循环；recovery tier 也全部 rejected 或部分 rejected
- **实际分支**: recovery loop 中 `tier_result.rejected_attempts` 从未写入 EventLog；transaction 内只迭代 `result.rejected_attempts`（初始 operation 的 rejected attempts）
- **预期行为**: 所有 proposal attempt 的 rejected 事件都应写入 EventLog，包括 recovery tier
- **实际行为**: `for rejected in result.rejected_attempts:` (行 ~1401) 只遍历初始 operation 的 rejected attempts；recovery tier 的 `tier_result.rejected_attempts` 被丢弃
- **直接证据**: recovery loop 行 1343-1376 只保存 `accepted_result` 和 `accepted_request`；transaction 内只读初始 `result`
- **影响**: recovery tier 的 `CompactionRejectedAttemptDiagnostic` artifact ref 从 durable 审计链中消失
- **建议改法**: 合并所有 recovery tier 的 rejected attempts 到统一列表，transaction 内遍历全部写入
- **修复风险（中）**: 需要调整 recovery loop 和 transaction 之间的数据传递结构
- **严重程度（低/中/高/严重）**: 高
- **处置**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit

### 3-deferred-中-Recovery tier 无测试覆盖

- **入口/函数**: `HostDispatchScheduler._proactive_compaction_recovery_attempts()`, `_proactive_compaction_recovery_request()`, `_execute_proactive_compaction()` recovery loop
- **文件(行号)**: `dayu/host/dispatch.py:1343-1376, 1486-1610`
- **输入场景**: proactive compaction 首次 rejected 后进入 recovery tier
- **实际分支**: 无测试覆盖 recovery tier 路径
- **预期行为**: ~180 行新增 recovery tier 代码（3 个 tier 构造、cancellation 检查、attempt number 计算、accepted result 合并）需要测试
- **实际行为**: `grep -rn "recovery_attempts\|recovery_request\|tier_1_fallback\|tier_2_section\|tier_3_delta" tests/` 无匹配
- **直接证据**: `dispatch.py:1486-1610` 的 `_proactive_compaction_recovery_attempts()` 无测试
- **影响**: recovery tier 正确性未经验证，回归风险高
- **建议改法**: 补充 tier 1/2/3 成功/失败/cancellation 路径测试
- **修复风险（低）**: 纯测试补充
- **严重程度（低/中/高/严重）**: 中
- **处置**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit

### 4-deferred-中-Session stale recovery result protection

- **入口/函数**: `HostDispatchScheduler._execute_proactive_compaction()` → recovery loop
- **文件(行号)**: `dayu/host/dispatch.py:1343-1376, 1408-1423`
- **输入场景**: Session 在 recovery loop 期间关闭，recovery tier 产出 accepted result
- **实际分支**: recovery loop 不检查 session 状态；transaction 内检查 session 状态
- **预期行为**: session 关闭后不应继续执行 recovery tier
- **实际行为**: recovery loop 在事务外执行，不检查 session 状态；session 关闭后 recovery tier 仍会执行并产出 accepted result，被 transaction 内 stale check 拦截
- **直接证据**: 行 1343-1376 无 session 检查；行 1408 `_run_session_allows_proactive_compaction()` 只在 transaction 内检查
- **影响**: session 关闭后 recovery tier 的计算资源被浪费；stale result 的 rejected attempts 丢失（与 Finding 2 复合）
- **建议改法**: 在 recovery loop 中增加 session 状态检查
- **修复风险（低）**: 增加 cancellation check
- **严重程度（低/中/高/严重）**: 中
- **处置**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit

### 5-deferred-低-`_completed_compaction_proposal_attempt_count` 语义不一致

- **入口/函数**: `_completed_compaction_proposal_attempt_count()`
- **文件(行号)**: `dayu/host/dispatch.py:4118-4143`
- **输入场景**: recovery tier 成功后计算 `accepted_attempt_number`
- **实际分支**: `completed_attempt_count` 在初始 operation 和失败 recovery tier 间累加；成功 tier 的 `_accepted_attempt_number()` 包含内部 rejected attempts
- **预期行为**: `completed_attempt_count` 应包含所有已完成 proposal call
- **实际行为**: 数值正确但命名和累加方式增加认知负担，容易引入 off-by-one
- **直接证据**: 行 1342/1374-1376/1358-1361 的累加逻辑
- **影响**: 轻微 — 当前逻辑正确，但命名不直观
- **建议改法**: 补充测试覆盖跨 tier attempt number 计算
- **修复风险（低）**: 命名改进
- **严重程度（低/中/高/严重）**: 低
- **处置**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit

### 6-deferred-低-Recovery tier 共享 operation_id 导致 EventLog 归属模糊

- **入口/函数**: `HostDispatchScheduler._execute_proactive_compaction()` → recovery loop
- **文件(行号)**: `dayu/host/dispatch.py:1353`
- **输入场景**: 多个 recovery tier 使用同一个 `pending.operation_id`
- **实际分支**: 所有 recovery tier 共享 `pending.operation_id`
- **预期行为**: 每个 proposal attempt 的 EventLog 事件应可唯一归属到具体 attempt
- **实际行为**: `CONTEXT_COMPACTION_ATTEMPT_REJECTED` payload 的 `operation_id` 相同，区分 attempt 只能靠 `attempt_number`
- **直接证据**: 行 1353 `compaction_operation_id=pending.operation_id`
- **影响**: 当前因 Finding 2 不写入 recovery tier EventLog，此问题暂不可见
- **建议改法**: 修复 Finding 2 时增加 recovery tier 标识
- **修复风险（低）**: 增加字段
- **严重程度（低/中/高/严重）**: 低
- **处置**: `deferred-with-owner`，owner = 后续 recovery-tier compact audit diagnostics work unit

### 7-deferred-中-Orphan diagnostic artifact 文件

- **入口/函数**: `write_compaction_rejected_attempt_diagnostic_artifact()`
- **文件(行号)**: `dayu/host/compaction_operation.py:980-982`
- **输入场景**: 调用方事务在 artifact 文件写入成功后被 SQLite 层回滚
- **实际分支**: `artifact_store.write_artifact_bytes()`（文件系统）先于 `payload_store.write_payload_descriptor_for_artifact()`（SQLite）
- **预期行为**: 事务回滚后不应留下无法追踪的 artifact 文件
- **实际行为**: 文件系统中残留 content-addressed artifact 文件，payload_descriptors 行随事务回滚消失
- **直接证据**: `compaction_operation.py:980-982` 先写文件再写 SQL；plan 已将此列为 residual risk
- **影响**: 含 raw 文本的 orphan 文件残留在磁盘上，无法通过 descriptor 索引追踪
- **建议改法**: 在 docstring 中明确此 tradeoff；确认 artifact maintenance model 覆盖孤儿清理
- **修复风险（低）**: 仅增加 best-effort 清理逻辑
- **严重程度（低/中/高/严重）**: 中
- **处置**: `deferred-with-owner`，owner = existing storage maintenance / artifact orphan model

### 8-deferred-低-Duplicated diagnostic helpers

- **入口/函数**: `_diagnostic_offending_section` / `_diagnostic_offending_kind` / `_diagnostic_offending_label` / `_diagnostic_offending_ordinal` / `_diagnostic_offending_text_digest` / `_diagnostic_offending_text_length`
- **文件(行号)**: `dayu/host/dispatch.py:4482-4557` 与 `dayu/host/engine_ingest.py:6251-6326`
- **输入场景**: 任一模块的 `_append_*_compaction_attempt_rejected_event()` 调用
- **实际分支**: 6 个 module-level private helper 在两处逐字重复
- **预期行为**: 这些纯投影函数应提取到共享位置
- **实际行为**: 两个模块各自定义语义完全相同的 6 个 helper
- **直接证据**: `dispatch.py:4482-4557` 与 `engine_ingest.py:6251-6326` 函数体完全相同
- **影响**: 维护负担，未来字段修改需同步两处
- **建议改法**: 提取到 `compaction_operation.py` 作为配套投影函数
- **修复风险（低）**: 纯函数提取
- **严重程度（低/中/高/严重）**: 低
- **处置**: `rejected-with-reason` — 当前只有两处调用，提取成本高于收益；若第三处调用出现则重新评估

### 9-already_accepted-低-Offending block ordinal 边界守卫无直接测试

- **入口/函数**: `_offending_block_artifact_json()`
- **文件(行号)**: `dayu/host/compaction_operation.py:1802-1806`
- **状态**: 已修复（AgentDS F3 accepted），守卫逻辑正确
- **残余风险**: 无专项测试覆盖 ordinal 越界分支。当前调用链中 ordinal 与 `previous_blocks` 同源（均来自 `enumerate(previous_blocks)`），实际不会触发

### 10-already_accepted-低-Diagnostic 构造失败静默返回 None

- **入口/函数**: `_proposal_failure_diagnostic()`
- **文件(行号)**: `dayu/host/compaction_operation.py:1507`
- **状态**: 设计决策 — diagnostic 构造是 best-effort，失败时返回 None 并保留原本的 rejected attempt 行为
- **残余风险**: 若 `block.to_json()` 或 `sha256_digest_json()` 因意外输入抛出异常，整个 diagnostic 静默丢失，只留 warning 日志

## Open Questions

- **Finding 1 根因**: `test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` 回归失败的根因需要定位。可能是 recovery tier 逻辑、context fallback caps、compact material view 构建、或 `_compaction_result_accepted()` 判断逻辑的变更导致 proactive compaction 行为从 "fallback" 变为 "compactor call"。此问题阻塞分支 merge。

## Residual Risk

- **Recovery tier 审计语义**: Findings 2-6 的 deferred 项需要在后续 recovery-tier compact audit diagnostics work unit 中统一解决。这些 findings 有直接证据支撑，应作为该 work unit 的 pre-existing known issues 记录。
- **Production compact root cause**: `previous reference continuity text is invalid` parser 根因仍未修复。diagnostic artifact slice 提供了可观测性，但不修复 parser 本身。
- **Diagnostic parser mirror drift**: `compaction_operation.py` 的 `_previous_reference_continuity_line_invalid()` 镜像了 `compact_material.py` 的 `_parse_previous_reference_continuity_text()` 的协议。若 parser 协议变更但 diagnostic mirror 未同步更新，offending block 检测会静默失效。
- **`_proposal_failure_stage()` 分类精度**: 对 generic compactor path（非 `CompactorProposalPreparedCompactor`），`compactor.compact()` 失败且 `proposal_manifest_reference=None` 时会被归类为 `material_pack_to_compact_input`，即使实际失败发生在 compactor 执行阶段。当前只有 legacy compactor 走此路径，影响有限。
- **Orphan artifact 清理**: 仍归属于现有 artifact storage maintenance model。
- **Ordinal 守卫无直接测试**: 修复已到位但无直接测试覆盖。

## Conclusion

**Diagnostic artifact slice 本身实现质量良好**，经过 plan → implementation → code review → adjudication → re-review 完整流程，8 个原始 findings 的处置全部合理（1 accepted+fixed, 1 rejected, 6 deferred）。核心链路 `run_compaction_operation() → CompactionRejectedAttemptDiagnostic → write_compaction_rejected_attempt_diagnostic_artifact() → EventLog payload` 被 `test_rejected_attempt_diagnostic_captures_invalid_previous_reference` 覆盖，EventLog payload 不含 raw block text，artifact JSON 包含完整诊断信息。

**但分支整体存在一个 blocking issue**：`test_proactive_compact_duplicate_prompt_falls_back_without_lossy_anchor` 测试失败，compaction 行为从 "fallback" 变为 "compactor call"。此回归不在 diagnostic artifact slice 内，但在同一分支中，需要在 merge 前定位根因并修复或更新测试断言。

**Deferred items** 主要集中在 recovery tier（~180 行新代码无测试、rejected attempts 不写入 EventLog、session stale 保护），这些是分支合并后的 follow-up work unit。
