# UF-FIX10 same-request-concurrency 实施计划 re-review（fix 后）

- **review target**: `docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md`（fix gate 修订版）
- **work unit**: `UF-FIX10 same-request-concurrency`
- **review 日期**: 2026-08-16 22:57:32（本机系统时钟）
- **review 类型**: plan fix 后 re-review。只验证 DS F1-F7、MiMo 01-04、DS OQ1-2 是否真实闭合，并主动查找修正引入的新 material blocker。
- **review 方式**: 只读审查。未修改 plan、生产代码、测试；未 commit。
- **fix artifact**: `docs/gateflow/uf-fix10-plan-review-fix-20260816.md`（Controller 全量 accepted，无 rejected/deferred）
- **原 review**: `docs/reviews/plan-review-20260816-222742.md`（DS）、`docs/reviews/plan-review-20260816-221939.md`（MiMo）
- **直接证据来源（本轮新核对）**: `docling_upload_service.py`（prepare_upload 330-449、_can_skip_upload 1264-1296、_build_pending_assets 838-941、_build_upsert_meta 943-981、commit/rollback helper 1033-1116）、`sec_upload_workflow.py`（150-313、341-392）、`_filing_upload_fresh_validation.py`（全文）、`ingestion_runtime.py`（664-1144 usage codes、1405-1526 validator）、`upload_failure.py`（全文）、`repository_protocols.py`（139-228）、`_fs_company_meta_core.py`（58-90）、`_fs_storage_infra.py`（416-531 begin_batch、889-962 identity reader、1363-1440 rollback/resolve_active_batch）、`_fs_filing_upload_state_core.py`（全文）、`_fs_source_integrity.py`（60-209、855-1030）、`source_integrity.py`（262-287）、测试夹具构造点 grep 全仓、`test_fins_ingestion_runtime.py`（230-348、10635-10710）、`test_fins_storage_atomicity.py`（3450-3490、3700-3720、8110-8145 既有 spawn 基建）。

## 0. 总评

修订版 plan 已把两份 review 的全部 accepted finding 落为可验证的冻结契约，且与代码事实一致：early skip 路径、closed failure 映射表、required 字段影响面、staging company meta 唯一 reader、跨进程测试基建与 cancel checkpoint 均有直接代码证据支撑。逐项复核未发现伪修复，也未发现修正引入的新 material blocker；存在 3 个非阻塞 residual risk（见 §3），其中 2 个属于 plan 明确的设计决策、1 个属于 S1 实现的机械约束风险，均不阻止 implementation authorization。

## 1. 逐项 closure 验证

### DS F1（高）preparation early skip 绕过 final arbitration — **已修复**

- plan 修复点：§6.3（filing identical 不再返回终态 skip，必须完成 conversion 形成 `_PreparedAssetMutation` candidate；material 保留 early skip）、§7.1.1-7.1.2（filing 只有 cancelled 可在 batch 前终结）、§7.3、§7.4 表。
- 代码事实核对：
  - 当前 early skip 确实发生在 conversion 之前：`docling_upload_service.py:388-410` `_can_skip_upload` 命中即返回 `UploadOperationResult(status="skipped")`，conversion（`_build_pending_assets`）在 `:413` 之后。plan 的改法（identical 继续执行 conversion）与代码结构吻合，`prepare_upload` 是唯一修改点，不需要动 workflow 短路结构。
  - `_can_skip_upload(:1264-1296)` 同时服务 material 与 filing；plan 要求 material 行为不变，实现只需按 `source_kind` 分支，`_build_skipped_file_events`（`:1421`）等 material 语义保持可用。
  - conversion 全程内存态：`_PendingFileAsset.data: bytes`（`:143`），conversion 不产生 batch 外落盘副作用，skip 无需额外清理，成功信号 3 的“零 mutation”可实现。
  - identical candidate 的 `document_version` 由 `_resolve_document_version(previous_meta, source_fingerprint)`（`:426`）确定；winner 与 loser 同 previous_meta 同 fingerprint，版本相等，identity exact equality 不因版本漂移失真。
  - conversion 失败路径（`:901-914`）在 batch 前以既有 typed `FinsUploadFailureError` 终结，不引入新终端。
  - §7.1 步骤 2 与 §10.2 明确 workflow 只保留 cancelled 结果的 batch 前终结，与现有 `sec_upload_workflow.py:231-232` 的 `isinstance(prepared_upload, UploadOperationResult)` 分支可机械兼容。
- 结论：真实闭合。原“两套不相容 skip 条件”已收敛为 publication owner 唯一 skip。

### DS F2（中）batch-time failure 映射与 explicit create 失败 terminal 真源 — **已修复**

- plan 修复点：§6.4 封闭映射表、§7.1.5/7.1.10、§10.1 断言 6-8、§10.2 断言 3。
- 代码事实核对：
  - validator 中 state-dependent usage raise 点精确为四个：`EXISTING_SOURCE_REPAIR_REQUIRES_AUTO`（`ingestion_runtime.py:1476`）、`CREATE_TARGET_EXISTS`（`:1500-1501`）、`UPDATE_TARGET_MISSING`（`:1502-1503`）、`COMPANY_NAME_REQUIRED`（`:1514`）。其余 usage codes（`:1018-1048`）全部 request-static，同一 raw request 已过 initial validation，batch 再现只能是 internal invariant breach，plan 将之映射 `UNEXPECTED_RUNTIME` 与 `fins_upload_failure_from_exception` 的 `ValueError` fall-through（`upload_failure.py:278-284`）一致。
  - UNSAFE 映射：validator `:1470-1473` 抛 `FinsUploadPrevalidationError(fins_upload_source_integrity_unsafe_failure())`，plan 保留 `SOURCE_INTEGRITY_UNSAFE` 有直接实现依据。
  - 新 code 挂接机制存在：`upload_failure.py:181-209` 的 kind/code 分组互斥+完整自检，新增 `SOURCE_PUBLICATION_CONFLICT` 只需入 `_STORAGE_FAILURE_CODES`，JSON roundtrip（`:419-450`）自动生效，`upload_failure_reason_from_json` 的未知值拒绝语义兼容。
  - explicit create 失败 terminal 真源可实现：`_build_sec_filing_failure_event`（`sec_upload_workflow.py:369-371`）用 `request.resolved_action` 投影 filing/resolved action、`requested_action` 单独传 raw；conflict 路径无 fresh request 时按 plan 取 initial authoritative request 即得 `create/create`，无需改 builder 签名。
  - I/O/corruption 映射：`_filing_upload_fresh_validation.py:64-69` 已有 `OSError/RuntimeFileLockError → prevalidation_io`、`ValueError → prevalidation_corruption` 的既有 owner 先例，batch 版复用同 reason 可行。
  - repair revision/blocked：`docling_upload_service.py:589/593` 已有 publish reset 期 `SOURCE_REVISION_STALE`/`SOURCE_REPAIR_BLOCKED` raise 点，plan 保留既有 code 与 `commit_prepared_upload_batch` primary-error 契约（`:1080-1086`）一致。
- 结论：真实闭合。“或”字歧义已消除，映射表按代码事实封闭。

### DS F3（中）required 字段导致 S1 allowed files 不闭合 — **已修复**

- plan 修复点：§8.2/§10.1 加入四个 fixture 文件，限机械补 `publication_identity=None`；断言 12 要求全仓 constructor 显式提供该字段。
- 代码事实核对（全仓 grep）：
  - `FilingUploadPublishedState(` 全部构造点：生产 `_fs_filing_upload_state_core.py:94/134`（S1 已含）；测试 `test_fins_ingestion_runtime.py:276/321/326/332/338/344`、`test_fins_service_runtime.py:79`、`test_cli_fins_commands.py:3067`、`test_service/test_fins_direct.py:746` — 恰好全部落在四个新增 fixture 文件内，无遗漏站点。
  - `test_fins_ingestion_runtime.py:321-348` 的 5 个断言构造点是纯结构校验（`__post_init__` 契约测试），机械补 `None` 不改断言语义。
  - `test_fins_ingestion_tools.py:794`、`test_cn_pipeline.py:1603/1919`、`test_sec_pipeline_upload_filing_stream.py:1128/1604/1819` 均为类型标注或函数参数，非构造点，不受影响。
  - protocol 扩展的 pyright 影响：`FilingUploadStateRepositoryProtocol` 唯一具体实现是 `FsFilingUploadStateRepository`（生产，S1 已含）；测试侧 fake（`_ForbiddenFilingUploadStateRepository`/`_FixedFilingUploadStateRepository`，`test_fins_ingestion_runtime.py:10635/10675`）是普通类并经 `cast(FilingUploadStateRepositoryProtocol, ...)` 注入，pyright 不查 conformance，无需为其补方法 stub。
- 结论：真实闭合，影响面与 allowed files 一致。

### DS F4 / MiMo 02（中）staging company meta 同版读取 owner — **已修复**

- plan 修复点：§6.2 冻结 `_fs_company_meta_core.py::_read_company_meta_from_ticker_dir_unguarded(external_ticker, ticker_dir)` 为唯一 strict reader；published/staging 共用；batch 只传 `state.staging_ticker_dir`。
- 代码事实核对：
  - 现有 `_get_company_meta_unguarded`（`_fs_company_meta_core.py:58-90`）已具备全部可复用结构：`_lstat_optional_storage_path` + `_read_published_company_identity(ticker_dir, expected_storage_key=ticker_dir.name, known_directory_stat=...)` + canonical ticker 严格比对。抽取 helper 与 plan 描述零间隙。
  - staging 目录名就是 storage key：`begin_batch`（`_fs_storage_infra.py:439/442`）`staging_ticker_dir = staging_root_dir / _derive_storage_key(...)`，且 `:486-490` 已用同一 strict `_read_identity_descriptor` 校验 staging 树；`expected_storage_key=ticker_dir.name` 与 canonical 校验对 staging 同样成立，不读 published path。
  - `_inspect_source_kind_unguarded`（`_fs_source_integrity.py:162-208`）docstring 明确支持 “published ticker 或真实 staging ticker 的稳定根目录”，且只读 `ticker_dir` 内。
  - `_resolve_active_batch(batch, batch.ticker)`（`_fs_storage_infra.py:1399-1440`）校验 open/同 core/ticker 匹配，plan §6.2 直接引用，可行。
  - 无 published target 的新 ticker：staging 由 `_ensure_ticker_structure` 生成，descriptor 存在而 `meta.json` 缺失，identity reader 返回 `company_meta=None`，与 published 读的 `FileNotFoundError → None` 语义一致。
- 结论：真实闭合，唯一 parser owner 不引入双 ownership。

### DS F5（低）FileExistsError 投影证据 — **已修复（证据修正）**

- plan §2.1.4 现表述为 “`FileExistsError` 是 `OSError` 子类，SEC/CN/HK workflow 的 `except OSError` 先于 generic handler，`fins_upload_failure_from_exception()` 实际将它投影为 `STORAGE_IO` / `storage_io`，而非 `UNEXPECTED_RUNTIME`”。
- 代码事实核对：`upload_failure.py:270-277` `isinstance(error, (OSError, RuntimeFileLockError)) → STORAGE_IO`；`sec_upload_workflow.py:296-304` 的 `except OSError` 先于 `:305` 的 `except Exception`。与修正后表述一致。
- 结论：已修复，root cause 叙事与代码同源。

### DS F6（低）跨进程 winner/loser 覆盖 — **已修复**

- plan 修复点：§10.2 断言 9（spawn 双进程 real FS exact auto，process-safe barrier + bounded queue，断言 `{ok, skipped}`/`{1,0}` 与单次 publication）。
- 代码事实核对：
  - 既有跨进程基建可直接复用：`test_fins_storage_atomicity.py:3469-3474/3713-3718/8126-8131` 均使用 `multiprocessing.get_context("spawn")` + 模块级 worker + `str(workspace_root)` 参数 + Pipe；filelock 跨进程阻塞已由既有测试证明。
  - workflow/宿主均为模块级可 pickle-by-reference 对象，worker 在子进程内自建 repository/workflow 的路径与既有 `_read_source_ids_in_process` 模式一致；结果对象（`UploadOperationResult`/pipeline result）为 JsonObject 承载，可 pickle。
  - barrier 语义（两进程在 initial MISSING 下完成 prepare 后才竞争 writer）与 per-ticker writer 的跨进程串行化（`_fs_storage_infra.py:453` `_acquire_ticker_lock`）匹配。
- 结论：已修复，测试主张与既有基建匹配。

### DS F7（低）SKIP 路径 cancellation checkpoint — **已修复**

- plan 修复点：§6.4 映射表行（SKIP terminal 前观察 cancel，先 rollback 成功才返回 cancelled，rollback 失败为 `STORAGE_IO`）、§7.1.8、§10.1 断言 8、§10.2 断言 11。
- 代码事实核对：
  - `execute_prepared_filing_publication` 签名已含 `cancellation: CancellationToken | None`（§6.5），owner 内观察取消无额外 seam。
  - rollback 契约支持：`rollback_prepared_upload_batch(batch, operation_error=None)`（`docling_upload_service.py:1089-1116`）正常路径返回 `None`，失败则抛 rollback 异常，owner 可按 plan 转 `STORAGE_IO`；`_build_cancelled_result`（`:1756`）与 `_is_cancelled`（`:1784`）模块级可复用。
  - 测试可实现性：loser 等待 writer 期间（阻塞 `begin_batch`）从测试线程 set cancel，writer 释放后 loser 仲裁 skip、观察 cancel → cancelled，时序确定无需 sleep。
- 结论：已修复，与现有 prepare/commit 两侧的 cancel checkpoint 模式（`docling_upload_service.py:354/376/424`）一致。

### MiMo 01（中）MISSING→COMPLETE 与 publish/skip 矛盾 — **已修复**

- plan 修复点：§7.2（MISSING→COMPLETE 不 stable 但在 §7.3 专用 exact-auto predicate 成立时明确 SKIP，prepared=create/fresh=update 为合法专用例外）、§7.4 封闭裁决表第 2/3 行、§13.1 OQ3 冻结。
- 代码事实核对：fresh validator 对 COMPLETE+auto 解析 update（`ingestion_runtime.py:1484-1487`），对 MISSING+auto 解析 create，prepared/fresh action 分叉是该场景的必然事实；§7.4 表第 2 行显式豁免 action invariant，与 §7.2 末段 “不得把此规则误用到 MISSING→COMPLETE 专用分支” 自洽。旧 §9.8 的 “changed-but-equivalent” 措辞已删除，两套规则收敛为一套。
- 结论：已修复，实施者不再需要自行解读。

### MiMo 02（中）batch company meta 来源 — **已修复**

- 与 DS F4 同一修复（§6.2 helper + allowed files + spy 断言），见 DS F4 证据。已修复。

### MiMo 03（低）resolved-action invariant 与 exact-auto 例外矛盾 — **已修复**

- §7.2 已将一致性检查限定在 stable 分支并显式豁免 `MISSING → COMPLETE` exact-auto SKIP 专用分支；§7.4 表把两种优先级固化为单表。已修复。

### MiMo 04（低）asset source 跨模块常量对齐 — **已修复**

- plan 修复点：§6.1 冻结 `FilingUploadAssetSource` 与两个 typed constant 由 `repository_protocols.py` 唯一定义并从 `dayu.fins.storage` 导出；`docling_upload_service.py` 与 `_fs_source_integrity.py` 必须直接使用，删除各自 private alias/constant。
- 代码事实核对：
  - 两组私有常量确实存在且值恰好相同：`docling_upload_service.py:77-78`（`_ASSET_SOURCE_*`，用于 `:626/:831/:936`，material+filing 共用）、`_fs_source_integrity.py:63-64`（`_FILE_SOURCE_*`，用于 `:781-782/:887-890/:987-989`）。
  - `docling_upload_service.py` 已依赖 `dayu.fins.storage` 仓储协议，直接改用 contract 常量无 import 方向问题；`repository_protocols.py` 只依赖 domain 层，无新增循环。
  - 契约命名偏 filing 但值集与 material 完全一致，material 复用同 constant 无语义冲突。
- 结论：已修复，实现巧合升级为契约保证。

### DS OQ1 — **已修复（冻结）**

- §6.1 不变量 + §13.1 OQ1：`primary_original_asset_name` 与 docling `derived_from` 精确命中 original descriptor 的 storage `name`，绝不使用 `original_filename`/用户 basename。
- 代码事实：prepared 侧 `derived_from = original_asset.name`（`:919`，storage identity）；durable 侧验证器按 `descriptor.name == derived_from` 校验（`_fs_source_integrity.py:905-909`）且 basename 只做可读投影校验（`:911-925`）。两端语义同源。
- 结论：已修复。

### DS OQ2 — **已修复（冻结）**

- §6.1 + §6.3 + §13.1 OQ2：prepared/durable `assets` 与 companions 均按 storage `name` 字典序排序，equality 不依赖输入顺序。
- 代码事实：prepared `pending_assets` 顺序为 selection 序 + 追加 docling（`:868/927`），describe helper 必须重排，plan 已显式 pin；durable `declared_files` 顺序来自 meta files 声明，投影时排序。两端可对齐。
- 结论：已修复。

### DS OQ3（条件性 open question）— **证据失效**

- Controller 已接受 DS F1（删除 filing early-skip terminal），OQ3 的 “保留 early skip 时的共存与 residual owner” 前提不再存在。fix artifact 明确记录为条件消失而非 rejected。判定：证据失效（前提消除），不留 residual。

## 2. 修正引入的新问题排查（new blocker hunt）

按用户指定重点逐项排查，未发现新 material blocker：

1. **conversion 落盘副作用**：转换产物为内存 bytes（`_PendingFileAsset.data`），skip 无需清理 batch 外文件，零 mutation 主张成立。无问题。
2. **protocol 扩展的 pyright 波及**：唯一具体实现为生产 wrapper（S1 内含）；测试 fakes 均为普通类 + `cast` 注入，不受 conformance 检查。无问题。
3. **required 字段构造点全表**：全仓 10 个构造点全部落在 S1 allowed files 内（4 生产/夹具文件），无遗漏。无问题。
4. **identity 两端字段可投影性**：prepared 侧（ticker/document/internal/form/company_id/ingest_method/fiscal_*/report_kind/filing_date/report_date/amended/source_provider/is_deleted/document_version/source_fingerprint/primary_document/assets）全部来自 `_PreparedAssetMutation` 与 meta；durable 侧全部来自同一次 trusted inspection 的 `business_meta`/`provenance`/declared files/primary（`_fs_source_integrity.py:81-107/855-931`）。无缺失字段。
5. **begin_batch 克隆与稳定 view**：writer 先于 copytree 取得（`:453`→`:485`），staging 树含 company meta 与 source，`_read_company_meta_from_ticker_dir_unguarded` 读 staging 可行。无问题。
6. **跨进程测试可实现性**：既有 spawn + 模块级 worker + Pipe 基建存在且模式匹配。无问题。
7. **cancel checkpoint**：rollback-normal 路径与 cancelled result builder 均存在，时序确定。无问题。
8. **stable observation 全表**：§7.4 九行 + company-only 变化规则 + UNSAFE 优先，逐行与 validator/repair 代码事实核对一致，无未定义状态组合。无问题。

## 3. Residual risks（非阻塞）

| # | 风险 | 性质 | 建议去向 |
| --- | --- | --- | --- |
| R1 | explicit `update` 且内容 identical 的 sequential 重传：pre-fix 提前 skip（already_uploaded），post-fix 因 §7.3.1 要求 raw action 为 exact `auto`，将走 `COMPLETE→COMPLETE` stable PUBLISH（幂等重写，terminal 由 skipped 变 ok）。这是 plan 显式冻结的设计决策（成功信号 4），但属于用户可见 terminal 变化 | 设计决策 | S2 落地后 README/实现报告明确记录，或 goal confirmation 知会 |
| R2 | conflict 路径在 writer 等待期间收到 cancel：plan 只在 SKIP terminal 前观察 cancel，CONFLICT 分支直接构造 typed failure。与 pre-fix stale-create 失败路径（OSError→STORAGE_IO failed）的 cancel 语义持平，非回归，但与 PUBLISH/SKIP 的 cancel-first 语义不一致 | 边界未 pin | 实现期以既有行为为准；若 Controller 要求统一，在 code review 阶段裁决 |
| R3 | S1 对四个 fixture 文件的约束是 “只有机械 None diff”：若 S1 全量 pytest 中暴露既有 fake-built state 与真实 production state 的 equality 断裂，S1 无权限改断言语义。当前证据显示 runtime 测试无此类比较（builder 24 处调用均无 `==` 对照真实 state），风险低 | S1 约束紧 | 实现 Agent 在 S1 遇到该情况时 stop 并回到 plan review，不得自行改断言 |

## 4. Open questions

无。原 DS OQ1-2 已冻结进 plan §13.1；OQ3 前提消除。修订版 plan §13.1 声称 “无blocking或non-blocking open question” 属实。

## 5. 结论

**pass**

- DS F1-F7、MiMo 01-04 全部真实闭合（已修复）；DS OQ3 证据失效；无未修复、无部分修复。
- 未发现修正引入的新 material blocker；R1-R3 为非阻塞 residual risk，其中 R1/R2 是 plan 显式冻结的行为语义，R3 已有 S1 stop condition 兜底。
- 修订版 plan 满足 code-generation-ready：§6 契约、§7 状态机与裁决表、§8-§10 slice 边界与 exact assertions、§13 冻结答案彼此一致，且与既有代码事实同源。
- 本 re-review 未修改任何 plan/生产代码/测试，未 commit。
