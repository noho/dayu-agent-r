# Code Review

## Scope

- Mode: current changes (workspace changes relative to HEAD)
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: HEAD
- Output file: `docs/reviews/code-review-20260618-191048.md`
- Reviewer: AgentDS
- Included scope: `dayu/host/run_input.py` (uncommitted), `tests/host/test_run_input_builder.py` (uncommitted)
- Excluded scope: committed branch history, `docs/reviews/wu-cm-12-fix-r1-s2-implementation-codex-20260618.md` (implementation report, read for context only)
- Plan reference: `docs/host/host-issues/wu-cm-12-fix-r1-material-guard-plan.md` Slice 2
- Design truth: `docs/host/design.md`, `docs/engine/design.md`
- Parallel review coverage: 无

## Findings

### 1-未修复-低-test_gap_provider_compact_scenario

- **入口/函数**: `DurableAcceptedToolEvidenceMaterialProvider._load_accepted_tool_evidence_materials_tx` → `build_pre_dispatch_compact_material_view`
- **文件(行号)**: `dayu/host/run_input.py:1380-1385`
- **输入场景**: Session 中存在已接受的 `CONTEXT_COMPACTED` 事件，且 compact 之后的 post-compact delta 中包含 `TOOL_RESULT_ACCEPTED` 事件
- **实际分支**: `build_pre_dispatch_compact_material_view` 内部 `_latest_compacted_event_before_current_input` 返回非 None → `delta_start = compact.event_sequence + 1` → `_post_compact_delta_rows` 从 compact 之后开始读取
- **预期行为**: provider 应只返回 compact 之后且未被 compact event mapping 覆盖的 accepted evidence blocks
- **实际行为**: 三个新增测试均未创建 `CONTEXT_COMPACTED` 事件，因此 provider 在 compact 存在场景下的行为未经 `test_run_input_builder.py` 验证
- **直接证据**:
  - `test_run_input_builder_accepted_tool_evidence_material_has_no_private_row_cap`（line 871）：只调用 `_append_prior_accepted_tool_evidence_batch` + `_seed_current_run`，无 compact 写入
  - `test_run_input_builder_represented_refs_exclude_whole_accepted_evidence_blocks`（line 905）：同上，只通过 `_StaticCompactArtifactProvider` 注入 `CompactArtifactView`，但 EventLog 中无对应 `CONTEXT_COMPACTED` 事件
  - `test_run_input_builder_accepted_tool_evidence_uses_raw_outcome_text`（line 957）：同上
  - `_seed_current_run`（line 4281）：不写入任何 `CONTEXT_COMPACTED` 事件
- **影响**: 若 `delta_start` 计算有误或 compact event mapping 与 compact artifact view 的 `represented_evidence_refs` 不一致，provider 可能遗漏 post-compact evidence 或重复已 compact 的 evidence。当前 `build_pre_dispatch_compact_material_view` 在 `test_compact_material.py` 中有独立的 compact-present 测试覆盖（如 `test_pre_dispatch_material_view_preserves_post_compact_evidence`），部分缓解了此风险
- **建议改法和验证点**: 新增一个 provider 入口测试：先写入 `CONTEXT_COMPACTED` 事件（覆盖部分 evidence），再写入 compact 之后的 TOOL_RESULT_ACCEPTED 事件，验证 provider 只返回 compact 之后的未覆盖 evidence blocks
- **修复风险（低）**: 纯测试新增，不修改生产代码
- **严重程度（低）**: `build_pre_dispatch_compact_material_view` 已有独立测试覆盖 compact-present 路径；provider 层只是 thin filter，且当前生产路径中 compact 与 evidence 的 EventLog truth 一致性由 `compact_material.py` 保证

## Open Questions

- `_accepted_tool_evidence_delta_blocks`（`compact_material.py:2263`）构造 `RunInputMaterialBlock` 时不设置 `artifact_refs`（默认为空 tuple）。旧 `build_accepted_tool_evidence_material_blocks` 通过 `collect_selected_compaction_request_evidence_inputs` 曾设置 `artifact_refs=material.artifact_refs`。当前 TOOL_RESULT_ACCEPTED 的 evidence payload 是否可能引用 artifact 存储的 tool outcome？如果可能，切换到 `compact_material.py` source builder 后会丢失 artifact_refs provenance。从当前代码路径看，TOOL_RESULT_ACCEPTED 的 raw outcome 通过 `event_payload_object_for_result_ref` 读取，走的是 payload ref/digest 路径而非 artifact 路径，但未找到明确排除 artifact 存储的文档或断言。

## Residual Risk

- 大 EventLog delta 下 `build_pre_dispatch_compact_material_view` 构造完整 material view 的成本：provider 只为提取 ACCEPTED_TOOL_EVIDENCE blocks 却构造了包含 USER_INPUT、ASSISTANT_FINAL_ANSWER 和所有 provenance 字段的完整 view。plan 已将此标记为 deferred-with-owner 到未来 Host material source performance hardening WU，当前不新增私有 row limit 或 page correctness cap
- 三个新增测试均依赖 `_append_prior_accepted_tool_evidence_tx` 测试 helper，该 helper 把 TOOL_CALL_REQUESTED 写为 CANONICAL_FACT（line 3619-3664），但生产代码中 TOOL_CALL_REQUESTED 是 CANONICAL_FACT 还是 NON_CANONICAL 取决于 Host ingest policy。当前 `_post_compact_delta_rows` SQL 只选择 USER_INPUT_ACCEPTED / RUN_SUCCEEDED / TOOL_RESULT_ACCEPTED 三种 CANONICAL_FACT 类型，因此 TOOL_CALL_REQUESTED 的事件类型不影响 delta 结果，但测试 helper 的 event_class 选择与生产行为的一致性未经独立验证
- 未覆盖项：`current_display_text` 与 EventLog `display_text` 不一致时的 fail-closed 路径（`_validated_current_input_event` line 1826-1827 抛出 `HostDurableError`）在 provider 入口层无测试覆盖；该路径由 `compact_material.py` 的独立测试间接覆盖
