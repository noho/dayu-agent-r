# Code Review — WU-ENG-02-R1 Re-Review Gate

## Scope

- Mode: current changes (re-review gate)
- Branch: `phase/wu-eng-02-r1`
- Base: `913875da`
- Output file: `docs/reviews/re-review-wu-eng-02-r1-20260620-215031.md`
- Included scope: controller-accepted findings verification against implementation and tests
- Excluded scope: rejected finding `_lost_host_event` suffix — respected per controller decision
- Parallel review coverage: 无

## Accepted Findings Verification

### Finding 1: terminal diagnostics helper direct tests

**Accepted requirement:** terminal diagnostics helper direct tests cover only provider id, only client id, both ids, both absent, message=None, message empty string, and id truncation.

**Evidence (PASS):**

文件 `tests/host/test_terminal_diagnostics.py` 包含 8 个直接测试：

| # | 测试函数 | 覆盖场景 |
|---|---------|---------|
| 1 | `test_terminal_diagnostic_suffix_includes_only_provider_id` (L15) | 仅 provider_request_id |
| 2 | `test_terminal_diagnostic_suffix_includes_only_client_id` (L31) | 仅 client_correlation_id |
| 3 | `test_terminal_diagnostic_suffix_includes_both_ids_in_stable_order` (L47) | 双 id 同时存在 |
| 4 | `test_terminal_diagnostic_suffix_returns_original_message_when_ids_absent` (L67) | 双 id 均缺失 |
| 5 | `test_terminal_diagnostic_suffix_returns_none_when_message_and_ids_absent` (L83) | message=None + 双 id 缺失 |
| 6 | `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_none` (L99) | message=None + id 存在 |
| 7 | `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_empty` (L118) | message="" + id 存在 |
| 8 | `test_terminal_diagnostic_suffix_truncates_ids` (L134) | id 超长截断 |

所有 8 个测试全部通过。覆盖矩阵完整。

**结论: PASS**

---

### Finding 2: double-id suffix format locked with provider_request_id before client_correlation_id

**Accepted requirement:** 双 id 后缀格式固定为 `provider_request_id` 在前、`client_correlation_id` 在后。

**Evidence (PASS):**

实现 `_terminal_diagnostic_suffix` (`dayu/host/_terminal_diagnostics.py:40-66`)：

```python
parts: list[str] = []
if provider_request_id is not None:
    parts.append(...)
if client_correlation_id is not None:
    parts.append(...)
```

顺序由代码中的 `if` 语句顺序决定，provider 在前，client 在后。

测试 `test_terminal_diagnostic_suffix_includes_both_ids_in_stable_order` (L47) 明确断言：

```python
assert message == (
    f"{_MESSAGE}\n"
    f"provider_request_id={_PROVIDER_REQUEST_ID}\n"
    f"client_correlation_id={_CLIENT_CORRELATION_ID}"
)
```

顺序锁定。

**结论: PASS**

---

### Finding 3: Tool Trace diagnostic with provider_request_id=None, client_correlation_id present, raw_payload_ref present

**Accepted requirement:** provider_request_id=None、client_correlation_id 存在、raw_payload_ref 存在时，diagnostic_ref=raw_payload_ref 且保留 client_correlation_id。

**Evidence (PASS):**

实现 `_extract_diagnostic_trace` (`dayu/host/tool_trace.py:900-952`)：

```python
provider_request_id = _optional_text(payload, _FIELD_PROVIDER_REQUEST_ID)       # None
client_correlation_id = _optional_text(payload, _FIELD_CLIENT_CORRELATION_ID)   # 存在
raw_payload_ref = _optional_text(payload, "raw_payload_ref")                    # 存在
diagnostic_refs = tuple(
    ref for ref in (raw_payload_ref, provider_request_id) if ref is not None
)
# diagnostic_refs = ("raw-ref-fallback",) — raw_payload_ref 在前
return _ToolTraceExtract(
    ...
    provider_request_id=provider_request_id,        # None
    client_correlation_id=client_correlation_id,    # 保留
    diagnostic_ref=diagnostic_refs[0] if len(diagnostic_refs) > 0 else None,  # "raw-ref-fallback"
    ...
)
```

测试 `test_diagnostic_trace_uses_raw_payload_ref_without_provider_id` (L1668) 断言：

```python
assert row.provider_request_id is None
assert row.diagnostic_ref == "raw-ref-fallback"
assert row.trace_summary["client_correlation_id"] == "client-fallback"
assert cold_lines[0]["diagnostic_refs"] == ["raw-ref-fallback"]
assert cold_lines[0]["client_correlation_id"] == "client-fallback"
```

测试通过。

**结论: PASS**

---

### Finding 4: message="" helper behavior

**Accepted requirement:** message="" 时不再产生前导空行，且不改变 durable payload 语义。

**Evidence (PASS):**

实现 `_append_terminal_diagnostic_suffix` (`dayu/host/_terminal_diagnostics.py:13-37`)：

```python
if suffix is None:
    return message
if message is None or message == "":
    return suffix
return f"{message}\n{suffix}"
```

当 `message=""` 且 suffix 存在时，返回纯 suffix（无前导空行）。当 `message=""` 且 suffix 为 None 时，返回原始 `""`。

测试 `test_terminal_diagnostic_suffix_uses_suffix_when_message_is_empty` (L118) 断言：

```python
message = _append_terminal_diagnostic_suffix(
    "",
    provider_request_id=None,
    client_correlation_id=_CLIENT_CORRELATION_ID,
)
assert message == f"client_correlation_id={_CLIENT_CORRELATION_ID}"
```

无前导空行。helper 只用于 terminal public projection 展示，不写入 durable payload（模块 docstring 明确约束）。durable payload 语义不受影响。

**结论: PASS**

---

## Rejected Finding Respecting

**Rejected requirement:** 不要求 `_lost_host_event` 后缀，除非有新直接证据表明当前 RUN_LOST payload 携带 provider/client correlation ids。

**Evidence (RESPECTED):**

- `outbox.py` 中 `_TERMINAL_STATUS_BY_EVENT_TYPE` 不包含 `RUN_LOST` (L81-85)，RUN_LOST 走单独的 skipped 路径。
- `_append_terminal_diagnostic_suffix` 仅在 `RUN_FAILED` 时被 outbox 调用 (L396-397)。
- 未添加 `_lost_host_event` 相关后缀逻辑。

**结论: RESPECTED**

---

## Open Questions

无

## Residual Risk

- `test_terminal_diagnostics.py` 覆盖了 helper 的所有分支，但未覆盖 `_bounded_diagnostic_id` 的独立行为（通过 truncation 测试间接覆盖）。
- Tool Trace diagnostic 测试覆盖了 `provider_request_id=None + client_correlation_id 存在 + raw_payload_ref 存在` 场景，但未覆盖 `provider_request_id 存在 + client_correlation_id=None` 的 diagnostic 场景（此场景由 canonical trace 路径覆盖）。
