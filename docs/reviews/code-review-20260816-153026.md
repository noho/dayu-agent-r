# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `a29c8eb5`（HEAD of previous slice）
- Output file: `docs/reviews/code-review-20260816-153026.md`
- Included scope: 5 production files, 7 test files，相对 `a29c8eb5` 的全部 workspace + committed changes
- Excluded scope: 无
- Parallel review coverage: 无

## Adversarial Verification Summary

本 review 按用户指定的 8 个 adversarial focus 逐一走读真实代码路径，结论如下：

### 1. upload-state 是否只消费同一 inspector classification/business_meta

**结论：通过。** `_fs_filing_upload_state_core.py:113-132` 在同一 publication guard 内调用一次 `_inspect_source_kind_unguarded()`，从 `inspection.target` 同时取得 `classification` 与 `business_meta`。旧的 `_get_source_meta_unguarded()` 和 `_classify_source_integrity_unguarded()` 已从 Protocol 和实现中删除（diff 行 383-416）。`MISSING/UNSAFE` 固定 `source_meta=None`；`COMPLETE/REPAIR_REQUIRED` 使用同一 inspection 的 `inspected_target.business_meta`。无二次 raw meta/descriptor 旁路。

### 2. repair contract union 是否封闭，direct replace/构造不能伪造

**结论：通过。** `ExistingSourceRepairDisposition = NoExistingSourceRepair | ExistingSourceAutoRepair`（`upload_repair_contract.py:80`）。两个成员均为 `frozen=True, slots=True`，`__post_init__` 校验 discriminator 唯一允许值。`ValidatedFinsUploadFilingRequest.__post_init__`（`ingestion_runtime.py:808-821`）对 `repair_disposition` 做 isinstance 检查，非封闭 union 成员抛 `TypeError`。`ExistingSourceAutoRepair` 要求 `SourceKind.FILING + REPAIR_REQUIRED + non-null revision`。直接 `replace()` 篡改 expected_integrity 或 file_selection 会被 `__post_init__` 的 identity/equality/selection 校验拦截。

### 3. validator precedence 是否精确

**结论：通过。** `validate_fins_upload_filing_request()`（`ingestion_runtime.py:1403-1463`）的裁决顺序为：

1. `_validate_filing_upload_published_state()` 校验 producer identity invariant（ticker/source_kind/document_id）与 status/meta invariant
2. `UNSAFE` → `FinsUploadPrevalidationError`，在 action/company decision 前抛出
3. `REPAIR_REQUIRED` + 非 exact `auto` → `EXISTING_SOURCE_REPAIR_REQUIRES_AUTO` usage error
4. `REPAIR_REQUIRED` + exact `auto` → `update` + `ExistingSourceAutoRepair`
5. `MISSING/COMPLETE` → 原 action/overwrite/deleted 解析 + `NoExistingSourceRepair`
6. company decision 最后解析

`create/update/delete/overwrite/deleted` 不回退：REPAIR_REQUIRED 分支固定 `resolved_action=update`，不进入 else 分支的 action/overwrite 逻辑。

### 4. 三个 failure code/factory closed/path-free/actionable 且只接 UNSAFE

**结论：通过。** `SOURCE_INTEGRITY_UNSAFE`、`SOURCE_REVISION_STALE`、`SOURCE_REPAIR_BLOCKED` 均在 `_STORAGE_FAILURE_CODES`（`upload_failure.py:181-189`）。三个模块级 factory 产生固定 message/retry_hint，无路径、revision 或内部异常文本。JSON round-trip 由 `test_source_repair_storage_failure_factories_are_closed_path_free_and_actionable` 验证。本 slice 仅 validator 使用 `fins_upload_source_integrity_unsafe_failure()`；stale/blocked factory 未提前接线。

### 5. raw runtime start 在 job/observation/converter/batch 前原样传播

**结论：通过。** `_validate_runtime_upload_request()`（`ingestion_runtime.py:4600-4634`）在 `start_upload()`/`prepare_observed_upload()` 的 durable job 创建和 observation 创建前调用。`FinsUploadPrevalidationError` 未被 catch/re-raise 为其他类型，原样传播。测试 `test_raw_runtime_unsafe_prevalidation_creates_no_job_observation_or_mutation` 精确断言：zero durable jobs、zero executor operations、zero runner requests、zero observations、workspace tree 不变。

### 6. same-basename owner 修复以 storage name/derived_from 唯一关联

**结论：通过。** `_classify_role_projection()`（`_fs_source_integrity.py:890-916`）以 `derived_from` 精确命中 storage name 为主路径，只校验该 exact asset 的 `original_filename` 同源。`derived_from` 损坏时 fallback 到 basename，但要求唯一命中；同 basename 命中多个 assets 返回 `None`（UNSAFE）。旧的全局 basename 唯一性检查已删除。测试 `test_source_integrity_uses_storage_name_for_same_basename_asset_identity` 验证：合法同 basename 为 `COMPLETE`，移除 exact derived identity 后的真实歧义为 `UNSAFE/FILE_DECLARATION_UNTRUSTED`。UF-FIX07 的两个 Docling 节点回归测试（`test_filing_same_basename_assets_are_collision_free_and_path_private`、`test_ambiguous_filing_primary_forces_versions_then_recovers_safe_skip`）通过。

### 7. tests/CLI/SEC/CN fixture 迁移是否改变测试意图

**结论：通过。** `_filing_upload_published_state()` 新增 required `status`/`reasons` 参数，全部调用点已显式迁移，无 default/loose inference。SEC/CN pipeline 测试从 `replace(validated, file_selection=...)` 改为直接调用 owner validator，测试意图从"验证 pipeline 消费 selection"不变，但更严格地证明 validator 是唯一 selection producer。CLI fixture 从单文件改为 original+Docling 双文件 entry，匹配 UF-FIX07 的严格 files contract。无 compatibility shim。

### 8. scope/docstring/strict typing/README trigger 与 1802 tests/pyright 证据

**结论：通过。** 独立验证结果：

- `pyright dayu/ tests/ utils/`：0 errors, 0 warnings, 0 informations
- `pytest tests/fins`：1802 passed, 1 skipped, 3 warnings
- `git diff --check`：通过
- 新增 diff 签名扫描：无 `object`/`Any`/伪 `callable` cast
- README diff：无输出（用户明确要求本 slice 不修改 README）

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- repair disposition 尚未触发 staged reset、revision recheck 或原子 publication（accepted Slice 4）。
- stale/blocked failure factories 尚未接 publication exception（accepted Slice 4/5）。
- SEC/CN/HK fresh prevalidation 的 async typed failed event 尚未实现（accepted Slice 5）。
- `_classify_role_projection()` 中 `derived_from` 命中多个 originals 时直接走 fallback 到 basename，而非立即返回 UNSAFE。当前上游已保证 `derived_from` 唯一性，但如果未来 inspector 放宽该约束，此 fallback 路径可能掩盖歧义。当前不构成 blocking risk。
