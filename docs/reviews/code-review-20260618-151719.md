# Code Review

## Scope

- Mode: current changes
- Branch: wu-cm-12-conversation-memory-drift
- Base: main
- Output file: docs/reviews/code-review-20260618-151719.md
- Included scope: S2 Turn-Group Selected Recent Window And Fallback Selection — workspace changes across `dayu/host/memory.py`, `dayu/host/compact_material.py`, `dayu/host/context_fallback.py`, `dayu/host/dispatch.py`, `dayu/host/engine_ingest.py`, `tests/host/test_memory_projection.py`, `tests/host/test_compact_material.py`, `tests/host/test_run_input_builder.py`, `tests/host/test_dispatch_scheduler.py`
- Excluded scope: `dayu/host/run_input.py` (only S1 turn_group_id population remains; no S2 selector changes); `docs/reviews/wu-cm-12-s2-implementation-codex-20260618.md` (implementation artifact, not production code); pre-existing test failures on main (see Residual Risk)
- Parallel review coverage: 无。本 review 单一 reviewer 逐链路走读全部 S2 变更。

## Findings

### 1-未修复-中-`_protected_recent_turn_group_ids` 和 `_is_turn_group_material_block` 在 compact_material.py 和 context_fallback.py 中重复实现

- **入口/函数**: `compact_material.py::_protected_recent_turn_group_ids` / `context_fallback.py::_protected_recent_turn_group_ids`
- **文件(行号)**: `dayu/host/compact_material.py:1530-1566` 与 `dayu/host/context_fallback.py:614-647`；`_is_turn_group_material_block` 在 `dayu/host/compact_material.py:1569-1580` 与 `dayu/host/context_fallback.py:711-722`
- **输入场景**: 任意调用路径触发 fallback selection 或 compact segment selection。
- **实际分支**: 两个模块各自定义了语义完全相同的私有函数，对 `RunInputMaterialBlock` 执行 newest N non-null `turn_group_id` group floor 计算。
- **预期行为**: Plan §S2 明确要求 "Replace raw item floor logic with a shared internal helper that computes newest N non-null turn_group_id groups." 应对 `RunInputMaterialBlock` 类型提供一个共享的内部辅助函数，compact_material 和 context_fallback 都从同一处引用。
- **实际行为**: 两处实现逐行相同（排序 key 为 `(event_sequence, event_sub_index, block_id)`、group 去重用 `latest_by_group` dict、取 top-N 后 frozenset 返回），但各自独立维护。`_is_turn_group_material_block` 同样完全重复。
- **直接证据**:
  - `context_fallback.py:17` 只 import `RunInputMaterialBlock`，未复用 compact_material 中已存在的 `_protected_recent_turn_group_ids`（`compact_material.py:1530`）。
  - `context_fallback.py:596` 调用自己的 `_is_turn_group_material_block`，未复用 `compact_material.py:1569` 的同名函数。
  - `context_fallback.py:614-647` 与 `compact_material.py:1530-1566` 逐行比对，除 `field_name` 在 ValueError message 中略有差异外完全相同。
- **影响**: 未来对 group floor 算法的修改（如排序字段调整、边界条件修正）需要同步改两处，遗漏一处会导致 compact segment 与 fallback selection 行为分歧。当前行为正确但维护风险存在。
- **建议改法和验证点**:
  1. 将 `_protected_recent_turn_group_ids` 和 `_is_turn_group_material_block` 提升为 `compact_material.py` 的模块级非私有函数（去掉 `_` 前缀），或提取到一个新的 `dayu/host/turn_group.py` 共享模块。
  2. `context_fallback.py` 从该共享位置 import 而非重复定义。
  3. 验证：现有 S2 测试全量通过，compact segment 和 fallback selection 的 protected group 行为保持一致。
- **修复风险（低）**: 纯重构，不改变算法语义，只需确保 import 路径正确。
- **严重程度（中）**: 不影响当前正确性，但违反 plan 的 "shared internal helper" 意图，且存在未来行为分叉风险。

### 2-未修复-低-`_is_raw_turn_block` 为死代码

- **入口/函数**: `compact_material.py::_is_raw_turn_block`
- **文件(行号)**: `dayu/host/compact_material.py:1583-1593`
- **输入场景**: 无调用点。
- **实际分支**: 函数定义了但整个代码库无引用。
- **预期行为**: 不应保留未被使用的旧实现。
- **实际行为**: `grep -rn "_is_raw_turn_block" dayu/host/ --include="*.py"` 仅命中定义处（`compact_material.py:1583`），无任何调用点。该函数是旧 raw item floor 逻辑的残留——仅覆盖 `USER_INPUT` 和 `ASSISTANT_FINAL_ANSWER`，而新逻辑 `_is_turn_group_material_block` 正确覆盖了 `USER_INPUT`、`ASSISTANT_FINAL_ANSWER` 和 `ACCEPTED_TOOL_EVIDENCE`。
- **直接证据**: `grep -rn "_is_raw_turn_block" dayu/host/ --include="*.py" | grep -v __pycache__` 仅返回 `dayu/host/compact_material.py:1583`。
- **影响**: 死代码增加阅读负担，可能被误认为是仍在使用中的替代路径。无运行时影响。
- **建议改法和验证点**: 删除 `_is_raw_turn_block` 函数。验证 pyright 通过且无新增 import 错误。
- **修复风险（低）**: 删除无引用的私有函数。
- **严重程度（低）**: 纯清理项，不影响正确性。

## Open Questions

1. `memory.py::_protected_recent_run_ids` 是否也应该统一到共享 helper？它操作 `SelectedRecentWindowItem`（有 `run_id` 字段），与 `RunInputMaterialBlock`（有 `turn_group_id` 字段）类型不同，但算法完全相同。如果要统一，需要定义一个 protocol 或泛型约束，当前 plan 未要求此层次抽象。**建议**: 在 S3 或后续 slice 中考虑提取 `HasTurnGroupId` protocol，避免三处独立维护。

## Residual Risk

- **预存在测试失败（非 S2 引入）**: `test_second_proactive_compact_uses_previous_view_without_old_raw_replay` 和 `test_multi_turn_proactive_compact_feeds_subsequent_run_input` 在 main 分支和当前分支均失败。根因是测试策略中的 context budget 阈值过紧，导致 seeded 数据触发 `hard_threshold_before_dispatch` 而非期望的 `CONTEXT_COMPACTED` 路径。与 S2 的 turn-group floor / fallback selection 变更无关。建议在 S3 或 S5 修复测试参数。
- **S2 未覆盖 tier 1-3 compact recovery fallback**: Plan §S4 的 tier 1-3 恢复逻辑不在当前 slice scope，dispatch 路径仍直接从 compact 失败跳到 tier 4/5 fallback。
- **S2 未覆盖 selected-id provenance guards**: Plan §S3 的 `_fallback_context_messages` 强化检查（source refs mismatch、turn_group_id consistency 等）不在当前 slice scope。
- **`run_input.py` 的 `build_accepted_tool_evidence_material_blocks` 中的 `run_id_by_event_id` lookup**: 如果 `material.tool_result_event_ref` 不在 `rows` 中的任何 `event_id`，`run_id_by_event_id[material.tool_result_event_ref]` 会抛出 `KeyError`。这是 S1 的变更（`run_input.py:1460-1461`），当前 S2 未修改此路径。该 `KeyError` 会导致 material 构建失败，属于 fail-closed 行为，符合 plan 要求；但异常类型为 `KeyError` 而非更明确的 `HostDurableError`，可读性略差。建议在 S3 中统一为更明确的异常类型。

## Verification Results

| 验证项 | 结果 |
|--------|------|
| `pytest tests/host/test_compact_material.py tests/host/test_memory_projection.py tests/host/test_run_input_builder.py tests/host/test_dispatch_scheduler.py` | 195 passed, 2 failed（2 个失败为预存在，非 S2 引入） |
| `pyright dayu/host/memory.py ...` (全部 affected 文件) | 0 errors, 0 warnings, 0 informations |
| `git diff --check` | 无 whitespace 错误 |
| Public smoke: `test_deterministic_two_turn_request_contains_prior_final_answer` | passed |
| Public smoke: `test_mock_tool_result_feeds_same_run_and_later_run_continuity` | passed |

## Review Checklist 逐项结论

1. **floor 是否按 newest non-null host_run_id/turn_group_id group 计算**: **通过**。三处实现均使用 `latest_by_group` dict 按 `(event_sequence, sub_index, original_index)` 取 max，然后 sorted reverse 取 top-N。`_protected_recent_run_ids`（memory.py:2003）、`_protected_recent_turn_group_ids`（compact_material.py:1530）、`_protected_recent_turn_group_ids`（context_fallback.py:614）算法一致。

2. **floor/cap/hard-budget priority**: **通过**。`build_recent_window_fallback_selection`（context_fallback.py:334）严格按 plan 算法执行：floor 先构建 → required budget 估算 → floor-only 超 hard budget 时由调用方 fail closed（不裁剪 floor）→ 非 floor 整块追加受 fallback caps 限制 → cap 拒绝后 break 不 later backfill → 每块追加后 hard budget 重估 → 超 hard budget 时整块回滚并 break。

3. **eligible raw block missing run_id/turn_group_id 是否 fail closed**: **通过**。三处实现均在 `selected_recent_window_turn_floor > 0` 时检查 eligible block 是否缺少 `turn_group_id`/`run_id`，缺则 `raise ValueError`。测试覆盖：`test_selected_recent_window_floor_rejects_missing_run_id`（test_memory_projection.py:442）、`test_proactive_segment_recent_floor_rejects_missing_turn_group_id`（test_compact_material.py:359）、`test_recent_window_fallback_rejects_missing_turn_group_id_for_floor`（test_run_input_builder.py:1191）。

4. **production proactive 和 reactive 路径都传 MemoryProjectionPolicy**: **通过**。Proactive: `dispatch.py:1989` 传 `self._local_execution.memory_projection_policy`。Reactive: `engine_ingest.py:1404` 传 `self._memory_projection_policy` 到 `_ReactiveCompactPending`，`engine_ingest.py:3754` 传 `pending.memory_projection_policy` 到 `_reactive_fallback_decision`。测试覆盖：`test_pre_start_governance_compact_failure_is_attempt_free`（test_dispatch_scheduler.py:4630）验证 proactive production 路径 fallback caps 生效；`test_reactive_fallback_decision_uses_memory_policy_caps`（test_dispatch_scheduler.py:5131）验证 reactive production helper 使用 fallback caps。

5. **tests 是否真实覆盖 production path，有无 private-helper coupling 风险**: **通过（有注意事项）**。Fallback 选择器测试（test_run_input_builder.py）直接调用 `build_recent_window_fallback_selection`，属于 public API 级别的集成测试，无私有 helper coupling。Memory projection 测试（test_memory_projection.py）调用 `build_conversation_memory_snapshot_from_events` public API。Compact segment 测试（test_compact_material.py）调用 `select_compact_segment` public API。Dispatch scheduler 测试（test_dispatch_scheduler.py）走完整 proactive/reactive production 路径。注意事项：`test_reactive_fallback_decision_uses_memory_policy_caps` 直接构造 `_ReactiveCompactPending`（私有类），存在一定的私有 helper coupling 风险——如果 `_ReactiveCompactPending` 字段变更，测试需要同步更新。建议在 S3 中考虑通过 `EngineEventIngestor` 公共接口间接触发 reactive fallback 以降低耦合。

6. **no public API/durable schema/EventLog/Engine contract drift**: **通过**。变更限于：
   - `RunInputMaterialBlock.turn_group_id`（新字段，有默认值 `None`，向后兼容）
   - `run_input_material_block()` 新增 `turn_group_id` 参数（有默认值 `None`）
   - `build_recent_window_fallback_selection()` 新增 `memory_policy` 参数（有默认值 `None`，向后兼容）
   - `_ReactiveCompactPending` 新增 `memory_projection_policy` 字段（私有 dataclass，不影响 public API）
   - 移除 `CURRENT_INPUT_ANCHOR_TEXT_MAX_CHARS` 常量（未在 `__all__` 中暴露）
   - 无新 EventLog 类型、无 durable schema 变更、无 Engine contract 变更。

7. **formatting churn 无无关改动**: **通过**。`git diff` 检查确认：
   - `dispatch.py` 仅新增 1 行（`memory_policy=self._local_execution.memory_projection_policy`）
   - `engine_ingest.py` 新增 `memory_projection_policy` 字段到 `_ReactiveCompactPending` dataclass（含 docstring）、构造时传值、`_reactive_fallback_decision` 调用时传值
   - `test_dispatch_scheduler.py` 新增 import 和测试逻辑；`test_pre_start_governance_compact_failure_is_attempt_free` 扩写以覆盖 production fallback caps
   - 无空白行重排、无 import 顺序扰动、无注释格式变化等无关改动。
   - `_is_raw_turn_block` 残留（见 Finding 2）属于功能遗留而非格式扰动。

## Conclusion

**PASS** — S2 实现的 floor/cap/hard-budget 算法正确，production proactive/reactive 路径均已接线 MemoryProjectionPolicy，missing turn_group_id fail-closed 行为正确，无 public API/durable schema/EventLog/Engine contract drift，测试覆盖充分。

2 个 findings（均为非阻塞）：
- Finding 1（中）：`_protected_recent_turn_group_ids` 和 `_is_turn_group_material_block` 在 compact_material.py 与 context_fallback.py 中重复实现，应提取为共享 helper。
- Finding 2（低）：`_is_raw_turn_block` 为死代码，应删除。

2 个预存在测试失败（非 S2 引入）记录在 Residual Risk。
