# Plan Re-Review: wu-cli-interactive-02 S5/F13 第三次 utils-closure amendment accepted-finding fix

## Review metadata

- provider: MiMo
- reviewer: AgentMiMo
- reviewed target: `docs/host/wu-cli-interactive-02-conformance-fixes-plan.md` §9.1、§10.5、§13、§15 与 `docs/reviews/wu-cli-interactive-02-s5-f13-utils-closure-plan-amendment-proposal-codex.md`
- base HEAD: `e7f578dc7bdfafb51a859be2db584300e08f81fb`
- review timestamp: `20260802-002254`
- scope: 第三次 S5/F13 utils-closure amendment accepted-finding fix 的独立 re-review
- artifact path: `docs/reviews/plan-review-20260802-002254.md`

## 1. Goal Confirmation 与 scope contract

本次 re-review 的目标是独立验证第三次 utils-closure amendment 的 accepted-finding fix 是否正确闭合。

Controller adjudication 已确认：
- MiMo finding 001（total union 算术偏差 33 ≠ 32）为 `accepted-medium-with-terminology-correction`
- AgentDS A5 / final pass 的 arithmetic conclusion 为 `rejected-set-arithmetic`

本次 re-review 的 binding scope 由 Controller adjudication §3 定义。

## 2. Assumptions tested

| # | Assumption | 验证方式 | 结论 |
|---|---|---|---|
| A1 | identity union 27 files | `rg` 三类 pattern + `sort -u` | ✅ 27 匹配 |
| A2 | builder union 8 files | `rg` 两类 builder + `sort -u` | ✅ 8 匹配 |
| A3 | 交集精确 2 files | `comm -12` | ✅ 2 匹配 |
| A4 | builder-only set difference 6 files | `comm -13` | ✅ 6 匹配 |
| A5 | 完整 mechanical union 33 files | `sort -u` 合并 | ✅ 33 匹配 |
| A6 | 第二 amendment 新增 allowed-file delta 5 | builder-only 6 - `test_context_compact_events.py` | ✅ 5 匹配 |
| A7 | `test_context_compact_events.py` 已在 S5 allowed owner tests | §9.1 第 542 行 + 表第 668 行 | ✅ 已允许 |
| A8 | §10.5 五类 pattern 去重可执行 | 执行完整脚本 | ✅ 全部通过 |
| A9 | active 32 残留为零 | `grep` 扫描 plan | ✅ 零残留 |
| A10 | 2-file/4-call utils delta | `rg -n` 精确计数 | ✅ 2/4 匹配 |
| A11 | identity source 同源性 | 逐行读取 4 个 call site | ✅ 全部同源 |
| A12 | 20-file dirty set 未变 | `git diff --name-only` | ✅ 20 files |
| A13 | 两个 utils 未修改 | `git status utils/` | ✅ 未修改 |
| A14 | plan/proposal 已修改 | `git status` | ✅ 已修改 |

**所有 14 项关键 assumption 均通过独立复核。**

## 3. Findings

**无 material findings。**

经独立复核，第三次 utils-closure amendment 的 accepted-finding fix 以下 claims 全部成立：

### 3.1 集合算术闭合

1. **identity union 27 files**：`FinalAnswerData` 21 files + `EngineRunOutcomeFinalAnswer` 4 files + `ContextCompactor` typed-return 7 files = 27 unique files。✅
2. **builder union 8 files**：`build_context_compacted_payload` 6 files + `build_context_compaction_attempt_rejected_payload` 4 files = 8 unique files。✅
3. **交集精确 2 files**：`tests/host/test_compaction_operation.py` 与 `tests/host/test_dispatch_scheduler.py`。✅
4. **builder-only set difference 6 files**：`test_compact_material.py`、`test_compaction_terminal.py`、`test_context_compact_events.py`、`test_memory_projection.py`、`test_proactive_compaction_operation.py`、`test_run_input_builder.py`。✅
5. **完整 mechanical union 33 files**：`27 + 8 - 2 = 33`。✅
6. **第二 amendment 新增 allowed-file delta 5**：builder-only 6 files 中 `test_context_compact_events.py` 已在 S5 allowed owner tests，其余 5 files 为新增。✅

### 3.2 §10.5 五类 pattern 去重验证

§10.5 的完整五类 pattern 去重脚本已独立执行，所有断言通过：

- `identity_files` count = 27 ✅
- `builder_files` count = 8 ✅
- `overlap_files` count = 2 ✅
- `builder_only_files` count = 6 ✅
- `mechanical_files` count = 33 ✅
- overlap files 精确匹配 `test_compaction_operation.py` + `test_dispatch_scheduler.py` ✅
- builder-only files 精确匹配预期 6 files ✅

### 3.3 术语边界

plan 与 proposal 正确区分三种集合事实：
- 第二 amendment 新增 allowed-file delta = 5（用于 allowed scope 检查）
- builder closure 相对 identity closure 的 set difference = 6（完整差集）
- 完整 mechanical union = 33（全集去重）

没有把 5-file allowed-file delta 误当 builder-only set difference。

### 3.4 2-file/4-call utils delta

| 文件 | call | 行号 | identity source |
|---|---|---|---|
| `smoke_host_public_awaiting_entrypoint.py` | `FinalAnswerData(...)` | 2010 | `self._request` (L1976) |
| `smoke_host_public_conversation_memory_scenarios.py` | `EngineRunOutcomeFinalAnswer(...)` | 1748 | `request` 参数 (L1733) |
| `smoke_host_public_conversation_memory_scenarios.py` | `EngineRunOutcomeFinalAnswer(...)` | 1794 | `request` 参数 (L1779) |
| `smoke_host_public_conversation_memory_scenarios.py` | `FinalAnswerData(...)` | 1843 | `snapshot` + `request` (L1643-1645) |

两个文件不在 20-file dirty set 中，确认尚未修改。✅

### 3.5 Identity source 同源性

所有 4 个 call site 的 identity 来源均为当前 `AgentRunRequest`：
- `run_id` → `request.run_id` ✅
- `attempt_id/execution_id` → `request.attempt_id/execution_id` ✅
- `effective_provider/effective_model` → `request.runner_spec.provider/model` ✅

### 3.6 Active 32 残留扫描

plan 中无 active 的 "32" 残留引用。唯一命中是 `prompt.P32-existing-dayu-no-config` scenario id，与 S5 mechanical union 无关。✅

### 3.7 Smoke/non-goals/validation drift 检查

- §10.5 validation plan 的三条 utils smoke 命令保持不变 ✅
- §7 non-goals 边界完整 ✅
- §13 checklist 已更新为 33-file union ✅
- §15 fix trace 已追加第三次 amendment 的 arithmetic correction ✅

## 4. Open questions

无阻塞 open question。

## 5. Residual risks

| 风险 | 分类 | 跟踪 |
|---|---|---|
| accepted HEAD 后出现新 constructor/typed-return consumer | proposal 已覆盖 | implementation pre-inventory fail closed |
| utils identity 被误当真实 provider evidence | proposal 已覆盖 | 行为项 29/G06 外部 evidence 不变 |
| 两个 utils 未新增测试/coverage | AGENTS.md 规则覆盖 | pyright + inventory + smoke + code review |

## 6. 结论

**pass**

第三次 utils-closure amendment 的 accepted-finding fix 已正确闭合。所有集合算术（identity union 27、builder union 8、交集 2、builder-only set difference 6、完整 union 33）均通过独立验证。第二 amendment 新增 allowed-file delta 仍为 5，术语边界清晰无混淆。§10.5 五类 pattern 去重脚本可执行且结果正确。active 32 残留为零。2-file/4-call utils delta 精确，identity source 同源性可验证。plan/proposal 已正确更新，20-file implementation dirty set 与两个 utils 文件未被修改。
