# F20 RR4 独立计划复审

## Reviewed target and scope

- Work unit：F20。
- Review round：RR4。
- Reviewer：AgentDS；未与 AgentMiMo 交换意见。
- Target：`docs/gateflow/pr-190-f20-plan-20260808.md`。
- Target SHA-256：`afb34d790c8773fa01e764a218b82554c790ff76bab96a0739f862cb294c7094`。
- Plan-review fix SHA-256：`ba54e241a7f7a8d3e118965f2c72edfca78a51c935a2480266adcb351f80936f`。
- Binding goal SHA-256：`14c18553cbb3b1ca0efb4c820476ac3bc913dd75c55ef2cc4d11f0bcdfd97c67`。
- RR3 adjudication SHA-256：`beb39341708f78c79692ede4bc16f7f8230cd80155054cfc9aa1b0293967b626`。
- RR2 adjudication SHA-256：`ab0298a36308558efae24f962f3eec09f291be712fb6af22fc3003b81642bcc7`。
- Storage audit SHA-256：`b2baa721f9b43087863b0dff099963df942683a8559922ac57a1dace947d4bbd`。
- 执行边界：只做provider-free read-only docs/code/runtime核验；未调用provider、未访问network、未修改plan/fix/goal/产品/oracle/scenario/F18/F19 bundle，未读取或触碰明确排除的review文件。

## Assumptions tested

1. 新binding goal与formal stock production CLI + post-segment canonical evidence acceptance gate是否同一语义，是否还残留pre-forward wrapper/interception/zero-call主张。
2. provider-free proof是否只经production`HostDispatchScheduler.open`消费同一`HostLocalExecutionOptions`的`worker_factory`和`context_compactor`两个独立typed ports，且各自ledger与production manifests/attempts/terminals对账。
3. OS sandbox是否是process-tree唯一network deny owner；parent Python audit hook是否只拥有parent outbound ledger，并在三个DNS resolver event进入解析前先ledger再raise；child是否无hook、无allowlist、无target/argv/PID/pickle推断。
4. Fins业务事实是否由Host tool request/result/canonical terminal与`dayu.fins.storage`owner exact closure拥有，且tool失败、terminal missing/extra、storage drift均fail closed。
5. global/allocation/active deadline union、storage-only materials、clean-seed隔离、R4第三operation/245 call预算、publication exact schema与last-writer是否退化。
6. architecture boundary、最佳实践、方案最小性、过度设计与过度耦合风险是否留下implementation Agent必须重新设计的缺口。

## Evidence and adversarial results

### Goal/formal consistency

- Goal success signal明确：formal不包装`DefaultLocalEngineWorkerFactory`、不拦截request，只在segment结束后用canonical Host budget/manifest/candidate/action truth做证据接受裁决。
- Plan §2、§3、§5.2、§7、§8、§10与该语义一致：`soft <= predicted < hard`且action=`compact_soft_threshold`才计入B2，否则原样seal并停止后续segment；没有恢复不存在的transparent wrapper、delegate-zero-call或race-free interception。
- B2保持`unadjudicated`，overall readiness保持`not ready`。

### Proof composition and typed owners

- `dayu/host/api.py:836-899`直接证明`HostLocalExecutionOptions`分别拥有`worker_factory`与`context_compactor`。
- `dayu/host/dispatch.py:1411-1482`直接证明`HostDispatchScheduler.open`消费同一typed local-execution options与transaction runner。
- Plan明确禁止proof调用`open_host(OpenHostOptions)`、direct SQL/store mutation或把compactor calls记入ordinary factory；两个ledger分别与ordinary runner manifests及compactor proposals/attempts/terminals exact equality。该composition与产品owner边界一致。

### Network enforcement and audit boundary

- `/usr/bin/sandbox-exec`当前file SHA-256为`8290e4be7387a0df83cd1559e86afd880464f269450573d012795761fe298f16`，与计划冻结值一致。
- OS-only negative matrix只要求parent/spawned-child numeric TCP/UDP在sandbox中返回`EPERM`且无endpoint接触；不加载Dayu、不安装parent audit hook，也不把DNS错误码冒充OS owner语义。
- Parent audit hook只处理`socket.__new__`、`socket.connect`、`socket.sendto`和三个name-resolution events；命中时先写typed ledger，再抛run-owned proof exception。provider-free runtime probe确认Python 3.11.15的`socket.getaddrinfo`、`socket.gethostbyname`、`socket.getnameinfo`均能被对应audit event在调用返回前截断，三项各产生唯一event并抛冻结异常。
- Plan明确不检查child creation，不要求child DNS hook，不解析`resource_tracker`、`spawn_main`、argv/PID/order或pipe/pickle payload。`dayu/runtime/interruptible_process.py:286-326`的multiprocessing private边界不再被错误投影成业务target identity；RR3 finding已关闭。

### Host tool/storage closure

- `dayu/host/tool_runtime.py:1594-1619`表明process-backed Fins execution由production typed capability/target factory路径产生；plan不再要求external tooling读取私有target pickle。
- 每个预期recipe必须由canonical`TOOL_CALL_REQUESTED`/`TOOL_RESULT_ACCEPTED`、tool terminal与storage repository返回的document/section identity exact闭合；missing/extra/failure/drift均在Slice 1阻断，禁止direct/in-process fallback。
- Materials只来自冻结storage audit与fresh repository API重算；R1 sibling chunks和R2 top-level siblings仍满足无ancestor/descendant、digest唯一、双向无全文包含，未伪造storage未公开的range。

### State machines, budgets and publication

- clean seed仍位于run tree与F18/F19 bundles之外，并以resolved same/ancestor/descendant/symlink/alias gate和tree SHA证明隔离；fresh workspace不含durable state。
- Deadline仍分为global、每chain immutable allocation与attempt-only active owner。`provider_not_started`只能是`active_deadline_state=not_created`且禁止active fields；attempted必须有activation/cutoff/effective seconds/ref/SHA。
- R4第三operation允许当前已启动segment在99-call cap与deadline内产生canonical terminal，随后seal并停止新segment；三链预算仍为`99/73/73=245`，没有漏算或第四链。
- Publication继续从private typed truth独立重算budget/count/terminal/deadline，保持attempted/skipped封闭union、terminal双向穷尽、Tool Trace、digest domain、secret-scan last writer与zero-write-after corruption checks。

## Findings

没有material findings。

## Open questions

没有blocking open question。OS sandbox与production Fins process-backed tool能否在当前host完整共存，已被正确留到Slice 1真实provider-free self-test；若tool执行失败、OS descendant coverage不成立或owner closure漂移，计划要求`setup-blocked`且provider start为0，不存在fallback。

## Residual risks and tracking

- 真实storage materials能否在complete production estimator下形成两个`soft <= predicted < hard`边界，仍由Slice 1 proof裁决；不满足时登记为setup/design residual。
- exact 160-char caps candidate是否被production parser接受、真实MiMo是否自然触发repair/fallback及R4是否保持两次operation，属于formal observation residual；不得padding、output injection、同链重跑或重标PASS。
- `intent_type`/`reason`等未治理字段缺少universal finite serialized upper owner，继续作为owner residual，不得从本场景存在性外推。
- 这些风险应进入F20 root-cause/implementation artifact及最终observed-behavior report，不需要扩大产品、README或oracle scope。

## Final plan review conclusion

二值结论：`PASS`。

在冻结SHA范围内，RR3的goal一致性与child-target语义所有权finding均已关闭；首轮PA01–PA05及RR2的formal seam/deadline分型没有退化。计划已达到进入Slice 1所需的code-generation-ready程度，但本PASS本身不授权provider、formal observation、B2裁决或readiness变更；仍须满足同一revision的另一独立RR4 review与Controller gate。
