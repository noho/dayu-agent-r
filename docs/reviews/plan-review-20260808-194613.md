# PR 190 F19 B2 final evidence closeout review（DS 路）

## 二值结论

**PASS**。

在冻结审查边界内，没有发现会推翻 F19 closeout 的证据缺口、交叉文件矛盾或夸大陈述。该结论只表示：现有报告准确记录了本次 non-covering 真实观察及其 publication 完整性；不表示 B2 mandatory behavior 已被证明，也不替用户完成 Oracle 裁决。

## 冻结边界

- 审查时 repo HEAD：`e55b9c4f43aaaea0bc5a0f7675b80ffb001e06ab`。
- 候选仓库改动仅为 `docs/cli_ci.md` 与 `docs/gateflow/pr-190-f19-observed-behavior-report-20260808.md`；后者 SHA-256 为 `98f162e4178194bb415dc5cf8eafefb2e91f5f053392a6b0e31034daa44f856c`。
- 正式观察使用的产品提交由 execution identity 固定为 `ce0c171a022a073c6355ace44e7c5e34a668d4bb`；模型为 `mimo-v2.5-pro-plan`。
- 本次复审未调用 provider，未写 public/private evidence，未修改被审文档。

## 五项 verdict

| 维度 | Verdict | 独立核验结论 |
| --- | --- | --- |
| Implementation/setup | `PASS / no product change` | 候选范围没有产品、schema、profile、analyzer 或测试改动；execution identity、fixed profile 与 fresh workspace 记录一致。 |
| Real observation | `needs-more-evidence` | 两条 attempted chain 共 5 个真实 segment，全部 canonical `RUN_SUCCEEDED`，但 12 个 ordinary runner input 后仍为 0 个 `CONTEXT_COMPACTED`、0 个 compactor response。 |
| Publication | `PASS / conforming` | 每条 attempted chain 均有公开 Tool Trace；per-chain budget/count/terminal、execution index、final scan 与 digest 均闭合。 |
| B2 Oracle | `unadjudicated` | 报告没有把 non-covering 观察升级为 accepted；scenario 仍为 `unadjudicated`。 |
| Overall readiness | `not ready` | 报告与 `docs/cli_ci.md` 均保持 registry `calibration`，没有提前标 ready。 |

## 直接证据核验

### 1. Final scan 与 digest

- `evidence/public/secret-scan.json` 实际 SHA-256 为 `716d2128233fc7ce023621e1542afb948f108dd2f5a6fa8336a76882fa9019b4`。
- 按 scan 自述 domain 独立枚举得到 94 个 evidence regular files、2,991,916 bytes；按 public scan domain 得到 10 个 public regular files、201,385 bytes，均与记录一致。
- exact secret、public 机器私有绝对路径、public raw database、public symlink 与 scan error 均为 0；4 个可用 credential exact values 已纳入扫描。
- `evidence/public/digest.json` 实际 SHA-256 为 `b9215b71938922d1fe381a987d1022635cf321df15dc21a22a3ebc1364d00bbb`。其 domain 精确等于 public 下排除 `digest.json` 与 `secret-scan.json` 的 9 个 regular files；逐文件 size 与 byte SHA 复算均无 mismatch。
- 文件纳秒时间戳显示 `secret-scan.json` 晚于其余全部 public files，当前 public bytes 也与 scan/digest 身份一致，没有 scan 后 public 写入迹象。

### 2. Budget、count 与 canonical terminal

- 全局 provider window 为 1800 秒，finalization reserve 为 180 秒；chain 01/02 各有 540 秒 chain deadline，chain 03 未创建 deadline、未启动 provider。
- Chain 01：R1/R2；2 个 `USER_INPUT_ACCEPTED`、2 个 `RUN_SUCCEEDED`、6 个 `CONTEXT_BUDGET_EVALUATED`、6 个 `RUNNER_CALL_INPUT_ASSEMBLED`、0 个 compact。terminal sequence 分别为 `3→37` 与 `39→73`。
- Chain 02：R1/R2/R3；3 个 `USER_INPUT_ACCEPTED`、3 个 `RUN_SUCCEEDED`、6 个 `CONTEXT_BUDGET_EVALUATED`、6 个 `RUNNER_CALL_INPUT_ASSEMBLED`、0 个 compact。terminal sequence 分别为 `3→37`、`39→62` 与 `64→76`。
- 五个 private `run-terminals.json` 的 run/session/sequence/terminal identity 与各链最终 SQLite `event_log` 直接计数完全一致。五个 PTY process exit 均为 1；报告将 process exit 与 Host canonical success 分开陈述，没有互相覆盖。

### 3. 每条 attempted chain 的 Tool Trace

- Chain 01 同时具备 path-redacted JSON/Markdown Tool Trace：26 个 valid events、0 invalid、2 runs、4 tool calls、0 compactor response。
- Chain 02 同时具备 path-redacted JSON/Markdown Tool Trace：27 个 valid events、0 invalid、3 runs、3 tool calls、0 compactor response。
- Chain 03 未尝试，execution index 中 Tool Trace 保持 null，没有伪造 attempt evidence。
- execution index 所列 allocation、deadline、setup、typed counts、chain seals、wrapper projection、Tool Trace 与五套 private capture refs/hash 共 81 项独立校验均通过；公开报告集合与 public 目录实际 11 个文件完全相等。

### 4. Private capture source truth

- 五个 attempted segment 均具备 screen、argv/command、按键/actions、process exit、filesystem before/after/diff、runtime before/after、SQLite before/after、raw PTY 与 run terminal capture。
- Chain 02 R2 的 Tool Trace 与 private capture 均直接显示只调用 `read_section`，没有先调用 `get_document_sections`；报告将其如实标为 instruction drift。
- Chain 01/02 R2 的真实输出均给出 FY2025 total net sales `416,161`、operating income `133,050`、期间、单位与 canonical SEC provenance，并明确保持 `21.7%/18.2%` 未证实；Chain 02 R3 延续相同约束。报告只将这些归类为 ordinary succeeded output，没有冒充 compactor accepted fact。

### 5. 材料、F18 与 registry 冻结

- 三个 workspace 的真实 `sections.json` SHA-256 相同。独立读取 owner data 得到：`s_0003` 为 68,039 chars / 9,782 words，列出的六个目标值均 absent；`s_0013` 为 61,252 chars，包含 `416,161` 与 `133,050`，不包含 `21.7` 或 `18.2`。与报告一致。
- F18 原 bundle 的六个冻结 public artifact SHA 与既有 closeout identity 逐项相同；F19 使用独立 bundle root，execution summary 也明确 `copied_or_relabelled_as_f19=false`。
- `docs/cli_ci_oracles.json` 与 `docs/cli_ci_scenarios.json` 的当前 SHA 分别为 `5812f7c98f2daa793646a1443699491786534ae53d178eaf31b978fefbfb38cb`、`3a63b8aca88a2ecb3ebc12a3e4e024777e6793c84d9b2e0e9ec07026ec1cc208`，均与 HEAD 字节完全相同；B2 scenario 仍为 `unadjudicated`，`applicable_from` 仍要求 Oracle controller 裁决后生效。

## 对 mandatory evidence 1–10 的审查

- 第 1、3、4、6、7、8、9 项均因没有 compactor input、accepted replacement 或 compact truth 而标 `needs-more-evidence`，证据与状态一致。
- 第 2 项只把 12 次 canonical budget evaluation 标为 `partial / needs-more-evidence`，没有把 ordinary runner budget 冒充 compact usage audit。
- 第 5 项准确区分了 ordinary succeeded output 中的 FY2025 provenance/unsupported ratio 约束与尚未发生的 compactor acceptance。
- 第 10 项限定为 `observed for attempted segments`；五套 private capture、两条 public Tool Trace、SQLite owner state 与 final scan 足以支持这一限定陈述。

因此，报告 1–10 没有夸大。本次没有 finding，也不需要修改被审文档或 evidence。
