# UF-FIX10 same-request-concurrency Plan 最终定点 Re-Review（DS，C-F1/C-F2 闭合验证）

## Reviewed target and scope

- **target**: `docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md`（C-F1/C-F2 fix 后版本）
- **fix artifact**: `docs/gateflow/uf-fix10-final-plan-review-fix-20260816.md`
- **scope**: 只验证 Controller-accepted blockers C-F1/C-F2 的闭合，及修订是否破坏已通过的 C-R1/C-R2。验证项：
  1. C-F1：S1 是否真正零行为变化且完整 `tests/fins` green；S2 是否原子接线、无 expected-red 中间态；allowed files / validation commands / goal mapping 三者是否一致；
  2. C-F2：`_build_upsert_meta` 是否被钉为现有 prepare 与 rebase 的唯一九字段 owner，fresh timestamps / version / storage revision 断言是否闭合；
  3. C-R1/C-R2 条款未被本次修订破坏。
- **日期**: 2026-08-16（本机系统时钟 23:44:55）
- **分支**: `codex/upload-filing-oracle`
- **review 方式**: 只读审查；未修改 plan、生产代码、测试、README，未 commit。

## 1. C-F1 验证

### 1.1 S1 零行为变化（根因层面）

上一轮 F-1 指出的红路径是「S1 改变 `prepare_upload()` filing 分支（identical 继续 conversion）但 S2 未接线，SEC/CN workflow 既有 skip 断言必红」。修订后 plan 已将行为切换整体移入 S2：

- §6.3 明确「S1 只新增上述类型与下列 Docling helper，不把它接入 `prepare_upload()`」「S1 结束时，现有 filing `_can_skip_upload` 命中仍在 preparation 阶段返回既有 `UploadOperationResult(status="skipped")`，不会继续 conversion」。
- §6.5「S1 不得调用 `begin_batch()`、stage、commit、rollback，也不得改变现有 filing route 行为」；`execute_prepared_filing_publication()` 只在 S2 新增/完成。
- §7.1「S1 不得部分启用其中任一步骤；S1 仍保持现有 filing identical 在 `prepare_upload()` early skip、现有 workflow begin/stage/commit 路径及全部 SEC/CN/HK observable」。
- §10.1 exact allowed changes 第 3 点「S1 不得让 `prepare_upload()` 产生 filing subtype/disposition，不得删除现有 filing early-skip 或令 identical filing 继续 conversion」；第 5 点禁止编辑 SEC/CN/HK workflow/workflow tests/README。
- S1 validation commands 显式包含完整 `pytest tests/fins -q`；expected completion signal 要求「owner tests 与现有全量 `tests/fins` 全部通过，无 expected red」；stop condition 规定任一现有 Fins test 变红即停止并回到 plan review。

**结论：F-1 的根因（S1 行为切换）已被消除，不是声明 expected red，而是行为切换不存在于 S1。** 无 blocker。

### 1.2 S1 剩余 diff 的零红可行性（逐项代码证据）

S1 仍然新增 required 字段、新 failure code、模块级 helper 提升与新协议方法。逐项核对其是否可能让现有 `tests/fins` 变红：

1. **`FilingUploadPublishedState.publication_identity` required 字段**（§6.1）：全仓 11 个构造点（生产 `_fs_filing_upload_state_core.py:94/134`；测试 `test_fins_ingestion_runtime.py:276/321/326/332/338/344`、`test_fins_service_runtime.py:79`、`test_cli_fins_commands.py:3067`、`test_service/test_fins_direct.py:746`）。逐一核实：测试构造点全部为注入 fake 或 `__post_init__` 校验场景，无一处与真实 repository read 做 dataclass 全等断言。真实 read 的既有断言（`test_fins_storage_atomicity.py:370/420/450/481`）全部为字段级断言（`.company_meta is None`、`.source_integrity.status`、`.source_meta["primary_document"]`），新增字段不破坏。四个 fixture 文件机械补 `publication_identity=None` 可行，与 §10.1 断言 9 一致。
2. **新 failure code `SOURCE_PUBLICATION_CONFLICT`**：`tests/fins/test_upload_failure.py:263-268` 的 exhaustive grouping 测试从模块 `upload_failure._FAILURE_CODES_BY_KIND` 派生，新 code 加入 `_STORAGE_FAILURE_CODES` 后自动保持绿，无需改既有断言。§6.4 映射表与 `upload_failure.py:175-209` 分组自检机制吻合。
3. **`_build_upsert_meta` 提升为模块级**：核实 `docling_upload_service.py:943-981` 方法体只使用四个参数与 `now_iso8601()`/`FinsSourceProvider.USER_UPLOAD`，**无任何 `self` 引用**；全仓唯一 caller 为 `:427`，其 keyword 参数（`previous_meta`/`source_fingerprint`/`document_version`/`base_meta`）与 plan §6.3 的模块级签名完全一致。提升是纯函数形态变化，prepare 行为不变。
4. **asset-source 常量替换**（§6.1 要求 `docling_upload_service.py` 与 `_fs_source_integrity.py` 删除各自 private alias 改用 contract 常量）：全仓 grep 确认 private alias 只存在于 `docling_upload_service.py:76-78`（`_AssetSource`/`_ASSET_SOURCE_ORIGINAL`/`_ASSET_SOURCE_DOCLING`），无任何测试导入该私有常量，删除/替换零红。
5. **新协议方法 `read_filing_upload_state_in_batch`**：唯一生产实现为 `FsFilingUploadStateRepository`（S1 allowed file 内）。所有测试 fake（`test_fins_ingestion_runtime.py:10635/10675`、`test_fins_ingestion_tools.py:790`、`test_fins_service_runtime.py:61`）均为不继承 Protocol 的普通类、经 `cast` 注入（`:10816` 已核实），pyright 无 conformance 破坏。

**结论：S1 的全部允许 diff 均有零红代码证据，`tests/fins` 全绿的声明可信。** 无 blocker。

### 1.3 S2 原子接线、无 expected-red 中间态

- §10.2 objective：「在单一原子 slice 内启用完整新 filing 行为：filing identical 继续 conversion、typed disposition 真实接线、shared publication route 与 SEC/CN/HK workflow/terminal/tests 同步落地；任何一步都不得单独形成 accepted checkpoint」。
- §10.2 exact allowed changes 第 4 点：「同步更新 Docling/publication owner tests、SEC/CN/HK workflow tests 与 runtime terminal 断言；不存在『生产接线完成但 workflow tests 尚未迁移』或反向的中间验收态」。
- §10.2 stop condition：「若 S2 无法在同一 implementation/review slice 同时完成 prepare 行为切换、shared route 与全部 workflow tests，停止并回到 plan review，不接受部分接线或 expected red」。
- §10.2 prerequisites 要求 S1 focused/full-Fins/pyright 全绿且既有行为未变。

**结论：S2 的原子性边界、禁止 expected-red 与 S1 前置条件三处一致冻结。** 无 blocker。

### 1.4 allowed files / commands / goal mapping 一致性

- **allowed files**：§10.1 S1 生产文件 9 个（storage 契约/reader/identity 投影 + docling helper + 纯 arbitration + failure），不含 `sec_upload_workflow.py`/`cn_pipeline.py`；§10.2 S2 生产文件 4 个（docling/publication/两 workflow），与 §8.1 表的「仅 S2」标注逐行一致。S1 测试 8 个（含四个 fixture 文件仅机械 diff）；S2 测试 5 个 + 2 README，与 §8.2 一致。S2 中 `test_fins_ingestion_runtime.py` 的权限明确限定为「在 S1 机械 fixture diff 之上新增 conflict/skipped fresh-action terminal 断言；不得改 production runtime」，与 §8.2 行一致。
- **commands**：S1/S2 均含完整 `pytest tests/fins -q`；S1 另含 focused 测试、逐文件 coverage（≥80）、全仓 pyright、`git diff --check`；S2 对应 coverage/pyright/diff 命令齐备，可执行。
- **goal mapping**：§10 表 S1 行「为成功信号 2/5/7 提供 owner contract；以成功信号 11 与既有全量行为零回归作为本 slice 验收；不声称成功信号 1/3/4/6/8/10 已实现」，S2 行「成功信号 1-11」；§3.2 成功信号 11 已同步改为「S1 全绿绝无 expected red；S2 以同一原子 slice 启用…」。三者无漂移。

**结论：allowed files / commands / goal mapping 一致。** 无 blocker。

## 2. C-F2 验证

1. **唯一九字段 owner 已钉死**：§5 ownership 表、§6.3（「该 helper 是九字段 staging meta 的唯一构造 owner：现有 prepare call path 与 `rebase_prepared_filing_create_overwrite()` 必须共同调用它，禁止复制或局部拼装」）、§10.1 S1 test 6（「模块级 `_build_upsert_meta()` 是九字段唯一构造 owner；现有 prepare 与 rebase helper 都调用它，无复制字段拼装」）三处一致。plan 列的九字段（`updated_at`/`first_ingested_at`/`created_at`/`document_version`/`source_fingerprint`/`ingest_complete`/`source_provider`/`is_deleted`/`deleted_at`）与实现 body（`docling_upload_service.py:966-981`）**逐字段精确吻合**，无遗漏无多余。
2. **fresh timestamps 闭合**：现有 helper 已从 `previous_meta` 保持 `first_ingested_at/created_at`（`:967-974`）；plan 钉死 rebase 以 fresh source meta 为 `previous_meta` 真源，S1 test 6 断言「rebase candidate 的 `previous_meta` 取 fresh meta，`first_ingested_at/created_at` 精确保持 fresh 值」，S2 test 3 端到端再断言。闭环。
3. **version 规则闭合**：plan 钉「同 fingerprint 保持 fresh `document_version`，异 fingerprint 在 fresh version 上递增」；`_resolve_document_version`（`:1393-1418`）实现语义与之一致（same fingerprint + identical_skip_safe → 保持；不同 fingerprint → increment）。S1 test 6 + S2 test 3 双层断言。
4. **revision 职责闭合**：plan 钉「staging meta 不携带 revision 真源；真正 publish 时 storage source-document owner 必须覆盖写入新 revision」；S1 test 7 要求 owner-level real storage publish 断言新 revision 且与 meta 断言分开，S2 test 3 端到端断言。闭环。

**结论：C-F2 的唯一 owner、九字段清单、fresh 时间/version 规则与 revision 职责分离均闭合。** 无 blocker。

## 3. C-R1/C-R2 非回归确认

- **C-R1**：§6.3 S2 接线仍「该判定与既有 skip 语义同源，不在 publication owner 中从 raw action 或 fingerprint 重算」；§7.3.A 仍「该分支不限 raw action 为 `auto`；它精确保留现有 `_can_skip_upload` 能够判定的 sequential identical skip，特别包含 explicit `update` + `overwrite=False`」；§7.4 第 8 行仍「changed `COMPLETE -> COMPLETE` 即使 identity exact equal 也不得冒充 stable 重传或 MISSING 收敛」。未被修订触碰。
- **C-R2**：§7.1 步骤 4/7 双 checkpoint、§6.4 两行取消映射（等待 writer 期间取消与 arbitration 后取消均 rollback-first cancelled、rollback 失败 path-free `STORAGE_IO`）、S2 test 12 三类候选覆盖均保留。未被修订触碰。

**结论：C-R1/C-R2 均未被破坏。** 无 blocker。

## 4. Findings

无 blocker 级 finding。无 material 级未修复 finding。

两条 non-blocking 观察（不属于 finding 格式的 blocker，记入 open questions / residual）：

- 观察 1：§6.1 声称 `_fs_source_integrity.py` 也有需删除的「各自的 filing asset-source private alias/constant」——实际该文件无此类私有常量（仅 `docling_upload_service.py:76-78` 有）。plan 文本轻微过度陈述，但可执行要求（删除 docling 侧 alias、改用 contract 常量）不受影响。
- 观察 2：S1 identity 投影若实现期需要给 `_SourceKindPublicationInspection` 增加 required 字段，`tests/fins/test_fins_storage_atomicity.py:7697` 有一处既有直接构造需机械补参——该文件在 S1 allowed tests 内，但 §10.1 的「仅机械 diff」授权只覆盖四个 fixture 文件。此风险已由 S1 stop condition（任一现有 Fins test 变红即回 plan review）兜底，plan 无自相矛盾。

## 5. Open questions

无 blocking open question。观察 2 是否触发由 S1 实现期事实决定：若投影可完全从既有 `_InspectedSourceFile`/`SourceSnapshotFileDescriptor`/`business_meta` 字段形成（当前证据表明 descriptor 已承载文件级事实），则无需改 inspection dataclass 形状。

## 6. Residual risks 与建议跟踪去向

| # | 风险 | 分类 / 去向 |
| --- | --- | --- |
| r-1 | S1 四 fixture 文件机械 diff 之外，若 identity 投影需改 inspection dataclass，`test_fins_storage_atomicity.py:7697` 需机械补参 | S1 stop condition 兜底；owner 为本 work unit S1 slice；不构成 blocker |
| r-2 | §6.1 关于 `_fs_source_integrity.py` private alias 的过度陈述 | 实施 Agent 按可执行要求（删 docling alias、用 contract 常量）执行即可；无需改 plan |

上一轮 DS R-1（S1→S2 expected-red 中间态）已与 C-F1 同源从根因消除；R-2/R-3/R-4 及 later-work-unit 分类维持上轮结论不变。

## 7. Final plan review conclusion

**pass**

C-F1：S1 的行为切换已整体移入 S2（F-1 根因消除，非 expected-red 声明）；S1 全部允许 diff（required 字段 11 构造点、新 failure code、meta helper 提升、常量替换、新协议方法）逐一有零红代码证据，完整 `tests/fins` 全绿声明可信；S2 原子接线与禁止 expected-red 在 objective/allowed changes/stop condition 三处一致；allowed files、validation commands、goal mapping 与 §8.1/§3.2 一致。C-F2：`_build_upsert_meta` 被钉为现有 prepare 与 rebase 的唯一九字段 owner，九字段清单与实现 body 逐字段吻合，fresh 时间/version 规则与 storage revision 职责分离均有 S1 owner 级与 S2 端到端双层断言闭合。C-R1/C-R2 条款未被本次修订破坏。

无 blocker。两条 non-blocking 观察已记入 open questions / residual risks，不影响 implementation authorization。

本 re-review 未修改任何 plan/生产代码/测试/README，未 commit。
