# Code Review — UF-FIX01 Implementation Fix Re-Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `69bc9d2af91788303c839d01ad937cf9b802eb1d`
- HEAD: `5a6d80c2b0e87c5810a3bbedb1ab74524a205adc`
- Commits reviewed: `3caca6fa` (implementation) → `54c867f8` (adjudication) → `5a6d80c2` (close findings)
- Adjudication input: `docs/gateflow/uf-fix01-validation-atomic-boundary-implementation-review-adjudication-20260813.md`
- Two-path initial review inputs: `docs/reviews/code-review-20260813-114536.md` (MiMo), `docs/reviews/code-review-20260813-115401.md` (DS)
- Implementation fix artifact: `docs/gateflow/uf-fix01-validation-atomic-boundary-implementation-fix-20260813.md`
- Output file: `docs/reviews/code-review-20260813-123814.md`
- Included scope: 38 files changed (production + tests + READMEs),逐行走读 CLI → Service → Runtime → Runner → SEC/CN workflow → DoclingUploadService → upload_failure 全链路
- Excluded scope: `docling_process_converter.py`（零 diff）、`_fs_repository_factory.py`（零 diff）、Host/Engine（零 diff）、frozen evidence/registry
- Parallel review coverage: 2 路 Explore subagent（A1 全链路验证、A2-A5/C1-C3 逐项验证）+ 主 reviewer 独立走读 CLI 入口、runner handoff、workflow authoritative recheck、typed catch 顺序、prevalidation 异常投影

## 逐项裁决

### A1 — authoritative request/decision handoff — CLOSED/PASS

裁决要求：SEC/CN/HK facade 与 workflow 接收 typed validated request；workflow fresh recheck + identity fail-closed；只允许 authoritative resolved_action/published_state/company_meta_decision 驱动 prepare/stage/commit；prepare_upload(previous_meta=...)不再自行读 state；filing 以 stage_upload_company_meta_decision 替代旧 upsert_company_meta_for_upload。

逐项核销：

| 子项 | 状态 | 证据 |
| --- | --- | --- |
| Runner 透传 typed request 不拆散参 | PASS | `service_runtime.py:157` 签名 `request: ValidatedFinsUploadFilingRequest`；`:179-189` 直接传 `request` 给 SEC/CN facade，无字段提取 |
| SEC facade 签名收 typed request | PASS | `sec_pipeline.py:745` `upload_filing(request: ValidatedFinsUploadFilingRequest)`；`:774` `upload_filing_stream` 同签名 |
| CN facade 签名收 typed request | PASS | `cn_pipeline.py:740` `upload_filing(request: ValidatedFinsUploadFilingRequest)`；`:769` `upload_filing_stream` 同签名 |
| Workflow fresh state read via 注入 repository | PASS | `sec_upload_workflow.py:167-170` 调 `host._filing_upload_state_repository.read_filing_upload_state`；`cn_pipeline.py:787-790` 同模式 |
| Workflow 调同一 validator 做 authoritative recheck | PASS | `sec_upload_workflow.py:171-174` 调 `validate_fins_upload_filing_request(raw_request, published_state=fresh_state)`；`cn_pipeline.py:791-794` 同 |
| Identity assertion fail-closed | PASS | `sec_upload_workflow.py:175` 调 `_assert_authoritative_filing_identity`；`:352-374` 逐项比对 canonical ticker/document_id/internal_document_id，不一致 raise `RuntimeError`；`cn_pipeline.py:795,1796-1818` 同模式 |
| 只消费 authoritative resolved_action | PASS | `sec_upload_workflow.py:186` `normalized_action = authoritative_request.resolved_action`；`cn_pipeline.py:806` 同 |
| 只消费 authoritative published_state.source_meta 作为 previous_meta | PASS | `sec_upload_workflow.py:185` `previous_meta = authoritative_request.published_state.source_meta`；`cn_pipeline.py:805` 同 |
| 只消费 authoritative company_meta_decision | PASS | `sec_upload_workflow.py:235-239` 调 `stage_upload_company_meta_decision(decision=authoritative_request.company_meta_decision)`；`cn_pipeline.py:855-858` 同 |
| prepare_upload 接收 previous_meta 参数 | PASS | `docling_upload_service.py:238` 签名含 `previous_meta: Mapping[str, JsonValue] \| None`；`:288` 直接消费该参数 |
| prepare_upload 不自行读 state | PASS | `docling_upload_service.py:226-310` 全函数无 `_safe_get_document_meta` 或等价 state 读取调用 |
| filing 以 stage_upload_company_meta_decision 替代 upsert_company_meta_for_upload | PASS | SEC filing 路径 `sec_upload_workflow.py:235` 调 `stage_upload_company_meta_decision`；CN filing 路径 `cn_pipeline.py:855` 同。`upsert_company_meta_for_upload` 仅在 material 路径调用（`:530`/`:1137`） |

**结论：A1 全部子项 CLOSED/PASS。** typed validated request 从 CLI prevalidation 经 Service → Runtime → Runner → SEC/CN facade → workflow 全链路不拆散参；workflow 独立做 fresh snapshot + 同一 validator + identity fail-closed；authoritative resolved_action/published_state/company_meta_decision 是唯一驱动源。

### A2 — 前置冲突投影为 typed usage 而非 generic/storage failure — CLOSED/PASS

裁决要求：普通冲突由同一 validator 以 closed actionable usage reason 拒绝；不以异常字符串或 generic/storage 文案重分类；恢复 owner-level exact reason assertions。

核销：

- `validate_fins_upload_filing_request` 在 workflow authoritative recheck（`sec_upload_workflow.py:171`、`cn_pipeline.py:791`）中调用，precondition 冲突（CREATE_TARGET_EXISTS/UPDATE_TARGET_MISSING/COMPANY_NAME_REQUIRED）在 `prepare_upload` 前以 `FinsUploadUsageError` 拒绝，携带精确 usage code 与 actionable message。
- `docling_upload_service.py:295-298` 保留的 `FileExistsError`/`FileNotFoundError` 仅在 workflow 级 TOCTOU 竞争时可达（recheck 与 prepare 之间状态变化），属于 defense-in-depth。
- `test_sec_pipeline_upload_filing_stream.py:573-612` 的 `test_upload_filing_stream_stale_company_meta_requires_company_name` 断言精确 usage code `COMPANY_NAME_REQUIRED` 与 exact actionable message `"当前公司缺少有效元数据；create/update 必须提供 --company-name"`，未被弱化为 generic 文案。

**结论：A2 CLOSED/PASS。**

### A3 — S3 原子状态机 owner tests — CLOSED/PASS

裁决要求：SEC 与 CN 至少各一组 BatchToken identity、begin/commit/rollback 计数、fresh/existing tree 与逐文件 SHA-256、company/source stage rollback、并发旧派生值丢弃测试。

核销：

| 断言 | SEC 证据 | CN 证据 |
| --- | --- | --- |
| 同一 BatchToken identity（company+source） | `test_sec_pipeline_upload_filing_stream.py:466-472` `assert company.stage_tokens == batching.begin_tokens` | `test_cn_pipeline.py:848-852` 同模式 |
| begin=1/commit=1/rollback=0 success | 同上 `assert len(batching.begin_tokens) == 1; assert batching.rollback_tokens == []` | 同上 |
| rollback=1/commit=0 stage failure | `:697-741` `assert batching.commit_tokens == []; assert batching.rollback_tokens == batching.begin_tokens` | `test_cn_pipeline.py:1060-1103` 同模式 |
| rollback 后 tree/SHA-256 不变 | `:732` `assert published_tree_sha256(...) == before_tree == {}` | `test_cn_pipeline.py:1095` 同 |
| 并发旧派生值丢弃 | `:744-795` `test_upload_filing_fresh_recheck_discards_stale_action_and_company_decision` 断言 stale create→fresh update、stale company name 未写入 | `test_cn_pipeline.py:1107-1158` 同模式 |
| rollback failure primary/recovery evidence | SEC suite 覆盖（fix artifact 声明） | — |

**结论：A3 CLOSED/PASS。**

### A4 — prevalidation operational failure path-free — CLOSED/PASS

裁决要求：prevalidation I/O/lock/corruption 进入专用 typed operational exception；CLI 固定 path-free bounded reason、exit 1；operator log 保留 chained internal cause。

核销：

- `service_runtime.py:85-93`：`OSError`/`RuntimeFileLockError` → `FinsUploadPrevalidationError(fins_upload_prevalidation_io_failure())`；`ValueError` → `FinsUploadPrevalidationError(fins_upload_prevalidation_corruption_failure())`。
- `upload_failure.py:163-202`：两个 factory 函数返回 `FinsUploadFailureReason`，message 经 `_validate_failure_reason_text`（`:280-297`）拒绝 `/`/`\\` 路径分隔符。
- `fins.py:208-211`：`FinsUploadPrevalidationError` catch 在 generic `Exception` 前，stderr 只含 `exc.failure.message`（path-free），raw exception 仅进 `_LOGGER.exception`。
- `test_fins_commands.py:1109-1181`：`test_upload_filing_prevalidation_io_failure_is_typed_bounded_and_path_free` 断言 exit 1、stderr 不含 `tmp_path`/`Traceback`/`PermissionError`。
- `test_fins_commands.py:1183-1232`：`test_upload_filing_prevalidation_descriptor_corruption_is_typed_and_path_free` 断言 exit 1、stderr 不含 `tmp_path`/`Traceback`/`ValueError`。

**结论：A4 CLOSED/PASS。**

### A5 — CLI docstring 准确性 — CLOSED/PASS

裁决要求：更新 `_upload_filing_stream` 的异常说明。

核销：

- `fins.py:634-652`：docstring 写 `:raises Exception: Service/runtime stream 打开失败时原样透传`，与"只透传 validated request、不自行校验 ticker/files"的实现一致。旧 `:raises CliFinsUsageError:` 已删除。

**结论：A5 CLOSED/PASS。**

### C1 — frozen CLI usage matrix 完整覆盖 — CLOSED/PASS

裁决要求：对 frozen UF cases 的每个 usage code 以参数化 CLI owner test 断言 exit 2、stdout empty、stderr exact、factory/service zero、workspace tree zero。

核销：

- `test_fins_commands.py:914-1107`：`test_upload_filing_usage_matrix_precedes_service_factory_and_workspace_mutation` 参数化覆盖 25 个 frozen case：UF-003/004/005/006/015/016/017/018/019/021/022/023/024/026/027/028/030/031/032/033/034/035/036/037/038。
- 每 case 断言（`:1099-1106`）：`exit_code == EXIT_USAGE_ERROR`、`captured.out == ""`、`captured.err == f"dayu-cli upload_filing: {expected_reason}\n"`、`factory_calls == []`、`service.upload_filing_requests == []`、`service.stream_calls == []`、`not workspace_root.exists()`。
- 测试通过真实 CLI boundary（`cli_main.main(argv)`），非 validator-level fake。
- UF-007–014/020/025/029 属 download-side 或不适用于 upload_filing，不在本 WU scope。

**结论：C1 CLOSED/PASS。**

### C2 — typed catch 顺序与 operator root cause — CLOSED/PASS

裁决要求：filing workflow 显式按 cancelled → Docling → OSError/storage → generic 顺序处理；public result 只投影 closed typed reason；operator log 保留原始 cause；marker tests 固定顺序。

核销：

- `sec_upload_workflow.py:286-349`：显式 catch 顺序 — `DoclingConversionCancelledError`(`:286`) → `DoclingConversionError`(`:323`) → `OSError`(`:332`) → `Exception`(`:341`)。
- `cn_pipeline.py:906-969`：同顺序 — `:906` → `:943` → `:952` → `:961`。
- public result 只含 `failure_reason.to_json()`（path-free closed typed reason），operator log 含 `_LOGGER.exception(...)` 保留原始 cause。
- `test_sec_pipeline_upload_filing_stream.py:843-926`：`test_upload_filing_observably_classifies_cancelled_docling_storage_and_generic` 参数化 4 种异常，断言 `str(error) not in str(result)`（cause 不泄漏 public）且 `str(error) in caplog.text`（cause 在 operator log）。
- `test_cn_pipeline.py:1161-1219`：CN 同模式 marker test。
- `FinsUploadUsageError` 从 authoritative recheck（workflow try 块前，`:171`）自然传播到 CLI，被 `fins.py:205` catch 为 exit 2 — 不落入 workflow 的 generic catch。

**结论：C2 CLOSED/PASS。**

### C3 — material non-goal 保持 — CLOSED/PASS

裁决要求：material 继续使用既有 previous-meta owner、事务与 str(exc) 用户语义；SEC/CN material regression tests 固定该 non-goal。

核销：

- `sec_upload_workflow.py:498-502`：material 路径仍调 `host._safe_get_document_meta(..., SourceKind.MATERIAL)` 自行读 previous_meta。
- `cn_pipeline.py:1105-1109`：同模式。
- `sec_upload_workflow.py:606-633`：material failure handler 使用 `message=str(exc)`，无 `fins_upload_failure_from_exception`。
- `cn_pipeline.py:1213-1240`：同模式。
- `test_cn_pipeline.py:961-999`：`test_upload_material_failure_preserves_existing_user_visible_semantics` 断言 `result["message"]` 为 raw str(exc)、`"failure" not in result`。
- material 路径的 `upsert_company_meta_for_upload`（`sec_upload_workflow.py:530`、`cn_pipeline.py:1137`）未被替换。

**结论：C3 CLOSED/PASS。**

## 已核查通过项（无 finding，仅记录）

- **CLI prevalidation 在 Service factory 前**：`fins.py:240-243`（prevalidation）vs `:244`（factory）。
- **CLI catch 顺序正确**：`FinsUploadUsageError`(`:205`) → `FinsUploadPrevalidationError`(`:208`) → `FinsDirectStreamProtocolError`(`:212`) → `KeyboardInterrupt`(`:215`) → `Exception`(`:217`)。
- **23-member FinsUploadUsageCode 闭集**：`ingestion_runtime.py:624-786` exhaustive mapping，测试逐成员穷尽断言。
- **lazy bootstrap**：`DefaultFinsRuntime.create` 使用 `create_directories=False`；`FsFinsIngestionJobStore.__post_init__` 零副作用；`create_job` 首写 mkdir。
- **storage snapshot**：fresh absent 在 guard/lock/mkdir 前短路；单 guard 双读；corruption fail closed。
- **UF-FIX09 共享 converter 不回归**：composition test 断言 SEC upload、CN upload、CN/HK download 共享同一 `ProcessDoclingConverter` 实例；`docling_process_converter.py` 零 diff。
- **non-goals 不回归**：两套 suffix allow-list 零 diff；year 0 合法域保留；date 无新 domain；Host/Engine 零 diff。
- **导入所有权**：`upload_failure.py` 由 pipeline 与 runtime 共同依赖，无 pipeline→ingestion_runtime 反向导入；无新增 `hasattr/getattr`、str(exc) 分类、lazy import、compat shim。
- **production diff audit**：filing 路径不再调用旧 company upsert；`prepare_upload` 不再读取 previous meta；无补偿删除、二次 batch 或 late rollback。

## Open Questions

- **UF-PF01 focused-real evidence**：按 Controller 明确指示未运行，属于 re-review PASS 后下一 approved gate，不构成本轮缺口。C1 已通过真实 CLI boundary 覆盖全部 25 个 frozen usage case 的 exit/stdout/stderr/factory/service/tree 断言，UF-PF01 的 CLI owner 证据基础已建立。
- 无。

## Residual Risk

- `fins_upload_failure_from_exception` 对未来新增 `DoclingConversionFailureKind` 会 KeyError（当前 exhaustive，仅演进风险）。
- `stage_upload_company_meta_decision` 为死代码的顾虑已消除 — SEC/CN filing workflow 均已调用。
- material 事务的 failure 文案从 `str(exc)` 保持不变（C3 确认），但 material 路径的 `upsert_company_meta_for_upload` 仍独立做 freshness 判断，与 filing 路径的 `stage_upload_company_meta_decision` 模式不一致 — 这是 material non-goal 的 deliberate 保留。
- 未执行仓库全量 pytest；已执行 plan 指定受影响测试集（591 passed, 1 skipped）与 pyright（0 errors）。

## Conclusion

**PASS。** A1–A5/C1–C3 全部 CLOSED/PASS。

implementation fix 完整实现了 adjudication 要求的所有修复：

1. **A1**：typed validated request 从 CLI → Service → Runtime → Runner → SEC/CN facade → workflow 全链路不拆散参；workflow 独立做 fresh snapshot + 同一 validator + identity fail-closed；authoritative resolved_action/published_state/company_meta_decision 是唯一驱动源；`prepare_upload(previous_meta=...)` 不再自行读 state；filing 以 `stage_upload_company_meta_decision` 替代旧 `upsert_company_meta_for_upload`。
2. **A2**：precondition 冲突由 validator 以 closed actionable usage reason 拒绝；owner-level exact reason assertions 已恢复。
3. **A3**：SEC/CN 各有完整 BatchToken identity/计数/SHA-256/rollback/并发丢弃测试。
4. **A4**：prevalidation operational failure 经 typed bounded path-free 投影，CLI 固定 exit 1 + 无绝对路径/traceback。
5. **A5**：docstring 与实现一致。
6. **C1**：25 个 frozen usage case 全部参数化穿过真实 CLI owner boundary。
7. **C2**：explicit catch order（cancelled → Docling → OSError → generic）+ marker tests 固定顺序；operator log 保留 cause。
8. **C3**：material 继续使用既有 previous-meta owner 与 str(exc) 语义，regression tests 固定。

Review artifact: `docs/reviews/code-review-20260813-123814.md`
