# F18 final closeout DeepReview

- 审查模式：`$deepreview 当前改动` / Current Changes Mode。
- 审查时间：2026-08-08 17:38:12 CST。
- 当前分支：`codex/interactive-oracle`。
- Selected base：`HEAD=1c92a5bebdf6dd428eaf7577c5d97f5105079d6e`；用户限定的 F18 当前工作树差异优先于更宽的 main diff。
- 审查范围：F18 tracked handbook/plan/oracle closeout diff，新增 material/final-closeout、两份 plan review、两份 final code/evidence review，以及冻结 public bundle `pr190-f18-b2-fixed-XUmH8YBg` 的只读完整性与 owner contract。
- 明确排除：产品/provider 写操作、external public bundle 写入、提交/暂存，以及 ownership 不明的 `docs/reviews/plan-review-20260808-095346.md`；未读取、触碰或暂存该文件。

## Binary verdict

**FAIL / publication-nonconforming**

Product/setup implementation verdict 可保持 `PASS / no product change / setup correction`，但 F18 evidence publication 不满足已确认 plan 的 minimum contract，不能作为 B2 mandatory evidence 或 readiness proof。当前 tracked closeout 已诚实投影该失败；这不会让冻结 bundle 追溯合规。

## Findings

### [High] 冻结 public tree 缺少 minimum publication owner artifacts

直接证据：

1. `docs/gateflow/pr-190-f18-plan-20260808.md:255-266` 明确要求 public tree 至少包含：带 per-chain budgets、actual counts、terminal refs 的 `observation-summary.json`；每条 attempted chain 的 path-redacted Host resolver/Tool Trace analysis JSON/Markdown；`execution-index.json`；再由 digest 与最终 scan 封口。
2. 冻结 public tree 实际只有 observed report、observation summary、material audit、calibration summary、digest 与 secret scan 六个文件。没有 per-attempt public resolver/Tool Trace analysis，也没有 `execution-index.json`。
3. public `observation-summary.json.attempts` 只有 outcome/reason/private ref，provider ledger 只有聚合总数；没有 plan 要求的 per-chain budget、actual count 和 terminal ref。private `execution-index.json` 与 report 文字不能替代明确要求的 public structured owner contract。
4. `secret-scan.json` 的扫描集合准确覆盖 digest 加四个 payload，共 5 files / 27,450 bytes；这只证明当前不完整 tree 的完整性与卫生，不证明缺失 artifact 已发布。

影响：public consumer 无法只依靠 path-redacted owner output 逐链核验 attempt identity、terminal、预算、skip/seal reason 和 relative refs。两条 non-covering 运行与一条 provider-not-started 运行虽被诚实保留，仍不能满足 publication closeout。

修复边界：现有 `secret-scan.json` 已是 final writer，F18 bundle 必须保持 immutable `FAIL/nonconforming`。F19 只能在新 bundle 中从 owner truth 发布完整 per-attempt analyses、public execution index 与 per-chain summary fields，再重建 digest/final scan；不得用 private capture、tracked 文本或 analyzer fallback 补偿，也不得覆盖旧结果。

### [High] `material_audit_sha256` 未声明 digest domain，跨 artifact identity 不能 fail-closed

直接证据：

1. public `observation-summary.json.material_audit_sha256` 为 `37eee3c9665c21874b8caf545acd0a49af0e2d5faa69a52a257ff4702acf10e4`。
2. `material-audit-summary.json` 的 file-byte SHA-256 为 `6399d1280a7c567ed96e563739f0338b32371f01ea3c578357582ff38c15af8f`；它也是 public digest、secret scan 与 tracked closeout 共同冻结的 artifact identity。
3. sorted compact JSON 保留 writer newline 时为 `18284f2ecd59a08ce1ad2887e6d9c6f3cdefc88a3053f082c8408511f058f2c8`，去掉末尾 newline 时可复现 `37eee3c...`。因此 `37eee3c...` 不是随机值，但字段/schema 没有声明 file bytes、canonical object、serialization algorithm/version 或 newline policy，消费者不能凭字段名确定唯一 owner 语义。

影响：外层 digest 能证明包含该歧义字段的 summary 未被篡改，却不能证明 summary 指向 bundle 中同一个 material artifact；reviewer 不能用猜测 serializer 规则代替公共 contract。

修复边界：F19 summary producer 应直接从最终 artifact owner bytes 生成明确命名的 file digest，或显式声明 canonical digest domain、serialization 与版本，并在 final scan 前做 cross-file exact validation。不得原地改写已封口的 F18 bundle。

## 已通过的直接核验

### Tracked closeout 与状态隔离

- `docs/gateflow/pr-190-f18-final-closeout-20260808.md:5-15` 已分别给出 product/setup `PASS`、publication `FAIL/nonconforming`、real observation `needs-more-evidence` 与 B2 `unadjudicated`；没有把局部实现 verdict 冒充整体 closeout PASS。
- plan、material amendment、handbook、oracle adjudication 与 final closeout 已同源记录两项 publication findings，且明确禁止把该 bundle 用作 readiness proof。
- `docs/cli_ci_scenarios.json`、`docs/cli_ci_oracles.json` 相对 HEAD 无 diff；B2 仍为 `status=unadjudicated`、pending adjudication、after-adjudication applicable identity，两个 registry 均为 `calibration`。未替用户接受 B2，overall readiness 未标 ready。
- `docs/gateflow/pr-190-f18-final-closeout-20260808.md:41-45` 将 F19 隔离为新 work unit、新 deadline、新 bundle、fresh workspace，并禁止复制 F18 durable state、覆盖 non-covering 结果或伪造模型未触发分支；handoff 边界安全。

### Publication hashes、final writer 与原始结果

- 六个 public 文件的 byte SHA-256 均与 material/final closeout 完全一致：report `0969738...`、summary `1bc5eeb1...`、material `6399d128...`、calibration `2b855885...`、digest `36fd642a...`、secret scan `40f7885d...`。
- digest 精确列出四个 payload；final scan 精确列出 digest 加四个 payload，统计为 5 files / 27,450 bytes，secret hits、absolute-path violations、validation errors 均为零。
- 纳秒级 mtime 顺序为 calibration → material → summary → report → digest → secret scan；`secret-scan.json` 确为最后 writer，审查期间未写 external bundle。
- 两条真实 segment 保持 `RUN_SUCCEEDED` 但 non-covering，合计 7 ordinary calls / 42.5896s / 0 compactor calls；第三条保持 CLI spawn 前拒绝、provider not started。没有隐藏失败、覆盖结果或把零 compactor calls 冒充 B2 coverage。

### Owner、scope 与 hygiene

- target evidence repo HEAD 为用户指定的 `ce0c171a022a073c6355ace44e7c5e34a668d4bb`。production owner 代码直接支持 scene override `max_iterations=20`、Host `ORDINARY`/`CONTINUATION` stage 分工和 `read_section_max_chars=80,000`；material setup correction 没有要求产品兼容补偿。
- 当前 tracked diff 只有 `docs/cli_ci.md`、F18 plan 与 oracle adjudication；没有 `dayu/`、tests、schema、runtime、harness、README 或 registry JSON 改动。
- tracked diff 与全部新增 F18 artifacts 的 whitespace check 通过；registry JSON 可解析，状态断言通过。
- public exact-secret/environment-value 与 absolute-private-path 检查均无命中。reviewed adjudication 文档第 29 行存在 HEAD 已有的 F15 历史绝对 Root，但不在本次 changed hunk；本次 F18 新增/修改内容没有引入新的绝对私有路径。

## Adversarial failure pass

- 用顶层 SHA/final scan 掩盖 publication 缺件：发现并阻断。
- 用可复现但未声明的 serializer digest 冒充 artifact identity：发现并阻断。
- 用 `RUN_SUCCEEDED` 或进程状态冒充 scenario coverage：未发现。
- 用 private execution index/Tool Trace 补造 public contract：当前 closeout 已明确禁止。
- 提前接受 B2、翻转 registry/readiness 或污染 B1：未发现。
- F19 复用 F18/Trial durable state、deadline 或 publication root：未发现。
- 下游展示层解释性补偿 owner 错误：当前 tracked closeout只报告失败，没有改写 public truth。

## Open questions

无阻断性 open question；两项 finding 均有直接 owner/byte-level 证据。

## Residual risks

1. B2 的 replacement、same-boundary repair、budget-exhausted fallback、accepted durable truth 与 reconnect 均未观察；必须在用户裁决前继续保持 `unadjudicated` / not ready。
2. MiMo 自然分支仍可能不触发；F19 应如实输出 `needs-more-evidence`，不得增加 output injection 或无界重跑。
3. 旧 Trial2 sequence 327 cause、cross-opener profile contract、cold analyzer `compactor_responses=0` 与 provider-native request id unavailable 继续作为既有 residual；本审查未将其改写为 B2 mandatory gap。

## Final conclusion

**FAIL / publication-nonconforming**。更新后的 tracked closeout 对失败性质、B2 状态与 F19 隔离的记录是诚实且 owner-aligned 的；F18 bundle 仍因 public minimum 缺件与未声明 digest domain 不能通过 final closeout。现有 bundle不得回写，修正只能进入 F19 新 work unit、新 bundle与新的 final scan。
