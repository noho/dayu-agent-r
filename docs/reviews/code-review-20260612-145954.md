# Code Review (Re-Review)

## Metadata

- Reviewer: AgentMiMo
- Mode: current changes scoped re-review
- Work unit: WU-CM-05 LLM Compaction Proposal Typed Parsing
- Slice: WU-CM-05-S2 fix re-review
- Previous review artifacts: `docs/reviews/code-review-20260612-144954.md` (AgentDS), `docs/reviews/code-review-20260612-145145.md` (AgentMiMo)
- Accepted finding: `code-review-20260612-144954.md` Finding 1
- Fix artifact: `docs/reviews/wu-cm-05-s2-implementation-report.md` (Fix Gate Update section)
- Base: `7f2ce2c5` (S1 accepted commit)
- Branch: `work/cm-05-06-08-09`
- Output file: `docs/reviews/code-review-20260612-145954.md`

## Scope

- Mode: current changes (S2 + fix dirty diff)
- Included scope:
  - `dayu/host/llm_compaction.py` S2 diff (`_required_enum` diagnostic change)
  - `tests/host/test_llm_compaction.py` S2 + fix diff (17 new tests + 2 updated tests + 1 new helper)
  - `docs/reviews/wu-cm-05-s2-implementation-report.md` (Fix Gate Update)
- Excluded scope:
  - `docs/host/issues-implementation-control.md`（Controller bookkeeping dirty file，仅核对状态）
  - S1 accepted commit `7f2ce2c5` 已通过 review，不重新审查
- Parallel review coverage: 无

## Verification

- Tests: **37 passed** in 0.26s (py311, `source .venv/bin/activate && pytest tests/host/test_llm_compaction.py -q`)
- Pyright: **0 errors, 0 warnings, 0 informations** (`python -m pyright dayu/host/llm_compaction.py tests/host/test_llm_compaction.py`)
- Implementation report Fix Gate Update claims independently verified

## Finding 1 Fix Verification

### 修复状态：已修复

`code-review-20260612-144954.md` Finding 1 要求：新增 explicit `forward_intents[0].intent_type` invalid enum diagnostic test，与 `evidence_kind`、`status`、`reason` 三个已覆盖的 enum 字段对齐。

**新增测试** (`tests/host/test_llm_compaction.py:480-507`)：

```python
def test_parse_conversation_compact_output_vnext_reports_forward_intent_type_enum_value() -> None:
    # ... 构造 forward_intents[0].intent_type = "bad_intent_type" 的非法 proposal
    with pytest.raises(
        LLMCompactionProposalError,
        match=r"forward_intents\[0\]\.intent_type.*bad_intent_type",
    ):
        parse_conversation_compact_output_vnext(compact_input, json.dumps(proposal, sort_keys=True))
```

**验证要点**：

1. 测试通过 public parser entry point `parse_conversation_compact_output_vnext` 调用，不依赖内部实现
2. match pattern `r"forward_intents\[0\]\.intent_type.*bad_intent_type"` 同时断言 field path 和 illegal value，与 `evidence_kind`/`status`/`reason` 测试格式一致
3. `_required_enum` (`llm_compaction.py:1108-1114`) 对所有 4 个 enum 字段统一执行 redact + truncate + raise，测试正确反映了这一行为

### Fix 范围检查

| 变更文件 | 变更类型 | 是否预期 |
|---|---|---|
| `tests/host/test_llm_compaction.py` | 新增 1 个测试 | ✓ Finding 1 要求 |
| `docs/reviews/wu-cm-05-s2-implementation-report.md` | Fix Gate Update 段落 | ✓ gate 流程 |
| `dayu/host/llm_compaction.py` | 无新变更（S2 已有） | ✓ 不在 fix scope |
| `docs/host/issues-implementation-control.md` | 控制文档状态更新 | ✓ gate 流程 |

Fix 未触及生产代码语义，仅新增测试和更新 gate 文档。

### S2 Invalid Enum Diagnostic Coverage 自洽性

| Enum 字段 | 测试函数 | 覆盖 |
|---|---|---|
| `evidence_backed_facts[i].evidence_kind` | `test_..._reports_fact_evidence_kind_path` | ✓ |
| `forward_intents[i].intent_type` | `test_..._reports_forward_intent_type_enum_value` | ✓ **[fix 新增]** |
| `forward_intents[i].status` | `test_..._reports_forward_status_enum_value` | ✓ |
| `reference_continuity_items[i].reason` | `test_..._reports_reference_reason_enum_value` | ✓ |

4/4 enum 字段全部有 explicit invalid value diagnostic 覆盖。`_required_enum` 是统一实现，所有字段共享同一 redact → truncate → raise 路径，测试断言格式一致（`{field_path}.*{bad_value}`）。自洽。

### 新增问题检查

未发现新增 correctness / stability / maintainability 问题。Fix 为纯测试新增，不涉及生产代码变更、类型签名变更、异常类型变更或新的依赖引入。

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

1. **DS-F02 延期至 Slice 3**：`_proposal_json` (line 761-775) 和 `_required_mapping` (line 821-831) 仍使用 `cast`。此风险已由 S1 review 记录并在 implementation report 中明确延期至 Slice 3，不阻塞 S2。

2. **枚举值脱敏覆盖范围**：`redact_sensitive_diagnostic_values` 仅覆盖 Bearer token、API key、password、secret、token 赋值模式。对于 enum 字段的典型非法值完全足够。若未来 LLM 在 enum 字段中产生非凭证类的敏感内部标识符，当前脱敏不会捕获，但 240 字符截断限制了滥用窗口，风险可接受。

## Conclusion

**PASS**。Finding 1 已修复，fix 范围正确，S2 invalid enum diagnostic coverage 完整自洽，无新增问题，pyright/pytest 验证通过。
