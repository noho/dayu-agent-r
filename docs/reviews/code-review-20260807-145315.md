# Code Review

## Scope

- **Mode**: current changes
- **Branch**: `codex/interactive-oracle`
- **Base**: `0d215296` (HEAD, PR190 F17 plan acceptance commit)
- **Output file**: `docs/reviews/code-review-20260807-145315.md`
- **Included scope**: 仅当前未提交 working tree changes（2 files, 2 hunks, 2 insertions + 2 deletions）
- **Excluded scope**: 基线 `0d215296` 之前已提交的 PR190 历史；`docs/gateflow/pr-190-f17-implementation-20260807.md`（untracked gateflow artifact，非产品代码）
- **Parallel review coverage**: 无

## Change Summary

PR190 F17 slice 闭合 frozen workspace publication manifest 的 digest 派生链。旧 manifest entry `conversation_compaction_user.md` 的 SHA-256 与实际 prompt 文件不匹配，导致 `validate_publication_tree(...)` strict audit fail closed。本 slice 仅更新两个硬编码 digest 值，使链重新闭合：

1. `docs/cli_init_workspace_manifest_v1.json` line 40：entry `content_sha256` 从 `59b50e13...` 更新为 `22e7bc50...`（匹配 prompt raw bytes）
2. `tests/cli/test_smoke_cli_init_provider_matrix.py` line 95：`FROZEN_MANIFEST_SHA256` 从 `d95de68e...` 更新为 `064f8066...`（匹配更新后 manifest raw bytes）

未修改 prompt 内容、production transaction、validator、fixture、assertion、schema、public contract 或 README。

## Independent Verification Results

### Raw-byte digest chain（四条链路全部独立验证通过）

| 链路 | 源 | 目标 | 验证方式 | 结果 |
| --- | --- | --- | --- | --- |
| prompt → manifest entry | `dayu/config/prompts/scenes/conversation_compaction_user.md` raw bytes | manifest `files[].content_sha256` | 独立 `shasum -a 256` + Python `hashlib.sha256(path.read_bytes())` | `22e7bc50...` ≡ `22e7bc50...` ✓ |
| manifest → test pin | `docs/cli_init_workspace_manifest_v1.json` 保存后 raw bytes | `FROZEN_MANIFEST_SHA256` | 独立 `shasum -a 256` + Python `hashlib.sha256(path.read_bytes())` | `064f8066...` ≡ `064f8066...` ✓ |
| old digest 消除 | manifest 全量 files entries | 旧值 `59b50e13...` | Python 结构化断言遍历 43 entries | 0 次出现 ✓ |

### Production FIRST transaction / strict validator 同源

- `load_manifest` 与 `validate_publication_tree` 在同一模块 `utils/smoke_cli_init_provider_matrix.py`
- 测试 fixture `production_publication_tree` 通过 `publish_workspace_transaction(prepare_workspace_transaction(...))` 构造真实 `InitMode.FIRST` workspace
- `FROZEN_MANIFEST_PATH` 指向 `docs/cli_init_workspace_manifest_v1.json`，与 validator 消费的 manifest 完全一致
- `PACKAGE_CONFIG_ROOT` 指向 `dayu/config`，与 production 发布源一致

### Fresh tree path/digest/model-pointer 完整性

- Inventory：5 directories / 43 files / 16 model owner paths — manifest 与 actual publication 一致
- 43 file paths 全部唯一，无重复
- 16 model owner paths 全部 ⊆ 43 file paths
- 16 `json_pointer` 全部为 `/model/default_model_id`
- `.dayu-init.lock` 存在且为空（`b""`）
- 无 `README.md`、`.DS_Store` 等非托管文件泄漏

### OpenAI/Ollama projected validation

- `test_projected_publication_accepts_only_role_model_pointer_changes`：OpenAI choice 的 16 个 role owner 投影通过冻结 oracle（`report.valid=True, issues=()`）
- `test_projected_publication_validates_ollama_dynamic_owner_fields`：Ollama strict 模式正确拒绝 `models.json` digest 漂移；`allow_ollama_dynamic=True` 正确接受三个 owner 定义的动态字段
- `test_ollama_expected_identity_uses_init_owned_dynamic_truth`：expected identity 来自 init publication 而非 package placeholder

### Exact two-hunk diff

- `git diff HEAD --numstat`：2 files，各 `1\t1\t<path>`（1 insertion, 1 deletion）
- 无其他 staged/unstaged changes
- 唯一 untracked file 为 `docs/gateflow/pr-190-f17-implementation-20260807.md`（gateflow artifact，非产品代码）

### 71 owner suite / static evidence

- `pytest --collect-only`：71 tests collected
- `pytest ... -q`：71 passed, 3 warnings（第三方 edgar deprecation，非本项目）
- `pyright`：0 errors, 0 warnings, 0 informations
- `ruff check`：All checks passed
- `python -m json.tool` manifest：parse 通过
- `compileall`：通过

### Attack surface 专项

| 攻击面 | 验证 | 结论 |
| --- | --- | --- |
| duplicate truth | file digest 仅出现在 manifest；manifest digest 仅出现在 `FROZEN_MANIFEST_SHA256` | 未发现重复 |
| loose/dynamic expected | 两个 pin 均为硬编码常量；validator 使用 exact `==` 比较 | 未发现 loose matching |
| scope contamination | 仅 2 个预期文件变更；无其他产品/测试路径 diff | 未发现污染 |

## Findings

未发现实质性问题。

本 slice 是最小可行变更：两个单行 hunk，仅更新硬编码 digest 值以闭合 manifest → prompt → manifest → test pin 的派生链。变更不涉及 control flow、state machine、external protocol、concurrency 或 error handling。三个独立验证路径（`shasum`、Python `hashlib`、`pytest` 71-test suite）均确认 digest 链完整闭合。

## Open Questions

无。

## Residual Risk

- **未来 package asset 再次漂移**：现有 strict validator（`validate_publication_tree`）与 frozen digest test（`test_checked_in_manifest_digest_is_stable_across_validation`）会 fail closed，但无自动修复写路径。当前 slice 不新增自动更新机制，属于设计决策而非遗漏。
- **真实 provider / Oracle replacement scenarios 未重跑**：F17 未改变 provider、Oracle/scenario/readiness 或产品行为，此类重跑风险仍属于后续 work unit。
