# Code Review — WU-CM-12 S2 Fix Re-Review

## Scope

- Mode: current changes (focused re-review)
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: `main`
- Output file: `docs/reviews/code-review-20260618-152833.md`
- Included scope: 仅复核 accepted findings S2-F1、S2-F2 的修复结果，以及修复是否引入新的 architecture/public API drift。Working tree unstaged changes in `dayu/host/compact_material.py`、`dayu/host/context_fallback.py`。
- Excluded scope: committed S2 changes（`memory.py`、`dispatch.py`、`engine_ingest.py`、`run_input.py`、tests）已通过原 review 和 adjudication，不在本次复核范围。plan 定义的 S3/S4 后续 slice 不在本次复核范围。
- Source artifacts:
  - `docs/reviews/code-review-20260618-151719.md`（DayuScript review，提出 F1/F2）
  - `docs/reviews/code-review-20260618-151848.md`（AgentMiMo review，确认 F2）
  - `docs/reviews/code-review-wu-cm-12-s2-adjudication-20260618-152125.md`（Controller adjudication，accept F1/F2）
  - `docs/reviews/wu-cm-12-s2-implementation-codex-20260618.md`（AgentCodex implementation + fix artifact）
- Parallel review coverage: 无。单一 reviewer 逐项复核。

## Findings

未发现实质性问题。

S2-F1 与 S2-F2 的修复均已正确实施，无新增 architecture/public API drift。

### S2-F1 复核：duplicate turn-group helper → 已修复

- **修复前状态**（来自 adjudication artifact）：
  - `_protected_recent_turn_group_ids` 在 `compact_material.py:1530-1566` 与 `context_fallback.py:614-647` 逐行重复。
  - `_is_turn_group_material_block` 在 `compact_material.py:1569-1580` 与 `context_fallback.py:711-722` 逐行重复。
  - `context_fallback.py:17` 只 import `RunInputMaterialBlock`，未复用 compact_material 中的同名 helper。

- **修复后状态**（直接证据）：
  - `context_fallback.py:16-20` 从 `dayu.host.compact_material` 导入 `is_turn_group_material_block` 和 `protected_recent_turn_group_ids_for_material_blocks`。
  - `context_fallback.py` 中不再定义任何 `_protected_recent_turn_group_ids`、`_is_turn_group_material_block`、`_is_raw_turn_block`。`grep "def _protected_recent_turn_group_ids\|def _is_turn_group_material_block" dayu/host/context_fallback.py` 无结果。
  - `compact_material.py:1530` 定义 `protected_recent_turn_group_ids_for_material_blocks`（无下划线前缀，模块级非私有函数）作为单一定义真源。
  - `compact_material.py:1571` 定义 `is_turn_group_material_block`（无下划线前缀）作为单一定义真源。
  - 两个函数均不在 `compact_material.py:3471-3499` 的 `__all__` 中，符合 "Host-internal shared helper，不作为 public contract" 的修复要求。
  - `context_fallback.py:379` 调用 `protected_recent_turn_group_ids_for_material_blocks(...)`、`context_fallback.py:600` 调用 `is_turn_group_material_block(block)`、`context_fallback.py:680` 调用 `is_turn_group_material_block(block)`——均使用 import 的共享 helper。
  - `compact_material.py:1512,1516,1524` 同样使用共享 helper。

- **architecture boundary 检查**：两个 helper 操作 `RunInputMaterialBlock` 类型（定义在 `compact_material.py`），语义真源是 compact material 的 turn-group 分组逻辑。`context_fallback.py` 作为 consumer 从真源模块 import，不构成反向依赖，不违反分层架构。helper 不在 `__all__` 中，不暴露为 public API。

- **结论**：**PASS**。重复实现已消除，共享 helper 归一到 `compact_material.py`，非 `__all__` 导出，`context_fallback.py` 干净复用。

### S2-F2 复核：dead `_is_raw_turn_block` → 已修复

- **修复前状态**（来自 adjudication artifact）：
  - `compact_material.py:1583-1593` 定义 `_is_raw_turn_block`，仅覆盖 `USER_INPUT` 和 `ASSISTANT_FINAL_ANSWER`，整个代码库无调用点。

- **修复后状态**（直接证据）：
  - `grep -rn "_is_raw_turn_block" dayu/ tests/` 返回 exit code 1（无任何匹配）。
  - `compact_material.py` 中 `_is_raw_turn_block` 已完全删除。
  - 原 `_is_raw_turn_block` 所在行号区间（原 1583-1593）已被新的 `is_turn_group_material_block`（1571-1582）替代，新函数额外包含 `ACCEPTED_TOOL_EVIDENCE`，语义与 S2 turn-group floor 一致。
  - `context_fallback.py` 中原有的私有 `_is_raw_turn_block` 也已删除，替换为从 `compact_material.py` import 的 `is_turn_group_material_block`。

- **结论**：**PASS**。死代码已完全删除，无残留。

### S2 行为不变性检查

- **compact segment selection**：`_protected_recent_turn_group_block_ids`（原 `_protected_recent_raw_block_ids`）仍通过 `protected_recent_turn_group_ids_for_material_blocks` 计算 floor group IDs，再收集这些 group 中所有 eligible blocks 作为 protected。行为等价：从 "保护 newest N raw blocks" 升级为 "保护 newest N turn groups 的全部 eligible blocks"，与 plan §S2 一致。

- **fallback selection**：`_reverse_chronological_turn_group_blocks`（原 `_reverse_chronological_raw_blocks`）新增 turn_group_id 缺失校验（`selected_recent_window_turn_floor > 0` 时调用 `protected_recent_turn_group_ids_for_material_blocks` 做 fail-closed 检查），floor IDs 使用共享 helper 计算。排序 key 不变。行为等价且 fail-closed 语义强化。

- **未修改的模块**：`memory.py`、`dispatch.py`、`engine_ingest.py`、`run_input.py`、tests 的 unstaged diff 为空——修复仅触及 `compact_material.py` 和 `context_fallback.py`，不影响其他 S2 已实现行为。

### 验证结果

| 验证项 | 命令 | 结果 |
|--------|------|------|
| Tests (S2 affected) | `pytest tests/host/test_compact_material.py tests/host/test_memory_projection.py tests/host/test_run_input_builder.py tests/host/test_dispatch_scheduler.py::test_pre_start_governance_compact_failure_is_attempt_free tests/host/test_dispatch_scheduler.py::test_reactive_compact_failure_fallback_dispatch_uses_failed_view tests/host/test_dispatch_scheduler.py::test_reactive_fallback_decision_uses_memory_policy_caps -q` | **130 passed in 0.86s** |
| Pyright | `pyright dayu/host/memory.py dayu/host/compact_material.py dayu/host/context_fallback.py dayu/host/run_input.py dayu/host/dispatch.py dayu/host/engine_ingest.py tests/host/test_memory_projection.py tests/host/test_compact_material.py tests/host/test_run_input_builder.py tests/host/test_dispatch_scheduler.py` | **0 errors, 0 warnings, 0 informations** |
| `git diff --check` | | **无 whitespace 错误** |
| `_is_raw_turn_block` dead code | `grep -rn "_is_raw_turn_block" dayu/ tests/` | **无匹配（已完全删除）** |
| `context_fallback.py` 无重复 helper 定义 | `grep "def _protected_recent_turn_group_ids\|def _is_turn_group_material_block" dayu/host/context_fallback.py` | **无结果** |
| 共享 helper 不在 `__all__` | `grep -n "is_turn_group_material_block\|protected_recent_turn_group_ids_for_material_blocks" dayu/host/compact_material.py \| grep "__all__"` | **无匹配** |
| Tests 未直接引用共享 helper | `grep -rn "is_turn_group_material_block\|protected_recent_turn_group_ids_for_material_blocks" tests/` | **无匹配（tests 通过 public API 测试）** |

### Architecture / Public API Drift 检查

- **新增 public API**：无。`is_turn_group_material_block` 和 `protected_recent_turn_group_ids_for_material_blocks` 不在 `__all__` 中，属于 Host-internal 跨模块共享 helper。
- **`__all__` 变更**：无。`compact_material.py:3471-3499` 的 `__all__` 与 committed S2 一致（已移除 `CURRENT_INPUT_ANCHOR_TEXT_MAX_CHARS`，该移除在原 review 中已通过）。
- **新增 import 方向**：`context_fallback.py` → `compact_material.py`（import 两个 Host-internal helper + `RunInputMaterialBlock`）。`RunInputMaterialBlock` 定义在 `compact_material.py`，`context_fallback.py` 消费它，方向正确，无反向依赖。
- **新增跨层依赖**：无。所有变更限于 `dayu.host` 层内部。
- **EventLog / durable schema / Engine contract**：无变更。修复是纯 Host 内部重构。
- **Public function 签名变更**：无。`build_recent_window_fallback_selection` 的 `memory_policy` 参数在 committed S2 中已添加（原 review 通过），修复未改变其签名。

## Open Questions

无。

## Residual Risk

- S3 selected-id provenance guards（plan §S3）不在当前修复范围。
- S4 tier 1-3 compact recovery fallback（plan §S4）不在当前修复范围。
- 预存在测试失败 `test_second_proactive_compact_uses_previous_view_without_old_raw_replay` / `test_multi_turn_proactive_compact_feeds_subsequent_run_input`（根因：context budget 阈值过紧，与 S2 变更无关）仍存在，建议在 S3/S5 修复。

## Conclusion

**PASS** — S2-F1（duplicate turn-group helper）和 S2-F2（dead `_is_raw_turn_block`）修复正确、完整。`context_fallback.py` 不再 import underscored private helpers，`RunInputMaterialBlock` turn-group helper 归一到 `compact_material.py` 单一 Host-internal owner，不在 `__all__` 中，不作为 public contract。`_is_raw_turn_block` 已完全删除。S2 behavior 不变。Tests（130 passed）、pyright（0 errors）、`git diff --check`（no whitespace errors）全部通过。无新增 architecture/public API drift。
