# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: `a9f77611` (S1 accepted commit)
- Output file: `docs/reviews/code-review-20260612-181714.md`
- Included scope: uncommitted S2 changes only — `dayu/host/storage_maintenance.py`, `dayu/host/__init__.py`, `tests/host/test_storage_maintenance.py`, `tests/host/test_package_exports.py`, `docs/reviews/wu-cm-09-s2-implementation-report.md`, `docs/host/issues-implementation-control.md`
- Excluded scope: S1 code (`dayu/host/durable/memory.py` etc.), non-S2 test files
- Parallel review coverage: 无

## Findings

未发现实质性问题。

### 审查逐项说明

#### 1. run_storage_maintenance 是否仍 read-only

`inspect_memory_snapshot_integrity(transaction)` 在 `_ReadStorageMaintenanceStateOperation.__call__` 中调用（`storage_maintenance.py:372`），该 operation 经 `host._run_read()` 执行。函数本身只执行 `SELECT` 查询（`memory.py:1182-1195`），不写入任何 row。当 `reclaim_orphan_artifacts=False`（默认）时，`run_storage_maintenance` 整条链路无写操作。**结论：read-only 语义保持。**

#### 2. memory_snapshot_integrity_issues 是否 public result 自解释且不泄漏 payload

`MemorySnapshotIntegrityIssue.json_value()`（`memory.py:178-192`）返回字段：`failure_kind`（枚举值）、`message`（短错误摘要）、`snapshot_id`、`session_id`、`consumer_id`、`policy_digest`、`checkpoint_event_sequence`。全部为 row identity 和分类元数据，不包含 `snapshot_json`、`item_json`、payload content 或 event payload。`snapshot_id` 等 identity 字段用于 operator 定位损坏 row，不构成 payload 泄漏。**结论：自解释且不泄漏 payload。**

#### 3. __post_init__ 类型校验

`HostStorageMaintenanceResult.__post_init__`（`storage_maintenance.py:197-199`）对 `memory_snapshot_integrity_issues` 调用 `_require_memory_snapshot_integrity_issue_tuple`，校验 `isinstance(value, tuple)` 和每个 item 的 `isinstance(item, MemorySnapshotIntegrityIssue)`。模式与同文件中 `_require_string_tuple` 一致。`MemorySnapshotIntegrityIssue.__post_init__`（`memory.py:136-176`）校验 `failure_kind` 类型、非空文本字段和非负 cursor。**结论：类型校验完整，模式一致。**

#### 4. root exports / package exports

- `dayu/host/storage_maintenance.py:627` `__all__` 包含 `MemorySnapshotIntegrityIssue` ✓
- `dayu/host/__init__.py:108` import `MemorySnapshotIntegrityIssue` ✓
- `dayu/host/__init__.py:163` `__all__` 包含 `MemorySnapshotIntegrityIssue` ✓
- `tests/host/test_package_exports.py:132` `EXPECTED_STORAGE_MAINTENANCE_EXPORTS` 包含 `MemorySnapshotIntegrityIssue` ✓
- 测试 `test_host_all_matches_current_public_contracts` 验证 `dayu.host.__all__` 与期望集合一致 ✓

**结论：三层导出一致，package exports 测试覆盖。**

#### 5. tests 是否覆盖 invalid JSON report 和 empty report

- **invalid JSON**: `test_storage_maintenance_reports_memory_snapshot_integrity_issue`（`test_storage_maintenance.py:380-401`）通过 `_insert_invalid_memory_snapshot_json` 插入 `"{not-json"` 作为 `snapshot_json`，断言 `failure_kind.value == "invalid_json"`、`snapshot_id`、`session_id`、`checkpoint_event_sequence` ✓
- **empty report**: `test_storage_maintenance_dry_run_reports_candidates_without_deleting`（`test_storage_maintenance.py:351`）断言 `result.memory_snapshot_integrity_issues == ()` ✓
- **json_value 键稳定性**: `test_storage_maintenance_result_json_value_is_stable_self_explaining_and_non_negative`（`test_storage_maintenance.py:700,709`）验证 `memory_snapshot_integrity_issues` 键存在且空时为 `[]` ✓

**结论：S2 scope 内测试覆盖完整。其它 failure kind（`schema_mismatch`、`digest_mismatch`、`unsupported_item_kind`、`storage_read_failed`）在 S1 `tests/host/test_memory_projection.py` 中已覆盖（26 passed）。**

#### 6. README defer-to-S3

Implementation report 记录 `dayu/host/README.md` 和 `tests/README.md` 更新 deferred to S3。当前 S2 变更为向现有 maintenance result dataclass 添加一个只读诊断字段并导出新类型，不改变 `run_storage_maintenance` 的架构语义或公共接口结构。README 的 `Agent更新约束` 要求只写"当前代码已实现且对开发者稳定有用的说明"，S3 作为文档关闭 slice 统一更新合理。**结论：可接受。**

#### 7. pyright / coverage

- `python -m pyright dayu/host/storage_maintenance.py dayu/host/__init__.py tests/host/test_storage_maintenance.py tests/host/test_package_exports.py` → 0 errors, 0 warnings ✓
- `pytest --cov=dayu.host.storage_maintenance --cov-report=term-missing tests/host/test_storage_maintenance.py -q` → 12 passed, 88% coverage ✓
- `pytest tests/host/test_storage_maintenance.py tests/host/test_package_exports.py -q` → 25 passed ✓
- `git diff --check` → clean ✓

未覆盖行（19 行）为 `__post_init__` 中的防御性 TypeError 分支、`_require_bool` / `_require_non_negative_float` / `_require_non_negative_int` / `_require_string_tuple` / `_require_non_empty_text` 的类型错误路径，以及 `report_storage_usage` / `run_storage_maintenance` 的 OSError / HostDurableError 异常路径。这些是 dataclass 构造校验和 I/O 异常的防御性分支，不影响 S2 新增逻辑的正确性验证。

## Open Questions

- 无

## Residual Risk

- S2 新增的 `memory_snapshot_integrity_issues` 只有 `invalid_json` 一个 failure kind 的端到端测试。其它 failure kind（`schema_mismatch`、`digest_mismatch`、`unsupported_item_kind`、`storage_read_failed`）的端到端覆盖依赖 S1 `test_memory_projection.py`（26 passed），不阻塞 S2。
- `run_storage_maintenance` 在 `inspect_memory_snapshot_integrity` 抛出非 `sqlite3.Error` 异常时（如 `HostDurableError`），会被外层 `except HostDurableError` 捕获并转为 `HostApiError`。这与 S1 的 fail-closed 语义一致，但没有独立测试覆盖该路径。风险低：`inspect_memory_snapshot_integrity` 内部已捕获 `sqlite3.Error`，只在 identity 读取失败时抛出 `HostDurableError`。
