# Code Review: codex/host-p2-toolruntime-truncate

**分支**: codex/host-p2-toolruntime-truncate
**审查日期**: 2026-05-06 21:39
**审查范围**: 当前分支相对 main 的全部变更（已提交 + 未暂存 + 未跟踪）

---

## Findings

### 1-已修复. [Low] 冗余代码：`_ = requested_event`

**文件**: `dayu/host/_tool_runtime.py:459`
**问题**: 第 459 行 `_ = requested_event` 是冗余代码。`requested_event` 在第 392 行被赋值后并未被后续逻辑使用，这行代码只是消除未使用变量警告，但实际上可以删除。
**原因**: `requested_event` 是 `_append_fetch_requested` 的返回值，但后续流程中没有被消费。
**后果**: 代码可读性略受影响，但不影响正确性。
**建议**: 删除第 459 行。

**修复状态**: 已修复，冗余赋值已删除，`_append_fetch_requested` 保留为必要的 EventLog 写入。

### 2-后移. [Info] 测试覆盖：缺少并发场景

**文件**: `tests/host/test_phase2_tool_runtime_*.py`
**问题**: 当前测试覆盖了截断、补读、TTL 过期、scope 绑定等核心语义，但缺少并发场景测试。
**原因**: `InMemoryToolRuntime` 使用普通 dict 存储 cursor 记录，在高并发异步场景下可能存在竞态条件。
**后果**: 在极端并发场景下可能出现数据不一致。
**建议**: 考虑添加并发测试或在文档中说明当前实现仅适用于单进程场景。

**修复状态**: 后移。P2 plan / README 已明确 in-memory cursor store 仅保证单进程语义，多进程并发、
持久 cursor、lease / fencing / recovery 后移 P6 / P8。

### 3-不修. [Info] 类型转换：`cast` 使用

**文件**: `dayu/host/_tool_runtime.py:229`
**问题**: 第 229 行使用 `cast(_TargetValue, outcome.result.value)` 进行类型转换。
**原因**: `JsonValue` 与 `_TargetValue` 类型不完全兼容，需要显式转换以满足类型检查器。
**后果**: 运行时如果 `outcome.result.value` 不是 `_TargetValue` 类型，会在后续处理中失败。
**建议**: 当前实现可接受，但建议在 docstring 中说明类型假设。

**修复状态**: 不修。该 `cast` 用于在 `ToolResultSuccess.value: JsonValue` 与 ToolRuntime 截断目标闭集之间
收窄类型；实际策略入口仍会按运行时类型检查决定是否截断。

---

## 总体评价

本次变更实现了 Host P2 的 ToolRuntime 截断与补读功能，整体质量较高：

1. **架构符合约束**: 严格遵循 `UI -> Service -> Host -> Engine` 分层架构，Engine 不感知 Host 内部实现。
2. **边界清晰**: Host public API 只暴露 Run 级补读入口与契约类型，内部实现（`InMemoryToolRuntime`、`ToolRuntimeToolExecutor`）不泄漏。
3. **安全性**: scope token 不进入 RunEvent 或日志，只能通过受控 handle 交付。
4. **测试覆盖**: 核心语义测试全面，覆盖截断策略、单次使用、TTL 过期、scope 绑定、跨 run 拒绝等场景。
5. **文档同步**: README 已同步更新，反映当前实现状态。

**结论**: 未发现实质性问题，代码可以合并。建议删除冗余代码 `_ = requested_event`。
