# Code Review

## Scope

- Mode: current changes (re-review of accepted docstring fix)
- Branch: `work/cm-05-06-08-09`
- Base: `main`
- Output file: `docs/reviews/code-review-20260612-151955.md`
- Review date: 2026-06-12T15:19:55+08:00
- Included scope:
  - `tests/host/test_llm_compaction.py`（dirty diff：`_proposal_json` docstring `:raises` 修正）
  - `docs/host/issues-implementation-control.md`（dirty diff：control doc 状态更新至 re-review gate）
- Excluded scope:
  - 生产代码 `dayu/host/llm_compaction.py`（本次未变更）
  - `tests/host/fake_compaction.py`（不在本次 dirty diff 范围内）
- Parallel review coverage: 无
- Previous review artifact: `docs/reviews/code-review-20260612-151142.md`

## Context

本次 re-review 仅验证 AgentDS 在 `docs/reviews/code-review-20260612-151142.md` 中指出的
`_proposal_json` docstring `:raises TypeError` 过期问题的修复。Controller 已裁决接受该 note，
并完成修复：`_proposal_json` 现在声明 `json.JSONDecodeError` 与 `AssertionError`；S3 implementation
report 与 control doc 已同步更新。re-review 不审查 S3 整体实现（已在 #151038 / #151142 中完成）。

## Verification Results

### 1. Docstring note 修复验证

**状态**: 已修复。

**证据**:
- `tests/host/test_llm_compaction.py:797-798`（当前文件）:
  ```
  :raises json.JSONDecodeError: fake proposal 不是合法 JSON 时抛出。
  :raises AssertionError: fake proposal 不是 JSON object 时抛出。
  ```
- 旧 docstring `:raises TypeError:` 已完全移除，`git diff` 中无残留。
- 声明与函数体实际行为一致：
  - `json.loads(raw)`（line 803）可抛出 `json.JSONDecodeError`
  - `dict(_required_mapping(parsed, field_name="proposal"))`（line 804）中
    `_required_mapping` 通过 `assert _is_json_mapping(value)` 抛出 `AssertionError`

### 2. 新问题检查

**状态**: 未引入新问题。

**证据**:
- `git diff -- tests/host/test_llm_compaction.py` 显示本次 dirty diff 即 S3 完整实现
  （移除 `cast`、引入 `TypeGuard`、新增 `_compact_input_json` / `_is_json_mapping` helper、
  修正 `_proposal_json` docstring）。docstring 修正仅为 `:raises` 声明的两行替换，
  不涉及任何代码逻辑变更。
- 所有 helper 签名含完整类型标注；`TypeGuard` 用法合法；无 `cast`/`Any`/`object`/裸弱类型逃逸。
- 生产代码未变更，README 不更新判断不变。
- pyright: 0 errors, 0 warnings, 0 informations。
- pytest: 37 passed in 0.26s。

### 3. S3 DS-F02 closure 验证

**状态**: DS-F02 仍成立。

**证据**:
- `_proposal_json`（line 791-804）无 `cast` 调用：`cast` 已被 `_compact_input_json` +
  `dict(_required_mapping(...))` 替代。
- `_required_mapping`（line 863-873）使用 `assert _is_json_mapping(value)` + `return value`，
  TypeGuard 完成类型缩窄，无 `cast`。
- `_is_json_mapping`（line 876-883）使用 `isinstance(value, Mapping)` →
  `TypeGuard[Mapping[str, JsonValue]]`，标准库类型缩窄。
- `_compact_input_json`、`_material_json_from_compactor_prompt` 均通过
  `_required_mapping` 做显式 runtime narrowing，无 `cast`。
- `rg -n "cast\(|from typing import .*cast" tests/host/test_llm_compaction.py` 无匹配。

### 4. pytest/pyright 结果可信度

**状态**: 结果可信。

**证据**:
- `pytest tests/host/test_llm_compaction.py -q`: 37 passed in 0.26s。全部测试覆盖
  public parser `parse_conversation_compact_output_vnext` 边界，不依赖私有 parser。
- `python -m pyright dayu/ tests/ utils/`: 0 errors, 0 warnings, 0 informations。
  全量 pyright，无新增或扩散类型错误。
- 测试覆盖 malformed JSON、top-level non-object、missing required key、各类 field type path、
  array overlimit、illegal enum、unknown/stale/cross-section label、anchor ordinal 等
  关键边界条件。

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- `tests/host/fake_compaction.py:465` 仍含 `cast(Mapping[str, JsonValue], value)`，
  属同一测试目录下的遗留弱类型模式。该文件不在本次 S3 dirty diff 范围内，
  已在 `docs/reviews/code-review-20260612-151142.md` Residual Risk 中记录，
  建议后续 slice 一并清理。

## Conclusion

**PASS**

WU-CM-05-S3 re-review 确认：
1. `_proposal_json` docstring `:raises` 声明已从过期的 `TypeError` 修正为
   `json.JSONDecodeError` 与 `AssertionError`，与函数体实际行为一致。
2. 未引入新问题；pyright 0 errors / pytest 37 passed 结果可信。
3. S3 DS-F02（`_proposal_json` 不再使用 `cast`）closure 仍成立。
4. 遗留 `fake_compaction.py` `cast` 已在之前 review 中追踪，非本次引入。
