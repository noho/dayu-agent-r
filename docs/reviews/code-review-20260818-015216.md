# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `f6b2d04c`
- Output file: `docs/reviews/code-review-20260818-015216.md`
- Included scope:
  - `README.md`
  - `dayu/fins/README.md`
  - `dayu/fins/tools/upload_tools.py`
  - `tests/README.md`
  - `tests/cli/test_fins_commands.py`
  - `tests/fins/test_fins_ingestion_tools.py`
  - `docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-s2-implementation-20260817.md`
- Excluded scope: 无
- Parallel review coverage: 无

## Findings

### 001-未修复-低-tool schema description 缺少闭集枚举声明

- **入口/函数**: `_upload_parameters_schema()` in `dayu/fins/tools/upload_tools.py`
- **文件(行号)**: `dayu/fins/tools/upload_tools.py:253-258`
- **输入场景**: LLM 读取 tool schema 获取参数约束
- **实际分支**: `fiscal_period` 的 `description` 写了"只支持 FY、H1、Q1、Q2、Q3、Q4"
- **预期行为**: 根据 CLAUDE.md LLM-facing 文本约束，结构化输出必须在当前 prompt 中自足说明字段名、含义、类型、必填性、允许值和最小示例。当前 description 列出了闭集值，但未像 `action` 那样使用 `enum` 字段声明允许值列表
- **实际行为**: `fiscal_period` 只有 `type: "string"` 和 `description`，没有 `enum` 约束。对比 `action` 字段（行 233-238）有 `enum: upload_action_enum`，`upload_kind` 字段（行 230-232）有 `enum: upload_kind_enum`
- **直接证据**: `dayu/fins/tools/upload_tools.py:253-258` — `fiscal_period` 只有 `type` 和 `description`，无 `enum`
- **影响**: LLM 需要从自然语言 description 推断允许值，而非从结构化 `enum` 约束直接获知。当前 description 已列出全部值，LLM 实际能正确解析，但不符合 schema 自足性最佳实践
- **建议改法和验证点**: 为 `fiscal_period` 添加 `enum: ("FY", "H1", "Q1", "Q2", "Q3", "Q4")`，与 `action`/`upload_kind` 保持一致模式。验证 tool schema 构造输出包含该 enum
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 002-未修复-低-零副作用证明依赖 runtime.upload_runner is None 断言

- **入口/函数**: `test_upload_tool_filing_static_invalid_input_has_zero_side_effects` in `tests/fins/test_fins_ingestion_tools.py`
- **文件(行号)**: `tests/fins/test_fins_ingestion_tools.py:1324`
- **输入场景**: 非法 fiscal_period 触发 static admission 失败
- **实际分支**: 测试断言 `runtime.upload_runner is None`
- **预期行为**: 需要证明 runner/converter 均未被调用。当前断言 `runtime.upload_runner is None` 证明的是 runtime 未装配 runner（因为 `_runtime_with_static_admission_guard` 使用默认 `DefaultFinsRuntime.create`，其 `upload_runner` 初始为 `None`），而非证明"即使 runner 已装配也不会被调用"
- **实际行为**: `runtime.upload_runner is None` 断言在 `_runtime_with_static_admission_guard` 构造的 runtime 上始终为真，无论 static admission 是否失败。这是一个结构性真值，不是行为证明
- **直接证据**: `tests/fins/test_fins_ingestion_tools.py:2583-2613` — `_runtime_with_static_admission_guard` 使用 `DefaultFinsRuntime.create`，其 `upload_runner` 默认为 `None`；行 1324 断言 `runtime.upload_runner is None` 是结构性常量
- **影响**: 当前测试仍通过 `_ForbiddenFilingUploadStateRepository`（行 1321）、`executor.submitted_job_ids == ()`（行 1323）、`runtime._observations == {}`（行 1325）和 workspace 快照不变（行 1327）提供了充分的零副作用覆盖。`upload_runner is None` 断言是冗余的但无害的，它不削弱已有覆盖
- **建议改法和验证点**: 若希望更严格的证明，可在 runtime 上装配一个 recording runner 并断言其未被调用。当前已有 `_ForbiddenFilingUploadStateRepository` + executor + observation + workspace 四重覆盖，实际风险极低
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 无

## Residual Risk

- `_required_text` 在 tool adapter 层对 `fiscal_period` 做了 `strip()`，而 domain `normalize_fiscal_period` 也做 `strip().upper()`。两层 strip 是冗余的但无害——adapter strip 是通用输入清理，domain strip/upper 是业务规范化。当前实现正确，但若未来 adapter strip 行为变化（如不再 strip），domain 层仍独立保证正确性。
- CLI 路径的 `_optional_stripped_text(args.fiscal_period)` 在进入 `FinsUploadFilingRequest` 前已 strip；tool 路径的 `_required_text` 也 strip。两条入口最终都由同一 domain owner `normalize_fiscal_period` 完成规范化，canonical handoff 证据链完整。
- 测试全部使用 `tmp_path` 临时目录，pytest 自动清理，无资源泄漏风险。
