# Plan Re-Review (3rd pass): `WU-CLI-DOWNLOAD-02-DL-F12-F14`

## Review Target

- **Revised Plan**: `docs/gateflow/wu-cli-download-02-plan-20260810.md`（经 fix loop 2 修订）
- **Fix Loop 2**: `docs/gateflow/wu-cli-download-02-plan-fix-2-20260810.md`
- **Adjudication**: `docs/gateflow/wu-cli-download-02-plan-review-adjudication-20260810.md`（含第一轮 re-review 裁决 N1/N2）
- **Prior Reviews**: `docs/reviews/plan-review-20260810-164812.md` (MiMo), `docs/reviews/plan-review-20260810-164911.md` (DS)
- **Code baseline**: HEAD `3811f95c82fbf0daf15740a5d217eed4d8b49df5`

## Scope

1. 核验 DS N1/N2 是否关闭
2. 核验 MiMo C01/C07 是否关闭
3. 再次检查没有新增 blocking gap

## DS N1: `cn_report_selection.py` 不在 Slice 2 — **已关闭**

**核验**:
- Plan line 313: `cn_report_selection.py` 已加入 Slice 2 allowed production files
- Plan line 328: "其中 `cn_report_selection.py` 当前两处 `query.target_periods` 访问必须在本 slice 只机械改名为 `query.discovery_periods`，protocol/docstring 只使用 discovery 语义，不提前修改 classifier 或 candidate projection"
- Plan line 354-358: 共有文件边界表新增 `cn_report_selection.py` 行：Slice 2 = "仅将两处 `query.target_periods` 机械重命名为 `query.discovery_periods`"；Slice 3 = "重写 category-first classification，构造/消费 `CnReportPeriodProjection`，selection sort 复用既有 `CN_FISCAL_PERIOD_ORDER`"
- Plan line 360: 明确说明 "Slice 2 只做 query field rename，使该 slice 独立通过 pyright"

修复充分。Slice 2 现在可以在不触及 classification 逻辑的前提下独立完成 `target_periods → discovery_periods` 全量重命名并通过 pyright。

## DS N2: `CN_FISCAL_PERIOD_ORDER` slice 归属 — **已关闭**

**核验**:
- Plan line 139: "CN/HK 的 canonical period order 在 **Slice 2** 由 `cn_download_models.py` 新增唯一常量 `CN_FISCAL_PERIOD_ORDER = (FY,H1,Q1,Q2,Q3,Q4)`，供同 slice 的 form policy canonicalization 使用；Slice 3 的 `CnReportPeriodProjection` 与 selection sort 只复用该既有常量"
- Plan line 327: "在 `cn_download_models.py` 先定义唯一 `CN_FISCAL_PERIOD_ORDER`"
- Plan line 354: 共有文件边界表 `cn_download_models.py` Slice 2 列 = "定义唯一 `CN_FISCAL_PERIOD_ORDER`；完成 `CnReportQuery.target_periods -> discovery_periods` 与 query docstring"
- Plan line 354: Slice 3 列 = "新增 `CnReportPeriodProjection` 并复用既有 order 做 canonical validation"

常量归属在 Slice 2 首次定义，Slice 3 只复用。边界清晰。

## MiMo C01: `FISCAL_PERIODS` 是否存在 — **已关闭**

**核验**:
- Plan line 248: "当前基线已存在 `dayu/fins/domain/filing_semantics.py::FISCAL_PERIODS`（定义于该模块现有第 79 行附近），公共类型直接复用它做成员校验"
- fix-2 line 52: 直接代码证据 `dayu/fins/domain/filing_semantics.py:79` 定义 `FISCAL_PERIODS: Final[frozenset[FiscalPeriod]]`
- Plan 明确 `FISCAL_PERIODS`（通用 membership）与 `CN_FISCAL_PERIOD_ORDER`（CN/HK download canonical order）职责不重叠

符号存在且职责划分清晰。

## MiMo C07: `form_type` 参数删除描述 — **已关闭**

**核验**:
- Plan line 245: "`commit_cn_filing_source_document(...)` 与 `_build_base_meta(...)` 只删除**对各自 caller 暴露**的冗余 `form_type` 参数"
- Plan line 245: "`commit_cn_filing_source_document(...)` 内部唯一取局部值 `identity_period = candidate.period_projection.identity_period`"
- Plan line 245: "继续显式调用 `_build_upsert_request(..., form_type=identity_period)`。`_build_upsert_request(...)` 的内部参数不删除，因为它机械构造 storage request；该值来自 candidate identity，不是 caller 可提供的第二真源"

措辞精确区分了 caller-facing 参数删除与内部 builder 参数保留，消除了表面矛盾。

## 全面复核：无新增 blocking gap

对修订后 plan 逐项检查：

| 检查维度 | 结果 |
|---|---|
| category-first matrix 封闭性 | 通过。material family 三元判定 + family 内 7 行矩阵，正负例完备 |
| `CN_FISCAL_PERIOD_ORDER` 单一 ownership | 通过。Slice 2 定义，Slice 3 复用，domain `FISCAL_PERIODS` 职责分离 |
| fresh-schema strict parse | 通过。§5.3 line 246 缺字段/畸形 fail closed，无 `.get()` 默认 |
| all constructors/consumers | 通过。§4.3 `rg` 穷举 6 类 typed contract，SEC/JSON/CLI 全覆盖 |
| Slice 2/3 共有文件边界 | 通过。5 个共有文件（含 `cn_report_selection.py`）均有明确拆分 |
| Slice 2 独立 pyright 可行性 | 通过。所有 `target_periods` 消费者已在 Slice 2，`CN_FISCAL_PERIOD_ORDER` 在 Slice 2 定义 |
| coverage 口径 | 通过。整文件 line >=80%，不豁免 |
| README 更新范围 | 通过。三份 README 按各自写作约束更新 |
| production evidence scope | 通过。§9 完整 6 步，含 PDF 内容证据 |
| mode invariant 唯一 helper | 通过。`_validate_download_mutation_mode` 唯一，request/filter 复用 |
| SEC 构造点 | 通过。`sec_pipeline.py` 在 Slice 3 allowed files，显式传 `()` |
| JSON serializer | 通过。`to_json_value()` 显式写入 `covered_fiscal_periods`，§7.3 round-trip 验证 |
| rebuild missing 回归 | 通过。Slice 2 item 9、Slice 3 item 8 显式断言空 missing |

## Open Questions

无 blocking open questions。

## Residual Risks

| 风险 | 跟踪 |
|---|---|
| `ingestion_runtime.py` 整文件 80% 覆盖率成本 | implementation validation |
| 全 results group 真实分页量 | post-fix evidence |
| category matrix 在更多发行人措辞上的边界 | parameterized tests + 0700 evidence |
| implementation 开始前代码基线可能新增 consumer | plan 要求重跑 `rg` 防漂移 |

## Conclusion

**pass**

DS N1、N2 和 MiMo C01、C07 全部关闭。修订后 plan 在 category-first matrix、`CN_FISCAL_PERIOD_ORDER` ownership、fresh-schema strict parse、constructors/consumers 穷举、slice allowed files（含共有文件边界）、coverage/README/production evidence scope 各维度均通过 adversarial challenge，无新增 blocking gap。plan 达到 code-generation-ready。

## Artifact

- Path: `docs/reviews/plan-review-20260810-165548.md`
