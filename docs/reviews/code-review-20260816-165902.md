# Code Review — Re-review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `1fd52c96` (HEAD，未提交 workspace diff)
- Output file: `docs/reviews/code-review-20260816-165902.md`
- Included scope:
  - 初审 artifacts：`docs/reviews/code-review-20260816-162505.md`（PASS）、`docs/reviews/code-review-20260816-163507.md`（FAIL，3 findings）
  - 修复 artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-code-review-fix-20260816.md`
  - 更新后 implementation artifact：`docs/gateflow/uf-fix08-existing-source-auto-repair-slice4-implementation-20260816.md`
  - 当前完整 workspace diff（`git diff`，10 files，+1850/-59）
  - 只读走读修复涉及的 owner：`_fs_source_integrity.py`（`_derive_repair_blocked_reason` 全文）、`_fs_source_document_core.py`（`_canonical_remaining_manifest_items_for_repair` + `reset_source_document_for_repair` 全文）
- Excluded scope: Slice 1–3 已提交代码只作为 contract 真源走读；`upload_repair_contract.py`、`upload_failure.py`、`source_integrity.py` 无 diff
- Parallel review coverage: 无

## Review Method 摘要

- 逐行走读修复后的 `reset_source_document_for_repair()` 完整调用链：input validation → filing exact inspection → target conflict gate → identity comparison → material whole-kind inspection → filing blocked reason → material blocked reason → reset → manifest rewrite
- 逐行走读 `_canonical_remaining_manifest_items_for_repair()`：inspector reason 消费 → item shape/unique invariant → item 收集
- 逐行走读 `_derive_repair_blocked_reason()`：target exclusion → UNSAFE/root → manifest untrusted → content/reasons → canonical availability → aggregate
- 交叉核对 fix artifact 声称的顺序与实际代码行号
- 逐项验证初审 3 个 findings 的修复证据
- 验证 9 项对抗审查面无回退
- 验证 scope、pyright、coverage 声称

## 初审 Findings 闭环验证

### Finding 1（高）：material source damage 对 Phase B 不可见

**修复状态：已闭环**

直接代码证据：

- `reset_source_document_for_repair()` 在 filing identity comparison 通过后（line 453），执行 material whole-kind inspection（lines 455-466）：`_inspect_source_kind_unguarded(source_kind=SourceKind.MATERIAL, requested_document_id=None)`。
- `SourceIntegrityPreflightError`（root structural corruption）精确映射为 `SourceIntegrityRepairBlockedError(CROSS_SOURCE_PUBLICATION_UNSAFE)`（lines 463-466），不泄漏到 commit-time。
- `material_inspection.repair_blocked_reason` 直接消费（lines 472-475），非 `None` 时抛 `SourceIntegrityRepairBlockedError`。
- Material clean/empty：`_derive_repair_blocked_reason` 对空 non-target inventory 返回 `None`（无 non-target item → 无 UNSAFE/content/canonical 检查命中）。

测试证据：

- `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once`（`test_docling_upload_service.py:3382`）参数化覆盖 `content_missing` → `NON_TARGET_SOURCE_INCOMPLETE`、`manifest_missing` → `CANONICAL_MANIFEST_UNAVAILABLE`、`structural_unsafe` → `CROSS_SOURCE_PUBLICATION_UNSAFE`。三类均断言 `SOURCE_REPAIR_BLOCKED` code、`rollback_calls == 1`、`commit_calls == 0`、old tree SHA 不变、filing/material meta 不变、无 company meta。
- `test_material_whole_inspection_owns_repair_blocked_reason`（`test_fins_storage_atomicity.py:7330`）直接断言 inspector owner 的 typed reason。
- 既有成功路径测试（`test_source_repository_repair_facade_resets_target_and_rewrites_canonical_sibling_manifest`）保留 complete material，证明 clean material 不阻断。

### Finding 2（中）：repair_blocked_predicate 双 owner 且闭合不一致

**修复状态：已闭环**

直接代码证据：

- `_canonical_remaining_manifest_items_for_repair()`（`_fs_source_document_core.py:87-131`）简化为：
  1. `inspection.repair_blocked_reason is not None` → raise（line 108-111）。唯一 blocked 事实来源。
  2. 逐 non-target 收集 item，仅做 producer invariant 检查：`canonical_item is None` 或 `document_id` mismatch → `RuntimeError`（line 119-122）；duplicate `document_id` → `RuntimeError`（line 123-126）。
  3. 无任何 content status、classification status/reasons 的二次业务判断。
- `_derive_repair_blocked_reason()`（`_fs_source_integrity.py:1256-1306`）成为唯一 predicate owner：target exclusion → UNSAFE/root fact → `SOURCE_MANIFEST_UNTRUSTED` → content non-COMPLETE / reasons ≠ shared → canonical item availability → aggregate count。
- `CANONICAL_MANIFEST_UNAVAILABLE` 现在可达：whole-kind mode（`target is None`）+ `shared_manifest_reasons` 非空时 line 1300-1301 触发；non-target `canonical_manifest_item is None` 时 line 1302-1303 触发。

测试证据：

- `test_filing_sibling_manifest_mismatch_owner_matches_phase_b_block`（`test_fins_storage_atomicity.py:7395`）：synthetic payload 注入 sibling `SOURCE_MANIFEST_PROJECTION_MISMATCH`，断言 owner 产生 `NON_TARGET_SOURCE_INCOMPLETE`，Phase B collector 传播同一 reason。
- `test_repair_manifest_collection_rejects_clean_owner_shape_violation`（`test_fins_storage_atomicity.py:7480`）：clean payload + wrong `document_id` shape → `RuntimeError("canonical item shape 违约")`，非 `SourceIntegrityRepairBlockedError`。

### Finding 3（低）：fail-closed 与 manifest rewrite failure 缺测试

**修复状态：已闭环**

测试证据：

- `test_prepare_upload_rejects_invalid_repair_disposition`（`test_docling_upload_service.py:3036`）：非 union disposition → 固定 `ValueError("repair_disposition 必须是封闭 repair contract")`。
- `test_prepare_upload_rejects_delete_with_existing_repair`（`test_docling_upload_service.py:3082`）：`delete` + `ExistingSourceAutoRepair` → 固定 `ValueError("delete 上传不得携带 existing source repair 授权")`。
- `test_existing_filing_repair_manifest_rewrite_failure_rolls_back_once`（`test_docling_upload_service.py:3511`）：patch `_write_json` 抛 `OSError`，断言 rollback once、commit zero、old tree SHA 不变、filing meta 不变、无 company meta、batch cleanup。

## 九项对抗审查面回退验证

### 1. required disposition 原样贯穿

无回退。`_can_skip_upload` 首条 `ExistingSourceAutoRepair -> False` 保持（`docling_upload_service.py:1287`）。`_PreparedAssetMutation.repair_disposition` 仍为 required。`prepare_upload` 校验封闭类型后原样携带。

### 2. storage core 唯一 owner / 恰一次 inspection / 先 gate 后 compare

无回退。filing exact-target inspection 恰一次（line 427-433）。target conflict gate（lines 438-444）在 identity comparison（lines 445-453）之前。新增 material whole-kind inspection 一次（lines 455-462）在 identity comparison 之后。comparison `ValueError` → conflict 转换（lines 450-451）仅包围 comparison。

### 3. target-local damage 不误判 blocked

无回退。helper 不再二次判断 content/status/reasons，只消费 `inspection.repair_blocked_reason`。aggregate 为空时 repair 仍成功（已有测试覆盖）。

### 4. reset/manifest rewrite/blob/final 失败仅污染 staging

无回退。新增 manifest rewrite `OSError` 测试。既有 blob/final/rollback-secondary 测试保持。

### 5. facade 纯 delegate / transitional helper 删除

无回退。`FsSourceDocumentRepository.reset_source_document_for_repair()` 仍是一行 delegate。protocol body 仍为 `...`。

### 6. stale/blocked 精确映射

无回退。`target_unsafe` 和 `shared_untrusted` 新增到 service drift grid，均映射为 `SOURCE_REVISION_STALE`（非 `SOURCE_REPAIR_BLOCKED`），证明 target conflict gate 优先于 blocked reason 消费。

### 7. Controller 扩展严格只机械接线

无回退。SEC/CN caller diff 仅含 import + required kwarg。

### 8. tests 覆盖

修复新增：

| 对抗点 | 测试 |
|---|---|
| material content missing → BLOCKED | `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once("content_missing")` |
| material manifest missing → BLOCKED | `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once("manifest_missing")` |
| material structural unsafe → BLOCKED | `test_existing_filing_repair_maps_material_damage_to_blocked_and_rolls_back_once("structural_unsafe")` |
| material owner reason | `test_material_whole_inspection_owns_repair_blocked_reason` |
| sibling manifest mismatch owner | `test_filing_sibling_manifest_mismatch_owner_matches_phase_b_block` |
| clean payload shape violation | `test_repair_manifest_collection_rejects_clean_owner_shape_violation` |
| invalid disposition | `test_prepare_upload_rejects_invalid_repair_disposition` |
| delete × repair | `test_prepare_upload_rejects_delete_with_existing_repair` |
| manifest rewrite OSError | `test_existing_filing_repair_manifest_rewrite_failure_rolls_back_once` |
| target_unsafe → STALE | `test_existing_filing_repair_maps_real_staged_failures_and_rolls_back_once("target_unsafe")` |
| shared_untrusted → STALE | `test_existing_filing_repair_maps_real_staged_failures_and_rolls_back_once("shared_untrusted")` |
| drift: shared_untrusted | `test_source_repository_repair_rejects_staged_revision_and_status_drift("shared_untrusted")` |

### 9. 无兼容/default/loose parsing/Any/object/反射

无回退。`_canonical_remaining_manifest_items_for_repair` 删除所有 content/status/reasons 二次判断，只消费 typed reason。`_derive_repair_blocked_reason` 是唯一 predicate owner。无新增 `Any`/`object`/`getattr`/`hasattr`。

## 特别挑战验证

### repair_blocked_reason 仅由 storage inspector owner 派生，core 只机械消费

已验证。`_derive_repair_blocked_reason()` 是唯一 predicate owner（`_fs_source_integrity.py:1256-1306`）。`_canonical_remaining_manifest_items_for_repair()` 只检查 `inspection.repair_blocked_reason is not None`（line 108）并传播，不做任何 content/status/reasons 判断。producer invariant 违约抛 `RuntimeError`，非 `SourceIntegrityRepairBlockedError`。

### staged batch 内 filing exact inspection 恰一次、material whole-kind inspection 恰一次

已验证。filing：`_inspect_source_kind_unguarded(source_kind=FILING, requested_document_id=external_document_id)`（line 427-433）。material：`_inspect_source_kind_unguarded(source_kind=MATERIAL, requested_document_id=None)`（line 456-462）。两者在同一 `state.staging_ticker_dir` 内，同一 `_resolve_active_batch` 返回的 state。

### material 内容/manifest/root 损坏在 reset 前投影 BLOCKED，clean/empty 不阻断

已验证。material blocked reason 在 reset（line 476）之前检查（lines 472-475）。`SourceIntegrityPreflightError`（root structural）在 reset 之前映射（lines 463-466）。clean/empty material：`_derive_repair_blocked_reason` 对空 non-target inventory 返回 `None` → 不阻断。

### target local unsafe 与 shared filing manifest untrusted 的 stale/conflict 优先级

已验证。target conflict gate（lines 438-444）在 material inspection（line 455）和 filing blocked reason（line 468）之前。`target_unsafe` 和 `shared_untrusted` 在 service 测试中均映射为 `SOURCE_REVISION_STALE`，证明 conflict 优先于 blocked。

## Validation 声称验证

- 修复 artifact 声称：`345 passed, 1 skipped, 3 warnings in 16.89s`。未由本 reviewer 独立复现全量运行，但修复新增的 12 个测试逻辑走读正确。
- 修复 artifact 声称：`1827 passed` 完整 Fins suite。未独立复现。
- 修复 artifact 声称：pyright `0 errors, 0 warnings`。未独立复现。
- 修复 artifact 声称：逐文件 coverage `80%–97%`。未独立复现。
- `git diff -- README.md dayu/fins/README.md tests/README.md docs/cli_ci_oracles.json docs/cli_ci_scenarios.json docs/host/design.md docs/engine/design.md`：未由本 reviewer 独立运行，但 fix artifact 声称为空。
- scope 验证：当前 diff（10 files）全部落在 allowed + Controller 扩展 + Controller 授权的 `_fs_source_integrity.py` 内。

## Open Questions

无。

## Residual Risk

- 未独立复现全量测试运行（`345 passed`、`1827 passed`、pyright `0 errors`、coverage）。建议 Controller 在最终 gate 确认。
- `_derive_repair_blocked_reason` 的 `target is None and shared_manifest_reasons` 分支（line 1300-1301）在 exact mode 下不可达（exact mode 始终有 target），只在 whole-kind mode 下触发。当前 material whole-kind 调用使用 whole-kind mode，该分支可达。无问题。
- Slice 5/6 的 SEC/CN/HK fresh reread、download unsafe、README 不在本 slice scope。

## Review Conclusion

**PASS**。初审 3 个 findings 全部闭环：Finding 1（高）通过 material whole-kind inspection 在 reset 前阻断 damaged material；Finding 2（中）通过 `_derive_repair_blocked_reason` 收敛为唯一 predicate owner、core 只机械消费 typed reason；Finding 3（低）通过 3 个新增测试覆盖。9 项对抗审查面无回退。特别挑战的 4 项验证点均通过。未发现阻塞 merge 的实质性问题。
