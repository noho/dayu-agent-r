# Code Review

## Scope

- Mode: current changes
- Branch: codex/upload-filing-oracle
- Base: 2f5ec121
- Range: 2f5ec121..8c94312d
- Output file: docs/reviews/code-review-20260813-141000-ds.md
- Included scope: 3 个关键文件 diff + gateflow fix implementation 文档
- Parallel review coverage: 无

## Findings

未发现实质性问题（无阻塞 finding）。

## 逐项裁决

### 1. lastResort 可证伪与全局恢复 — PASS

测试先清空 leaking_logger 与 root handlers、propagate=True、disabled=False，并在隔离外断言 `logging.lastResort is not None` 且控制 marker 确实到达 capfd 观察的 FD2——前提可证伪：若隔离失效，隔离内同路径 warning 必出现在 captured.err。故 `captured.err == ""` 有判别力。lastResort 持有原 sys.stderr TextIO，隔离时经 dup2 后的 FD2 落入 /dev/null，FD 级拦截成立。logger/root 全部状态（handlers/level/propagate/disabled）在 finally 中完整恢复，无全局泄漏。

### 2. sys.exception 下 primary vs flush-only 与 dup2/close — PASS

生产逻辑：finally 首行 `active_error = sys.exception()`（3.11 合法；finally 处于异常解除中时返回活动异常），exit flush 仅 `except Exception` 且 `active_error is None` 才 re-raise——主体异常在途时次生 flush 异常被抑制、主异常分类不变；无主体异常时 flush 失败仍传播，与原契约一致。FD 顺序正确：dup2 恢复先于 close 复制 FD；dup 失败不进 close 路径，open(/dev/null) 失败仍由外层 finally 关闭复制 FD，无泄漏。owner test 覆盖双失败：descriptor 保持 converter_construction、flush_calls==2、dup/close 各一次且 EBADF、FD2 恢复（capfd 可见恢复 marker）。测试补丁全局 sys.stderr 与 stdlib os.dup/os.close 属测试窗口内替换，单线程 pytest 下无并发风险，非阻塞。

### 3. 自建 corrupt PDF 仍真实 CLI — PASS

仅替换输入 fixture：绝对路径 calibration 文件改为 tmp_path 内 `b"not a PDF"` 自建文件；subprocess.run 仍真实调用 venv 内 `dayu-cli` 可执行文件（cwd=仓库根、timeout=60），完整保留真实 CLI/subprocess/converter/Docling 链路与原有全部断言（exit 1、typed content reason、failure_kind="content"、stderr≤1024、无 Traceback、无路径、fresh workspace 零 mutation）。文档声明与 calibration 同字节，验证 1 passed。消除绝对路径依赖，可移植成立。

### 4. scope / UF-FIX09 不变 — PASS

可见 delta 仅触及 3 个声明文件；无 upload owner/workflow/storage、UF-FIX09 生命周期改动；与文档 scope exclusions 一致。

## Open Questions

- 无。

## Residual Risk

- flush-only 分支（无主体异常时 flush 失败传播）在可见 delta 中无专属测试；文档称单文件覆盖率 95%，或由既有测试覆盖，限界 delta 内无法证实，归为非阻塞 test-gap 关注项。
- 双失败 owner test 的 flush_calls==2 计数依赖全局 sys.stderr 补丁窗口内无第三方 flush，属测试鲁棒性关注，非生产风险。
- 文档所述 README 变更与 Windows 差异实跑不在本次限界读取范围内，未复核。

## 结论

PASS——未发现阻塞 finding；可进入 Controller 指定的下一 gate。
