# F14 aggregate deepreview — AgentController

## Review metadata

- mode: aggregate deepreview
- base: `b222b8b064f096d899a9de708e45cd1fb6e732e6`
- head: `6eb41ac1`
- scope: implementation commit的全部production、tests、Host docs与Gateflow/review artifacts
- governing truth: Goal Confirmation、accepted plan、code review adjudication、两路accepted re-review、项目AGENTS约束
- verdict: `PASS`

## Findings

没有未解决的 correctness、stability、maintainability 或semantic ownership finding。

此前Controller发现的`run_id=None` silent-consumption缺口已在Host owner修复并有red→green owner regression；MiMo发现的多轮monotonicity/reconnect覆盖缺口及文档时序问题均已修复；DeepSeek的cross-reference/docstring建议已吸收。两路原reviewer均对最终diff给出`ACCEPTED`。

## Root-cause and owner audit

- F14根因是Host把accepted terminal的ledger位置误作raw material consumption frontier；`CONTEXT_COMPACTED.event_sequence + 1`没有表达replacement实际coverage。
- 唯一accepted consumption真源继续是strict `ContextCompactedSemanticPayload.compacted_source_refs`，它由immutable source boundary的represented/omitted exact partition机械派生。
- builder按accepted terminal order strict读取当前Session chain，累积ordered-unique consumed refs；latest replacement与latest evidence aggregate仍只从最新terminal投影，没有改作cumulative truth。
- metadata-first阶段只允许“非空run_id + 唯一canonical user anchor + anchor ref已消费”建立whole-group prefix proof；缺失/重复anchor或`run_id=None`保守进入typed path。
- suffix阶段复用`_atomic_material_units`证明block all-in/none-in、unit all-or-none与consumed-prefix/unconsumed-suffix，拒绝partial、split、gap或reordering。
- material frontier从最终未消费block的首个canonical sequence派生；latest terminal id/sequence仅作为provenance保留。
- current input ref不进入本次consumed coverage；typed `PREVIOUS_*` refs只允许exact回指同Session更早accepted terminal。

## Adversarial failure pass

### Protected/unselected raw lifecycle

- regression把accepted terminal放在protected correction group之后，而replacement只覆盖旧prefix；新实现仍返回protected group首sequence并保留完整user/answer/evidence group。
- 三轮阶段性test证明frontier严格单调，只跨越完整已消费groups；每阶段canonical order、exact-once、no-gap、no-duplicate成立。
- `run_id=None` consumed user无法冒充whole-group proof，当前实现经typed/atomic owner fail closed。

### Accepted vs non-accepted lifecycle

- accepted initial/repair/tier 1–3最终都只形成canonical `CONTEXT_COMPACTED`，共享chain读取路径。
- rejected、repair exhausted、failed、cancelled、stale/late与tier 4/5 fallback没有accepted terminal，不进入chain查询；material owner test加既有scheduler/operation/cancellation union共同覆盖。
- restart/reconnect只重读durable EventLog、payload/artifact与Memory snapshot，不依赖进程内cache或第二cursor。

### Correction and evidence ownership

- M2 integration用真实`TOOL_CALL_REQUESTED`/`TOOL_RESULT_ACCEPTED`+`AcceptedEvidenceEnvelope`建立旧/新evidence atoms，evidence id与producer EventLog id角色分离。
- 首terminal old EvidenceFact只绑定旧ref与首terminal provenance；第二terminal提交后重新strict parse首terminal，typed semantics exact不变。
- second proposal不retain旧fact；new EvidenceFact只绑定新ref与第二terminal provenance，old ref不被借用或重写。
- 四轮aging后raw correction user/answer/evidence退出bounded recent window；reopen Memory与ordinary RunInput只出现正式new claim/summary一次，不靠raw correction或UI fallback表面正确。
- 无工具证据的21.7%继续由既有strict acceptance owner tests约束；本slice没有新增heuristic或伪造EvidenceFact。

### Same-source projection

- artifact/canonical terminal仍由同一`CompactAcceptedTruthV4`序列化；writer未改。
- Conversation Memory从strict accepted replacement与`compacted_source_refs`投影；RunInput/reconnect复用同一pre-dispatch material builder。
- Tool Trace renderer未改，继续读取strict accepted terminal；没有第二provenance ledger或字符串/label反推。
- latest `represented_evidence_refs`保持latest replacement逐fact refs union语义，与cumulative consumption明确分离。

## Project instruction audit

- 生产修改仅`dayu/host/compact_material.py`，符合Host语义owner与`UI -> Service -> Host -> Engine`分层；没有runtime反向依赖。
- 无`Any`/`object`/loose parsing/`hasattr`/`getattr`/lazy import/compatibility wrapper/默认fallback/extra payload。
- 新增私有typed entry与模块级helpers，没有God object或重复cursor/projector。
- changed函数/测试helper均有中文docstring；复杂metadata/atomic proof有中文意图注释。
- 无schema、DB migration、public contract、LLM-facing schema/prompt/provider/CLI/UI/Fins/Oracle变更。
- 已按README触发规则更新Host README与Host design；Engine design truth check后边界不变，tests README职责未变化。

## Coupling and maintainability audit

- accepted chain strict parse是线性扫描，成本随Session accepted terminals增长；这是为避免第二durable cursor且被accepted plan显式接受的权衡，不构成当前correctness finding。
- metadata stage与atomic stage通过同一material selector/`_atomic_material_units`不变量衔接；代码与Host docs均显式cross-reference，避免semantic ownership drift。
- large owner fixtures使用production governance acceptance、evidence envelope、Memory projection、durable Run/Attempt和ordinary RunInput builder；没有测试专用production branch。用于阶段性material view的`RunRow`只包装真实canonical input boundary，不替代最终durable Run owner。
- fixture变化把synthetic current/source refs迁移为真实canonical refs，没有为保旧测试降低production strictness。

## Validation evidence

- C1 old implementation red: `Failed: DID NOT RAISE HostDurableError`
- finding-focused: `5 passed`
- affected union: `343 passed`
- changed production file coverage: `85%` (`190 passed`)
- focused pyright: `0 errors`
- focused Ruff: pass
- `git diff --check`: pass
- forbidden scope diff: empty
- pre-fix full pytest baseline: `6764 passed, 10 skipped, 6 deselected, 4 failed`；4 failures均为未修改frozen publication manifest/config digest不一致
- pre-fix full Ruff: 89个未修改文件中的既有错误；changed files focused Ruff通过

## Residual risks

- full pyright、full pytest与full Ruff将在final validation gate对最终HEAD重跑；当前aggregate review不把较早快照冒充最终验证。
- production provider/CLI非确定性与真实AAPL corpus尚未在本gate观察；后续必须fresh workspace运行正式rolling-correction场景并人工核对labels/claims/refs。
- raw SQLite含用户已裁决允许的API key，但公开evidence必须排除raw DB并做exact-value secret scan。
- formal scenario仍由Oracle/用户裁决，任何post-fix observation不能自行标记accepted/ready。

## Decision

Aggregate deepreview通过，等待MiMo/DeepSeek两路aggregate结论。若二者无新finding，则可进入final validation与fresh production CLI observation；尚不push、不改PR状态。
