# WU-TOOLS-01-F01-02-R1 Plan Re-Review Gate

## Metadata

- Work unit: `WU-TOOLS-01-F01-02-R1`
- Gate: plan re-review after fix
- Reviewer: AgentMiMo
- Timestamp: `2026-06-21T18:20:47+0800`
- Plan artifact: `docs/host/wu-tools-01-f01-02-r1-plan.md`
- Controller adjudication: `docs/reviews/wu-tools-01-f01-02-r1-plan-review-controller-adjudication.md`
- Plan-fix artifact: `docs/reviews/wu-tools-01-f01-02-r1-plan-fix-codex.md`

## Scope

This is a narrow re-review gate. The scope is limited to verifying that all controller accepted findings have been closed by the plan-fix, and that no new blocking risks were introduced. A full adversarial plan review is not repeated.

## Finding Re-Review

### C-F01 activation terminal guarantee / lock-order

**Controller requirement:**
- `activate_observation(handle)` and `cancel_observation(handle)` coordinate through the same observation lock.
- Activation checks cancellation / terminal / submitted state and marks submitted under that lock before submit.
- Any activation failure path after accepted wait must ensure observation reaches terminal `FAILED` or `LOST`; ToolRuntime catch is only a safety net.
- Tests include unexpected activation exception and deterministic cancel/activate ordering.

**Plan evidence after fix:**

1. Section 7 states: "activate_observation(handle) and cancel_observation(handle) must coordinate through the same observation lock used by current observation mutation, i.e. the implementation must not introduce a separate activation-only lock or rely on an unlocked status check."

2. Section 7 states: "Activate must, while holding that same observation lock, look up the observation by handle, check cancellation state, terminal status and submitted flag, then atomically mark the record submitted before any executor submit can occur."

3. Section 6 states: "Fins activation is the primary owner for recording terminal observable state. After Host wait accept has succeeded, every activation failure path must leave the prepared observation terminal FAILED or LOST when the observation handle can identify a record; ToolRuntime exception handling is only a safety net for bounded diagnostics."

4. Section 7 states: "ToolRuntime should catch unexpected activation adapter exceptions, emit bounded diagnostic through existing diagnostic emitter, and still return awaiting outcome because Host wait truth has already been accepted. This catch is only a safety net; implementation must not rely on ToolRuntime catch as the primary mechanism that makes the Fins observation terminal."

5. Slice 2 expected assertions include: "deterministic cancel/activate ordering test proves cancellation and activation share one observation lock, using a barrier, holding executor, or equivalent deterministic fixture rather than timing sleeps" and "unexpected activation exception after a prepared observation exists is observable as terminal failed/lost and cannot remain indefinitely PENDING."

**Status: 已修复**

All five sub-requirements are addressed with explicit plan text. The lock coordination, under-lock state checks, terminal failure guarantee, ToolRuntime safety-net-only framing, and deterministic test requirements are all present.

---

### C-F02 Slice 1 allowed test fixture changes

**Controller requirement:**
- Slice 1 exact allowed changes explicitly allow test fixture extension to inject `wait_activation_registry` and spy/stub activation adapters.

**Plan evidence after fix:**

Slice 1 exact allowed changes now includes: "Extend Host test fixture / harness construction in `tests/host/test_toolruntime_executor.py` to inject `wait_activation_registry` and spy/stub activation adapters, so tests can assert exact activation call counts without production Fins wiring."

Slice 1 expected assertions now includes: "Host tests use a spy/stub activation adapter injected through the extended fixture, not production Fins runtime, to prove activation call count and non-activation paths."

**Status: 已修复**

The plan now explicitly permits Host test fixture injection and requires spy/stub verification.

---

### C-F03 prepared-but-unaccepted observation behavior

**Controller requirement:**
- Add accept rejected / timeout / stale execution behavior for prepared-but-unaccepted observations.
- Avoid adding a durable cleanup ledger or public contract.
- Keep cleanup best-effort and process-local, or explicitly document harmless orphan semantics.

**Plan evidence after fix:**

Section 7 now includes a dedicated `Prepared-but-unaccepted observations` subsection stating:

- "For accept rejected, accept timeout, stale execution rejected by Host accept, or cancellation before a wait is accepted, ToolRuntime must not activate the prepared observation."
- "These paths must not introduce a durable cleanup ledger, public cleanup contract, new Host wait state, or new ToolAwaitingOutcome field."
- "A prepared-but-unaccepted observation has no Host wait record, has not submitted executor work, and is process-local to the Fins runtime. It is therefore harmless if it remains as an unreachable process-local orphan until runtime teardown."
- "Implementation may add only narrowly scoped best-effort process-local abandon if the existing prepared handle is already available without changing public contracts. If no safe existing handle path is available, leaving the harmless process-local orphan is the intended behavior for this WU."

**Status: 已修复**

The plan now explicitly defines the behavior: no activation, no durable cleanup, no public contract expansion, and documented harmless process-local orphan semantics with optional best-effort abandon only when a safe handle path exists.

---

### C-F04 unified registry alternative

**Controller decision:** `rejected-with-reason` — the controller decided the separate `WaitActivationRegistry` is the better minimal design and did not require a design change.

**Controller requirement:**
- Optionally note that the separate activation registry is an intentional boundary choice.

**Plan evidence after fix:**

Section 7 `Why this is not overdesigned` now states: "The separate WaitActivationRegistry is an intentional boundary choice for this WU: executable provider activation remains construction-time Host wiring instead of being added to the public wait binding metadata."

**Status: 已修复 (evidence for rejected-with-reason)**

The plan now explicitly documents the separate registry as an intentional design choice, as the controller requested.

---

## New Blocking Findings

No new blocking findings were identified during this re-review. The plan-fix addressed all controller accepted findings without introducing new structural risks.

## Open Questions

None.

## Residual Risks

Residual risks remain unchanged from the controller adjudication:

- Production poller scheduling / backoff / fencing / retry remains owned by GitHub Issue #90.
- External provider physical cancel / revoke / abandon remains owned by GitHub Issue #92.
- Callback endpoint / auth / replay remains owned by GitHub Issue #89.
- Process-local observation loss on Host restart remains consistent with the current lightweight observation design and is owned by later wait hardening work.

## Conclusion

**pass**

All four controller accepted findings have been closed by the plan-fix:
- C-F01: 已修复 — activation lock coordination, terminal failure guarantee, safety-net-only ToolRuntime catch, deterministic test requirements all present.
- C-F02: 已修复 — Slice 1 explicitly permits test fixture injection of activation registry and spy/stub adapters.
- C-F03: 已修复 — prepared-but-unaccepted observation behavior is explicit with harmless orphan semantics and no durable cleanup or public contract expansion.
- C-F04: 已修复 (evidence for rejected-with-reason) — separate WaitActivationRegistry documented as intentional boundary choice.

The plan is now code-generation-ready for the implementation agent.
