# Code Review

## Scope

- Mode: current changes（re-review，同一 UF-FIX08 Slice 2 cycle）
- Branch: `codex/upload-filing-oracle`
- Base: `cc07db75`（用户指定；accepted plan commit，Slice 1 已合入）
- Output file: `docs/reviews/code-review-20260816-144206.md`
- Review 时间：2026-08-16 14:42
- Review inputs：上一份 FAIL review `docs/reviews/code-review-20260816-142141.md`、另一份 PASS review `docs/reviews/code-review-20260816-143020.md`、更新后的 implementation artifact（含 Controller adjudication 表 A–G）
- Included scope：相对 HEAD 的全部 workspace changes
  - 生产：`_fs_source_integrity.py`（新，1727 行）、`_fs_source_document_core.py`、`_fs_source_snapshot.py`、`_fs_storage_infra.py`、以及 Controller 授权的 4 个 owner-contract files：`repository_protocols.py`、`fs_batching_repository.py`、`_fs_filing_upload_state_core.py`、`fs_filing_upload_state_repository.py`
  - 测试：`tests/fins/test_fins_storage_atomicity.py`（+1304）、`test_fins_storage_provider.py`、`test_processor_read_consistency.py`
  - 文档：implementation artifact、两份 review artifact（只读输入）
- Excluded scope：无；本 fix cycle 新增 4 个文件的 diff 已逐行阅读，`_fs_source_integrity.py` 关键变更段（`_apply_manifest_facts`、`_derive_repair_blocked_reason`、`_manifest_projection_for_comparison`）已重新全文核对
- Parallel review coverage：无；主 reviewer 直接走读
- 独立复现：351 passed（30.27s，3 warnings 均为 edgar deprecated import）；pyright 0 errors/0 warnings/0 informations；`git diff --check` 通过

## 旧 findings 逐项闭环

| 旧 finding | Controller 裁决 | 本 re-review 验证结论 |
| --- | --- | --- |
| 142141 #1 commit_batch Raises 未声明 `SourceIntegrityPreflightError` | A accepted | **已闭环**。protocol（`repository_protocols.py:432-433`）与 facade（`fs_batching_repository.py:74-75`）Raises 均声明 typed preflight；真实 corruption 测试 `test_complete_validator_rejects_identity_descriptor_symlink_and_mismatch` 断言 exception type、`reason is UNSAFE_PUBLICATION`、capability 终态消费（二次 commit 抛「transaction 未在当前 storage core 登记」）、published ticker 目录零写入；新增契约测试 `test_commit_batch_contract_declares_whole_tree_preflight_error` 锁定两处公开 Raises 文档 |
| 142141 #2 upload-state 行为/Raises 漂移 | B accepted | **已闭环**。seam 签名从 `meta_path` 改为 `ticker_dir`（`_target_ticker_dir` 为纯路径拼接，`_fs_storage_infra.py:2806-2824`，不在 inspector 前读 descriptor）；core（`_fs_filing_upload_state_core.py:115-120`）、protocol（`repository_protocols.py:215-218`）、facade（`fs_filing_upload_state_repository.py:64-67`）三处 Raises 同步为「可归属 target 的结构损坏返回 UNSAFE typed state」；meta 缺失与 descriptor 缺失两个测试均断言 typed state（reasons 精确为 `(META_UNTRUSTED,)` / `(IDENTITY_UNTRUSTED,)`）、`source_meta=None` 与零 mutation；state core 内无 `_source_meta_path_for_read` 残留 |
| 142141 #3 commit 通用 `ValueError` 文案 | F covered by later approved slice | 裁决记录；未重新报告（见 Residual Risk） |
| 142141 #4 exact 扫描成本 | G residual / later work unit | 裁决记录；未重新报告（见 Residual Risk） |
| 142141 #5 snapshot containment root 恒真 | rejected-with-reason | 裁决记录；`physical_path` 由 inspector 以 normalized 单组件 name 构造，`physical_path.parent` 即 document directory，无 correctness defect |
| 142141 #6 trusted manifest + local UNSAFE 虚构 cross/shared reason | C accepted | **已闭环**。`_apply_manifest_facts` 逐项比较循环（`_fs_source_integrity.py:1130-1137`）在 `expected is None`（damaged source 无法产生 canonical projection）时不再计入 mismatch；新测试 `test_trusted_manifest_preserves_local_unsafe_without_cross_reason` 断言 exact target reasons 仅 `(META_UNTRUSTED,)`、exact/whole shared reasons 均为空、exact/whole inventory 相等；真实 cross 场景仍拒绝——unsafe grid 的 `cross_source_inconsistency`（sibling 损坏 + manifest 删除）与 trusted-manifest 的 dangling/duplicate/ticker-conflict 测试均通过 |
| 142141 #7 whole-kind `NON_TARGET_SOURCE_INCOMPLETE` 语义失真 | D accepted | **已闭环**。`_derive_repair_blocked_reason`（`_fs_source_integrity.py:1284-1287`）whole-kind 分支提前返回：inventory 非空且 canonical 空 → `CANONICAL_MANIFEST_UNAVAILABLE`，否则 None；unsafe 恒 `CROSS_SOURCE_PUBLICATION_UNSAFE`。direct payload 测试断言两分支（`test_trusted_manifest_preserves_local_unsafe_without_cross_reason:6746-6748` 与 `test_damaged_fixture_classifier_snapshot_and_commit_are_consistent:6872-6874`） |
| 142141 #8 + 143020 #2 revision field/helper 双 owner | E accepted | **已闭环**。`_SOURCE_REVISION_META_FIELD`、`_source_revision_from_meta`、`_source_meta_without_revision` 仅在 `_fs_source_integrity.py` 定义；document core（import 于 `:39-41`）与 snapshot（import 于 `:32-37`）直接依赖该真源；infra 的定义与 import 已删除、无兼容 re-export；grep 全 storage 目录确认无其它定义点；写路径（document core `:1689-1691` 生成新 revision）与 marker retry（snapshot `:987`）均消费同一真源 |
| 143020 #1 marker retry 旧独立读取 | covered by later approved slice / pre-existing | 裁决记录；marker retry 与 FD/marker/resource lifecycle 保留未动（`_read_source_snapshot` 的 `_STABLE_READ_ATTEMPT_LIMIT` 循环、`_acquire_snapshot_attempt` 的 guard/cleanup 结构均与上一轮一致），snapshot lifecycle 无回退 |
| 143020 #3 inventory 排序依赖循环顺序 | rejected-with-reason | 裁决记录；固定 kind tuple（FILING 在 MATERIAL 前）与 inspector 内 `sorted(inspections_by_id)` 提供稳定顺序 |

## Findings

### 1-未修复-低-exact-mode `NON_TARGET_SOURCE_INCOMPLETE` 分支无 inspection-level direct payload 测试

- **入口/函数**: `_derive_repair_blocked_reason`（exact-target mode 分支）
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:1288-1294`
- **输入场景**: exact mode 下 target content 非 UNSAFE、某 non-target source content 非 COMPLETE（例如 sibling original missing）→ 返回 `NON_TARGET_SOURCE_INCOMPLETE`
- **实际分支**: `item.document_id != target_id and item.content_classification.status is not COMPLETE` 命中
- **预期行为**: Slice 4 的 repair 消费方依赖该 reason 区分「target 可修但 sibling 损坏」与其它阻断
- **实际行为**: 该分支目前只有 Slice 1 的 error-type 构造测试（`test_fins_storage_atomicity.py:7100-7102`），没有任何真实 inspection payload 测试锁住「exact mode + sibling repairable damaged → NON_TARGET_SOURCE_INCOMPLETE」行为；D 的 direct payload 测试只覆盖了 whole-kind 两分支
- **直接证据**: grep atomicity 中 `repair_blocked` 断言仅 3 处（6746、6872、7100）；6746/6872 为 whole-kind 场景，7100 为 error-type 构造
- **影响**: 分支逻辑当前正确（代码走读验证），但在 Slice 4 消费前无行为锁，后续改动可静默破坏而测试不红
- **建议改法和验证点**: Slice 4 实现 repair 消费时补 direct payload 测试（exact mode + complete target + repairable sibling → `NON_TARGET_SOURCE_INCOMPLETE`）；本 slice 不阻塞
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-契约测试用 docstring 字符串匹配锁定 Raises 面

- **入口/函数**: `test_commit_batch_contract_declares_whole_tree_preflight_error`
- **文件(行号)**: `tests/fins/test_fins_storage_atomicity.py:4217-4235`
- **输入场景**: 未来 Raises 文档改写措辞（如类型名拆行、翻译）但语义保留
- **实际分支**: `assert "SourceIntegrityPreflightError" in protocol_doc` 纯子串匹配
- **预期行为**: 契约测试应锁定「行为与声明一致」，而不是锁定具体字符串
- **实际行为**: 字符串重构（无行为变化）会使测试红；反之若 docstring 含类型名但语义错误（如声明在错误方法上），该测试不红。真实行为已被 `test_complete_validator_rejects_identity_descriptor_symlink_and_mismatch` 的异常类型断言锁定，本测试是补充锚点而非唯一防线
- **直接证据**: 测试函数体仅 4 行 `in` 断言
- **影响**: 测试脆弱性，无 correctness 影响；真实行为有独立测试覆盖
- **建议改法和验证点**: 可保留（低成本锚点）；若后续要求更高保真，改为结构化契约检查或直接删除（真实行为测试已覆盖）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

1. **manifest-missing + local-damaged 组合的 cross 叠加语义**：`_apply_manifest_facts` 的 `not manifest.exists` 分支（`_fs_source_integrity.py:1100-1113`）对「manifest 缺失 + 存在 damaged source」仍叠加 `CROSS_SOURCE_INCONSISTENCY`（shared 为 `SOURCE_MANIFEST_MISSING`）。C 的修复范围是「trusted manifest 不虚构」，该组合在 scope 外且被既有测试冻结（unsafe grid `cross_source_inconsistency` 删除 manifest + 损坏 sibling）。语义上「manifest 缺失使任何 source 的 manifest 关系不可验证」的跨 source 标记是否精确，未在 plan §11.1 明确定义——记录备 controller 知悉，不作为 finding。
2. **`_manifest_projection_for_comparison` 的 expected=None 跳过比较后**，若 Slice 4 的 manifest rewrite 需要区分「item 存在但不可比对」与「item 缺失」，当前 payload 不携带该区分信息——Slice 4 消费面可能需要在 inspection 中显式化该事实。

## Residual Risk

- implementation 文档声称的逐文件 coverage（86%/84%/92%/85%/98%/94%/100%/92%，TOTAL 87%）未独立复现（tests/pyright/diff-check 已复现）。
- 未运行真实 CLI、UF-PF08、UF-PF12、provider/converter evidence——与 implementation 文档声明一致。
- commit 内部通用 path-free `ValueError` 文案（旧 finding #3）按 Controller 裁决由 Slice 5 bounded failure projection 拥有；在 Slice 5 接线前，commit 失败诊断仍是通用消息。
- exact-target mode 全 kind 扫描性能（旧 finding #4）按 Controller 裁决为 residual，待独立 performance work unit 量化。
- snapshot marker retry 的旧独立读取路径（143020 #1）对 descriptor 损坏仍抛 raw `ValueError`——Controller 裁决为 pre-existing、covered by later slice；后续迁移时需独立验证 retry 语义。
- Finding 1 的 exact-mode `NON_TARGET_SOURCE_INCOMPLETE` 分支在 Slice 4 消费前无 direct payload 行为锁。

## 结论

- **Blocking findings**: 无。Controller accepted A/B/C/D/E 五项均 root-cause 闭环：契约文件与行为同步（A/B）、reason 语义与真实事实一致且真实 cross 仍 fail-closed（C）、whole-kind blocked reason 准确（D）、revision schema 事实单一 owner（E）；新增 4 个文件范围均有明确 owner 理由（contract owner / facade owner / state core owner），snapshot guard/FD/marker/retry lifecycle 无回退。
- **Nonblocking findings**: 2 项（低）。
- **验证复现**: 351 passed；pyright 0 errors；`git diff --check` 通过——与更新后 implementation 文档一致。
- **总体判定**: **PASS**（无 blocking findings；未修代码、未提交，与 deepreview 边界一致）
