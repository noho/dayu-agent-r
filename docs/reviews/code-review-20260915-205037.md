# Code Review：aggregate deepreview

## Scope

- Mode：current changes；Gate：aggregate deepreview；work unit：midea-q1-full-report。
- Reviewer：controller，按 deepreview skill 执行；timestamp 来自本机系统时钟。
- Branch：codex/upload-material-oracle；Base/HEAD：d30a07c870e3602638d8943ab71a5eaa3e01fc9b。
- Included：最终六文件 diff：selection.py、selection tests、workflow tests、根/Fins/tests README；本轮 plan/review/fix/implementation 产物及来源审核链。
- Excluded：无关既有分支工作、投资 workspace 写入、全仓 exhaustive review、未修改的 OCR/预处理实现。
- 子 Agent coverage：DS 独立 code review/re-review 六文件；Codex 实施并验证真实仓储回归；MiMo 候选诊断经 controller 复核，其错误叙述和失效 manifest 被排除；controller 独立保存原始响应并执行真实 CLI、PDF 视觉与全量最终验证。
- Artifact：`docs/reviews/code-review-20260915-205037.md`。

## Findings

未发现新增实质性问题。

前序审查 F1 rejected-with-reason/证据失效；F2、F3 accepted/已修复，DS 同轮复审通过。Controller 复核：storage COMPLETE 有文件及 manifest 检查，不能称“同财期永远 skip”；amended 现由同一 helper 投影；同日混合修订新增用例确实走两种输入顺序。

审查记录数字纠正：DS 的结尾将初审 209 与复审单文件 77 混合，并写成 77+52+80=209。最终以 controller 保存的 `review-fixed-tests.txt` 为准：**210 passed，coverage 92%**；`review-fixed-pyright.txt` 为全库 0 errors。初审结果 209/91% 是较早版本，不能作为最终版本数字。

## 实际调用链与 adversarial failure pass

- 公开 CLI download 参数通过既有 runtime/pipeline 进入 CninfoDiscoveryClient；provider 分类公告按股票、查询窗归一，selection 在同财年财期 group 内先过滤再 max；只有选中项调用 HEAD，然后投影 candidate。
- key 入参为 typed raw announcement。empty group 原逻辑返回 None；非空组依次比较修订、日期、非正文、ID、URL。bool 排序与旧 int 标记等价，日期仍 ISO 日粒度。只有第三及后续平手规则新增，没有吞异常或额外网络。
- 标题缺“全文”不会降为正文；仅正文不被过滤。摘要及带英文标记公告在排序前被排除；更正公告仍走既有 notice filter。真实更正/较新报告的优先级不受形态规则覆盖。
- `_is_cninfo_amended_title` 对排序与 `candidate.amended` 使用相同函数；不存在选择修订而投影非修订的双真源。
- CN identity 仍按财期及 amended 分配；新 source_id 在同一槽位遇 COMPLETE 默认 skip。overwrite 原有参数由单份 filing 阶段机实际消费，阻止 skip/reuse；批次最终发布来源字段、PDF/Docling 和清单。不改 storage 以文件大小猜完整性。
- 取消、失败回滚、并发 source revision recheck 没有生产改动，现有 workflow tests 连同扩展回归全部通过。新增排序是无状态纯函数，没有新增 I/O、可变共享状态或调度状态。
- 元数据 current projection 与真实 bytes 独立勾稽：每文件 SHA/size、pdf_sha、内容 fingerprint、remote fingerprint、exact 文件集合、manifest/period/provider identity。成功后再次增量冻结快照字节完全相同。

## 项目指令、架构、所有权与耦合

- 改动在候选选择 owner；downloader 不承接产品筛选新规则，storage 不判断财报内容，UI 不补偿语义。
- 生产仅一文件，两个私有 helper、一个常量，无新依赖、schema、公开字段、反向 import、兼容分支或 fallback。
- 新增函数完整中文 docstring，类型参数齐全；无 Any/object/hasattr/getattr。
- README 各自在现有读者职责内说明当前行为，不塞入治理状态或未来实现；测试边界跟 owner，不重写 pipeline 排序 fake。
- 数据链采用真实来源 → 不覆盖 Raw → 脚本审核 → 最终结果；未写投资 workspace、未直接替换受管 PDF/meta/manifest。

## Validation

- 最终 focused pytest：210 passed；selection statement coverage：92%。
- 最终全量 `python -m pyright dayu/ tests/ utils/`：0 errors, 0 warnings。
- `git diff --check`：通过。
- 公共 CLI 六次执行全部 exit 0，baseline 正文、改后旧缓存 skip、overwrite 全文、再次 skip、fresh 全文、最后代码版本再 skip；`reconciliation-v1.json` 全部检查通过。
- Raw 字节 query 两窗口各 3 行、hasMore=false、唯一 ID、同股票、同时间戳、同集合及顺序；源审核底稿 `source-probe-20260915T124607546138Z-audit.json` 全部通过。
- 主 Agent 图像核查 17 页全文的第 11–12 页确认 2021-03-31 合并原表、货币资金/借款/股本行，未把图片文字提取缺失伪装成未披露。

## Open Questions

无阻塞问题。

## Residual Risk 与决策

- fixed in current slice：同日形态漏选、输入顺序平手、amended 双点、混合修订测试缺口；无未修复 accepted finding。
- assigned to later work unit，Docling/财表提取维护者：图片财表行名识别不完整，不能从 JSON 直接认定财务字段审核通过。
- assigned to later work unit，原投资 workflow 主 Agent：独立核数并决定投资 workspace 补源及 G1 接受；本轮不改投资状态。
- 已批准范围边界：跨日期及修订优先级、report_date=null/UTC filing_date 投影、overwrite 历史版本策略均保持既有合同；不宣称已完成通用完整性或日期修复。未证实的纯英文标题/其他修订词/HK 平手边界归各自后续 owner 按真实反例另立 work unit，不在当前补丁静默扩展。
- 旧 MiMo 证据叙述及 v2 manifest 自 hash：证据失效，最终报告只引用 controller source-probe 及 CLI 审核产物；原 Raw 保留，差异原因已记录。

## Completion

Aggregate deepreview：PASS；无新增 accepted finding，fix/re-review 无需执行；accepted deepreview commit：no-commit（用户明确约束）。
当前本地修复可供独立验证。push/create draft PR/PR review 不执行；用户“不自动提交”优先于技能默认发布链，不宣称 draft-PR-pass 或 final-closeout-pass。交付本地 closeout summary 后由原投资 Agent 独立验证；未来若另行授权 Git 发布，从 accepted plan/slice/deepreview checkpoints 的实际提交开始补齐，不假装已有 commit/PR。
