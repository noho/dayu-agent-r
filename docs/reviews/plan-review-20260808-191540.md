# F19 RR5 独立最终 plan / wrapper 复审（DS 路）

- 时间：2026-08-08T19:15:40+08:00
- 结论：**PASS**
- goal SHA-256：`ddebaf1159bf8a19fbc3d171e75b88fdb54df23b60089015c03191cf6a3df004`
- plan SHA-256：`8764962d9b615ee1d383d3e933971e2d62c929e8d53825dcd8d1f45edd5eb8ac`
- driver SHA-256：`f3e151283d21715076d9ef8f41f67ba5170fc5114efce5de3b01c0586ddd141e`
- R1-R4 prompt SHA-256：`681d525c...6869` / `bbe66106...28d` / `82aaf14a...2c6` / `345cac1f...86f`

## 1. Reviewed target and scope

本轮只复审 RR4 的 frozen PTY-owner finding 是否在唯一 owner boundary 关闭，并执行必要回归：RR3 real-path matrix、repo/CLI location non-goal、RR2 typed canonical Run/R4 compact，以及首轮 identity/deadline/allocation/offset/projection/preflight。

全部验证均为 provider-free synthetic fixture 或只读 identity 检查；未创建 formal run root/deadline，未调用 provider，未修改被审文件或 F18，未执行 commit/push。plan 不含 machine-local absolute private path；driver 与四个 prompts 均由 Git ignore，private absolute constant未进入 plan/public projection。

## 2. Assumptions tested

### 2.1 PTY owner 使用唯一派生真源

- `_pty_owner_path(harness)` 是 frozen harness executable sibling 的唯一派生 helper。
- `_execution_identity()` 通过 `_sha256(_pty_owner_path(args.harness))` 读取 owner bytes/SHA。
- `_validate_real_paths()` 也先由同一 helper 得到 `pty_owner`，并将其纳入：
  - checked absolute/traversal path 集合；
  - exact `<root>/evidence/private/controller/prompt_observe_calibration_frozen.py` layout；
  - 从 filesystem root 到 trusted parent、run root及 leaf 的 lexical `lstat` no-symlink；
  - existing non-symlink regular-file gate；
  - resolved path 相对 run root containment。
- real `main()` 在 `_validate_real_paths()` 之后才读取 prompt/JSON，execution SHA 更晚发生；projection `_emit()` 与 subprocess spawn 位于全部 gate 之后。

### 2.2 PTY-owner provider-free 直接反例

使用隔离、自动清理的 synthetic run tree，保留实际 main sequencing，并把 `_emit` 与 subprocess 替换为只计数、不写文件/不启动进程的 probe：

| 场景 | 结果 | owner helper | owner bytes read | emit | spawn | leaf 状态 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| controller 内 regular frozen owner | PASS | 2 | 1 | 1 | 1 | unchanged |
| owner symlink 指向 run root 外 existing frozen bytes | fail closed | 1 | 0 | 0 | 0 | unchanged |
| owner symlink 指向 run root 内 existing frozen bytes | fail closed | 1 | 0 | 0 | 0 | unchanged |
| owner symlink 指向 dangling target | fail closed | 1 | 0 | 0 | 0 | unchanged |

三类反例的 wrapper projection 与 evidence leaf均未创建。regular 正例中 helper 恰好分别服务 path gate 与 execution SHA，证明没有第二套 owner path 推导；owner SHA 与冻结值 exact match。

### 2.3 必要回归

- RR3 path matrix：授权 direct-child 正例通过；same-basename outside、F18-like nested、canonical-parent ancestor、run-root、`workspaces`、`evidence`、`private`、声明 leaf symlink、`..` 与 wrong layout均在 owner bytes读取前 fail closed，且 `emit=0/spawn=0`、fresh leaves未创建。
- repo/CLI 仍位于 run root 外且合法正例通过；它们只受 detached target/clean 与 file SHA owner约束，没有被纳入 canonical run-root location contract。
- typed R4：第三 Run accepted-terminal 区间内唯一同 session/run compact 正例通过；缺失、重复、错 Run/session、accepted 前、terminal 后与 strict payload drift 均失败。
- R1-R4 offset 仍固定为 `0/1/2/3`；allocation/deadline owner代码保持 run/root/chain/workspace/label/digest、1800/180/540 秒与 finite-number gate。
- stdout-only preflight通过并保持 `spawn=false/provider=false`、allocation/budget `not_frozen`、sentinel未创建；错误 CLI/model/harness/target及 empty prompt均失败。
- driver通过 `py_compile`、Ruff、pyright；prompt SHA未变化，仍为 strict UTF-8、non-empty、NUL absent。

## 3. Findings

无未修复 material finding。RR4 的 PTY-owner executable leaf 现已由同一 helper同时归属 path gate 与 execution SHA owner，三类 symlink 反例均在任何 owner bytes读取、写入或 spawn 前失败。

## 4. Open questions

无。

## 5. Residual risks and tracking

- 本 PASS 只判定 F19 plan/private wrapper 已满足 pre-provider gate，不是 real observation verdict。
- replacement/reconnect、repair/fallback 自然覆盖以及最终 publication同源性仍须由 formal execution直接观察；未自然触发的分支必须保持 `needs-more-evidence`。
- B2 仍为 `unadjudicated`，用户裁决前 overall readiness不得标 ready。

## 6. Final plan review conclusion

**PASS。** RR4 finding 已在正确的 private driver owner boundary关闭，必要 path/state/identity回归无异常。就本轮冻结 plan/driver/prompts 而言，没有阻止 Controller 进入后续 formal setup/execution gate 的未修复 finding。
