# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `a29c8eb5`（UF-FIX08 Slice 2 HEAD）
- Output file: `docs/reviews/code-review-20260816-153600.md`
- Included scope: 5 个生产文件（`ingestion_runtime.py`、`upload_failure.py`、`upload_repair_contract.py`、
  `storage/_fs_filing_upload_state_core.py`、`storage/_fs_source_integrity.py`）与 7 个测试文件的全部
  workspace changes（相对 `a29c8eb5` 无 staged/committed 增量）
- Excluded scope: 无
- Parallel review coverage: 无（单 reviewer 全链路走读）
- 参考输入：accepted plan §5.3/§5.4/§5.5、§9 Slice 3、§11 validator tests；
  `docs/gateflow/uf-fix08-existing-source-auto-repair-slice3-implementation-20260816.md`；`AGENTS.md` 语义 owner 约束

## Adversarial Focus 结论（8 项）

1. **同 guard 单一 inspection 消费：通过。** `read_filing_upload_state()`（`_fs_filing_upload_state_core.py:107-139`）在
   既有 publication guard 内只调用一次 `_inspect_source_kind_unguarded()`，从同一 `inspection.target` 取
   `classification` 与 `business_meta`；`MISSING/UNSAFE` 固定 `source_meta=None`，`COMPLETE/REPAIR_REQUIRED` 只消费
   同一 inspection 的 `business_meta`（inspector 已剥离 `_published_source_revision` 私有字段，见
   `_fs_source_integrity.py:578`）。`_get_source_meta_unguarded`/`_classify_source_integrity_unguarded` 已从 state-core
   Protocol 删除；残留于 `_fs_source_document_core.py` 的同名 seam 只服务 `get_source_meta` 等既有公开 API，不属于
   upload-state 路径（Slice 2 已冻结，非本 slice 引入）。exact-target mode 的 `inspection.target` 恒非 None
   （`_select_exact_target` 对缺失 target 返回 MISSING、不可信 manifest 返回 UNSAFE），两个新 `RuntimeError` 是
   生产者不变量、不可达。
2. **repair contract 封闭性：通过。** 两个 disposition 均 `frozen+slots`，discriminator 与
   `ExistingSourceAutoRepair.expected_integrity`（FILING + REPAIR_REQUIRED + trusted revision）由各自 `__post_init__`
   锁定；`ValidatedFinsUploadFilingRequest.__post_init__` 对 disposition 做 isinstance 封闭校验、`expected_integrity`
   与 published integrity 全字段相等校验、raw action/resolved action/selection 一致校验。生产代码中
   `ValidatedFinsUploadFilingRequest` 与两个 disposition 的唯一构造点是 validator
   （`ingestion_runtime.py:1417/1441/1452`），无下游可伪造入口。唯一保留意见见 Finding 1（selection 校验的
   实现方式重复了 static owner 派生规则，而非复用 owner helper）。
3. **validator precedence：通过。** 顺序精确为 static admission → `_validate_filing_upload_published_state`
   （producer identity/status/meta 不变量）→ `UNSAFE` 抛 `FinsUploadPrevalidationError`（先于 action/company，
   测试用 `pytest.fail` monkeypatch 证明下游零调用）→ `REPAIR_REQUIRED` 只接受 raw action 精确 `"auto"`
   （大小写/带空白伪 auto 及 create/update/delete 均得固定 usage code）且 selection 非空 → 固定 `update` +
   `ExistingSourceAutoRepair`；`MISSING/COMPLETE` 保持原 action/overwrite/deleted 解析（diff 为纯搬迁）；
   company decision 最后。`overwrite`/`is_deleted` 不改变 repair eligibility（parametrize 覆盖）。
4. **三个 failure factory：通过。** 三个 storage-kind closed code 加入 `_STORAGE_FAILURE_CODES`，唯一模块级 factory
   的 message/retry hint 固定、无路径分隔符（`FinsUploadFailureReason.__post_init__` 与
   `_validate_failure_reason_text` 双重拦截）、无 revision/internal reason；JSON round-trip 测试钉住 exact kind/code。
   本 slice 仅 validator 消费 unsafe factory；stale/blocked 未接线属 plan 授权的 Slice 4/5 序列。
5. **raw runtime start 传播：通过。** `start_upload`/`prepare_observed_upload`/`upload` 均先
   `_validate_runtime_upload_request` 再创建 job/observation/producer；Raises 契约显式加入
   `FinsUploadPrevalidationError`。owner 测试证明零 job、零 observation、零 runner、零 workspace mutation 且
   failure 为 exact unsafe factory。
6. **same-basename owner 修复：通过（含 1 个测试缺口，见 Finding 4）。** `_classify_role_projection` 改为 storage
   name 唯一权威 identity：`derived_from` 精确命中后只校验该 asset 的 `original_filename` 同源；exact identity
   损坏时仅唯一 basename 命中才可修复，同 basename 命中多个 originals 仍闭合
   `UNSAFE/FILE_DECLARATION_UNTRUSTED`。两个 UF-FIX07 Docling 节点恢复通过，未放行真实歧义；重复 storage name、
   多 Docling、original 缺 `original_filename` 等既有拒绝规则未放宽。
7. **fixture 迁移：通过（含 1 个测试意图漂移，见 Finding 3）。** `_filing_upload_published_state` 的
   status/reasons 改为 required keyword（删除从 `source_meta is None` 反推），与 controller 约束一致；
   CLI/SEC/CN/ingestion fixtures 从单文件 original-primary 迁移为 original+docling 双 entries（Slice 2 inspector
   的 user-upload 规则所必需），测试意图（prevalidation 投影、authoritative handoff、atomic failure）保持；
   SEC/CN 两处 `replace()` 伪造 file_selection 的旧测试改为真实 validator 产物并断言 `validator_calls ==
   [request.request]`，属加强而非削弱。唯一意图漂移见 Finding 3。
8. **scope/typing/README/证据：通过。** `git diff --check` 干净；README/oracle/scenario/design 零 diff（plan §12
   README 属 Slice 6，controller 已裁决本 slice 不改）；新增 diff 无 `object`/`Any`/`hasattr`/`getattr`/伪
   callable cast；docstring 整体完整（两处 Raises 遗漏见 Finding 2）。独立复跑：owner tests 333 passed；
   storage+UF-FIX07 定向 300 passed；SEC/CN/CLI 196 passed；完整 Fins suite **1802 passed, 1 skipped**
   （与实现 doc 声明一致）；pyright **0 errors, 0 warnings**；五个修改生产文件 branch coverage 独立复测
   85%/93%/82%/88%/87%，均 ≥80%。

## Findings

### 1-未修复-中-validated request 的 selection 不变量重复实现 static selection owner 的派生规则
- **入口/函数**: `ValidatedFinsUploadFilingRequest.__post_init__` → `_validate_filing_selection_matches_request`
- **文件(行号)**: `dayu/fins/ingestion_runtime.py:807、832-912`；owner 实现在同文件 `1120-1159`
  （`_select_fins_upload_filing_files`）与 `1218-1252`（static admission）
- **输入场景**: 任意 `ValidatedFinsUploadFilingRequest` 构造（validator return 路径，测试中 `replace()` 亦触发）
- **实际分支**: `__post_init__` 从 raw request 重新 normalize files/selectors、重判 selector 数量、单文件隐式
  primary、membership，重算 companions 并重建 `FinsUploadFilingFiles.for_upsert(...)` 与存储 selection 比较
- **预期行为**: selection 语义唯一 owner 是 static admission（`_select_fins_upload_filing_files`）；validated
  request 的不变量校验应复用同一 owner projection helper，而不是从 raw fields 第二次派生
- **实际行为**: 同一套派生规则（844-900 行）以 ValueError 镜像再次实现；当前两套规则逐条等价且同模块，无行为
  差异，但 owner 规则一旦演进（如 UF-FIX07 类角色/选择规则调整）必须双点同步修改，构成 AGENTS「禁止各自从 raw
  fields 反推语义」所指的语义漂移风险
- **直接证据**: `_select_fins_upload_filing_files`（1141-1158）的 selector 数量/隐式 primary/membership/companions
  保序规则与 `_validate_filing_selection_matches_request`（861-891）逐条一一对应，但两函数互不调用
- **影响**: 维护性/语义漂移风险；无当前行为错误
- **建议改法和验证点**: 抽取模块级纯派生 helper（files/selectors → primary/companions，抛 ValueError），static
  admission 在 helper 之上做 usage code 投影，`__post_init__` 调用同一 helper 重建 expected selection 比较；
  验证点：现有 `test_filing_validator_*` selection 测试与
  `test_validated_filing_repair_contract_rejects_incomplete_selection_and_target` 全绿，另确认两调用点引用同一
  helper
- **修复风险（低）**:
- **严重程度（中）**:

### 2-未修复-低-新增异常未写入 docstring Raises 契约
- **入口/函数**: `_FsFilingUploadStateMixin.read_filing_upload_state`、`ValidatedFinsUploadFilingRequest.__post_init__`、
  `_validate_filing_selection_matches_request`
- **文件(行号)**: `_fs_filing_upload_state_core.py:75-83`（Raises 缺新抛的 `RuntimeError`，实抛于 122/131）；
  `ingestion_runtime.py:793-796、844-847`（Raises 缺经 `_normalize_fins_upload_path`（1097-1099）可能抛出的
  `FinsUploadUsageError`）
- **输入场景**: state-core 生产者不变量违约（target/business_meta 为 None，理论不可达）；或 validated request
  构造时 path expand/resolve 失败（校验与构造间的竞态）
- **实际分支**: 两个 `raise RuntimeError(...)` 与 `_normalize_fins_upload_path` 的 usage 分支均可真实执行
- **预期行为**: CLAUDE.md 要求 docstring 至少覆盖参数、返回值、异常
- **实际行为**: Raises 段落未列出上述异常，Service 边界等按 Raises 契约写 except 的调用方可能漏接
- **直接证据**: `_fs_filing_upload_state_core.py:120-131` 新增两处 `raise RuntimeError`，docstring（75-83）未列；
  `__post_init__` 经 `_validate_filing_selection_matches_request` 调用 `_normalize_fins_upload_path`，该函数
  `_raise_upload_usage(FILE_NOT_FOUND)` 可抛出 `FinsUploadUsageError`，两处 docstring 未列
- **影响**: 契约文档漂移；仅文档层面
- **建议改法和验证点**: 三个 docstring 的 Raises 补 `RuntimeError` / `FinsUploadUsageError`；pyright 全仓复跑
- **修复风险（低）**:
- **严重程度（低）**:

### 3-未修复-低-SEC/CN 身份错配测试被构造不变量前置拦截，workflow 守卫失去直接覆盖
- **入口/函数**: `test_upload_filing_authoritative_identity_mismatch_fails_closed`（SEC/CN 各一）
- **文件(行号)**: `tests/fins/test_sec_pipeline_upload_filing_stream.py:1506-1525`、
  `tests/fins/test_cn_pipeline.py:1826-1845`；生产守卫 `dayu/fins/pipelines/sec_upload_workflow.py:309-330`、
  `dayu/fins/pipelines/cn_pipeline.py:1771`
- **输入场景**: monkeypatch validator 注入 `document_id` 漂移
- **实际分支**: `replace(validated, document_id=...)` 触发新 `__post_init__`（`ingestion_runtime.py:803-804`）在
  构造期抛 `ValueError("published document 不一致")`，异常在 monkeypatched validator 内部产生，
  `_assert_authoritative_filing_identity`（workflow 级 preflight-vs-fresh 三字段比较）不再被执行
- **预期行为**: 原测试意图是证明 workflow 拒绝 fresh validator 的身份漂移（该守卫比较 canonical ticker /
  document_id / internal_document_id）
- **实际行为**: 测试名与 fail-closed/零 batch 断言保持，但被验证的生产路径从 workflow 守卫换成了 validated
  request 构造不变量；`internal_document_id` 与 canonical ticker 漂移无任何测试注入，workflow 守卫的漂移路径
  回归覆盖成为空洞
- **直接证据**: `mismatched_validator` 注入字段为 `document_id`；`__post_init__`（803-804）先于 workflow
  `_assert_authoritative_filing_identity`（sec_upload_workflow.py:169 调用点之后才执行到）抛错；
  全仓 grep 无 `internal_document_id` 漂移注入测试
- **影响**: 生产 workflow 守卫 regression coverage 空洞；不产生行为错误
- **建议改法和验证点**: `mismatched_validator` 改为注入 `internal_document_id` 漂移（或 canonical ticker 漂移），
  使 `__post_init__` 放行、workflow 守卫真正被命中；或新增一条内部身份漂移专测；验证点：两测试继续
  断言 fail-closed 与 `begin_tokens == []`
- **修复风险（低）**:
- **严重程度（低）**:

### 4-未修复-低-same-basename 修复的行为面扩大（矛盾派生场景 UNSAFE→REPAIR_REQUIRED）无直接回归测试
- **入口/函数**: `_classify_role_projection`
- **文件(行号)**: `dayu/fins/storage/_fs_source_integrity.py:895-910`；回归测试
  `tests/fins/test_fins_storage_atomicity.py:6493-6563`
- **输入场景**: Docling `derived_from` 精确命中 original A，但 Docling `original_filename` 属于另一 original B
  （两个持久化事实互相矛盾）
- **实际分支**: `len(derived_matches) == 1` 分支只校验 A 的 basename 同源，不比较 B；`derived_from not in
  original_names` 不成立，最终仅得 `DERIVED_PROJECTION_MISMATCH`
- **预期行为**: 按 controller 裁决的「storage name = authoritative identity」owner 语义，该场景是 A 的
  basename 投影损坏、可确定性重转 A 修复，分类 REPAIR_REQUIRED 是设计意图；但此分类变化（旧实现 UNSAFE）应被
  测试钉住
- **实际行为**: 新回归测试只覆盖 (a) 同 basename 合法 COMPLETE、(b) derived 缺失+双 basename 命中 UNSAFE；
  矛盾场景（derived 命中 A + basename 命中 B）与 derived 缺失+唯一 basename 的 REPAIR_REQUIRED 转换未被新测试
  显式覆盖（后者由既有 repairable grid 的 `derived_projection_mismatch` 覆盖，前者无任何覆盖）
- **直接证据**: 新测试 `test_source_integrity_uses_storage_name_for_same_basename_asset_identity` 两个 phase 的
  fixture 均未构造「derived 指向 A 且 Docling basename 等于 B 的 basename」；旧实现该分支
  `derived_matches[0] != filename_matches[0] → return None`（UNSAFE），新实现放行为 repairable
- **影响**: 若未来把该场景重新视为歧义，repair 会按错误 original 重建且无测试信号；属测试缺口而非已证实的缺陷
- **建议改法和验证点**: 补一条直接 owner 测试：双 original 异名，derived_from 命中 A 且 Docling
  `original_filename` 改为 B 的 basename → 断言 `REPAIR_REQUIRED` + 仅 `DERIVED_PROJECTION_MISMATCH`；
  并考虑在 slice doc 明示该行为面变化
- **修复风险（低）**:
- **严重程度（低）**:

## Open Questions

- 无。

## Residual Risk

- SEC/CN/HK async job 内 fresh validator 的 `FinsUploadPrevalidationError` typed failed event 未实现（plan 授权
  Slice 5）。当前 `_run_upload_job` 兜底 `_save_failed_from_exception` 落 `str(exc)`＝bounded failure message（无
  路径），但 result failure_reason 仍为 generic，属于 Slice 5 前中间态。
- `read_filing_upload_state` 的 MISSING fast-path（ticker 目录不存在时直接构造 classification，不经过
  inspector）为基线既有行为，与 inspector rule 1（无 dangling fact）语义等价，本 slice 未变更。
- `SOURCE_REVISION_STALE`/`SOURCE_REPAIR_BLOCKED` factory 未接线（plan Slice 4/5）。
- 本 review 未独立复跑逐文件 coverage 全集（仅复测 5 个修改文件，均 ≥80%）；未运行真实 CLI/provider evidence
  （属 UF-PF08/UF-PF12，plan 禁止）。
- 发现本 session 之前已存在的未追踪 artifact `docs/reviews/code-review-20260816-153026.md`（另一份指向同一
  base 的 slice 3 review，非本 review 产物、未修改）；controller 需注意两份 review 的裁决合并，避免重复计票。

## 复验证据（本 review 独立执行）

```text
pytest -q tests/fins/test_fins_ingestion_runtime.py tests/fins/test_fins_service_runtime.py tests/fins/test_upload_failure.py
  333 passed
pytest -q <两 UF-FIX07 Docling 节点 + 新 same-basename 回归> tests/fins/test_fins_storage_atomicity.py tests/fins/test_fins_storage_provider.py
  300 passed
pytest -q tests/fins/test_sec_pipeline_upload_filing_stream.py tests/fins/test_cn_pipeline.py tests/cli/test_fins_commands.py
  196 passed
pytest -q tests/fins
  1802 passed, 1 skipped（skip 为仓库既有环境条件 skip；3 warnings 来自已安装 edgar 包）
python -m pyright dayu/ tests/ utils/
  0 errors, 0 warnings, 0 informations
coverage（focused matrix，独立重跑，五个修改生产文件）:
  _fs_source_integrity.py 85%、_fs_filing_upload_state_core.py 93%、upload_repair_contract.py 82%、
  ingestion_runtime.py 88%、upload_failure.py 87%（全部 >=80%）
git diff --check：干净
README/oracle/scenario/design/registry diff：无输出
新增 diff 签名扫描：无 object/Any/hasattr/getattr/伪 callable cast
```

## Verdict

**PASS**。8 个 adversarial focus 全部通过；4 个 findings 均为 non-blocking（1 中 3 低），无语义 owner 违例、
无状态机/原子性缺陷、无兼容分支或 default/loose inference。中危 Finding 1 建议在下个 slice 顺手收敛，
低危 Findings 2-4 不阻塞进入 Slice 4。
