# Code Review

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `d97d233b`
- Output file: `docs/reviews/code-review-20260817-015152.md`
- Review date/time: 2026-08-17 01:51:52（本机时钟）
- Included scope: 相对 base 的全部 workspace diff（17 个文件，+2197/-125）：
  - 生产：`dayu/fins/storage/repository_protocols.py`、`_fs_company_meta_core.py`、`_fs_filing_upload_state_core.py`、`_fs_source_integrity.py`、`fs_filing_upload_state_repository.py`、`__init__.py`、`dayu/fins/pipelines/docling_upload_service.py`、`dayu/fins/pipelines/filing_upload_publication.py`（新增）、`dayu/fins/upload_failure.py`
  - 测试：`tests/fins/test_filing_upload_publication.py`（新增）、`test_docling_upload_service.py`、`test_fins_storage_atomicity.py`、`test_fins_ingestion_runtime.py`、`test_fins_ingestion_tools.py`、`test_upload_failure.py`、`test_fins_service_runtime.py`、`tests/cli/test_fins_commands.py`、`tests/service/test_fins_direct.py`
  - 文档：`docs/gateflow/uf-fix10-s1-implementation-20260816.md`
- Excluded scope: generated/vendor/build/cache；S2 lifecycle wiring；SEC/CN/HK workflow；README
- Parallel review coverage: 无
- 参考文档：accepted adjudication（`uf-fix10-s1-code-review-adjudication-20260817.md`）、两份前审 artifact（`code-review-20260817-012504.md`、`code-review-20260817-013652.md`）、S1 implementation artifact、accepted plan（`uf-fix10-same-request-concurrency-plan-20260816.md`）

## 结论

**Pass。** accepted F2–F5 + C1 全部按 owner boundary 完成修复且测试真实；F1 rejected-with-reason 与 Q1/Q2 rejected-with-reason 均符合 accepted plan 裁决；S1 未接 S2，SEC/CN/HK/workflow/README 零改动。无遗留 blocker。

## Findings

未发现实质性问题。

以下为对 adjudication accepted 项的逐项定点验证与已确认的 non-blocking 观察。

## Accepted Fix 验证

### F2 — prepared/durable identity 跨 owner 等价测试

- **裁决**：`accepted-blocker`
- **修复位置**：`tests/fins/test_docling_upload_service.py:5315-5396`（`test_prepared_identity_exactly_matches_real_published_storage_projection`）
- **验证**：真实多文件 prepare → `_publish_prepared_upload` → `FsFilingUploadStateRepository.read_filing_upload_state` → `durable_state.publication_identity == prepared_identity`。逐字段断言覆盖 primary_original_asset_name、companion_original_asset_names、每个 asset 的 (name, original_filename, derived_from, sha256, size, content_type, source) 与 source role 集合。使用 `_prepare_filing_candidate_for_test` 真实 conversion/meta owner 构造 candidate，不使用手工 identity。
- **结论**：✓ 通过。prepared 与 durable identity 从同一 business fact 投影且 equality 已被真实存储路径证明。

### F3 — §7.4 conflict grid、UNSAFE 与 stable invariant 测试

- **裁决**：`accepted`
- **修复位置**：`tests/fins/test_filing_upload_publication.py:526-651`（`test_arbitration_conflict_grid_is_closed_and_typed`，5 格参数化）、`:654-703`（`test_arbitration_rejects_unsafe_observation_at_either_entry`）、`:706-744`（`test_arbitration_rejects_stable_repair_action_invariant_drift`）
- **验证**：
  - 五格 conflict grid：`MISSING→REPAIR_REQUIRED`、`COMPLETE→MISSING`、`COMPLETE→REPAIR_REQUIRED`、`auto+overwrite=True MISSING→COMPLETE`（identity 不匹配）、`explicit create+overwrite=False MISSING→COMPLETE`（手动构造 validator 不产生的 fresh create）。每格断言 `disposition is CONFLICT`、`failure_reason.code is SOURCE_PUBLICATION_CONFLICT`、非 SKIP。
  - UNSAFE 入口：initial/fresh 各一侧伪造 UNSAFE，`pytest.raises(ValueError, match="UNSAFE source observation")`。
  - stable REPAIR_REQUIRED invariant：伪造两侧 `resolved_action="create"`（非 "update"），`pytest.raises(ValueError, match="stable REPAIR_REQUIRED observation")`。
- **结论**：✓ 通过。封闭表覆盖完整，explicit create 绝不 skip 已断言固定。

### F4 — identity required text/content_type 运行时类型校验

- **裁决**：`accepted`
- **修复位置**：`dayu/fins/storage/repository_protocols.py:222-229`（`FilingUploadPublicationIdentity.__post_init__` required_texts loop 改为 `if not isinstance(value, str)`）、`:161-163`（`FilingUploadAssetDescriptor.__post_init__` name/original_filename `isinstance` 检查）、`:175-178`（content_type `isinstance` 检查）
- **验证**：
  - `FilingUploadPublicationIdentity`：required_texts loop 对每个字段先 `isinstance(value, str)` 再 `not value`。
  - `FilingUploadAssetDescriptor`：name/original_filename 先 `isinstance(self.name, str)`；content_type `not isinstance(self.content_type, str)` 抛 TypeError。
  - 测试 `test_publication_identity_rejects_non_string_business_text`：`replace(identity, fiscal_period=2024)` → TypeError；`replace(identity.assets[0], name=7)` → TypeError；`replace(identity.assets[0], content_type=5)` → TypeError。
- **结论**：✓ 通过。closed dataclass contract 的运行时类型校验已补齐。

### F5(a) — 空 staging 合法 document batch read 测试

- **裁决**：`accepted`
- **修复位置**：`tests/fins/test_fins_storage_atomicity.py:530-557`（`test_filing_upload_state_batch_reader_returns_missing_from_empty_staging_without_mutation`）
- **验证**：空 workspace → `begin_batch("AAPL")` → `_snapshot_workspace_tree` → `read_filing_upload_state_in_batch(batch, "aapl-2024-fy")` → 断言 company_meta is None、status is MISSING、revision is None、reasons == ()、source_meta is None、publication_identity is None → `_snapshot_workspace_tree` unchanged → `rollback_batch`。
- **结论**：✓ 通过。S2 新 ticker 首发 fresh read 已有回归防护。

### F5(b) — published empty document_id fail-fast 测试

- **裁决**：`accepted-as-intended-contract`
- **修复位置**：`tests/fins/test_fins_storage_atomicity.py:560-574`（`test_filing_upload_state_published_reader_rejects_empty_document_before_mutation`）
- **验证**：`FsFilingUploadStateRepository(workspace_root).read_filing_upload_state("AAPL", "")` → `pytest.raises(ValueError, match="document_id")` → `_snapshot_workspace_tree` unchanged。
- **结论**：✓ 通过。public input contract 收紧已测试固定，workspace 零 mutation。

### C1 — stable REPAIR_REQUIRED action 检查

- **裁决**：`accepted-low`
- **修复位置**：`dayu/fins/pipelines/filing_upload_publication.py:280`（`fresh_request.resolved_action != "update"` 替代旧行 `initial_request.resolved_action != "update"`）
- **验证**：
  - 代码路径：`:261` 先检查 `initial_request.resolved_action != fresh_request.resolved_action`（equal guard）→ `:278-286` 进入 REPAIR_REQUIRED 分支 → `:280` 检查 `fresh_request.resolved_action != "update"`。equal guard 已保证两侧一致，检查 fresh 是精确 owner 语义。
  - 测试 `test_arbitration_rejects_stable_repair_action_invariant_drift`：伪造两侧 `resolved_action="create"`（均非 "update"），`pytest.raises(ValueError, match="stable REPAIR_REQUIRED observation")`。
- **结论**：✓ 通过。重复 initial 条件已消除，fresh 检查精确。

### F1 — rebase version 路径分歧

- **裁决**：`rejected-with-reason`
- **验证**：`rebase_prepared_filing_create_overwrite()` 在 `:1293-1297` 保持 `identical_skip_safe=True`，与 adjudication 裁决一致：accepted plan §6.3 冻结 rebase 同 fingerprint 保留 fresh version、不同 fingerprint 递增。未引入 sequential/rebase version 必须相等的新 contract。
- **结论**：✓ 符合裁决。deferred residual（多文件 role-ambiguous 版本分歧）已登记。

## S1 边界确认

### S2 lifecycle 未接线

- `filing_upload_publication.py` 无任何生产 import（`execute_prepared_filing_publication` 不存在于代码库）。
- `_PreparedFilingAssetMutation` 未接入 `prepare_upload()` 返回路径。
- `commit_prepared_upload_batch` 未修改。
- SEC/CN/HK workflow 零 diff（`git diff d97d233b --stat -- dayu/fins/pipelines/` 只有 `docling_upload_service.py` 和新增 `filing_upload_publication.py`）。

### SEC/CN/HK/workflow/README 零改

- `dayu/host/`、`dayu/engine/`、`dayu/service/`、`dayu/ui/`、`dayu/runtime/`：零 diff。
- `README.md`、`dayu/README.md`、`dayu/fins/README.md`、`tests/README.md`：零 diff。
- `dayu/fins/workflows/`、`dayu/fins/oracle/`、`dayu/fins/scenario/`、`dayu/fins/registry/`：零 diff。

### Protocol fake conformance

- `test_fins_ingestion_runtime.py`：`_FixedFilingUploadStateRepository` 与 `_ForbiddenFilingUploadStateRepository` 均实现 `read_filing_upload_state_in_batch()`，独立 `batch_calls` 记录，forbidden fake record-then-`AssertionError`。`cast(FilingUploadStateRepositoryProtocol, state_repository)` 已移除（fake 现在精确 conform）。
- `test_fins_ingestion_tools.py`：`_ForbiddenFilingUploadStateRepository` 同上。新增 `test_tool_static_admission_state_fake_conforms_to_required_batch_read_contract`。
- 三个现有 filing static-admission cases 均补充 `batch_calls == []` 断言。

## Open Questions

- 无。

## Residual Risk

1. **`test_fins_service_runtime.py` 动态 monkeypatch fake**：按 Controller acceptance（Q3）维持未来触发式、非阻塞 residual。本 work unit 无 batch-read 路径经过该 fake。

2. **`has_same_source_publication_identity` 命名**：该函数比较的是 `revision` equality 而非 `publication_identity` equality（`source_integrity.py:262-287`），命名可能造成混淆。pre-existing，非 S1 引入。

3. **Coverage 数据**：implementation artifact 报告四模块合计 85%（≥80% 目标）。逐文件：arbitration 87%、Docling 88%、storage batch-state core 95%、typed failure 84%。未独立验证，依赖 implementation artifact 声明。

4. **未覆盖的 arbitration 状态转换**：pre-existing residual（前审 #1），`COMPLETE→MISSING`、`COMPLETE→REPAIR_REQUIRED`、`MISSING→REPAIR_REQUIRED` 三个 changed-observation 格已被 F3 conflict grid 测试覆盖。已闭合。
