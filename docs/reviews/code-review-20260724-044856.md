# Code Review

## Scope

- Mode: current changes (WU-CTX-01 Slice 1 implementation re-review)
- Branch: `feat/wu-ctx-01`
- Base: `ed43bcf2`
- Output file: `docs/reviews/code-review-20260724-044856.md`
- Included scope: `dayu/host/*.py`, `tests/host/*.py`, `dayu/host/README.md`, `tests/README.md` — 58 files, 13380 insertions, 4140 deletions
- Excluded scope: Controller-owned `docs/host/issues-implementation-control.md`, Controller adjudication (`docs/reviews/wu-ctx-01-slice-1-implementation-review-controller-adjudication.md`), 既有 review artifacts (`docs/reviews/code-review-20260724-*`, `docs/reviews/wu-ctx-01-slice-1-implementation-review-fix-codex.md`), `docs/reviews/wu-ctx-01-slice-1-implementation-resume-codex.md`)
- Parallel review coverage: 5 个子 agent 分别覆盖 —— (1) CTRL-IMPL-01 dispatch effective tools 与 shared contract chain，(2) CTRL-IMPL-02/03/04 NoBudgetDispatchStart stage、continuation typed reason、EventLog row/hot identity，(3) REVIEW-IMPL-05/06/07/08/09 5x3 matrix、compactor invariant、dead code、docstring，(4) adversarial failure pass 与 4 项途中复核，(5) Slice boundary、attempt producer manifest-before-start、direct promotion 残留、overcoupling。主 reviewer 独立复核全部 CTRL-IMPL 关键代码路径并交叉验证 subagent 结论一致性。

## Decision

**pass**

WU-CTX-01 Slice 1 实现的 9 项 accepted findings（CTRL/REVIEW-IMPL-01..09）已逐项正确关闭。四项途中复核——repair replay 永久 no-tool、continuation limited manifest 不误判 pre-start duplicate 但仍校验 row/hot、steer 保留 SUBSET/ALL/NONE caller intent、source failure 固定 projection→tool→policy→request typed precedence——全部验证通过。所有 actual new Attempt producer 遵守 manifest-before-start 不变量，startup exact replay 不重新评估 candidate，wait completed/cancelled only 创建新 Attempt、failed/lost 零新 Attempt，5-stage/15-cell 矩阵完整，compactor invariant 在 manifest parser 中强制执行，direct promotion 零残留。未发现 Slice 2 `CONTEXT_BUDGET_EVALUATED`、Slice 3 usage-anchor 执行路径、新增双向依赖或语义所有权漂移。

## Findings

### 关闭矩阵

| Finding | 严重度 | 状态 | 直接证据 |
| --- | --- | --- | --- |
| CTRL-IMPL-01 | 高 | 已关闭 | `EffectiveToolFacts` shared contract 在 `admission.py:239`，`validate_effective_tool_facts_runtime` 在 `admission.py:4032`，`dispatch.py:2603` 消费 frozen exact names 且 drift fail closed；`_selected_tool_names_from_effective_tool_set` 已删除 |
| CTRL-IMPL-02 | 中 | 已关闭 | `NoBudgetDispatchStart.stage` 在 `dispatch.py:731`，三个 caller 分别传入 `ORDINARY`(:1829)、`POST_COMPACT`(:2424)、`DISPATCH_FALLBACK`(:1999/:3300)，commit owner `:2742` 只读 `plan.stage` |
| CTRL-IMPL-03 | 中 | 已关闭 | `PreparedRunnerCallSourceFailureCategory` typed enum 在 `run_input.py:411`，`_continuation_frozen_sources` 在 `engine_ingest.py:6190` 按 typed category 映射，`_continuation_reason_from_source_failure` 在 `:6257` 用 identity comparison 不用 message parsing |
| CTRL-IMPL-04 | 中 | 已关闭 | `_find_existing_runner_call_manifest_event` 在 `run_input.py:6258-6264` 校验 row session/run，`:6271-6278` 校验 hot identity，continuation manifests 在之前通过 identity check 后在 `:6288-6289` skip duplicate detection |
| REVIEW-IMPL-05 | 中 | 已关闭 | `test_context_budget.py:66-159` 冻结 15 cells (5×3)，包含 CONTINUATION×3，docstring `:167` 写 "5x3"，unknown stage/pressure fail-closed `:208-225` |
| REVIEW-IMPL-06 | 中 | 已关闭 | `_runner_call_manifest.py:1383-1387` 拒绝 compactor_proposal+COMPLETE，`:1430-1433` 拒绝 dispatch-relevant+NOT_APPLICABLE |
| REVIEW-IMPL-07 | 低 | 已关闭 | `_source_policy_snapshot_in_transaction` 与相关 imports/constants 已从 `engine_ingest.py` 删除 |
| REVIEW-IMPL-08 | 低 | 已关闭 | `_create_running_admission_result` 与 direct-running imports 已从 `admission.py` 删除 |
| REVIEW-IMPL-09 | 低 | 已关闭 | `BudgetEstimate.estimator_digest` docstring 在 `context_budget.py:279` 写 "估算器契约、完整估算输入与固定估算常量的 digest" |

### 四项途中复核

| 复核项 | 状态 | 直接证据 |
| --- | --- | --- |
| Repair replay 永久 no-tool | 通过 | `admission.py:2732` 传 `tooling_options=None`，`dispatch.py:2598-2601` 对 REPLAY 传 `None`；`validate_effective_tool_facts_runtime` 对二者用空 definitions 对空 digest 校验 |
| Continuation limited manifest 不误判 pre-start duplicate | 通过 | `run_input.py:6288-6289` 对 iteration pair 非空 `continue`，`:6294-6297` 重复检测在 skip 之后；`:6258-6278` row/hot identity 在 skip 之前 |
| Steer 保留 SUBSET/ALL/NONE caller intent | 通过 | `admission.py:3288-3295` SUBSET+非空 names+非 replay+frozen policy disabled fail closed；ALL/NONE 优雅降级为 NO_TOOL_DISABLED |
| Source failure 固定 projection→tool→policy→request | 通过 | `run_input.py:2851-3004` 按 TOOL_SCHEMA→POLICY→REQUEST_SEMANTICS 顺序执行，同类型内短路径返回；`engine_ingest.py:6267-6277` 以 identity comparison 映射 typed category |

### 新发现（本次独立 review）

**未发现实质性问题。**

adversarial failure pass 确认了以下防御性设计正确运作：
- `effective_tool_facts_json` strict field-matching (`admission.py:3968`) 对缺失/额外/类型错误字段全部 fail closed
- `_find_existing_runner_call_manifest_event` 在 SQLite WAL write transaction 内运行，TOCTOU 受 snapshot isolation 保护
- admission 工具冻结与 dispatch 校验使用 content-addressed digest 防静默漂移
- 所有终端状态 advancement 路径正确守卫

### semantic ownership drift 检查

未发现 semantic ownership drift。`EffectiveToolFacts` 的 producer（`effective_tool_facts_json`）、parser（`parse_effective_tool_facts`）与 validator（`validate_effective_tool_facts_runtime`）同在 `admission.py`，consumer（`dispatch.py`）通过同一 typed contract 消费。continuation source failure category 由 `run_input.py` strict owner 产生并通过 typed enum 投影到 `engine_ingest.py`，Engine 不按异常 message 或调用顺序猜 reason。

### 过度耦合检查

未发现过度耦合。`dispatch.py → admission.py` 是单向依赖，`admission.py` 不反向 import `dispatch.py`。`_runner_call_manifest.py` 不依赖 dispatch/admission。`run_input.py` 不 import `engine_ingest.py`。全部层间依赖为合法的单向关系。

## Open Questions

无。

## Residual Risk

- Optional real compactor smoke 未启用（Controller 明确保留）
- Gemini 等真实 provider quota/endpoint smoke 未执行（Controller 明确保留）
- 默认 pytest 配置排除 6 个 stress tests（Controller 明确保留）
- `test_open_host_active_cancel_watchdog_public_watch_observes_cancelled` 存在间歇性测试隔离失败（单独运行通过，全量运行时偶发性失败）；非本次 change 引入
