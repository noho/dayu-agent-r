# WU-CM-08 Plan Fix Focused Re-Review — AgentDS

## Review Metadata

| 字段 | 值 |
|---|---|
| reviewed artifact | `docs/host/host-issues/wu-cm-08-compaction-material-readability-smoke-plan.md` (fix version) |
| prior reviews | `docs/reviews/plan-review-20260612-164857.md` (AgentDS), `docs/reviews/plan-review-20260612-165055.md` (AgentMiMo) |
| review type | Focused re-review: verify all prior findings are resolved |
| reviewer | AgentDS |
| review date | 2026-06-12 |

## Conclusion: PASS

All findings from both prior reviews are resolved. The plan is code-generation-ready for both slices.

---

## Finding Resolution Matrix

### AgentDS (plan-review-20260612-164857.md) — 6 findings, all resolved

| # | Severity | Finding | Resolution in Plan |
|---|---|---|---|
| 1 | Low | S1 stale-name exclusion test partially pre-existing; plan should acknowledge and justify strengthening | L38: "Strengthen the **existing** stale-name coverage... it already excludes `stable_input`, `history_input`, and `evidence_input`; add positive assertions that all six current top-level JSON keys are present" — acknowledges existing test, specifies positive assertion of all 6 keys including `instruction` |
| 2 | Medium | S1 "summarize material pack / vNext input shape" too vague | L34-37: Concrete `_MaterialPackShape` NamedTuple (6 typed fields) and `_VNextInputShape` NamedTuple (6 typed fields); boundary-specific failure message requirement with explicit section name list |
| 3 | Low | S1 allowed files incomplete for conditional production fix path | L42: "Do not modify production files in this slice. If the strengthened tests expose a real production defect... stop the slice, record the defect... and request a separate production-fix scope instead of silently broadening WU-CM-08." — clean stop-the-line approach, no conditional allowed files |
| 4 | Medium | S2 `public_smoke_support.py` not in allowed files | L56: Added to allowed files with constraint: "only if avoiding duplication requires extracting a genuinely general public-smoke assertion helper; do not move compact-specific helpers there and do not change existing helper signatures or behavior." |
| 5 | Medium | S2 extracted helpers lack self-correctness validation | L68: "Add positive and negative tests for each newly extracted non-trivial helper. Negative cases must assert the boundary-specific failure message" with concrete failure message examples |
| 6 | Low | Cross-cutting: residual risk tracking and control doc update not addressed | L80-86: New "Artifacts And State Transitions" section covering plan acceptance gate, S1/S2 implementation reports, aggregate deepreview, residual risk recording, and next work unit (WU-CM-09) |

### AgentDS Non-Finding Recommendations — all addressed

| Recommendation | Plan Resolution |
|---|---|
| §5: Add `git diff --stat` to validation | L49, L78: `git diff --name-only` added to both S1 and S2 validation |
| §6: README conditional for new shared helpers | L93: "If a new shared helper module is introduced... update `tests/README.md` to document the maintenance pattern." |
| §7: `fake_compaction.py` cast residual risk | L71: "If new assertions interact with `tests/host/fake_compaction.py` and encounter the pre-existing `cast(...)` residual from WU-CM-05, document it in the implementation report and do not fix it in WU-CM-08 unless it causes a direct test or pyright failure inside the approved slice." |
| §9.1: `instruction` as sixth top-level key | L38: Six-key list includes `instruction` |

### AgentMiMo (plan-review-20260612-165055.md) — 3 actionable suggestions, all resolved

| # | Severity | Suggestion | Resolution in Plan |
|---|---|---|---|
| 1 | Low | S1 should clarify strengthen existing vs add new test | L38: "Strengthen the existing stale-name coverage in `test_conversation_compact_input_vnext_maps_material_without_citable_current_anchor`" — unambiguous: strengthen existing |
| 2 | Low | S2 helper overlap with `_assert_compactor_material_instruction_contract`; plan should clarify refactor vs parallel | L62: "compactor material instruction contract, by extending or splitting the existing `_assert_compactor_material_instruction_contract(...)` rather than adding a parallel overlapping helper" |
| 3 | Low | S1/S2 validation should explicitly require baseline re-verification | L46, L75: "Before edits: `pytest tests/host/test_compact_material.py tests/host/test_public_compact_smoke.py -q` to confirm the 41 passed, 1 skipped baseline has not drifted." in both slice validations |
| 4 | Info | Comment Issue #95 after implementation | Not a plan amendment; informational only |

---

## Scope Boundary Re-verification

The plan fix did not expand scope. Verified:

- **No new allowed files** beyond what was reasonably requested (only `public_smoke_support.py` added as conditional).
- **No new production code changes** authorized; the conditional path was replaced with a stop-the-line approach (Finding 3).
- **No new test layers or semantics** introduced.
- **Non-goals unchanged** from the original plan.

---

## Residual Notes (Non-Binding)

1. The plan's "Artifacts And State Transitions" section references `implementation` gate in control doc. The implementation agent should verify the exact state field name in `issues-implementation-control.md` before updating.

2. The aggregate deepreview mentioned in Artifacts should include a check that the `instruction` key (sixth top-level JSON key) is properly covered in S1 positive assertions — the plan lists all six but the implementation agent must not forget `instruction`.

3. No new blocking risks introduced by the fix. The plan remains a test-maintenance-only WU with clean boundaries.
