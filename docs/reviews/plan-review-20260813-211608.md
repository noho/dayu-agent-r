# Plan Re-Review — UF-FIX03 summary-and-bounded-errors（定向 N1–N3 复核）

- **Reviewed target**: `docs/gateflow/uf-fix03-summary-bounded-errors-plan-20260813.md`（N1–N3 fix 后版本）
- **Binding goal contract**: `docs/gateflow/uf-fix03-summary-bounded-errors-goal-confirmation-20260813.md`
- **Controller adjudication**: `docs/gateflow/uf-fix03-plan-rereview-adjudication-20260813.md`
- **Fix artifact**: `docs/gateflow/uf-fix03-plan-rereview-fix-20260813.md`
- **Prior re-review**: `docs/reviews/plan-review-20260813-210506.md`（AgentDS，`pass`，发现 N1–N3）
- **Reviewer**: 定向 plan re-review gate（独立验证，未采纳 fix artifact 自述结论）
- **Timestamp**: 20260813-211608
- **基线**: HEAD `31817dc5da64126d1779fe276898f69488626a1a` 与 plan §1 一致；冻结 SHA 实测一致。
- **约束遵守**: 本 gate 未修改 plan、生产代码、测试、README、冻结 JSON 或任何既有 artifact。
- **Scope**: 仅复核 controller accepted findings N1–N3；不复核 M1–M7、D1–D5、C1–C5（已由前轮 re-review 闭合）。

## Review method

独立读取 plan（修订后 799 行）、fix artifact（126 行）、adjudication（51 行）与前轮 re-review（130 行），并用以下代码位置的直接事实核对 N1–N3 的 plan 修订：

- `dayu/fins/ingestion_runtime.py:295–309`（`_UPLOAD_TERMINAL_DISPOSITIONS`，5 项闭集）
- `dayu/fins/ingestion_runtime.py:1185–1201`（`FinsUploadPipelineResult.from_pipeline_json`，parser 调 `cls(...)`）
- `dayu/fins/ingestion_runtime.py:1204–1251`（`FinsUploadResultSummary`，当前 `__post_init__` 只校 failure/status 配对）
- `dayu/fins/upload_failure.py:17`（`_FAILURE_KEYS`，当前四键 frozenset）
- `dayu/fins/upload_failure.py:74–99`（`FinsUploadFailureReason.__post_init__`，校 message/retry_hint，不校 file_label）
- `dayu/fins/upload_failure.py:218–235`（`upload_failure_reason_from_json`，exact key 读取 + kind/code 一致性校验）
- `dayu/fins/direct_events.py:45–59`（`_DISALLOWED_TEXT_FRAGMENTS`，14 项）
- `dayu/fins/direct_events.py:1067–1104`（`_validate_safe_text`，max_chars/URL/fragment/job/path 拒绝）
- `dayu/fins/pipelines/sec_upload_workflow.py:280–349`（SEC filing catch chain）
- `dayu/fins/pipelines/cn_pipeline.py:900–969`（CN filing catch chain）

## N1 验证 — pipeline `cancelled` 状态矩阵在 constructor owner 闭合

### Plan 修订内容

§5.2 明确 pipeline status 闭集为 `{ok, skipped, deleted, failed, cancelled}`，新增 `cancelled -> stored_file_count == 0` 行，显式禁止依赖 summary/renderer/下游兜底。S1 断言 3 覆盖 direct constructor 与 JSON parser 接受 `cancelled+0`、拒绝 `cancelled+positive`。fix artifact C4 事实勘误已记录。

### 代码事实核对

| Plan 声称 | 代码事实 | 匹配 |
| --- | --- | --- |
| pipeline status 闭集包含 `cancelled` | `_UPLOAD_TERMINAL_DISPOSITIONS` 有 5 项，`"cancelled" -> CANCELLED`（:305） | ✓ |
| `cancelled` 由 shared cancelled producer 产出 | `sec_upload_workflow.py:287`、`cn_pipeline.py:907`、`docling_upload_service.py:1409`、`prepare_upload` 取消分支 :301–302 | ✓ |
| `from_pipeline_json` 调 `cls(...)` 触发 `__post_init__` | `ingestion_runtime.py:1191` 为唯一 production `FinsUploadPipelineResult(...)` 构造点 | ✓ |
| 当前 `__post_init__` 不校验 count | `ingestion_runtime.py:1233–1250` 只校 failure/status 配对，无 count 校验 | ✓（plan 正确识别缺口） |
| fix artifact C4 事实前提错误 | C4 原文"避免给 pipeline 虚构 cancelled 状态"；代码明确有 cancelled | ✓（勘误正确） |

### 结论

**N1 闭合**。plan §5.2 owner 矩阵与代码事实完全一致；constructor owner 是唯一校验点；parser 做 exact 取值后调 constructor；cancelled 状态在代码中真实存在且有 producer。

---

## N2 验证 — 合法超长 basename 固定隐藏并保持 known content kind/code，pathful 拒绝

### Plan 修订内容

§5.4 指定：canonicalizer 对合法 basename 超过 `240` 时确定性返回固定标签 `输入文件（文件名已隐藏）`，与 fragment/URL/job/path/`Cc/Cf` 同源处理。pathful 输入仍拒绝。filing empty/conversion producer 先 canonicalize 再构造 reason；超长合法 basename 仍保持原 closed content kind/code。S2 断言 5 覆盖超长合法 basename 场景。

### 代码事实核对

| Plan 声称 | 代码事实 | 匹配 |
| --- | --- | --- |
| `_validate_safe_text` 对 `>max_chars` 拒绝 | `direct_events.py:1079`: `if len(value) > max_chars: raise ValueError(...)` | ✓（canonicalizer 需避免此路径） |
| `_DISALLOWED_TEXT_FRAGMENTS` 含 fragment | 14 项，含 `job_id`、`财报正文` 等 | ✓ |
| `_validate_safe_text` 无 `Cc/Cf` 检查 | 实测代码无 Unicode category 检查，与 plan 声称一致 | ✓ |
| 依赖方向 `upload_failure -> direct_events` 安全 | `direct_events` 不 import `upload_failure` | ✓ |
| SEC/CN catch chain 结构 | `DoclingConversionCancelledError -> DoclingConversionError -> OSError -> Exception`，均调 `fins_upload_failure_from_exception(exc)` | ✓ |
| canonicalizer 返回固定标签时 content kind/code 不变 | plan §5.4 明确"超长合法 basename 的 empty/conversion failure 必须保留原 closed content kind/code"；canonicalizer 只影响 label，不改变 `FinsUploadFailureCode` | ✓ |
| pathful 输入仍拒绝 | §5.4"非空、无 `/`、`\\`，且不是 `.`/`..`；pathful 输入仍直接拒绝" | ✓ |

### 结论

**N2 闭合**。plan 正确识别 `_validate_safe_text` 对 `>240` 的拒绝路径会导致 typed content failure 降级；修复方案（canonicalizer 对合法超长 basename 返回固定标签）保持了 content kind/code 不变、pathful 仍拒绝、与 fragment/`Cc/Cf` 同源处理。producer 先 canonicalize 再构造 reason 的顺序确保 label 安全。

---

## N3 验证 — reason `__post_init__` 调唯一 validator 且 parser 只 delegation

### Plan 修订内容

§5.4 指定 `FinsUploadFailureReason.__post_init__()` 在 `file_label is not None` 时调用唯一 `validate_fins_public_file_label(...)`。`upload_failure_reason_from_json(...)` 只做 five-field exact key/type 读取并调用 constructor，不直接调用 validator，不复制长度/fragment/control/path 规则。S2 断言 6 覆盖 direct constructor 注入 raw label 被拒、parser delegation test。

### 代码事实核对

| Plan 声称 | 代码事实 | 匹配 |
| --- | --- | --- |
| 当前 `__post_init__` 校 message/retry_hint 不校 file_label | `upload_failure.py:79–94`：`_validate_failure_reason_text(self.message, ...)` 和 `if self.retry_hint is not None: _validate_failure_reason_text(...)` | ✓（file_label 字段尚不存在，plan 正确识别需要新增校验） |
| 当前 `_FAILURE_KEYS` 为四键 | `upload_failure.py:17`: `frozenset({"kind", "code", "message", "retry_hint"})` | ✓（plan 要求改为五键含 `file_label`） |
| parser 调 constructor 不自建规则 | `upload_failure.py:218–235`：exact key 读取 + kind/code 一致性校验后 `return FinsUploadFailureReason(...)` | ✓（当前无 file_label，新增后 parser 只读取并传入 constructor） |
| owner 内有 `__post_init__` 校验模式 | `upload_failure.py:79–94` 对 message/retry_hint 的校验模式是既有 pattern | ✓（file_label 校验遵循同一模式） |
| `validate_fins_public_file_label` 由 `direct_events` 提供 | plan §5.4 指定在 `direct_events.py` 增加 `canonicalize_fins_public_file_label` 与 `validate_fins_public_file_label`，二者共享私有判定实现 | ✓ |

### 结论

**N3 闭合**。plan 正确要求 `__post_init__` 在 owner 层对非 null `file_label` 调用唯一 validator；parser 做 exact key/type 读取后调 constructor，不复制规则。当前代码对 message/retry_hint 已有 `__post_init__` 校验模式，file_label 遵循同一模式是一致性修复。direct constructor owner test 覆盖非法 label 注入被拒。

---

## Open questions

无。N1–N3 均为 plan 文本与测试要求的小修订，不涉及口径重新确认或架构变更。

## Residual risks

- N1–N3 fix 后的实现质量仍待 implementation slices 验证；本 gate 只验证 plan 文本与代码事实的一致性。
- 真实 Docling 跨平台底层异常差异、material generic failure、旧 durable record 不可读等既有 residual risk 不变。

## Final plan re-review conclusion: **pass**

判断依据：

1. **N1 闭合**：pipeline `cancelled` 状态在代码中真实存在（5 项 `_UPLOAD_TERMINAL_DISPOSITIONS`），有明确 producer（sec/cn/docling 三处），`from_pipeline_json` 调 `cls(...)` 触发 `__post_init__`。plan §5.2 矩阵与代码事实完全一致，fix artifact C4 勘误正确。
2. **N2 闭合**：`_validate_safe_text` 对 `>240` 的拒绝路径确实存在，canonicalizer 返回固定标签的方案保持 content kind/code 不变、pathful 仍拒绝、与 fragment/`Cc/Cf` 同源处理。依赖方向安全。
3. **N3 闭合**：`__post_init__` 对 message/retry_hint 已有校验模式，file_label 校验遵循同一模式；parser 做 exact key/type 读取后调 constructor，不复制规则。owner 闭合。

三项 finding 均为 plan 文本 + 测试断言的小修订，不涉及架构变更、goal drift 或 scope creep。plan 足够 code-generation-ready，可交给 implementation agent。

## Artifact path

`docs/reviews/plan-review-20260813-211608.md`
