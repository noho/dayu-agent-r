# Code Review

## Scope

- Mode: PR（Gateflow PR review）
- Branch or PR: PR #197 `draft: 000333 巨潮空公告列表解析修复（含分支既有 work unit 提交）`
  https://github.com/noho/dayu-agent-r/pull/197 （author `noho`，head `codex/upload-material-oracle` → base `main`，draft/OPEN）
- Base: `main`（PR base branch）
- Output file: `docs/reviews/pr-197-review-20260925-205425.md`
- Included scope（本 work unit，PR body 已声明边界的 3 个 commit `72e5a370`/`ee46801e`/`569d1a3d`）：
  `dayu/fins/downloaders/cninfo_downloader.py`、`tests/fins/test_cninfo_downloader.py`、
  `docs/gateflow/cninfo-empty-announcement-parse-*.md`、`docs/reviews/*20260925*.md`，
  及 work-unit 运行时验证证据（CLI 复验输出、integrity inventory、meta/SHA 对账）。
- Excluded scope：PR 全量 diff 为 118 files / +19119（分支捎带的其它 work unit：upload-material oracle、
  docling 2.127 升级、下载链路修复等 20 个 commit / 111 files）。PR body 已按事实声明“本 PR 未对其重新评审”，
  各有其自身工作流的 review/adjudication 记录；本 review 不重复评审，也不为其背书。
- Parallel review coverage: 无（work-unit 部分此前已经 slice code review 与 aggregate deepreview 两轮全量走读，
  本轮为 PR 级终审 + body 事实核对）

## Findings

未发现实质性问题。

PR 级终审要点：

1. **PR body ↔ 代码/证据一致性逐项核对**：
   - 双根因描述与直接证据一致（`announcements: null` ↔ `totalRecordNum=0` 两次实测；`.claude/.cc-writes`
     0 文件残留 → `unsafe_filesystem_entry` → 预检拒绝，inventory 前后对照在案）；
   - “改动仅 2 个代码文件”与 PR diff 中本 work unit 部分一致（其余为 docs artifacts）；
   - 四分支解析描述与 `cninfo_downloader.py:475-490` 实现逐字相符；
   - 验证数字（2124 passed / 1 环境伪失败 / pyright 0 errors / 退出码 0 / `skipped=1 integrity_complete` /
     SHA 对账）与实测输出一致；
   - `Fins missing periods: "H1,Q1,Q3"` 解释口径与 plan §9 一致；
   - 捎带提交清单 20 个 hash 与 `git log main..HEAD` 一一对应、分组与主题相符（5+12+3=20）；
   - 非 GitHub issue 故无 closing keyword——与 gateflow 规则一致，body 已声明。
2. **work-unit diff 终审**（fresh pass）：分支优先级无重叠（missing→null→list→空 list 四分支互斥）；
   分页中途缺 key 维持 fail-closed 与改动前行为一致（`payload.get` 旧路径同样 raise）；falsy 非法值不落空页；
   无 public API / `__all__` 变更；测试参数化签名与夹具一致。
3. **CI/check**：`gh pr checks 197` → `no checks reported on the 'codex/upload-material-oracle' branch`
   （仓库未对该分支配置 CI）。本 work unit 的验证以本地测试/pyright/CLI 复验为准，已在 body 披露。

## Open Questions

- 无。

## Residual Risk

- PR 体量大（118 files，捎带 111 files 未经本 review 重新评审）：merge 前 reviewer 应按 body 分节区分责任边界；
  如需对捎带 work unit 重新评审，应另开 review 任务。【分类：assigned to later work unit / 用户 merge 决策】
- 仓库无 CI checks：merge 门槛依赖人工验证记录（tests/pyright/CLI 复验均为本地执行）。
  【分类：requiring new issue or explicit user decision，owner：仓库维护方】
- 其余 residual risks 与 aggregate deepreview 记录一致（storage 预检异常投影缺陷需新 issue；跳过路径验收、
  全新下载链路未端到端演练；其它 ticker 可能同类残留污染；巨潮未来省略 key 的 fail-closed 边界）。
  【分类：见 docs/reviews/code-review-20260925-203518.md 对应行】
