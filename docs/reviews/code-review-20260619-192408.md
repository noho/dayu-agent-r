# Code Review — WU-CM-14 Focused Re-review

## Scope

- Mode: current changes (focused re-review)
- Branch: wu-cm-14-final-answer-preservation
- Base: d4b271cb (accepted plan commit)
- Output file: docs/reviews/code-review-20260619-192408.md
- Included scope: test-only additions to `tests/host/test_run_input_builder.py` and `tests/host/test_dispatch_scheduler.py`
- Excluded scope: production code unchanged from previous review
- Parallel review coverage: 无
- Previous review artifacts:
  - docs/reviews/code-review-20260619-190815.md
  - docs/reviews/code-review-20260619-191152.md

## Finding Verification

### DS F1: `_llm_facing_evidence_source_text` / `_is_internal_evidence_source_part` focused tests

**Status: CLOSED**

Added two parametrized tests in `tests/host/test_run_input_builder.py`:

1. `test_llm_facing_evidence_source_text_filters_internal_provenance` — 5 parametrized cases:
   - `(None, None)` — None 输入
   - `("", None)` — 空字符串
   - `("tool_call_event:event-a, payload:payload-a, digest:sha256-a", None)` — 全内部
   - `("tool_call_event:event-a, filing:msft-10k, payload:payload-a", "filing:msft-10k")` — 混合
   - `("filing:msft-10k, page:42", "filing:msft-10k, page:42")` — 全业务可读

2. `test_internal_evidence_source_part_detection` — 8 parametrized cases:
   - 7 个内部 prefix 各返回 `True`
   - 1 个业务 part `"filing:msft-10k"` 返回 `False`

覆盖充分，边界条件清晰。

### MiMo 01 / DS F4: Reactive frozen material stop-condition test

**Status: CLOSED**

`test_reactive_root_compact_selection_passes_protected_recent_floor` 已扩展为直接验证 `_frozen_reactive_material_blocks` 输出：

- Seeds 三种 material：`USER_INPUT_ACCEPTED`（display_text）、`RUN_SUCCEEDED`（final_answer）、`TOOL_CALL_REQUESTED` + `TOOL_RESULT_ACCEPTED`（accepted evidence）✅
- 直接调用 `_frozen_reactive_material_blocks(context, display_text, material_view)` 获取 frozen blocks ✅
- 断言恰好一个 `CURRENT_INPUT_ANCHOR` block ✅
- 断言 protected blocks 的 `kind` 集合包含 `USER_INPUT`、`ASSISTANT_FINAL_ANSWER`、`ACCEPTED_TOOL_EVIDENCE` ✅
- 断言 protected blocks 不是 `CURRENT_INPUT_ANCHOR` section ✅
- 断言 protected blocks 的 text 包含 user input、final answer、evidence 原文 ✅
- 断言每个 protected block 在 compact selection 中被 `protected_recent_raw_floor` 排除 ✅

满足 plan §5.9 全部要求。

## New Issue Check

- 生产代码 diff 与上次 review 完全一致（md5 校验通过），无新增或修改
- 新增测试代码仅在 `tests/` 下，不影响生产路径
- 测试 helper `_append_run_success`、`_append_accepted_tool_evidence`、`_read_rows` 按已有测试模式编写，类型签名和 docstring 符合 AGENTS.md
- 无新增 import 反向依赖或架构边界违反

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- EventLog double-read remains WU-CM-13 residual (deferred, not a blocker).
- Content digest normalization mismatch remains nonblocking (source-ref/evidence-ref dedupe is the correctness path).
