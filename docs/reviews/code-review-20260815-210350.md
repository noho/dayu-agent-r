# Code Review — Re-Review

## Scope

- Mode: current changes（re-review）
- Branch: `codex/upload-filing-oracle`
- Base: `3911c790`
- Output file: `docs/reviews/code-review-20260815-210350.md`
- Included scope:
  - `dayu/fins/pipelines/docling_upload_service.py`（production, +219/-0）
  - `tests/fins/test_docling_upload_service.py`（+709/-0）
  - `tests/fins/test_fins_ingestion_runtime.py`（+70/-0）
  - `tests/fins/test_sec_pipeline_upload_filing_stream.py`（+20/-0）
  - `tests/fins/test_cn_pipeline.py`（+20/-0）
  - `tests/fins/test_processor_read_consistency.py`（+83/-0）
  - `docs/gateflow/uf-fix07-slice3-implementation-20260815.md`
  - `docs/gateflow/uf-fix07-slice3-review-fix-20260815.md`
- Excluded scope: storage/processor/Host/Engine/runtime 生产文件（未修改）；README（Slice 4 职责）；UF-FIX08/10/11 议题；oracle/scenario/frozen evidence
- Prior review inputs:
  - `docs/reviews/code-review-20260815-204338.md`（PASS，无 finding）
  - `docs/reviews/code-review-20260815-205240.md`（2 LOW findings）
- Review-fix artifact: `docs/gateflow/uf-fix07-slice3-review-fix-20260815.md`（claims all 3 items fixed）
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Prior Findings Closure Verification

### Finding LOW 1: docstring Raises 声明不符 — CLOSED

- **Claim**: `_build_stored_file_entry` 和 `_build_upload_source_fingerprint` 的 `Raises: 无` 与实际 `ValueError` 路径矛盾。
- **Fix verification**: `git diff 3911c790` 确认两个 helper 的 Raises 段已更新：
  - `_build_upload_source_fingerprint` (line 1237-1239): `ValueError: filing asset 缺少 original_filename，或 source kind 不受支持时抛出。`
  - `_build_stored_file_entry` (line 1611-1613): `ValueError: filing asset 缺少 original_filename 时抛出。`
- **Status**: CLOSED — docstring 与真实异常路径一致。

### Finding LOW 2: fresh target blob store failure 缺少直接零发布断言 — CLOSED

- **Claim**: blob store 失败只有 existing-update 变体，缺少 fresh target 变体的 `begin=1/commit=0/rollback=1/零发布` 断言。
- **Fix verification**: `test_filing_fresh_blob_store_failure_rolls_back_once_with_zero_publication` 已添加（line 2092），parametrize `fail_at=(1,2,3)`：
  - 使用 `_FailingNthUploadBlobRepository` 在第 N 次 `store_file` 注入 `RuntimeError`。
  - 断言 `begin_calls == 1`、`commit_calls == 0`、`rollback_calls == 1`、`published_tree_sha256 == {}`、`get_source_meta` 抛 `FileNotFoundError`。
  - 3 个变体全部通过（1st companion original / 2nd primary original / 3rd primary derived）。
- **Plan alignment**: accepted plan line 567-568 要求「第 N 次 blob store/final source/commit failure：rollback exactly once；fresh target 零发布，existing update 的 tree SHA 不变」——fresh 变体现在由本测试覆盖，existing 变体由 `test_existing_replacement_blob_failure_keeps_entire_published_tree` 覆盖。
- **Status**: CLOSED — blob/final/commit 三类失败均含 fresh 零发布与 existing 不变。

### Residual: tmp_path realpath 隐式依赖 — CLOSED

- **Claim**: 新增测试把未 resolve 的 `tmp_path` 子路径直接传给 `_build_filing_original_asset_identity`，依赖 pytest 9 的 `tmp_path` 已 resolve 行为。
- **Fix verification**: `grep -c "resolve(strict=False)" tests/fins/test_docling_upload_service.py` 命中 20+ 处。所有新增 direct-service test paths 在构造 typed filing selection 或 expected identity 前显式 `(tmp_path / name).resolve(strict=False)`。
- **Production contract unchanged**: `_build_filing_original_asset_identity` 仍要求 `is_absolute() and resolve(strict=False) == self`，fail-closed 未放宽。
- **Status**: CLOSED — 测试显式 resolve，生产 contract 不变。

## Targeted Adversarial Verification

### A. Fresh-target N-th blob store failure — 真正零发布

**测试路径**: `test_filing_fresh_blob_store_failure_rolls_back_once_with_zero_publication[1/2/3]`

**执行链走读**:
1. `_execute_upload` → `_selection_for_test(filing_primary=primary)` → `FinsUploadFilingFiles.for_upsert(primary, companions)` → `prepare_upload` → `_build_original_assets` (读 companion + primary) → `_build_pending_assets` (转换 primary) → `_PreparedAssetMutation`。
2. `_publish_prepared_upload` → `commit_prepared_upload_batch` → `publish_prepared_upload` → `_store_upload_assets`。
3. `_store_upload_assets` 的 blob 循环（line 547-584）：对 `pending_assets` 逐个 `store_file`。`_FailingNthUploadBlobRepository` 在第 N 次调用时抛 `RuntimeError`。
4. 异常传播到 `commit_prepared_upload_batch` 的 `try` 块（line 997-1020）：`batch_terminal_started=False` → `finally` → `rollback_prepared_upload_batch`。
5. 断言：`begin_calls=1`（batch 已开）、`commit_calls=0`（未到 commit）、`rollback_calls=1`（finally 回滚）、`published_tree_sha256={}`（staging 清空）、`get_source_meta` 抛 `FileNotFoundError`（source 不存在）。

**结论**: fresh target blob store failure 在第 1/2/3 次写入失败时均实现真正零发布。batch 回滚清空 staging，source meta 不存在。

### B. resolve(strict=False) — 未放宽生产 fail-closed contract

**生产代码** (`_build_filing_original_asset_identity`, line 1104-1107):
```python
if not normalized_path.is_absolute() or normalized_path.resolve(strict=False) != normalized_path:
    raise ValueError("filing asset identity 输入必须是 absolute normalized path")
```

**测试代码** (example, line 1266-1267):
```python
first_dir = (tmp_path / "first").resolve(strict=False)
second_dir = (tmp_path / "second").resolve(strict=False)
```

**分析**:
- `resolve(strict=False)` 在测试中用于确保路径已 resolve，与生产 validator (`_normalize_fins_upload_path`) 的行为一致。
- 生产代码的校验 `resolve(strict=False) != normalized_path` 未被修改或绕过。
- 测试中 `resolve(strict=False)` 是对输入的预处理（使其符合生产 contract），不是对校验的放宽。
- 若测试不 resolve，`(tmp_path / "first")` 在某些 pytest 配置下可能不是 resolved path，会触发生产校验的 `ValueError`，导致假失败——这是测试健壮性改进，不是 contract 放宽。

**结论**: `resolve(strict=False)` 在测试中消除 tmp_path realpath 隐式依赖，生产 fail-closed contract 不受影响。

### C. Semantic ownership — 无 drift

- **Identity 公式**: `_build_filing_original_asset_identity` 是唯一 owner，下游（stored entry、event、fingerprint、derived identity）均消费其输出。
- **Original/derived 关联**: `_PendingFileAsset.original_filename` / `derived_from` 由 `_build_original_assets` 和 `_build_pending_assets` 两处显式赋值，不从 raw fields 反推。
- **Filing fingerprint**: `_build_upload_source_fingerprint` 使用 `original_filename/sha256/size/source`，排除 path-derived identity。
- **Stored entry**: `_build_stored_file_entry` 按 `source_kind` 分支追加 filing-only 字段。
- **Event projection**: `_store_upload_assets` 按 `source_kind` 选择 event name（basename vs identity）。
- **Downstream consumption**: snapshot/preprocess/read 均只消费 `get_primary_source()` 的 exact derived primary（测试直接证明，生产代码未修改）。

**结论**: 每个业务语义有唯一清晰 owner，无 fallback/兼容 shim/下游修正。

### D. Atomicity — 完整覆盖

| 失败场景 | 测试 | fresh 零发布 | existing tree 不变 |
| --- | --- | --- | --- |
| Primary conversion failure | `test_corrupt_primary_with_valid_companions_fails_without_publication` | ✓ (batch 未开) | N/A |
| Identity collision | `test_filing_generated_identity_collision_fails_before_converter_and_batch` | ✓ (batch 未开) | N/A |
| Blob store failure (fresh) | `test_filing_fresh_blob_store_failure_rolls_back_once_with_zero_publication` | ✓ | N/A |
| Blob store failure (existing) | `test_existing_replacement_blob_failure_keeps_entire_published_tree` | N/A | ✓ |
| Final source failure (fresh) | `test_filing_final_source_failure_rolls_back_once_with_zero_publication` | ✓ | N/A |
| Commit failure (fresh) | `test_filing_commit_failure_leaves_fresh_target_unpublished` | ✓ | N/A |
| Cancel at checkpoint | `test_existing_replacement_cancellation_keeps_entire_published_tree` | N/A | ✓ |

### E. Type check — PASS

pyright 0 errors, 0 warnings, 0 informations（6 production + test files）。

### F. Test validation — PASS

Full affected suite: 630 passed, 1 skipped, 3 warnings。与 review-fix artifact 声明一致。

## Open Questions

无。

## Residual Risk

| 风险 | 分类 | owner |
| --- | --- | --- |
| 旧 basename-based source schema 兼容/首次重传/自动修复 | later work unit | UF-FIX08 |
| 同 request/document 并发 writer | later work unit | UF-FIX10 |
| fresh company meta warning | later work unit | UF-FIX11 |
| material basename/stem collision 与 duplicate-path 行为 | regression 保持现状 | material 不在本 slice scope |
| optional real Docling integration 未启用 | later evidence work | deterministic fake-converter tests 已通过 |
| SHA-256 理论 collision | fixed in current slice | 完整 digest + request-local uniqueness fail-closed |
| frozen registry/oracle/evidence 与 UF-PF07/UF-PF12 | later evidence work | 等待另行授权 |

## Review Conclusion

**PASS。** 两份 prior review 的所有 findings 已闭环：

1. LOW 1 (docstring Raises): 已修复，两个 helper 的 Raises 段与真实异常路径一致。
2. LOW 2 (fresh blob failure test): 已修复，`test_filing_fresh_blob_store_failure_rolls_back_once_with_zero_publication` 覆盖第 1/2/3 次 blob store failure，断言 begin=1/commit=0/rollback=1/零发布/source 不存在。
3. Residual (tmp_path resolve): 已修复，测试显式 `resolve(strict=False)`，生产 fail-closed contract 不变。

Slice 3 的核心契约全部经真实链路验证成立，无 blocker 或中高级别 finding。
