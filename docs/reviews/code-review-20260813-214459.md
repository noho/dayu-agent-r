# Code Review

## Scope

- Mode: current changes（仅未提交 UF-FIX03-S1 workspace changes）
- Branch: `codex/upload-filing-oracle`（HEAD = base，无未 merge 提交）
- Base: `662c9ad4b234894e54c62b56368c7682a09f596e`（gateflow: accept plan for UF-FIX03）
- Intent/contract 输入:
  - `docs/gateflow/uf-fix03-summary-bounded-errors-plan-20260813.md`（S1 节）
  - `docs/gateflow/uf-fix03-s1-implementation-20260813.md`
  - 仓库指令: `AGENTS.md`（与 `CLAUDE.md` 内容一致，已全文核对）
- Included scope: 全部 11 个未提交变更文件（5 生产 + 6 测试）+ 1 个未跟踪 implementation artifact，均逐文件完整阅读；
  沿真实生产链走读 `prepare_upload → publish/commit_prepared_upload_batch → SEC/CN filing/material stream → pipeline facade →
  FinsUploadPipelineResult → ProductionFinsUploadRunner → FinsUploadResultSummary → durable job record / direct RESULT` 全程。
- Excluded scope: 未修改的 `sec_pipeline.py`/`cn_pipeline.py` facade 其余部分只读走读；S2/S3 范围文件（`upload_failure.py`、
  `direct_events.py`、CLI、README）按 plan 冻结边界确认 no-touch。
- Parallel review coverage: 无（单人完整走读；无 subagent 分片）。
- Review 时间: 2026-08-13 21:44（本机时钟生成）

## 结论（Verdict）

**FAIL — 需修复后重新 review，或由 controller 逐项裁决后再进 accepted slice commit。**

功能真链本身验证正确（original-only 计数、commit 后消费、状态矩阵、requested owner、durable/direct 同源、progress/fingerprint
不变、material 仅机械迁移、全部 constructor 点均成立），但 plan §8.3 覆盖 gate 未闭合：`cn_pipeline.py` 整文件 68% 低于 80%，
且实测有 4 个文件的 S1 新增/修改行落在 uncovered 区域，其中两个是 plan §5.3 明确要求的四个 summary constructor 中的两个，
只有 AST keyword audit 而无行为测试；implementation artifact 的覆盖声明与实测不符。详见 F1/F2。

## 验证复跑记录（可执行证据）

| 检查项 | 命令/来源 | 结果 |
| --- | --- | --- |
| focused tests | `pytest -q tests/fins/test_docling_upload_service.py tests/fins/test_sec_pipeline_upload_filing_stream.py tests/fins/test_sec_pipeline_upload_material_stream.py tests/fins/test_cn_pipeline.py tests/fins/test_fins_ingestion_runtime.py tests/fins/test_fins_service_runtime.py` | `283 passed, 3 warnings in 5.14s`（3 warning 为 `edgar` 依赖弃用，非本 slice） |
| pyright | `python -m pyright dayu/ tests/ utils/` | `0 errors, 0 warnings, 0 informations` |
| old-field audit | `rg -n '\buploaded_files\b' dayu tests --glob '*.py'` | 零命中（exit 1） |
| progress key | `rg -n '_PAYLOAD_FILE_COUNT\|"file_count"' dayu/fins/ingestion_runtime.py` | 249/7229/7617 仍命中，`file_count` 保留 |
| whitespace | `git diff --check` | 通过 |
| 冻结 JSON | `shasum -a 256 docs/cli_ci_scenarios.json docs/cli_ci_oracles.json` | `a357e5a1…efb` / `88b04ca4…70b8`，与 plan §1 完全一致 |
| coverage | plan §8.3 同款 `coverage run`（6 个 S1 focused 测试文件）+ `coverage report -m` | 复现 impl doc 数值；missing 行明细见 F1 |

**已核实的真链事实（通过项，非 findings）：**

1. **original-only stored count**: `docling_upload_service.py:505-531` 逐资产 `store_file` 成功后仅当
   `asset.source == _ASSET_SOURCE_ORIGINAL` 时 `stored_original_count += 1`（518-519）；derived Docling 资产不计数；
   不按 `len(stored_entries)`。测试 `test_execute_upload_counts_only_successful_original_stores` 断言 4 资产落盘、count=2。
2. **commit 后消费**: workflow 只在 `commit_prepared_upload_batch` 成功返回后消费 `upload_result.stored_file_count`
   （sec_upload_workflow.py:247-253/278、cn_pipeline.py:867-873/898）；`commit_prepared_upload_batch`
   （docling_upload_service.py:849-902）在 `commit_batch` 返回后才 `return result`，commit 抛错时 `batch_terminal_started=True`
   不触发 caller rollback、result 不交付，workflow 进 failed builder 显式 `stored_file_count=0`。
   SEC 新增测试 `test_upload_filing_commit_failure_never_publishes_staged_count` 注入 OSError/RuntimeError 证明
   `failed/stored=0`、分类保持 `storage/storage_io` 与 `runtime/unexpected_runtime`、published tree SHA 不变、无 caller rollback。
3. **完整状态矩阵**: `FinsUploadPipelineResult.__post_init__`（ingestion_runtime.py:1193-1219）`ok→>=1`、
   `skipped/deleted/failed/cancelled→==0`，failed 必须有 failure、非 failed 禁止 failure；
   `FinsUploadResultSummary.__post_init__`（1283-1320）`ok→requested>=1 且 stored==requested`、`skipped→requested>=1 且 stored==0`、
   `deleted/cancelled/failed→stored==0`（requested>=0），bool/负数拒绝。矩阵逐项由 constructor+parser 双侧测试覆盖，
   其中 `cancelled+0` 接受、`cancelled+positive` 拒绝（test_upload_pipeline_count_owner_accepts/rejects_* 系列）。
4. **requested owner**: `_upload_summary_from_result`（service_runtime.py:287-318）只取 `len(request.files)`；
   `validate_fins_upload_filing_request` 原样携带 raw request（ingestion_runtime.py:1010-1011），与 workflow 实际
   `prepare_upload(files=list(raw_request.files))` 同源，count 与实际上传集一致。
5. **durable/direct 同源**: 同一 `FinsUploadResultSummary` 实例同时投影 `to_json_summary()`（job record，4423-4464）
   与 `_upload_result_details()`（direct RESULT，6329-6364），details 读 typed 字段 `summary.requested_file_count/stored_file_count`；
   direct RESULT details 测试断言 `requested files`/`stored files` 值且无 `uploaded files`。
6. **progress/fingerprint 不变**: `_PAYLOAD_FILE_COUNT` 与 `_upload_context_request_progress_payload`（7593-7618）、
   `_upload_context_summary_progress_payload`（7621-7652）未新增 count key；progress 测试断言 payload 无两个新 key。
   `_build_upload_source_fingerprint` 未改；fixture digest 测试断言
   `099dc9636e306c75f1d5d64dd0210123956ba73888e968088c7279baab1d7fdd`。
7. **material 仅机械迁移**: SEC/CN material 的 diff 仅 success/cancel/failure 构造补 `stored_file_count`；
   generic `except Exception` 的 `message=str(exc)`、company-first publication 顺序、raw error 均未改，既有回归
   `test_upload_material_failure_preserves_existing_user_visible_semantics`（SEC+CN 双侧）通过。
8. **constructor 全量审计**: 生产 `UploadOperationResult(...)` 恰 6 点（docling_upload_service 317/398/557/1419、
   sec_upload_workflow 288、cn_pipeline 908），`FinsUploadResultSummary(...)` 恰 4 点（service_runtime 131/305、
   ingestion_runtime 3945/4423），`FinsUploadPipelineResult` 生产唯一 `cls(...)`（parser 内）均显式传 required count，
   无 default；AST audit 测试存在且通过。无 `hasattr/getattr`、无兼容 alias、无魔法 count 键。
9. **AGENTS.md 合规**: 新增函数均有完整中文 docstring；分层未破（未触 Host/Engine/runtime/config/storage/Service/CLI）；
   无兼容代码；README 更新按 plan §6.3 归 S3，与本 slice 写入范围冻结一致。

## Findings

### 1-未修复-中-plan §8.3 覆盖 gate 未闭合，且 implementation artifact 覆盖声明与实测不符

- **入口/函数**: 无单一运行入口；触发于 plan §8.3 coverage 判定与 implementation artifact §Coverage/§Findings 声明。
- **文件(行号)**: 实测 missing 区域与 S1 修改行的对照（`coverage report -m` 复跑输出）:
  - `dayu/fins/pipelines/cn_pipeline.py`: missing `908-940, 947-949, …`；S1 修改行 **912、937** 落在 `908-940` 内。
  - `dayu/fins/pipelines/sec_upload_workflow.py`: missing `288-320, …`；S1 修改行 **292、317** 落在 `288-320` 内。
  - `dayu/fins/ingestion_runtime.py`: missing `3941-3956, …`；S1 修改行 **3948-3949**（direct runner-unavailable
    summary constructor）落在其中；另新增拒绝分支 **1312**（`skipped` 且 `requested_file_count<1` 的 raise）未覆盖。
  - `dayu/fins/service_runtime.py`: missing `131, …`；S1 修改行 **134-135**（`run_upload` early-cancelled summary
    constructor）落在其中。
  - `docling_upload_service.py` 全部 S1 修改行均已覆盖。
- **输入场景**: 运行 plan §8.3 同款 `coverage run -m pytest`（6 个 S1 focused 测试文件）+ `coverage report -m`；已复跑，
  数值与 impl doc 完全一致（cn_pipeline 417/133/68%、sec 149/10/93%、ingestion 2167/203/91%、service 124/15/88%）。
- **实际分支**: 上述四个文件各有一段 S1 修改行所在分支从未被任何测试执行。
- **预期行为**: plan §8.3 判定"每个修改生产文件目标 `>=80%`；若大型既有文件低于目标，必须报告具体 uncovered branches
  **并补 owner 行为测试**，不得用 pragma: no cover、降低阈值或移除正确断言掩盖"；plan §5.3 要求四个 summary constructor
  的 count 语义由测试锁定；implementation artifact 声明"S1 修改路径已有 owner/integration/static coverage，
  不存在未分类 S1 correctness finding"。
- **实际行为**: `cn_pipeline.py` 整文件 68% < 80%；plan §5.3 四个 summary constructor 中的两个
  （service_runtime:134-135 early-cancelled、ingestion_runtime:3948-3949 direct runner-unavailable）只有 AST keyword
  audit（`test_production_upload_count_constructors_are_explicit_and_complete` 仅断言关键字存在，不断言
  `requested=len(files)`/`stored=0` 数值语义），无任何行为测试；新增的 skipped+requested=0 拒绝分支（1312）无 reject 用例。
  implementation artifact 对 68% 的处置是"assigned to later work unit"整体 defer，未按 plan 判定句补 owner 行为测试，
  且其"S1 修改路径已有 coverage"的声明被上述 missing 行直接证伪。
- **直接证据**: `coverage report -m` missing 明细（上文复跑记录）；`git diff 662c9ad4` 的修改行号与 missing 区间逐行对照；
  `rg 'upload_runner=None' tests/` 仅命中 job 路径测试（4811 附近），无 direct 路径（3940-3956）测试；
  无任何测试调用 `ProductionFinsUploadRunner.run_upload` 且传入立即取消的 checker。
- **影响**: 两个 runner 边界构造点的 requested/stored 数值语义失去回归护栏（这是最容易被未来改动漂移、且恰好不在
  happy path 上的点）；plan gate 未满足即宣称 S1 PASS，controller 若按 artifact 现状放行，将接受一个低于自身验收标准的 slice。
- **建议改法和验证点**:
  1. 新增行为测试 A：direct upload 入口（无 upload_runner）→ RESULT 为 failure、details 中 `requested files` 等于
     request 文件数、`stored files` 等于 `0`，错误文案为 runtime-unavailable。
  2. 新增行为测试 B：`ProductionFinsUploadRunner.run_upload` 传入入口即取消的 `FinsJobCancellationChecker` →
     cancelled summary `requested_file_count=len(raw_request.files)`、`stored_file_count=0`。
  3. reject 矩阵补 `("skipped", 0, 0)` 用例覆盖 1312。
  4. 修正 implementation artifact 覆盖声明（区分"owner 测试存在"与"修改行已覆盖"）。
  验证点: 复跑 coverage 后上述四组行号退出 missing 集。
- **修复风险（低）**: 均为纯测试新增，不动生产语义。
- **严重程度（中）**: 无正确性损害，但违反 plan 自身验收 gate 且文档声明失真；在 gateflow 下应阻止直接进入
  accepted slice commit。

### 2-未修复-中-workflow 级 DoclingConversionCancelledError catch 是当前拓扑下的死代码，S1 向其新增 4 行且不可测试

- **入口/函数**: `run_upload_filing_stream` 的 `except DoclingConversionCancelledError` 分支
  （sec_upload_workflow.py:287-325、cn_pipeline.py:907-945）。
- **文件(行号)**: `dayu/fins/pipelines/sec_upload_workflow.py:287-325`（S1 新增 292、317）、
  `dayu/fins/pipelines/cn_pipeline.py:907-945`（S1 新增 912、937）。
- **输入场景**: 任何触发 Docling 转换取消的上传请求。
- **实际分支**: converter 内 `DoclingConversionCancelledError` 的唯一 raise 点是
  `docling_process_converter.py:401/509`；生产链路中 converter 只在 `prepare_upload → _build_pending_assets` 被调用，
  而 `docling_upload_service.py:336-340` 已 catch 该异常并返回 `_build_cancelled_result`。try 块后续
  `stage_upload_company_meta_decision` / `publish_prepared_upload` / `_store_upload_assets` / `commit_batch` 均无该异常
  的 raise 点，因此 workflow 级 catch 在现有生产拓扑下不可达。
- **预期行为**: plan §6.1 将这两个 inline constructor 列为 6 点清单中的两点并要求显式 stored 0（实现已满足）；同时
  plan §8.3 要求修改行有覆盖或 uncovered branch 得到报告+owner 测试。
- **实际行为**: S1 新增的 4 行无法被任何测试触达；新增测试
  `test_upload_filing_conversion_cancelled_has_zero_stored_count` 实际走 `prepare_upload` 服务级映射（converter 抛错 →
  prepare_upload 吸收 → cancelled result），coverage 实测该 catch 区仍全部 missing（SEC `288-320`、CN `908-940`），
  与注入场景互相印证。真实取消终态由 `_build_cancelled_result`（清单第 4 点）与 `commit_prepared_upload_batch`
  precommit-cancel 路径完整承担，该两条路径均有行为测试覆盖。
- **直接证据**: coverage missing 区间；converter raise 点 grep；`prepare_upload` catch 于
  `docling_upload_service.py:336-340`；测试注入 `_FailingCnUploadConverter` 走 service 级映射。
- **影响**: 死代码使 constructor 清单 6 点中有 2 点不可达（清单膨胀）；新增行零覆盖直接构成 F1 的 gate 缺口；
  若未来 `prepare_upload` 行为变化（如不再吸收该异常）使 catch 复活，其 stored 0 语义没有任何测试锁定。
- **建议改法和验证点**:
  - 推荐：删除两个 workflow 级 catch 及其 inline `UploadOperationResult` 构造（真实取消路径已被
    `_build_cancelled_result` + `commit_prepared_upload_batch` precommit-cancel 完整覆盖），AST audit 由 6 点收紧为
    4 点，F1 的 cn/sec missing 区间随删除消失。
  - 备选：controller 在 plan amendment 中显式接受这两个 dead-catch 的零覆盖（须注明不可达性证据），保留防御分支。
  验证点: 删除后 focused tests 全绿、pyright 0 error、audit 断言同步为 4 点。
- **修复风险（低-中）**: 删除后若未来新增 raise 路径，异常将落入 generic catch 并被映射为 runtime 分类；该风险由 S2
  的 filing typed catch 设计一并收敛。当前拓扑下删除无行为变化。
- **严重程度（中）**: 无运行期正确性损害（不可达），但 plan 验收 gate 因此无法闭合，且"6 点清单"向 controller 呈现的
  是含 2 点死代码的 inventory。

## Open Questions

- 无。阻碍 confident judgment 的问题已在 F1/F2 中给出可执行证据与建议。

## Residual Risk

- `cn_pipeline.py` 其余 68% 缺口（1349-1779 等 download/facade/helper 区间）为既有非 S1 代码，本 slice 未引入；
  但其中 CN filing `except DoclingConversionError` 分支（946-954，missing `947-949`）无 CN 侧测试——CN 测试用
  `_FailingCnUploadConverter` 直接抛 OSError/RuntimeError，绕过 DoclingConversionError catch（SEC 侧等价 catch 已覆盖）。
  CN failure builder 的 `stored_file_count=0`（1868 行）本身已通过 OSError/generic 路径覆盖，风险低，归既有 CN 测试面。
- CN filing `commit_batch` 抛错场景无 CN 专属测试（SEC 有）；共享 `commit_prepared_upload_batch` 与 CN failure builder
  均已分别覆盖，组合场景风险低。
- `deleted-with-files` 的 requested 保留语义由 service_runtime 汇合点测试覆盖（`("deleted", 0, 2)`），但无端到端
  workflow 级测试（请求带文件 + delete action → terminal requested>0/stored=0）；语义无漂移迹象，风险低。
- S2/S3 范围（empty/corrupt label、CLI bounded stderr、direct no-artifact、README）按 plan 冻结未实现，不在本 slice
  验收内，未伪造执行。
- 未运行 UF-PF03 与 broader regression（`tests/fins` 全量 + CLI + service）；本 review 只复跑了 S1 focused 命令。
