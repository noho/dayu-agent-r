# Plan Re-Review: WU-CM-12-FIX-R1 Accepted Findings Closure Verification

**Reviewer**: AgentDS (focused re-review)
**Reviewed artifact**: `docs/host/host-issues/wu-cm-12-fix-r1-material-guard-plan.md`
**Original reviews**: `docs/reviews/plan-review-20260618-182749.md` (AgentDS), `docs/reviews/plan-review-20260618-182916.md` (AgentMiMo)
**Date**: 2026-06-18 18:37:10 CST
**Review type**: focused accepted-findings closure verification — only verify the 4 accepted findings are closed in the current plan; do NOT re-audit the entire plan.

---

## Scope

Verify that the following 4 findings accepted from the original reviews are now closed in the current plan:

1. Slice 2 mapping for `build_pre_dispatch_compact_material_view` is explicit
2. Chunk helper ambiguity is closed (delete, not retain)
3. Former chunking test migration assertions are concrete
4. Performance residual is deferred to future WU (not WU-CM-13), no reintroduction of private limits

---

## Finding 1: Slice 2 mapping for `build_pre_dispatch_compact_material_view`

**Source findings**: FS-1 (AgentDS original), F1 (AgentMiMo)

**Required closure criteria**:
- `current_display_text=current_facts.user_prompt`
- Accepted evidence block filtering by `CompactMaterialBlockKind.ACCEPTED_TOOL_EVIDENCE`
- Represented refs second filter via `_represented_evidence_refs(memory, compact)`
- EventLog-backed material view declared authoritative
- No requirement to keep old direct-SQL path for comparison
- Tests validate key fields / provenance / represented refs exclusion

**Plan evidence**:

- §5.3: "provider 的精确映射为：在读 transaction 中调用 `build_pre_dispatch_compact_material_view(transaction, event_log_store, run=current_facts.run, current_display_text=current_facts.user_prompt)`；`current_display_text` 必须来自 `current_facts.user_prompt`。" → `current_display_text` 映射明确。
- §5.3: "从返回 view 的 `material_blocks` 中只选择 `kind == CompactMaterialBlockKind.ACCEPTED_TOOL_EVIDENCE` 的 whole blocks。" → block kind 过滤明确。
- §5.3: "再计算 `represented_refs = _represented_evidence_refs(memory, compact)`，把 represented refs 作为第二道 whole-block filter 应用到上述 accepted evidence blocks" → 二次过滤明确。
- §5.3: "新的 EventLog-backed material view 是 accepted evidence material 的权威路径；删除旧 direct-SQL builder 后，不要求为了新旧字段对比保留旧路径。" → 权威路径声明，不保留旧路径。
- §6 Slice 2 测试要求: "测试 key fields 存在且语义来源正确：`text` / `raw_result_text` 来自完整 raw outcome，… `accepted_evidence_id` 来自 accepted evidence canonical id，`canonical_source_refs` / provenance refs 保留 payload / artifact / event refs，represented refs 命中的 evidence 被 whole-block 排除。" → 测试覆盖 key fields、provenance、represented refs 排除。

**Verdict**: ✅ **CLOSED**. Plan now has explicit mapping for all required elements. Implementation agent can directly execute without inferring parameter bindings, block kind filtering, or secondary represented refs filtering. The authoritative path is declared and the old path is explicitly deleted.

---

## Finding 2: Chunk helper ambiguity

**Source findings**: FS-2 (AgentDS original), F2 (AgentMiMo)

**Required closure criteria**:
- `_evidence_chunks` deleted when no production caller remains
- `_EvidenceChunk` deleted
- `evidence_chunk_label` deleted
- `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` deleted
- No dead explicit budget helper retained or renamed

**Plan evidence**:

- §5.2: "本 WU 不保留无生产调用方的显式 budget chunk helper。默认 chunking 移除后，如果 `_evidence_chunks`、`_EvidenceChunk`、`evidence_chunk_label` 与 `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` 没有生产调用方，必须一并删除；不得为未来可能的 budget chunking 留 dead code。" → 明确删除，不留 dead code。
- §6 Slice 1: "默认 chunking 移除后，如果 `_evidence_chunks`、`_EvidenceChunk`、`evidence_chunk_label` 与 `EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` 没有生产调用方，必须删除，不得重命名后保留为未接入的显式 budget chunk helper。" → 明确禁止重命名保留。
- §8: "本 WU 不保留无生产调用方的显式 budget chunk helper。" → 风险章节重申。
- §6 Slice 3 rg 预期: "`EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` 不得通过重命名方式作为显式 budget chunk helper 继续保留" → 验证命令层面的二次确认。

**Verdict**: ✅ **CLOSED**. Plan is unambiguous: delete all four items when no production caller remains; do not rename and retain as dead code. The tension between "allow retain as explicit budget helper" (old plan) and "must delete" (Slice 3 rg) is resolved in favor of deletion. Implementation agent has a single clear path.

---

## Finding 3: Former chunking test migration assertions

**Source finding**: FS-3 (AgentDS original)

**Required closure criteria**:
- Single `E1` label (no `E1.1`/`E1.2`)
- Full text/response preserved
- Full digest (not chunk digest)
- Provenance/payload refs preserved
- No `chunk_parent_label`/`chunk_ordinal` semantics

**Plan evidence**:

- §6 Slice 1 测试要求: "最小断言必须包含：单个 `E1` label、`raw_result_text` / vNext `response_text` 保持全文、`content_digest` 基于全文、payload / provenance refs 保留、没有 `E1.1` / `E1.2` label、没有 `chunk_parent_label` / `chunk_ordinal` 语义残留。"

**Verdict**: ✅ **CLOSED**. Plan specifies concrete, exhaustive assertion requirements. Every required criterion is explicitly listed: single E1, full text, full digest, provenance refs, no chunk labels, no chunk ordinal semantics. Implementation agent does not need to infer what "migrate to no-chunk" means.

---

## Finding 4: Performance residual deferral

**Source finding**: F3 (AgentMiMo)

**Required closure criteria**:
- Deferred to future Host material source performance hardening WU
- Explicitly NOT WU-CM-13
- Does not reintroduce private row limit or page correctness cap

**Plan evidence**:

- §8: "若真实长 session evidence scan 成本被观测到，deferred-with-owner 到未来 Host material source performance hardening WU；WU-CM-13 是 reactive compact recovery owner，不是该性能 hardening owner。" → 明确 deferred owner 不是 WU-CM-13。
- §5.3: "不新增'最多读 N 条 evidence'的私有常量。如果需要 transaction/page 粒度优化，必须是 page size，不得改变 correctness 或材料合法性；本修复不要求新增 page helper。" → 不重新引入私有 row limit。
- §4 Non-goals: "不实现 WU-CM-13 reactive compact recovery tier 1-3。" → WU-CM-13 明确排除。

**Verdict**: ✅ **CLOSED**. Performance risk is explicitly deferred to a future "Host material source performance hardening WU" that is distinct from WU-CM-13. The plan explicitly forbids reintroducing private row limit or page correctness cap as a workaround. The deferred owner is named and the separation from WU-CM-13 is unambiguous.

---

## Summary

| Finding | Source | Status |
|---|---|---|
| Slice 2 `build_pre_dispatch_compact_material_view` mapping | FS-1 (AgentDS), F1 (AgentMiMo) | ✅ CLOSED |
| Chunk helper ambiguity | FS-2 (AgentDS), F2 (AgentMiMo) | ✅ CLOSED |
| Test migration assertions | FS-3 (AgentDS) | ✅ CLOSED |
| Performance residual deferral | F3 (AgentMiMo) | ✅ CLOSED |

---

## Conclusion

**Pass.**

All four accepted findings from the original reviews are definitively closed in the current plan. The plan now provides:

- Exact parameter bindings and block filtering steps for Slice 2 integration
- Unambiguous delete-all directive for chunk helpers (no dead code, no renaming)
- Concrete, exhaustive assertion requirements for test migration
- Explicit performance residual deferral to a named future WU distinct from WU-CM-13, with no reintroduction of private limits

No new material issues were found in the plan sections touched by these closures. The plan remains code-generation-ready.
