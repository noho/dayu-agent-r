# WU-WAIT-04 Plan Re-review — AgentDS

## Re-review Target and Scope

- **Re-review type**: plan fix re-review（gate assignment for AgentDS）
- **Target plan**: `docs/host/wu-wait-04-production-awaiting-e2e-smoke-plan.md`（已修复版本）
- **Fix report**: `docs/reviews/wu-wait-04-plan-fix-codex.md`
- **Prior review artifacts**:
  - `docs/reviews/plan-review-20260705-201401.md`（AgentDS）
  - `docs/reviews/plan-review-20260705-201420.md`（AgentMiMo）
- **Controller adjudication**: `docs/reviews/wu-wait-04-plan-review-controller-adjudication.md`
- **Re-review scope**: 仅验证 controller adjudication 中 accepted findings 是否已修复，以及修复后的 plan 是否 code-generation-ready。不实施修复、不修改 plan、不修改源码/测试/docs（除本 artifact）。

## Accepted Findings Re-review

Controller adjudication 共接受 7 条 finding，逐条验证如下。

### Finding 1: Service/Fins poll adapter registry data flow — 已修复

- **来源**: DS F1
- **Controller 要求**: 显式指定 `_tooling_options_from_discovery` 中 `wait_poll_adapter_registry` 的构造逻辑：仅当 Fins awaiting runtime 和 awaiting tool bindings 同时存在时构造；否则 `None`；复用与 wait binding/activation registry 相同的 runtime；增加 enabled/disabled 测试预期。
- **修复后 plan 证据**:
  - S1 line 147: builder `build_fins_wait_poll_adapter_registry(runtime, tool_names) -> WaitPollAdapterRegistry`
  - S1 lines 148-149: `_fins_wait_poll_adapter_registry_from_provider_configs(...)` 调用 `_fins_awaiting_registry_inputs_from_provider_configs`；输入 `None` 时返回 `None`；输入非 `None` 时要求 `fins_awaiting_runtime is not None` 且为 ingestion runtime，否则 fail fast；共享同一 runtime。
  - S1 line 149: `_tooling_options_from_discovery` 将结果赋给局部变量并在 `HostToolingOptions(...)` 中显式传入。Fins awaiting runtime 与 awaiting tool bindings 同时存在时非 `None`；否则 `None`。
  - S1 line 151: 测试覆盖 enabled/disabled 两种情况。
- **代码证据**: `_fins_awaiting_registry_inputs_from_provider_configs`（line 1825）与 `_fins_wait_activation_registry_from_provider_configs`（line 1789）均存在于 `dayu/service/host_assembly.py`，plan 引用的模式真实可循。
- **判定**: 已修复。数据流完整，None guard 和 runtime 复用均显式指定。

### Finding 2: Deterministic poll adapter synchronization gate — 已修复

- **来源**: DS F2 / MiMo 02
- **Controller 要求**: 指定测试控制的同步原语（event/gate），在 public activity callback 和/或 `get_run` 观察到 `WAITING` 之前 hold Ready。
- **修复后 plan 证据**:
  - S2 line 181: "deterministic adapter 实例必须持有测试控制的同步门控，例如 `asyncio.Event` / gate：gate 未打开时每次 `poll_wait` 返回 `WaitPollNotReady`；测试通过 public activity callback 和/或 `get_run` 观察到 `WAITING` 后才打开 gate；gate 打开后下一次 `poll_wait` 返回 `WaitPollReady(...)`"
  - S2 line 182: "禁止使用简单无约束的 NotReady-then-Ready 计数器作为唯一同步，因为它可能在 public WAITING 观察前提前 resolve。"
  - S2 line 182: "deterministic adapter 的状态必须是 smoke 生命周期内的实例状态或显式测试 fixture 状态，不得使用模块级全局计数器"
- **判定**: 已修复。同步门控已显式指定，无约束计数器被明确禁止，状态生命周期明确。

### Finding 3: on_activity WAITING 和 get_run WAITING 双重观察 — 已修复

- **来源**: DS F3
- **Controller 要求**: `on_activity` 必须观察到 `EntrypointActivityStatus.WAITING`，`get_run(...).status == WAITING` 作为额外 public snapshot 断言。
- **修复后 plan 证据**:
  - Success Signal line 12: "`on_activity` 必须观察到 `EntrypointActivityStatus.WAITING`，证明 Service activity 投影可展示等待态；`get_run(accepted_run_id).status == RunStatus.WAITING` 是额外 public snapshot 断言。"
  - S2 line 184: "`on_activity` 必须收集并断言至少一次 `EntrypointActivityStatus.WAITING`；测试同时用 `host.get_run(accepted_run_id)` 断言 public snapshot 曾观察到 `RunStatus.WAITING`。建议 `on_activity` 在看到 waiting activity 后设置 `waiting_activity_seen` event，测试再确认 `get_run(...).status == RunStatus.WAITING`，最后打开 poll adapter gate。"
- **判定**: 已修复。两种观察路径均为必须（非二选一），且与 synchronization gate 的协作顺序已给出。

### Finding 4: Outbox backfill 使用具体 public API — 已修复

- **来源**: DS F4 / MiMo 01
- **Controller 要求**: 使用具体 public path：terminal 后直接调用 `host.read_outbox_terminal_items`，或仅在刻意测试 reconnect 语义时使用实际存在的 `startup_reconnect_entrypoint_session`。
- **修复后 plan 证据**:
  - S2 line 186: 使用具体 `host.read_outbox_terminal_items(ReadOutboxTerminalItemsRequest(session_id=session_id, after=OutboxTerminalCursor(event_sequence=0), limit=...))`
  - S2 line 186: "不要引用不存在的 reconnect helper；只有刻意测试 Service startup reconnect 语义时才可另用实际存在的 `startup_reconnect_entrypoint_session`，本 smoke 首选直接 public outbox read。"
- **代码证据**: `ReadOutboxTerminalItemsRequest`（`dayu/host/api.py:2869`）、`OutboxTerminalCursor`（`dayu/host/api.py`）均在 `dayu/host/__init__.py` 中公开导出。`startup_reconnect_entrypoint_session`（`dayu/service/entrypoint_runtime.py:722`）真实存在。
- **判定**: 已修复。不存在的 helper 已替换为具体 public API，所有引用的类型均存在且公开导出。

### Finding 5: S1/S2 framing 澄清 — 已修复

- **来源**: MiMo 03
- **Controller 要求**: 澄清 S1 是 production assembly slice，S2 是 public workflow smoke slice；S2 可使用 direct public opener assembly，但必须验证与 S1 相同的 public poller contract。
- **修复后 plan 证据**:
  - Implementation Decisions line 127: "S1 是 production assembly slice，目标是让真实 Service/Fins 装配能得到 poller policy 与 poll adapter registry；S2 是 public workflow smoke slice，可直接组装 public `OpenHostOptions` 和 deterministic public poller contract，不需要依赖 S1 的 Service assembly helper，但必须验证与 S1 相同的 public poller contract"
  - S2 line 179: "S2 可直接组装这些 public options，不必通过 `compose_open_host_options`，但 smoke 必须证明 production poller 只通过 public `wait_poll_adapter_registry` 解锁恢复。"
- **判定**: 已修复。S1/S2 关系和独立性已澄清，不再过度声明依赖。

### Finding 6: Forbidden-path validation 实用性 — 已修复

- **来源**: DS F5 / MiMo 04
- **Controller 要求**: 使用 import-oriented grep pattern，包含 `dayu.host.durable`，要求 implementation report 解释 benign match。
- **修复后 plan 证据**:
  - S2 line 187: "测试文件不得 import `dayu.host.durable` 或 `dayu.host.tool_runtime`"
  - S2 line 194: grep pattern 更新为 `from dayu\\.host\\.durable|import dayu\\.host\\.durable|from dayu\\.host\\.tool_runtime|import dayu\\.host\\.tool_runtime|...` — import-oriented
  - S2 line 188: "若 implementation report 中出现 grep 命中，必须逐条解释是否为 benign match，不能把 benign match 当作已通过证明。"
  - S2 line 194: "若出现仅来自注释 / docstring 的 benign match，implementation report 必须解释，但测试源码应主动避免这些文字。"
  - Risk R4: "用 import-oriented pattern 覆盖 `dayu.host.durable` 等 durable module import"
- **判定**: 已修复。Grep pattern 已精确化为 import-oriented，benign match 处理流程明确。

### Finding 7: WaitPollAdapter typing boundary — 已修复

- **来源**: DS open question（controller 提升为 accepted）
- **Controller 要求**: smoke assertion path 不得读 durable row 或导入 durable helper；若 strict typing 需要 public type alias/export 或 protocol-compatible signature，implementation 必须选择最小 public-contract-preserving 方案并过 pyright。
- **修复后 plan 证据**:
  - S2 line 182: "adapter 可以实现 public `WaitPollAdapter` protocol 并消费 Host 传入的 wait snapshot，但 smoke 的断言路径不得读取 durable rows、不得导入 durable helper、不得用 wait id 桥接。"
  - Risk R5: "smoke 的 assertion path 不得读取 durable row 或导入 durable helper；若严格 typing 需要 public type alias/export 或 protocol-compatible adapter signature，implementation 必须选择最小、保持 public contract 的方案并通过 pyright，不得以此授权 durable store query、internal wait mutation 或测试私有 wait id bridging。"
  - Success Signal line 16: 禁止读取 durable wait row / dispatch row / scheduler internals / ToolRuntime internals / EngineEvent / wait record mutation helper / 测试私有 durable wait id 桥接 / manual resolve。
- **判定**: 已修复。Typing boundary 约束明确：adapter 可通过 protocol 接收 wait snapshot，但 assertion path 不得独立查询 durable row，不得以此授权内部 wait mutation。

## Critical Re-review Checks 逐条确认

| # | Check | 结果 |
|---|-------|------|
| 1 | Service/Fins `wait_poll_adapter_registry` data flow 显式足以为 `_tooling_options_from_discovery` 提供 None guard 和 runtime 复用 | **通过** — S1 lines 147-150 显式指定 |
| 2 | Deterministic poll adapter 有同步门控，WAITING 公开观察后 Ready | **通过** — S2 lines 181-182 要求 asyncio.Event gate，禁止无约束计数器 |
| 3 | `on_activity` WAITING 和 `get_run` WAITING 均为必须 | **通过** — line 12 和 line 184 双重必须 |
| 4 | Outbox backfill 使用具体 public API，不引用不存在 helper | **通过** — line 186 使用已验证存在的 `ReadOutboxTerminalItemsRequest` / `OutboxTerminalCursor` |
| 5 | S1/S2 framing 清晰 | **通过** — line 127 和 line 179 已澄清 |
| 6 | Forbidden-path validation 实用，禁止 durable wait row read / internal dispatch / scheduler / ToolRuntime / EngineEvent / manual resolve | **通过** — lines 187-188/194 import-oriented pattern + benign match 处理 |
| 7 | WaitPollAdapter typing boundary 处理，不授权 durable store query 或 internal wait mutation | **通过** — line 182 + Risk R5 |
| 8 | 无新增 blocker（public-contract-only 违规或 code-generation 歧义） | **通过** — 见下方 New Blocker Scan |

## New Blocker Scan

对修复后 plan 全文扫描，检查是否引入新的 public-contract-only 违规或 code-generation 歧义：

- **引用的 public API 类型验证**: `ReadOutboxTerminalItemsRequest`、`OutboxTerminalCursor`、`startup_reconnect_entrypoint_session`、`read_outbox_terminal_items` 均真实存在且公开导出。**无新增不存在引用**。
- **引用的内部 pattern 验证**: `_fins_awaiting_registry_inputs_from_provider_configs`（line 1825）、`_fins_wait_activation_registry_from_provider_configs`（line 1789）均存在于 `dayu/service/host_assembly.py`，plan 引为 pattern 参照真实可循。**无新增不存在引用**。
- **同步原语**: `asyncio.Event` 是 Python 标准库原语，无歧义。
- **Durable store 授权**: plan 未在任何位置授权 durable wait row 读取、internal wait mutation 或 manual resolve。已修复 plan 的 Risk R5 和 S2 line 182 重申了此红线。
- **Adapter 状态管理**: deterministic adapter 状态限定为 smoke 生命周期实例状态或 fixture 状态，禁止模块级全局计数器。**无歧义**。
- **S1 函数签名占位符**: line 148 使用 `...` 作为参数占位符（如 `_fins_awaiting_registry_inputs_from_provider_configs(provider_configs, available_tool_names=...)`），但 pattern 参照的真实函数签名在代码中可查。这是 plan 中的常规写法，不构成 code-generation 阻塞。
- **"建议"措辞**: line 184 使用 "建议" 描述 waiting_activity_seen → get_run → open gate 的顺序，但同步门控本身（line 181）是强制要求（"必须持有"）。ordering recommendation 足够具体，implementation agent 不会产生歧义。**不构成 blocker**。

**结论: 无新增 blocker。**

## Residual Risks（修复后残余）

| Risk | Severity | 说明 |
|------|----------|------|
| S2 smoke 的 `submit_entrypoint_turn_and_wait` 是复杂 async 函数，构造所有必需输入（`EntrypointTurnRequest`、`PreparedSceneInputs`、`ServiceOpenHostAssemblyResult`）可能成为 implementation 中的实际困难 | Low | 属于 implementation 正常范围，非 plan 缺陷 |
| Deterministic adapter 的 `asyncio.Event` gate 实现若与 poller 内部 backoff/retry 交互可能导致意外行为 | Low | Plan 已通过短 poll interval + bounded `asyncio.wait_for` 缓解 |
| S1 `_fins_wait_poll_adapter_registry_from_provider_configs` 的具体签名需 implementation agent 参照已有 pattern 自行确定 | Low | Pattern 函数存在且清晰，属正常 implementation 判断 |

## Final Re-review Conclusion

**pass**

所有 7 条 controller-accepted findings 均已修复。修复后的 plan 具备 code-generation-ready 品质：数据流完整、同步门控明确、双重观察路径必须、公共 API 引用真实存在、forbidden-path 验证实用、typing boundary 约束清晰。无新增 blocker。

## Accepted Finding Statuses

| Finding | 来源 | 状态 |
|---------|------|------|
| Service/Fins poll adapter registry data flow | DS F1 | 已修复 |
| Deterministic poll adapter synchronization gate | DS F2 / MiMo 02 | 已修复 |
| on_activity WAITING + get_run WAITING 双重观察 | DS F3 | 已修复 |
| Outbox backfill concrete public API | DS F4 / MiMo 01 | 已修复 |
| S1/S2 framing clarification | MiMo 03 | 已修复 |
| Forbidden-path validation practicality | DS F5 / MiMo 04 | 已修复 |
| WaitPollAdapter typing boundary | DS open question | 已修复 |
