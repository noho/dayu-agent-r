# Code Re-Review: S1 Accepted Findings Verification

## Scope

- Mode: current changes (re-review of fix)
- Work unit: `WU-CLI-FINS-OBS-01`
- Slice: `S1-fins-job-event-contract`
- Gate: re-review
- Agent: AgentDS
- Branch: `phaseflow/host-issues-implementation`
- Base: `HEAD`（当前未提交 workspace changes）
- Output file: `docs/reviews/code-review-20260615-184311.md`
- Fix artifact: `docs/reviews/wu-cli-fins-obs-01-s1-fix-codex.md`
- Original reviews:
  - `docs/reviews/code-review-20260615-183010.md` (AgentMiMo)
  - `docs/reviews/code-review-20260615-183203.md` (AgentDS)
- Adjudication: `docs/reviews/wu-cli-fins-obs-01-s1-code-review-adjudication-20260615-183453.md`
- Included scope: accepted findings MiMo-001, DS-F002, DS-F003 only
- Excluded scope: deferred/rejected findings (sidecar O(n), empty payload WARN formatting, terminal duplicate idempotency)

## Accepted Findings Status

| Finding | 状态 | 证据 |
|---|---|---|
| MiMo-001: non-terminal event append failure WARN test coverage | **已修复** | 见下文 MiMo-001 详情 |
| DS-F002: removal of event type re-export from `ingestion_runtime.__all__` | **已修复** | 见下文 DS-F002 详情 |
| DS-F003: README sync (`dayu/fins/README.md`, `tests/README.md`) | **已修复** | 见下文 DS-F003 详情 |

## Evidence

### MiMo-001: non-terminal event append failure WARN test coverage — 已修复

- **新测试**: `test_non_terminal_event_append_failure_warns_and_job_still_succeeds`（`tests/fins/test_fins_ingestion_runtime.py:1625-1689`）
- **触发条件**: monkeypatch 使 `FsFinsIngestionJobStore.append_job_event` 对 `JOB_RUNNING` 抛出 `OSError("event sidecar unavailable")`（行 1662-1663）
- **断言验证**:
  - job 仍正常进入 `SUCCEEDED` 终态，业务结果正确（`downloaded_count == 1`）（行 1678-1679）
  - sidecar 中仅包含 `JOB_QUEUED` 和 `JOB_SUCCEEDED`（`JOB_RUNNING` 写入失败后不出现）（行 1680-1683）
  - caplog 包含 `fins.ingestion.job_event_append_failed` WARN（行 1684）
  - WARN 包含 `job_id`、`event_type=job_running`、`error_type=OSError`、`error_summary=event_append_failed`（行 1685-1688）
  - 异常消息本身不泄漏到日志（行 1689）
- **原有终端事件测试** `test_terminal_event_append_failure_warns_without_rolling_back_terminal_record`（行 1692+）保持完整，未被替换或削弱
- **测试通过**: `48 passed` (vs 原始 47，+1 新测试)
- **pyright**: `0 errors, 0 warnings, 0 informations`

### DS-F002: removal of event type re-export from `ingestion_runtime.__all__` — 已修复

- **`ingestion_runtime.__all__`**（行 3979-4004）: 已移除 `FinsIngestionJobEventAppend`、`FinsIngestionJobEventRecord`、`FinsIngestionJobEventType`
- **内部导入保留**（行 42-47）: runtime 内部仍从 `dayu.fins.ingestion_events` 导入这些类型用于内部实现，但不再通过 `__all__` 公开
- **canonical 公共路径**: `dayu.fins.ingestion_events.__all__`（行 230-237）保持这三个类型为唯一公共导出
- **测试导入**: `tests/fins/test_fins_ingestion_runtime.py` 从 `dayu.fins.ingestion_events` 导入（使用 canonical 路径，非 runtime re-export）
- **pyright**: 0 errors，无导入断链

### DS-F003: README sync — 已修复

#### `dayu/fins/README.md`

- **公共契约章节**（行 268）: `dayu.fins.ingestion_events` 列为 Fins 专属契约 canonical 公共入口，导出 `FinsIngestionJobEventAppend`、`FinsIngestionJobEventRecord`、`FinsIngestionJobEventType` 与有界 payload 校验 helper
- **接口章节**（行 147）: `read_job_events(job_id, *, after_sequence=0, limit=100) -> tuple[FinsIngestionJobEventRecord, ...]` 列为 `FinsIngestionRuntime` 公共 API
- **Ingestion runtime 章节**（行 409）: event sidecar 语义完整记录——JSONL 路径、sequence 游标、status/observation 事件分类、bounded payload 约束、WARN-and-continue 策略
- 所有描述均为当前已落地代码事实，符合 README Agent更新约束

#### `tests/README.md`

- **`tests/fins/` 章节**（行 173）: `test_fins_ingestion_runtime.py` 描述新增 "ingestion job event sidecar 的 queued/running/terminal/status observation 序列、`read_job_events(...)` 游标读取、bounded payload 与敏感内容不落 sidecar、并发 sequence 分配、非法 payload 拒绝、non-terminal 与 terminal event append failure 的 WARN-and-continue 行为"
- 所有描述均为当前已存在测试事实，符合 README 更新边界约束

## Deferred / Rejected Findings (Verified Not Fixed)

以下 finding 按 adjudication 不应修复，确认当前代码中未被改动：

- **sidecar sequence lookup O(n)** (MiMo-002 / DS-F001): `_last_event_sequence_locked` 仍全量读取 JSONL。符合 deferred-to-S2 裁决，未修改。
- **empty payload WARN formatting** (MiMo-003): `_append_job_event_warn` 仍输出 `payload_key_count=0 payload_keys=`。符合 rejected 裁决，未修改。
- **terminal duplicate idempotency**: `_append_terminal_job_event_warn` 仍无幂等守卫。符合 deferred non-blocking 裁决，未修改。

## Remaining Blockers

无。所有 accepted findings 已修复。Deferred/rejected findings 按 adjudication 保持不变。

## Residual Risks

- Event sidecar sequence lookup scalability 仍由 S2 承担（adjudication 已记录 owner）
- S2 payload 构造 bug 被 WARN 吞掉的 risk 仍由 S2 review gate 承担
- Terminal event duplicate idempotency 仍为 deferred non-blocking hardening
- `ingestion_events.py` 无独立单元测试（helper 函数边界条件仅通过 runtime 集成测试间接覆盖）

## Final Conclusion

**PASS** — 3/3 accepted findings 已修复。

- MiMo-001: 新增 non-terminal event append failure WARN test，覆盖 `JOB_RUNNING` 写入失败时 job 仍可达终态且产生 bounded WARN
- DS-F002: event 类型已从 `ingestion_runtime.__all__` 移除，canonical 导入路径统一为 `dayu.fins.ingestion_events`
- DS-F003: `dayu/fins/README.md` 和 `tests/README.md` 按各自约束更新，仅记录已落地 S1 facts

验证通过: pytest 48 passed, pyright 0 errors.

## Completion Report

- **Artifact path**: `docs/reviews/code-review-20260615-184311.md`
- **Conclusion**: PASS
- **Accepted findings fixed**: 3/3 (MiMo-001, DS-F002, DS-F003)
- **Remaining blockers**: 0
- **Files changed**: `dayu/fins/ingestion_runtime.py`, `tests/fins/test_fins_ingestion_runtime.py`, `dayu/fins/README.md`, `tests/README.md`
