# Code Review

## Scope

- Mode: current changes (workspace uncommitted changes relative to HEAD)
- Branch: `wu-cm-12-conversation-memory-drift`
- Base: HEAD
- Output file: `docs/reviews/code-review-20260618-185121.md`
- Included scope: `dayu/host/compaction.py`, `dayu/host/compact_material.py`, `tests/host/test_compact_material.py`, `tests/host/test_llm_compaction.py`, `docs/reviews/wu-cm-12-fix-r1-s1-implementation-codex-20260618.md`
- Excluded scope: `dayu/host/run_input.py` (Slice 2), `tests/host/test_compaction_operation.py` (not in slice allowed files, but impacted by production code change)
- Review focus: WU-CM-12-FIX-R1 Slice 1 — compact input DTO guard removal and default evidence chunking removal

## Findings

### 001-未修复-高-`test_compaction_operation.py` 未更新导致测试回归

- **入口/函数**: `test_evidence_chunks_share_same_durable_query_text`
- **文件(行号)**: `tests/host/test_compaction_operation.py:1632`
- **输入场景**: 运行 `pytest tests/host/test_compaction_operation.py`
- **实际分支**: `_evidence_blocks` / `_pack_evidence_blocks` 不再调用 `_evidence_chunks`，单个 evidence block 输出单个 `E1` label
- **预期行为**: 所有受影响测试在实现变更后必须通过
- **实际行为**: `test_evidence_chunks_share_same_durable_query_text` 断言 `("E1.1", "E1.2", "E1.3")` 但实际输出 `("E1",)`，测试 FAIL
- **直接证据**: `tests/host/test_compaction_operation.py:1721-1725` 断言 `E1.1/E1.2/E1.3`；`dayu/host/compact_material.py:1318-1332` 新代码对每个 `InitialEvidenceMaterial` 只生成一个 `CompactEvidenceBlock`，label 为 `material_label(EVIDENCE_MATERIAL, index)` 即 `E1`；`_evidence_chunks` 已删除
- **影响**: 测试回归；任何运行 `test_compaction_operation.py` 的 CI 或本地验证都会报 1 failed
- **建议改法和验证点**: 更新 `test_evidence_chunks_share_same_durable_query_text` 断言为单个 `E1` label，query_text 仍为同一 durable query text；或删除该测试并用新的 no-chunk 语义测试替代。验证：`pytest tests/host/test_compaction_operation.py::test_evidence_chunks_share_same_durable_query_text -q` 通过
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

## Open Questions

- 无

## Residual Risk

- `test_compaction_operation.py` 不在本 slice 允许文件范围内，但生产代码变更直接影响该文件中的测试。实施 agent 只运行了 `test_compact_material.py` 和 `test_llm_compaction.py`（79 passed），未运行 `test_compaction_operation.py`（1 failed, 47 passed），导致回归未被发现。实施报告的 "79 passed" 验证结论不完整。
- `_ACCEPTED_TOOL_EVIDENCE_MATERIAL_LIMIT` 仍在 `dayu/host/run_input.py` 中，这是 Slice 2 范围，本 slice 不处理。
- 单条超长 evidence 不再默认拆分后，compactor input 可能超预算；这应由 Context Governance selection / fallback / fail-closed 处理，不在 DTO 字段 cap 中处理。

## Review Details

### DTO guard 移除正确性

`CurrentInputAnchorVNext.__post_init__` 已从 `_require_bounded_non_empty_text(..., max_chars=CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS)` 改为 `_require_non_empty(self.text, ...)`（`compaction.py:664-665`）。`EvidenceReadableItemVNext.__post_init__` 同理（`compaction.py:1045-1048`）。两个旧常量 `CURRENT_INPUT_ANCHOR_VNEXT_TEXT_MAX_CHARS` 和 `EVIDENCE_BLOCK_CHUNK_VNEXT_TEXT_MAX_CHARS` 已从 production code 和 `__all__` 中删除。

`MAX_VNEXT_*` output parser safety guards 正确保留：`SessionSummaryCandidateVNext`、`EvidenceBackedFactCandidateVNext`、`ForwardIntentCandidateVNext`、`ReferenceContinuityCandidateVNext`、`CompactCandidateDiagnosticVNext`、`AnswerAnchorCandidateVNext` 等 LLM-generated output candidate 类仍使用 `_require_bounded_non_empty_text` + `MAX_VNEXT_*` 常量。这些是 output accept barrier，不是 EventLog-derived input material legality guard。

### 默认 evidence chunking 移除正确性

`_evidence_chunks`、`_EvidenceChunk`、`evidence_chunk_label`、`EVIDENCE_BLOCK_CHUNK_TEXT_MAX_CHARS` 已从 production code 和 `__all__` 中删除。`_evidence_blocks`、`_evidence_provenance`、`_pack_evidence_blocks`、`_provenance_from_evidence_blocks` 四个函数已改为一个 source evidence block 输出一个 `CompactEvidenceBlock` / `PromptLocalProvenanceEntry`。

`validate_material_label` 仍保留对 `E1.1` 形式的解析能力（`compact_material.py:750-756`），仅作为 label parser，不作为默认 chunking 依据。

### provenance / content digest 语义保留

- `content_digest` 使用 `_text_digest(material.raw_result_text)` 计算全文 digest，不再计算 chunk digest。正确。
- `chunk_parent_label` 和 `chunk_ordinal` 在新代码中不设置，dataclass 默认为 `None`。正确。
- `payload_refs`、`artifact_refs`、`source_locator_refs` 等 provenance refs 保留完整。

### 测试覆盖

- `test_compact_material.py`：长 current input 经 `build_compact_material_pack` 后进入 `conversation_compact_input_vnext_from_material_pack`，完整落入 `CurrentInputAnchorVNext.text`。正确。
- `test_compact_material.py`：长 accepted evidence 经 material pack 和 vNext input 后保持单个 `E1`，`raw_result_text` / `response_text` 保持全文，无 `E1.1` / `E1.2`。正确。
- `test_compact_material.py`：原大 evidence chunking 测试迁移为 no-default-chunk 断言，包含单 `E1` label、全文 digest、provenance 保留、`chunk_parent_label` / `chunk_ordinal` 为 `None`。正确。
- `test_llm_compaction.py`：`test_llm_context_compactor_prepares_same_source_runner_input` 覆盖长 current input 和长 evidence 不在 prepare path 失败。正确。
- `test_compaction_operation.py`：**未更新**，导致 `test_evidence_chunks_share_same_durable_query_text` 回归。Finding 001。
