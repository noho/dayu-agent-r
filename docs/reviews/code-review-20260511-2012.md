# Code Review

## Scope

- Mode: current changes
- Branch: `host/phase_0_design`
- Base: `main`
- Output file: `docs/reviews/code-review-20260511-2012.md`
- Included scope: 18 files changed (committed + unstaged)，涵盖 Engine 事件契约重命名、EngineEvent 字段精简、Engine README 重写、文档约束收紧、dayu/README.md 微调
- Excluded scope: `docs/reviews/`（review artifacts）、`dayu/engine/runners/openai/`（Runner 实现层，本次未修改）
- Parallel review coverage: 无

## Findings

### 01-已修复-低-`tool_awaiting` 事件类型未被 README 事实测试覆盖

- **Controller decision status**: accepted
- **Fix status**: fixed
- **Re-review status**: fixed
- **入口/函数**: `tests/engine/test_engine_readme_phase2.py::test_engine_readme_covers_current_interface_facts`
- **文件(行号)**: `tests/engine/test_engine_readme_phase2.py(22-63)`
- **输入场景**: Engine README 中 `tool_awaiting` 行被意外删除或改写
- **实际分支**: `required_fragments` 元组包含 `tool_call_requested`、`tool_result_accepted`、`context_compaction_requested` 等事件类型字符串，但不包含 `tool_awaiting`
- **预期行为**: `tool_awaiting` 是 `EngineEventType` 枚举成员（`engine_events.py:40`），已在 README 第 97 行列为当前接口事实，测试应覆盖该字符串以防止 README 回归
- **实际行为**: 若 `tool_awaiting` 从 README 中被意外移除，`test_engine_readme_covers_current_interface_facts` 仍会通过，不会捕获该回归
- **直接证据**: `tests/engine/test_engine_readme_phase2.py:22-63` 的 `required_fragments` 不含 `"tool_awaiting"`；`dayu/engine/contracts/engine_events.py:40` 定义 `TOOL_AWAITING = "tool_awaiting"`；`dayu/engine/README.md:97` 列出 `tool_awaiting`
- **影响**: 文档回归不会被测试捕获；当前行为正确，只是测试覆盖不足
- **建议改法和验证点**: 在 `required_fragments` 元组中补充 `"tool_awaiting"`，运行 `pytest tests/engine/test_engine_readme_phase2.py` 验证通过
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **Gateflow 回写**: README 当前接口事实测试已补充 `"tool_awaiting"`，Engine README 接口、事件流与状态机章节均保留 `tool_awaiting` 当前事件事实；见 `docs/reviews/gateflow-implementation-engine-readme-20260511.md` 与 `docs/reviews/gateflow-re-review-engine-contract-readme-20260511.md`。

## Open Questions

- 无

## Gateflow 修复状态

- Review artifact: `docs/reviews/code-review-20260511-2012.md`
- Implementation artifact: `docs/reviews/gateflow-implementation-engine-readme-20260511.md`
- Re-review artifact: `docs/reviews/gateflow-re-review-engine-contract-readme-20260511.md`
- Re-review conclusion: fixed，无 blocker。

### Finding 01 状态：accepted / fixed

- Controller decision: accepted。
- Fix status: fixed。
- 修复方式：`tests/engine/test_engine_readme_phase2.py` 的 README 当前接口事实断言已包含 `"tool_awaiting"`；`dayu/engine/README.md` 的接口、事件流与状态机章节均保留 `tool_awaiting` 当前事件事实。
- Re-review status: fixed。

### 验证

- `source .venv/bin/activate && pytest tests/engine/test_engine_readme_phase2.py -q`：4 passed。
- `source .venv/bin/activate && pytest tests/engine -q`：301 passed。
- `source .venv/bin/activate && python -m pyright dayu/ tests/ utils/`：0 errors。
- `git diff --check`：通过。

## Residual Risk

- `EngineEvent` 移除 `event_id` / `sequence` 是有意的契约收紧。当前所有 Engine 内部测试和 package export 测试均已同步更新。但如果存在未纳入本次 diff 的外部调用方（Host 层、Service 层）依赖这两个字段做幂等写入或事件关联，迁移时需要确认替代方案。本次 review 范围限于 `dayu/engine/` 和相关测试，未覆盖 Host / Service 层消费路径。
- `docs/engine/design.md` 中 `correlation_id` 描述已从 "EngineEvent sequence 建立关联" 改为 "Engine 不用它承载 trace、持久化游标或幂等键"，但 Host 侧 ToolRuntime 实际如何建立关联（如果需要）未在本次变更中说明。这是设计意图的显式让步，不是 defect，但 Host 迁移时需注意。
