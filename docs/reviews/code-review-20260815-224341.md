# Code Review — UF-FIX07 Aggregate Re-Review（独立执行）

## Scope

- Mode: current changes
- Branch: `codex/upload-filing-oracle`
- Base: `64050349`（用户指定；也用于核验分支前既有失败）
- HEAD: `6b80400139aba1ba43d950635a6e735467db4316`（实施基线，未移动）+ 未提交 aggregate fix diff
- Output file: `docs/reviews/code-review-20260815-224341.md`
- Included scope: amendment §7 四个 allowed paths 的 aggregate fix diff
  - `dayu/fins/pipelines/docling_upload_service.py`（production，+122/-35）
  - `tests/fins/test_docling_upload_service.py`（tests，+596）
  - `dayu/fins/README.md`（docs，+4/-4 行）
  - `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md`（artifact，只读核对）
- Excluded scope: 原 plan、goal confirmation、四份 code/plan review inputs、registry、oracle、scenario、frozen
  evidence、UF-PF07/PF12、docs/reviews 下其它 agent 并发的 artifact（`code-review-20260815-222906.md`，只读未动）
- Parallel review coverage: 无。单 reviewer 沿真实链路逐行走读：
  `FinsUploadFilingFiles.require_primary()` → `_prepare_upload_selection` → `_UploadSelectionPreparation.filing_primary` →
  `_validate_source_files` → `_build_original_assets` → `_build_upload_source_fingerprint` →
  `_can_skip_upload` / `_resolve_document_version` → `prepare_upload` skip/convert/version 控制流 →
  `_build_upsert_meta` / `_PreparedAssetMutation` → storage `primary_document` / `derived_from` /
  `get_primary_source()` → `ingestion_runtime` 对 `source_fingerprint` 的下游消费 → material 分支回归。
- Contract 依据：
  - `docs/gateflow/uf-fix07-aggregate-review-plan-amendment-20260815.md`（authoritative amendment）
  - `docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md`（aggregate fix artifact）
  - `docs/gateflow/uf-fix07-multi-file-primary-collision-goal-confirmation-20260815.md`（binding scope，未 supersede）

## Findings

### 1-未修复-低-aggregate fix 新增 fail-closed 防御分支无测试覆盖
- **入口/函数**: `_build_upload_source_fingerprint()`
- **文件(行号)**: `dayu/fins/pipelines/docling_upload_service.py:1267`（filing 空 originals）、`:1269`（缺 authoritative
  primary）、`:1273`（primary identity 未 exact 命中一次）、`:1318`（material 非法携带 filing primary）
- **输入场景**: 直接以空 asset 列表、`filing_primary=None`、primary 不在 originals 或 material 携带 filing primary
  调用该私有 builder
- **实际分支**: 四处 `raise ValueError` fail-closed 分支
- **预期行为**: 按 amendment §4.2 以 ValueError fail closed；且这些 contract 分支应有 owner 级测试锁定
- **实际行为**: 实现正确 fail closed；但 `coverage report` 显示 1267/1269/1273/1318 四行均为 Miss，owner tests 未断言任何
  一个新 ValueError 分支
- **直接证据**: `python -m coverage report --include='dayu/fins/pipelines/docling_upload_service.py' --show-missing`
  输出 Missing 列表含 `1267, 1269, 1273, 1318`；测试文件 grep 无对应 `pytest.raises` 断言。amendment §5 矩阵未要求这些
  分支，属测试缺口而非 contract violation
- **影响**: 低。唯一生产 caller（`prepare_upload` 经 `_prepare_upload_selection`）在构造上保证前置条件成立；若未来重构
  移除/弱化 exact-match 守卫（如按 basename 匹配），错误会以 `StopIteration` 或静默错选角色出现，现有测试只能间接捕获
- **建议改法和验证点**: 在 `tests/fins/test_docling_upload_service.py` 增加 3~4 条 `pytest.raises(ValueError)` owner 断言
  （空 assets、缺 primary、identity 未命中、material 带 primary），并重跑 coverage 确认四行转绿；不改 production
- **修复风险（低）**: 纯测试新增，不触生产行为
- **严重程度（低）**:

## 逐项对抗性验证状态

| # | 验证项 | 状态 | 直接证据 |
| --- | --- | --- | --- |
| 1 | 不同 descriptor primary flip：create `v1` → flip `uploaded v2`（digest 变、converter 只收新 primary、`primary_document`/`derived_from`/`get_primary_source()` 全部切到 B）→ 同角色重放（companions 重排）`skipped` 保持 `v2` | PASS | `test_distinguishable_filing_primary_flip_updates_v2_then_skips_replay`；生产路径 `docling_upload_service.py:1299-1315`（primary 独立投影）+ `:1091-1092`（converter_inputs 只含 primary） |
| 2 | 完全相同 descriptor 双路径：digest 相同、`identical_skip_safe=False`；create/replay/flip 精确 `v1→v2→v3`，flip 后 identity 指针指向新 primary | PASS | `test_ambiguous_filing_primary_forces_versions_then_recovers_safe_skip` 直接断言两次 v2 digest 相等且 unsafe、repository meta 版本链 v1/v2/v3、`derived_from`/uri 指向 P2 identity；代码 `:1315`（primary descriptor 落入 companion 等价类即 unsafe）+ `:1358-1359`（unsafe 无条件 increment） |
| 3 | 恢复 safe：改变一 descriptor 使 primary 唯一 → `uploaded v4`，原样 replay `skipped` 保持 `v4`，converter call count 精确为 4 | PASS | 同一测试：`recovered_meta["document_version"] == "v4"`、`safe_replay.status == "skipped"`、`calls == [first, first, second, second]`；代码 `:1315`（恢复唯一后 `safe=True`） |
| 4 | 整组跨目录 move：两组 path-derived identities 全部变化，但 v2 digest 相同且 safe → `skipped`、版本保持 `v1`、converter 不重跑、repository 保留旧已发布 tree | PASS | `test_safe_multifile_whole_set_move_keeps_v1_and_published_tree`：`old_identities.isdisjoint(new_identities)`、digest 相等、`moved.status=="skipped"`、`meta["document_version"]=="v1"`、published originals == old identities、`calls == [old_primary.name]`；payload 不含 path（`:1309-1313` 只序列化 descriptors + 版本字面量） |
| 5 | 旧 v1 多文件 digest：独立旧公式 fixture（不调 production builder）seed 后首次 v2 upsert `uploaded v2`，v2 digest replay `skipped` 保持 `v2`；单文件旧/新 digest 相等；production 无 legacy helper/dual-read | PASS | `test_old_v1_multifile_fingerprint_transitions_once_to_v2_and_then_skips`：`_build_old_filing_fingerprint_for_test` 独立复现旧公式、`_can_skip_upload(...) is False`、`prepared.document_version=="v2"`、replay `skipped`；production grep 无 legacy/dual-read/兼容分支 |
| 6 | overwrite / delete / material / single fixed vector | PASS | overwrite 分支顺序原样保留（`:1234`）；delete 在 fingerprint 前返回 `_PreparedDeleteMutation`（`:335-341`）且 `filing_primary=None`（`:1091`）；material payload 构造逐字节不变、`identical_skip_safe=True`、固定 digest `099dc9636e306c75f1d5d64dd0210123956ba73888e968088c7279baab1d7fdd` 断言通过；single 固定 digest `e7d70a19bec88c733e519eace405aea9e0a357db2f7a53cdc9450d545c430848` 断言通过 |
| 7 | only `.value` 持久化，无 path/order/safety 泄漏 | PASS | `:402`、`:419` 只写 `source_fingerprint.value`；测试断言 meta 无 `identical_skip_safe`/`filing_primary` 键；payload 只含 descriptors + `filing-primary-role-v2` 字面量（模块级 `Final`，`:66`）；path-derived identity 仅内存角色关联（`:1270-1271`），属 amendment 明确保留的 storage asset identity owner；`ingestion_runtime` 只把 `source_fingerprint` 当不透明 bounded string 投影，不解析语义 |
| 8 | README primary-first 与 companions 相对顺序 | PASS | README diff 精确落实 §6 五项：role order 读取、single/multi fingerprint 边界、flip update、ambiguity conservative update（未声称 role bool 可解决）、single move/rename/content 与 material 不变；不暴露 path digest/内部类型/迁移方案；`Agent更新约束` 章节存在且本次只写已实现行为 |

## 四 Slice 不回退与验证门

| 验证项 | 结果 |
| --- | --- |
| 13-file affected suite（amendment §8 命令） | `1362 passed, 1 skipped, 3 warnings`（43.56s，exit 0；3 warnings 均为 .venv edgar deprecation，非本次 diff） |
| focused owner tests | `76 passed`（1.28s） |
| full pyright `dayu/ tests/ utils/` | `0 errors, 0 warnings, 0 informations` |
| 六生产文件 branch coverage | `ingestion_runtime 88%`、`upload_format_contract 89%`、`arg_parsing 99%`、`commands/fins 81%`、`upload_tools 92%`、`docling_upload_service 85%`，全部 `>=80` PASS |
| `git diff --check` | PASS |
| HEAD | `6b80400139aba1ba43d950635a6e735467db4316`，未移动 |
| tracked diff 范围 | 仅 §7 四个 allowed paths；`docs/gateflow/uf-fix07-aggregate-review-fix-20260815.md` 为唯一新增 untracked |
| 只读 review inputs / 原 plan / goal confirmation | `git diff` 为空（本次结束时复核仍为 0 行） |
| 禁改项 | 未修改 registry/oracle/scenario/frozen evidence；未运行 UF-PF07/PF12；未 commit/push/PR |
| 全仓 `pytest tests/` | `7704 passed, 9 failed, 10 skipped, 6 deselected`。9 个失败经两步隔离证明与本次工作无关：① stash 三个工作文件后同 8 个失败照旧（1 个 host 测试属 flaky）；② 在 base `64050349` 临时 worktree 运行同集合同样失败。失败文件（`tests/host/*`、`tests/service/test_import_boundary.py`、`tests/cli/test_init_workspace.py`、`tests/cli/test_upload_filings_from_command.py`）均不在 base..HEAD diff 与工作 diff 范围内 |
| 生产 diff 无 `hasattr/getattr`/`Any`/`object`/`type: ignore`/TODO | grep 全空 |

## Open Questions

- 无。amendment §4.2/§4.3/§4.4 的 branch contract、§5 断言矩阵、§6 README 条款与实现逐项对照一致；
  `_resolve_document_version` / `_can_skip_upload` 与 amendment pseudocode 逐分支一致。

## Residual Risk

- 9 个全仓失败测试为 base `64050349` 即存在的既有失败（host/service/cli 子系统），非 UF-FIX07 或本 aggregate fix
  引入；属四 Slice 之外的历史债务，未纳入本次 13-file affected gate。
- 上述 Finding 1：4 个新增 fail-closed ValueError 分支无直接测试覆盖（file branch coverage 85% 整体达标）。
- amendment 已接受的保守边界继续有效：ambiguous replay 持续 version churn；旧无角色多文件 digest 首次 v2 upsert 会
  update/version increment（无 dual-read/shim）；UF-FIX08/10/11 与 UF-PF07/PF12 evidence 工作留待独立授权。

## 结论

**PASS**：两个 accepted findings 均已在正确 owner boundary 修复；八个指定对抗性场景全部通过；四 Slice 行为无回退
（13-file suite、pyright、六文件 coverage、diff/scope/forbidden 全绿）；仅 1 项 LOW 测试缺口（新增 fail-closed 分支无直接
断言），不阻塞 aggregate re-review 通过，建议在后续任一 gate 顺手补齐或由 controller 裁决 deferred。
