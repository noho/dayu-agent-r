# UF-FIX10 same-request-concurrency 最终定点 Re-Review（MiMo）

## Reviewed target and scope

- **target**: `docs/gateflow/uf-fix10-same-request-concurrency-plan-20260816.md`（C-F1/C-F2 fix 后版本）
- **scope**: 对 C-F1/C-F2 两项 Controller-accepted blocker 的闭合验证。仅验证：
  1. C-F1：S1 是否真正零行为变化且完整 `tests/fins` green、S2 是否原子接线无 expected-red 中间态、allowed files/命令/goal mapping 是否一致
  2. C-F2：`_build_upsert_meta` 是否被钉为现有 prepare 与 rebase 唯一九字段 owner、fresh timestamps/version/storage revision 断言是否闭合
  3. 修订未破坏已通过的 C-R1/C-R2
- **日期**: 2026-08-16 23:40:44（用户指定文件名时间戳；本机系统时钟 23:42:44）
- **分支**: `codex/upload-filing-oracle`
- **前置 artifacts**: `docs/reviews/plan-review-20260816-231939-ds.md`（DS 最终 review）、`docs/gateflow/uf-fix10-final-plan-review-fix-20260816.md`（Controller fix artifact）
- **review 方式**: 只读审查；未修改 plan、生产代码、测试、README，未 commit

## 1. C-F1 验证：S1 零行为变化 / S2 原子接线 / allowed files 一致性

### 1.1 S1 零行为变化

| 验证项 | plan 声明 | 代码事实 | 结论 |
| --- | --- | --- | --- |
| S1 不把 disposition 接入 `prepare_upload()` | §6.3 L231「S1只新增上述类型与下列Docling helper，不把它接入 `prepare_upload()`」 | 当前 `_can_skip_upload` 唯一调用在 `prepare_upload` L1264，返回 True 时直接 `UploadOperationResult(status="skipped")` 终结；S1 不编辑该路径 | ✓ 一致 |
| S1 不删除现有 filing early-skip | §10.1 L498「不得删除现有filing early-skip」 | `_can_skip_upload` 返回 True → `prepare_upload` 直接返回 skipped，converter 不执行；S1 allowed files 不含 SEC/CN/HK workflow | ✓ 一致 |
| S1 不新增可达 `execute_prepared_filing_publication()` | §6.5 L303「S1不得调用 begin_batch()…也不得改变现有filing route行为」；§10.1 L497「不得新增或接通execute_prepared_filing_publication() batch lifecycle route」 | S1 只在 `filing_upload_publication.py` 放置无 I/O pure arbitration helper | ✓ 一致 |
| S1 完整 `tests/fins` 全绿 | §10.1 L516-517 validation commands 包含 `pytest tests/fins -q` | 命令存在且完整 | ✓ 一致 |

### 1.2 S2 原子接线无 expected-red 中间态

| 验证项 | plan 声明 | 结论 |
| --- | --- | --- |
| S2 在同一 slice 完成 prepare 切换 + shared route + workflow tests | §10.2 L526「在单一原子slice内启用完整新filing行为」；§10.2 L544「同步更新…不存在'生产接线完成但workflow tests尚未迁移'或反向的中间验收态」 | ✓ 一致 |
| S2 prerequisites 要求 S1 全绿 | §10.2 L540-541「S1 implementation/code review/fix/re-review/accepted slice commit完成；S1 focused tests、完整pytest tests/fins -q与pyright均green」 | ✓ 一致 |
| S2 stop condition 禁止拆出 expected-red checkpoint | §10.2 L572「若S2无法在同一implementation/review slice同时完成…停止并回到plan review，不接受部分接线或expected red」 | ✓ 一致 |
| §3.2 成功信号 11 明确 S1 全绿、S2 全绿 | L87「S1完成后现有prepare_upload() filing early-skip与SEC/CN/HK workflow行为不变，focused owner tests及完整tests/fins全绿，绝无expected red；S2以同一原子slice启用…后…继续通过」 | ✓ 一致 |

### 1.3 Allowed files / 命令 / goal mapping 一致性

| 验证项 | 验证方法 | 结论 |
| --- | --- | --- |
| S1 allowed production files = §8.1 减 SEC/CN/HK workflow | §8.1 共 12 个文件；§10.1 L476-484 列出 9 个，恰好排除 `sec_upload_workflow.py` 和 `cn_pipeline.py` | ✓ 一致 |
| S2 allowed production files = 剩余 4 个 pipeline 文件 | §10.2 L528-531 列出 `docling_upload_service.py`、`filing_upload_publication.py`、`sec_upload_workflow.py`、`cn_pipeline.py` | ✓ 一致 |
| 四个机械补 `publication_identity=None` 的测试文件均存在 | `test_fins_ingestion_runtime.py`、`test_fins_service_runtime.py`、`test_cli_fins_commands.py`、`test_service/test_fins_direct.py` — 全部在磁盘存在 | ✓ 一致 |
| S1 goal mapping 只声称成功信号 2/5/7 | §10 L469「为成功信号2/5/7提供owner contract；以成功信号11与既有全量行为零回归作为本slice验收；不声称成功信号1/3/4/6/8/10已实现」 | ✓ 一致 |
| S2 goal mapping 声称成功信号 1-11 | §10 L470 | ✓ 一致 |
| §10.1 expected completion signal 包含全量 Fins | L521「owner tests与现有全量tests/fins全部通过，无expected red」 | ✓ 一致 |

## 2. C-F2 验证：`_build_upsert_meta` 唯一九字段 owner

### 2.1 plan 声明 vs 代码事实

| 验证项 | plan 声明 | 代码事实 | 结论 |
| --- | --- | --- | --- |
| `_build_upsert_meta` 当前形态 | §5 L122「模块级私有 `_build_upsert_meta()`」；§6.3 L262「将不依赖 `self` 的实例方法提升/重构为模块级私有helper」 | 当前为 `DoclingUploadService` 实例方法（`docling_upload_service.py:943-981`），签名含 `self` | ✓ plan 声明"提升/重构"，即 S1 实施时将其从实例方法改为模块级函数 |
| 九字段集合 | §5 L122 列出 9 个字段 | 代码 L966-981 构造 `updated_at`、保留 `first_ingested_at/created_at`、写入 `document_version`、`source_fingerprint`、`ingest_complete=True`、`source_provider`、`is_deleted=False`、`deleted_at=None` — 共 9 个 | ✓ 一致 |
| 唯一调用点 | §5「现有prepare与create-overwrite rebase必须共用」 | 当前唯一调用在 `prepare_upload` L427；S1 新增 `rebase_prepared_filing_create_overwrite` 为第二调用点 | ✓ 一致 |
| `first_ingested_at/created_at` 从 fresh previous meta 保持 | §5 L122「从传入的fresh previous meta保持」 | 代码 L971-975：`previous_meta` 非空时保留这两个字段 | ✓ 一致 |
| version 由 `_resolve_document_version()` 同源计算 | §5 L122 | `_resolve_document_version` 为模块级函数（L1393-1418），`prepare_upload` L424 调用它获取 `current_version` 后传入 `_build_upsert_meta` | ✓ 一致 |
| revision 只由 storage source-document owner 新写 | §5 L123「Docling staging meta不得生成、保持或推断revision」；§6.3 L264「revision不进入Docling meta真源」 | `_build_upsert_meta` 不设置 revision 字段；`_create_source_document`（storage owner）写入 `uuid4().hex` 新 revision | ✓ 一致 |

### 2.2 rebase 断言闭合性

| 验证项 | plan 声明 | 结论 |
| --- | --- | --- |
| rebase 以 fresh previous meta 为 `previous_meta` 真源 | §6.3 L263「以fresh source meta为previous_meta真源」 | ✓ |
| 同 fingerprint 保持 fresh `document_version` | §6.3 L264「同fingerprint保持fresh document_version」；`_resolve_document_version` L1393-1418 同 fingerprint 时返回前值 | ✓ |
| 异 fingerprint 从 fresh version 递增 | §6.3 L264；`_resolve_document_version` 不同 fingerprint 时调用 `_increment_document_version` | ✓ |
| `first_ingested_at/created_at` 精确保持 fresh 值 | §6.3 L264 | ✓ |
| staging meta 不携带 revision | §6.3 L264「staging meta不携带revision真源」 | ✓ |
| S1 test 7 断言 rebase 字段/version/revision 职责 | §10.1 L510-511 | ✓ |

## 3. C-R1/C-R2 未被破坏的确认

### 3.1 C-R1：typed initial disposition 保留 explicit update identical stable skip

| 验证项 | plan 位置 | 结论 |
| --- | --- | --- |
| disposition 由 `_can_skip_upload` 同源产生 | §6.3 L232 | ✓ — 该函数无 action 参数，`overwrite=False` + identical fingerprint + `previous_meta is not None` + `is_deleted=False` → True → `IDENTICAL_PUBLICATION` |
| explicit `update` identical stable 仍 skip | §7.3.A L366-368；§7.4 L388 | ✓ — predicate A 不限 raw action 为 auto，显式包含 explicit `update` + `overwrite=False` |
| changed `COMPLETE -> COMPLETE` 不误 skip | §7.2 L352；§7.3 L377；§7.4 L390 | ✓ — 三处一致声明固定 `SOURCE_PUBLICATION_CONFLICT`；每次发布写新 revision（`uuid4().hex`）结构性保证 observation changed |

### 3.2 C-R2：双 cancel checkpoint

| 验证项 | plan 位置 | 结论 |
| --- | --- | --- |
| checkpoint 1：token 后、fresh read 前 | §7.1 步骤 4 L336 | ✓ — 三类 prospective 候选统一 cancel-first |
| checkpoint 2：arbitration 后、mutation/terminal 前 | §7.1 步骤 7 L339 | ✓ — 三类 closed 裁决统一 cancel-first |
| rollback 失败 → path-free `STORAGE_IO` | §6.4 L294-297 | ✓ |
| 不破坏 existing commit ownership late-cancel boundary | §7.1 步骤 8 L340 | ✓ — 「不新增第三套语义」 |

## 4. Findings

**无 blocker finding。**

DS 最终 review 的两个 non-blocker（F-1 S1→S2 中间红态声明、F-2 meta owner pin）已由 C-F1/C-F2 fix 完全关闭：

- F-1：C-F1 通过重划 activation boundary 从根因消除（S1 全绿、S2 原子切换），不再需要声明中间红态。
- F-2：C-F2 将 `_build_upsert_meta` 提升为模块级唯一 owner，plan §5/§6.3/§10.1 一致冻结。

## 5. Open questions

无 blocking 或 non-blocking open question。

## 6. Residual risks

DS 最终 review 的 R-1 至 R-4 均已在 plan §13.2 中分类并分配 owner：

| 风险 | 状态 |
| --- | --- |
| R-1（S1→S2 中间红态） | C-F1 从根因消除，不再存在 |
| R-2（concurrent COMPLETE → COMPLETE conflict 而非 skip） | 设计 tradeoff，由 S2 route tests + README 覆盖 |
| R-3（converter 非确定导致 sequential identical 从 skip 变 publish） | 设计 tradeoff，由 S1 identity tests + README 覆盖 |
| R-4（SHA-256 碰撞 / post-COMMITTED guard / manual writer / UF-FIX11 / material concurrency / oracle） | 均有 later-work-unit owner |

没有未分类 residual risk。

## 7. Final plan review conclusion

**pass**

C-F1/C-F2 两项 blocker 全部闭合：

1. **C-F1**：S1 allowed files 精确为 §8.1 减去 SEC/CN/HK workflow（9 个生产文件 + 8 个测试文件）；S1 不把 disposition 接入 `prepare_upload()`、不删除现有 filing early-skip、不新增可达 shared route；validation commands 包含完整 `pytest tests/fins -q`；S2 在同一原子 slice 完成 prepare 切换 + shared route + workflow tests，stop condition 禁止 expected-red 中间态；goal mapping S1→{2,5,7,11}、S2→{1-11} 一致。
2. **C-F2**：`_build_upsert_meta` 被 plan 钉为 `docling_upload_service.py` 模块级私有唯一九字段 owner；现有 prepare 与 `rebase_prepared_filing_create_overwrite()` 共同调用；fresh `first_ingested_at/created_at` 保持、同/异 fingerprint version 规则精确、revision 只由 storage source-document owner 新写——断言闭合。
3. **C-R1/C-R2 未被破坏**：typed disposition 保留 explicit update identical stable skip；changed `COMPLETE → COMPLETE` 固定 conflict；双 cancel checkpoint 对三类裁决统一 cancel-first 且保持 existing commit ownership boundary。

本 re-review 未修改 plan/生产代码/测试/README，未 commit。
