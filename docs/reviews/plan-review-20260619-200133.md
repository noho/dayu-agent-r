# WU-CM-13 Plan Final Focused Re-Review

- **Reviewed target**: `docs/host/host-issues/wu-cm-13-unified-compact-pipeline-plan.md` (post-N1–N4 fix revision)
- **Previous re-review**: `plan-review-20260619-195507.md` (pass-with-risks; N1–N4 open)
- **Review type**: final focused re-review — verify N1–N4 closure only
- **Timestamp**: 2026-06-19 20:01:33 CST

## 1. N1–N4 Closure Verification

### N1: `compact_pipeline_source_snapshot_from_pre_dispatch_view(...)` signature ✅ CLOSED

- **位置**: §5.2, lines 229–246
- **证据**: 函数签名已列入 §5.2 function signatures：
  ```python
  def compact_pipeline_source_snapshot_from_pre_dispatch_view(
      *,
      trigger_source: ContextCompactionTriggerSource,
      run: RunRow,
      material_view: PreDispatchCompactMaterialView,
  ) -> CompactPipelineSourceSnapshot: ...
  ```
- **字段来源规则已明确**:
  - `session_id`, `run_id`, `current_input_ref`, `input_event_sequence` → `run`
  - `current_input_text`, `material_blocks`, `previous_compacted_view`, `source_boundary` → `material_view`
  - `material_view_digest` → `selected_material_view_digest(material_view.material_blocks)`
  - `material_source_refs` → `selected_material_source_refs(material_blocks=..., selected_block_ids=...)`
- **Validation**: `run.input_event_sequence` 与 `source_boundary.current_input_event_sequence` 不一致时抛 typed validation error
- **§6.1 / §6.2 wiring** 均使用此构造函数

### N2: `policy_digest` → `selection_policy_digest` with consistent `MemoryProjectionPolicy` source ✅ CLOSED

- **位置**: §5.2, line 254; §6.1, line 372; §6.2, line 405
- **证据**:
  - §5.2 参数名已改为 `selection_policy_digest: str`
  - docstring 明确: "必须由同一个 `MemoryProjectionPolicy` 调 `digest_memory_projection_policy(memory_policy)` 得到；它不是 `BudgetEstimate.estimator_digest`，也不是 `source_snapshot.material_view_digest`"
  - §6.1 proactive: `selection_policy_digest = digest_memory_projection_policy(memory_policy)`
  - §6.2 reactive: `selection_policy_digest = digest_memory_projection_policy(memory_policy)` — 与 proactive 一致

### N3: `CompactPipelineFallbackSelectedMaterialHandoff` production consumption ✅ CLOSED

- **位置**: §5.1, lines 188–211; §6.4, lines 469–471
- **证据**:
  - §5.1 语义: "只服务 production fallback branch 的 selected material 校验/渲染，由 `build_fallback_decision_input(...)` 构造并挂到 `CompactPipelineFallbackDecisionInput.fallback_handoff`"
  - `CompactPipelineFallbackDecisionInput` 新增字段 `fallback_handoff: CompactPipelineFallbackSelectedMaterialHandoff | None`（当 `selection is None` 时为 `None`）
  - §6.1 proactive 消费: `fallback_handoff = fallback_input.fallback_handoff` → 校验/渲染，不持久化新字段
  - §6.2 reactive 消费: `fallback_handoff = fallback_input.fallback_handoff` → 同上
  - §6.4 明确: "是 production fallback decision input 的一部分，不是 test-only type"

### N4: `build_tier_recovery_request_plans` uses `build_compact_material_pack(...)` ✅ CLOSED

- **位置**: §5.2, lines 267–275
- **证据**: docstring 明确: "每个 tier 必须调用/复用 `build_compact_material_pack(...)` 构造完整 `CompactionRequest.material_pack`，再返回含完整 `CompactionRequest` 的 `CompactPipelineRequestPlan`；§5 允许 import `compact_material.py` 已覆盖该依赖。"

## 2. No New Findings

Post-fix review of the plan across all sections reveals no new issues:

- **Type consistency**: `CompactPipelineFallbackDecisionInput.fallback_handoff` is typed `CompactPipelineFallbackSelectedMaterialHandoff | None`; wiring pseudocode correctly handles `None` case (when selection fails).
- **Function coverage**: All 8 function signatures in §5.2 now cover all calls in §6 wiring pseudocode. No missing constructors.
- **Semantic consistency**: `selection_policy_digest` is now single-sourced from `digest_memory_projection_policy(memory_policy)` in both proactive and reactive paths.
- **Production vs test boundary**: `CompactPipelineFallbackSelectedMaterialHandoff` is unambiguously a production type carried in `CompactPipelineFallbackDecisionInput.fallback_handoff`.

## 3. Final Plan Review Conclusion

**Verdict: PASS**

All 4 residual findings (N1–N4) from the previous re-review are confirmed closed with direct evidence in the plan text. The plan is code-generation-ready:

- 9 concrete dataclasses with full field lists
- 8 function signatures with parameter types and return types
- Clear field provenance for the source snapshot constructor
- Consistent `selection_policy_digest` semantics across proactive/reactive callers
- Production fallback selected-material handoff with unambiguous lifecycle
- Recovery request builder explicitly depends on `build_compact_material_pack(...)`
- Tier 5 deferred out of scope
- WU-CM-14 provider settled as pipeline-owned audited second-read
- Fallback scope limited to selection/payload-input/action-hint
- 18-row evidence migration mapping table
- 11-field memory equivalence standard

No blocking findings remain. The plan can safely be handed to an implementation agent.
