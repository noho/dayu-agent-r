# Plan Re-Review: UF-FIX01 fiscal-period prevalidation residual (post-fix)

## 元数据

- reviewed target：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-plan-20260817.md`（plan-fix-complete 版本）
- work unit：`UF-FIX01-fiscal-period-prevalidation-residual`
- gate：`re-review`
- review 日期：2026-08-18（本机时钟）
- goal artifact：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-goal-confirmation-20260817.md`（`confirmed-by-user`）
- adjudication：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-plan-review-adjudication-20260818.md`
- fix artifact：`docs/gateflow/uf-fix01-fiscal-period-prevalidation-residual-plan-review-fix-20260818.md`
- 原 review：`docs/reviews/plan-review-20260818-000654.md`（MiMo）、`docs/reviews/plan-review-20260818-001109.md`（DS）
- 结论：**pass**

## Review Scope

逐项验证 adjudication 中 M-001..M-005 和 D-F1..D-F3 的 plan amendment 是否落实；检查新 amendment 是否引入新的 material finding、slice 红窗口、owner/LLM-facing/coverage/pyright/README 风险。基于 plan 文本与当前代码/测试直接证据。未修改 plan 或代码，未提交。

## Finding 逐项验证

### M-001 `derive_report_kind` 的 None 处理

- **adjudication 要求**：参数收窄为 `FiscalPeriod`，不消费 optional owner 结果
- **plan 落实**：§5 Type strengthening 明确 "`derive_report_kind` 的 period 参数同步收窄为 `FiscalPeriod`；三者只消费 canonical typed value，不再执行 strip/uppercase 或 domain revalidation"。§6 decision #4 同步确认。`FiscalPeriod` 是 `Literal[...]` 非 optional 类型，None 在类型层面不可传入
- **终态**：**已修复**

### M-002 类型收窄贯穿下游

- **adjudication 要求**：upload 专用 `build_cn_filing_ids` / `build_sec_filing_ids` 参数同步收窄
- **plan 落实**：§5 Type strengthening 明确 "`build_cn_filing_ids`、`build_sec_filing_ids` 与 `derive_report_kind` 的 period 参数同步收窄为 `FiscalPeriod`"。§7 S1 change #3 确认 "将两个 upload ID builder 参数与 `derive_report_kind` 参数标注为 `FiscalPeriod`"
- **终态**：**已修复**

### M-003 删除 builder 内 strip/uppercase

- **adjudication 要求**：删除两个 upload ID builder 内的 `strip().upper()`
- **plan 落实**：§6 decision #4 "删除两个 ID builder 内的 `strip().upper()`，不在 lower consumer 重做 owner 规则"。§7 S1 change #5 "删除两个 upload ID builder 的 strip/uppercase"。§7 Invariants "build SEC/CN IDs 只消费 canonical typed period"
- **终态**：**已修复**

### M-004 exact domain-error mapping

- **adjudication 要求**：exact try/except，ValueError 映射通用 code，None invariant
- **plan 落实**：§6 decision #3 给出完整伪代码：

  ```python
  try:
      normalized_period = normalize_fiscal_period(request.fiscal_period, field_name="--fiscal-period")
  except ValueError:
      _raise_upload_usage(FinsUploadUsageCode.UNSUPPORTED_FISCAL_PERIOD)
  if normalized_period is None:
      raise AssertionError("required fiscal_period owner 返回缺失值")
  ```

  §7 S1 change #4 "按 §6 exact try/except 调用 normalize_fiscal_period；所有 market 共享同一分支；domain error 映射为 UNSUPPORTED_FISCAL_PERIOD，None fail closed 为 invariant breach"
- **终态**：**已修复**

### M-005 closed enum/value/message 和旧名称扫描

- **adjudication 要求**：显式列出 closed enum set、exact usage message 和旧名称全仓扫描
- **plan 落实**：§5 Changed closed contract 列出删除 `UNSUPPORTED_CN_FISCAL_PERIOD`、新增 `UNSUPPORTED_FISCAL_PERIOD`、market-neutral 文案。§7 S1 Validation commands 的 rg 检查覆盖 `UNSUPPORTED_CN_FISCAL_PERIOD`、`unsupported_cn_fiscal_period`、`CN/HK --fiscal-period 仅支持`。§7 S1 change #2 "更新 closed code set、exact message tests、现有 CLI UF-024 exact reason，并删除所有旧 enum/value/message 断言"
- **终态**：**已修复**

### D-F1 CLI UF-024 测试纳入 S1

- **adjudication 要求**：CLI UF-024 exact reason 移入 S1 allowed files/validation
- **plan 落实**：§7 S1 Allowed files 包含 `tests/cli/test_fins_commands.py`（注释"仅同步现有 UF-024 exact reason，不新增 S2 cases"）。§7 S1 Validation commands 的 pytest 包含该文件。§7 S1 change #2 明确 "现有 CLI UF-024 exact reason" 更新
- **终态**：**已修复**。原 review F1 的 S1 红窗口问题消除：S1 修改 usage message 后同步更新 CLI 断言，S1 验证命令可发现

### D-F2 coverage include 集合明确化

- **adjudication 要求**：coverage exact include 固定为实际修改的 production files，不为 `filing_semantics.py` 扩 scope
- **plan 落实**：§8 S2 Coverage 段给出 exact 命令 `--include='dayu/fins/ingestion_runtime.py,dayu/fins/pipelines/docling_upload_service.py,dayu/fins/tools/upload_tools.py'`。紧接说明 "这三个文件是 plan 允许修改的最终 production 集合；filing_semantics.py production 不修改，因此不为本 work unit 扩展其无关 sec-form/date/quality 测试来追逐整文件覆盖率"。§7 S1 change #1 "不修改 filing_semantics.py production"
- **终态**：**已修复**。原 review F2 的 coverage 矛盾消除

### D-F3 tool schema 纳入 S2

- **adjudication 要求**：S2 修改 `upload_tools.py` fiscal_period description，自足列出全部六值
- **plan 落实**：§8 S2 Allowed files 包含 `dayu/fins/tools/upload_tools.py`。§8 S2 change #5 "把 tool schema 的 fiscal_period description 改为自足闭集说明：只支持 FY、H1、Q1、Q2、Q3、Q4，并保持 filing 必填/material 可选的现有 requiredness；在现有 schema contract test 增加 exact assertion"
- **终态**：**已修复**。原 review F3 的 LLM-facing 缺口消除

## 新 Amendment 引入风险检查

### Slice 红窗口

- S1 现在包含 CLI 测试文件，usage message 变更与 CLI 断言更新在同一 slice，无红窗口。S2 新增 US/HK case 在 S1 之后，不影响 S1 绿状态。**无新风险**。

### Owner 边界

- `FiscalPeriod` / `normalize_fiscal_period` 继续由 `filing_semantics.py` 唯一拥有。`FinsUploadUsageCode.UNSUPPORTED_FISCAL_PERIOD` 由 `ingestion_runtime.py` 拥有。两个 owner 边界清晰，无交叉。**无新风险**。

### LLM-facing 文本

- tool schema description 将同步为完整闭集（S2 change #5）。usage message 将改为 market-neutral（S1 change #2）。两个 LLM-facing 投影点的一致性由 plan 保障。**无新风险**。

### Coverage

- coverage include 集合明确为三个实际修改文件。`filing_semantics.py` 不修改、不纳入。baseline 数据：`ingestion_runtime.py` 91%、`docling_upload_service.py` 89%（原 DS review 实测）。`upload_tools.py` 新纳入 S2，其覆盖率需实施时验证，但 plan 已在 S2 Validation commands 中包含该文件的测试。**无新风险**。

### Pyright

- 全仓 baseline 0 errors / 0 warnings / 0 informations（原 review 实测）。类型收窄构造点全仓仅一处（`ingestion_runtime.py:1520`）。S1 范围 pyright + S2 全仓 pyright 兜底。**无新风险**。

### README

- plan §10 Docs decision 正确识别触发规则。S2 包含三个 README 更新。Host/Engine 未修改，不更新其 README。**无新风险**。

## 已验证但未构成 finding 的攻击面

- **adjudication residual risks 清理**：原 residual risks 中 "build ID helper 保留 normalization" 已删除（fix artifact 确认），与 plan §6 decision #4 一致。其余 residual items（UF-PF01/PF12 calibration、frozen evidence、material metadata、download aliases、旧 durable data、AgentCodex 停滞）维持原分类，不构成本 work unit blocker。
- **§6 decision #5 CLI/tool trim helper 层次**：plan 如实描述 "CLI/tool 通用文本 helper 可能在 request 构造前 strip，入口测试只声明'最终 request 共享 canonical contract'"。与原 review Open Question #1 一致。
- **S1 不修改 `filing_semantics.py` production**：plan §7 change #1 明确 "不修改 filing_semantics.py production"，只补 owner tests。与 D-F2 一致。

## Open questions

无 blocking open question。

## Residual risks

| Risk | 分类 | Tracking destination |
|---|---|---|
| 真实 subprocess 的 exit 2/no-traceback/no-mutation 证据未在本 work unit 生成 | `assigned to later work unit` | 用户 UF-PF01/UF-PF12 校准流程 |
| 冻结 evidence/oracle/scenario 未刷新 | `assigned to later work unit` | 用户校准流程 |
| 旧 durable 非法 fiscal period 数据兼容读取 | `assigned to later work unit` | 后续 work unit |
| material fiscal metadata 未统一 | `assigned to later work unit` | 后续 work unit |
| download filter aliases 与 filing closed values 不一致 | `rejected-with-reason`（独立 filter owner 有意承诺） | 无 |
| AgentCodex plan/fix pane 可复现无产出停滞 | workflow execution risk | controller durable artifact |

## Final conclusion

**pass**

全部 8 项 accepted findings（M-001..M-005、D-F1..D-F3）已在 plan 中落实：类型收窄贯穿 derive/builder、exact domain-error mapping 带 None invariant、closed enum/message 显式列出、CLI 测试纳入 S1 消除红窗口、coverage include 集合明确、tool schema 纳入 S2 补齐 LLM-facing 闭集。新 amendment 未引入新的 material finding、slice 红窗口、owner/LLM-facing/coverage/pyright/README 风险。Plan 可进入 accepted plan commit 并开始 S1 实施。
