# Code Review

## Scope

- Mode: current changes
- Branch: `work/wu-obs-00`
- Base: `e1799abc3341872ba19ff609de15b236813a3533`（accepted plan commit）
- Output file: `docs/reviews/code-review-20260724-124007.md`
- Included scope: accepted plan commit 到当前 worktree 的全部 Slice 1 diff，含 staged、unstaged 与 untracked 文件
- Excluded scope: 无（全部改动均在 review 范围内）
- Parallel review coverage: 无

## Reviewed files

Production：

- `dayu/host/__init__.py`（新增 3 个 public export）
- `dayu/host/durable/connection.py`（新增 `HostDurableReadStore`、`open_host_durable_read_store`、read-only helpers）
- `dayu/host/durable/transaction.py`（新增 `configure_read_only_connection_pragmas`、PRAGMA validator）
- `dayu/host/durable/tool_trace.py`（新增 `read_tool_trace_page`）
- `dayu/host/tool_trace.py`（新增 `_tool_trace_cold_lock_path`、`_TOOL_TRACE_LOCK_FILE_SUFFIX`；重构 lock path owner）
- `dayu/host/open_host.py`（删除 `_default_tool_trace_lock_path`、`_TOOL_TRACE_LOCK_FILE_SUFFIX`；复用 owner helper）
- `dayu/host/tool_trace_analysis_contracts.py`（新增：public Source、Policy、InputMode）
- `dayu/host/tool_trace_analysis_input.py`（新增：完整 S1 可信输入读取与完整性边界）

Tests：

- `tests/host/test_durable_connection.py`（新增 read-only store 测试）
- `tests/host/test_tool_trace_projection.py`（新增 lock path owner 测试）
- `tests/host/test_tool_trace_queries.py`（新增 `read_tool_trace_page` 测试）
- `tests/host/test_tool_trace_analysis_input.py`（新增：S1 完整输入测试）

Docs：

- `docs/host/issues-implementation-control.md`（gate 状态更新）

## Verification

- focused tests：`111 passed in 1.13s` ✅
- targeted pyright：`0 errors, 0 warnings, 0 informations` ✅
- full pyright：`0 errors, 0 warnings, 0 informations` ✅（由 Codex implementation artifact 确认）

## Findings

未发现实质性问题。

以下逐项对抗检查全部通过，无 actionable finding：

### 1. public ToolTraceAnalysisSource 严格五字段

`dayu/host/tool_trace_analysis_contracts.py`：`ToolTraceAnalysisSource` 精确五字段：`requested_path`、`mode`、`cold_jsonl_path`、`hot_db_path`、`artifact_root`。无 `cold_lock_path`。`dayu/host/__init__.py` 仅导出 `ToolTraceAnalysisPolicy`、`ToolTraceAnalysisSource`、`ToolTraceInputMode` 三个 class。`_tool_trace_cold_lock_path` 未从 Host root 导出。测试 `test_source_public_contract_has_exactly_five_fields_and_no_lock_export` 断言五字段顺序与 lock helper 不在 `__all__`。

### 2. Host internal lock helper 唯一 owner

`dayu/host/tool_trace.py:263`：`_tool_trace_cold_lock_path` 从 `open_host.py` 的旧 `_default_tool_trace_lock_path` 收敛为 `tool_trace.py` owner 内部 helper。`open_host.py:2604` 改为导入并调用同一 helper。`tool_trace_analysis_input.py:738` 也导入同一 helper。旧 `open_host.py` 中的重复 `_TOOL_TRACE_LOCK_FILE_SUFFIX` 和 `_default_tool_trace_lock_path` 已删除。测试 `test_tool_trace_producer_and_reader_share_adjacent_lock_owner` 断言 producer 与 reader 共用同一路径。

### 3. hot-first 同一 read transaction 和 watermark

`tool_trace_analysis_input.py:548-573`：`_load_hot_snapshot` 在单个 `open_host_durable_read_store` context 内执行 `run_read`，在该 transaction 内分页读取全部 hot rows 并记录 `watermark = rows[-1].event_sequence if rows else 0`。`load_tool_trace_analysis_input` 先调用 `_load_hot_snapshot`，context 关闭后再调用 `_load_cold_snapshot`。hot transaction 与 cold lock 不同时持有。

### 4. cold 锁内仅 binary open+fstat，锁外 same-handle exact-prefix，writer 无 timeout

`tool_trace_analysis_input.py:729-818`：`_capture_cold_prefix` 在 `file_lock` context 内仅执行 `_open_cold_binary_file`（`path.open("rb")`）和 `os.fstat(handle.fileno())`，记录 `prefix_byte_length=st_size` 与 `device/inode` identity，随后释放锁。锁外从同一 handle 调用 `_read_exact_prefix` 精确读取 `prefix_byte_length` bytes。reader 使用 producer 同源的 `_LOCK_TIMEOUT_SECONDS=5.0`。测试 `test_prefix_snapshot_releases_lock_before_real_producer_append` 用 barrier 证明 slow prefix read 不占 producer 的 lock timeout。

### 5. short read/truncate/replacement/close/lock 全 fail closed

- `tool_trace_analysis_input.py:843-845`：`_read_exact_prefix` 在 `chunk == b""` 时 raise `OSError("cold snapshot short read")`。
- `tool_trace_analysis_input.py:796-800`：final fstat 检查 `st_size < prefix_byte_length`。
- 测试 `test_truncate_or_short_read_is_fatal_not_partial_dataset`（truncate → short read → fatal）。
- 测试 `test_snapshot_keeps_open_handle_when_path_is_replaced`（path 替换后保持旧 handle）。
- 测试 `test_cold_handle_close_failure_is_fatal`（close 失败 → fatal）。
- 测试 `test_lock_failures_are_fatal_without_unlocked_fallback`（lock timeout/failure → fatal）。

### 6. 物理 mode=ro/query_only 且无 WAL/bootstrap/DDL/write

- `connection.py:312-325`：`open_host_durable_read_store` 使用 `db_path.as_uri() + "?mode=ro"` URI 模式。
- `transaction.py:492`：`configure_read_only_connection_pragmas` 设置 `query_only=ON` 并验证。
- `transaction.py:473-507`：只读 PRAGMA helper 不设置 `journal_mode=WAL` 或 `wal_autocheckpoint`。
- 测试 `test_read_only_store_is_physical_ro_and_does_not_bootstrap`：monkeypatch 写侧 helper 为 fail → 只读 opener 不调用写侧 PRAGMA；关闭 `query_only=OFF` 后物理仍只读（`DELETE` 抛错）；DB 文件 size 不变。
- 测试 `test_read_only_store_missing_path_does_not_create_database`：缺失路径不创建 DB 或 parent。

### 7. strict current schema parser、digest/ref、hot-cold join、descriptor/runner resolver

- `tool_trace_analysis_input.py:920-1050`：`_parse_cold_record` 严格校验 `schema_version == 1`、字段集合 == `_REQUIRED_COLD_FIELDS`、required/optional 类型、digest 形状、preimage line_digest、cold_trace_digest == line_digest、cold_trace_ref == `tool-trace-cold:<event_id>`。
- `tool_trace_analysis_input.py:1101-1180`：`_join_hot_and_cold` 按 event_id 主键 join，secondary 校验 event_sequence/event_type/event_class/session_id/run_id/cold_trace_ref/cold_trace_digest。
- `tool_trace_analysis_input.py:601-653`：`_read_hot_snapshot_in_transaction` 在同一 transaction 内 resolve payloads 和 runner-call projections。
- 测试覆盖：digest/ref corruption × 3、identity mismatch、duplicate/conflict、hot-only/cold-only、resolver failure。

### 8. hot missing 与 existing-invalid 分类

- `tool_trace_analysis_input.py:514-539`：`_load_hot_snapshot` 对 `FileNotFoundError` 返回 limitation（hot missing），对 `OSError`（metadata unreadable）和 `HostDurableError`/`OSError`（open/schema/corrupt）抛出 fatal。
- 测试 `test_existing_non_dayu_hot_store_is_fatal_not_cold_only`：非 Dayu schema DB → fatal `HOT_STORE_READ_FAILED`。

### 9. hot-empty/cold-late limitation

- `tool_trace_analysis_input.py:645`：`watermark = rows[-1].event_sequence if rows else 0`。
- `tool_trace_analysis_input.py:1156-1180`：cold event_sequence > watermark → `input_changed_during_analysis` limitation；<= watermark 但无 hot → `MISSING_HOT_TRACE` diagnostic。
- 测试 `test_hot_empty_cold_empty_is_normal_and_cold_late_is_all_limited`：hot-empty + cold-late → 全部 limitation。

### 10. 无 S2/Service/CLI creep

diff 中无 `dayu/service/`、`dayu/cli/` 修改。未定义 report/finding/vendor public skeleton。未预定义 `ToolTraceAnalysisReport`、`ToolTraceFinding` 等 S2 类型。`dayu/host/__init__.py` 仅新增 S1 消费的三个 public class。

### 11. 中文 docstring、严格类型、耦合/semantic ownership drift

全部新增 public/private 函数和类提供完整中文 docstring，含参数、返回值、异常。无 `Any`、`object`、无类型参数。`ToolTraceAnalysisSource.__post_init__` 调用 `_validate_source` 进行 mode-specific 布局校验。`_revalidate_source` 在 load 前再次校验路径状态。无 `hasattr`/`getattr`。无兼容性代码。无 semantic ownership drift。

### 12. 测试质量与 coverage

111 个 focused tests 覆盖：public Source 五字段/四模式/布局/存在性/类型矩阵、policy 校验、read-only opener 默认/override timeout/物理只读/query-only/无 bootstrap、unfiltered hot query pagination、production baseline join/resolver、syntax/type/integrity corruption、duplicate/conflict、hot-only/cold-only、identity mismatch、hot-empty/cold-late、barrier 驱动并发、path replacement、truncate/short-read/close/lock fatal matrix、file-only 不打开 ref。8 个修改 production 文件 branch coverage 均 `>=81%`。

## Open Questions

- 无。

## Residual Risk

1. **Validation environment blocker**：当前真实 workspace DB schema=20，current fresh schema=24。这不是代码 finding，是 live validation environment blocker。Controller 已裁决为 `pass-to-review-with-acceptance-blocked`，冻结 Slice 1 acceptance/commit 与 Slice 2。代码本身对 schema mismatch 正确 fail closed（`validate_host_durable_schema` 抛出 `HostSchemaMismatchError` → `HOT_STORE_READ_FAILED` fatal）。

2. **Stale package export test**：`tests/host/test_package_exports.py` 的 `EXPECTED_API_EXPORTS` 尚未包含新增的三个 public export。Controller 已裁决为计划文件清单遗漏，不构成代码缺陷。fix gate 时只需更新该文件的 expected set。

3. **Platform residual**：same-handle exact-prefix、path replacement、truncate/short read 与真实 producer non-interference 已在 macOS/Python 3.11 验证；未获得其它 OS CI 证据。

4. **Live resolver not reached**：live schema gate 在 resolver 之前 fail closed，因此当前 workspace descriptor 的 live resolver 验证尚未发生；unit/fixture resolver contract 已通过。

## Verdict

0 actionable findings。Slice 1 实现严格对齐 accepted plan，对抗检查全部通过。唯一阻塞项为 live workspace schema 20 vs current fresh schema 24 的 validation environment blocker，已由 Controller 裁决为独立于代码正确性的问题。
