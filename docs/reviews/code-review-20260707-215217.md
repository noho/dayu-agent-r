# Code Review

## Scope

- Mode: current changes
- Branch or PR: `phase/host-issues-control`
- Base: current workspace diff
- Output file: `docs/reviews/code-review-20260707-215217.md`
- Included scope:
  - `dayu/config/prompts/manifests/prompt.json`
  - `dayu/config/prompts/base/tools.md`
  - `dayu/service/scene_context.py`
  - `dayu/tools/utils/provider.py`
  - `tests/runtime/test_scene_assets_migration.py`
  - `tests/service/test_entrypoint_runtime.py`
  - `tests/service/test_entrypoint_runtime_prompt_path.py`
  - `tests/service/test_entrypoint_runtime_interactive_path.py`
  - `tests/cli/test_prompt_command.py`
  - `tests/cli/test_interactive_command.py`
  - `tests/runtime/test_scene_prepare.py`
  - `tests/service/test_host_assembly.py`
  - `dayu/config/README.md`
  - `tests/README.md`
- Excluded scope: unrelated repository files and historical `docs/reviews/` artifacts.
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Review Notes

- `prompt.json` 只删除 `"utils"` tag，仍保留 `"fins-read"` 与 `"web"`；没有影响 `interactive.json` / `wechat.json` 的 `"utils"` tag。
- `base/tools.md` 的 `get_current_time` 指引位于 `<when_tool get_current_time>` 条件块内；真实 `prepare_scene` 测试覆盖了 `prompt` 不选择工具时该块被过滤。
- `current_time()` 仍在同一 Service helper 中生成上海时间，只增加 LLM-facing 静态边界说明，没有改变时区转换逻辑。
- `build_get_current_time_tool_definition()` 只修改 schema description，没有改变参数 schema、工具 tags、执行函数或返回 payload。
- 测试同时覆盖 `prompt` 不选择 `get_current_time`、`interactive` / `wechat` 选择该工具、`current_time` 文本边界、工具 description 边界和 CLI / Service 入口组装。

## Open Questions

无。

## Residual Risk

- 未执行真实 LLM 端到端调用；当前由真实 `ScenePrepare` 装配测试和 CLI / Service 入口组装测试覆盖模型可见输入。分类：accepted residual risk for local unit/integration scope。
