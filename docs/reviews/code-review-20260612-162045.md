# Code Review — WU-CM-06-S1 Fix Re-review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: HEAD (dirty workspace diff only)
- Output file: `docs/reviews/code-review-20260612-162045.md`
- Included scope: 4 modified files — 3 test files + 1 control doc
- Excluded scope: 无生产代码变更
- Parallel review coverage: 无

## Review Checklist

### Fix 1: hydration test naming — durable projection adapter ownership

**Finding 001 来源**: `docs/reviews/code-review-20260612-161139.md` Finding 001（低严重度）

**验证结论：修复完成，命名准确反映 adapter ownership。**

旧名称 `test_projection_consumer_hydrates_terminal_content_as_final_answer` 已重命名为 `test_durable_projection_hydrates_terminal_content_as_selected_recent_window`（`tests/host/test_memory_projection.py:386`）。

直接证据：
- 新名称明确指出 "durable projection"（不是 consumer）执行 hydration
- 新名称指出结果进入 "selected_recent_window"（不是 "final_answer"）
- docstring 同步更新为 "durable projection adapter hydrate terminal content 后进入 continuity"
- 测试体新增结构化断言：`assert len(selected) == 1` + `assert selected[0].included_reason is MemoryIncludedReason.SELECTED_RECENT_WINDOW` + `assert evidence_backed_facts == ()`

与 plan 文档一致性：`docs/host/host-issues/wu-cm-06-terminal-summary-text-policy-plan.md:36` 明确 "durable projection / run-input adapters may hydrate descriptor-backed terminal summary artifact content into transient final_answer"。新命名与此一致。

### Fix 2: malformed terminal_summary_digest symmetric coverage

**Finding 002 来源**: `docs/reviews/code-review-20260612-161139.md` Finding 002（低严重度）

**验证结论：修复完成，对称路径已覆盖。**

新增测试 `test_continuity_resolver_rejects_malformed_terminal_digest`（`tests/host/test_terminal_summary_payload.py:319-349`）。

直接证据 — 生产代码 `_terminal_answer.py:69-87`：
- `_optional_descriptor_text` 对 `terminal_summary_ref` 和 `terminal_summary_digest` 使用完全相同的 `isinstance(value, str)` 检查
- `field_name` 作为参数传入，错误消息格式为 `f"payload field {field_name} must be text"`
- 新测试传入 `terminal_summary_ref="payload-terminal-summary"`（合法字符串）+ `terminal_summary_digest=123`（int 非法）
- assert `pytest.raises(HostDurableError, match="terminal_summary_digest")` 正确匹配错误消息

与已有测试 `test_continuity_resolver_rejects_malformed_terminal_descriptor`（line 286）形成对称覆盖：
- 旧测试：ref 为 int，digest 为合法字符串 → match "terminal_summary_ref"
- 新测试：ref 为合法字符串，digest 为 int → match "terminal_summary_digest"

### 控制文档更新

`docs/host/issues-implementation-control.md` 已更新：
- gate 从 "accepted plan commit" 改为 "re-review"
- implementation status 更新为 "WU-CM-06-S1 code review completed；accepted low findings fixed，awaiting re-review"
- next entry point 更新为 "WU-CM-06-S1 re-review gate"
- WU-CM-06 状态段新增 S1 code review artifacts 和 fix 记录

### 未引入新问题检查

- 新增测试函数均有完整类型标注（`-> None`）
- 无 `Any`、`object`、无类型参数
- 测试只新增断言，不修改生产代码
- 旧测试名称在 diff 中只出现 rename，无残留引用

### pytest/pyright 可信

- `pytest tests/host/test_terminal_summary_payload.py tests/host/test_read_api_terminal_policy.py tests/host/test_engine_ingest_mapping.py tests/host/test_memory_projection.py -q`：**95 passed in 0.83s**（原 94 + 新增 1 个 malformed digest 测试）
- `python -m pyright dayu/ tests/ utils/`：**0 errors, 0 warnings, 0 informations**

## Findings

未发现实质性问题。

## Open Questions

无。

## Residual Risk

- S2 docstring 收敛尚未执行：`dayu/host/terminal_summary_payload.py` 和 `dayu/host/_terminal_answer.py` 的 module/function docstring 尚未按 plan 收紧。
- Residual risk 来自前次 review（code-review-20260612-161139.md）的 Residual Risk 段，本次 fix 不涉及，不重复列出。

## Conclusion

**PASS**

两个 accepted findings 均已正确修复：

1. **Finding 001（test naming）**: 重命名为 `test_durable_projection_hydrates_terminal_content_as_selected_recent_window`，准确反映 durable projection adapter ownership 和 selected_recent_window 语义。docstring 和断言同步增强。
2. **Finding 002（malformed digest coverage）**: 新增 `test_continuity_resolver_rejects_malformed_terminal_digest`，覆盖 `terminal_summary_digest` 为 int 的对称路径。与已有 malformed ref 测试形成完整对称覆盖。

控制文档已同步更新 gate 状态。pytest 95 passed / pyright 0 errors，均可信。未引入新问题。
