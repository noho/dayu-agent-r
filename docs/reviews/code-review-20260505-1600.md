# Code Review — 2026-05-05 16:00

**分支**: `refactory/engine_phase1`
**范围**: `dayu/contracts/`、`dayu/engine/`、`tests/`（含全部 Phase 0 契约 + Phase 1 OpenAI Runner）
**验证**: pyright 0 errors / 0 warnings, pytest 200 passed (0.33s)

---

## 1. `dayu/contracts/` — 共享协作契约

**结论: 无问题。**

- 22 个公共符号，`__all__` 与 import 完全一致。
- 零 `Any`、零 `object`、零裸 `dict`/`list`、零无类型签名。
- 六个核心协作契约（`CancellationToken`、`ToolExecutor`、`ToolExecutionRequest`、`ToolResultEnvelope`、`ToolExecutionOutcome`、`ToolAwaitSpec`）正确放置于此。
- 无向上依赖（不 import engine/host/fins/service/UI）。
- 全量中文 docstring，无魔法数字/字符串。

## 2. `dayu/engine/contracts/` — Engine 语义真源契约

**结论: 无问题。**

- 53 个符号，`__all__` 严格匹配。
- `RunnerEvent` 仅含 `type`、`data`、`occurred_at`，不含 Host 治理字段。
- `EngineEvent` 含全部 7 个稳定字段（`event_id`、`sequence`、`occurred_at`、`session_id`、`run_id`、`type`、`data`）+ `metadata`。
- 终态集合 `frozenset({FINAL_ANSWER, RUN_FAILED, RUN_CANCELLED, RUN_SUSPENDED})`，封闭且互斥。
- `ToolExecutionOutcome` 为 3 成员封闭联合。
- `AsyncRunner` Protocol 仅暴露 `call`、`is_supports_tool_calling`、`close` 三个方法。
- 全量强类型，无 `Any`/`object`/裸容器。

## 3. `dayu/engine/runners/openai/` — OpenAI Runner 实现

**结论: 架构合规，协议正确，有 4 个低严重度 finding。**

### 3.1 架构合规（全部 PASS）

- 不 import host/fins/service/trace/tools/processors。
- 仅产出 `RunnerEvent`，不产出 `EngineEvent`。
- 不执行工具，不依赖 `ToolExecutor`/`ToolRegistry`。
- 无 `set_tools`，`call()` 无 `**kwargs`/`**extra_payloads`。
- 不读取配置文件，`RunnerSpec` 来自显式强类型输入。
- Provider 私有参数通过 `ProviderRequestExtension` 封闭联合处理。

### 3.2 状态机（全部正确）

逐路径验证：content-only、reasoning+content、tool call streaming、arguments 分片、usage-only chunk、正常 done、协议错误、HTTP 错误、retry 成功、retry 耗尽、cancel before request、cancel during stream、close 资源释放。全部正确。

### 3.3 Findings

### 1-已修复-低-`_usage_emitted` 死状态
- **入口/函数**: `SSEParser._handle_usage()`
- **文件(行号)**: `sse_parser.py:115`（初始化）、`sse_parser.py:429`（写入）
- **输入场景**: 任何含 usage 的 SSE 流
- **实际分支**: `_usage_emitted` 被设为 `True`，但从未被任何分支读取
- **预期行为**: 若存在防重复发射逻辑，`_finalize_success` 或 `_handle_choice` 应检查此标记
- **实际行为**: 标记仅写不读，是死状态
- **直接证据**: `grep -rn "_usage_emitted"` 仅命中 115 行（init）和 429 行（set True），无读取点
- **影响**: 静默失效 — 无功能影响，但增加维护困惑
- **建议改法和验证点**: 删除 `_usage_emitted` 字段及其赋值；或在 `_finalize_success` 中加入 usage 去重检查（若业务需要）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **修复结果**: 已删除 `_usage_emitted` 字段及其赋值（`sse_parser.py:115` 初始化点 + `sse_parser.py:429` 写入点）。NEW 单次 `call` 实例化新 parser，无需跨调用去重；保留死状态只增加维护困惑。

### 2-已修复-低-取消测试中未使用的 `monkeypatch` 参数
- **入口/函数**: `test_cancel_before_connect_returns_no_done`、`test_cancel_during_sse_chunk_wait_returns_no_done`
- **文件(行号)**: `test_cancellation_boundaries.py:34`、`test_cancellation_boundaries.py:66`
- **输入场景**: 测试运行
- **实际分支**: `monkeypatch` 参数被声明但未使用
- **预期行为**: 未使用的 fixture 参数应移除
- **实际行为**: 参数存在但函数体内无引用
- **直接证据**: 两个函数体内均无 `monkeypatch.` 调用；第三个测试 `test_cancel_during_retry_sleep`（line 140）实际使用了 `monkeypatch.setattr`
- **影响**: 无功能影响，代码清洁度问题
- **建议改法和验证点**: 从两个函数签名中移除 `monkeypatch` 参数；运行测试确认无回归
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **修复结果**: 已从 `test_cancel_before_connect_returns_no_done`、`test_cancel_during_sse_chunk_wait_returns_no_done` 两个函数签名中移除 `monkeypatch` 参数。`test_cancel_during_retry_sleep` 实际使用，未动。

### 3-已修复-低-`# type: ignore[typeddict-item]` 类型摩擦
- **入口/函数**: `SSEParser._coerce_tool_call_delta()`
- **文件(行号)**: `sse_parser.py:352`
- **输入场景**: 含 tool call delta 的 SSE 流
- **实际分支**: `func_payload: dict[str, str]` 被赋值给 `delta["function"]`，触发 TypedDict 兼容性告警
- **预期行为**: 类型安全赋值，无需 `type: ignore`
- **实际行为**: `_OpenAIToolCallFunction` 的 `arguments` 字段为 `total=False`，`dict[str, str]` 结构兼容但 pyright 不认可直接赋值
- **直接证据**: `_OpenAIToolCallFunction` 定义中 `arguments: str` 为 `total=False`；`func_payload` 可能不含 `arguments` key
- **影响**: 类型安全局部削弱，不影响运行时
- **建议改法和验证点**: 将 `func_payload` 类型改为 `_OpenAIOutboundToolCallFunction`（使用 TypedDict 构造）或拆分为显式构造
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **修复结果**: 已把 `func_payload: dict[str, str]` 改为 `_OpenAIToolCallFunction`（已有的 `TypedDict(total=False)`），并补充导入。`# type: ignore[typeddict-item]` 已删除。

### 4-豁免-低-payload 协议字面量未提取为常量
- **入口/函数**: `_serialize_tool_call_to_payload()`
- **文件(行号)**: `payload.py:107`（`"function"`）、`payload.py:271`（`"auto"`）
- **输入场景**: 所有含 tool 的请求
- **实际分支**: 字符串字面量直接写在 payload 构造中
- **预期行为**: 魔法字符串应提取为命名常量（项目编码硬约束）
- **实际行为**: `"function"` 和 `"auto"` 作为字面量直接出现
- **直接证据**: payload.py:107 `{"type": "function", ...}` 和 payload.py:271 `{"tool_choice": "auto", ...}`
- **影响**: 极低 — 这些是 OpenAI API 协议固定值，不会变化
- **建议改法和验证点**: 提取为 `_TOOL_CALL_TYPE_FUNCTION: str = "function"` 和 `_TOOL_CHOICE_AUTO: str = "auto"` 常量；但因属于 tool schema 范畴，按 review 指南可豁免
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **修复结果**: 不修复。`CLAUDE.md` 明确「工具 schema 例外，schema 内允许直接写字面量字符串」，本 finding 自身亦注明「按 review 指南可豁免」。保持原状。

## 4. 测试套件

**结论: 质量高，覆盖全面，有 2 个结构性缺口。**

### 4.1 强项

- 三层 import boundary 测试（contracts → engine → engine/contracts）正确执行 AST 扫描。
- 弱类型守卫覆盖 `Any`、`object`、缺失注解、裸容器泛型。
- 包导出锁定使用严格相等（非子集）。
- 30+ Runner 测试文件覆盖 SSE 解析、错误分类、重试退避、取消、协议对等、事件排序不变量。
- 测试辅助类（`_factories.py`、`_fakes.py`、`_sse_helpers.py`）全量强类型。

### 4.2 缺口

1. **无 Engine loop / 状态机测试**: Runner 测试详尽，但消费 `RunnerEvent`、分派 tool call、管理 suspend/resume 的 Engine 主循环无测试覆盖。`tests/README.md` 提到"状态机测试必须覆盖输入事实、状态分支、事件顺序、终态收口"，但当前无此类测试。

2. **弱类型守卫无负面测试**: AST 扫描逻辑本身无注入违规的验证测试。若扫描器有静默跳过的 bug，只要生产代码碰巧干净，测试仍会通过。

---

## 汇总

| 编号 | 严重程度 | 修复风险 | 简述 | 状态 |
|------|---------|---------|------|------|
| 1 | 低 | 低 | `_usage_emitted` 死状态 | 已修复 |
| 2 | 低 | 低 | 取消测试未使用 `monkeypatch` 参数 | 已修复 |
| 3 | 低 | 低 | `# type: ignore[typeddict-item]` 类型摩擦 | 已修复 |
| 4 | 低 | 低 | payload 协议字面量（tool schema 豁免） | 豁免（不修复） |

**阻塞问题**: 无。
**架构违规**: 无。
**总体评价**: 代码质量高，架构分层严格，类型系统完备，测试覆盖全面。3 个 low finding 已在本次修复中清理，第 4 项按编码规范豁免。Engine loop 状态机测试缺口是当前最大的结构性风险，建议在 Phase 2 启动前补齐。

**修复后验证**:

```text
source .venv/bin/activate && pytest tests/contracts tests/engine -q
200 passed in 0.33s

source .venv/bin/activate && pyright
0 errors, 0 warnings, 0 informations
```
