# Code Review

## Scope

- Mode: current changes
- Branch: `work/cm-05-06-08-09`
- Base: HEAD (dirty workspace diff only)
- Output file: `docs/reviews/code-review-20260612-161139.md`
- Included scope: 4 modified files + 1 new file（test-only diff）
- Excluded scope: 无
- Parallel review coverage: 无。review scope 较小且集中于测试，单 reviewer 逐条走读即可覆盖。

## Findings

### 001-未修复-低-test_memory_direct_consumer 测试命名存在轻微偏差

- **入口/函数**: `test_memory_direct_consumer_does_not_follow_terminal_descriptor` — `tests/host/test_memory_projection.py:468`
- **文件(行号)**: `tests/host/test_memory_projection.py:468-510`
- **输入场景**: 构造一个 `RUN_SUCCEEDED` 的 `MemoryProjectionEvent`，其 payload 包含 `terminal_summary_ref`、`terminal_summary_digest`、`content`、`summary_text` 但无 `final_answer` 字段，直接调用 `build_conversation_memory_snapshot_from_events`。
- **实际分支**: `project_conversation_memory_event` → `_selected_assistant_item` → `assistant_final_answer_text_from_run_payload` 读取 `final_answer` 字段 → 缺失返回 `None` → `_selected_assistant_item` 返回 `None` → 不追加 selected_recent_window item。
- **预期行为**: 测试正确断言 `selected_recent_window == ()` 且 `evidence_backed_facts == ()`。行为正确。
- **实际行为**: 行为正确。
- **直接证据**: 测试用例名称为 `test_memory_direct_consumer_does_not_follow_terminal_descriptor`，与附件测试 `test_projection_consumer_hydrates_terminal_content_as_final_answer`（`tests/host/test_memory_projection.py:453`）命名形成对比时，后者名称暗示 "consumer hydrates terminal content as final answer"——但实际 hydration 由 durable projection / run-input adapter 完成，consumer 只读取 inline `final_answer`。Plan 文档 `docs/host/host-issues/wu-cm-06-terminal-summary-text-policy-plan.md:36` 已明确："durable projection / run-input adapters may hydrate descriptor-backed terminal summary artifact `content` into transient `final_answer`"。
- **影响**: 仅命名可读性。测试断言本身正确，不会误导行为验证。
- **建议改法和验证点**: 将 `test_projection_consumer_hydrates_terminal_content_as_final_answer` 更名为更精确的名称，例如 `test_durable_projection_adapter_hydrated_final_answer_enters_as_selected_recent_window`。此为 Slice 2 docstring 收敛范围，可在 S2 一并处理。
- **修复风险（低）**: 仅改名，无行为变更。
- **严重程度（低）**: 不影响正确性。

### 002-未修复-低-缺少 malformed terminal_summary_digest 的单独覆盖

- **入口/函数**: `test_continuity_resolver_rejects_malformed_terminal_descriptor` — `tests/host/test_terminal_summary_payload.py:286`
- **文件(行号)**: `tests/host/test_terminal_summary_payload.py:286-316`
- **输入场景**: `terminal_summary_ref` 为 `123`（int），`terminal_summary_digest` 为合法字符串。
- **实际分支**: `_optional_descriptor_text` 对 `terminal_summary_ref` 的值 `123` 执行 `isinstance(value, str)` 检查 → `False` → 抛出 `HostDurableError("payload field terminal_summary_ref must be text")`。
- **预期行为**: 测试正确断言 `pytest.raises(HostDurableError, match="terminal_summary_ref")`。
- **实际行为**: 行为正确。
- **直接证据**: `_terminal_answer.py:83` — `_optional_descriptor_text` 对 `terminal_summary_ref` 和 `terminal_summary_digest` 使用相同的类型检查逻辑。当前测试只覆盖了 `terminal_summary_ref` 为 int 的场景，未覆盖 `terminal_summary_digest` 为 int 且 `terminal_summary_ref` 为合法字符串的场景。两条路径共享同一个 helper 函数，行为一致，但缺少反向覆盖。
- **影响**: 极低。两条路径调用完全相同的 `_optional_descriptor_text` helper，只是 `field_name` 参数不同。如果真的出现 regressions（例如 helper 被拆分），当前测试不会捕获 `terminal_summary_digest` 侧的退化。
- **建议改法和验证点**: 新增一个测试或扩展现有测试，覆盖 `terminal_summary_digest` 为 int 的场景。预期行为相同（`HostDurableError` with match `"terminal_summary_digest"`）。
- **修复风险（低）**: 仅新增一个 assertion，无行为变更。
- **严重程度（低）**: 对称路径缺失覆盖，风险极低。

## Open Questions

无。

## Residual Risk

- **Slice 2 docstring 收敛尚未执行**：`dayu/host/terminal_summary_payload.py` 和 `dayu/host/_terminal_answer.py` 的 module/function docstring 尚未按 plan 收紧为明确的 "assistant final-answer continuity text source selection" 语义。当前 S1 只覆盖测试，不涉及生产代码变更。S2 执行后需重新验证 docstring 与 policy matrix 的一致性。
- **Run input adapter hydration 路径未在 S1 测试**：plan 要求 "Strengthen the existing durable projection test proving descriptor-backed terminal summary artifact `content` is hydrated into transient `final_answer`"——当前 `test_projection_consumer_hydrates_terminal_content_as_final_answer` 通过 fixture `_make_hydrated_events` 间接覆盖了 hydration 结果，但未直接测试 `run_input` 模块中的 hydration 逻辑。若 hydration adapter 被意外修改，现有测试可能仍然通过（因为 fixture 直接构造了已 hydration 的 payload）。
- **Overlong text truncation 验证不在 scope**：plan 明确声明 "Caller-side truncation verification is out of scope for WU-CM-06"。当前测试只验证 source-selection helper 保留长文本，不验证各 caller（display、storage、budget）是否正确截断。此项风险由各 caller 的 budget/display 测试承担。
- **未覆盖 plan 中的 "descriptor incomplete — malformed" 复合场景**：当前 `test_continuity_resolver_requires_complete_terminal_descriptor` 覆盖了 descriptor 缺一侧（missing），`test_continuity_resolver_rejects_malformed_terminal_descriptor` 覆盖了字段类型非法。但未覆盖 "一侧 missing + 另一侧 malformed" 的复合场景（例如 `terminal_summary_ref` 缺失但 `terminal_summary_digest` 为 int）。此场景在实际 EventLog 损坏时可能出现，当前行为取决于字段检查顺序（ref 先于 digest），先命中的 malformed 检查会抛错，后命中的 missing 检查不可达。此路径顺序依赖尚未被测试固化。

## Validation Summary

- **pytest**: `tests/host/test_terminal_summary_payload.py tests/host/test_read_api_terminal_policy.py tests/host/test_engine_ingest_mapping.py tests/host/test_memory_projection.py -q` → **94 passed in 0.83s**。
- **pyright**: `dayu/ tests/ utils/` → **0 errors, 0 warnings, 0 informations**。

## Conclusion

**PASS**

S1 Policy Matrix Tests 正确覆盖了 WU-CM-06 plan 中定义的全部 5 个测试维度：

1. read_api `EventLogRow → HostEvent` projection：RUN_FAILED / RUN_CANCELLED / RUN_LOST 的 `final_answer=None` 通过真实 `_host_event_from_row` 路径验证，非 dataclass 构造测试。
2. terminal summary payload 测试：preference（inline final_answer 优先于 artifact）、descriptor incomplete（缺一侧返回 None）、descriptor malformed（类型非法抛 HostDurableError）、overlong preserved 均已覆盖。
3. empty_final_answer governance shape：ATTEMPT_FAILED 和 RUN_FAILED 的 payload 均不包含 `content` 或 `final_answer`。
4. memory projection assertions：assistant terminal text 只进入 `selected_recent_window` with `SELECTED_RECENT_WINDOW` reason；direct consumer 不跟随 terminal summary descriptor；hydration 后的 content 不成为 evidence-backed fact。断言方向符合 consumer/adapter 分离事实，无误写反。
5. 未新增弱类型、无类型签名、Any 或 object。新增测试文件 `test_read_api_terminal_policy.py` 全部函数带完整类型注解。README 更新决策符合 plan 触发规则。
6. pytest 94 passed / pyright 0 errors，均可信。
