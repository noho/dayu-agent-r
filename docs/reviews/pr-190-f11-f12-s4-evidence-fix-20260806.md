# PR 190 F11/F12 S4 evidence fix（2026-08-06）

## Gate 与范围

- Work unit：PR 190 F11/F12 S4 real-provider observation evidence fix。
- Current gate：fix complete；next entry point 是总控双路 re-review。
- Review inputs：`docs/reviews/pr-190-f11-f12-s4-evidence-mimo-review-20260805.md`、`docs/reviews/pr-190-f11-f12-s4-evidence-ds-review-20260806.md`。
- Adjudication：`docs/reviews/pr-190-f11-f12-s4-evidence-review-adjudication-20260806.md`。
- Reviewed/fixed target：`docs/reviews/pr-190-f11-f12-s4-real-provider-observation-20260805.md`。
- Immutable evidence root：`/Users/leo/workspace/.dayu-cli-ci/interactive-memory-v3-20260805T-s4-restart-uOZytY`。
- Root `digest.json` SHA-256：`38f0b01f12c2ab55ce1af3c16080b71013d1a19512d65051f5532b747f71da0d`。
- Allowed changes：repo observation artifact 的可审计勘误与本 durable fix artifact。
- Excluded scope：immutable evidence root、production、tests、oracle/scenario/registry、README；不运行 provider，不调用 reviewer，不 commit/push。

## First-principles 与 owner 判定

问题真实存在，但仅限证据可复核性：repo observation 使用的 `0f9c...` 派生 digest 无法从 bundle 的 machine-readable evidence 直接定位，因此不能作为 same-boundary truth。该语义的 canonical owner 是单一 `CONTEXT_COMPACTION_REQUESTED` operation 及其冻结材料摘要；修复应位于 repo observation/fix artifact，不应回写已封存 evidence 或改动运行时。

其余 findings 要么比较了不同生命周期阶段的投影，要么把配置 selector 当作实际调用身份，要么与直接 Memory 数据或 Host 计量规则相反。它们不构成 production bug；仅在 observation 中补充最小 owner 边界说明，避免读者重复混淆。该方案没有引入新契约、兼容分支或运行时改动，因而不存在超出当前 evidence fix 所需的设计。

## Finding 裁决与直接证据

| Finding | 裁决 | Fix 状态 | 直接证据与处理 |
| --- | --- | --- | --- |
| MiMo-01：fallback 应改为 selected=8/dropped=0 | rejected-with-reason | 证据失效 | `evidence/03-mimo-exhausted-fallback/compact-eventlog.json` 与 `evidence/08-deepseek-exhausted-fallback/compact-eventlog.json` 的 canonical `CONTEXT_COMPACTION_FAILED.payload.fallback_input_window` 均为 selected 9、dropped 2；对应 `memory.json.snapshot.trace_memory.selected_recent_window` 均为 8 items，属于 dispatch 后另一投影，且不拥有 `dropped_block_ids`。保留 9/2，并在 observation 明确 owner boundary。 |
| MiMo-02：repair source material digest 不可由 machine-readable bundle 定位 | accepted | 已修复（待双路 re-review） | `evidence/06-deepseek-bounded-repair/compact-eventlog.json` 的 canonical request event/operation 均为 `event-context-compact-requested-7aea6b1297414d9fb79656dd80b254ff`，`payload.frozen_material_list_digest` 为 `sha256:b798e8e51bb7e3a9f16c5f27a2e55cf11ec3e43c2a4c3a55de873a786bfe25ee`；public runner projections 显示 attempt 1/2 均绑定该 operation。observation 已移除 `0f9c...` 声明，改引 canonical field/value，并说明 immutable `observed-report.md` 不回写。 |
| DS-01：assembly diagnostic 应显示 DeepSeek model | rejected-with-reason | 证据失效 | `screen/04-deepseek-baseline.txt` 的 `compactor_model_id=mimo-v2.5-pro-plan` 是 assembly selector；`workspaces/deepseek-session/config/models.json` 明确配置该 selector `extends=deepseek-v4-flash`；`evidence/04-deepseek-baseline/compactor-attempts.json` 与 provider identity 记录 actual `deepseek/deepseek-v4-flash`。observation 已简短区分 selector 与 actual truth。 |
| DS-02：baseline 与 replacement Memory summary 均为 null | rejected-with-reason | 证据失效 | `evidence/04-deepseek-baseline/memory.json.snapshot.session_summary_memory.summary_text` 非空；`evidence/05-deepseek-replacement-constrained/memory.json` 对应字段才为 `null`。直接 before/after 数据证明 replacement clear，不需要修复。 |
| DS-03：answer anchor 36 chars 与 detail 28 不一致 | rejected-with-reason | 证据失效 | attempt 1 的 title 为 7 chars、detail 为 28 chars；owner 规则按 `title + "\n" + detail` 计量，故为 7 + 1 + 28 = 36，repair feedback 同样记录 `36 > 30`。observation 已补全公式，不改变 cap rejection 结论。 |

## Open question 裁决

- System prompt digest：完整 messages 已在 bundle 中，可逐 attempt 复核；initial/repair 差异位于 user projection，不是本 fix blocker。
- Repair binding：以 canonical request operation、`frozen_material_list_digest` 与两个 attempt 的 operation binding 为真源，不再依赖报告作者的派生 hash。
- Private SQLite：仅承担隔离审计，不是 public F11 evidence；本 fix 未读取或修改 quarantine 内容。

## Changed files 与 docs decision

- 修改 `docs/reviews/pr-190-f11-f12-s4-real-provider-observation-20260805.md`：修正 MiMo-02，并加入 fallback projection、selector/effective identity、36-char 公式澄清。
- 新增 `docs/reviews/pr-190-f11-f12-s4-evidence-fix-20260806.md`：持久化 finding 裁决、直接证据、验证与 gate 状态。
- 未命中项目 README 更新触发条件；无需修改 README。

## Validation

- 已复核 immutable root `digest.json` SHA-256 与裁决值一致。
- 已从 machine-readable evidence 复核 repair operation、`frozen_material_list_digest`、两次 attempt operation binding、两类 fallback terminal 的 9/2、post-dispatch Memory 的 8 items、DeepSeek selector extends、actual provider/model、summary before/after 与 36-char 计量公式。
- 文档引用存在性核验通过：两路 review、总控裁决、observation、fix artifact 及其引用的 machine-readable evidence/config/report 文件均存在。
- Root manifest/hash 完整性核验通过：`digest.json` 自身 SHA-256 精确匹配裁决值，声明/实测均为 159 个 covered entries，159 项逐文件 SHA-256 全部通过，root 总文件数为 160（含 self-excluded `digest.json`）。
- `git diff --check` 通过；因本 gate 文档均为未跟踪文件，另以 `git diff --no-index --check /dev/null <file>` 核验本次修改的 observation 与新增 fix artifact，均无 whitespace error。
- 未运行测试或 pyright：本 gate 只修改 review 文档，没有 production/test/type surface 变化；未运行 provider。

## Residual risks / uncovered areas

- Formal oracle 仍为 PENDING；分类为 `assigned to later work unit`，owner 是后续独立 oracle gate。
- 双路 re-review 尚未执行；这是当前 fix 后的必经 next gate，不是已通过状态。本轮按总控指令停在 re-review 入口。
- 未发现需要新 issue 或用户裁决的未分类 residual risk。

## Completion status

- Fix status：MiMo-02 已按总控裁决修正；其余 findings 已按直接 owner/canonical evidence 记录为 rejected-with-reason。
- Gate decision：尚未通过 evidence review loop；必须等待 MiMo、DS 两路分别 re-review 并均接受。
- Artifact path：`docs/reviews/pr-190-f11-f12-s4-evidence-fix-20260806.md`。
- Stop condition：用户明确要求不调用 reviewer，并在总控双路 re-review gate 停止；未 commit/push。
