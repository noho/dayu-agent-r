# WU-CTX-01 Slice 1 Plan Amendment — Directed Code-Generation-Readiness Re-Review

## Metadata

- **review date**: 2026-07-24
- **reviewer**: AgentDS (Claude Code review lane)
- **review type**: 定向 plan re-review（仅复核 CTRL-PR-01~04 与 DS accepted findings）
- **target plan**: `docs/reviews/wu-ctx-01-plan-codex.md`（2876 行）
- **amendment handoff**: `docs/reviews/wu-ctx-01-slice-1-first-call-producer-plan-amendment-codex.md`
- **Controller adjudication**: `docs/reviews/wu-ctx-01-slice-1-first-call-producer-plan-review-controller-adjudication.md`
- **design truth**: `docs/host/design.md` §25 (line 3556–3755)
- **code baseline**: branch=`feat/wu-ctx-01`, HEAD=`5afe71fe`
- **上轮 DS review**: `docs/reviews/plan-review-20260724-014307.md`
- **上轮 MiMo review**: `docs/reviews/plan-review-20260724-014131.md`

## Re-Review Scope

只复核以下维度，不重复上轮已通过的假设：

1. **CTRL-PR-01**：failed wait 被错误纳入 resume eligibility → 是否已收窄为 completed/cancelled
2. **CTRL-PR-02**：source policy strict load 循环依赖 → call graph 是否无循环
3. **CTRL-PR-03**：direct queue promotion production bypass → 所有 production symbols 与专属 tests 删除 allowlist 是否完整，scheduler queued ordinary/wake-only 是否自足
4. **CTRL-PR-04**：Slice 1/2/3 边界、new fact identity 与 usage absence conservative fallback 是否无混淆
5. **DS-PR-001**：5-stage/15-cell exhaustive match 实现约束是否到位
6. **DS-PR-002**：`SessionContinuityView.source_refs` 全 construction-site 审计是否完整
7. **DS-PR-003**：promotion 删除的 transition 路径与 test migration 粒度是否足够
8. **DS-PR-004**：test allowlist 去重
9. **DS-PR-005**：recorder identity type-safety（Controller rejected）
10. **DS-PR-006**：wait payload projection 类型（Controller accepted clarification）
11. **DS-PR-007**：steer rollback/idempotency（Controller rejected）
12. **DS-PR-008**：source helper 用途（已纳入 CTRL-PR-02）
13. **DS-PR-009**：第三项产品修改（Controller rejected clarification）

附加挑战维度：是否仍需临场架构发明；是否误删共享类型或遗漏 export/import/README/test。

## Production Direct Evidence

本轮复核使用的 production 直接证据：

| 证据 | 文件 | 关键事实 |
| --- | --- | --- |
| wait routing | `waiting.py:979-997` | `(ResolveWaitCompletedOutcome, ResolveWaitCancelledOutcome)` → `_resolve_resume`；`ResolveWaitFailedOutcome` → `_resolve_failed`；`ResolveWaitLostOutcome` → `_resolve_lost`。confirmed：仅 completed/cancelled 创建 resume Attempt。 |
| `promote_queued_run_in_transaction` | `run_transition.py:1784-1863` | 绕过 governance 直接创建 `RUN_STARTED`、`ATTEMPT_STARTED`、pending dispatch row；使用 `_promotion_run_started_event_request`（line 3582）、`_promotion_attempt_started_event_request`（line 3620）、`_promotion_attempt_row`、`_promotion_dispatch_record_row`、`promote_queued_run_row`（`state.py`）。 |
| admission promotion method | `admission.py:870-904` | `promote_next_queued_run` 调用 `_PromoteNextQueuedRunOperation`（line 1483），后者调用 `promote_queued_run_in_transaction`。 |
| `SessionContinuityView` construction sites | `run_input.py:1239,1240,2197`; `tests/host/test_run_input_builder.py:643`; `tests/host/test_tool_trace_queries.py:919` | 当前全用 `SessionContinuityView(messages=...)`，无 `source_refs`。 |
| `ContextSizingStage` enum | `context_budget.py:83-89` | 当前 4 值：`ORDINARY`, `POST_COMPACT`, `REACTIVE_POST_COMPACT`, `DISPATCH_FALLBACK`。无 `CONTINUATION`。 |
| `_pressure_and_decision` | `context_budget.py:815-852` | 当前非穷举：hard 只对 `REACTIVE_POST_COMPACT` 特殊处理，soft 只对 `ORDINARY` 特殊处理；其余走 default branch。 |
| `create_host_admission_service` signature | `admission.py:907-919` | 当前无 `payload_store`、`context_budget_policy`、`memory_projection_policy`、`enable_truncation_manager`、`owner_host_instance_id`。 |
| Engine continuation sizing | `engine_ingest.py:6509` | 当前硬编码 `sizing_stage=ContextSizingStage.ORDINARY`。 |

## Controller Finding Re-Review

### CTRL-PR-01: failed wait 被错误纳入 resume eligibility → FIXED

- **plan 证据**：
  - §5.3.1 wait resume 行："wait completed/cancelled resume...failed/lost不调用continuity helper，不写candidate/manifest，也不创建new Attempt"
  - §5.3.1 failure matrix："wait failed / lost outcome → 保持existing terminal failure/lost owner；零continuation manifest、零new Attempt、零pending dispatch"
  - §6.5："wait completed/cancelled: include typed planned accepted-result continuity before start; failed/lost stay terminal with no manifest/new Attempt"
  - §8.2 exact change #22："wait resume只覆盖completed/cancelled...failed/lost保持existing terminal owner且零manifest/new Attempt"
  - §8.3 exact change #11："failed/lost仍零fact/manifest/new Attempt"
- **production 证据对照**：`waiting.py:979-988` 已正确路由 `(CompletedOutcome, CancelledOutcome)` → `_resolve_resume`，`FailedOutcome` → `_resolve_failed`。plan 收窄与 production truth 一致。
- **verdict**：**FIXED**。closed set 收窄为 completed/cancelled，反例（failed/lost 零 manifest/new Attempt）已编码到 owner-level assertions。

### CTRL-PR-02: source policy strict load 循环依赖 → FIXED

- **plan 证据**：
  - §5.3.1 定义 `load_run_input_policy_snapshot_in_transaction(transaction, event_log_store, *, run)`：从 `RunRow.input_event_id` → `USER_INPUT_ACCEPTED` → `effective_execution_config` → shared strict parser → `PolicySnapshot`
  - §5.3.1 定义 `load_prepared_runner_call_source_in_transaction(transaction, event_log_store, *, run_id, attempt_id, execution_id)`：先调 policy helper，再 strict-load manifest/candidate 并逐项校验 policy ref/digest、request-semantics digest
  - §5.3.1 worker loader："保留现有caller参数以校验Attempt-frozen dispatch truth，但实现必须先委托同一strict source helper，再额外比较caller policy的typed equality"
  - §8.2 exact change #20："startup recovery先由RunInput helper从source Run当前`input_event_id`的exact `USER_INPUT_ACCEPTED.effective_execution_config`经共享strict parser重建policy，再strict-load source candidate+sizing"
  - design §25（line 3621）已同步此链路
- **call graph 验证**：`RunRow.input_event_id` → `EventLogStore` 读取 exact event → `effective_execution_snapshot_from_json` → `PolicySnapshot` → `load_prepared_runner_call_source_in_transaction` → validate manifest/candidate → worker loader delegate + additional caller policy check。**无循环**：policy 始终从 source input event 重建，不依赖 candidate/manifest 内部仅存的 ref/digest。
- **verdict**：**FIXED**。call graph 闭环且无循环。

### CTRL-PR-03: direct queue promotion production bypass → FIXED

- **plan 证据**：
  - §7.1 `run_transition.py`："删除`PromoteQueuedRunInput`、`PromotionResult`/`PromotionSkipReason`、`promote_queued_run_in_transaction`及其专属validation/event/row helpers"
  - §7.1 `durable/state.py`："删除仅服务legacy direct queue promotion的row mutation"
  - §8.2 exact change #19 完整列出删除项：`promote_next_queued_run`、`_PromoteNextQueuedRunOperation`、admission/durable promotion result/skip/input types、`promote_queued_run_in_transaction`、`promote_queued_run_row` 及专属 helpers/exports
  - §8.2 exact change #19 明确 tests 迁移："删除`test_admission_queue.py`、`test_admission_multiprocess.py`、`test_run_attempt_transitions.py`、`test_state_schema.py`中仅证明该旁路的cases/imports/fixtures"
  - §8.2 owner-level assertion："admission direct promotion method、durable transition、专属types/helpers/exports均不存在"
  - §9.4 static audit：`rg -n "promote_next_queued_run|promote_queued_run_in_transaction|promote_queued_run_row|PromoteQueuedRunInput|PromotionSkipReason" dayu/host tests/host` → 预期 exit 1
  - 明确撤销 `run_transition.py` 零 diff 承诺："`run_transition.py`只允许promotion专属删除diff"
- **production 证据对照**：
  - `admission.py:870` `promote_next_queued_run` ✓ 在删除列表
  - `admission.py:1483` `_PromoteNextQueuedRunOperation` ✓ 在删除列表
  - `run_transition.py:1784` `promote_queued_run_in_transaction` ✓ 在删除列表
  - `run_transition.py:3582` `_promotion_run_started_event_request` → 属于"专属event helpers"
  - `run_transition.py:3620` `_promotion_attempt_started_event_request` → 同上
  - `run_transition.py:1838` `_promotion_attempt_row` → 属于"专属row helpers"
  - `run_transition.py:1845` `_promotion_dispatch_record_row` → 同上
  - `state.py` `promote_queued_run_row` ✓ 在删除列表
- **完整性验证**：所有符号可经 `promote_queued_run_in_transaction` 的调用关系追溯识别。scheduler `_read_startable_run` 已选择 `QUEUED` → ordinary governance 路径，terminal/recovery `wake_queue_promotion` 只唤醒 scheduler，不自建 Attempt/pending。closed。
- **minor gap（见下）**：`__init__.py` 的 promotion re-exports 清理未进入 Slice 1 allowlist。

### CTRL-PR-04: Slice 1/2/3 边界混淆 → FIXED

- **plan 证据**：
  - amendment handoff §4.3 明确三 slice 边界
  - §8.1 切分依据完整
  - §8.2 Slice 1 objective："所有当前dispatch-relevant sizing仍先保持`conservative_fallback`，不实现canonical context-budget fact或public view"
  - §8.3 Slice 2 prerequisite："Slice 1 accepted...本slice不依赖任何usage anchor"
  - §8.4 Slice 3 prerequisite："Slice 1、2 accepted...只允许method/value从同一result自然变为anchored"
  - §5.3.1："Slice 1只冻结candidate、conservative sizing atoms...Slice 2才为每条完整sizing路径在manifest后、start/link前写canonical fact；startup从matching source manifest/fact校验并复用canonical sizing atoms，以new Attempt identity和`CONTINUATION`重新派生action、追加新fact，不复用source fact identity。Slice 3才允许eligible complete新candidate使用usage anchor"
- **verdict**：**FIXED**。三 slice 边界明确，new fact identity、usage absence fallback 无混淆。

## DS Accepted Findings Re-Review

### DS-PR-001: exhaustive match → FIXED
- plan §5.5："production实现必须显式穷举五个stage和三种pressure，不能靠generic/default fall-through恰好得到表中action...Python 3.11实现用closed `match`或等价显式分支，穷尽后对不可能值使用`assert_never`/明确异常fail closed"
- plan §8.2 exact change #3："action必须显式穷举5-stage/15-cell，unknown value fail closed，不能依赖default fall-through"
- plan §8.2 owner-level assertions："实现显式穷举五stage，unknown值fail closed且不能落入generic branch"
- **verdict**：**FIXED**。实现约束已到位，不再依赖 default branch 巧合。

### DS-PR-002: source_refs construction sites → FIXED
- plan §8.2 exact change #25："`SessionContinuityView.source_refs`不设默认值；审计并更新全部production/test construction site，ordinary显式传`()`，wait传exact request/result refs"
- plan §9.4 static audit：`rg -n "SessionContinuityView\\(" dayu/host tests/host`
- production audit 确认 5 个 construction site（`run_input.py:1239,1240,2197`；`test_run_input_builder.py:643`；`test_tool_trace_queries.py:919`），plan 已全覆盖。
- **verdict**：**FIXED**。

### DS-PR-003: promotion 删除粒度 → FIXED（合并至 CTRL-PR-03）
- plan exact change #19 现在详细列出删除项与 test migration 方向。
- **verdict**：**FIXED**。

### DS-PR-004: allowlist 去重 → FIXED
- plan §7.3、§8.2、§8.3、§8.4 的 test lists 已去重。
- **verdict**：**FIXED**。

### DS-PR-005: recorder type-safety → CONTROLLER REJECTED
- Controller 裁决：为两个字符串新增 value object 是过度设计。plan 改为要求 identity mismatch fail closed 和 tests，不引入新 bag/type。
- **verdict**：**ACCEPTED AS REJECTED**。实现 agent 按 Controller 裁决执行即可。

### DS-PR-006: wait payload projection → CONTROLLER ACCEPTED CLARIFICATION
- Controller 要求 `run_input` 复用 existing accepted-result strict projection。plan §5.3.1 现在要求 `project_wait_resume_continuity` 只消费 typed `AcceptedToolResultProjection`，只接受 completed/cancelled status。`accepted_result_projection.py` 暴露 shared strict core，planned 和 committed path 都委托同一 core。
- **verdict**：**FIXED**。typed projection boundary 清晰。

### DS-PR-007: steer rollback/idempotency → CONTROLLER REJECTED
- Controller 裁决：第一次 transaction rollback 后没有 durable duplicate；existing transaction-local idempotency record 与 CAS 足够。
- **verdict**：**ACCEPTED AS REJECTED**。

### DS-PR-008: source helper 用途 → FIXED（合并至 CTRL-PR-02）
- plan §5.3.1 明确 source helper 是 existing loader 的严格超集；policy 先从 exact source input fact strict 重建，旧 loader 委托 source helper 并校验 caller policy identity。
- **verdict**：**FIXED**。

### DS-PR-009: 第三项产品修改 → CONTROLLER REJECTED
- Controller 裁决：compact source boundary / memory pruning 是 exact candidate foundation 对既有 design truth 的 owner 修复，不新增 WU 第三个 public product contract。plan 保留"两项独立产品修改 + 必要 owner 修复"表述。
- **verdict**：**ACCEPTED AS REJECTED**。

## Additional Findings

### DS-PR-RE-001-未修复-低-`__init__.py` promotion re-exports 清理未进入 Slice 1 allowlist

- **位置**: §7.1（`__init__.py` 在"public Host type exports"）vs §8.2 Slice 1 allowed production files（不含 `__init__.py`）
- **问题类型**: 不可直接实施
- **当前写法**: §8.2 exact change #19 要求删除 `PromotionResult`、`PromoteQueuedRunInput` 等 public types 及其 exports。§7.1 列出 `__init__.py` 为"public Host type exports"。但 Slice 1 allowed production files **未包含** `dayu/host/__init__.py`。Slice 2 的 allowed files 才包含它。
- **反例/失败场景**: Slice 1 删除 `PromotionResult`、`PromoteQueuedRunInput`、`PromotionSkipReason` 后，若 `__init__.py` 仍 re-export 这些符号，Python import 将在 Slice 1 完成时失败（被删符号不再存在）。按 plan 的 stop condition，任何 allowed files 外 production 修改都触发 stop。实现 agent 面临两难：要么在 Slice 1 修改 `__init__.py`（违反 allowlist），要么 Slice 1 留下 broken re-exports 直到 Slice 2（不可接受）。
- **直接证据**: plan §7.1 line 1752-1753 "`dayu/host/__init__.py` — public Host type exports"；plan §8.2 slice 1 allowed production files（line 1927-1949）不含 `__init__.py`；plan §8.3 slice 2 allowed production files（line 2267-2284）包含 `dayu/host/__init__.py`。
- **影响**: 低。实现 agent 自然会判断应将 `__init__.py` 加入 Slice 1 allowlist 以同步删除 re-exports，不构成实质性 blocker。
- **建议改法和验证点**: 将 `dayu/host/__init__.py` 加入 §8.2 Slice 1 allowed production files；或在 exact change #19 中注明"同步删除 `__init__.py` 中 promotion 相关 re-exports，本改动为 Slice 1 allowed scope 的 implicit extension"。
- **修复风险**: 低
- **严重程度**: 低

### DS-PR-RE-002-unresolved-低-`_promotion_*` 私有 helper 函数未穷举名称

- **位置**: §7.1、§8.2 exact change #19
- **问题类型**: 不可直接实施（轻微）
- **当前写法**: plan 要求删除"专属validation/event/row helpers"，但未穷举 `_promotion_run_started_event_request`（line 3582）、`_promotion_attempt_started_event_request`（line 3620）、`_promotion_attempt_row`、`_promotion_dispatch_record_row`、`_validate_promote_input` 等私有函数的具体名称。
- **反例/失败场景**: 实现 agent 可能删除 `promote_queued_run_in_transaction` 和 `PromoteQueuedRunInput` 等 public symbols，但遗漏个别只被已删除函数调用的 `_promotion_*` private helper，留下 dead code。pyright/coverage 可能会捕获（unused private function / uncovered），但不是 plan 级别的保证。
- **直接证据**: `run_transition.py:3582` `_promotion_run_started_event_request`；`run_transition.py:3620` `_promotion_attempt_started_event_request`；`run_transition.py:1838,1845` `_promotion_attempt_row` / `_promotion_dispatch_record_row`。plan §7.1 只以"专属validation/event/row helpers"概括。
- **影响**: 低。这些私有 helpers 只被 `promote_queued_run_in_transaction` 调用，实现 agent 通过 IDE "find references" 可自然识别。且 static audit §9.4 的 `rg` 检查能捕获残留符号（虽然 grep pattern 不覆盖 `_promotion_*` 前缀）。
- **建议改法和验证点**: 可在 exact change #19 中加一句"所有仅被 `promote_queued_run_in_transaction` 调用的 `_promotion_*` 私有 helper 一并删除"。不强求；现有 grep audit 与类型检查器足以兜底。
- **修复风险**: 低
- **严重程度**: 低

## 临场架构发明检查

逐一检查 plan 是否要求实现 agent 自行发明未定义的设计：

| 决策点 | plan 是否完整定义 | evidence |
| --- | --- | --- |
| start 前 policy snapshot 来源 | **是** | `load_run_input_policy_snapshot_in_transaction` 完整签名 + design §25 链路 |
| 5-stage/15-cell 穷举方式 | **是** | §5.5 显式 table + unknown fail closed + `assert_never` 约束 |
| wait planned/committed event id 一致性 | **是** | §5.3.1 明确 `event_plan.tool_result_event_id` 与 transition committed id 逐字相等 |
| Engine continuation manifest 不调用 pre-start recorder | **是** | §5.3.1 + §8.2 exact change #13 |
| reactive recovery start transaction 编排 | **是** | §5.6 完整 sequencing + failure matrix |
| `HostAdmissionService` 新增字段装配 | **是** | §5.3.1 target signature + `open_host.py` factory 逐项传参 |
| direct promotion 删除范围 | **是**（minor gap 见 DS-PR-RE-002） | exact change #19 + §9.4 audit |
| `SessionContinuityView.source_refs` 全 site 更新 | **是** | exact change #25 + production/tests audit |
| Slice 1/2/3 边界 | **是** | amendment §4.3 + §8.1-8.4 |
| 各 producer 的 CAS/rollback private exception | **是** | §5.6 `_StartCandidateCasMissRollback` + `_ReactiveRecoveryStartCasMissRollback` |

**结论**：无不需临场发明的设计缺口。所有关键契约（类型签名、sequencing、failure handling、composition wiring）在 plan 中已完整定义。

## 误删共享类型 / 遗漏 export-import-README-test 检查

- **误删风险**：plan 要求删除的符号（`PromoteQueuedRunInput`、`PromotionResult`、`PromotionSkipReason`、`promote_queued_run_in_transaction`、`promote_queued_run_row`）经 production grep audit 确认**只在 direct promotion 路径使用**，不被任何 governed/recovery/wait transition 共享。`create_running_run_with_starting_attempt_in_transaction` 无 production caller。删除安全。
- **export 清理**：`__init__.py` 的 re-export 清理见 DS-PR-RE-001（需在 Slice 1 同步处理）。
- **import 边界**：plan §9.4 已要求 `rg -n "from dayu\.host|import dayu\.host" dayu/engine` 与 `rg -n "dayu\.host\.durable" dayu/service` 零命中。
- **README 触发**：plan §11 按目标 README 约束逐项判断，预期 `host/README.md`、`service/README.md`、`tests/README.md` 更新，Engine/root README 无变更。各 slice completion 要求先读目标 README 自身约束。
- **test 覆盖**：plan 要求每个 changed production file 逐文件 coverage ≥80%，full Host test suite 作为 gate。未发现遗漏。

## Residual Risks

| risk | severity | tracking destination |
| --- | --- | --- |
| `__init__.py` 不在 Slice 1 allowlist 但需同步删 re-exports | 低 | Slice 1 implementation 自判或 plan minor amend |
| `_promotion_*` private helpers 未穷举名称，dead code 残留 | 低 | 实现时 IDE/类型检查器自然兜底，§9.4 audit 可捕获 |
| partial implementation 尚未按本次 amendment 更新 | 高（已标记） | plan §0 已声明 `not accepted`，需双路 re-review pass 后才恢复 |
| Slice 2 fact append 需改动所有 Slice 1 producer | 中（已标记） | Slice 2 实现时确认 fact append 是纯增量 |
| manifest v2 全新起库无 compat reader | 低（accepted design decision） | §10 已声明，tests 覆盖 v1 rejection |

## Plan Review Conclusion

**结论: `pass`**

### 通过理由

1. **CTRL-PR-01（failed wait）已修复**：resume 闭集正确收窄为 completed/cancelled；failed/lost 保持 terminal 且零 manifest/new Attempt/pending dispatch。

2. **CTRL-PR-02（source policy 循环）已修复**：call graph 通过 `RunRow.input_event_id` → `USER_INPUT_ACCEPTED.effective_execution_config` → shared strict parser → `PolicySnapshot` 先行重建，再 strict-load source。worker loader 委托 source helper + extra caller policy check。无循环。

3. **CTRL-PR-03（direct promotion bypass）已修复**：admission method、durable transition、state mutation、专属 types/helpers/exports 全部进入删除列表。scheduler ordinary governance 成为唯一 owner。`run_transition.py` 零 diff 承诺已撤销。

4. **CTRL-PR-04（slice 边界）已修复**：Slice 1 只 freeze candidate/sizing atoms；Slice 2 追加 new fact（startup 不复用 source fact identity）；Slice 3 启用 anchor。usage 缺失继续 Slice 1/2 conservative estimator。

5. **DS accepted findings** 全部 closed：exhaustive match（DS-PR-001）、source_refs（DS-PR-002）、promotion 粒度（DS-PR-003 合并至 CTRL-PR-03）、allowlist 去重（DS-PR-004）、wait projection（DS-PR-006）、source helper（DS-PR-008）均已 fix。Controller rejected 的 DS-PR-005、DS-PR-007、DS-PR-009 保持 rejected。

6. **无需临场架构发明**：所有关键类型签名、sequencing、failure handling、composition wiring 在 plan 中完整定义。实现 agent 无自行发明的设计决策。

7. **无误删共享类型**：需删除符号经 production grep audit 确认只在 direct promotion 路径使用。`create_running_run_with_starting_attempt_in_transaction` 无 production caller。

8. **design §25 已同步**：五 stage、continuation eligibility、三 pressure allow、startup exact policy source、steer same-payload strict parse、wait closed resume set、Engine limited manifest 均已写入 design §25。

### 两个 minor gap 不构成 blocker

- DS-PR-RE-001（`__init__.py` allowlist）：实现 agent 可自判处理，不造成结构性错误。
- DS-PR-RE-002（`_promotion_*` 未穷举名称）：IDE 和类型检查器自然兜底。

### Original Blocker Disposition

Controller 第三次 stop 的 4 项 blocking findings（CTRL-PR-01 到 CTRL-PR-04）**全部已由本次 plan amendment 修复**。DS 上轮 review 的 high/medium findings 全部 closed。**无未解决的 structural blocker**。

### Next Gate Recommendation

Plan 已达到 code-generation-ready 标准。可交回 Controller 判定双路 re-review 结论。若 Controller accept，可创建 protected plan amendment commit 并恢复 Slice 1 implementation。
